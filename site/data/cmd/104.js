window.LCL_BUCKET(104,{"agate":{"n":"agate","t":"Serve content over the Gemini protocol","s":[["TAGLINE","Serve content over the Gemini protocol"],["TLDR","**Start** a Gemini server\n\n```agate --content [/var/gemini] --hostname [example.com]```\n\nUse **custom certificates**\n\n```agate --content [/var/gemini] --hostname [example.com] --certs [/etc/agate/certs]```\n\nRun on **custom port**\n\n```agate --content [/var/gemini] --hostname [example.com] --addr [0.0.0.0:1965]```\n\nEnable **logging**\n\n```agate --content [/var/gemini] --hostname [example.com] --log-ip```"],["SYNOPSIS","**agate** --content _dir_ --hostname _host_ [_options_]"],["DESCRIPTION","**agate** is a simple, secure server for the Gemini protocol, a lightweight alternative to HTTP focused on privacy and simplicity. It serves static content over TLS with minimal configuration.\n\nThe server automatically generates TLS certificates if not provided, handles multiple virtual hosts, and serves Gemini text files (.gmi) and other content types based on file extensions."],["PARAMETERS","**--content** _dir_\n> Root directory for content\n\n**--hostname** _host_\n> Hostname for the server (can be repeated for virtual hosting)\n\n**--addr** _address_\n> Address to bind (default: 0.0.0.0:1965)\n\n**--certs** _dir_\n> Directory holding the TLS certificate/key pairs (default: .certificates). Certificates are generated automatically per hostname if missing.\n\n**--lang** _code_\n> Default language code for text/gemini responses.\n\n**--log-ip**\n> Log client IP addresses.\n\n**-C**, **--central-conf**\n> Read a single .meta configuration file from the content root instead of per-directory files.\n\n**--serve-secret**\n> Serve files and directories whose names start with a dot (served as hidden by default).\n\n**-3**, **--only-tls13**\n> Disable TLSv1.2, accepting only TLSv1.3 connections.\n\n**--skip-port-check**\n> Do not require the port in the request URL to match the listening port (useful behind a proxy).\n\n**-s**, **--silent**\n> Disable logging output."],["CAVEATS","Gemini requires TLS; unencrypted connections are not supported. The default Gemini port 1965 may require root privileges on some systems. Client support is limited compared to HTTP browsers."],["HISTORY","**agate** was created as part of the growing Gemini ecosystem after the Gemini protocol was designed by Solderpunk in **2019**. It provides a simple, Rust-based server implementation following the protocol's minimalist philosophy."],["INSTALL","```aur: yay -S agate```\n\n```nix: nix profile install nixpkgs#agate```"],["SEE ALSO","[nginx](/man/nginx)(8)"],["RESOURCES","```[Source code](https://github.com/mbrubeck/agate)```"]],"e":["nginx"],"x":"agate --content [/var/gemini] --hostname [example.com]"},"apache2ctl":{"n":"apache2ctl","t":"Manage the Apache HTTP Server on Debian","s":[["TAGLINE","Manage the Apache HTTP Server on Debian"],["TLDR","**Start** the Apache daemon\n\n```sudo apache2ctl start```\n\n**Stop** the Apache daemon\n\n```sudo apache2ctl stop```\n\n**Restart** the Apache daemon\n\n```sudo apache2ctl restart```\n\nTest **syntax** of the configuration file\n\n```sudo apache2ctl -t```\n\nList **loaded modules**\n\n```sudo apache2ctl -M```\n\nRestart **gracefully** without dropping connections\n\n```sudo apache2ctl graceful```"],["SYNOPSIS","**apache2ctl** _command_\n\n**apache2ctl** [_httpd-argument_]"],["DESCRIPTION","**apache2ctl** is a front end to the Apache HyperText Transfer Protocol (HTTP) server designed to help administrators manage the Apache daemon. This command comes with Debian-based operating systems; for RHEL-based ones see **httpd**."],["PARAMETERS","**start**\n> Start the Apache daemon; errors if already running\n\n**stop**\n> Stop the Apache daemon\n\n**restart**\n> Restart Apache via SIGHUP; includes config validation\n\n**graceful**\n> Restart without terminating active connections\n\n**graceful-stop**\n> Stop Apache without aborting current connections\n\n**status**\n> Show brief operational status report\n\n**fullstatus**\n> Display comprehensive status via mod_status (requires lynx)\n\n**configtest**\n> Validate syntax in configuration files\n\n**-t**\n> Test configuration file syntax\n\n**-M**\n> List loaded (static and shared) modules\n\n**-S**\n> Show the parsed virtual host settings and listening ports\n\n**-V**\n> Show version, build settings, and compile-time configuration\n\n**-l**\n> List modules compiled into the server"],["CONFIGURATION","**/etc/apache2/apache2.conf**\n> Main configuration file on Debian/Ubuntu systems.\n\n**/etc/apache2/sites-available/**\n> Virtual host configuration files, enabled via a2ensite.\n\n**/etc/apache2/mods-available/**\n> Module configuration files, enabled via a2enmod.\n\n**/etc/apache2/envvars**\n> Environment variables for the Apache process (user, group, pid file).\n\n**/etc/apache2/ports.conf**\n> Listen directives specifying which ports Apache binds to."],["CAVEATS","Debian requires environment variables **APACHE_RUN_USER**, **APACHE_RUN_GROUP**, and **APACHE_PID_FILE** set in /etc/apache2/envvars."],["HISTORY","Part of the **Apache HTTP Server** project, one of the oldest and most widely used web servers, developed by the Apache Software Foundation."],["INSTALL","```apt: sudo apt install apache2```\n\n```zypper: sudo zypper install apache2```\n\n```brew: brew install apache2```"],["SEE ALSO","[apachectl](/man/apachectl)(8), [a2enmod](/man/a2enmod)(8), [a2dismod](/man/a2dismod)(8), [a2ensite](/man/a2ensite)(8), [httpd](/man/httpd)(8)"],["RESOURCES","```[Source code](https://github.com/apache/httpd)```\n\n```[Homepage](https://httpd.apache.org/)```\n\n```[Documentation](https://manpages.debian.org/bookworm/apache2/apache2ctl.8.en.html)```"]],"e":["apachectl","a2enmod","a2dismod","a2ensite","httpd"],"x":"sudo apache2ctl start"},"apt-mark":{"n":"apt-mark","t":"Change the install state of packages.","s":[["TAGLINE","Change the install state of packages."],["TLDR","Mark a package as **automatically installed**\n\n```sudo apt-mark auto [package]```\n\nMark a package as **manually installed**\n\n```sudo apt-mark manual [package]```\n\n**Hold** a package at its current version\n\n```sudo apt-mark hold [package]```\n\n**Allow** a package to be updated again\n\n```sudo apt-mark unhold [package]```\n\nShow **manually installed** packages\n\n```apt-mark showmanual```\n\nShow **automatically installed** packages\n\n```apt-mark showauto```\n\nShow **held** packages\n\n```apt-mark showhold```"],["SYNOPSIS","**apt-mark** [_options_] _command_ _package_..."],["DESCRIPTION","**apt-mark** is a utility to change the status of installed packages. It can mark packages as automatically installed (making them candidates for autoremove), manually installed, or held at their current version."],["PARAMETERS","**auto package**\n> Mark package as automatically installed\n\n**manual package**\n> Mark package as manually installed\n\n**hold package**\n> Hold package at current version, preventing upgrades\n\n**unhold package**\n> Remove hold, allowing package to be upgraded\n\n**showmanual**\n> Show list of manually installed packages\n\n**showauto**\n> Show list of automatically installed packages\n\n**showhold**\n> Show list of held packages\n\n**showinstall**\n> Show list of packages marked as install\n\n**showremove**\n> Show list of packages marked for removal\n\n**showpurge**\n> Show list of packages marked for purge\n\n**minimize-manual**\n> Mark all dependency packages as auto, keeping only directly needed packages as manual\n\n**-f, --file filename**\n> Read/write package stats from the specified filename instead of default location\n\n**-v, --verbose**\n> Verbose output\n\n**-c, --config-file**\n> Specify a configuration file to use\n\n**-o, --option**\n> Set a configuration option (syntax: -o Foo::Bar=bar)"],["CAVEATS","Holding packages may cause dependency issues during system upgrades. Use sparingly."],["HISTORY","Part of the **APT** (Advanced Package Tool) suite developed for Debian-based systems."],["INSTALL","```apt: sudo apt install apt```\n\n```dnf: sudo dnf install apt```\n\n```pacman: sudo pacman -S apt```\n\n```apk: sudo apk add apt```\n\n```brew: brew install apt```\n\n```nix: nix profile install nixpkgs#apt```"],["SEE ALSO","[apt](/man/apt)(8), [apt-get](/man/apt-get)(8), [dpkg](/man/dpkg)(1)"]],"e":["apt","apt-get","dpkg"],"x":"sudo apt-mark auto [package]"},"arpaname":{"n":"arpaname","t":"Convert IP addresses to reverse DNS ARPA names.","s":[["TAGLINE","Convert IP addresses to reverse DNS ARPA names."],["TLDR","Translate an **IP address** to its corresponding ARPA name\n\n```arpaname [ip_address]```\n\nTranslate an **IPv6 address** to ARPA format\n\n```arpaname [2001:db8::1]```"],["SYNOPSIS","**arpaname** _ip_address_..."],["DESCRIPTION","**arpaname** converts IP addresses (both IPv4 and IPv6) to their corresponding reverse DNS ARPA domain names. These names are used for reverse DNS lookups, which map IP addresses back to hostnames.\n\nFor IPv4 addresses, the output is in the **in-addr.arpa** domain. For IPv6 addresses, the output is in the **ip6.arpa** domain with each hexadecimal digit reversed and separated by dots."],["CAVEATS","This is a simple conversion utility that does not perform DNS queries. It only formats IP addresses into ARPA notation. Use **dig** or **host** with the **-x** flag to perform actual reverse DNS lookups."],["HISTORY","**arpaname** is part of the BIND (Berkeley Internet Name Domain) utilities, the most widely used DNS software. ARPA names follow the format defined in RFC 1035 for IPv4 and RFC 3596 for IPv6."],["INSTALL","```apt: sudo apt install bind9```\n\n```apk: sudo apk add bind-tools```"],["SEE ALSO","[dig](/man/dig)(1), [host](/man/host)(1), [nslookup](/man/nslookup)(1)"],["RESOURCES","```[Source code](https://gitlab.isc.org/isc-projects/bind9)```\n\n```[Homepage](https://www.isc.org/bind/)```\n\n```[Documentation](https://bind9.readthedocs.io)```"]],"e":["dig","host","nslookup"],"x":"arpaname [ip_address]"},"bootc":{"n":"bootc","t":"Transactional OS updates using container images","s":[["TAGLINE","Transactional OS updates using container images"],["TLDR","Show **deployments** in bootloader order\n\n```bootc status```\n\nCheck if **updates are available**\n\n```bootc upgrade --check```\n\nPrepare a new **update and reboot**\n\n```bootc upgrade --apply```\n\n**Change OS** base to new container image\n\n```bootc switch [image]```\n\n**Reboot** into previous deployment\n\n```bootc rollback```\n\nApply transactional **configuration changes**\n\n```bootc edit```\n\n**Install to a target block device**\n\n```bootc install to-disk --target-no-signature-verification [/dev/sda]```\n\n**Show status in JSON format**\n\n```bootc status --format json```"],["SYNOPSIS","**bootc** _command_ [_options_]"],["DESCRIPTION","**bootc** manages transactional, in-place operating system updates using OCI/Docker container images. It enables atomic system upgrades where the entire OS is replaced by a new container image at boot time.\n\nThe tool works with image-based Linux distributions like Fedora CoreOS and RHEL and provides rollback capability if updates cause issues. It builds on OSTree for atomic deployments and uses standard container registries for image distribution."],["SUBCOMMANDS","**status**\n> Show current deployments and their order in bootloader\n\n**upgrade**\n> Fetch and stage a new version of the OS\n\n**switch**\n> Change to a different container image\n\n**rollback**\n> Revert to the previous OS deployment\n\n**edit**\n> Apply configuration changes transactionally\n\n**install to-disk**\n> Install the container image to a target block device\n\n**install to-filesystem**\n> Install to an existing filesystem (supports LVM, LUKS, RAID)"],["PARAMETERS","**--check**\n> Check for updates without applying them (used with upgrade)\n\n**--apply**\n> Apply upgrade and reboot (used with upgrade)\n\n**--format** _FORMAT_\n> Output format for status: json or yaml\n\n**--quiet**\n> Suppress non-error output"],["CAVEATS","Only works on systems designed for bootc (image-based Linux distributions). Changes to root filesystem are lost unless using configuration management. Requires container image infrastructure."],["HISTORY","**bootc** is part of the growing ecosystem of image-based Linux systems, building on technologies like OSTree and container images. It represents a modern approach to system updates pioneered by projects like CoreOS. Developed under the **containers** GitHub organization, it became a key component of RHEL image mode."],["INSTALL","```dnf: sudo dnf install bootc```\n\n```nix: nix profile install nixpkgs#bootc```"],["SEE ALSO","[bootc-switch](/man/bootc-switch)(1), [rpm-ostree](/man/rpm-ostree)(1), [podman](/man/podman)(1), [skopeo](/man/skopeo)(1), [ostree](/man/ostree)(1)"]],"e":["bootc-switch","rpm-ostree","podman","skopeo","ostree"],"x":"bootc status"},"branchlet":{"n":"branchlet","t":"Interactive Git worktree manager TUI","s":[["TAGLINE","Interactive Git worktree manager TUI"],["TLDR","**Launch** the worktree manager\n\n```branchlet```"],["SYNOPSIS","**branchlet** [_options_]"],["DESCRIPTION","**branchlet** is an interactive terminal user interface for managing **Git worktrees**. It simplifies the workflow of creating, switching between, and removing Git worktrees through a visual interface rather than memorizing git worktree subcommands.\n\nThe TUI displays existing worktrees, their associated branches, and paths. Users can create new worktrees from branches, delete worktrees, and navigate between them with keyboard shortcuts."],["CAVEATS","Must be run inside a Git repository. Requires Git with worktree support (Git 2.5+). Deleting a worktree through branchlet also cleans up the Git worktree metadata."],["SEE ALSO","[git](/man/git)(1), [git-worktree](/man/git-worktree)(1), [lazygit](/man/lazygit)(1)"]],"e":["git","git-worktree","lazygit"],"x":"branchlet"},"callme":{"n":"callme","t":"Ring or text your iPhone from AI agents and scripts","s":[["TAGLINE","Ring or text your iPhone from AI agents and scripts"],["TLDR","**Pair** once with the number from the Call Me app\n\n```callme pair [5551234567]```\n\n**Call** and wait for a spoken answer (transcribed)\n\n```callme call \"[Deploy staging or prod first?]\"```\n\nSend a **text** / push notification\n\n```callme text \"[Build green, PR merged.]\"```\n\nPrint **setup** instructions\n\n```callme setup```\n\nShow a **QR** for the App Store link\n\n```callme qr```\n\nRead the paired **number**\n\n```callme number```"],["SYNOPSIS","**callme** {_pair_|_call_|_text_|_listen_|_events_|_setup_|_qr_|_number_|...} [_args_]"],["PARAMETERS","**pair** _number_\n> Store the 10-digit Call Me number from the iOS app in `~/.aiphone/config.json`\n\n**call** _message_\n> Place a CallKit call; TTS speaks the question; blocks until voice reply is transcribed\n\n**text** _message_\n> Send a push-notification style message to the phone\n\n**listen** / **events**\n> Fetch replies for non-plugin agent sessions\n\n**setup**\n> Print pairing and usage steps\n\n**qr**\n> Print a scannable QR code for the app download link\n\n**number**\n> Print the currently paired number"],["DESCRIPTION","**callme** is a small bash+curl CLI that connects terminal agents and scripts to the **Call Me** iOS app (formerly AI Phone). After pairing, agents can ring your real iPhone, speak a question via TTS, and receive your spoken answer as text, or send asynchronous texts.\n\nCredentials live in `~/.aiphone/config.json` (mode 0600), not in agent instruction files. A Claude Code plugin variant installs the same flow into interactive sessions."],["CAVEATS","Requires the Call Me iOS app and network access to the Call Me service. Calls block until answered—unsuitable for unattended automation without timeouts. Phone numbers are credentials; do not commit config files."],["HISTORY","Agent integration for the **Call Me** iPhone app by radres; standalone skill under `skill/callme` in the open-source repo."],["SEE ALSO","[curl](/man/curl)(1), [jq](/man/jq)(1)"],["RESOURCES","```[Source code](https://github.com/radres/call-me)```\n\n```[Homepage](https://apps.apple.com/app/id6789575165)```"]],"e":["curl","jq"],"x":"callme pair [5551234567]"},"cam":{"n":"cam","t":"Interact with cameras via libcamera","s":[["TAGLINE","Interact with cameras via libcamera"],["TLDR","**List** available cameras\n\n```cam -l```\n\nList **controls** of a camera\n\n```cam -c [camera_index] --list-controls```\n\n**Capture frames** to files\n\n```cam -c [camera_index] --capture=[frames] --file```\n\nDisplay camera feed in a **window**\n\n```cam -c [camera_index] --capture --sdl```"],["SYNOPSIS","**cam** [_options_]"],["DESCRIPTION","**cam** is a command-line tool for interacting with cameras through the libcamera framework. It can list cameras, display their controls, capture frames, and show live video in a window.\n\nlibcamera provides a modern camera stack for Linux, replacing older V4L2-only approaches with a more flexible architecture."],["PARAMETERS","**-l, --list**\n> List available cameras\n\n**-c, --camera** _index_\n> Select camera by index\n\n**--list-controls**\n> Show camera controls\n\n**--capture** [_frames_]\n> Capture specified number of frames (0 for unlimited)\n\n**--file**\n> Write captured frames to files\n\n**--sdl**\n> Display video in SDL window\n\n**--list-properties**\n> Show camera properties\n\n**--stream** _config_\n> Configure stream parameters (e.g. width, height, pixelformat, role)\n\n**--monitor**\n> Monitor for camera hotplug and removal events"],["CAVEATS","Requires libcamera support, which may not be available for all cameras. Some cameras work better with direct V4L2 access."],["INSTALL","```apt: sudo apt install libcamera-tools```\n\n```dnf: sudo dnf install libcamera-tools```\n\n```pacman: sudo pacman -S libcamera-tools```\n\n```apk: sudo apk add libcamera-tools```"],["SEE ALSO","[v4l2-ctl](/man/v4l2-ctl)(1), [rpicam-still](/man/rpicam-still)(1), [rpicam-vid](/man/rpicam-vid)(1)"]],"e":["v4l2-ctl","rpicam-still","rpicam-vid"],"x":"cam -l"},"cargo-search":{"n":"cargo-search","t":"Search for crates on a registry","s":[["TAGLINE","Search for crates on a registry"],["TLDR","**Search for crate**\n\n```cargo search [serde]```\n\n**Search with more results**\n\n```cargo search [async] --limit [50]```\n\n**Search specific registry**\n\n```cargo search [package] --registry [my-registry]```"],["SYNOPSIS","**cargo search** [_options_] [_query_...]"],["DESCRIPTION","**cargo search** performs a textual search for crates on crates.io or another registry. Results are displayed in a TOML-compatible format showing the package name, latest version, and description, making them easy to copy directly into a Cargo.toml dependencies section.\n\nBy default, up to 10 results are returned, sorted by relevance. The `--limit` flag can increase this up to 100. For more advanced browsing, the crates.io website or tools like `cargo-seek` provide richer filtering and categorization."],["PARAMETERS","**--limit** _n_\n> Number of results (default: 10, max: 100)\n\n**--registry** _name_\n> Registry to search\n\n**--index** _url_\n> Registry index URL\n\n**-v**, **--verbose**\n> Verbose output\n\n**-q**, **--quiet**\n> Suppress output\n\n**--color** _when_\n> Color output: auto, always, never.\n\n**--config** _KEY=VALUE_\n> Override Cargo configuration values.\n\n**--frozen**, **--locked**, **--offline**\n> Network/lockfile behavior; **--offline** prevents Cargo from contacting the registry index."],["OUTPUT FORMAT","Results display package name, version, and description:\n\n```\nserde = \"1.0.130\"     # A generic serialization/deserialization framework\nserde_json = \"1.0.68\" # JSON serialization format\n```"],["CAVEATS","Searches crates.io by default. For interactive TUI search, consider cargo-seek. Results sorted by relevance."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-add](/man/cargo-add)(1), [cargo-info](/man/cargo-info)(1)"]],"e":["cargo","cargo-add","cargo-info"],"x":"cargo search [serde]"},"cbonsai":{"n":"cbonsai","t":"animated ASCII art bonsai tree generator","s":[["TAGLINE","animated ASCII art bonsai tree generator"],["TLDR","**Generate random bonsai tree**\n\n```cbonsai```\n\n**Live animation mode**\n\n```cbonsai --live```\n\n**Screensaver mode**\n\n```cbonsai --screensaver```\n\n**Infinite mode**\n\n```cbonsai --infinite```\n\n**Add message**\n\n```cbonsai -m \"[Your message]\"```\n\n**Set animation speed** (seconds per step in live mode)\n\n```cbonsai -l -t [0.05]```\n\n**Customize leaf characters**\n\n```cbonsai -c \"[&,*,o]\"```"],["SYNOPSIS","**cbonsai** [_options_]"],["DESCRIPTION","**cbonsai** generates procedurally created ASCII art bonsai trees in the terminal. Each run produces a unique tree using randomized growth algorithms, and a seed value can be specified for reproducibility.\n\nThe tool supports live animation of tree growth, infinite mode for continuous tree regeneration, and a screensaver mode. Written in C using ncurses, it supports customizable leaf characters, colors, a plant base, and messages displayed alongside the tree."],["PARAMETERS","**-l**, **--live**\n> Live mode: show each step of growth\n\n**-t** _TIME_, **--time** _TIME_\n> In live mode, seconds between growth steps (default 0.03)\n\n**-i**, **--infinite**\n> Infinite mode: keep growing trees\n\n**-w** _TIME_, **--wait** _TIME_\n> In infinite mode, seconds between each tree (default 4.00)\n\n**-S**, **--screensaver**\n> Screensaver mode; equivalent to -liWC, quits on any keypress\n\n**-m** _MSG_, **--message** _MSG_\n> Attach a message next to the tree\n\n**-b** _N_, **--base** _N_\n> ASCII-art plant base to use; 0 is none\n\n**-c** _LIST_, **--leaf** _LIST_\n> Comma-delimited strings randomly chosen for leaves (default &)\n\n**-M** _MULT_, **--multiplier** _MULT_\n> Branch multiplier (0-20; default 5)\n\n**-L** _LIFE_, **--life** _LIFE_\n> Starting life value (0-200; default 32)\n\n**-s** _SEED_, **--seed** _SEED_\n> Seed the random number generator\n\n**-W** _FILE_, **--save** _FILE_\n> Save progress to file (default ~/.cache/cbonsai)\n\n**-C** _FILE_, **--load** _FILE_\n> Load progress from file\n\n**-p**, **--print**\n> Print tree to terminal when finished\n\n**-v**, **--verbose**\n> Increase output verbosity"],["CAVEATS","Requires ncurses library. Color support depends on terminal capabilities."],["INSTALL","```dnf: sudo dnf install cbonsai```\n\n```apk: sudo apk add cbonsai```\n\n```zypper: sudo zypper install cbonsai```\n\n```brew: brew install cbonsai```\n\n```nix: nix profile install nixpkgs#cbonsai```"],["SEE ALSO","[cmatrix](/man/cmatrix)(1), [sl](/man/sl)(1), [cowsay](/man/cowsay)(1)"]],"e":["cmatrix","sl","cowsay"],"x":"cbonsai"},"conda-remove":{"n":"conda-remove","t":"package uninstaller","s":[["TAGLINE","package uninstaller"],["TLDR","**Remove a package**\n\n```conda remove [package_name]```\n\n**Remove from specific environment**\n\n```conda remove -n [env_name] [package_name]```\n\n**Remove an entire environment**\n\n```conda remove -n [env_name] --all```\n\n**Remove multiple packages**\n\n```conda remove [package1] [package2]```\n\n**Remove without confirmation**\n\n```conda remove -y [package_name]```"],["SYNOPSIS","**conda** **remove** [_options_] [_packages..._]"],["PARAMETERS","**-n**, **--name** _name_\n> Remove from named environment.\n\n**--all**\n> Remove entire environment.\n\n**-y**, **--yes**\n> Don't ask for confirmation.\n\n**--force**\n> Force removal without dependency checking."],["CONFIGURATION","**~/.condarc**\n> User-level conda configuration for channels, package settings, and solver options.\n\n**/opt/conda/.condarc**\n> System-wide conda configuration."],["DESCRIPTION","**conda remove** uninstalls packages from a conda environment. With **--all**, removes the entire environment.\n\nDependencies that are no longer needed may remain; use **conda clean** to remove them."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[conda](/man/conda)(1), [conda-install](/man/conda-install)(1), [conda-clean](/man/conda-clean)(1)"],["RESOURCES","```[Source code](https://github.com/conda/conda)```\n\n```[Documentation](https://docs.conda.io/projects/conda/en/stable/commands/remove.html)```"]],"e":["conda","conda-install","conda-clean"],"x":"conda remove [package_name]"},"crane-pull":{"n":"crane-pull","t":"download container image as tarball","s":[["TAGLINE","download container image as tarball"],["TLDR","**Pull image** to tarball\n\n```crane pull [image:tag] [output.tar]```\n\n**Pull specific platform**\n\n```crane pull --platform [linux/amd64] [image:tag] [output.tar]```"],["SYNOPSIS","**crane** **pull** [_options_] _image_ _output_"],["PARAMETERS","**--platform** _platform_\n> Pull specific platform.\n\n**--format** _format_\n> Output format (tarball or legacy)."],["DESCRIPTION","**crane pull** downloads a container image and saves it as a tarball. The tarball can be loaded into Docker with **docker load**."],["INSTALL","```pacman: sudo pacman -S crane```\n\n```apk: sudo apk add crane```\n\n```zypper: sudo zypper install crane```\n\n```brew: brew install crane```\n\n```nix: nix profile install nixpkgs#crane```"],["SEE ALSO","[crane](/man/crane)(1), [crane-push](/man/crane-push)(1), [crane-export](/man/crane-export)(1)"],["RESOURCES","```[Source code](https://github.com/google/go-containerregistry)```\n\n```[Documentation](https://github.com/google/go-containerregistry/blob/main/cmd/crane/doc/crane_pull.md)```"]],"e":["crane","crane-push","crane-export"],"x":"crane pull [image:tag] [output.tar]"},"daphne":{"n":"daphne","t":"HTTP, HTTP2, and WebSocket protocol server for ASGI applications","s":[["TAGLINE","HTTP, HTTP2, and WebSocket protocol server for ASGI applications"],["TLDR","**Run a Django Channels application**\n\n```daphne [myproject.asgi]:application```\n\n**Run on specific host and port**\n\n```daphne -b [0.0.0.0] -p [8000] [myproject.asgi]:application```\n\n**Run with Unix socket**\n\n```daphne -u [/tmp/daphne.sock] [myproject.asgi]:application```\n\n**Run with multiple worker threads**\n\n```daphne --threads [4] [myproject.asgi]:application```\n\n**Run with SSL**\n\n```daphne -e ssl:443:privateKey=[key.pem]:certKey=[cert.pem] [myproject.asgi]:application```\n\n**Enable verbose logging**\n\n```daphne -v 2 [myproject.asgi]:application```"],["SYNOPSIS","**daphne** [_options_] _application_"],["PARAMETERS","**-b**, **--bind** _address_\n> IP address to bind (default: 127.0.0.1).\n\n**-p**, **--port** _port_\n> Port to listen on (default: 8000).\n\n**-u**, **--unix-socket** _path_\n> Unix socket path (alternative to TCP).\n\n**--fd** _fd_\n> File descriptor to listen on.\n\n**-e**, **--endpoint** _spec_\n> Twisted endpoint specification.\n\n**--threads** _n_\n> Number of worker threads.\n\n**-v**, **--verbosity** _level_\n> Logging verbosity (0-2).\n\n**--access-log** _path_\n> Access log file path.\n\n**--websocket_timeout** _seconds_\n> Maximum time to allow a WebSocket to be connected (-1 for infinite).\n\n**--websocket_connect_timeout** _seconds_\n> Maximum time to allow a connection to handshake (-1 for infinite).\n\n**--ping-interval** _seconds_\n> Seconds a WebSocket must be idle before a keepalive ping is sent.\n\n**--ping-timeout** _seconds_\n> Seconds before a WebSocket is closed if no response to a keepalive ping.\n\n**--application-close-timeout** _seconds_\n> Seconds an ASGI application has to exit after client disconnect before it is killed (default: 10).\n\n**--proxy-headers**\n> Enable X-Forwarded-For handling.\n\n**--proxy-headers-host** _header_\n> Header for host detection behind proxy.\n\n**--root-path** _path_\n> Root path prefix for the ASGI scope (for reverse proxy setups)."],["DESCRIPTION","**Daphne** is an HTTP, HTTP2, and WebSocket protocol server for ASGI (Asynchronous Server Gateway Interface) applications, particularly Django Channels. It enables Django applications to handle WebSocket connections and other asynchronous protocols.\n\nUnlike WSGI servers (Gunicorn, uWSGI) that handle synchronous HTTP requests, Daphne supports long-lived connections required for real-time features: WebSockets, Server-Sent Events, and HTTP long polling.\n\nDaphne is built on Twisted's networking engine. It's typically used with Django Channels for chat applications, live notifications, and other real-time features. In production, it's often run behind a reverse proxy like nginx."],["CAVEATS","Not suitable for serving static files; use nginx or whitenoise. Production deployments should use multiple workers (via process manager). WebSocket connections consume resources while idle. SSL termination is better handled by a reverse proxy in most deployments."],["HISTORY","Daphne was created by **Andrew Godwin** as part of the **Django Channels** project, first released around **2016**. It was developed to bring WebSocket support to Django, enabling the framework to handle real-time applications. Django Channels became an official Django project, with Daphne as its reference ASGI server."],["INSTALL","```apk: sudo apk add py3-daphne```"],["SEE ALSO","[uvicorn](/man/uvicorn)(1), [hypercorn](/man/hypercorn)(1), [gunicorn](/man/gunicorn)(1), [nginx](/man/nginx)(1)"]],"e":["uvicorn","hypercorn","gunicorn","nginx"],"x":"daphne [myproject.asgi]:application"},"dnf-changelog":{"n":"dnf-changelog","t":"display package changelogs","s":[["TAGLINE","display package changelogs"],["TLDR","View **all changelogs** for a package\n\n```dnf changelog [package]```\n\nView changelogs **since date**\n\n```dnf changelog --since [date] [package]```\n\nView **last n** changelogs\n\n```dnf changelog --count [number] [package]```\n\nShow **new items** for upgradeable packages\n\n```dnf changelog --upgrades [package]```"],["SYNOPSIS","**dnf changelog** [_options_] _package_"],["DESCRIPTION","**dnf changelog** displays package changelogs, which document changes, bug fixes, and security updates included in package versions. It's part of dnf-plugins-core and requires that plugin to be installed.\n\nChangelogs are maintained by package maintainers and provide insight into what has changed between versions. This is particularly useful before upgrading to review security fixes, new features, or bug corrections. You can filter changelogs by date or count to focus on recent changes, or use --upgrades to see only what's new in available updates compared to your installed version."],["PARAMETERS","**--since** _date_\n> Show entries after specified date\n\n**--count** _n_\n> Show last n entries\n\n**--upgrades**\n> Show only new entries for available upgrades\n\n**--help-cmd**\n> Display help"],["CAVEATS","Requires dnf-plugins-core. Changelog availability depends on package maintainers including them in the RPM."],["SEE ALSO","[dnf](/man/dnf)(8), [rpm](/man/rpm)(8)"],["RESOURCES","```[Source code](https://github.com/rpm-software-management/dnf-plugins-core)```\n\n```[Documentation](https://dnf-plugins-core.readthedocs.io/en/latest/changelog.html)```"]],"e":["dnf","rpm"],"x":"dnf changelog [package]"},"dnf-deplist":{"n":"dnf-deplist","t":"query package dependencies","s":[["TAGLINE","query package dependencies"],["TLDR","This command is an alias of **dnf repoquery --deplist**.\n\nSee documentation for the original command.\n\n```tldr dnf repoquery```"],["SEE ALSO","[dnf](/man/dnf)(8), [dnf-repoquery](/man/dnf-repoquery)(8)"],["RESOURCES","```[Source code](https://github.com/rpm-software-management/dnf)```\n\n```[Documentation](https://dnf.readthedocs.io/en/latest/command_ref.html)```"]],"e":["dnf","dnf-repoquery"],"x":"tldr dnf repoquery"},"doctl-databases-firewalls":{"n":"doctl-databases-firewalls","t":"manage database cluster firewall rules","s":[["TAGLINE","manage database cluster firewall rules"],["TLDR","**List firewall rules**\n\n```doctl databases firewalls list [cluster_id]```\n\n**Add firewall rule for IP**\n\n```doctl databases firewalls append [cluster_id] --rule ip_addr:[ip]```\n\n**Add firewall rule for droplet**\n\n```doctl databases firewalls append [cluster_id] --rule droplet:[droplet_id]```\n\n**Remove firewall rule**\n\n```doctl databases firewalls remove [cluster_id] [rule_uuid]```"],["SYNOPSIS","**doctl** **databases** **firewalls** _command_ [_options_]"],["SUBCOMMANDS","**list**\n> List firewall rules.\n\n**append**\n> Add firewall rule.\n\n**remove**\n> Remove firewall rule."],["DESCRIPTION","**doctl databases firewalls** manages trusted sources (firewall rules) for DigitalOcean managed database clusters. Rules control which IPs, droplets, Kubernetes clusters, or tags can connect.\n\nBy default, managed database clusters deny all incoming connections for security. Firewall rules explicitly allow access from specific sources. You can authorize individual IP addresses, entire droplets, Kubernetes clusters, or resources identified by tags. This enables secure database access while preventing unauthorized connections.\n\nRules can be added, listed, and removed to adapt to changing infrastructure requirements. Each rule is identified by a UUID for management operations."],["INSTALL","```dnf: sudo dnf install doctl```\n\n```pacman: sudo pacman -S doctl```\n\n```apk: sudo apk add doctl```\n\n```brew: brew install doctl```\n\n```nix: nix profile install nixpkgs#doctl```"],["SEE ALSO","[doctl-databases](/man/doctl-databases)(1)"]],"e":["doctl-databases"],"x":"doctl databases firewalls list [cluster_id]"},"dolt-clone":{"n":"dolt-clone","t":"create local copy of remote database repository","s":[["TAGLINE","create local copy of remote database repository"],["TLDR","**Clone from DoltHub**\n\n```dolt clone [owner/repo]```\n\n**Clone to specific** directory\n\n```dolt clone [owner/repo] [directory]```\n\n**Clone with specific** remote name\n\n```dolt clone --remote [name] [owner/repo]```\n\n**Clone specific branch**\n\n```dolt clone -b [branch] [owner/repo]```\n\n**Shallow clone** with limited history depth\n\n```dolt clone --depth [1] [owner/repo]```\n\n**Clone a private** repository with authentication\n\n```dolt clone --user [username] [owner/repo]```"],["SYNOPSIS","**dolt clone** [_options_] _repository_ [_directory_]"],["PARAMETERS","_REPOSITORY_\n> Remote repository (owner/repo format for DoltHub).\n\n_DIRECTORY_\n> Local directory name (defaults to repo name).\n\n**-b**, **--branch** _BRANCH_\n> Clone specific branch.\n\n**--remote** _NAME_\n> Name for the remote (default: origin).\n\n**--depth** _N_\n> Perform a shallow clone with history limited to N commits.\n\n**--single-branch**\n> Clone only one branch.\n\n**--user** _USERNAME_\n> User name for authentication with the remote. Password is read from the DOLT_REMOTE_PASSWORD environment variable.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dolt clone** creates a local copy of a remote Dolt repository. It downloads the database including all tables, commit history, and branches from DoltHub or other Dolt remotes.\n\nThe cloned repository maintains a connection to its origin, enabling push and pull operations. By default, all branches are fetched, though single-branch cloning is available for efficiency.\n\nThis is typically the first step in collaborating on a Dolt database, bringing a shared dataset to your local environment for querying and modification."],["CAVEATS","Large repositories may take time to clone. Network connectivity required. Storage space needed for full history. Authentication may be required for private repos."],["HISTORY","dolt clone is part of **Dolt**, implementing Git's clone semantics for database repositories. It enables distributed collaboration on versioned databases through DoltHub and other remotes."],["INSTALL","```pacman: sudo pacman -S dolt```\n\n```brew: brew install dolt```\n\n```nix: nix profile install nixpkgs#dolt```"],["SEE ALSO","[dolt](/man/dolt)(1), [dolt-fetch](/man/dolt-fetch)(1), [dolt-branch](/man/dolt-branch)(1), [git-clone](/man/git-clone)(1)"]],"e":["dolt","dolt-fetch","dolt-branch","git-clone"],"x":"dolt clone [owner/repo]"},"faster-whisper":{"n":"faster-whisper","t":"optimized Whisper speech recognition engine","s":[["TAGLINE","optimized Whisper speech recognition engine"],["TLDR","**Transcribe an audio file**\n\n```faster-whisper [audio.mp3]```\n\n**Transcribe with a specific model**\n\n```faster-whisper [audio.mp3] --model [large-v3]```\n\n**Transcribe with language hint**\n\n```faster-whisper [audio.mp3] --language [en]```\n\n**Output as SRT subtitles**\n\n```faster-whisper [audio.mp3] --output_format srt```\n\n**Translate to English**\n\n```faster-whisper [audio.mp3] --task translate```\n\n**Save output to directory**\n\n```faster-whisper [audio.mp3] --output_dir [/path/to/output]```\n\n**Transcribe with word timestamps**\n\n```faster-whisper [audio.mp3] --word_timestamps true```"],["SYNOPSIS","**faster-whisper** _audio_ [_--model size_] [_--language lang_] [_--task task_] [_options_]"],["PARAMETERS","**--model** _SIZE_\n> Model size: tiny, base, small, medium, large-v1, large-v2, large-v3 (default: small).\n\n**--language** _LANG_\n> Language code (en, de, fr, etc.) or auto-detect.\n\n**--task** _TASK_\n> Task: transcribe or translate.\n\n**--output_format** _FORMAT_\n> Output format: txt, vtt, srt, tsv, json, all.\n\n**--output_dir** _DIR_\n> Output directory for results.\n\n**--word_timestamps** _BOOL_\n> Include word-level timestamps.\n\n**--device** _DEVICE_\n> Device: cpu, cuda, auto (default: auto).\n\n**--compute_type** _TYPE_\n> Compute type: int8, float16, float32 (default: int8 on CPU).\n\n**--beam_size** _N_\n> Beam search size (default: 5).\n\n**--vad_filter** _BOOL_\n> Enable voice activity detection filter (uses Silero VAD).\n\n**--initial_prompt** _TEXT_\n> Optional text to provide as initial prompt for the decoder.\n\n**--threads** _N_\n> Number of CPU threads."],["DESCRIPTION","**faster-whisper** is a reimplementation of OpenAI's Whisper using **CTranslate2**, a fast inference engine for Transformer models. It provides up to 4x faster transcription than the original Whisper while using less memory.\n\nThe tool supports all Whisper model sizes. Larger models are more accurate but slower. The compute type parameter controls precision: int8 is fastest and most memory-efficient, float16 is a good balance on GPU, float32 is highest precision.\n\nVoice activity detection (VAD) filtering skips silent sections, improving both speed and accuracy. Language detection is automatic but specifying the language avoids detection overhead.\n\nThe base library is installed via `pip install faster-whisper` (Python API only). For CLI usage, install a wrapper such as `pip install faster-whisper-cli` or `pip install whisper-ctranslate2`. CTranslate2 handles model conversion automatically. GPU acceleration requires CUDA toolkit."],["CAVEATS","Large models require significant memory. CUDA toolkit needed for GPU. First run downloads and converts models. Accuracy varies by audio quality. No speaker diarization in CLI (available via API)."],["HISTORY","**faster-whisper** was created by **Guillaume Klein** (SYSTRAN) in **2023** using CTranslate2 to optimize Whisper inference. It became the preferred Whisper implementation for production use due to its speed and memory advantages. The project achieved wide adoption in transcription workflows."],["SEE ALSO","[whisper](/man/whisper)(1), [deepspeech](/man/deepspeech)(1), [ffmpeg](/man/ffmpeg)(1)"]],"e":["whisper","deepspeech","ffmpeg"],"x":"faster-whisper [audio.mp3]"},"fossil-forget":{"n":"fossil-forget","t":"untrack files without deleting them","s":[["TAGLINE","untrack files without deleting them"],["TLDR","**Forget a file**\n\n```fossil forget [file]```\n\n**Forget multiple files**\n\n```fossil forget [file1] [file2]```\n\n**Forget with dry run**\n\n```fossil forget --dry-run [file]```"],["SYNOPSIS","**fossil** **forget** [_options_] _file_..."],["PARAMETERS","**--dry-run**\n> Show what would be forgotten."],["DESCRIPTION","**fossil forget** removes files from source control but leaves them in the working directory. This is useful when you've added files by mistake and want to untrack them without deleting them.\n\nUnlike **fossil rm** or **fossil delete**, forgotten files remain on disk untouched. The command only affects the staging area, removing the scheduled addition of files that haven't been committed yet."],["INSTALL","```apt: sudo apt install fossil```\n\n```dnf: sudo dnf install fossil```\n\n```pacman: sudo pacman -S fossil```\n\n```apk: sudo apk add fossil```\n\n```zypper: sudo zypper install fossil```\n\n```brew: brew install fossil```\n\n```nix: nix profile install nixpkgs#fossil```"],["SEE ALSO","[fossil-rm](/man/fossil-rm)(1), [fossil-add](/man/fossil-add)(1)"],["RESOURCES","```[Homepage](https://fossil-scm.org/)```\n\n```[Documentation](https://fossil-scm.org/home/help?cmd=forget)```"]],"e":["fossil-rm","fossil-add"],"x":"fossil forget [file]"},"gdal_contour":{"n":"gdal_contour","t":"generates contour lines from elevation data","s":[["TAGLINE","generates contour lines from elevation data"],["TLDR","**Generate contours**\n\n```gdal_contour -a [elev] -i [10] [dem.tif] [contours.shp]```\n\n**Specify interval**\n\n```gdal_contour -i [100] [dem.tif] [contours.shp]```\n\n**Fixed contour levels**\n\n```gdal_contour -fl [100] [500] [1000] [dem.tif] [contours.shp]```\n\n**Output to GeoJSON**\n\n```gdal_contour -f \"GeoJSON\" -i [10] [dem.tif] [contours.geojson]```\n\n**Generate 3D contour polygons**\n\n```gdal_contour -p -3d -a [elev] -i [50] [dem.tif] [contours.shp]```"],["SYNOPSIS","**gdal_contour** [_options_] _input_ _output_"],["PARAMETERS","_INPUT_\n> Input elevation raster (DEM).\n\n_OUTPUT_\n> Output vector file.\n\n**-a** _NAME_\n> Attribute name for elevation.\n\n**-i** _INTERVAL_\n> Contour interval.\n\n**-fl** _LEVELS_\n> Fixed contour levels.\n\n**-f** _FORMAT_\n> Output format.\n\n**-b** _BAND_\n> Source band number (default: 1).\n\n**-3d**\n> Force production of 3D vectors instead of 2D.\n\n**-p**\n> Generate contour polygons rather than contour lines.\n\n**-off** _OFFSET_\n> Offset from zero relative to which to interpret intervals.\n\n**-snodata** _VALUE_\n> Input nodata value to ignore.\n\n**-nln** _NAME_\n> Output layer name.\n\n**-q**\n> Quiet mode, suppress progress output."],["DESCRIPTION","**gdal_contour** generates contour lines from elevation rasters (DEMs). It creates vector features representing lines of equal elevation.\n\nThe tool supports regular interval or specified level contours. Output includes elevation attributes for labeling. Multiple output formats are available.\n\ngdal_contour is used for cartography, terrain analysis, and visualization of elevation data."],["CAVEATS","Quality depends on DEM resolution. Dense contours may be slow. Output size varies with complexity."],["HISTORY","gdal_contour is part of **GDAL** (Geospatial Data Abstraction Library), providing contour generation from digital elevation models for mapping and analysis."],["INSTALL","```apt: sudo apt install gdal-bin```\n\n```apk: sudo apk add gdal-tools```"],["SEE ALSO","[gdaldem](/man/gdaldem)(1), [gdalinfo](/man/gdalinfo)(1), [ogr2ogr](/man/ogr2ogr)(1)"]],"e":["gdaldem","gdalinfo","ogr2ogr"],"x":"gdal_contour -a [elev] -i [10] [dem.tif] [contours.shp]"},"gfold":{"n":"gfold","t":"Track multiple Git repositories status","s":[["TAGLINE","Track multiple Git repositories status"],["TLDR","**Show** status of all repos in current directory\n\n```gfold```\n\n**Show** all repos recursively\n\n```gfold -r```\n\n**Show** only repos with uncommitted changes\n\n```gfold --dirty```\n\n**Display** in JSON format\n\n```gfold --json```"],["SYNOPSIS","**gfold** [_options_] [_path_]"],["PARAMETERS","**-r, --recursive**\n> Search recursively for repositories\n\n**--dirty**\n> Show only repositories with uncommitted changes\n\n**--json**\n> Output in JSON format\n\n**-p, --path** _PATH_\n> Path to search for repositories (default: current directory)\n\n**-v, --version**\n> Display version and exit\n\n**-h, --help**\n> Display help and exit"],["DESCRIPTION","**gfold** is a CLI tool that tracks the status of multiple Git repositories. It scans directories for Git repositories and displays their status in a clean, organized format.\n\nThe tool shows whether repositories are clean, have uncommitted changes, are ahead/behind the remote, or have untracked files. It's useful for developers working with many repositories who want a quick overview of all their projects."],["OUTPUT FORMAT","The output is organized by category:\n- **Clean**: Repository is up to date\n- **Unclean**: Has uncommitted changes\n- **Unpushed**: Commits not yet pushed\n- **Untracked**: Has untracked files"],["CAVEATS","Large directory trees may take time to scan. Requires read access to repository directories. Status may not reflect upstream changes without fetch."],["HISTORY","**gfold** was created as a modern alternative to manually checking multiple Git repositories, providing a unified view of repository status across projects."],["INSTALL","```pacman: sudo pacman -S gfold```\n\n```apk: sudo apk add gfold```\n\n```brew: brew install gfold```\n\n```nix: nix profile install nixpkgs#gfold```"],["SEE ALSO","[git](/man/git)(1), [ghq](/man/ghq)(1)"]],"e":["git","ghq"],"x":"gfold"},"git-bundle":{"n":"git-bundle","t":"Create and manipulate portable bundle files","s":[["TAGLINE","Create and manipulate portable bundle files"],["TLDR","**Create bundle of entire repository**\n\n```git bundle create [repo.bundle] --all```\n\n**Create bundle of branch**\n\n```git bundle create [branch.bundle] [branch]```\n\n**Create bundle since commit**\n\n```git bundle create [recent.bundle] [commit]..HEAD```\n\n**Verify bundle**\n\n```git bundle verify [repo.bundle]```\n\n**Clone from bundle**\n\n```git clone [repo.bundle] [directory]```\n\n**List bundle contents**\n\n```git bundle list-heads [repo.bundle]```"],["SYNOPSIS","**git** **bundle** _command_ [_options_] [_refs_]"],["SUBCOMMANDS","**create**\n> Create a bundle.\n\n**verify**\n> Verify a bundle.\n\n**list-heads**\n> List references.\n\n**unbundle**\n> Extract objects."],["PARAMETERS","**--all**\n> Include all refs."],["DESCRIPTION","**git bundle** creates and manipulates bundle files for transferring repository data offline. Bundles are portable binary archives containing Git objects and references, functioning as standalone repository snapshots.\n\nThe primary use case is sharing repository data without network access, such as air-gapped environments or slow connections. Bundles can contain entire repositories or specific branches and commit ranges. They act as read-only remotes when cloned or fetched from.\n\nUnlike archives, bundles preserve Git history and can be incrementally fetched from. They're verified cryptographically to ensure data integrity. Common workflows include creating bundles on one machine, physically transferring them, and cloning or fetching on another machine."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-archive](/man/git-archive)(1), [git-clone](/man/git-clone)(1), [git-fetch](/man/git-fetch)(1), [git-pull](/man/git-pull)(1)"]],"e":["git-archive","git-clone","git-fetch","git-pull"],"x":"git bundle create [repo.bundle] --all"},"git-read-tree":{"n":"git-read-tree","t":"Read tree information into index","s":[["TAGLINE","Read tree information into index"],["TLDR","**Read tree into index**\n\n```git read-tree [tree-ish]```\n\n**Merge trees**\n\n```git read-tree -m [base] [ours] [theirs]```\n\n**Read with prefix**\n\n```git read-tree --prefix=[dir/] [tree-ish]```\n\n**Merge and update working tree**\n\n```git read-tree -m -u [base] [ours] [theirs]```\n\n**Reset index**\n\n```git read-tree --reset HEAD```\n\n**Empty the index**\n\n```git read-tree --empty```\n\n**Dry run to check for errors**\n\n```git read-tree -n -m [tree-ish]```"],["SYNOPSIS","**git read-tree** [_options_] _tree-ish_"],["PARAMETERS","_TREE-ISH_\n> Tree to read.\n\n**-m**\n> Merge mode.\n\n**-u**\n> Update working tree.\n\n**--reset**\n> Same as -m, discard unmerged.\n\n**--prefix=**_DIR/_\n> Keep current index contents and read tree into subdirectory at prefix.\n\n**-i**\n> Only update index, do not check working tree for consistency with current head.\n\n**-n**, **--dry-run**\n> Check for errors without updating the index or working tree.\n\n**-v**\n> Show progress of checking files out.\n\n**--trivial**\n> Only perform merge for trivial cases, leave conflicts unresolved.\n\n**--aggressive**\n> Resolve more cases internally during three-way merge.\n\n**--index-output** _FILE_\n> Write results to specified file instead of $GIT_INDEX_FILE.\n\n**--empty**\n> Instead of reading tree into the index, empty it.\n\n**--no-sparse-checkout**\n> Disable sparse checkout support even if core.sparseCheckout is true.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git read-tree** reads tree information into the index. It is a plumbing command used internally by porcelain commands like `git checkout` and `git merge` to manipulate the staging area.\n\nThe command can read a single tree or perform three-way merges of trees. It updates the index without touching the working tree unless the `-u` flag is specified to synchronize the working directory."],["CAVEATS","Plumbing command. Usually invoked by other commands. Can overwrite index state."],["HISTORY","git read-tree is a core **Git** plumbing command implementing index manipulation, used internally by checkout, merge, and other commands."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-write-tree](/man/git-write-tree)(1), [git-ls-tree](/man/git-ls-tree)(1), [git-checkout](/man/git-checkout)(1), [git-merge](/man/git-merge)(1)"]],"e":["git-write-tree","git-ls-tree","git-checkout","git-merge"],"x":"git read-tree [tree-ish]"},"git-verify-pack":{"n":"git-verify-pack","t":"Validate packed Git archive files","s":[["TAGLINE","Validate packed Git archive files"],["TLDR","**Verify** a pack index file\n\n```git verify-pack .git/objects/pack/pack-hash.idx```\n\nVerify and **list all objects** in the pack\n\n```git verify-pack -v .git/objects/pack/pack-hash.idx```\n\nShow only **delta chain statistics** without verifying\n\n```git verify-pack -s .git/objects/pack/pack-hash.idx```"],["SYNOPSIS","**git** **verify-pack** [**-v** | **--verbose**] [**-s** | **--stat-only**] [**--**] _pack_.idx..."],["PARAMETERS","**-v**, **--verbose**\n> After verifying the pack, show the list of objects contained in the pack and a histogram of delta chain length\n\n**-s**, **--stat-only**\n> Do not verify the pack contents; only show the histogram of delta chain length. With **--verbose**, the list of objects is also shown\n\n**--**\n> Do not interpret any more arguments as options"],["DESCRIPTION","**git verify-pack** reads each given idx file and verifies the corresponding pack file and its index. It reports any corruption or inconsistencies found.\n\nWith **-v**, for non-deltified objects it shows: object-name, type, size, size-in-packfile, offset-in-packfile. For deltified objects it additionally shows: depth and base-object-name."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-repack](/man/git-repack)(1), [git-fsck](/man/git-fsck)(1)"]],"e":["git-repack","git-fsck"],"x":"git verify-pack .git/objects/pack/pack-hash.idx"},"gitgres":{"n":"gitgres","t":"Store Git objects and refs inside PostgreSQL","s":[["TAGLINE","Store Git objects and refs inside PostgreSQL"],["TLDR","**Initialize** a new repository inside the database\n\n```gitgres-backend init \"[dbname=gitgres]\" [repo_name]```\n\n**Push a local Git repo** into PostgreSQL\n\n```gitgres-backend push \"[dbname=gitgres]\" [repo_name] [path/to/repo]```\n\n**Clone** the database-backed repo back to disk\n\n```gitgres-backend clone \"[dbname=gitgres]\" [repo_name] [path/to/dest]```\n\n**List the refs** stored in the database\n\n```gitgres-backend ls-refs \"[dbname=gitgres]\" [repo_name]```\n\n**Import an existing repo** without compiling the backend\n\n```./import/gitgres-import.sh [path/to/repo] \"[dbname=gitgres]\" [repo_name]```"],["SYNOPSIS","**gitgres-backend** _command_ _connection_string_ _repo_name_ [_path_]"],["DESCRIPTION","**gitgres** stores Git objects and refs as rows in **PostgreSQL** tables. A small **libgit2**-based backend (**gitgres-backend**) translates standard Git protocol operations such as **push** and **clone** into SQL, so existing Git clients work against a database instead of a filesystem.\n\nThe project's stated goal is to let Git forges (e.g. **Forgejo**, **Gitea**) drop their filesystem storage entirely and rely on the database for repositories, objects, refs, and reflog. The schema includes tables for repositories, objects, refs, and reflog, with helpers for hashing, the object store, and tree and commit parsing."],["COMMANDS","**init** _conn_ _repo_\n> Create the database tables (if needed) and register a new repository named _repo_.\n\n**push** _conn_ _repo_ _path_\n> Push the on-disk Git repository at _path_ into the database under name _repo_.\n\n**clone** _conn_ _repo_ _path_\n> Materialize the database-backed repository _repo_ to a normal on-disk Git repository at _path_.\n\n**ls-refs** _conn_ _repo_\n> Print the refs currently stored in the database for _repo_."],["REQUIREMENTS","**PostgreSQL** with the **pgcrypto** extension is required. The backend links against **libgit2**, **libpq**, and **OpenSSL**. The shell-based importer (_./import/gitgres-import.sh_) needs only a working **psql** client and a Git checkout."],["CAVEATS","The project is research-grade: storing repositories in PostgreSQL incurs different I/O and locking patterns than a filesystem. Performance, vacuuming, and replication characteristics differ from traditional Git hosting and should be benchmarked before production use. Backups must include the database dump, not the working tree."],["HISTORY","**gitgres** was created by **Andrew Nesbitt** to explore eliminating filesystem dependencies for Git forges, motivated by the operational simplification of having a single backing store (PostgreSQL) for both metadata and Git data."],["SEE ALSO","[git](/man/git)(1), [psql](/man/psql)(1)"]],"e":["git","psql"],"x":"gitgres-backend init \"[dbname=gitgres]\" [repo_name]"},"gluerun":{"n":"gluerun","t":"Orchestration engine for parallel AI coding agents","s":[["TAGLINE","Orchestration engine for parallel AI coding agents"],["TLDR","**Scaffold** configuration and documentation in the current repository\n\n```gluerun init```\n\n**Validate** dependencies and repository configuration\n\n```gluerun doctor```\n\n**Run one orchestration cycle** (import, recover, integrate, dispatch, snapshot)\n\n```gluerun reconcile --actuate```\n\n**Execute a single task** through the agent pipeline\n\n```gluerun drive [TASK-0001]```\n\n**Run the autonomy loop** within a wall-clock budget\n\n```gluerun auto```\n\n**Pin** a specific engine version\n\n```gluerun update [version]```"],["SYNOPSIS","**gluerun** _command_ [_arguments_] [_options_]"],["DESCRIPTION","**gluerun** is an orchestration engine that coordinates multiple AI coding agents to work in parallel on a software repository. It implements a multi-tier scheduling model with lease management, audit workflows, and isolated execution using **git worktrees** so that concurrent agents do not interfere with each other.\n\nEach task is dispatched to a supported runner CLI (such as **claude** or **codex**), driven through a pipeline, and then integrated back with an audit trail of the evidence produced. The engine is installed once per machine and pinned per repository through a **.gluerun-version** file, so improvements propagate by bumping the version rather than re-copying scripts."],["PARAMETERS","**init**\n> Scaffold the initial configuration and documentation in a consumer repository.\n\n**doctor**\n> Validate required dependencies and the repository configuration.\n\n**reconcile** [**--actuate**]\n> Run one orchestration cycle: import, recover, integrate, dispatch, and snapshot. Without **--actuate** the cycle is a dry run.\n\n**drive** _TASK-ID_\n> Execute a single task through the agent pipeline.\n\n**auto**\n> Run the autonomy loop, dispatching work until a wall-clock budget is exhausted.\n\n**update** _version_\n> Pin a new engine version for the current repository."],["CAVEATS","Requires Bash 4 or newer, Python 3, Git, and at least one supported agent runner CLI on the PATH. Because the engine grants autonomous agents the ability to edit and commit code, run it only on repositories where you can review the resulting changes."],["HISTORY","gluerun (glueRun-go) is a community project written primarily in Bash and Python, built around git-worktree isolation and task evidence for agentic coding workflows. It is distributed under the GPL-3.0 license."],["SEE ALSO","[git-worktree](/man/git-worktree)(1), [claude](/man/claude)(1)"],["RESOURCES","```[Source code](https://github.com/alex-reysa/glueRun-go)```"]],"e":["git-worktree","claude"],"x":"gluerun init"},"gpu-screen-recorder":{"n":"gpu-screen-recorder","t":"Hardware-accelerated screen recorder","s":[["TAGLINE","Hardware-accelerated screen recorder"],["TLDR","Select a source using a **desktop portal** and record it\n\n```gpu-screen-recorder -w portal -o [path/to/video.mp4]```\n\nSpecify a **specific video source**\n\n```gpu-screen-recorder -w [screen|DP-1|HDMI-A1|...] -o [path/to/video.mp4]```\n\nList **video capture** sources\n\n```gpu-screen-recorder --list-capture-options```\n\nList **audio capture** sources\n\n```gpu-screen-recorder --list-audio-devices```\n\nRecord using the **replay buffer**\n\n```gpu-screen-recorder -w screen -r 30 -c mp4 -ro [path/to/directory] -o whatever```\n\n**Capture** a video from the replay buffer\n\n```pkill -SIGUSR1 -f gpu-screen-recorder```\n\nRun gpu-screen-recorder in the **background**\n\n```systemctl start --user gpu-screen-recorder```"],["SYNOPSIS","**gpu-screen-recorder** [_options_] **-w** _source_ **-o** _output_"],["PARAMETERS","**-w** _SOURCE_\n> Video source: window ID, monitor name, focused, portal, region, or V4L2 device path. Combine with |.\n\n**-o** _FILE_\n> Output file path, or directory in replay mode.\n\n**-r** _SECONDS_\n> Replay buffer duration in seconds (2-86400). Saves only on signal.\n\n**-ro** _DIR_\n> Regular recording output directory during replay/streaming mode.\n\n**-c** _CONTAINER_\n> Container format: mp4, mkv, flv, webm. Defaults to file extension.\n\n**-f** _FPS_\n> Frame rate (default: 60).\n\n**-fm** _cfr|vfr|content_\n> Frame rate mode (default: vfr).\n\n**-k** _CODEC_\n> Video codec: auto, h264, hevc, av1, vp8, vp9, hevc_hdr, av1_hdr (default: auto).\n\n**-q** _QUALITY_\n> Quality preset: medium, high, very_high, ultra (default: very_high). In CBR mode, specifies bitrate.\n\n**-bm** _auto|qp|vbr|cbr_\n> Bitrate mode (default: auto). CBR recommended for replay/streaming.\n\n**-a** _SOURCE_\n> Audio source: default_output, default_input, device:name, or app:name. Repeatable.\n\n**-ac** _aac|opus|flac_\n> Audio codec (default: opus for mp4/mkv, aac otherwise).\n\n**-s** _WxH_\n> Output resolution limit. Use 0x0 for original resolution.\n\n**-cr** _limited|full_\n> Color range (default: limited).\n\n**-cursor** _yes|no_\n> Include mouse cursor (default: yes).\n\n**-encoder** _gpu|cpu_\n> Encoding device (default: gpu). CPU limited to H264.\n\n**-keyint** _SECONDS_\n> Keyframe interval in seconds (default: 2.0).\n\n**--list-capture-options**\n> List available video capture sources.\n\n**--list-audio-devices**\n> List available audio devices.\n\n**--list-application-audio**\n> List available audio application names."],["DESCRIPTION","**gpu-screen-recorder** is a hardware-accelerated screen recorder that uses GPU encoding (NVIDIA NVENC, AMD VAAPI/AMF, or Intel VAAPI) for minimal CPU impact. It supports H264, HEVC, AV1, VP8, and VP9 codecs.\n\nKey features include a replay buffer (recording the last N seconds like instant replay), support for both X11 and Wayland via portals, and efficient hardware encoding. The replay buffer is saved by sending SIGUSR1 to the process and stopped cleanly with SIGINT. The tool supports multiple monitors, specific window capture, region capture, and simultaneous system audio and microphone recording. A CPU fallback encoder is available for H264."],["CAVEATS","Requires compatible GPU with hardware encoding support (NVIDIA with NVENC, AMD with VAAPI/AMF, or Intel with VAAPI). Wayland support depends on desktop portal implementation. Some desktop environments may require additional configuration."],["HISTORY","gpu-screen-recorder was created to provide an efficient, GPU-accelerated alternative to software-based screen recorders. It's particularly popular among Linux gamers for its low overhead and instant replay capabilities."],["INSTALL","```pacman: sudo pacman -S gpu-screen-recorder```\n\n```zypper: sudo zypper install gpu-screen-recorder```\n\n```nix: nix profile install nixpkgs#gpu-screen-recorder```"],["SEE ALSO","[obs](/man/obs)(1), [ffmpeg](/man/ffmpeg)(1), [wf-recorder](/man/wf-recorder)(1)"]],"e":["obs","ffmpeg","wf-recorder"],"x":"gpu-screen-recorder -w portal -o [path/to/video.mp4]"},"gradle-tasks":{"n":"gradle-tasks","t":"Display available Gradle project tasks","s":[["TAGLINE","Display available Gradle project tasks"],["TLDR","**List main tasks**\n\n```gradle tasks```\n\n**List all tasks including internal ones**\n\n```gradle tasks --all```\n\n**List with Gradle wrapper**\n\n```./gradlew tasks```\n\n**List tasks for a subproject**\n\n```gradle :app:tasks```\n\n**List tasks in a specific group**\n\n```gradle tasks --group [build]```\n\n**Get help on a specific task**\n\n```gradle help --task [taskName]```"],["SYNOPSIS","**gradle** **tasks** [**--all**] [**--group** _name_]"],["PARAMETERS","**--all**\n> Show all tasks including internal and lifecycle tasks without a group.\n\n**--group** _name_\n> Show only tasks belonging to the specified group.\n\n**--no-report**\n> Hide the task rule report at the bottom of the output."],["DESCRIPTION","**gradle tasks** displays available tasks for the selected project. By default, it shows only tasks assigned to a task group that have descriptions. Use **--all** to include ungrouped, internal, and lifecycle tasks. Use **--group** to filter by a specific task group. Use **gradle help --task** to get detailed information about a specific task including its type, path, and options.\n\nTasks are organized into groups such as build, verification, documentation, and help. Custom task groups defined in build scripts also appear in the output."],["INSTALL","```pacman: sudo pacman -S gradle```\n\n```apk: sudo apk add gradle```\n\n```brew: brew install gradle```\n\n```nix: nix profile install nixpkgs#gradle```"],["SEE ALSO","[gradle](/man/gradle)(1), [gradle-projects](/man/gradle-projects)(1), [ant](/man/ant)(1), [mvn](/man/mvn)(1)"]],"e":["gradle","gradle-projects","ant","mvn"],"x":"gradle tasks"},"habitctl":{"n":"habitctl","t":"Minimalist command-line habit tracker","s":[["TAGLINE","Minimalist command-line habit tracker"],["TLDR","**Open daily check-in** (alias often **h**)\n\n```habitctl```\n\n**Edit habit list** in $EDITOR\n\n```habitctl edith```\n\n**First run** creates config under ~/.habitctl\n\n```habitctl```"],["SYNOPSIS","**habitctl** [*command*]"],["DESCRIPTION","**habitctl** is a minimalist terminal habit tracker. Habits and a log live as plain files under **~/.habitctl/**. On first run it creates **habits** and **log**. Use **edith** to define habits with a period number (1 = daily, 7 = weekly, 0 = log-only) and a name. Running **habitctl** interactively prompts **y/n/s** (yes / no / skip) per habit and draws a compact history graph.\n\nBuild from source with Rust: **cargo build --release** (binary **target/release/habitctl**). Many users alias it to **h**."],["PARAMETERS","*(no args)*\n\n> Interactive daily log entry and history view.\n\n**edith**\n\n> Open the habit list in **$EDITOR**.\n\nOther subcommands, if present in your build, are shown by **habitctl --help**."],["CONFIGURATION","**~/.habitctl/habits**\n\n> Habit definitions: lines like **1 Meditated** or **7 Cleaned the apartment**.\n\n**~/.habitctl/log**\n\n> Append-only log of answers."],["CAVEATS","Designed for personal local use; no sync or multi-device support. Graph density depends on terminal width. Skipping (**s**) is for days when the habit did not apply."],["SEE ALSO","[klog](/man/klog)(1), [task](/man/task)(1), [todo.sh](/man/todo.sh)(1)"],["RESOURCES","```[Source code](https://github.com/blinry/habitctl)```"]],"e":["klog","task","todo.sh"],"x":"habitctl"},"htpasswd":{"n":"htpasswd","t":"manages user authentication files for Apache HTTP Server's basic authentication","s":[["TAGLINE","manages user authentication files for Apache HTTP Server's basic authentication"],["TLDR","**Create a new password file** with a user\n\n```htpasswd -c [path/to/.htpasswd] [username]```\n\n**Add or update a user** in existing file\n\n```htpasswd [path/to/.htpasswd] [username]```\n\n**Add user with password** on command line (insecure)\n\n```htpasswd -b [path/to/.htpasswd] [username] [password]```\n\n**Delete a user** from the file\n\n```htpasswd -D [path/to/.htpasswd] [username]```\n\n**Verify a user's password**\n\n```htpasswd -v [path/to/.htpasswd] [username]```\n\n**Use bcrypt** encryption (more secure)\n\n```htpasswd -B [path/to/.htpasswd] [username]```\n\n**Display password hash** without updating file\n\n```htpasswd -n [username]```\n\n**Create file using SHA** encryption\n\n```htpasswd -c -s [path/to/.htpasswd] [username]```"],["SYNOPSIS","**htpasswd** [_-cimBdpsDv_] [_-C cost_] _passwordfile_ _username_ [_password_]"],["PARAMETERS","**-c**\n> Create a new file (overwrites existing).\n\n**-n**\n> Display results on stdout, don't update file.\n\n**-b**\n> Use password from command line (batch mode, insecure).\n\n**-i**\n> Read password from stdin without verification.\n\n**-m**\n> Use MD5 encryption (default on most systems).\n\n**-B**\n> Use bcrypt encryption (most secure).\n\n**-C** _cost_\n> Set bcrypt cost (4-17, default 5, higher = slower).\n\n**-d**\n> Use crypt() encryption (insecure, limited to 8 chars).\n\n**-s**\n> Use SHA encryption (insecure).\n\n**-p**\n> Use plaintext (insecure, for testing only).\n\n**-D**\n> Delete the specified user.\n\n**-v**\n> Verify password for user."],["DESCRIPTION","**htpasswd** manages user authentication files for Apache HTTP Server's basic authentication. It creates and updates flat-file databases containing usernames and encrypted passwords used with **.htaccess** or Apache configuration directives.\n\nThe password file format is simple: one line per user with **username:encrypted_password**. Apache's mod_auth_basic reads this file to authenticate requests. The file should be stored outside the web root and have restrictive permissions.\n\nPassword encryption defaults to MD5-based algorithm (prefixed with $apr1$). The **-B** flag enables bcrypt, which is more resistant to brute-force attacks and recommended for new installations. The cost factor (-C) controls bcrypt's computational intensity.\n\nFor non-interactive use in scripts, **-b** allows specifying the password on the command line, though this exposes the password in process lists. The **-i** flag reads from stdin, which is safer for scripting.\n\nThe tool is often used with Nginx as well, since Nginx can read Apache-format password files for basic authentication."],["CAVEATS","The -b flag exposes passwords in process listings. Basic authentication sends credentials in base64 (not encrypted) - always use with HTTPS. Old algorithms (crypt, SHA, plaintext) are insecure. File permissions must be restrictive. Not for high-security applications."],["HISTORY","**htpasswd** has been part of the Apache HTTP Server project since its early days in the mid-1990s. It evolved from NCSA HTTPd's password management tools. The bcrypt option was added later as password security requirements increased. The tool remains the standard for managing Apache basic auth despite the availability of more sophisticated authentication methods."],["INSTALL","```apt: sudo apt install apache2-utils```\n\n```apk: sudo apk add apache2-utils```\n\n```zypper: sudo zypper install apache2-utils```"],["SEE ALSO","[apache2](/man/apache2)(8), [nginx](/man/nginx)(8), [openssl](/man/openssl)(1)"]],"e":["apache2","nginx","openssl"],"x":"htpasswd -c [path/to/.htpasswd] [username]"},"jstack":{"n":"jstack","t":"prints Java thread stack traces for a given Java process","s":[["TAGLINE","prints Java thread stack traces for a given Java process"],["TLDR","**Print thread dump for Java process**\n\n```jstack [pid]```\n\n**Force thread dump on hung process**\n\n```jstack -F [pid]```\n\n**Print concurrent locks info**\n\n```jstack -l [pid]```\n\n**Print mixed mode stack traces**\n\n```jstack -m [pid]```"],["SYNOPSIS","**jstack** [_options_] _pid_"],["PARAMETERS","**-F**\n> Force thread dump when process is hung.\n\n**-l**\n> Long listing with lock information.\n\n**-m**\n> Print mixed mode (Java and native) stack traces.\n\n**-h**, **--help**\n> Display help message."],["DESCRIPTION","**jstack** prints Java thread stack traces for a given Java process. It is useful for diagnosing deadlocks, performance issues, and analyzing thread states. Part of the JDK diagnostic tools suite."],["CAVEATS","The target process must be running with the same user or you need root privileges. The -F option should only be used when the process is not responding."],["SEE ALSO","[jps](/man/jps)(1), [jmap](/man/jmap)(1)"]],"e":["jps","jmap"],"x":"jstack [pid]"},"keen":{"n":"keen","t":"Minimal terminal AI coding agent with pluggable LLM providers","s":[["TAGLINE","Minimal terminal AI coding agent with pluggable LLM providers"],["TLDR","**Start the agent** in the current directory\n\n```keen```\n\n**Run without a global install** via npx\n\n```npx keen-code```\n\n**Check the installed version**\n\n```keen --version```\n\n**Switch to a different LLM provider mid-session** (inside the agent)\n\n```/model```\n\n**Adjust the extended-reasoning effort level** (inside the agent)\n\n```/thinking```\n\n**Compress the conversation** when nearing the context limit (inside the agent)\n\n```/compact```"],["SYNOPSIS","**keen** [_options_]"],["DESCRIPTION","**keen** (distributed as the **keen-code** npm package) is a small, terminal-based coding agent written in Go. It treats the AI model as a generic backend and ships a deliberately tiny tool surface: **read_file**, **write_file**, **edit_file**, **glob**, **grep**, and **bash**. That is the entire toolbox the agent uses to navigate, edit, and run code, by design favouring simplicity over feature breadth.\n\nInvoking **keen** with no arguments drops you into an interactive session anchored at the current working directory. The agent reads files, edits them in place, and runs shell commands on your behalf, conversing through the terminal. Slash commands such as **/model**, **/thinking**, and **/compact** control provider selection, reasoning depth, and context compaction without leaving the session.\n\nThe agent is provider-agnostic: it can drive Anthropic, OpenAI (including ChatGPT OAuth), Google AI (Gemini), DeepSeek, Kimi, GLM, MiniMax, Codex, and the Go port of OpenCode. Switching providers is done at runtime with **/model** rather than via separate CLI flags, so a single **keen** install covers every supported backend.\n\nA notable trait of the project: every line of code in the repository is reported to have been written by an AI agent, with the prompts and design decisions archived under **.ai-interactions/**."],["PARAMETERS","**--version**\n\n> Print the **keen-code** version and exit. Also reachable as **npx keen-code --version** for users who skip the global install."],["SLASH COMMANDS","**/model**\n\n> Switch between configured LLM providers without restarting the session.\n\n**/thinking**\n\n> Adjust the extended-reasoning effort level for providers that support it.\n\n**/compact**\n\n> Compress the running conversation when the context window fills up."],["INSTALLATION","**Via npm** (recommended, installs the **keen** binary on **PATH**):\n\n```\nnpm install -g keen-code\n```\n\n**Via the install script** (drops the binary in **/usr/local/bin** or **$HOME/.local/bin**):\n\n```\ncurl -fsSL https://raw.githubusercontent.com/mochow13/keen-code/main/scripts/install.sh | bash\n```\n\n**One-off run** without installing:\n\n```\nnpx keen-code\n```"],["CAVEATS","The minimal six-tool design is intentional and means **keen** does not expose specialised helpers other agents bake in (web fetching, structured planning tools, sub-agents). Workflows that depend on such features may need a different agent.\n\nEach LLM provider must be configured with its own credentials. Costs and rate limits accrue against whichever provider is currently selected via **/model**."],["HISTORY","**keen-code** is a 2026 project (latest release v0.20.3 as of June 2, 2026) published under the **MIT** license by **mochow13** on GitHub. It is part of the broader wave of minimalist terminal coding agents inspired by Claude Code, Codex, and OpenCode, distinguishing itself by limiting the tool surface and by being entirely AI-authored."],["SEE ALSO","[claude](/man/claude)(1), [codex](/man/codex)(1), [paseo](/man/paseo)(1)"]],"e":["claude","codex","paseo"],"x":"keen"},"keepnote":{"n":"keepnote","t":"note-taking application","s":[["TAGLINE","note-taking application"],["TLDR","**Start KeepNote**\n\n```keepnote```\n\n**Open notebook**\n\n```keepnote [notebook_path]```\n\n**Open specific page**\n\n```keepnote [notebook_path] --page [page_id]```"],["SYNOPSIS","**keepnote** [_options_] [_notebook_]"],["PARAMETERS","_NOTEBOOK_\n> Path to notebook directory.\n\n**--page** _ID_\n> Open specific page.\n\n**--version**\n> Show version.\n\n**--help**\n> Display help information."],["DESCRIPTION","**KeepNote** is a desktop note-taking application that organizes notes into hierarchical, tree-structured notebooks with rich text formatting support. Notes can contain styled text, images, hyperlinks, and file attachments, making it suitable for research documentation, journaling, and technical notes. The GTK-based interface presents a familiar three-pane layout with a notebook tree, page list, and editor.\n\nNotebooks are stored on disk as directories of HTML files and associated resources, which makes them easy to back up, version control, or browse outside of the application. Each note is a self-contained HTML document, so the data format remains accessible even without KeepNote installed. The application is written in Python and supports full-text search across all notes within a notebook."],["CAVEATS","Python/GTK application. Development slowed. Consider alternatives like Joplin."],["HISTORY","KeepNote was created by **Matt Rasmussen** as an open-source note-taking application with hierarchical organization."],["INSTALL","```aur: yay -S keepnote```"],["SEE ALSO","[joplin](/man/joplin)(1)"]],"e":["joplin"],"x":"keepnote"},"kibi":{"n":"kibi","t":"Text editor in less than 1024 lines of Rust","s":[["TAGLINE","Text editor in less than 1024 lines of Rust"],["TLDR","**Open a file**\n\n```kibi [path/to/file]```\n\n**Create a new file**\n\n```kibi```"],["SYNOPSIS","**kibi** [_file_]"],["DESCRIPTION","**kibi** is a configurable text editor with UTF-8 support, incremental search, syntax highlighting, and line numbers, written in less than 1024 lines of Rust with minimal dependencies. It supports syntax highlighting configuration for **38+ languages**.\n\nInspired by **kilo** (created by Salvatore Sanfilippo), kibi aims to be a tiny but functional editor that guarantees memory safety through Rust while keeping the codebase extremely small."],["KEY BINDINGS","**Ctrl+S** — Save\n**Ctrl+Q** — Quit\n**Ctrl+F** — Find (incremental search)\n**Ctrl+G** — Go to line\n**Ctrl+D** — Duplicate line"],["CAVEATS","Limited feature set by design — no plugin system or advanced editing features. The 1024-line constraint means some features found in full editors are absent."],["HISTORY","**kibi** was created by **Ilaï Deutel** (ilai-deutel) and is written in **Rust**. It is compatible with Linux, macOS, Windows 10, and WASI. The name \"kibi\" means 1024 (as in kibibyte), referencing the line count constraint."],["INSTALL","```apk: sudo apk add kibi```\n\n```brew: brew install kibi```\n\n```nix: nix profile install nixpkgs#kibi```"],["SEE ALSO","[nano](/man/nano)(1), [micro](/man/micro)(1), [vim](/man/vim)(1)"]],"e":["nano","micro","vim"],"x":"kibi [path/to/file]"},"local":{"n":"local","t":"bash builtin that declares variables with local scope within a function","s":[["TAGLINE","bash builtin that declares variables with local scope within a function"],["TLDR","**Declare a local variable**\n\n```local [varname]```\n\n**Declare a local variable with an initial value**\n\n```local [varname]=\"[value]\"```\n\n**Declare a local integer variable**\n\n```local -i [count]=0```\n\n**Declare a local indexed array**\n\n```local -a [array]=([a] [b] [c])```\n\n**Declare a local readonly variable**\n\n```local -r [constant]=\"[value]\"```\n\n**Inherit attributes and value from a variable in the surrounding scope**\n\n```local -I [varname]```"],["SYNOPSIS","**local** [_option_] _name_[=_value_] ..."],["PARAMETERS","**-a**\n> Declare as indexed array.\n\n**-A**\n> Declare as associative array.\n\n**-i**\n> Declare as integer.\n\n**-l**\n> Convert to lowercase.\n\n**-u**\n> Convert to uppercase.\n\n**-r**\n> Make readonly.\n\n**-n**\n> Name reference to another variable.\n\n**-x**\n> Mark for export to child processes.\n\n**-I**\n> Inherit attributes and value from a variable with the same name at a surrounding scope."],["DESCRIPTION","**local** is a bash builtin that declares variables with local scope within a function. Local variables are not visible outside the function where they are declared.\n\nUsing local prevents function variables from polluting or conflicting with the global namespace. The return status is zero unless local is used outside a function, an invalid name is supplied, or the variable is readonly."],["EXAMPLE","```bash\nmy_function() {\n    local result=\"success\"\n    local -i count=5\n    echo \"$result: $count\"\n}"],["$result and $count not accessible here","```"],["CAVEATS","Only valid inside functions. Not POSIX-compliant (use typeset for portability). Local variables shadow global ones. Subshells inherit but cannot modify."],["SEE ALSO","[declare](/man/declare)(1), [typeset](/man/typeset)(1), [export](/man/export)(1), [readonly](/man/readonly)(1), [unset](/man/unset)(1), [bash](/man/bash)(1)"]],"e":["declare","typeset","export","readonly","unset","bash"],"x":"local [varname]"},"lp":{"n":"lp","t":"prints files to a printer","s":[["TAGLINE","prints files to a printer"],["TLDR","**Print file**\n\n```lp [file.pdf]```\n\n**Print to specific printer**\n\n```lp -d [printer_name] [file]```\n\n**Print multiple copies**\n\n```lp -n [3] [file]```\n\n**Print double-sided**\n\n```lp -o sides=two-sided-long-edge [file]```\n\n**Print specific pages**\n\n```lp -o page-ranges=[1-5] [file]```\n\n**Print from stdin**\n\n```cat [file] | lp```"],["SYNOPSIS","**lp** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Files to print.\n\n**-d** _PRINTER_\n> Destination printer.\n\n**-n** _COPIES_\n> Number of copies.\n\n**-o** _OPTION_\n> Printer option.\n\n**-P** _PAGES_\n> Page list to print.\n\n**-q** _PRIORITY_\n> Job priority (1-100).\n\n**--help**\n> Display help information."],["DESCRIPTION","**lp** prints files to a printer. It's the System V print command used with CUPS.\n\nThe tool submits print jobs to the print queue. Options control copies, pages, and print quality."],["CAVEATS","Requires CUPS. Printer must be configured. Options vary by printer."],["HISTORY","lp is the **System V** print command, now implemented by CUPS as the standard Linux/macOS print interface."],["INSTALL","```apt: sudo apt install lprng```\n\n```dnf: sudo dnf install cups-client```\n\n```apk: sudo apk add cups-client```\n\n```zypper: sudo zypper install cups-client```"],["SEE ALSO","[lpr](/man/lpr)(1), [lpq](/man/lpq)(1), [lprm](/man/lprm)(1), [lpoptions](/man/lpoptions)(1)"]],"e":["lpr","lpq","lprm","lpoptions"],"x":"lp [file.pdf]"},"lxc-profile":{"n":"lxc-profile","t":"manages configuration profiles for LXD containers and virtual machines","s":[["TAGLINE","manages configuration profiles for LXD containers and virtual machines"],["TLDR","**List** all available profiles\n\n```lxc profile list```\n\n**Show** the configuration of a specific profile\n\n```lxc profile show [profile_name]```\n\n**Edit** a specific profile in the default editor\n\n```lxc profile edit [profile_name]```\n\n**Create** a new profile\n\n```lxc profile create [profile_name]```\n\n**Launch** a new container with specific profiles\n\n```lxc launch [container_image] [container_name] -p [profile1] -p [profile2]```\n\n**Change the profiles** of a running container\n\n```lxc profile assign [container_name] [profile1,profile2]```"],["SYNOPSIS","**lxc profile** _command_ [_options_]"],["PARAMETERS","**list**\n> List all available profiles\n\n**show _profile_**\n> Display the full configuration of a profile\n\n**edit _profile_**\n> Open a profile for editing in the default editor\n\n**create _profile_**\n> Create a new empty profile\n\n**delete _profile_**\n> Remove a profile\n\n**copy _source_ _dest_**\n> Copy a profile to a new name\n\n**rename _old_ _new_**\n> Rename a profile\n\n**assign _instance_ _profiles_**\n> Set the profiles for an instance (comma-separated list)\n\n**add _instance_ _profile_**\n> Add a profile to an instance's existing profiles\n\n**remove _instance_ _profile_**\n> Remove a profile from an instance\n\n**set _profile_ _key_ _value_**\n> Set a profile configuration key\n\n**get _profile_ _key_**\n> Get a profile configuration value\n\n**device add _profile_ _device_ _type_ [_options_]**\n> Add a device to a profile\n\n**device remove _profile_ _device_**\n> Remove a device from a profile"],["DESCRIPTION","**lxc profile** manages configuration profiles for LXD containers and virtual machines. Profiles define reusable sets of configuration options including resource limits, device mappings, network settings, and security policies.\n\nContainers can use multiple profiles, which are applied in order (later profiles override earlier ones). The **default** profile is automatically applied to new containers and typically includes basic network and storage configuration.\n\nProfile configuration uses YAML format, specifying options like CPU/memory limits, disk quotas, network interfaces, and GPU passthrough."],["CAVEATS","Profile changes don't automatically apply to running containers; restart the container or reassign profiles to apply changes. Deleting a profile that's in use by containers will fail. The default profile cannot be deleted."],["INSTALL","```apt: sudo apt install lxd-client```\n\n```dnf: sudo dnf install lxc```\n\n```pacman: sudo pacman -S lxc```\n\n```zypper: sudo zypper install lxc```\n\n```brew: brew install lxc```\n\n```nix: nix profile install nixpkgs#lxc```"],["SEE ALSO","[lxc](/man/lxc)(1), [lxc-create](/man/lxc-create)(1), [lxc-start](/man/lxc-start)(1), [lxc-ls](/man/lxc-ls)(1)"]],"e":["lxc","lxc-create","lxc-start","lxc-ls"],"x":"lxc profile list"},"mame":{"n":"mame","t":"emulates arcade game hardware","s":[["TAGLINE","emulates arcade game hardware"],["TLDR","**Run arcade ROM**\n\n```mame [romname]```\n\n**List available games**\n\n```mame -listfull```\n\n**Run with fullscreen**\n\n```mame -window 0 [romname]```\n\n**Verify ROM set**\n\n```mame -verifyroms [romname]```\n\n**Show controls for game**\n\n```mame -listctrl [romname]```\n\n**Configure controls**\n\n```mame [romname] -ui_active```"],["SYNOPSIS","**mame** [_options_] [_system_] [_media_...]"],["PARAMETERS","**-window**\n> Run windowed (1) or fullscreen (0).\n\n**-listfull**\n> List all supported systems.\n\n**-verifyroms**\n> Verify ROM integrity.\n\n**-rompath** _path_\n> ROM directory.\n\n**-listctrl**\n> Show control configuration.\n\n**-ui_active**\n> Activate UI during emulation.\n\n**-createconfig**\n> Create default config."],["DESCRIPTION","**MAME** (Multiple Arcade Machine Emulator) emulates arcade game hardware. It preserves gaming history by documenting and emulating arcade machines, allowing original software to run on modern computers.\n\nMAME supports thousands of arcade systems and some home consoles."],["DIRECTORY STRUCTURE","```\n~/.mame/\n├── roms/      # Game ROMs\n├── cfg/       # Configurations\n├── nvram/     # Saved data\n└── snap/      # Screenshots\n```"],["CAVEATS","ROMs must be legally obtained. ROM sets change between versions. Some games need CHD files. Performance varies by emulated system."],["HISTORY","MAME was started by **Nicola Salmoria** in **1997**. It merged with MESS in 2015 to become a universal emulation project preserving both arcade and home systems."],["INSTALL","```dnf: sudo dnf install mame```\n\n```pacman: sudo pacman -S mame```\n\n```zypper: sudo zypper install mame```\n\n```brew: brew install mame```\n\n```nix: nix profile install nixpkgs#mame```"],["SEE ALSO","[retroarch](/man/retroarch)(1), [dosbox](/man/dosbox)(1)"]],"e":["retroarch","dosbox"],"x":"mame [romname]"},"mgrtopbm":{"n":"mgrtopbm","t":"converts MGR window system bitmap format to PBM","s":[["TAGLINE","converts MGR window system bitmap format to PBM"],["TLDR","**Convert MGR bitmap to PBM**\n\n```mgrtopbm [input.mgr] > [output.pbm]```"],["SYNOPSIS","**mgrtopbm** [_file_]"],["DESCRIPTION","**mgrtopbm** converts MGR window system bitmap format to PBM. MGR was a windowing system developed at Bellcore in the late 1980s. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmtomgr](/man/pbmtomgr)(1), [xbmtopbm](/man/xbmtopbm)(1)"]],"e":["pbmtomgr","xbmtopbm"],"x":"mgrtopbm [input.mgr] > [output.pbm]"},"minikube":{"n":"minikube","t":"run a local single-node Kubernetes cluster","s":[["TAGLINE","run a local single-node Kubernetes cluster"],["TLDR","**Start the cluster** with the default driver\n\n```minikube start```\n\n**Start with a specific** driver\n\n```minikube start --driver=[docker]```\n\n**Start with a Kubernetes** version pin\n\n```minikube start --kubernetes-version=[v1.30.0]```\n\n**Stop the running** cluster\n\n```minikube stop```\n\n**Delete the cluster** and its data\n\n```minikube delete```\n\n**Check cluster** status\n\n```minikube status```\n\n**Open the Kubernetes dashboard** in a browser\n\n```minikube dashboard```\n\n**SSH into the cluster node**\n\n```minikube ssh```\n\n**Get a service URL** exposed inside the cluster\n\n```minikube service [service_name] --url```\n\n**Enable a built-in addon**\n\n```minikube addons enable [ingress]```"],["SYNOPSIS","**minikube** _command_ [_options_]"],["PARAMETERS","**start**\n> Create and start the local Kubernetes cluster.\n\n**stop**\n> Stop the cluster while preserving its state.\n\n**delete**\n> Delete the cluster, releasing all VM and disk resources.\n\n**status**\n> Show the status of host, kubelet, apiserver, and kubeconfig.\n\n**dashboard**\n> Launch the Kubernetes web dashboard.\n\n**ssh**\n> Open a shell on the underlying VM or container.\n\n**service** _NAME_\n> Print the URL of a Kubernetes service, optionally opening it in the browser.\n\n**addons** _SUBCOMMAND_\n> Manage built-in addons (ingress, metrics-server, registry, ...).\n\n**tunnel**\n> Create a network route into the cluster for LoadBalancer services.\n\n**profile** _NAME_\n> Switch between named clusters managed by minikube.\n\n**--driver** _NAME_\n> Select the hypervisor or runtime: docker, podman, kvm2, hyperkit, virtualbox, qemu, vfkit, parallels, hyperv, none.\n\n**--cpus** _N_\n> Number of CPUs allocated to the node.\n\n**--memory** _MB_\n> Amount of RAM in MB (or with suffix like 4g).\n\n**--disk-size** _SIZE_\n> Disk size for the node, e.g. **20g**.\n\n**--container-runtime** _RUNTIME_\n> containerd (default since v1.24), cri-o, or docker.\n\n**--kubernetes-version** _VERSION_\n> Kubernetes release to deploy, e.g. **v1.30.0**.\n\n**--addons** _LIST_\n> Comma-separated list of addons to enable at start.\n\n**--help**\n> Display help information."],["DESCRIPTION","**minikube** provisions and runs a local single-node (or small multi-node) Kubernetes cluster suitable for development, learning, and CI. It abstracts the underlying compute platform: depending on the **--driver** flag it can spin up a VM (kvm2, virtualbox, hyperkit, qemu) or a container (docker, podman) hosting the Kubernetes components, then writes a context to **~/.kube/config** so **kubectl** can target the cluster transparently.\n\nMinikube ships with optional addons (ingress, metrics-server, registry, storage-provisioner) that can be enabled with **minikube addons enable**. The **service** and **tunnel** commands bridge cluster networking to the host so applications running inside the cluster are reachable from the developer's machine.\n\nMultiple clusters can coexist via **--profile**, each with its own VM, addons, and kubeconfig context."],["CAVEATS","Single-node by default; multi-node clusters (**--nodes=N**) exist but are intended for local testing only. Resource-hungry: a typical cluster needs at least 2 CPUs and 2 GiB of RAM. The **none** driver runs Kubernetes directly on the host and is destructive to existing system configuration. On macOS Apple Silicon, prefer the **docker**, **qemu**, or **vfkit** drivers; **hyperkit** is x86-only."],["HISTORY","**minikube** was created by the Kubernetes community to provide a reliable on-laptop development experience. It graduated from the **kubernetes-incubator** to the main **kubernetes** GitHub organization and has tracked upstream Kubernetes releases closely since version **0.1** (2016)."],["INSTALL","```pacman: sudo pacman -S minikube```\n\n```zypper: sudo zypper install minikube```\n\n```brew: brew install minikube```\n\n```nix: nix profile install nixpkgs#minikube```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kind](/man/kind)(1), [k3s](/man/k3s)(1), [k3d](/man/k3d)(1)"]],"e":["kubectl","kind","k3s","k3d"],"x":"minikube start"},"morse":{"n":"morse","t":"Translate text to Morse code","s":[["TAGLINE","Translate text to Morse code"],["TLDR","**Translate text to Morse code (dots and dashes)**\n\n```morse -s \"[Hello World]\"```\n\n**Translate plaintext to audible Morse (speaker)**\n\n```morse -p \"[Hello World]\"```\n\n**Decode Morse dots/dashes back to text**\n\n```morse -d \"[.... . .-.. .-.. ---]\"```\n\n**Read text from a file instead of arguments**\n\n```morse -e [file.txt]```\n\n**Adjust words-per-minute when playing audio**\n\n```morse -p -w [20] \"[CQ]\"```"],["SYNOPSIS","**morse** [**-dlps**] [**-e** _file_] [**-w** _wpm_] [**-c** _cpm_] [_string_...]"],["PARAMETERS","_string_\n> Text to encode (or Morse dots/dashes to decode with **-d**).\n\n**-d**\n> Decode Morse (dots and dashes) to text.\n\n**-e** _file_\n> Read input from _file_ instead of the command line.\n\n**-l**\n> Generate \"line-mode\" output suitable for LED flashers.\n\n**-p**\n> Play Morse audibly via the speaker.\n\n**-s**\n> Output short dots/dashes instead of \"dit\"/\"dah\".\n\n**-w** _wpm_\n> Words-per-minute when playing audio (default 20).\n\n**-c** _cpm_\n> Characters-per-minute. Alternative to **-w**."],["DESCRIPTION","**morse** reads input text from arguments or a file and prints the corresponding Morse code. With **-s** the output is the familiar **... --- ...** form; the default form spells out **dit** and **dah**. With **-p** each symbol is played through the system speaker at a configurable speed. With **-d** the tool performs the inverse translation, converting dots and dashes back to text.\n\nLower-case and upper-case letters are treated the same. Unknown characters are silently skipped."],["CAVEATS","Audible output (**-p**) requires a console or PC speaker; on most modern Linux systems it falls back to nothing unless **pcspkr** is loaded. The **bsdgames** implementation ships different flags on Linux and macOS — consult your local man page if the exact flags differ."],["HISTORY","**morse** is one of the classic **BSD games**, present since early 4BSD distributions as a teaching aid for amateur radio operators."],["INSTALL","```apt: sudo apt install morse```\n\n```brew: brew install morse```"],["SEE ALSO","[beep](/man/beep)(1), [espeak](/man/espeak)(1)"]],"e":["beep","espeak"],"x":"morse -s \"[Hello World]\""},"motion":{"n":"motion","t":"Detect motion using a video4linux device or network camera","s":[["TAGLINE","Detect motion using a video4linux device or network camera"],["TLDR","**Start motion detection with default config**\n\n```motion```\n\n**Use a specific config file**\n\n```motion -c [/etc/motion/motion.conf]```\n\n**Run in foreground (non-daemon mode)**\n\n```motion -n```\n\n**Run as a background daemon**\n\n```motion -b```\n\n**Run with verbose logging at a specific level**\n\n```motion -d [1-9]```\n\n**Log to a specific file**\n\n```motion -l [/var/log/motion.log]```\n\n**Start in pause mode (motion detection disabled until activated)**\n\n```motion -m```\n\n**Set process ID file**\n\n```motion -p [/var/run/motion.pid]```"],["SYNOPSIS","**motion** [**-hbnsm**] [**-c** _config_file_] [**-d** _level_] [**-k** _level_] [**-p** _pid_file_] [**-l** _log_file_]"],["PARAMETERS","**-c** _FILE_\n> Specifies the path to the configuration file.\n\n**-n**\n> Run in non-daemon mode (foreground).\n\n**-b**\n> Run as a background daemon.\n\n**-s**\n> Enable setup mode; forces non-daemon execution.\n\n**-d** _level_\n> Set debug verbosity level from 1 to 9.\n\n**-k** _level_\n> Set message log type from 1 to 9.\n\n**-l** _FILE_\n> Specify the log file path.\n\n**-p** _FILE_\n> Specify the process ID file location.\n\n**-m**\n> Start in pause mode (motion detection initially disabled).\n\n**-h**\n> Display help information."],["DESCRIPTION","**motion** is a program that detects motion using a Video4Linux device or network camera. It monitors video feeds for movement and can capture images and video sequences when activity is detected, as well as execute automated actions for notifications or snapshots.\n\nThe tool supports multiple cameras, live streaming, and configurable motion detection sensitivity. Command-line arguments override corresponding settings in configuration files."],["CAVEATS","Can be CPU intensive depending on camera resolution and detection settings. Requires camera access (Video4Linux or network camera). Configuration can become complex for multi-camera and advanced detection setups."],["HISTORY","motion was created as an **open source** motion detection system for Linux security and surveillance applications. It uses the Video4Linux interface and has been widely adopted for home security and wildlife monitoring."],["INSTALL","```apt: sudo apt install motion```\n\n```pacman: sudo pacman -S motion```\n\n```zypper: sudo zypper install motion```\n\n```nix: nix profile install nixpkgs#motion```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [v4l2-ctl](/man/v4l2-ctl)(1), [vlc](/man/vlc)(1), [fswebcam](/man/fswebcam)(1), [streamer](/man/streamer)(1)"]],"e":["ffmpeg","v4l2-ctl","vlc","fswebcam","streamer"],"x":"motion"},"mpicxx":{"n":"mpicxx","t":"compiles MPI C++ programs","s":[["TAGLINE","compiles MPI C++ programs"],["TLDR","**Compile an MPI C++ program**\n\n```mpicxx -o [program] [source.cpp]```\n\n**Compile with optimization**\n\n```mpicxx -O3 -o [program] [source.cpp]```\n\n**Show the full underlying compiler command**\n\n```mpicxx --showme```\n\n**Show only compiler flags**\n\n```mpicxx --showme:compile```\n\n**Show only linker flags**\n\n```mpicxx --showme:link```\n\n**Compile and link against an additional library**\n\n```mpicxx -o [program] [source.cpp] -l[library]```"],["SYNOPSIS","**mpicxx** [_options_] _source_files_..."],["PARAMETERS","**-o** _file_\n> Output file name.\n\n**-O** _level_\n> Optimization level (0, 1, 2, 3).\n\n**--showme**\n> Show the full compiler command that would be invoked without actually compiling.\n\n**--showme:compile**\n> Show only the compiler flags added by the MPI wrapper.\n\n**--showme:link**\n> Show only the linker flags added by the MPI wrapper.\n\n**--showme:command**\n> Show the underlying C++ compiler command.\n\n**--showme:incdirs**\n> Show include directories used by the MPI wrapper.\n\n**--showme:libdirs**\n> Show library directories used by the MPI wrapper.\n\n**--showme:libs**\n> Show library names used for linking.\n\n**--showme:version**\n> Show the version of Open MPI.\n\n**-I** _dir_\n> Add directory to the include search path.\n\n**-L** _dir_\n> Add directory to the library search path.\n\n**-l** _library_\n> Link against the specified library."],["DESCRIPTION","**mpicxx** is the Open MPI C++ wrapper compiler. It is not a compiler itself but a convenience wrapper that adds the necessary compiler and linker flags for MPI programs, then invokes the underlying system C++ compiler. It is equivalent to **mpic++**.\n\nThe wrapper transparently adds **-I**, **-L**, and **-l** flags required to compile and link against the MPI library. All other flags are passed directly to the underlying C++ compiler. Users can inspect exactly what flags are added using the **--showme** family of options.\n\nEnvironment variables such as **OMPI_CXX** (Open MPI) or **MPICH_CXX** (MPICH) can override the underlying C++ compiler used by the wrapper."],["INSTALL","```apk: sudo apk add openmpi-dev```"],["SEE ALSO","[mpirun](/man/mpirun)(1), [mpiexec](/man/mpiexec)(1), [mpic++](/man/mpic++)(1), [mpicc](/man/mpicc)(1), [g++](/man/g++)(1)"]],"e":["mpirun","mpiexec","mpic++","mpicc","g++"],"x":"mpicxx -o [program] [source.cpp]"},"nmon":{"n":"nmon","t":"systems administrator, tuner, and benchmark tool that monitors and displays","s":[["TAGLINE","systems administrator, tuner, and benchmark tool that monitors and displays"],["TLDR","Start **interactive** nmon\n\n```nmon```\n\nSave records to **file** with defaults\n\n```nmon -f```\n\nSave with **30 second** interval, **240 measurements**\n\n```nmon -f -s 30 -c 240```\n\nEnable **capacity planning** mode\n\n```nmon -x```\n\nInclude **top processes** in output\n\n```nmon -fdt```"],["SYNOPSIS","**nmon** [**-h**] [**-s** _seconds_] [**-c** _count_] [**-f** **-d** _disks_ **-t** **-r** _name_] [**-x**]"],["DESCRIPTION","**nmon** is a systems administrator, tuner, and benchmark tool that monitors and displays CPU, memory, network, disks, file systems, NFS, top processes, and resource information. It can run interactively or output data to a spreadsheet-compatible file."],["PARAMETERS","**-h**\n> Display full help information\n\n**-f**\n> Enable spreadsheet output format for file recording\n\n**-s SECONDS**\n> Set refresh interval in seconds (default: 2)\n\n**-c COUNT**\n> Set number of refreshes to capture\n\n**-d DISKS**\n> Increase disk monitoring count (default: 256)\n\n**-t**\n> Include top processes in spreadsheet output\n\n**-x**\n> Enable capacity planning mode with 15-minute intervals over 1 day (-fdt -s 900 -c 96)\n\n**-r NAME**\n> Set the filename prefix for output files"],["CAVEATS","In interactive mode, press \"h\" for help on available keyboard shortcuts. The default spreadsheet mode uses -s300 -c288 (5-minute intervals for 24 hours). Output files are in CSV format suitable for analysis tools."],["HISTORY","**nmon** was created by Nigel Griffiths at IBM and has become a popular performance monitoring tool for Linux systems."],["INSTALL","```apt: sudo apt install nmon```\n\n```dnf: sudo dnf install nmon```\n\n```pacman: sudo pacman -S nmon```\n\n```apk: sudo apk add nmon```\n\n```zypper: sudo zypper install nmon```\n\n```nix: nix profile install nixpkgs#nmon```"],["SEE ALSO","[top](/man/top)(1), [vmstat](/man/vmstat)(8), [iostat](/man/iostat)(1), [sar](/man/sar)(1)"]],"e":["top","vmstat","iostat","sar"],"x":"nmon"},"npm-view":{"n":"npm-view","t":"View npm registry information for a package","s":[["TAGLINE","View npm registry information for a package"],["TLDR","**View package info**\n\n```npm view [package]```\n\n**View a specific field**\n\n```npm view [package] version```\n\n**View all published versions**\n\n```npm view [package] versions```\n\n**View package dependencies**\n\n```npm view [package] dependencies```\n\n**View a nested field**\n\n```npm view [package] repository.url```\n\n**Output as JSON**\n\n```npm view [package] --json```\n\n**View dist-tags** (latest, next, etc.)\n\n```npm view [package] dist-tags```\n\n**View a specific version's info**\n\n```npm view [package]@[2.0.0]```\n\n**View info from a custom registry**\n\n```npm view [package] --registry [https://registry.example.com]```"],["SYNOPSIS","**npm** **view** [_package_[@_version_]] [_field_[._subfield_]...]"],["PARAMETERS","_PACKAGE_\n> Package name, optionally with @version or @tag.\n\n_FIELD_\n> Top-level field to display (version, dependencies, license, repository, etc.).\n\n_FIELD.SUBFIELD_\n> Nested field access (e.g., repository.url, dist.tarball).\n\n**versions**\n> List all published versions.\n\n**dependencies**\n> Show production dependencies.\n\n**dist-tags**\n> Show distribution tags.\n\n**--json**\n> Output in JSON format.\n\n**--registry** _url_\n> Query a custom registry URL."],["DESCRIPTION","**npm view** fetches and displays metadata about a package from the npm registry. It shows the full package manifest by default, or specific fields when requested.\n\nThe command supports nested field access with dot notation (e.g., `repository.url`). When a field contains an array, each element is printed on a separate line. Multiple fields can be specified to show several values at once.\n\nAliases: **npm info**, **npm show**."],["CAVEATS","Requires network access to query the registry. When viewing scoped packages, the scope must be included (e.g., @scope/package). Output format varies between single values and arrays. Private packages require authentication."],["HISTORY","**npm view** has been available since early versions of npm, providing command-line access to the package registry metadata that powers npm's dependency resolution."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-search](/man/npm-search)(1), [npm-pack](/man/npm-pack)(1), [npm-install](/man/npm-install)(1), [npm-publish](/man/npm-publish)(1), [npm-ls](/man/npm-ls)(1), [npm-outdated](/man/npm-outdated)(1)"]],"e":["npm","npm-search","npm-pack","npm-install","npm-publish","npm-ls","npm-outdated"],"x":"npm view [package]"},"nvidia-settings":{"n":"nvidia-settings","t":"configures NVIDIA graphics driver settings","s":[["TAGLINE","configures NVIDIA graphics driver settings"],["TLDR","**Open settings GUI**\n\n```nvidia-settings```\n\n**Query GPU temperature**\n\n```nvidia-settings -q GPUCoreTemp```\n\n**Set fan speed**\n\n```nvidia-settings -a \"[gpu:0]/GPUFanControlState=1\" -a \"[fan:0]/GPUTargetFanSpeed=[70]\"```\n\n**Query all attributes**\n\n```nvidia-settings -q all```\n\n**Load settings from file**\n\n```nvidia-settings --load-config-only```\n\n**Set PowerMizer mode**\n\n```nvidia-settings -a \"[gpu:0]/GPUPowerMizerMode=[1]\"```"],["SYNOPSIS","**nvidia-settings** [_options_]"],["PARAMETERS","**-q** _attr_\n> Query attribute.\n\n**-a** _attr=value_\n> Assign attribute.\n\n**-c** _display_\n> X display to use.\n\n**--load-config-only**\n> Load config, exit.\n\n**-t**\n> Terse output.\n\n**-V** _attr_\n> Show valid values."],["DESCRIPTION","**nvidia-settings** configures NVIDIA graphics driver settings. It provides a GUI and command-line interface for adjusting display, performance, and GPU settings."],["COMMON ATTRIBUTES","```\nGPUCoreTemp          - GPU temperature\nGPUCurrentClockFreqs - Clock speeds\nGPUFanControlState   - Manual fan control\nGPUTargetFanSpeed    - Fan speed %\nGPUPowerMizerMode    - Performance mode\n```"],["CONFIG FILE","```"],["~/.nvidia-settings-rc","[gpu:0]/GPUPowerMizerMode=1\n[gpu:0]/GPUFanControlState=1\n```"],["CAVEATS","Requires NVIDIA proprietary driver. Some settings need X running. Fan control may void warranty. Settings reset on driver update."],["HISTORY","nvidia-settings is developed by **NVIDIA Corporation** as the official configuration tool for their Linux graphics drivers."],["INSTALL","```pacman: sudo pacman -S nvidia-settings```\n\n```zypper: sudo zypper install nvidia-settings```"],["SEE ALSO","[nvidia-smi](/man/nvidia-smi)(1), [xrandr](/man/xrandr)(1)"]],"e":["nvidia-smi","xrandr"],"x":"nvidia-settings"},"ollama":{"n":"ollama","t":"runs large language models locally","s":[["TAGLINE","runs large language models locally"],["TLDR","**Run a model** interactively\n\n```ollama run [llama3]```\n\n**Run a model** with a prompt\n\n```ollama run [llama3] \"[What is the capital of France?]\"```\n\n**List installed models**\n\n```ollama list```\n\n**Pull a model**\n\n```ollama pull [mistral]```\n\n**Show model info**\n\n```ollama show [llama3]```\n\n**List running models**\n\n```ollama ps```\n\n**Remove a model**\n\n```ollama rm [model_name]```\n\n**Start the API server**\n\n```ollama serve```"],["SYNOPSIS","**ollama** [_command_] [_options_]"],["PARAMETERS","**run** _MODEL_ [_PROMPT_]\n> Run model interactively or with a one-off prompt.\n\n**pull** _MODEL_\n> Download model from registry.\n\n**push** _MODEL_\n> Push model to registry.\n\n**list** (or **ls**)\n> List locally available models.\n\n**show** _MODEL_\n> Show model information (architecture, parameters, license).\n\n**ps**\n> List currently running models.\n\n**stop** _MODEL_\n> Stop a running model.\n\n**rm** _MODEL_\n> Remove a local model.\n\n**cp** _SOURCE_ _DESTINATION_\n> Copy a model locally under a new name.\n\n**serve**\n> Start the Ollama API server (default port 11434).\n\n**create** _NAME_ **-f** _MODELFILE_\n> Create a custom model from a Modelfile.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ollama** runs large language models locally. It handles model downloads, serving via a REST API, and interactive chat sessions.\n\nSupports a wide range of open models including Llama, Mistral, Gemma, Phi, Qwen, DeepSeek, and others. Models are pulled from the Ollama registry and cached locally.\n\nThe API server provides OpenAI-compatible endpoints for chat completions, embeddings, and model management. Custom models can be created using Modelfiles that specify base models, system prompts, parameters, and adapter layers."],["CAVEATS","Requires sufficient RAM/VRAM depending on model size. GPU acceleration is supported (NVIDIA, AMD, Apple Silicon). The API server listens on localhost:11434 by default; configure with OLLAMA_HOST environment variable."],["HISTORY","**Ollama** was created by **Jeffrey Morgan** and first released in **2023**. Built on **llama.cpp**, it simplifies the process of downloading, running, and managing open-source language models locally. The project quickly gained popularity as interest in running LLMs without cloud APIs grew."],["INSTALL","```dnf: sudo dnf install ollama```\n\n```pacman: sudo pacman -S ollama```\n\n```apk: sudo apk add ollama```\n\n```zypper: sudo zypper install ollama```\n\n```brew: brew install ollama```\n\n```nix: nix profile install nixpkgs#ollama```"],["SEE ALSO","[llama.cpp](/man/llama.cpp)(1), [llamafile](/man/llamafile)(1)"]],"e":["llama.cpp","llamafile"],"x":"ollama run [llama3]"},"openssl-req":{"n":"openssl-req","t":"creates and processes certificate signing requests","s":[["TAGLINE","creates and processes certificate signing requests"],["TLDR","**Generate CSR with new key**\n\n```openssl req -new -newkey rsa:[4096] -keyout [private.key] -out [request.csr]```\n\n**Generate CSR from existing key**\n\n```openssl req -new -key [private.key] -out [request.csr]```\n\n**Generate self-signed certificate**\n\n```openssl req -x509 -newkey rsa:[4096] -keyout [key.pem] -out [cert.pem] -days [365] -noenc```\n\n**View CSR contents**\n\n```openssl req -in [request.csr] -text -noout```\n\n**Verify CSR signature**\n\n```openssl req -in [request.csr] -verify -noout```\n\n**Generate CSR with subject on command line**\n\n```openssl req -new -key [private.key] -out [request.csr] -subj \"/C=[US]/ST=[State]/L=[City]/O=[Org]/CN=[example.com]\"```\n\n**Generate CSR with config file**\n\n```openssl req -new -config [openssl.cnf] -keyout [key.pem] -out [request.csr]```\n\n**Generate self-signed cert with SAN extension**\n\n```openssl req -x509 -newkey rsa:[4096] -keyout [key.pem] -out [cert.pem] -days [365] -noenc -addext \"subjectAltName=DNS:[example.com],DNS:[www.example.com]\"```"],["SYNOPSIS","**openssl req** [_options_]"],["PARAMETERS","**-new**\n> Generate new CSR.\n\n**-x509**\n> Output certificate instead of CSR.\n\n**-newkey** _type:bits_\n> Generate new key.\n\n**-key** _file_\n> Use existing key.\n\n**-keyout** _file_\n> Output key file.\n\n**-out** _file_\n> Output file.\n\n**-days** _n_\n> Validity period (only with -x509).\n\n**-noenc**\n> Don't encrypt the output key. Replaces deprecated -nodes.\n\n**-nodes**\n> Don't encrypt key. Deprecated in OpenSSL 3.0; use -noenc instead.\n\n**-subj** _subj_\n> Set subject DN (e.g., /C=US/O=Org/CN=host).\n\n**-addext** _ext_\n> Add a certificate extension (e.g., subjectAltName=DNS:example.com).\n\n**-config** _file_\n> Use alternative configuration file.\n\n**-text**\n> Print the CSR or certificate in human-readable form.\n\n**-noout**\n> Suppress output of the encoded request.\n\n**-verify**\n> Verify the signature on the CSR.\n\n**-in** _file_\n> Input CSR file.\n\n**-inform** _DER|PEM_\n> Input format. Default is PEM.\n\n**-outform** _DER|PEM_\n> Output format. Default is PEM.\n\n**-*digest***\n> Message digest to sign the request (e.g., -sha256, -sha384)."],["DESCRIPTION","**openssl req** creates and processes certificate signing requests (CSRs). It can also generate self-signed certificates for testing.\n\nCSRs are submitted to Certificate Authorities to obtain signed certificates."],["SUBJECT FORMAT","```bash\nopenssl req -new -key key.pem -out csr.pem \\\n  -subj \"/C=US/ST=State/L=City/O=Org/CN=example.com\"\n```"],["CAVEATS","Self-signed certificates are not trusted by browsers without manual import. The -nodes flag is deprecated since OpenSSL 3.0; use -noenc instead. CSR does not contain the private key."],["HISTORY","Certificate request functionality has been part of OpenSSL since its SSL/TLS implementation origins."],["INSTALL","```apt: sudo apt install openssl```\n\n```dnf: sudo dnf install openssl```\n\n```pacman: sudo pacman -S openssl```\n\n```apk: sudo apk add openssl```\n\n```zypper: sudo zypper install openssl```\n\n```brew: brew install openssl```\n\n```nix: nix profile install nixpkgs#openssl```"],["SEE ALSO","[openssl](/man/openssl)(1), [openssl-x509](/man/openssl-x509)(1), [openssl-ca](/man/openssl-ca)(1), [openssl-genpkey](/man/openssl-genpkey)(1), [openssl-pkey](/man/openssl-pkey)(1), [openssl-rsa](/man/openssl-rsa)(1)"]],"e":["openssl","openssl-x509","openssl-ca","openssl-genpkey","openssl-pkey","openssl-rsa"],"x":"openssl req -new -newkey rsa:[4096] -keyout [private.key] -out [request.csr]"},"pass-otp":{"n":"pass-otp","t":"extends pass with TOTP/HOTP support","s":[["TAGLINE","extends pass with TOTP/HOTP support"],["TLDR","**Generate OTP code**\n\n```pass otp [path/to/entry]```\n\n**Insert OTP secret**\n\n```pass otp insert [path/to/entry]```\n\n**Insert from URI**\n\n```pass otp insert -u [path/to/entry]```\n\n**Append OTP to existing entry**\n\n```pass otp append [path/to/entry]```\n\n**Show OTP URI**\n\n```pass otp uri [path/to/entry]```\n\n**Copy OTP to clipboard**\n\n```pass otp -c [path/to/entry]```\n\n**Validate OTP configuration**\n\n```pass otp validate [path/to/entry]```"],["SYNOPSIS","**pass otp** [_options_] _command_ [_entry_]"],["PARAMETERS","**insert**\n> Add new OTP entry.\n\n**append**\n> Add OTP to existing entry.\n\n**uri**\n> Show OTP URI.\n\n**validate**\n> Validate configuration.\n\n**-c**, **--clip**\n> Copy to clipboard.\n\n**-q**, **--qrcode**\n> Display QR code.\n\n**-u**, **--uri**\n> Read from otpauth URI.\n\n**-s**, **--secret**\n> Prompt for secret only."],["DESCRIPTION","**pass-otp** extends pass (password-store) with TOTP/HOTP support. It generates time-based one-time passwords.\n\nOTP secrets are stored encrypted alongside passwords. The standard otpauth:// URI format is supported.\n\nToken generation uses the current time for TOTP. Codes refresh every 30 seconds by default.\n\nQR code display enables backup to authenticator apps. URIs can be imported from other authenticators.\n\nClipboard integration copies codes for easy pasting. Codes clear automatically after timeout."],["CAVEATS","Requires pass (password-store) installed. OTP secrets should be backed up separately. Time synchronization affects TOTP accuracy."],["HISTORY","**pass-otp** was created as an extension to **pass** (password-store) by **zx2c4**. It brings two-factor authentication capabilities to the Unix password manager."],["INSTALL","```dnf: sudo dnf install pass-otp```\n\n```pacman: sudo pacman -S pass-otp```\n\n```zypper: sudo zypper install pass-otp```\n\n```brew: brew install pass-otp```"],["SEE ALSO","[pass](/man/pass)(1), [oathtool](/man/oathtool)(1), [gpg](/man/gpg)(1)"]],"e":["pass","oathtool","gpg"],"x":"pass otp [path/to/entry]"},"pbmtomgr":{"n":"pbmtomgr","t":"Convert PBM bitmaps to MGR window system format","s":[["TAGLINE","Convert PBM bitmaps to MGR window system format"],["TLDR","**Convert PBM to MGR format**\n\n```pbmtomgr [input.pbm] > [output.mgr]```\n\n**Convert from stdin**\n\n```cat [input.pbm] | pbmtomgr > [output.mgr]```"],["SYNOPSIS","**pbmtomgr** [_file_]"],["PARAMETERS","_file_\n> Input PBM file. Reads from stdin if omitted."],["DESCRIPTION","**pbmtomgr** reads a PBM image as input and produces a MGR bitmap as output. MGR is a window manager that was a smaller alternative to the X Window System, developed at Bellcore in the 1980s. Part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[mgrtopbm](/man/mgrtopbm)(1), [xbmtopbm](/man/xbmtopbm)(1)"]],"e":["mgrtopbm","xbmtopbm"],"x":"pbmtomgr [input.pbm] > [output.mgr]"},"pct-snapshot":{"n":"pct-snapshot","t":"create a snapshot of a Proxmox LXC container","s":[["TAGLINE","create a snapshot of a Proxmox LXC container"],["TLDR","**Create** a snapshot named pre-upgrade\n\n```pct snapshot 100 pre-upgrade```\n\nCreate a snapshot with a **description**\n\n```pct snapshot 100 pre-upgrade --description \"before package upgrade\"```"],["SYNOPSIS","**pct snapshot** _vmid_ _snapname_ [_OPTIONS_]"],["PARAMETERS","**vmid**\n> The numeric ID of the container (100–999999999)\n\n**snapname**\n> Name of the new snapshot\n\n**--description** _text_\n> A textual description or comment for the snapshot"],["DESCRIPTION","**pct snapshot** creates a snapshot of a Proxmox VE LXC container, capturing its disk and configuration state under the given name. Snapshots can later be listed with **pct listsnapshot**, restored with **pct rollback**, or removed with **pct delsnapshot**."],["CAVEATS","Snapshot support depends on the underlying storage (for example LVM-thin, ZFS, or Ceph). Not all storage types support snapshots. Taking a snapshot while the container is under heavy write load may briefly affect I/O."],["HISTORY","**pct snapshot** is part of the **Proxmox VE** **pct** tool for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-listsnapshot](/man/pct-listsnapshot)(1), [pct-rollback](/man/pct-rollback)(1), [pct-delsnapshot](/man/pct-delsnapshot)(1)"],["RESOURCES","```[Documentation](https://pve.proxmox.com/pve-docs/pct.1.html)```\n\n```[Homepage](https://www.proxmox.com)```"]],"e":["pct","pct-listsnapshot","pct-rollback","pct-delsnapshot"],"x":"pct snapshot 100 pre-upgrade"},"pg_restore":{"n":"pg_restore","t":"restores PostgreSQL databases from archives created by pg_dump","s":[["TAGLINE","restores PostgreSQL databases from archives created by pg_dump"],["TLDR","**Restore from custom format**\n\n```pg_restore -d [database] [backup.dump]```\n\n**Restore specific table**\n\n```pg_restore -d [database] -t [tablename] [backup.dump]```\n\n**List contents of backup**\n\n```pg_restore -l [backup.dump]```\n\n**Restore with parallelism**\n\n```pg_restore -d [database] -j [4] [backup.dump]```\n\n**Clean before restore**\n\n```pg_restore -d [database] -c [backup.dump]```"],["SYNOPSIS","**pg_restore** [_options_] [_file_]"],["PARAMETERS","**-d**, **--dbname** _name_\n> Database to restore to.\n\n**-t**, **--table** _name_\n> Restore specific table.\n\n**-n**, **--schema** _name_\n> Restore specific schema.\n\n**-j**, **--jobs** _num_\n> Parallel jobs.\n\n**-c**, **--clean**\n> Drop objects before creating.\n\n**-C**, **--create**\n> Create the database.\n\n**-l**, **--list**\n> List archive contents.\n\n**-L** _file_\n> Use table of contents file."],["DESCRIPTION","**pg_restore** restores PostgreSQL databases from archives created by pg_dump in custom, directory, or tar format. It cannot restore plain SQL dumps (use psql for those).\n\nThe tool can selectively restore objects using a table-of-contents file, run multiple parallel jobs (directory format only), and optionally create the target database before restoring."],["CAVEATS","Cannot restore plain SQL dumps. Parallel restore requires directory format. May need superuser for some objects."],["HISTORY","pg_restore is part of **PostgreSQL**, complementing pg_dump for custom format backup restoration."],["INSTALL","```apk: sudo apk add postgresql17-client```"],["SEE ALSO","[pg_dump](/man/pg_dump)(1), [pg_dumpall](/man/pg_dumpall)(1), [psql](/man/psql)(1)"]],"e":["pg_dump","pg_dumpall","psql"],"x":"pg_restore -d [database] [backup.dump]"},"pgmdeshadow":{"n":"pgmdeshadow","t":"attempts to compensate for uneven lighting or shadows in scanned","s":[["TAGLINE","attempts to compensate for uneven lighting or shadows in scanned"],["TLDR","**Remove uneven lighting from scan**\n\n```pgmdeshadow [input.pgm] > [output.pgm]```"],["SYNOPSIS","**pgmdeshadow** [_file_]"],["DESCRIPTION","**pgmdeshadow** attempts to compensate for uneven lighting or shadows in scanned images by analyzing and normalizing local brightness. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pgmnorm](/man/pgmnorm)(1), [ppmntsc](/man/ppmntsc)(1)"]],"e":["pgmnorm","ppmntsc"],"x":"pgmdeshadow [input.pgm] > [output.pgm]"},"phpdox":{"n":"phpdox","t":"Generate PHP API documentation from source code","s":[["TAGLINE","Generate PHP API documentation from source code"],["TLDR","**Generate documentation using the default config**\n\n```phpdox```\n\n**Use a specific configuration file**\n\n```phpdox -f [phpdox.xml]```\n\n**Run only the collector phase** (parse source code)\n\n```phpdox -c```\n\n**Run only the generator phase** (produce documentation)\n\n```phpdox -g```\n\n**Generate a skeleton configuration file**\n\n```phpdox --skel > [phpdox.xml]```\n\n**Generate a skeleton config without comments**\n\n```phpdox --skel --strip > [phpdox.xml]```\n\n**List available output engines**\n\n```phpdox --engines```"],["SYNOPSIS","**phpdox** [_options_]"],["PARAMETERS","**-f**, **--file** _FILE_\n> Use specified XML configuration file (defaults to ./phpdox.xml or ./phpdox.xml.dist).\n\n**-c**, **--collector**\n> Run only the collector process (source parsing).\n\n**-g**, **--generator**\n> Run only the generator process (documentation output).\n\n**--backends**\n> Show a list of available backends and exit.\n\n**--engines**\n> Show a list of available output engines and exit.\n\n**--enrichers**\n> Show a list of available output enrichers and exit.\n\n**--skel**\n> Show an annotated skeleton configuration XML file and exit.\n\n**--strip**\n> Strip comments from skeleton config XML when using **--skel**.\n\n**-v**, **--version**\n> Display application version.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**phpDox** generates PHP API documentation from source code using standard technologies (SRC, DOCBLOCK, XML and XSLT). It parses PHP source files, extracts PHPDoc comment blocks, and produces HTML documentation. Configuration is defined in an XML file (typically **phpdox.xml**).\n\nThe documentation process has two phases: the **collector** parses source code into an XML representation, and the **generator** transforms it into output using configurable engines and enrichers."],["CONFIGURATION","**phpdox.xml**\n> XML configuration file specifying source directories, output paths, template settings, and build targets. Required for operation. Generate a template with **--skel**."],["CAVEATS","Requires an XML configuration file. PHPDoc comment blocks in source code are recommended for useful output. The project is no longer actively maintained."],["INSTALL","```aur: yay -S phpdox```"],["SEE ALSO","[phpdoc](/man/phpdoc)(1), [doxygen](/man/doxygen)(1), [php](/man/php)(1)"]],"e":["phpdoc","doxygen","php"],"x":"phpdox"},"pnmtosir":{"n":"pnmtosir","t":"Convert PNM to Solitaire recorder format","s":[["TAGLINE","Convert PNM to Solitaire recorder format"],["TLDR","**Convert PNM to Solitaire image**\n\n```pnmtosir [input.pnm] > [output.sir]```"],["SYNOPSIS","**pnmtosir** [_file_]"],["DESCRIPTION","**pnmtosir** converts PNM images to Solitaire image recorder format for film recording. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[sirtopnm](/man/sirtopnm)(1), [pnmtotiff](/man/pnmtotiff)(1)"]],"e":["sirtopnm","pnmtotiff"],"x":"pnmtosir [input.pnm] > [output.sir]"},"pppoe-discovery":{"n":"pppoe-discovery","t":"Discover PPPoE access concentrators","s":[["TAGLINE","Discover PPPoE access concentrators"],["TLDR","**Discover PPPoE servers**\n\n```pppoe-discovery -I [eth0]```\n\n**Verbose discovery**\n\n```pppoe-discovery -I [eth0] -V```\n\n**Specify service name**\n\n```pppoe-discovery -I [eth0] -S [service]```"],["SYNOPSIS","**pppoe-discovery** [_options_]"],["PARAMETERS","**-I** _INTERFACE_\n> Network interface.\n\n**-V**\n> Verbose output.\n\n**-S** _NAME_\n> Service name filter.\n\n**-A** _NAME_\n> AC name filter."],["DESCRIPTION","**pppoe-discovery** sends PPPoE Active Discovery Initiation (PADI) packets on a network interface to find available PPPoE access concentrators. It reports the names and services offered by any responding concentrators without actually establishing a connection.\n\nThis diagnostic tool is useful for verifying that a DSL modem or network is properly configured and that the ISP's access concentrator is reachable before attempting a full PPPoE connection."],["CAVEATS","Discovery only, no connection. Requires interface up."],["HISTORY","pppoe-discovery provides **PPPoE server discovery** functionality."],["INSTALL","```apt: sudo apt install ppp```\n\n```dnf: sudo dnf install ppp```\n\n```pacman: sudo pacman -S ppp```\n\n```apk: sudo apk add ppp-pppoe```\n\n```zypper: sudo zypper install ppp```\n\n```nix: nix profile install nixpkgs#ppp```"],["SEE ALSO","[pppoe](/man/pppoe)(8), [pppoe-connect](/man/pppoe-connect)(8)"]],"e":["pppoe","pppoe-connect"],"x":"pppoe-discovery -I [eth0]"},"puppet-agent":{"n":"puppet-agent","t":"Apply configuration from Puppet server","s":[["TAGLINE","Apply configuration from Puppet server"],["TLDR","**Run agent once**\n\n```puppet agent --test```\n\n**Run in foreground**\n\n```puppet agent --no-daemonize --verbose```\n\n**Dry run (noop)**\n\n```puppet agent --test --noop```\n\n**Run with specific server**\n\n```puppet agent --test --server [puppet.example.com]```\n\n**Enable agent**\n\n```puppet agent --enable```\n\n**Disable agent**\n\n```puppet agent --disable \"[reason]\"```"],["SYNOPSIS","**puppet agent** [_--test_] [_--noop_] [_--server host_] [_options_]"],["PARAMETERS","**--test**\n> Single run with output.\n\n**--noop**\n> Dry run mode.\n\n**--server** _HOST_\n> Puppet server.\n\n**--no-daemonize**\n> Run in foreground.\n\n**--enable**\n> Enable agent runs.\n\n**--disable** _MSG_\n> Disable with message.\n\n**--verbose**\n> Verbose output.\n\n**--debug**\n> Debug output."],["DESCRIPTION","**puppet agent** is the client component of the Puppet configuration management system. It connects to a Puppet server, retrieves a compiled catalog of the desired system state, and applies the necessary changes to bring the local machine into compliance. The agent handles SSL certificate management, fact submission via Facter, and detailed reporting of applied changes.\n\nIn daemon mode, the agent runs as a background service and checks in with the server at a configurable interval, typically every 30 minutes. For manual operations, **--test** performs a single run with verbose output, while **--noop** previews what changes would be made without actually applying them. The agent can be temporarily disabled with a lock message to prevent runs during maintenance windows."],["CONFIGURATION","**/etc/puppetlabs/puppet/puppet.conf**\n> Agent configuration including server hostname, run interval, environment, certificate name, and splay settings.\n\n**/etc/puppetlabs/puppet/ssl/**\n> SSL certificate directory containing the agent's private key, signed certificate, and CA certificate for server authentication.\n\n**/opt/puppetlabs/puppet/cache/state/agent_disabled.lock**\n> Lock file created when the agent is disabled, containing the disable message as JSON."],["CAVEATS","Requires Puppet installation. Needs server or local manifests. Root privileges required."],["HISTORY","**Puppet** was created by **Luke Kanies** in 2005. The agent component manages system configuration across infrastructure."],["INSTALL","```dnf: sudo dnf install puppet```\n\n```pacman: sudo pacman -S puppet```"],["SEE ALSO","[puppet](/man/puppet)(1), [facter](/man/facter)(1), [ansible](/man/ansible)(1)"]],"e":["puppet","facter","ansible"],"x":"puppet agent --test"},"pwdsafety":{"n":"pwdsafety","t":"Command-line tool for checking password safety","s":[["TAGLINE","Command-line tool for checking password safety"],["TLDR","**Check a password from stdin**\n\n```echo \"[password]\" | pwdsafety```"],["SYNOPSIS","**pwdsafety** [_options_]"],["DESCRIPTION","**pwdsafety** is a command-line tool that reads a password from standard input and evaluates its safety. The score combines several checks: whether the password (or its reverse) appears in a built-in dictionary of known weak passwords, the presence of uppercase letters, lowercase letters, digits, and symbols, the password length, and a Shannon-entropy calculation.\n\nThe tool prints the numeric score along with explanations of which rules passed or failed. When the score is 68 or lower, **pwdsafety** also suggests a random, stronger password."],["EXIT STATUS","Returns **0** when a password is evaluated successfully (regardless of score) and non-zero on input errors."],["CAVEATS","Does not store, log, or transmit any password information — evaluation is fully local. Reads only from standard input, so passwords supplied on a shell command line will be visible in the process list and shell history; pipe from a secure source instead."],["HISTORY","**pwdsafety** was created by **edoardottt** and is written in **Go**."],["INSTALL","```nix: nix profile install nixpkgs#pwdsafety```"],["SEE ALSO","[pass](/man/pass)(1), [pwgen](/man/pwgen)(1)"]],"e":["pass","pwgen"],"x":"echo \"[password]\" | pwdsafety"},"qwen-code":{"n":"qwen-code","t":"AI-powered agentic coding assistant from Alibaba","s":[["TAGLINE","AI-powered agentic coding assistant from Alibaba"],["TLDR","**Start an interactive session**\n\n```qwen```\n\n**Run in headless mode with a prompt**\n\n```qwen -p \"[explain this codebase]\"```\n\n**Reference a local file in your query**\n\n```qwen``` then ```@[src/main.ts] [explain this file]```\n\n**Configure authentication**\n\n```qwen``` then ```/auth```\n\n**Show session statistics**\n\n```qwen``` then ```/stats```\n\n**Compress conversation history to save tokens**\n\n```qwen``` then ```/compress```"],["SYNOPSIS","**qwen** [_options_]"],["PARAMETERS","**-p** _PROMPT_\n> Run in headless mode with the provided prompt"],["IN-SESSION COMMANDS","**/help**\n> Display available commands\n\n**/auth**\n> Configure authentication method\n\n**/clear**\n> Clear conversation history\n\n**/compress**\n> Compress history to save tokens\n\n**/stats**\n> Show current session information\n\n**/bug**\n> Submit a bug report\n\n**/exit**, **/quit**\n> Exit the application"],["KEYBOARD SHORTCUTS","**Ctrl+C**\n> Cancel current operation\n\n**Ctrl+D**\n> Exit on empty line\n\n**Up/Down**\n> Navigate command history\n\n**@**_filepath_\n> Reference local files in queries"],["DESCRIPTION","**Qwen Code** is an open-source AI coding agent for the terminal from Alibaba, optimized for the Qwen3-Coder model. It helps developers understand large codebases, automate tedious work, and ship faster with agentic coding capabilities.\n\nBuilt in TypeScript and forked from Gemini CLI, it offers a similar experience but is open to any model supporting the OpenAI completions API. Features include Skills, SubAgents, and Plan Mode for full agentic workflows. The tool supports deep code understanding beyond normal context windows and workflow automation for PR handling, rebases, and formatting.\n\nAuthentication options include Qwen OAuth (2,000 free requests/day) or OpenAI-compatible APIs. IDE integrations are available for VS Code, Zed, and JetBrains."],["CAVEATS","Requires authentication via Qwen OAuth or OpenAI-compatible API keys. Environment variables **OPENAI_API_KEY**, **OPENAI_BASE_URL**, and **OPENAI_MODEL** can be set for custom providers. Version 0.5.0 supports running four concurrent instances in a single terminal."],["HISTORY","Qwen Code was launched by **Alibaba** in **July 2025** alongside the Qwen3-Coder model. The flagship model is Qwen3-Coder-480B-A35B-Instruct, a 480B-parameter Mixture-of-Experts model with 35B active parameters supporting 256K native context and 1M tokens with extrapolation. The tool is released as open-source on GitHub."],["INSTALL","```pacman: sudo pacman -S qwen-code```\n\n```brew: brew install qwen-code```\n\n```nix: nix profile install nixpkgs#qwen-code```"],["SEE ALSO","[gemini](/man/gemini)(1), [claude](/man/claude)(1), [opencode](/man/opencode)(1), [aider](/man/aider)(1)"]],"e":["gemini","claude","opencode","aider"],"x":"qwen"},"rabbitmq-upgrade":{"n":"rabbitmq-upgrade","t":"Check and prepare RabbitMQ upgrades","s":[["TAGLINE","Check and prepare RabbitMQ upgrades"],["TLDR","**Check upgrade readiness**\n\n```rabbitmq-upgrade await_online_quorum_plus_one```\n\n**Drain node for upgrade**\n\n```rabbitmq-upgrade drain```\n\n**Revive node after upgrade**\n\n```rabbitmq-upgrade revive```"],["SYNOPSIS","**rabbitmq-upgrade** _command_ [_options_]"],["PARAMETERS","**await_online_quorum_plus_one**\n> Wait for sufficient online replicas.\n\n**drain**\n> Prepare node for shutdown/upgrade.\n\n**revive**\n> Restore node after upgrade.\n\n**post_upgrade**\n> Run post-upgrade tasks.\n\n**-n** _node_\n> Target node."],["DESCRIPTION","**rabbitmq-upgrade** assists with RabbitMQ cluster upgrades. Ensures safe rolling upgrades by managing node state and checking quorum health. Part of RabbitMQ CLI tools."],["INSTALL","```dnf: sudo dnf install rabbitmq-server```\n\n```apk: sudo apk add rabbitmq-server```\n\n```zypper: sudo zypper install rabbitmq-server```\n\n```nix: nix profile install nixpkgs#rabbitmq-server```"],["SEE ALSO","[rabbitmqctl](/man/rabbitmqctl)(1), [rabbitmq-queues](/man/rabbitmq-queues)(1)"]],"e":["rabbitmqctl","rabbitmq-queues"],"x":"rabbitmq-upgrade await_online_quorum_plus_one"},"rabbitmqctl":{"n":"rabbitmqctl","t":"Command-line tool for managing RabbitMQ","s":[["TAGLINE","Command-line tool for managing RabbitMQ"],["TLDR","**List queues**\n\n```rabbitmqctl list_queues```\n\n**List queues with details**\n\n```rabbitmqctl list_queues name messages consumers```\n\n**List exchanges**\n\n```rabbitmqctl list_exchanges```\n\n**List connections**\n\n```rabbitmqctl list_connections```\n\n**Add user**\n\n```rabbitmqctl add_user [username] [password]```\n\n**Set user permissions**\n\n```rabbitmqctl set_permissions [username] \".*\" \".*\" \".*\"```\n\n**Delete queue**\n\n```rabbitmqctl delete_queue [queue_name]```\n\n**Cluster status**\n\n```rabbitmqctl cluster_status```"],["SYNOPSIS","**rabbitmqctl** [_-n node_] [_-t timeout_] _command_ [_args_]"],["PARAMETERS","**list_queues** [_COLUMNS_]\n> List queues with selected info.\n\n**list_exchanges** [_COLUMNS_]\n> List exchanges.\n\n**list_bindings**\n> List bindings.\n\n**list_connections** [_COLUMNS_]\n> List connections.\n\n**list_channels** [_COLUMNS_]\n> List channels.\n\n**list_users**\n> List users.\n\n**add_user** _USER_ _PASS_\n> Add user.\n\n**delete_user** _USER_\n> Delete user.\n\n**set_permissions** _USER_ _CONF_ _WRITE_ _READ_\n> Set permissions.\n\n**delete_queue** _QUEUE_\n> Delete queue.\n\n**purge_queue** _QUEUE_\n> Empty queue.\n\n**cluster_status**\n> Show cluster status.\n\n**-n** _NODE_\n> Target node.\n\n**-t** _TIMEOUT_\n> Command timeout."],["DESCRIPTION","**rabbitmqctl** manages RabbitMQ message broker. It controls server operations, user management, and provides monitoring information.\n\nQueue listings show message counts, consumer connections, and memory usage. Column selection customizes output for monitoring scripts.\n\nUser management handles authentication. Permission patterns (configure, write, read) control access to virtual hosts and resources.\n\nCluster commands manage distributed deployments. Status shows node membership and synchronization state.\n\nThe tool connects to the Erlang distribution for control. Node names identify targets in clustered environments.\n\nMonitoring commands support integration with external systems. JSON output enables automated processing."],["CAVEATS","Requires access to Erlang cookie. Some commands need administrator privileges. Cluster operations need careful coordination."],["HISTORY","**rabbitmqctl** has been RabbitMQ's management CLI since the broker's release by **Rabbit Technologies** (acquired by VMware, now part of Broadcom) starting around **2007**. It remains the primary administrative interface."],["INSTALL","```dnf: sudo dnf install rabbitmq-server```\n\n```apk: sudo apk add rabbitmq-server```\n\n```zypper: sudo zypper install rabbitmq-server```\n\n```nix: nix profile install nixpkgs#rabbitmq-server```"],["SEE ALSO","[rabbitmq-server](/man/rabbitmq-server)(8), [rabbitmq-plugins](/man/rabbitmq-plugins)(8), [rabbitmq-diagnostics](/man/rabbitmq-diagnostics)(8)"]],"e":["rabbitmq-server","rabbitmq-plugins","rabbitmq-diagnostics"],"x":"rabbitmqctl list_queues"},"rgrep":{"n":"rgrep","t":"Recursive grep through directories","s":[["TAGLINE","Recursive grep through directories"],["TLDR","**Search recursively**\n\n```rgrep \"[pattern]\" [path]```\n\n**Case insensitive**\n\n```rgrep -i \"[pattern]\" [path]```\n\n**Show line numbers**\n\n```rgrep -n \"[pattern]\" [path]```\n\n**List matching files only**\n\n```rgrep -l \"[pattern]\" [path]```\n\n**Count matches**\n\n```rgrep -c \"[pattern]\" [path]```\n\n**Exclude directory**\n\n```rgrep --exclude-dir=[node_modules] \"[pattern]\" [path]```"],["SYNOPSIS","**rgrep** [_-i_] [_-n_] [_-l_] [_options_] _pattern_ [_path_]"],["PARAMETERS","**-i**\n> Case insensitive search.\n\n**-n**\n> Show line numbers.\n\n**-l**\n> Print only names of files with matches.\n\n**-c**\n> Print only a count of matching lines per file.\n\n**-v**\n> Invert match (select non-matching lines).\n\n**-w**\n> Match whole words only.\n\n**-E**\n> Use extended regular expressions (same as egrep).\n\n**-P**\n> Use Perl-compatible regular expressions.\n\n**--color** _WHEN_\n> Colorize matches: auto, always, or never.\n\n**--exclude-dir** _DIR_\n> Skip directory.\n\n**--include** _GLOB_\n> Search only files matching pattern.\n\n**--exclude** _GLOB_\n> Skip files matching pattern."],["DESCRIPTION","**rgrep** is a convenience wrapper equivalent to **grep -r**, providing recursive text searching through directory trees. It traverses all subdirectories from the specified path, searching file contents for the given pattern using standard grep regular expression syntax.\n\nAll standard grep options work with rgrep, including case-insensitive search (**-i**), line numbers (**-n**), listing matching files only (**-l**), and inverted matching (**-v**). The **--exclude-dir** and **--include** flags allow filtering which files and directories are searched, which is important for skipping large directories like node_modules or build output.\n\nOn most GNU/Linux systems, rgrep is installed by default as part of the GNU grep package. For better performance on large codebases, consider using ripgrep (**rg**) which is significantly faster and automatically respects .gitignore rules."],["CAVEATS","By default follows symlinks on the command line. May be slow on large trees. Deprecated in GNU grep in favor of **grep -r**, but still provided for backward compatibility. Consider ripgrep (**rg**) for better performance on large codebases."],["INSTALL","```apt: sudo apt install grep```\n\n```dnf: sudo dnf install grep```\n\n```pacman: sudo pacman -S grep```\n\n```zypper: sudo zypper install grep```\n\n```brew: brew install grep```"],["SEE ALSO","[grep](/man/grep)(1), [egrep](/man/egrep)(1), [fgrep](/man/fgrep)(1), [rg](/man/rg)(1)"]],"e":["grep","egrep","fgrep","rg"],"x":"rgrep \"[pattern]\" [path]"},"riff":{"n":"riff","t":"Syntax highlighter for diff output","s":[["TAGLINE","Syntax highlighter for diff output"],["TLDR","**Highlight changed portions of a diff**\n\n```git diff | riff```\n\n**Set riff as the default git diff pager**\n\n```git config --global pager.diff riff```\n\n**Highlight differences between two files**\n\n```diff [file1.txt] [file2.txt] | riff```"],["SYNOPSIS","_diff-command_ | **riff** [_options_]"],["DESCRIPTION","**riff** is a wrapper around diff that highlights which parts of lines have changed, showing unchanged portions in a distinct color. It hyperlinks file names and hunk headers to source code lines and also highlights merge conflicts. It integrates with git as a pager for enhanced diff viewing."],["HISTORY","**riff** was created by **Johan Walles** (walles) and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S riff```\n\n```apk: sudo apk add riff```\n\n```zypper: sudo zypper install riff```\n\n```brew: brew install riff```\n\n```nix: nix profile install nixpkgs#riff```"],["SEE ALSO","[delta](/man/delta)(1), [diff-so-fancy](/man/diff-so-fancy)(1), [colordiff](/man/colordiff)(1)"]],"e":["delta","diff-so-fancy","colordiff"],"x":"git diff | riff"},"ropgadget":{"n":"ropgadget","t":"Find ROP gadgets in binary executables","s":[["TAGLINE","Find ROP gadgets in binary executables"],["TLDR","**Find gadgets in binary**\n\n```ROPgadget --binary [binary]```\n\n**Search for specific gadget**\n\n```ROPgadget --binary [binary] --string \"[/bin/sh]\"```\n\n**Filter gadgets by regex**\n\n```ROPgadget --binary [binary] --re \"[pop.*ret]\"```\n\n**Filter out gadgets with bad bytes in address**\n\n```ROPgadget --binary [binary] --badbytes \"[0a|0d|00]\"```\n\n**Limit gadget depth**\n\n```ROPgadget --binary [binary] --depth [5]```\n\n**Generate ropchain**\n\n```ROPgadget --binary [binary] --ropchain```\n\n**Output to file**\n\n```ROPgadget --binary [binary] > [gadgets.txt]```"],["SYNOPSIS","**ROPgadget** [_--binary file_] [_--string str_] [_--re pattern_] [_options_]"],["PARAMETERS","**--binary** _FILE_\n> Target binary.\n\n**--string** _STR_\n> Find string in binary.\n\n**--re** _REGEX_\n> Filter by regex.\n\n**--ropchain**\n> Generate ROP chain.\n\n**--depth** _N_\n> Gadget instruction depth.\n\n**--badbytes** _BYTES_\n> Reject gadgets containing specific bytes in their address (e.g., \"0a|0d|00\").\n\n**--opcode** _OPCODES_\n> Search for specific opcodes in executable segments.\n\n**--offset** _OFFSET_\n> Add offset to gadget addresses.\n\n**--nojop**\n> Exclude JOP (Jump-Oriented Programming) gadgets.\n\n**--nosys**\n> Exclude syscall gadgets.\n\n**--multibr**\n> Enable multiple branch gadgets.\n\n**--all**\n> Search all sections, not just executable ones."],["DESCRIPTION","**ROPgadget** finds ROP (Return-Oriented Programming) gadgets in binaries. It's used for exploit development and security research.\n\nGadgets are small instruction sequences ending in returns. They're chained to build exploits.\n\nString searching finds useful data in binaries. Shell paths and other strings located.\n\nAutomatic ropchain generation builds exploit templates. Creates execve chains for common scenarios.\n\nDepth limits instruction sequence length. Shorter gadgets are generally more useful."],["CAVEATS","For authorized security research only. Understanding assembly required. Results need manual analysis."],["HISTORY","**ROPgadget** was created by **Jonathan Salwan** for ROP exploit development. It's widely used in CTF competitions and security research."],["INSTALL","```pacman: sudo pacman -S ropgadget```\n\n```nix: nix profile install nixpkgs#ropgadget```"],["SEE ALSO","[ropper](/man/ropper)(1), [objdump](/man/objdump)(1), [pwntools](/man/pwntools)(1), [radare2](/man/radare2)(1)"]],"e":["ropper","objdump","pwntools","radare2"],"x":"ROPgadget --binary [binary]"},"sdk":{"n":"sdk","t":"Manage parallel SDK versions for JVM languages","s":[["TAGLINE","Manage parallel SDK versions for JVM languages"],["TLDR","**List all available candidates** (Java, Scala, Kotlin, etc.)\n\n```sdk list```\n\n**List available versions** of a candidate\n\n```sdk list java```\n\n**Install the latest stable version** of a candidate\n\n```sdk install java```\n\n**Install a specific version**\n\n```sdk install java [21.0.4-tem]```\n\n**Switch to a version** for the current shell session\n\n```sdk use java [17.0.12-tem]```\n\n**Set a version as default** for all shells\n\n```sdk default java [21.0.4-tem]```\n\n**Show currently active versions**\n\n```sdk current```\n\n**Upgrade outdated candidates**\n\n```sdk upgrade```"],["SYNOPSIS","**sdk** _command_ [_candidate_] [_version_]"],["COMMANDS","**install** _candidate_ [_version_]\n> Install a candidate version; latest stable if version omitted\n\n**uninstall** _candidate_ _version_\n> Remove an installed version\n\n**list** [_candidate_]\n> List available candidates or versions of a specific candidate\n\n**use** _candidate_ _version_\n> Switch version for current terminal session only\n\n**default** _candidate_ _version_\n> Set version as default for all future shells\n\n**current** [_candidate_]\n> Show active version of a candidate or all candidates\n\n**upgrade** [_candidate_]\n> Upgrade outdated versions\n\n**version**\n> Display SDKMAN version information\n\n**home** _candidate_ _version_\n> Return absolute installation path for a version\n\n**env** [**init**|**install**|**clear**]\n> Manage project-specific .sdkmanrc configuration\n\n**offline** [**enable**|**disable**]\n> Toggle offline mode\n\n**selfupdate** [**force**]\n> Update SDKMAN itself\n\n**update**\n> Refresh candidate metadata cache\n\n**flush** [**tmp**|**broadcast**|**archives**|**version**]\n> Clear local cache and state\n\n**config**\n> Open SDKMAN configuration file"],["DESCRIPTION","**SDKMAN** (Software Development Kit Manager) is a tool for managing parallel versions of multiple SDKs on Unix-based systems. It simplifies installing, switching, and removing development kits for the JVM ecosystem.\n\nSupported candidates include **Java** (multiple distributions like Temurin, Zulu, GraalVM), **Scala**, **Kotlin**, **Groovy**, **Maven**, **Gradle**, **Ant**, **sbt**, **Spring Boot**, **Spark**, and many more. SDKMAN automatically manages **JAVA_HOME** and **PATH** environment variables.\n\nProject-specific versions can be configured using **.sdkmanrc** files. With **sdkman_auto_env=true** in the config, SDKMAN automatically switches versions when entering a project directory."],["CONFIGURATION","**~/.sdkman/etc/config**\n> SDKMAN configuration file controlling auto-answer, auto-env, color, and other settings.\n\n**.sdkmanrc**\n> Per-project file specifying required SDK versions. SDKMAN automatically switches versions when entering the directory if **sdkman_auto_env=true** is set.\n\n**SDKMAN_DIR**\n> Environment variable overriding the default SDKMAN installation directory (~/.sdkman)."],["CAVEATS","SDKMAN requires **curl**, **zip**, and **unzip** to be installed. It modifies shell configuration files during installation. The **use** command only affects the current shell; use **default** for persistent changes across sessions."],["HISTORY","SDKMAN was originally created as **GVM** (Groovy enVironment Manager) by **Marco Vermeulen** around **2012**. It was later renamed to SDKMAN to reflect its broader support for multiple SDK candidates beyond Groovy. The tool draws inspiration from version managers like **RVM** (Ruby), **nvm** (Node.js), and **pyenv** (Python)."],["SEE ALSO","[java](/man/java)(1), [javac](/man/javac)(1), [gradle](/man/gradle)(1), [mvn](/man/mvn)(1), [sbt](/man/sbt)(1)"]],"e":["java","javac","gradle","mvn","sbt"],"x":"sdk list"},"tar":{"n":"tar","t":"Archive and compress files","s":[["TAGLINE","Archive and compress files"],["TLDR","**Create an archive** from files\n\n```tar -cvf [archive.tar] [file1] [file2]```\n\n**Create a gzip-compressed archive**\n\n```tar -czvf [archive.tar.gz] [directory/]```\n\n**Create a bzip2-compressed archive**\n\n```tar -cjvf [archive.tar.bz2] [directory/]```\n\n**Create an xz-compressed archive**\n\n```tar -cJvf [archive.tar.xz] [directory/]```\n\n**Extract an archive**\n\n```tar -xvf [archive.tar]```\n\n**Extract a compressed archive**\n\n```tar -xzvf [archive.tar.gz]```\n\n**Extract to a specific directory**\n\n```tar -xvf [archive.tar] -C [directory/]```\n\n**List contents** of an archive\n\n```tar -tvf [archive.tar]```\n\n**Extract specific files**\n\n```tar -xvf [archive.tar] [file1] [file2]```"],["SYNOPSIS","**tar** [_options_] [_archive_] [_file_...]"],["PARAMETERS","**-c**, **--create**\n> Create a new archive\n\n**-x**, **--extract**\n> Extract files from archive\n\n**-t**, **--list**\n> List archive contents\n\n**-v**, **--verbose**\n> Verbose output\n\n**-f** _file_, **--file**=_file_\n> Use specified archive file\n\n**-z**, **--gzip**\n> Filter through gzip\n\n**-j**, **--bzip2**\n> Filter through bzip2\n\n**-J**, **--xz**\n> Filter through xz\n\n**-a**, **--auto-compress**\n> Determine compression from file extension\n\n**-C** _dir_, **--directory**=_dir_\n> Change to directory before operation\n\n**-p**, **--preserve-permissions**\n> Preserve file permissions\n\n**--exclude**=_pattern_\n> Exclude files matching pattern\n\n**-r**, **--append**\n> Append files to archive\n\n**-u**, **--update**\n> Update archive with newer files\n\n**-k**, **--keep-old-files**\n> Don't replace existing files when extracting\n\n**--zstd**\n> Filter through zstd compression\n\n**-T** _FILE_, **--files-from**=_FILE_\n> Read list of files to extract or create from FILE\n\n**-X** _FILE_, **--exclude-from**=_FILE_\n> Exclude patterns listed in FILE\n\n**--strip-components**=_N_\n> Strip N leading path components\n\n**-h**, **--dereference**\n> Follow symbolic links; archive the files they point to\n\n**-O**, **--to-stdout**\n> Extract files to standard output"],["DESCRIPTION","**tar** (tape archive) creates, extracts, and manages archive files. It bundles multiple files and directories into a single file while preserving permissions, ownership, and directory structure.\n\nTar itself doesn't compress; compression is handled by external programs (gzip, bzip2, xz) invoked via options or automatically based on file extension with **-a**.\n\nCommon extensions: **.tar** (uncompressed), **.tar.gz** or **.tgz** (gzip), **.tar.bz2** (bzip2), **.tar.xz** (xz), **.tar.zst** (zstd). Modern tar auto-detects compression when extracting.\n\nThe order of options matters with short flags. **-cvf** works; **-fvc archive.tar** may not, as -f expects its argument immediately after."],["CAVEATS","By default, tar extracts with paths from the archive. Archives with absolute paths or **../** can overwrite files outside the target directory. Use **--strip-components** or extract to an empty directory first.\n\nExtracting as root preserves original ownership. As a regular user, files are owned by you regardless of archive metadata.\n\nGNU tar differs from BSD tar in some options. For portability, stick to common options or check the specific implementation."],["HISTORY","Tar originated in Version 7 Unix in **1979**, designed for tape backups (hence \"tape archive\"). It became the standard Unix archiving tool and is specified by POSIX. GNU tar added many extensions including compression integration."],["INSTALL","```apt: sudo apt install tar```\n\n```dnf: sudo dnf install tar```\n\n```pacman: sudo pacman -S tar```\n\n```apk: sudo apk add tar```\n\n```zypper: sudo zypper install tar```"],["SEE ALSO","[gzip](/man/gzip)(1), [bzip2](/man/bzip2)(1), [xz](/man/xz)(1), [zstd](/man/zstd)(1), [zip](/man/zip)(1), [cpio](/man/cpio)(1), [ar](/man/ar)(1)"]],"e":["gzip","bzip2","xz","zstd","zip","cpio","ar"],"x":"tar -cvf [archive.tar] [file1] [file2]"},"wine":{"n":"wine","t":"Run Windows applications on Unix systems","s":[["TAGLINE","Run Windows applications on Unix systems"],["TLDR","Run a specific program inside the **wine environment**\n\n```wine [command]```\n\nRun a program in **background**\n\n```wine start [command]```\n\n**Install** an MSI package\n\n```wine msiexec /i [path/to/package.msi]```\n\n**Uninstall** an MSI package\n\n```wine msiexec /x [path/to/package.msi]```\n\nRun **File Explorer**, **Notepad**, or **WordPad**\n\n```wine [explorer|notepad|write]```\n\nRun **Registry Editor**, **Control Panel**, or **Task Manager**\n\n```wine [regedit|control|taskmgr]```\n\nRun the **configuration** tool\n\n```wine winecfg```"],["SYNOPSIS","**wine** [_options_] _program_ [_arguments_]"],["PARAMETERS","**winecfg**\n> Open Wine configuration dialog\n\n**regedit**\n> Open Wine Registry Editor\n\n**control**\n> Open Wine Control Panel\n\n**explorer**\n> Open Wine File Explorer\n\n**taskmgr**\n> Open Wine Task Manager\n\n**msiexec** **/i** _package.msi_\n> Install MSI package\n\n**msiexec** **/x** _package.msi_\n> Uninstall MSI package\n\n**start** _program_\n> Run program in background (detached)\n\n**--version**\n> Display Wine version\n\n**WINEPREFIX=**_path_\n> Environment variable to specify Wine prefix directory\n\n**WINEDEBUG=**_channels_\n> Environment variable for debug output"],["DESCRIPTION","**wine** (Wine Is Not an Emulator) runs Windows applications on Unix-like operating systems by translating Windows API calls into POSIX calls in real-time. It implements the Windows runtime environment without requiring a Windows license or virtual machine.\n\nWine creates isolated \"prefixes\" (virtual C: drives) containing Windows directory structure, registry, and installed programs. Each prefix can be configured independently with different Windows versions and settings via winecfg."],["CAVEATS","Not all Windows software works; check the Wine Application Database (AppDB) for compatibility. Some applications require additional runtime libraries installed via **winetricks**. 32-bit support may need to be enabled separately on 64-bit systems."],["HISTORY","Development began in **1993** by Bob Amstadt and Eric Youngdale, making Wine one of the oldest continuous free software projects. The recursive backronym \"Wine Is Not an Emulator\" emphasizes that Wine reimplements Windows APIs rather than emulating hardware. Major milestones include Wine 1.0 in **2008** and version 9.0 in **2024**."],["INSTALL","```dnf: sudo dnf install wine```\n\n```pacman: sudo pacman -S wine```\n\n```apk: sudo apk add wine```\n\n```zypper: sudo zypper install wine```\n\n```nix: nix profile install nixpkgs#wine```"],["SEE ALSO","[winetricks](/man/winetricks)(1), [winecfg](/man/winecfg)(1)"]],"e":["winetricks","winecfg"],"x":"wine [command]"},"x11docker":{"n":"x11docker","t":"Run GUI Docker containers securely","s":[["TAGLINE","Run GUI Docker containers securely"],["TLDR","**Run GUI app**\n\n```x11docker [image] [command]```\n\n**With GPU acceleration**\n\n```x11docker --gpu [image] [command]```\n\n**Share home directory**\n\n```x11docker --home [image]```\n\n**With audio**\n\n```x11docker --pulseaudio [image]```\n\n**Clipboard sharing**\n\n```x11docker --clipboard [image]```"],["SYNOPSIS","**x11docker** [_--gpu_] [_--home_] [_--pulseaudio_] [_options_] _image_ [_command_]"],["PARAMETERS","**--gpu**\n> Enable hardware OpenGL acceleration.\n\n**--home**\n> Provide a persistent per-container home directory on the host.\n\n**--pulseaudio**\n> Enable PulseAudio sound support.\n\n**--alsa**\n> Enable ALSA sound support.\n\n**--pipewire**\n> Enable PipeWire sound support.\n\n**--clipboard**\n> Share clipboard between host and container.\n\n**--desktop**\n> Run a full desktop environment rather than a single application.\n\n**--xephyr**\n> Use the Xephyr nested X server (default fallback).\n\n**--xpra**\n> Use Xpra as the X server backend.\n\n**--nxagent**\n> Use nxagent as the X server backend.\n\n**--wayland**\n> Run in a Wayland compositor.\n\n**--hostdisplay**\n> Share the host X socket (less secure, fallback).\n\n**--share** _PATH_\n> Share a host file or folder with the container.\n\n**--network**, **-I**\n> Allow network access from the container.\n\n**--init** _SYSTEM_\n> Use an init system (e.g., systemd, runit, tini).\n\n**--backend** _BACKEND_\n> Choose container backend: docker, podman, or host.\n\n**--webcam**\n> Share webcams with the container.\n\n**--printer**\n> Share host CUPS printers.\n\n**--user** _USER_\n> Run the container as a specific user."],["DESCRIPTION","**x11docker** runs graphical applications from Docker containers with secure X11 display forwarding. It provides isolated display access using multiple backends including Xephyr, Xpra, and nxagent, preventing containers from accessing the host's X server directly.\n\nThe tool supports GPU hardware acceleration, PulseAudio integration, and clipboard sharing between the container and host. This security-focused approach allows running untrusted GUI applications in containers while maintaining display isolation from the host desktop environment."],["CAVEATS","Requires Docker or Podman. At least one nested X server backend (Xephyr, Xpra, nxagent, or Xwayland) must be installed on the host. Using **--hostdisplay** reduces isolation. GPU acceleration requires compatible drivers inside the container."],["HISTORY","**x11docker** was created by **Martin Viereck** to run Docker containers with GUI applications while preserving host security. It predates modern Wayland-native container workflows and remains a widely-used solution for desktop containerization."],["INSTALL","```dnf: sudo dnf install x11docker```\n\n```nix: nix profile install nixpkgs#x11docker```"],["SEE ALSO","[docker](/man/docker)(1), [xhost](/man/xhost)(1), [xephyr](/man/xephyr)(1)"]],"e":["docker","xhost","xephyr"],"x":"x11docker [image] [command]"},"xclip":{"n":"xclip","t":"X11 clipboard command-line interface","s":[["TAGLINE","X11 clipboard command-line interface"],["TLDR","Copy output to **primary selection**\n\n```echo 123 | xclip```\n\nCopy to **system clipboard**\n\n```echo 123 | xclip -selection clipboard```\n\nCopy **file contents** to clipboard\n\n```xclip -selection clipboard input.txt```\n\nCopy **image** to clipboard\n\n```xclip -selection clipboard -target image/png image.png```\n\nCopy from **user input**\n\n```xclip -i```\n\n**Paste** from primary selection\n\n```xclip -o```\n\n**Paste** from clipboard\n\n```xclip -o -selection clipboard```"],["SYNOPSIS","**xclip** [_-i_|_-o_] [_-selection sel_] [_-target type_] [_file_...]"],["DESCRIPTION","**xclip** is an X11 clipboard manipulation tool. It can read data from standard input or files and write it to any X selection (primary, secondary, or clipboard), or output selection contents to stdout."],["PARAMETERS","**-i, -in**\n> Read from stdin or files (default mode)\n\n**-o, -out**\n> Write selection to stdout\n\n**-se, -selection SELECTION**\n> Specify selection: primary, secondary, or clipboard\n\n**-t, -target TYPE**\n> Specify MIME target type (e.g., image/png, text/html)\n\n**-d, -display DISPLAY**\n> X display to connect to\n\n**-l, -loops N**\n> Number of paste requests to serve before exiting\n\n**-f, -filter**\n> Print input to stdout while copying\n\n**-q, -quiet**\n> Suppress informational output\n\n**-v, -verbose**\n> Print extra information\n\n**-silent**\n> Fork into background (used with -loops)"],["X11 SELECTIONS","**primary**: Middle-click paste (highlight to copy)\n**secondary**: Rarely used secondary selection\n**clipboard**: Ctrl+C/Ctrl+V clipboard"],["CAVEATS","Data is only available while xclip is running (unless using -loops or -silent). For persistent clipboard storage, use a clipboard manager. The default selection is primary, not clipboard."],["HISTORY","**xclip** was written by Kim Saunders as a command-line interface to X11 selections. It provides scriptable clipboard access without GUI dependencies."],["INSTALL","```apt: sudo apt install xclip```\n\n```dnf: sudo dnf install xclip```\n\n```pacman: sudo pacman -S xclip```\n\n```apk: sudo apk add xclip```\n\n```zypper: sudo zypper install xclip```\n\n```brew: brew install xclip```\n\n```nix: nix profile install nixpkgs#xclip```"],["SEE ALSO","[xsel](/man/xsel)(1), [wl-copy](/man/wl-copy)(1), [wl-paste](/man/wl-paste)(1)"]],"e":["xsel","wl-copy","wl-paste"],"x":"echo 123 | xclip"},"yadm-upgrade":{"n":"yadm-upgrade","t":"Migrate yadm configuration version","s":[["TAGLINE","Migrate yadm configuration version"],["TLDR","**Upgrade yadm configuration**\n\n```yadm upgrade```"],["SYNOPSIS","**yadm** **upgrade**"],["DESCRIPTION","**yadm upgrade** migrates an existing yadm installation to the layout used by the current version. In particular, it relocates the yadm repository and configuration from the legacy **~/.yadm** directory to the XDG-compliant locations under **$XDG_DATA_HOME/yadm** and **$XDG_CONFIG_HOME/yadm** (typically **~/.local/share/yadm** and **~/.config/yadm**).\n\nIt also normalizes encrypted file storage and other on-disk layouts that have changed across yadm releases. Run this once after upgrading yadm so subsequent commands find files in the expected places."],["CAVEATS","The upgrade is a one-time, mostly idempotent operation; running it on an already-upgraded repository is safe but performs no work. Back up your dotfiles repository before running it on systems with custom yadm hooks or unusual configurations."],["INSTALL","```pacman: sudo pacman -S yadm```\n\n```apk: sudo apk add yadm```\n\n```zypper: sudo zypper install yadm```\n\n```brew: brew install yadm```\n\n```nix: nix profile install nixpkgs#yadm```"],["SEE ALSO","[yadm](/man/yadm)(1), [git](/man/git)(1)"]],"e":["yadm","git"],"x":"yadm upgrade"},"yaourt":{"n":"yaourt","t":"Deprecated Arch Linux AUR helper","s":[["TAGLINE","Deprecated Arch Linux AUR helper"],["TLDR","**Update** all packages including AUR\n\n```yaourt -Syua```\n\n**Install** a package\n\n```yaourt -S [package]```\n\n**Remove** a package and its unneeded dependencies\n\n```yaourt -Rs [package]```\n\n**Search** for a package in repositories and AUR\n\n```yaourt -Ss [query]```\n\n**List** installed packages\n\n```yaourt -Q```\n\n**Download and build** a package from AUR without installing\n\n```yaourt -Sb [package]```\n\n**Get information** about a package\n\n```yaourt -Si [package]```"],["SYNOPSIS","**yaourt** [_OPTIONS_] [_PACKAGE..._]"],["PARAMETERS","**-S** _PACKAGE_\n> Install a package from repositories or AUR.\n\n**-Syua**\n> Synchronize databases and update all packages including AUR.\n\n**-Sb** _PACKAGE_\n> Download and build a package without installing.\n\n**-Rs** _PACKAGE_\n> Remove a package and its unneeded dependencies.\n\n**-Ss** _QUERY_\n> Search for a package in the sync databases and AUR.\n\n**-Si** _PACKAGE_\n> Display detailed information about a package.\n\n**-Q**\n> List installed packages.\n\n**-Qm**\n> List foreign (AUR-installed) packages.\n\n**--noconfirm**\n> Do not ask for any confirmation."],["DESCRIPTION","**yaourt** (Yet AnOther User Repository Tool) is an AUR helper for Arch Linux that wraps pacman with additional features for building packages from the Arch User Repository.\n\nIt combines official repository and AUR package management in a single interface with colorized output, interactive search/install, and orphan detection. Since the majority of yaourt's options are the same as pacman's, it can be used as a drop-in replacement."],["HISTORY","**yaourt** was one of the first popular AUR helpers but is now deprecated. Users are recommended to switch to yay, paru, or other maintained alternatives."],["CAVEATS","Deprecated and no longer maintained. Consider using yay or paru instead. AUR packages are not officially supported."],["SEE ALSO","[yay](/man/yay)(8), [paru](/man/paru)(8), [pacman](/man/pacman)(8)"]],"e":["yay","paru","pacman"],"x":"yaourt -Syua"},"zle":{"n":"zle","t":"Zsh line editor builtin","s":[["TAGLINE","Zsh line editor builtin"],["TLDR","**Define** a new widget from a shell function\n\n```zle -N [widget_name] [function_name]```\n\n**List** all user-defined widgets\n\n```zle -l```\n\n**List** all widgets including builtins\n\n```zle -la```\n\n**List** widgets as recreatable zle commands\n\n```zle -lL```\n\n**Delete** a user-defined widget\n\n```zle -D [widget_name]```\n\n**Create** a widget alias\n\n```zle -A [old_widget] [new_widget]```\n\n**Invoke** a widget from within a shell function\n\n```zle [widget_name]```\n\n**Display** a message below the command line\n\n```zle -M \"[message]\"```"],["SYNOPSIS","**zle** [_-lLaD_] [_widget ..._]\n**zle** **-N** _widget_ [_function_]\n**zle** **-C** _widget_ _completion-widget_ _function_\n**zle** **-R** [**-c**] [_display-string_] [_string ..._]\n**zle** **-M** _string_\n**zle** **-U** _string_\n**zle** **-K** _keymap_\n**zle** **-F** [**-Lw**] [_fd_ [_handler_]]\n**zle** _widget_ [**-n** _num_] [**-f** _flag_] [**-Nw**] [**-K** _keymap_] [**--** _args ..._]"],["PARAMETERS","**-N** _widget_ [_function_]\n> Define a new user-defined widget backed by a shell function. If function is omitted, defaults to the widget name\n\n**-C** _widget_ _completion-widget_ _function_\n> Create a user-defined completion widget inheriting behavior from an existing builtin completion widget\n\n**-l** [**-L** | **-a**]\n> List user-defined widgets. **-L**: format as zle commands. **-a**: include builtins\n\n**-D** _widget ..._\n> Delete named user-defined widgets\n\n**-A** _old-widget_ _new-widget_\n> Create an alias; both names refer to the same widget\n\n**-R** [**-c**] [_display-string_] [_string ..._]\n> Redisplay the command line. Optional display-string appears in status line. **-c** clears completion lists\n\n**-M** _string_\n> Display a message string below the command line that persists after the widget returns\n\n**-U** _string_\n> Push characters onto ZLE's input stack for processing after the current widget finishes\n\n**-K** _keymap_\n> Select the named keymap for subsequent keystrokes\n\n**-F** [**-Lw**] [_fd_ [_handler_]]\n> Install or remove a handler for a file descriptor. **-w**: handler is a widget. **-L**: list handlers\n\n**-I**\n> Invalidate the current display before external output\n\n**-f** _flag ..._\n> Set flags on the current widget: **yank**, **yankbefore**, **kill**, **vichange**, **nolast**"],["DESCRIPTION","**zle** is a builtin command of **zsh** that controls the Zsh Line Editor, the interactive command-line editing subsystem. It manages widgets (named editing actions), keymaps, display control, and the edit buffer. When called with no arguments, it returns zero if ZLE is currently active. Full documentation is in the **zshzle(1)** man page.\n\nZLE activates automatically in interactive zsh sessions. It operates in **multiline mode** (default) or **single-line mode** (when the **SINGLE_LINE_ZLE** option is set). Special parameters like **BUFFER**, **CURSOR**, **LBUFFER**, and **RBUFFER** allow widget functions to manipulate the edit buffer directly."],["KEYMAPS","ZLE provides eight built-in keymaps:\n\n**emacs** -- Emacs-style editing\n**viins** -- Vi insert mode\n**vicmd** -- Vi command mode\n**viopp** -- Vi operator-pending mode\n**visual** -- Vi visual selection mode\n**isearch** -- Incremental search mode\n**command** -- Command name reading mode\n**.safe** -- Immutable fallback keymap\n\nThe **main** keymap links to either **emacs** or **viins** depending on the **EDITOR** environment variable, or can be set with **bindkey -e** (emacs) or **bindkey -v** (vi)."],["CONFIGURATION","ZLE is configured primarily through **.zshrc** using **bindkey** commands and **zle -N** widget definitions. The **KEYTIMEOUT** parameter (default 40, in hundredths of a second) controls how long ZLE waits for multi-character key sequences. Hook widgets like **zle-line-init**, **zle-line-finish**, and **zle-keymap-select** run automatically at specific events.\n\nExample custom widget that prepends sudo:\n\n```\nprepend-sudo() {\n  BUFFER=\"sudo $BUFFER\"\n  CURSOR+=5\n}\nzle -N prepend-sudo\nbindkey '^s' prepend-sudo\n```"],["SPECIAL PARAMETERS","When a user-defined widget function runs, these read/write parameters are available:\n\n**BUFFER** -- The entire edit buffer contents\n**CURSOR** -- Cursor position (index into BUFFER)\n**LBUFFER** -- Buffer contents left of the cursor\n**RBUFFER** -- Buffer contents right of the cursor\n**WIDGET** -- Name of the widget being executed\n**LASTWIDGET** -- Name of the last widget executed\n**KEYS** -- Keys typed to invoke this widget\n**NUMERIC** -- Numeric prefix argument, if any\n**KEYMAP** -- Currently selected keymap"],["CAVEATS","**zle** is only available inside **zsh** and is not a POSIX command or available in bash. Widgets can only be invoked when ZLE is active in interactive mode. Widget shell functions run with stdin redirected to **/dev/null**. Built-in widgets have a dot-prefixed variant (e.g., **.end-of-line**) that remains accessible even when the widget is overridden."],["HISTORY","ZLE is a fundamental component of zsh, the Z Shell, originally written by **Paul Falstad** in **1990** at Princeton University. It provides zsh with sophisticated line editing modeled after both Emacs and vi editing modes but is zsh's own independent implementation with a widget-based architecture, distinct from GNU Readline used by bash."],["SEE ALSO","[zsh](/man/zsh)(1), [bindkey](/man/bindkey)(1)"]],"e":["zsh","bindkey"],"x":"zle -N [widget_name] [function_name]"}});
