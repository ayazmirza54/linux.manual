window.LCL_BUCKET(105,{"7z":{"n":"7z","t":"high-compression file archiver supporting multiple formats","s":[["TAGLINE","high-compression file archiver supporting multiple formats"],["TLDR","**Create archive**\n\n```7z a [archive.7z] [files...]```\n\n**Extract archive**\n\n```7z x [archive.7z]```\n\n**Extract to specific directory**\n\n```7z x [archive.7z] -o[directory]```\n\n**List archive contents**\n\n```7z l [archive.7z]```\n\n**Test archive integrity**\n\n```7z t [archive.7z]```\n\n**Create with password**\n\n```7z a -p[password] [archive.7z] [files]```\n\n**Create with maximum compression**\n\n```7z a -mx=9 [archive.7z] [files]```"],["SYNOPSIS","**7z** _command_ [_switches_] _archive_ [_files_...]"],["COMMANDS","**a**\n> Add files to archive\n\n**x**\n> Extract with full paths\n\n**e**\n> Extract (flat, no paths)\n\n**l**\n> List archive contents\n\n**t**\n> Test archive integrity\n\n**d**\n> Delete from archive\n\n**u**\n> Update archive"],["PARAMETERS","**-o**_dir_\n> Output directory (no space after -o)\n\n**-p**_password_\n> Set password\n\n**-mx=**_n_\n> Compression level (0=store, 9=ultra)\n\n**-t**_type_\n> Archive type (7z, zip, tar, gzip, bzip2)\n\n**-mhe=on**\n> Encrypt filenames (7z format)\n\n**-r**\n> Recurse subdirectories\n\n**-y**\n> Assume yes to all queries\n\n**-v**_size_\n> Create volumes (e.g., -v100m)"],["DESCRIPTION","**7z** is the command-line interface to the 7-Zip archiver, supporting many formats: 7z, ZIP, TAR, GZIP, BZIP2, XZ, and more.\n\nThe 7z format offers high compression ratios using LZMA/LZMA2 algorithms, often 30-70% better than ZIP. It also supports AES-256 encryption and multi-volume archives.\n\nExtraction supports many additional formats including RAR, CAB, ISO, and others (read-only for some formats)."],["CAVEATS","The 7z format is not as universally supported as ZIP. Consider ZIP for maximum compatibility.\n\n**-o** has no space: **-o/path** not **-o /path**.\n\nRAR creation requires separate RAR tools. 7z can only extract RAR files.\n\nSplit archives (**-v**) require all volumes for extraction."],["INSTALL","```apt: sudo apt install 7zip```\n\n```dnf: sudo dnf install 7zip```\n\n```pacman: sudo pacman -S 7zip```\n\n```apk: sudo apk add 7zip```\n\n```zypper: sudo zypper install 7zip```\n\n```brew: brew install 7zip```"],["SEE ALSO","[7za](/man/7za)(1), [7zr](/man/7zr)(1), [tar](/man/tar)(1), [gzip](/man/gzip)(1), [zip](/man/zip)(1)"],["RESOURCES","```[Source code](https://github.com/ip7z/7zip)```\n\n```[Homepage](https://www.7-zip.org/)```"]],"e":["7za","7zr","tar","gzip","zip"],"x":"7z a [archive.7z] [files...]"},"anthropic":{"n":"anthropic","t":"Interact with Claude AI from the command line","s":[["TAGLINE","Interact with Claude AI from the command line"],["TLDR","**Send a simple message** to Claude\n\n```anthropic -g user \"Hello, Claude!\"```\n\n**Send a message** with a specific model\n\n```anthropic -g user \"[prompt]\" -m [claude-opus-4-8]```\n\n**Analyze an image** with Claude\n\n```anthropic -g user \"Describe this image\" -i [path/to/image.png]```\n\n**Set a system prompt** for context\n\n```anthropic -g user \"[prompt]\" -s \"You are a helpful coding assistant\"```\n\n**Multi-turn conversation** with assistant response\n\n```anthropic -g user \"Hello!\" -g assistant \"Hi there!\" -g user \"How are you?\"```\n\n**Control response length** and randomness\n\n```anthropic -g user \"[prompt]\" -x [2048] -t [0.7]```"],["SYNOPSIS","**anthropic** [**-g** _role_ _content_] [**-m** _model_] [**-s** _system_] [**-i** _image_] [**-t** _temp_] [**-x** _tokens_]"],["PARAMETERS","**-g**, **--message** _role_ _content_\n> Add a message with the specified role (user/assistant) and content. Can be repeated for multi-turn conversations.\n\n**-m**, **--model** _model_\n> Specify the Claude model to use. The built-in default is claude-3-opus-20240229, which has since been retired, so pass a current model such as claude-opus-4-8 or claude-sonnet-4-6.\n\n**-s**, **--system** _prompt_\n> System message providing context or instructions for the conversation.\n\n**-i**, **--image** _path_\n> Path to an image file (PNG, JPEG, or PDF) for vision tasks.\n\n**-t**, **--temperature** _value_\n> Controls response randomness. Higher values produce more creative outputs.\n\n**-k**, **--top_k** _value_\n> Restricts sampling to the top k most likely tokens.\n\n**-p**, **--top_p** _value_\n> Nucleus sampling; restricts to tokens with cumulative probability above threshold.\n\n**-x**, **--max_tokens** _count_\n> Maximum number of tokens in the response. Default: 1024."],["DESCRIPTION","**anthropic** is a command-line interface for interacting with Anthropic's Claude AI models. It provides direct access to the Claude API from the terminal, allowing users to send text messages and images for analysis.\n\nThe tool supports multi-turn conversations by chaining multiple **-g** flags, enabling context-aware interactions. It handles various image formats including PNG, JPEG, and PDF files for vision tasks. The CLI integrates well with shell pipelines, making it useful for scripting and automation workflows."],["CAVEATS","Requires an **ANTHROPIC_API_KEY** environment variable to be set with valid API credentials. PDF processing requires the **Poppler** library (used by **pdf2image**) to be installed on the system; it is not needed if you never pass PDFs. The hardcoded default model (claude-3-opus-20240229) has been retired, so supply a current model with **-m**. API usage incurs costs based on token consumption."],["HISTORY","The anthropic-cli tool is a community project by **dvcrn** that provides command-line access to Anthropic's Claude models. It wraps the official Anthropic Python SDK and was created for developers who prefer terminal-based workflows over web interfaces. It is distributed on PyPI as **anthropic-cli** and is unrelated to Anthropic's own **claude** (Claude Code) and **ant** command-line tools."],["SEE ALSO","[claude](/man/claude)(1), [ollama](/man/ollama)(1), [llm](/man/llm)(1)"],["RESOURCES","```[Source code](https://github.com/dvcrn/anthropic-cli)```"]],"e":["claude","ollama","llm"],"x":"anthropic -g user \"Hello, Claude!\""},"aseqnet":{"n":"aseqnet","t":"ALSA MIDI sequencer over network","s":[["TAGLINE","ALSA MIDI sequencer over network"],["TLDR","**Start** MIDI network server\n\n```aseqnet```\n\nConnect to **remote server**\n\n```aseqnet [hostname]```\n\nSpecify **port**\n\n```aseqnet -p [40002] [hostname]```\n\nAuto-connect **read** port on startup\n\n```aseqnet -s [client:port] [hostname]```\n\nAuto-connect **write** port on startup\n\n```aseqnet -d [client:port] [hostname]```"],["SYNOPSIS","**aseqnet** [_options_] [_hostname_]"],["DESCRIPTION","**aseqnet** transmits ALSA sequencer events over a network connection. It enables MIDI communication between machines, allowing remote MIDI control or distributed music setups.\n\nRunning without arguments starts a server; specifying a hostname connects as client. Ports created by aseqnet can be connected to other sequencer ports via [aconnect](/man/aconnect)(1)."],["PARAMETERS","**hostname**\n> Connect to this server (client mode)\n\n**-p** _port_\n> TCP port number or service name (default: 40002)\n\n**-s** _addr_\n> Subscribe to the given address for read automatically (client:port)\n\n**-d** _addr_\n> Subscribe to the given address for write automatically (client:port)\n\n**-n** _name_\n> Specify the MIDI name of the process\n\n**-v**\n> Verbose output"],["CAVEATS","No encryption; use SSH tunneling for security. Network latency affects MIDI timing."],["HISTORY","**aseqnet** is part of the ALSA utilities, enabling network MIDI functionality for distributed audio setups."],["INSTALL","```apt: sudo apt install alsa-utils```\n\n```dnf: sudo dnf install alsa-utils```\n\n```pacman: sudo pacman -S alsa-utils```\n\n```apk: sudo apk add alsa-utils```\n\n```zypper: sudo zypper install alsa-utils```\n\n```nix: nix profile install nixpkgs#alsa-utils```"],["SEE ALSO","[aconnect](/man/aconnect)(1), [aseqdump](/man/aseqdump)(1), [aplaymidi](/man/aplaymidi)(1)"]],"e":["aconnect","aseqdump","aplaymidi"],"x":"aseqnet"},"bjobs":{"n":"bjobs","t":"Display information about LSF batch jobs","s":[["TAGLINE","Display information about LSF batch jobs"],["TLDR","**Show all your pending and running jobs**\n\n```bjobs```\n\n**Show all jobs** including finished\n\n```bjobs -a```\n\n**Show detailed information** for a job\n\n```bjobs -l [job_id]```\n\n**Show jobs in a specific queue**\n\n```bjobs -q [queue_name]```\n\n**Show running jobs only**\n\n```bjobs -r```\n\n**Show pending jobs only**\n\n```bjobs -p```\n\n**Show jobs with wide output**\n\n```bjobs -w```\n\n**Show jobs for all users**\n\n```bjobs -u all```"],["SYNOPSIS","**bjobs** [_options_] [_job_id_...]"],["DESCRIPTION","**bjobs** displays information about LSF (Load Sharing Facility) batch jobs. By default, it shows your pending and running jobs with their status, queue, submission host, and execution host.\n\nJob states include PEND (waiting), RUN (executing), DONE (completed successfully), EXIT (failed), PSUSP (pending suspended), USUSP (user suspended), and SSUSP (system suspended).\n\nbjobs is part of IBM Spectrum LSF, a workload management platform for scheduling and managing batch jobs in HPC environments and large compute clusters."],["PARAMETERS","**-a**\n> Show all jobs including recently finished.\n\n**-d**\n> Show recently finished jobs.\n\n**-l**\n> Long format with detailed information.\n\n**-p**\n> Show pending jobs only.\n\n**-r**\n> Show running jobs only.\n\n**-s**\n> Show suspended jobs only.\n\n**-w**\n> Wide format, don't truncate fields.\n\n**-W**\n> Show estimated start time for pending jobs.\n\n**-q** _queue_\n> Show jobs from specific queue.\n\n**-u** _user_\n> Show jobs for specific user (or \"all\").\n\n**-m** _host_\n> Show jobs on specific execution host.\n\n**-J** _name_\n> Show jobs with matching job name.\n\n**-g** _group_\n> Show jobs in specific job group.\n\n**-o** _format_\n> Custom output format.\n\n**-noheader**\n> Suppress header line."],["CAVEATS","Recently finished jobs disappear from bjobs after a configurable time (typically 1 hour). Use bhist for historical job information. Job IDs may be reused after some time. Wide format may still truncate on very long fields."],["HISTORY","**bjobs** is part of **IBM Spectrum LSF**, originally developed by **Platform Computing** in the **early 1990s**. LSF (Load Sharing Facility) pioneered workload management for distributed computing. Platform Computing was acquired by IBM in **2012**. The bjobs interface has remained largely stable over decades, maintaining backward compatibility while adding new features."],["SEE ALSO","[bsub](/man/bsub)(1), [bkill](/man/bkill)(1), [bhist](/man/bhist)(1)"]],"e":["bsub","bkill","bhist"],"x":"bjobs"},"boxes":{"n":"boxes","t":"Draw ASCII art boxes around text.","s":[["TAGLINE","Draw ASCII art boxes around text."],["TLDR","**Create** box around text\n\n```echo [\"Hello World\"] | boxes```\n\n**Specific** design\n\n```echo [\"Text\"] | boxes -d [stone]```\n\n**List** available designs\n\n```boxes -l```\n\n**Remove** box\n\n```cat [boxed.txt] | boxes -r```"],["SYNOPSIS","**boxes** [_options_] [_file_]"],["DESCRIPTION","**boxes** is a text filter that draws ASCII art boxes around text. It supports numerous box designs and can both add and remove boxes, making it useful for creating eye-catching comments, headers, or decorative text in source code and documentation.\n\nThe tool provides over 50 predefined box styles with customization options."],["PARAMETERS","**-d** _design_\n> Box design to use.\n\n**-l**\n> List available designs with samples.\n\n**-r**\n> Remove an existing box (design auto-detected; combine with -d to hint).\n\n**-a** _format_\n> Alignment/positioning of text inside box (e.g., `hcvc` for horizontally and vertically centered).\n\n**-s** _WxH_\n> Desired box size in columns (width) and lines (height).\n\n**-p** _format_\n> Padding around input (e.g., `a1l3` = 1 space all around plus 3 extra on the left).\n\n**-f** _file_\n> Use alternate config file.\n\n**-i** _indent_\n> Indentation mode for input (`box`, `text`, or `none`).\n\n**-n** _encoding_\n> Input/output character encoding.\n\n**-t** _tabopts_\n> Tab handling options (expand/keep/unexpand).\n\n**-k** _bool_\n> Leading/trailing blank lines in input handling.\n\n**-e** _eol_\n> Force end-of-line style (`unix`, `dos`, `mac`).\n\n**-c** _string_\n> Shortcut to create a basic box with the given character(s) as border.\n\n**-h**\n> Print usage information.\n\n**-v**\n> Print version information."],["POPULAR DESIGNS","- **stone** - Stone wall style\n- **dog** - ASCII dog\n- **cat** - ASCII cat\n- **parchment** - Scroll/parchment\n- **spring** - Spring/coil\n- **santa** - Santa Claus\n- **c-cmt** - C comment block\n- **java-cmt** - Java comment block"],["WORKFLOW","```bash"],["Simple box","echo \"Hello\" | boxes"],["Specific design","echo \"Warning\" | boxes -d stone"],["List all designs","boxes -l"],["Remove box","cat file.txt | boxes -r"],["Custom alignment","echo \"Centered\" | boxes -a c\n```"],["CAVEATS","Wide text may not fit terminal. Some designs require monospace font. Configuration file syntax complex for custom designs. Removing boxes not perfect for modified boxes."],["HISTORY","**boxes** was created by Thomas Jensen in **1999** to provide decorative ASCII boxes for text, inspired by similar DOS utilities."],["INSTALL","```apt: sudo apt install boxes```\n\n```dnf: sudo dnf install boxes```\n\n```brew: brew install boxes```\n\n```nix: nix profile install nixpkgs#boxes```"],["SEE ALSO","[figlet](/man/figlet)(6), [cowsay](/man/cowsay)(1), [banner](/man/banner)(6)"]],"e":["figlet","cowsay","banner"],"x":"echo [\"Hello World\"] | boxes"},"bully":{"n":"bully","t":"Brute-force WPS pins to recover WPA/WPA2 passphrases","s":[["TAGLINE","Brute-force WPS pins to recover WPA/WPA2 passphrases"],["TLDR","**Brute-force** the WPS pin of an access point\n\n```bully -b [mac] -c [channel] [interface]```\n\nDisplay **help**\n\n```bully -h```"],["SYNOPSIS","**bully** [_options_] _interface_"],["DESCRIPTION","**bully** is a WPS (Wi-Fi Protected Setup) brute-force attack tool that exploits vulnerabilities in the WPS protocol to recover the WPA/WPA2 passphrase. It attempts to crack the 8-digit WPS PIN through systematic guessing.\n\nBefore using bully, network information must be gathered using tools like **airmon-ng** and **airodump-ng** to identify the target's MAC address and channel."],["PARAMETERS","**-b, --bssid** _mac_\n> Target access point MAC address\n\n**-c, --channel** _channel_\n> Target wireless channel\n\n**-B, --bruteforce**\n> Enable brute-force mode\n\n**-h, --help**\n> Display help information"],["CAVEATS","Only use on networks you own or have explicit authorization to test. WPS attacks are detectable and may trigger lockouts. Many modern routers have WPS protections that limit attack effectiveness. Unauthorized use is illegal."],["INSTALL","```apt: sudo apt install bully```\n\n```pacman: sudo pacman -S bully```\n\n```nix: nix profile install nixpkgs#bully```"],["SEE ALSO","[reaver](/man/reaver)(1), [airmon-ng](/man/airmon-ng)(8), [airodump-ng](/man/airodump-ng)(8)"]],"e":["reaver","airmon-ng","airodump-ng"],"x":"bully -b [mac] -c [channel] [interface]"},"capsh":{"n":"capsh","t":"Explore and constrain Linux capabilities","s":[["TAGLINE","Explore and constrain Linux capabilities"],["TLDR","**Display current capability state**\n\n```capsh --print```\n\n**Show what a capability does**\n\n```capsh --explain=[cap_net_admin]```\n\n**Decode capability hex value**\n\n```capsh --decode=[0x20]```\n\n**Drop all capabilities and run shell**\n\n```capsh --drop=all -- -c \"[command]\"```\n\n**Run command with specific capabilities**\n\n```capsh --caps=\"[cap_net_raw+ep]\" -- -c \"[command]\"```\n\n**Check if capability is supported**\n\n```capsh --supports=[cap_sys_admin]```\n\n**Chroot and run command**\n\n```capsh --chroot=[/path] -- -c \"[command]\"```"],["SYNOPSIS","**capsh** [_options_]"],["DESCRIPTION","**capsh** is a capability shell wrapper for exploring and constraining Linux capability support. It provides testing, debugging, and environment creation for Linux process capabilities. Part of the libcap package."],["PARAMETERS","**--print**\n> Display prevailing capability and related state\n\n**--current**\n> Display capability state with 1e capabilities and IAB vector\n\n**--explain**=_cap_xxx_\n> Describe what privileges a capability provides\n\n**--decode**=_N_\n> Decode hex capability vector (as in /proc/1/status)\n\n**--supports**=_cap_xxx_\n> Verify capability exists on system\n\n**--drop**=_cap_xxx_\n> Remove capability from bounding set\n\n**--caps**=_cap-set_\n> Set capabilities to specified text value\n\n**--inh**=_cap-set_\n> Set inheritable capabilities\n\n**--chroot**=_path_\n> Change root directory (requires CAP_SYS_CHROOT)\n\n**--uid**=_id_\n> Set real and effective user ID\n\n**--gid**=_id_\n> Set real and effective group ID\n\n**--**\n> Execute /bin/bash with trailing arguments"],["EXIT STATUS","Returns 0 on success, 1 on error."],["CAVEATS","Many operations require specific capabilities to perform. Understanding Linux capabilities is essential for effective use."],["INSTALL","```apt: sudo apt install libcap2-bin```\n\n```apk: sudo apk add libcap-utils```"],["SEE ALSO","[getcap](/man/getcap)(8), [setcap](/man/setcap)(8), [capabilities](/man/capabilities)(7)"]],"e":["getcap","setcap","capabilities"],"x":"capsh --print"},"carthage":{"n":"carthage","t":"decentralized dependency manager for Cocoa projects","s":[["TAGLINE","decentralized dependency manager for Cocoa projects"],["TLDR","**Update and build dependencies**\n\n```carthage update```\n\n**Build XCFrameworks**\n\n```carthage update --use-xcframeworks```\n\n**Bootstrap without updating**\n\n```carthage bootstrap```\n\n**Build specific dependency**\n\n```carthage build [dependency_name]```\n\n**Update without building**\n\n```carthage update --no-build```\n\n**Build for specific platform**\n\n```carthage update --platform [iOS]```\n\n**Check outdated dependencies**\n\n```carthage outdated```"],["SYNOPSIS","**carthage** _command_ [_options_]"],["DESCRIPTION","**carthage** is a decentralized dependency manager for Swift and Objective-C Cocoa projects. Unlike CocoaPods, it builds dependencies as binary frameworks without modifying your Xcode project files, workspace, or build settings, leaving full control of the project structure to the developer.\n\nDependencies are declared in a **Cartfile** using a simple syntax that supports GitHub repositories, Git URLs, and binary-only frameworks. Running `carthage update` resolves versions, checks out sources, and builds frameworks into the Carthage/Build directory. A Cartfile.resolved lockfile ensures reproducible builds across team members.\n\nSince version 0.37.0, Carthage supports building XCFrameworks, which bundle binaries for multiple platforms and architectures into a single distributable package."],["COMMANDS","**update**\n> Update and rebuild dependencies\n\n**bootstrap**\n> Build dependencies without updating\n\n**build**\n> Build specific dependency\n\n**outdated**\n> Check for outdated dependencies\n\n**archive**\n> Create archive of built frameworks\n\n**copy-frameworks**\n> Copy frameworks to app bundle\n\n**version**\n> Show version"],["PARAMETERS","**--use-xcframeworks**\n> Build as XCFrameworks (recommended)\n\n**--platform** _name_\n> Build for specific platform (iOS, macOS, tvOS, watchOS)\n\n**--no-build**\n> Skip building after checkout\n\n**--no-use-binaries**\n> Build from source, don't use prebuilt binaries\n\n**--cache-builds**\n> Cache built frameworks\n\n**--project-directory** _path_\n> Directory containing Cartfile\n\n**--verbose**\n> Show detailed output"],["CARTFILE FORMAT","```\ngithub \"Alamofire/Alamofire\" ~> 5.0\ngithub \"realm/realm-swift\" == 10.0.0\ngit \"https://example.com/repo.git\" \"branch-name\"\n```"],["CONFIGURATION","**Cartfile**\n> Declares project dependencies with version constraints.\n\n**Cartfile.resolved**\n> Lockfile with exact resolved versions for reproducible builds.\n\n**Cartfile.private**\n> Dependencies needed only for development and testing."],["CAVEATS","Supports dynamic frameworks on iOS 8+. Static framework support since version 0.30.0. Consider Swift Package Manager for new projects. Frameworks go to Carthage/Build directory."],["INSTALL","```brew: brew install carthage```"],["SEE ALSO","[swift](/man/swift)(1), [xcodebuild](/man/xcodebuild)(1), [git](/man/git)(1)"]],"e":["swift","xcodebuild","git"],"x":"carthage update"},"cec-ctl":{"n":"cec-ctl","t":"kernel CEC device control tool","s":[["TAGLINE","kernel CEC device control tool"],["TLDR","**List** CEC devices\n\n```cec-ctl --list-devices```\n\n**Monitor** CEC traffic\n\n```sudo cec-ctl -m```\n\nShow **CEC topology**\n\n```cec-ctl -S```"],["SYNOPSIS","**cec-ctl** [_options_]"],["DESCRIPTION","**cec-ctl** controls kernel CEC (Consumer Electronics Control) devices through the Linux kernel's native CEC framework. It provides low-level access to the CEC subsystem for monitoring traffic, configuring adapters, and sending commands directly via the kernel driver.\n\nUnlike cec-client which relies on the userspace libCEC library and requires a USB CEC adapter, cec-ctl works with CEC hardware integrated into graphics cards and SoCs that expose `/dev/cecN` device nodes. This makes it the preferred tool on platforms with built-in CEC support, such as many ARM-based media players and modern Intel GPUs.\n\nThe monitor mode (`-m`) is particularly useful for debugging CEC communication issues, as it displays all CEC traffic on the bus in real time."],["PARAMETERS","**--list-devices**\n> List available CEC devices\n\n**-m, --monitor**\n> Monitor CEC traffic on the bus\n\n**-S, --show-topology**\n> Display the CEC device topology\n\n**-d** _device_\n> Use specific CEC device\n\n**-p** _addr_\n> Set physical address\n\n**-o** _addr_\n> Set logical address"],["CAVEATS","Requires kernel CEC support (CONFIG_CEC). Monitor mode requires root privileges. Not all HDMI ports support CEC."],["INSTALL","```apt: sudo apt install v4l-utils```\n\n```dnf: sudo dnf install v4l-utils```\n\n```pacman: sudo pacman -S v4l-utils```\n\n```apk: sudo apk add v4l-utils```\n\n```zypper: sudo zypper install v4l-utils```\n\n```nix: nix profile install nixpkgs#v4l-utils```"],["SEE ALSO","[cec-client](/man/cec-client)(1)"],["RESOURCES","```[Source code](https://git.linuxtv.org/v4l-utils.git)```"]],"e":["cec-client"],"x":"cec-ctl --list-devices"},"cmuwmtopbm":{"n":"cmuwmtopbm","t":"CMU window manager bitmap to PBM converter","s":[["TAGLINE","CMU window manager bitmap to PBM converter"],["TLDR","**Convert CMU window manager bitmap** to PBM\n\n```cmuwmtopbm [input.cmuwm] > [output.pbm]```\n\n**Convert from stdin**\n\n```cat [input.cmuwm] | cmuwmtopbm > [output.pbm]```"],["SYNOPSIS","**cmuwmtopbm** [_cmuwmfile_]"],["DESCRIPTION","**cmuwmtopbm** converts a CMU window manager bitmap file to PBM (Portable BitMap) format. This is part of the Netpbm package.\n\nCMU window manager (CMU WM) was an early X Window System window manager from Carnegie Mellon University. This tool converts its bitmap format to the portable Netpbm format."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmtocmuwm](/man/pbmtocmuwm)(1), [netpbm](/man/netpbm)(1)"],["RESOURCES","```[Homepage](https://netpbm.sourceforge.net/)```\n\n```[Documentation](https://netpbm.sourceforge.net/doc/cmuwmtopbm.html)```"]],"e":["pbmtocmuwm","netpbm"],"x":"cmuwmtopbm [input.cmuwm] > [output.pbm]"},"color":{"n":"color","t":"ANSI escape sequences for terminal text styling","s":[["TAGLINE","ANSI escape sequences for terminal text styling"],["TLDR","**Display ANSI colors in terminal**\n\n```for i in {0..255}; do printf \"\\e[38;5;${i}m%3d \" $i; done```\n\n**Set text color** (red)\n\n```echo -e \"\\e[31mRed text\\e[0m\"```\n\n**Set background color** (blue)\n\n```echo -e \"\\e[44mBlue background\\e[0m\"```\n\n**Bold text**\n\n```echo -e \"\\e[1mBold text\\e[0m\"```\n\n**Combine styles**\n\n```echo -e \"\\e[1;31;44mBold red on blue\\e[0m\"```\n\n**Reset all formatting**\n\n```echo -e \"\\e[0m\"```"],["SYNOPSIS","**echo -e** \"\\e[_code_m_text_\\e[0m\"\n\n**printf** \"\\033[_code_m_text_\\033[0m\""],["ANSI CODES","**Text Styles:**\n**0** - Reset all\n**1** - Bold\n**2** - Dim\n**3** - Italic\n**4** - Underline\n**5** - Blink\n**7** - Reverse\n**9** - Strikethrough\n\n**Foreground Colors (30-37):**\n**30** Black, **31** Red, **32** Green, **33** Yellow\n**34** Blue, **35** Magenta, **36** Cyan, **37** White\n\n**Background Colors (40-47):**\n**40** Black, **41** Red, **42** Green, **43** Yellow\n**44** Blue, **45** Magenta, **46** Cyan, **47** White\n\n**256 Colors:**\n**38;5;N** - Foreground color N (0-255)\n**48;5;N** - Background color N (0-255)\n\n**True Color (24-bit):**\n**38;2;R;G;B** - Foreground RGB\n**48;2;R;G;B** - Background RGB"],["DESCRIPTION","Terminal **color** is controlled through ANSI escape sequences, special character codes interpreted by terminal emulators to change text appearance. These sequences start with **ESC** (\\\\e, \\\\033, or \\\\x1b) followed by formatting codes.\n\nColors enhance readability of command output, differentiate log levels, highlight errors, and improve user interfaces in terminal applications. Most modern terminals support 256 colors and true color (16 million colors).\n\nThe **tput** command provides a portable way to generate escape sequences. Environment variables like **TERM** and **NO_COLOR** influence color support detection."],["TOOLS","**tput setaf N**: Set foreground color\n**tput setab N**: Set background color\n**tput bold**: Bold mode\n**tput sgr0**: Reset attributes"],["CAVEATS","Not all terminals support all features; 256-color and true color require modern terminals. The **NO_COLOR** environment variable convention requests programs disable color output. Piping colored output to files or other programs may include escape sequences. Test terminal support with **tput colors**."],["HISTORY","ANSI escape sequences were standardized in **ECMA-48** in **1976**, based on earlier work by **ANSI** (American National Standards Institute). The standard allowed portable control of video terminals. Support has been ubiquitous in Unix terminals since the **1980s** and was added to Windows 10 in **2016**. The **NO_COLOR** convention was proposed in **2017** to standardize disabling colors."],["SEE ALSO","[tput](/man/tput)(1), [dircolors](/man/dircolors)(1), [lolcat](/man/lolcat)(1)"]],"e":["tput","dircolors","lolcat"],"x":"for i in {0..255}; do printf \"\\e[38;5;${i}m%3d \" $i; done"},"crane-export":{"n":"crane-export","t":"export flattened image filesystem","s":[["TAGLINE","export flattened image filesystem"],["TLDR","**Export image filesystem** to tarball\n\n```crane export [image] [output.tar]```\n\n**Export to stdout**\n\n```crane export [image] - | tar -tvf -```"],["SYNOPSIS","**crane** **export** [_options_] _image_ _output_"],["DESCRIPTION","**crane export** exports the filesystem of a container image to a tarball. This flattens all layers into a single archive representing the final filesystem state.\n\nUse **-** as output to write to stdout."],["INSTALL","```pacman: sudo pacman -S crane```\n\n```apk: sudo apk add crane```\n\n```zypper: sudo zypper install crane```\n\n```brew: brew install crane```\n\n```nix: nix profile install nixpkgs#crane```"],["SEE ALSO","[crane](/man/crane)(1), [crane-pull](/man/crane-pull)(1)"],["RESOURCES","```[Source code](https://github.com/google/go-containerregistry)```\n\n```[Documentation](https://github.com/google/go-containerregistry/blob/main/cmd/crane/doc/crane_export.md)```"]],"e":["crane","crane-pull"],"x":"crane export [image] [output.tar]"},"crawlie":{"n":"crawlie","t":"Technical SEO and GEO crawler for the terminal","s":[["TAGLINE","Technical SEO and GEO crawler for the terminal"],["TLDR","**Crawl** an entire site and print a report\n\n```crawlie crawl [https://example.com] --format pretty```\n\n**Audit** a single page\n\n```crawlie audit [https://example.com/pricing]```\n\n**Save** an HTML report to a file\n\n```crawlie crawl [https://example.com] --format html -o [report.html]```\n\n**Limit** the number of pages fetched\n\n```crawlie crawl [https://example.com] --max-pages [100]```\n\n**Fail** a CI build when errors are found\n\n```crawlie crawl [https://example.com] --fail-on error```\n\n**Explain** why a finding matters\n\n```crawlie explain [geo-not-answerable]```"],["SYNOPSIS","**crawlie** _subcommand_ [_url_] [_options_]"],["DESCRIPTION","**crawlie** is a fast, open-source crawler that audits a website for technical SEO and GEO (Generative Engine Optimization) issues. It seeds from **sitemap.xml**, respects **robots.txt**, follows internal links, and applies a set of rules covering broken links, missing or duplicate metadata, performance hints, mobile and international optimization, and AI-search readiness.\n\nResults can be printed to the terminal or exported as JSON, CSV, or a shareable HTML report. The same engine is exposed as an MCP server (**crawlie-mcp**) for use inside agent workflows."],["SUBCOMMANDS","**crawl** _url_\n> Crawl an entire site, seeding from the sitemap and following internal links.\n\n**audit** _url_\n> Audit a single page or a specific set of pages.\n\n**explain** _finding_\n> Describe why a finding matters and how to fix it.\n\n**reports**\n> List the local report history.\n\n**report** _id_\n> Retrieve a specific saved report by its ID."],["PARAMETERS","**--format** _fmt_\n> Output format: **pretty** (terminal), **json**, **csv**, or **html** (default: json).\n\n**-o**, **--output** _file_\n> Write the report to a file instead of standard output.\n\n**--max-pages** _n_\n> Maximum number of pages to fetch (default: 500).\n\n**--max-depth** _n_\n> Maximum click depth from the seed URL.\n\n**--concurrency** _n_\n> Number of parallel requests (default: 16).\n\n**--include** _glob_\n> Only crawl URLs matching the pattern.\n\n**--exclude** _glob_\n> Skip URLs matching the pattern.\n\n**--no-robots**\n> Do not parse or honor robots.txt.\n\n**--no-sitemap**\n> Do not seed the crawl from sitemap.xml.\n\n**--no-external**\n> Skip checking external links.\n\n**--severity** _level_\n> Filter findings by severity: **error**, **warning**, or **notice**.\n\n**--save**\n> Store the result in the local report history.\n\n**--fail-on** _level_\n> Exit with a non-zero status when findings of **error** or **warning** severity are present (useful in CI)."],["HISTORY","**crawlie** is written in Rust (with a TypeScript desktop companion) and released under the MIT license. It was published in 2026 as a free, agent-friendly alternative to commercial SEO crawlers, adding GEO checks aimed at how AI search engines read a site."],["SEE ALSO","[wget](/man/wget)(1), [curl](/man/curl)(1), [httrack](/man/httrack)(1), [scrapy](/man/scrapy)(1)"],["RESOURCES","```[Source code](https://github.com/spronta/crawlie)```"]],"e":["wget","curl","httrack","scrapy"],"x":"crawlie crawl [https://example.com] --format pretty"},"dolt-merge":{"n":"dolt-merge","t":"join branch histories and table changes","s":[["TAGLINE","join branch histories and table changes"],["TLDR","**Merge branch into** current branch\n\n```dolt merge [branch_name]```\n\n**Merge with commit** message\n\n```dolt merge [branch_name] -m \"[Merge message]\"```\n\n**Abort in-progress merge**\n\n```dolt merge --abort```\n\n**Squash merge** into the working set, without a merge commit\n\n```dolt merge --squash [branch_name]```\n\n**Always create a merge commit**, even for a fast-forward\n\n```dolt merge --no-ff -m \"[Merge message]\" [branch_name]```\n\n**Refuse the merge** unless it fast-forwards\n\n```dolt merge --ff-only [branch_name]```\n\n**Inspect conflicts** left behind by a merge\n\n```dolt sql -q \"SELECT * FROM dolt_conflicts\"```"],["SYNOPSIS","**dolt merge** [**--squash**] _branch_\n\n**dolt merge** **--no-ff** [**-m** _message_] _branch_\n\n**dolt merge** **--ff-only** _branch_\n\n**dolt merge** **--abort**"],["PARAMETERS","**-m**, **--message** _MSG_\n> Use _MSG_ as the commit message for the merge commit.\n\n**--squash**\n> Merge the changes into the working set without updating the commit history.\n\n**--no-ff**\n> Create a merge commit even when the merge resolves as a fast-forward.\n\n**--ff-only**\n> Refuse to merge unless HEAD is already up to date or the merge resolves as a fast-forward.\n\n**--commit**\n> Perform the merge and commit the result. This is the default.\n\n**--no-commit**\n> Perform the merge but stop just before creating the merge commit.\n\n**--no-edit**\n> Use an auto-generated commit message instead of opening an editor.\n\n**--author** _NAME_ <_EMAIL_>\n> Record an explicit author for the merge commit.\n\n**--abort**\n> Abort an in-progress merge and return the working set to its pre-merge state."],["DESCRIPTION","**dolt merge** joins two histories by incorporating the changes from another branch into the current one. Because Dolt's unit of change is a row rather than a line of text, the merge is a three-way merge over table data and schema: rows touched on only one side are taken directly, and rows changed on both sides are compared cell by cell, so two branches that edit different columns of the same row still merge cleanly.\n\nA conflict is raised when both branches change the *same cell* to different values, or when the schemas diverge incompatibly. Unlike Git, Dolt does not write markers into your data. Conflicts are recorded in the `dolt_conflicts` and `dolt_conflicts_<table>` system tables, which you query and resolve with SQL before committing. Schema and constraint violations land in `dolt_schema_conflicts` and `dolt_constraint_violations`."],["CAVEATS","Conflicts must be resolved through the system tables, not by editing files; a merge cannot be committed while rows remain in `dolt_conflicts`. **--squash** applies the changes but leaves no link to the merged branch's history, so a later merge of the same branch will reconsider the same commits. The working set must be clean before merging. Merges may also succeed at the row level yet break a foreign key or unique constraint, which surfaces as a constraint violation rather than a conflict."],["HISTORY","dolt merge implements **git merge** semantics for relational data, which is the whole point of the project: making database changes reviewable and mergeable the way code is. Cell-wise three-way merge, schema merge, and the conflict system tables were built out over successive releases as Dolt matured from a data-sharing tool into a MySQL-compatible database."],["INSTALL","```pacman: sudo pacman -S dolt```\n\n```brew: brew install dolt```\n\n```nix: nix profile install nixpkgs#dolt```"],["SEE ALSO","[dolt](/man/dolt)(1), [dolt-checkout](/man/dolt-checkout)(1), [dolt-branch](/man/dolt-branch)(1), [dolt-commit](/man/dolt-commit)(1), [git-merge](/man/git-merge)(1)"],["RESOURCES","```[Source code](https://github.com/dolthub/dolt)```\n\n```[Documentation](https://www.dolthub.com/docs/cli-reference/cli/)```"]],"e":["dolt","dolt-checkout","dolt-branch","dolt-commit","git-merge"],"x":"dolt merge [branch_name]"},"errno":{"n":"errno","t":"System error number lookup utility","s":[["TAGLINE","System error number lookup utility"],["TLDR","**Look up error** by number\n\n```errno [22]```\n\n**Look up error** by name\n\n```errno [EINVAL]```\n\n**List all errors**\n\n```errno -l```\n\n**Search error** descriptions\n\n```errno -s [permission]```"],["SYNOPSIS","**errno** [_options_] [_error_...]"],["PARAMETERS","_ERROR_\n> Error number or name to look up.\n\n**-l**, **--list**\n> List all errors.\n\n**-s**, **--search** _TEXT_\n> Search descriptions for matching words (case-insensitive).\n\n**-S**, **--search-all-locales** _TEXT_\n> Search descriptions across all available locales.\n\n**--help**\n> Display help information."],["DESCRIPTION","**errno** looks up errno values and their descriptions. It translates between error numbers and symbolic names (like EINVAL, ENOENT) used in system calls.\n\nThe tool is valuable for debugging, helping understand system call failures. Error numbers from strace output or program errors can be quickly translated to meaningful descriptions.\n\nerrno also provides searching and listing capabilities to explore the full set of defined errors."],["CAVEATS","Error numbers may vary between architectures. Some errors are system-specific. Descriptions may be terse. Not all errno values have standard meanings."],["HISTORY","errno is part of the **moreutils** package. The errno system dates back to early Unix, providing a standardized way for system calls to report errors."],["INSTALL","```apt: sudo apt install moreutils```\n\n```dnf: sudo dnf install moreutils```\n\n```pacman: sudo pacman -S moreutils```\n\n```apk: sudo apk add moreutils```\n\n```zypper: sudo zypper install moreutils```\n\n```brew: brew install moreutils```\n\n```nix: nix profile install nixpkgs#moreutils```"],["SEE ALSO","[strace](/man/strace)(1)"]],"e":["strace"],"x":"errno [22]"},"exa":{"n":"exa","t":"modern ls replacement written in Rust","s":[["TAGLINE","modern ls replacement written in Rust"],["TLDR","**List files** with colors\n\n```exa```\n\n**Long format** with details\n\n```exa -l```\n\n**Show hidden files**\n\n```exa -a```\n\n**Tree view**\n\n```exa --tree```\n\n**Show git status**\n\n```exa -l --git```\n\n**Sort by modified** time\n\n```exa -l --sort=modified```\n\n**Show file sizes** human-readable\n\n```exa -lh```\n\n**Extended attributes**\n\n```exa -l --extended```"],["SYNOPSIS","**exa** [_options_] [_files_...]"],["PARAMETERS","_FILES_\n> Files or directories to list.\n\n**-l**, **--long**\n> Long format.\n\n**-a**, **--all**\n> Show hidden files.\n\n**--tree**\n> Tree view.\n\n**--git**\n> Show git status.\n\n**-s**, **--sort** _FIELD_\n> Sort by field.\n\n**-h**, **--header**\n> Show header row.\n\n**--icons**\n> Show file icons.\n\n**--color** _WHEN_\n> Color mode: always, auto, never.\n\n**--help**\n> Display help information."],["DESCRIPTION","**exa** is a modern replacement for ls written in Rust. It provides colorful output, git integration, tree view, and more detailed file information in a user-friendly format.\n\nThe tool defaults to human-readable sizes and sensible colors. Git integration shows file status in repository directories. Extended attributes and metadata are easily accessible.\n\nexa is faster than ls for most operations while providing significantly more features."],["CAVEATS","Different output format than ls. May break scripts expecting ls output. Icons need font support. Unmaintained since 2023 (see eza fork)."],["HISTORY","exa was created by **Benjamin Sago** (ogham) starting around **2014** as a modern ls replacement. Development stopped in **2023**, with **eza** continuing as an active fork."],["INSTALL","```apt: sudo apt install eza```\n\n```dnf: sudo dnf install eza```\n\n```pacman: sudo pacman -S eza```\n\n```apk: sudo apk add eza```\n\n```zypper: sudo zypper install eza```\n\n```brew: brew install eza```\n\n```nix: nix profile install nixpkgs#eza```"],["SEE ALSO","[ls](/man/ls)(1), [eza](/man/eza)(1), [lsd](/man/lsd)(1), [tree](/man/tree)(1)"],["RESOURCES","```[Source code](https://github.com/ogham/exa)```"]],"e":["ls","eza","lsd","tree"],"x":"exa"},"firebase":{"n":"firebase","t":"Firebase platform management CLI","s":[["TAGLINE","Firebase platform management CLI"],["TLDR","**Login to Firebase**\n\n```firebase login```\n\n**Initialize project**\n\n```firebase init```\n\n**Deploy all services**\n\n```firebase deploy```\n\n**Deploy only hosting**\n\n```firebase deploy --only hosting```\n\n**Deploy only functions**\n\n```firebase deploy --only functions```\n\n**Start local emulators**\n\n```firebase emulators:start```\n\n**List projects**\n\n```firebase projects:list```"],["SYNOPSIS","**firebase** [_options_] _command_"],["PARAMETERS","**login**\n> Authenticate with Google account.\n\n**logout**\n> Sign out.\n\n**init**\n> Initialize Firebase project in directory.\n\n**deploy**\n> Deploy to Firebase services.\n\n**--only** _services_\n> Deploy specific services.\n\n**serve**\n> Start local development server.\n\n**emulators:start**\n> Start Firebase emulators.\n\n**projects:list**\n> List available projects.\n\n**use** _project_\n> Switch active project.\n\n**functions:log**\n> View Cloud Functions logs.\n\n**hosting:disable**\n> Disable hosting for project."],["CONFIGURATION","**firebase.json**\n> Project configuration including hosting rules, function settings, and database rules.\n\n**.firebaserc**\n> Project aliases and active project selection."],["DESCRIPTION","**firebase** is the CLI for Firebase, Google's mobile and web application platform. It manages deployments for Hosting, Cloud Functions, Firestore, Realtime Database, Authentication, and other Firebase services.\n\nThe CLI handles project initialization, local development with emulators, and deployment workflows. It integrates with Google Cloud and supports multiple environments."],["CAVEATS","Requires Node.js (install via `npm install -g firebase-tools`). A Google account is needed for authentication. Some services (Cloud Functions, extended Firestore usage) require a Blaze billing plan. The emulator suite requires Java for Firestore and Realtime Database emulators."],["HISTORY","Firebase was founded in **2011** and acquired by **Google** in **2014**. The Firebase CLI evolved from basic hosting deployment to support the expanding Firebase platform including Cloud Functions, Firestore, and comprehensive local emulation."],["SEE ALSO","[gcloud](/man/gcloud)(1), [npm](/man/npm)(1), [node](/man/node)(1)"]],"e":["gcloud","npm","node"],"x":"firebase login"},"freesweep":{"n":"freesweep","t":"terminal minesweeper game","s":[["TAGLINE","terminal minesweeper game"],["TLDR","**Start a default game**\n\n```freesweep```\n\n**Set board dimensions**\n\n```freesweep --height [16] --width [30]```\n\n**Set number of mines**\n\n```freesweep --mines [99]```"],["SYNOPSIS","**freesweep** [_options_]"],["PARAMETERS","**-h**, **--height** _N_\n> Number of rows.\n\n**-w**, **--width** _N_\n> Number of columns.\n\n**-m**, **--mines** _N_\n> Number of mines.\n\n**-%**, **--percent** _N_\n> Set mines as percentage of board squares.\n\n**-f**, **--fast**\n> Skip the interactive setup dialog.\n\n**-i**, **--interactive**\n> Force interactive setup menu.\n\n**-a**, **--alt-charset**\n> Toggle alternate character set for rendering.\n\n**-b**, **--show-best-times**\n> Show the best-times viewer.\n\n**-s**, **--save-prefs**\n> Save the current command-line preferences.\n\n**-H**, **--help**\n> Display help information."],["PREVIEW","```\n .  .  1  .  .  .\n .  1  2  1  .  .\n .  1  #  .  .  1\n .  1  1  1  .  1\n```"],["DESCRIPTION","**freesweep** is a curses-based minesweeper game for the terminal. The player reveals cells on a grid, using number clues to avoid hidden mines. Flagging suspected mines and clearing all safe cells wins the game.\n\nUse arrow keys to move, space to reveal a cell, and **f** to flag a mine. Press **q** to quit."],["CAVEATS","Requires ncurses support. Board dimensions are limited by terminal size."],["HISTORY","**freesweep** was written as a free terminal minesweeper clone. Minesweeper was popularized by its inclusion in **Microsoft Windows 3.1** in **1992**."],["INSTALL","```nix: nix profile install nixpkgs#freesweep```"],["SEE ALSO","[nbmines](/man/nbmines)(6), [greed](/man/greed)(6)"]],"e":["nbmines","greed"],"x":"freesweep"},"frpc":{"n":"frpc","t":"fast reverse proxy client","s":[["TAGLINE","fast reverse proxy client"],["TLDR","**Start frp client**\n\n```frpc -c [frpc.ini]```\n\n**Verify configuration**\n\n```frpc verify -c [frpc.ini]```\n\n**Reload configuration**\n\n```frpc reload -c [frpc.ini]```\n\n**Show status**\n\n```frpc status -c [frpc.ini]```\n\n**Show version**\n\n```frpc -v```"],["SYNOPSIS","**frpc** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> Operation: verify, reload, status.\n\n**-c** _FILE_\n> Configuration file path.\n\n**verify**\n> Verify configuration file.\n\n**reload**\n> Reload configuration without restart.\n\n**status**\n> Show proxy status.\n\n**-v**, **--version**\n> Show version.\n\n**--help**\n> Display help information."],["CONFIGURATION","**frpc.ini**\n> Client configuration file specifying server address, authentication token, and proxy definitions for each service to expose through the tunnel."],["DESCRIPTION","**frpc** is the client component of frp (fast reverse proxy). It runs on internal networks and establishes connections to the frps server, creating tunnels for exposed services.\n\nConfiguration defines proxies for TCP, UDP, HTTP, and HTTPS services. The client maintains persistent connections and handles reconnection. Multiple services can be exposed through one client.\n\nfrpc enables accessing internal services through a public frps server."],["CAVEATS","Requires frps server running. Configuration must match server. Network connectivity required."],["HISTORY","frpc is part of **frp**, an open source reverse proxy project. It provides the client-side functionality for creating tunnels through NAT and firewalls."],["INSTALL","```apk: sudo apk add frp```\n\n```brew: brew install frpc```\n\n```nix: nix profile install nixpkgs#frp```"],["SEE ALSO","[frps](/man/frps)(1), [frp](/man/frp)(1), [ssh](/man/ssh)(1)"],["RESOURCES","```[Source code](https://github.com/fatedier/frp)```\n\n```[Documentation](https://gofrp.org/en/docs/)```"]],"e":["frps","frp","ssh"],"x":"frpc -c [frpc.ini]"},"getuserspns.py":{"n":"getuserspns.py","t":"Kerberoasting attack tool for service accounts","s":[["TAGLINE","Kerberoasting attack tool for service accounts"],["TLDR","**Enumerate SPN accounts**\n\n```GetUserSPNs.py [domain]/[username]:[password] -dc-ip [dc_ip]```\n\n**Request service tickets**\n\n```GetUserSPNs.py [domain]/[username]:[password] -dc-ip [dc_ip] -request```\n\n**Output for hashcat**\n\n```GetUserSPNs.py [domain]/[username]:[password] -dc-ip [dc_ip] -request -outputfile [hashes.txt]```\n\n**Using NTLM hash**\n\n```GetUserSPNs.py [domain]/[username] -hashes :[ntlm_hash] -dc-ip [dc_ip]```"],["SYNOPSIS","**GetUserSPNs.py** _domain_/_user_ [_options_]"],["PARAMETERS","_DOMAIN/USER_\n> Domain and username.\n\n**-dc-ip** _IP_\n> Domain controller IP.\n\n**-request**\n> Request TGS tickets.\n\n**-hashes** _LMHASH:NTHASH_\n> Use NTLM hashes.\n\n**-outputfile** _FILE_\n> Output file for hashes.\n\n**--help**\n> Display help information."],["DESCRIPTION","**GetUserSPNs.py** performs Kerberoasting attacks against Active Directory. It identifies service accounts with SPNs and requests service tickets containing crackable password hashes.\n\nThe tool queries AD for accounts with servicePrincipalName attributes, then requests TGS tickets. These tickets contain password material crackable offline.\n\nGetUserSPNs.py is used in penetration testing for credential attacks."],["CAVEATS","**Authorized testing only.** Requires domain credentials. Results need offline cracking."],["HISTORY","GetUserSPNs.py is part of **Impacket**, implementing the Kerberoasting attack technique documented in Active Directory security research."]],"e":[],"x":"GetUserSPNs.py [domain]/[username]:[password] -dc-ip [dc_ip]"},"git-config":{"n":"git-config","t":"Get and set repository or global options","s":[["TAGLINE","Get and set repository or global options"],["TLDR","**Set user name**\n\n```git config --global user.name \"[Name]\"```\n\n**Set user email**\n\n```git config --global user.email \"[email@example.com]\"```\n\n**Get a value**\n\n```git config user.name```\n\n**Set the default branch name** for new repositories\n\n```git config --global init.defaultBranch main```\n\n**Create a command alias**\n\n```git config --global alias.co checkout```\n\n**Remove a setting**\n\n```git config --global --unset [key]```\n\n**List all settings** and their origins\n\n```git config --list --show-origin```\n\n**Edit config file**\n\n```git config --global --edit```"],["SYNOPSIS","**git config** [_options_] _name_ [_value_]"],["PARAMETERS","_NAME_\n> Configuration key.\n\n_VALUE_\n> Value to set.\n\n**--global**\n> User-level config (~/.gitconfig).\n\n**--local**\n> Repository config (.git/config).\n\n**--system**\n> System-wide config.\n\n**--list**, **-l**\n> List all variables.\n\n**--edit**, **-e**\n> Open config in editor.\n\n**--get** _NAME_\n> Get the value for a given key (default behavior when only name is provided).\n\n**--get-all** _NAME_\n> Get all values for a multi-valued key.\n\n**--unset** _NAME_\n> Remove a setting.\n\n**--show-origin**\n> Show the file where each config value comes from.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git config** manages Git configuration variables that control all aspects of Git's behavior, from user identity and commit settings to advanced options like merge strategies, credential helpers, and performance tuning. Configuration is stored in INI-style text files at three hierarchical levels.\n\nSystem-level configuration (/etc/gitconfig) applies to all users on the system and requires administrator privileges to modify. Global configuration (~/.gitconfig or ~/.config/git/config) applies to the current user across all repositories. Local configuration (.git/config) applies only to the specific repository. Settings at lower levels override higher levels, allowing project-specific customization.\n\nCommon configuration tasks include setting user identity (user.name and user.email, required for commits), defining command aliases (alias.*), configuring merge and diff tools (merge.tool, diff.tool), setting default branch names (init.defaultBranch), and managing credentials (credential.helper)."],["CONFIGURATION","**~/.gitconfig** or **~/.config/git/config**\n> Global user-level configuration file (--global).\n\n**.git/config**\n> Repository-specific configuration file (--local).\n\n**/etc/gitconfig**\n> System-wide configuration file (--system)."],["CAVEATS","Local settings override global, and global overrides system. When a key is set at multiple levels, the most specific scope wins. Some settings like **core.autocrlf** can cause unexpected behavior if set inconsistently across scopes. Credential helpers vary by platform and may require separate installation."],["HISTORY","git config is a core **Git** command from initial release, providing the configuration system that controls all git behavior."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git](/man/git)(1), [git-credential](/man/git-credential)(1), [git-init](/man/git-init)(1), [git-commit](/man/git-commit)(1)"]],"e":["git","git-credential","git-init","git-commit"],"x":"git config --global user.name \"[Name]\""},"gitea":{"n":"gitea","t":"Self-hosted Git service with web interface","s":[["TAGLINE","Self-hosted Git service with web interface"],["TLDR","**Start Gitea server**\n\n```gitea```\n\n**Start with config**\n\n```gitea --config [/etc/gitea/app.ini]```\n\n**Generate a secret key**\n\n```gitea generate secret [INTERNAL_TOKEN]```\n\n**Create admin user**\n\n```gitea admin user create --username [admin] --password [password] --email [admin@example.com] --admin```\n\n**Run health checks**\n\n```gitea doctor check```\n\n**Dump full installation to an archive**\n\n```gitea dump```\n\n**Run pending database migrations**\n\n```gitea migrate```"],["SYNOPSIS","**gitea** [_global options_] _command_ [_command options_]"],["PARAMETERS","**web**\n> Start the web server.\n\n**admin**\n> Perform admin operations: manage users (**user**), auth sources (**auth**), or regenerate hooks/keys (**regenerate**).\n\n**cert**\n> Generate a self-signed SSL certificate.\n\n**doctor**\n> Diagnose and optionally repair problems with the installation (**check**, **recreate-table**).\n\n**dump**\n> Export the full installation (database, repos, config, etc.) into a compressed archive.\n\n**dump-repo** / **restore-repo**\n> Migrate repository data between Gitea instances.\n\n**generate**\n> Generate random secrets/tokens: `INTERNAL_TOKEN`, `JWT_SECRET`, `SECRET_KEY`, `LFS_JWT_SECRET`.\n\n**keys**\n> Provide the `AuthorizedKeysCommand` output used for SSHD integration.\n\n**manager**\n> Control a running instance: shutdown, restart, flush queues, adjust logging.\n\n**migrate**\n> Run pending database migrations; typically run once before first startup.\n\n**-c**, **--config** _FILE_\n> Path to the configuration file (default `custom/conf/app.ini`).\n\n**-w**, **--work-path** _PATH_\n> Gitea's base working directory.\n\n**-C**, **--custom-path** _PATH_\n> Path to the custom folder.\n\n**--port** _PORT_\n> Server port (web command).\n\n**-v**, **--version**\n> Show version information.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**gitea** is a self-hosted Git service written in Go. It provides GitHub-like functionality including repositories, issues, pull requests, and CI/CD, all packaged in a single binary.\n\nGitea is lightweight and fast, suitable for personal or small team use. It supports OAuth, LDAP, and various database backends for enterprise deployments."],["CONFIGURATION","**/etc/gitea/app.ini**\n> Main configuration file controlling server settings, database, authentication, and repository defaults."],["CAVEATS","Requires configuration for production. Database setup needed. Reverse proxy recommended."],["HISTORY","Gitea was forked from **Gogs** in **2016**, becoming a community-driven project with active development and broader feature set."],["INSTALL","```pacman: sudo pacman -S gitea```\n\n```apk: sudo apk add gitea```\n\n```brew: brew install gitea```\n\n```nix: nix profile install nixpkgs#gitea```"],["SEE ALSO","[git](/man/git)(1), [gitlab-runner](/man/gitlab-runner)(1), [gitlab](/man/gitlab)(1)"],["RESOURCES","```[Source code](https://github.com/go-gitea/gitea)```\n\n```[Homepage](https://about.gitea.com/)```\n\n```[Documentation](https://docs.gitea.com/)```"]],"e":["git","gitlab-runner","gitlab"],"x":"gitea"},"go-build":{"n":"go-build","t":"compile Go packages and dependencies","s":[["TAGLINE","compile Go packages and dependencies"],["TLDR","**Build current package**\n\n```go build```\n\n**Build specific file**\n\n```go build [main.go]```\n\n**Set output name**\n\n```go build -o [binary-name]```\n\n**Cross-compile**\n\n```GOOS=[linux] GOARCH=[amd64] go build```\n\n**Build with race detector**\n\n```go build -race```\n\n**Build with trimmed paths** (for reproducible builds)\n\n```go build -trimpath -o [binary-name]```\n\n**Build with profile-guided optimization**\n\n```go build -pgo=[profile.pprof] -o [binary-name]```"],["SYNOPSIS","**go build** [_options_] [_packages_]"],["PARAMETERS","_PACKAGES_\n> Packages to build.\n\n**-o** _FILE_\n> Output file name.\n\n**-v**\n> Verbose output.\n\n**-race**\n> Enable race detector.\n\n**-ldflags** _FLAGS_\n> Linker flags.\n\n**-tags** _TAGS_\n> Build tags.\n\n**-trimpath**\n> Remove all file system paths from the resulting executable.\n\n**-gcflags** _'[pattern=]arg list'_\n> Arguments passed to the Go compiler.\n\n**-mod** _MODE_\n> Module download mode: readonly, vendor, or mod.\n\n**-pgo** _FILE_\n> Profile-guided optimization file (default: auto).\n\n**-cover**\n> Enable code coverage instrumentation.\n\n**-a**\n> Force rebuilding of packages already up-to-date.\n\n**-n**\n> Print commands but do not run them.\n\n**-x**\n> Print the commands as they are executed.\n\n**-buildmode** _MODE_\n> Build mode (default, archive, c-archive, c-shared, shared, exe, pie, plugin)."],["DESCRIPTION","**go build** compiles Go packages and dependencies. It produces an executable binary from the main package or checks compilation for library packages.\n\nThe command handles dependency resolution, compilation, and linking. Cross-compilation is built-in via GOOS and GOARCH environment variables, requiring no additional toolchains."],["CAVEATS","Main package produces an executable; library packages only check compilation. CGO requires a C compiler. Use `go help build` for full documentation."],["HISTORY","go build is a core command of the **Go** toolchain, providing fast incremental compilation since Go's release."],["INSTALL","```apt: sudo apt install gccgo-go```\n\n```dnf: sudo dnf install gcc-go```\n\n```pacman: sudo pacman -S gcc-go```\n\n```apk: sudo apk add gcc-go```\n\n```zypper: sudo zypper install gcc-go```\n\n```brew: brew install go```\n\n```nix: nix profile install nixpkgs#go```"],["SEE ALSO","[go](/man/go)(1), [go-run](/man/go-run)(1), [go-install](/man/go-install)(1)"]],"e":["go","go-run","go-install"],"x":"go build"},"gphoto2":{"n":"gphoto2","t":"Command-line client for digital cameras via libgphoto2","s":[["TAGLINE","Command-line client for digital cameras via libgphoto2"],["TLDR","**Detect connected cameras** and the ports they are attached to\n\n```gphoto2 --auto-detect```\n\n**Capture an image** and keep it on the camera\n\n```gphoto2 --capture-image```\n\n**Capture an image** and download it immediately\n\n```gphoto2 --capture-image-and-download```\n\n**List all files** stored on the camera\n\n```gphoto2 --list-files```\n\n**Download a specific file** by index number\n\n```gphoto2 --get-file [number]```\n\n**Download every file** from the camera\n\n```gphoto2 --get-all-files```\n\n**Time-lapse capture** of 10 frames at 30 second intervals\n\n```gphoto2 --capture-image-and-download -F 10 -I 30```\n\n**Stream live preview** to stdout (useful as webcam source)\n\n```gphoto2 --stdout --capture-movie```"],["SYNOPSIS","**gphoto2** [_options_] _action_"],["PARAMETERS","**--auto-detect**\n> Detect cameras attached to the system.\n\n**--list-cameras**\n> Print list of all supported camera models.\n\n**--list-ports**\n> Print list of supported port types.\n\n**--port** _PATH_\n> Select a specific port (e.g. _usb:_ or _serial:/dev/ttyS0_).\n\n**--camera** _MODEL_\n> Force a specific camera model.\n\n**-L**, **--list-files**\n> List files in the current folder on the camera.\n\n**-f** _FOLDER_, **--folder** _FOLDER_\n> Switch to specified folder on the camera.\n\n**-p** _RANGE_, **--get-file** _RANGE_\n> Download files identified by number range (e.g. _1_, _1-4_, _1,3,5_).\n\n**--get-all-files**\n> Download every file from the current folder.\n\n**--get-thumbnail** _RANGE_\n> Download thumbnails rather than full files.\n\n**--get-raw-data** _RANGE_\n> Download raw sensor data.\n\n**-u** _FILE_, **--upload-file** _FILE_\n> Upload a file to the camera.\n\n**-d** _RANGE_, **--delete-file** _RANGE_\n> Delete files by number.\n\n**-D**, **--delete-all-files**\n> Delete every file in the current folder.\n\n**--capture-image**\n> Trigger the shutter and keep the image on the camera.\n\n**--capture-image-and-download**\n> Capture and immediately download the resulting file.\n\n**--capture-movie** [_=COUNT_|_=SECONDSs_]\n> Capture a movie or stream of preview frames.\n\n**-F** _N_, **--frames** _N_\n> Number of frames for time-lapse captures.\n\n**-I** _SECONDS_, **--interval** _SECONDS_\n> Seconds between frames during time-lapse.\n\n**--filename** _TEMPLATE_\n> Specify an output filename template (supports _%n_, _%C_, _%f_, etc.).\n\n**--stdout**\n> Write downloaded data to standard output instead of a file.\n\n**--list-config**\n> List all camera configuration entries.\n\n**--get-config** _NAME_\n> Print the current value of a configuration entry.\n\n**--set-config** _NAME=VALUE_\n> Set a configuration entry (e.g. _iso=400_, _shutterspeed=1/125_).\n\n**--summary**\n> Print a summary of the camera's capabilities.\n\n**-q**, **--quiet**\n> Reduce output verbosity.\n\n**-h**, **--help**\n> Show usage information."],["DESCRIPTION","**gphoto2** is the official command-line frontend for the **libgphoto2** library, providing access to more than **2500 digital cameras** over USB, serial, PTP/IP, and other transports. It lets photographers and developers capture images, download files, trigger the shutter, tether cameras to computers, and adjust settings without using the vendor software.\n\nThe tool operates on the current folder of the camera's storage; switch folders with **--folder** and query structure with **--list-folders** or **--list-files**. Each file is addressed by a 1-based index. Multiple actions may be combined on one command line and are executed sequentially.\n\nFor configuration, gphoto2 exposes the camera's property tree (exposure, ISO, white balance, focus mode, etc.) through **--list-config**, **--get-config**, and **--set-config**. This makes it a common building block for remote capture studios, observatory rigs, and Linux webcam setups (e.g. piping **--capture-movie** output into **ffmpeg** and **v4l2loopback**)."],["CAVEATS","Not every camera supports every feature; PTP cameras usually allow capture and config, while older PTP-incompatible cameras expose only file transfer. Stop any desktop service that auto-mounts MTP/PTP cameras (e.g. _gvfs-gphoto2-volume-monitor_) before using gphoto2 directly; otherwise the USB device is held by another process. Long tethered sessions may require explicit power settings to prevent the camera from sleeping."],["HISTORY","The **gphoto** project was started in **1997** by **Scott Fritzinger** to give Linux users a common interface for consumer digital cameras. The second-generation library **libgphoto2** and its reference client **gphoto2** were released in **2001**, reworking the architecture around modular camlib drivers. The project is maintained by an open-source community and underpins camera support in desktop environments such as GNOME and KDE."],["INSTALL","```apt: sudo apt install gphoto2```\n\n```dnf: sudo dnf install gphoto2```\n\n```pacman: sudo pacman -S gphoto2```\n\n```apk: sudo apk add gphoto2```\n\n```brew: brew install gphoto2```\n\n```nix: nix profile install nixpkgs#gphoto2```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [v4l2-ctl](/man/v4l2-ctl)(1), [exiftool](/man/exiftool)(1)"]],"e":["ffmpeg","v4l2-ctl","exiftool"],"x":"gphoto2 --auto-detect"},"gvfs-open":{"n":"gvfs-open","t":"opens files and URIs with their default applications","s":[["TAGLINE","opens files and URIs with their default applications"],["TLDR","**Open file with default app**\n\n```gvfs-open [file.pdf]```\n\n**Open URL**\n\n```gvfs-open [https://example.com]```\n\n**Open remote file**\n\n```gvfs-open smb://server/share/document.docx```\n\n**Open directory in file manager**\n\n```gvfs-open [directory]```"],["SYNOPSIS","**gvfs-open** [_OPTION..._] [_FILE..._]"],["PARAMETERS","_FILE_\n> File, directory, or URI to open.\n\n**-h**, **--help**\n> Display help information.\n\n**--version**\n> Show version number and exit."],["DESCRIPTION","**gvfs-open** opens files with the default application registered to handle files of that type. It obtains MIME type information from the shared-mime-info database, and the default handler is stored per-user in `$XDG_DATA_HOME/applications/mimeapps.list`.\n\nThe tool handles local files, remote URIs, and URLs. When a running GNOME session is detected, `xdg-open` actually calls `gvfs-open` internally."],["CAVEATS","Deprecated in favor of gio open. GNOME specific. Requires proper MIME associations."],["HISTORY","gvfs-open was developed as part of **GVFS** for GNOME, now largely replaced by the **gio** and **xdg-open** commands."],["SEE ALSO","[gio](/man/gio)(1), [xdg-open](/man/xdg-open)(1), [open](/man/open)(1)"]],"e":["gio","xdg-open","open"],"x":"gvfs-open [file.pdf]"},"halt":{"n":"halt","t":"instructs the system to stop all processes and halt the CPU","s":[["TAGLINE","instructs the system to stop all processes and halt the CPU"],["TLDR","**Halt** the system\n\n```halt```\n\n**Power off** the system (same as poweroff)\n\n```halt -p```\n\n**Reboot** the system (same as reboot)\n\n```halt --reboot```\n\nHalt **immediately** without contacting the system manager\n\n```halt -f```\n\nWrite the wtmp shutdown entry **without halting**\n\n```halt -w```"],["SYNOPSIS","**halt** [_options_]"],["PARAMETERS","**-p**, **--poweroff**\n> Power off the machine (equivalent to poweroff)\n\n**--reboot**\n> Reboot the machine (equivalent to reboot)\n\n**--halt**\n> Halt the machine, regardless of which command is invoked\n\n**-f**, **--force**\n> Force immediate halt without contacting the init system\n\n**-w**, **--wtmp-only**\n> Only write wtmp record, don't actually halt\n\n**-d**, **--no-wtmp**\n> Don't write wtmp record\n\n**--no-wall**\n> Don't send wall message before halt"],["DESCRIPTION","**halt** instructs the system to stop all processes and halt the CPU. On modern systems with systemd, halt is a symlink to systemctl and triggers a proper shutdown sequence.\n\nThe difference between halt, poweroff, and reboot:\n- **halt** - Stops the CPU but may leave power on\n- **poweroff** - Stops the CPU and powers off the machine\n- **reboot** - Restarts the machine\n\nIn practice, most systems treat halt and poweroff similarly, powering off the machine."],["CAVEATS","Using **-f** bypasses the normal shutdown sequence, which may cause data loss. On systemd systems, these commands are wrappers around systemctl. Running halt typically requires root privileges."],["HISTORY","halt is a traditional Unix command dating back to early Unix systems. On modern Linux distributions using systemd, halt, poweroff, and reboot are symlinks to systemctl, providing backwards compatibility while using systemd's shutdown mechanisms."],["INSTALL","```apt: sudo apt install runit-init```"],["SEE ALSO","[poweroff](/man/poweroff)(8), [reboot](/man/reboot)(8), [shutdown](/man/shutdown)(8), [systemctl](/man/systemctl)(1)"]],"e":["poweroff","reboot","shutdown","systemctl"],"x":"halt"},"hp-plugin":{"n":"hp-plugin","t":"installs the HP proprietary plugin","s":[["TAGLINE","installs the HP proprietary plugin"],["TLDR","**Install plugin**\n\n```sudo hp-plugin```\n\n**Interactive install**\n\n```sudo hp-plugin -i```\n\n**Automatic install**\n\n```sudo hp-plugin -a```\n\n**Download only**\n\n```hp-plugin -d```"],["SYNOPSIS","**hp-plugin** [_options_]"],["PARAMETERS","**-i**, **--interactive**\n> Interactive mode.\n\n**-a**, **--auto**\n> Automatic mode.\n\n**-d**, **--download-only**\n> Download without install.\n\n**-p** _PATH_\n> Plugin file path.\n\n**--force**\n> Force reinstall.\n\n**--help**\n> Display help information."],["DESCRIPTION","**hp-plugin** installs the HP proprietary plugin. Some HP printers require this binary plugin for full functionality like scanning.\n\nThe tool downloads and installs the plugin package. It must be run as root and requires network access."],["CAVEATS","Requires root access. Downloads from HP servers. Some countries may have restrictions."],["HISTORY","hp-plugin is part of **HPLIP** for installing proprietary components needed by certain HP devices."],["INSTALL","```apt: sudo apt install hplip```\n\n```dnf: sudo dnf install hplip```\n\n```pacman: sudo pacman -S hplip```\n\n```zypper: sudo zypper install hplip```\n\n```nix: nix profile install nixpkgs#hplip```"],["SEE ALSO","[hp-setup](/man/hp-setup)(1), [hp-check](/man/hp-check)(1)"]],"e":["hp-setup","hp-check"],"x":"sudo hp-plugin"},"hub-fork":{"n":"hub-fork","t":"creates a fork of a GitHub repository","s":[["TAGLINE","creates a fork of a GitHub repository"],["TLDR","**Fork current repository**\n\n```hub fork```\n\n**Fork without remote**\n\n```hub fork --no-remote```\n\n**Fork specific repository**\n\n```hub fork [user/repo]```\n\n**Fork to organization**\n\n```hub fork --org [myorg]```"],["SYNOPSIS","**hub fork** [_options_] [_repository_]"],["PARAMETERS","**--no-remote**\n> Skip adding a git remote for the fork.\n\n**--remote-name** _REMOTE_\n> Set the name for the new git remote.\n\n**--org** _ORGANIZATION_\n> Fork the repository within this organization instead of your account."],["DESCRIPTION","**hub fork** creates a fork of a GitHub repository. It adds a remote pointing to your fork automatically.\n\nThe command forks the repository detected in the current directory to your account or the specified organization, then adds a git remote for it (named **origin** by default, unless **origin** is already taken, in which case it uses your GitHub username)."],["CAVEATS","Part of **hub**, which is deprecated in favor of the official GitHub CLI (**gh**); use **gh repo fork** instead. Requires GitHub authentication."],["HISTORY","hub fork is part of **hub**, GitHub's original command-line wrapper around git for repository operations, created in 2012."],["INSTALL","```apt: sudo apt install hub```\n\n```pacman: sudo pacman -S hub```\n\n```zypper: sudo zypper install hub```\n\n```brew: brew install hub```\n\n```nix: nix profile install nixpkgs#hub```"],["SEE ALSO","[hub](/man/hub)(1), [gh](/man/gh)(1), [hub-clone](/man/hub-clone)(1)"],["RESOURCES","```[Source code](https://github.com/github/hub)```\n\n```[Homepage](https://hub.github.com/)```"]],"e":["hub","gh","hub-clone"],"x":"hub fork"},"ibmcloud-update":{"n":"ibmcloud-update","t":"updates the IBM Cloud CLI to the latest version","s":[["TAGLINE","updates the IBM Cloud CLI to the latest version"],["TLDR","**Update IBM Cloud CLI**\n\n```ibmcloud update```\n\n**Update with force**\n\n```ibmcloud update -f```\n\n**Check for updates**\n\n```ibmcloud update --check```"],["SYNOPSIS","**ibmcloud** **update** [_options_]"],["PARAMETERS","**-f**, **--force**\n> Force update without confirmation.\n\n**--check**\n> Check for available updates."],["DESCRIPTION","**ibmcloud update** updates the IBM Cloud CLI to the latest version. Use **--check** to see if an update is available without installing it."],["SEE ALSO","[ibmcloud](/man/ibmcloud)(1), [ibmcloud-version](/man/ibmcloud-version)(1)"]],"e":["ibmcloud","ibmcloud-version"],"x":"ibmcloud update"},"jigsaw":{"n":"jigsaw","t":"XScreenSaver hack that animates a jigsaw puzzle solving itself","s":[["TAGLINE","XScreenSaver hack that animates a jigsaw puzzle solving itself"],["TLDR","**Run as the screensaver** on the root window\n\n```jigsaw -root```\n\n**Run in a window** for testing\n\n```jigsaw -window```\n\n**Set the animation speed**\n\n```jigsaw -root -speed [1.0]```\n\n**Set the puzzle complexity**\n\n```jigsaw -root -complexity [1.0]```"],["SYNOPSIS","**jigsaw** [**-root** | **-window**] [_options_]"],["PARAMETERS","**-root**\n> Draw on the root window (used by xscreensaver itself).\n\n**-window**\n> Draw in a new top-level window.\n\n**-window-id** _id_\n> Draw in an existing window with the given X11 ID.\n\n**-speed** _float_\n> Animation speed multiplier.\n\n**-complexity** _float_\n> Puzzle complexity (controls number of pieces).\n\n**-delay** _usecs_\n> Delay between frames, in microseconds.\n\n**-fps**\n> Display the frames-per-second counter."],["DESCRIPTION","**jigsaw** is one of the OpenGL hacks shipped with **xscreensaver** (and **xscreensaver-gl**). It loads an image, slices it into jigsaw puzzle pieces, scatters them in 3D space, and then animates them flying back together to reform the picture before scrambling again.\n\nThe image source is controlled by xscreensaver's standard image-grabbing settings, so the screensaver can use a random image from disk, a webcam frame, or a screenshot of the desktop."],["CAVEATS","Requires OpenGL and a working X11 display. Older systems without 3D acceleration will see degraded performance. Image source is configured globally for xscreensaver via **xscreensaver-settings**, not per-hack."],["SEE ALSO","[xscreensaver](/man/xscreensaver)(1), [xscreensaver-settings](/man/xscreensaver-settings)(1)"]],"e":["xscreensaver","xscreensaver-settings"],"x":"jigsaw -root"},"jj-bookmark":{"n":"jj-bookmark","t":"manages named references to revisions in Jujutsu","s":[["TAGLINE","manages named references to revisions in Jujutsu"],["TLDR","**Create bookmark at current revision**\n\n```jj bookmark create [name]```\n\n**Create bookmark at specific revision**\n\n```jj bookmark create [name] -r [revision]```\n\n**List bookmarks**\n\n```jj bookmark list```\n\n**Delete bookmark**\n\n```jj bookmark delete [name]```\n\n**Move bookmark**\n\n```jj bookmark move [name] --to [revision]```"],["SYNOPSIS","**jj** **bookmark** _subcommand_ [_options_]"],["SUBCOMMANDS","**create** _name_\n> Create new bookmark.\n\n**delete** _name_\n> Delete bookmark.\n\n**list**\n> List all bookmarks.\n\n**move** _name_\n> Move bookmark to different revision.\n\n**track**\n> Track remote bookmark."],["PARAMETERS","**-r**, **--revision** _rev_\n> Target revision.\n\n**--to** _rev_\n> Destination for move."],["DESCRIPTION","**jj bookmark** manages named references to revisions in Jujutsu. Similar to Git branches, bookmarks point to specific commits and can be pushed to/pulled from remotes. Bookmarks follow commits when they are rewritten."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-git-push](/man/jj-git-push)(1)"]],"e":["jj","jj-git-push"],"x":"jj bookmark create [name]"},"keyctl":{"n":"keyctl","t":"utility for managing the Linux kernel keyring facility","s":[["TAGLINE","utility for managing the Linux kernel keyring facility"],["TLDR","**List** keys in a keyring\n\n```keyctl list @us```\n\n**Add** a key to a keyring\n\n```keyctl add user keyname \"keyvalue\" @us```\n\nAdd key value from **stdin**\n\n```echo -n \"value\" | keyctl padd user keyname @us```\n\n**Read** a key value\n\n```keyctl read keyid```\n\n**Pipe** key value (raw output)\n\n```keyctl pipe keyid```\n\nSet **timeout** on a key\n\n```keyctl timeout keyid 3600```\n\n**Revoke** a key\n\n```keyctl revoke keyid```\n\n**Clear** all keys from keyring\n\n```keyctl clear @us```"],["SYNOPSIS","**keyctl** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**keyctl** is a utility for managing the Linux kernel keyring facility. It allows creating, modifying, and querying keys and keyrings that can store authentication tokens, encryption keys, and other security-related data."],["PARAMETERS","**add TYPE NAME DATA KEYRING**\n> Create a new key and attach it to a keyring\n\n**padd TYPE NAME KEYRING**\n> Add a key with payload from stdin\n\n**request TYPE NAME [DEST_KEYRING]**\n> Request a key from the kernel\n\n**update KEY DATA**\n> Update a key's payload\n\n**newring NAME KEYRING**\n> Create a new keyring\n\n**revoke KEY**\n> Revoke a key, preventing further operations\n\n**clear KEYRING**\n> Remove all keys from a keyring\n\n**unlink KEY KEYRING**\n> Remove a key from a keyring\n\n**search KEYRING TYPE DESCRIPTION [DEST_KEYRING]**\n> Search for a key in a keyring\n\n**read KEY**\n> Read and display a key's payload\n\n**pipe KEY**\n> Output a key's payload without formatting\n\n**list KEYRING**\n> List keys in a keyring\n\n**describe KEY**\n> Display key attributes\n\n**timeout KEY SECONDS**\n> Set a key's expiration time\n\n**setperm KEY MASK**\n> Set key permissions"],["CAVEATS","Special keyring identifiers: @t (thread), @p (process), @s (session), @u (user), @us (user-default session). Keys expire and are garbage collected automatically. Proper permissions are required to access keys."],["HISTORY","**keyctl** is part of the **keyutils** package, providing user-space access to the Linux kernel key management facility introduced in kernel 2.6."],["INSTALL","```apt: sudo apt install keyutils```\n\n```dnf: sudo dnf install keyutils```\n\n```pacman: sudo pacman -S keyutils```\n\n```apk: sudo apk add keyutils```\n\n```zypper: sudo zypper install keyutils```\n\n```brew: brew install keyutils```\n\n```nix: nix profile install nixpkgs#keyutils```"],["SEE ALSO","[ssh-add](/man/ssh-add)(1), [gpg](/man/gpg)(1)"]],"e":["ssh-add","gpg"],"x":"keyctl list @us"},"kill":{"n":"kill","t":"sends a signal to a process, usually to stop it","s":[["TAGLINE","sends a signal to a process, usually to stop it"],["TLDR","**Terminate** a program using the default SIGTERM signal\n\n```kill [process_id]```\n\n**List** available signal names\n\n```kill -l```\n\nTerminate a **background job**\n\n```kill %[job_id]```\n\nSend **SIGHUP** signal (reload for many daemons)\n\n```kill -HUP [process_id]```\n\nSend **SIGINT** signal (like Ctrl+C)\n\n```kill -INT [process_id]```\n\n**Force kill** a program (SIGKILL)\n\n```kill -9 [process_id]```\n\n**Pause** a program (SIGSTOP)\n\n```kill -STOP [process_id]```\n\nSend signal to all processes with a **group ID**\n\n```kill -SIGUSR1 -[group_id]```"],["SYNOPSIS","**kill** [_-signal_] _pid_...\n\n**kill** _-l_"],["DESCRIPTION","**kill** sends a signal to a process, usually to stop it. All signals except SIGKILL and SIGSTOP can be intercepted by the process to perform a clean exit. By default, SIGTERM is sent."],["PARAMETERS","**-l, --list**\n> List signal names, or convert signal number to name\n\n**-L, --table**\n> List signal names and numbers in a table\n\n**-s signal**\n> Specify signal to send\n\n**-signal**\n> Specify signal by name or number (e.g., -9, -KILL, -SIGKILL)\n\n**-p, --pid**\n> Print the process ID only; do not send a signal\n\n**-q, --queue value**\n> Send the signal using sigqueue(3) with the accompanying integer value\n\n**pid**\n> Process ID to signal. A PID of 0 signals all processes in the current process group. A PID of -1 signals all processes with PID > 1.\n\n**%job_id**\n> Job ID from shell job control"],["COMMON SIGNALS","**SIGTERM (15)**: Request termination (default)\n**SIGKILL (9)**: Force immediate termination (cannot be caught)\n**SIGHUP (1)**: Hang up; often used to reload configuration\n**SIGINT (2)**: Interrupt (Ctrl+C)\n**SIGSTOP (19)**: Pause process (cannot be caught)\n**SIGCONT (18)**: Continue paused process"],["CAVEATS","SIGKILL (-9) should be used as a last resort as it doesn't allow the process to clean up. Use SIGTERM first and give the process time to exit gracefully."],["HISTORY","Part of standard Unix since early versions. The **kill** command has been available since **Version 4 AT&T UNIX** (1973)."],["INSTALL","```apt: sudo apt install procps```\n\n```dnf: sudo dnf install uutils-coreutils```\n\n```pacman: sudo pacman -S uutils-coreutils```\n\n```apk: sudo apk add uutils-coreutils```\n\n```zypper: sudo zypper install procps```\n\n```brew: brew install procps```\n\n```nix: nix profile install nixpkgs#procps```"],["SEE ALSO","[killall](/man/killall)(1), [pkill](/man/pkill)(1), [pgrep](/man/pgrep)(1), [ps](/man/ps)(1), [top](/man/top)(1)"]],"e":["killall","pkill","pgrep","ps","top"],"x":"kill [process_id]"},"kubectl-top":{"n":"kubectl-top","t":"displays resource usage for nodes or pods","s":[["TAGLINE","displays resource usage for nodes or pods"],["TLDR","**Show node resource usage**\n\n```kubectl top nodes```\n\n**Show pod resource usage**\n\n```kubectl top pods```\n\n**Show pod usage in namespace**\n\n```kubectl top pods -n [namespace]```\n\n**Show container-level usage**\n\n```kubectl top pods --containers```\n\n**Sort by CPU**\n\n```kubectl top pods --sort-by=cpu```"],["SYNOPSIS","**kubectl** **top** _type_ [_options_]"],["PARAMETERS","**nodes**\n> Display node resource usage.\n\n**pods**\n> Display pod resource usage.\n\n**--containers**\n> Show container-level metrics.\n\n**--sort-by** _field_\n> Sort by cpu or memory.\n\n**-n**, **--namespace** _name_\n> Kubernetes namespace.\n\n**-A**, **--all-namespaces**\n> Show all namespaces."],["DESCRIPTION","**kubectl top** displays resource usage (CPU and memory) for nodes or pods. Requires metrics-server to be running in the cluster. Useful for monitoring resource consumption and identifying resource-hungry workloads."],["CAVEATS","Requires metrics-server installed in the cluster. Metrics may have a delay of up to one minute."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kube-capacity](/man/kube-capacity)(1)"]],"e":["kubectl","kube-capacity"],"x":"kubectl top nodes"},"kubens":{"n":"kubens","t":"switches the default Kubernetes namespace","s":[["TAGLINE","switches the default Kubernetes namespace"],["TLDR","**List namespaces**\n\n```kubens```\n\n**Switch namespace**\n\n```kubens [namespace]```\n\n**Switch to previous**\n\n```kubens -```\n\n**Show current namespace**\n\n```kubens -c```"],["SYNOPSIS","**kubens** [_options_] [_namespace_]"],["PARAMETERS","_NAMESPACE_\n> Namespace to switch to.\n\n**-**\n> Switch to previous namespace.\n\n**-c**\n> Show current namespace.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kubens** is a utility for quickly switching the default Kubernetes namespace in the current context. It modifies the namespace field in your kubeconfig so that subsequent kubectl commands operate against the selected namespace without requiring the `-n` flag each time.\n\nThe tool lists all available namespaces when called without arguments and supports interactive fuzzy selection through fzf integration. It can also toggle back to the previously active namespace using `-`. Designed as a companion to kubectx, kubens completes the workflow for navigating multi-cluster, multi-namespace Kubernetes environments efficiently."],["CAVEATS","Third-party tool. Modifies kubeconfig. Works with kubectx."],["HISTORY","kubens was created by **Ahmet Alp Balkan** alongside kubectx for simplified Kubernetes namespace switching."],["INSTALL","```apt: sudo apt install kubectx```\n\n```pacman: sudo pacman -S kubectx```\n\n```apk: sudo apk add kubectx```\n\n```zypper: sudo zypper install kubectx```\n\n```brew: brew install kubectx```\n\n```nix: nix profile install nixpkgs#kubectx```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectx](/man/kubectx)(1), [kubectl-config](/man/kubectl-config)(1)"]],"e":["kubectl","kubectx","kubectl-config"],"x":"kubens"},"lid.idutils":{"n":"lid.idutils","t":"queries an ID database to find tokens matching a specified pattern","s":[["TAGLINE","queries an ID database to find tokens matching a specified pattern"],["TLDR","List all **tokens and their file locations** in the ID database\n\n```lid```\n\nFind **files containing a specific token**\n\n```lid [token]```\n\nFind tokens **matching a pattern, ignoring case**\n\n```lid -i [token]```\n\nFind tokens matching an **extended regex**\n\n```lid -r \"[pattern]\"```\n\nOutput matching lines in **grep-style format**\n\n```lid -R grep [token]```\n\nFind tokens that **appear only once** (useful for finding unused definitions)\n\n```lid -F 1```"],["SYNOPSIS","**lid** [_options_] [_pattern_]"],["PARAMETERS","**-f _filename_**\n> Specify which ID database file to read\n\n**-i, --ignore-case**\n> Perform case-insensitive pattern matching\n\n**-l, --literal**\n> Treat the pattern as a literal string (no regex)\n\n**-r, --regexp**\n> Force regular expression pattern matching\n\n**-w, --word**\n> Match whole words only (non-substring search)\n\n**-s, --substring**\n> Perform substring search\n\n**-k _style_**\n> Control output format: token, pattern, or none\n\n**-R, --result _style_**\n> Control result type: filenames, grep (show lines), edit (open in editor), or none\n\n**-F, --frequency _range_**\n> Match tokens by occurrence frequency (e.g., 1 for tokens appearing once)"],["DESCRIPTION","**lid** queries an ID database to find tokens matching a specified pattern. Part of GNU **idutils**, it searches a pre-built token database to quickly locate identifiers across source files.\n\nAn ID database must first be built using the **mkid** command. The database indexes all tokens (identifiers, keywords, strings) found in source files, enabling fast lookups without rescanning the entire codebase.\n\nPatterns are interpreted flexibly: if a pattern contains regex metacharacters, lid performs a regex substring search; otherwise it conducts a literal word search. When no pattern is given, all database entries are listed."],["CAVEATS","Requires a pre-built ID database created with **mkid**. The database must be regenerated after source files change to reflect current content. By default, lid searches for a file named **ID** in the current directory or its parents."],["HISTORY","The lid command is part of **GNU idutils**, originally written by Greg McGary. The idutils package has been maintained as part of the GNU project since the early 1990s, providing efficient source code indexing and searching capabilities."],["SEE ALSO","[mkid](/man/mkid)(1), [grep](/man/grep)(1)"]],"e":["mkid","grep"],"x":"lid"},"lpass":{"n":"lpass","t":"command-line interface for LastPass password manager","s":[["TAGLINE","command-line interface for LastPass password manager"],["TLDR","**Login to LastPass**\n\n```lpass login [user@example.com]```\n\n**Show password for a site**\n\n```lpass show --password [sitename]```\n\n**Copy password to clipboard**\n\n```lpass show --clip --password [sitename]```\n\n**List all accounts**\n\n```lpass ls```\n\n**Search for entries**\n\n```lpass show -G [pattern]```\n\n**Add a new password**\n\n```lpass add [Folder/sitename]```\n\n**Generate a password**\n\n```lpass generate [sitename] [20]```\n\n**Logout**\n\n```lpass logout```"],["SYNOPSIS","**lpass** _command_ [_options_] [_arguments_]"],["PARAMETERS","**login** _EMAIL_\n> Authenticate with LastPass.\n\n**logout**\n> End session and clear cache.\n\n**show** _NAME_\n> Display entry details.\n\n**ls** [_GROUP_]\n> List entries, optionally filtered by group.\n\n**add** _NAME_\n> Add new entry interactively.\n\n**edit** _NAME_\n> Modify existing entry.\n\n**rm** _NAME_\n> Delete entry.\n\n**generate** _NAME_ _LENGTH_\n> Generate and store random password.\n\n**sync**\n> Synchronize local cache with server.\n\n**export**\n> Export vault as CSV.\n\n**import** _FILE_\n> Import entries from CSV.\n\n**--password**\n> Show only password field.\n\n**--username**\n> Show only username field.\n\n**--url**\n> Show only URL field.\n\n**--clip**, **-c**\n> Copy to clipboard instead of printing.\n\n**-G**, **--basic-regexp**\n> Match using regular expression.\n\n**--sync=auto|now|no**\n> Control synchronization behavior.\n\n**--color=auto|always|never**\n> Color output control."],["DESCRIPTION","**lpass** is the command-line interface for LastPass password manager. It provides full access to your vault from the terminal, enabling scripted password retrieval and management.\n\nAuthentication creates an encrypted local cache. The agent keeps your session active, avoiding repeated master password entry. Session timeout is configurable via LPASS_AGENT_TIMEOUT environment variable.\n\nEntries are organized in folders with full paths (e.g., \"Work/email\"). Fields include username, password, URL, and notes. Secure notes store arbitrary text. Custom fields are supported.\n\nFor automation, **--clip** pipes to clipboard utilities, and **--password** outputs just the credential for use in scripts. The **generate** command creates random passwords meeting complexity requirements.\n\nSharing and enterprise features accessible via the web vault are partially supported. Two-factor authentication works with TOTP apps."],["CAVEATS","Requires internet for initial sync. Local cache is encrypted but exists on disk. Clipboard contents may be logged by some systems. Agent timeout affects security vs convenience tradeoff. Some enterprise features unavailable via CLI. LastPass data breaches (2022) raised security concerns."],["HISTORY","**lpass** was developed by **LastPass** and released as open source around **2014**. It provides command-line access to the LastPass service, which was founded in **2008**. LastPass was acquired by LogMeIn in **2015** and later sold to private equity. The CLI tool remains popular for automation and terminal-based workflows despite competition from tools like Bitwarden and 1Password."],["INSTALL","```dnf: sudo dnf install lastpass-cli```\n\n```apk: sudo apk add lastpass-cli```\n\n```brew: brew install lastpass-cli```\n\n```nix: nix profile install nixpkgs#lastpass-cli```"],["SEE ALSO","[pass](/man/pass)(1), [rbw](/man/rbw)(1), [bw](/man/bw)(1), [gopass](/man/gopass)(1), [keepassxc-cli](/man/keepassxc-cli)(1)"]],"e":["pass","rbw","bw","gopass","keepassxc-cli"],"x":"lpass login [user@example.com]"},"mdtt":{"n":"mdtt","t":"Markdown table editor TUI with vim-style keybindings","s":[["TAGLINE","Markdown table editor TUI with vim-style keybindings"],["TLDR","**Edit a markdown table** from a file\n\n```mdtt [path/to/file.md]```\n\n**Create a new table** interactively\n\n```mdtt```"],["SYNOPSIS","**mdtt** [_options_] [_file_]"],["DESCRIPTION","**mdtt** is a terminal-based Markdown table editor with vim-style keybindings. Navigation uses **hjkl** keys, **i** switches to insert mode for editing cell content, and **Esc** returns to normal mode.\n\nFeatures include adding rows and columns with **o**/**vo**, deleting with **dd**/**vd**, output to stdout and in-place file editing, delegating cell editing to an external **$EDITOR**, and HTML support within table cells for rich content formatting."],["CAVEATS","Only edits Markdown tables. Other Markdown content in the file is preserved but not editable through the TUI."],["HISTORY","**mdtt** was created by **szktkfm** and is written in **Go**."],["INSTALL","```aur: yay -S mdtt```"],["SEE ALSO","[vim](/man/vim)(1)"]],"e":["vim"],"x":"mdtt [path/to/file.md]"},"mendeley":{"n":"mendeley","t":"reference manager and academic social network","s":[["TAGLINE","reference manager and academic social network"],["TLDR","**Start Mendeley Desktop**\n\n```mendeleydesktop```\n\n**Open specific database**\n\n```mendeleydesktop --db [database.sqlite]```\n\n**Start minimized**\n\n```mendeleydesktop --start-minimized```"],["SYNOPSIS","**mendeleydesktop** [_options_]"],["PARAMETERS","**--db** _file_\n> Specify database file.\n\n**--start-minimized**\n> Start minimized to tray.\n\n**--help**\n> Show help."],["DESCRIPTION","**Mendeley** is a reference manager and academic social network. Mendeley Desktop helps researchers organize PDFs, generate citations, collaborate with others, and discover new research.\n\nIt syncs across devices and integrates with word processors for citation insertion."],["KEY FEATURES","```\n- PDF organization and annotation\n- Citation generation (BibTeX, etc.)\n- Word processor plugins\n- Cloud sync\n- Research discovery\n```"],["FILE LOCATIONS","```\n~/.local/share/data/Mendeley Ltd./Mendeley Desktop/\n```"],["CAVEATS","Requires Elsevier account. Sync storage limited on free tier. Desktop app being phased out for web version."],["HISTORY","Mendeley was founded in **2007** in London by **Victor Henning**, **Jan Reichelt**, and **Paul Foeckler**. It was acquired by **Elsevier** in **2013**."],["INSTALL","```nix: nix profile install nixpkgs#mendeley```"],["SEE ALSO","[zotero](/man/zotero)(1), [jabref](/man/jabref)(1), [papis](/man/papis)(1)"]],"e":["zotero","jabref","papis"],"x":"mendeleydesktop"},"mere.run":{"n":"mere.run","t":"Local multi-modal AI inference runtime and CLI","s":[["TAGLINE","Local multi-modal AI inference runtime and CLI"],["TLDR","**Inspect** the machine and list recommended models\n\n```mere.run setup```\n\n```mere.run model capabilities --recommended```\n\n**Pull** a small image model and **generate** an image\n\n```mere.run model pull image-zimage-nano```\n\n```mere.run image generate --model image-zimage-nano --prompt \"[a ceramic mug in soft morning light]\" --output [./mug.png]```\n\n**Chat** with a local text model\n\n```mere.run text chat --model [text-chat-gemma4-12b-4bit] --prompt \"[Explain unified memory.]\"```\n\n**Synthesize speech** to a WAV file\n\n```mere.run speech synthesize \"[Hello from mere.run]\" --output [./hello.wav]```\n\n**Serve** an OpenAI-compatible local API on loopback\n\n```mere.run api serve --engine [text-chat-gemma4]```\n\n**List** installed models and runtime status\n\n```mere.run model list```\n\n```mere.run status```"],["SYNOPSIS","**mere.run** [_global-options_] _command_ [_subcommand_] [_options_] [_args_...]"],["PARAMETERS","**setup**\n> Guided first-run setup for the local runtime and model store.\n\n**guide** [_command-path_]\n> Read packaged offline cookbooks for a command path; use **guide --list** to list them.\n\n**model** {_list_, _pull_, _remove_, _info_, _capabilities_, _runtime_, _benchmark_, ...}\n> Manage the local model store: pull by managed ID, inspect install status, and report what the machine can run.\n\n**image** {_generate_, _train-lora_, _run-plan_, _validate_, ...}\n> Local image generation, LoRA training, and related image workflows.\n\n**text** {_chat_, _code_, _embed_, _anonymize_, _train-lora_}\n> Local chat, code generation (llama.cpp), embeddings, and PII redaction.\n\n**speech** {_synthesize_, _transcribe_, _diarize_, _listen_, _profile_}\n> Text-to-speech, ASR, diarization, and voice profiles.\n\n**vision** {_caption_, _inspect_, _face_, _ground_, _segment_, _track_, _pose_, _ocr_, ...}\n> Captioning, VQA, face analysis, grounding, segmentation, tracking, pose, and OCR.\n\n**music** {_analyze_, _generate_, _realtime_, _transcribe_}\n> Music generation, analysis, realtime performance, and MIDI transcription.\n\n**sfx** {_generate_, _video_, _ae_, _clap_, ...}\n> Sound-effect generation, including video-conditioned Foley.\n\n**video** {_generate_, _animate_, _prepare-masks_, _session_, ...}\n> Local video generation and subject animation pipelines.\n\n**api serve**\n> Serve OpenAI-compatible chat, embeddings, images, TTS, and STT on a local host/port.\n\n**status**\n> Show local server, served model, model-store path, and installed models.\n\n**plugin** {_list_, _info_, _install_, _doctor_}\n> Discover and manage companion plugin executables.\n\n**--models-root** _path_\n> Override the local model store directory for this invocation.\n\n**--help**\n> Show the public command tree or help for a group/subcommand."],["DESCRIPTION","**mere.run** is a local-first inference runtime and public CLI for multi-modal generation and understanding. One command covers image, text, speech, vision, music, sound, video, 3D-related reconstruction helpers, model management, and an optional OpenAI-compatible API server. The optional macOS Studio app uses the same CLI, model store, and run history rather than a separate backend.\n\nThe CLI is modality-first: creative and analysis work lives under groups such as **image**, **text**, **speech**, **vision**, **music**, **sfx**, and **video**, while **model**, **adapter**, **api**, **status**, and **plugin** handle operations. Managed model IDs are pulled into a local store (by default under Application Support on macOS); use **model capabilities** before large downloads. Packaged offline cookbooks are available via **mere.run guide**.\n\nPrimary development targets Apple Silicon macOS; headless Linux CLI packages are supported for the runtime without the macOS SwiftUI studio. Media workflows typically expect **ffmpeg** and **ffprobe** on **PATH** (overridable with **MERERUN_FFMPEG** / **MERERUN_FFPROBE**)."],["CONFIGURATION","**~/Library/Application Support/MereRun/models**\n> Default local model store on macOS. Override with **MERERUN_MODELS_DIR** or **--models-root**.\n\n**MERERUN_HUB_CACHE** / **MERERUN_MODEL_CACHE_HOME**\n> Control the Hugging Face snapshot cache used by **model pull** and auto-download paths.\n\n**MERERUN_API_KEY**\n> API key for non-loopback **api serve** binds; loopback can run without auth by default.\n\n**MERERUN_FFMPEG** / **MERERUN_FFPROBE**\n> Absolute paths to ffmpeg/ffprobe when not discovered on **PATH**.\n\nUse **mere.run config** {_get_, _set_, _unset_, _list_, _path_} for runtime configuration next to the active model store."],["CAVEATS","Large models need substantial RAM/unified memory; check **model capabilities** before pulling. Licensing, download size, and hardware fit vary by model ID—see project model-sources docs before redistributing weights. Linux support is headless CLI-oriented; the SwiftUI studio and DMG installer are macOS-only. Binding **api serve** beyond loopback requires an explicit API key. Tool-loop and shell features in chat require explicit opt-in flags that weaken default safety posture."],["HISTORY","**mere.run** is an open-source Swift/MLX local inference runtime and CLI by **sawfwair**, packaging multi-modal creative workflows (image, text, speech, vision, audio, video) behind a single **mere.run** executable, with optional macOS Studio and Linux headless packages."],["SEE ALSO","[ollama](/man/ollama)(1), [llama.cpp](/man/llama.cpp)(1), [ffmpeg](/man/ffmpeg)(1)"],["RESOURCES","```[Source code](https://github.com/sawfwair/mere-run)```\n\n```[Homepage](https://mere.run)```\n\n```[Documentation](https://docs.mere.run/)```"]],"e":["ollama","llama.cpp","ffmpeg"],"x":"mere.run setup"},"moltbook-tui":{"n":"moltbook-tui","t":"TUI client for the Moltbook AI social network","s":[["TAGLINE","TUI client for the Moltbook AI social network"],["TLDR","**Launch the Moltbook TUI**\n\n```moltbook-tui```"],["SYNOPSIS","**moltbook-tui** [_options_]"],["DESCRIPTION","**moltbook-tui** is a terminal client for Moltbook, a Reddit-style social network where AI agents post, comment, and interact. The TUI provides a real-time feed with sections for New, Top, Discussed, and Random posts, along with threaded comment viewing and a leaderboard.\n\nFeatures include filtering the feed by time period (day, month, year, all time), browsing topic-based communities (submolts), viewing platform metrics, and vim-style navigation with mouse support."],["CAVEATS","Requires an internet connection and a Moltbook account. Content is generated by AI agents."],["HISTORY","**moltbook-tui** is written in **Rust**."],["SEE ALSO","[hackernews-tui](/man/hackernews-tui)(1), [lobtui](/man/lobtui)(1)"]],"e":["hackernews-tui","lobtui"],"x":"moltbook-tui"},"nix3-develop":{"n":"nix3-develop","t":"enters development shells","s":[["TAGLINE","enters development shells"],["TLDR","**Enter dev shell**\n\n```nix3 develop```\n\n**Develop from flake**\n\n```nix3 develop [.#devShell]```\n\n**Run command**\n\n```nix3 develop --command [make]```"],["SYNOPSIS","**nix3** **develop** [_options_] [_installable_]"],["PARAMETERS","_INSTALLABLE_\n> Flake reference.\n\n**--command** _CMD_\n> Run command in shell.\n\n**--impure**\n> Allow impure evaluation.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nix3 develop** enters development shells. Alternative name for nix develop.\n\nThe tool provides build dependencies. Based on flake devShells output."],["CAVEATS","Alias for nix develop. Flakes required. Environment setup."],["HISTORY","nix3 develop is the **new Nix CLI** command for reproducible development environments."],["SEE ALSO","[nix](/man/nix)(1), [nix-develop](/man/nix-develop)(1), [nix-shell](/man/nix-shell)(1)"]],"e":["nix","nix-develop","nix-shell"],"x":"nix3 develop"},"npm-dedupe":{"n":"npm-dedupe","t":"reduces duplication in the package tree by moving dependencies higher","s":[["TAGLINE","reduces duplication in the package tree by moving dependencies higher"],["TLDR","**Deduplicate dependencies**\n\n```npm dedupe```\n\n**Dry run (show what would change)**\n\n```npm dedupe --dry-run```\n\n**Include dev dependencies**\n\n```npm dedupe --include=dev```\n\n**Dedupe with legacy bundling**\n\n```npm dedupe --legacy-bundling```"],["SYNOPSIS","**npm dedupe** [_options_]"],["PARAMETERS","**--dry-run**\n> Show changes without applying.\n\n**--include** _type_\n> Include dependency type.\n\n**--omit** _type_\n> Omit dependency type.\n\n**--legacy-bundling**\n> Use legacy algorithm.\n\n**--strict-peer-deps**\n> Fail on peer dep issues."],["DESCRIPTION","**npm dedupe** reduces duplication in the package tree by moving dependencies higher in the tree where they can be shared. This can reduce node_modules size significantly.\n\nThe command restructures but doesn't change package-lock.json versions."],["EXAMPLE","```bash"],["Before dedupe","node_modules/\n├── a/\n│   └── node_modules/lodash@4.0.0\n└── b/\n    └── node_modules/lodash@4.0.0"],["After dedupe","node_modules/\n├── a/\n├── b/\n└── lodash@4.0.0\n```"],["CAVEATS","May not reduce size if version ranges differ. Run after npm install. Restructures tree only."],["HISTORY","npm dedupe was introduced to help manage the duplicate dependency problem common in npm's nested node_modules structure."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-prune](/man/npm-prune)(1), [npm-install](/man/npm-install)(1)"]],"e":["npm","npm-prune","npm-install"],"x":"npm dedupe"},"open.fish":{"n":"open.fish","t":"Fish shell function that opens files with the default application","s":[["TAGLINE","Fish shell function that opens files with the default application"],["TLDR","**Open a file with its default application**\n\n```open [file]```\n\n**Open a URL in the default browser**\n\n```open [https://example.com]```\n\n**Open a directory in the file manager**\n\n```open [directory]```\n\n**Open the current directory**\n\n```open .```\n\n**Open multiple files**\n\n```open [file1] [file2] [file3]```"],["SYNOPSIS","**open** [_file_...]"],["PARAMETERS","_FILE_\n> One or more files, directories, or URLs to open."],["DESCRIPTION","**open** is a built-in function shipped with the Fish shell that opens files, directories, and URLs with the user's default application, regardless of the host operating system. It is defined in **share/fish/functions/open.fish** in the Fish installation.\n\nOn macOS and Haiku the function delegates to the native **open** binary. On other Unix-like systems it dispatches to **xdg-open**, **gnome-open**, or **kde-open**, whichever is present. On systems lacking any of these helpers the function prints an error explaining that no opener is available.\n\nBecause it is implemented as a Fish function, it only exists inside interactive Fish sessions and is not available to scripts run with **sh**, **bash** or **zsh**."],["CAVEATS","Only defined inside the Fish shell; not an executable on **$PATH**. Behavior on Linux depends on the installed desktop helpers — on a minimal server without **xdg-open** the function will fail. On macOS, Fish prefers the system **open(1)** and bypasses any user-defined function named **open**."],["HISTORY","The **open** function has shipped with Fish since its early releases as a portable alternative to macOS's **open(1)** and Linux's **xdg-open**. It is updated along with Fish itself."],["SEE ALSO","[open](/man/open)(1), [xdg-open](/man/xdg-open)(1), [fish](/man/fish)(1)"]],"e":["open","xdg-open","fish"],"x":"open [file]"},"openparser":{"n":"openparser","t":"Parse PDFs and images via the OpenParser API","s":[["TAGLINE","Parse PDFs and images via the OpenParser API"],["TLDR","**Install** the CLI and log in\n\n```npm install -g @openparser/cli && openparser auth login```\n\n**Parse** a document synchronously as JSON\n\n```openparser parse sync [path/to/file.pdf] --json```\n\n**Extract** structured fields with a JSON schema\n\n```openparser extract sync [path/to/file.pdf] --schema [path/to/schema.json]```\n\n**Check** API connectivity and active profile\n\n```openparser status --json```\n\n**List** recent jobs\n\n```openparser jobs list --status succeeded --limit 10```\n\n**Upload** a file for reuse across jobs\n\n```openparser files upload [path/to/file.pdf]```"],["SYNOPSIS","**openparser** _command_ [_subcommand_] [_options_] [_args_]"],["DESCRIPTION","**openparser** is the terminal client for OpenParser, a document intelligence API that turns PDFs and images into text, layout, and schema-constrained structured data. The CLI is aimed at interactive use, CI jobs, and agent tooling.\n\nStdout carries data payloads (JSON, tables, file bytes); stderr carries status lines. Prefer **--json** when scripting, and **--quiet** / **-q** to silence non-error status on stderr.\n\nAuthentication uses saved profiles (**openparser auth login**) or **OPENPARSER_API_KEY** (and optional **OPENPARSER_BASE_URL**, **OPENPARSER_PROFILE**) for headless environments. Flag options override environment variables, which override the active profile."],["COMMANDS","**status**\n\n> Verify API connectivity and profile context.\n\n**auth**\n\n> Profile management (**login**, **use**, named profiles for staging/production).\n\n**models**\n\n> List OCR and LLM model catalogs.\n\n**parse**\n\n> Synchronous, async, and batch parse admission (**parse sync**, **parse batch**, ...).\n\n**extract**\n\n> Schema-constrained extraction, batch extract, and schema suggestion.\n\n**jobs**\n\n> List jobs and fetch status, results, and source bytes.\n\n**files**\n\n> Upload and reuse pooled files.\n\n**pipelines**\n\n> Manage saved extraction pipelines."],["CAVEATS","Requires network access to the OpenParser API (default **https://api.openparser.dev**) and a valid API key or login. This CLI is a cloud client, not an offline local OCR engine."],["SEE ALSO","[curl](/man/curl)(1), [jq](/man/jq)(1), [pdftotext](/man/pdftotext)(1)"],["RESOURCES","```[Source code](https://github.com/eigenpal/openparser)```\n\n```[Homepage](https://openparser.dev)```\n\n```[Documentation](https://docs.openparser.dev)```"]],"e":["curl","jq","pdftotext"],"x":"npm install -g @openparser/cli && openparser auth login"},"perplexity":{"n":"perplexity","t":"command-line interface for Perplexity AI, enabling terminal-based","s":[["TAGLINE","command-line interface for Perplexity AI, enabling terminal-based"],["TLDR","**Ask a question** to Perplexity AI\n\n```perplexity \"What is quantum computing?\"```\n\n**Ask with a specific model**\n\n```perplexity -m [sonar-pro] \"[question]\"```\n\n**Display citations** with the answer\n\n```perplexity -c \"[question]\"```\n\n**Show token usage** statistics\n\n```perplexity -u \"[question]\"```\n\n**Search across notes and chats** with RAG\n\n```perplexity rag \"[query]\"```\n\n**Set up API key** and configuration\n\n```perplexity setup```"],["SYNOPSIS","**perplexity** [_options_] \"_question_\"\n\n**perplexity** _command_ [_options_]"],["PARAMETERS","**-m**, **--model** _model_\n> Select language model: sonar, sonar-pro, sonar-reasoning, sonar-reasoning-pro.\n\n**-c**, **--citations**\n> Display source citations with the answer.\n\n**-u**, **--usage**\n> Show token consumption metrics.\n\n**-v**, **--verbose**\n> Enable debug mode with detailed output.\n\n**-g**, **--glow**\n> Apply Glow-compatible markdown formatting.\n\n**-a**, **--api-key** _key_\n> Provide API key directly instead of environment variable."],["COMMANDS","**ask** _question_\n> Query Perplexity AI with optional model and topic parameters.\n\n**rag** _query_\n> Search across notes and chat history using RAG. Supports --mode (vector/keyword) and --source (notes/chats).\n\n**rag-migrate**\n> Migrate existing data to RAG knowledge base.\n\n**rag-stats**\n> View knowledge base statistics.\n\n**history**\n> Display chat history.\n\n**note** --title _title_ --content _content_\n> Create a new note with optional tags.\n\n**list-notes**\n> Display all saved notes.\n\n**export-chat** _id_ --format _format_\n> Export conversation to markdown, JSON, or Excel.\n\n**setup**\n> Configure API key and settings interactively."],["DESCRIPTION","**perplexity** is a command-line interface for Perplexity AI, enabling terminal-based access to their AI-powered search and question-answering capabilities. It leverages Perplexity's Sonar models which combine large language models with real-time web search.\n\nThe CLI supports multiple models ranging from **sonar** (fastest) to **sonar-reasoning-pro** (most capable with deep reasoning). Answers can include citations showing the sources used to generate responses.\n\nAdvanced features include RAG (Retrieval Augmented Generation) for searching across local notes and chat history using semantic vector search or keyword matching. The knowledge base uses BGE embeddings for lightning-fast semantic search.\n\nChat history and notes are stored locally, enabling export to markdown, JSON, or Excel formats. The tool works offline for local operations while requiring internet connectivity for AI queries."],["CAVEATS","Requires a **PERPLEXITY_API_KEY** environment variable with a valid API key from Perplexity. API usage incurs costs based on the model selected and tokens consumed. Some features require Python 3.12 or higher."],["HISTORY","Perplexity AI was founded in **2022** by former Google and Meta engineers, launching as an AI-powered search engine. The company introduced their API in **2024**, enabling programmatic access to their Sonar models. Community CLI tools emerged to provide terminal access, with various implementations supporting different feature sets from simple queries to advanced RAG capabilities."],["INSTALL","```aur: yay -S perplexity```"],["SEE ALSO","[anthropic](/man/anthropic)(1), [ollama](/man/ollama)(1), [llm](/man/llm)(1)"]],"e":["anthropic","ollama","llm"],"x":"perplexity \"What is quantum computing?\""},"pngquant":{"n":"pngquant","t":"Lossy PNG compression via color quantization","s":[["TAGLINE","Lossy PNG compression via color quantization"],["TLDR","**Compress PNG with quality range**\n\n```pngquant --quality=[65-80] [image.png]```\n\n**Compress to specific output file**\n\n```pngquant [image.png] -o [output.png]```\n\n**Compress and overwrite original**\n\n```pngquant --ext=.png --force [image.png]```\n\n**Compress with specific color count**\n\n```pngquant [256] [image.png]```\n\n**Compress multiple files**\n\n```pngquant [*.png]```\n\n**Compress with maximum quality**\n\n```pngquant --quality=[90-100] [image.png]```\n\n**Skip if quality cannot be met**\n\n```pngquant --skip-if-larger [image.png]```\n\n**Output to stdout** (for piping)\n\n```pngquant - < [input.png] > [output.png]```"],["SYNOPSIS","**pngquant** [_--quality min-max_] [_--speed N_] [_--output file_] [_--ext suffix_] [_colors_] _files_"],["PARAMETERS","**--quality** _MIN_-_MAX_\n> Quality range (0-100). Skip if quality below MIN.\n\n**-o**, **--output** _FILE_\n> Output file name.\n\n**--ext** _SUFFIX_\n> Set output filename suffix (default: -or8.png or -fs8.png depending on dithering).\n\n**-f**, **--force**\n> Overwrite existing files.\n\n**-s** _N_, **--speed** _N_\n> Speed/quality tradeoff (1=slowest/best, 10=fastest; default 3).\n\n**--skip-if-larger**\n> Don't write if output larger than input.\n\n**--strip**\n> Remove metadata.\n\n**--posterize** _BITS_\n> Reduce precision of color channels.\n\n**--nofs**\n> Disable Floyd-Steinberg dithering.\n\n**--floyd**=_LEVEL_\n> Set dithering level (0.0-1.0).\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-**\n> Read from stdin, write to stdout.\n\n_NUM_\n> Maximum number of colors (default: 256)."],["DESCRIPTION","**pngquant** compresses PNG images by reducing the number of colors using lossy quantization. It converts 24/32-bit PNG to 8-bit palette PNG, dramatically reducing file size while maintaining visual quality.\n\nThe algorithm uses median-cut quantization with dithering to hide color reduction artifacts. Quality settings control how aggressively colors are reduced - higher quality preserves more detail but produces larger files.\n\nQuality range (e.g., 65-80) sets minimum acceptable quality. If the minimum cannot be achieved, pngquant exits without writing (exit code 99). This prevents unacceptably degraded images.\n\nSpeed setting trades processing time for output quality. Slower speeds find better color palettes. For automated pipelines, moderate speeds (3-4) offer good balance.\n\nThe tool excels at compressing graphics, icons, and images with limited colors. Photographs may show more visible quality loss due to the 256-color limit."],["CAVEATS","Lossy compression - some quality is lost. 256 color maximum limits complex images. Photographs may not compress well. Original transparency is preserved but reduced to 8-bit. Metadata stripped by default. Results vary significantly by image content."],["HISTORY","**pngquant** was created by **Greg Roelofs** around **1997** with the original PNG specification work. The project was later taken over by **Kornel Lesinski** who rewrote it with the libimagequant library around **2011**. It became essential for web optimization, used in build tools and image CDNs to reduce bandwidth."],["INSTALL","```apt: sudo apt install pngquant```\n\n```dnf: sudo dnf install pngquant```\n\n```pacman: sudo pacman -S pngquant```\n\n```apk: sudo apk add pngquant```\n\n```zypper: sudo zypper install pngquant```\n\n```brew: brew install pngquant```\n\n```nix: nix profile install nixpkgs#pngquant```"],["SEE ALSO","[optipng](/man/optipng)(1), [oxipng](/man/oxipng)(1), [imagemagick](/man/imagemagick)(1), [jpegoptim](/man/jpegoptim)(1)"]],"e":["optipng","oxipng","imagemagick","jpegoptim"],"x":"pngquant --quality=[65-80] [image.png]"},"ppmtoyuv":{"n":"ppmtoyuv","t":"Convert PPM to Abekas YUV format","s":[["TAGLINE","Convert PPM to Abekas YUV format"],["TLDR","**Convert PPM to Abekas YUV format**\n\n```ppmtoyuv [input.ppm] > [output.yuv]```"],["SYNOPSIS","**ppmtoyuv** [_file_]"],["DESCRIPTION","**ppmtoyuv** converts PPM images to Abekas YUV format for video equipment. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[yuvtoppm](/man/yuvtoppm)(1), [ppmtoeyuv](/man/ppmtoeyuv)(1)"]],"e":["yuvtoppm","ppmtoeyuv"],"x":"ppmtoyuv [input.ppm] > [output.yuv]"},"prisma":{"n":"prisma","t":"Node.js ORM with type-safe database access","s":[["TAGLINE","Node.js ORM with type-safe database access"],["TLDR","**Initialize Prisma**\n\n```npx prisma init```\n\n**Generate client**\n\n```npx prisma generate```\n\n**Run migrations**\n\n```npx prisma migrate dev```\n\n**Push schema to database**\n\n```npx prisma db push```\n\n**Open Prisma Studio**\n\n```npx prisma studio```"],["SYNOPSIS","**prisma** _command_ [_options_]"],["PARAMETERS","**init**\n> Initialize Prisma in project.\n\n**generate**\n> Generate Prisma Client.\n\n**migrate dev**\n> Create and apply migrations.\n\n**migrate deploy**\n> Apply pending migrations.\n\n**db push**\n> Push schema without migration.\n\n**db pull**\n> Introspect database.\n\n**studio**\n> Open visual editor.\n\n**format**\n> Format schema file."],["DESCRIPTION","**Prisma** is a next-generation ORM for Node.js and TypeScript. It provides type-safe database access, automated migrations, and a visual database browser."],["EXAMPLES","```bash"],["Initialize new project","npx prisma init"],["After schema changes","npx prisma generate"],["Development migration","npx prisma migrate dev --name add_users"],["Production deployment","npx prisma migrate deploy"],["Reset database","npx prisma migrate reset"],["Introspect existing DB","npx prisma db pull\n```"],["SCHEMA EXAMPLE","```prisma\nmodel User {\n  id    Int     @id @default(autoincrement())\n  email String  @unique\n  name  String?\n  posts Post[]\n}\n```"],["CAVEATS","Requires Node.js. Schema in schema.prisma. Generate after schema changes."],["HISTORY","Prisma was founded by **Johannes Schickling** and **Søren Bramer Schmidt** in 2016, evolving from Graphcool."],["SEE ALSO","[sequelize](/man/sequelize)(1), [typeorm](/man/typeorm)(1), [knex](/man/knex)(1)"]],"e":["sequelize","typeorm","knex"],"x":"npx prisma init"},"pvmove":{"n":"pvmove","t":"Migrate LVM data between physical volumes","s":[["TAGLINE","Migrate LVM data between physical volumes"],["TLDR","**Move data between physical volumes**\n\n```pvmove [/dev/sda1] [/dev/sdb1]```\n\n**Move all data from PV**\n\n```pvmove [/dev/sda1]```\n\n**Show progress**\n\n```pvmove -v [/dev/sda1]```\n\n**Abort move**\n\n```pvmove --abort```"],["SYNOPSIS","**pvmove** [_options_] _source_ [_dest_]"],["PARAMETERS","_SOURCE_\n> Source physical volume.\n\n_DEST_\n> Destination physical volume.\n\n**-v**\n> Verbose output.\n\n**--abort**\n> Abort in-progress move.\n\n**-b**\n> Background mode.\n\n**-n** _LV_\n> Move only this LV."],["DESCRIPTION","**pvmove** migrates data between LVM physical volumes while the logical volumes remain online and accessible. This enables live storage migration, allowing administrators to replace disks, rebalance storage, or evacuate a physical volume before removing it from a volume group.\n\nWhen called with a source and destination physical volume, pvmove relocates all physical extents from the source to the destination. If no destination is specified, LVM automatically distributes the data across available physical volumes in the same volume group. The **-n** flag restricts the move to a specific logical volume. Operations can be run in the background with **-b** and aborted with **--abort** if needed."],["CAVEATS","Requires LVM. Can be slow for large volumes. Interruptible."],["HISTORY","pvmove is part of **LVM2** for physical volume data migration."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvm](/man/lvm)(8), [pvcreate](/man/pvcreate)(8), [vgreduce](/man/vgreduce)(8)"]],"e":["lvm","pvcreate","vgreduce"],"x":"pvmove [/dev/sda1] [/dev/sdb1]"},"rc":{"n":"rc","t":"Plan 9 command shell for Unix","s":[["TAGLINE","Plan 9 command shell for Unix"],["TLDR","**Start an interactive rc shell**\n\n```rc```\n\n**Execute a script**\n\n```rc [script.rc]```\n\n**Execute a command string**\n\n```rc -c '[command]'```\n\n**Start rc without reading profile**\n\n```rc -n```\n\n**Start a login shell**\n\n```rc -l```"],["SYNOPSIS","**rc** [**-einvxl**] [**-c** _command_] [_file_ [_arguments_]]"],["PARAMETERS","**-c** _command_\n> Execute the given command string and exit\n\n**-e**\n> Exit if any command returns non-zero status\n\n**-i**\n> Run interactively (read commands from terminal)\n\n**-l**\n> Behave as a login shell (read profile)\n\n**-n**\n> Do not read the personal profile ($home/lib/profile)\n\n**-v**\n> Echo input to standard error as it is read\n\n**-x**\n> Print commands and arguments as they are executed"],["DESCRIPTION","**rc** is the Plan 9 command interpreter, offering a cleaner alternative to the Bourne shell. Originally designed for Plan 9 from Bell Labs, it has been ported to Unix systems and provides a simpler, more consistent syntax.\n\nUnlike the Bourne shell's string variables, rc provides list-valued variables (arrays of strings) as a fundamental type. This eliminates many quoting problems and makes argument handling more natural.\n\nRc uses C-like syntax for control structures: **if**, **while**, **for**, **switch**, and **fn** for function definitions. The syntax is defined by a formal grammar (implemented with yacc), making it unambiguous and predictable.\n\nPipes, redirections, and command substitution work similarly to other shells. Background execution uses **&**, and command grouping uses **{}** instead of subshells."],["SYNTAX DIFFERENCES FROM BOURNE SHELL","**Variables**: `var=value` becomes `var=value` (same), but `$var` expands to a list\n**Lists**: `list=(a b c)` defines a list, accessed as `$list(1)`, `$list(2)`, etc.\n**Functions**: `fn name { commands }` instead of `name() { commands }`\n**If/else**: `if(test) cmd` and `if not cmd` or `else cmd`\n**For loop**: `for(i in list) cmd`\n**Quoting**: Single quotes only, doubled to include literal quote: `'it''s'`"],["CAVEATS","Rc is not POSIX-compatible. Scripts written for bash or sh will not run correctly in rc without modification.\n\nThe Unix ports (Rakitzis implementation and Plan 9 from User Space) have slight differences from the original Plan 9 rc, particularly in profile locations and some features.\n\nLimited availability in default package repositories. May require building from source or installing Plan 9 ports."],["HISTORY","Rc was created by **Tom Duff** at Bell Labs in **1989** for the Research Unix Version 10 and later became the standard shell for Plan 9. The name stands for \"run commands.\" Byron Rakitzis created a popular Unix reimplementation in **1991**, which continues to be maintained. Duff is also known for \"Duff's device,\" an optimization technique in C."],["INSTALL","```dnf: sudo dnf install rc```\n\n```nix: nix profile install nixpkgs#rc```"],["SEE ALSO","[sh](/man/sh)(1), [bash](/man/bash)(1), [zsh](/man/zsh)(1), [es](/man/es)(1)"]],"e":["sh","bash","zsh","es"],"x":"rc"},"sfdk-deploy":{"n":"sfdk-deploy","t":"Deploy Sailfish apps to devices or emulators","s":[["TAGLINE","Deploy Sailfish apps to devices or emulators"],["TLDR","**Deploy application to device**\n\n```sfdk deploy```\n\n**Deploy with manual method**\n\n```sfdk deploy --manual```\n\n**Deploy with zypper dup method**\n\n```sfdk deploy --zypper-dup```\n\n**Preview deployment without applying**\n\n```sfdk deploy --dry-run```"],["SYNOPSIS","**sfdk deploy** [_options_]"],["PARAMETERS","**--sdk**\n> Deploy via SDK connection.\n\n**--manual**\n> Manual deployment (copy RPMs and install).\n\n**--rsync**\n> Use rsync for deployment.\n\n**--zypper-dup**\n> Deploy using zypper distribution upgrade.\n\n**--dry-run**\n> Preview deployment without actually applying changes."],["DESCRIPTION","**sfdk deploy** deploys built Sailfish OS application packages to devices or emulators previously registered within the Sailfish IDE. It always deploys the latest built version found in the output directory while obeying package dependencies. Multiple deployment methods are available depending on the target setup."],["SEE ALSO","[sfdk](/man/sfdk)(1), [sfdk-prepare](/man/sfdk-prepare)(1), [sfdk-undeploy](/man/sfdk-undeploy)(1)"]],"e":["sfdk","sfdk-prepare","sfdk-undeploy"],"x":"sfdk deploy"},"sidecar":{"n":"sidecar","t":"TUI dashboard for AI coding agents","s":[["TAGLINE","TUI dashboard for AI coding agents"],["TLDR","**Launch from any project directory**\n\n```sidecar```\n\n**Specify a project root**\n\n```sidecar --project [/path/to/project]```"],["SYNOPSIS","**sidecar** [**--project** _path_] [**--debug**] [**--version**]"],["DESCRIPTION","**sidecar** is a terminal companion that runs alongside AI coding agents like Claude Code and Codex. It provides a split-pane interface showing git diffs, file trees, conversation history, and task management. You can plan tasks, review diffs, stage commits, and manage workspaces without leaving the terminal."],["HISTORY","**sidecar** was created by **marcus** and is written in **Go**."],["SEE ALSO","[lazygit](/man/lazygit)(1), [tig](/man/tig)(1), [gitui](/man/gitui)(1)"]],"e":["lazygit","tig","gitui"],"x":"sidecar"},"soffice":{"n":"soffice","t":"LibreOffice suite launcher and converter","s":[["TAGLINE","LibreOffice suite launcher and converter"],["TLDR","**Open LibreOffice**\n\n```soffice```\n\n**Open document**\n\n```soffice [document.odt]```\n\n**Convert to PDF**\n\n```soffice --convert-to pdf [document.docx]```\n\n**Headless conversion**\n\n```soffice --headless --convert-to pdf [document.docx]```\n\n**Start Writer**\n\n```soffice --writer```\n\n**Start Calc**\n\n```soffice --calc```\n\n**Start in safe mode**\n\n```soffice --safe-mode```"],["SYNOPSIS","**soffice** [_--headless_] [_--convert-to format_] [_options_] [_files_]"],["PARAMETERS","**--headless**\n> No GUI.\n\n**--convert-to** _FMT_\n> Convert format.\n\n**--writer**\n> Start Writer.\n\n**--calc**\n> Start Calc.\n\n**--impress**\n> Start Impress.\n\n**--safe-mode**\n> Safe mode start.\n\n**--outdir** _DIR_\n> Output directory."],["DESCRIPTION","**soffice** is the main launcher for LibreOffice, the open-source office productivity suite. It can open documents in Writer (word processing), Calc (spreadsheets), Impress (presentations), Draw (vector graphics), and other components based on the file type or explicit module selection.\n\nThe **--headless** flag runs LibreOffice without a graphical interface, enabling server-side document processing and scripting. Combined with **--convert-to**, it provides powerful batch document conversion between formats including PDF, DOCX, XLSX, HTML, and many others.\n\nHeadless mode is commonly used in automated pipelines to convert documents, generate PDFs from templates, or perform mail merges without user interaction. Multiple files can be processed in a single invocation, and the output directory can be specified with **--outdir**."],["CAVEATS","Heavy application. Conversion may be slow. Format fidelity varies."],["HISTORY","**soffice** is the main launcher for **LibreOffice**, the free office suite forked from OpenOffice.org."],["INSTALL","```apk: sudo apk add libreoffice-common```"],["SEE ALSO","[libreoffice](/man/libreoffice)(1), [lowriter](/man/lowriter)(1), [unoconv](/man/unoconv)(1)"]],"e":["libreoffice","lowriter","unoconv"],"x":"soffice"},"spring":{"n":"spring","t":"Spring Boot application framework CLI","s":[["TAGLINE","Spring Boot application framework CLI"],["TLDR","**Create new project** (via start.spring.io)\n\n```spring init --dependencies=[web,data-jpa,postgresql] [myapp]```\n\n**Create with specific options**\n\n```spring init --build=[gradle] --java-version=[17] --packaging=[war] [myapp.zip]```\n\n**List available dependencies and options**\n\n```spring init --list```\n\n**Encode a password** using bcrypt\n\n```spring encodepassword [password]```\n\n**Run Spring Boot application** with Maven wrapper\n\n```./mvnw spring-boot:run```\n\n**Run with Gradle** wrapper\n\n```./gradlew bootRun```\n\n**Package as executable JAR**\n\n```./mvnw package```\n\n**Show CLI version**\n\n```spring --version```"],["SYNOPSIS","**spring** _command_ [_options_] [_args_...]\n\n**./mvnw** spring-boot:_goal_\n\n**./gradlew** boot_Task_"],["SPRING CLI COMMANDS","**init** [_options_] [_location_]\n> Create new project from start.spring.io.\n\n**encodepassword** [_options_] _password_\n> Encode a password for use with Spring Security.\n\n**shell**\n> Start interactive shell.\n\n**version**\n> Show CLI version.\n\n**help** [_command_]\n> Display help."],["INIT OPTIONS","**-d**, **--dependencies** _list_\n> Comma-separated list of dependency identifiers.\n\n**-b**, **--build** _type_\n> Build system: maven (default), gradle.\n\n**-j**, **--java-version** _version_\n> Java version: 17, 21.\n\n**-l**, **--language** _lang_\n> Language: java, kotlin, groovy.\n\n**-n**, **--name** _name_\n> Project name.\n\n**-g**, **--group-id** _group_\n> Maven group ID.\n\n**-a**, **--artifact-id** _artifact_\n> Maven artifact ID.\n\n**-p**, **--packaging** _type_\n> Packaging: jar (default), war.\n\n**-v**, **--version** _version_\n> Project version (e.g. '0.0.1-SNAPSHOT').\n\n**--boot-version** _version_\n> Spring Boot version.\n\n**-x**, **--extract**\n> Extract the project archive.\n\n**-f**, **--force**\n> Force overwrite of existing files.\n\n**--list**\n> List available options and dependencies."],["MAVEN/GRADLE GOALS","**spring-boot:run** / **bootRun**\n> Run application.\n\n**spring-boot:build-image** / **bootBuildImage**\n> Build OCI container image.\n\n**package** / **bootJar**\n> Create executable JAR.\n\n**spring-boot:start** / **bootStart**\n> Start application in background.\n\n**spring-boot:stop** / **bootStop**\n> Stop background application."],["DESCRIPTION","**spring** CLI and Spring Boot Maven/Gradle plugins manage Spring Boot applications. The CLI scaffolds new projects and encodes passwords; plugins handle building and running. The **run** command was removed in Spring Boot 3.0.\n\n**spring init** fetches project templates from start.spring.io with selected dependencies. Dependencies like web, data-jpa, security add starters that auto-configure functionality.\n\nRunning applications during development uses **bootRun** (Gradle) or **spring-boot:run** (Maven). DevTools enables hot reload when included as dependency.\n\n**bootJar** / **package** creates an executable JAR with embedded server. Run with **java -jar app.jar**. **bootBuildImage** creates OCI container images without Dockerfile.\n\nNative image support via GraalVM compiles to native executables with **-Pnative** profile. Requires GraalVM and produces fast-startup binaries."],["CAVEATS","The **spring run** command was removed in Spring Boot 3.0; use Maven/Gradle plugins to run applications. CLI is primarily for project creation. Auto-configuration may conflict with custom config. Native image requires additional metadata. DevTools should be excluded from production."],["HISTORY","Spring Boot was created by **Pivotal** (now VMware) team led by **Phil Webb** and released in **2014**. It simplified Spring Framework development with convention over configuration. Spring Boot 2.0 (2018) updated to Spring Framework 5. Version 3.0 (2022) required Java 17, removed the CLI **run** command, and added native compilation support. Spring Boot is the most widely used Java application framework."],["INSTALL","```pacman: sudo pacman -S spring```"],["SEE ALSO","[mvn](/man/mvn)(1), [gradle](/man/gradle)(1), [quarkus](/man/quarkus)(1)"]],"e":["mvn","gradle","quarkus"],"x":"spring init --dependencies=[web,data-jpa,postgresql] [myapp]"},"stressapptest":{"n":"stressapptest","t":"Google memory and hardware stress tester","s":[["TAGLINE","Google memory and hardware stress tester"],["TLDR","**Run memory stress**\n\n```stressapptest -s [60]```\n\n**With specific memory**\n\n```stressapptest -M [1024] -s [60]```\n\n**File test**\n\n```stressapptest -f [/tmp/testfile] -s [60]```\n\n**Verbose output**\n\n```stressapptest -v [2] -s [60]```\n\n**Multiple memory copy threads**\n\n```stressapptest -m [4] -s [60]```\n\n**With CPU stress threads**\n\n```stressapptest -C [4] -s [60]```\n\n**Direct disk test** on a block device\n\n```stressapptest -d [/dev/sda] -s [60]```"],["SYNOPSIS","**stressapptest** [_-s seconds_] [_-M mbytes_] [_-m threads_] [_options_]"],["PARAMETERS","**-s** _SEC_\n> Duration seconds.\n\n**-M** _MB_\n> Memory in MB.\n\n**-m** _N_\n> Number of memory copy threads.\n\n**-C** _N_\n> Number of CPU stress threads.\n\n**-i** _N_\n> Number of memory invert threads.\n\n**-f** _FILE_\n> Add a disk thread with tempfile.\n\n**-d** _DEV_\n> Add a direct write disk thread with block device (or file).\n\n**-n** _IP_\n> Add a network thread connecting to the specified IP address.\n\n**-v** _LEVEL_\n> Verbosity level.\n\n**-l** _FILE_\n> Log file.\n\n**-W**\n> Use warm copy (memory copy with cache warming).\n\n**-A**\n> Run in degraded mode on incompatible systems."],["DESCRIPTION","**stressapptest** (Stressful Application Test) is a memory and hardware verification tool developed by Google to find errors in memory subsystems, disk I/O, and other hardware components. It generates realistic high-load patterns that exercise the memory bus, cache hierarchy, and storage pathways to expose intermittent hardware faults.\n\nThe tool allocates a configurable amount of memory and continuously writes data patterns, reads them back, and verifies correctness. This approach is effective at detecting ECC memory errors, faulty DIMM modules, memory controller issues, and thermal throttling problems. File I/O tests can be included to stress disk and storage controller hardware simultaneously.\n\nStressapptest is particularly useful for qualifying new hardware, burn-in testing servers before production deployment, and diagnosing systems that exhibit intermittent crashes or data corruption under load."],["CAVEATS","Generates heat. May trigger failures. Use carefully on production."],["HISTORY","**stressapptest** was developed by **Google** to stress test memory subsystems and find hardware errors."],["INSTALL","```apt: sudo apt install stressapptest```\n\n```dnf: sudo dnf install stressapptest```\n\n```zypper: sudo zypper install stressapptest```\n\n```nix: nix profile install nixpkgs#stressapptest```"],["SEE ALSO","[stress](/man/stress)(1), [memtester](/man/memtester)(1), [mprime](/man/mprime)(1)"]],"e":["stress","memtester","mprime"],"x":"stressapptest -s [60]"},"sysdig":{"n":"sysdig","t":"System-level event capture and analysis","s":[["TAGLINE","System-level event capture and analysis"],["TLDR","**Capture** all events from live system\n\n```sysdig```\n\n**Save** events to file\n\n```sysdig -w [path/to/file].scap```\n\n**Read** events from file\n\n```sysdig -r [path/to/file].scap```\n\n**Filter** events by process and event type\n\n```sysdig proc.name=cat and evt.type=open```\n\n**List** available chisels\n\n```sysdig -cl```\n\n**Use** a chisel to analyze traffic\n\n```sysdig -c spy_ip [ip_address]```"],["SYNOPSIS","**sysdig** [_options_] [_filter_]"],["PARAMETERS","**-w, --write _file_**\n> Write captured events to file\n\n**-r, --read _file_**\n> Read events from file\n\n**-c, --chisel _name_**\n> Run specified chisel\n\n**-cl, --list-chisels**\n> List available chisels\n\n**-l, --list**\n> List available fields\n\n**-I _source_**\n> Use specified input source\n\n**-n _num_**\n> Stop after num events\n\n**-p _format_**\n> Custom output format\n\n**-j, --json**\n> Output in JSON format\n\n**-A, --print-ascii**\n> Print event buffers as ASCII (skip non-printable bytes).\n\n**-X, --print-hex-ascii**\n> Print event buffers as hex + ASCII.\n\n**-s** _len_, **--snaplen** _len_\n> Capture the first _len_ bytes of each I/O buffer (default 80).\n\n**-pc**, **-pcontainer**\n> Use container-aware output formatting (host + container.id, container.name).\n\n**-M** _seconds_\n> Stop capturing after the given number of seconds.\n\n**--list-fields**\n> List all filter/format fields available."],["DESCRIPTION","**sysdig** is a system-level exploration and troubleshooting tool. It captures system calls and events, providing deep visibility into system behavior. It supports filtering, chisels (Lua scripts for analysis), and various output formats.\n\nSysdig can be used for performance analysis, security monitoring, and debugging."],["FILTER SYNTAX","Filters use **field=value** syntax with AND, OR, and NOT operators:\n- proc.name=nginx\n- evt.type=open and fd.name contains /etc\n- container.id!=host"],["CAVEATS","Requires root privileges for live capture. Kernel headers may be needed for certain features. Generates significant data on busy systems."],["INSTALL","```pacman: sudo pacman -S sysdig```\n\n```zypper: sudo zypper install sysdig```\n\n```brew: brew install sysdig```\n\n```nix: nix profile install nixpkgs#sysdig```"],["SEE ALSO","[strace](/man/strace)(1), [tcpdump](/man/tcpdump)(8), [perf](/man/perf)(1)"]],"e":["strace","tcpdump","perf"],"x":"sysdig"},"systemctl-daemon-reexec":{"n":"systemctl-daemon-reexec","t":"Re-execute systemd manager binary","s":[["TAGLINE","Re-execute systemd manager binary"],["TLDR","**Reexecute** systemd manager\n\n```systemctl daemon-reexec```"],["SYNOPSIS","**systemctl daemon-reexec**"],["DESCRIPTION","**systemctl daemon-reexec** reexecutes the systemd manager (PID 1), replacing the running systemd binary with a new copy while preserving the current state of all units. This is used after upgrading the systemd package to apply the new binary without rebooting.\n\nUnlike `daemon-reload`, which only reloads configuration, `daemon-reexec` replaces the entire systemd process. All running services continue undisturbed during the transition."],["CAVEATS","Requires root privileges. Should only be used after systemd package upgrades. Service states are preserved but any in-flight transactions may be affected. Not typically needed during normal operation."],["HISTORY","The **daemon-reexec** subcommand enables live upgrades of systemd itself without requiring a system reboot, supporting modern zero-downtime maintenance practices."],["SEE ALSO","[systemctl-daemon-reload](/man/systemctl-daemon-reload)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-daemon-reload","systemctl"],"x":"systemctl daemon-reexec"},"timg":{"n":"timg","t":"Display images and videos in terminal","s":[["TAGLINE","Display images and videos in terminal"],["TLDR","**Display an image** in the terminal\n\n```timg [path/to/image.png]```\n\n**Display multiple images** in a grid\n\n```timg --grid=2 [image1.png] [image2.png]```\n\n**Play a video** in the terminal\n\n```timg [path/to/video.mp4]```\n\n**Display an animated GIF** looping forever\n\n```timg --loops=-1 [animation.gif]```\n\n**Display image from stdin**\n\n```curl -s [url/to/image.png] | timg -```\n\n**Use Kitty graphics protocol** for high resolution\n\n```timg -pk [path/to/image.png]```\n\n**Display with specific dimensions**\n\n```timg -g [80x24] [path/to/image.png]```"],["SYNOPSIS","**timg** [_options_] _file_ [_file_...]"],["PARAMETERS","**-g** _WxH_\n> Output geometry in character cells. Default is terminal size.\n\n**-p** _mode_\n> Pixelation mode: h (half blocks), q (quarter blocks), k (kitty protocol), i (iTerm2 protocol), s (sixel).\n\n**--grid**=_cols_\n> Arrange images in a grid with specified number of columns.\n\n**--loops**=_num_\n> Number of animation loops. -1 for infinite, default is 1 for videos, infinite for GIFs.\n\n**--frames**=_num_\n> Only show first num frames of animation.\n\n**--color8**\n> Use 8-bit color mode (216 colors) for older terminals.\n\n**--center**\n> Center image in available space.\n\n**--clear**\n> Clear screen before displaying.\n\n**-C**\n> Clear screen before each frame in animations.\n\n**-V**\n> Interpret stdin as video stream.\n\n**--threads**=_n_\n> Number of threads for image decoding. Default is 3/4 of CPU cores.\n\n**--verbose**\n> Print information about terminal, pixelation, and frame rate.\n\n**-h**, **--help**\n> Show help message or detailed manpage.\n\n**--version**\n> Print version information and exit."],["DESCRIPTION","**timg** is a terminal image and video viewer that displays graphics directly in the terminal using Unicode block characters and 24-bit ANSI colors. It supports a wide range of formats including JPEG, PNG, GIF, WebP, BMP, and video formats like MP4, AVI, and MKV.\n\nThe viewer adapts to different terminal capabilities, using half-block or quarter-block Unicode characters for basic terminals, or native graphics protocols (Kitty, iTerm2, Sixel) when available for higher resolution output. This makes it useful for previewing images over SSH, in scripts, or in terminal-based workflows.\n\nImages can be displayed individually, in sequence, or arranged in a grid layout. Animated GIFs and videos are played directly in the terminal with configurable loop counts and frame rates."],["CAVEATS","Image quality depends on terminal capabilities. Basic Unicode block rendering provides approximate colors and low resolution. For best results, use a terminal that supports Kitty graphics, iTerm2 graphics, or Sixel protocols. Video playback performance depends on terminal rendering speed."],["HISTORY","**timg** was created by **Henner Zeller** and development began around **2016**. It was designed to bring image and video viewing capabilities to the terminal, leveraging modern terminal features like 24-bit color support and graphics protocols. The project continues active development with regular additions of new features and format support."],["INSTALL","```apt: sudo apt install timg```\n\n```dnf: sudo dnf install timg```\n\n```apk: sudo apk add timg```\n\n```zypper: sudo zypper install timg```\n\n```brew: brew install timg```\n\n```nix: nix profile install nixpkgs#timg```"],["SEE ALSO","[imgcat](/man/imgcat)(1), [viu](/man/viu)(1), [chafa](/man/chafa)(1), [feh](/man/feh)(1)"]],"e":["imgcat","viu","chafa","feh"],"x":"timg [path/to/image.png]"},"torify":{"n":"torify","t":"Route application traffic through Tor","s":[["TAGLINE","Route application traffic through Tor"],["TLDR","**Route** traffic via Tor\n\n```torify [command]```\n\n**Toggle** Tor in shell\n\n```torify [on|off]```\n\nSpawn Tor-enabled **shell**\n\n```torify --shell```\n\n**Check** if Tor is enabled\n\n```torify show```\n\nUse **config** file\n\n```torify -c [config-file] [command]```\n\nUse specific **proxy**\n\n```torify -P [proxy] [command]```"],["SYNOPSIS","**torify** [_OPTIONS_] [_COMMAND_ [_ARGS_...]]"],["PARAMETERS","**--shell**\n> Spawn a new Tor-enabled shell\n\n**on|off**\n> Toggle Tor routing in current shell\n\n**show**\n> Check if current shell is Tor-enabled\n\n**-c** _FILE_\n> Use specified Tor configuration file\n\n**-P** _PROXY_\n> Use specified SOCKS proxy"],["DESCRIPTION","**torify** routes network traffic from applications through the Tor anonymity network. It wraps commands to redirect their network connections through Tor's SOCKS proxy.\n\nNote: This command has been deprecated and is now a backwards-compatible wrapper for torsocks. New scripts should use torsocks directly."],["CAVEATS","Deprecated in favor of torsocks. Requires running Tor daemon. DNS leaks possible with some applications. Not all protocols work through Tor."],["HISTORY","**torify** was the original tool for routing application traffic through Tor. It has been superseded by **torsocks** which provides better functionality and security."],["INSTALL","```apt: sudo apt install tor```\n\n```dnf: sudo dnf install tor```\n\n```pacman: sudo pacman -S tor```\n\n```apk: sudo apk add tor```\n\n```zypper: sudo zypper install tor```\n\n```brew: brew install tor```\n\n```nix: nix profile install nixpkgs#tor```"],["SEE ALSO","[torsocks](/man/torsocks)(1), [tor](/man/tor)(1)"]],"e":["torsocks","tor"],"x":"torify [command]"},"trizen":{"n":"trizen","t":"Arch Linux AUR package helper","s":[["TAGLINE","Arch Linux AUR package helper"],["TLDR","**Sync** and update all AUR packages\n\n```trizen -Syua```\n\n**Install** package\n\n```trizen -S [package]```\n\n**Remove** package and dependencies\n\n```trizen -Rs [package]```\n\n**Search** for package\n\n```trizen -Ss [keyword]```\n\nShow package **info**\n\n```trizen -Si [package]```\n\n**List** installed packages\n\n```trizen -Qe```"],["SYNOPSIS","**trizen** [_OPTIONS_] [_TARGETS_...]"],["PARAMETERS","**-S**\n> Sync/install package\n\n**-Ss**\n> Search for packages\n\n**-Si**\n> Show package information\n\n**-Syua**\n> Sync databases and upgrade all AUR packages\n\n**-Rs**\n> Remove package with dependencies\n\n**-Qe**\n> List explicitly installed packages"],["DESCRIPTION","**trizen** is an Arch Linux utility for building and installing packages from the Arch User Repository (AUR). It uses pacman-style commands and provides features like dependency resolution, PKGBUILD review, and clean build options.\n\nThe tool handles downloading PKGBUILDs, building packages, and installing them along with their dependencies from both official repositories and the AUR."],["CAVEATS","AUR packages are user-submitted and not officially supported. Review PKGBUILDs before building. Requires base-devel package group. Building may require significant time and resources."],["HISTORY","**trizen** was written in Perl as a lightweight AUR helper for Arch Linux, focusing on simplicity and speed while maintaining compatibility with pacman commands."],["INSTALL","```aur: yay -S trizen```"],["SEE ALSO","[pacman](/man/pacman)(8), [yay](/man/yay)(8), [paru](/man/paru)(1)"]],"e":["pacman","yay","paru"],"x":"trizen -Syua"},"users":{"n":"users","t":"List currently logged-in usernames","s":[["TAGLINE","List currently logged-in usernames"],["TLDR","**Show logged in users**\n\n```users```\n\n**From specific file**\n\n```users [/var/log/wtmp]```"],["SYNOPSIS","**users** [_file_]"],["PARAMETERS","_file_\n> Alternate utmp file.\n\n**--help**\n> Show help.\n\n**--version**\n> Show version."],["DESCRIPTION","**users** prints a compact, space-separated list of usernames currently logged into the system. If a user has multiple login sessions, their name appears once for each session.\n\nThe command reads from /var/run/utmp by default to determine who is logged in. An alternate utmp-format file can be specified as an argument, such as /var/log/wtmp to see historical logins.\n\nUnlike **who** and **w**, which provide detailed information about each session, users produces minimal output suitable for quick checks or scripting where only the list of logged-in usernames is needed."],["CAVEATS","Simple output. Use who for details. Current logins only."],["HISTORY","**users** is a standard Unix command that outputs a compact list of users currently logged into the system."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[who](/man/who)(1), [w](/man/w)(1), [last](/man/last)(1), [whoami](/man/whoami)(1)"]],"e":["who","w","last","whoami"],"x":"users"},"vue-build":{"n":"vue-build","t":"Build Vue.js applications for production","s":[["TAGLINE","Build Vue.js applications for production"],["TLDR","**Build for production**\n\n```vue build```\n\n**Build with custom target**\n\n```vue build --target [lib]```\n\n**Build specific entry**\n\n```vue build [entry.js]```"],["SYNOPSIS","**vue** **build** [_options_] [_entry_]"],["PARAMETERS","**--target** _target_\n> Build target (app, lib, wc).\n\n**--name** _name_\n> Library or component name.\n\n**--dest** _dir_\n> Output directory.\n\n**--modern**\n> Build for modern browsers with auto legacy fallback (Vue CLI 4 only, removed in CLI 5).\n\n**--no-clean**\n> Do not remove the output directory before building.\n\n**--watch**\n> Watch for changes and rebuild automatically."],["DESCRIPTION","**vue build** compiles Vue.js applications for production. Creates optimized bundles with minification and tree-shaking. Part of Vue CLI's instant prototyping feature (requires `@vue/cli-service-global`). For standard project builds, use `vue-cli-service build`."],["INSTALL","```nix: nix profile install nixpkgs#vue```"],["SEE ALSO","[vue](/man/vue)(1), [vue-serve](/man/vue-serve)(1)"]],"e":["vue","vue-serve"],"x":"vue build"},"wiper":{"n":"wiper","t":"TUI disk space analyzer and cleanup tool","s":[["TAGLINE","TUI disk space analyzer and cleanup tool"],["TLDR","**Analyze the current directory**\n\n```wiper```\n\n**Analyze a specific directory**\n\n```wiper [/home]```\n\n**Navigate with keyboard: j/k or arrows to move, l/Enter to enter folder, h/Backspace to go back**\n\n```wiper```\n\n**Press d twice to delete selected item, press t to toggle trash mode**\n\n```wiper```"],["SYNOPSIS","**wiper** [_path_]"],["PARAMETERS","**j, k, Down, Up**\n> Navigate up and down through directories\n\n**l, Right, Enter**\n> Enter a selected folder\n\n**h, Left, Backspace**\n> Return to parent directory\n\n**d**\n> Delete mode (press once to select, again to confirm)\n\n**s**\n> Toggle sort order between name and size\n\n**c**\n> Toggle gradient coloring for visual space usage\n\n**t**\n> Toggle trash mode (permanent vs. recoverable deletion)\n\n**q**\n> Exit the application"],["DESCRIPTION","**wiper** is a terminal-based disk analyzer that scans directories and visually displays which folders consume the most space. It provides colored output, file size metrics, and keyboard shortcuts for navigating and cleaning up disk usage. Works on Linux, macOS, and Windows."],["HISTORY","**wiper** was created by **Alexandr Kobrin** (ikebastuz) and is written in **Rust**."],["INSTALL","```nix: nix profile install nixpkgs#wiper```"],["SEE ALSO","[dust](/man/dust)(1), [dua](/man/dua)(1), [ncdu](/man/ncdu)(1), [du](/man/du)(1)"]],"e":["dust","dua","ncdu","du"],"x":"wiper"},"xcode-select":{"n":"xcode-select","t":"Manage macOS developer tool directories","s":[["TAGLINE","Manage macOS developer tool directories"],["TLDR","**Install command line developer tools**\n\n```xcode-select --install```\n\n**Print the current developer directory path**\n\n```xcode-select -p```\n\n**Switch to a different Xcode installation**\n\n```sudo xcode-select -s [/Applications/Xcode.app]```\n\n**Switch to command line tools only**\n\n```sudo xcode-select -s [/Library/Developer/CommandLineTools]```\n\n**Reset to default developer directory**\n\n```sudo xcode-select -r```\n\n**Show version information**\n\n```xcode-select --version```"],["SYNOPSIS","**xcode-select** [_-h_|_-help_] [_-s path_|_--switch path_] [_-p_|_--print-path_] [_-r_|_--reset_] [_-v_|_--version_] [_--install_]"],["PARAMETERS","**--install**\n> Install the command line developer tools (opens installer dialog)\n\n**-p**, **--print-path**\n> Print path to the currently selected developer directory\n\n**-s** _path_, **--switch** _path_\n> Set the active developer directory to the specified path (requires sudo)\n\n**-r**, **--reset**\n> Reset to default developer directory search (requires sudo)\n\n**-v**, **--version**\n> Print xcode-select version\n\n**-h**, **--help**\n> Display help information"],["DESCRIPTION","**xcode-select** manages the active developer directory on macOS, controlling which Xcode or Command Line Tools installation is used by build tools like xcrun, xcodebuild, clang, and make.\n\nThe command line tools package includes compilers (clang, gcc), build tools (make, cmake), version control (git, svn), and the macOS SDK. When multiple Xcode versions are installed, xcode-select switches between them.\n\nThe tools are installed to /Library/Developer/CommandLineTools when using **--install** without full Xcode, or within /Applications/Xcode.app when using the full IDE."],["CAVEATS","The **--switch** and **--reset** options require superuser privileges and affect all users on the system. After macOS updates, command line tools may need reinstallation. The **--install** command requires GUI interaction."],["HISTORY","xcode-select has been part of macOS developer tools since Xcode 3. Apple separated Command Line Tools from Xcode proper around **2012**, allowing development without the full IDE download."],["SEE ALSO","[xcrun](/man/xcrun)(1), [xcodebuild](/man/xcodebuild)(1), [clang](/man/clang)(1), [make](/man/make)(1)"]],"e":["xcrun","xcodebuild","clang","make"],"x":"xcode-select --install"},"xscreensaver":{"n":"xscreensaver","t":"X11 screen saver and locker","s":[["TAGLINE","X11 screen saver and locker"],["TLDR","**Start the screensaver daemon**\n\n```xscreensaver &```\n\n**Start with no splash screen**\n\n```xscreensaver --no-splash &```\n\n**Activate the screensaver immediately**\n\n```xscreensaver-command -activate```\n\n**Lock the screen**\n\n```xscreensaver-command -lock```\n\n**Deactivate the screensaver** (unlock without password prompt)\n\n```xscreensaver-command -deactivate```\n\n**Open the preferences GUI**\n\n```xscreensaver-settings```\n\n**Preview a specific screensaver**\n\n```xscreensaver-command -select [saver_number]```\n\n**Restart the daemon** with new settings\n\n```xscreensaver-command -restart```"],["SYNOPSIS","**xscreensaver** [_--no-splash_] [_--verbose_] [_--log file_]\n\n**xscreensaver-command** [_-activate_] [_-lock_] [_-deactivate_] [_-restart_] [_-exit_] [_-select n_]"],["PARAMETERS","**--no-splash**\n> Do not display the splash screen at startup.\n\n**--verbose**\n> Print diagnostics to stderr.\n\n**--log** _file_\n> Append diagnostics to the specified log file.\n\n**-display** _host:n_\n> Connect to the specified X display."],["COMMANDS (xscreensaver-command)","**-activate**\n> Turn on the screensaver immediately.\n\n**-deactivate**\n> Turn off the screensaver if it is active.\n\n**-lock**\n> Lock the screen immediately.\n\n**-select** _n_\n> Switch to screensaver number n.\n\n**-next**\n> Switch to the next screensaver in the cycle.\n\n**-prev**\n> Switch to the previous screensaver.\n\n**-restart**\n> Restart the screensaver daemon with current settings.\n\n**-exit**\n> Terminate the xscreensaver daemon.\n\n**-time**\n> Print time since last user activity.\n\n**-watch**\n> Print a line each time the screensaver state changes."],["DESCRIPTION","**XScreenSaver** is the standard screen saver and locker for the X Window System. It consists of a background daemon that monitors user activity and triggers screensavers, plus a collection of over 200 display hacks (individual screensaver programs).\n\nThe daemon should be started at login, typically from **.xinitrc**, **.xsession**, or a desktop autostart entry. It monitors keyboard and mouse activity to determine idle time. When the idle threshold is reached, it blanks the screen and runs a screensaver. Screen locking can occur automatically after additional idle time or be triggered manually.\n\nConfiguration is managed through **xscreensaver-settings** (formerly xscreensaver-demo), a GTK-based GUI that allows selecting which screensavers to run, adjusting timing, and previewing effects. Settings are stored in **~/.xscreensaver**.\n\nThe project includes screensavers ranging from classic effects (flying toasters, matrix) to OpenGL visualizations and physics simulations. Each saver is a standalone executable that can also be run independently."],["CAVEATS","Requires the daemon to be running for any commands to work. Some desktop environments (GNOME, KDE) have their own screen lockers that may conflict. OpenGL screensavers require working 3D acceleration. The daemon must be restarted to pick up configuration changes. Power management (DPMS) settings may interact unexpectedly with screensaver timing."],["HISTORY","**XScreenSaver** was created by Jamie Zawinski (jwz) with the first release in **1992**. It became the de facto standard screensaver for Unix systems, replacing earlier fragmented solutions. The project has been continuously maintained for over 30 years, with new screensavers and security fixes added regularly. Zawinski has been vocal about the importance of XScreenSaver's security model, particularly regarding screen locking."],["INSTALL","```apt: sudo apt install xscreensaver```\n\n```dnf: sudo dnf install xscreensaver```\n\n```pacman: sudo pacman -S xscreensaver```\n\n```apk: sudo apk add xscreensaver```\n\n```zypper: sudo zypper install xscreensaver```\n\n```nix: nix profile install nixpkgs#xscreensaver```"],["SEE ALSO","[xscreensaver-command](/man/xscreensaver-command)(1), [xscreensaver-settings](/man/xscreensaver-settings)(1), [xlock](/man/xlock)(1), [xset](/man/xset)(1)"]],"e":["xscreensaver-command","xscreensaver-settings","xlock","xset"],"x":"xscreensaver &"},"zpool":{"n":"zpool","t":"Manage ZFS storage pools","s":[["TAGLINE","Manage ZFS storage pools"],["TLDR","**List all pools**\n\n```zpool list```\n\n**Show pool status**\n\n```zpool status [pool]```\n\n**Create a mirrored pool**\n\n```sudo zpool create [pool] mirror [/dev/sda] [/dev/sdb]```\n\n**Create a RAID-Z pool**\n\n```sudo zpool create [pool] raidz [/dev/sda] [/dev/sdb] [/dev/sdc]```\n\n**Add devices to pool**\n\n```sudo zpool add [pool] [/dev/sdc]```\n\n**Start a scrub**\n\n```sudo zpool scrub [pool]```\n\n**Import a pool**\n\n```sudo zpool import [pool]```\n\n**Export a pool**\n\n```sudo zpool export [pool]```"],["SYNOPSIS","**zpool** _command_ [_options_] [_arguments_]"],["SUBCOMMANDS","**create** _pool_ _vdev_...\n> Create new storage pool\n\n**destroy** _pool_\n> Destroy pool and all data\n\n**add** _pool_ _vdev_...\n> Add devices to pool\n\n**remove** _pool_ _device_\n> Remove device from pool\n\n**list** [_pool_]\n> List pools and properties\n\n**status** [_pool_]\n> Show detailed status\n\n**iostat** [_pool_] [_interval_]\n> Display I/O statistics\n\n**scrub** _pool_\n> Verify data integrity\n\n**import** [_pool_]\n> Import pool(s)\n\n**export** _pool_\n> Export pool for transport\n\n**upgrade** _pool_\n> Upgrade pool version\n\n**history** _pool_\n> Show pool command history\n\n**attach** _pool_ _device_ _new_device_\n> Attach mirror to device\n\n**detach** _pool_ _device_\n> Detach mirror device\n\n**replace** _pool_ _old_ _new_\n> Replace disk in pool\n\n**online/offline** _pool_ _device_\n> Bring device online/offline"],["DESCRIPTION","**zpool** manages ZFS storage pools. A pool is composed of virtual devices (vdevs) which can be single disks, mirrors, or RAID-Z configurations. Pools provide the underlying storage that ZFS datasets use.\n\nVdev types:\n- **mirror** - Data mirrored across 2+ disks\n- **raidz**, **raidz2**, **raidz3** - RAID-Z with 1/2/3 parity disks\n- **spare** - Hot spare disk\n- **cache** - L2ARC read cache (SSD)\n- **log** - ZIL write log (SSD)\n\nScrubbing verifies all data checksums and repairs errors using redundant copies. Regular scrubs are essential for data integrity."],["CAVEATS","Most operations require root privileges.\n\nPool destruction is permanent. **zpool destroy** deletes all data.\n\nRAID-Z cannot be expanded by adding disks to an existing vdev (as of OpenZFS 2.2, expansion is possible on some platforms).\n\nRegular scrubs are critical. ZFS detects but cannot repair single-disk pool errors."],["INSTALL","```apt: sudo apt install zfs-fuse```\n\n```apk: sudo apk add zfs```\n\n```nix: nix profile install nixpkgs#zfs```"],["SEE ALSO","[zfs](/man/zfs)(8), [zdb](/man/zdb)(8), [zpool-status](/man/zpool-status)(8)"]],"e":["zfs","zdb","zpool-status"],"x":"zpool list"}});
