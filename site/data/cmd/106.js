window.LCL_BUCKET(106,{"arduino-cli-interactive":{"n":"arduino-cli-interactive","t":"Interactive TUI wrapper for arduino-cli","s":[["TAGLINE","Interactive TUI wrapper for arduino-cli"],["TLDR","**Launch the interactive interface**\n\n```aci```"],["SYNOPSIS","**aci**"],["DESCRIPTION","**arduino-cli-interactive** (invoked as **aci**) is a Bash-based terminal user interface that wraps the official arduino-cli tool with interactive menus. Instead of memorizing board FQBNs and command-line arguments, users can select boards, ports, and operations from scrollable menus powered by Charm's gum library.\n\nThe tool supports board management and selection, sketch creation/editing/compiling/uploading, library management, core installation for ESP8266/ESP32 and third-party boards, a built-in serial monitor, and beginner example code."],["CAVEATS","Requires **arduino-cli** and **gum** to be installed. Initial setup requires installing the Arduino AVR core. Written as a Bash shell script so it primarily targets Linux and macOS."],["HISTORY","**arduino-cli-interactive** was created by **Vaishnav-Sabari-Girish**, an ECE student in India, out of frustration with the Arduino IDE on Linux. Licensed under Apache 2.0 and first released in December 2024."],["INSTALL","```pacman: sudo pacman -S arduino-cli```\n\n```brew: brew install arduino-cli```\n\n```nix: nix profile install nixpkgs#arduino-cli```"],["SEE ALSO","[arduino-cli](/man/arduino-cli)(1), [screen](/man/screen)(1), [minicom](/man/minicom)(1)"]],"e":["arduino-cli","screen","minicom"],"x":"aci"},"avahi-set-host-name":{"n":"avahi-set-host-name","t":"Change the mDNS hostname advertised on the network","s":[["TAGLINE","Change the mDNS hostname advertised on the network"],["TLDR","**Set the mDNS hostname**\n\n```avahi-set-host-name [newhost]```\n\n**Set hostname with verbose output**\n\n```avahi-set-host-name -v [newhost]```"],["SYNOPSIS","**avahi-set-host-name** _hostname_"],["DESCRIPTION","**avahi-set-host-name** changes the mDNS hostname advertised by the Avahi daemon. This allows you to change how your system appears on the local network without modifying system hostname files.\n\nThe change is immediate and affects service discovery on the local network."],["PARAMETERS","_hostname_\n> The new mDNS hostname to advertise. Will be published as _hostname_.local on the network.\n\n**-v**, **--verbose**\n> Enable verbose mode.\n\n**-V**, **--version**\n> Show version information.\n\n**-h**, **--help**\n> Show help."],["CAVEATS","Requires avahi-daemon running. This operation is usually privileged. Does not change the system hostname (use **hostname** or **hostnamectl** for that). Changes are not persistent across daemon restarts. Hostname must be a valid DNS name."],["HISTORY","**avahi-set-host-name** is part of the Avahi suite, a free Zeroconf/Bonjour implementation started by Lennart Poettering around **2005**."],["INSTALL","```apt: sudo apt install avahi-utils```\n\n```dnf: sudo dnf install avahi-tools```\n\n```apk: sudo apk add avahi-tools```\n\n```zypper: sudo zypper install avahi-utils```"],["SEE ALSO","[avahi-daemon](/man/avahi-daemon)(8), [avahi-resolve](/man/avahi-resolve)(1), [hostname](/man/hostname)(1), [hostnamectl](/man/hostnamectl)(1)"]],"e":["avahi-daemon","avahi-resolve","hostname","hostnamectl"],"x":"avahi-set-host-name [newhost]"},"az-sshkey":{"n":"az-sshkey","t":"Manage SSH public key resources in Azure","s":[["TAGLINE","Manage SSH public key resources in Azure"],["TLDR","**Create an SSH key resource** (auto-generates a key pair)\n\n```az sshkey create --name [MySSHKey] --resource-group [MyResourceGroup]```\n\n**Create an SSH key from an existing public key**\n\n```az sshkey create --name [MySSHKey] --resource-group [MyResourceGroup] --public-key \"@[~/.ssh/id_rsa.pub]\"```\n\n**Create an Ed25519 SSH key resource**\n\n```az sshkey create --name [MySSHKey] --resource-group [MyResourceGroup] --encryption-type [Ed25519]```\n\n**List all SSH key resources**\n\n```az sshkey list --resource-group [MyResourceGroup]```\n\n**Show SSH key details**\n\n```az sshkey show --name [MySSHKey] --resource-group [MyResourceGroup]```\n\n**Delete an SSH key resource**\n\n```az sshkey delete --name [MySSHKey] --resource-group [MyResourceGroup]```"],["SYNOPSIS","**az** **sshkey** _subcommand_ [_options_]"],["SUBCOMMANDS","**create**\n> Create a new SSH public key resource.\n\n**delete**\n> Delete an SSH public key resource.\n\n**list**\n> List SSH public key resources.\n\n**show**\n> Get information about an SSH public key resource.\n\n**update**\n> Update an SSH public key resource."],["PARAMETERS","**--name** **--ssh-public-key-name** **-n**\n> Name of the SSH public key resource.\n\n**--resource-group** **-g**\n> Name of the resource group.\n\n**--public-key**\n> SSH public key content, or `@filename` to read from a file. If omitted on create, a key pair is generated. A provided key must be at least 2048-bit and in ssh-rsa format.\n\n**--encryption-type**\n> Algorithm for a generated key. Accepts **RSA** (default) or **Ed25519**.\n\n**--location** **-l**\n> Azure region for the resource."],["DESCRIPTION","**az sshkey** manages Azure SSH public key resources, which store SSH public keys in Azure for reuse when creating VMs. This simplifies deployment by referencing a stored key rather than providing the key each time.\n\nWhen **--public-key** is omitted on **create**, Azure generates a new key pair and returns the private key in the command output. Save it immediately, as it cannot be retrieved later."],["CAVEATS","Requires Azure CLI to be installed and authenticated; this is a core command, so no extension is needed. Only the public key is stored in Azure. When a key is auto-generated, capture the returned private key right away."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[azure-cli](/man/azure-cli)(1), [az-vm](/man/az-vm)(1), [ssh-keygen](/man/ssh-keygen)(1)"],["RESOURCES","```[Source code](https://github.com/Azure/azure-cli)```\n\n```[Documentation](https://learn.microsoft.com/en-us/cli/azure/sshkey)```\n\n```[Homepage](https://learn.microsoft.com/en-us/cli/azure/)```"]],"e":["azure-cli","az-vm","ssh-keygen"],"x":"az sshkey create --name [MySSHKey] --resource-group [MyResourceGroup]"},"bitwarden-cli":{"n":"bitwarden-cli","t":"Manage passwords and secrets from the terminal","s":[["TAGLINE","Manage passwords and secrets from the terminal"],["TLDR","**Log in to Bitwarden**\n\n```bw login [email]```\n\n**Unlock the vault**\n\n```bw unlock```\n\n**List all items**\n\n```bw list items```\n\n**Search for a password**\n\n```bw list items --search [query]```\n\n**Get a specific item** by ID\n\n```bw get item [item_id]```\n\n**Get password for an item**\n\n```bw get password [item_id]```\n\n**Create a new login item**\n\n```echo '[json]' | bw create item```\n\n**Generate a password**\n\n```bw generate -ulns --length [20]```\n\n**Sync the vault**\n\n```bw sync```"],["SYNOPSIS","**bw** _command_ [_options_]"],["PARAMETERS","**login** [_email_]\n> Authenticate with Bitwarden server.\n\n**unlock**\n> Unlock the vault with master password.\n\n**lock**\n> Lock the vault.\n\n**sync**\n> Synchronize with the Bitwarden server.\n\n**list** _object_\n> List items (items, folders, collections, organizations, org-members, org-collections, send).\n\n**get** _object_ _id_\n> Get a specific object (item, password, username, uri, totp, notes, exposed, attachment, folder, template).\n\n**create** _object_\n> Create a new item (item, folder, attachment, org-collection); accepts base64-encoded JSON.\n\n**edit** _object_ _id_\n> Edit an existing item (item, folder, org-collection).\n\n**delete** _object_ _id_\n> Delete an item. Use **--permanent** to bypass the trash.\n\n**restore** _object_ _id_\n> Restore an item from the trash.\n\n**import** _format_ _path_\n> Import data from a third-party format or JSON export.\n\n**export**\n> Export the vault. Options: **--format** json|csv|encrypted_json, **--output** _path_.\n\n**generate**\n> Generate a password or passphrase.\n\n**encode**\n> Base64-encode stdin (commonly used to pipe JSON into create/edit).\n\n**status**\n> Show status (server URL, last sync, email, vault state) as JSON.\n\n**serve**\n> Start a local REST API server (**--port**, **--hostname**).\n\n**config** _setting_ _value_\n> Configure CLI settings (e.g. `bw config server <url>`).\n\n**--session** _key_\n> Session key from unlock (or set BW_SESSION env var).\n\n**--search** _query_\n> Search filter for list commands.\n\n**--pretty**\n> Pretty-print JSON output.\n\n**--raw**\n> Output raw value instead of JSON.\n\n**--nointeraction**\n> Do not prompt for input (for scripts).\n\n**-u**, **-l**, **-n**, **-s**\n> Password generation: uppercase, lowercase, numbers, special.\n\n**--length** _n_\n> Password length for generation (minimum 5).\n\n**--passphrase**\n> Generate a passphrase instead of a password (use with **--words**, **--separator**, **--capitalize**)."],["DESCRIPTION","**bw** is the official command-line interface for Bitwarden, an open-source password manager. It provides full access to your encrypted vault from the terminal, enabling password retrieval, item management, and integration with scripts and automation.\n\nAfter login, the vault must be unlocked with your master password. This returns a session key that must be provided to subsequent commands via **--session** or the **BW_SESSION** environment variable. The vault locks automatically after a timeout.\n\nItems are represented as JSON, making the CLI suitable for scripting and integration with other tools. The generate command creates secure passwords without storing them."],["CAVEATS","The session key provides full access to your vault; protect it accordingly. Commands output JSON by default, which may require parsing with jq. The vault must be unlocked before most operations. API rate limits may apply to self-hosted instances."],["HISTORY","Bitwarden was founded by **Kyle Spearrin** and launched in **2016** as an open-source alternative to proprietary password managers. The CLI was introduced to complement the web, desktop, and mobile applications. Bitwarden has grown to become one of the most popular password managers, valued for its transparent open-source model and affordable pricing."],["INSTALL","```pacman: sudo pacman -S bitwarden-cli```\n\n```brew: brew install bitwarden-cli```\n\n```nix: nix profile install nixpkgs#bitwarden-cli```"],["SEE ALSO","[pass](/man/pass)(1), [gopass](/man/gopass)(1), [keepassxc-cli](/man/keepassxc-cli)(1), [op](/man/op)(1)"]],"e":["pass","gopass","keepassxc-cli","op"],"x":"bw login [email]"},"charm":{"n":"charm","t":"Encrypted cloud storage and key-value database","s":[["TAGLINE","Encrypted cloud storage and key-value database"],["TLDR","**Store a key-value pair**\n\n```charm kv set [key] [value]```\n\n**Retrieve a value**\n\n```charm kv get [key]```\n\n**List all keys**\n\n```charm kv list```\n\n**Delete a key**\n\n```charm kv delete [key]```\n\n**Encrypt** a file using Charm keys\n\n```charm crypt encrypt [path/to/file]```\n\n**Decrypt** a file encrypted with Charm keys\n\n```charm crypt decrypt [path/to/file.enc]```\n\n**Sync** local data to Charm Cloud\n\n```charm sync```\n\n**Show** account identity and linked devices\n\n```charm id```\n\n**Back up** encryption keys to a file\n\n```charm backup-keys```"],["SYNOPSIS","**charm** _command_ [_options_] [_arguments_]"],["PARAMETERS","**kv set** _key_ _value_\n> Store a key-value pair.\n\n**kv get** _key_\n> Retrieve value for key.\n\n**kv list**\n> List all stored keys.\n\n**kv delete** _key_\n> Delete a key-value pair.\n\n**crypt encrypt** _file_\n> Encrypt a file.\n\n**crypt decrypt** _file_\n> Decrypt a file.\n\n**sync**\n> Synchronize with Charm Cloud.\n\n**id**\n> Show or manage account identity.\n\n**link**\n> Link a new device to account.\n\n**backup-keys**\n> Export encryption keys.\n\n**fs**\n> Access Charm file system.\n\n**-o**, **--output** _file_\n> Output file for operations."],["DESCRIPTION","**charm** is a command-line tool from Charmbracelet providing end-to-end encrypted cloud storage, key-value database, and file encryption. It enables syncing data securely across devices without managing your own server.\n\nThe **kv** commands provide a simple key-value store useful for configuration, secrets, and application state. All data is encrypted client-side before syncing to Charm Cloud, ensuring privacy.\n\nFile encryption via **charm crypt** uses your Charm keys to encrypt any file. The **fs** command provides a virtual file system stored in the cloud. Device linking allows accessing the same data from multiple machines."],["CAVEATS","Data is encrypted, but requires trusting Charm Cloud infrastructure. The free tier has storage limits. Key backup is essential - losing keys means losing access to data. Self-hosting is possible but requires more setup."],["HISTORY","Charm was created by **Charmbracelet**, a company focused on making the command line more delightful, founded in **2020**. Known for TUI libraries like Bubble Tea and Lip Gloss, they developed Charm Cloud as infrastructure for CLI tools needing cloud sync. The service launched to provide developers with easy, encrypted cloud storage without complex setup."],["INSTALL","```pacman: sudo pacman -S charm```\n\n```brew: brew install charm```\n\n```nix: nix profile install nixpkgs#charm```"],["SEE ALSO","[pass](/man/pass)(1), [age](/man/age)(1), [glow](/man/glow)(1), [gpg](/man/gpg)(1)"]],"e":["pass","age","glow","gpg"],"x":"charm kv set [key] [value]"},"cline":{"n":"cline","t":"Model-agnostic AI coding agent for the terminal","s":[["TAGLINE","Model-agnostic AI coding agent for the terminal"],["TLDR","**Start an interactive session** in the current directory\n\n```cline```\n\n**Run a task with a prompt**\n\n```cline \"[fix the login bug]\"```\n\n**Run in fully autonomous mode** (no interactive UI)\n\n```cline -y \"[refactor auth module]\"```\n\n**Set up authentication** with an AI provider\n\n```cline auth -p [anthropic] -k [sk-ant-api-xxxxx]```\n\n**Run in plan mode** to preview changes before acting\n\n```cline -p \"[add unit tests for auth]\"```\n\n**Pipe context** to cline in headless mode\n\n```cat [error.log] | cline -y \"[explain these errors]\"```"],["SYNOPSIS","**cline** [_options_] [_prompt_]\n\n**cline** **auth** [_options_]\n\n**cline** **config**\n\n**cline** **history** [_options_]\n\n**cline** **update**"],["PARAMETERS","**-y**, **--yolo**\n> Run in fully autonomous mode with no interactive UI. Output streams to stdout.\n\n**-a**, **--act**\n> Run in act mode (execute changes directly).\n\n**-p**, **--plan**\n> Run in plan mode (preview changes before acting).\n\n**-m**, **--model** _MODEL_\n> Specify the model to use.\n\n**-i**, **--images** _PATHS_\n> Attach image files to the prompt.\n\n**--json**\n> Stream structured JSON output for programmatic consumption.\n\n**--thinking**\n> Enable extended thinking mode.\n\n**--timeout** _SECONDS_\n> Set a timeout for the task.\n\n**--continue**\n> Continue the most recent task.\n\n**--acp**\n> Run as an ACP (Agent Client Protocol) compliant agent for editor integration.\n\n**--config** _PATH_\n> Custom configuration directory.\n\n**-c**, **--cwd** _PATH_\n> Set the working directory.\n\n**-v**, **--verbose**\n> Show detailed output.\n\n**--help**\n> Display help information."],["SUBCOMMANDS","**auth**\n> Set up authentication with AI providers. Use **-p** for provider, **-k** for API key, **-m** for model ID, **-b** for base URL.\n\n**config**\n> Interactive configuration interface for settings, rules, workflows, and hooks.\n\n**history**\n> Browse task history. Use **-n** for limit and **-p** for page.\n\n**update**\n> Install the latest CLI version.\n\n**version**\n> Display the installed version number."],["DESCRIPTION","**cline** is an AI coding agent that works in the terminal, supporting multiple AI model providers including OpenAI, Anthropic, Google, AWS Bedrock, Ollama, and any OpenAI-compatible API. It can read and write files, execute terminal commands, and perform multi-step development tasks autonomously.\n\nThe tool operates in two main modes: interactive mode (default when running in a terminal) and headless mode (activated with **-y**/**--yolo** or when piping input/output). Headless mode is designed for automation, CI/CD pipelines, and scripting.\n\nCline began as a VS Code extension and expanded to include a CLI interface, making it accessible from any terminal environment. It uses a model-agnostic architecture, allowing users to choose their preferred AI backend. The **--acp** flag enables the Agent Client Protocol for integration with editors like JetBrains, Zed, Neovim, and Emacs."],["CAVEATS","Requires API keys for cloud-hosted model providers. Local model support via Ollama requires separate installation and model downloads. Token usage and API costs vary by provider and model. Run **cline auth** to configure providers before first use."],["HISTORY","Cline was originally created as a Visual Studio Code extension under the name **Claude Dev** in **2024**, then renamed to Cline to reflect its model-agnostic design. It quickly grew to become one of the most popular open-source AI coding tools. **Cline CLI 2.0** was released in **February 2026**, rebuilding the full agent for terminal-first workflows with headless mode and ACP support."],["INSTALL","```brew: brew install cline```"],["SEE ALSO","[claude](/man/claude)(1), [aider](/man/aider)(1), [codex](/man/codex)(1), [copilot](/man/copilot)(1)"]],"e":["claude","aider","codex","copilot"],"x":"cline"},"cmatrix":{"n":"cmatrix","t":"terminal-based Matrix digital rain effect","s":[["TAGLINE","terminal-based Matrix digital rain effect"],["TLDR","**Display the Matrix** with default settings\n\n```cmatrix```\n\n**Use bold characters** for brighter display\n\n```cmatrix -b```\n\n**Use a specific color** (green, red, blue, yellow, white, magenta, cyan)\n\n```cmatrix -C [green]```\n\n**Enable asynchronous scrolling** for a more realistic effect\n\n```cmatrix -a```\n\n**Adjust the scrolling speed** (0-9, higher is faster)\n\n```cmatrix -s -u [2]```\n\n**Display using rainbow colors**\n\n```cmatrix -r```"],["SYNOPSIS","**cmatrix** [_-abBflohnsmVx_] [_-u update_] [_-C color_]"],["PARAMETERS","**-a**\n> Enable asynchronous scrolling mode\n\n**-b**\n> Bold characters on\n\n**-B**\n> All bold characters (overrides -b)\n\n**-f**\n> Force the Linux $TERM type to be used\n\n**-l**\n> Linux mode (uses matrix console font)\n\n**-o**\n> Use old-style scrolling\n\n**-h**\n> Print usage and exit\n\n**-n**\n> No bold characters (overrides -b and -B)\n\n**-s**\n> Screensaver mode, exits on first keystroke\n\n**-x**\n> X window mode, exits on each keypress if run in a X terminal\n\n**-V**\n> Print version information and exit\n\n**-u** _delay_\n> Screen update delay (0-9, default 4)\n\n**-C** _color_\n> Use specified color (green, red, blue, white, yellow, cyan, magenta, black)\n\n**-r**\n> Rainbow mode\n\n**-m**\n> Lambda mode (display lambda instead of characters)"],["DESCRIPTION","**cmatrix** is a terminal-based program that simulates the iconic digital rain effect from the Matrix movie series. It displays scrolling green characters falling down the screen, creating a visual effect reminiscent of the film's representation of the Matrix's code.\n\nThe program is primarily used for aesthetic purposes, as a screensaver, or simply for entertainment. It can be customized with various colors, speeds, and display modes to suit user preferences."],["CAVEATS","Requires a terminal that supports ANSI escape codes and curses library. Heavy CPU usage may occur on slower systems at high speeds. The Linux console font mode (**-l**) only works on actual Linux consoles, not terminal emulators."],["HISTORY","CMatrix was originally written by **Chris Allegretta** in **1999**, inspired by the visual effects in the 1999 film \"The Matrix.\" It was created as a fun terminal application for Unix-like systems and has become a popular way to add visual flair to terminal sessions. The project is open source and continues to receive updates from the community."],["INSTALL","```apt: sudo apt install cmatrix```\n\n```dnf: sudo dnf install cmatrix```\n\n```pacman: sudo pacman -S cmatrix```\n\n```apk: sudo apk add cmatrix```\n\n```zypper: sudo zypper install cmatrix```\n\n```brew: brew install cmatrix```\n\n```nix: nix profile install nixpkgs#cmatrix```"],["SEE ALSO","[sl](/man/sl)(1), [cowsay](/man/cowsay)(1), [figlet](/man/figlet)(1), [lolcat](/man/lolcat)(1)"]],"e":["sl","cowsay","figlet","lolcat"],"x":"cmatrix"},"dbus-daemon":{"n":"dbus-daemon","t":"message bus daemon for inter-process communication","s":[["TAGLINE","message bus daemon for inter-process communication"],["TLDR","Run with a **configuration file**\n\n```dbus-daemon --config-file [path/to/file]```\n\nRun **session** message bus\n\n```dbus-daemon --session```\n\nRun **system** message bus\n\n```dbus-daemon --system```\n\nSet **address** to listen on\n\n```dbus-daemon --address [address]```\n\nOutput **PID** to stdout\n\n```dbus-daemon --print-pid```\n\nRun in **foreground** without forking\n\n```dbus-daemon --session --nofork```\n\nPrint **version**\n\n```dbus-daemon --version```"],["SYNOPSIS","**dbus-daemon** [_options_]"],["DESCRIPTION","**dbus-daemon** is the D-Bus message bus daemon that enables inter-process communication (IPC) between applications on Linux and Unix systems. It acts as a message router, receiving messages from one application and delivering them to the appropriate destination based on bus names and object paths.\n\nTwo main bus types exist: the **session bus** (per-user) for desktop applications and user services, and the **system bus** (system-wide) for system services and hardware events. The session bus is created when a user logs in and exists for the duration of their session. The system bus is started at boot and runs with elevated privileges.\n\nD-Bus is fundamental to modern Linux desktop environments and system services. Applications use it for everything from notification delivery to hardware management. For example, NetworkManager, systemd, PulseAudio, and most desktop applications communicate through D-Bus.\n\nThe daemon enforces security policies defined in its configuration files, controlling which applications can own bus names, send messages to specific services, and receive broadcasts. This prevents unauthorized access to sensitive system functionality."],["PARAMETERS","**--config-file** _file_\n> Use specified configuration file\n\n**--session**\n> Use session bus configuration\n\n**--system**\n> Use system bus configuration\n\n**--address** _address_\n> Listen address override\n\n**--print-pid**[=_DESCRIPTOR_]\n> Print PID to stdout, or to the given file descriptor\n\n**--print-address**[=_DESCRIPTOR_]\n> Print bus address to stdout, or to the given file descriptor\n\n**--syslog**\n> Enable syslog logging in addition to stderr\n\n**--nofork**\n> Do not fork into background, even if configured to do so\n\n**--fork**\n> Fork into background, even if not configured to do so\n\n**--nopidfile**\n> Do not write a PID file even if configured to do so\n\n**--nosyslog**\n> Force output to stderr only, do not use syslog\n\n**--syslog-only**\n> Force syslog logging only, do not duplicate messages to stderr\n\n**--introspect**\n> Print introspection information for all D-Bus internal interfaces and exit\n\n**--systemd-activation**\n> Enable systemd-style service activation\n\n**--version**\n> Print daemon version and exit"],["CONFIGURATION","**/etc/dbus-1/system.conf**\n> System bus configuration file controlling security policies and allowed services.\n\n**/etc/dbus-1/session.conf**\n> Session bus configuration template defining per-user session settings."],["CAVEATS","Usually started automatically by systemd or session managers. Direct invocation is rarely needed. System bus requires root privileges. Configuration affects security and available services."],["INSTALL","```apt: sudo apt install dbus-daemon```\n\n```dnf: sudo dnf install dbus-daemon```\n\n```pacman: sudo pacman -S dbus```\n\n```apk: sudo apk add dbus```\n\n```brew: brew install dbus```\n\n```nix: nix profile install nixpkgs#dbus```"],["SEE ALSO","[dbus-send](/man/dbus-send)(1), [dbus-monitor](/man/dbus-monitor)(1), [dbus-launch](/man/dbus-launch)(1), [busctl](/man/busctl)(1)"]],"e":["dbus-send","dbus-monitor","dbus-launch","busctl"],"x":"dbus-daemon --config-file [path/to/file]"},"docusaurus":{"n":"docusaurus","t":"static site generator optimized for documentation","s":[["TAGLINE","static site generator optimized for documentation"],["TLDR","**Create a new Docusaurus site** using the classic template\n\n```npx create-docusaurus@latest [my-website] classic```\n\n**Start the development server** (with hot reload)\n\n```npx docusaurus start```\n\n**Start on a specific port**\n\n```npx docusaurus start --port [8080]```\n\n**Build the site for production**\n\n```npx docusaurus build```\n\n**Serve the production build locally**\n\n```npx docusaurus serve```\n\n**Deploy to GitHub Pages**\n\n```npx docusaurus deploy```\n\n**Clear generated files and caches**\n\n```npx docusaurus clear```\n\n**Tag a new documentation version**\n\n```npx docusaurus docs:version [1.0]```"],["SYNOPSIS","**npx docusaurus** _command_ [_options_]"],["PARAMETERS","**start** [**--port** _n_]\n> Start development server (default port 3000).\n\n**build** [**--out-dir** _path_]\n> Build static site for production.\n\n**serve** [**--port** _n_]\n> Serve built site locally.\n\n**deploy**\n> Deploy to GitHub Pages.\n\n**clear**\n> Clear generated files and caches.\n\n**swizzle** _theme_ _component_\n> Eject or wrap a theme component for customization (interactive).\n\n**docs:version** _version_\n> Tag the current docs as a new version snapshot.\n\n**write-translations**\n> Extract translatable strings to JSON translation files.\n\n**write-heading-ids**\n> Add explicit heading IDs to Markdown documents.\n\n**--locale** _locale_\n> Specify locale for the command.\n\n**--config** _path_\n> Use a custom configuration file.\n\n**--out-dir** _path_\n> Custom output directory for build (default: build/)."],["DESCRIPTION","**Docusaurus** is a static site generator optimized for documentation websites. It uses React for the UI, Markdown/MDX for content, and provides features like versioning, i18n, and search out of the box.\n\nSites are configured via **docusaurus.config.js**, defining metadata, themes, plugins, and navigation. Content lives in **docs/** for documentation and **blog/** for blog posts.\n\nThe development server provides hot reloading. Production builds generate static HTML, CSS, and JavaScript that can be hosted anywhere. Built-in deployment support targets GitHub Pages."],["PROJECT STRUCTURE","```\nmy-website/\n├── docs/           # Documentation markdown\n├── blog/           # Blog posts\n├── src/\n│   ├── components/ # React components\n│   └── pages/      # Custom pages\n├── static/         # Static assets\n├── docusaurus.config.js\n└── sidebars.js     # Sidebar configuration\n```"],["CONFIGURATION","**docusaurus.config.js**\n> Main configuration defining site metadata, themes, plugins, navbar, footer, and build options.\n\n**sidebars.js**\n> Sidebar structure configuration defining documentation hierarchy and navigation."],["CAVEATS","Requires Node.js 18+. Large sites may have slow build times. Custom React components need familiarity with React. Swizzling (customizing) theme components may break on version upgrades. Full-text search requires additional setup (Algolia DocSearch or a local search plugin)."],["HISTORY","Docusaurus was created at **Facebook** (Meta) and open-sourced in **2017**. It was designed to power documentation for Facebook's open-source projects. Version 2, a complete rewrite using React, was released in **2022**. The project is maintained by Meta and a community of contributors."],["SEE ALSO","[gatsby](/man/gatsby)(1), [vitepress](/man/vitepress)(1), [mkdocs](/man/mkdocs)(1), [hugo](/man/hugo)(1)"]],"e":["gatsby","vitepress","mkdocs","hugo"],"x":"npx create-docusaurus@latest [my-website] classic"},"dvc-destroy":{"n":"dvc-destroy","t":"remove DVC initialization from project","s":[["TAGLINE","remove DVC initialization from project"],["TLDR","**Remove DVC from project**\n\n```dvc destroy```\n\n**Force destroy without confirmation**\n\n```dvc destroy -f```"],["SYNOPSIS","**dvc** **destroy** [_options_]"],["PARAMETERS","**-f**, **--force**\n> Force destroy without confirmation."],["DESCRIPTION","**dvc destroy** completely removes DVC initialization from a project, effectively uninstalling DVC from the repository. It deletes the .dvc directory (containing cache, configuration, and internal state) along with all .dvc metadata files throughout the project.\n\nImportantly, the actual data files tracked by DVC are preserved. Only the DVC tracking infrastructure is removed. This means your large datasets and model files remain in the workspace, they simply lose their version control status.\n\nThis command is irreversible and should be used with caution. Before destroying, ensure you've pushed important data to remote storage if you might need to restore DVC tracking later. After destroying, .gitignore entries added by DVC remain and may need manual cleanup."],["CAVEATS","This operation is irreversible. Make sure to backup any data or push to remote storage before destroying."],["INSTALL","```brew: brew install dvc```\n\n```nix: nix profile install nixpkgs#dvc```"],["SEE ALSO","[dvc-init](/man/dvc-init)(1), [dvc](/man/dvc)(1)"],["RESOURCES","```[Source code](https://github.com/iterative/dvc)```\n\n```[Homepage](https://dvc.org/)```\n\n```[Documentation](https://dvc.org/doc/command-reference/destroy)```"]],"e":["dvc-init","dvc"],"x":"dvc destroy"},"dvd+rw-mediainfo":{"n":"dvd+rw-mediainfo","t":"display DVD media information","s":[["TAGLINE","display DVD media information"],["TLDR","**Show media information**\n\n```dvd+rw-mediainfo [/dev/dvd]```"],["SYNOPSIS","**dvd+rw-mediainfo** _device_"],["PARAMETERS","_DEVICE_\n> DVD/BD drive device path (e.g., /dev/dvd, /dev/sr0)."],["DESCRIPTION","**dvd+rw-mediainfo** displays detailed information about optical media (DVD/BD) currently in the drive and the drive itself. It reports disc type, capacity, booktype, write speed, and current usage status.\n\nThe output includes manufacturer information, disc state (empty, appendable, complete), and layer information for dual-layer media. This helps verify disc compatibility and available capacity before burning. It is especially useful for debugging, and its output should be included when reporting bugs in dvd+rw-tools.\n\nThis program does not accept any command-line options beyond the device path."],["CAVEATS","Requires media in drive for most info. Some information depends on drive capabilities. May need root permissions for device access."],["HISTORY","dvd+rw-mediainfo is part of **dvd+rw-tools**, providing disc analysis capabilities essential for DVD authoring and burning on Linux systems."],["INSTALL","```apt: sudo apt install dvd+rw-tools```\n\n```dnf: sudo dnf install dvd+rw-tools```\n\n```pacman: sudo pacman -S dvd+rw-tools```\n\n```apk: sudo apk add dvd+rw-tools```\n\n```zypper: sudo zypper install dvd+rw-tools```\n\n```brew: brew install dvd+rw-tools```"],["SEE ALSO","[dvd+rw-booktype](/man/dvd+rw-booktype)(1), [growisofs](/man/growisofs)(1), [cdrecord](/man/cdrecord)(1)"]],"e":["dvd+rw-booktype","growisofs","cdrecord"],"x":"dvd+rw-mediainfo [/dev/dvd]"},"eddy":{"n":"eddy","t":"Debian package installer for elementary OS","s":[["TAGLINE","Debian package installer for elementary OS"],["TLDR","**Install** a .deb package file\n\n```eddy [package.deb]```\n\n**Install** multiple .deb packages at once\n\n```eddy [package1.deb] [package2.deb]```\n\n**Launch** the graphical installer\n\n```eddy```"],["SYNOPSIS","**eddy** [_options_] [_files_]"],["PARAMETERS","**--debug**\n> Enable debug output for troubleshooting."],["DESCRIPTION","**eddy** is a graphical Debian package installer designed for elementary OS. It provides a simple GTK-based interface to install, update, uninstall, and view information about .deb packages. It uses PackageKit as its backend, which also provides limited support for other package formats like .rpm.\n\nThe application is intended as a user-friendly alternative to command-line package management for handling individual package files downloaded outside of the system package manager."],["CAVEATS","Designed primarily for elementary OS but may work on other GTK-based distributions. Requires PackageKit for its backend. Not a full package manager replacement; intended for installing individual package files."],["INSTALL","```nix: nix profile install nixpkgs#eddy```"],["SEE ALSO","[dpkg](/man/dpkg)(1), [apt](/man/apt)(1), [gdebi](/man/gdebi)(1)"]],"e":["dpkg","apt","gdebi"],"x":"eddy [package.deb]"},"elif":{"n":"elif","t":"Conditional else-if construct in shell scripts","s":[["TAGLINE","Conditional else-if construct in shell scripts"],["TLDR","**Use elif in** bash script\n\n```if [condition1]; then echo \"first\"; elif [condition2]; then echo \"second\"; fi```\n\n**Multiple elif** conditions\n\n```if [ $x -lt 0 ]; then echo \"negative\"; elif [ $x -eq 0 ]; then echo \"zero\"; elif [ $x -gt 0 ]; then echo \"positive\"; fi```\n\n**Branch on a command's exit status** rather than a test\n\n```if command -v nvim; then echo \"nvim\"; elif command -v vim; then echo \"vim\"; else echo \"none\"; fi```"],["SYNOPSIS","**if** _condition_; **then** _commands_; **elif** _condition_; **then** _commands_; **fi**"],["DESCRIPTION","**elif** is a shell keyword used in bash and other POSIX shells for conditional branching. It's short for \"else if\" and allows multiple conditions to be tested sequentially.\n\nelif follows an if statement and precedes then. Multiple elif blocks can appear before an optional else. Only the first matching condition's block executes.\n\nThis construct is fundamental to shell scripting for implementing multi-way branching logic."],["CAVEATS","elif is a shell reserved word, not a program, so `which elif` finds nothing and it cannot be used outside an if block. Each elif needs its own **then**, and the whole chain still ends with a single **fi**. Inside `[ ... ]` every token must be separated by spaces: `[$x -eq 1]` is a syntax error. The condition is a command list, so it branches on the exit status, and any command works there, not just test."],["HISTORY","elif is part of POSIX shell syntax, inherited from the Bourne shell created by **Stephen Bourne** at Bell Labs in the 1970s. It provides cleaner syntax than nested if-else blocks."],["SEE ALSO","[if](/man/if)(1), [else](/man/else)(1), [fi](/man/fi)(1), [test](/man/test)(1), [bash](/man/bash)(1)"]],"e":["if","else","fi","test","bash"],"x":"if [condition1]; then echo \"first\"; elif [condition2]; then echo \"second\"; fi"},"espeak-ng":{"n":"espeak-ng","t":"next-generation formant-based speech synthesis","s":[["TAGLINE","next-generation formant-based speech synthesis"],["TLDR","**Speak text**\n\n```espeak-ng \"[Hello world]\"```\n\n**Use specific voice**\n\n```espeak-ng -v [en-gb] \"[Hello]\"```\n\n**Read from file**\n\n```espeak-ng -f [document.txt]```\n\n**Output to WAV**\n\n```espeak-ng -w [output.wav] \"[Hello]\"```\n\n**Adjust speaking rate**\n\n```espeak-ng -s [175] \"[Hello]\"```\n\n**List voices**\n\n```espeak-ng --voices```\n\n**Use phoneme input**\n\n```espeak-ng -x \"[h@l'oU]\"```"],["SYNOPSIS","**espeak-ng** [_options_] [_words_]"],["PARAMETERS","_WORDS_\n> Text to speak.\n\n**-v** _VOICE_\n> Select voice/language.\n\n**-f** _FILE_\n> Read from file.\n\n**-w** _FILE_\n> Write to WAV file.\n\n**-s** _SPEED_\n> Words per minute.\n\n**-p** _PITCH_\n> Pitch adjustment.\n\n**-p** _PITCH_\n> Pitch adjustment (0 to 99, default 50).\n\n**-a** _AMPLITUDE_\n> Amplitude/volume (0 to 200, default 100).\n\n**-g** _GAP_\n> Word gap, in units of 10 ms.\n\n**-x**\n> Write the translated phoneme mnemonics to stdout instead of speaking.\n\n**--ipa**\n> Write phonemes using the International Phonetic Alphabet.\n\n**--stdout**\n> Write the audio as WAV data to standard output.\n\n**--voices**[=_lang_]\n> List available voices, optionally filtered by language.\n\n**--help**\n> Display help information."],["DESCRIPTION","**espeak-ng** (eSpeak New Generation) is a fork of eSpeak with active development, additional features, and improved voice quality. It's a formant-based speech synthesizer supporting 100+ languages.\n\nThe tool provides text-to-speech capabilities for accessibility, voice assistants, and applications. It includes improvements in pronunciation rules, language support, and phoneme handling over the original eSpeak.\n\nespeak-ng is the default TTS engine in many Linux distributions and speech synthesis frameworks."],["CAVEATS","Still sounds robotic (formant synthesis). Some advanced SSML features unsupported. Voice quality varies by language. Output format options limited."],["HISTORY","espeak-ng was forked from eSpeak by **Reece H. Dunn** to continue development after the original project became inactive. It's now the actively maintained version used in most Linux distributions."],["INSTALL","```apt: sudo apt install espeak-ng```\n\n```dnf: sudo dnf install espeak-ng```\n\n```pacman: sudo pacman -S espeak-ng```\n\n```apk: sudo apk add espeak-ng```\n\n```zypper: sudo zypper install espeak-ng```\n\n```brew: brew install espeak-ng```\n\n```nix: nix profile install nixpkgs#espeak-ng```"],["SEE ALSO","[espeak](/man/espeak)(1), [festival](/man/festival)(1), [mimic](/man/mimic)(1), [pico2wave](/man/pico2wave)(1)"],["RESOURCES","```[Source code](https://github.com/espeak-ng/espeak-ng)```\n\n```[Documentation](https://github.com/espeak-ng/espeak-ng/blob/master/docs/guide.md)```"]],"e":["espeak","festival","mimic","pico2wave"],"x":"espeak-ng \"[Hello world]\""},"fgconsole":{"n":"fgconsole","t":"print foreground virtual terminal number","s":[["TAGLINE","print foreground virtual terminal number"],["TLDR","Print **current** terminal number\n\n```fgconsole```\n\nPrint **next** unallocated virtual terminal number\n\n```fgconsole --next-available```"],["SYNOPSIS","**fgconsole** [_options_]"],["DESCRIPTION","**fgconsole** prints the number of the foreground virtual terminal. If the terminal is serial, it prints \"serial\" instead of a number.\n\nPart of kbd package for console management utilities."],["PARAMETERS","**-n**, **--next-available**\n> Print next unallocated VT number\n\n**-V**, **--version**\n> Print version number and exit\n\n**-h**, **--help**\n> Display help and exit"],["CAVEATS","Only works on Linux console. Returns error if not on a virtual terminal. Part of kbd package."],["INSTALL","```apt: sudo apt install kbd```\n\n```dnf: sudo dnf install kbd```\n\n```pacman: sudo pacman -S kbd```\n\n```apk: sudo apk add kbd```\n\n```zypper: sudo zypper install kbd```\n\n```nix: nix profile install nixpkgs#kbd```"],["SEE ALSO","[chvt](/man/chvt)(1), [deallocvt](/man/deallocvt)(1), [dumpkeys](/man/dumpkeys)(1)"]],"e":["chvt","deallocvt","dumpkeys"],"x":"fgconsole"},"figlist":{"n":"figlist","t":"figlet font listing utility","s":[["TAGLINE","figlet font listing utility"],["TLDR","**List available figlet fonts**\n\n```figlist```"],["SYNOPSIS","**figlist** [_options_]"],["DESCRIPTION","**figlist** enumerates all installed figlet fonts and control files by scanning the default figlet font directory, typically /usr/share/figlet. It provides a simple way to discover available font names for use with the figlet command.\n\nThe output includes both .flf font files and .flc control files, showing the complete set of available character rendering options. This is more convenient than manually browsing the font directory.\n\nfiglist is particularly useful when exploring different text banner styles or writing scripts that need to verify font availability before rendering."],["INSTALL","```apt: sudo apt install figlet```\n\n```dnf: sudo dnf install figlet```\n\n```pacman: sudo pacman -S figlet```\n\n```apk: sudo apk add figlet```\n\n```zypper: sudo zypper install figlet```\n\n```brew: brew install figlet```\n\n```nix: nix profile install nixpkgs#figlet```"],["SEE ALSO","[figlet](/man/figlet)(6), [showfigfonts](/man/showfigfonts)(6)"],["RESOURCES","```[Source code](https://github.com/cmatsuoka/figlet)```\n\n```[Homepage](http://www.figlet.org/)```"]],"e":["figlet","showfigfonts"],"x":"figlist"},"galleta":{"n":"galleta","t":"Internet Explorer cookie forensic parser","s":[["TAGLINE","Internet Explorer cookie forensic parser"],["TLDR","**Parse IE cookies**\n\n```galleta [index.dat]```\n\n**Output to file**\n\n```galleta [index.dat] > [cookies.txt]```\n\n**Parse with delimiters**\n\n```galleta -d [index.dat]```"],["SYNOPSIS","**galleta** [_options_] _file_"],["PARAMETERS","_FILE_\n> Internet Explorer cookie file (index.dat).\n\n**-d**\n> Delimited output for spreadsheets.\n\n**--help**\n> Display help information."],["DESCRIPTION","**galleta** is a digital forensics tool that parses Internet Explorer cookie files. It extracts cookie data including domains, paths, values, and timestamps from index.dat files.\n\nThe tool produces human-readable or delimited output for analysis. It's used in forensic investigations to examine web browsing history and user activity.\n\ngalleta handles the binary index.dat format used by older versions of Internet Explorer."],["CAVEATS","Only for IE index.dat format. Modern browsers use different formats. File must be accessible (not locked)."],["HISTORY","galleta (Spanish for \"cookie\") is part of forensic toolkit collections for analyzing Internet Explorer artifacts. It parses the proprietary index.dat format used by IE for cookie storage."],["INSTALL","```apt: sudo apt install galleta```\n\n```nix: nix profile install nixpkgs#galleta```"],["SEE ALSO","[strings](/man/strings)(1)"]],"e":["strings"],"x":"galleta [index.dat]"},"git-fame":{"n":"git-fame","t":"Generate contribution statistics by author","s":[["TAGLINE","Generate contribution statistics by author"],["TLDR","**Show line statistics**\n\n```git fame```\n\n**Sort by number of commits**\n\n```git fame --sort=commits```\n\n**Exclude paths (comma-separated or regex)**\n\n```git fame --excl=[vendor/]```\n\n**Show specific directory**\n\n```git fame [src/]```\n\n**Show emails instead of names**\n\n```git fame --show-email```\n\n**Detect moved/copied lines for more accurate attribution**\n\n```git fame -M -C```"],["SYNOPSIS","**git fame** [_options_] [_gitdir_...]"],["PARAMETERS","_GITDIR_\n> Git directory to analyze (default: current directory). May be repeated.\n\n**--branch** _BRANCH_\n> Branch or tag to analyze (default: HEAD).\n\n**--sort** _KEY_\n> Sort by loc (default), commits, files, hours, or months.\n\n**--excl** _PATTERN_\n> Excluded files, comma-separated or regex.\n\n**--incl** _PATTERN_\n> Included files (default: all).\n\n**--loc** _TYPE_\n> Count surviving, insertions, or deletions.\n\n**--since** _DATE_, **--until** _DATE_\n> Restrict to commits in a date range.\n\n**--cost** _METHOD_\n> Estimate cost via months, cocomo, hours, or commits.\n\n**-e**, **--show-email**\n> Show author emails instead of names.\n\n**-w**, **--ignore-whitespace**\n> Ignore whitespace-only changes when attributing blame.\n\n**-M**\n> Detect intra-file line moves and copies.\n\n**-C**\n> Detect inter-file line moves and copies.\n\n**-R**, **--recurse**\n> Recursively find repositories and submodules.\n\n**-t**, **--bytype**\n> Break down stats per file extension.\n\n**--format** _FORMAT_\n> Output format: pipe, md, yaml, json, csv, tsv, or tabulate.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**git fame** generates contribution statistics showing lines of code, commits, and files per author. It provides a comprehensive view of who contributed what to the codebase, using blame analysis to count current lines attributed to each author.\n\nOutput includes percentages for relative contribution comparison, and can optionally estimate development cost using the COCOMO model or elapsed commit time. The tool is useful for understanding code ownership patterns and team contribution distribution."],["CAVEATS","Blame-based counting can be slow on large histories. Binary files are skipped by default (**--warn-binary** to be notified). Only the current tree's last modifier gets credit unless **-M**/**-C** are used to trace moved or copied lines back to their original author."],["HISTORY","git fame is a Python rewrite by **Casper da Costa-Luis**, distinct from the earlier **git-fame-rb** Ruby gem by Linus Oleander that inspired the name."],["INSTALL","```dnf: sudo dnf install git-fame```\n\n```nix: nix profile install nixpkgs#git-fame```"],["SEE ALSO","[git-effort](/man/git-effort)(1), [git-authors](/man/git-authors)(1), [git-shortlog](/man/git-shortlog)(1), [git-blame](/man/git-blame)(1)"],["RESOURCES","```[Source code](https://github.com/casperdcl/git-fame)```\n\n```[Homepage](https://pypi.org/project/git-fame/)```"]],"e":["git-effort","git-authors","git-shortlog","git-blame"],"x":"git fame"},"goose":{"n":"goose","t":"Database migration tool","s":[["TAGLINE","Database migration tool"],["TLDR","**Create** a new migration\n\n```goose create [migration-name] [sql|go]```\n\n**Apply** migrations\n\n```goose up```\n\n**Rollback** last migration\n\n```goose down```\n\n**Check** migration status\n\n```goose status```"],["SYNOPSIS","**goose** [_options_] _command_"],["PARAMETERS","**up**\n> Migrate up\n\n**up-by-one**\n> Migrate up by one version\n\n**down**\n> Migrate down\n\n**down-to** _VERSION_\n> Migrate down to specific version\n\n**status**\n> Show migration status\n\n**create** _NAME_ _TYPE_\n> Create new migration (sql or go)\n\n**fix**\n> Apply sequential ordering to migrations\n\n**version**\n> Print goose version\n\n**-dir** _DIR_\n> Migrations directory (default: migrations)\n\n**-table** _TABLE_\n> Migrations table name (default: goose_db_version)\n\n**-s, --allow-missing**\n> Allow missing migrations\n\n**-v, --version**\n> Display version and exit\n\n**-h, --help**\n> Display help and exit"],["DESCRIPTION","**goose** is a database migration tool that supports both SQL and Go migrations. It manages database schema changes by versioning migration files and tracking which migrations have been applied.\n\nThe tool supports PostgreSQL, MySQL, SQLite, SQL Server, and other databases, providing a consistent interface for schema migrations."],["DATABASE DRIVERS","Supported databases:\n- PostgreSQL\n- MySQL/MariaDB\n- SQLite3\n- SQL Server\n- ClickHouse\n- Redshift\n- TiDB\n- Turso"],["CAVEATS","Requires database connection. Migration files must follow naming conventions. Down migrations should be tested carefully. Concurrent migrations may need locking."],["HISTORY","**goose** was created as a language-agnostic migration tool supporting both SQL and Go-based migrations, filling the gap for Go developers needing database migrations."],["INSTALL","```dnf: sudo dnf install goose```\n\n```brew: brew install goose```\n\n```nix: nix profile install nixpkgs#goose```"],["SEE ALSO","[migrate](https://github.com/golang-migrate/migrate), [dbmate](https://github.com/amacneil/dbmate), [flyway](https://flywaydb.org/), [liquibase](https://www.liquibase.org/)"]],"e":[],"x":"goose create [migration-name] [sql|go]"},"has":{"n":"has","t":"Check presence of command line tools and their versions","s":[["TAGLINE","Check presence of command line tools and their versions"],["TLDR","**Check if specific tools are available**\n\n```has [git] [node] [python3]```\n\n**Check a list of tools** and report missing ones\n\n```has [docker] [kubectl] [helm] [terraform]```\n\n**Use in a script** to verify dependencies\n\n```has [make] [gcc] && echo \"Ready to build\"```"],["SYNOPSIS","**has** _command_ [_command_...]"],["DESCRIPTION","**has** is a simple shell script that checks the presence of various command-line tools on your PATH and reports their installed versions. It provides a quick way to verify that all required dependencies are available before running a build, deployment, or setup script.\n\nFor each command passed as an argument, **has** checks if it exists on the PATH and attempts to determine its version. Found tools are displayed with a green checkmark and their version number, while missing tools are shown with a red cross. The exit status code reflects the number of commands not found."],["CAVEATS","**has** is a single bash script with no dependencies beyond bash itself. Version detection relies on common flags like **--version** and **-v**, which may not work for all tools. Some tools may report version information in non-standard formats."],["HISTORY","**has** was created by **Kunal Dabir** (kdabir) as a lightweight utility for verifying development environment prerequisites. It is implemented as a single **bash** script, making it trivially installable on any Unix-like system."],["INSTALL","```brew: brew install has```\n\n```nix: nix profile install nixpkgs#has```"],["SEE ALSO","[which](/man/which)(1), [command](/man/command)(1), [type](/man/type)(1)"]],"e":["which","command","type"],"x":"has [git] [node] [python3]"},"incplot":{"n":"incplot","t":"CLI tool for drawing plots in the terminal using Unicode","s":[["TAGLINE","CLI tool for drawing plots in the terminal using Unicode"],["TLDR","**Plot data from stdin**\n\n```echo \"1 2 3 4 5\" | incplot```\n\n**Plot from a CSV file**\n\n```cat [data.csv] | incplot```\n\n**Plot with a specific chart type**\n\n```cat [data.csv] | incplot --chart [bar]```"],["SYNOPSIS","**incplot** [_options_]"],["DESCRIPTION","**incplot** is a command-line tool for drawing attractive plots in the terminal using **Unicode characters**. It automatically infers what to display and how based on the data piped in, with smart features like automatic color scheme detection from the terminal, automatic filtering of extreme values, automatic sizing, and value labels with metric prefix scaling.\n\nThe tool is designed for user ergonomics and simplicity — pipe data in and get a readable chart with minimal configuration."],["CAVEATS","Requires a terminal with Unicode support for proper rendering. Output quality depends on terminal font and character support. Designed for quick visualization rather than publication-quality charts."],["HISTORY","**incplot** was created by **InCom-0** and is written in **C++**. It was designed as a quick visualization tool that prioritizes ergonomics and automatic formatting over manual configuration."],["INSTALL","```aur: yay -S incplot```"],["SEE ALSO","[gnuplot](/man/gnuplot)(1), [youplot](/man/youplot)(1)"]],"e":["gnuplot","youplot"],"x":"echo \"1 2 3 4 5\" | incplot"},"iw-phy":{"n":"iw-phy","t":"Show and configure wireless PHY (physical device) settings","s":[["TAGLINE","Show and configure wireless PHY (physical device) settings"],["TLDR","**List capabilities** of all wireless devices\n\n```iw phy```\n\n**Show detailed info** for a specific PHY\n\n```iw phy [phy0] info```\n\n**Set the channel** on a PHY\n\n```iw phy [phy0] set channel [6]```\n\n**Set a fixed transmit power** (in mBm)\n\n```iw phy [phy0] set txpower fixed [1500]```\n\n**Add a virtual interface** of a given type\n\n```iw phy [phy0] interface add [mon0] type [monitor]```\n\n**Rename** a wireless PHY\n\n```iw phy [phy0] set name [newname]```"],["SYNOPSIS","**iw** **phy** [_phyname_] _command_"],["DESCRIPTION","**iw phy** operates on a wireless PHY, the physical radio device that one or more network interfaces are built on. With no arguments it lists every wiphy and its capabilities: supported bands and channels, bitrates, ciphers, antenna configuration, and which interface modes and features the hardware allows.\n\nTargeting a named PHY (for example **phy0**) lets you query it with **info** or change radio-wide settings such as the operating channel, transmit power, retry limits, and antenna mask. The **interface add** form creates additional virtual interfaces (managed, monitor, AP, mesh, and so on) on top of the same radio."],["PARAMETERS","**info**\n> Print the full capability listing for the PHY.\n\n**set channel** _N_ _[HT/VHT]_\n> Set the operating channel, optionally with a width specifier.\n\n**set freq** _FREQ_\n> Set the operating frequency directly.\n\n**set txpower** _auto_|_fixed_|_limit_ _[mBm]_\n> Control transmit power, fixed or capped at the given level.\n\n**set name** _NAME_\n> Rename the wireless PHY.\n\n**set antenna** _TX_ _RX_\n> Configure the TX and RX antenna bitmasks.\n\n**interface add** _NAME_ **type** _TYPE_\n> Create a new virtual interface of _TYPE_ on this PHY."],["CAVEATS","Most **set** operations require root privileges, and some can only be applied while the interface is down or unused. The exact set of supported channels, powers, and interface types depends on the driver and the active regulatory domain."],["INSTALL","```apt: sudo apt install iw```\n\n```dnf: sudo dnf install iw```\n\n```pacman: sudo pacman -S iw```\n\n```apk: sudo apk add iw```\n\n```zypper: sudo zypper install iw```\n\n```nix: nix profile install nixpkgs#iw```"],["SEE ALSO","[iw](/man/iw)(8), [iwconfig](/man/iwconfig)(8), [nmcli](/man/nmcli)(1)"],["RESOURCES","```[Source code](https://git.kernel.org/pub/scm/linux/kernel/git/jberg/iw.git)```\n\n```[Documentation](https://wireless.wiki.kernel.org/en/users/documentation/iw)```"]],"e":["iw","iwconfig","nmcli"],"x":"iw phy"},"iwlist":{"n":"iwlist","t":"retrieve detailed wireless information from a network interface","s":[["TAGLINE","retrieve detailed wireless information from a network interface"],["TLDR","**Scan** for networks\n\n```iwlist wlan0 scanning```\n\nList **frequencies**\n\n```iwlist wlan0 frequency```\n\nList **bit rates**\n\n```iwlist wlan0 rate```\n\nShow **WPA** authentication\n\n```iwlist wlan0 auth```\n\nShow **WPA keys**\n\n```iwlist wlan0 wpakeys```\n\nShow **encryption** keys\n\n```iwlist wlan0 keys```\n\nShow **power** management\n\n```iwlist wlan0 power```"],["SYNOPSIS","**iwlist** _interface_ _action_"],["DESCRIPTION","**iwlist** retrieves detailed information from a wireless network interface including available networks, supported frequencies, bit rates, and encryption capabilities."],["PARAMETERS","**scan**[**ning**]\n> List access points and ad-hoc cells in range with detailed info\n\n**frequency**\n> Show available frequencies and channels\n\n**rate**\n> Show supported bit rates\n\n**keys**\n> Show WEP encryption keys and key sizes\n\n**auth**\n> Display WPA authentication parameters\n\n**wpakeys**\n> Show WPA encryption keys\n\n**genie**\n> Show Generic Information Elements (used for WPA)\n\n**power**\n> Display power management settings\n\n**txpower**\n> Show transmit power settings\n\n**retry**\n> Show transmit retry limits and retry lifetime\n\n**event**\n> List wireless events supported by the device\n\n**modulation**\n> List supported and enabled modulations"],["CAVEATS","iwlist is deprecated in favor of iw. Scanning may briefly disrupt connectivity. Some information requires root privileges to display."],["HISTORY","**iwlist** was part of the **wireless-tools** package, providing wireless network information on Linux."],["INSTALL","```apt: sudo apt install wireless-tools```\n\n```apk: sudo apk add wireless-tools```\n\n```zypper: sudo zypper install wireless-tools```"],["SEE ALSO","[iw](/man/iw)(8), [iwconfig](/man/iwconfig)(8), [nmcli](/man/nmcli)(1)"]],"e":["iw","iwconfig","nmcli"],"x":"iwlist wlan0 scanning"},"joplin":{"n":"joplin","t":"open-source note-taking application supporting Markdown","s":[["TAGLINE","open-source note-taking application supporting Markdown"],["TLDR","**Start Joplin terminal app**\n\n```joplin```\n\n**Create a new note**\n\n```joplin mknote \"[Note Title]\"```\n\n**Create a new notebook**\n\n```joplin mkbook \"[Notebook Name]\"```\n\n**List all notebooks**\n\n```joplin ls /```\n\n**List notes in a notebook**\n\n```joplin ls [notebook]```\n\n**Edit a note in your text editor**\n\n```joplin edit \"[note title]\"```\n\n**Search for notes**\n\n```joplin search \"[query]\"```\n\n**Sync with cloud storage**\n\n```joplin sync```\n\n**Add a tag to a note**\n\n```joplin tag add \"[tag]\" \"[note title]\"```"],["SYNOPSIS","**joplin** [_command_] [_options_]"],["PARAMETERS","**mknote** _title_\n> Create new note.\n\n**mkbook** _name_\n> Create new notebook.\n\n**ls** [_path_]\n> List notebooks or notes.\n\n**edit** _note_\n> Edit note in editor.\n\n**cat** _note_\n> Display note content.\n\n**rmnote** _note_\n> Delete note.\n\n**mv** _note_ _notebook_\n> Move note.\n\n**sync**\n> Synchronize with cloud.\n\n**tag** _command_ [_tag_] [_note_]\n> Manage tags (add, remove, list, notetags).\n\n**search** _query_\n> Search notes.\n\n**import** _path_ [_notebook_]\n> Import data (supports md, jex, enex formats).\n\n**export** _path_\n> Export data (supports jex, md, raw, md_frontmatter formats).\n\n**ren** _item_ _name_\n> Rename a note or notebook.\n\n**cp** _note_ [_notebook_]\n> Duplicate a note to a notebook.\n\n**mktodo** _title_\n> Create a new to-do item.\n\n**done** _note_\n> Mark a to-do as done.\n\n**undone** _note_\n> Mark a to-do as not done.\n\n**config** _key_ [_value_]\n> Get/set configuration.\n\n**status**\n> Display summary of notes and notebooks.\n\n**use** _notebook_\n> Select default notebook."],["DESCRIPTION","**Joplin** is an open-source note-taking application supporting Markdown. The terminal version provides command-line access to notes, complementing the desktop and mobile apps.\n\nJoplin supports end-to-end encryption, sync via Nextcloud/Dropbox/OneDrive/Joplin Cloud, and import from Evernote. Notes are stored in Markdown format and can be tagged and organized into notebooks."],["SYNC CONFIGURATION","```bash\njoplin config sync.target 2  # Nextcloud\njoplin config sync.2.path \"https://...\"\njoplin config sync.2.username \"user\"\n```"],["CAVEATS","Terminal version lacks some GUI features. Sync requires configuration. First sync may take time. Conflicts possible with simultaneous edits."],["HISTORY","Joplin was created by **Laurent Cozic** in **2017** as an open-source Evernote alternative. It emphasizes privacy with end-to-end encryption and local-first storage."],["INSTALL","```aur: yay -S joplin```"],["SEE ALSO","[vim](/man/vim)(1), [notable](/man/notable)(1)"]],"e":["vim","notable"],"x":"joplin"},"kde-inhibit":{"n":"kde-inhibit","t":"temporarily inhibits various KDE Plasma desktop functions while a specified","s":[["TAGLINE","temporarily inhibits various KDE Plasma desktop functions while a specified"],["TLDR","Inhibit **power management** while command runs\n\n```kde-inhibit --power [command] [arguments]```\n\nInhibit **screen saver**\n\n```kde-inhibit --screenSaver [command] [arguments]```\n\nInhibit **color correction** (night mode)\n\n```kde-inhibit --colorCorrect [command]```"],["SYNOPSIS","**kde-inhibit** [_options_] _command_ [_arguments_]"],["PARAMETERS","**--power**\n> Inhibit power management (prevent sleep/suspend)\n\n**--screenSaver**\n> Inhibit screen saver activation\n\n**--colorCorrect**\n> Inhibit color correction (night light/night mode)"],["DESCRIPTION","**kde-inhibit** temporarily inhibits various KDE Plasma desktop functions while a specified command is running. This prevents the desktop from interfering with long-running tasks or media playback.\n\nThe inhibition automatically ends when the command exits. Multiple inhibition types can be combined by specifying multiple options."],["CAVEATS","Only works in KDE Plasma desktop environment. Requires the command to keep running; inhibition ends when it exits. Does not work with commands that immediately background themselves."],["HISTORY","kde-inhibit is part of KDE's command-line tools, providing a way to control desktop behavior from scripts and applications without requiring direct DBus interaction."],["INSTALL","```apt: sudo apt install kde-cli-tools```\n\n```dnf: sudo dnf install kde-cli-tools```\n\n```pacman: sudo pacman -S kde-cli-tools```\n\n```apk: sudo apk add kde-cli-tools```"],["SEE ALSO","[systemd-inhibit](/man/systemd-inhibit)(1), [caffeine](/man/caffeine)(1)"]],"e":["systemd-inhibit","caffeine"],"x":"kde-inhibit --power [command] [arguments]"},"ktor":{"n":"ktor","t":"CLI for Ktor, a Kotlin framework for building asynchronous servers","s":[["TAGLINE","CLI for Ktor, a Kotlin framework for building asynchronous servers"],["TLDR","**Create a new project** (interactive)\n\n```ktor new```\n\n**Create with specific plugins**\n\n```ktor new --plugins [routing,content-negotiation,ktor-serialization]```\n\n**Create with custom name**\n\n```ktor new --name [myapp]```\n\n**Generate with Gradle Kotlin DSL**\n\n```ktor new --build-system gradle-kotlin```\n\n**List available plugins**\n\n```ktor plugins list```\n\n**Search for plugins**\n\n```ktor plugins search [auth]```\n\n**Show version**\n\n```ktor --version```"],["SYNOPSIS","**ktor** _command_ [_options_]"],["COMMANDS","**new**\n> Create new Ktor project.\n\n**plugins** list|search\n> Browse available plugins.\n\n**--version**\n> Display CLI version.\n\n**--help**\n> Display help information."],["NEW OPTIONS","**--name** _name_\n> Project name.\n\n**--package** _package_\n> Base package name.\n\n**--plugins** _list_\n> Comma-separated plugin list.\n\n**--build-system** _type_\n> Build system: gradle-kotlin, gradle-groovy, maven.\n\n**--engine** _engine_\n> Server engine: netty, jetty, tomcat, cio.\n\n**--configuration** _type_\n> Configuration method: hocon, yaml.\n\n**--ktor-version** _version_\n> Ktor version.\n\n**--kotlin-version** _version_\n> Kotlin version.\n\n**--output** _path_\n> Output directory.\n\n**--interactive**\n> Interactive mode."],["PLUGINS","**routing**\n> HTTP routing.\n\n**content-negotiation**\n> Content type negotiation.\n\n**ktor-serialization-kotlinx-json**\n> JSON serialization.\n\n**auth**, **auth-jwt**\n> Authentication support.\n\n**call-logging**\n> Request logging.\n\n**websockets**\n> WebSocket support.\n\n**cors**\n> Cross-origin resource sharing.\n\n**sessions**\n> Session management.\n\n**status-pages**\n> Error handling."],["DESCRIPTION","**ktor** is the CLI for Ktor, a Kotlin framework for building asynchronous servers and clients. It scaffolds new projects with selected features.\n\n**ktor new** generates a project with build files, application configuration, and plugin setup. Plugins add functionality like authentication, serialization, and WebSockets.\n\nKtor applications configure features in Application.kt using DSL. Plugins are installed in modules: **install(ContentNegotiation)**, **install(Authentication)**, etc.\n\nThe framework runs on multiple engines: Netty, Jetty, Tomcat, or CIO (Kotlin coroutines). Choose based on requirements—Netty is default for production.\n\nConfiguration via HOCON (application.conf) or YAML specifies ports, hosts, and plugin settings. Environment variables override configuration values."],["CAVEATS","Requires Kotlin knowledge. Plugin compatibility may vary across Ktor versions. CIO engine has limited features compared to Netty. Some plugins require additional configuration."],["HISTORY","Ktor was created by **JetBrains** and first released in **2017**. It was designed as a native Kotlin framework leveraging coroutines for asynchronous programming. Unlike Spring, Ktor is lightweight with explicit configuration. Version 2.0 (2022) brought significant API changes. Ktor is popular for microservices and API development in Kotlin projects."],["INSTALL","```brew: brew install ktor```"],["SEE ALSO","[gradle](/man/gradle)(1), [kotlin](/man/kotlin)(1)"]],"e":["gradle","kotlin"],"x":"ktor new"},"lowfat":{"n":"lowfat","t":"Filter CLI output to reduce noise and LLM token usage","s":[["TAGLINE","Filter CLI output to reduce noise and LLM token usage"],["TLDR","**Run a command** through lowfat's default filter pipeline\n\n```lowfat git status```\n\n**Show** active filters and compression level for a command\n\n```lowfat info git```\n\n**Display** lifetime token savings\n\n```lowfat stats```\n\n**Set** the global compression level\n\n```lowfat level ultra```\n\n**Prefix** a one-off command at a specific level\n\n```LOWFAT_LEVEL=lite lowfat git log```\n\n**Install** the OpenCode plugin\n\n```lowfat opencode install```"],["SYNOPSIS","**lowfat** [_options_] _command_ [_args_...]\n\n**lowfat** **info** [_command_] [_options_]\n\n**lowfat** **stats** [_options_]\n\n**lowfat** **history**\n\n**lowfat** **level** _lite_|_balanced_|_ultra_\n\n**lowfat** **hook**\n\n**lowfat** **shell-init** _zsh_|_bash_\n\n**lowfat** **plugin** **new** _name_\n\n**lowfat** **plugin** **doctor**\n\n**lowfat** **filter** [_options_]"],["PARAMETERS","**--config**\n> Print the full resolved configuration.\n\n**--explain**\n> Show which filters ran and why (used with **filter**).\n\n**--audit**\n> Include recent plugin execution details in **stats**.\n\n**--sub** _name_\n> Select a plugin sub-pipeline (used with **filter**).\n\n**--level** _level_\n> Override compression level for a single invocation (**lite**, **balanced**, **ultra**)."],["DESCRIPTION","**lowfat** is a lightweight Rust CLI that sits in front of shell commands and strips redundant output before it reaches a human or an AI coding agent. It targets the common case where tools such as **git**, **docker**, and **ls** emit verbose text that wastes context window space without adding decision value.\n\nThe tool ships as a single binary with built-in processors and a plugin system. Plugins are **.lf** filter scripts stored under **~/.lowfat/plugins/**; **lowfat plugin new** scaffolds a new one and **lowfat plugin doctor** validates installed plugins.\n\nIntegration modes include direct prefixing (**lowfat docker ps**), shell hook injection via **eval \"$(lowfat shell-init zsh)\"** (auto-activates when **CLAUDECODE=1**, **CODEX_ENV**, or **LOWFAT_ENABLE=1** is set), Claude Code **PreToolUse** hooks (**lowfat hook**), and an OpenCode plugin (**lowfat opencode install**). **lowfat history** ranks commands by potential savings so you can tune filters for your workflow.\n\nCompression levels (**lite**, **balanced**, **ultra**) control how aggressively output is trimmed. Configuration lives in **~/.lowfat** and can be overridden per invocation with **LOWFAT_LEVEL** or **--level**."],["CONFIGURATION","**~/.lowfat/**\n> User configuration directory for pipeline definitions and plugins.\n\n**LOWFAT_LEVEL**\n> Environment variable to force a compression level for one command.\n\n**LOWFAT_ENABLE=1**\n> Force shell integration on even outside agent environments."],["CAVEATS","**lowfat** reduces tokens by removing detail; verify critical output with **lowfat info** or by running the underlying command without the prefix when debugging. Shell integration rewrites commands transparently, which can surprise users who forget it is active. Python-based plugins may require **uv** for dependency resolution (**lowfat plugin doctor** checks this)."],["INSTALL","```aur: yay -S lowfat```"],["SEE ALSO","[git](/man/git)(1), [docker](/man/docker)(1), [awk](/man/awk)(1)"]],"e":["git","docker","awk"],"x":"lowfat git status"},"mergiraf":{"n":"mergiraf","t":"Syntax-aware Git merge conflict resolver","s":[["TAGLINE","Syntax-aware Git merge conflict resolver"],["TLDR","**Register mergiraf as a Git merge driver**\n\n```mergiraf register```\n\n**Use as a Git merge driver** (configure in .gitattributes)\n\n```*.rs merge=mergiraf```\n\n**Resolve conflicts** in a file manually\n\n```mergiraf resolve [path/to/conflicted_file]```\n\n**Resolve with explicit language** specification\n\n```mergiraf resolve -L [rust] [path/to/conflicted_file]```\n\n**Review mergiraf's automatic resolution**\n\n```mergiraf review [path/to/file]```\n\n**List supported languages**\n\n```mergiraf languages```"],["SYNOPSIS","**mergiraf** _command_ [_options_] [_file_]"],["SUBCOMMANDS","**resolve**\n> Attempt to resolve merge conflicts in a file.\n\n**review**\n> Review mergiraf's automatic conflict resolution.\n\n**register**\n> Register mergiraf as a Git merge driver in global git config.\n\n**languages**\n> List supported languages and file extensions.\n\n**report**\n> Generate an archive to reproduce a faulty merge."],["PARAMETERS","**-L, --language** _LANG_\n> Specify language by name or file extension."],["DESCRIPTION","**mergiraf** is a syntax-aware Git merge driver that resolves merge conflicts by parsing the syntax tree of code and structured files. It supports many languages including TypeScript, Python, Java, Rust, JSON, and YAML.\n\nIt operates as a drop-in driver for **git merge**, **git rebase**, and **cherry-pick**, but can also be run manually after a conflict. When it cannot safely auto-merge, it leaves clear conflict markers for manual review."],["CAVEATS","Cannot resolve all conflicts — semantic conflicts that require understanding program logic still need manual resolution. Language support varies by file type. For best results, use Git v2.44.0 or newer."],["HISTORY","**mergiraf** was created by **Antonin Delpeuch** and is written in **Rust**. The source is hosted on Codeberg."],["INSTALL","```pacman: sudo pacman -S mergiraf```\n\n```apk: sudo apk add mergiraf```\n\n```zypper: sudo zypper install mergiraf```\n\n```brew: brew install mergiraf```\n\n```nix: nix profile install nixpkgs#mergiraf```"],["SEE ALSO","[git-merge](/man/git-merge)(1), [git-mergetool](/man/git-mergetool)(1)"]],"e":["git-merge","git-mergetool"],"x":"mergiraf register"},"nbsudoku":{"n":"nbsudoku","t":"terminal sudoku puzzle game","s":[["TAGLINE","terminal sudoku puzzle game"],["TLDR","**Play sudoku**\n\n```nbsudoku```\n\n**Play with a larger board**\n\n```nbsudoku -s [4]```\n\n**Use fast puzzle generation**\n\n```nbsudoku -f```"],["SYNOPSIS","**nbsudoku** [_options_]"],["PARAMETERS","**-s** _SIZE_\n> Set puzzle size. The grid becomes (size*size) x (size*size). Default is 3 (a standard 9x9 grid); 2 gives 4x4, 4 gives 16x16.\n\n**-f**\n> Fast (flawed) puzzle generation. Generates a puzzle that may have more than one solution but runs in reasonable time. Automatically enabled for sizes larger than 4.\n\n**-h**\n> Display help information."],["PREVIEW","```\n 5 3 . | . 7 . | . . .\n 6 . . | 1 9 5 | . . .\n . 9 8 | . . . | . 6 .\n```"],["DESCRIPTION","**nbsudoku** is a terminal-based Sudoku puzzle game from the **nbsdgames** collection. Fill the grid so that each row, column, and box contains all digits exactly once.\n\nControls: arrow keys or hjkl to move cursor, number keys to enter digits, q to quit. Mouse input is also supported."],["CAVEATS","Requires a terminal with ncurses support. Part of the nbsdgames collection. Large board sizes (above 4) require fast generation mode."],["HISTORY","**nbsudoku** is part of **nbsdgames**, a package of 18 text-based terminal games created by **abakh**. Sudoku puzzles were popularized worldwide in **2004** after being featured in *The Times* newspaper."],["SEE ALSO","[nudoku](/man/nudoku)(1), [nbmines](/man/nbmines)(1), [nbfifteen](/man/nbfifteen)(1)"]],"e":["nudoku","nbmines","nbfifteen"],"x":"nbsudoku"},"nhentai":{"n":"nhentai","t":"downloads galleries from nhentai","s":[["TAGLINE","downloads galleries from nhentai"],["TLDR","**Download by ID**\n\n```nhentai --id [123456]```\n\n**Download to directory**\n\n```nhentai --id [123456] --output [downloads/]```\n\n**Download as CBZ**\n\n```nhentai --id [123456] --cbz```\n\n**Search for content**\n\n```nhentai --search \"[query]\"```\n\n**Download favorites**\n\n```nhentai --favorites```"],["SYNOPSIS","**nhentai** [_options_]"],["PARAMETERS","**--id** _ID_\n> Gallery ID to download.\n\n**--output** _DIR_\n> Output directory.\n\n**--cbz**\n> Save as CBZ archive.\n\n**--search** _QUERY_\n> Search galleries.\n\n**--favorites**\n> Download favorites.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nhentai** downloads galleries from nhentai. It's a command-line downloader.\n\nThe tool saves images locally. Supports batch downloading."],["CAVEATS","Adult content site. Respect terms of service. Network dependent."],["HISTORY","nhentai CLI was created to **download galleries** from the nhentai website."],["INSTALL","```nix: nix profile install nixpkgs#nhentai```"],["SEE ALSO","[gallery-dl](/man/gallery-dl)(1), [wget](/man/wget)(1), [curl](/man/curl)(1)"]],"e":["gallery-dl","wget","curl"],"x":"nhentai --id [123456]"},"nix-build":{"n":"nix-build","t":"builds Nix expressions","s":[["TAGLINE","builds Nix expressions"],["TLDR","**Build default.nix**\n\n```nix-build```\n\n**Build specific file**\n\n```nix-build [path/to/file.nix]```\n\n**Build with attribute**\n\n```nix-build -A [attribute]```\n\n**Build from expression**\n\n```nix-build -E \"[with import <nixpkgs> {}; hello]\"```\n\n**Don't create result symlink**\n\n```nix-build --no-out-link```\n\n**Build and keep output**\n\n```nix-build -o [result-link]```\n\n**Show build log**\n\n```nix-build -v```"],["SYNOPSIS","**nix-build** [_options_] [_path_]"],["PARAMETERS","_PATH_\n> Nix expression file.\n\n**-A** _ATTR_\n> Build specific attribute.\n\n**-E** _EXPR_\n> Build expression.\n\n**--no-out-link**\n> Don't create result link.\n\n**-o** _LINK_\n> Output symlink name.\n\n**-v**\n> Verbose mode.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nix-build** builds Nix expressions. It creates derivations and builds them.\n\nThe tool produces output in /nix/store. Creates result symlink by default."],["CAVEATS","Legacy command. Consider nix build. Nix expressions required."],["HISTORY","nix-build is part of **Nix**, providing the traditional build command interface."],["INSTALL","```apt: sudo apt install nix-bin```\n\n```dnf: sudo dnf install nix```\n\n```pacman: sudo pacman -S nix```\n\n```apk: sudo apk add nix```\n\n```zypper: sudo zypper install nix```\n\n```nix: nix profile install nixpkgs#nix```"],["SEE ALSO","[nix](/man/nix)(1), [nix-shell](/man/nix-shell)(1), [nix-env](/man/nix-env)(1)"]],"e":["nix","nix-shell","nix-env"],"x":"nix-build"},"npm-test":{"n":"npm-test","t":"runs the \"test\" script defined in package","s":[["TAGLINE","runs the \"test\" script defined in package"],["TLDR","**Run tests**\n\n```npm test```\n\n**Run tests with arguments**\n\n```npm test -- --coverage```\n\n**Run tests silently**\n\n```npm test --silent```\n\n**Shorthand**\n\n```npm t```"],["SYNOPSIS","**npm test** [-- _args_]"],["PARAMETERS","**--**\n> Pass arguments to test script.\n\n**--silent**\n> Reduce output.\n\n**--ignore-scripts**\n> Don't run scripts."],["DESCRIPTION","**npm test** runs the \"test\" script defined in package.json. It's an alias for \"npm run test\" and is the conventional command for running a project's test suite.\n\nExit code 0 indicates success; non-zero indicates test failure."],["PACKAGE.JSON","```json\n{\n  \"scripts\": {\n    \"test\": \"jest\",\n    \"test:watch\": \"jest --watch\",\n    \"test:coverage\": \"jest --coverage\"\n  }\n}\n```"],["COMMON TEST SCRIPTS","```bash"],["Jest","\"test\": \"jest\""],["Mocha","\"test\": \"mocha\""],["Vitest","\"test\": \"vitest\""],["TAP","\"test\": \"tap test/*.js\"\n```"],["CAVEATS","Requires test script to be defined. Default exits with error if no script. Use npm run test:name for custom test scripts."],["HISTORY","npm test became the standard test command for Node.js projects, integrated into CI/CD pipelines universally."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-run-script](/man/npm-run-script)(1), [npm-start](/man/npm-start)(1)"]],"e":["npm","npm-run-script","npm-start"],"x":"npm test"},"numlockx":{"n":"numlockx","t":"Control the state of NumLock","s":[["TAGLINE","Control the state of NumLock"],["TLDR","Report whether Num Lock is **on or off**\n\n```numlockx status```\n\nTurn Num Lock **on**\n\n```numlockx on```\n\nTurn Num Lock **off**\n\n```numlockx off```\n\n**Flip** the current Num Lock state\n\n```numlockx toggle```"],["SYNOPSIS","**numlockx** [_on_|_off_|_toggle_|_status_]"],["PARAMETERS","**on**\n> Turn Num Lock on\n\n**off**\n> Turn Num Lock off\n\n**toggle**\n> Toggle current Num Lock state\n\n**status**\n> Display current Num Lock state"],["DESCRIPTION","**numlockx** is a program to control the NumLock key inside X11 session scripts. It's commonly used in login scripts or desktop startup files to ensure Num Lock is in the desired state when a session begins.\n\nThe command directly manipulates the keyboard LED and state through X11, making it useful for systems where the BIOS Num Lock setting doesn't persist or for users who prefer a specific Num Lock state at login.\n\nWithout arguments, numlockx turns Num Lock on (equivalent to **numlockx on**)."],["CAVEATS","Only works in X11 sessions; does not work in Wayland or virtual consoles. The state may be reset by some desktop environments during login. For console Num Lock control, use **setleds** instead."],["INSTALL","```apt: sudo apt install numlockx```\n\n```dnf: sudo dnf install numlockx```\n\n```pacman: sudo pacman -S numlockx```\n\n```zypper: sudo zypper install numlockx```\n\n```nix: nix profile install nixpkgs#numlockx```"],["SEE ALSO","[setleds](/man/setleds)(1), [xset](/man/xset)(1), [xmodmap](/man/xmodmap)(1)"]],"e":["setleds","xset","xmodmap"],"x":"numlockx status"},"opencode-upgrade":{"n":"opencode-upgrade","t":"Update OpenCode to the latest version","s":[["TAGLINE","Update OpenCode to the latest version"],["TLDR","**Upgrade** OpenCode to the latest version\n\n```opencode upgrade```"],["SYNOPSIS","**opencode upgrade**"],["DESCRIPTION","**opencode upgrade** updates the OpenCode installation to the latest available version. It downloads and replaces the current binary with the newest release, preserving existing configuration and session data."],["CAVEATS","Requires internet access to download the latest release. May require elevated permissions depending on the installation location of the OpenCode binary."],["SEE ALSO","[opencode](/man/opencode)(1), [opencode-uninstall](/man/opencode-uninstall)(1)"]],"e":["opencode","opencode-uninstall"],"x":"opencode upgrade"},"pacman-database":{"n":"pacman-database","t":"operates directly on the package database, allowing modification of package","s":[["TAGLINE","operates directly on the package database, allowing modification of package"],["TLDR","Mark as **dependency**\n\n```sudo pacman -D --asdeps package```\n\nMark as **explicitly** installed\n\n```sudo pacman -D --asexplicit package```\n\n**Check** dependencies\n\n```pacman -Dk```\n\nCheck **sync** database\n\n```pacman -Dkk```\n\nCheck in **quiet** mode\n\n```pacman -Dkq```"],["SYNOPSIS","**pacman -D** [_OPTIONS_] [_PACKAGE_...]"],["DESCRIPTION","**pacman -D** (database) operates directly on the package database, allowing modification of package attributes without reinstalling. It can change installation reason and verify database integrity."],["PARAMETERS","**-D, --database**\n> Operate on the package database\n\n**--asdeps**\n> Mark packages as installed as dependencies\n\n**--asexplicit**\n> Mark packages as explicitly installed\n\n**-k, --check**\n> Check local database for consistency\n\n**-kk**\n> Also check sync databases\n\n**-q, --quiet**\n> Suppress normal output, show only errors"],["CAVEATS","Marking packages incorrectly can affect orphan detection. Use **--asdeps** carefully as those packages may be removed when cleaning orphans. Double **-k** performs a more thorough check including sync databases."],["HISTORY","**pacman** is the package manager for **Arch Linux** and its derivatives."],["SEE ALSO","[pacman](/man/pacman)(8), [pacman-query](/man/pacman-query)(8)"]],"e":["pacman","pacman-query"],"x":"sudo pacman -D --asdeps package"},"pamtopnm":{"n":"pamtopnm","t":"converts PAM images to PNM format","s":[["TAGLINE","converts PAM images to PNM format"],["TLDR","**Convert PAM to PNM format**\n\n```pamtopnm [input.pam] > [output.pnm]```\n\n**Convert, overriding tuple type check with -assume**\n\n```pamtopnm -assume [input.pam] > [output.pnm]```\n\n**Convert silently without informational messages**\n\n```pamtopnm -quiet [input.pam] > [output.pnm]```"],["SYNOPSIS","**pamtopnm** [**-assume**] [**-quiet**] [_file_]"],["PARAMETERS","**-assume**\n> Override the tuple type requirement. Asserts that the tuples contain valid PBM, PGM, or PPM data regardless of the PAM tuple type. The depth must still conform to the target format.\n\n**-quiet**\n> Suppress informational messages to standard error."],["DESCRIPTION","**pamtopnm** reads a PAM image as input and produces an equivalent PBM, PGM, or PPM (i.e. PNM) image as output. The output format is chosen automatically based on the input: BLACKANDWHITE becomes PBM, GRAYSCALE becomes PGM, and RGB becomes PPM.\n\nIf the input PAM does not have an appropriate tuple type (BLACKANDWHITE, GRAYSCALE, or RGB) and depth, pamtopnm fails unless **-assume** is specified. To force conversion of images with extra channels, run the input through **pamchannel** first to select the desired channels.\n\nPart of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamtopam](/man/pamtopam)(1), [netpbm](/man/netpbm)(1)"]],"e":["pamtopam","netpbm"],"x":"pamtopnm [input.pam] > [output.pnm]"},"pants":{"n":"pants","t":"scalable build system for monorepos","s":[["TAGLINE","scalable build system for monorepos"],["TLDR","**List targets**\n\n```pants list ::```\n\n**Run tests**\n\n```pants test ::```\n\n**Format code**\n\n```pants fmt ::```\n\n**Lint code**\n\n```pants lint ::```\n\n**Build package**\n\n```pants package [path/to:target]```\n\n**Run specific target**\n\n```pants run [path/to:target]```\n\n**Check types**\n\n```pants check ::```\n\n**Show dependencies**\n\n```pants dependencies [path/to:target]```"],["SYNOPSIS","**pants** [_options_] _goal_ [_targets_...]"],["PARAMETERS","**list**\n> List matching targets.\n\n**test**\n> Run tests for matching test targets.\n\n**fmt**\n> Format source files in-place using configured formatters.\n\n**lint**\n> Run lint checks on matching targets.\n\n**check**\n> Run type/compilation checks (e.g. mypy, javac).\n\n**package**\n> Build deployable artifacts (wheels, pex, jars, docker images).\n\n**run**\n> Run an executable target.\n\n**publish**\n> Publish deployable artifacts to a remote registry.\n\n**dependencies**\n> Show the transitive dependencies of a target.\n\n**dependents**\n> Show targets that depend on the given target.\n\n**roots**\n> List all root targets that match.\n\n**peek**\n> Print detailed metadata about matching targets.\n\n**repl**\n> Start an interactive REPL preloaded with the specified targets.\n\n**tailor**\n> Auto-generate or update BUILD files.\n\n**update-build-files**\n> Apply deprecation fixes to existing BUILD files.\n\n**--changed-since** _REF_\n> Limit targets to those changed since the given git ref.\n\n**--changed-dependents=**_MODE_\n> Include dependents of changed targets (_none_, _direct_, _transitive_).\n\n**-l**, **--level** _LEVEL_\n> Log level (trace, debug, info, warn, error).\n\n**--keep-sandboxes=**_MODE_\n> Keep the execution sandbox for debugging (_always_, _on_failure_, _never_).\n\n**--no-local-cache**, **--no-remote-cache**\n> Disable the local or remote build cache for the run."],["DESCRIPTION","**pants** is a scalable build system for monorepos. It supports Python, Go, Java, Scala, Shell, and Docker.\n\nTarget notation uses colons to separate path and target name. Double colons (::) match all targets recursively.\n\nCaching and memoization make repeated builds fast. Remote caching enables team-wide build sharing.\n\nDependency inference automatically discovers imports. Explicit dependencies can be declared in BUILD files.\n\nFine-grained invalidation rebuilds only affected targets. Concurrent execution uses available cores."],["CAVEATS","Requires BUILD files defining targets. Initial setup has learning curve. Large codebase initial run can be slow."],["HISTORY","**Pants** was originally developed at **Twitter** around **2011**. Version 2.x was a ground-up rewrite focusing on Python ergonomics. Now maintained by **Toolchain** as an open-source project."],["SEE ALSO","[bazel](/man/bazel)(1), [buck](/man/buck)(1), [make](/man/make)(1), [gradle](/man/gradle)(1)"]],"e":["bazel","buck","make","gradle"],"x":"pants list ::"},"phpstan":{"n":"phpstan","t":"performs static analysis on PHP code, finding bugs without running the code","s":[["TAGLINE","performs static analysis on PHP code, finding bugs without running the code"],["TLDR","**Analyze source directory**\n\n```phpstan analyse [src/]```\n\n**Analyze with specific level**\n\n```phpstan analyse --level [5] [src/]```\n\n**Analyze using config file**\n\n```phpstan analyse -c [phpstan.neon]```\n\n**Analyze with max level**\n\n```phpstan analyse --level max [src/]```\n\n**Generate baseline**\n\n```phpstan analyse --generate-baseline```\n\n**Clear cache**\n\n```phpstan clear-result-cache```\n\n**Show version**\n\n```phpstan --version```"],["SYNOPSIS","**phpstan** [_analyse_] [_--level n_] [_-c config_] [_options_] [_paths_]"],["PARAMETERS","**analyse**, **analyze**\n> Run analysis.\n\n**--level**, **-l** _N_\n> Rule level (0-9 or max).\n\n**-c** _FILE_\n> Configuration file.\n\n**--configuration** _FILE_\n> Configuration file (long form).\n\n**--memory-limit** _SIZE_\n> Memory limit.\n\n**--debug**\n> Debug mode.\n\n**--generate-baseline**\n> Generate error baseline.\n\n**-b** _FILE_\n> Baseline file.\n\n**--error-format** _FMT_\n> Output format (table, json, raw, etc.).\n\n**--no-progress**\n> Disable progress bar.\n\n**clear-result-cache**\n> Clear analysis cache."],["DESCRIPTION","**phpstan** performs static analysis on PHP code, finding bugs without running the code. It catches type errors, undefined methods, and incorrect function calls.\n\nAnalysis levels range from 0 (basic) to 9 (strictest). Higher levels check more conditions but may find more false positives in legacy code. Start low and increase gradually.\n\nConfiguration files (phpstan.neon) define paths, excludes, and custom rules. They enable consistent analysis across team members and CI pipelines.\n\nBaseline files ignore existing errors, enabling gradual adoption. New code must pass analysis while legacy issues can be fixed over time.\n\nThe tool integrates with IDEs through plugins, showing errors as you type. CI integration catches regressions before merge.\n\nPHPStan supports PHPDoc annotations for type hints in older code. Modern PHP type declarations are preferred when possible."],["CAVEATS","High levels may be impractical for legacy code. Some dynamic PHP patterns can't be analyzed. Extensions needed for frameworks. Memory usage scales with codebase size."],["HISTORY","**PHPStan** was created by **Ondřej Mirtes** in **2016** to bring static typing benefits to PHP. It became one of the most popular PHP static analysis tools, competing with Psalm and Phan."],["INSTALL","```brew: brew install phpstan```\n\n```nix: nix profile install nixpkgs#phpstan```"],["SEE ALSO","[psalm](/man/psalm)(1), [php](/man/php)(1), [composer](/man/composer)(1), [phpcs](/man/phpcs)(1)"]],"e":["psalm","php","composer","phpcs"],"x":"phpstan analyse [src/]"},"picotool":{"n":"picotool","t":"manages Raspberry Pi Pico and RP2040 devices","s":[["TAGLINE","manages Raspberry Pi Pico and RP2040 devices"],["TLDR","**Show device info**\n\n```picotool info```\n\n**Load binary**\n\n```picotool load [firmware.uf2]```\n\n**Save flash contents**\n\n```picotool save -a [backup.bin]```\n\n**Verify binary**\n\n```picotool verify [firmware.uf2]```\n\n**Reboot device**\n\n```picotool reboot```\n\n**Show binary info**\n\n```picotool info [firmware.elf]```\n\n**Erase flash**\n\n```picotool load --erase```"],["SYNOPSIS","**picotool** _command_ [_options_] [_file_]"],["PARAMETERS","**info**\n> Show device/file info.\n\n**load**\n> Load binary to device.\n\n**save**\n> Save from device.\n\n**verify**\n> Verify device contents.\n\n**reboot**\n> Reboot device.\n\n**version**\n> Show version.\n\n**-a**, **--all**\n> All flash contents.\n\n**-x**, **--execute**\n> Execute after load.\n\n**-f**, **--force**\n> Force operation."],["DESCRIPTION","**picotool** manages Raspberry Pi Pico and RP2040 devices. It loads firmware, reads device info, and controls boot mode.\n\nDevice info shows chip details and running program. Binary info displays embedded metadata.\n\nLoading writes firmware to flash. UF2 and ELF formats are supported.\n\nSave extracts flash contents for backup. The entire flash or specific regions can be dumped.\n\nVerification compares device contents to a file. Useful for confirming successful programming."],["CAVEATS","Device must be in BOOTSEL mode for many operations. USB connection required. Some operations erase existing firmware."],["HISTORY","**picotool** was created by **Raspberry Pi** for their RP2040 microcontroller. Released with the Pico in **2021**, it provides official tooling for device management."],["INSTALL","```apt: sudo apt install picotool```\n\n```zypper: sudo zypper install picotool```\n\n```brew: brew install picotool```\n\n```nix: nix profile install nixpkgs#picotool```"],["SEE ALSO","[openocd](/man/openocd)(1), [gdb](/man/gdb)(1), [stlink](/man/stlink)(1)"]],"e":["openocd","gdb","stlink"],"x":"picotool info"},"pio-boards":{"n":"pio-boards","t":"lists supported development boards in PlatformIO","s":[["TAGLINE","lists supported development boards in PlatformIO"],["TLDR","**List all supported boards**\n\n```pio boards```\n\n**Search for boards**\n\n```pio boards [arduino]```\n\n**List boards in JSON format**\n\n```pio boards --json-output```\n\n**Filter by installed platforms**\n\n```pio boards --installed```"],["SYNOPSIS","**pio boards** [_options_] [_filter_]"],["PARAMETERS","**--installed**\n> Show only installed boards.\n\n**--json-output**\n> Output in JSON format.\n\n_filter_\n> Search filter string."],["DESCRIPTION","**pio boards** lists supported development boards in PlatformIO. It displays a formatted table with board ID, MCU type, frequency, flash memory, RAM, and board name. Use board IDs with **pio init** for project setup.\n\nAn optional filter argument narrows results by platform, MCU, or board name (e.g., `pio boards arduino`, `pio boards atmega168`)."],["INSTALL","```aur: yay -S pio```"],["SEE ALSO","[pio](/man/pio)(1), [pio-init](/man/pio-init)(1), [pio-run](/man/pio-run)(1), [pio-platform](/man/pio-platform)(1)"]],"e":["pio","pio-init","pio-run","pio-platform"],"x":"pio boards"},"pio-run":{"n":"pio-run","t":"builds PlatformIO projects","s":[["TAGLINE","builds PlatformIO projects"],["TLDR","**Build project**\n\n```pio run```\n\n**Build and upload to device**\n\n```pio run --target upload```\n\n**Build specific environment**\n\n```pio run -e [uno]```\n\n**Clean build files**\n\n```pio run --target clean```\n\n**Build specific environment** and upload\n\n```pio run -e [esp32dev] --target upload```\n\n**Build from a specific** project directory\n\n```pio run -d [/path/to/project]```\n\n**Build with verbose** output for debugging\n\n```pio run -v```\n\n**Build with parallel** jobs for faster compilation\n\n```pio run -j [4]```"],["SYNOPSIS","**pio run** [_options_]"],["PARAMETERS","**-e**, **--environment** _name_\n> Process specific environment defined in platformio.ini.\n\n**-t**, **--target** _target_\n> Process target (upload, clean, program, uploadfs, etc.).\n\n**-d**, **--project-dir** _dir_\n> Project directory (default: current directory).\n\n**--upload-port** _port_\n> Upload port (e.g., /dev/ttyUSB0 or COM3).\n\n**-j**, **--jobs** _num_\n> Number of parallel build jobs.\n\n**-v**, **--verbose**\n> Verbose output showing full compiler commands.\n\n**-s**, **--silent**\n> Suppress all output except errors.\n\n**--disable-auto-clean**\n> Disable automatic cleaning if environment configuration has changed.\n\n**--list-targets**\n> List available project targets."],["DESCRIPTION","**pio run** builds PlatformIO projects. Compiles source code, links libraries, and optionally uploads firmware to target devices. It reads the `platformio.ini` configuration file to determine build environments, board settings, and library dependencies. This is the core command for the embedded development workflow with PlatformIO.\n\nMultiple environments can be defined in platformio.ini and selectively built with the **-e** flag. Common targets include **upload** (flash firmware), **clean** (remove build artifacts), and **uploadfs** (upload filesystem image)."],["INSTALL","```aur: yay -S pio```"],["SEE ALSO","[pio-init](/man/pio-init)(1), [pio-device](/man/pio-device)(1), [pio-test](/man/pio-test)(1), [pio-check](/man/pio-check)(1), [pio-debug](/man/pio-debug)(1), [pio-boards](/man/pio-boards)(1)"]],"e":["pio-init","pio-device","pio-test","pio-check","pio-debug","pio-boards"],"x":"pio run"},"pnmhisteq":{"n":"pnmhisteq","t":"Equalize histogram of PNM images","s":[["TAGLINE","Equalize histogram of PNM images"],["TLDR","**Equalize histogram**\n\n```pnmhisteq [input.pnm] > [output.pnm]```\n\n**Verbose output**\n\n```pnmhisteq -verbose [input.pnm] > [output.pnm]```\n\n**Equalize grayscale**\n\n```ppmtopgm [input.ppm] | pnmhisteq > [output.pgm]```"],["SYNOPSIS","**pnmhisteq** [_options_] [_pnmfile_]"],["PARAMETERS","**-gray**\n> Equalize on gray value.\n\n**-verbose**\n> Print information."],["DESCRIPTION","**pnmhisteq** performs histogram equalization on a PNM image. This process spreads out the most frequent intensity values, improving contrast in images with clustered brightness levels.\n\nPart of Netpbm toolkit for image enhancement."],["EXAMPLES","```bash"],["Basic histogram equalization","pnmhisteq photo.pgm > enhanced.pgm"],["Color image via grayscale","jpegtopnm photo.jpg | pnmhisteq > enhanced.ppm"],["Chain with conversion","pnmhisteq dark.pgm | pnmtopng > enhanced.png\n```"],["ALGORITHM","Histogram equalization maps input intensities to output intensities so that the output histogram is approximately uniform. This maximizes image contrast."],["CAVEATS","May increase noise in uniform areas. Results depend on image content. Better for grayscale than color images."],["HISTORY","pnmhisteq is part of **Netpbm** by **Jef Poskanzer** and contributors, implementing classic histogram equalization."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pgmhist](/man/pgmhist)(1), [pnmnorm](/man/pnmnorm)(1), [pgmenhance](/man/pgmenhance)(1), [netpbm](/man/netpbm)(1)"]],"e":["pgmhist","pnmnorm","pgmenhance","netpbm"],"x":"pnmhisteq [input.pnm] > [output.pnm]"},"prowler-azure":{"n":"prowler-azure","t":"Azure cloud security assessment","s":[["TAGLINE","Azure cloud security assessment"],["TLDR","**Run Azure security assessment**\n\n```prowler azure```\n\n**Run specific checks**\n\n```prowler azure --checks [storage_account_public_access]```\n\n**Run with compliance framework**\n\n```prowler azure --compliance [cis_2.0_azure]```\n\n**Specify subscription**\n\n```prowler azure --subscription-ids [sub-id-1]```"],["SYNOPSIS","**prowler azure** [_options_]"],["PARAMETERS","**--checks** _checks_\n> Specific checks to run.\n\n**--services** _services_\n> Services to check.\n\n**--compliance** _framework_\n> Compliance framework.\n\n**--subscription-ids** _ids_\n> Azure subscription IDs.\n\n**-M**, **--output-modes** _format_\n> Output format.\n\n**-o**, **--output-directory** _dir_\n> Output directory."],["DESCRIPTION","**prowler azure** performs security assessment of Azure subscriptions. Checks configuration against best practices and compliance frameworks. Identifies security issues in Azure resources."],["INSTALL","```brew: brew install prowler```\n\n```nix: nix profile install nixpkgs#prowler```"],["SEE ALSO","[prowler](/man/prowler)(1), [az](/man/az)(1)"]],"e":["prowler","az"],"x":"prowler azure"},"pueue-stash":{"n":"pueue-stash","t":"Move pueue tasks to stashed state","s":[["TAGLINE","Move pueue tasks to stashed state"],["TLDR","**Stash a queued task**\n\n```pueue stash [task_id]```\n\n**Stash multiple tasks**\n\n```pueue stash [id1] [id2] [id3]```\n\n**Stash all tasks in a group**\n\n```pueue stash --group [group_name]```\n\n**Unstash (enqueue) a stashed task**\n\n```pueue enqueue [task_id]```\n\n**Add a new task directly to stash**\n\n```pueue add --stashed \"[command]\"```"],["SYNOPSIS","**pueue stash** [_options_] [_task_ids_...]"],["PARAMETERS","_task_ids_\n> One or more task IDs to stash.\n\n**--group** _name_\n> Stash all tasks in the specified group.\n\n**--all**\n> Stash all queued tasks."],["DESCRIPTION","**pueue stash** moves queued tasks to a stashed state, preventing them from being automatically started. Stashed tasks remain in pueue but won't run until explicitly enqueued with **pueue enqueue**. This is useful for preparing tasks to run later or temporarily holding tasks without losing them."],["STASH VS PAUSE","```\nStash  - Remove from queue entirely, won't auto-start\nPause  - Stop execution but keep in queue position\n```"],["CAVEATS","Stashed tasks do not maintain their original queue position when re-enqueued. Only queued (not running) tasks can be stashed."],["HISTORY","pueue stash is part of **pueue** by **Arne Beer** for managing task queue states."],["INSTALL","```pacman: sudo pacman -S pueue```\n\n```apk: sudo apk add pueue```\n\n```zypper: sudo zypper install pueue```\n\n```brew: brew install pueue```\n\n```nix: nix profile install nixpkgs#pueue```"],["SEE ALSO","[pueue](/man/pueue)(1), [pueue-enqueue](/man/pueue-enqueue)(1), [pueue-add](/man/pueue-add)(1), [pueue-status](/man/pueue-status)(1)"]],"e":["pueue","pueue-enqueue","pueue-add","pueue-status"],"x":"pueue stash [task_id]"},"puppet-apply":{"n":"puppet-apply","t":"Apply Puppet manifests locally","s":[["TAGLINE","Apply Puppet manifests locally"],["TLDR","**Apply a manifest**\n\n```puppet apply [manifest.pp]```\n\n**Dry run without making changes**\n\n```puppet apply --noop [manifest.pp]```\n\n**Apply with verbose output**\n\n```puppet apply --verbose [manifest.pp]```\n\n**Apply with full debug output**\n\n```puppet apply --debug [manifest.pp]```\n\n**Execute inline Puppet code**\n\n```puppet apply -e \"package { 'nginx': ensure => present }\"```\n\n**Apply with common testing options** (verbose, detailed-exitcodes, show_diff)\n\n```puppet apply --test [manifest.pp]```\n\n**Log output to a file**\n\n```puppet apply --logdest [/var/log/puppet.log] [manifest.pp]```"],["SYNOPSIS","**puppet apply** [_options_] _manifest_"],["PARAMETERS","_MANIFEST_\n> Puppet manifest file to compile and apply.\n\n**--noop**\n> Dry run mode; shows what changes would be made without applying them.\n\n**-v**, **--verbose**\n> Print extra information during execution.\n\n**-d**, **--debug**\n> Enable full debugging output.\n\n**-e**, **--execute** _CODE_\n> Execute a specific piece of Puppet code instead of a manifest file.\n\n**--test**\n> Enable common testing options: verbose, detailed-exitcodes, and show_diff.\n\n**--detailed-exitcodes**\n> Provide extra information via exit codes (0=no changes, 2=changes applied, 4=failures, 6=both).\n\n**-l**, **--logdest** _DEST_\n> Where to send log messages (syslog, eventlog, console, or a file path). Defaults to console.\n\n**--catalog** _CATALOG_\n> Apply a JSON catalog instead of compiling a manifest. Accepts a file path or stdin.\n\n**-L**, **--loadclasses**\n> Load any stored classes from the classfile.\n\n**--modulepath** _PATH_\n> Module search path for resolving classes and defined types."],["DESCRIPTION","**puppet apply** executes Puppet manifests directly on the local system without requiring a Puppet server. It compiles and applies the specified manifest file, making it ideal for standalone configuration management, testing manifests during development, and bootstrapping systems before they join a Puppet infrastructure.\n\nThe command accepts manifest files written in Puppet's declarative DSL or inline code via the **-e** flag. It supports **--noop** for dry-run previews of changes, **--verbose** and **--debug** for detailed execution output, and **--modulepath** for specifying where to find Puppet modules. Since it operates independently of a server, puppet apply is commonly used in container image builds, Vagrant provisioning, and one-off system configuration tasks."],["CONFIGURATION","**/etc/puppetlabs/puppet/puppet.conf**\n> Configuration file controlling module paths, log level, environment settings, and other Puppet behavior for local apply runs.\n\n**/etc/puppetlabs/code/modules/**\n> Default module path searched when resolving classes and defined types referenced in manifests."],["CAVEATS","Requires Puppet installed. Operates without a Puppet server (masterless mode). Must be run as root to manage system resources."],["HISTORY","puppet apply provides **local Puppet** manifest execution."],["INSTALL","```dnf: sudo dnf install puppet```\n\n```pacman: sudo pacman -S puppet```"],["SEE ALSO","[puppet](/man/puppet)(1), [puppet-agent](/man/puppet-agent)(1)"]],"e":["puppet","puppet-agent"],"x":"puppet apply [manifest.pp]"},"qm-nbdstop":{"n":"qm-nbdstop","t":"Stop embedded NBD server for a Proxmox VM","s":[["TAGLINE","Stop embedded NBD server for a Proxmox VM"],["TLDR","**Stop the embedded NBD server for VM 100**\n\n```qm nbdstop [100]```"],["SYNOPSIS","**qm nbdstop** _vmid_"],["PARAMETERS","**_vmid_**\n> The unique ID of the VM (integer, 100 to 999999999)."],["DESCRIPTION","**qm nbdstop** stops the embedded NBD (Network Block Device) server for a virtual machine. The NBD server is used internally during disk operations such as backup and live migration to export VM disk images over the network.\n\nThis command terminates any active NBD export for the specified VM."],["CAVEATS","Stopping the NBD server during active transfers may cause data corruption or incomplete backups. Ensure no operations are using the NBD connection before stopping. Requires root privileges on the Proxmox VE host."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools for virtual machine administration. The NBD stop command was added to support advanced storage management and live migration workflows."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-migrate](/man/qm-migrate)(1), [qm-start](/man/qm-start)(1), [qm-stop](/man/qm-stop)(1), [qm-monitor](/man/qm-monitor)(1)"]],"e":["qm","qm-migrate","qm-start","qm-stop","qm-monitor"],"x":"qm nbdstop [100]"},"robotfindskitten":{"n":"robotfindskitten","t":"zen simulation game where a robot finds kitten","s":[["TAGLINE","zen simulation game where a robot finds kitten"],["TLDR","**Start a game**\n\n```robotfindskitten```\n\n**Set number of non-kitten items**\n\n```robotfindskitten [20]```"],["SYNOPSIS","**robotfindskitten** [_num_items_]"],["PREVIEW","```\n        #\n  .  &  @  %\n     $\n  ?     !\n```"],["DESCRIPTION","**robotfindskitten** is a zen simulation. The player guides robot (**#**) around the screen, bumping into various objects. Each object displays a humorous description. One of the items is kitten — find it and the game is won.\n\nUse arrow keys or hjkl to move. The game features hundreds of whimsical descriptions for the non-kitten items."],["CAVEATS","The point of the game is the journey, not the destination. There is no score, no time limit, and no way to lose."],["HISTORY","**robotfindskitten** was created by **Leonard Richardson** in **1997**. It has become a beloved classic in hacker culture, with ports to over 100 platforms."],["INSTALL","```dnf: sudo dnf install robotfindskitten```\n\n```pacman: sudo pacman -S robotfindskitten```\n\n```brew: brew install robotfindskitten```\n\n```nix: nix profile install nixpkgs#robotfindskitten```"],["SEE ALSO","[adventure](/man/adventure)(6), [wump](/man/wump)(6)"]],"e":["adventure","wump"],"x":"robotfindskitten"},"rocky":{"n":"rocky","t":"Programmatic HTTP and WebSocket reverse proxy CLI","s":[["TAGLINE","Programmatic HTTP and WebSocket reverse proxy CLI"],["TLDR","**Forward** all traffic from a local port to an upstream server\n\n```rocky --port [8080] --forward [http://server:9000]```\n\n**Forward selected routes** while ignoring everything else\n\n```rocky --port [8080] --forward [http://server] --route \"[/api/*, /images/*]\"```\n\n**Replay traffic** to a second backend (shadow / mirror requests)\n\n```rocky --port [8080] --forward [http://primary] --replay [http://shadow]```\n\n**Load balance** between multiple upstream servers\n\n```rocky --port [8080] --balance \"[http://a:9000, http://b:9000]\"```\n\n**Run from a TOML config file**\n\n```rocky --config [path/to/rocky.toml]```\n\n**Serve over HTTPS** with a key and certificate\n\n```rocky --port [8443] --key [server.key] --cert [server.crt] --forward [http://backend]```"],["SYNOPSIS","**rocky** [_options_]"],["PARAMETERS","**-c** _file_, **--config** _file_\n> Load configuration from a TOML file.\n\n**-p** _port_, **--port** _port_\n> TCP port for the local HTTP server.\n\n**-f** _url_, **--forward** _url_\n> Default upstream URL to forward traffic to.\n\n**-r** _url_, **--replay** _url_\n> Additional upstream that receives a copy of every request (replay/shadow target). Repeatable.\n\n**-t** _routes_, **--route** _routes_\n> Comma-separated list of route patterns to handle. Glob-style wildcards are supported.\n\n**-b** _urls_, **--balance** _urls_\n> Comma-separated list of upstream URLs to balance traffic between.\n\n**-k** _file_, **--key** _file_\n> Path to the SSL/TLS private key.\n\n**-e** _file_, **--cert** _file_\n> Path to the SSL/TLS certificate.\n\n**-s**, **--secure**\n> Enable strict SSL certificate validation when proxying to HTTPS upstreams.\n\n**-m**, **--mute**\n> Disable HTTP traffic logging.\n\n**-d**, **--debug**\n> Enable verbose debug output.\n\n**-v**, **--version**\n> Print the version and exit.\n\n**-h**, **--help**\n> Show usage help."],["DESCRIPTION","**rocky** is a middleware-oriented HTTP and WebSocket reverse proxy built on Node.js. The **rocky-cli** front-end exposes its core features through a single command so a proxy can be started without writing JavaScript. It can forward requests to one or more backends, replay traffic to shadow services, balance load across upstreams, and intercept or transform requests and responses on the fly.\n\nRoutes are expressed as glob patterns and matched against the request path, letting you proxy only specific endpoints while ignoring others. Replay targets receive a copy of each forwarded request, which is useful for blue/green deployments, regression testing against a new backend, or recording traffic for later analysis.\n\nConfiguration can be passed entirely on the command line or loaded from a TOML file via **--config**, making it easy to commit reproducible proxy setups alongside the rest of a project."],["CONFIGURATION","A typical **rocky.toml** file looks like this:\n\n```\nport = 8080\nforward = \"http://127.0.0.1:9000\"\nreplay = [\"http://127.0.0.1:9001\"]\n\n[[routes]]\npath = \"/api/*\"\nforward = \"http://api.internal:8080\"\n\n[[routes]]\npath = \"/static/*\"\nforward = \"http://cdn.internal\"\n```\n\nCommand-line flags override values from the configuration file when both are supplied."],["CAVEATS","The upstream **rocky** project is **deprecated** and no longer actively maintained, so it should not be relied on for new production deployments. Because it runs on Node.js, performance under heavy load is lower than that of native proxies such as **nginx** or **HAProxy**. Glob route matching is greedy; order patterns from most specific to most general to avoid unexpected forwarding decisions."],["HISTORY","**rocky** was created by **Tomás Aparicio** (h2non) and released as an open-source Node.js HTTP proxy framework. The companion **rocky-cli** package shipped shortly afterwards to expose the same functionality without writing code. Both projects have been archived as deprecated, with users typically migrating to dedicated proxies such as **nginx**, **HAProxy**, or **Envoy**."],["INSTALL","```aur: yay -S rocky```"],["SEE ALSO","[nginx](/man/nginx)(8), [haproxy](/man/haproxy)(1), [caddy](/man/caddy)(1), [mitmproxy](/man/mitmproxy)(1), [socat](/man/socat)(1)"]],"e":["nginx","haproxy","caddy","mitmproxy","socat"],"x":"rocky --port [8080] --forward [http://server:9000]"},"rudoc":{"n":"rudoc","t":"Fast dependency-free document converter (Pandoc alternative)","s":[["TAGLINE","Fast dependency-free document converter (Pandoc alternative)"],["TLDR","**Convert Markdown to DOCX** (formats auto-detected from extensions)\n\n```rudoc [README.md] [README.docx]```\n\n**Convert DOCX to PDF**\n\n```rudoc [report.docx] [report.pdf]```\n\n**Convert Markdown to HTML** with explicit formats\n\n```rudoc -f md -t html [README.md]```\n\n**Write output** to a path\n\n```rudoc [README.md] -o [/tmp/readme.pdf]```\n\n**Convert CSV to XLSX**\n\n```rudoc [data.csv] [report.xlsx]```\n\n**Convert Markdown to PowerPoint slides**\n\n```rudoc [notes.md] [slides.pptx]```\n\n**Pipe Markdown to HTML** on stdout\n\n```cat [README.md] | rudoc -f md -t html```\n\n**Merge multiple Markdown files** into one document\n\n```rudoc [ch1.md] [ch2.md] [ch3.md] -t html -o [book.html]```"],["SYNOPSIS","**rudoc** [**-f** _FORMAT_] [**-t** _FORMAT_] [**-o** _FILE_] [_options_] [_input_...]"],["PARAMETERS","**-f**, **--from** _FORMAT_\n\n> Input format (auto-detected from file extension when omitted). Names are case-insensitive; aliases include **md**/**markdown**/**gfm**, **html**, **txt**, **docx**, **typ**/**typst**, **pdf**, **pptx**, **csv**, **xlsx**, **xml**, **opml**, **json**.\n\n**-t**, **--to** _FORMAT_\n\n> Output format (auto-detected from the output path or second path argument when omitted).\n\n**-o**, **--output** _FILE_\n\n> Output path. Default is stdout when formats allow streaming.\n\n**--standalone**\n\n> Emit a full HTML document with head metadata and CSS.\n\n**--slide-level** _N_\n\n> Heading level that starts a new slide when writing **pptx** (default: **1**).\n\n**--sheet** _NAME_\n\n> Sheet name for XLSX input/output (default: **Sheet1**).\n\n**--pdf-paper** _SIZE_\n\n> Paper size for PDF: **a4**, **a3**, **a5**, **us-letter**, **us-legal** (default: **a4**).\n\n**--pdf-font** _NAME_\n\n> Body font for Typst/PDF output (default: **Arial**).\n\n**--wrap** _COLS_\n\n> Line-wrap width for **md**/**txt** output (**0** = off).\n\n**-q**, **--quiet**\n\n> Suppress progress messages.\n\n**-v**, **--verbose**\n\n> Show intermediate IR stats and timing.\n\n**-h**, **--help**\n\n> Print help.\n\n**--version**\n\n> Print version."],["DESCRIPTION","**rudoc** is a small, static Rust document converter aimed at the common conversions people use every day. It is designed as a focused alternative to **pandoc**: a few megabytes, sub-5 ms startup, no language runtime, and a curated set of formats rather than dozens of backends.\n\nSupported rich-text conversions are bidirectional among **Markdown**, **HTML**, **plain text**, **DOCX**, **Typst** (**.typ**), and **PDF** (PDF input is text extraction only; layout is not preserved). Presentations go **Markdown → PPTX**, with heading levels controlling slide boundaries. Tabular data converts **CSV ↔ XLSX**. Structured data converts **XML ↔ OPML ↔ JSON** with lossless round-trips inside that group.\n\nFormats are usually inferred from file extensions, so many invocations need no flags. Explicit **-f** / **-t** overrides, stdin/stdout piping, multi-file Markdown merge, and PDF paper/font options cover scripted and batch use. For PDF output, rudoc prefers the **typst** CLI when present, can use a built-in printpdf path when compiled with the **pdf** feature, and otherwise prints install guidance."],["CAVEATS","Rudoc intentionally supports fewer formats than pandoc (roughly a dozen focused paths). PDF **reading** extracts text only and does not preserve layout. Highest-quality PDF **writing** works best with **typst** on **PATH**; without it you need a build that includes the optional built-in PDF feature, or conversion stops with an error. Presentation export is Markdown → PPTX only (not full bidirectional slide conversion)."],["SEE ALSO","[pandoc](/man/pandoc)(1), [typst](/man/typst)(1), [libreoffice](/man/libreoffice)(1), [soffice](/man/soffice)(1)"],["RESOURCES","```[Source code](https://github.com/asong56/rudoc)```"]],"e":["pandoc","typst","libreoffice","soffice"],"x":"rudoc [README.md] [README.docx]"},"sacctmgr":{"n":"sacctmgr","t":"Manage Slurm accounts, users, and clusters","s":[["TAGLINE","Manage Slurm accounts, users, and clusters"],["TLDR","**Show** current configuration\n\n```sacctmgr show configuration```\n\n**Add** a cluster to the Slurm database\n\n```sacctmgr add cluster [cluster_name]```\n\n**Add** an account to the Slurm database\n\n```sacctmgr add account [account_name] cluster=[cluster_name]```\n\n**Add** a user to an account\n\n```sacctmgr add user [username] account=[account_name] cluster=[cluster_name]```\n\n**Show** details with custom format columns\n\n```sacctmgr show account format=Account%20,Description%30```\n\n**Show** QOS definitions\n\n```sacctmgr show qos```\n\n**Dump** cluster association data to a file\n\n```sacctmgr dump [cluster_name] file=[output.cfg]```\n\n**Modify** a user's QOS\n\n```sacctmgr modify user where name=[username] set qos=[qos_name]```"],["SYNOPSIS","**sacctmgr** [_options_] [_command_]"],["PARAMETERS","**show** / **list**\n> Display information about accounts, clusters, users, associations, QOS, etc.\n\n**add** / **create**\n> Add new accounts, clusters, users, or QOS to the database.\n\n**modify**\n> Modify existing entries.\n\n**delete** / **remove**\n> Remove entries from the database.\n\n**dump**\n> Export cluster association data to a file.\n\n**load**\n> Import cluster association data from a file.\n\n**archive dump** / **archive load**\n> Archive or restore database records to/from flat files.\n\n**ping**\n> Test connection to the slurmdbd daemon.\n\n**reconfigure**\n> Reconfigure the SlurmDBD daemon.\n\n**-i**, **--immediate**\n> Commit changes without asking for confirmation.\n\n**-n**, **--noheader**\n> Omit header from output.\n\n**-p**, **--parsable**\n> Pipe-delimited output with trailing pipe.\n\n**-P**, **--parsable2**\n> Pipe-delimited output without trailing pipe.\n\n**-Q**, **--quiet**\n> Suppress non-error messages.\n\n**-r**, **--readonly**\n> Prevent any modifications (interactive mode).\n\n**-s**, **--associations**\n> Display user associations when showing users.\n\n**-v**, **--verbose**\n> Enable detailed output.\n\n**--json**\n> Output in JSON format.\n\n**--yaml**\n> Output in YAML format."],["DESCRIPTION","**sacctmgr** is the Slurm Account Manager tool for viewing and managing accounts, clusters, and users in the Slurm accounting database. It is essential for configuring multi-user HPC cluster environments.\n\nThe tool manages the hierarchical structure of accounts, which controls resource allocation, job prioritization, and usage limits in Slurm-managed clusters. Account information is recorded based on associations, which combine user, cluster, partition, and account parameters.\n\nEntity types that can be managed include: accounts, users, clusters, associations, QOS (Quality of Service), resources, reservations, coordinators, federations, and TRES (trackable resources)."],["CAVEATS","Requires Slurm database (slurmdbd) to be configured and running. Administrative privileges typically required for modifications. Changes may affect running job scheduling. Coordinators can only modify entities within their delegated accounts."],["INSTALL","```apt: sudo apt install slurm-client```"],["SEE ALSO","[sacct](/man/sacct)(1), [scontrol](/man/scontrol)(1), [squeue](/man/squeue)(1), [sbatch](/man/sbatch)(1), [sinfo](/man/sinfo)(1), [srun](/man/srun)(1)"]],"e":["sacct","scontrol","squeue","sbatch","sinfo","srun"],"x":"sacctmgr show configuration"},"sattach":{"n":"sattach","t":"Attach to a running Slurm job step","s":[["TAGLINE","Attach to a running Slurm job step"],["TLDR","**Attach** to a job step\n\n```sattach jobid.stepid```\n\nAttach with **input filter** for specific task\n\n```sattach --input-filter 0 jobid.stepid```\n\nAttach with **output filter** for specific task\n\n```sattach --output-filter 0 jobid.stepid```\n\nAttach with **error filter** for specific task\n\n```sattach --error-filter 0 jobid.stepid```"],["SYNOPSIS","**sattach** [_options_] _jobid.stepid_"],["DESCRIPTION","**sattach** attaches to a Slurm job step's input/output streams, allowing you to interact with a running job. It redirects the stdout, stderr, and stdin of a Slurm job step to the current terminal."],["PARAMETERS","**jobid.stepid**\n> The job ID and step ID to attach to (format: jobid.stepid)\n\n**--input-filter TASK**\n> Use the current console's input as stdin to the specified task\n\n**--output-filter TASK**\n> Only redirect stdout of the specified task\n\n**--error-filter TASK**\n> Only redirect stderr of the specified task\n\n**-l, --label**\n> Prepend task number to lines of output\n\n**-Q, --quiet**\n> Suppress informational messages\n\n**-v, --verbose**\n> Increase verbosity of messages\n\n**--pty**\n> Execute task zero in pseudo terminal. Not compatible with filter options\n\n**--layout**\n> Print task layout information and exit without attaching\n\n**-V, --version**\n> Display Slurm version number and exit"],["CAVEATS","Only the job owner can attach to a job step. The job must be in a running state. Not all MPI implementations support attachment."],["HISTORY","**sattach** is part of the **Slurm** workload manager, providing job scheduling and resource management for HPC clusters."],["INSTALL","```apt: sudo apt install slurm-client```"],["SEE ALSO","[srun](/man/srun)(1), [sbatch](/man/sbatch)(1), [squeue](/man/squeue)(1), [scontrol](/man/scontrol)(1), [scancel](/man/scancel)(1)"]],"e":["srun","sbatch","squeue","scontrol","scancel"],"x":"sattach jobid.stepid"},"scalafmt":{"n":"scalafmt","t":"Opinionated Scala code formatter","s":[["TAGLINE","Opinionated Scala code formatter"],["TLDR","**Format files**\n\n```scalafmt [file.scala]```\n\n**Format in place**\n\n```scalafmt -i [file.scala]```\n\n**Check formatting**\n\n```scalafmt --check [file.scala]```\n\n**Format directory**\n\n```scalafmt [src/]```\n\n**Use config file**\n\n```scalafmt -c [.scalafmt.conf] [file.scala]```\n\n**Show diff**\n\n```scalafmt --diff [file.scala]```\n\n**Format stdin**\n\n```cat [file.scala] | scalafmt --stdin```"],["SYNOPSIS","**scalafmt** [_-i_] [_--check_] [_-c config_] [_options_] [_files_]"],["PARAMETERS","**-i**, **--inplace**\n> Modify files in place.\n\n**--check**\n> Check only, no changes.\n\n**-c**, **--config** _FILE_\n> Config file.\n\n**--diff**\n> Show changes.\n\n**--stdin**\n> Read from stdin.\n\n**--stdout**\n> Write to stdout.\n\n**--exclude** _PATTERN_\n> Exclude files.\n\n**--help**\n> Show help."],["DESCRIPTION","**scalafmt** is an opinionated code formatter for Scala that enforces consistent style across projects. It reformats source code according to configurable rules covering line length, indentation, alignment, and import ordering, supporting both Scala 2 and Scala 3 syntax through dialect configuration.\n\nThe **--check** mode verifies formatting without modifying files, returning a non-zero exit code when changes would be made, which is useful for CI pipeline enforcement. The **-i** (in-place) mode directly modifies files during development. Configuration is defined in a **.scalafmt.conf** file at the project root using HOCON format."],["CONFIGURATION","**.scalafmt.conf**\n> Project-level configuration file in HOCON format defining formatting rules including maxColumn, dialect, align settings, and rewrite rules."],["CAVEATS","May change code structure subtly. Configuration options are many. Editor integration recommended."],["HISTORY","**scalafmt** was created by **Olafur Pall Geirsson** for Scala formatting. It's widely used in the Scala ecosystem for code style enforcement."],["INSTALL","```nix: nix profile install nixpkgs#scalafmt```"],["SEE ALSO","[scala](/man/scala)(1), [sbt](/man/sbt)(1), [metals](/man/metals)(1)"]],"e":["scala","sbt","metals"],"x":"scalafmt [file.scala]"},"setfattr":{"n":"setfattr","t":"Set extended attributes on files","s":[["TAGLINE","Set extended attributes on files"],["TLDR","Set a **user-defined attribute** on a file\n\n```setfattr -n user.[attribute_name] -v \"[value]\" [path/to/file]```\n\nSet an **attribute name** without a value\n\n```setfattr -n user.[attribute_name] [path/to/file]```\n\n**Remove** a specific attribute from a file\n\n```setfattr -x user.[attribute_name] [path/to/file]```\n\n**Restore** attributes from a dump file\n\n```setfattr --restore=[dumpfile]```\n\nModify attributes on a **symbolic link** itself (not target)\n\n```setfattr -h -n user.[attribute_name] -v \"[value]\" [path/to/symlink]```"],["SYNOPSIS","**setfattr** [**-h**] **-n** _name_ [**-v** _value_] _pathname_...\n\n**setfattr** [**-h**] **-x** _name_ _pathname_...\n\n**setfattr** [**-h**] **--restore**=_file_"],["PARAMETERS","**-n, --name** _name_\n> Specifies the name of the extended attribute to set\n\n**-v, --value** _value_\n> Specifies the new value of the extended attribute. Value can be a string, hex (0x prefix), or base64 (0s prefix)\n\n**-x, --remove** _name_\n> Remove the named extended attribute entirely\n\n**-h, --no-dereference**\n> Do not follow symbolic links; operate on the link itself\n\n**--restore** _file_\n> Restore extended attributes from a file created by getfattr --dump\n\n**--raw**\n> Do not decode the attribute value\n\n**--version**\n> Print version and exit\n\n**--help**\n> Print help message"],["DESCRIPTION","**setfattr** sets or modifies extended attributes on files and directories. Extended attributes are name-value pairs permanently associated with files, similar to environment variables but stored in the filesystem. Attribute names must be in the format **namespace.name** where namespace is typically **user**, **system**, **security**, or **trusted**.\n\nThe **user** namespace is available to regular users for arbitrary data storage. System namespaces require appropriate capabilities or root privileges. Values can be specified as quoted strings with escape sequences, hexadecimal with **0x** prefix, or base64 with **0s** prefix."],["CAVEATS","Not all filesystems support extended attributes; common supported filesystems include ext2/3/4, XFS, Btrfs, and JFS. The **user** namespace may be disabled on some mount configurations. Attribute names have a maximum length of 255 bytes. Total attribute storage per file is filesystem-dependent (often 4KB-64KB)."],["HISTORY","Extended attributes support was added to Linux kernel **2.4** and became fully integrated in **2.6**. The **setfattr** command is part of the **attr** package, developed as part of the XFS project at SGI and later adopted for general Linux filesystem use."],["INSTALL","```apt: sudo apt install attr```\n\n```dnf: sudo dnf install attr```\n\n```pacman: sudo pacman -S attr```\n\n```apk: sudo apk add attr```\n\n```zypper: sudo zypper install attr```\n\n```brew: brew install attr```\n\n```nix: nix profile install nixpkgs#attr```"],["SEE ALSO","[getfattr](/man/getfattr)(1), [attr](/man/attr)(1), [setfacl](/man/setfacl)(1), [getfacl](/man/getfacl)(1)"]],"e":["getfattr","attr","setfacl","getfacl"],"x":"setfattr -n user.[attribute_name] -v \"[value]\" [path/to/file]"},"size":{"n":"size","t":"List binary object file section sizes","s":[["TAGLINE","List binary object file section sizes"],["TLDR","Display section sizes of an **executable**\n\n```size [path/to/file]```\n\nDisplay sizes in **octal**\n\n```size -o [path/to/file]```\n\nDisplay sizes in **decimal**\n\n```size -d [path/to/file]```\n\nDisplay sizes in **hexadecimal**\n\n```size -x [path/to/file]```\n\nDisplay **totals** for multiple files\n\n```size -t [path/to/file1] [path/to/file2]```\n\nUse **System V** output format\n\n```size -A [path/to/file]```"],["SYNOPSIS","**size** [**-A**|**-B**|**-G**] [**-d**|**-o**|**-x**] [**-t**] [_objfile_...]"],["PARAMETERS","**-A, --format=sysv**\n> Use System V output format (detailed section listing)\n\n**-B, --format=berkeley**\n> Use Berkeley output format (default: text, data, bss, total)\n\n**-G, --format=gnu**\n> Use GNU output format\n\n**-d, --radix=10**\n> Display sizes in decimal\n\n**-o, --radix=8**\n> Display sizes in octal\n\n**-x, --radix=16**\n> Display sizes in hexadecimal\n\n**-t, --totals**\n> Show aggregate totals for all listed object files\n\n**--common**\n> Include common symbols in the bss size\n\n**--target** _bfdname_\n> Specify object code format for the file\n\n**-V, --version**\n> Display version information\n\n**-h, --help**\n> Display help information"],["DESCRIPTION","**size** lists the section sizes and total size of binary object files. For each object file, it displays the sizes of the text (code), data, and bss (uninitialized data) sections, along with the total size in bytes and hexadecimal.\n\nThe default Berkeley format shows a concise one-line output per file. System V format provides a detailed listing of all sections. If no files are specified, **a.out** is examined by default."],["CAVEATS","Only works on object files and executables in supported formats (ELF, COFF, etc.). Static libraries show sizes for each object module within. Results may vary based on compilation options and linking."],["HISTORY","**size** is part of GNU **binutils**, the collection of binary tools maintained by the Free Software Foundation. The command originated in early Unix systems at Bell Labs in the **1970s** and has been a standard development tool ever since. GNU binutils provides compatible implementations for various platforms."],["INSTALL","```apt: sudo apt install binutils```\n\n```dnf: sudo dnf install binutils```\n\n```pacman: sudo pacman -S binutils```\n\n```apk: sudo apk add binutils```\n\n```zypper: sudo zypper install binutils```\n\n```brew: brew install binutils```\n\n```nix: nix profile install nixpkgs#binutils```"],["SEE ALSO","[objdump](/man/objdump)(1), [readelf](/man/readelf)(1), [nm](/man/nm)(1), [strip](/man/strip)(1)"]],"e":["objdump","readelf","nm","strip"],"x":"size [path/to/file]"},"sntop":{"n":"sntop","t":"Simple network top for monitoring host connectivity","s":[["TAGLINE","Simple network top for monitoring host connectivity"],["TLDR","**Monitor hosts defined in the default config**\n\n```sntop```\n\n**Poll hosts from a custom config every 10 seconds**\n\n```sntop -r 10 -f [/path/to/hosts.conf]```\n\n**Generate an HTML status page**\n\n```sntop -w -e [/var/www/status.html]```"],["SYNOPSIS","**sntop** [**-d**] [**-o**] [**-c**] [**-r** _time_] [**-f** _file_] [**-a** _file_] [**-l** _file_]"],["PARAMETERS","**-r** _TIME_\n> Set the polling interval in seconds.\n\n**-f** _FILE_\n> Use a custom hosts configuration file.\n\n**-w**\n> Enable HTML output mode.\n\n**-d**\n> Run in daemon mode."],["DESCRIPTION","**sntop** is an ncurses-based console utility that polls a list of hosts at regular intervals using fping or ping and displays their online/offline status in a top-like table. It supports HTML generation, daemon mode, alarm script execution on host status changes, and color output."],["HISTORY","**sntop** was created by **Robert M. Love** and **Chris M. Rivera** and is written in **C**."],["INSTALL","```apt: sudo apt install sntop```\n\n```brew: brew install sntop```"],["SEE ALSO","[fping](/man/fping)(1), [ping](/man/ping)(1), [trippy](/man/trippy)(1)"]],"e":["fping","ping","trippy"],"x":"sntop"},"stack":{"n":"stack","t":"Haskell build tool and project manager","s":[["TAGLINE","Haskell build tool and project manager"],["TLDR","**Create new project**\n\n```stack new [project_name]```\n\n**Build project**\n\n```stack build```\n\n**Run project executable**\n\n```stack run```\n\n**Run tests**\n\n```stack test```\n\n**Start REPL with project**\n\n```stack ghci```\n\n**Install package globally**\n\n```stack install [package_name]```\n\n**Execute Haskell file as script**\n\n```stack script [script.hs]```\n\n**Update package index**\n\n```stack update```\n\n**Show project paths**\n\n```stack path```"],["SYNOPSIS","**stack** _command_ [_--snapshot version_] [_options_] [_arguments_]"],["PARAMETERS","**new** _NAME_ [_TEMPLATE_]\n> Create new project.\n\n**init**\n> Initialize stack in existing project.\n\n**build**\n> Build project.\n\n**run**\n> Build and run executable.\n\n**test**\n> Build and run tests.\n\n**bench**\n> Build and run benchmarks.\n\n**ghci**\n> Start REPL with project loaded.\n\n**install** _PACKAGES_\n> Build and copy executables to bin.\n\n**exec** _COMMAND_\n> Run command in build environment.\n\n**script** _FILE_\n> Run a Haskell script.\n\n**setup**\n> Download and install GHC for the project.\n\n**path**\n> Display paths used by stack.\n\n**ls**\n> List information (snapshots, dependencies, etc.).\n\n**clean**\n> Clean build artifacts.\n\n**purge**\n> Delete the stack working directories (clean --full).\n\n**upgrade**\n> Upgrade stack itself.\n\n**--snapshot** _SNAPSHOT_, **--resolver** _RESOLVER_\n> Stackage snapshot (e.g., lts-22.0, nightly-2025-01-01).\n\n**--system-ghc**\n> Use system GHC instead of stack-managed.\n\n**--no-install-ghc**\n> Don't automatically install GHC.\n\n**--fast**\n> Disable optimizations for faster build.\n\n**--test**\n> Enable test building.\n\n**--bench**\n> Enable benchmark building.\n\n**--file-watch**\n> Rebuild on file changes.\n\n**--ghc-options** _OPTS_\n> Additional GHC options.\n\n**--jobs** _N_, **-j** _N_\n> Number of concurrent jobs.\n\n**--stack-yaml** _FILE_\n> Override project stack.yaml file."],["DESCRIPTION","**stack** is a build tool and project manager for Haskell. It provides reproducible builds by pinning GHC versions and package sets via Stackage snapshots.\n\nStackage snapshots (LTS or Nightly) define curated package sets known to build together. This avoids \"dependency hell\" by ensuring compatible versions. The resolver in stack.yaml specifies which snapshot to use.\n\nStack manages GHC installations automatically. Different projects can use different GHC versions without system-wide conflicts. The correct version downloads and builds on first use.\n\nProject structure includes stack.yaml (configuration), package.yaml (package definition), and source directories. Templates provide starting points for libraries, executables, or specific frameworks.\n\nThe REPL (ghci) loads project code for interactive development. File watching (--file-watch) rebuilds on changes, enabling rapid iteration.\n\nIntegration with Cabal packages works through package.yaml or .cabal files. Stack can use Hackage packages not in Stackage via extra-deps."],["CAVEATS","Initial builds download GHC and many dependencies. Different from cabal workflow - teams should standardize. Stackage may lag behind Hackage for new releases. Disk space usage can be significant. Some advanced Cabal features require workarounds."],["HISTORY","**stack** was created by **FP Complete** in **2015** to solve Haskell's dependency management challenges. It built on work from Stackage (stable package sets). The tool became widely adopted for its reproducibility guarantees and simplified workflow. Development continues with community involvement."],["INSTALL","```apt: sudo apt install haskell-stack```\n\n```dnf: sudo dnf install stack```\n\n```pacman: sudo pacman -S stack```\n\n```apk: sudo apk add stack```\n\n```brew: brew install haskell-stack```\n\n```nix: nix profile install nixpkgs#stack```"],["SEE ALSO","[ghc](/man/ghc)(1), [ghci](/man/ghci)(1), [cabal](/man/cabal)(1), [runghc](/man/runghc)(1)"]],"e":["ghc","ghci","cabal","runghc"],"x":"stack new [project_name]"},"steamos-update":{"n":"steamos-update","t":"Update SteamOS system image","s":[["TAGLINE","Update SteamOS system image"],["TLDR","**Update** SteamOS to the latest version\n\n```sudo steamos-update```\n\n**Check** if updates are available without installing\n\n```steamos-update check```\n\n**Enable read-write** mode first (if modifying root filesystem before updates)\n\n```sudo steamos-readonly disable```"],["SYNOPSIS","**steamos-update** [_COMMAND_]"],["COMMANDS","(no argument)\n> Download and install available updates\n\n**check**\n> Check if updates are available without installing"],["DESCRIPTION","**steamos-update** updates SteamOS to the latest available version on the selected branch. Updates are downloaded and installed to the inactive A/B partition, then the system reboots to the updated partition.\n\nThe A/B partition scheme ensures a reliable fallback if an update fails - the previous working partition remains available for recovery.\n\n**steamos-update** returns exit code **0** when an update was applied or is available (depending on subcommand), and **7** when the system is already up to date (useful for scripting)."],["CAVEATS","Requires network connectivity and root privileges to apply updates. Updates can be large. The system reboots into the updated partition after installation. Update availability depends on the selected branch (stable, beta, or main), chosen with **steamos-select-branch**. Running on non-SteamOS systems is not supported."],["HISTORY","**steamos-update** is part of the **SteamOS** utilities developed by **Valve**. SteamOS 3.0 uses an image-based update system similar to Chrome OS and Android, ensuring atomic updates and easy rollback."],["SEE ALSO","[steamos-select-branch](/man/steamos-select-branch)(1), [steamos-finalize-install](/man/steamos-finalize-install)(1), [steamos-readonly](/man/steamos-readonly)(1)"]],"e":["steamos-select-branch","steamos-finalize-install","steamos-readonly"],"x":"sudo steamos-update"},"taskset":{"n":"taskset","t":"Set process CPU affinity","s":[["TAGLINE","Set process CPU affinity"],["TLDR","**Get** a running process' CPU affinity by PID\n\n```taskset -p -c [pid]```\n\n**Set** a running process' CPU affinity by PID\n\n```taskset -p -c [cpu_id] [pid]```\n\n**Start** a new process with affinity for a single CPU\n\n```taskset -c [cpu_id] [command]```\n\n**Start** a new process with affinity for multiple non-sequential CPUs\n\n```taskset -c [cpu_id_1,cpu_id_2,cpu_id_3] [command]```\n\n**Start** a new process with affinity for CPUs 1 through 4\n\n```taskset -c [1-4] [command]```\n\n**Set affinity using a hex bitmask** (CPUs 0 and 1)\n\n```taskset -p [0x3] [pid]```\n\n**Set affinity for all threads** of a process\n\n```taskset -a -p -c [0-3] [pid]```"],["SYNOPSIS","**taskset** [_options_] [_mask_|_list_] [_pid_|_command_]"],["PARAMETERS","**-p, --pid**\n> Operate on an existing PID\n\n**-c, --cpu-list**\n> Specify CPUs as a list instead of a bitmask\n\n**-a, --all-tasks**\n> Set/get affinity of all tasks (threads)\n\n**-h, --help**\n> Display help information\n\n**-V, --version**\n> Display version information"],["DESCRIPTION","**taskset** retrieves or sets a process's CPU affinity, which controls which CPUs the process can run on. CPU affinity can be specified as a bitmask or a comma-separated list of CPU IDs.\n\nThis is useful for performance tuning, isolating processes to specific cores, or testing how software behaves on limited CPU resources."],["CAVEATS","CPU IDs start at 0. Setting affinity may not improve performance and can hurt it if done incorrectly. The kernel may still migrate processes for load balancing unless CPU isolation is configured. The CPU list supports stride syntax (e.g. 0-10:2 means CPUs 0,2,4,6,8,10). Part of the util-linux package."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[chrt](/man/chrt)(1), [nice](/man/nice)(1), [renice](/man/renice)(1), [numactl](/man/numactl)(8)"]],"e":["chrt","nice","renice","numactl"],"x":"taskset -p -c [pid]"},"tts":{"n":"tts","t":"Neural text-to-speech synthesis","s":[["TAGLINE","Neural text-to-speech synthesis"],["TLDR","**Synthesize speech**\n\n```tts --text \"[Hello world]\"```\n\n**Save to file**\n\n```tts --text \"[Hello]\" --out_path [output.wav]```\n\n**List models**\n\n```tts --list_models```\n\n**Use specific model**\n\n```tts --text \"[text]\" --model_name [tts_models/en/ljspeech/tacotron2-DDC]```\n\n**List speakers**\n\n```tts --model_name [model] --list_speakers```"],["SYNOPSIS","**tts** [_--text text_] [_--out_path file_] [_--model_name model_] [_options_]"],["PARAMETERS","**--text** _TEXT_\n> Input text.\n\n**--out_path** _FILE_\n> Output audio file.\n\n**--model_name** _MODEL_\n> TTS model.\n\n**--list_models**\n> Show models.\n\n**--speaker_idx** _ID_\n> Speaker selection."],["DESCRIPTION","**tts** is the command-line interface for Coqui TTS, an open-source deep learning text-to-speech toolkit. It converts text input into natural-sounding speech audio using neural network models, supporting multiple languages and voice styles.\n\nThe tool ships with a variety of pre-trained models that can be listed and selected by name. Multi-speaker models allow choosing between different voice characteristics via speaker indices. Audio output can be saved to WAV files or played directly.\n\nModels are downloaded automatically on first use and cached locally. While CPU inference is supported, a GPU is recommended for faster synthesis, especially with larger models."],["CAVEATS","Python required. Large model downloads. GPU recommended."],["HISTORY","**TTS** (Coqui TTS) is an open-source deep learning text-to-speech toolkit with multiple models."],["INSTALL","```nix: nix profile install nixpkgs#tts```"],["SEE ALSO","[espeak](/man/espeak)(1), [festival](/man/festival)(1), [piper](/man/piper)(1)"]],"e":["espeak","festival","piper"],"x":"tts --text \"[Hello world]\""},"utop":{"n":"utop","t":"Enhanced interactive OCaml toplevel REPL","s":[["TAGLINE","Enhanced interactive OCaml toplevel REPL"],["TLDR","**Start utop REPL**\n\n```utop```\n\n**Load a file into utop**\n\n```utop -init [file.ml]```\n\n**Start with specific library**\n\n```utop -require [lwt]```\n\n**Evaluate expression and exit**\n\n```utop -e \"[print_endline \\\"Hello\\\"]\"```\n\n**Start without initialization file**\n\n```utop -no-init```"],["SYNOPSIS","**utop** [_options_] [_file_]"],["PARAMETERS","**-init** _file_\n> Load specified file on startup.\n\n**-require** _package_\n> Require specified findlib package.\n\n**-e** _expression_\n> Evaluate expression and exit.\n\n**-no-init**\n> Skip loading initialization files.\n\n**-emacs**\n> Run in Emacs mode.\n\n**-stdin**\n> Read input from standard input.\n\n**-help**\n> Display help."],["COMMANDS","**#help**: Show available commands.\n\n**#quit** or Ctrl-D: Exit utop.\n\n**#use** _file_: Load file into the session.\n\n**#require** _package_: Load findlib package.\n\n**#list**: List loaded modules.\n\n**#utop_bindings**: Show key bindings."],["DESCRIPTION","**utop** is an enhanced toplevel (REPL) for OCaml with features including real-time context-sensitive completion, syntax highlighting, history, and support for multiple editing modes.\n\nThe completion bar displays possible completions as you type and updates dynamically. Navigate completions with Alt-Left/Right and select with Alt-Down. The interface supports multiline input without requiring explicit line continuation.\n\nutop integrates with Emacs using the Tuareg, caml, and ReasonML modes. From Emacs, use M-x utop to start a session with additional key bindings for evaluating code from buffers."],["CONFIGURATION","**~/.config/utop/init.ml**: Initialization script.\n\n**~/.utoprc**: Configuration file for colors and settings.\n\n**~/.ocamlinit**: Alternative initialization (also used by standard ocaml)."],["CAVEATS","Requires OCaml installation. Some features need findlib. Emacs integration requires mode setup. Heavy packages may slow startup."],["HISTORY","**utop** was created as a modern replacement for the standard OCaml toplevel, providing a significantly improved user experience with completion, history, and editor integration. It's maintained by the OCaml community and has become the recommended REPL for OCaml development."],["INSTALL","```apt: sudo apt install utop```\n\n```dnf: sudo dnf install utop```\n\n```apk: sudo apk add utop```"],["SEE ALSO","[ocaml](/man/ocaml)(1), [ocamlfind](/man/ocamlfind)(1), [opam](/man/opam)(1)"]],"e":["ocaml","ocamlfind","opam"],"x":"utop"},"yad":{"n":"yad","t":"GTK dialog tool for shell scripts","s":[["TAGLINE","GTK dialog tool for shell scripts"],["TLDR","**Display an info dialog**\n\n```yad --info --text=\"[Message text]\"```\n\n**Show a file selection dialog**\n\n```yad --file --title=\"[Select a file]\"```\n\n**Display a text entry dialog**\n\n```yad --entry --text=\"[Enter your name:]\"```\n\n**Create a form** with multiple fields\n\n```yad --form --field=\"[Name]\" --field=\"[Email]\" --field=\"[Age]:NUM\"```\n\n**Show a list selection dialog**\n\n```yad --list --column=\"[ID]\" --column=\"[Name]\" [1] \"[Alice]\" [2] \"[Bob]\"```\n\n**Display a progress bar** from stdin\n\n```[command] | yad --progress --text=\"[Processing...]\" --auto-close```\n\n**Create a calendar dialog**\n\n```yad --calendar --title=\"[Select date]\"```\n\n**Show a notification icon** in system tray\n\n```yad --notification --image=\"[icon-name]\" --text=\"[Tooltip text]\"```"],["SYNOPSIS","**yad** [_--info_|_--warning_|_--error_|_--question_] [_--entry_] [_--file_] [_--form_] [_--list_] [_--progress_] [_--calendar_] [_--notification_] [_options_]"],["PARAMETERS","**--info**\n> Display an information dialog.\n\n**--warning**\n> Display a warning dialog.\n\n**--error**\n> Display an error dialog.\n\n**--question**\n> Display a question dialog with Yes/No buttons.\n\n**--entry**\n> Display a text entry dialog.\n\n**--file**\n> Display a file selection dialog.\n\n**--directory**\n> Display a directory selection dialog.\n\n**--form**\n> Display a form with multiple input fields.\n\n**--list**\n> Display a list/table selection dialog.\n\n**--progress**\n> Display a progress bar dialog.\n\n**--calendar**\n> Display a calendar date selection dialog.\n\n**--color**\n> Display a color selection dialog.\n\n**--font**\n> Display a font selection dialog.\n\n**--notification**\n> Display a system tray notification icon.\n\n**--text** _string_\n> Set the dialog text.\n\n**--title** _string_\n> Set the dialog window title.\n\n**--width** _n_\n> Set the dialog width.\n\n**--height** _n_\n> Set the dialog height.\n\n**--field** _label[:type]_\n> Add a field to a form (types: TEXT, NUM, CHK, CB, etc.).\n\n**--column** _name_\n> Add a column to list dialog.\n\n**--auto-close**\n> Close dialog when progress reaches 100%.\n\n**--button** _label:action_\n> Add custom button.\n\n**--separator** _char_\n> Set output field separator."],["DESCRIPTION","**YAD** (Yet Another Dialog) is a powerful GTK+ dialog tool for creating graphical dialogs from shell scripts. It is a fork and enhancement of Zenity with significantly more features and customization options.\n\nYAD supports numerous dialog types: message boxes, file/directory selection, forms, lists, progress bars, calendars, color pickers, font selectors, and system tray notifications. Forms can include various field types including text, numbers, checkboxes, combo boxes, file choosers, and more.\n\nDialog output is written to stdout, making it easy to capture user input in shell scripts. The exit code indicates which button was pressed. Custom buttons can be defined with specific return codes or actions.\n\nFor complex interfaces, YAD supports multi-pane layouts with tabs and notebooks. The **--plug** and **--tabnum** options allow embedding multiple dialogs into a single window. HTML content can be displayed using the **--html** option with WebKit support."],["CAVEATS","Requires GTK+ 3 libraries. Some features like HTML rendering require WebKit. Dialog appearance varies with GTK theme. Complex forms may require careful escaping of special characters. System tray support depends on desktop environment compatibility."],["HISTORY","**YAD** was created by Victor Ananjevsky as an enhanced fork of Zenity. Development began around **2008** to address limitations in Zenity and add features needed for more sophisticated shell script GUIs. The project has been actively maintained with regular releases adding new dialog types and options."],["INSTALL","```apt: sudo apt install yad```\n\n```dnf: sudo dnf install yad```\n\n```pacman: sudo pacman -S yad```\n\n```apk: sudo apk add yad```\n\n```nix: nix profile install nixpkgs#yad```"],["SEE ALSO","[zenity](/man/zenity)(1), [dialog](/man/dialog)(1), [kdialog](/man/kdialog)(1), [whiptail](/man/whiptail)(1)"]],"e":["zenity","dialog","kdialog","whiptail"],"x":"yad --info --text=\"[Message text]\""},"yq":{"n":"yq","t":"Lightweight YAML and JSON processor","s":[["TAGLINE","Lightweight YAML and JSON processor"],["TLDR","**Read a value** from a YAML file\n\n```yq '.[key]' [file.yaml]```\n\n**Read a nested value**\n\n```yq '.[parent].[child]' [file.yaml]```\n\n**Convert YAML to JSON**\n\n```yq -o=json [file.yaml]```\n\n**Convert JSON to YAML**\n\n```yq -p=json [file.json]```\n\n**Edit a file in place**\n\n```yq -i '.[key] = \"value\"' [file.yaml]```\n\n**Merge multiple YAML files**\n\n```yq eval-all 'select(fileIndex == 0) * select(fileIndex == 1)' [file1.yaml] [file2.yaml]```\n\n**Select items from an array**\n\n```yq '.items[] | select(.name == \"foo\")' [file.yaml]```\n\n**Count array elements**\n\n```yq '.items | length' [file.yaml]```"],["SYNOPSIS","**yq** [_options_] _expression_ [_file ..._]"],["PARAMETERS","**-i**, **--inplace**\n> Edit files in place.\n\n**-p**, **--input-format** _format_\n> Input format: yaml, json, xml, props, csv, tsv, toml.\n\n**-o**, **--output-format** _format_\n> Output format: yaml, json, xml, props, csv, tsv.\n\n**-I**, **--indent** _n_\n> Set indentation level (default 2).\n\n**-C**, **--colors**\n> Force colored output.\n\n**-M**, **--no-colors**\n> Disable colored output.\n\n**-N**, **--no-doc**\n> Don't print document separators.\n\n**-e**, **--exit-status**\n> Set exit status based on expression result.\n\n**--from-file** _file_\n> Load expression from a file.\n\n**-n**, **--null-input**\n> Don't read input; useful for creating new documents.\n\n**-s**, **--split-exp** _expression_\n> Split output into multiple files.\n\n**-v**, **--verbose**\n> Enable verbose output.\n\n**-h**, **--help**\n> Display help message.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**yq** is a lightweight command-line YAML, JSON, XML, CSV, TOML, and properties processor. It uses jq-like syntax for querying and manipulating structured data, making it easy to extract values, modify documents, and convert between formats.\n\nThe tool supports reading from files or stdin, and can output to stdout or edit files in place. Expressions follow a path-based syntax similar to jq, allowing complex queries and transformations.\n\nTwo implementations exist: **mikefarah/yq** (Go-based, standalone binary) and **kislyuk/yq** (Python wrapper around jq). The Go version is more commonly installed via package managers."],["CAVEATS","The two yq implementations have different syntax and capabilities. The Go version (mikefarah/yq) uses its own expression language, while the Python version (kislyuk/yq) uses jq syntax directly. Check which version is installed with **yq --version**."],["HISTORY","The Python yq wrapper was created by **Andrey Kislyuk** and released in **2017** as a thin jq wrapper for YAML. The Go-based yq was created by **Mike Farah** around the same time as a standalone tool. The Go version has become more popular due to being a single binary with no dependencies and supporting multiple formats beyond YAML."],["INSTALL","```dnf: sudo dnf install yq```\n\n```pacman: sudo pacman -S yq```\n\n```apk: sudo apk add yq-go```\n\n```zypper: sudo zypper install yq```\n\n```brew: brew install yq```\n\n```nix: nix profile install nixpkgs#yq-go```"],["SEE ALSO","[jq](/man/jq)(1), [xmlstarlet](/man/xmlstarlet)(1), [dasel](/man/dasel)(1), [fx](/man/fx)(1)"]],"e":["jq","xmlstarlet","dasel","fx"],"x":"yq '.[key]' [file.yaml]"},"zapier-convert":{"n":"zapier-convert","t":"Convert legacy Zapier integrations to CLI","s":[["TAGLINE","Convert legacy Zapier integrations to CLI"],["TLDR","**Convert legacy integration**\n\n```zapier convert [app_id] [path]```\n\n**Convert with version**\n\n```zapier convert [app_id] [path] --version [version]```"],["SYNOPSIS","**zapier** **convert** _app_id_ _path_ [_options_]"],["PARAMETERS","**--version** _version_\n> Specific version to convert."],["DESCRIPTION","**zapier convert** migrates legacy Web Builder integrations to CLI format. Converts visual integrations to code-based projects for advanced development."],["SEE ALSO","[zapier](/man/zapier)(1), [zapier-init](/man/zapier-init)(1)"]],"e":["zapier","zapier-init"],"x":"zapier convert [app_id] [path]"}});
