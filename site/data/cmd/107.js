window.LCL_BUCKET(107,{"7zr":{"n":"7zr","t":"minimal 7z-only file archiver","s":[["TAGLINE","minimal 7z-only file archiver"],["TLDR","**Create 7z archive**\n\n```7zr a [archive.7z] [files...]```\n\n**Extract 7z archive**\n\n```7zr x [archive.7z]```\n\n**List archive contents**\n\n```7zr l [archive.7z]```\n\n**Test archive integrity**\n\n```7zr t [archive.7z]```\n\n**Create with maximum compression**\n\n```7zr a -mx=9 [archive.7z] [files]```"],["SYNOPSIS","**7zr** _command_ [_switches_] _archive_ [_files_...]"],["COMMANDS","**a**\n> Add files to archive\n\n**x**\n> Extract with full paths\n\n**e**\n> Extract (flat, no paths)\n\n**l**\n> List archive contents\n\n**t**\n> Test archive integrity\n\n**d**\n> Delete from archive\n\n**u**\n> Update archive"],["PARAMETERS","**-o**_dir_\n> Output directory\n\n**-mx=**_n_\n> Compression level (0-9)\n\n**-r**\n> Recurse subdirectories\n\n**-y**\n> Assume yes to queries"],["DESCRIPTION","**7zr** is a minimal, reduced version of the 7-Zip command-line tool that only supports the 7z format. It is the lightest-weight option in the 7-Zip family.\n\n7zr is ideal when only the 7z format is needed, when minimal dependencies are desired, or when embedding in scripts or systems with size constraints.\n\nThe command syntax matches 7z and 7za. For 7z-only operations, 7zr provides the smallest footprint."],["CAVEATS","7zr only handles the 7z format. It cannot process ZIP, TAR, GZIP, or any other formats.\n\nNo encryption support in some builds.\n\nThe \"r\" stands for \"reduced\" - minimal format support, not recursive."],["INSTALL","```apt: sudo apt install 7zip```\n\n```dnf: sudo dnf install 7zip```\n\n```pacman: sudo pacman -S 7zip```\n\n```zypper: sudo zypper install 7zip```\n\n```brew: brew install 7zip```"],["SEE ALSO","[7z](/man/7z)(1), [7za](/man/7za)(1), [tar](/man/tar)(1)"],["RESOURCES","```[Source code](https://github.com/ip7z/7zip)```\n\n```[Homepage](https://www.7-zip.org/)```"]],"e":["7z","7za","tar"],"x":"7zr a [archive.7z] [files...]"},"boxxy":{"n":"boxxy","t":"Redirect application file paths using Linux namespaces.","s":[["TAGLINE","Redirect application file paths using Linux namespaces."],["TLDR","**Run application with file redirection rules**\n\n```boxxy [application]```\n\n**Use custom configuration file**\n\n```boxxy --config [boxxy.yaml] [application]```\n\n**Add rule at command line**\n\n```boxxy --rule \"[rule_name]:~/.config/app:~/.local/share/app\" [application]```\n\n**Scan homedir for rule suggestions**\n\n```boxxy --scan```\n\n**Run without loading config files**\n\n```boxxy --no-config [application]```"],["SYNOPSIS","**boxxy** [_options_] [_application_] [_arguments_]"],["DESCRIPTION","**boxxy** puts misbehaving Linux applications in a box, forcing them to put their files and directories in the right place without symlinks. It uses Linux namespaces to redirect file operations transparently.\n\nThe tool is designed to manage applications that create dotfiles or directories in unwanted locations, redirecting them to XDG-compliant paths or other preferred locations."],["PARAMETERS","**--config** _file_\n> Use specified configuration file\n\n**--rule** _rule_\n> Add rule at command line (format: name:target:rewrite)\n\n**--no-config**\n> Disable loading configuration files\n\n**--scan**\n> Scan home directory and suggest rules\n\n**-v, --verbose**\n> Enable verbose output\n\n**-h, --help**\n> Display help and exit"],["CONFIGURATION","Rules in boxxy.yaml include:\n\n**name**\n> User-friendly rule identifier\n\n**target**\n> File or directory to shadow\n\n**rewrite**\n> Replacement file or directory\n\n**mode**\n> Either \"directory\" or \"file\" (default: directory)\n\n**commands**\n> Optional list of commands the rule applies to"],["CAVEATS","Requires Linux namespaces support (Linux kernel). Rules must specify mode when the target is a file, especially if the target doesn't exist yet. Project-local boxxy.yaml files and .env files can be loaded automatically."],["INSTALL","```pacman: sudo pacman -S boxxy```\n\n```nix: nix profile install nixpkgs#boxxy```"],["SEE ALSO","[firejail](/man/firejail)(1), [bwrap](/man/bwrap)(1), [unshare](/man/unshare)(1)"]],"e":["firejail","bwrap","unshare"],"x":"boxxy [application]"},"brew-list":{"n":"brew-list","t":"List installed Homebrew packages","s":[["TAGLINE","List installed Homebrew packages"],["TLDR","**List all installed packages**\n\n```brew list```\n\n**List installed formulae only**\n\n```brew list --formula```\n\n**List installed casks only**\n\n```brew list --cask```\n\n**List with versions**\n\n```brew list --versions```\n\n**Check if specific package is installed**\n\n```brew list [package]```\n\n**List with verbose file information**\n\n```brew list --verbose [package]```\n\n**List top-level packages (not dependencies)**\n\n```brew leaves```"],["SYNOPSIS","**brew list** [_options_] [_formula|cask_]"],["DESCRIPTION","**brew list** displays all installed formulae and casks. Without arguments, it lists all packages. With a package name, it shows the installed files for that package.\n\nAliases: **brew ls**"],["PARAMETERS","**--formula**\n> List installed formulae only\n\n**--cask**\n> List installed casks only\n\n**--versions**\n> Show version numbers alongside packages\n\n**--verbose**\n> Display installed files for each package\n\n**--pinned**\n> List pinned formulae only\n\n**--multiple**\n> Only show formulae with multiple versions installed\n\n**-1**\n> Force output to one entry per line\n\n**-l**\n> List formulae and casks in long format\n\n**-r**\n> Reverse the sort order, listing the oldest entries first\n\n**-t**\n> Sort by time modified, listing most recently modified first"],["RELATED COMMANDS","**brew leaves**\n> Show top-level packages that are not dependencies\n\n**brew deps --tree --installed**\n> Show installed packages with dependency tree"],["CAVEATS","Output format varies based on terminal capabilities and flags. Use --versions to distinguish between multiple installed versions of the same package."],["SEE ALSO","[brew](/man/brew)(1), [brew-info](/man/brew-info)(1), [brew-outdated](/man/brew-outdated)(1), [brew-leaves](/man/brew-leaves)(1)"],["RESOURCES","```[Source code](https://github.com/Homebrew/brew)```\n\n```[Homepage](https://brew.sh)```\n\n```[Documentation](https://docs.brew.sh)```"]],"e":["brew","brew-info","brew-outdated","brew-leaves"],"x":"brew list"},"cbt":{"n":"cbt","t":"command-line interface for Google Cloud Bigtable","s":[["TAGLINE","command-line interface for Google Cloud Bigtable"],["TLDR","**List all tables in an instance**\n\n```cbt -project [project_id] -instance [instance_id] ls```\n\n**Read rows from a table**\n\n```cbt -project [project_id] -instance [instance_id] read [table_name]```\n\n**Count rows in a table**\n\n```cbt -project [project_id] -instance [instance_id] count [table_name]```\n\n**Create a new table**\n\n```cbt -project [project_id] -instance [instance_id] createtable [table_name]```\n\n**Create a column family**\n\n```cbt -project [project_id] -instance [instance_id] createfamily [table_name] [family_name]```\n\n**Write a value to a cell**\n\n```cbt -project [project_id] -instance [instance_id] set [table_name] [row_key] [family]:[column]=[value]```"],["SYNOPSIS","**cbt** [**-project** _project_] [**-instance** _instance_] [**-creds** _file_] [**-timeout** _duration_] _command_ [_arguments..._]"],["DESCRIPTION","**cbt** is a command-line interface for Google Cloud Bigtable, a fully managed NoSQL database service. It allows performing administrative and data operations on Bigtable instances, tables, column families, and rows.\n\nThe tool can be configured via a **~/.cbtrc** file containing project and instance defaults, or by passing flags directly."],["PARAMETERS","**-project** _project_id_\n> Google Cloud project ID. Uses gcloud default if unset\n\n**-instance** _instance_id_\n> Cloud Bigtable instance name\n\n**-creds** _file_\n> Path to credentials JSON file\n\n**-timeout** _duration_\n> Operation timeout (e.g., 10s, 5m)\n\n**Common commands:**\n\n**ls** - List tables\n**read** _table_ - Read rows from table\n**count** _table_ - Count rows in table\n**createtable** _table_ - Create a table\n**deletetable** _table_ - Delete a table\n**createfamily** _table_ _family_ - Create column family\n**set** _table_ _row_ _family:col=val_ - Write cell value\n**deleterow** _table_ _row_ - Delete a row\n**help** - Print help text"],["CONFIGURATION","**~/.cbtrc**\n> Default project and instance settings, avoiding the need to pass -project and -instance flags on every invocation."],["CAVEATS","The cbt CLI does not implement smart retries or advanced error handling. For production environments, use a Bigtable client library or the Google Cloud console instead.\n\nBash shell syntax rules apply: values with spaces or operators require quotes, and arbitrary byte values need dollar-sign prefix with single quotes."],["HISTORY","The **cbt** tool was developed by Google as part of the Cloud Bigtable ecosystem, written in Go using the official Go client library for Bigtable. It provides a quick way to interact with Bigtable for development and debugging purposes."],["SEE ALSO","[gcloud](/man/gcloud)(1), [bq](/man/bq)(1)"],["RESOURCES","```[Source code](https://github.com/googleapis/google-cloud-go/tree/main/bigtable/cmd/cbt)```\n\n```[Documentation](https://cloud.google.com/bigtable/docs/cbt-reference)```"]],"e":["gcloud","bq"],"x":"cbt -project [project_id] -instance [instance_id] ls"},"claude-pee":{"n":"claude-pee","t":"Drop-in wrapper around the claude CLI for scripted interactive sessions","s":[["TAGLINE","Drop-in wrapper around the claude CLI for scripted interactive sessions"],["TLDR","**Run an interactive session** through the wrapper\n\n```claude-pee```\n\nSend a **one-shot prompt** and print the reply\n\n```claude-pee -p \"[summarize this file]\"```\n\nRequest **JSON output** for downstream parsing\n\n```claude-pee -p \"[prompt]\" --output-format json```\n\nStream **incremental JSON** events as the model responds\n\n```claude-pee -p \"[prompt]\" --output-format stream-json```\n\n**Forward extra flags** verbatim to the underlying claude CLI\n\n```claude-pee --model [opus] -p \"[prompt]\"```"],["SYNOPSIS","**claude-pee** [_options_] [**-p** _prompt_]"],["DESCRIPTION","**claude-pee** is a Rust front-end that wraps the official **claude** command-line tool. It spawns claude inside a pseudo-terminal (PTY), assigns a fresh **--session-id** (UUIDv4), forwards every other flag unchanged, optionally injects a one-shot prompt with **-p**, and then tails the matching session transcript JSONL file until the assistant's reply is complete.\n\nThe wrapper exists so that programmatic scripts and CI pipelines can drive **claude** without being billed against its programmatic-usage credit pool, while still benefitting from the interactive session semantics. Output is collected and printed to stdout in plain text, JSON, or streaming JSON depending on **--output-format**.\n\nA sentinel file combined with claude's Stop hooks signals when the response has finished so the wrapper can exit cleanly instead of polling indefinitely."],["PARAMETERS","**-p** _prompt_\n> Inject a one-shot prompt and exit when the reply is complete. Without **-p** the wrapper hands the PTY back to the user for an interactive session.\n\n**--output-format** _FORMAT_\n> Select output mode: **text** (default), **json**, or **stream-json**.\n\n**--session-id** _ID_\n> Override the auto-generated UUIDv4 used to correlate the underlying claude transcript.\n\nAll other flags are forwarded verbatim to **claude**, so options like **--model**, **--add-dir**, or **--allowedTools** behave exactly as documented for the real CLI."],["CAVEATS","Requires the **claude** CLI to be installed and on **$PATH**, plus Rust **1.85+** to build from source via **cargo build --release**. Because the wrapper depends on claude's transcript JSONL layout and Stop hook behavior, future changes to the official client may break it. This is a community project, not an Anthropic product."],["HISTORY","**claude-pee** was published in **2026** as a community response to the cost difference between interactive and programmatic Claude Code usage, debuting on Hacker News and growing from a single-file Rust prototype into a small drop-in shim."],["INSTALL","```aur: yay -S claude```"],["SEE ALSO","[claude](/man/claude)(1), [jq](/man/jq)(1), [tmux](/man/tmux)(1)"]],"e":["claude","jq","tmux"],"x":"claude-pee"},"clip-view":{"n":"clip-view","t":"display system clipboard contents","s":[["TAGLINE","display system clipboard contents"],["TLDR","**View clipboard contents**\n\n```clip-view```\n\n**View clipboard in specific format**\n\n```clip-view --format [text]```"],["SYNOPSIS","**clip-view** [_options_]"],["DESCRIPTION","**clip-view** displays the current contents of the system clipboard from the command line. It supports viewing clipboard data in different formats, making it useful for inspecting what is currently stored in the clipboard.\n\nThe tool is particularly handy for scripting workflows that involve the clipboard, or for debugging copy-paste issues where you need to verify the exact content being transferred between applications."],["SEE ALSO","[xclip](/man/xclip)(1), [xsel](/man/xsel)(1)"]],"e":["xclip","xsel"],"x":"clip-view"},"coverage":{"n":"coverage","t":"Python code coverage measurement tool","s":[["TAGLINE","Python code coverage measurement tool"],["TLDR","**Run a Python script with coverage**\n\n```coverage run [script.py]```\n\n**Run pytest with coverage**\n\n```coverage run -m pytest```\n\n**Show coverage report in terminal**\n\n```coverage report```\n\n**Generate HTML coverage report**\n\n```coverage html```\n\n**Fail if total coverage is below 80%** (exit code 2)\n\n```coverage report --fail-under=80```\n\n**Combine coverage from multiple runs**\n\n```coverage combine```\n\n**Erase collected coverage data**\n\n```coverage erase```\n\n**Show coverage for specific file**\n\n```coverage report --include=[path/to/file.py]```\n\n**Run with branch coverage** measurement\n\n```coverage run --branch -m pytest```\n\n**Generate XML report** (Cobertura format for CI)\n\n```coverage xml```"],["SYNOPSIS","**coverage** _command_ [_options_] [_args_]"],["PARAMETERS","**run** [_options_] _program_\n> Run a program and collect coverage data.\n\n**report**\n> Display coverage report in terminal.\n\n**html**\n> Generate HTML report in htmlcov/.\n\n**xml**\n> Generate Cobertura XML report.\n\n**json**\n> Generate JSON report.\n\n**combine**\n> Combine data from multiple coverage files.\n\n**erase**\n> Delete collected coverage data.\n\n**annotate**\n> Annotate source files with coverage markers.\n\n**lcov**\n> Generate LCOV report.\n\n**-m** _module_\n> Run library module as script (like python -m).\n\n**--source** _paths_\n> Limit coverage to specified packages/directories.\n\n**--include** _patterns_\n> Include only files matching patterns.\n\n**--omit** _patterns_\n> Omit files matching patterns.\n\n**--branch**\n> Enable branch coverage measurement.\n\n**--fail-under** _n_\n> Exit with failure if coverage is below n%."],["DESCRIPTION","**Coverage.py** measures code coverage for Python programs, showing which lines and branches are executed during testing. It helps identify untested code paths and ensures comprehensive test suites.\n\nThe tool instruments Python bytecode to track execution. After running tests with **coverage run**, reports show percentage of lines covered per file. HTML reports provide visual highlighting of covered and uncovered lines.\n\nBranch coverage (**--branch**) additionally tracks which conditional branches are taken, catching cases where both sides of an if statement aren't tested."],["CONFIGURATION","**pyproject.toml**\n> Configuration in [tool.coverage.run] and [tool.coverage.report] sections for source paths, branch coverage, and reporting thresholds.\n\n**setup.cfg**\n> Alternative configuration location using [coverage:run] and [coverage:report] sections.\n\n**.coveragerc**\n> Dedicated configuration file for coverage settings, omit patterns, and report options."],["CAVEATS","Coverage measurement adds runtime overhead. Dynamic code generation and exec() may not be tracked correctly. 100% line coverage doesn't guarantee bug-free code. Branch coverage is more thorough but harder to achieve. Multiprocessing requires special configuration."],["HISTORY","Coverage.py was created by **Ned Batchelder** and first released in **2004**. It has become the standard code coverage tool for Python, integrated with test runners like pytest and unittest. The tool is widely used in CI/CD pipelines and is supported by coverage reporting services like Codecov and Coveralls."],["INSTALL","```apk: sudo apk add py3-coverage```"],["SEE ALSO","[pytest](/man/pytest)(1), [python](/man/python)(1), [tox](/man/tox)(1), [codecov](/man/codecov)(1)"]],"e":["pytest","python","tox","codecov"],"x":"coverage run [script.py]"},"devpod":{"n":"devpod","t":"reproducible development environments on any infrastructure","s":[["TAGLINE","reproducible development environments on any infrastructure"],["TLDR","**Create a new workspace**\n\n```devpod up [github.com/user/repo]```\n\n**Create workspace** with specific provider\n\n```devpod up [github.com/user/repo] --provider [docker]```\n\n**List workspaces**\n\n```devpod list```\n\n**Connect to workspace** via SSH\n\n```devpod ssh [workspace_name]```\n\n**Stop a workspace**\n\n```devpod stop [workspace_name]```\n\n**Delete a workspace**\n\n```devpod delete [workspace_name]```\n\n**Add a provider**\n\n```devpod provider add [kubernetes]```"],["SYNOPSIS","**devpod** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Operation: up, list, ssh, stop, delete, provider.\n\n**up** _SOURCE_\n> Create workspace from git repo, folder, or image.\n\n**--provider** _PROVIDER_\n> Specify infrastructure provider.\n\n**--ide** _IDE_\n> IDE to use (vscode, openvscode, cursor, etc.).\n\n**list**\n> List all workspaces.\n\n**ssh** _WORKSPACE_\n> SSH into workspace.\n\n**stop** _WORKSPACE_\n> Stop running workspace.\n\n**delete** _WORKSPACE_\n> Delete workspace.\n\n**--help**\n> Display help information."],["DESCRIPTION","**DevPod** is an open-source tool for creating reproducible development environments based on devcontainer.json specifications. It abstracts infrastructure by supporting multiple providers including Docker, Kubernetes, and cloud VMs.\n\nDevPod workspaces are created from git repositories, local directories, or container images. The tool automatically provisions infrastructure, builds the dev container, and connects your IDE. It supports VS Code, JetBrains IDEs, and SSH access.\n\nThe provider architecture allows running the same development environment locally with Docker or remotely on cloud infrastructure, maintaining consistency regardless of where the environment runs."],["CONFIGURATION","**.devcontainer/devcontainer.json**\n> Dev container configuration defining the development environment.\n\n**~/.devpod/provider/**\n> Provider configurations for different infrastructure backends."],["CAVEATS","Requires configured providers for target infrastructure. Cloud providers incur costs. Large dev containers may have slow initial setup. Some features require specific IDE integrations."],["HISTORY","DevPod was created by **Loft Labs** and released in **2023**. It was designed to provide a vendor-neutral, open-source alternative to cloud development environments, with support for the devcontainer specification."],["INSTALL","```nix: nix profile install nixpkgs#devpod```"],["SEE ALSO","[devcontainer](/man/devcontainer)(1), [docker](/man/docker)(1), [code](/man/code)(1)"],["RESOURCES","```[Source code](https://github.com/loft-sh/devpod)```\n\n```[Homepage](https://devpod.sh)```\n\n```[Documentation](https://devpod.sh/docs)```"]],"e":["devcontainer","docker","code"],"x":"devpod up [github.com/user/repo]"},"dnsmasq":{"n":"dnsmasq","t":"lightweight DNS forwarder and DHCP server","s":[["TAGLINE","lightweight DNS forwarder and DHCP server"],["TLDR","**Start dnsmasq** as DNS forwarder\n\n```dnsmasq```\n\n**Start with specific config file**\n\n```dnsmasq -C [/etc/dnsmasq.conf]```\n\n**Run in foreground** with debug output\n\n```dnsmasq -d```\n\n**Test configuration** syntax\n\n```dnsmasq --test```\n\n**Start with DHCP** for a subnet\n\n```dnsmasq --dhcp-range=[192.168.1.50,192.168.1.150,12h]```\n\n**Specify upstream DNS server**\n\n```dnsmasq --server=[8.8.8.8]```\n\n**Start as authoritative** for a local domain\n\n```dnsmasq --local=/[mydomain.local]/```\n\n**Block ads** using hosts file\n\n```dnsmasq --addn-hosts=[/etc/hosts.ads]```"],["SYNOPSIS","**dnsmasq** [_options_]"],["DESCRIPTION","**dnsmasq** is a lightweight DNS forwarder and DHCP server designed for small networks. It provides DNS caching, DHCP, TFTP, and PXE boot services in a single, efficient package.\n\nAs a DNS server, dnsmasq forwards queries to upstream servers and caches responses, reducing latency and bandwidth. It reads /etc/hosts for local name resolution and supports custom local domains. It can also provide DNS-based ad blocking.\n\nThe DHCP server provides dynamic and static address assignment with support for BOOTP, PXE, and TFTP for network booting. dnsmasq is commonly used in routers, embedded systems, and as a local development DNS/DHCP solution."],["PARAMETERS","**-d**\n> Debug mode, foreground, log to stderr.\n\n**-k**\n> Keep in foreground, don't daemonize.\n\n**-C** _file_\n> Use specified configuration file.\n\n**--test**\n> Check configuration syntax.\n\n**-p** _port_\n> Listen on specified DNS port.\n\n**-a** _address_\n> Listen on specified address.\n\n**--dhcp-range=** _range_\n> Enable DHCP with address range.\n\n**--dhcp-host=** _config_\n> Static DHCP assignment.\n\n**-h**, **--no-hosts**\n> Don't read /etc/hosts.\n\n**-H**, **--addn-hosts=** _file_\n> Additional hosts file.\n\n**--local=** _domain_\n> Answer authoritatively for domain.\n\n**--server=** _address_\n> Upstream DNS server.\n\n**-R**, **--no-resolv**\n> Don't read /etc/resolv.conf for upstream servers.\n\n**-r**, **--resolv-file=** _file_\n> Read upstream nameservers from specified file instead of /etc/resolv.conf.\n\n**--cache-size=** _n_\n> DNS cache size (default 150).\n\n**--address=** _/domain/address_\n> Return a specific address for all hosts in a domain. Useful for blocking or redirecting.\n\n**--log-queries**\n> Log DNS queries."],["CONFIGURATION","**/etc/dnsmasq.conf**\n> Main configuration file. Contains DNS and DHCP settings, upstream servers, domain configurations, and DHCP ranges.\n\n**/etc/hosts**\n> Local hostname to IP mappings. Read by dnsmasq for local DNS resolution unless --no-hosts is specified.\n\n**/etc/dnsmasq.d/**\n> Directory for additional configuration snippets. Files here are automatically included in the main configuration."],["CAVEATS","Only one DNS/DHCP server should run on each interface. Conflicts with systemd-resolved on some systems (disable or configure to coexist). DHCP requires appropriate network permissions. Large networks may need a full-featured DHCP server."],["HISTORY","**dnsmasq** was created by **Simon Kelley** in **2000** as a simple DNS forwarder for dial-up connections. It evolved to include DHCP and became popular in embedded Linux systems, routers (OpenWrt, DD-WRT), and development environments. The project remains actively maintained and is included in most Linux distributions."],["INSTALL","```apt: sudo apt install dnsmasq-base```\n\n```dnf: sudo dnf install dnsmasq```\n\n```pacman: sudo pacman -S dnsmasq```\n\n```apk: sudo apk add dnsmasq```\n\n```zypper: sudo zypper install dnsmasq```\n\n```brew: brew install dnsmasq```\n\n```nix: nix profile install nixpkgs#dnsmasq```"],["SEE ALSO","[named](/man/named)(8), [dhcpd](/man/dhcpd)(8)"]],"e":["named","dhcpd"],"x":"dnsmasq"},"efibootmgr":{"n":"efibootmgr","t":"UEFI boot manager configuration tool","s":[["TAGLINE","UEFI boot manager configuration tool"],["TLDR","**List** all boot options with verbose details\n\n```efibootmgr -v```\n\n**Add** a new UEFI boot entry\n\n```sudo efibootmgr -c -d [/dev/sda] -p [1] -l \"[\\EFI\\boot\\bootx64.efi]\" -L \"[My Entry]\"```\n\n**Change** current boot order\n\n```sudo efibootmgr -o [0002,0008,0001,001A]```\n\n**Set** boot entry for next boot only\n\n```sudo efibootmgr -n [0002]```\n\n**Delete** a boot option\n\n```sudo efibootmgr -b [0008] -B```\n\n**Set** boot manager timeout in seconds\n\n```sudo efibootmgr -t [10]```\n\n**Set** a boot entry as inactive\n\n```sudo efibootmgr -b [0002] -A```"],["SYNOPSIS","**efibootmgr** [_options_]"],["DESCRIPTION","**efibootmgr** manipulates the UEFI Boot Manager by modifying EFI variables stored in NVRAM. It can create, delete, and modify boot entries, change boot order, and set the next boot device.\n\nEssential for managing dual-boot systems and UEFI boot configuration."],["PARAMETERS","**-c, --create**\n> Create a new boot entry.\n\n**-d, --disk** _disk_\n> Disk containing the loader (defaults to /dev/sda).\n\n**-p, --part** _part_\n> Partition number on the disk (defaults to 1).\n\n**-l, --loader** _name_\n> EFI loader filename (defaults to \\EFI\\arch\\grub.efi).\n\n**-L, --label** _label_\n> Boot entry display label (defaults to \"Linux\").\n\n**-o, --bootorder** _order_\n> Set boot order (comma-separated hex boot numbers).\n\n**-b, --bootnum** _num_\n> Boot entry number (hex) to operate on.\n\n**-B, --delete-bootnum**\n> Delete the entry specified by -b.\n\n**-n, --bootnext** _num_\n> Set the boot entry to be used on next boot only. Supersedes BootOrder for one boot.\n\n**-N, --delete-bootnext**\n> Delete the BootNext setting.\n\n**-a, --active**\n> Set the boot entry specified by -b as active.\n\n**-A, --inactive**\n> Set the boot entry specified by -b as inactive.\n\n**-t, --timeout** _seconds_\n> Set the boot manager timeout in seconds.\n\n**-T, --delete-timeout**\n> Delete the boot manager timeout variable.\n\n**-u, --unicode**\n> Pass extra command line arguments in Unicode.\n\n**-v, --verbose**\n> Print additional information including device paths."],["CAVEATS","Requires root privileges. Modifies UEFI NVRAM variables. Use with caution as incorrect settings can prevent system boot. Backup boot entries before making changes. Only works on systems booted in UEFI mode, not legacy BIOS."],["INSTALL","```apt: sudo apt install efibootmgr```\n\n```dnf: sudo dnf install efibootmgr```\n\n```pacman: sudo pacman -S efibootmgr```\n\n```apk: sudo apk add efibootmgr```\n\n```zypper: sudo zypper install efibootmgr```\n\n```nix: nix profile install nixpkgs#efibootmgr```"],["SEE ALSO","[efivar](/man/efivar)(1), [grub-install](/man/grub-install)(8), [mokutil](/man/mokutil)(1)"]],"e":["efivar","grub-install","mokutil"],"x":"efibootmgr -v"},"einfo":{"n":"einfo","t":"NCBI Entrez database information tool","s":[["TAGLINE","NCBI Entrez database information tool"],["TLDR","**Print** all database names\n\n```einfo -dbs```\n\n**Print** all information of a database in XML format\n\n```einfo -db [protein]```\n\n**Print** all fields of a database\n\n```einfo -db [nuccore] -fields```\n\n**Print** all links of a database\n\n```einfo -db [protein] -links```"],["SYNOPSIS","**einfo** [_options_]"],["DESCRIPTION","**einfo** provides information about NCBI Entrez databases. It shows the number of records indexed in each field, last update dates, and available cross-database links.\n\nPart of the NCBI EDirect utilities for programmatic access to biological databases."],["PARAMETERS","**-dbs**\n> List all database names.\n\n**-db** _database_\n> Specify database to query.\n\n**-fields**\n> Show available search fields for the database.\n\n**-links**\n> Show available cross-database links.\n\n**-help**\n> Print usage information.\n\n**-email** _address_\n> E-mail address to send with requests."],["CAVEATS","Part of the edirect package from NCBI. Requires internet connection. Output is in XML format by default."],["INSTALL","```apt: sudo apt install epub-utils```\n\n```dnf: sudo dnf install ebook-tools```\n\n```pacman: sudo pacman -S ebook-tools```\n\n```apk: sudo apk add ebook-tools```\n\n```zypper: sudo zypper install ebook-tools```\n\n```brew: brew install ebook-tools```"],["SEE ALSO","[esearch](/man/esearch)(1), [elink](/man/elink)(1)"]],"e":["esearch","elink"],"x":"einfo -dbs"},"eza":{"n":"eza","t":"modern ls replacement with git integration","s":[["TAGLINE","modern ls replacement with git integration"],["TLDR","**List files with** icons and colors\n\n```eza --icons```\n\n**Long format with** git status\n\n```eza -l --git```\n\n**Show hidden files**\n\n```eza -a```\n\n**Tree view**\n\n```eza --tree --level [2]```\n\n**Sort by modification** time\n\n```eza -l --sort modified```"],["SYNOPSIS","**eza** [_options_] [_files_...]"],["PARAMETERS","_FILES_\n> Files or directories to list (default: current directory).\n\n**-l**, **--long**\n> Display detailed list format.\n\n**-a**, **--all**\n> Show hidden files (dotfiles).\n\n**--tree**\n> Display as tree structure.\n\n**--level** _N_\n> Tree depth limit.\n\n**--git**\n> Show git status for each file.\n\n**--icons**\n> Display file type icons.\n\n**-h**, **--header**\n> Show column headers.\n\n**--sort** _FIELD_\n> Sort by: name, size, modified, accessed, created.\n\n**-r**, **--reverse**\n> Reverse sort order.\n\n**--group-directories-first**\n> List directories before files.\n\n**--help**\n> Display help information."],["DESCRIPTION","**eza** is a modern replacement for ls, written in Rust. It provides colorful output, git integration, extended attributes display, and tree view capabilities out of the box.\n\nThe tool improves on ls with features like git status integration, file icons, and better defaults for human-readable output. It recognizes many file types and displays them with distinctive colors and icons.\n\neza is designed as a drop-in ls replacement while adding modern features developers expect from file listing tools."],["CAVEATS","Requires terminal with color support. Icons need compatible font (Nerd Font). Git integration adds overhead in large repos. Not POSIX-compliant."],["HISTORY","eza is a maintained fork of **exa**, which was unmaintained since 2023. It continues development with new features and bug fixes, preserving exa's modern approach to file listing while actively maintaining the codebase."],["INSTALL","```apt: sudo apt install eza```\n\n```dnf: sudo dnf install eza```\n\n```pacman: sudo pacman -S eza```\n\n```apk: sudo apk add eza```\n\n```zypper: sudo zypper install eza```\n\n```brew: brew install eza```\n\n```nix: nix profile install nixpkgs#eza```"],["SEE ALSO","[ls](/man/ls)(1), [tree](/man/tree)(1), [lsd](/man/lsd)(1), [exa](/man/exa)(1)"],["RESOURCES","```[Source code](https://github.com/eza-community/eza)```\n\n```[Homepage](https://eza.rocks/)```"]],"e":["ls","tree","lsd","exa"],"x":"eza --icons"},"flex":{"n":"flex","t":"fast lexical analyzer generator","s":[["TAGLINE","fast lexical analyzer generator"],["TLDR","**Generate** an analyzer from a Lex file (output: lex.yy.c)\n\n```flex [analyzer.l]```\n\n**Specify the output file**\n\n```flex -o [analyzer.c] [analyzer.l]```\n\nWrite analyzer to **stdout**\n\n```flex -t [analyzer.l]```\n\nGenerate a **batch scanner** instead of interactive\n\n```flex -B [analyzer.l]```\n\nGenerate **case-insensitive** scanner\n\n```flex -i [analyzer.l]```\n\nGenerate scanner with **debugging** enabled\n\n```flex -d [analyzer.l]```\n\n**Compile** the generated C file\n\n```cc lex.yy.c -o [executable] -lfl```"],["SYNOPSIS","**flex** [_options_] [_file..._]"],["PARAMETERS","**-o, --outfile** _file_\n> Write generated scanner to specified file\n\n**-t, --stdout**\n> Write scanner to stdout instead of file\n\n**-B, --batch**\n> Generate batch scanner (optimized for throughput)\n\n**-I, --interactive**\n> Generate interactive scanner (default)\n\n**-i, --case-insensitive**\n> Generate case-insensitive scanner\n\n**-d, --debug**\n> Enable debug mode in generated scanner\n\n**-b, --backup**\n> Generate backup information file (lex.backup)\n\n**-p, --perf-report**\n> Generate performance report to stderr\n\n**-v, --verbose**\n> Print scanner statistics to stderr\n\n**-f, --full**\n> Generate fast, large scanner with full tables (-Cfr).\n\n**-F, --fast**\n> Generate fast scanner with alternate table representation (-CFr).\n\n**--header-file** _file_\n> Generate C header file\n\n**-+, --c++**\n> Generate C++ scanner class\n\n**--yylineno**\n> Track line numbers in generated scanner"],["DESCRIPTION","**Flex** (fast lexical analyzer generator) generates programs that perform pattern-matching on text. Given a specification file describing patterns and actions (typically with **.l** extension), flex generates C source code implementing a lexical analyzer.\n\nThe generated scanner reads input, matches it against patterns defined in the specification, and executes associated actions. This is commonly used to build tokenizers for compilers and interpreters, working alongside parser generators like **bison** or **yacc**.\n\nBy default, flex generates output to **lex.yy.c**. The generated code requires linking with **-lfl** (flex library) or defining **yywrap()** to return 1."],["CAVEATS","The generated scanner depends on the flex runtime library (**-lfl**) unless **%option noyywrap** is specified. Large pattern sets may produce sizable table-driven scanners; use compression options (**-Ce**, **-Cm**) to reduce size at some performance cost. The default interactive mode adds buffering overhead; use **-B** for batch processing."],["HISTORY","Flex was written by Vern Paxson around **1987** as a free, open-source replacement for the original **lex** utility from AT&T Unix (created by Mike Lesk and Eric Schmidt in the mid-1970s). The name stands for \"fast lexical analyzer generator.\" Flex became the standard lex implementation on most Unix-like systems and continues to be maintained on GitHub."],["INSTALL","```apt: sudo apt install flex```\n\n```dnf: sudo dnf install flex```\n\n```pacman: sudo pacman -S flex```\n\n```apk: sudo apk add flex```\n\n```zypper: sudo zypper install flex```\n\n```brew: brew install flex```\n\n```nix: nix profile install nixpkgs#flex```"],["SEE ALSO","[lex](/man/lex)(1), [bison](/man/bison)(1), [yacc](/man/yacc)(1), [gcc](/man/gcc)(1), [cc](/man/cc)(1)"]],"e":["lex","bison","yacc","gcc","cc"],"x":"flex [analyzer.l]"},"fossil-rm":{"n":"fossil-rm","t":"schedule files for removal","s":[["TAGLINE","schedule files for removal"],["TLDR","**Remove a file**\n\n```fossil rm [file]```\n\n**Remove with force**\n\n```fossil rm -f [file]```\n\n**Remove with dry run**\n\n```fossil rm --dry-run [file]```"],["SYNOPSIS","**fossil** **rm** [_options_] _file_..."],["PARAMETERS","**--dry-run**\n> Show what would be removed.\n\n**-f**, **--force**\n> Force removal.\n\n**--hard**\n> Also delete from disk."],["DESCRIPTION","**fossil rm** schedules files for removal from the repository on the next commit. By default, files remain on disk and are only removed from version control.\n\nThe **--hard** option deletes files from both the repository and the filesystem. This is useful for cleaning up unwanted files while also removing them from version control.\n\nUnlike **fossil forget**, which only works on files not yet committed, **fossil rm** handles files already tracked in the repository."],["INSTALL","```apt: sudo apt install fossil```\n\n```dnf: sudo dnf install fossil```\n\n```pacman: sudo pacman -S fossil```\n\n```apk: sudo apk add fossil```\n\n```zypper: sudo zypper install fossil```\n\n```brew: brew install fossil```\n\n```nix: nix profile install nixpkgs#fossil```"],["SEE ALSO","[fossil-add](/man/fossil-add)(1), [fossil-forget](/man/fossil-forget)(1)"],["RESOURCES","```[Homepage](https://fossil-scm.org/)```\n\n```[Documentation](https://fossil-scm.org/home/help?cmd=rm)```"]],"e":["fossil-add","fossil-forget"],"x":"fossil rm [file]"},"fslsfonts":{"n":"fslsfonts","t":"list fonts from X font server","s":[["TAGLINE","list fonts from X font server"],["TLDR","**List all fonts**\n\n```fslsfonts```\n\n**List fonts matching** pattern\n\n```fslsfonts -fn '[*-courier-*]'```\n\n**Show long listing**\n\n```fslsfonts -l```\n\n**List from specific** server\n\n```fslsfonts -server [hostname:7100]```\n\n**Show font columns**\n\n```fslsfonts -C```"],["SYNOPSIS","**fslsfonts** [_options_] [_pattern_]"],["PARAMETERS","_PATTERN_\n> Font pattern to match.\n\n**-fn** _PATTERN_\n> XLFD pattern of fonts to list (defaults to **\\***).\n\n**-l**\n> Long listing — show font properties. Repeat as **-ll** or **-lll** for progressively more detail.\n\n**-m**\n> Include minimum and maximum character bounds in long listings.\n\n**-C**\n> Multi-column output.\n\n**-1**\n> Single-column output (one font per line).\n\n**-w** _WIDTH_\n> Set the column width used by **-C** (default 79).\n\n**-n** _COLUMNS_\n> Force the output to use _COLUMNS_ columns (implies **-C**).\n\n**-u**\n> Leave the output unsorted.\n\n**-server** _HOST:PORT_\n> Query the named font server (e.g. **tcp/host:7100**, **unix/:7100**) instead of the **FONTSERVER** environment variable."],["DESCRIPTION","**fslsfonts** lists fonts available from X font servers. It queries the font server and displays matching font names in XLFD (X Logical Font Description) format.\n\nThe tool helps identify available fonts for X applications. Patterns can match specific font families, weights, or sizes. Long format shows additional font properties.\n\nfslsfonts is the font server equivalent of xlsfonts for local X fonts."],["CAVEATS","Requires running X font server. XLFD patterns can be complex. Less relevant with modern fontconfig."],["HISTORY","fslsfonts is part of the **X Window System** font server utilities. It queries xfs font servers, which were the standard way to share fonts across X terminals and workstations."],["INSTALL","```apt: sudo apt install x11-xfs-utils```"],["SEE ALSO","[xlsfonts](/man/xlsfonts)(1), [xfs](/man/xfs)(1), [fc-list](/man/fc-list)(1)"]],"e":["xlsfonts","xfs","fc-list"],"x":"fslsfonts"},"gcloud-sql-export-sql":{"n":"gcloud-sql-export-sql","t":"exports Cloud SQL data to storage","s":[["TAGLINE","exports Cloud SQL data to storage"],["TLDR","**Export database to Cloud Storage**\n\n```gcloud sql export sql [instance] [gs://bucket/file.sql] --database [db_name]```\n\n**Export all databases**\n\n```gcloud sql export sql [instance] [gs://bucket/file.sql]```\n\n**Export asynchronously**\n\n```gcloud sql export sql [instance] [gs://bucket/file.sql] --async```"],["SYNOPSIS","**gcloud** **sql** **export** **sql** _instance_ _uri_ [_options_]"],["PARAMETERS","**--database** _name_\n> Database to export.\n\n**--table** _name_\n> Tables to export.\n\n**--async**\n> Don't wait for completion.\n\n**--offload**\n> Offload to replica."],["DESCRIPTION","**gcloud sql export sql** exports data from a Cloud SQL instance to a SQL dump file stored in Google Cloud Storage. The command creates a database backup in standard SQL format that can be imported into other database systems or restored to Cloud SQL instances.\n\nThe tool supports both MySQL and PostgreSQL instances. Exports can target specific databases or tables, or dump all databases at once. The async mode allows long-running exports to continue in the background without blocking the terminal. Offloading exports to read replicas minimizes performance impact on production instances."]],"e":[],"x":"gcloud sql export sql [instance] [gs://bucket/file.sql] --database [db_name]"},"gh-gpg-key":{"n":"gh-gpg-key","t":"Manage GPG keys on GitHub for commit signing","s":[["TAGLINE","Manage GPG keys on GitHub for commit signing"],["TLDR","**List GPG keys** on your GitHub account\n\n```gh gpg-key list```\n\n**Add a GPG key** from a file\n\n```gh gpg-key add [key.pub]```\n\n**Add a GPG key** from stdin\n\n```gpg --armor --export [key_id] | gh gpg-key add -```\n\n**Add a GPG key with a title**\n\n```gh gpg-key add [key.pub] --title \"[Work laptop]\"```\n\n**Delete a GPG key** by ID\n\n```gh gpg-key delete [key_id]```"],["SYNOPSIS","**gh** **gpg-key** _subcommand_ [_options_]"],["SUBCOMMANDS","**list**\n> List the GPG keys associated with the authenticated user.\n\n**add** [_key-file_]\n> Add a GPG public key to your GitHub account. Reads from stdin if **-** is passed instead of a file.\n\n**delete** _key-id_\n> Remove a GPG key from your GitHub account by its numeric ID."],["PARAMETERS","**-t**, **--title** _string_\n> Title of the new key (used by **add**).\n\n**--yes**\n> Skip the interactive confirmation prompt (used by **delete**)."],["DESCRIPTION","**gh gpg-key** manages GPG keys associated with your GitHub account. GitHub uses these keys to verify signed commits and tags, displaying a \"Verified\" badge in the web UI when signatures match a registered key.\n\nKeys must be exported in ASCII-armored format (e.g. via **gpg --armor --export**). To enable verification, the key's email must also match a verified email on the account, and commits must be signed locally (e.g. via **git config commit.gpgsign true**)."],["CAVEATS","Requires authentication via **gh auth login**. The acting user must have **admin:gpg_key** scope on their token. Keys uploaded to GitHub cannot be used to decrypt anything — GitHub only stores public keys for signature verification."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-ssh-key](/man/gh-ssh-key)(1), [gpg](/man/gpg)(1), [git-commit](/man/git-commit)(1)"]],"e":["gh","gh-ssh-key","gpg","git-commit"],"x":"gh gpg-key list"},"git-column":{"n":"git-column","t":"Display data in columns","s":[["TAGLINE","Display data in columns"],["TLDR","**Format input into columns (column-first layout)**\n\n```seq 1 24 | git column --mode=column --padding=[5]```\n\n**Format input into columns (row-first layout)**\n\n```seq 1 21 | git column --mode=row --padding=[5]```\n\n**Format input with a specific terminal width**\n\n```echo \"[one two three four five six]\" | git column --mode=column --width=[40]```\n\n**Add indentation to each line**\n\n```seq 1 12 | git column --mode=column --indent=\"[  ]\"```"],["SYNOPSIS","**git** **column** [_options_]"],["PARAMETERS","**--command** _name_\n> Look up layout mode using configuration variable column._name_ and column.ui.\n\n**--mode** _mode_\n> Specify layout mode. Supported values include column (fill columns top to bottom), row (fill rows left to right), and plain (no formatting).\n\n**--raw-mode** _n_\n> Same as **--mode** but takes mode encoded as a number. Mainly used by other commands that have already parsed layout mode.\n\n**--width** _width_\n> Specify the terminal width. By default **git column** detects the terminal width, or falls back to 80 if unable to do so.\n\n**--indent** _string_\n> String to be printed at the beginning of each line.\n\n**--nl** _string_\n> String to be printed at the end of each line, including newline character.\n\n**--padding** _N_\n> The number of spaces between columns. One space by default."],["DESCRIPTION","**git column** is a utility command that formats its standard input into a table with multiple columns, where each input line occupies one cell. While primarily used internally by Git to display lists (such as branch names, tag lists, or file listings) in a space-efficient columnar format, it is also available as a standalone formatting tool.\n\nThe command reads input from stdin and arranges it into columns based on terminal width or specified constraints. It supports different layout modes: column-first (fill columns top to bottom), row-first (fill rows left to right), or plain (no formatting)."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[column](/man/column)(1), [git-branch](/man/git-branch)(1), [git-tag](/man/git-tag)(1)"]],"e":["column","git-branch","git-tag"],"x":"seq 1 24 | git column --mode=column --padding=[5]"},"gotz":{"n":"gotz","t":"CLI timezone info tool for cross-timezone teams","s":[["TAGLINE","CLI timezone info tool for cross-timezone teams"],["TLDR","**Show** current time in multiple zones\n\n```gotz```\n\n**Show** time for specific city\n\n```gotz [city-name]```\n\n**Convert** time between zones\n\n```gotz \"[time]\" [from-zone] [to-zone]```\n\n**List** all supported timezones\n\n```gotz --list```"],["SYNOPSIS","**gotz** [_options_] [_timezone-or-query_]"],["PARAMETERS","**-l, --list**\n> List all supported timezones\n\n**-c, --config** _FILE_\n> Configuration file path\n\n**-f, --format** _FORMAT_\n> Time format string\n\n**-v, --version**\n> Display version and exit\n\n**-h, --help**\n> Display help and exit"],["DESCRIPTION","**gotz** is a command-line tool for working with timezones. It shows current times in different locations and converts times between timezones, making it useful for teams working across multiple regions.\n\nThe tool can display multiple timezones simultaneously and supports fuzzy matching for city names."],["EXAMPLES","Show configured timezones:\n```gotz```\n\nShow time in Tokyo:\n```gotz tokyo```\n\nConvert 3pm NYC to Tokyo:\n```gotz \"3pm\" America/New_York Asia/Tokyo```"],["CONFIGURATION","Config file (~/.config/gotz/config.toml):\n\n```toml\n[zones]\ndefault = [\"America/New_York\", \"Europe/London\", \"Asia/Tokyo\"]\n```"],["CAVEATS","Timezone names must follow IANA database. Fuzzy matching may return unexpected results. DST transitions may cause confusion."],["HISTORY","**gotz** was created to simplify timezone conversions for distributed teams, providing a quick CLI alternative to online tools."],["INSTALL","```brew: brew install gotz```"],["SEE ALSO","[date](/man/date)(1), [tzselect](/man/tzselect)(1), [timedatectl](/man/timedatectl)(1)"]],"e":["date","tzselect","timedatectl"],"x":"gotz"},"gsql":{"n":"gsql","t":"TigerGraph graph database query shell","s":[["TAGLINE","TigerGraph graph database query shell"],["TLDR","**Connect to TigerGraph**\n\n```gsql```\n\n**Run query from file**\n\n```gsql [query.gsql]```\n\n**Run inline query**\n\n```gsql -g [graphname] \"[GSQL query]\"```\n\n**Specify graph**\n\n```gsql -g [mygraph]```\n\n**Run as user**\n\n```gsql -u [username] -p [password]```"],["SYNOPSIS","**gsql** [_options_] [_file_|_query_]"],["PARAMETERS","**-g** _graph_\n> Graph name to use.\n\n**-u** _user_\n> Username.\n\n**-p** _password_\n> Password.\n\n**-ip** _host_\n> Server IP address.\n\n**--ssl**\n> Use SSL connection."],["DESCRIPTION","**GSQL** is the query language and shell for TigerGraph, a graph database. It provides SQL-like syntax for graph queries, data loading, and schema management.\n\nGSQL supports pattern matching, graph algorithms, and procedural extensions for complex graph analytics."],["QUERY EXAMPLE","```gsql\nUSE GRAPH social\n\nCREATE QUERY findFriends(VERTEX<Person> p) FOR GRAPH social {\n  Start = {p};\n  Friends = SELECT t FROM Start:s -(Knows:e)- Person:t;\n  PRINT Friends;\n}\n\nINSTALL QUERY findFriends\nRUN QUERY findFriends(\"person1\")\n```"],["CAVEATS","Requires TigerGraph installation. Query installation needed before running. Syntax differs from standard SQL. Graph schema must be defined first."],["HISTORY","GSQL was developed by **TigerGraph** (formerly GraphSQL), founded by **Yu Xu** in **2012**. TigerGraph focuses on real-time analytics on massive graphs, with GSQL designed for both simple queries and complex graph algorithms."],["SEE ALSO","[neo4j](/man/neo4j)(1)"]],"e":["neo4j"],"x":"gsql"},"hddtemp":{"n":"hddtemp","t":"displays the temperature of hard disk drives via S","s":[["TAGLINE","displays the temperature of hard disk drives via S"],["TLDR","Display **temperature** of a SATA drive\n\n```hddtemp SATA:/dev/sda```\n\nDisplay in **Fahrenheit**\n\n```hddtemp -u F SATA:/dev/sda```\n\nDisplay **numeric** value only\n\n```hddtemp -n SATA:/dev/sda```\n\nLog to **syslog** every N seconds\n\n```hddtemp -S 60 SATA:/dev/sda```\n\n**Wake** drive before reading temperature\n\n```hddtemp -w SATA:/dev/sda```\n\nRun in **daemon** mode\n\n```hddtemp -d SATA:/dev/sda```\n\nShow **debug** information\n\n```hddtemp -D SATA:/dev/sda```"],["SYNOPSIS","**hddtemp** [_options_] [_type_]:_device_"],["DESCRIPTION","**hddtemp** displays the temperature of hard disk drives via S.M.A.R.T. (Self-Monitoring, Analysis and Reporting Technology). It can show temperature for SATA, PATA, and SCSI drives that support S.M.A.R.T."],["PARAMETERS","**-n, --numeric**\n> Print only the numeric temperature value without units\n\n**-u, --unit=UNIT**\n> Set temperature unit (C for Celsius, F for Fahrenheit)\n\n**-S, --syslog=SECONDS**\n> Log temperatures to syslog every N seconds\n\n**-d, --daemon**\n> Run as daemon (TCP/IP server mode)\n\n**-l, --listen=ADDR**\n> Listen on specified address in daemon mode\n\n**-p, --port=PORT**\n> Listen on specified port in daemon mode (default: 7634)\n\n**-w, --wake-up**\n> Wake up the drive before reading temperature\n\n**-q, --quiet**\n> Suppress drive type compatibility warnings\n\n**-D, --debug**\n> Show S.M.A.R.T. fields and their values\n\n**-f, --file=FILE**\n> Use specified drive database file"],["CAVEATS","Requires root privileges to access drive S.M.A.R.T. data. The drive type prefix (SATA, PATA, SCSI) is required for proper operation. Not all drives support temperature monitoring through S.M.A.R.T."],["HISTORY","**hddtemp** was developed to provide simple temperature monitoring for hard drives using S.M.A.R.T. data, commonly used for system health monitoring."],["INSTALL","```dnf: sudo dnf install hddtemp```\n\n```pacman: sudo pacman -S hddtemp```\n\n```nix: nix profile install nixpkgs#hddtemp```"],["SEE ALSO","[smartctl](/man/smartctl)(8), [sensors](/man/sensors)(1)"]],"e":["smartctl","sensors"],"x":"hddtemp SATA:/dev/sda"},"hledger-print":{"n":"hledger-print","t":"outputs transactions from the journal","s":[["TAGLINE","outputs transactions from the journal"],["TLDR","**Print all transactions**\n\n```hledger print```\n\n**Print specific account**\n\n```hledger print [account-pattern]```\n\n**Print date range**\n\n```hledger print -b [2024-01-01] -e [2024-12-31]```\n\n**Print with cost**\n\n```hledger print --cost```\n\n**Output as CSV**\n\n```hledger print -O csv```"],["SYNOPSIS","**hledger print** [_options_] [_patterns_]"],["PARAMETERS","_PATTERNS_\n> Account or description patterns.\n\n**-b** _DATE_\n> Begin date.\n\n**-e** _DATE_\n> End date.\n\n**--cost**\n> Convert to cost basis.\n\n**--explicit**\n> Make amounts explicit.\n\n**-O** _FORMAT_\n> Output format (txt, csv).\n\n**--help**\n> Display help information."],["DESCRIPTION","**hledger print** outputs transactions from the journal. It reproduces transaction records, optionally filtered or transformed.\n\nThe command is useful for extracting subsets of transactions or converting between formats. Output is valid journal syntax."],["CAVEATS","Outputs in journal format by default. Filtering reduces output. Part of hledger suite."],["HISTORY","Print is a core **hledger** command for displaying and extracting transactions from journals."],["INSTALL","```apt: sudo apt install hledger```\n\n```dnf: sudo dnf install hledger```\n\n```pacman: sudo pacman -S hledger```\n\n```apk: sudo apk add hledger```\n\n```zypper: sudo zypper install hledger```\n\n```brew: brew install hledger```\n\n```nix: nix profile install nixpkgs#hledger```"],["SEE ALSO","[hledger](/man/hledger)(1), [hledger-import](/man/hledger-import)(1)"]],"e":["hledger","hledger-import"],"x":"hledger print"},"httm":{"n":"httm","t":"Interactive file-level Time Machine for ZFS, btrfs, and nilfs2","s":[["TAGLINE","Interactive file-level Time Machine for ZFS, btrfs, and nilfs2"],["TLDR","**Show all available versions** of a file\n\n```httm [path/to/file]```\n\n**Browse snapshots interactively**\n\n```httm -i [path/to/file]```\n\n**Restore a file** from a snapshot\n\n```httm -r [path/to/file]```\n\n**List deleted files** in a directory\n\n```httm -d [path/to/directory]```\n\n**Recursively list deleted files**\n\n```httm -R [path/to/directory]```\n\n**Prune snapshots interactively**\n\n```httm --prune```"],["SYNOPSIS","**httm** [**-i**|**-r**|**-d**|**-R**] [_options_] [_path_]"],["DESCRIPTION","**httm** prints the size, date, and corresponding locations of available unique versions of files residing on filesystem snapshots. It works with **ZFS**, **btrfs**, **nilfs2** snapshots, as well as **Restic** and **Time Machine** backups.\n\nThe tool has four native interactive modes: **browse** for navigating snapshot versions, **select** for choosing a specific version, **prune** for removing old snapshots, and **restore** for recovering files. It can search for and recursively list deleted files, including files hidden behind deleted directories."],["CAVEATS","Requires a filesystem with snapshot support (ZFS, btrfs, or nilfs2) or compatible backup system. Performance depends on the number of snapshots. Restoration overwrites the current file version."],["HISTORY","**httm** was created by **kimono-koans** and is written in **Rust**. It was designed to bring the intuitive file-level browsing experience of macOS Time Machine to Linux snapshot-capable filesystems."],["INSTALL","```apk: sudo apk add httm```\n\n```brew: brew install httm```\n\n```nix: nix profile install nixpkgs#httm```"],["SEE ALSO","[zfs](/man/zfs)(8), [btrfs](/man/btrfs)(8), [restic](/man/restic)(1)"]],"e":["zfs","btrfs","restic"],"x":"httm [path/to/file]"},"jj-diff":{"n":"jj-diff","t":"shows differences between revisions in Jujutsu","s":[["TAGLINE","shows differences between revisions in Jujutsu"],["TLDR","**Show working copy diff**\n\n```jj diff```\n\n**Diff specific revision**\n\n```jj diff -r [revision]```\n\n**Diff between revisions**\n\n```jj diff --from [rev1] --to [rev2]```\n\n**Diff specific file**\n\n```jj diff [file]```"],["SYNOPSIS","**jj** **diff** [_options_] [_paths_...]"],["PARAMETERS","**-r**, **--revision** _rev_\n> Show changes in revision.\n\n**--from** _rev_\n> Start revision for comparison.\n\n**--to** _rev_\n> End revision for comparison.\n\n**-s**, **--summary**\n> Show summary only.\n\n**--stat**\n> Show diffstat."],["DESCRIPTION","**jj diff** shows differences between revisions in Jujutsu. By default, shows changes in the working copy. Use **-r** for specific revision or **--from/--to** for range comparisons."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-status](/man/jj-status)(1)"]],"e":["jj","jj-status"],"x":"jj diff"},"llvm-bcanalyzer":{"n":"llvm-bcanalyzer","t":"analyzes LLVM bitcode files","s":[["TAGLINE","analyzes LLVM bitcode files"],["TLDR","**Analyze bitcode file**\n\n```llvm-bcanalyzer [file.bc]```\n\n**Show detailed dump**\n\n```llvm-bcanalyzer --dump [file.bc]```\n\n**Show only statistics**\n\n```llvm-bcanalyzer --dump-blockinfo [file.bc]```"],["SYNOPSIS","**llvm-bcanalyzer** [_options_] _file_"],["PARAMETERS","**--dump**\n> Dump low-level bitcode contents.\n\n**--dump-blockinfo**\n> Show block info statistics.\n\n**--show-binary-blobs**\n> Show binary blob data."],["DESCRIPTION","**llvm-bcanalyzer** analyzes LLVM bitcode files. Shows statistics about bitcode structure, including block types, record counts, and bit distribution. Useful for debugging bitcode generation and understanding LLVM IR serialization."],["INSTALL","```apt: sudo apt install llvm```\n\n```dnf: sudo dnf install llvm```\n\n```pacman: sudo pacman -S llvm```\n\n```apk: sudo apk add llvm```\n\n```zypper: sudo zypper install llvm```\n\n```brew: brew install llvm```\n\n```nix: nix profile install nixpkgs#llvm```"],["SEE ALSO","[llvm-dis](/man/llvm-dis)(1), [llvm-as](/man/llvm-as)(1)"]],"e":["llvm-dis","llvm-as"],"x":"llvm-bcanalyzer [file.bc]"},"lspci":{"n":"lspci","t":"displays information about PCI buses in the system and devices connected to them","s":[["TAGLINE","displays information about PCI buses in the system and devices connected to them"],["TLDR","Show **brief list** of devices\n\n```lspci```\n\nDisplay **verbose** information\n\n```lspci -v```\n\nDisplay **kernel drivers** and modules\n\n```lspci -k```\n\nSelect **specific device**\n\n```lspci -s 00:18.3```\n\nMachine **readable** output\n\n```lspci -vm```\n\nShow vendor/device as **numbers and names**\n\n```lspci -nn```\n\nDisplay as **tree**\n\n```lspci -t```"],["SYNOPSIS","**lspci** [_options_]"],["DESCRIPTION","**lspci** displays information about PCI buses in the system and devices connected to them. It provides details about hardware, drivers, and capabilities of PCI devices."],["PARAMETERS","**-v**\n> Verbose output with device details\n\n**-vv**\n> Very verbose output\n\n**-vvv**\n> Maximum verbosity\n\n**-k**\n> Show kernel drivers and available modules\n\n**-n**\n> Show vendor/device codes as numbers only\n\n**-nn**\n> Show both numbers and names\n\n**-m**\n> Machine-readable output format\n\n**-mm**\n> Machine-readable format for scripts\n\n**-t**\n> Show as tree diagram\n\n**-s SLOT**\n> Show only device in specified slot\n\n**-d VENDOR:DEVICE**\n> Show only devices with specified IDs\n\n**-x**\n> Hexadecimal dump of configuration space\n\n**-xxx**\n> Full PCI configuration space (root only)\n\n**-D**\n> Always show PCI domain numbers\n\n**-P**\n> Show devices by bridge path\n\n**-A** _METHOD_\n> Use specified PCI access method (see -A help for list)\n\n**-M**\n> Bus mapping mode; thorough scan including misconfigured bridges (root only)\n\n**-q**\n> Query central database for unknown devices\n\n**-i** _FILE_\n> Use alternate PCI ID file"],["CAVEATS","Some information requires root privileges. The **-xxx** option shows full configuration space but needs root access."],["HISTORY","**lspci** is part of **pciutils**, providing PCI device information and configuration utilities for Linux systems."],["INSTALL","```apt: sudo apt install pciutils```\n\n```dnf: sudo dnf install pciutils```\n\n```pacman: sudo pacman -S pciutils```\n\n```apk: sudo apk add pciutils```\n\n```zypper: sudo zypper install pciutils```\n\n```brew: brew install pciutils```\n\n```nix: nix profile install nixpkgs#pciutils```"],["SEE ALSO","[lsusb](/man/lsusb)(8)"]],"e":["lsusb"],"x":"lspci"},"lux":{"n":"lux","t":"Fast multi-site video and media downloader","s":[["TAGLINE","Fast multi-site video and media downloader"],["TLDR","**Download** a video from a URL\n\n```lux \"[url]\"```\n\n**List available streams** without downloading\n\n```lux -i \"[url]\"```\n\n**Download a specific stream** by format id\n\n```lux -f [stream_id] \"[url]\"```\n\n**Download an entire playlist**\n\n```lux -p \"[playlist_url]\"```\n\n**Set output directory and filename**\n\n```lux -o [path/to/dir] -O [name] \"[url]\"```\n\n**Download with cookies** (e.g. authenticated sites)\n\n```lux -c \"[cookie_string]\" \"[url]\"```"],["SYNOPSIS","**lux** [*OPTIONS*] *URL*..."],["DESCRIPTION","**lux** is a fast video and media downloader written in Go. It extracts playable streams from many popular sites (YouTube, Bilibili, Douyin, and others) and downloads them, optionally merging audio/video with FFmpeg. When given a direct resource URL that is not a known site, it falls back to a plain download.\n\nUse **-i** to inspect available qualities and stream ids, then **-f** to pick one. Playlist mode (**-p**), multi-threaded multi-part downloads, proxies, cookies, and aria2 RPC are supported for larger or authenticated jobs."],["PARAMETERS","**-i**\n\n> Information only: list available streams and metadata without downloading.\n\n**-f** *stream*\n\n> Select a specific stream id (from **-i** output) to download.\n\n**-p**\n\n> Download a playlist instead of a single item.\n\n**-n** *int*\n\n> Number of download threads for multi-part videos (default: 10).\n\n**-c** *cookie*\n\n> Cookie string for authenticated or region-locked content.\n\n**-r** *referrer*\n\n> HTTP Referer header to send.\n\n**-o** *path*\n\n> Output directory.\n\n**-O** *name*\n\n> Output file name.\n\n**-C**\n\n> Download subtitles (YouTube and supported sites). Combine with **-items** for language codes.\n\n**-retry** *int*\n\n> Retry count on download failure (default: 10).\n\n**-start** *n*, **-end** *n*, **-items** *list*\n\n> Playlist range or item selection (e.g. **1,5,8-10**).\n\n**-aria2**\n\n> Offload download to an aria2 RPC endpoint (**-aria2addr**, **-aria2token**, etc.).\n\n**-d**\n\n> Debug mode.\n\n**-j**\n\n> Print extracted data as JSON.\n\n**-v**\n\n> Show version."],["CAVEATS","FFmpeg is required for merging separate audio/video streams into a final file; downloads still work without it. Site support and stream ids change as upstream sites change their APIs. Prefer official packages or release binaries over outdated distro builds when a site stops working."],["INSTALL","```zypper: sudo zypper install lux```\n\n```brew: brew install lux```\n\n```nix: nix profile install nixpkgs#lux```"],["SEE ALSO","[yt-dlp](/man/yt-dlp)(1), [youtube-dl](/man/youtube-dl)(1), [ffmpeg](/man/ffmpeg)(1), [aria2c](/man/aria2c)(1)"],["RESOURCES","```[Source code](https://github.com/iawia002/lux)```"]],"e":["yt-dlp","youtube-dl","ffmpeg","aria2c"],"x":"lux \"[url]\""},"lvmsadc":{"n":"lvmsadc","t":"was the LVM system activity data collector for gathering I/O","s":[["TAGLINE","was the LVM system activity data collector for gathering I/O"],["TLDR","Start the **legacy LVM1** statistics collector (obsolete on modern LVM2)\n\n```lvmsadc```\n\nOn current systems, report I/O stats with **dmstats** instead\n\n```dmstats report [/dev/mapper/device]```"],["SYNOPSIS","**lvmsadc**"],["DESCRIPTION","**lvmsadc** was the LVM system activity data collector for gathering I/O statistics on logical volumes. It collected raw performance data that could be analyzed with the companion tool **lvmsar**.\n\nThis command is **not supported under LVM2**. Modern LVM2 installations should use **dmstats** instead, which provides more comprehensive device-mapper statistics including histogram data and region-based measurements."],["CAVEATS","**Deprecated**: This command only works on legacy LVM1 systems. For LVM2, use **dmstats** to collect and report I/O statistics on device-mapper devices."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvmsar](/man/lvmsar)(8), [iostat](/man/iostat)(1), [lvm](/man/lvm)(8)"]],"e":["lvmsar","iostat","lvm"],"x":"lvmsadc"},"m4b-tool":{"n":"m4b-tool","t":"build, split, and tag M4B audiobooks from MP3 or other audio sources","s":[["TAGLINE","build, split, and tag M4B audiobooks from MP3 or other audio sources"],["TLDR","**Merge a folder of MP3s** into an M4B with chapters\n\n```m4b-tool merge [folder/] --output-file=[audiobook.m4b]```\n\n**Split an audiobook** by chapters into separate files\n\n```m4b-tool split --audio-format=mp3 [audiobook.m4b]```\n\n**Refresh embedded chapter** markers\n\n```m4b-tool chapters --adjust-by-silence [audiobook.m4b]```\n\n**Merge with full metadata**\n\n```m4b-tool merge [folder/] --output-file=[book.m4b] --name=\"[Title]\" --artist=\"[Author]\" --album=\"[Series]\"```\n\n**Convert a single MP3** to M4B\n\n```m4b-tool merge [file.mp3] --output-file=[file.m4b]```\n\n**Add a cover image** to an existing file\n\n```m4b-tool meta --cover=[cover.jpg] [audiobook.m4b]```"],["SYNOPSIS","**m4b-tool** _command_ [_options_] [_input_]"],["PARAMETERS","**merge**\n> Combine multiple input files into one M4B with chapters preserved.\n\n**split**\n> Split an M4B by chapter into one file per chapter.\n\n**chapters**\n> Inspect, generate, or rewrite chapter markers (supports **--adjust-by-silence**).\n\n**meta**\n> Read or update tags such as title, artist, cover art, and description.\n\n**--output-file** _FILE_\n> Path to the output file (M4B).\n\n**--audio-format** _FORMAT_\n> Output audio format for **split** (m4b, m4a, mp3, ...).\n\n**--name** _TITLE_\n> Book or album title.\n\n**--artist** _NAME_\n> Author or narrator name.\n\n**--album** _TEXT_\n> Album/series name.\n\n**--cover** _FILE_\n> Embed a cover image (JPEG or PNG).\n\n**--jobs** _N_\n> Number of parallel encoding jobs.\n\n**--no-cache**\n> Disable caching of intermediate analysis results."],["DESCRIPTION","**m4b-tool** is a PHP-based command-line wrapper around **ffmpeg** and **mp4v2** that creates, splits, retags, and inspects M4B audiobook files. The **merge** subcommand concatenates a directory of audio files (MP3, M4A, WAV, FLAC, etc.) into a single AAC-encoded M4B, automatically inserting chapter markers based on the order of input files or an accompanying **chapters.txt**.\n\nThe **split** subcommand reverses the process, exporting each chapter as a separate file in a chosen format. **chapters** allows inspecting or rewriting the chapter table, optionally aligning markers to detected silences with **--adjust-by-silence** for better playback experience. **meta** edits ID3/MP4 tags and can embed cover artwork.\n\nA persistent cache speeds up repeated runs across the same files; disable it with **--no-cache** when input changes are not being detected."],["CAVEATS","Requires **ffmpeg** and **mp4v2** binaries on PATH plus a working **PHP 7.4+** runtime. Encoding quality is governed by ffmpeg flags; high-bitrate libfdk_aac builds give the best results. Long audiobooks can take significant CPU and disk space for the merge stage."],["HISTORY","**m4b-tool** was written by **Andreas Sandberg** (sandreas) and is distributed under the MIT license. It became popular as an open-source alternative to commercial M4B builders, particularly for Linux users who maintain audiobook libraries with Audiobookshelf or Plex."],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [mp4box](/man/mp4box)(1), [sox](/man/sox)(1)"]],"e":["ffmpeg","mp4box","sox"],"x":"m4b-tool merge [folder/] --output-file=[audiobook.m4b]"},"mastui":{"n":"mastui","t":"Modern Mastodon TUI client for the terminal","s":[["TAGLINE","Modern Mastodon TUI client for the terminal"],["TLDR","**Launch the Mastodon client**\n\n```mastui```\n\n**Log in to a Mastodon instance**\n\n```mastui login```\n\n**Select a theme**\n\n```mastui theme```"],["SYNOPSIS","**mastui** [_options_]"],["PARAMETERS","**login**\n> Authenticate with a Mastodon instance.\n\n**theme**\n> Select a UI theme.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**mastui** is a fast, modern TUI client for Mastodon built with Python and Textual. It displays multi-column timelines (Home, Notifications, Federated) with persistent caching for smooth scrolling.\n\nFeatures include in-terminal image viewing (ANSI/Sixel/TGP), multiple light and dark themes, configurable per-timeline refresh intervals, a full-screen post composer with content warnings and language selection, and poll creation and management."],["CAVEATS","Requires a Mastodon account and API access token. Image display capabilities depend on terminal emulator support."],["HISTORY","**mastui** was created by **kimusan** and is written in **Python** using the Textual framework."],["SEE ALSO","[toot](/man/toot)(1)"]],"e":["toot"],"x":"mastui"},"metals":{"n":"metals","t":"Scala language server with rich IDE features","s":[["TAGLINE","Scala language server with rich IDE features"],["TLDR","**Install Metals using Coursier**\n\n```cs bootstrap org.scalameta:metals_2.13:[version] -o metals -f```\n\n**Start Metals as an MCP server for a workspace**\n\n```metals-mcp --workspace [/path/to/project]```\n\n**Start Metals MCP server on a specific port**\n\n```metals-mcp --workspace [/path/to/project] --port [8080]```\n\n**Generate editor config for a specific client**\n\n```metals-mcp --workspace [/path/to/project] --client [vscode]```"],["SYNOPSIS","**metals** [_options_]\n\n**metals-mcp** **--workspace** _path_ [_options_]"],["PARAMETERS","**--workspace** _path_\n> Path to the Scala project (required for MCP server).\n\n**--port** _number_\n> HTTP port to listen on. Auto-assigned by default.\n\n**--transport** _type_\n> Transport type: http (default) or stdio.\n\n**--client** _name_\n> Client to generate config for (e.g., vscode, cursor).\n\n**-v**, **--version**\n> Print version information.\n\n**-h**, **--help**\n> Print usage message."],["DESCRIPTION","**Metals** is a Scala language server implementing the Language Server Protocol (LSP). It provides IDE features like code completion, navigation, refactoring, and error checking for Scala projects.\n\nMetals works with various editors including VS Code, Vim/Neovim, Emacs, and Sublime Text."],["SUPPORTED BUILD TOOLS","```\n- sbt\n- Mill\n- Gradle\n- Maven\n- Bloop\n```"],["FEATURES","```\n- Code completion\n- Go to definition\n- Find references\n- Rename refactoring\n- Code actions\n- Diagnostics\n```"],["CAVEATS","Requires JDK 11+. Initial indexing takes time. Build tool must be supported. Performance depends on project size."],["HISTORY","Metals was created by **Ólafur Páll Geirsson** at the **Scala Center** in **2018** as the official Scala language server."],["INSTALL","```brew: brew install metals```\n\n```nix: nix profile install nixpkgs#metals```"],["SEE ALSO","[scala](/man/scala)(1), [sbt](/man/sbt)(1), [javac](/man/javac)(1)"]],"e":["scala","sbt","javac"],"x":"cs bootstrap org.scalameta:metals_2.13:[version] -o metals -f"},"moc":{"n":"moc","t":"terminal audio player","s":[["TAGLINE","terminal audio player"],["TLDR","**Start MOC**\n\n```mocp```\n\n**Start server only**\n\n```mocp -S```\n\n**Play file**\n\n```mocp -l [file.mp3]```\n\n**Add to playlist**\n\n```mocp -a [file.mp3]```\n\n**Play/pause**\n\n```mocp -G```\n\n**Next track**\n\n```mocp -f```\n\n**Stop server**\n\n```mocp -x```"],["SYNOPSIS","**mocp** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Audio files to play.\n\n**-S**\n> Start server.\n\n**-l** _FILE_\n> Play file.\n\n**-a** _FILE_\n> Add to playlist.\n\n**-G**\n> Toggle play/pause.\n\n**-f**\n> Next track.\n\n**-x**\n> Stop server.\n\n**--help**\n> Display help information."],["DESCRIPTION","**moc** (Music On Console) is a terminal audio player. It uses a client-server architecture.\n\nThe tool plays in background. Supports many formats via plugins. ncurses interface."],["CAVEATS","Server runs separately. ncurses interface. Plugin-based format support."],["HISTORY","MOC was created to provide a console-based **music player** with background playback support."],["INSTALL","```apt: sudo apt install qtchooser```\n\n```dnf: sudo dnf install qtchooser```\n\n```apk: sudo apk add qt5-qtbase-dev```\n\n```zypper: sudo zypper install moc```\n\n```brew: brew install moc```\n\n```nix: nix profile install nixpkgs#moc```"],["SEE ALSO","[mpd](/man/mpd)(1), [cmus](/man/cmus)(1), [mpv](/man/mpv)(1)"]],"e":["mpd","cmus","mpv"],"x":"mocp"},"mock":{"n":"mock","t":"builds RPMs in clean chroot environments","s":[["TAGLINE","builds RPMs in clean chroot environments"],["TLDR","**Build package in chroot**\n\n```mock -r [fedora-39-x86_64] [package.src.rpm]```\n\n**Initialize chroot**\n\n```mock -r [fedora-39-x86_64] --init```\n\n**Clean chroot**\n\n```mock -r [fedora-39-x86_64] --clean```\n\n**Shell into chroot**\n\n```mock -r [fedora-39-x86_64] --shell```\n\n**Build from spec**\n\n```mock -r [fedora-39-x86_64] --spec [package.spec] --sources [sources/]```\n\n**Rebuild SRPM**\n\n```mock -r [fedora-39-x86_64] --rebuild [package.src.rpm]```"],["SYNOPSIS","**mock** [_options_] _srpm_"],["PARAMETERS","_SRPM_\n> Source RPM to build.\n\n**-r** _CONFIG_\n> Chroot configuration.\n\n**--init**\n> Initialize chroot.\n\n**--clean**\n> Clean chroot.\n\n**--shell**\n> Enter chroot shell.\n\n**--rebuild**\n> Rebuild SRPM.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mock** builds RPMs in clean chroot environments. It isolates builds from the host system.\n\nThe tool creates reproducible builds. Used for Fedora/RHEL package building."],["CAVEATS","Fedora/RHEL focused. Requires mock group membership. Needs chroot configs."],["HISTORY","Mock was created by the **Fedora** project for building RPM packages in isolated environments."],["INSTALL","```dnf: sudo dnf install mock```"],["SEE ALSO","[rpmbuild](/man/rpmbuild)(8), [dnf](/man/dnf)(8), [koji](/man/koji)(1)"]],"e":["rpmbuild","dnf","koji"],"x":"mock -r [fedora-39-x86_64] [package.src.rpm]"},"mscore":{"n":"mscore","t":"museScore music notation software","s":[["TAGLINE","museScore music notation software"],["TLDR","**Open MuseScore**\n\n```mscore```\n\n**Open score file**\n\n```mscore [score.mscz]```\n\n**Convert to PDF**\n\n```mscore [score.mscz] -o [output.pdf]```\n\n**Export to MIDI**\n\n```mscore [score.mscz] -o [output.mid]```\n\n**Export to audio**\n\n```mscore [score.mscz] -o [output.mp3]```\n\n**Run in headless mode**\n\n```mscore --no-gui [score.mscz] -o [output.pdf]```"],["SYNOPSIS","**mscore** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Score files to open.\n\n**-o** _FILE_\n> Output file (format from extension).\n\n**--no-gui**\n> Run without graphical interface.\n\n**-S** _STYLE_\n> Use style file.\n\n**-r** _DPI_\n> Set image resolution.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mscore** is the MuseScore music notation software. It creates and edits sheet music.\n\nThe tool supports MIDI, MusicXML, and audio export. Professional notation features."],["CAVEATS","GUI application. Large dependency footprint. Soundfont needed for audio."],["HISTORY","MuseScore was created as **open-source notation** software, first released in 2002."],["INSTALL","```apt: sudo apt install musescore```\n\n```dnf: sudo dnf install musescore```\n\n```pacman: sudo pacman -S musescore```\n\n```apk: sudo apk add musescore```\n\n```zypper: sudo zypper install musescore```\n\n```nix: nix profile install nixpkgs#musescore```"],["SEE ALSO","[musescore](/man/musescore)(1), [lilypond](/man/lilypond)(1), [timidity](/man/timidity)(1)"]],"e":["musescore","lilypond","timidity"],"x":"mscore"},"multipass":{"n":"multipass","t":"lightweight VM manager for Ubuntu instances","s":[["TAGLINE","lightweight VM manager for Ubuntu instances"],["TLDR","**Launch a new Ubuntu instance**\n\n```multipass launch --name [myvm]```\n\n**Launch with specific Ubuntu version**\n\n```multipass launch [22.04] --name [myvm]```\n\n**Launch with custom resources**\n\n```multipass launch --name [myvm] --cpus [2] --memory [4G] --disk [20G]```\n\n**List all instances**\n\n```multipass list```\n\n**Open shell in instance**\n\n```multipass shell [instance_name]```\n\n**Execute command in instance**\n\n```multipass exec [instance_name] -- [command]```\n\n**Stop an instance**\n\n```multipass stop [instance_name]```\n\n**Delete and purge instance**\n\n```multipass delete [instance_name] && multipass purge```"],["SYNOPSIS","**multipass** _command_ [_options_] [_arguments_]"],["PARAMETERS","**launch** [_image_]\n> Create and start a new instance.\n\n**list**, **ls**\n> List all instances.\n\n**shell** _instance_\n> Open shell in instance.\n\n**exec** _instance_ **--** _command_\n> Run command in instance.\n\n**start** _instance_\n> Start stopped instance.\n\n**stop** _instance_\n> Stop running instance.\n\n**restart** _instance_\n> Restart instance.\n\n**delete** _instance_\n> Delete instance (recoverable until purge).\n\n**purge**\n> Permanently remove deleted instances.\n\n**recover** _instance_\n> Restore deleted instance.\n\n**info** _instance_\n> Show instance details.\n\n**mount** _source_ _instance:target_\n> Mount host directory in instance.\n\n**umount** _mount_\n> Unmount directory.\n\n**transfer** _source_ _destination_\n> Copy files between host and instance.\n\n**find**\n> List available images.\n\n**--name** _NAME_\n> Instance name.\n\n**--cpus** _NUM_\n> Number of CPUs.\n\n**--memory** _SIZE_\n> RAM allocation (e.g., 2G).\n\n**--disk** _SIZE_\n> Disk size (e.g., 10G).\n\n**--cloud-init** _FILE_\n> Cloud-init configuration file.\n\n**--network** _SPEC_\n> Add a network interface to the instance.\n\n**--bridged**\n> Add a network interface bridged to the default bridge."],["DESCRIPTION","**Multipass** is a lightweight VM manager for Ubuntu instances. Developed by Canonical, it provides a fast way to launch Ubuntu virtual machines on Linux, macOS, and Windows, optimized for cloud-style workflows.\n\nInstances boot quickly from cached cloud images. The hypervisor backend varies by platform: QEMU or LXD on Linux, QEMU (with Hypervisor.framework) on macOS, and Hyper-V on Windows. Instances get DHCP networking with host connectivity out of the box.\n\nCloud-init support enables automated provisioning. You can pass cloud-init YAML to install packages, configure users, run scripts, and more during first boot - matching cloud deployment workflows locally.\n\nMount functionality shares host directories with instances, useful for development. File transfers work both directions. Multiple instances can run simultaneously, limited by system resources.\n\nThe tool is particularly useful for testing Ubuntu server configurations, developing cloud applications locally, and quick disposable development environments."],["CAVEATS","Primarily Ubuntu images, though other images may be available via URL. Requires hardware virtualization support. Hypervisor conflicts possible (VirtualBox, Docker Desktop). Default instance resources are modest (1 CPU, 1 GB RAM, 5 GB disk). Network bridge configuration varies by backend. Some features require specific hypervisor backends."],["HISTORY","**Multipass** was developed by **Canonical** and released around **2017** to provide a streamlined Ubuntu VM experience across platforms. It grew from the need for quick, consistent Ubuntu environments for development and testing. The project emphasizes simplicity over full virtualization features, targeting developers who need Ubuntu instances without complex VM management."],["INSTALL","```aur: yay -S multipass```"],["SEE ALSO","[lxc](/man/lxc)(1), [vagrant](/man/vagrant)(1), [docker](/man/docker)(1), [qemu](/man/qemu)(1)"]],"e":["lxc","vagrant","docker","qemu"],"x":"multipass launch --name [myvm]"},"nasm":{"n":"nasm","t":"portable x86 and x86-64 assembler using Intel syntax","s":[["TAGLINE","portable x86 and x86-64 assembler using Intel syntax"],["TLDR","**Assemble to object file**\n\n```nasm -f [elf64] [source.asm] -o [output.o]```\n\n**Assemble for Linux 32-bit**\n\n```nasm -f elf32 [source.asm]```\n\n**Assemble for Windows 64-bit**\n\n```nasm -f win64 [source.asm]```\n\n**Generate listing file**\n\n```nasm -f [elf64] -l [listing.lst] [source.asm]```\n\n**Define preprocessor macro**\n\n```nasm -D [MACRO_NAME]=[value] [source.asm]```\n\n**Include search path**\n\n```nasm -I [/path/to/includes/] [source.asm]```\n\n**Preprocess only** (output to stdout)\n\n```nasm -E [source.asm]```\n\n**Show available output formats**\n\n```nasm -hf```"],["SYNOPSIS","**nasm** [_-f format_] [_-o outfile_] [_-l listfile_] [_-D name=value_] [_options_] _source.asm_"],["PARAMETERS","**-f** _format_\n> Output format (elf64, elf32, win64, win32, macho64, bin, etc.).\n\n**-o** _file_\n> Output file name.\n\n**-l** _file_\n> Generate listing file.\n\n**-M**\n> Generate Makefile dependencies.\n\n**-E**\n> Preprocess only, output to stdout.\n\n**-a**\n> Preprocess only, no output.\n\n**-D** _macro_[=_value_]\n> Define preprocessor macro.\n\n**-U** _macro_\n> Undefine preprocessor macro.\n\n**-I** _path_\n> Add include file search directory.\n\n**-P** _file_\n> Pre-include file before source.\n\n**-w**[+|-]_warning_\n> Enable/disable warning type.\n\n**-g**\n> Generate debug information.\n\n**-F** _format_\n> Debug information format.\n\n**-O** _level_\n> Optimization level (0, 1, x for multi-pass).\n\n**-@** _file_\n> Read additional command-line options from file.\n\n**-Z** _file_\n> Redirect error messages to file.\n\n**-s**\n> Output errors to stdout.\n\n**-v**\n> Display version.\n\n**-y**\n> List available debug info formats for a given output format.\n\n**-h**\n> Display help.\n\n**-hf**\n> List available output formats."],["DESCRIPTION","**NASM** (Netwide Assembler) is a portable x86 and x86-64 assembler using Intel syntax. It produces object files for various operating systems and formats, making it suitable for cross-platform assembly development.\n\nThe Intel syntax NASM uses is generally considered more readable than AT&T syntax: destination comes first (mov eax, 1), memory references use brackets ([var]), and size specifiers are explicit (dword, qword).\n\nOutput formats cover major platforms: ELF for Linux/Unix, PE/COFF for Windows, Mach-O for macOS, and flat binary for bootloaders and embedded systems. The -f option selects the target format.\n\nNASM includes a powerful macro preprocessor supporting multi-line macros, conditional assembly, string manipulation, and file inclusion. Context-local labels enable reusable code structures.\n\nFor debugging, NASM generates debugging information in formats like DWARF and CodeView, compatible with GDB, LLDB, and Visual Studio debuggers."],["CAVEATS","Intel syntax differs from GNU as (AT&T syntax). Object files must be linked (ld, gcc, link.exe) to create executables. Platform-specific calling conventions matter for function calls. Binary output requires manual memory layout. 16-bit and 32-bit modes need attention to processor mode."],["HISTORY","**NASM** was written by **Simon Tatham** and **Julian Hall** starting in **1996**, aiming to create a free, portable assembler with clean Intel syntax. It became the assembler of choice for many open-source projects needing low-level code. The project has seen contributions from many developers and remains actively maintained for modern processor extensions including AVX-512."],["INSTALL","```apt: sudo apt install nasm```\n\n```dnf: sudo dnf install nasm```\n\n```pacman: sudo pacman -S nasm```\n\n```apk: sudo apk add nasm```\n\n```zypper: sudo zypper install nasm```\n\n```brew: brew install nasm```\n\n```nix: nix profile install nixpkgs#nasm```"],["SEE ALSO","[as](/man/as)(1), [yasm](/man/yasm)(1), [ld](/man/ld)(1), [objdump](/man/objdump)(1), [gdb](/man/gdb)(1)"]],"e":["as","yasm","ld","objdump","gdb"],"x":"nasm -f [elf64] [source.asm] -o [output.o]"},"ngrep":{"n":"ngrep","t":"network grep","s":[["TAGLINE","network grep"],["TLDR","**Search packets for a pattern in quiet mode**\n\n```ngrep -q \"[pattern]\"```\n\n**Search on a specific interface and port**\n\n```ngrep -d [eth0] \"[pattern]\" port [80]```\n\n**Search HTTP traffic with line-oriented output**\n\n```ngrep -q -W byline \"[GET|POST]\" port [80]```\n\n**Search traffic from a specific host**\n\n```ngrep -q \"[pattern]\" host [192.168.1.1]```\n\n**Show timestamps with matched packets**\n\n```ngrep -q -t \"[pattern]\"```\n\n**Read from a pcap capture file**\n\n```ngrep -I [file.pcap] \"[pattern]\"```\n\n**Case-insensitive search and write matches to pcap file**\n\n```ngrep -qi -O [output.pcap] \"[pattern]\"```\n\n**Match only a specific number of packets**\n\n```ngrep -q -n [10] \"[pattern]\"```"],["SYNOPSIS","**ngrep** [_options_] _pattern_ [_filter_]"],["PARAMETERS","_PATTERN_\n> Extended regex pattern to match against packet payloads.\n\n_FILTER_\n> BPF filter expression (same syntax as tcpdump).\n\n**-q**\n> Quiet mode; only output packet headers and payloads.\n\n**-d** _IFACE_\n> Interface to listen on (default: auto-selected).\n\n**-W** _MODE_\n> Output mode: normal (default), byline (honor linefeeds), single (one line per packet), none.\n\n**-i**\n> Case-insensitive matching.\n\n**-I** _FILE_\n> Read packets from a pcap dump file.\n\n**-O** _FILE_\n> Write matched packets to a pcap dump file.\n\n**-t**\n> Print a timestamp alongside each matched packet.\n\n**-n** _NUM_\n> Match only NUM packets total, then exit.\n\n**-s** _SNAPLEN_\n> Set the BPF capture length (default: 65536).\n\n**-X**\n> Treat the match expression as a hexadecimal string.\n\n**-w**\n> Match the regex expression as a word."],["DESCRIPTION","**ngrep** is a pcap-aware tool that applies GNU grep-like pattern matching to network packet payloads. It recognizes TCP, UDP, and ICMP across Ethernet, PPP, SLIP, FDDI, and null interfaces, and understands BPF filter logic in the same fashion as tcpdump. Matched packets can be displayed or written to pcap files for further analysis."],["CAVEATS","Requires root or appropriate capabilities for live capture. Only matches against packet payload data (not encrypted traffic). BPF filter expressions follow tcpdump syntax."],["HISTORY","ngrep was created to provide **grep-like** functionality for network packet inspection."],["INSTALL","```apt: sudo apt install ngrep```\n\n```dnf: sudo dnf install ngrep```\n\n```pacman: sudo pacman -S ngrep```\n\n```apk: sudo apk add ngrep```\n\n```zypper: sudo zypper install ngrep```\n\n```brew: brew install ngrep```\n\n```nix: nix profile install nixpkgs#ngrep```"],["SEE ALSO","[tcpdump](/man/tcpdump)(1), [grep](/man/grep)(1), [wireshark](/man/wireshark)(1)"]],"e":["tcpdump","grep","wireshark"],"x":"ngrep -q \"[pattern]\""},"nix-run":{"n":"nix-run","t":"runs programs from Nix packages","s":[["TAGLINE","runs programs from Nix packages"],["TLDR","**Run package from nixpkgs**\n\n```nix run nixpkgs#[hello]```\n\n**Run from flake**\n\n```nix run [.#default]```\n\n**Run with arguments**\n\n```nix run nixpkgs#[cowsay] -- \"[Hello]\"```\n\n**Run from GitHub**\n\n```nix run [github:owner/repo]```\n\n**Run specific app**\n\n```nix run [.#apps.x86_64-linux.default]```"],["SYNOPSIS","**nix** **run** [_options_] _installable_ [-- _args_]"],["PARAMETERS","_INSTALLABLE_\n> Flake reference to run.\n\n_ARGS_\n> Arguments to pass to program.\n\n**--impure**\n> Allow impure evaluation.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nix run** runs programs from Nix packages. No installation required.\n\nThe tool fetches and runs packages. Temporary execution without pollution."],["CAVEATS","Requires flakes enabled. Downloads on first run. Network dependent."],["HISTORY","nix run is part of the **new Nix CLI** for running packages without installation."],["INSTALL","```nix: nix profile install nixpkgs#nix-run```"],["SEE ALSO","[nix](/man/nix)(1), [nix-shell](/man/nix-shell)(1), [nix-build](/man/nix-build)(1)"]],"e":["nix","nix-shell","nix-build"],"x":"nix run nixpkgs#[hello]"},"npm-unpublish":{"n":"npm-unpublish","t":"removes a package from registry","s":[["TAGLINE","removes a package from registry"],["TLDR","**Unpublish package version**\n\n```npm unpublish [package]@[version]```\n\n**Unpublish entire package**\n\n```npm unpublish [package] --force```\n\n**Dry run**\n\n```npm unpublish [package]@[version] --dry-run```"],["SYNOPSIS","**npm** **unpublish** [_package_] [_options_]"],["PARAMETERS","_PACKAGE_\n> Package name and optional version.\n\n**--force**\n> Required to unpublish an entire package (all versions) instead of a single version.\n\n**--dry-run**\n> Show what would be removed without actually unpublishing.\n\n**--workspace** _NAME_\n> Run the command in the context of the specified workspace.\n\n**--workspaces**\n> Run the command across all configured workspaces.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npm unpublish** removes a published package or version from the npm registry. Because removing a package can break consumers anywhere in the dependency graph, the registry restricts what may be removed and when.\n\nWithin **72 hours** of publication, you may freely unpublish a version or the entire package. After that, the registry only allows unpublishing if no other package depends on it and the package owner is the sole maintainer; otherwise contact npm support. After unpublishing an entire package, the same name cannot be republished for **24 hours**, and any unpublished _name@version_ pair can never be reused."],["CAVEATS","May break consumers that depend on the version being removed. Use **npm deprecate** instead when you only want to discourage use without deleting the artifact."],["HISTORY","npm unpublish provides **package removal** with restrictions to prevent ecosystem breakage."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-publish](/man/npm-publish)(1), [npm-deprecate](/man/npm-deprecate)(1)"]],"e":["npm","npm-publish","npm-deprecate"],"x":"npm unpublish [package]@[version]"},"openlaked":{"n":"openlaked","t":"OpenLake storage daemon for LLM inference and GPU workloads","s":[["TAGLINE","OpenLake storage daemon for LLM inference and GPU workloads"],["TLDR","Start the store with a **config** file\n\n```openlaked --config [path/to/config.toml]```\n\nStart after **building** from source\n\n```./target/release/openlaked --config crates/openlake_server/configs/storage-tcp-local.toml```\n\nInstall the **vLLM connector** helper package\n\n```pip install openlake-vllm```\n\n```openlaked```"],["SYNOPSIS","**openlaked** [_--config_ _path_] [_options_]"],["PARAMETERS","**--config** _path_\n> Path to OpenLake server configuration (TCP local storage, RDMA KV, etc.)"],["DESCRIPTION","**openlaked** is the OpenLake server binary: a high-performance storage engine for GPU-centric workloads, including KV-cache offload for LLM inference, checkpoint storage, and S3-compatible object storage. It is implemented in Rust and optimized for Linux (`io_uring`, optional RDMA/GPUDirect paths).\n\nTypical use: run `openlaked` on GPU hosts, then point vLLM (via `openlake-vllm` / OpenLakeConnector) at the node list so long-context prefixes can be reused from host RAM/disk. In object-store mode it exposes an S3-compatible endpoint for tools like the AWS CLI."],["CAVEATS","Production RDMA and GPUDirect features target Linux GPU clusters; macOS is for development only. Single-node defaults use local data directories you must create. S3 demo credentials in docs are for local testing only—change for any shared deployment."],["HISTORY","**OpenLake** is an Apache-2.0 project for AI infrastructure storage (KV pool + object store)."],["SEE ALSO","[vllm](/man/vllm)(1), [aws](/man/aws)(1)"],["RESOURCES","```[Source code](https://github.com/openlake-project/openlake)```\n\n```[Homepage](https://theopenlake.com)```\n\n```[Documentation](https://github.com/openlake-project/openlake/tree/main/docs)```"]],"e":["vllm","aws"],"x":"openlaked --config [path/to/config.toml]"},"openssl-ca":{"n":"openssl-ca","t":"minimal certificate authority application","s":[["TAGLINE","minimal certificate authority application"],["TLDR","**Sign certificate request**\n\n```openssl ca -in [request.csr] -out [certificate.crt]```\n\n**Sign with specific CA**\n\n```openssl ca -config [ca.cnf] -cert [ca.crt] -keyfile [ca.key] -in [request.csr] -out [cert.crt]```\n\n**Revoke certificate**\n\n```openssl ca -revoke [certificate.crt]```\n\n**Generate CRL**\n\n```openssl ca -gencrl -out [crl.pem]```\n\n**List issued certificates**\n\n```openssl ca -status [serial_number]```"],["SYNOPSIS","**openssl ca** [_options_] [**-in** _csr_] [**-out** _cert_] [**-infiles** _csr_...]"],["PARAMETERS","**-in** _file_\n> Input CSR (PEM-encoded). Use **-infiles** instead to sign multiple CSRs in one invocation.\n\n**-infiles** _file_...\n> Sign every CSR listed after this option (must be the last option on the line).\n\n**-out** _file_\n> Output certificate file (default: stdout).\n\n**-config** _file_\n> OpenSSL configuration file (defaults to **/etc/ssl/openssl.cnf**).\n\n**-cert** _file_\n> CA certificate used for signing.\n\n**-keyfile** _file_\n> CA private key (PEM, ENGINE URI, or PKCS#11 URI).\n\n**-days** _n_\n> Certificate validity in days from today.\n\n**-startdate** _YYMMDDHHMMSSZ_, **-not_before** _date_\n> Explicit certificate start date.\n\n**-enddate** _YYMMDDHHMMSSZ_, **-not_after** _date_\n> Explicit certificate expiry date.\n\n**-md** _alg_\n> Message digest algorithm (e.g. **sha256**, **sha384**).\n\n**-policy** _name_\n> CA policy section in the config file (controls which DN fields must match the CA).\n\n**-extensions** _section_\n> Config section containing certificate extensions to add.\n\n**-extfile** _file_\n> Read extensions from an extra file (combined with **-extensions**).\n\n**-subj** _dn_\n> Override the subject name from the CSR (e.g. `/CN=example/O=Acme`).\n\n**-batch**\n> Non-interactive mode — sign without prompting for confirmation.\n\n**-notext**\n> Do not include a human-readable text dump in the output.\n\n**-noemailDN**\n> Strip the **emailAddress** RDN from the certificate Subject.\n\n**-create_serial**\n> Create a fresh random serial if the serial file is missing.\n\n**-revoke** _file_\n> Mark the given certificate as revoked in the CA database.\n\n**-crl_reason** _reason_\n> Reason for revocation (**unspecified**, **keyCompromise**, **CACompromise**, **affiliationChanged**, **superseded**, **cessationOfOperation**, **certificateHold**, **removeFromCRL**).\n\n**-status** _serial_\n> Print the revocation status of the certificate with the given serial.\n\n**-gencrl**\n> Generate a CRL using the current database.\n\n**-crldays** _n_, **-crlhours** _n_\n> Validity period until the next CRL is expected."],["DESCRIPTION","**openssl ca** is a minimal certificate authority application. It signs certificate requests (CSRs), maintains a flat-file database (**index.txt**) of issued certificates, tracks the next serial number (**serial**), and generates X.509 Certificate Revocation Lists.\n\nBy default the command reads its configuration from the **[ca]** section of **openssl.cnf**, which selects a default-CA section (**default_ca**) describing where to find the CA cert/key, the database, the serial file, the directory of issued certs (**newcerts/**), the policy, and which extensions to apply. Most options can be set there instead of on the command line."],["CA SETUP","```"],["Initialize CA","mkdir -p demoCA/{certs,crl,newcerts,private}\ntouch demoCA/index.txt\necho '01' > demoCA/serial\n```"],["CAVEATS","Use proper CA software for production. Database format is proprietary. Configuration complex for beginners."],["HISTORY","The openssl ca command has been part of OpenSSL since early versions, providing basic CA functionality."],["INSTALL","```apt: sudo apt install openssl```\n\n```dnf: sudo dnf install openssl```\n\n```pacman: sudo pacman -S openssl```\n\n```apk: sudo apk add openssl```\n\n```zypper: sudo zypper install openssl```\n\n```brew: brew install openssl```\n\n```nix: nix profile install nixpkgs#openssl```"],["SEE ALSO","[openssl-req](/man/openssl-req)(1), [openssl-x509](/man/openssl-x509)(1), [openssl-crl](/man/openssl-crl)(1)"]],"e":["openssl-req","openssl-x509","openssl-crl"],"x":"openssl ca -in [request.csr] -out [certificate.crt]"},"prqlc":{"n":"prqlc","t":"Compile PRQL queries to SQL","s":[["TAGLINE","Compile PRQL queries to SQL"],["TLDR","**Compile PRQL to SQL**\n\n```prqlc compile [query.prql]```\n\n**Compile from stdin**\n\n```echo \"from employees | select name\" | prqlc compile```\n\n**Format PRQL**\n\n```prqlc fmt [query.prql]```\n\n**Output to file**\n\n```prqlc compile [query.prql] -o [output.sql]```"],["SYNOPSIS","**prqlc** _command_ [_options_] [_file_]"],["PARAMETERS","**compile**\n> Compile PRQL to SQL.\n\n**fmt**\n> Format PRQL code.\n\n**-o** _FILE_\n> Output file.\n\n**--target** _DB_\n> Target database."],["DESCRIPTION","**prqlc** is the compiler for PRQL (Pipelined Relational Query Language), a modern query language designed as a more readable and composable alternative to SQL. It transpiles PRQL source code into standard SQL that can be executed against any supported database.\n\nPRQL uses a pipeline syntax where data transformations are chained with the pipe operator, making complex queries easier to read and write than equivalent nested SQL. The compiler supports multiple SQL dialects through the **--target** option, generating database-specific SQL for PostgreSQL, MySQL, SQLite, BigQuery, and others.\n\nThe **fmt** subcommand formats PRQL source code for consistent style, and the compiler accepts input from files or stdin for use in build pipelines."],["CAVEATS","PRQL is experimental. Supports multiple SQL dialects."],["HISTORY","PRQL was created as a **modern alternative** to SQL syntax."],["INSTALL","```brew: brew install prqlc```\n\n```nix: nix profile install nixpkgs#prqlc```"],["SEE ALSO","[psql](/man/psql)(1), [sqlite3](/man/sqlite3)(1)"]],"e":["psql","sqlite3"],"x":"prqlc compile [query.prql]"},"pvchange":{"n":"pvchange","t":"Modify LVM physical volume attributes","s":[["TAGLINE","Modify LVM physical volume attributes"],["TLDR","**Allow** allocation on physical volume\n\n```sudo pvchange -x y /dev/sdXN```\n\n**Disallow** allocation on physical volume\n\n```sudo pvchange -x n /dev/sdXN```\n\n**Ignore** metadata areas\n\n```sudo pvchange --metadataignore y /dev/sdXN```\n\n**Stop ignoring** metadata areas\n\n```sudo pvchange --metadataignore n /dev/sdXN```\n\n**Add tag** to physical volume\n\n```sudo pvchange --addtag mytag /dev/sdXN```\n\nGenerate **new UUID**\n\n```sudo pvchange --uuid /dev/sdXN```\n\nChange **all** visible physical volumes\n\n```sudo pvchange -a -x y```"],["SYNOPSIS","**pvchange** [_options_] _PhysicalVolume_..."],["DESCRIPTION","**pvchange** changes the attributes of a physical volume in LVM. It can control allocation, metadata handling, tagging, and UUID generation for physical volumes."],["PARAMETERS","**-x, --allocatable {y|n}**\n> Allow or disallow allocation of physical extents on this volume\n\n**--metadataignore {y|n}**\n> Ignore or use metadata areas on this physical volume\n\n**--addtag TAG**\n> Add a tag to the physical volume\n\n**--deltag TAG**\n> Remove a tag from the physical volume\n\n**--uuid**\n> Generate a new UUID for the physical volume\n\n**-a, --all**\n> Change all visible physical volumes\n\n**-f, --force**\n> Force operation\n\n**-v, --verbose**\n> Verbose mode"],["CAVEATS","Use --uuid with caution as it can cause problems if the volume group is active or if backups refer to the old UUID. Disabling allocation on a PV prevents new extents from being placed on it."],["HISTORY","**pvchange** is part of **LVM2** (Logical Volume Manager), providing flexible disk management and storage virtualization for Linux systems."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[pvs](/man/pvs)(8), [pvdisplay](/man/pvdisplay)(8), [pvcreate](/man/pvcreate)(8), [vgchange](/man/vgchange)(8)"]],"e":["pvs","pvdisplay","pvcreate","vgchange"],"x":"sudo pvchange -x y /dev/sdXN"},"qm-migrate":{"n":"qm-migrate","t":"Migrate Proxmox VM to another node","s":[["TAGLINE","Migrate Proxmox VM to another node"],["TLDR","**Migrate** a virtual machine to another node\n\n```qm migrate 100 target_node```\n\nMigrate with **bandwidth limit** (10 KiB/s)\n\n```qm migrate 100 target_node --bwlimit 10```\n\n**Force** migration with local devices\n\n```qm migrate 100 target_node --force true```\n\nUse **online/live** migration for running VMs\n\n```qm migrate 100 target_node --online true```\n\nEnable live **storage migration** for local disks\n\n```qm migrate 100 target_node --with-local-disks true```"],["SYNOPSIS","**qm migrate** _vmid_ _target_ [_options_]"],["PARAMETERS","**--bwlimit** _kbps_\n> Bandwidth limit in KiB/s\n\n**--force** _true|false_\n> Allow migration with local devices (root only)\n\n**--online** _true|false_\n> Use online/live migration for running VMs\n\n**--with-local-disks** _true|false_\n> Enable live storage migration for local disks"],["DESCRIPTION","**qm migrate** migrates a virtual machine to another node in the Proxmox VE cluster. It supports both offline and online (live) migration for running VMs.\n\nLive migration allows moving VMs between nodes with minimal downtime. Storage migration enables moving VMs with local disks."],["CAVEATS","Online migration requires shared storage or --with-local-disks option. Network bandwidth and latency affect migration time. Force option requires root privileges."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools. VM migration is essential for cluster maintenance and load balancing."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [pvecm](/man/pvecm)(1)"]],"e":["qm","pvecm"],"x":"qm migrate 100 target_node"},"rancid-run":{"n":"rancid-run","t":"Collect and version-control network device configurations","s":[["TAGLINE","Collect and version-control network device configurations"],["TLDR","**Run RANCID for all groups**\n\n```rancid-run```\n\n**Run for specific group**\n\n```rancid-run [group_name]```\n\n**Run with email notification**\n\n```rancid-run -m [email@example.com]```"],["SYNOPSIS","**rancid-run** [_options_] [_group_...]"],["PARAMETERS","**-m** _email_\n> Send results to email.\n\n**-r** _device_\n> Process single device.\n\n**-f**\n> Force run."],["DESCRIPTION","**rancid-run** executes RANCID (Really Awesome New Cisco confIg Differ) to collect and version-control network device configurations. It connects to routers, switches, and firewalls to backup their configs."],["EXAMPLES","```bash"],["Run all groups","rancid-run"],["Specific group","rancid-run routers"],["Multiple groups","rancid-run routers switches firewalls"],["With email","rancid-run -m admin@example.com routers\n```"],["SETUP","```bash"],["1. Create group","/var/lib/rancid/bin/rancid-cvs groupname"],["/var/lib/rancid/groupname/router.db","router1.example.com:cisco:up"],["~/.cloginrc","add user router1.example.com admin\nadd password router1.example.com {password}"],["4. Run","rancid-run groupname\n```"],["CONFIGURATION","**/etc/rancid/rancid.conf**\n> Main configuration defining device groups, version control system, and working directory paths.\n\n**~/.cloginrc**\n> Device login credentials file specifying usernames, passwords, and enable passwords for each network device.\n\n**router.db**\n> Per-group device inventory listing hostnames, device types (cisco, juniper, etc.), and up/down status."],["CAVEATS","Requires CVS or Git for version control. Device credentials in .cloginrc. Usually run via cron."],["HISTORY","RANCID was developed by **Shrubbery Networks** for automating network device configuration backup and change tracking."],["INSTALL","```apt: sudo apt install rancid```\n\n```dnf: sudo dnf install rancid```\n\n```apk: sudo apk add rancid```\n\n```brew: brew install rancid```\n\n```nix: nix profile install nixpkgs#rancid```"],["SEE ALSO","[clogin](/man/clogin)(1), [rancid-cvs](/man/rancid-cvs)(1)"]],"e":["clogin","rancid-cvs"],"x":"rancid-run"},"readcd":{"n":"readcd","t":"Read data from CD/DVD optical discs","s":[["TAGLINE","Read data from CD/DVD optical discs"],["TLDR","**Read** a CD and copy it to an ISO file\n\n```readcd dev=/dev/sr0 f=[path/to/file.iso]```\n\n**List** available SCSI/optical devices\n\n```readcd -scanbus```\n\n**Read** a specific range of sectors\n\n```readcd dev=/dev/sr0 sectors=[0]-[10000] f=[out.bin]```\n\n**Read** at a reduced speed to improve reliability\n\n```readcd dev=/dev/sr0 speed=[4] f=[file.iso]```\n\n**Make a full clone** including sub-channel data and TOC\n\n```readcd dev=/dev/sr0 -clone f=[disc.img]```"],["SYNOPSIS","**readcd** [_options_] dev=_device_ f=_file_"],["PARAMETERS","**dev=**_device_\n> CD/DVD/BluRay device to read from (e.g. /dev/sr0 or SCSI notation like 1,0,0).\n\n**f=**_file_\n> Output file for read operations (input file when writing). Use '-' for stdout/stdin.\n\n**sectors=**_range_\n> Range of sectors to read, e.g. 150-10000 (the end sector is excluded).\n\n**speed=**_n_\n> Read speed as a multiple of single speed (150 KB/s for CD). Lower speeds can improve readability.\n\n**retries=**_n_\n> Number of high-level retries for unreadable sectors. Defaults to 128.\n\n**-clone**\n> Read the whole disc including all sub-channel data and the full table of contents (saved with a .toc file).\n\n**-fulltoc**\n> Retrieve and print the full table of contents in hex.\n\n**-c2scan**\n> Scan the disc for C2 (uncorrectable) errors.\n\n**-w**\n> Switch to write mode (DVD-RAM only).\n\n**-scanbus**\n> List all available SCSI devices.\n\n**-v**\n> Increase verbosity, reporting read progress."],["DESCRIPTION","**readcd** reads Compact Disc, DVD, and BluRay media data, typically to create ISO images or raw dumps of physical discs. It can also write data back, but only to DVD-RAM media (with **-w**).\n\nThe tool accesses the raw disc data and supports retries, sector ranges, and full cloning (sub-channel data plus TOC), making it suitable for creating exact copies of media."],["CAVEATS","Requires read access to the optical drive device. Some copy-protected discs may not read correctly."],["HISTORY","Part of **cdrtools** (or cdrkit on some distributions), providing CD/DVD burning and reading utilities."],["INSTALL","```apk: sudo apk add cdrkit```\n\n```zypper: sudo zypper install readcd```\n\n```nix: nix profile install nixpkgs#cdrkit```"],["SEE ALSO","[cdrecord](/man/cdrecord)(1), [dd](/man/dd)(1), [wodim](/man/wodim)(1)"]],"e":["cdrecord","dd","wodim"],"x":"readcd dev=/dev/sr0 f=[path/to/file.iso]"},"reptyr":{"n":"reptyr","t":"Reattach running process to new terminal","s":[["TAGLINE","Reattach running process to new terminal"],["TLDR","**Attach** process to current terminal\n\n```reptyr [pid]```\n\nAttach by **process name**\n\n```reptyr $(pidof [htop])```"],["SYNOPSIS","**reptyr** [**-s**] [**-T**] _pid_"],["PARAMETERS","**-s**\n> Attach via script method (more compatible)\n\n**-T**\n> Attach process and its children\n\n**-l**\n> List PIDs that can be attached\n\n**-v**\n> Verbose output\n\n**pid**\n> Process ID to attach"],["DESCRIPTION","**reptyr** moves a running process to a new terminal. This is invaluable when you start a long-running command in a regular terminal and later realize it should be in screen or tmux.\n\nThe tool uses ptrace to reparent the process's controlling terminal, allowing it to receive input from and send output to the new terminal."],["CAVEATS","Requires ptrace capability (may need root or sysctl adjustment). Some processes may not work due to terminal state. Background the process first with Ctrl+Z and bg. Process must be owned by current user."],["HISTORY","**reptyr** was created by **Nelson Elhage** to solve the common problem of wanting to move a running process to a different terminal or screen session without restarting it."],["INSTALL","```apt: sudo apt install reptyr```\n\n```dnf: sudo dnf install reptyr```\n\n```pacman: sudo pacman -S reptyr```\n\n```apk: sudo apk add reptyr```\n\n```zypper: sudo zypper install reptyr```\n\n```nix: nix profile install nixpkgs#reptyr```"],["SEE ALSO","[screen](/man/screen)(1), [tmux](/man/tmux)(1), [nohup](/man/nohup)(1), [disown](/man/disown)(1)"]],"e":["screen","tmux","nohup","disown"],"x":"reptyr [pid]"},"rexec":{"n":"rexec","t":"Execute commands on remote hosts","s":[["TAGLINE","Execute commands on remote hosts"],["TLDR","Execute a **command** on a remote host\n\n```rexec -h remote_host ls -l```\n\nSpecify the **remote username**\n\n```rexec -u username -h remote_host ps aux```\n\nSuppress **error stream** creation\n\n```rexec -n -h remote_host ls -l```\n\nSpecify the **remote port**\n\n```rexec -P 1234 -h remote_host ls -l```"],["SYNOPSIS","**rexec** [_options_] _command_"],["PARAMETERS","**-h**, **--host** _host_\n> Specify remote host\n\n**-u**, **--username** _user_\n> Specify remote username\n\n**-P**, **--port** _port_\n> Specify remote port\n\n**-n**, **--noerr**\n> Do not create a separate error stream\n\n**-d**, **--password** _password_\n> Specify password (use '-' to read from stdin)"],["DESCRIPTION","**rexec** executes commands on a remote host using the rexec protocol (TCP port 512). It authenticates with a username and password, then runs the specified command on the remote system.\n\nNote: rexec transmits credentials and data in plain text and is insecure. Use SSH for encrypted remote command execution."],["CAVEATS","Transmits credentials and data unencrypted. Not recommended for use over untrusted networks. Consider SSH as a secure alternative."],["HISTORY","Part of **GNU inetutils**, providing classic Unix networking utilities. Largely superseded by SSH for security reasons."],["SEE ALSO","[ssh](/man/ssh)(1), [rsh](/man/rsh)(1), [rlogin](/man/rlogin)(1)"]],"e":["ssh","rsh","rlogin"],"x":"rexec -h remote_host ls -l"},"rpcinfo":{"n":"rpcinfo","t":"Query RPC service registrations","s":[["TAGLINE","Query RPC service registrations"],["TLDR","**Show full table** of all RPC services on localhost\n\n```rpcinfo```\n\n**Probe portmapper** and list all registered programs\n\n```rpcinfo -p [hostname]```\n\n**Show concise table** of RPC services\n\n```rpcinfo -s [hostname]```\n\n**Display rpcbind statistics**\n\n```rpcinfo -m```\n\n**List entries for a specific service and version** on a remote host\n\n```rpcinfo -l [hostname] [mountd] [2]```\n\n**Check if a service is available** via TCP\n\n```rpcinfo -t [hostname] [nfs]```\n\n**Delete registration** for a service version (requires root)\n\n```rpcinfo -d [mountd] [1]```"],["SYNOPSIS","**rpcinfo** [_options_] [_host_]"],["PARAMETERS","**-p** _host_\n> Probe the portmapper on host and list all registered RPC programs.\n\n**-s** _host_\n> Show concise table of all registered services.\n\n**-m**\n> Display rpcbind operation statistics.\n\n**-l** _host_ _program_ _version_\n> List all entries matching the given program and version.\n\n**-t** _host_ _program_ [_version_]\n> Probe the service via TCP using an RPC call to procedure 0.\n\n**-u** _host_ _program_ [_version_]\n> Probe the service via UDP using an RPC call to procedure 0.\n\n**-b** _program_ _version_\n> Make an RPC broadcast and report all responding hosts.\n\n**-d** _program_ _version_ [_transport_]\n> Delete registration for the specified program and version. If transport is specified, unregister only on that transport, otherwise on all transports (root only).\n\n**-a** _serv_address_ **-T** _transport_ _program_ [_version_]\n> Ping procedure 0 of the specified program at the given universal address on the specified transport.\n\n**-T** _transport_\n> Specify the transport to use (e.g., tcp, udp).\n\n**-n** _portnum_\n> Use portnum instead of the portmapper-assigned port for -t and -u probes."],["DESCRIPTION","**rpcinfo** queries RPC servers and reports registered services. It is primarily used to diagnose RPC connectivity issues and view available network services like NFS, mountd, and other RPC-based services.\n\nThe tool provides information about program numbers, versions, protocols, and ports for registered RPC services. It can probe specific services to check availability, broadcast to discover services on the network, and manage service registrations."],["CAVEATS","Requires rpcbind to be running on the target host. The **-d** option requires root privileges. The **-b** broadcast option requires network broadcast support."],["HISTORY","Part of standard Unix network utilities for RPC service management. Essential for NFS troubleshooting and configuration."],["INSTALL","```apt: sudo apt install rpcbind```\n\n```dnf: sudo dnf install rpcbind```\n\n```pacman: sudo pacman -S rpcbind```\n\n```apk: sudo apk add rpcbind```\n\n```zypper: sudo zypper install rpcbind```\n\n```nix: nix profile install nixpkgs#rpcbind```"],["SEE ALSO","[rpcbind](/man/rpcbind)(8), [nfsstat](/man/nfsstat)(8), [showmount](/man/showmount)(8)"]],"e":["rpcbind","nfsstat","showmount"],"x":"rpcinfo"},"sane-find-scanner":{"n":"sane-find-scanner","t":"Detect SCSI and USB scanner devices","s":[["TAGLINE","Detect SCSI and USB scanner devices"],["TLDR","**Find all SCSI and USB scanners**\n\n```sane-find-scanner```\n\n**Verbose output** with device details\n\n```sane-find-scanner -v```\n\n**Very verbose** with SCSI inquiry and USB descriptors\n\n```sane-find-scanner -vv```\n\n**Quiet mode** showing only device names\n\n```sane-find-scanner -q```\n\n**Probe for parallel port scanners**\n\n```sane-find-scanner -p```\n\n**Force specific devices** to be checked\n\n```sane-find-scanner -f [/dev/sg0] [/dev/usb/scanner0]```"],["SYNOPSIS","**sane-find-scanner** [_-hvqpf_] [_-F filename_] [_devname_...]"],["PARAMETERS","**-h**, **-?**\n> Print usage message\n\n**-v**\n> Verbose output; use twice for SCSI inquiry and USB descriptors\n\n**-q**\n> Quiet mode; print only device names without comments\n\n**-p**\n> Probe for parallel port scanners\n\n**-f**\n> Force treating specified devices as SCSI or USB scanners\n\n**-F** _filename_\n> Read USB descriptors from file (Linux /proc/bus/usb/devices format)"],["DESCRIPTION","**sane-find-scanner** detects SCSI and USB scanners and reports their device files. It helps verify that scanners are properly connected and accessible to SANE backends.\n\nFor SCSI scanners, it sends inquiry commands to generic device files like /dev/sg0 looking for scanner or processor device types. For USB scanners, it checks kernel scanner device files and uses libusb to detect devices, identifying vendor and product IDs along with the USB chip type.\n\nThe tool can find scanners even if they are not supported by any SANE backend, making it useful for initial hardware detection and troubleshooting."],["CAVEATS","Cannot detect most parallel port scanners except some Mustek models with **-p**. Proprietary port scanners are not detected. USB support requires Linux, FreeBSD, NetBSD, or OpenBSD with libusb."],["HISTORY","sane-find-scanner is part of the **SANE** (Scanner Access Now Easy) project, which provides standardized access to scanner hardware on Unix-like systems. The project began in **1996** to create a universal scanner API."],["INSTALL","```apt: sudo apt install sane-utils```\n\n```apk: sudo apk add sane-utils```"],["SEE ALSO","[scanimage](/man/scanimage)(1), [saned](/man/saned)(8)"]],"e":["scanimage","saned"],"x":"sane-find-scanner"},"silentcast":{"n":"silentcast","t":"Create silent screencasts and GIFs","s":[["TAGLINE","Create silent screencasts and GIFs"],["TLDR","**Launch** silentcast\n\n```silentcast```\n\nLaunch on a **specific display**\n\n```silentcast --display=[display]```"],["SYNOPSIS","**silentcast** [**--display**=_display_]"],["PARAMETERS","**--display** _display_\n> Specify the X display to record from"],["DESCRIPTION","**silentcast** creates silent video screencasts in MKV format and animated GIFs. It provides a graphical interface for recording screen activity without audio, making it ideal for creating tutorials, documentation, or demonstrations.\n\nThe workflow involves launching the application, positioning the green recording rectangle over the desired window or area, pressing Return to start recording, and clicking the system tray icon to stop. Recordings can be processed into animated GIFs or edited as needed."],["CAVEATS","Cannot run multiple instances simultaneously. Does not support recording its own interface. Requires GTK3, FFmpeg, and ImageMagick as dependencies. Some desktop environments (KDE Plasma 5, i3wm) may require special keyboard shortcut configuration for proper operation."],["HISTORY","**silentcast** was created by **Colin Keenan** as an open-source screen recording tool. Version 3.0 introduced a completely redesigned GUI interface. The project is released under the GPL-3.0 license and maintained on GitHub."],["INSTALL","```aur: yay -S silentcast```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [byzanz-record](/man/byzanz-record)(1), [recordmydesktop](/man/recordmydesktop)(1), [peek](/man/peek)(1)"]],"e":["ffmpeg","byzanz-record","recordmydesktop","peek"],"x":"silentcast"},"simtool":{"n":"simtool","t":"TUI for managing iOS simulators on macOS","s":[["TAGLINE","TUI for managing iOS simulators on macOS"],["TLDR","**Launch the simulator browser**\n\n```simtool```\n\n**Start with an all-apps view**\n\n```simtool --apps```\n\n**Display available color themes**\n\n```simtool --list-themes```"],["SYNOPSIS","**simtool** [**--apps**] [**--generate-config**] [**--list-themes**]"],["DESCRIPTION","**simtool** is a terminal user interface for browsing and managing iOS Simulators on macOS. It lets you navigate simulators, browse installed apps, explore file systems, and preview content with syntax highlighting. It features Vim-style navigation, 60+ color themes, SQLite inspection, and lazy loading."],["CAVEATS","macOS only. Requires Xcode with simulator runtimes installed."],["HISTORY","**simtool** was created by **Aziz Uysal** (azizuysal) and is written in **Go**."],["SEE ALSO","[xcrun](/man/xcrun)(1)"]],"e":["xcrun"],"x":"simtool"},"snitch":{"n":"snitch","t":"Friendlier alternative to ss and netstat","s":[["TAGLINE","Friendlier alternative to ss and netstat"],["TLDR","**Launch the interactive TUI with live-updating connections**\n\n```snitch```\n\n**List TCP established connections as a styled table**\n\n```snitch ls -t -e```\n\n**Output all connections in JSON format**\n\n```snitch json```"],["SYNOPSIS","**snitch** [_command_] [_flags_]"],["DESCRIPTION","**snitch** is a TUI and CLI tool for inspecting TCP and UDP network connections on Linux and macOS. It serves as a modern, human-friendly alternative to **ss** and **netstat**, displaying a live-updating connection table with DNS resolution, filtering, and multiple output formats (styled tables, JSON, CSV)."],["HISTORY","**snitch** was created by **Karol Broda** and is written in **Go**."],["INSTALL","```brew: brew install snitch```\n\n```nix: nix profile install nixpkgs#snitch```"],["SEE ALSO","[ss](/man/ss)(1), [netstat](/man/netstat)(1), [nethogs](/man/nethogs)(1)"]],"e":["ss","netstat","nethogs"],"x":"snitch"},"sprofile":{"n":"sprofile","t":"View Spotify listening activity in the terminal","s":[["TAGLINE","View Spotify listening activity in the terminal"],["TLDR","**Launch the Spotify activity viewer**\n\n```sprofile```"],["SYNOPSIS","**sprofile**"],["DESCRIPTION","**sprofile** is a TUI application for viewing your Spotify listening activity. Built with the Ratatui framework, it provides an interactive terminal interface to review your Spotify history and profile data."],["HISTORY","**sprofile** was created by **Sushant Ray** (GoodBoyNeon) and is written in **Rust**."],["INSTALL","```aur: yay -S sprofile```"],["SEE ALSO","[ncspot](/man/ncspot)(1)"]],"e":["ncspot"],"x":"sprofile"},"steamos-factory-reset":{"n":"steamos-factory-reset","t":"Reset SteamOS to factory settings","s":[["TAGLINE","Reset SteamOS to factory settings"],["TLDR","Begin **factory reset**\n\n```steamos-factory-reset```\n\nDisplay **help**\n\n```steamos-factory-reset --help```"],["SYNOPSIS","**steamos-factory-reset** [_OPTIONS_]"],["DESCRIPTION","**steamos-factory-reset** resets SteamOS to factory settings, erasing user data and configurations while preserving the operating system. The command requires user confirmation before proceeding.\n\nThis is typically used to prepare a device for resale, troubleshoot persistent issues, or restore the system to a clean state."],["CAVEATS","This operation is destructive and cannot be undone. All user data, installed games, and configurations will be erased. Backup important data before running. Requires confirmation prompt."],["HISTORY","**steamos-factory-reset** is part of the **SteamOS** utilities developed by **Valve**. Factory reset functionality is essential for the Steam Deck as a consumer device."],["SEE ALSO","[steamos-factory-reset-config](/man/steamos-factory-reset-config)(1), [steamos-update](/man/steamos-update)(1)"]],"e":["steamos-factory-reset-config","steamos-update"],"x":"steamos-factory-reset"},"systemctl-get-default":{"n":"systemctl-get-default","t":"Show default boot target","s":[["TAGLINE","Show default boot target"],["TLDR","Show **default target**\n\n```systemctl get-default```"],["SYNOPSIS","**systemctl get-default**"],["DESCRIPTION","**systemctl get-default** displays the default target (equivalent to runlevel) that the system boots into. Common targets include `graphical.target` (GUI environment), `multi-user.target` (text-mode multi-user), and `rescue.target` (single-user recovery).\n\nThe default target is determined by the symlink at `/etc/systemd/system/default.target`."],["CAVEATS","Only shows the configured default, not the currently active target. Use `systemctl list-units --type=target` to see currently active targets."],["HISTORY","The **get-default** subcommand provides a simple way to check the system's boot target, replacing the traditional examination of `/etc/inittab` runlevel configuration."],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-set-default](/man/systemctl-set-default)(1), [systemctl-default](/man/systemctl-default)(1)"]],"e":["systemctl","systemctl-set-default","systemctl-default"],"x":"systemctl get-default"},"systemctl-list-paths":{"n":"systemctl-list-paths","t":"List path-activated units","s":[["TAGLINE","List path-activated units"],["TLDR","List **path** units\n\n```systemctl list-paths```\n\nFilter by **pattern**\n\n```systemctl list-paths [pattern]```\n\nList **all** including inactive\n\n```systemctl list-paths -a```\n\nFilter by **state**\n\n```systemctl list-paths --state [state]```\n\nShow **unit types**\n\n```systemctl list-paths --show-types```"],["SYNOPSIS","**systemctl list-paths** [_OPTIONS_] [_PATTERN_...]"],["PARAMETERS","**-a, --all**\n> Include inactive path units\n\n**--state=** _STATE_\n> Filter by state\n\n**--show-types**\n> Display unit types in output\n\n**--no-legend**\n> Suppress header and footer\n\n**--no-pager**\n> Disable pager"],["DESCRIPTION","**systemctl list-paths** displays path units currently loaded in memory, ordered by the paths they watch. Path units activate other units when specified filesystem paths are modified.\n\nThe output shows the watched path, the path unit name, and its current state."],["CAVEATS","Only shows path units, not related service units. Inactive path units are hidden by default. Path monitoring uses inotify, which has kernel limits on watched paths."],["HISTORY","The **list-paths** subcommand provides a focused view of filesystem-triggered units, which are systemd's replacement for incron or similar file-watching mechanisms."],["SEE ALSO","[systemctl-list-units](/man/systemctl-list-units)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-list-units","systemctl"],"x":"systemctl list-paths"},"systemctl-status":{"n":"systemctl-status","t":"Show runtime status of units","s":[["TAGLINE","Show runtime status of units"],["TLDR","Show the **status** of a systemd unit\n\n```systemctl status [unit].service```\n\nShow **system overview** status\n\n```systemctl status```\n\nShow status of **multiple units**\n\n```systemctl status [unit1] [unit2]```\n\nShow status by **PID**\n\n```systemctl status [pid]```\n\nShow status with more **journal lines**\n\n```systemctl status -n [50] [unit]```\n\nShow the status of a **user unit**\n\n```systemctl status --user [unit]```"],["SYNOPSIS","**systemctl** [_OPTIONS_...] **status** [_PATTERN_...|_PID_...]"],["PARAMETERS","**--user**\n> Query user service manager instead of system\n\n**-n**, **--lines=**_NUM_\n> Number of journal lines to show\n\n**-o**, **--output=**_MODE_\n> Journal output mode (short, verbose, json, etc.)\n\n**--no-pager**\n> Do not pipe output to pager\n\n**-l**, **--full**\n> Do not ellipsize unit names, process tree entries, or journal output"],["DESCRIPTION","**systemctl status** displays the current state of systemd units including services, sockets, targets, and timers. It shows whether units are active, enabled, or have failed, along with recent log entries from the journal.\n\nWithout arguments, it shows an overview of system state. With a unit name, it provides detailed status including the main PID, memory usage, control group, and recent log lines."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-start](/man/systemctl-start)(1), [systemctl-restart](/man/systemctl-restart)(1), [journalctl](/man/journalctl)(1)"]],"e":["systemctl","systemctl-start","systemctl-restart","journalctl"],"x":"systemctl status [unit].service"},"updatectl":{"n":"updatectl","t":"Systemd-based system update controller","s":[["TAGLINE","Systemd-based system update controller"],["TLDR","**Check** for updates\n\n```updatectl check```\n\n**Update** to latest version\n\n```updatectl update```\n\n**List** update targets\n\n```updatectl list```\n\nShow **target** data\n\n```updatectl list [target]```\n\nList target **offline**\n\n```updatectl --offline list [target]```\n\nUpdate and **reboot**\n\n```updatectl --reboot update [target]```\n\n**Clean up** old versions\n\n```updatectl vacuum [target]```"],["SYNOPSIS","**updatectl** [_OPTIONS_] _COMMAND_ [_TARGET_]"],["COMMANDS","**check**\n> Check for available updates\n\n**update**\n> Update to latest version\n\n**list**\n> Show update targets and versions\n\n**vacuum**\n> Clean up old versions"],["PARAMETERS","**--offline**\n> Use local data without network fetch\n\n**--reboot**\n> Reboot after applying update\n\n**-h, --help**\n> Display help information"],["DESCRIPTION","**updatectl** is a systemd-based system update utility. It manages system updates including checking for new versions, applying updates, and cleaning up old versions.\n\nThe tool works with update targets which can represent different system components that can be updated independently."],["CAVEATS","Systemd-based systems only. Some operations require root privileges. Reboot may be required for certain updates to take effect."],["SEE ALSO","[systemctl](/man/systemctl)(1)"]],"e":["systemctl"],"x":"updatectl check"},"vagrant-destroy":{"n":"vagrant-destroy","t":"Delete Vagrant virtual machines","s":[["TAGLINE","Delete Vagrant virtual machines"],["TLDR","**Destroy VM**\n\n```vagrant destroy```\n\n**Force destroy without confirmation**\n\n```vagrant destroy -f```\n\n**Destroy specific VM**\n\n```vagrant destroy [name]```\n\n**Gracefully shut down** before destroying\n\n```vagrant destroy --graceful```"],["SYNOPSIS","**vagrant** **destroy** [_options_] [_name_]"],["PARAMETERS","**-f**, **--force**\n> Destroy without confirmation prompt.\n\n**-g**, **--graceful**\n> Gracefully shut down the machine before destroying it.\n\n**--[no-]parallel**\n> Enable or disable parallel destruction of multiple machines. Enabled by default if the provider supports it."],["DESCRIPTION","**vagrant destroy** stops and deletes all resources for a Vagrant machine. Removes the virtual machine and associated storage. The Vagrantfile and provisioning remain intact."],["CAVEATS","This permanently destroys VM data. Shared folders and host files are not affected. The destroy command does not remove the box that was downloaded during `vagrant up`. Use `vagrant box remove` to reclaim that disk space."],["INSTALL","```apt: sudo apt install vagrant```\n\n```dnf: sudo dnf install vagrant```\n\n```nix: nix profile install nixpkgs#vagrant```"],["SEE ALSO","[vagrant](/man/vagrant)(1), [vagrant-halt](/man/vagrant-halt)(1), [vagrant-box](/man/vagrant-box)(1), [vagrant-status](/man/vagrant-status)(1)"]],"e":["vagrant","vagrant-halt","vagrant-box","vagrant-status"],"x":"vagrant destroy"},"windres":{"n":"windres","t":"GNU Windows resource compiler","s":[["TAGLINE","GNU Windows resource compiler"],["TLDR","**Compile resource script** to COFF object file\n\n```windres [input.rc] -o [output.o]```\n\n**Compile to binary resource format**\n\n```windres -O res [input.rc] -o [output.res]```\n\n**Convert res file** to COFF object\n\n```windres -i [input.res] -o [output.o]```\n\n**Specify input format** explicitly\n\n```windres -I rc -i [input.rc] -O coff -o [output.o]```\n\n**Include directory** for headers\n\n```windres --include-dir=[path/to/includes] [input.rc] -o [output.o]```\n\n**Define preprocessor symbol**\n\n```windres -D [SYMBOL=value] [input.rc] -o [output.o]```\n\n**Cross-compile for 64-bit Windows**\n\n```x86_64-w64-mingw32-windres [input.rc] -o [output.o]```"],["SYNOPSIS","**windres** [**-i** _infile_] [**-o** _outfile_] [**-I** _format_] [**-O** _format_] [**-F** _target_] [**--include-dir** _dir_] [**-D** _sym_] [_options_]"],["PARAMETERS","**-i** _file_, **--input** _file_\n> Input file name. Without this, uses first non-option argument.\n\n**-o** _file_, **--output** _file_\n> Output file name.\n\n**-I** _format_, **--input-format** _format_\n> Input format: rc (resource script), res (binary), coff (object).\n\n**-O** _format_, **--output-format** _format_\n> Output format: rc, res, or coff.\n\n**-F** _target_, **--target** _target_\n> Specify BFD target format for COFF output.\n\n**--include-dir** _dir_\n> Add directory to search path for included files.\n\n**-D** _sym_[=_val_], **--define** _sym_[=_val_]\n> Define preprocessor symbol.\n\n**-U** _sym_, **--undefine** _sym_\n> Undefine preprocessor symbol.\n\n**--preprocessor** _prog_\n> Preprocessor program to use. Default is `gcc -E -xc-header -DRC_INVOKED`.\n\n**--preprocessor-arg** _arg_\n> Additional argument for preprocessor.\n\n**--no-use-temp-file**\n> Use pipes for communication with the preprocessor (default).\n\n**--use-temp-file**\n> Use a temporary file for preprocessor communication instead of pipes.\n\n**@** _file_\n> Read command-line options from file.\n\n**-v**, **--verbose**\n> Enable verbose mode.\n\n**--help**\n> Display help message.\n\n**--version**\n> Display version information."],["DESCRIPTION","**windres** is the GNU resource compiler for Windows applications, part of GNU Binutils. It compiles resource script (.rc) files containing UI elements like icons, cursors, menus, dialogs, and version information into binary formats usable by Windows executables.\n\nThe tool supports three formats: \"rc\" (text resource scripts), \"res\" (binary resource files), and \"coff\" (object files for linking). Typical workflow involves compiling .rc files to COFF objects, then linking them with the application.\n\nFor cross-compilation from Linux to Windows, windres is typically used as part of the MinGW-w64 toolchain with architecture-specific prefixes (e.g., x86_64-w64-mingw32-windres for 64-bit Windows targets).\n\nResource scripts are preprocessed by gcc before compilation, allowing use of #include and #define directives."],["CAVEATS","Resource script syntax follows Microsoft conventions but may have minor compatibility differences. Some complex resources may require adjustments when moving from Microsoft's rc.exe. The tool is primarily useful for Windows cross-compilation; native Windows development typically uses Microsoft tools."],["HISTORY","**windres** was developed as part of **GNU Binutils** to provide a free alternative to Microsoft's Resource Compiler (rc.exe). It has been part of the MinGW and MinGW-w64 toolchains since the **late 1990s**, enabling Windows application development on Unix-like systems. The tool continues to be maintained as part of the Binutils project."],["SEE ALSO","[gcc](/man/gcc)(1), [ld](/man/ld)(1), [objcopy](/man/objcopy)(1), [objdump](/man/objdump)(1)"]],"e":["gcc","ld","objcopy","objdump"],"x":"windres [input.rc] -o [output.o]"},"xh":{"n":"xh","t":"Fast and friendly HTTP client","s":[["TAGLINE","Fast and friendly HTTP client"],["TLDR","**Make GET request**\n\n```xh [https://api.example.com/users]```\n\n**Make POST with JSON data**\n\n```xh POST [https://api.example.com/users] name=John email=john@example.com```\n\n**Make POST with raw JSON**\n\n```xh POST [https://api.example.com/users] < [data.json]```\n\n**Set custom headers**\n\n```xh [https://api.example.com] Authorization:\"Bearer [token]\"```\n\n**Download file**\n\n```xh --download [https://example.com/file.zip]```\n\n**Show only response body**\n\n```xh -b [https://api.example.com/users]```\n\n**Show only headers**\n\n```xh -h [https://api.example.com/users]```\n\n**Follow redirects**\n\n```xh --follow [https://example.com/redirect]```"],["SYNOPSIS","**xh** [_method_] _URL_ [_items_] [_--json_] [_--download_] [_options_]"],["PARAMETERS","**(method)**\n> HTTP method: GET, POST, PUT, PATCH, DELETE, HEAD.\n\n**-b**, **--body**\n> Show only response body.\n\n**-h**, **--headers**\n> Show only response headers.\n\n**-d**, **--download**\n> Download file.\n\n**-c**, **--continue**\n> Resume download.\n\n**-o** _FILE_, **--output** _FILE_\n> Save response to file.\n\n**-f**, **--form**\n> Send form data.\n\n**-j**, **--json**\n> Send JSON data (default for POST).\n\n**-F**, **--follow**\n> Follow redirects.\n\n**--offline**\n> Build request without sending.\n\n**-p** _WHAT_, **--print** _WHAT_\n> What to print: H(request headers), B(request body), h(response headers), b(response body).\n\n**-v**, **--verbose**\n> Show request and response.\n\n**--timeout** _SECS_\n> Request timeout.\n\n**-A** _TYPE_, **--auth-type** _TYPE_\n> Auth type: basic, bearer, digest.\n\n**-a** _USER:PASS_, **--auth** _USER:PASS_\n> Authentication credentials.\n\n**--proxy** _PROTO:URL_\n> Proxy URL.\n\n**-I**, **--ignore-stdin**\n> Ignore stdin data."],["DESCRIPTION","**xh** is a friendly HTTP client, designed as a faster Rust reimplementation of HTTPie. It formats output with syntax highlighting and provides intuitive syntax for requests.\n\nRequest items use special syntax: key=value for JSON/form data, key:=value for raw JSON values, key:value for headers. This enables building requests without complex quoting.\n\nOutput is formatted and colored by default: JSON is pretty-printed, headers are highlighted. The -b flag outputs raw body for piping to other tools.\n\nAuthentication supports basic, bearer token, and digest. Session support persists cookies and auth across requests.\n\nDownload mode saves response bodies to files, with resume support for interrupted transfers. Progress is shown during download.\n\nThe offline mode builds and displays requests without sending, useful for debugging or generating curl commands."],["CAVEATS","Some HTTPie features not yet implemented. Output formatting adds overhead for large responses. Session handling differs from HTTPie. Some terminal color schemes may need adjustment."],["HISTORY","**xh** was created by **ducaale** around **2020** as a Rust reimplementation of HTTPie. The name \"xh\" is HTTPie's h reversed. Written for performance and portability, it provides near-identical syntax while being significantly faster and shipping as a single binary."],["INSTALL","```apt: sudo apt install xh```\n\n```pacman: sudo pacman -S xh```\n\n```apk: sudo apk add xh```\n\n```zypper: sudo zypper install xh```\n\n```brew: brew install xh```\n\n```nix: nix profile install nixpkgs#xh```"],["SEE ALSO","[curl](/man/curl)(1), [httpie](/man/httpie)(1), [wget](/man/wget)(1)"]],"e":["curl","httpie","wget"],"x":"xh [https://api.example.com/users]"},"ykman":{"n":"ykman","t":"YubiKey device management CLI","s":[["TAGLINE","YubiKey device management CLI"],["TLDR","**List YubiKeys**\n\n```ykman list```\n\n**Show device info**\n\n```ykman info```\n\n**List OATH accounts**\n\n```ykman oath accounts list```\n\n**Generate TOTP code**\n\n```ykman oath accounts code [account_name]```\n\n**Add OATH account**\n\n```ykman oath accounts add -t [name] [secret]```\n\n**Reset FIDO2**\n\n```ykman fido reset```\n\n**Set FIDO2 PIN**\n\n```ykman fido access change-pin```\n\n**List PIV certificates**\n\n```ykman piv certificates list```"],["SYNOPSIS","**ykman** [_options_] _command_ [_subcommand_] [_args_]"],["PARAMETERS","**list**\n> List devices.\n\n**info**\n> Device info.\n\n**oath**\n> OATH commands.\n\n**fido**\n> FIDO commands.\n\n**piv**\n> PIV commands.\n\n**openpgp**\n> OpenPGP commands.\n\n**config**\n> Configuration.\n\n**-d** _SERIAL_\n> Device serial."],["DESCRIPTION","**ykman** (YubiKey Manager) is the official CLI from Yubico for managing all aspects of YubiKey hardware security tokens. It provides a unified interface to configure and interact with the various applications available on the device.\n\nThe **oath** subcommand manages TOTP and HOTP accounts, storing two-factor authentication secrets on the hardware and generating one-time codes. The **fido** subcommand handles FIDO2/WebAuthn credentials for passwordless authentication, including PIN management and resident key storage. The **piv** subcommand manages X.509 certificates for smart card operations, and **openpgp** configures PGP keys for signing, encryption, and SSH authentication.\n\nThe **config** subcommand controls device-level settings such as enabling or disabling USB and NFC interfaces for specific applications. The **info** and **list** commands display device details including serial number, firmware version, and available applications. The **-d** flag selects a specific device when multiple YubiKeys are connected."],["CAVEATS","Requires YubiKey. Some operations are destructive. Backup before reset."],["HISTORY","**ykman** (YubiKey Manager) is the official CLI from **Yubico**. It replaces older tools with unified management."],["INSTALL","```dnf: sudo dnf install yubikey-manager```\n\n```pacman: sudo pacman -S yubikey-manager```\n\n```apk: sudo apk add yubikey-manager```\n\n```zypper: sudo zypper install yubikey-manager```\n\n```brew: brew install ykman```\n\n```nix: nix profile install nixpkgs#yubikey-manager```"],["SEE ALSO","[gpg](/man/gpg)(1), [ssh-keygen](/man/ssh-keygen)(1)"]],"e":["gpg","ssh-keygen"],"x":"ykman list"},"ykpersonalize":{"n":"ykpersonalize","t":"Configure YubiKey authentication slots","s":[["TAGLINE","Configure YubiKey authentication slots"],["TLDR","**Show YubiKey status**\n\n```ykpersonalize -v```\n\n**Program slot 1 with Yubico OTP**\n\n```ykpersonalize -1```\n\n**Program slot 2 with challenge-response**\n\n```ykpersonalize -2 -ochal-resp -ochal-hmac```\n\n**Set static password in slot 2**\n\n```ykpersonalize -2 -ostatic-ticket```\n\n**Set CCID mode**\n\n```sudo ykpersonalize -m 86```\n\n**Program with specific AES key**\n\n```ykpersonalize -1 -a[hex_key]```\n\n**Set access code to protect configuration**\n\n```ykpersonalize -oaccess=[hex_code]```"],["SYNOPSIS","**ykpersonalize** [_options_]"],["PARAMETERS","**-1** / **-2**\n> Program slot 1 or slot 2.\n\n**-a** _key_\n> Set AES key (hex encoded).\n\n**-c** _code_\n> Current access code to unlock.\n\n**-oaccess=** _code_\n> Set new access code.\n\n**-ochal-resp**\n> Enable challenge-response mode.\n\n**-ochal-hmac**\n> Use HMAC-SHA1 for challenge-response.\n\n**-ostatic-ticket**\n> Enable static password mode.\n\n**-m** _mode_\n> Set USB mode (OTP, CCID, etc.).\n\n**-v**\n> Verbose output.\n\n**-y**\n> Auto-confirm operations."],["DESCRIPTION","**ykpersonalize** configures YubiKey hardware tokens from Yubico. It can program the two configuration slots with various authentication modes including Yubico OTP, HMAC-SHA1 challenge-response, and static passwords.\n\nEach YubiKey has two slots: slot 1 activates on short touch, slot 2 on long touch. Slots can be independently configured for different use cases like OTP authentication and disk encryption.\n\nAccess codes can protect configuration from unauthorized changes. The tool requires direct USB access to the YubiKey."],["CAVEATS","Programming overwrites existing slot configuration. Access codes prevent reconfiguration without the code. Some operations require root. Superseded by ykman for newer features."],["HISTORY","**ykpersonalize** was developed by Yubico as part of the yubikey-personalization package for configuring YubiKey tokens. While still functional, Yubico now recommends ykman (YubiKey Manager) for newer YubiKey models and features."],["INSTALL","```apt: sudo apt install yubikey-personalization```\n\n```dnf: sudo dnf install ykpers```\n\n```pacman: sudo pacman -S yubikey-personalization```\n\n```apk: sudo apk add ykpers```\n\n```zypper: sudo zypper install ykpers```\n\n```brew: brew install yubikey-personalization```\n\n```nix: nix profile install nixpkgs#yubikey-personalization```"],["SEE ALSO","[ykman](/man/ykman)(1), [ykinfo](/man/ykinfo)(1)"]],"e":["ykman","ykinfo"],"x":"ykpersonalize -v"},"ytfzf":{"n":"ytfzf","t":"Search and play YouTube videos with fzf","s":[["TAGLINE","Search and play YouTube videos with fzf"],["TLDR","**Search** with thumbnails\n\n```ytfzf -t [search_pattern]```\n\nPlay **audio** in loop\n\n```ytfzf -m -a -l [search_pattern]```\n\n**Download** from history\n\n```ytfzf -d --choose-from-history```\n\nPlay **all** audio results\n\n```ytfzf -m -A [search_pattern]```\n\nShow **trending** videos\n\n```ytfzf --trending --ext-menu [search_pattern]```\n\nSearch **PeerTube**\n\n```ytfzf --peertube [search_pattern]```"],["SYNOPSIS","**ytfzf** [_OPTIONS_] [_SEARCH_]"],["PARAMETERS","**-t, --show-thumbnails**\n> Show thumbnail previews\n\n**-m, --audio-only**\n> Play only audio\n\n**-a, --auto-select**\n> Auto-select first result\n\n**-l, --loop**\n> Loop playback\n\n**-d, --download**\n> Download instead of play\n\n**-A, --select-all**\n> Select all results\n\n**--trending**\n> Show trending videos\n\n**--peertube**\n> Search PeerTube instead of YouTube\n\n**--ext-menu**\n> Use external menu program"],["DESCRIPTION","**ytfzf** is a POSIX shell script for finding and playing videos from YouTube and other platforms. It uses fzf for selection and mpv for playback.\n\nThe tool supports searching, streaming, and downloading videos with thumbnail previews."],["CAVEATS","Requires fzf, mpv, and yt-dlp or youtube-dl. Thumbnail preview requires ueberzug or similar."],["INSTALL","```pacman: sudo pacman -S ytfzf```\n\n```apk: sudo apk add ytfzf```\n\n```nix: nix profile install nixpkgs#ytfzf```"],["SEE ALSO","[yt-dlp](/man/yt-dlp)(1), [youtube-dl](/man/youtube-dl)(1), [mpv](/man/mpv)(1)"]],"e":["yt-dlp","youtube-dl","mpv"],"x":"ytfzf -t [search_pattern]"}});
