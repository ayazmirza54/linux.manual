window.LCL_BUCKET(109,{"ab":{"n":"ab","t":"HTTP server benchmarking tool","s":[["TAGLINE","HTTP server benchmarking tool"],["TLDR","**Benchmark** a URL with 100 requests using 10 concurrent connections\n\n```ab -n 100 -c 10 [http://example.com/]```\n\n**POST** data to a URL\n\n```ab -n 100 -c 10 -p [data.json] -T application/json [http://example.com/api]```\n\nUse **keep-alive** connections\n\n```ab -n 100 -c 10 -k [http://example.com/]```\n\nSet a **custom header**\n\n```ab -n 100 -c 10 -H \"Authorization: Bearer [token]\" [http://example.com/]```"],["SYNOPSIS","**ab** [_-n requests_] [_-c concurrency_] [_-t timelimit_] [_options_] _URL_"],["DESCRIPTION","**ab** (Apache Benchmark) is a command-line tool for benchmarking HTTP servers. It generates load by sending multiple requests to a specified URL and measures server response times, throughput, and other performance metrics.\n\nThe tool reports statistics including requests per second, time per request, transfer rate, and connection times broken down by percentiles. It supports both HTTP and HTTPS, custom headers, POST data, cookies, and HTTP authentication."],["PARAMETERS","**-n** _requests_\n> Number of requests to perform for the benchmarking session\n\n**-c** _concurrency_\n> Number of multiple requests to perform at a time (simultaneous connections)\n\n**-t** _timelimit_\n> Maximum number of seconds to spend benchmarking (implies -n 50000)\n\n**-k**\n> Enable HTTP KeepAlive feature (persistent connections)\n\n**-p** _file_\n> File containing data to POST\n\n**-T** _content-type_\n> Content-type header for POST/PUT data\n\n**-H** _header_\n> Append extra header to the request (repeatable)\n\n**-A** _auth_\n> HTTP Basic Authentication credentials (user:password)\n\n**-C** _cookie_\n> Add a Cookie header (name=value)\n\n**-s** _timeout_\n> Maximum seconds to wait before socket times out (default 30)\n\n**-v** _verbosity_\n> Verbosity level (2=warnings, 3=response codes, 4+=headers)\n\n**-u** _file_\n> File containing data to PUT (also set -T)\n\n**-X** _proxy[:port]_\n> Use a proxy server for the requests\n\n**-e** _csv-file_\n> Write a CSV file with response time percentiles (1%-100%)\n\n**-g** _gnuplot-file_\n> Write measured values as a gnuplot/TSV file\n\n**-i**\n> Use HEAD requests instead of GET\n\n**-l**\n> Accept variable document length (for dynamic pages)\n\n**-r**\n> Don't exit on socket receive errors\n\n**-q**\n> Suppress progress messages on stderr (for >150 requests)"],["CAVEATS","**ab** creates synthetic load that may not reflect real-world traffic patterns. It does not parse HTML or execute JavaScript, so it only tests raw HTTP performance. Results can be misleading if the server implements rate limiting or caching. For more realistic load testing, consider tools like **wrk**, **siege**, or **k6**."],["HISTORY","**ab** was created as part of the Apache HTTP Server project in the late **1990s**. It was designed as a simple benchmarking tool for testing Apache configurations during development. The tool has remained largely unchanged and is bundled with Apache HTTPD installations worldwide."],["INSTALL","```apt: sudo apt install apache2-utils```\n\n```apk: sudo apk add apache2-utils```\n\n```zypper: sudo zypper install apache2-utils```"],["SEE ALSO","[curl](/man/curl)(1), [wrk](/man/wrk)(1), [siege](/man/siege)(1)"]],"e":["curl","wrk","siege"],"x":"ab -n 100 -c 10 [http://example.com/]"},"algebruh":{"n":"algebruh","t":"Classify arithmetic equality claims with independent proof checkers","s":[["TAGLINE","Classify arithmetic equality claims with independent proof checkers"],["TLDR","**Check** whether an equality holds\n\n```algebruh \"[2 = 3]\"```\n\n**Prove** a claim from assumptions\n\n```algebruh --assume \"[a = c]\" --assume \"[c = b]\" \"[a = b]\"```\n\n**Compare** multiple interpretations (integers, reals, bit-vectors, modular)\n\n```algebruh --interpret [int,real,bv8,mod:1] \"[2 = 3]\"```\n\n**Declare** a function injective and check equality of arguments\n\n```algebruh --injective [f] --assume \"[f(a) = f(b)]\" \"[a = b]\"```\n\n**Run all checkers** and print a JSON report\n\n```algebruh --all --json \"[a = b]\"```\n\n**Emit** SMT-LIB and Z3 proof artifacts\n\n```algebruh --emit [result] \"[x + 0 = x]\"```\n\n**Check** which optional external solvers are available\n\n```algebruh doctor```"],["SYNOPSIS","**algebruh** [**--all**] [**--json**] [**--interpret** _LIST_] [**--assume** _EXPR_]... [**--injective** _NAME_]... [**--ai-command** _CMD_] [**--emit** _PREFIX_] _CLAIM_\n\n**algebruh** **doctor** [**--json**]"],["PARAMETERS","**--all**\n> Run additional checkers (cvc5, Carcara, Lean/Mathlib, Vampire, E when available), extra solver seeds, and sufficient-premise search. Prints every attempt.\n\n**--json**\n> Print the complete report as JSON instead of human-readable text.\n\n**--interpret** _LIST_\n> Comma-separated interpretation list. Default is integers (**int**). Values include **int**, **real**, **bv**_N_ / **sbv**_N_ (signed N-bit, N=1..256), **ubv**_N_ (unsigned), **mod:**_N_, **quot:**_N_, **equiv:**_N_, **f32**, **f64**, and **singleton**. Integer is always evaluated as a baseline.\n\n**--assume** _EXPR_\n> Add an equality or disequality premise. May be repeated. Each assumption must contain exactly one **=**, **==**, or **!=**.\n\n**--injective** _NAME_\n> Declare a unary function name as injective. May be repeated. Other function applications remain uninterpreted.\n\n**--ai-command** _CMD_\n> Send the problem as JSON to _CMD_. The command's stdout must be a Lean tactic, which is accepted only after Lean kernel verification.\n\n**--emit** _PREFIX_\n> Write SMT-LIB (**_PREFIX_.smt2**) and Z3 proof (**_PREFIX_.proof**) when a selected Z3 proof is available. Does not overwrite existing files.\n\n_CLAIM_\n> Arithmetic equality or disequality to classify (for example **\"2 = 3\"** or **\"a + 0 = a\"**).\n\n**doctor**\n> Report availability of optional external tools on **PATH**. Exit code 1 if any listed tool is missing. Accepts **--json**."],["DESCRIPTION","**algebruh** classifies arithmetic equality and disequality claims. Given a claim and optional assumptions, it reports whether the claim is proved, refuted, contingent, vacuous, or unknown, and can surface disagreements between independent checkers.\n\nThe default path links against the Z3 library and independently checks candidates with exact evaluation, equality saturation, bounded model search, and LRAT replay when applicable. With **--all**, it also tries external tools found on **PATH** (cvc5, Carcara, Lean with Mathlib, Vampire, E). External tools run under Bubblewrap and **prlimit** where available.\n\nExpressions support integers, variable names, unary applications such as **f(x)**, parentheses, unary minus, **+**, **-**, **\\***, **/**, **%**, **=**, **==**, and **!=**. Multiplication, division, and modulo bind tighter than addition and subtraction. Each claim or assumption must contain one equality or disequality; comparisons such as **<** and **>=** are not supported.\n\nOutcome labels include **PROVED**, **REFUTED** (exit 0), **CONTINGENT**, **CONDITIONAL**, **VACUOUS**, **UNKNOWN**, **UNSAFE_AXIOM**, **CHECKER_BUG_CANDIDATE** (exit 1), and **REINTERPRETED** when a non-integer interpretation proves a claim that integers do not (exit 0). Input, sandbox, and solver errors use exit code 2.\n\nBuild requires Rust 1.85+, Cargo, pkg-config, and Z3 development libraries (or the project's **nix-shell**). Install by building **target/release/algebruh** and placing it on **PATH**."],["CAVEATS","Nonlinear arithmetic may return **UNKNOWN**. Division and modulo by zero follow Z3 semantics. User-defined equality is limited to modular **equiv:**_N_. Optional checkers must be installed separately; **algebruh doctor** reports what is available. **--emit** never overwrites existing artifact files."],["HISTORY","Algebruh is a Rust CLI by Sebastian Korotkiewicz for cross-checking arithmetic claims with Z3 and optional independent solvers (cvc5, Lean, and others). Distributed as source from GitHub."],["SEE ALSO","[z3](/man/z3)(1), [cargo](/man/cargo)(1)"],["RESOURCES","```[Source code](https://github.com/skorotkiewicz/algebruh)```"]],"e":["z3","cargo"],"x":"algebruh \"[2 = 3]\""},"amtool":{"n":"amtool","t":"Manage Prometheus Alertmanager from the command line","s":[["TAGLINE","Manage Prometheus Alertmanager from the command line"],["TLDR","**List all current alerts**\n\n```amtool alert```\n\n**Query alerts matching a label**\n\n```amtool alert query [alertname=\"HighMemory\"]```\n\n**Create a silence** for an alert\n\n```amtool silence add [alertname=\"HighMemory\"] --comment=\"[Maintenance]\" --duration=[2h]```\n\n**List all active silences**\n\n```amtool silence query```\n\n**Expire a silence** by ID\n\n```amtool silence expire [silence_id]```\n\n**Check Alertmanager configuration**\n\n```amtool check-config [alertmanager.yml]```\n\n**Show cluster status**\n\n```amtool cluster show```"],["SYNOPSIS","**amtool** _command_ [_options_] [_arguments_]"],["PARAMETERS","**alert query**\n> View and search through current alerts.\n\n**alert add**\n> Add a new alert to Alertmanager.\n\n**silence add** _matchers_\n> Create a new silence matching the specified labels.\n\n**silence query**\n> List active silences.\n\n**silence update** _id_\n> Update an existing silence (e.g., extend its duration).\n\n**silence expire** _id_\n> Remove a silence by its ID.\n\n**silence import**\n> Import silences from a JSON file or stdin.\n\n**check-config** _file_\n> Validate Alertmanager configuration files.\n\n**cluster show**\n> Display cluster membership status and peers.\n\n**config show**\n> Display the current Alertmanager configuration.\n\n**config routes show**\n> Display routing tree from configuration.\n\n**config routes test** _labels_\n> Test which route matches given labels.\n\n**template render**\n> Render a template definition for testing.\n\n**--alertmanager.url** _url_\n> Alertmanager URL (default: **http://localhost:9093/**).\n\n**--comment** _text_\n> Comment for a silence (required unless configured otherwise).\n\n**--author** _name_\n> Author of a silence (CreatedBy field).\n\n**--duration** _duration_\n> Duration for a silence (default: 1h, e.g., 30m, 2h).\n\n**--end** _time_\n> Set silence expiration as an RFC3339 timestamp.\n\n**--timeout** _duration_\n> Timeout for API requests (default: 30s).\n\n**-o**, **--output** _format_\n> Output format: simple, extended, json (default: simple)."],["DESCRIPTION","**amtool** is the official command-line client for interacting with Prometheus Alertmanager. It provides capabilities for querying alerts, managing silences, testing routing configurations, and validating configuration files.\n\nThe tool connects to an Alertmanager instance via its HTTP API to perform operations. It is particularly useful for creating silences during maintenance windows, debugging alert routing, and integrating Alertmanager management into automation scripts.\n\nConfiguration can be validated offline using **check-config** without requiring a running Alertmanager instance. The **config routes test** command helps verify that alerts will be routed to expected receivers based on their labels."],["CONFIGURATION","**~/.config/amtool/config.yml**\n> Persistent configuration for the Alertmanager URL, author, and output format defaults."],["CAVEATS","Requires network access to the Alertmanager API endpoint. Silences require a comment; creating them without **--comment** will fail. The **--alertmanager.url** must be specified if Alertmanager is not running on localhost:9093. Configuration file validation may not catch all runtime issues."],["HISTORY","**amtool** was developed alongside Alertmanager as part of the Prometheus ecosystem. It was created to provide operators with a scriptable interface for alert management, complementing the web UI for automation and CI/CD integration. The tool is maintained by the Prometheus project and distributed as part of the Alertmanager release."],["INSTALL","```apt: sudo apt install prometheus-alertmanager```\n\n```dnf: sudo dnf install alertmanager```\n\n```pacman: sudo pacman -S alertmanager```\n\n```apk: sudo apk add alertmanager```\n\n```nix: nix profile install nixpkgs#prometheus-alertmanager```"],["SEE ALSO","[alertmanager](/man/alertmanager)(1), [prometheus](/man/prometheus)(1), [promtool](/man/promtool)(1)"],["RESOURCES","```[Source code](https://github.com/prometheus/alertmanager)```\n\n```[Documentation](https://prometheus.io/docs/alerting/latest/alertmanager/)```"]],"e":["alertmanager","prometheus","promtool"],"x":"amtool alert"},"arjun":{"n":"arjun","t":"Discover hidden HTTP parameters in web applications","s":[["TAGLINE","Discover hidden HTTP parameters in web applications"],["TLDR","**Find GET parameters** for a URL\n\n```arjun -u [https://example.com/page]```\n\n**Find POST parameters**\n\n```arjun -u [https://example.com/api] -m POST```\n\n**Scan with a custom wordlist**\n\n```arjun -u [https://example.com] -w [path/to/wordlist.txt]```\n\n**Scan multiple URLs** from a file\n\n```arjun -i [urls.txt]```\n\n**Use JSON body** for parameter discovery\n\n```arjun -u [https://example.com/api] -m JSON```\n\n**Set custom headers**\n\n```arjun -u [https://example.com] --headers \"[Cookie: session=abc]\"```\n\n**Output results** to a JSON file\n\n```arjun -u [https://example.com] -o [results.json]```\n\n**Set number of threads**\n\n```arjun -u [https://example.com] -t [20]```"],["SYNOPSIS","**arjun** [_-u url_] [_-i file_] [_-m method_] [_-w wordlist_] [_-o output_] [_-t threads_] [_options_]"],["PARAMETERS","**-u**, **--url** _url_\n> Target URL to scan for parameters.\n\n**-i**, **--input** _file_\n> File containing URLs to scan (one per line).\n\n**-m**, **--method** _method_\n> HTTP method to use (GET, POST, JSON, XML). Default: GET.\n\n**-w**, **--wordlist** _file_\n> Custom wordlist for parameter names.\n\n**-o** _file_\n> Save results to a JSON output file.\n\n**-oT** _file_\n> Save results to a text output file.\n\n**-oB** [_proxy_]\n> Send results to Burp Suite proxy.\n\n**-t**, **--threads** _n_\n> Number of concurrent threads (default: 5).\n\n**--json**\n> Send POST data as JSON instead of form data.\n\n**--headers** _headers_\n> Custom headers in format \"Header: value\".\n\n**--passive**\n> Use passive sources to find parameters.\n\n**--stable**\n> Use more reliable but slower detection.\n\n**-d**, **--delay** _seconds_\n> Delay between requests.\n\n**--include** _params_\n> Parameters to always include in requests.\n\n**-T**, **--timeout** _seconds_\n> HTTP request timeout in seconds (default: 15).\n\n**-c** _n_, **--chunks** _n_\n> Number of parameters to send per request.\n\n**-q**, **--quiet**\n> Suppress output except results."],["DESCRIPTION","**Arjun** is a parameter discovery tool designed for web application security testing. It identifies hidden or undocumented HTTP parameters by sending crafted requests and analyzing responses for differences that indicate valid parameters.\n\nThe tool uses a combination of techniques to detect parameters: it monitors response length, status codes, and content changes when different parameter names are submitted. This allows it to find parameters that trigger distinct server behavior, even when the application doesn't explicitly advertise them.\n\nArjun includes a built-in wordlist of common parameter names but supports custom wordlists for targeted testing. It can discover parameters in GET requests (query strings), POST form data, and JSON bodies. The passive mode queries external sources like web archives and search engines for known parameters.\n\nThe tool is commonly used in penetration testing and bug bounty hunting to find attack surface that might be missed during initial reconnaissance. Parameters discovered may reveal hidden functionality, debug modes, or additional attack vectors."],["CAVEATS","Should only be used on applications you have permission to test. High thread counts or no delay may trigger rate limiting or WAF blocks. Detection is heuristic-based and may produce false positives/negatives. Large wordlists significantly increase scan time."],["HISTORY","**Arjun** was created by Somdev Sangwan (s0md3v) and released as an open-source security tool. It gained popularity in the bug bounty and penetration testing community for its effectiveness at finding hidden parameters. The tool is written in Python and actively maintained with updates for new detection techniques."],["INSTALL","```aur: yay -S arjun```\n\n```brew: brew install arjun```\n\n```nix: nix profile install nixpkgs#arjun```"],["SEE ALSO","[ffuf](/man/ffuf)(1), [wfuzz](/man/wfuzz)(1), [burpsuite](/man/burpsuite)(1), [httpx](/man/httpx)(1), [paramspider](/man/paramspider)(1)"]],"e":["ffuf","wfuzz","burpsuite","httpx","paramspider"],"x":"arjun -u [https://example.com/page]"},"aws-dynamodb":{"n":"aws-dynamodb","t":"Manage tables and items in Amazon DynamoDB.","s":[["TAGLINE","Manage tables and items in Amazon DynamoDB."],["TLDR","**Create a table** with a partition key\n\n```aws dynamodb create-table --table-name [my-table] --attribute-definitions AttributeName=id,AttributeType=S --key-schema AttributeName=id,KeyType=HASH --billing-mode PAY_PER_REQUEST```\n\n**Put an item** into a table\n\n```aws dynamodb put-item --table-name [my-table] --item '{\"id\": {\"S\": \"[item-id]\"}, \"name\": {\"S\": \"[value]\"}}'```\n\n**Get an item** by primary key\n\n```aws dynamodb get-item --table-name [my-table] --key '{\"id\": {\"S\": \"[item-id]\"}}'```\n\n**Query items** by partition key\n\n```aws dynamodb query --table-name [my-table] --key-condition-expression \"id = :id\" --expression-attribute-values '{\":id\": {\"S\": \"[item-id]\"}}'```\n\n**Scan all items** in a table\n\n```aws dynamodb scan --table-name [my-table]```\n\n**Update an item** attribute\n\n```aws dynamodb update-item --table-name [my-table] --key '{\"id\": {\"S\": \"[item-id]\"}}' --update-expression \"SET #n = :val\" --expression-attribute-names '{\"#n\": \"name\"}' --expression-attribute-values '{\":val\": {\"S\": \"[new-value]\"}}'```\n\n**Delete an item** by key\n\n```aws dynamodb delete-item --table-name [my-table] --key '{\"id\": {\"S\": \"[item-id]\"}}'```"],["SYNOPSIS","**aws dynamodb** _command_ [_options_]"],["DESCRIPTION","**aws dynamodb** is the AWS CLI interface for Amazon DynamoDB, a fully managed NoSQL key-value and document database. DynamoDB provides single-digit millisecond performance at any scale with built-in security, backup, and in-memory caching.\n\nTables store items identified by primary keys (partition key, or partition + sort key). DynamoDB supports on-demand and provisioned capacity modes, global tables for multi-region replication, and transactions for ACID operations."],["COMMANDS","**create-table**\n> Create a new DynamoDB table\n\n**delete-table**\n> Delete a table\n\n**describe-table**\n> Get table metadata and status\n\n**put-item**\n> Insert or replace an item\n\n**get-item**\n> Retrieve an item by primary key\n\n**update-item**\n> Modify item attributes\n\n**delete-item**\n> Remove an item by primary key\n\n**query**\n> Retrieve items by partition key with optional filtering\n\n**scan**\n> Read all items in a table\n\n**batch-write-item**\n> Write or delete multiple items across tables (up to 25 per request)\n\n**batch-get-item**\n> Retrieve multiple items by key across tables (up to 100 per request)\n\n**transact-write-items**\n> Execute multiple writes atomically\n\n**list-tables**\n> List table names in the current region"],["CAVEATS","Item size is limited to 400KB. Scan operations read the entire table and can be expensive on large tables. Query is preferred over scan for performance. Provisioned capacity can lead to throttling if exceeded. Eventually consistent reads are the default; strongly consistent reads cost more."],["HISTORY","Amazon DynamoDB launched in **January 2012** as a successor to SimpleDB. Global Tables for multi-region replication came in **2017**, on-demand capacity mode in **2018**, and PartiQL query language support in **2020**. It remains one of AWS's most widely used database services."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-dynamodbstreams](/man/aws-dynamodbstreams)(1), [aws-rds](/man/aws-rds)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/dynamodb/index.html)```"]],"e":["aws","aws-dynamodbstreams","aws-rds"],"x":"aws dynamodb create-table --table-name [my-table] --attribute-definitions AttributeName=id,AttributeType=S --key-schema AttributeName=id,KeyType=HASH --billing-mode PAY_PER_REQUEST"},"b4-send":{"n":"b4-send","t":"Send a prepared patch series to its recipients","s":[["TAGLINE","Send a prepared patch series to its recipients"],["TLDR","**Send the current** prepared series\n\n```b4 send```\n\n**Preview without sending** what would go out\n\n```b4 send -d```\n\n**Send a test copy** only to yourself\n\n```b4 send --reflect```\n\n**Write the messages** to a directory instead of sending\n\n```b4 send -o [path/to/dir]```\n\n**Edit the cover letter** before sending\n\n```b4 send --edit-cover```\n\n**Resend** a previously sent revision\n\n```b4 send --resend```"],["SYNOPSIS","**b4** **send** [_options_]"],["DESCRIPTION","**b4 send** delivers a series prepared with **b4 prep**. It turns the tracked branch into a numbered patch set, attaches the stored cover letter, fills in the To and Cc lists, and dispatches the messages over your configured SMTP server (or a project web endpoint where supported).\n\nBy default b4 adds a cryptographic attestation so reviewers can verify the patches were not altered in transit. Use the dry-run and reflect options to rehearse a submission before it reaches a public list."],["PARAMETERS","**-d**, **--dry-run**\n> Print what would be sent without actually sending anything.\n\n**-o** _DIR_, **--output-dir** _DIR_\n> Write the messages as files into _DIR_ instead of sending them.\n\n**--reflect**\n> Send the series only to yourself for testing.\n\n**--resend** _[VER]_\n> Resend a previously sent revision unchanged.\n\n**--no-sign**\n> Do not add cryptographic attestation to the messages.\n\n**-e**, **--edit-cover**\n> Edit the cover letter before sending.\n\n**--preview-to** _ADDR_\n> Send a preview copy to the given address(es)."],["CAVEATS","Sending requires a working SMTP or **git send-email** configuration. Always rehearse with **-d** or **--reflect** before posting to a public mailing list, since patches cannot be unsent."],["INSTALL","```dnf: sudo dnf install b4```\n\n```pacman: sudo pacman -S b4```\n\n```apk: sudo apk add b4```\n\n```zypper: sudo zypper install b4```\n\n```brew: brew install b4```\n\n```nix: nix profile install nixpkgs#b4```"],["SEE ALSO","[b4](/man/b4)(1), [b4-prep](/man/b4-prep)(1), [git-send-email](/man/git-send-email)(1)"],["RESOURCES","```[Documentation](https://b4.docs.kernel.org/)```"]],"e":["b4","b4-prep","git-send-email"],"x":"b4 send"},"bookokrat":{"n":"bookokrat","t":"Terminal EPUB, PDF and DJVU book reader","s":[["TAGLINE","Terminal EPUB, PDF and DJVU book reader"],["TLDR","**Open** an EPUB file\n\n```bookokrat [path/to/book.epub]```\n\n**Open** a PDF file\n\n```bookokrat [path/to/book.pdf]```\n\n**Open** a DJVU file\n\n```bookokrat [path/to/book.djvu]```\n\n**Open** a book in **zen mode** (distraction-free reading)\n\n```bookokrat --zen-mode [path/to/book.epub]```"],["SYNOPSIS","**bookokrat** [_--zen-mode_] [_file_]"],["PARAMETERS","**--zen-mode**\n> Launch in zen mode with sidebar and status bar hidden for distraction-free reading."],["DESCRIPTION","**bookokrat** is a terminal-based ebook reader that supports **EPUB**, **PDF**, and **DJVU** formats. It renders book content directly in the terminal with full HTML rendering, inline images, and MathML support, providing a distraction-free reading experience without a graphical environment.\n\nThe reader features Vim-style keybindings, split-view layout with library/table of contents and reader panels, search, bookmarks, jump list history, inline annotations, and reading statistics. Press **?** inside the app to open the built-in help."],["CAVEATS","Complex layouts and rich formatting in PDFs may not render well in a text-only environment. Best suited for text-heavy books. EPUB support is generally better than PDF due to the reflowable nature of the format."],["INSTALL","```brew: brew install bookokrat```"],["SEE ALSO","[zathura](/man/zathura)(1), [mupdf](/man/mupdf)(1), [calibre](/man/calibre)(1)"]],"e":["zathura","mupdf","calibre"],"x":"bookokrat [path/to/book.epub]"},"bun-pm-pkg":{"n":"bun-pm-pkg","t":"Read and modify package.json fields","s":[["TAGLINE","Read and modify package.json fields"],["TLDR","**Get a package.json field**\n\n```bun pm pkg get [field]```\n\n**Set a package.json field**\n\n```bun pm pkg set [field]=[value]```\n\n**Delete a package.json field**\n\n```bun pm pkg delete [field]```"],["SYNOPSIS","**bun** **pm** **pkg** _subcommand_ [_args_]"],["SUBCOMMANDS","**get** _field_\n> Get the value of a package.json field.\n\n**set** _field=value_\n> Set a package.json field.\n\n**delete** _field_\n> Delete a package.json field."],["DESCRIPTION","**bun pm pkg** provides commands to read and modify package.json fields programmatically. This is useful for scripting and automation."],["INSTALL","```pacman: sudo pacman -S bun```\n\n```brew: brew install bun```\n\n```nix: nix profile install nixpkgs#bun```"],["SEE ALSO","[bun](/man/bun)(1), [bun-pm](/man/bun-pm)(1)"],["RESOURCES","```[Source code](https://github.com/oven-sh/bun)```\n\n```[Homepage](https://bun.sh)```\n\n```[Documentation](https://bun.sh/docs)```"]],"e":["bun","bun-pm"],"x":"bun pm pkg get [field]"},"bun-why":{"n":"bun-why","t":"Show why a package is installed","s":[["TAGLINE","Show why a package is installed"],["TLDR","**Show why a package is installed**\n\n```bun why [package]```"],["SYNOPSIS","**bun** **why** _package_"],["DESCRIPTION","**bun why** shows why a specific package is installed in your project. It displays the dependency chain that leads to the package, showing which packages depend on it directly or transitively.\n\nThis is useful for understanding why a package appears in your node_modules, especially for transitive dependencies."],["SEE ALSO","[bun](/man/bun)(1), [bun-list](/man/bun-list)(1), [npm-explain](/man/npm-explain)(1)"],["RESOURCES","```[Source code](https://github.com/oven-sh/bun)```\n\n```[Homepage](https://bun.sh)```\n\n```[Documentation](https://bun.sh/docs)```"]],"e":["bun","bun-list","npm-explain"],"x":"bun why [package]"},"cargo-geiger":{"n":"cargo-geiger","t":"Detect unsafe Rust code in dependency tree","s":[["TAGLINE","Detect unsafe Rust code in dependency tree"],["TLDR","**Scan** the current project for unsafe code\n\n```cargo geiger```\n\n**Output** in JSON format\n\n```cargo geiger --output-format json```\n\n**Include only build dependencies**\n\n```cargo geiger --build-dependencies```\n\n**Scan** without default features\n\n```cargo geiger --no-default-features```"],["SYNOPSIS","**cargo geiger** [_options_]"],["PARAMETERS","**--all-dependencies**\n> Include all dependencies in the scan.\n\n**--build-dependencies**\n> Include build dependencies.\n\n**--dev-dependencies**\n> Include development dependencies.\n\n**--output-format** _FORMAT_\n> Output format: ascii-tree (default) or json.\n\n**--no-default-features**\n> Do not activate the default feature.\n\n**--features** _FEATURES_\n> Space or comma separated list of features to activate.\n\n**--update-readme**\n> Update a safety report in the project README.\n\n**--forbid-only**\n> Only report crates that don't use `#![forbid(unsafe_code)]`."],["DESCRIPTION","**cargo-geiger** is a Cargo subcommand that scans a Rust project's dependency tree to detect and report usage of **unsafe** Rust code. It counts unsafe expressions, function declarations, trait implementations, and method calls in both the project and all its dependencies.\n\nThe output shows a tree view of the dependency graph annotated with counts of unsafe code usage, helping developers assess the safety profile of their dependency chain. Crates using `#![forbid(unsafe_code)]` are clearly marked as verified safe.\n\nThe name is a reference to a **Geiger counter**, which detects radiation — similarly, cargo-geiger detects potentially dangerous unsafe code."],["CAVEATS","Analysis is based on source-level detection and may not catch all forms of unsafe behavior (e.g., unsafe code hidden behind macros or generated at build time). Large dependency trees can take significant time to analyze. Requires the source code of all dependencies to be available."],["HISTORY","**cargo-geiger** was created by **Anders Kaseorg** and the Rust community as a security auditing tool. It addresses the concern that Rust's safety guarantees only hold for safe code, and that unsafe blocks in dependencies can undermine the safety of the entire application."],["INSTALL","```pacman: sudo pacman -S cargo-geiger```\n\n```brew: brew install cargo-geiger```\n\n```nix: nix profile install nixpkgs#cargo-geiger```"],["SEE ALSO","[cargo](/man/cargo)(1)"]],"e":["cargo"],"x":"cargo geiger"},"ccc":{"n":"ccc","t":"ContextCodeCache generator for AI coding agents","s":[["TAGLINE","ContextCodeCache generator for AI coding agents"],["TLDR","**Scan a project** and generate the `.ccc/` cache\n\n```ccc scan```\n\n**Scan and also produce a token stream**\n\n```ccc scan . --tokens```\n\n**Check whether the committed cache is up to date** (CI)\n\n```ccc check .```\n\n**Install the binary to PATH** (Linux)\n\n```ccc install```"],["SYNOPSIS","**ccc** scan [_PATH_] [--tokens]\n\n**ccc** check [_PATH_] [--format json]\n\n**ccc** tokenize [_PATH_]\n\n**ccc** install [--dir DIR] [--force]"],["DESCRIPTION","ccc scans a source tree and produces a compact, machine-readable `.ccc/` directory containing per-file summaries: constants, functions (with types and doc summaries), call-graph edges inside the file, and marker comments (TODO, FIXME, etc.).\n\nThe cache is designed to be committed and consumed by AI agents so they have a cheap, always-fresh structural index instead of repeatedly grepping or re-reading source.\n\nSupported languages: Rust, Python, JavaScript, TypeScript (+ TSX), and Go (via tree-sitter). Unsupported files are skipped; hidden dirs, common build/vendor dirs, and `.gitignore` rules are honored."],["INSTALL","Requires Rust ≥ 1.77 and a recent cargo:\n\n```bash\ncargo build --release\n./target/release/ccc install\n```\n\nThe `ccc install` subcommand copies the binary to `~/.local/bin` (or a directory you choose with `--dir`). Use `--force` to overwrite an existing install."],["PARAMETERS","**scan** [_PATH_]\n> Regenerate the `.ccc` directory for the given path (defaults to current directory).\n\n**--tokens**\n> Also emit a pre-encoded token stream (`tokens.bin` + `tokens.json`) for models that can consume raw token IDs.\n\n**check** [_PATH_]\n> Exit non-zero if the on-disk cache is stale relative to current sources. Useful in CI.\n\n**--format json**\n> Machine-readable output listing changed files (`{ root, up_to_date, files[], changes[] }`).\n\n**tokenize** [_PATH_]\n> Pre-encode an existing `.ccc` into `tokens.bin` + `tokens.json` without a full rescan.\n\n**install** [--dir DIR] [--force]\n> Copy the running binary onto your PATH (Linux). Default dir: `~/.local/bin`."],["CAVEATS","The token stream uses approximate tiktoken IDs (OpenAI vocabulary, e.g. `o200k_base`) and is **not compatible with Anthropic models**. Use the Markdown form of the cache with Claude.\n\nRegenerate the cache whenever source files that affect the index change. A CI step of `ccc check .` fails the build if the cache is out of date."],["RESOURCES","```[Source code](https://github.com/colwill/ccc)```"]],"e":[],"x":"ccc scan"},"chronic":{"n":"chronic","t":"Silences command output unless it fails","s":[["TAGLINE","Silences command output unless it fails"],["TLDR","**Run command silently unless it fails**\n\n```chronic [command]```\n\n**Verbose mode with stderr/stdout distinction**\n\n```chronic -v [command]```\n\n**Trigger on stderr output**\n\n```chronic -e [command]```\n\n**Verbose with stderr triggering**\n\n```chronic -ve [command]```\n\n**Use in a cron job instead of redirecting to /dev/null**\n\n```0 1 * * * chronic backup```\n\n**Run a backup script, showing output only if it fails or produces stderr**\n\n```chronic -ve /usr/local/bin/backup.sh```"],["SYNOPSIS","**chronic** [-ev] _command_ ..."],["DESCRIPTION","**chronic** runs a command quietly, showing output only if the command fails (non-zero exit or crash). Part of moreutils. Ideal for cron jobs where you want verbose commands but silent success."],["PARAMETERS","**-v**\n> Verbose mode: distinguish stdout/stderr, report return value\n\n**-e**\n> Stderr triggering: show output if stderr is non-empty"],["EXIT STATUS","**0**\n> Command succeeded with no stderr (in -e mode)\n\n**1**\n> Command failed\n\n**2**\n> Command succeeded but produced stderr (in -e mode)"],["BEHAVIOR","- Success (exit 0): output discarded\n- Failure (exit != 0): output displayed\n- With -e: stderr output triggers display even on success"],["CAVEATS","Part of moreutils package. Better than redirecting to /dev/null because you still see errors."],["INSTALL","```apt: sudo apt install moreutils```\n\n```dnf: sudo dnf install moreutils```\n\n```pacman: sudo pacman -S moreutils```\n\n```apk: sudo apk add moreutils```\n\n```zypper: sudo zypper install moreutils```\n\n```brew: brew install moreutils```\n\n```nix: nix profile install nixpkgs#moreutils```"],["SEE ALSO","[moreutils](/man/moreutils)(1), [cron](/man/cron)(8), [crontab](/man/crontab)(1), [sponge](/man/sponge)(1), [ts](/man/ts)(1)"]],"e":["moreutils","cron","crontab","sponge","ts"],"x":"chronic [command]"},"cidr":{"n":"cidr","t":"IP subnet and CIDR notation calculator","s":[["TAGLINE","IP subnet and CIDR notation calculator"],["TLDR","**Calculate subnet info**\n\n```ipcalc [192.168.1.0/24]```\n\n**Show network range**\n\n```sipcalc [10.0.0.0/8]```\n\n**Calculate with netmask**\n\n```ipcalc [192.168.1.0] [255.255.255.0]```\n\n**Design subnets**\n\n```ipcalc [192.168.1.0/24] [/27]```"],["SYNOPSIS","**ipcalc** [_options_] _address_[/_prefix_] [_netmask_]\n**sipcalc** [_options_] _address_[/_prefix_]"],["DESCRIPTION","CIDR calculators compute network information from IP addresses and subnet masks. Common tools include **ipcalc**, **sipcalc**, and **whatmask**. They calculate network and broadcast addresses, usable host ranges, wildcard masks, and other subnet parameters.\n\nThese utilities are essential for network planning, subnetting, and troubleshooting. Given an IP address and prefix length (or netmask), they break down all relevant addressing details. Some tools also support IPv6, subnet splitting, and supernetting.\n\nAvailable on most Linux distributions via package managers. For visual CIDR notation exploration, online tools like cidr.xyz provide interactive visualizations."],["IPCALC OPTIONS","**-n**\n> Don't show hostname\n\n**-b**\n> Don't show broadcast\n\n**-s** _hosts_\n> Split network for hosts\n\n**-r** _hosts_\n> Calculate needed prefix"],["SIPCALC OPTIONS","**-a**\n> All information\n\n**-d**\n> Resolve hostnames\n\n**-4**\n> IPv4 only\n\n**-6**\n> IPv6 only"],["OUTPUT FIELDS","**Network**\n> First address in subnet\n\n**Broadcast**\n> Last address in subnet\n\n**HostMin/HostMax**\n> Usable address range\n\n**Hosts/Net**\n> Number of usable addresses\n\n**Wildcard**\n> Cisco wildcard mask"],["INSTALLATION","```\napt install ipcalc sipcalc\n```"],["CAVEATS","Use for network planning and troubleshooting. Online tools available at cidr.xyz for visualization."],["INSTALL","```apt: sudo apt install cidr```\n\n```brew: brew install cidr```"],["SEE ALSO","[ip](/man/ip)(8), [ifconfig](/man/ifconfig)(8)"]],"e":["ip","ifconfig"],"x":"ipcalc [192.168.1.0/24]"},"cmark":{"n":"cmark","t":"CommonMark Markdown parser and renderer","s":[["TAGLINE","CommonMark Markdown parser and renderer"],["TLDR","**Convert Markdown to HTML**\n\n```cmark [input.md]```\n\n**Convert to specific format**\n\n```cmark --to [html|xml|man|latex] [input.md]```\n\n**Read from stdin**\n\n```cat [input.md] | cmark```\n\n**Output to file**\n\n```cmark [input.md] > [output.html]```\n\n**Enable smart typography**\n\n```cmark --smart [input.md]```\n\n**Convert to groff man page** format\n\n```cmark --to man [input.md]```\n\n**Validate UTF-8** and replace invalid sequences\n\n```cmark --validate-utf8 [input.md]```\n\n**Render with hard line breaks**\n\n```cmark --hardbreaks [input.md]```"],["SYNOPSIS","**cmark** [_options_] [_file..._]"],["PARAMETERS","**--to** _format_\n> Output format: html, xml, man, commonmark, latex.\n\n**--smart**\n> Enable smart punctuation (curly quotes, dashes).\n\n**--safe**\n> Omit raw HTML and potentially unsafe links.\n\n**--validate-utf8**\n> Validate UTF-8 and replace invalid sequences.\n\n**--hardbreaks**\n> Render soft breaks as hard breaks.\n\n**--nobreaks**\n> Render soft breaks as spaces.\n\n**--width** _N_\n> Wrap text at N columns (default 0, no wrap). Only affects commonmark and man output.\n\n**--sourcepos**\n> Include source position information in output."],["DESCRIPTION","**cmark** is the reference C implementation of CommonMark, a strongly specified and highly compatible variant of Markdown. It parses Markdown input and converts it to various output formats including HTML, XML, groff man pages, CommonMark, and LaTeX.\n\nThe parser is designed for correctness and performance, strictly following the CommonMark specification to eliminate the ambiguities present in the original Markdown description. This makes it suitable for applications that require predictable, consistent rendering across different tools.\n\ncmark reads from files or standard input and writes to standard output, making it easy to integrate into pipelines. The **--smart** option provides typographic enhancements like curly quotes and em-dashes, while **--safe** strips potentially dangerous raw HTML from input."],["HISTORY","**cmark** is the reference C implementation of the **CommonMark** specification, developed by John MacFarlane and contributors starting in **2014**. CommonMark was created to provide a formal, unambiguous specification for Markdown."],["INSTALL","```apt: sudo apt install cmark```\n\n```dnf: sudo dnf install cmark```\n\n```pacman: sudo pacman -S cmark```\n\n```apk: sudo apk add cmark```\n\n```zypper: sudo zypper install cmark```\n\n```brew: brew install cmark```\n\n```nix: nix profile install nixpkgs#cmark```"],["SEE ALSO","[pandoc](/man/pandoc)(1), [markdown](/man/markdown)(1), [grip](/man/grip)(1)"]],"e":["pandoc","markdown","grip"],"x":"cmark [input.md]"},"cronitor":{"n":"cronitor","t":"cron job monitoring service CLI","s":[["TAGLINE","cron job monitoring service CLI"],["TLDR","**Sync cron jobs with Cronitor**\n\n```cronitor sync```\n\n**Execute command with monitoring**\n\n```cronitor exec [monitor-key] -- [command]```\n\n**List all cron jobs**\n\n```cronitor list```\n\n**Check monitor status**\n\n```cronitor status```\n\n**Send telemetry ping**\n\n```cronitor ping [monitor-key]```\n\n**Start web dashboard**\n\n```cronitor dash```\n\n**Configure API key**\n\n```cronitor configure --api-key [key]```"],["SYNOPSIS","**cronitor** [_global_options_] _command_ [_options_]"],["DESCRIPTION","**cronitor** is the CLI companion for Cronitor, a cloud-based monitoring service for cron jobs and scheduled tasks. The tool monitors cron job execution, tracks timing and frequency, and sends alerts when jobs fail, miss their schedule, or run longer than expected.\n\nThe CLI automatically syncs cron jobs from the system to the Cronitor service, wraps command execution to send telemetry pings, and provides a dashboard for viewing job status. It helps teams maintain reliability of scheduled maintenance tasks, backups, and automated processes by ensuring timely notification of problems."],["COMMANDS","**sync**\n> Sync cron jobs to Cronitor\n\n**exec**\n> Execute command with monitoring\n\n**list**\n> List and search cron jobs\n\n**ping**\n> Send telemetry ping\n\n**status**\n> View monitor status\n\n**dash**\n> Start web dashboard\n\n**configure**\n> Save configuration\n\n**select**\n> Select a cron job to run interactively\n\n**activity**\n> View monitor activity\n\n**shell**\n> Run cron-like shell\n\n**update**\n> Update to latest version"],["GLOBAL OPTIONS","**-k**, **--api-key** _key_\n> Cronitor API key\n\n**-c**, **--config** _file_\n> Config file path\n\n**--env** _name_\n> Cronitor environment\n\n**-n**, **--hostname** _name_\n> Host identifier\n\n**-u**, **--users** _list_\n> Comma-separated user list\n\n**-p**, **--ping-api-key** _key_\n> Telemetry Events API key\n\n**-v**, **--verbose**\n> Verbose output\n\n**-l**, **--log** _file_\n> Debug log file"],["CONFIGURATION","**~/.config/cronitor/cronitor.json**\n> User configuration file containing API key, environment, hostname, and other settings."],["CAVEATS","Requires Cronitor account and API key. Use sudo for system-wide crontab monitoring. Includes MCP server for AI tool integration."],["SEE ALSO","[cron](/man/cron)(8), [crontab](/man/crontab)(1)"]],"e":["cron","crontab"],"x":"cronitor sync"},"diffnav":{"n":"diffnav","t":"Git diff pager with file tree UI","s":[["TAGLINE","Git diff pager with file tree UI"],["TLDR","**View** git diffs with an interactive file tree\n\n```diffnav```\n\n**View** diffs for a specific commit\n\n```diffnav [commit_hash]```\n\n**View** diffs between two commits\n\n```diffnav [commit1] [commit2]```\n\n**View** diffs for specific files only\n\n```diffnav -- [file1] [file2]```"],["SYNOPSIS","**diffnav** [_options_] [_commit_range_]"],["PARAMETERS","**-d, --diff** _MODE_\n> Diff mode: unified, context, or side-by-side\n\n**-w, --width** _N_\n> Set display width for side-by-side view\n\n**--no-color**\n> Disable colored output\n\n**-h, --help**\n> Display help and exit\n\n**-v, --version**\n> Display version and exit"],["DESCRIPTION","**diffnav** is a git diff pager that provides an interactive file tree interface for navigating code changes. It combines the functionality of traditional diff viewers with a visual file explorer, making it easier to browse large changesets across multiple files.\n\nThe tool integrates with git to show file-by-file differences with syntax highlighting, collapsible sections, and keyboard navigation. It's particularly useful when reviewing pull requests or browsing commits that touch many files."],["KEYBINDINGS","**↑/↓**\n> Navigate through files in the tree\n\n**Enter**\n> View diff for selected file\n\n**Space**\n> Toggle file expansion/collapse\n\n**n/p**\n> Jump to next/previous file with changes\n\n**/**\n> Search within diffs\n\n**q**\n> Quit"],["CAVEATS","Requires git to be installed and accessible in PATH. Large repositories may experience slower loading times. Syntax highlighting depends on file extension detection. Some terminal emulators may have limited color support."],["HISTORY","**diffnav** was developed to improve the git diff browsing experience by adding a visual file tree component to the traditional pager interface, making it easier to understand the scope of changes across multiple files."],["INSTALL","```pacman: sudo pacman -S diffnav```\n\n```brew: brew install diffnav```\n\n```nix: nix profile install nixpkgs#diffnav```"],["SEE ALSO","[git](/man/git)(1), [diff](/man/diff)(1), [less](/man/less)(1), [difftastic](/man/difftastic)(1)"]],"e":["git","diff","less","difftastic"],"x":"diffnav"},"discordo":{"n":"discordo","t":"Discord client for the terminal","s":[["TAGLINE","Discord client for the terminal"],["TLDR","**Start** Discordo\n\n```discordo```\n\n**Login** with a token / browser flow (see first-run prompts)\n\n```discordo```"],["SYNOPSIS","**discordo** [*options*]"],["DESCRIPTION","**discordo** is a lightweight TUI Discord client written in Go. It supports browsing guilds/channels, sending messages, and keyboard-driven navigation. Install via distro packages, nightly binaries, or build from source.\n\n**Important:** Automating user accounts (“self-bots”) violates Discord’s Terms of Service. Prefer official bot accounts where applicable; use at your own risk."],["PARAMETERS","Primarily interactive. Configuration and token storage paths are documented in the upstream README. Run **discordo --help** for flags on your build."],["CAVEATS","ToS risk for user-token logins. WIP project—expect breaking changes. Handle auth tokens like passwords."],["INSTALL","```nix: nix profile install nixpkgs#discordo```"],["SEE ALSO","[discord](/man/discord)(1), [weechat](/man/weechat)(1)"],["RESOURCES","```[Source code](https://github.com/ayn2op/discordo)```"]],"e":["discord","weechat"],"x":"discordo"},"display.im6":{"n":"display.im6","t":"ImageMagick version 6 image viewer","s":[["TAGLINE","ImageMagick version 6 image viewer"],["TLDR","**Display an image** (ImageMagick 6)\n\n```display.im6 [image.jpg]```\n\n**Display with title**\n\n```display.im6 -title \"[Title]\" [image.jpg]```\n\n**Display resized**\n\n```display.im6 -resize [50%] [image.jpg]```\n\n**Display multiple images**\n\n```display.im6 [image1.jpg] [image2.jpg]```"],["SYNOPSIS","**display.im6** [_options_] _file_..."],["PARAMETERS","_FILE_\n> Image file(s) to display.\n\n**-resize** _GEOMETRY_\n> Resize image.\n\n**-title** _STRING_\n> Set window title.\n\n**-geometry** _GEOMETRY_\n> Window geometry.\n\n**-delay** _TICKS_\n> Slideshow delay in hundredths of a second between images.\n\n**-density** _GEOMETRY_\n> Set image rendering resolution (DPI).\n\n**-rotate** _DEGREES_\n> Rotate image by specified degrees.\n\n**-help**\n> Display help information."],["DESCRIPTION","**display.im6** is the ImageMagick version 6 image viewer, provided for compatibility when both ImageMagick 6 and 7 are installed on a system. It functions identically to the display command but explicitly uses the version 6 libraries.\n\nThis versioned command allows scripts and users to specify which ImageMagick version to use when both are available, ensuring consistent behavior regardless of system defaults.\n\nAll features of the standard display command are available, including interactive viewing, image manipulation, format conversion, and slideshow capabilities."],["CAVEATS","Requires ImageMagick 6 installed. May behave differently than ImageMagick 7 version. Primarily exists for compatibility during transition periods."],["HISTORY","The .im6 suffix was added when **ImageMagick 7** introduced breaking changes from version 6. This naming convention allows both versions to coexist, with im6 explicitly invoking the legacy version."],["SEE ALSO","[display](/man/display)(1), [identify.im6](/man/identify.im6)(1)"]],"e":["display","identify.im6"],"x":"display.im6 [image.jpg]"},"dnf5-copr":{"n":"dnf5-copr","t":"Manage Copr repositories with DNF5","s":[["TAGLINE","Manage Copr repositories with DNF5"],["TLDR","**Enable** a Copr repository\n\n```sudo dnf5 copr enable [owner]/[project]```\n\nEnable a Copr repository for a specific **chroot**\n\n```sudo dnf5 copr enable [owner]/[project] [fedora-rawhide-ppc64le]```\n\nEnable a Copr repository from a specific **hub**\n\n```sudo dnf5 copr enable [hub]/[owner]/[project]```\n\n**Disable** a Copr repository\n\n```sudo dnf5 copr disable [owner]/[project]```\n\n**Remove** a Copr repository\n\n```sudo dnf5 copr remove [owner]/[project]```\n\n**List** configured Copr repositories\n\n```dnf5 copr list```\n\nPrint **debug** information about the system\n\n```dnf5 copr debug```"],["SYNOPSIS","**dnf5 copr** {_list_|_enable_|_disable_|_remove_|_debug_} [_options_] [_project-spec_] [_chroot_]"],["PARAMETERS","**list**\n> List Copr repositories configured on the system\n\n**enable** _project-spec_ [_chroot_]\n> Download repository info from a Copr server and install it as a `/etc/yum.repos.d/*.repo` file\n\n**disable** _project-spec_\n> Disable the specified Copr repository (sets `enabled=0`; keeps the `.repo` file)\n\n**remove** _project-spec_\n> Remove the specified Copr repository and its `/etc/yum.repos.d/*.repo` file\n\n**debug**\n> Print system information useful for debugging\n\n**--hub** _hostname_\n> Copr hub (web-UI/API server) hostname; default is `copr.fedorainfracloud.org`\n\n**project-spec**\n> Project ID as `OWNER/PROJECT` or `HUB/OWNER/PROJECT` (owner may be a username or `@@groupname`)\n\n**chroot**\n> Chroot in `NAME-RELEASE-ARCH` form (e.g. `fedora-rawhide-ppc64le`); auto-detected when omitted"],["DESCRIPTION","**dnf5 copr** manages Copr repositories on Fedora-based systems. Copr (Cool Other Package Repo) hosts community and third-party package repositories that can be enabled with a single command.\n\nEnabling a project installs a repository definition under `/etc/yum.repos.d/`. Disabling keeps the definition but turns it off; removing deletes the definition entirely."],["CAVEATS","Copr projects are user-maintained and are not official Fedora packages. Review project pages before enabling. Incorrect chroots may install packages built for the wrong distribution or architecture."],["HISTORY","**copr** is a DNF5 plugin for Fedora's Copr build service, replacing the older `dnf copr` plugin from DNF4."],["SEE ALSO","[dnf5](/man/dnf5)(8), [dnf](/man/dnf)(8)"],["RESOURCES","```[Source code](https://github.com/rpm-software-management/dnf5)```\n\n```[Documentation](https://dnf5.readthedocs.io/en/latest/dnf5_plugins/copr.8.html)```"]],"e":["dnf5","dnf"],"x":"sudo dnf5 copr enable [owner]/[project]"},"dotenvx":{"n":"dotenvx","t":"encrypted environment variable manager","s":[["TAGLINE","encrypted environment variable manager"],["TLDR","**Run command with .env loaded**\n\n```dotenvx run -- [command]```\n\n**Run with specific env file**\n\n```dotenvx run -f [.env.production] -- [command]```\n\n**Encrypt env file**\n\n```dotenvx encrypt```\n\n**Decrypt env file**\n\n```dotenvx decrypt```\n\n**Set a key**\n\n```dotenvx set [KEY] [value]```\n\n**Get a key**\n\n```dotenvx get [KEY]```\n\n**Convert to encrypted format**\n\n```dotenvx convert```"],["SYNOPSIS","**dotenvx** _command_ [_options_]"],["SUBCOMMANDS","**run**\n> Load env and run command.\n\n**get**\n> Get environment variable.\n\n**set**\n> Set environment variable.\n\n**encrypt**\n> Encrypt .env file.\n\n**decrypt**\n> Decrypt .env file.\n\n**convert**\n> Convert to encrypted format.\n\n**genexample**\n> Generate .env.example."],["PARAMETERS","**-f**, **--env-file** _file_\n> Path to env file.\n\n**--overload**\n> Override existing env vars."],["CONFIGURATION","**.env**\n> Default environment variables file loaded automatically.\n\n**.env.keys**\n> Encryption keys for decrypting .env files."],["DESCRIPTION","**dotenvx** is an enhanced dotenv tool that loads environment variables from .env files with support for encryption. It extends the traditional dotenv pattern with built-in encryption capabilities, allowing teams to securely commit encrypted environment files to version control.\n\nThe tool supports multiple environment files (.env.production, .env.staging), automatic decryption at runtime, and key management for team-based secret sharing. It provides a modern approach to managing application secrets without external secret management services."],["INSTALL","```nix: nix profile install nixpkgs#dotenvx```"],["SEE ALSO","[env](/man/env)(1), [direnv](/man/direnv)(1), [sops](/man/sops)(1)"]],"e":["env","direnv","sops"],"x":"dotenvx run -- [command]"},"dovecot":{"n":"dovecot","t":"secure IMAP and POP3 email server","s":[["TAGLINE","secure IMAP and POP3 email server"],["TLDR","**Start Dovecot** daemon\n\n```dovecot```\n\n**Run in foreground** (do not daemonize)\n\n```dovecot -F```\n\n**Show non-default configuration** settings\n\n```dovecot -n```\n\n**Show full configuration** (all settings)\n\n```dovecot -a```\n\n**Use alternate configuration** file\n\n```dovecot -c [/path/to/dovecot.conf]```\n\n**Reload configuration** without restarting\n\n```dovecot reload```\n\n**Stop Dovecot** daemon\n\n```dovecot stop```\n\n**Show version**\n\n```dovecot --version```"],["SYNOPSIS","**dovecot** [_options_] [_command_]"],["PARAMETERS","**-F**\n> Run in foreground, do not daemonize\n\n**-n**\n> Print non-default configuration and exit\n\n**-a**\n> Print all configuration settings and exit\n\n**-c** _config_file_\n> Use specified configuration file instead of /etc/dovecot/dovecot.conf\n\n**-p**\n> Prompt for SSL key password on startup\n\n**reload**\n> Reload configuration without restarting\n\n**stop**\n> Stop the running daemon\n\n**--build-options**\n> Show build options and exit\n\n**--version**\n> Show version and exit"],["CONFIGURATION","**/etc/dovecot/dovecot.conf**\n> Main configuration file for protocols, authentication, and mail storage settings.\n\n**/etc/dovecot/conf.d/**\n> Modular configuration directory for protocol-specific and feature-specific settings."],["DESCRIPTION","**Dovecot** is an open-source IMAP and POP3 email server for Unix-like systems. It provides secure, fast, and standards-compliant access to email stored on the server.\n\nThe server supports various authentication methods, virtual users, and mailbox formats including Maildir and mbox. It is designed for both small and large-scale deployments with features like quota management and full-text search.\n\nDovecot's modular architecture allows plugins for LDAP authentication, Sieve filtering, and integration with other mail system components. The **-F** flag is commonly used in container environments or with process supervisors."],["CAVEATS","Requires proper configuration before use. TLS certificate setup needed for secure connections. Permission issues common with mailbox access. Use **dovecot -n** to verify configuration before deploying changes."],["HISTORY","Dovecot was created by **Timo Sirainen** and first released in **2002**. It was designed as a secure, fast alternative to existing IMAP servers, emphasizing correctness and standards compliance."],["INSTALL","```apt: sudo apt install dovecot-core```\n\n```dnf: sudo dnf install dovecot```\n\n```pacman: sudo pacman -S dovecot```\n\n```apk: sudo apk add dovecot```\n\n```zypper: sudo zypper install dovecot```\n\n```brew: brew install dovecot```\n\n```nix: nix profile install nixpkgs#dovecot```"],["SEE ALSO","[postfix](/man/postfix)(1), [doveadm](/man/doveadm)(1)"]],"e":["postfix","doveadm"],"x":"dovecot"},"e1s":{"n":"e1s","t":"AWS ECS terminal dashboard (like k9s for ECS)","s":[["TAGLINE","AWS ECS terminal dashboard (like k9s for ECS)"],["TLDR","**Launch** the ECS dashboard\n\n```e1s```\n\n**Connect** to specific AWS profile\n\n```e1s --profile [profile_name]```\n\n**View** specific cluster on startup\n\n```e1s --cluster [cluster_name]```\n\n**Connect** to specific region\n\n```e1s --region [us-west-2]```"],["SYNOPSIS","**e1s** [_options_]"],["PARAMETERS","**--profile** _NAME_\n> AWS profile to use.\n\n**--region** _REGION_\n> AWS region (default: from config).\n\n**--cluster** _NAME_\n> Default ECS cluster to view.\n\n**--service** _NAME_\n> Default ECS service to view (requires --cluster).\n\n**--read-only**\n> Start in read-only mode.\n\n**-c**, **--config-file** _FILE_\n> Config file path (default: ~/.config/e1s/config.yml).\n\n**-r**, **--refresh** _SECONDS_\n> Auto-refresh interval in seconds (default: 30, -1 to disable).\n\n**-s**, **--shell** _PATH_\n> Shell for interactive exec (default: /bin/sh).\n\n**-d**, **--debug**\n> Enable debug logging.\n\n**-l**, **--log-file** _FILE_\n> Custom log file path.\n\n**-j**, **--json**\n> Output logs in JSON format.\n\n**-v**, **--version**\n> Display version and exit.\n\n**-h**, **--help**\n> Display help and exit."],["DESCRIPTION","**e1s** is a terminal user interface for Amazon ECS (Elastic Container Service), inspired by k9s for Kubernetes. It provides a real-time dashboard for monitoring ECS clusters, services, tasks, and containers.\n\nThe tool allows users to view resource status, view logs, execute commands in containers, and manage deployments without leaving the terminal. It integrates with AWS credentials and supports multiple profiles and regions."],["KEYBINDINGS","**h/j/k/l**\n> Vim-style navigation (left/down/up/right)\n\n**Enter**\n> View resource details\n\n**/**\n> Filter resources\n\n**ctrl-p**\n> Switch AWS profile\n\n**ctrl-d**\n> Exit interactive exec session\n\n**q**\n> Quit\n\n**?**\n> Show all keybindings"],["CAVEATS","Requires AWS credentials with appropriate ECS permissions. Some operations require additional IAM permissions. Log streaming may incur AWS CloudWatch costs. Network connectivity to AWS required."],["HISTORY","**e1s** was created as the ECS equivalent of k9s, bringing the powerful terminal-based management experience to AWS ECS users. It fills the gap for users managing containerized applications on ECS."],["INSTALL","```brew: brew install e1s```\n\n```nix: nix profile install nixpkgs#e1s```"],["SEE ALSO","[aws](/man/aws)(1), [k9s](/man/k9s)(1), [docker](/man/docker)(1)"]],"e":["aws","k9s","docker"],"x":"e1s"},"footclient":{"n":"footclient","t":"client for foot terminal server","s":[["TAGLINE","client for foot terminal server"],["TLDR","**Open a new terminal window** via foot server\n\n```footclient```\n\n**Open a terminal running** a specific command\n\n```footclient [command]```\n\n**Open a terminal in** a specific directory\n\n```footclient -D [path/to/directory]```\n\n**Open a terminal with** a custom title\n\n```footclient -T \"[My Terminal]\"```\n\n**Connect to a custom socket** path\n\n```footclient --server-socket=[path/to/foot.sock]```\n\n**Open a maximized terminal** window\n\n```footclient --maximized```\n\n**Open a fullscreen terminal** window\n\n```footclient --fullscreen```"],["SYNOPSIS","**footclient** [_OPTIONS_] [_COMMAND_ [_ARGS_...]]"],["PARAMETERS","**-D**, **--working-directory**=_DIR_\n> Start terminal in specified directory.\n\n**-T**, **--title**=_TITLE_\n> Set initial window title.\n\n**-a**, **--app-id**=_ID_\n> Set Wayland app-id (for window manager matching).\n\n**-m**, **--maximized**\n> Start window maximized.\n\n**-F**, **--fullscreen**\n> Start window in fullscreen mode.\n\n**-s**, **--server-socket**=_PATH_\n> Connect to foot server at specified socket path.\n\n**-H**, **--hold**\n> Keep terminal open after command exits.\n\n**-N**, **--no-wait**\n> Detach from server immediately (don't wait for window close).\n\n**-h**, **--help**\n> Display help message.\n\n**-v**, **--version**\n> Display version information."],["DESCRIPTION","**footclient** is a client for the foot terminal emulator's server mode. When foot runs as a server (**foot --server**), footclient connects to it to open new terminal windows. This architecture provides faster startup times and reduced memory usage since fonts, glyph caches, and configuration are shared across all terminal windows.\n\nThe server listens on a Unix socket, defaulting to **$XDG_RUNTIME_DIR/foot-$WAYLAND_DISPLAY.sock**. Footclient remains running until the associated terminal window closes, unless **--no-wait** is specified.\n\nTypical usage involves starting **foot --server** at login (often via systemd socket activation) and using footclient for all subsequent terminal launches."],["CAVEATS","Requires foot server to be running. All terminals share the same server process, so a server crash affects all windows. Heavy load on multiple terminals may reduce performance compared to standalone foot instances. Only works on Wayland; foot does not support X11."],["HISTORY","Foot was created by Daniel Eklöf as a fast, lightweight terminal emulator designed specifically for Wayland compositors. The server/client architecture was implemented to address the startup time and memory overhead of launching multiple terminal instances. Foot has gained popularity in the Wayland ecosystem for its performance and simplicity."],["INSTALL","```apt: sudo apt install foot```\n\n```dnf: sudo dnf install foot```\n\n```pacman: sudo pacman -S foot```\n\n```apk: sudo apk add foot```\n\n```zypper: sudo zypper install foot```\n\n```brew: brew install foot```\n\n```nix: nix profile install nixpkgs#foot```"],["SEE ALSO","[foot](/man/foot)(1), [alacritty](/man/alacritty)(1), [kitty](/man/kitty)(1), [wezterm](/man/wezterm)(1)"],["RESOURCES","```[Source code](https://codeberg.org/dnkl/foot)```"]],"e":["foot","alacritty","kitty","wezterm"],"x":"footclient"},"fprintd-verify":{"n":"fprintd-verify","t":"verify a fingerprint against enrolled prints","s":[["TAGLINE","verify a fingerprint against enrolled prints"],["TLDR","Verify **all stored fingerprints** for the current user\n\n```fprintd-verify```\n\nVerify a **specific fingerprint** for the current user\n\n```fprintd-verify -f [left-thumb|left-index-finger|right-thumb|...]```\n\nVerify fingerprints for a **specific user**\n\n```fprintd-verify [username]```\n\nVerify a **specific fingerprint** for a **specific user**\n\n```fprintd-verify -f [finger_name] [username]```\n\nDisplay **help**\n\n```fprintd-verify -h```"],["SYNOPSIS","**fprintd-verify** [_options_] [_username_]"],["PARAMETERS","**-f**, **--finger** _FINGER_\n> Specify which finger to verify: left-thumb, left-index-finger, left-middle-finger, left-ring-finger, left-little-finger, right-thumb, right-index-finger, right-middle-finger, right-ring-finger, right-little-finger\n\n**-h**, **--help**\n> Display help message"],["DESCRIPTION","**fprintd-verify** tests enrolled fingerprints against the fprintd database. It prompts the user to scan their finger and reports whether the scan matches any enrolled fingerprint. It communicates with the fprintd daemon over D-Bus.\n\nThis is useful for testing fingerprint enrollment quality and troubleshooting authentication issues. Without specifying a finger, the system attempts to match against any enrolled finger."],["CAVEATS","Verification success depends on scan quality and may require multiple attempts. Environmental factors like moisture or dirt on the sensor can affect results. This command is for testing purposes; actual login authentication uses the PAM module **pam_fprintd**. The fprintd daemon must be running for this command to work."],["INSTALL","```apt: sudo apt install fprintd```\n\n```dnf: sudo dnf install fprintd```\n\n```pacman: sudo pacman -S fprintd```\n\n```apk: sudo apk add fprintd```\n\n```zypper: sudo zypper install fprintd```\n\n```nix: nix profile install nixpkgs#fprintd```"],["SEE ALSO","[fprintd](/man/fprintd)(1), [fprintd-enroll](/man/fprintd-enroll)(1), [fprintd-delete](/man/fprintd-delete)(1), [fprintd-list](/man/fprintd-list)(1)"]],"e":["fprintd","fprintd-enroll","fprintd-delete","fprintd-list"],"x":"fprintd-verify"},"frida-ps":{"n":"frida-ps","t":"List processes on local and remote devices using Frida","s":[["TAGLINE","List processes on local and remote devices using Frida"],["TLDR","**List** all processes on the local machine\n\n```frida-ps```\n\n**List** processes on a **USB-connected** device\n\n```frida-ps -U```\n\n**List** only running **applications** on a USB device\n\n```frida-ps -Ua```\n\n**List** all **installed** applications on a USB device (running or not)\n\n```frida-ps -Uai```\n\n**List** processes on a **specific device** by ID\n\n```frida-ps -D [device_id]```\n\n**Connect** to a remote frida-server on a specific **host**\n\n```frida-ps -H [192.168.1.100]```"],["SYNOPSIS","**frida-ps** [_options_]"],["PARAMETERS","**-a**, **--applications**\n> List only applications, not all system processes.\n\n**-i**, **--installed**\n> Include all installed applications (requires **-a**).\n\n**-U**, **--usb**\n> Connect to USB device.\n\n**-R**, **--remote**\n> Connect to remote frida-server.\n\n**-H** _HOST_, **--host** _HOST_\n> Connect to remote frida-server on HOST.\n\n**-D** _ID_, **--device** _ID_\n> Connect to device with the given ID.\n\n**--certificate** _CERTIFICATE_\n> Speak TLS with HOST, expecting CERTIFICATE.\n\n**--origin** _ORIGIN_\n> Connect to remote server with Origin header set to ORIGIN.\n\n**--token** _TOKEN_\n> Authenticate with HOST using TOKEN.\n\n**--keepalive-interval** _INTERVAL_\n> Set keepalive interval in seconds, or 0 to disable.\n\n**-O** _FILE_, **--options-file** _FILE_\n> Text file containing additional command-line options."],["DESCRIPTION","**frida-ps** is a command-line tool for listing processes, part of the Frida dynamic instrumentation toolkit. It functions similarly to the Unix **ps** command but is designed to work with both local and remote devices (USB-connected phones, remote frida-server instances). It can list all running processes or filter to only applications, including installed but not running ones."],["CAVEATS","To list processes on a remote or USB-connected device, **frida-server** must be running on that device with appropriate permissions. The **-i** flag requires **-a**; using **--installed** without **--applications** will report an error. On the local machine, listing processes of other users may require elevated privileges."],["HISTORY","**frida-ps** is included in the **frida-tools** package, installable via **pip install frida-tools**. Frida was created by **Ole Andre Vadla Ravnas** and publicly released in **2014**."],["SEE ALSO","[frida](/man/frida)(1), [frida-trace](/man/frida-trace)(1), [ps](/man/ps)(1)"]],"e":["frida","frida-trace","ps"],"x":"frida-ps"},"gh-rs":{"n":"gh-rs","t":"View GitHub repository rulesets (alias for gh ruleset)","s":[["TAGLINE","View GitHub repository rulesets (alias for gh ruleset)"],["TLDR","**List rulesets** for the current repository\n\n```gh rs list```\n\n**List rulesets** for a different repository, including inherited ones\n\n```gh rs list --repo [owner/repo] --parents```\n\n**List organization-wide** rulesets\n\n```gh rs list --org [org-name]```\n\n**View a specific ruleset** by ID\n\n```gh rs view [43]```\n\n**Open a ruleset** in the web browser\n\n```gh rs view [43] --web```\n\n**Check rules** that apply to the current branch\n\n```gh rs check```\n\n**Check rules** that apply to the default branch of another repository\n\n```gh rs check --default --repo [owner/repo]```"],["SYNOPSIS","**gh rs** _command_ [_flags_]"],["PARAMETERS","**list**\n> List rulesets for a repository or organization.\n\n**view** [_ruleset-id_]\n> View information about a ruleset. Prompts interactively if no ID is given.\n\n**check** [_branch_]\n> View rules that would apply to a given branch. Uses the current branch if none is given.\n\n**-R**, **--repo** _[HOST/]OWNER/REPO_\n> Select another repository instead of the current one."],["LIST FLAGS","**-L**, **--limit** _int_\n> Maximum number of rulesets to list (default 30).\n\n**-o**, **--org** _string_\n> List organization-wide rulesets. Requires the **admin:org** token scope.\n\n**-p**, **--parents**\n> Include rulesets configured at higher levels that also apply (default true).\n\n**-w**, **--web**\n> Open the list of rulesets in the web browser."],["VIEW/CHECK FLAGS","**-o**, **--org** _string_\n> Organization name, when the ID given to **view** is an organization-level ruleset.\n\n**--default**\n> With **check**, view rules on the repository's default branch.\n\n**-w**, **--web**\n> Open the ruleset or branch rules page in the web browser."],["DESCRIPTION","**gh rs** is the built-in alias for **gh ruleset**, part of the official **GitHub CLI**. Rulesets let repository and organization admins enforce rules (required reviews, status checks, branch naming, force-push restrictions, etc.) on branches and tags. This command group is read-only: it lists and inspects rulesets and shows which rules apply to a given branch, but does not create or edit rulesets."],["CAVEATS","Read-only: rulesets themselves are created and edited on GitHub.com or via the API, not through **gh ruleset**. Listing organization-wide rulesets with **--org** requires a token with the **admin:org** scope, obtainable via **gh auth refresh -s admin:org**."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-repo](/man/gh-repo)(1)"],["RESOURCES","```[Manual](https://cli.github.com/manual/gh_ruleset)```\n\n```[Source code](https://github.com/cli/cli)```"]],"e":["gh","gh-repo"],"x":"gh rs list"},"git-bug":{"n":"git-bug","t":"Distributed bug tracker embedded in git","s":[["TAGLINE","Distributed bug tracker embedded in git"],["TLDR","**Add a new bug**\n\n```git bug add```\n\n**List bugs**\n\n```git bug ls```\n\n**Show bug details**\n\n```git bug show [bug-id]```\n\n**Add comment**\n\n```git bug comment [bug-id]```\n\n**Push bugs to remote**\n\n```git bug push```\n\n**Launch the web UI**\n\n```git bug webui```\n\n**Open interactive terminal UI**\n\n```git bug termui```"],["SYNOPSIS","**git bug** _command_ [_options_]"],["PARAMETERS","**add** [**-t** _title_] [**-m** _message_]\n> Create a new bug.\n\n**ls**, **list**\n> List bugs.\n\n**show** _ID_\n> Show bug details.\n\n**comment** _ID_\n> Add comment to bug.\n\n**label** _ID_ _label_\n> Add or remove labels on a bug.\n\n**status** _ID_\n> Display or change the status (open/close) of a bug.\n\n**push** [_remote_]\n> Push bugs to a git remote.\n\n**pull** [_remote_]\n> Pull bugs from a git remote.\n\n**close** _ID_\n> Mark a bug as closed.\n\n**open** _ID_\n> Reopen a closed bug.\n\n**user**\n> List, create, or adopt identities.\n\n**webui**\n> Launch the web user interface.\n\n**termui**\n> Launch the terminal user interface.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git-bug** is a distributed bug tracker embedded in git. Bugs are stored as git objects, enabling offline bug management, distributed synchronization, and integration with git workflows.\n\nThe tool provides full issue tracking including titles, descriptions, comments, labels, and status. Bugs sync with git push/pull, enabling decentralized issue management without relying on external services like GitHub Issues or Jira.\n\nBugs are stored as git objects and can be managed entirely offline, syncing with collaborators through standard git push and pull operations."],["CAVEATS","Separate from GitHub/GitLab issues. Requires setup on collaborators' machines. Web UI available separately."],["HISTORY","git-bug was created by **Michael Muré** to provide truly distributed bug tracking that lives alongside code in git repositories."],["INSTALL","```pacman: sudo pacman -S git-bug```\n\n```zypper: sudo zypper install git-bug```\n\n```brew: brew install git-bug```\n\n```nix: nix profile install nixpkgs#git-bug```"],["SEE ALSO","[git](/man/git)(1), [gh](/man/gh)(1)"]],"e":["git","gh"],"x":"git bug add"},"git-imap-send":{"n":"git-imap-send","t":"Upload patches to IMAP drafts folder","s":[["TAGLINE","Upload patches to IMAP drafts folder"],["TLDR","**Send patches via IMAP**\n\n```git format-patch origin | git imap-send```\n\n**Send from file**\n\n```git imap-send < [patches.mbox]```"],["SYNOPSIS","**git imap-send** [_options_]"],["PARAMETERS","**--curl**\n> Use libcurl for IMAP.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-q**, **--quiet**\n> Quiet mode.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git imap-send** uploads patches to an IMAP drafts folder, enabling review and composition of patch emails in your email client before sending. It reads mbox-formatted patches from stdin and uploads them to the configured IMAP server.\n\nThis supports the email-driven patch submission workflow used by projects like the Linux kernel, where patches are reviewed on mailing lists rather than through web-based pull requests."],["CONFIGURATION","**~/.gitconfig**\n> IMAP server configuration including host, user, port, and SSL settings under the [imap] section."],["CAVEATS","Requires IMAP server configuration. SSL configuration may be complex. Alternative to git send-email."],["HISTORY","git imap-send was created for projects using **email-based patch submission**, allowing integration with standard email clients."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-send-email](/man/git-send-email)(1), [git-format-patch](/man/git-format-patch)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-imap-send)```"]],"e":["git-send-email","git-format-patch"],"x":"git format-patch origin | git imap-send"},"git-mailinfo":{"n":"git-mailinfo","t":"Extract patch from email message","s":[["TAGLINE","Extract patch from email message"],["TLDR","**Extract patch info**\n\n```git mailinfo [msg.txt] [patch.txt] < [email.txt]```\n\n**Keep subject prefix**\n\n```git mailinfo -k [msg.txt] [patch.txt] < [email.txt]```"],["SYNOPSIS","**git mailinfo** [_options_] _msg_ _patch_"],["PARAMETERS","_MSG_\n> Output file for commit message.\n\n_PATCH_\n> Output file for patch.\n\n**-k**\n> Keep subject line intact.\n\n**-b**\n> Strip bracketed strings from subject.\n\n**-u**\n> Encode as UTF-8.\n\n**--scissors**\n> Remove text before scissors line.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git mailinfo** extracts patch and commit information from email messages. It parses email headers and body, separating the commit message from the patch content, and handles encoding and author extraction.\n\nThe command is a plumbing tool used internally by `git am`. It processes the email format used in patch-based development workflows, such as those used by the Linux kernel project."],["CAVEATS","Plumbing command. Usually called by git am. Input must be proper email format."],["HISTORY","git mailinfo is part of **Git's** email patch workflow, supporting the patch-by-email development model used by the Linux kernel."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-am](/man/git-am)(1), [git-mailsplit](/man/git-mailsplit)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-mailinfo)```"]],"e":["git-am","git-mailsplit"],"x":"git mailinfo [msg.txt] [patch.txt] < [email.txt]"},"glab-issue":{"n":"glab-issue","t":"Manage GitLab issues from the command line","s":[["TAGLINE","Manage GitLab issues from the command line"],["TLDR","**List issues**\n\n```glab issue list```\n\n**Create issue**\n\n```glab issue create```\n\n**View issue**\n\n```glab issue view [number]```\n\n**Close issue**\n\n```glab issue close [number]```\n\n**Reopen issue**\n\n```glab issue reopen [number]```\n\n**Create with title**\n\n```glab issue create -t \"[title]\" -d \"[description]\"```"],["SYNOPSIS","**glab** **issue** _command_ [_options_]"],["SUBCOMMANDS","**list**\n> List issues.\n\n**create**\n> Create an issue.\n\n**view**\n> View an issue.\n\n**update**\n> Update an issue (title, description, labels, etc.).\n\n**close**\n> Close an issue.\n\n**reopen**\n> Reopen an issue.\n\n**note**\n> Add a comment/note to an issue.\n\n**subscribe**\n> Subscribe to an issue.\n\n**unsubscribe**\n> Unsubscribe from an issue.\n\n**board**\n> View or manage issue boards.\n\n**delete**\n> Delete an issue."],["PARAMETERS","**-t**, **--title** _title_\n> Issue title.\n\n**-d**, **--description** _text_\n> Issue description.\n\n**-l**, **--label** _labels_\n> Add labels (comma-separated).\n\n**-a**, **--assignee** _users_\n> Assign users (comma-separated usernames).\n\n**-m**, **--milestone** _name_\n> Assign milestone.\n\n**-c**, **--confidential**\n> Create as a confidential issue.\n\n**-w**, **--web**\n> Open in browser."],["DESCRIPTION","**glab issue** manages GitLab issues from the command line. It provides subcommands for creating, viewing, listing, closing, and commenting on issues without leaving the terminal."],["INSTALL","```apt: sudo apt install glab```\n\n```dnf: sudo dnf install glab```\n\n```pacman: sudo pacman -S glab```\n\n```apk: sudo apk add glab```\n\n```zypper: sudo zypper install glab```\n\n```brew: brew install glab```\n\n```nix: nix profile install nixpkgs#glab```"],["SEE ALSO","[glab](/man/glab)(1), [glab-mr](/man/glab-mr)(1)"]],"e":["glab","glab-mr"],"x":"glab issue list"},"hackernews-tui":{"n":"hackernews-tui","t":"Terminal UI to browse Hacker News","s":[["TAGLINE","Terminal UI to browse Hacker News"],["TLDR","**Launch hackernews-tui** to browse front page stories\n\n```hackernews-tui```\n\n**Browse a specific page** of stories\n\n```hackernews-tui --page [page_type]```"],["SYNOPSIS","**hackernews-tui** [_options_]"],["DESCRIPTION","**hackernews-tui** is a fast and customizable terminal application for browsing Hacker News. It provides a full TUI with **vim-like key bindings** for navigating stories, comments, and discussions. The application includes a built-in reader view for viewing articles directly in the terminal without a web browser.\n\nIt uses the **HN Algolia API** and **HN Official API** to fetch data, supporting front page stories, newest stories, Ask HN, Show HN, and job listings. Stories can be opened in an external browser, and comments are displayed in a threaded view."],["KEY BINDINGS","**j/k** — Move down/up\n**Enter** — Open story/comment\n**o** — Open link in browser\n**b** — Go back\n**/** — Search\n**q** — Quit"],["CONFIGURATION","Configuration is stored in **$XDG_CONFIG_HOME/hackernews-tui/config.toml**. Options include custom key bindings, color themes, and the external browser command."],["CAVEATS","Requires an internet connection to fetch stories. The reader view may not render all web pages perfectly. Rate limiting by the HN API may affect heavy usage."],["HISTORY","**hackernews-tui** was created by **aome510** and is written in **Rust** using the **Cursive** TUI library. It was first released in **2021** to provide a native terminal experience for Hacker News readers."],["INSTALL","```nix: nix profile install nixpkgs#hackernews-tui```"],["SEE ALSO","[circumflex](/man/circumflex)(1), [curl](/man/curl)(1)"]],"e":["circumflex","curl"],"x":"hackernews-tui"},"ibmcloud-config":{"n":"ibmcloud-config","t":"manages IBM Cloud CLI configuration","s":[["TAGLINE","manages IBM Cloud CLI configuration"],["TLDR","**Show configuration**\n\n```ibmcloud config --list```\n\n**Set HTTP timeout**\n\n```ibmcloud config --http-timeout [60]```\n\n**Set trace**\n\n```ibmcloud config --trace [true]```\n\n**Set color output**\n\n```ibmcloud config --color [true]```\n\n**Set locale**\n\n```ibmcloud config --locale [en_US]```"],["SYNOPSIS","**ibmcloud config** [_options_]"],["PARAMETERS","**--list**\n> Show all settings.\n\n**--http-timeout** _SEC_\n> HTTP timeout.\n\n**--trace** _BOOL_\n> Enable tracing.\n\n**--color** _BOOL_\n> Color output.\n\n**--locale** _LOCALE_\n> Output locale.\n\n**--check-version** _BOOL_\n> Version check.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ibmcloud config** manages IBM Cloud CLI configuration. It sets options for timeouts, output formatting, and behavior.\n\nThe command stores settings for the current user. Settings persist across sessions and affect all ibmcloud commands."],["CAVEATS","User-specific settings. Some settings require restart. Part of ibmcloud CLI."],["HISTORY","ibmcloud config is part of the **IBM Cloud** CLI configuration system."],["SEE ALSO","[ibmcloud](/man/ibmcloud)(1), [ibmcloud-login](/man/ibmcloud-login)(1)"]],"e":["ibmcloud","ibmcloud-login"],"x":"ibmcloud config --list"},"ibmcloud-target":{"n":"ibmcloud-target","t":"sets or displays the targeted account, region, resource group, and Cloud","s":[["TAGLINE","sets or displays the targeted account, region, resource group, and Cloud"],["TLDR","**Show current target**\n\n```ibmcloud target```\n\n**Set target region**\n\n```ibmcloud target -r [us-south]```\n\n**Set target resource group**\n\n```ibmcloud target -g [resource-group]```\n\n**Set Cloud Foundry org and space**\n\n```ibmcloud target -o [org] -s [space]```\n\n**Set target account**\n\n```ibmcloud target -c [account-id]```\n\n**Unset resource group**\n\n```ibmcloud target --unset-resource-group```"],["SYNOPSIS","**ibmcloud** **target** [_options_]"],["PARAMETERS","**-r** _region_\n> Target region.\n\n**-g** _group_\n> Target resource group.\n\n**-c** _account_\n> Target account ID.\n\n**-o** _org_\n> Target Cloud Foundry organization.\n\n**-s** _space_\n> Target Cloud Foundry space.\n\n**--cf**\n> Target a Cloud Foundry environment and auto-select org/space.\n\n**--cf-api** _URL_\n> Target a specific Cloud Foundry API endpoint.\n\n**--unset-resource-group**\n> Clear the targeted resource group.\n\n**--output** _FORMAT_\n> Output format (e.g. `json`)."],["DESCRIPTION","**ibmcloud target** sets or displays the targeted account, region, resource group, and Cloud Foundry org/space. Subsequent CLI commands operate within the targeted context. Run without options to display current target."],["SEE ALSO","[ibmcloud](/man/ibmcloud)(1), [ibmcloud-login](/man/ibmcloud-login)(1)"]],"e":["ibmcloud","ibmcloud-login"],"x":"ibmcloud target"},"ii":{"n":"ii","t":"minimalist filesystem-based IRC client","s":[["TAGLINE","minimalist filesystem-based IRC client"],["TLDR","**Connect to IRC server**\n\n```ii -s [irc.libera.chat] -n [nickname]```\n\n**Connect with port**\n\n```ii -s [irc.libera.chat] -p [6697] -n [nickname]```\n\n**Specify IRC directory**\n\n```ii -s [server] -i [~/.irc] -n [nickname]```\n\n**Join channel**\n\n```echo \"/j #channel\" > ~/irc/server/in```\n\n**Send message**\n\n```echo \"Hello world\" > ~/irc/server/#channel/in```"],["SYNOPSIS","**ii** [_options_]"],["PARAMETERS","**-s** _server_\n> IRC server address.\n\n**-p** _port_\n> Server port (default 6667).\n\n**-n** _nickname_\n> IRC nickname (defaults to $USER).\n\n**-k** _env_var_\n> Environment variable that holds the IRC password (avoids exposing it on the command line).\n\n**-f** _fullname_\n> Real name (GECOS field).\n\n**-i** _prefix_\n> IRC directory prefix (default ~/irc).\n\n**-t**\n> Connect using TLS.\n\n**-v**\n> Do not verify the TLS certificate.\n\n**-4**\n> Only connect over IPv4.\n\n**-6**\n> Only connect over IPv6.\n\n**-u** _socket_\n> Connect to a UNIX domain socket instead of a network server."],["DESCRIPTION","**ii** (IRC It) is a minimalist filesystem-based IRC client. It creates a directory structure where each server and channel is a directory with **in** (write to send) and **out** (read for messages) files.\n\nThis design follows the Unix philosophy, allowing IRC interaction through standard file operations and shell scripts. It can be combined with tools like tail, cat, and custom scripts."],["DIRECTORY STRUCTURE","```\n~/irc/\n└── irc.libera.chat/\n    ├── in          # Send commands\n    ├── out         # Server messages\n    └── #channel/\n        ├── in      # Send to channel\n        └── out     # Channel messages\n```"],["USAGE","```bash"],["Monitor channel","tail -f ~/irc/server/#channel/out"],["Join channel","echo \"/j #linux\" > ~/irc/server/in"],["Send message","echo \"Hello!\" > ~/irc/server/#channel/in\n```"],["CAVEATS","No built-in interface; requires scripts or tools. No SSL support without wrapper. Limited feature set. Manual channel/nick management."],["HISTORY","ii was created as part of the **suckless** project, following their philosophy of minimal, filesystem-based tools. It demonstrates IRC can work with standard Unix file operations."],["INSTALL","```apt: sudo apt install ii```\n\n```dnf: sudo dnf install ii```\n\n```apk: sudo apk add ii```\n\n```brew: brew install ii```\n\n```nix: nix profile install nixpkgs#ii```"],["SEE ALSO","[irssi](/man/irssi)(1), [weechat](/man/weechat)(1), [sic](/man/sic)(1)"]],"e":["irssi","weechat","sic"],"x":"ii -s [irc.libera.chat] -n [nickname]"},"impacket-rdp_check":{"n":"impacket-rdp_check","t":"validates whether provided credentials allow RDP access to a Windows system","s":[["TAGLINE","validates whether provided credentials allow RDP access to a Windows system"],["TLDR","**Check if RDP credentials are valid**\n\n```impacket-rdp_check '[domain]/[user]:[password]@[192.168.1.100]'```\n\n**Check RDP access using NTLM hash**\n\n```impacket-rdp_check -hashes ':[nthash]' '[domain]/[user]@[192.168.1.100]'```\n\n**Check RDP on a non-standard port**\n\n```impacket-rdp_check '[domain]/[user]:[password]@[192.168.1.100]:3390'```"],["SYNOPSIS","**impacket-rdp_check** [_-h_] [_-hashes LMHASH:NTHASH_] _target_"],["PARAMETERS","**-hashes** _LMHASH:NTHASH_\n> Use NTLM hashes for authentication instead of password"],["DESCRIPTION","**impacket-rdp_check** validates whether provided credentials allow RDP (Remote Desktop Protocol) access to a Windows system. It attempts to authenticate via RDP without establishing a full session, making it useful for credential validation during penetration tests.\n\nThe tool performs the initial RDP handshake and authentication exchange, reporting whether the credentials are accepted by the target system."],["CAVEATS","Only validates credentials; does not establish a full RDP session. Network Level Authentication (NLA) settings on the target affect behavior. RDP must be enabled and accessible on the target. May trigger account lockout policies if used repeatedly with incorrect credentials."],["HISTORY","Part of the **Impacket** library by SecureAuth. Useful for validating credentials against RDP services without the overhead of full graphical session establishment."],["INSTALL","```pacman: sudo pacman -S impacket```"],["SEE ALSO","[xfreerdp](/man/xfreerdp)(1), [rdesktop](/man/rdesktop)(1), [nmap](/man/nmap)(1)"]],"e":["xfreerdp","rdesktop","nmap"],"x":"impacket-rdp_check '[domain]/[user]:[password]@[192.168.1.100]'"},"lazykiq":{"n":"lazykiq","t":"TUI for inspecting Sidekiq activity in the terminal","s":[["TAGLINE","TUI for inspecting Sidekiq activity in the terminal"],["TLDR","**Launch the Sidekiq monitor**\n\n```lazykiq```\n\n**Connect to a specific Redis URL**\n\n```lazykiq --redis [redis://localhost:6379/0]```"],["SYNOPSIS","**lazykiq** [_options_]"],["PARAMETERS","**--redis** _URL_\n> Redis connection URL (e.g., redis://localhost:6379/0)."],["DESCRIPTION","**lazykiq** is a terminal UI for inspecting Sidekiq activity. It connects via Redis to display Sidekiq processes, running jobs, queue contents, job lists, and error backtraces.\n\nFeatures include vim-style navigation, paging and list filtering, quick view switching between dashboards, queues, and metrics. Job details show payload, timestamps, retry counts, and exception traces. You can view retried, busy, scheduled, and dead jobs. Supports Nerd Fonts for enhanced display."],["CAVEATS","Requires access to the Redis instance used by Sidekiq. Read-only monitoring tool -- does not modify Sidekiq state."],["HISTORY","**lazykiq** was created by **kpumuk** and is written in **Go**."],["SEE ALSO","[lazydocker](/man/lazydocker)(1), [lazygit](/man/lazygit)(1), [redis-cli](/man/redis-cli)(1)"]],"e":["lazydocker","lazygit","redis-cli"],"x":"lazykiq"},"litecli":{"n":"litecli","t":"SQLite CLI with auto-completion","s":[["TAGLINE","SQLite CLI with auto-completion"],["TLDR","**Connect to database**\n\n```litecli [database.db]```\n\n**Create new database**\n\n```litecli [new.db]```\n\n**Execute query**\n\n```litecli [database.db] -e \"[SELECT * FROM table]\"```\n\n**Use specific output format**\n\n```litecli [database.db] --table```\n\n**Connect to in-memory database**\n\n```litecli :memory:```\n\n**Run SQL file**\n\n```litecli [database.db] < [script.sql]```"],["SYNOPSIS","**litecli** [_options_] [_database_]"],["PARAMETERS","_DATABASE_\n> SQLite database file path.\n\n**-e**, **--execute** _QUERY_\n> Execute query and exit.\n\n**--table**\n> Output as table format.\n\n**--csv**\n> Output as CSV.\n\n**--auto-vertical-output**\n> Automatically switch to vertical output for wide rows.\n\n**-l**, **--list-dsn**\n> List configured DSNs.\n\n**--llm-api-key** _KEY_\n> API key for AI-assisted SQL features.\n\n**--help**\n> Display help information."],["DESCRIPTION","**litecli** is a SQLite CLI with auto-completion. It provides syntax highlighting and intelligent suggestions.\n\nThe tool offers a better interactive experience than sqlite3 with multiline editing and history."],["CAVEATS","Python-based. Requires pip install. For SQLite databases only."],["HISTORY","litecli was inspired by **pgcli** and **mycli**, bringing similar enhanced CLI features to SQLite."],["INSTALL","```dnf: sudo dnf install litecli```\n\n```apk: sudo apk add litecli```\n\n```brew: brew install litecli```\n\n```nix: nix profile install nixpkgs#litecli```"],["SEE ALSO","[sqlite3](/man/sqlite3)(1), [mycli](/man/mycli)(1), [pgcli](/man/pgcli)(1)"]],"e":["sqlite3","mycli","pgcli"],"x":"litecli [database.db]"},"lxterminal":{"n":"lxterminal","t":"lightweight terminal emulator designed for the LXDE desktop environment","s":[["TAGLINE","lightweight terminal emulator designed for the LXDE desktop environment"],["TLDR","Open **terminal** window\n\n```lxterminal```\n\nRun **command** and exit\n\n```lxterminal -e \"[command]\"```\n\nOpen with **multiple tabs**\n\n```lxterminal --tabs=[tab1,tab2,tab3]```\n\nOpen with **title**\n\n```lxterminal --title=[title]```\n\nSet **working directory**\n\n```lxterminal --working-directory=[path/to/dir]```"],["SYNOPSIS","**lxterminal** [_options_]"],["DESCRIPTION","**lxterminal** is a lightweight terminal emulator designed for the LXDE desktop environment. It supports tabs, customizable appearance, and command execution."],["PARAMETERS","**-e, --command COMMAND**\n> Execute command in terminal\n\n**--tabs=TAB1,TAB2,...**\n> Open with named tabs\n\n**--title=TITLE**\n> Set window title\n\n**--working-directory=DIR**\n> Set working directory\n\n**--geometry=WxH**\n> Window geometry\n\n**-?, --help**\n> Display help information"],["CAVEATS","Part of LXDE desktop. Lightweight alternative to gnome-terminal or konsole."],["INSTALL","```apt: sudo apt install lxterminal```\n\n```dnf: sudo dnf install lxterminal```\n\n```pacman: sudo pacman -S lxterminal```\n\n```apk: sudo apk add lxterminal```\n\n```zypper: sudo zypper install lxterminal```\n\n```nix: nix profile install nixpkgs#lxterminal```"],["SEE ALSO","[xterm](/man/xterm)(1), [gnome-terminal](/man/gnome-terminal)(1), [konsole](/man/konsole)(1)"]],"e":["xterm","gnome-terminal","konsole"],"x":"lxterminal"},"mozillavpn":{"n":"mozillavpn","t":"mozilla's VPN client","s":[["TAGLINE","mozilla's VPN client"],["TLDR","**Connect to VPN**\n\n```mozillavpn activate```\n\n**Disconnect from VPN**\n\n```mozillavpn deactivate```\n\n**Check connection status**\n\n```mozillavpn status```\n\n**Select server location**\n\n```mozillavpn select [us-ny]```\n\n**List available servers**\n\n```mozillavpn servers```\n\n**Login to account**\n\n```mozillavpn login```"],["SYNOPSIS","**mozillavpn** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> Action to perform.\n\n**activate**\n> Connect to VPN.\n\n**deactivate**\n> Disconnect from VPN.\n\n**status**\n> Show connection status.\n\n**select** _SERVER_\n> Choose server location.\n\n**servers**\n> List available servers.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mozillavpn** is Mozilla's VPN client. It provides secure network connection through WireGuard protocol.\n\nThe tool encrypts internet traffic. Offers multiple server locations worldwide."],["CAVEATS","Requires subscription. WireGuard kernel module. Limited to 5 devices."],["HISTORY","mozillavpn was launched by **Mozilla** in 2020 as a privacy-focused VPN service using WireGuard technology."],["INSTALL","```nix: nix profile install nixpkgs#mozillavpn```"],["SEE ALSO","[wg](/man/wg)(1), [openvpn](/man/openvpn)(1)"]],"e":["wg","openvpn"],"x":"mozillavpn activate"},"mprime":{"n":"mprime","t":"great Internet Mersenne Prime Search client","s":[["TAGLINE","great Internet Mersenne Prime Search client"],["TLDR","**Start Prime95 (interactive)**\n\n```mprime```\n\n**Run torture test**\n\n```mprime -t```\n\n**Run specific test type**\n\n```mprime -t -W[1]```\n\n**Run as daemon**\n\n```mprime -d```\n\n**Set worker threads**\n\n```mprime -t -w[4]```"],["SYNOPSIS","**mprime** [_options_]"],["PARAMETERS","**-t**\n> Torture test mode.\n\n**-d**\n> Run as daemon.\n\n**-w** _n_\n> Number of workers.\n\n**-W** _type_\n> Torture test type.\n\n**-m**\n> Menu mode.\n\n**-v**\n> Print version."],["DESCRIPTION","**mprime** (Prime95 for Linux) is the Great Internet Mersenne Prime Search (GIMPS) client. It searches for Mersenne primes and can also be used for CPU stress testing and benchmarking.\n\nThe torture test mode is popular for testing system stability, especially after overclocking."],["TORTURE TEST TYPES","```\n1 - Small FFTs (tests L2 cache)\n2 - In-place FFTs (tests RAM)\n3 - Large FFTs (tests all)\n4 - Blend (mixed test)\n```"],["CONFIGURATION","**~/primenet.ini**\n> PrimeNet server communication settings and account configuration.\n\n**~/local.txt**\n> Local work preferences and computation settings."],["CAVEATS","Extremely CPU intensive. Generates significant heat. Not suitable for laptops. May require GIMPS account for prime search."],["HISTORY","Prime95/mprime was created by **George Woltman** in **1996** for the GIMPS project. It has discovered several record-breaking Mersenne primes."],["INSTALL","```nix: nix profile install nixpkgs#mprime```"],["SEE ALSO","[stress](/man/stress)(1), [stress-ng](/man/stress-ng)(1)"]],"e":["stress","stress-ng"],"x":"mprime"},"nativefier":{"n":"nativefier","t":"wraps websites as desktop applications using Electron","s":[["TAGLINE","wraps websites as desktop applications using Electron"],["TLDR","**Create app from website**\n\n```nativefier \"[https://example.com]\"```\n\n**Create with custom name**\n\n```nativefier --name \"[App Name]\" \"[https://example.com]\"```\n\n**Create with custom icon**\n\n```nativefier --icon [icon.png] \"[https://example.com]\"```\n\n**Create in specific directory**\n\n```nativefier \"[https://example.com]\" [/output/dir]```\n\n**Create with tray icon**\n\n```nativefier --tray \"[https://example.com]\"```\n\n**Create maximized window**\n\n```nativefier --maximize \"[https://example.com]\"```\n\n**Create single instance app**\n\n```nativefier --single-instance \"[https://example.com]\"```\n\n**Create with injected CSS**\n\n```nativefier --inject [style.css] \"[https://example.com]\"```"],["SYNOPSIS","**nativefier** [_--name name_] [_--icon path_] [_--tray_] [_options_] _url_ [_output_dir_]"],["PARAMETERS","**--name** _NAME_\n> Application name.\n\n**--icon** _PATH_\n> Custom icon file.\n\n**-p**, **--platform** _OS_\n> Target platform (mac, windows, linux).\n\n**-a**, **--arch** _ARCH_\n> Target architecture.\n\n**--tray**\n> Add system tray icon.\n\n**--maximize**\n> Start maximized.\n\n**--single-instance**\n> Only one instance allowed.\n\n**--inject** _FILE_\n> Inject CSS or JavaScript.\n\n**--user-agent** _STRING_\n> Custom user agent.\n\n**--internal-urls** _REGEX_\n> URLs to open internally.\n\n**--file-download-options** _JSON_\n> Download behavior settings.\n\n**--disable-context-menu**\n> Disable right-click menu.\n\n**--flash**\n> Enable Flash plugin.\n\n**--widevine**\n> Enable Widevine DRM."],["DESCRIPTION","**nativefier** wraps websites as desktop applications using Electron. The result is a standalone app that behaves like a native application.\n\nApplications get their own window, dock/taskbar icon, and can run independently of browsers. This is useful for web apps that benefit from dedicated window management.\n\nCustom icons, names, and window behavior make apps feel native. Tray mode minimizes to system tray. Single instance prevents multiple copies.\n\nCSS and JavaScript injection modifies the wrapped site. This can customize appearance, add features, or remove unwanted elements.\n\nInternal URL patterns control which links open in the app versus the default browser. This keeps the app focused on its core functionality.\n\nPlatform targeting creates apps for Windows, macOS, or Linux from any development machine."],["CAVEATS","Electron apps are large (100MB+). Security depends on wrapped site. Flash support deprecated. Some sites detect and block embedding. Project is in maintenance mode."],["HISTORY","**nativefier** was created by **Jia Hao Gao** around **2015** to easily create desktop apps from web pages. It became popular for wrapping services like Slack, WhatsApp Web, and internal tools. The project is now in maintenance mode with limited updates."],["SEE ALSO","[electron](/man/electron)(1), [pwa](/man/pwa)(1)"]],"e":["electron","pwa"],"x":"nativefier \"[https://example.com]\""},"nextflow":{"n":"nextflow","t":"Data-driven workflow orchestration for scalable scientific pipelines","s":[["TAGLINE","Data-driven workflow orchestration for scalable scientific pipelines"],["TLDR","**Run pipeline**\n\n```nextflow run [pipeline.nf]```\n\n**Run with parameters**\n\n```nextflow run [pipeline.nf] --input [data.txt]```\n\n**Run pipeline from GitHub**\n\n```nextflow run [user/repo]```\n\n**Resume failed run from checkpoint**\n\n```nextflow run [pipeline.nf] -resume```\n\n**Run with Docker containers**\n\n```nextflow run [pipeline.nf] -with-docker```\n\n**Run with a specific config profile**\n\n```nextflow run [pipeline.nf] -profile [docker]```\n\n**Generate execution report and timeline**\n\n```nextflow run [pipeline.nf] -with-report -with-timeline```\n\n**Show pipeline info**\n\n```nextflow info [pipeline]```\n\n**List previous runs**\n\n```nextflow log```\n\n**Clean up cached work directories**\n\n```nextflow clean -f```"],["SYNOPSIS","**nextflow** [_options_] _command_ [_arguments_]"],["PARAMETERS","**run** _PIPELINE_\n> Execute a pipeline project (local path, script, or GitHub repo).\n\n**pull** _PROJECT_\n> Download or update a remote project.\n\n**info** [_PROJECT_]\n> Show system or pipeline info.\n\n**log**\n> Show history of executions.\n\n**clean**\n> Clean up work directories from past executions.\n\n**drop** _PROJECT_\n> Delete a local project cache.\n\n**list**\n> List downloaded pipeline projects.\n\n**-resume** [_SESSION_]\n> Resume execution from cached results; optionally specify session ID.\n\n**-with-docker** [_IMAGE_]\n> Enable Docker container execution.\n\n**-with-podman** [_IMAGE_]\n> Enable Podman container execution.\n\n**-with-singularity** [_IMAGE_]\n> Enable Singularity container execution.\n\n**-with-conda** [_ENV_]\n> Enable Conda environment management.\n\n**-profile** _NAME_\n> Select a configuration profile defined in nextflow.config.\n\n**-c** _FILE_, **-config** _FILE_\n> Use an alternative configuration file.\n\n**-params-file** _FILE_\n> Load pipeline parameters from JSON/YAML file.\n\n**-w** _DIR_, **-work-dir** _DIR_\n> Directory for intermediate task files (default: ./work).\n\n**-with-report** [_FILE_]\n> Generate an HTML execution report.\n\n**-with-timeline** [_FILE_]\n> Generate a timeline HTML file.\n\n**-with-trace** [_FILE_]\n> Generate an execution trace file.\n\n**-with-dag** [_FILE_]\n> Generate a workflow DAG diagram.\n\n**-bg**\n> Run in background.\n\n**-r** _REV_\n> Use a specific Git revision/branch/tag.\n\n**-h**, **-help**\n> Display help information."],["DESCRIPTION","**nextflow** is a reactive workflow framework and DSL for writing data-intensive computational pipelines. It enables the rapid composition of parallel and distributed pipelines using isolated processes connected by dataflow channels.\n\nNextflow abstracts execution from pipeline logic, allowing the same workflow to run locally, on HPC clusters (SLURM, SGE, PBS, LSF), or on cloud platforms (AWS Batch, Azure Batch, Google Cloud, Kubernetes). It provides built-in support for containers (Docker, Singularity, Podman) and package managers (Conda), and uses content-addressable caching to enable safe resumption of failed runs."],["CAVEATS","Requires Java 11 or later. Workflow scripts use a Groovy-based DSL (DSL2 is the current standard). The **work** directory can grow large across runs; use `nextflow clean` to reclaim space. Pipelines pulled from GitHub are cached in `~/.nextflow/assets/`."],["HISTORY","Nextflow was created by **Paolo Di Tommaso** at the **Centre for Genomic Regulation (CRG)** in Barcelona, with initial public release in **2013**. It was designed for reproducible, scalable scientific workflows, initially in bioinformatics. The project is now maintained by **Seqera Labs** and underpins the **nf-core** community of curated pipelines."],["INSTALL","```brew: brew install nextflow```\n\n```nix: nix profile install nixpkgs#nextflow```"],["SEE ALSO","[snakemake](/man/snakemake)(1), [cwl-runner](/man/cwl-runner)(1), [luigi](/man/luigi)(1)"]],"e":["snakemake","cwl-runner","luigi"],"x":"nextflow run [pipeline.nf]"},"ocamlc":{"n":"ocamlc","t":"OCaml bytecode compiler","s":[["TAGLINE","OCaml bytecode compiler"],["TLDR","**Compile to bytecode**\n\n```ocamlc -o [program] [source.ml]```\n\n**Compile multiple files**\n\n```ocamlc -o [program] [file1.ml] [file2.ml]```\n\n**Compile with library**\n\n```ocamlc -I +[library] [library].cma [source.ml] -o [program]```\n\n**Compile to object file**\n\n```ocamlc -c [source.ml]```\n\n**Compile with debug info**\n\n```ocamlc -g -o [program] [source.ml]```"],["SYNOPSIS","**ocamlc** [_options_] _files_..."],["PARAMETERS","**-o** _file_\n> Output filename.\n\n**-c**\n> Compile only (no link).\n\n**-I** _dir_\n> Add include directory.\n\n**-g**\n> Include debug info.\n\n**-i**\n> Print inferred types.\n\n**-a**\n> Create library archive.\n\n**-linkall**\n> Link all modules."],["DESCRIPTION","**ocamlc** is the OCaml bytecode compiler. It compiles OCaml source files to bytecode that runs on the OCaml virtual machine.\n\nFor native code compilation with better performance, use ocamlopt instead."],["FILE EXTENSIONS","```\n.ml   - Implementation\n.mli  - Interface\n.cmo  - Bytecode object\n.cma  - Bytecode library\n.cmi  - Compiled interface\n```"],["EXAMPLE","```bash"],["Compile simple program","ocamlc -o hello hello.ml"],["With standard library","ocamlc -I +str str.cma program.ml -o program\n```"],["CAVEATS","Bytecode slower than native. Link order matters. Use ocamlopt for production."],["HISTORY","OCaml was developed at **INRIA** (French computing research institute), evolving from Caml Light. The bytecode compiler has been part of OCaml since its creation."],["INSTALL","```apt: sudo apt install ocaml```\n\n```dnf: sudo dnf install ocaml```\n\n```pacman: sudo pacman -S ocaml```\n\n```apk: sudo apk add ocaml5```\n\n```zypper: sudo zypper install ocaml```\n\n```brew: brew install ocaml```\n\n```nix: nix profile install nixpkgs#ocaml```"],["SEE ALSO","[ocamlopt](/man/ocamlopt)(1), [dune](/man/dune)(1), [opam](/man/opam)(1)"]],"e":["ocamlopt","dune","opam"],"x":"ocamlc -o [program] [source.ml]"},"opencode-debug":{"n":"opencode-debug","t":"Debug OpenCode connections and logging","s":[["TAGLINE","Debug OpenCode connections and logging"],["TLDR","**Enable** debug logging\n\n```opencode debug```\n\n**Debug** OAuth connection for MCP servers\n\n```opencode debug mcp```"],["SYNOPSIS","**opencode debug** [_options_]"],["DESCRIPTION","**opencode debug** enables debug logging and provides diagnostics for OpenCode. It can diagnose OAuth connection issues with MCP (Model Context Protocol) servers and output verbose logs for troubleshooting configuration or connectivity problems."],["INSTALL","```pacman: sudo pacman -S opencode```\n\n```brew: brew install opencode```\n\n```nix: nix profile install nixpkgs#opencode```"],["SEE ALSO","[opencode](/man/opencode)(1), [opencode-mcp](/man/opencode-mcp)(1)"]],"e":["opencode","opencode-mcp"],"x":"opencode debug"},"pamundice":{"n":"pamundice","t":"reassembles tiled images","s":[["TAGLINE","reassembles tiled images"],["TLDR","**Reassemble tiled images**\n\n```pamundice -across=[4] -down=[3] -listfile=[tiles.txt] > [output.pam]```\n\n**With naming pattern**\n\n```pamundice -across=[4] -down=[3] tile%02d_%02d.pam > [output.pam]```"],["SYNOPSIS","**pamundice** [_options_] [_input_pattern_]"],["PARAMETERS","**-across** _N_\n> Tiles across.\n\n**-down** _N_\n> Tiles down.\n\n**-listfile** _FILE_\n> File listing tile names.\n\n**-hoverlap** _PIXELS_\n> Horizontal overlap.\n\n**-voverlap** _PIXELS_\n> Vertical overlap."],["DESCRIPTION","**pamundice** reassembles tiled images. Reverse of pamdice operation.\n\nThe tool combines image tiles. Part of Netpbm."],["CAVEATS","Part of Netpbm. Tiles must match. Overlap handling."],["HISTORY","pamundice is part of **Netpbm** for reassembling tiled images."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamdice](/man/pamdice)(1), [pnmcat](/man/pnmcat)(1), [netpbm](/man/netpbm)(1)"]],"e":["pamdice","pnmcat","netpbm"],"x":"pamundice -across=[4] -down=[3] -listfile=[tiles.txt] > [output.pam]"},"pct-mount":{"n":"pct-mount","t":"mounts a stopped container's filesystem on the Proxmox host","s":[["TAGLINE","mounts a stopped container's filesystem on the Proxmox host"],["TLDR","**Mount** container filesystem\n\n```pct mount 100```"],["SYNOPSIS","**pct mount** _vmid_"],["DESCRIPTION","**pct mount** mounts a stopped container's filesystem on the Proxmox host. This allows direct access to container files for maintenance, recovery, or inspection without starting the container."],["PARAMETERS","**vmid**\n> The numeric ID of the container"],["CAVEATS","The container must be stopped to mount its filesystem. The mount point is typically at /var/lib/lxc/VMID/rootfs. Always unmount with **pct unmount** before starting the container."],["HISTORY","**pct mount** is part of the **Proxmox VE** virtualization platform for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-unmount](/man/pct-unmount)(1)"]],"e":["pct","pct-unmount"],"x":"pct mount 100"},"phpenmod":{"n":"phpenmod","t":"enables PHP extensions on Debian-based systems by creating symbolic","s":[["TAGLINE","enables PHP extensions on Debian-based systems by creating symbolic"],["TLDR","**Enable** extension for all PHP versions and SAPIs\n\n```sudo phpenmod [extension]```\n\nEnable extension for **specific version** and SAPI\n\n```sudo phpenmod -v [7.4] -s [cli] [extension]```"],["SYNOPSIS","**phpenmod** [**-v** _version_] [**-s** _sapi_] _module_"],["PARAMETERS","**-v _version_**\n> PHP version (e.g., 7.4, 8.0, 8.1)\n\n**-s _sapi_**\n> SAPI type (cli, fpm, apache2, cgi)\n\n**module**\n> Extension name to enable"],["DESCRIPTION","**phpenmod** enables PHP extensions on Debian-based systems by creating symbolic links in the conf.d directory pointing to module configuration files. Without version or SAPI specified, it enables the extension for all installed PHP versions and all SAPIs.\n\nThe command creates symlinks in /etc/php/VERSION/SAPI/conf.d/ that point to module configuration files in mods-available."],["CAVEATS","Debian/Ubuntu specific tool. Requires root privileges. Changes take effect after restarting PHP-FPM or Apache. Extension must be installed before it can be enabled."],["HISTORY","**phpenmod** is part of the **php-defaults** package maintained by the Debian PHP team. It provides a standardized way to manage PHP extensions, similar to how a2enmod/a2dismod manage Apache modules."],["SEE ALSO","[phpdismod](/man/phpdismod)(8), [phpquery](/man/phpquery)(8), [php](/man/php)(1)"]],"e":["phpdismod","phpquery","php"],"x":"sudo phpenmod [extension]"},"pigz":{"n":"pigz","t":"compresses files using multiple processors","s":[["TAGLINE","compresses files using multiple processors"],["TLDR","**Compress file using multiple processors**\n\n```pigz [file]```\n\n**Decompress file**\n\n```pigz -d [file.gz]```\n\n**Compress with specific compression level**\n\n```pigz -[9] [file]```\n\n**Compress keeping original file**\n\n```pigz -k [file]```\n\n**Compress with specific number of threads**\n\n```pigz -p [4] [file]```\n\n**Compress to stdout**\n\n```pigz -c [file] > [file.gz]```\n\n**Compress files recursively in a directory**\n\n```pigz -r [directory/]```\n\n**Test compressed file integrity**\n\n```pigz -t [file.gz]```"],["SYNOPSIS","**pigz** [_-dkcp_] [_-# level_] [_-p threads_] [_files_]"],["PARAMETERS","**-d**, **--decompress**\n> Decompress.\n\n**-k**, **--keep**\n> Keep original file.\n\n**-c**, **--stdout**\n> Write to stdout.\n\n**-p** _NUM_, **--processes** _NUM_\n> Number of compression threads.\n\n**-#** (0-9, 11)\n> Compression level (6 default, 11 = zopfli).\n\n**-f**, **--force**\n> Force compression even if file exists.\n\n**-r**, **--recursive**\n> Process directories recursively.\n\n**-t**, **--test**\n> Test compressed file integrity.\n\n**-l**, **--list**\n> List compression info.\n\n**-n**, **--no-name**\n> Don't store original name/time.\n\n**-N**, **--name**\n> Store original name/time.\n\n**-b** _SIZE_, **--blocksize** _SIZE_\n> Block size for compression.\n\n**-z**, **--zlib**\n> Compress to zlib format.\n\n**-K**, **--zip**\n> Compress to single-entry zip.\n\n**-q**, **--quiet**\n> Quiet mode.\n\n**-v**, **--verbose**\n> Verbose mode."],["DESCRIPTION","**pigz** (Parallel Implementation of GZip) compresses files using multiple processors. It produces gzip-compatible output significantly faster than standard gzip on multi-core systems.\n\nCompression is parallelized by dividing input into blocks processed by separate threads. The results are combined maintaining gzip compatibility. Decompression is less parallelizable but still benefits from parallel CRC checking.\n\nThe default thread count matches available processors. On high-core systems, diminishing returns may occur beyond 8-16 threads. Block size affects parallelism granularity.\n\nCompression levels work like gzip: 1 (fastest) to 9 (best). Level 11 enables zopfli compression for maximum compression at much slower speed, useful for files compressed once and served many times.\n\nThe tool is a drop-in replacement for gzip in most scenarios. Output format is identical, so files can be decompressed with standard gunzip."],["CAVEATS","Decompression is not fully parallel - limited by gzip format design. Memory usage increases with thread count. Very small files may not benefit from parallelization. Zopfli mode (level 11) is extremely slow. Compressed output may differ from gzip (same content, different bytes)."],["HISTORY","**pigz** was written by **Mark Adler** (co-creator of gzip and zlib) around **2007** to address the need for parallel compression. The name is a play on \"pig\" (parallel implementation of gzip) and the sound pigs make. It became essential for fast compression of large files on modern multi-core systems."],["INSTALL","```apt: sudo apt install pigz```\n\n```dnf: sudo dnf install pigz```\n\n```pacman: sudo pacman -S pigz```\n\n```apk: sudo apk add pigz```\n\n```zypper: sudo zypper install pigz```\n\n```brew: brew install pigz```\n\n```nix: nix profile install nixpkgs#pigz```"],["SEE ALSO","[gzip](/man/gzip)(1), [zstd](/man/zstd)(1), [lz4](/man/lz4)(1), [pbzip2](/man/pbzip2)(1)"]],"e":["gzip","zstd","lz4","pbzip2"],"x":"pigz [file]"},"pipewire-avb":{"n":"pipewire-avb","t":"PipeWire daemon variant for AVB audio-video bridging","s":[["TAGLINE","PipeWire daemon variant for AVB audio-video bridging"],["TLDR","**Start the AVB PipeWire daemon** with the default configuration\n\n```pipewire-avb```\n\n**Use a custom configuration file**\n\n```pipewire-avb -c [path/to/pipewire-avb.conf]```\n\n**Increase log verbosity**\n\n```pipewire-avb -v```\n\n**Show version information**\n\n```pipewire-avb --version```"],["SYNOPSIS","**pipewire-avb** [_options_]"],["PARAMETERS","**-h**, **--help**\n> Show help text and exit.\n\n**-v**, **--verbose**\n> Increase logging verbosity (may be repeated).\n\n**--version**\n> Show version information and exit.\n\n**-c** _FILE_, **--config**=_FILE_\n> Load the given configuration file (default **pipewire-avb.conf**).\n\n**-P** _PROPS_, **--properties**=_PROPS_\n> Add JSON properties to the daemon context."],["DESCRIPTION","**pipewire-avb** is a PipeWire daemon launched with an **AVB**-tailored configuration. AVB (Audio Video Bridging) is a set of IEEE 802.1 standards (gPTP, FQTSS, MSRP, AVTP) that provide deterministic, low-latency, time-synchronised media streaming over standard Ethernet. It is widely used in pro-audio, automotive, and broadcast environments.\n\nThe daemon loads the **avb** module to discover and announce AVB streams (talkers and listeners) on a chosen network interface, sets up a gPTP-driven media clock, and exposes streams as native PipeWire nodes. Standard PulseAudio, ALSA, and JACK clients can then send audio to or receive from AVB endpoints.\n\nThe binary is typically a symlink to **pipewire**(1); the program selects its default configuration based on **argv[0]**."],["CONFIGURATION","The default configuration is **/usr/share/pipewire/pipewire-avb.conf**. Copy it to **/etc/pipewire/** for system-wide changes or to **~/.config/pipewire/** for per-user changes. Drop-in fragments may be placed in a matching **pipewire-avb.conf.d/** directory.\n\nThe configuration selects the AVB network interface, gPTP clock parameters, and stream topology."],["CAVEATS","Requires a network interface and switch infrastructure that support **AVB/TSN** (gPTP and SRP) for guaranteed bandwidth and timing. Stream discovery uses the layer-2 protocols MSRP and MVRP, which often require **CAP_NET_ADMIN** capabilities or root privileges. Marked **EXPERIMENTAL** in upstream PipeWire releases."],["HISTORY","AVB support was added to **PipeWire** as part of its push into professional networked audio. PipeWire was created by **Wim Taymans** at Red Hat and first released in **2017**, and now ships as the default audio server in major Linux distributions."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```dnf: sudo dnf install pipewire```\n\n```pacman: sudo pacman -S pipewire```\n\n```apk: sudo apk add pipewire```\n\n```zypper: sudo zypper install pipewire```\n\n```brew: brew install pipewire```\n\n```nix: nix profile install nixpkgs#pipewire```"],["SEE ALSO","[pipewire](/man/pipewire)(1), [pipewire-aes67](/man/pipewire-aes67)(1), [pipewire-pulse](/man/pipewire-pulse)(1), [pw-cli](/man/pw-cli)(1), [pw-dump](/man/pw-dump)(1)"]],"e":["pipewire","pipewire-aes67","pipewire-pulse","pw-cli","pw-dump"],"x":"pipewire-avb"},"poetry-help":{"n":"poetry-help","t":"Display Poetry command help","s":[["TAGLINE","Display Poetry command help"],["TLDR","**Show general help**\n\n```poetry help```\n\n**Get help for command**\n\n```poetry help [command]```\n\n**Show install help**\n\n```poetry help install```"],["SYNOPSIS","**poetry help** [_command_]"],["PARAMETERS","_COMMAND_\n> Command to get help for."],["DESCRIPTION","**poetry help** displays help information for Poetry commands, showing available options, arguments, and usage examples. When called without arguments, it shows general Poetry help; when given a command name, it shows detailed help for that specific command.\n\nThis is equivalent to using the **--help** flag on any Poetry command."],["CAVEATS","Also available via --help flag."],["HISTORY","poetry help provides **built-in documentation** for Poetry commands."],["INSTALL","```dnf: sudo dnf install poetry```\n\n```apk: sudo apk add poetry```\n\n```brew: brew install poetry```\n\n```nix: nix profile install nixpkgs#poetry```"],["SEE ALSO","[poetry](/man/poetry)(1)"]],"e":["poetry"],"x":"poetry help"},"pretty-bytes":{"n":"pretty-bytes","t":"Convert byte values to human-readable strings","s":[["TAGLINE","Convert byte values to human-readable strings"],["TLDR","**Convert bytes to human readable**\n\n```pretty-bytes [1024]```\n\n**Convert from stdin**\n\n```echo [1048576] | pretty-bytes```\n\n**Convert large values**\n\n```pretty-bytes [1099511627776]```"],["SYNOPSIS","**pretty-bytes** _bytes_"],["PARAMETERS","_BYTES_\n> Byte value to convert."],["DESCRIPTION","**pretty-bytes** converts raw byte values into human-readable strings with appropriate unit suffixes (kB, MB, GB, TB, etc.). It automatically selects the best unit for readability, making large numbers easier to understand at a glance.\n\nThis Node.js CLI utility is useful in scripts and pipelines where file sizes or data amounts need to be displayed in a user-friendly format rather than as raw numbers."],["CAVEATS","Uses binary prefixes. Node.js package."],["HISTORY","pretty-bytes was created as a **Node.js utility** for byte formatting."],["INSTALL","```dnf: sudo dnf install pretty-bytes```"],["SEE ALSO","[numfmt](/man/numfmt)(1)"]],"e":["numfmt"],"x":"pretty-bytes [1024]"},"pwconv":{"n":"pwconv","t":"Enable shadow password storage","s":[["TAGLINE","Enable shadow password storage"],["TLDR","**Convert to shadow passwords**\n\n```pwconv```\n\n**Verbose conversion**\n\n```pwconv -R [/]```"],["SYNOPSIS","**pwconv** [_options_]"],["PARAMETERS","**-R** _ROOT_\n> Chroot directory."],["DESCRIPTION","**pwconv** creates or updates the **/etc/shadow** file from **/etc/passwd**, migrating password hashes from the world-readable passwd file to the root-only-readable shadow file. This is a fundamental security measure that prevents unprivileged users from accessing password hashes for offline cracking.\n\nThe command is part of **shadow-utils** and is typically run once during initial system setup or when converting a legacy system to shadow passwords. It preserves existing shadow entries and only adds or updates entries as needed."],["CAVEATS","Requires root. One-time migration. System administration."],["HISTORY","pwconv is part of **shadow-utils** for shadow password migration."],["INSTALL","```apt: sudo apt install passwd```\n\n```apk: sudo apk add shadow-conv```"],["SEE ALSO","[pwunconv](/man/pwunconv)(8), [grpconv](/man/grpconv)(8)"]],"e":["pwunconv","grpconv"],"x":"pwconv"},"pygmentize":{"n":"pygmentize","t":"Syntax highlight source code","s":[["TAGLINE","Syntax highlight source code"],["TLDR","**Highlight code file**\n\n```pygmentize [file.py]```\n\n**Specify output format**\n\n```pygmentize -f [html] [file.py]```\n\n**Output to file**\n\n```pygmentize -o [output.html] [file.py]```\n\n**Specify language**\n\n```pygmentize -l [python] [file]```\n\n**List available lexers**\n\n```pygmentize -L lexers```\n\n**Generate CSS**\n\n```pygmentize -S [monokai] -f html > [style.css]```"],["SYNOPSIS","**pygmentize** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Input file.\n\n**-f** _FORMATTER_\n> Output format.\n\n**-l** _LEXER_\n> Language lexer.\n\n**-o** _FILE_\n> Output file.\n\n**-S** _STYLE_\n> Syntax style.\n\n**-L**\n> List available options."],["DESCRIPTION","**pygmentize** is the command-line interface for the Pygments syntax highlighting library. It reads source code files and outputs syntax-highlighted versions in various formats including terminal ANSI colors, HTML, LaTeX, RTF, and SVG, supporting over 500 programming languages and markup formats.\n\nThe tool auto-detects the input language from file extensions or can be specified explicitly with **-l**. Output format is set with **-f**, and visual themes are controlled with **-S** for generating CSS stylesheets. pygmentize is widely used for generating highlighted code in documentation, blogs, and presentations."],["CAVEATS","Python library required. Many output formats."],["HISTORY","Pygments was created as a **syntax highlighting** library for Python."],["INSTALL","```apk: sudo apk add py3-pygments```"],["SEE ALSO","[highlight](/man/highlight)(1), [source-highlight](/man/source-highlight)(1)"]],"e":["highlight","source-highlight"],"x":"pygmentize [file.py]"},"qt-select":{"n":"qt-select","t":"Switch between Qt installation versions","s":[["TAGLINE","Switch between Qt installation versions"],["TLDR","**Show current Qt version**\n\n```qtchooser -print-env```\n\n**List available versions**\n\n```qtchooser -l```\n\n**Select Qt version**\n\n```export QT_SELECT=[qt5]```\n\n**Run tool with specific Qt**\n\n```qtchooser -run-tool=[qmake] -qt=[5]```"],["SYNOPSIS","**qtchooser** [_options_]"],["PARAMETERS","**-l**, **--list-versions**\n> List available Qt versions.\n\n**-print-env**\n> Print environment settings.\n\n**-run-tool=**_tool_\n> Run specific tool.\n\n**-qt=**_version_\n> Select Qt version."],["DESCRIPTION","**qtchooser** (or qt-select) allows switching between multiple Qt installations. It manages which Qt version's tools (qmake, moc, etc.) are used by default."],["EXAMPLES","```bash"],["List available","qtchooser -l"],["Show current","qtchooser -print-env"],["Use Qt 5","export QT_SELECT=qt5\nqmake --version"],["Use Qt 6","export QT_SELECT=qt6\nqmake --version"],["Run specific qmake","qtchooser -run-tool=qmake -qt=5 --version\n```"],["CONFIGURATION","**/usr/share/qtchooser/**\n> System-wide Qt version configuration files, each specifying bin and lib paths for a Qt installation.\n\n**~/.config/qtchooser/**\n> User-level Qt version configuration files overriding system defaults.\n\n**QT_SELECT**\n> Environment variable selecting the default Qt version (e.g., qt5, qt6).\n\n**QTCHOOSER_NO_GLOBAL_DIR**\n> Environment variable to ignore system-wide configuration directory when set to 1."],["CAVEATS","Not available on all distributions. May conflict with manually installed Qt. Use environment variable for persistent selection."],["HISTORY","qtchooser was created to manage multiple Qt installations on Linux systems with distribution packaging."],["SEE ALSO","[qmake](/man/qmake)(1), [update-alternatives](/man/update-alternatives)(1), [qt](/man/qt)(1)"]],"e":["qmake","update-alternatives","qt"],"x":"qtchooser -print-env"},"qt":{"n":"qt","t":"Cross-platform C++ application framework tools","s":[["TAGLINE","Cross-platform C++ application framework tools"],["TLDR","**Create Qt project**\n\n```qmake -project```\n\n**Generate Makefile**\n\n```qmake```\n\n**Build with CMake**\n\n```cmake -DCMAKE_PREFIX_PATH=[/path/to/Qt] ..```\n\n**Run Qt Designer**\n\n```designer```\n\n**Run Qt Creator**\n\n```qtcreator```"],["SYNOPSIS","Qt framework tools and utilities"],["TOOLS","**qmake**\n> Build system generator.\n\n**moc**\n> Meta-Object Compiler.\n\n**uic**\n> UI compiler.\n\n**rcc**\n> Resource compiler.\n\n**designer**\n> Visual UI designer.\n\n**qtcreator**\n> IDE.\n\n**qmlscene**\n> QML viewer."],["DESCRIPTION","**Qt** is a cross-platform C++ framework for GUI applications. It provides widgets, graphics, networking, and more. This covers the main Qt development tools."],["EXAMPLES","```bash"],["Create project file","qmake -project"],["Generate Makefile","qmake myproject.pro"],["Build","make"],["Run UI designer","designer"],["View QML file","qmlscene myapp.qml"],["Get Qt version","qmake --version\n```"],["CMAKE INTEGRATION","```cmake\ncmake_minimum_required(VERSION 3.16)\nproject(myapp)\n\nfind_package(Qt6 REQUIRED COMPONENTS Widgets)\nqt_standard_project_setup()\n\nqt_add_executable(myapp main.cpp)\ntarget_link_libraries(myapp Qt6::Widgets)\n```"],["ENVIRONMENT","```bash"],["Set Qt path","export PATH=/opt/Qt/6.x/gcc_64/bin:$PATH\nexport LD_LIBRARY_PATH=/opt/Qt/6.x/gcc_64/lib\n```"],["CAVEATS","Large framework. Licensing: GPL, LGPL, or commercial. Qt5 and Qt6 APIs differ."],["HISTORY","Qt was created by **Trolltech** in 1991, later acquired by **Nokia**, then **Digia**, now **The Qt Company**."],["INSTALL","```dnf: sudo dnf install qt```\n\n```brew: brew install qt```"],["SEE ALSO","[qmake](/man/qmake)(1), [cmake](/man/cmake)(1)"]],"e":["qmake","cmake"],"x":"qmake -project"},"quilt":{"n":"quilt","t":"Manage ordered series of patches","s":[["TAGLINE","Manage ordered series of patches"],["TLDR","**Create new patch**\n\n```quilt new [patch_name.patch]```\n\n**Add file to patch**\n\n```quilt add [file]```\n\n**Apply next patch**\n\n```quilt push```\n\n**Remove current patch**\n\n```quilt pop```\n\n**Refresh patch**\n\n```quilt refresh```\n\n**Show patch series**\n\n```quilt series```\n\n**Show diff** of current changes\n\n```quilt diff```\n\n**Apply all patches** in the series\n\n```quilt push -a```\n\n**Remove all applied patches**\n\n```quilt pop -a```\n\n**Delete a patch** from the series\n\n```quilt delete [patch_name.patch]```"],["SYNOPSIS","**quilt** _command_ [_options_]"],["PARAMETERS","**new** _NAME_\n> Create a new patch with the given name and insert it after the current top patch.\n\n**add** _FILE_\n> Register a file to be tracked in the current topmost patch before making changes.\n\n**push** [**-a**]\n> Apply the next unapplied patch in the series. Use **-a** to apply all remaining patches.\n\n**pop** [**-a**]\n> Remove the topmost applied patch, reverting its changes. Use **-a** to remove all applied patches.\n\n**refresh**\n> Update the topmost patch to reflect the current changes to tracked files.\n\n**series**\n> List all patches in the series file in order.\n\n**diff** [**-z**]\n> Show the differences between the current state of tracked files and the topmost patch.\n\n**delete** [**-r**] _NAME_\n> Remove a patch from the series file. Use **-r** to also remove the patch file from the patches directory.\n\n**edit** _FILE_\n> Add a file to the topmost patch and open it in the default editor.\n\n**top**\n> Print the name of the topmost applied patch.\n\n**applied**\n> List all currently applied patches.\n\n**unapplied**\n> List all patches that have not yet been applied."],["DESCRIPTION","**quilt** manages an ordered series of patches against a source tree, allowing patches to be applied, removed, and updated independently. It maintains a **series** file listing patches in order and a **patches/** directory containing the patch files, providing a structured workflow for maintaining modifications on top of upstream code.\n\nThe typical workflow involves creating a new patch with **new**, registering files to track with **add**, making changes, then saving the patch with **refresh**. Patches can be applied incrementally with **push** and removed with **pop**, and the entire series can be reordered or edited. Quilt is widely used in Linux distribution packaging to maintain downstream patches against upstream source."],["CAVEATS","Quilt creates a **patches/** directory and a **series** file in the working directory. Files must be registered with **add** before editing, or changes will not be captured by **refresh**. The patches use standard unified diff format."],["HISTORY","Quilt was inspired by **Andrew Morton's** patch scripts for kernel development."],["INSTALL","```dnf: sudo dnf install quilt```\n\n```pacman: sudo pacman -S quilt```\n\n```apk: sudo apk add quilt```\n\n```zypper: sudo zypper install quilt```\n\n```brew: brew install quilt```\n\n```nix: nix profile install nixpkgs#quilt```"],["SEE ALSO","[patch](/man/patch)(1), [diff](/man/diff)(1), [git](/man/git)(1)"]],"e":["patch","diff","git"],"x":"quilt new [patch_name.patch]"},"quota":{"n":"quota","t":"Display disk usage and quota limits","s":[["TAGLINE","Display disk usage and quota limits"],["TLDR","**Show user quota**\n\n```quota```\n\n**Show specific user's quota**\n\n```quota -u [username]```\n\n**Show group quota**\n\n```quota -g [groupname]```\n\n**Verbose output**\n\n```quota -v```\n\n**Show all filesystems**\n\n```quota -A```"],["SYNOPSIS","**quota** [_options_] [_user_]"],["PARAMETERS","_USER_\n> User to check.\n\n**-u**\n> User quota.\n\n**-g**\n> Group quota.\n\n**-v**\n> Verbose output.\n\n**-s**\n> Human-readable sizes.\n\n**-A**\n> All filesystems."],["DESCRIPTION","**quota** displays current disk usage and limits for users or groups on filesystems where quotas are enabled. It reads the kernel's quota subsystem to report how much space and how many inodes a user or group has consumed, alongside the configured soft and hard limits.\n\nWhen soft limits are exceeded, the tool shows the remaining grace period before enforcement takes effect. Running without arguments displays the calling user's quotas across all mounted filesystems, while the **-u** and **-g** flags query specific users or groups. The **-v** flag reports on all quota-enabled filesystems, including those where no space is currently consumed."],["CAVEATS","Requires quota system enabled. Filesystem support needed."],["HISTORY","quota is part of **Unix quota** system for disk usage limits."],["INSTALL","```apt: sudo apt install quota```\n\n```dnf: sudo dnf install quota```\n\n```pacman: sudo pacman -S quota-tools```\n\n```apk: sudo apk add quota-tools```\n\n```zypper: sudo zypper install quota```\n\n```nix: nix profile install nixpkgs#quota```"],["SEE ALSO","[edquota](/man/edquota)(8), [repquota](/man/repquota)(8), [quotacheck](/man/quotacheck)(8)"]],"e":["edquota","repquota","quotacheck"],"x":"quota"},"ripmime":{"n":"ripmime","t":"Extract MIME attachments from email messages","s":[["TAGLINE","Extract MIME attachments from email messages"],["TLDR","Extract attachments to **current directory**\n\n```ripmime -i path/to/file```\n\nExtract attachments to **specific directory**\n\n```ripmime -i path/to/file -d path/to/directory```\n\nExtract with **verbose output**\n\n```ripmime -i path/to/file -v```\n\nGet **debug information** about decoding\n\n```ripmime -i path/to/file --debug```"],["SYNOPSIS","**ripmime** [_options_] -i _file_"],["PARAMETERS","**-i** _file_\n> Input MIME encoded file\n\n**-d** _directory_\n> Output directory for extracted attachments\n\n**-v**\n> Enable verbose output\n\n**--debug**\n> Show detailed decoding information"],["DESCRIPTION","**ripmime** extracts attachments from MIME encoded email packages. It parses email files and saves embedded attachments to disk.\n\nThe tool handles various MIME encoding types and can process emails with multiple attachments or nested structures."],["CAVEATS","Be cautious when extracting attachments from untrusted emails as they may contain malware. Verify attachment safety before opening."],["HISTORY","Developed for automated email processing and attachment extraction. Commonly used in email filtering and archiving systems."],["INSTALL","```apt: sudo apt install ripmime```\n\n```dnf: sudo dnf install ripmime```\n\n```apk: sudo apk add ripmime```\n\n```brew: brew install ripmime```\n\n```nix: nix profile install nixpkgs#ripmime```"],["SEE ALSO","[munpack](/man/munpack)(1), [mpack](/man/mpack)(1)"]],"e":["munpack","mpack"],"x":"ripmime -i path/to/file"},"rpmbuild":{"n":"rpmbuild","t":"Build RPM packages from spec files","s":[["TAGLINE","Build RPM packages from spec files"],["TLDR","Build **binary and source** packages\n\n```rpmbuild -ba path/to/spec_file```\n\nBuild **binary package** only\n\n```rpmbuild -bb path/to/spec_file```\n\nBuild **source package** only\n\n```rpmbuild -bs path/to/spec_file```\n\nBuild with **custom macro** definitions\n\n```rpmbuild -bb --define \"version 1.0\" path/to/spec_file```\n\n**Prepare** sources only (unpack and patch)\n\n```rpmbuild -bp path/to/spec_file```\n\nBuild for **specific architecture**\n\n```rpmbuild -bb --target x86_64 path/to/spec_file```\n\n**Rebuild** from an existing **source package**\n\n```rpmbuild --rebuild path/to/package.src.rpm```"],["SYNOPSIS","**rpmbuild** {**-ba**|**-bb**|**-bp**|**-bc**|**-bi**|**-bl**|**-bs**|**-br**|**-bd**} [_options_] _SPECFILE_"],["DESCRIPTION","**rpmbuild** builds both binary and source software packages from spec files. A package consists of an archive of files and metadata used to install and erase the archive files. The tool processes spec files through multiple build stages."],["PARAMETERS","**-ba**\n> Build binary and source packages\n\n**-bb**\n> Build binary packages only\n\n**-bp**\n> Unpack sources and apply patches only (%prep stage)\n\n**-bf**\n> Configure the sources (%conf stage, equivalent to ./configure)\n\n**-bc**\n> Compile sources (%build stage)\n\n**-bi**\n> Install binaries into the build root and run %check\n\n**-bl**\n> Verify that all files listed in %files exist\n\n**-bs**\n> Build source package only\n\n**-br**\n> Build source package with dynamic build dependency resolution\n\n**-bd**\n> Check dynamic build dependencies and generate build requirements\n\n**-D**, **--define** _'MACRO EXPR'_\n> Define a macro with value\n\n**--target** _PLATFORM_\n> Set build target architecture (e.g. `x86_64`, `aarch64`)\n\n**--buildroot** _DIRECTORY_\n> Override the build root directory\n\n**--clean**\n> Remove build tree after completion\n\n**--nobuild**\n> Skip all build stages (spec file syntax check only)\n\n**--noprep**\n> Skip the %prep stage\n\n**--noclean**\n> Skip the %clean stage\n\n**--nocheck**\n> Skip the %check stage\n\n**--nodeps**\n> Do not verify build dependencies\n\n**--short-circuit**\n> Skip directly to the specified stage (usable with -bc, -bi, -bb only)\n\n**--rebuild** _SOURCEPKG_\n> Install the source package, build binary packages, then clean up\n\n**--recompile** _SOURCEPKG_\n> Like --rebuild but stops after the install stage without creating binary packages\n\n**--rmsource**\n> Remove source files after build\n\n**--rmspec**\n> Remove spec file after build\n\n**--with** _OPTION_\n> Enable a conditional build option defined in the spec file\n\n**--without** _OPTION_\n> Disable a conditional build option defined in the spec file"],["CONFIGURATION","**~/.rpmmacros**\n> User-level macro definitions overriding system defaults, commonly used to set **%_topdir** for the build tree location.\n\n**/etc/rpm/macros**\n> System-wide macro overrides for all users on the machine.\n\n**/usr/lib/rpm/macros**\n> Default RPM macro definitions shipped with the rpm package, defining build paths, compiler flags, and platform defaults."],["CAVEATS","The spec file defines how to build a package. Macros are defined in /usr/lib/rpm/macros, /etc/rpm/macros, and ~/.rpmmacros. Build dependencies must be installed before building unless --nodeps is used."],["HISTORY","**rpmbuild** is part of the **RPM** (RPM Package Manager) system, originally developed by Red Hat for managing software packages on Linux systems."],["INSTALL","```apt: sudo apt install rpm```\n\n```dnf: sudo dnf install rpm```\n\n```apk: sudo apk add rpm```\n\n```zypper: sudo zypper install rpm```\n\n```brew: brew install rpm```\n\n```nix: nix profile install nixpkgs#rpm```"],["SEE ALSO","[rpm](/man/rpm)(8), [rpmspec](/man/rpmspec)(8), [rpmsign](/man/rpmsign)(8), [mock](/man/mock)(1)"]],"e":["rpm","rpmspec","rpmsign","mock"],"x":"rpmbuild -ba path/to/spec_file"},"saned":{"n":"saned","t":"Network scanner sharing daemon","s":[["TAGLINE","Network scanner sharing daemon"],["TLDR","**Run saned in standalone daemon mode**\n\n```saned -a```\n\n**Run in debug mode** waiting for connections\n\n```saned -d```\n\n**Run with verbose debugging**\n\n```saned -d128```\n\n**Run in debug mode** with SANE_DEBUG output\n\n```saned -s```\n\n**Specify a custom data port range**\n\n```saned -a -b```"],["SYNOPSIS","**saned** [_-a_] [_-d [n]_] [_-s [n]_] [_-b_] [_-u_] [_-e_]"],["PARAMETERS","**-a**\n> Run in standalone daemon mode, detached from console\n\n**-d** [_n_]\n> Debug mode; wait for connections explicitly. Optional number sets verbosity (higher = more verbose)\n\n**-s** [_n_]\n> Debug mode with SANE_DEBUG output; optional verbosity level\n\n**-b**\n> Negotiate a data port range with the client\n\n**-u**\n> Run as user after binding to port\n\n**-e**\n> Exit after first client disconnects (debug mode only)"],["DESCRIPTION","**saned** is the SANE network daemon that allows remote clients to access locally-connected scanners. It listens on TCP port 6566 for scanning requests, communicates with local SANE backends, and returns scan data to clients.\n\nThe daemon can run standalone with **-a** or be managed by inetd/xinetd. Access control is configured in **/etc/sane.d/saned.conf**, which lists allowed hostnames, IP addresses, or CIDR subnets. Connections from localhost are always permitted.\n\nClients configure the server address in their **/etc/sane.d/net.conf** file to access remote scanners transparently through the network backend."],["CONFIGURATION","**/etc/sane.d/saned.conf**\n> Access control file listing hostnames, IP addresses, or CIDR subnets allowed to connect to the scanner daemon.\n\n**/etc/sane.d/net.conf**\n> Client-side configuration listing remote saned servers to connect to for network scanning."],["CAVEATS","saned is not designed for exposure to untrusted networks. Always restrict access via saned.conf and firewall rules. Using a single **+** in the access list allows any host to connect and poses a security risk. Firewall configuration may require specifying a data port range for the scanning data connection."],["HISTORY","saned is part of the **SANE** (Scanner Access Now Easy) project, which began in **1996** to provide standardized scanner access on Unix systems. The network daemon enables scanner sharing across local networks without proprietary protocols."],["INSTALL","```apt: sudo apt install sane-utils```\n\n```apk: sudo apk add sane-saned```\n\n```zypper: sudo zypper install sane-saned```"],["SEE ALSO","[scanimage](/man/scanimage)(1), [sane-find-scanner](/man/sane-find-scanner)(1)"]],"e":["scanimage","sane-find-scanner"],"x":"saned -a"},"sar":{"n":"sar","t":"Collect and report system activity statistics","s":[["TAGLINE","Collect and report system activity statistics"],["TLDR","Report **I/O statistics** every second\n\n```sar -b 1```\n\nReport **network** statistics every 2 seconds, 10 times\n\n```sar -n DEV 2 10```\n\nReport **CPU utilization** every 2 seconds\n\n```sar -u ALL 2```\n\nReport **memory** utilization\n\n```sar -r ALL 1 20```\n\nReport **run queue** and load averages\n\n```sar -q 1```\n\nReport **paging** statistics\n\n```sar -B 5```\n\nReport **disk** statistics\n\n```sar -d 1```"],["SYNOPSIS","**sar** [_options_] [_interval_ [_count_]]"],["DESCRIPTION","**sar** (System Activity Reporter) collects, reports, and saves system activity information. It can report on CPU, memory, I/O, network, and other subsystem performance."],["PARAMETERS","**-b**\n> Report I/O and transfer rate statistics\n\n**-B**\n> Report paging statistics\n\n**-d**\n> Report disk I/O statistics\n\n**-n KEYWORD**\n> Report network statistics (DEV, EDEV, NFS, SOCK, IP, etc.)\n\n**-q**\n> Report queue length and load averages\n\n**-r [ALL]**\n> Report memory utilization statistics\n\n**-u [ALL]**\n> Report CPU utilization\n\n**-v**\n> Report status of inode, file, and other kernel tables\n\n**-w**\n> Report task creation and system switching activity\n\n**-W**\n> Report swapping statistics\n\n**-y**\n> Report TTY devices activity\n\n**-f FILE**\n> Read data from specified file\n\n**-o FILE**\n> Write data to specified file\n\n**-s TIME**\n> Set start time for report\n\n**-e TIME**\n> Set end time for report\n\n**-P {cpu|ALL}**\n> Report per-processor statistics\n\n**-j {ID|LABEL|PATH|UUID}**\n> Display persistent device names"],["CONFIGURATION","**/var/log/sa/**\n> Directory containing binary system activity data files collected by sadc, named sa01-sa31 by day of month.\n\n**/etc/sysstat/sysstat**\n> Sysstat configuration file controlling data collection intervals, retention, and compression settings."],["CAVEATS","Without interval, sar reads from system activity data files. Data is collected by the sadc daemon and stored in /var/log/sa/. Historical data requires sysstat cron jobs to be enabled."],["HISTORY","**sar** is part of the **sysstat** package, originally from System V Unix. It provides comprehensive system activity reporting and historical data analysis."],["INSTALL","```dnf: sudo dnf install sysstat```\n\n```pacman: sudo pacman -S sysstat```\n\n```apk: sudo apk add sysstat```\n\n```zypper: sudo zypper install sysstat```\n\n```brew: brew install sysstat```\n\n```nix: nix profile install nixpkgs#sysstat```"],["SEE ALSO","[iostat](/man/iostat)(1), [mpstat](/man/mpstat)(1), [pidstat](/man/pidstat)(1), [sadc](/man/sadc)(8)"]],"e":["iostat","mpstat","pidstat","sadc"],"x":"sar -b 1"},"snakefmt":{"n":"snakefmt","t":"Opinionated code formatter for Snakemake workflow files","s":[["TAGLINE","Opinionated code formatter for Snakemake workflow files"],["TLDR","**Format Snakefile**\n\n```snakefmt [Snakefile]```\n\n**Check formatting**\n\n```snakefmt --check [Snakefile]```\n\n**Format directory**\n\n```snakefmt [workflow/]```\n\n**Show diff**\n\n```snakefmt --diff [Snakefile]```\n\n**Set line length**\n\n```snakefmt -l [100] [Snakefile]```\n\n**Format stdin**\n\n```cat [Snakefile] | snakefmt -```\n\n**Verbose output**\n\n```snakefmt -v [Snakefile]```"],["SYNOPSIS","**snakefmt** [_--check_] [_--diff_] [_-l length_] [_options_] _files_"],["PARAMETERS","**--check**\n> Check only, no changes.\n\n**--diff**\n> Show changes.\n\n**-l**, **--line-length** _N_\n> Max line length.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**--include** _PATTERN_\n> Regular expression for files to include when formatting directories.\n\n**--exclude** _PATTERN_\n> Regular expression for files to exclude when formatting directories.\n\n**--config** _FILE_\n> Read configuration from a pyproject.toml or .editorconfig file.\n\n**-**\n> Read from stdin."],["DESCRIPTION","**snakefmt** is an opinionated code formatter for Snakemake workflow files. It enforces a consistent style across Snakefiles and associated rules files, handling both standard Python syntax and Snakemake-specific extensions like **rule**, **checkpoint**, and **module** blocks.\n\nThe formatter is built on top of Python's **black** formatter, using the same default line length of 88 characters and similar formatting philosophy. It understands Snakemake's domain-specific syntax, correctly formatting rule parameters like **input**, **output**, **params**, and **shell** while preserving semantic meaning.\n\nCheck mode (**--check**) verifies formatting without modifying files, returning a non-zero exit code if changes are needed, which is useful for CI pipelines. Diff mode (**--diff**) previews the exact changes that would be made before committing to them."],["CAVEATS","Snakemake-specific and not suitable for general Python files. Uses the same default line length (88) as black. Configuration can be set in pyproject.toml. May not handle all custom Snakemake wrappers or heavily templated rules gracefully."],["HISTORY","**snakefmt** was created for the **Snakemake** workflow management system. It brings consistent formatting to bioinformatics pipelines."],["INSTALL","```brew: brew install snakefmt```\n\n```nix: nix profile install nixpkgs#snakefmt```"],["SEE ALSO","[snakemake](/man/snakemake)(1), [black](/man/black)(1), [python](/man/python)(1), [ruff](/man/ruff)(1), [yapf](/man/yapf)(1)"]],"e":["snakemake","black","python","ruff","yapf"],"x":"snakefmt [Snakefile]"},"socat":{"n":"socat","t":"Multipurpose bidirectional data relay","s":[["TAGLINE","Multipurpose bidirectional data relay"],["TLDR","**Forward TCP port** to another host\n\n```socat TCP-LISTEN:[8080],fork TCP:[remote.host]:[80]```\n\n**Create a simple TCP server**\n\n```socat TCP-LISTEN:[1234],reuseaddr,fork EXEC:[/bin/cat]```\n\n**Connect to a TCP port**\n\n```socat - TCP:[host]:[port]```\n\n**Forward Unix socket** to TCP\n\n```socat TCP-LISTEN:[1234],fork UNIX-CONNECT:[/var/run/app.sock]```\n\n**Create bidirectional pipe** between processes\n\n```socat EXEC:'[command1]' EXEC:'[command2]'```\n\n**Create a simple chat server**\n\n```socat TCP-LISTEN:[12345],fork,reuseaddr STDOUT```\n\n**SSL/TLS connection**\n\n```socat - OPENSSL:[host]:[443],verify=0```\n\n**Serial port to TCP**\n\n```socat TCP-LISTEN:[5000] /dev/ttyUSB0,b9600,raw,echo=0```\n\n**Create virtual terminal pair**\n\n```socat -d -d pty,raw,echo=0 pty,raw,echo=0```"],["SYNOPSIS","**socat** [_options_] _address1_ _address2_"],["DESCRIPTION","**socat** (SOcket CAT) is a multipurpose relay tool that establishes two bidirectional byte streams and transfers data between them. It's like a more powerful netcat that supports numerous address types and protocols.\n\nAddress types include TCP, UDP, Unix sockets, files, pipes, PTYs, processes (EXEC), SSL, serial devices, and many more. socat can connect, listen, and relay between any combination of these.\n\nCommon use cases include port forwarding, protocol conversion, debugging network services, creating tunnels, and connecting disparate systems. It's invaluable for system administration and network troubleshooting."],["PARAMETERS","**TCP-LISTEN:** _port_\n> Listen on TCP port.\n\n**TCP:** _host_ **:** _port_\n> Connect to TCP host:port.\n\n**UDP:** _host_ **:** _port_\n> UDP connection.\n\n**UNIX-CONNECT:** _path_\n> Connect to Unix socket.\n\n**UNIX-LISTEN:** _path_\n> Listen on Unix socket.\n\n**EXEC:** _command_\n> Execute command and connect to its I/O.\n\n**OPENSSL:** _host_ **:** _port_\n> SSL/TLS connection.\n\n**PTY**\n> Create pseudo-terminal.\n\n**STDIO** or **-**\n> Standard input/output.\n\n**FILE:** _path_\n> Open a file for reading and writing.\n\n**PIPE:** _path_\n> Create or connect to a named pipe.\n\n**SOCKS4:** _host_ **:** _port_\n> SOCKS4 proxy connection.\n\n**fork**\n> Handle multiple connections (address option).\n\n**reuseaddr**\n> Allow address reuse (address option).\n\n**verify=** _0|1_\n> SSL certificate verification (address option).\n\n**-d** **-d**\n> Verbose debugging output (repeat for more detail, up to -d -d -d -d).\n\n**-v**\n> Verbose data transfer logging.\n\n**-t** _TIMEOUT_\n> Total inactivity timeout in seconds.\n\n**-T** _TIMEOUT_\n> Transfer timeout in seconds.\n\n**-u**\n> Unidirectional mode (left to right only).\n\n**-U**\n> Unidirectional mode (right to left only)."],["CAVEATS","Complex address specifications require careful syntax. Fork option needed for multiple connections. SSL certificates may need configuration. Some features require elevated privileges."],["HISTORY","**socat** was created by **Gerhard Rieger** in **1999** as an extended version of netcat with support for many more address types. It filled the need for a universal relay tool that could bridge different communication mechanisms. The project continues active maintenance and is widely used for network administration."],["INSTALL","```apt: sudo apt install socat```\n\n```dnf: sudo dnf install socat```\n\n```pacman: sudo pacman -S socat```\n\n```apk: sudo apk add socat```\n\n```zypper: sudo zypper install socat```\n\n```brew: brew install socat```\n\n```nix: nix profile install nixpkgs#socat```"],["SEE ALSO","[nc](/man/nc)(1), [netcat](/man/netcat)(1), [stunnel](/man/stunnel)(1), [ssh](/man/ssh)(1)"]],"e":["nc","netcat","stunnel","ssh"],"x":"socat TCP-LISTEN:[8080],fork TCP:[remote.host]:[80]"},"spa-resample":{"n":"spa-resample","t":"PipeWire resampler debugging utility","s":[["TAGLINE","PipeWire resampler debugging utility"],["TLDR","**Resample** a WAV file to a different sample rate\n\n```spa-resample -r [48000] [input.wav] [output.wav]```\n\n**Resample** with a specific output **format**\n\n```spa-resample -r [48000] -f [s32] [input.wav] [output.wav]```\n\n**Resample** with **highest quality** setting\n\n```spa-resample -q 14 -r [48000] [input.wav] [output.wav]```\n\n**Resample** with **verbose** output\n\n```spa-resample -v -r [44100] -f [f32] [input.wav] [output.wav]```"],["SYNOPSIS","**spa-resample** [_OPTIONS_] _INFILE_ _OUTFILE_"],["PARAMETERS","**-r** _RATE_, **--rate=**_RATE_\n> Output sample rate.\n\n**-f** _FORMAT_, **--format=**_FORMAT_\n> Output sample format (**s8** | **s16** | **s32** | **f32** | **f64**).\n\n**-q** _QUALITY_, **--quality=**_QUALITY_\n> Resampler output quality (**0**-**14**). Higher values produce better quality at the cost of more CPU usage.\n\n**-c** _FLAGS_, **--cpuflags=**_FLAGS_\n> CPU feature flags for SIMD optimization selection. See spa/support/cpu.h for details.\n\n**-v**\n> Verbose operation.\n\n**-h**\n> Show help."],["DESCRIPTION","**spa-resample** is a command-line utility that uses the PipeWire SPA resampler to convert audio files from one sample rate and format to another. It reads a WAV input file, applies resampling with the specified parameters, and writes the result to a WAV output file.\n\nThe tool is primarily intended for **testing and debugging** the PipeWire resampler implementation rather than as a general-purpose audio conversion tool. It provides direct access to the same resampling algorithm that PipeWire uses internally for audio stream processing, making it useful for verifying resampler behavior and quality."],["CAVEATS","This tool is designed for testing and debugging purposes only, not for production audio conversion workflows. For general-purpose audio format conversion, dedicated tools like **sox** or **ffmpeg** are more appropriate. Only WAV files are supported as input and output formats. The quality parameter range of 0-14 is specific to the PipeWire SPA resampler and does not correspond to quality scales used by other resamplers."],["HISTORY","**spa-resample** is part of the **PipeWire** multimedia framework, which was created by **Wim Taymans** and first released in **2017**. PipeWire was designed as a unified audio and video server to replace both **PulseAudio** and **JACK** on Linux systems. The spa-resample utility provides standalone access to PipeWire's built-in SPA (Simple Plugin API) resampler, which supports multiple SIMD-optimized backends for efficient sample rate conversion."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-spa-tools```\n\n```zypper: sudo zypper install pipewire-spa-tools```"],["SEE ALSO","[pipewire](/man/pipewire)(1), [pw-cli](/man/pw-cli)(1), [pw-cat](/man/pw-cat)(1), [sox](/man/sox)(1), [ffmpeg](/man/ffmpeg)(1)"]],"e":["pipewire","pw-cli","pw-cat","sox","ffmpeg"],"x":"spa-resample -r [48000] [input.wav] [output.wav]"},"steamos-finalize-install":{"n":"steamos-finalize-install","t":"Complete SteamOS installation setup","s":[["TAGLINE","Complete SteamOS installation setup"],["TLDR","**Finalize** installation\n\n```sudo steamos-finalize-install```\n\nFinalize without **bootloaders** or kernel\n\n```sudo steamos-finalize-install --no-bootloaders --no-kernel```\n\nSkip **migration** steps\n\n```sudo steamos-finalize-install --no-migrate```\n\nSet specific **root hash**\n\n```sudo steamos-finalize-install --roothash [hash]```\n\n**Force** migration regardless of environment\n\n```sudo steamos-finalize-install --force```"],["SYNOPSIS","**steamos-finalize-install** [_OPTIONS_]"],["PARAMETERS","**--no-bootloaders**\n> Skip bootloader setup\n\n**--no-kernel**\n> Skip kernel installation\n\n**--no-migrate**\n> Skip all migration steps\n\n**--roothash** _HASH_\n> Set specific root hash during finalization\n\n**--force**\n> Force system migration regardless of environment"],["DESCRIPTION","**steamos-finalize-install** completes a SteamOS installation by setting up bootloaders, installing the kernel, and applying system updates. It performs necessary migration steps to transition from an incomplete installation to a fully bootable system.\n\nThis command is typically run as part of the installation process but can be manually invoked to repair or reconfigure boot components."],["CAVEATS","Requires root privileges. Incorrect use may render the system unbootable. The --force flag bypasses safety checks and should be used with caution. This command is specific to SteamOS 3.x on Steam Deck and compatible hardware."],["HISTORY","**steamos-finalize-install** is part of the **SteamOS** utilities developed by **Valve**. It handles the final stage of the A/B partition installation system used by SteamOS 3.0 on the Steam Deck."],["SEE ALSO","[steamos-update](/man/steamos-update)(1), [steamos-chroot](/man/steamos-chroot)(1)"]],"e":["steamos-update","steamos-chroot"],"x":"sudo steamos-finalize-install"},"swaynag":{"n":"swaynag","t":"Shows warning or error messages with buttons on Sway","s":[["TAGLINE","Shows warning or error messages with buttons on Sway"],["TLDR","**Show a simple warning** message\n\n```swaynag -m \"[Low battery]\"```\n\n**Show an error** (uses the red error style)\n\n```swaynag -t error -m \"[Disk full]\"```\n\n**Add a button** that runs a command\n\n```swaynag -m \"[Reboot required]\" -b \"[Reboot]\" \"[systemctl reboot]\"```\n\n**Read a detailed message from stdin** and show a toggle\n\n```dmesg | swaynag -m \"[Kernel messages]\" -l```\n\n**Anchor the bar at the bottom** of the screen\n\n```swaynag -m \"[Bottom notice]\" -e bottom```\n\n**Target a specific output**\n\n```swaynag -m \"[Hello]\" -o [HDMI-A-1]```\n\n**Use a custom config** file\n\n```swaynag -c [path/to/config] -m \"[Hi]\"```"],["SYNOPSIS","**swaynag** [_options..._]"],["PARAMETERS","**-m**, **--message** _text_\n> Message to display.\n\n**-l**, **--detailed-message**\n> Read a detailed message from standard input; adds a button that toggles the detailed text.\n\n**-L**, **--detailed-button** _text_\n> Text for the detailed-message toggle button (default: _Toggle details_).\n\n**-t**, **--type** _name_\n> Message type (_error_, _warning_, or a custom one defined in the config). Each type has its own colors.\n\n**-e**, **--edge** _top|bottom_\n> Screen edge on which to anchor (default: _top_).\n\n**-y**, **--layer** _overlay|top|bottom|background_\n> wlr-layer-shell layer used (default: _overlay_).\n\n**-o**, **--output** _name_\n> Name of the xdg_output on which to show the bar.\n\n**-b**, **--button** _text_ _command_\n> Add a button that runs _command_ in a terminal when pressed. Repeatable.\n\n**-B**, **--button-no-terminal** _text_ _command_\n> As **-b** but runs _command_ without a terminal.\n\n**-z**, **--button-dismiss** _text_ _command_\n> Button that runs _command_ and then dismisses swaynag.\n\n**-Z**, **--button-dismiss-no-terminal** _text_ _command_\n> Same as **-z** but without a terminal.\n\n**-s**, **--dismiss-button** _text_\n> Text for the dismiss (close) button (default: _X_).\n\n**-f**, **--font** _font_\n> Pango font description.\n\n**-c**, **--config** _file_\n> Load configuration from _file_.\n\n**-v**, **--version**\n> Print version and exit.\n\n**-h**, **--help**\n> Show help and exit."],["CONFIGURATION","**swaynag** loads the first readable file from:\n\n```\n$HOME/.swaynag/config\n$XDG_CONFIG_HOME/swaynag/config\nSYSCONFDIR/swaynag/config\n```\n\nConfig files use INI-style sections. The section name corresponds to the value of **-t**/**--type**; **[<default>]** applies to all types. Typical keys control colors, borders, padding, fonts, layer, and default buttons. See **swaynag**(5) for the full schema."],["DESCRIPTION","**swaynag** is the **Sway** compositor's on-screen message dialog. It draws a bar across the top or bottom of a screen with a message, an optional detailed body, and optional buttons. Sway itself invokes **swaynag** to surface configuration errors or important warnings, but users and scripts may call it directly to display confirmations, reboot prompts, or custom alerts.\n\nThe tool uses the **wlr-layer-shell** protocol, so it is specific to Wayland compositors that implement that protocol (Sway and other wlroots-based compositors)."],["HISTORY","**swaynag** is part of the **Sway** compositor, a tiling Wayland compositor compatible with i3. Sway was started by **Drew DeVault** in **2015**; **swaynag** was added as the replacement for the i3 nagbar that Sway historically launched for configuration errors."],["INSTALL","```apt: sudo apt install sway```\n\n```dnf: sudo dnf install sway```\n\n```pacman: sudo pacman -S sway```\n\n```apk: sudo apk add swaynag```\n\n```zypper: sudo zypper install sway```\n\n```nix: nix profile install nixpkgs#sway```"],["SEE ALSO","[sway](/man/sway)(1), [swaymsg](/man/swaymsg)(1), [swaybar](/man/swaybar)(5), [swaynag](/man/swaynag)(5)"]],"e":["sway","swaymsg","swaybar","swaynag"],"x":"swaynag -m \"[Low battery]\""},"systemctl-freeze":{"n":"systemctl-freeze","t":"Freeze unit processes via cgroup","s":[["TAGLINE","Freeze unit processes via cgroup"],["TLDR","**Freeze** a unit\n\n```systemctl freeze unit_name```\n\nFreeze **multiple** units\n\n```systemctl freeze unit1 unit2```\n\nFreeze **all** running units\n\n```systemctl freeze '*'```"],["SYNOPSIS","**systemctl freeze** _PATTERN_..."],["DESCRIPTION","**systemctl freeze** freezes one or more units using the cgroup freezer. Frozen units have all their processes suspended and cannot be scheduled until thawed. This is useful for temporarily pausing services without stopping them."],["PARAMETERS","**PATTERN**\n> One or more unit names or patterns to freeze"],["CAVEATS","Frozen units remain in memory but cannot execute. Use \"systemctl thaw\" to resume frozen units. Only service, scope, and slice unit types support freezing. Requires cgroup v2 (unified hierarchy) support in the kernel."],["HISTORY","**systemctl freeze** is part of **systemd**, providing cgroup-based process freezing for service management."],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-thaw](/man/systemctl-thaw)(1)"]],"e":["systemctl","systemctl-thaw"],"x":"systemctl freeze unit_name"},"systemctl-list-unit-files":{"n":"systemctl-list-unit-files","t":"List installed unit files and states","s":[["TAGLINE","List installed unit files and states"],["TLDR","List **installed** unit files\n\n```systemctl list-unit-files```\n\nFilter by **state**\n\n```systemctl list-unit-files --state [enabled|disabled|static]```\n\nFilter by **type**\n\n```systemctl list-unit-files -t [service|socket|timer]```\n\nFilter by **name pattern**\n\n```systemctl list-unit-files '[pattern*]'```\n\nOutput without **pager**\n\n```systemctl list-unit-files --no-pager```\n\nOutput without **headers**\n\n```systemctl list-unit-files --no-legend```"],["SYNOPSIS","**systemctl list-unit-files** [_OPTIONS_] [_PATTERN_...]"],["PARAMETERS","**--state=** _STATE_\n> Filter by enablement state (enabled, disabled, static, masked, etc.)\n\n**-t, --type=** _TYPE_\n> Filter by unit type\n\n**--no-pager**\n> Disable pager output\n\n**--no-legend**\n> Suppress header and footer"],["DESCRIPTION","**systemctl list-unit-files** displays all installed unit files and their enablement states. Unlike `list-units` which shows runtime state, this shows the boot configuration for all available units.\n\nThe output shows the unit file name and its state: enabled, disabled, static, masked, generated, transient, indirect, or alias."],["ENABLEMENT STATES","**enabled** - Starts automatically at boot\n\n**disabled** - Does not start at boot\n\n**static** - No install section, started as dependency\n\n**masked** - Completely blocked\n\n**indirect** - Enabled via another unit"],["CAVEATS","Shows all installed unit files regardless of runtime state. Some unit files may be present but never used. Static units cannot be enabled directly."],["HISTORY","The **list-unit-files** subcommand provides visibility into the full set of available units and their boot configuration, complementing `list-units` which shows only currently loaded units."],["SEE ALSO","[systemctl-list-units](/man/systemctl-list-units)(1), [systemctl-enable](/man/systemctl-enable)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-list-units","systemctl-enable","systemctl"],"x":"systemctl list-unit-files"},"tgcloud":{"n":"tgcloud","t":"Manage TigerGraph Cloud instances","s":[["TAGLINE","Manage TigerGraph Cloud instances"],["TLDR","**Log in** to TigerGraph Cloud\n\n```tgcloud login```\n\n**List solutions** (cloud instances)\n\n```tgcloud solution list```\n\n**Create a solution** interactively\n\n```tgcloud solution create```\n\n**Start a solution**\n\n```tgcloud solution start [solution_id]```\n\n**Stop a solution**\n\n```tgcloud solution stop [solution_id]```\n\n**Terminate (delete) a solution**\n\n```tgcloud solution terminate [solution_id]```\n\n**Backup a solution**\n\n```tgcloud backup create [solution_id]```"],["SYNOPSIS","**tgcloud** _command_ [_subcommand_] [_options_]"],["SUBCOMMANDS","**login**\n> Authenticate against TigerGraph Cloud using your account credentials or API token.\n\n**logout**\n> Sign out of the current session.\n\n**solution list**\n> List all solutions (cloud instances) in the account.\n\n**solution create**\n> Create a new solution (configure region, tier, version, etc.).\n\n**solution start** _id_ / **solution stop** _id_\n> Start or stop a solution.\n\n**solution terminate** _id_\n> Permanently delete a solution and its data.\n\n**backup create** _id_ / **backup list** _id_ / **backup restore** _id_\n> Manage solution backups.\n\n**user list**\n> List users in the organization."],["PARAMETERS","**--help**, **-h**\n> Show command help.\n\n**--verbose**\n> Verbose output."],["DESCRIPTION","**tgcloud** is the command-line interface for managing TigerGraph Cloud graph database instances (\"solutions\"). It provides programmatic control over cloud-hosted TigerGraph deployments, enabling instance lifecycle management (create, start, stop, terminate), backup operations, and user/organization management.\n\nThe tool is designed for automation and scripting workflows, allowing DevOps teams to manage TigerGraph Cloud infrastructure from the terminal or CI/CD pipelines without using the web console."],["CAVEATS","Requires a TigerGraph Cloud account and active subscription. Authentication via **tgcloud login** is mandatory before other commands. Terminate operations are irreversible and delete all data in the solution unless a backup exists."],["HISTORY","**tgcloud** is the official CLI for **TigerGraph Cloud**, the managed offering of the TigerGraph graph database platform."],["SEE ALSO","[gsql](/man/gsql)(1), [neo4j](/man/neo4j)(1)"]],"e":["gsql","neo4j"],"x":"tgcloud login"},"toolbox-create":{"n":"toolbox-create","t":"Create Toolbx development containers","s":[["TAGLINE","Create Toolbx development containers"],["TLDR","Create a Toolbx container for a **specific distribution**\n\n```toolbox create -d [distribution]```\n\nCreate a container for a specific **release** of the current distribution\n\n```toolbox create -r [release]```\n\nCreate a container with a **custom image**\n\n```toolbox create -i [name]```\n\nCreate from a **custom Fedora image**\n\n```toolbox create -i quay.io/fedora/fedora:[tag]```\n\nCreate using default image for a **specific Fedora release**\n\n```toolbox create -d fedora -r f[version]```"],["SYNOPSIS","**toolbox create** [_options_] [_container_]"],["PARAMETERS","**-d**, **--distro** _distro_\n> Create container for specified distribution\n\n**-r**, **--release** _release_\n> Create container for specified release version\n\n**-i**, **--image** _image_\n> Use specified container image\n\n**-c**, **--container** _name_\n> Assign custom name to the container"],["DESCRIPTION","**toolbox create** creates a new Toolbx container based on an OCI image. By default, it creates a container matching the host system's distribution and release. Custom images can be specified for development environments or cross-distribution work.\n\nThe container is configured for seamless integration with the host, including home directory access, user permissions, and graphical application support."],["CAVEATS","Requires podman to be installed. The default image is pulled from the distribution's registry if not cached locally. Container names must be unique within toolbox."],["INSTALL","```apt: sudo apt install podman-toolbox```\n\n```dnf: sudo dnf install toolbox```\n\n```pacman: sudo pacman -S toolbox```\n\n```zypper: sudo zypper install toolbox```\n\n```nix: nix profile install nixpkgs#toolbox```"],["SEE ALSO","[toolbox](/man/toolbox)(1), [toolbox-enter](/man/toolbox-enter)(1), [toolbox-list](/man/toolbox-list)(1)"]],"e":["toolbox","toolbox-enter","toolbox-list"],"x":"toolbox create -d [distribution]"},"tpm2":{"n":"tpm2","t":"Unified dispatcher for tpm2-tools utilities","s":[["TAGLINE","Unified dispatcher for tpm2-tools utilities"],["TLDR","**List all available** TPM2 subcommands\n\n```tpm2 --help```\n\n**Get 8 random bytes** from the TPM\n\n```tpm2 getrandom 8 | xxd -p```\n\n**Initialize the TPM** after boot\n\n```tpm2 startup -c```\n\n**Read PCR values** for the SHA-256 bank\n\n```tpm2 pcrread sha256:all```\n\n**Extend a PCR** with a precomputed hash\n\n```tpm2 pcrextend [index]:sha256=[hexdigest]```\n\n**Reset a resettable PCR** (typically index 16 or 23)\n\n```tpm2 pcrreset [index]```\n\n**Create a primary key** under the owner hierarchy\n\n```tpm2 createprimary -C o -c primary.ctx```\n\n**Use a specific TCTI** (e.g. the simulator)\n\n```tpm2 --tcti=mssim getrandom 8```"],["SYNOPSIS","**tpm2** _tool_ [_options_] [_arguments_]\n\n**tpm2_**_tool_ [_options_] [_arguments_]"],["DESCRIPTION","**tpm2** is a single busybox-style executable that combines all of the individual **tpm2-tools** utilities into one binary. Each historical tool, such as **tpm2_getrandom** or **tpm2_pcrread**, is available as a subcommand of **tpm2** (invoke **tpm2 getrandom**) as well as through a symlink of the original name.\n\nThe dispatcher exists primarily to shrink the footprint of **tpm2-tools** on size-constrained systems (initramfs images, embedded devices, recovery environments) where shipping dozens of separate binaries is wasteful. Because each _tpm2_<tool>_ symlink points back to the same binary, behaviour is identical between the two invocation styles.\n\nTools cover the full TPM 2.0 command surface: key creation and loading, NV storage, PCR operations, session and policy management, attestation, and auditing."],["COMMON OPTIONS","**-h**, **--help**[=_man_|_no-man_]\n> Show the help page for the selected tool. With **=man**, render the manpage via **man**; with **=no-man**, print a plain summary.\n\n**-v**, **--version**\n> Print version information for **tpm2-tools**.\n\n**-V**, **--verbose**\n> Increase console output; useful for debugging.\n\n**-Q**, **--quiet**\n> Suppress normal informational output.\n\n**-Z**, **--enable-errata**\n> Apply errata workarounds for known TPM bugs.\n\n**-T** _tcti_, **--tcti=**_tcti_\n> Select the TCTI (Transmission Interface Configuration) used to talk to the TPM.\n\n**-R**, **--autoflush**\n> Automatically flush transient objects from the TPM when the tool exits."],["SUBCOMMAND CATEGORIES","**Entropy / hashing**: getrandom, hash, hmac, stirrandom\n**Keys**: create, createprimary, createak, createek, load, loadexternal, import, evictcontrol\n**PCRs**: pcrread, pcrextend, pcrevent, pcrreset, pcrallocate\n**NV storage**: nvdefine, nvread, nvwrite, nvextend, nvundefine\n**Sessions & policy**: startauthsession, policypcr, policysigned, policypassword, policyauthvalue\n**Attestation**: quote, certify, activatecredential, makecredential\n**Signing / encryption**: sign, verifysignature, rsaencrypt, rsadecrypt, encryptdecrypt\n**Hierarchies**: clear, changeauth, dictionarylockout, hierarchycontrol"],["TCTI SELECTION","Communication with the TPM is abstracted by a **TCTI**. Selection priority:\n\n1. **-T** / **--tcti** on the command line\n2. **TPM2TOOLS_TCTI** environment variable\n3. Compile-time default\n\nCommon TCTIs:\n\n```\ntabrmd   Resource manager (tpm2-abrmd)\ndevice   Direct /dev/tpm0 or /dev/tpmrm0\nmssim    Microsoft TPM simulator (TCP)\nswtpm    libtpms-based software TPM\nnone     Offline mode (no TPM required)\n```"],["EXIT CODES","**0**\n> Success.\n\n**1**\n> General tool error.\n\n**2**\n> Option parsing error.\n\n**3**\n> Authentication failure.\n\n**4**\n> TCTI communication error.\n\n**5**\n> Unsupported scheme or algorithm."],["CAVEATS","Not every TPM exposes every hierarchy or PCR to userspace: only PCR 16 and 23 are typically resettable from locality 0, and certain operations require the owner password or a valid session. On systems using **tpm2-abrmd**, direct **device** access is blocked while the resource manager is running. When writing scripts, prefer the **tpm2 _tool_** form: it works even on minimal installs that only ship the dispatcher binary."],["HISTORY","The **tpm2-tools** project is maintained by the **tpm2-software** community and implements the user-space side of the **TCG TPM 2.0** specification on top of the **tpm2-tss** stack. The unified **tpm2** dispatcher was introduced to simplify packaging in constrained environments; the original per-tool invocation (**tpm2_**_tool_) is still supported and, on most distributions, remains the primary interface."],["INSTALL","```apt: sudo apt install tpm2-tools```\n\n```dnf: sudo dnf install tpm2-tools```\n\n```pacman: sudo pacman -S tpm2-tools```\n\n```apk: sudo apk add tpm2-tools```\n\n```nix: nix profile install nixpkgs#tpm2-tools```"]],"e":[],"x":"tpm2 --help"},"tuned-adm":{"n":"tuned-adm","t":"Manage system performance tuning profiles","s":[["TAGLINE","Manage system performance tuning profiles"],["TLDR","Show the **active** tuned profile\n\n```tuned-adm active```\n\n**List** profiles available on this system\n\n```tuned-adm list```\n\n**Switch** to a named profile\n\n```tuned-adm profile [profile_name]```\n\nAsk tuned for a **recommended** profile\n\n```tuned-adm recommend```\n\n**Turn off** dynamic tuning\n\n```tuned-adm off```"],["SYNOPSIS","**tuned-adm** [_command_] [_arguments_]"],["PARAMETERS","**list**\n> List available tuning profiles\n\n**active**\n> Show currently active profile\n\n**profile _name_**\n> Switch to specified profile\n\n**recommend**\n> Recommend optimal profile for this system\n\n**off**\n> Disable tuned and reset to defaults\n\n**verify**\n> Verify current profile settings match configuration\n\n**auto_profile**\n> Enable/disable automatic profile selection"],["DESCRIPTION","**tuned-adm** manages system performance tuning profiles provided by the **tuned** daemon. Profiles optimize various kernel and system parameters for specific workloads like throughput, latency, power saving, or virtualization.\n\nCommon profiles include: **balanced**, **throughput-performance**, **latency-performance**, **powersave**, **virtual-guest**, and **virtual-host**. The **recommend** command analyzes the system to suggest the best profile."],["CAVEATS","Requires the tuned service to be running. Profile changes take effect immediately. Some settings may require a reboot to fully apply. Available profiles vary by distribution."],["INSTALL","```dnf: sudo dnf install tuned```\n\n```pacman: sudo pacman -S tuned```\n\n```apk: sudo apk add tuned```\n\n```zypper: sudo zypper install tuned```\n\n```nix: nix profile install nixpkgs#tuned```"],["SEE ALSO","[tuned](/man/tuned)(8), [cpupower](/man/cpupower)(1)"]],"e":["tuned","cpupower"],"x":"tuned-adm active"},"unbound-control":{"n":"unbound-control","t":"Remote control interface for Unbound DNS resolver","s":[["TAGLINE","Remote control interface for Unbound DNS resolver"],["TLDR","**Check server status**\n\n```unbound-control status```\n\n**View statistics**\n\n```unbound-control stats```\n\n**Reload configuration**\n\n```unbound-control reload```\n\n**Flush entire cache**\n\n```unbound-control flush_zone [.]```\n\n**Flush specific domain**\n\n```unbound-control flush [example.com]```\n\n**Add local zone**\n\n```unbound-control local_zone [example.com] [static]```\n\n**Add local data**\n\n```unbound-control local_data \"[example.com. A 192.168.1.1]\"```\n\n**List forward zones**\n\n```unbound-control list_forwards```"],["SYNOPSIS","**unbound-control** [_options_] _command_"],["COMMANDS","**start**: Start the server.\n\n**stop**: Stop the server.\n\n**reload**: Reload configuration.\n\n**status**: Show server status.\n\n**stats**: Display statistics.\n\n**stats_noreset**: Stats without resetting counters.\n\n**flush** _name_: Flush name from cache.\n\n**flush_type** _name_ _type_: Flush specific RR type.\n\n**flush_zone** _name_: Flush entire zone.\n\n**local_zone** _name_ _type_: Add local zone.\n\n**local_data** _RR_: Add local data.\n\n**local_data_remove** _name_: Remove local data.\n\n**forward_add** _zone_ _addr_: Add forward zone.\n\n**forward_remove** _zone_: Remove forward zone.\n\n**list_forwards**: List forward zones.\n\n**list_stubs**: List stub zones.\n\n**dump_cache**: Dump cache to stdout.\n\n**load_cache**: Load cache from stdin."],["PARAMETERS","**-c** _file_\n> Configuration file path.\n\n**-s** _server_\n> Server address to connect to.\n\n**-q**\n> Quiet mode.\n\n**-h**\n> Display help."],["DESCRIPTION","**unbound-control** is a utility for managing the Unbound DNS resolver daemon. It communicates with a running unbound server over a secure channel using TLS certificates.\n\nBefore use, run **unbound-control-setup** to generate the required certificates and keys. The remote-control section must be enabled in unbound.conf.\n\nThe tool enables runtime configuration changes including cache management, adding local zones and data, and modifying forwarding without restarting the server."],["CAVEATS","Requires unbound-control-setup for initial certificate generation. Server must have remote-control enabled. Changes made via unbound-control are not persistent across restarts unless also in config file."],["HISTORY","**unbound-control** is part of the Unbound DNS resolver developed by NLnet Labs. Unbound is a validating, recursive, caching DNS resolver designed for high performance and security with DNSSEC support."],["INSTALL","```apt: sudo apt install unbound```\n\n```dnf: sudo dnf install unbound```\n\n```pacman: sudo pacman -S unbound```\n\n```apk: sudo apk add unbound```\n\n```zypper: sudo zypper install unbound```\n\n```brew: brew install unbound```\n\n```nix: nix profile install nixpkgs#unbound```"],["SEE ALSO","[unbound](/man/unbound)(8), [unbound-checkconf](/man/unbound-checkconf)(8), [unbound-host](/man/unbound-host)(1)"]],"e":["unbound","unbound-checkconf","unbound-host"],"x":"unbound-control status"},"usermod":{"n":"usermod","t":"Modify user account attributes","s":[["TAGLINE","Modify user account attributes"],["TLDR","Change **username**\n\n```sudo usermod -l new_username username```\n\nChange **user ID**\n\n```sudo usermod -u 1001 username```\n\nChange user **shell**\n\n```sudo usermod -s /bin/zsh username```\n\n**Add** user to supplementary groups\n\n```sudo usermod -aG group1,group2 username```\n\n**Set** user's supplementary groups (replaces existing)\n\n```sudo usermod -G group1,group2 username```\n\nChange **home directory** and move contents\n\n```sudo usermod -m -d /new/home username```\n\n**Lock** an account\n\n```sudo usermod -L username```\n\n**Unlock** an account\n\n```sudo usermod -U username```"],["SYNOPSIS","**usermod** [_options_] _LOGIN_"],["DESCRIPTION","**usermod** modifies system account files to change user account attributes. It can update username, user ID, groups, home directory, shell, and account status."],["PARAMETERS","**-a, --append**\n> Append to supplementary groups (use with -G)\n\n**-c, --comment COMMENT**\n> Change the comment field (GECOS)\n\n**-d, --home HOME_DIR**\n> Set new home directory\n\n**-e, --expiredate DATE**\n> Set account expiration date (YYYY-MM-DD)\n\n**-f, --inactive DAYS**\n> Days after password expires until account disabled\n\n**-g, --gid GROUP**\n> Change primary group\n\n**-G, --groups GROUP1,GROUP2**\n> Set supplementary groups (replaces existing)\n\n**-l, --login NEW_LOGIN**\n> Change username\n\n**-L, --lock**\n> Lock the user account\n\n**-m, --move-home**\n> Move home directory contents to new location\n\n**-o, --non-unique**\n> Allow non-unique UID when used with -u\n\n**-p, --password PASSWORD**\n> Set encrypted password (as returned by crypt(3))\n\n**-s, --shell SHELL**\n> Change login shell\n\n**-u, --uid UID**\n> Change user ID\n\n**-U, --unlock**\n> Unlock the user account\n\n**-Z, --selinux-user SEUSER**\n> Set SELinux user mapping"],["CAVEATS","When changing UID, files owned by the user need to be manually updated. Use **-a** with **-G** to add groups without removing existing ones. The user must not have running processes when changing username."],["HISTORY","**usermod** is part of the **shadow-utils** package for managing user accounts on Unix-like systems."],["INSTALL","```apt: sudo apt install passwd```\n\n```pacman: sudo pacman -S shadow```\n\n```apk: sudo apk add shadow```\n\n```zypper: sudo zypper install shadow```\n\n```nix: nix profile install nixpkgs#shadow```"],["SEE ALSO","[useradd](/man/useradd)(8), [userdel](/man/userdel)(8), [passwd](/man/passwd)(1), [groupmod](/man/groupmod)(8)"]],"e":["useradd","userdel","passwd","groupmod"],"x":"sudo usermod -l new_username username"},"xml-validate":{"n":"xml-validate","t":"Validate XML against schemas","s":[["TAGLINE","Validate XML against schemas"],["TLDR","**Validate against embedded DTD**\n\n```xml val [file.xml]```\n\n**Validate against an XSD schema**\n\n```xml val --xsd [schema.xsd] [file.xml]```\n\n**Validate against a RelaxNG schema**\n\n```xml val --relaxng [schema.rng] [file.xml]```\n\n**Check well-formedness only** (no schema validation)\n\n```xml val --well-formed [file.xml]```\n\n**Validate multiple files**\n\n```xml val [file1.xml] [file2.xml]```\n\n**Validate using embedded schema reference**\n\n```xml val -E [file.xml]```"],["SYNOPSIS","**xml val** [_options_] _file_..."],["PARAMETERS","**--xsd** _file_\n> Validate against XML Schema.\n\n**--dtd** _file_\n> Validate against DTD.\n\n**--relaxng** _file_\n> Validate against RelaxNG schema.\n\n**--well-formed**\n> Check well-formedness only.\n\n**-E**, **--embed**\n> Use embedded schema.\n\n**-e**, **--err**\n> Print errors."],["DESCRIPTION","**xml val** (or `xml validate`) validates XML documents against schemas. Part of the XMLStarlet toolkit. Supports DTD, XML Schema (XSD), and RelaxNG validation. Returns exit code 0 if valid, non-zero otherwise."],["INSTALL","```dnf: sudo dnf install xmlstarlet```\n\n```pacman: sudo pacman -S xmlstarlet```\n\n```apk: sudo apk add xmlstarlet```\n\n```zypper: sudo zypper install xmlstarlet```\n\n```brew: brew install xmlstarlet```\n\n```nix: nix profile install nixpkgs#xmlstarlet```"],["SEE ALSO","[xml-format](/man/xml-format)(1), [xmllint](/man/xmllint)(1)"]],"e":["xml-format","xmllint"],"x":"xml val [file.xml]"}});
