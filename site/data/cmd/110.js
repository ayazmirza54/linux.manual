window.LCL_BUCKET(110,{"apptainer-registry":{"n":"apptainer-registry","t":"Manage OCI/Docker registry authentication","s":[["TAGLINE","Manage OCI/Docker registry authentication"],["TLDR","**List** all configured OCI registry credentials\n\n```apptainer registry list```\n\n**Login** to a Docker registry\n\n```apptainer registry login --username [user] docker://[docker.io]```\n\n**Login** to an OCI registry with **password from stdin**\n\n```apptainer registry login --username [user] --password-stdin oras://[registry.example.com]```\n\n**Logout** from a registry\n\n```apptainer registry logout docker://[docker.io]```\n\n**Login** using a custom **auth file**\n\n```apptainer registry login --authfile [path/to/authfile.json] --username [user] docker://[registry.example.com]```"],["SYNOPSIS","**apptainer registry** [_subcommand_] [_options_]"],["PARAMETERS","**-c, --config** _string_\n> Path to the registry configuration file (default ~/.apptainer/remote.yaml)\n\n**-h, --help**\n> Display help for the registry command"],["DESCRIPTION","**apptainer registry** manages authentication to standalone OCI/Docker registries accessed via **docker://** or **oras://** protocols. It provides subcommands for logging in, logging out, and listing configured credentials for container registries.\n\nThis command handles credentials separately from Apptainer's remote endpoints, allowing direct authentication against any OCI-compatible registry such as Docker Hub, GitHub Container Registry, or private registries."],["SUBCOMMANDS","**apptainer registry list**\n> Display all configured OCI registry credentials\n\n**apptainer registry login**\n> Authenticate to an OCI/Docker registry\n\n**apptainer registry logout**\n> Remove authentication from an OCI/Docker registry"],["CAVEATS","Registry credentials are stored in the Apptainer configuration file at **~/.apptainer/remote.yaml** by default. This file should be kept secure as it contains authentication tokens. Credentials configured via **apptainer registry** are separate from those managed by **apptainer remote**, which handles Sylabs Cloud and library endpoints."],["INSTALL","```dnf: sudo dnf install apptainer```\n\n```pacman: sudo pacman -S apptainer```\n\n```apk: sudo apk add apptainer```\n\n```zypper: sudo zypper install apptainer```\n\n```brew: brew install apptainer```\n\n```nix: nix profile install nixpkgs#apptainer```"],["SEE ALSO","[apptainer](/man/apptainer)(1), [apptainer-pull](/man/apptainer-pull)(1), [apptainer-push](/man/apptainer-push)(1), [apptainer-search](/man/apptainer-search)(1)"]],"e":["apptainer","apptainer-pull","apptainer-push","apptainer-search"],"x":"apptainer registry list"},"balooctl":{"n":"balooctl","t":"Control KDE Baloo file indexing","s":[["TAGLINE","Control KDE Baloo file indexing"],["TLDR","Show indexer **status**\n\n```balooctl status```\n\n**Enable** file indexer\n\n```balooctl enable```\n\n**Disable** file indexer\n\n```balooctl disable```\n\n**Purge** index database\n\n```balooctl purge```\n\n**Suspend** indexing\n\n```balooctl suspend```\n\nShow **disk** space used\n\n```balooctl indexSize```\n\n**Check** for unindexed files\n\n```balooctl check```"],["SYNOPSIS","**balooctl** [_OPTIONS_] _COMMAND_"],["DESCRIPTION","**balooctl** is the control utility for Baloo, the file indexing and search framework used in KDE Plasma. It manages the file indexer daemon that powers fast desktop file search, allowing users to enable, disable, suspend, and monitor the indexing process.\n\nThe tool is useful for troubleshooting search issues, reclaiming disk space used by the index, and checking which files have been indexed."],["PARAMETERS","**status**\n> Display current indexer status\n\n**enable**\n> Enable the file indexer\n\n**disable**\n> Disable the file indexer\n\n**purge**\n> Delete the index database\n\n**suspend**\n> Temporarily suspend indexing\n\n**resume**\n> Resume suspended indexing\n\n**indexSize**\n> Show disk space used by index\n\n**check**\n> Find and index unindexed files\n\n**index** _file..._\n> Manually index specific files\n\n**clear** _file..._\n> Remove specific files from the index\n\n**config**\n> Modify Baloo configuration\n\n**monitor**\n> Monitor indexer activity\n\n**failed**\n> List files that could not be indexed"],["CONFIGURATION","**~/.config/baloofilerc**\n> Controls which directories to index, file types to exclude, and indexing behavior."],["CAVEATS","Purging the index requires re-indexing all files which can take time. The indexer respects KDE settings for which directories to index. High CPU usage during indexing is normal."],["HISTORY","**balooctl** is part of **Baloo**, the file indexing and search framework for KDE Plasma desktop."],["SEE ALSO","[locate](/man/locate)(1)"]],"e":["locate"],"x":"balooctl status"},"bracket":{"n":"bracket","t":"Evaluate conditional expressions in shell scripts","s":[["TAGLINE","Evaluate conditional expressions in shell scripts"],["TLDR","**Test condition (synonym for test)**\n\n```[ -f [file] ] && echo \"exists\"```\n\n**String comparison**\n\n```[ \"[string1]\" = \"[string2]\" ]```\n\n**Numeric comparison**\n\n```[ [5] -gt [3] ]```\n\n**File tests**\n\n```[ -d [directory] ] && echo \"is directory\"```\n\n**Combine conditions**\n\n```[ -f [file] ] && [ -r [file] ]```"],["SYNOPSIS","**[** _expression_ **]**"],["FILE TESTS","**-e** _file_: File exists\n**-f** _file_: Regular file\n**-d** _file_: Directory\n**-r** _file_: Readable\n**-w** _file_: Writable\n**-x** _file_: Executable\n**-s** _file_: Size > 0\n**-L** _file_: Symbolic link"],["STRING TESTS","**-z** _string_: Length is zero\n**-n** _string_: Length is non-zero\n_s1_ **=** _s2_: Strings equal\n_s1_ **!=** _s2_: Strings not equal"],["NUMERIC TESTS","_n1_ **-eq** _n2_: Equal\n_n1_ **-ne** _n2_: Not equal\n_n1_ **-lt** _n2_: Less than\n_n1_ **-le** _n2_: Less or equal\n_n1_ **-gt** _n2_: Greater than\n_n1_ **-ge** _n2_: Greater or equal"],["DESCRIPTION","**[** is the POSIX test command for evaluating conditional expressions. It's equivalent to **test** but requires a closing **]**. Spaces around **[** and **]** are mandatory.\n\nThe command returns exit status 0 (true) or 1 (false), used in if statements and conditional execution.\n\n```bash\nif [ -f \"$file\" ]; then\n    echo \"File exists\"\nfi\n\n[ -d \"$dir\" ] && cd \"$dir\"\n```\n\nFor bash/zsh, **[[** provides an enhanced version with pattern matching and safer syntax."],["CAVEATS","**[** is a command, not syntax. Spaces are required: **[ \"$a\" = \"$b\" ]** not **[\"$a\"=\"$b\"]**.\n\nAlways quote variables: **[ \"$var\" = \"test\" ]** to handle empty values and spaces.\n\nUse **-a** and **-o** for AND/OR, or combine with **&&** and **||** outside brackets.\n\n**=** is for strings, **-eq** for numbers: **[ \"01\" = \"1\" ]** is false, **[ 01 -eq 1 ]** is true."],["HISTORY","**[** (test) is one of the original Unix commands, specified in POSIX.1. The **[[** enhanced version was introduced in **ksh88** and adopted by bash and zsh."],["INSTALL","```aur: yay -S bracket```"],["SEE ALSO","[test](/man/test)(1), [bash](/man/bash)(1), [sh](/man/sh)(1)"]],"e":["test","bash","sh"],"x":"[ -f [file] ] && echo \"exists\""},"bun-build":{"n":"bun-build","t":"Bundle JavaScript and TypeScript for production","s":[["TAGLINE","Bundle JavaScript and TypeScript for production"],["TLDR","**Bundle JavaScript/TypeScript file**\n\n```bun build [./src/index.ts] --outdir [./dist]```\n\n**Create standalone executable**\n\n```bun build [./server.ts] --compile --outfile [server]```\n\n**Bundle with minification**\n\n```bun build [./src/index.ts] --outdir [./dist] --minify```\n\n**Generate source maps**\n\n```bun build [./src/index.ts] --outdir [./dist] --sourcemap```\n\n**Bundle for Node.js target**\n\n```bun build [./src/index.ts] --outdir [./dist] --target [node]```\n\n**Mark packages as external**\n\n```bun build [./src/index.ts] --outdir [./dist] --external [react]```\n\n**Bundle multiple entrypoints**\n\n```bun build [./src/index.ts] [./src/worker.ts] --outdir [./dist]```"],["SYNOPSIS","**bun build** _entrypoints_ [_options_]"],["DESCRIPTION","**bun build** is Bun's fast native bundler for JavaScript and TypeScript. It bundles code for production, generates standalone executables, and supports both server and client code in a single command.\n\nThe bundler automatically performs tree-shaking to eliminate unused code. It can target multiple runtimes including browsers, Node.js, and Bun itself. The **--compile** flag creates self-contained executables with the Bun runtime embedded, enabling distribution without requiring users to install Bun. Code splitting, source maps, and minification are supported through command-line flags or the equivalent JavaScript API via **Bun.build()**."],["PARAMETERS","**--outdir** _directory_\n> Output directory for bundled files\n\n**--outfile** _file_\n> Output file path (for single output)\n\n**--compile**\n> Create standalone executable with embedded Bun runtime\n\n**--minify**\n> Enable minification\n\n**--sourcemap**\n> Generate source maps (external, inline, or none)\n\n**--target** _runtime_\n> Target runtime: browser, bun, or node\n\n**--external** _package_\n> Exclude package from bundle\n\n**--splitting**\n> Enable code splitting\n\n**--format** _type_\n> Output format: esm or cjs\n\n**--define** _key=value_\n> Define global constants\n\n**--loader** _ext:loader_\n> Configure file type loaders"],["JAVASCRIPT API","```javascript\nawait Bun.build({\n  entrypoints: ['./src/index.js'],\n  outdir: './build',\n  minify: true,\n})\n```"],["CAVEATS","Standalone executables include a copy of the Bun runtime, increasing file size. Tree-shaking is always enabled. Some npm packages may not bundle correctly due to dynamic imports."],["INSTALL","```pacman: sudo pacman -S bun```\n\n```brew: brew install bun```\n\n```nix: nix profile install nixpkgs#bun```"],["SEE ALSO","[bun](/man/bun)(1), [bun-run](/man/bun-run)(1), [esbuild](/man/esbuild)(1), [webpack](/man/webpack)(1)"],["RESOURCES","```[Source code](https://github.com/oven-sh/bun)```\n\n```[Homepage](https://bun.sh)```\n\n```[Documentation](https://bun.sh/docs)```"]],"e":["bun","bun-run","esbuild","webpack"],"x":"bun build [./src/index.ts] --outdir [./dist]"},"cargo-help":{"n":"cargo-help","t":"Display help for Cargo commands","s":[["TAGLINE","Display help for Cargo commands"],["TLDR","**Show general help**\n\n```cargo help```\n\n**Show help for specific command**\n\n```cargo help [build]```\n\n**List all installed commands**\n\n```cargo --list```\n\n**Verbose command list**\n\n```cargo --list --verbose```\n\n**Explain error code**\n\n```cargo --explain [E0004]```"],["SYNOPSIS","**cargo help** [_command_]\n**cargo** --help\n**cargo** --list"],["DESCRIPTION","**cargo help** displays help information for Cargo commands. Use it to learn about available commands, their options, and usage patterns."],["PARAMETERS","**--help**, **-h**\n> Show help message\n\n**--list**\n> List all installed Cargo subcommands\n\n**--verbose**, **-v**\n> Print extra information with --list\n\n**--explain** _code_\n> Run rustc --explain for error code"],["COMMAND CATEGORIES","**General Commands**\n> cargo, cargo help\n\n**Build Commands**\n> build, check, clean, run, test, bench\n\n**Manifest Commands**\n> add, remove, fetch, generate-lockfile, update\n\n**Package Commands**\n> init, new, search, install, uninstall\n\n**Publishing Commands**\n> package, publish, yank, owner"],["CUSTOM COMMANDS","Custom subcommands installed via cargo install are listed with --list. Any executable named cargo-* in PATH becomes a cargo subcommand."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [rustc](/man/rustc)(1)"],["RESOURCES","```[Source code](https://github.com/rust-lang/cargo)```\n\n```[Documentation](https://doc.rust-lang.org/cargo/commands/cargo-help.html)```"]],"e":["cargo","rustc"],"x":"cargo help"},"cargo-locate-project":{"n":"cargo-locate-project","t":"Print the path to a project's Cargo.toml","s":[["TAGLINE","Print the path to a project's Cargo.toml"],["TLDR","**Show manifest path**\n\n```cargo locate-project```\n\n**Show workspace root manifest**\n\n```cargo locate-project --workspace```\n\n**Output as plain path**\n\n```cargo locate-project --message-format plain```\n\n**Output as JSON**\n\n```cargo locate-project --message-format json```\n\n**Locate project from a specific directory**\n\n```cargo locate-project --manifest-path [path/to/Cargo.toml]```"],["SYNOPSIS","**cargo locate-project** [_options_]"],["DESCRIPTION","**cargo locate-project** prints the path to the Cargo.toml manifest file for the current package. Searches current directory and parent directories for the manifest."],["PARAMETERS","**--workspace**\n> Locate workspace root Cargo.toml instead of current package\n\n**--message-format** _format_\n> Output format: json (default), plain\n\n**--manifest-path** _path_\n> Path to Cargo.toml\n\n**-v**, **--verbose**\n> Verbose output\n\n**-q**, **--quiet**\n> Suppress log messages\n\n**--color** _WHEN_\n> Control color output: auto, always, never"],["OUTPUT","**JSON format (default)**\n```json\n{\"root\":\"/path/to/project/Cargo.toml\"}\n```\n\n**Plain format**\n```\n/path/to/project/Cargo.toml\n```"],["EXIT STATUS","**0**\n> Success\n\n**101**\n> Failed to locate manifest"],["CAVEATS","Useful for scripts and tooling that need to find the project root. Returns exit code 101 if no Cargo.toml is found in the current or any parent directory."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-metadata](/man/cargo-metadata)(1), [cargo-pkgid](/man/cargo-pkgid)(1)"]],"e":["cargo","cargo-metadata","cargo-pkgid"],"x":"cargo locate-project"},"cargo-rustdoc":{"n":"cargo-rustdoc","t":"Build documentation with extra rustdoc flags","s":[["TAGLINE","Build documentation with extra rustdoc flags"],["TLDR","**Build docs with rustdoc options**\n\n```cargo rustdoc -- [rustdoc_flags]```\n\n**Document with private items**\n\n```cargo rustdoc -- --document-private-items```\n\n**Output JSON format (nightly)**\n\n```cargo rustdoc -Z unstable-options --output-format json```\n\n**Document specific binary**\n\n```cargo rustdoc --bin [name] -- [flags]```\n\n**Build docs for all features**\n\n```cargo rustdoc --all-features```"],["SYNOPSIS","**cargo rustdoc** [_options_] [-- _args_]"],["DESCRIPTION","**cargo rustdoc** builds documentation for the current package and passes extra options directly to the rustdoc tool. Arguments after `--` are forwarded only to the final rustdoc invocation for the specified target, not to dependencies.\n\nThis command is similar to `cargo doc` but provides fine-grained control over rustdoc flags. Common uses include documenting private items, generating JSON output for tooling (on nightly), and passing custom HTML or CSS settings. Only one target can be documented at a time when extra arguments are provided."],["PARAMETERS","**--open**\n> Open docs in browser after building\n\n**--lib**\n> Document library only\n\n**--bin** _name_\n> Document specified binary\n\n**--bins**\n> Document all binaries\n\n**--example** _name_\n> Document specified example\n\n**--examples**\n> Document all examples\n\n**-p**, **--package** _spec_\n> Package to document\n\n**--target** _triple_\n> Document for target platform\n\n**-j**, **--jobs** _n_\n> Parallel jobs\n\n**--features** _features_\n> Enable features\n\n**--all-features**\n> Enable all features\n\n**--no-default-features**\n> Disable default features"],["OUTPUT FORMATS","**html**\n> HTML documentation (default)\n\n**json**\n> JSON format (nightly, experimental)"],["CAVEATS","Only one target can be documented when passing extra arguments. Use target filters to select."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-doc](/man/cargo-doc)(1), [rustdoc](/man/rustdoc)(1)"],["RESOURCES","```[Source code](https://github.com/rust-lang/cargo)```\n\n```[Documentation](https://doc.rust-lang.org/cargo/commands/cargo-rustdoc.html)```"]],"e":["cargo","cargo-doc","rustdoc"],"x":"cargo rustdoc -- [rustdoc_flags]"},"ccgs":{"n":"ccgs","t":"Share Claude Code sessions through a git branch","s":[["TAGLINE","Share Claude Code sessions through a git branch"],["TLDR","**Publish your local sessions** to the shared branch\n\n```ccgs push```\n\n**Push only a specific session** by id or name\n\n```ccgs push [session_id]```\n\n**Fetch shared sessions** into the local project\n\n```ccgs pull```\n\n**Run without installing** through npx\n\n```npx claude-git-sessions pull```\n\n**Delete a shared session** by name without a prompt\n\n```ccgs delete [name] --yes```\n\n**Share memory facts** with the team\n\n```ccgs memory push```"],["SYNOPSIS","**ccgs** _command_ [_targets ..._] [_options_]"],["DESCRIPTION","**ccgs** (claude-git-sessions) shares **Claude Code** transcripts across a team by storing them on an **orphan git branch** (named @ccgs/<name>) inside an existing repository, with no server or extra infrastructure. Sessions are keyed by their Claude Code session UUID so authors do not collide.\n\nThe tool uses low-level git plumbing (**hash-object**, **write-tree**, **commit-tree**, **push**) against a temporary index, so it never touches the working tree or current branch and works even on a dirty repository. On **pull** the cwd field of each transcript is rewritten so that **claude --resume** works from the repository root. The same mechanism stores shared memory facts."],["COMMANDS","**pull** [**--force**] [**--exclude-memory**]\n> Fetch sessions (and, by default, shared memory) from the branch into the local project. --force overwrites local sessions that are newer.\n\n**push** [_targets ..._] [**--exclude-memory**]\n> Publish local sessions to the branch. Name specific sessions, or push all if no target is given. Creates the orphan branch on first push.\n\n**delete** _id|name_ [**--yes**] [**--local**]\n> Remove a session by UUID, UUID prefix (4 or more chars) or unique name. Prompts unless --yes; affects only the shared branch unless --local is given.\n\n**memory push** [**--all**]\n> Publish memory facts; shares project and reference facts by default, --all also includes user and feedback facts.\n\n**memory pull** [**--all**] [**--force**]\n> Fetch shared memory facts and merge them into the local memory directory."],["OPTIONS","**-b**, **--branch** _name_\n> Select the session set / branch suffix (default: default).\n\n**--remote** _remote_\n> Git remote to use (default: origin).\n\n**-v**, **--version**\n> Print the version.\n\n**-h**, **--help**\n> Show help."],["CAVEAT","Requires **Node 20+** and **git 2.5+**. Sessions live on a branch that shares no history with the main branch; pushing publishes transcript content to the remote, so treat that remote as you would any shared store of conversation data."],["HISTORY","**ccgs** is an open-source tool written in **TypeScript** by **Ingram Technologies**, distributed on npm as **claude-git-sessions** under the **MIT** license."],["SEE ALSO","[claude](/man/claude)(1), [git](/man/git)(1), [npx](/man/npx)(1), [npm](/man/npm)(1)"]],"e":["claude","git","npx","npm"],"x":"ccgs push"},"chdir":{"n":"chdir","t":"Change the current working directory","s":[["TAGLINE","Change the current working directory"],["TLDR","**Change to a specific directory**\n\n```chdir [path/to/directory]```\n\n**Change to home directory**\n\n```chdir ~```\n\n**Change to parent directory**\n\n```chdir ..```\n\n**Change to previous directory**\n\n```chdir -```"],["SYNOPSIS","**chdir** [_directory_]"],["DESCRIPTION","**chdir** changes the current working directory of the shell to the specified path. It is functionally equivalent to **cd** and available as a builtin in zsh, csh, and tcsh.\n\nWhen called without arguments, it changes to the user's home directory. The **CDPATH** variable is searched if the specified directory is not found relative to the current directory."],["CAVEATS","**chdir** is not available in bash (use **cd** instead). For portable scripts, always use **cd**. The command is a shell builtin and does not exist as a standalone executable."],["HISTORY","**chdir** is the original name of the directory-changing system call in **Unix**, dating back to the **First Edition** in **1971**. The **cd** command was introduced as a shorter alias. Shells like **csh** (1978) and later **zsh** retained **chdir** as a builtin alongside **cd**."],["SEE ALSO","[cd](/man/cd)(1), [pushd](/man/pushd)(1), [popd](/man/popd)(1), [pwd](/man/pwd)(1)"]],"e":["cd","pushd","popd","pwd"],"x":"chdir [path/to/directory]"},"chem":{"n":"chem","t":"groff preprocessor for chemical structure diagrams","s":[["TAGLINE","groff preprocessor for chemical structure diagrams"],["TLDR","**Process a chem file and view the output**\n\n```chem [path/to/file.chem] | groffer```\n\n**Process and convert to PostScript**\n\n```chem [path/to/file.chem] | groff -p -Tps > [output.ps]```\n\n**Process from stdin**\n\n```echo '.cstart\\nCH3\\nbond\\nCH3\\n.cend' | chem | groffer```\n\n**Display version information**\n\n```chem --version```"],["SYNOPSIS","**chem** [_option..._] [**--**] [_filespec..._]\n\n**chem** **-h** | **--help**\n\n**chem** **-v** | **--version**"],["DESCRIPTION","**chem** is a groff preprocessor for producing chemical structure diagrams. It translates chemical notation into **pic** language diagrams, which can then be processed by groff for rendering.\n\nThe tool is best suited for organic chemistry, supporting bonds, rings, moieties (like CH3, NH3), and strings. Chemical diagrams are enclosed between **.cstart** and **.cend** markers.\n\n**Example input:**\n```\n.cstart\nCH3\nbond\nCH3\n.cend\n```\n\nSince chem is a pic preprocessor, raw pic statements can be included within diagrams for custom drawings."],["PARAMETERS","**-h, --help**\n> Display usage message and exit\n\n**-v, --version**\n> Display version information and exit\n\n**filespec**\n> Input file or **-** for stdin. Without arguments, reads stdin"],["CAVEATS","No library or file inclusion mechanism exists. No shorthand for repetitive structures. The extension mechanism uses pic macros, which can be tricky to implement correctly.\n\nThere is no in-line chemistry syntax (like eqn's $...$ construct) and no way to control bond entry points on groups."],["HISTORY","The GNU version of **chem** was written by **Bernd Warken**, based on documentation of Brian Kernighan's original awk version. The chemical structure diagram language was developed at Bell Labs alongside other troff preprocessors like **eqn** and **pic**."],["INSTALL","```apt: sudo apt install groff```\n\n```dnf: sudo dnf install groff```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[groff](/man/groff)(1), [pic](/man/pic)(1), [eqn](/man/eqn)(1), [groffer](/man/groffer)(1)"],["RESOURCES","```[Source code](https://git.savannah.gnu.org/cgit/groff.git)```\n\n```[Homepage](https://www.gnu.org/software/groff/)```"]],"e":["groff","pic","eqn","groffer"],"x":"chem [path/to/file.chem] | groffer"},"cli4":{"n":"cli4","t":"Cloudflare API v4 command-line client","s":[["TAGLINE","Cloudflare API v4 command-line client"],["TLDR","**List all zones** (domains) in your Cloudflare account\n\n```cli4 /zones```\n\n**Get details for a specific zone** by ID\n\n```cli4 /zones/[zone_id]```\n\n**Purge entire cache** for a zone\n\n```cli4 --post /zones/[zone_id]/purge_cache purge_everything:=true```\n\n**List DNS records** for a zone\n\n```cli4 /zones/[zone_id]/dns_records```\n\n**Create a DNS record**\n\n```cli4 --post /zones/[zone_id]/dns_records type:='\"A\"' name:='\"www\"' content:='\"192.0.2.1\"'```\n\n**Delete a DNS record**\n\n```cli4 --delete /zones/[zone_id]/dns_records/[record_id]```\n\n**Update a page rule**\n\n```cli4 --put /zones/[zone_id]/pagerules/[rule_id] status:='\"active\"'```"],["SYNOPSIS","**cli4** [_options_] _/endpoint_ [_data_...]"],["PARAMETERS","**--get**\n> HTTP GET request (default).\n\n**--post**\n> HTTP POST request.\n\n**--put**\n> HTTP PUT request.\n\n**--patch**\n> HTTP PATCH request.\n\n**--delete**\n> HTTP DELETE request.\n\n**-q**, **--quiet**\n> Suppress output.\n\n**-v**, **--verbose**\n> Verbose output including HTTP headers.\n\n**-r**, **--raw**\n> Output raw JSON without formatting.\n\n**-d**, **--dump**\n> Dump the request instead of executing it."],["DESCRIPTION","**cli4** is a command-line tool for interacting with the Cloudflare API v4. It provides direct access to all Cloudflare API endpoints, allowing management of zones, DNS records, firewall rules, page rules, and other Cloudflare features from the terminal.\n\nThe tool uses a simple syntax where API endpoints are specified as paths and data is passed as key-value pairs. JSON values use the **:=** operator for proper typing, while string assignment uses **=**. Authentication is handled via environment variables or a configuration file.\n\ncli4 is particularly useful for automation and scripting Cloudflare operations. It can be integrated into CI/CD pipelines for cache purging, DNS updates, or firewall rule management."],["CONFIGURATION","**~/.cloudflare/cloudflare.cfg**\n> Stores API credentials (email, key, or token) as an alternative to environment variables."],["CAVEATS","Requires authentication via **CF_API_EMAIL** and **CF_API_KEY** environment variables, or **CF_API_TOKEN** for API tokens. Zone IDs must be retrieved separately; they are not the domain name. Rate limiting applies per Cloudflare API documentation."],["HISTORY","cli4 was created as an unofficial Python-based CLI for Cloudflare's API v4, which was released in **2014**. It provides a lightweight alternative to Cloudflare's official tools, focusing on direct API access rather than high-level abstractions."],["SEE ALSO","[wrangler](/man/wrangler)(1), [curl](/man/curl)(1), [flarectl](/man/flarectl)(1), [jq](/man/jq)(1)"]],"e":["wrangler","curl","flarectl","jq"],"x":"cli4 /zones"},"csslint":{"n":"csslint","t":"CSS code quality checker","s":[["TAGLINE","CSS code quality checker"],["TLDR","**Lint a CSS file**\n\n```csslint [file.css]```\n\n**Lint with specific rules**\n\n```csslint --errors=[rule1,rule2] [file.css]```\n\n**Lint with warnings**\n\n```csslint --warnings=[rule1,rule2] [file.css]```\n\n**Output in specific format**\n\n```csslint --format=[compact|json|text] [file.css]```\n\n**Ignore specific rules**\n\n```csslint --ignore=[rule1,rule2] [file.css]```"],["SYNOPSIS","**csslint** [_options_] _file.css_ [_files..._]"],["PARAMETERS","**--errors** _rules_\n> Comma-separated list of rules to treat as errors.\n\n**--warnings** _rules_\n> Comma-separated list of rules to treat as warnings.\n\n**--ignore** _rules_\n> Comma-separated list of rules to ignore.\n\n**--format** _format_\n> Output format: text, compact, json, checkstyle-xml, lint-xml, junit-xml.\n\n**--list-rules**\n> Show all available rules.\n\n**--quiet**\n> Suppress the `csslint: There are <n> problems` summary line.\n\n**--version**\n> Print the csslint version and exit.\n\n**--help**\n> Show usage information."],["DESCRIPTION","**csslint** is a tool for checking CSS code quality. It identifies potential problems like inefficient selectors, browser compatibility issues, and stylistic errors. The tool checks for patterns that may cause cross-browser issues, performance problems, or maintenance difficulties.\n\nRules are categorized by severity and cover topics like selector performance, box model compatibility, known browser bugs, and best practices. csslint can be integrated into build processes and continuous integration workflows."],["CONFIGURATION","**.csslintrc**\n> JSON configuration file for customizing rules and severity levels."],["SEE ALSO","[sass](/man/sass)(1), [prettier](/man/prettier)(1), [postcss](/man/postcss)(1)"]],"e":["sass","prettier","postcss"],"x":"csslint [file.css]"},"dasel":{"n":"dasel","t":"universal data selector for querying and modifying structured data","s":[["TAGLINE","universal data selector for querying and modifying structured data"],["TLDR","**Query a value** from a JSON file\n\n```dasel -f [file.json] '[selector]'```\n\n**Query a nested value** from YAML\n\n```dasel -f [config.yaml] '.database.host'```\n\n**Convert JSON to YAML**\n\n```dasel -f [input.json] -p json -w yaml```\n\n**Modify a value** in a JSON file\n\n```dasel put -f [file.json] -v '[new_value]' '[selector]'```\n\n**Delete a key** from a YAML file\n\n```dasel delete -f [file.yaml] '[selector]'```\n\n**Read from stdin** and output as JSON\n\n```cat [file.yaml] | dasel -p yaml -w json```\n\n**Query a value from XML**\n\n```dasel -f [file.xml] '.root.element'```"],["SYNOPSIS","**dasel** [_command_] [_flags_] [_selector_]\n\n**dasel** select [_-f file_] [_-p parser_] [_-w writer_] _selector_\n\n**dasel** put [_-f file_] [_-t type_] [_-v value_] _selector_\n\n**dasel** delete [_-f file_] _selector_"],["PARAMETERS","**-f**, **--file** _PATH_\n> Input file path; reads from stdin if omitted\n\n**-p**, **--parser** _FORMAT_\n> Parser for input data (json, yaml, toml, xml, csv, ini, hcl); auto-detected from file extension\n\n**-w**, **--write** _FORMAT_\n> Output format; defaults to input parser type\n\n**-t**, **--type** _TYPE_\n> Value type for put command (string, int, bool, json)\n\n**-v**, **--value** _VALUE_\n> Value to set with put command\n\n**-o**, **--out** _FILE_\n> Output file path; writes to stdout if omitted\n\n**-r**, **--read** _FORMAT_\n> Alias for --parser\n\n**--pretty**\n> Pretty-print output for readability\n\n**-n**, **--null**\n> Output null values explicitly\n\n**--colour**, **--color**\n> Enable colored output\n\n**--escape-html**\n> Escape HTML entities in output\n\n**-h**, **--help**\n> Display help information\n\n**-v**, **--version**\n> Display version information"],["DESCRIPTION","**dasel** (Data Selector) is a command-line tool for querying and modifying structured data files. It provides a unified interface for working with JSON, YAML, TOML, XML, CSV, INI, and HCL formats using a consistent selector syntax.\n\nThe tool supports three main operations: **select** for querying data, **put** for modifying or adding values, and **delete** for removing keys. Selectors use dot notation for nested access (e.g., `.database.host`) and bracket notation for array indices (e.g., `.users.[0].name`).\n\nDasel can convert between formats by specifying different input and output parsers, making it useful for configuration file transformations. It reads from files or stdin and writes to files or stdout, integrating well with shell pipelines."],["CAVEATS","Comments in YAML and TOML files are discarded when writing due to parser limitations. The entire document is loaded into memory, so very large files may cause high memory usage. Array indices are zero-based. When modifying files, the original formatting may not be preserved exactly."],["HISTORY","Dasel was created by **Tom Wright** and first released in **2020**. Written in **Go**, it was designed as a universal alternative to format-specific tools like **jq** (JSON), **yq** (YAML), and **xmlstarlet** (XML). The name is a portmanteau of \"data\" and \"select\". It gained popularity for its ability to use identical selector syntax across multiple data formats and its single-binary distribution with no dependencies."],["INSTALL","```apt: sudo apt install dasel```\n\n```apk: sudo apk add dasel```\n\n```brew: brew install dasel```\n\n```nix: nix profile install nixpkgs#dasel```"],["SEE ALSO","[jq](/man/jq)(1), [yq](/man/yq)(1), [xmlstarlet](/man/xmlstarlet)(1), [mlr](/man/mlr)(1)"]],"e":["jq","yq","xmlstarlet","mlr"],"x":"dasel -f [file.json] '[selector]'"},"dbt":{"n":"dbt","t":"data transformation workflow tool for analytics engineering","s":[["TAGLINE","data transformation workflow tool for analytics engineering"],["TLDR","**Initialize a new dbt** project\n\n```dbt init [project_name]```\n\n**Run all models**\n\n```dbt run```\n\n**Run specific model**\n\n```dbt run --select [model_name]```\n\n**Test data quality**\n\n```dbt test```\n\n**Generate documentation**\n\n```dbt docs generate```\n\n**Serve documentation** locally\n\n```dbt docs serve```\n\n**Build** all resources (run models, test, snapshot, seed)\n\n```dbt build```\n\n**Load seed CSV** files into warehouse\n\n```dbt seed```\n\n**Compile SQL without** executing\n\n```dbt compile```\n\n**Retry** the last failed command from point of failure\n\n```dbt retry```"],["SYNOPSIS","**dbt** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> dbt command to execute (run, test, build, compile, etc.).\n\n**--select**, **-s** _MODEL_\n> Select specific models to run.\n\n**--exclude** _MODEL_\n> Exclude specific models from run.\n\n**--target**, **-t** _TARGET_\n> Target profile to use.\n\n**--profiles-dir** _DIR_\n> Directory containing profiles.yml.\n\n**--project-dir** _DIR_\n> Directory containing dbt_project.yml.\n\n**--full-refresh**\n> Rebuild incremental models from scratch.\n\n**--threads** _N_\n> Number of threads to run models in parallel.\n\n**--vars** _JSON_\n> Pass variables as JSON.\n\n**--help**\n> Display help information."],["CONFIGURATION","**~/.dbt/profiles.yml**\n> Connection profiles for data warehouses, including credentials and connection parameters.\n\n**dbt_project.yml**\n> Project configuration defining models, tests, sources, and project-level settings."],["DESCRIPTION","**dbt** (data build tool) is a transformation workflow tool that enables data analysts and engineers to transform data in their warehouse using SQL. It follows software engineering practices like version control, testing, and documentation for data transformations.\n\ndbt works with your existing data warehouse (Snowflake, BigQuery, Redshift, PostgreSQL, etc.) and manages the T in ELT (Extract, Load, Transform). Models are defined as SQL SELECT statements that dbt materializes as tables or views.\n\nThe tool provides dependency management between models, automated testing with schema tests and custom tests, documentation generation, and incremental processing for efficient updates of large datasets."],["CAVEATS","Requires Python and a connection to a supported data warehouse. Complex dependencies may lead to long DAG resolution times. Resource usage scales with project size. Breaking changes occasionally occur between major versions."],["HISTORY","dbt was created by **Fishtown Analytics** (now dbt Labs) and released in **2016**. It pioneered the \"analytics engineering\" approach, bringing software development practices to data transformation and helping establish the modern data stack paradigm."],["INSTALL","```nix: nix profile install nixpkgs#dbt```"],["SEE ALSO","[python](/man/python)(1), [psql](/man/psql)(1), [bq](/man/bq)(1)"]],"e":["python","psql","bq"],"x":"dbt init [project_name]"},"docker-stats":{"n":"docker-stats","t":"display live container resource usage statistics","s":[["TAGLINE","display live container resource usage statistics"],["TLDR","**Show live stats for all containers**\n\n```docker stats```\n\n**Show stats for specific containers**\n\n```docker stats [container1] [container2]```\n\n**Show one-time snapshot**\n\n```docker stats --no-stream```\n\n**Custom output format**\n\n```docker stats --format \"table {{.Name}}\\t{{.CPUPerc}}\\t{{.MemUsage}}\"```\n\n**Include stopped containers**\n\n```docker stats -a```"],["SYNOPSIS","**docker** **stats** [_options_] [_container..._]"],["PARAMETERS","**-a**, **--all**\n> Show all containers (default shows running).\n\n**--no-stream**\n> Disable streaming, show single snapshot.\n\n**--no-trunc**\n> Don't truncate output.\n\n**--format** _string_\n> Format output using Go template."],["DESCRIPTION","**docker stats** displays a live stream of container resource usage statistics including CPU percentage, memory usage/limit, network I/O, and block I/O."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-container-stats](/man/docker-container-stats)(1), [docker-top](/man/docker-top)(1)"],["RESOURCES","```[Source code](https://github.com/docker/cli)```\n\n```[Documentation](https://docs.docker.com/reference/cli/docker/)```"]],"e":["docker-container-stats","docker-top"],"x":"docker stats"},"doctl-databases-replica":{"n":"doctl-databases-replica","t":"manage database read replicas","s":[["TAGLINE","manage database read replicas"],["TLDR","**List replicas**\n\n```doctl databases replica list [cluster_id]```\n\n**Create a read replica** in another region\n\n```doctl databases replica create [cluster_id] [replica_name] --region [fra1] --size [db-s-1vcpu-1gb]```\n\n**Get replica info**\n\n```doctl databases replica get [cluster_id] [replica_name]```\n\n**Retrieve connection details** for a replica\n\n```doctl databases replica connection [cluster_id] [replica_name]```\n\n**Delete a replica**\n\n```doctl databases replica delete [cluster_id] [replica_name]```\n\n**Promote a replica** to a standalone primary cluster\n\n```doctl databases replica promote [cluster_id] [replica_name]```"],["SYNOPSIS","**doctl** **databases** **replica** _command_ _database-cluster-id_ [_replica-name_] [_flags_]"],["SUBCOMMANDS","**list** _cluster-id_\n> Retrieve the list of read-only replicas for a cluster.\n\n**create** _cluster-id_ _name_\n> Create a read-only replica. Accepts **--region**, **--size**, and **--private-network-uuid**.\n\n**get** _cluster-id_ _name_\n> Retrieve information about a read-only replica.\n\n**delete** _cluster-id_ _name_\n> Delete a read-only replica. **-f**, **--force** skips the confirmation prompt.\n\n**connection** _cluster-id_ _name_\n> Retrieve the connection string, host, port, user, and password for a replica.\n\n**promote** _cluster-id_ _name_\n> Promote a read-only replica to become an independent primary cluster."],["OPTIONS","**--region** _SLUG_\n> Region to place the replica in, e.g. `nyc1` or `fra1`. Defaults to the primary's region.\n\n**--size** _SLUG_\n> Machine size for the replica, e.g. `db-s-1vcpu-1gb`.\n\n**--private-network-uuid** _UUID_\n> VPC to attach the replica to.\n\n**-o**, **--output** _FORMAT_\n> Output format: `text` (default) or `json`.\n\n**--format** _FIELDS_\n> Comma-separated list of columns to display."],["DESCRIPTION","**doctl databases replica** manages read replicas for DigitalOcean managed database clusters. Read replicas are read-only copies of a primary database that provide horizontal scaling for read-heavy workloads and geographic distribution of data.\n\nReplicas asynchronously replicate data from the primary cluster, allowing applications to distribute read queries across multiple database instances. This reduces load on the primary and improves query performance. Replicas can be placed in different regions for lower latency to geographically distributed users.\n\nIn disaster recovery scenarios, replicas can be promoted to standalone clusters, creating an independent database cluster from the replica. This provides failover capabilities and data migration options."],["CAVEATS","Replication is asynchronous, so a replica can lag behind the primary and serve slightly stale rows. **promote** is irreversible: the replica becomes an independent cluster, stops following the primary, and is billed as a full cluster from then on. Read replicas are not offered for every engine or plan (they are unavailable on the smallest shared-CPU tiers and on some engines), and each replica is billed at the price of its own size."],["INSTALL","```dnf: sudo dnf install doctl```\n\n```pacman: sudo pacman -S doctl```\n\n```apk: sudo apk add doctl```\n\n```brew: brew install doctl```\n\n```nix: nix profile install nixpkgs#doctl```"],["SEE ALSO","[doctl](/man/doctl)(1), [doctl-databases](/man/doctl-databases)(1), [doctl-databases-user](/man/doctl-databases-user)(1)"],["RESOURCES","```[Source code](https://github.com/digitalocean/doctl)```\n\n```[Documentation](https://docs.digitalocean.com/reference/doctl/reference/databases/replica/)```"]],"e":["doctl","doctl-databases","doctl-databases-user"],"x":"doctl databases replica list [cluster_id]"},"ds4":{"n":"ds4","t":"Command-line monitor for Sony DualShock 4 controllers on Linux","s":[["TAGLINE","Command-line monitor for Sony DualShock 4 controllers on Linux"],["TLDR","**Monitor** the first connected DualShock 4 controller\n\n```ds4```\n\n**Watch a specific** hidraw device\n\n```ds4 [/dev/hidraw0]```\n\n**Show** the embedded help\n\n```ds4 --help```"],["SYNOPSIS","**ds4** [_device_]"],["DESCRIPTION","**ds4** is a small command-line monitor for **Sony DualShock 4** (PS4) controllers connected to a Linux machine over USB or Bluetooth. It opens the controller's **hidraw** node, parses incoming HID input reports, and continuously prints the decoded state of every analog stick, trigger, button, touchpad point, gyroscope axis, accelerometer axis and battery indicator.\n\nUnlike **ds4drv**, which creates virtual gamepad and mouse devices via **uinput**, **ds4** does not emulate any input devices — it only reads and prints. This makes it useful for verifying that a controller is detected, debugging button mappings before configuring a wrapper such as **antimicrox** or **AntiMicroX**, reverse-engineering vendor-specific HID reports, or characterising sensor drift on the IMU.\n\nThe tool is written in Go and distributed in source form via the Go module path **github.com/kenshaw/ds4**. The first positional argument can override the auto-detected hidraw device path."],["CAVEATS","Reading from **/dev/hidraw\\*** typically requires **root** privileges or a udev rule granting access to the **input** group; otherwise the program will fail with a permission error on open. The decoder targets the first-generation DS4 (CUH-ZCT1U) and the revised CUH-ZCT2U; DualShock 3 and DualSense (PS5) controllers use different report layouts and are not supported. Bluetooth-connected controllers send larger reports than USB ones — both are handled, but the byte offsets differ between transports."],["HISTORY","**ds4** was written by **Kenneth Shaw** (kenshaw) and published on GitHub as a small Go utility for inspecting the DualShock 4 protocol on Linux. It complements the broader Linux PS4-controller ecosystem (**ds4drv**, **hid-playstation** kernel driver, **DualSense Tools**) by providing a minimal, dependency-free monitor."],["SEE ALSO","[evtest](/man/evtest)(1), [jstest](/man/jstest)(1)"]],"e":["evtest","jstest"],"x":"ds4"},"engrampa":{"n":"engrampa","t":"MATE desktop archive manager","s":[["TAGLINE","MATE desktop archive manager"],["TLDR","**Open** specific archives\n\n```engrampa [path/to/archive1.tar] [path/to/archive2.tar]```\n\n**Add** files to an archive and quit\n\n```engrampa --add-to=[path/to/archive.tar] [path/to/file_or_directory]```\n\n**Extract** archives to a directory and quit\n\n```engrampa --extract-to=[path/to/directory] [path/to/archive.tar]```\n\n**Extract** archives into a folder named after each archive and quit\n\n```engrampa --extract-here [path/to/archive.tar]```"],["SYNOPSIS","**engrampa** [_options_] [_files_]"],["DESCRIPTION","**engrampa** is the MATE desktop archive manager. It creates and extracts archives in various formats including tar, zip, 7z, rar, and more.\n\nA fork of file-roller adapted for the MATE desktop environment."],["PARAMETERS","**-a**, **--add-to** _archive_\n> Add files to the specified archive and quit.\n\n**-d**, **--add** _file_\n> Add files, prompting for the archive name, and quit.\n\n**-e**, **--extract-to** _folder_\n> Extract archives to the specified folder and quit.\n\n**-f**, **--extract**\n> Extract archives, prompting for the destination folder, and quit.\n\n**-h**, **--extract-here**\n> Extract archives using the archive name as destination folder and quit.\n\n**--default-dir** _folder_\n> Default folder to use for `--add` and `--extract` commands.\n\n**--force**\n> Create destination folder without asking for confirmation."],["CAVEATS","MATE desktop application. Requires appropriate libraries for different archive formats. Command-line options are limited compared to dedicated tools like tar or zip."],["INSTALL","```apt: sudo apt install engrampa```\n\n```dnf: sudo dnf install engrampa```\n\n```pacman: sudo pacman -S engrampa```\n\n```apk: sudo apk add engrampa```\n\n```zypper: sudo zypper install engrampa```\n\n```nix: nix profile install nixpkgs#engrampa```"],["SEE ALSO","[file-roller](/man/file-roller)(1), [tar](/man/tar)(1), [zip](/man/zip)(1), [unzip](/man/unzip)(1), [7z](/man/7z)(1)"]],"e":["file-roller","tar","zip","unzip","7z"],"x":"engrampa [path/to/archive1.tar] [path/to/archive2.tar]"},"exo":{"n":"exo","t":"command-line interface for Exoscale cloud services","s":[["TAGLINE","command-line interface for Exoscale cloud services"],["TLDR","**Configure CLI credentials**\n\n```exo config add```\n\n**List compute instances**\n\n```exo compute instance list```\n\n**Create a compute instance in a specific zone**\n\n```exo compute instance create [name] --zone [ch-gva-2]```\n\n**SSH to an instance**\n\n```exo compute instance ssh [instance_name]```\n\n**List IAM API keys**\n\n```exo iam api-key list```\n\n**List object storage buckets**\n\n```exo storage list```\n\n**Show current account limits**\n\n```exo limits```"],["SYNOPSIS","**exo** _command_ [_subcommand_] [_options_]"],["PARAMETERS","_COMMAND_\n> Service: compute, iam, storage, dns, etc.\n\n**compute** _SUBCOMMAND_\n> Manage compute resources (instances, security groups, load balancers, SKS clusters, etc.).\n\n**dbaas** _SUBCOMMAND_\n> Manage Database as a Service instances.\n\n**dns** _SUBCOMMAND_\n> Manage DNS zones and records.\n\n**iam** _SUBCOMMAND_\n> Identity and access management (API keys, roles, org policy).\n\n**storage** _SUBCOMMAND_\n> Object storage operations (buckets, upload, download).\n\n**config** _SUBCOMMAND_\n> Manage CLI configuration and credentials.\n\n**zone**\n> List available zones.\n\n**limits**\n> Show current account resource limits.\n\n**status**\n> Show Exoscale platform status.\n\n**version**\n> Print CLI version.\n\n**--help**\n> Display help information."],["CONFIGURATION","**~/.config/exoscale/exoscale.toml**\n> Stores API credentials and default zone/account settings."],["DESCRIPTION","**exo** is the command-line interface for Exoscale, a European cloud computing provider. It manages compute instances, storage, DNS, and other cloud resources.\n\nThe tool provides commands for all Exoscale services including SKS (Kubernetes), DBaaS, dedicated inference, and networking. Configuration stores API credentials for authentication.\n\nexo enables infrastructure automation and management without the web console."],["CAVEATS","Requires Exoscale account and API keys. Actions may incur costs. Zone availability varies. Some features require specific subscriptions."],["HISTORY","exo is the official CLI for **Exoscale**, a Swiss cloud provider founded in **2011** offering GDPR-compliant cloud infrastructure with a focus on European data sovereignty."],["INSTALL","```dnf: sudo dnf install exo```\n\n```pacman: sudo pacman -S exo```\n\n```apk: sudo apk add exoscale```\n\n```nix: nix profile install nixpkgs#exo```"],["SEE ALSO","[aws](/man/aws)(1), [gcloud](/man/gcloud)(1), [az](/man/az)(1), [doctl](/man/doctl)(1)"]],"e":["aws","gcloud","az","doctl"],"x":"exo config add"},"fcrackzip":{"n":"fcrackzip","t":"crack password-protected ZIP archives","s":[["TAGLINE","crack password-protected ZIP archives"],["TLDR","**Brute-force** alphanumeric password (4-8 chars)\n\n```fcrackzip [-b|--brute-force] [-l|--length] 4-8 [-c|--charset] aA1 [archive]```\n\nBrute-force with **custom charset**\n\n```fcrackzip [-v|--verbose] [-b|--brute-force] [-l|--length] 3 [-c|--charset] a:$% [archive]```\n\nBrute-force with **special characters**\n\n```fcrackzip [-b|--brute-force] [-l|--length] 4 [-c|--charset] a! [archive]```\n\nBrute-force **starting** from specific password\n\n```fcrackzip [-b|--brute-force] [-l|--length] 5 [-c|--charset] 1 [-p|--init-password] 12345 [archive]```\n\nCrack using **wordlist**\n\n```fcrackzip [-u|--use-unzip] [-D|--dictionary] [-p|--init-password] [wordlist] [archive]```\n\n**Benchmark** cracking performance\n\n```fcrackzip [-B|--benchmark]```"],["SYNOPSIS","**fcrackzip** [_options_] _archive_"],["DESCRIPTION","**fcrackzip** cracks password-protected ZIP archives using brute-force or dictionary attacks. It can test passwords against the archive's encryption.\n\nSecurity research and recovery tool for forgotten passwords."],["PARAMETERS","**-b, --brute-force**\n> Use brute-force mode\n\n**-D, --dictionary**\n> Use dictionary mode\n\n**-l, --length** _min-max_\n> Password length range\n\n**-c, --charset** _set_\n> Character set (a=lowercase, A=uppercase, 1=digits, !=special)\n\n**-p, --init-password** _pass_\n> Starting password or wordlist\n\n**-u, --use-unzip**\n> Use unzip to verify\n\n**-v, --verbose**\n> Verbose output\n\n**-B, --benchmark**\n> Performance benchmark"],["CAVEATS","Use only on archives you own or have permission to access. Brute-force is slow for long passwords. Dictionary attacks are faster for common passwords."],["INSTALL","```apt: sudo apt install fcrackzip```\n\n```pacman: sudo pacman -S fcrackzip```\n\n```brew: brew install fcrackzip```\n\n```nix: nix profile install nixpkgs#fcrackzip```"],["SEE ALSO","[zip](/man/zip)(1), [unzip](/man/unzip)(1), [john](/man/john)(1)"],["RESOURCES","```[Source code](https://github.com/hyc/fcrackzip)```\n\n```[Homepage](http://oldhome.schmorp.de/marc/fcrackzip.html)```"]],"e":["zip","unzip","john"],"x":"fcrackzip [-b|--brute-force] [-l|--length] 4-8 [-c|--charset] aA1 [archive]"},"fdisk":{"n":"fdisk","t":"partition table manipulator","s":[["TAGLINE","partition table manipulator"],["TLDR","**List** partitions\n\n```sudo fdisk -l```\n\nStart the **interactive** partition manipulator\n\n```sudo fdisk [/dev/sdX]```"],["SYNOPSIS","**fdisk** [_-l_] [_-u_] [_-s partition_] [_device_]"],["DESCRIPTION","**fdisk** is a dialog-driven program for managing partition tables and partitions on storage drives. It supports MBR, GPT, SGI, and Sun partition tables."],["PARAMETERS","**-l, --list**\n> List partition tables for all devices or specified device\n\n**-u, --units**\n> Display units in sectors (default) or cylinders\n\n**-s, --getsz partition**\n> Print size of partition in 512-byte sectors\n\n**-b, --sector-size size**\n> Specify physical and logical sector size\n\n**-c, --compatibility mode**\n> Specify compatibility mode (dos or nondos)\n\n**-L, --color**\n> Colorize output\n\n**-t, --type type**\n> Specify disk label type (dos, gpt, sgi, sun)"],["INTERACTIVE COMMANDS","**m**: Display help menu\n**p**: Print partition table\n**n**: Create new partition\n**d**: Delete partition\n**t**: Change partition type\n**l**: List known partition types\n**w**: Write changes and exit\n**q**: Quit without saving changes\n**g**: Create new GPT partition table\n**o**: Create new DOS partition table"],["CAVEATS","**Dangerous**: Changes are written when you press 'w'. Double-check before writing. Modern fdisk supports GPT natively. MBR partition tables (not fdisk) are limited to 2TB; use GPT for larger disks."],["HISTORY","Part of **util-linux** package. Based on the original fdisk from **Minix** and early Linux. One of the oldest Linux disk partitioning tools."],["INSTALL","```apt: sudo apt install fdisk```\n\n```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[parted](/man/parted)(8), [cfdisk](/man/cfdisk)(8), [gdisk](/man/gdisk)(8), [sfdisk](/man/sfdisk)(8), [partprobe](/man/partprobe)(8)"]],"e":["parted","cfdisk","gdisk","sfdisk","partprobe"],"x":"sudo fdisk -l"},"feed2exec":{"n":"feed2exec","t":"RSS/Atom feed automation tool","s":[["TAGLINE","RSS/Atom feed automation tool"],["TLDR","**Fetch all configured feeds**\n\n```feed2exec fetch```\n\n**Add a new feed**\n\n```feed2exec add [feedname] [https://example.com/feed.xml]```\n\n**List configured feeds**\n\n```feed2exec ls```\n\n**Remove a feed**\n\n```feed2exec rm [feedname]```\n\n**Run with custom config**\n\n```feed2exec --config [~/.config/feed2exec.ini] fetch```"],["SYNOPSIS","**feed2exec** [_options_] _command_ [_args_]"],["PARAMETERS","**fetch**\n> Fetch and process all feeds.\n\n**add** _name_ _url_\n> Add new feed.\n\n**rm** _name_\n> Remove feed.\n\n**ls**\n> List configured feeds.\n\n**parse** _url_\n> Parse feed without processing.\n\n**--config** _file_\n> Configuration file path.\n\n**--verbose**, **-v**\n> Verbose output.\n\n**--debug**\n> Debug output."],["CONFIGURATION","**~/.config/feed2exec.ini**\n> Main configuration file defining feeds, output formats, and processing commands."],["DESCRIPTION","**feed2exec** fetches RSS/Atom feeds and executes commands for each new entry. It's a flexible feed processor that can send emails, run scripts, save files, or trigger any command based on feed updates.\n\nUnlike feed readers, feed2exec is designed for automation. Common uses include email notifications, archiving, and triggering webhooks when feeds update."],["CAVEATS","Requires configuration for each feed. State tracked in SQLite database. Email sending needs configured MTA. Complex outputs may need custom plugins."],["HISTORY","feed2exec was written by **Antoine Beaupré** as a flexible replacement for rss2email and similar tools. It emphasizes Unix philosophy, executing external commands rather than implementing every feature internally."],["SEE ALSO","[rss2email](/man/rss2email)(1), [newsboat](/man/newsboat)(1), [curl](/man/curl)(1)"],["RESOURCES","```[Source code](https://gitlab.com/anarcat/feed2exec)```\n\n```[Documentation](https://feed2exec.readthedocs.io/)```"]],"e":["rss2email","newsboat","curl"],"x":"feed2exec fetch"},"gcloud-components":{"n":"gcloud-components","t":"Google Cloud SDK component manager","s":[["TAGLINE","Google Cloud SDK component manager"],["TLDR","**List available components**\n\n```gcloud components list```\n\n**Install component**\n\n```gcloud components install [kubectl]```\n\n**Update all components**\n\n```gcloud components update```\n\n**Remove component**\n\n```gcloud components remove [component-id]```\n\n**Install multiple components**\n\n```gcloud components install [kubectl] [gke-gcloud-auth-plugin]```"],["SYNOPSIS","**gcloud components** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Operation: list, install, update, remove, reinstall, repositories, copy-bundled-python.\n\n**list**\n> Show available and installed components, their size, and current version.\n\n**install** _COMPONENT_...\n> Install one or more components (and their dependencies).\n\n**update**\n> Update all installed components to the latest available version. Use **--version** to pin a specific SDK version.\n\n**remove** _COMPONENT_...\n> Uninstall one or more components.\n\n**reinstall**\n> Reinstall the SDK, removing and restoring all components at the latest version.\n\n**repositories**\n> Manage additional component repositories (add, list, remove) used for trusted-tester releases.\n\n**copy-bundled-python**\n> Copy the bundled Python interpreter to a user-specified location.\n\n**--quiet**, **-q**\n> Disable interactive prompts.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gcloud components** manages the modular component system of the Google Cloud SDK. The SDK follows a core-plus-components architecture where a minimal base installation can be extended with additional tools as needed.\n\nCommon optional components include kubectl for Kubernetes management, gsutil for Cloud Storage operations, bq for BigQuery interactions, and language-specific App Engine runtimes. This modular approach keeps the initial SDK installation small while allowing expansion for specific workflows.\n\nThe update command keeps all installed components current with the latest features and bug fixes. Component management is tracked locally, allowing different machines to have different component sets based on their specific use cases. Some components have dependencies on others, which the system handles automatically during installation."],["CAVEATS","Updates may change behavior. Some components have dependencies. Admin rights may be needed."],["SEE ALSO","[gcloud](/man/gcloud)(1), [gcloud-help](/man/gcloud-help)(1)"]],"e":["gcloud","gcloud-help"],"x":"gcloud components list"},"gettextize":{"n":"gettextize","t":"gettext infrastructure initialization tool","s":[["TAGLINE","gettext infrastructure initialization tool"],["TLDR","**Initialize gettext** infrastructure in current project\n\n```gettextize```\n\n**Copy files** instead of creating symlinks\n\n```gettextize --copy```\n\n**Force overwrite** of existing files\n\n```gettextize -f```\n\n**Dry run** to see what would be done without making changes\n\n```gettextize -n```\n\n**Specify a PO directory** with existing translations\n\n```gettextize --po-dir=[po]```"],["SYNOPSIS","**gettextize** [_options_] [_directory_]"],["PARAMETERS","_DIRECTORY_\n> Project directory (default: current).\n\n**-f, --force**\n> Force writing of new files even if old ones exist.\n\n**--copy**\n> Copy files instead of symlinking (default behavior).\n\n**--symlink**\n> Make symbolic links instead of copying files.\n\n**-n, --dry-run**\n> Print modifications but don't perform them.\n\n**--no-changelog**\n> Don't update or create ChangeLog files.\n\n**--po-dir**=_DIR_\n> Specify directory containing PO files. Can be given multiple times.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**gettextize** prepares a source package for internationalization. It adds necessary gettext infrastructure files, autoconf macros, and directory structure.\n\nThe tool creates po/ directory, adds m4 macros, and updates build system configuration. It sets up the framework for adding translations to a project.\n\ngettextize is the first step in internationalizing an autotools-based project."],["CAVEATS","Modifies build system files. May require manual adjustments. Best for autotools projects."],["HISTORY","gettextize is part of **GNU gettext**, providing project initialization for the gettext internationalization framework."],["INSTALL","```apt: sudo apt install gettext```\n\n```dnf: sudo dnf install gettext```\n\n```pacman: sudo pacman -S gettext```\n\n```apk: sudo apk add gettext```\n\n```brew: brew install gettext```\n\n```nix: nix profile install nixpkgs#gettext```"],["SEE ALSO","[gettext](/man/gettext)(1), [xgettext](/man/xgettext)(1), [msgfmt](/man/msgfmt)(1), [autoconf](/man/autoconf)(1)"]],"e":["gettext","xgettext","msgfmt","autoconf"],"x":"gettextize"},"gh-formatting":{"n":"gh-formatting","t":"display output formatting documentation","s":[["TAGLINE","display output formatting documentation"],["TLDR","**View formatting help**\n\n```gh formatting```"],["SYNOPSIS","**gh** **formatting**"],["DESCRIPTION","**gh formatting** displays comprehensive help about GitHub CLI output formatting capabilities. It documents the template syntax for customizing output, JSON output modes for machine-readable data, and field selection options.\n\nMany gh commands support the --json flag to output structured data that can be processed by tools like jq. Template formatting uses Go template syntax to create custom output layouts. The --jq flag provides direct integration with jq for JSON filtering.\n\nUnderstanding formatting options is essential for integrating gh into scripts and automated workflows."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-help](/man/gh-help)(1)"],["RESOURCES","```[Documentation](https://cli.github.com/manual/gh_help_formatting)```\n\n```[Source code](https://github.com/cli/cli)```"]],"e":["gh","gh-help"],"x":"gh formatting"},"ghdl":{"n":"ghdl","t":"Open-source VHDL simulator and synthesizer","s":[["TAGLINE","Open-source VHDL simulator and synthesizer"],["TLDR","**Analyze VHDL file**\n\n```ghdl -a [file.vhdl]```\n\n**Elaborate design**\n\n```ghdl -e [entity_name]```\n\n**Run simulation**\n\n```ghdl -r [entity_name]```\n\n**Run with VCD waveform output**\n\n```ghdl -r [entity_name] --vcd=[output.vcd]```\n\n**Run with GHW waveform output**\n\n```ghdl -r [entity_name] --wave=[output.ghw]```\n\n**Run simulation with stop time**\n\n```ghdl -r [entity_name] --stop-time=[100ns]```\n\n**Elaborate and run** in one step\n\n```ghdl --elab-run [entity_name] --vcd=[output.vcd]```\n\n**Analyze, elaborate and run** using combined command\n\n```ghdl -c [file.vhdl] -r [entity_name]```\n\n**Import files**\n\n```ghdl -i [file.vhdl]```\n\n**Syntax check only**\n\n```ghdl -s [file.vhdl]```"],["SYNOPSIS","**ghdl** _command_ [_options_] _files_"],["PARAMETERS","**-a** _FILE_\n> Analyze VHDL source file.\n\n**-e** _ENTITY_\n> Elaborate design entity.\n\n**-r** _ENTITY_\n> Run simulation.\n\n**-i** _FILE_\n> Import VHDL file.\n\n**-s** _FILE_\n> Syntax check only.\n\n**--vcd** _FILE_\n> Generate VCD waveform output.\n\n**--wave** _FILE_\n> Generate GHW waveform output.\n\n**--stop-time** _TIME_\n> Simulation stop time (e.g., 100ns, 1us, 10ms).\n\n**--std** _STANDARD_\n> VHDL standard to use: 87, 93, 93c, 00, 02, 08.\n\n**--work** _NAME_\n> Set the work library name (default: work).\n\n**--workdir** _DIR_\n> Set the directory for the work library.\n\n**--ieee** _LIBRARY_\n> Select IEEE library flavor: none, standard, synopsys.\n\n**-P** _DIRECTORY_\n> Add a library search path.\n\n**--elab-run**\n> Elaborate and run the design in a single step.\n\n**-m** _ENTITY_\n> Analyze outdated files and elaborate the design (make mode).\n\n**-c** _FILES_ **-r** _ENTITY_\n> Analyze and elaborate in a single command.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ghdl** is an open-source VHDL simulator and synthesizer. It analyzes, elaborates, and simulates VHDL designs, supporting VHDL-87, VHDL-93, VHDL-2002, and VHDL-2008 standards.\n\nThe typical workflow involves three steps: analyzing source files (**-a**) to check syntax and compile, elaborating the design hierarchy (**-e**) to resolve all dependencies, and running the simulation (**-r**). Waveform outputs in VCD and GHW formats can be generated for viewing in tools like GTKWave."],["CAVEATS","Some advanced VHDL features may have limitations. Synthesis support is experimental. Large simulations can be memory-intensive."],["HISTORY","GHDL was started by **Tristan Gingold** and is written in Ada. It provides a free software alternative to commercial VHDL simulators."],["INSTALL","```apt: sudo apt install ghdl-common```\n\n```dnf: sudo dnf install ghdl```\n\n```nix: nix profile install nixpkgs#ghdl```"],["SEE ALSO","[gtkwave](/man/gtkwave)(1), [iverilog](/man/iverilog)(1), [verilator](/man/verilator)(1)"]],"e":["gtkwave","iverilog","verilator"],"x":"ghdl -a [file.vhdl]"},"git-fresh-branch":{"n":"git-fresh-branch","t":"Create orphan branch with no commit history","s":[["TAGLINE","Create orphan branch with no commit history"],["TLDR","**Create orphan branch**\n\n```git fresh-branch [branch_name]```"],["SYNOPSIS","**git** **fresh-branch** _name_"],["PARAMETERS","_NAME_\n> Name of the new branch to create."],["DESCRIPTION","**git fresh-branch** creates a new orphan branch with no parent commits or history connection to the existing repository. It repoints HEAD at the new (as yet nonexistent) branch ref, clears the index, and runs **git clean -fdx** on the working tree, leaving a completely clean slate within the same repository.\n\nThe primary use case is gh-pages branches for GitHub Pages hosting, where documentation or website content lives in the same repository but shares no history with the codebase. Other uses include documentation branches, experimental branches, or vendor branches for third-party code.\n\nIf the working tree has untracked changes, the command prompts for confirmation before proceeding, since **git clean -fdx** deletes them irrecoverably."],["CAVEATS","Part of git-extras package. Destroys untracked files and the index without a real backup; commit or stash anything you want to keep before running it. Equivalent in effect to **git checkout --orphan** _name_ followed by clearing the index and working tree."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-checkout](/man/git-checkout)(1), [git-clean](/man/git-clean)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/man/git-fresh-branch.md)```"]],"e":["git-checkout","git-clean","git-extras"],"x":"git fresh-branch [branch_name]"},"git-http-push":{"n":"git-http-push","t":"Push objects over HTTP/WebDAV","s":[["TAGLINE","Push objects over HTTP/WebDAV"],["TLDR","**Push a branch via HTTP/DAV to a remote**\n\n```git http-push [url] [refs]```\n\n**Push all refs via HTTP to a remote**\n\n```git http-push --all [url]```\n\n**Dry run to see what would be pushed**\n\n```git http-push --dry-run [url] [refs]```\n\n**Force push via HTTP** overwriting remote ref\n\n```git http-push --force [url] [refs]```\n\n**Push with verbose output** showing object details\n\n```git http-push --verbose [url] [refs]```"],["SYNOPSIS","**git http-push** [_options_] _url_ _refs_"],["PARAMETERS","_URL_\n> Remote repository URL.\n\n_REFS_\n> References to push.\n\n**--all**\n> Push all refs. Verifies all objects in the entire local ref history exist in the remote repository rather than assuming completeness.\n\n**--force**\n> Allow updating a remote ref that is not an ancestor of the local ref (disables fast-forward check).\n\n**--dry-run**\n> Do everything except actually send the updates.\n\n**--verbose**\n> Report the list of objects being walked locally and the list of objects successfully sent to the remote repository.\n\n**-d**, **-D**\n> Remove refs from the remote repository instead of pushing to them."],["DESCRIPTION","**git http-push** pushes objects and updates refs in a remote repository over HTTP/WebDAV. It is a low-level plumbing command used internally by **git push** when the remote uses the older DAV-based HTTP protocol.\n\nThis command is rarely used directly. Modern HTTP Git servers use the smart HTTP protocol which is handled by **git push** automatically. The DAV-based push requires the remote server to have WebDAV write access enabled and is considered legacy."],["CAVEATS","Legacy protocol. Modern servers use smart HTTP. Rarely needed directly. The remote HTTP server must support WebDAV with write permissions. Authentication is handled via standard HTTP credentials."],["HISTORY","git http-push was one of the early remote protocols in **Git**, using WebDAV for HTTP push before the smart HTTP protocol was developed."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-push](/man/git-push)(1), [git-remote](/man/git-remote)(1), [git-fetch](/man/git-fetch)(1)"]],"e":["git-push","git-remote","git-fetch"],"x":"git http-push [url] [refs]"},"gnome-terminal":{"n":"gnome-terminal","t":"GNOME desktop terminal emulator","s":[["TAGLINE","GNOME desktop terminal emulator"],["TLDR","Open a new **GNOME terminal window**\n\n```gnome-terminal```\n\nRun a **specific command** in a new terminal window\n\n```gnome-terminal -- [command]```\n\nOpen a new **tab** in the last opened window\n\n```gnome-terminal --tab```\n\nSet the **title** of the new tab\n\n```gnome-terminal --tab -t \"[title]\"```"],["SYNOPSIS","**gnome-terminal** [_options_] [**--**] [_command_]"],["PARAMETERS","**--tab**\n> Open a new tab in existing window\n\n**--window**\n> Open a new window\n\n**-t**, **--title** _TITLE_\n> Set initial terminal title\n\n**--working-directory** _DIR_\n> Set working directory\n\n**--geometry** _GEOMETRY_\n> Set window size and position (WIDTHxHEIGHT+X+Y)\n\n**--profile** _PROFILE_\n> Use specified profile\n\n**-e**, **--command** _CMD_\n> Execute command (deprecated, use -- instead)\n\n**--wait**\n> Wait until terminal exits\n\n**--maximize**\n> Start maximized\n\n**--full-screen**\n> Start in full-screen mode"],["DESCRIPTION","**gnome-terminal** is the default terminal emulator for the GNOME desktop environment. It provides a feature-rich terminal with support for multiple tabs, profiles, and customization options.\n\nFeatures include multiple profiles with different colors and fonts, transparent backgrounds, custom key bindings, and VTE-based terminal emulation with Unicode support."],["CONFIGURATION","**dconf /org/gnome/terminal/**\n> Terminal settings stored in dconf, configurable via gsettings or the Preferences dialog."],["CAVEATS","On Wayland, some X11-specific features like geometry positioning may not work. The --geometry option is deprecated on modern setups. Heavy customization requires editing dconf settings."],["HISTORY","gnome-terminal has been part of GNOME since version 1.0 in **1999**. It uses the VTE (Virtual Terminal Emulator) widget library, which is also used by other terminals like Terminator and Tilix. The application has evolved significantly through GNOME's various iterations."],["INSTALL","```apt: sudo apt install gnome-terminal```\n\n```dnf: sudo dnf install gnome-terminal```\n\n```pacman: sudo pacman -S gnome-terminal```\n\n```apk: sudo apk add gnome-terminal```\n\n```zypper: sudo zypper install gnome-terminal```\n\n```nix: nix profile install nixpkgs#gnome-terminal```"],["SEE ALSO","[bash](/man/bash)(1), [xterm](/man/xterm)(1), [alacritty](/man/alacritty)(1)"]],"e":["bash","xterm","alacritty"],"x":"gnome-terminal"},"gobee":{"n":"gobee","t":"Transpile a subset of Go into BPF C","s":[["TAGLINE","Transpile a subset of Go into BPF C"],["TLDR","**Translate** a Go BPF program and emit C plus typed bindings\n\n```gobee translate --bindings-dir [./bpf] [./bpf/src]```\n\n**Install** the latest release\n\n```go install github.com/boratanrikulu/gobee/cmd/gobee@latest```\n\n**Diagnose** a verifier error against the generated sourcemap\n\n```gobee diagnose [verifier.log]```"],["SYNOPSIS","**gobee** _subcommand_ [_options_] [_paths_]"],["DESCRIPTION","**gobee** is a pure-Go transpiler that converts a strict subset of Go into BPF (Berkeley Packet Filter) C source code, together with typed Go bindings for the resulting programs. It targets developers who want to author eBPF programs without writing C directly, while keeping the resulting object compatible with **libbpf** and **clang**'s BPF backend.\n\nThe primary subcommand, **translate**, reads Go source files describing BPF programs and produces three artifacts in the target directory: a **.bpf.c** file, a sourcemap (**.bpf.c.map**) used for error attribution, and **\\*_bindings.go** files exposing typed loaders and program handles. **clang** with BPF target support is required to compile the generated C into an object file; **gobee** itself only depends on Go.\n\nThe tool also exposes typed stubs for the **libbpf** v1.5.0 helper set, performs kernel-version gating with **bpfvet**, and can map verifier errors back to the original Go positions for offline debugging."],["PARAMETERS","**translate**\n> Transpile Go BPF sources to C and generate Go bindings.\n\n**diagnose**\n> Map verifier output back to source positions using the generated sourcemap.\n\n**--bindings-dir  _dir_**\n> Output directory for the generated Go bindings."],["CAVEATS","Only a strict subset of Go is accepted; idiomatic Go features such as goroutines, interfaces with dynamic dispatch, and the full standard library are not available in transpiled code. Building loadable BPF objects still requires **clang** with the **bpf** target, and loading them at runtime requires elevated privileges."],["SEE ALSO","[clang](/man/clang)(1), [bpftool](/man/bpftool)(8), [bpftrace](/man/bpftrace)(8)"]],"e":["clang","bpftool","bpftrace"],"x":"gobee translate --bindings-dir [./bpf] [./bpf/src]"},"grub-glue-efi":{"n":"grub-glue-efi","t":"combine 32-bit and 64-bit EFI binaries","s":[["TAGLINE","combine 32-bit and 64-bit EFI binaries"],["TLDR","**Glue EFI binaries**\n\n```grub-glue-efi -3 [ia32.efi] -6 [x64.efi] -o [boot.efi]```\n\n**Create fat binary**\n\n```grub-glue-efi --input32 [grub32.efi] --input64 [grub64.efi] --output [grub.efi]```"],["SYNOPSIS","**grub-glue-efi** [_options_]"],["PARAMETERS","**-3**, **--input32** _FILE_\n> 32-bit EFI input.\n\n**-6**, **--input64** _FILE_\n> 64-bit EFI input.\n\n**-o**, **--output** _FILE_\n> Output file.\n\n**-v**, **--verbose**\n> Print verbose messages.\n\n**--help**\n> Display help information."],["DESCRIPTION","**grub-glue-efi** processes ia32 and amd64 EFI images and glues them according to Apple format, creating an Apple fat EFI binary from two EFI binaries. This enables booting on systems with either 32-bit or 64-bit EFI firmware from a single file.\n\nThe tool is primarily useful for creating universal EFI binaries for GRUB bootloaders on removable media that must boot on diverse hardware."],["CAVEATS","Part of GRUB package. Requires both architecture binaries. For special bootloader scenarios only."],["HISTORY","grub-glue-efi was developed as part of **GNU GRUB** to support universal EFI boot media creation."],["INSTALL","```apt: sudo apt install grub-common```\n\n```pacman: sudo pacman -S grub```\n\n```apk: sudo apk add grub```"],["SEE ALSO","[grub](/man/grub)(8), [grub-install](/man/grub-install)(8)"]],"e":["grub","grub-install"],"x":"grub-glue-efi -3 [ia32.efi] -6 [x64.efi] -o [boot.efi]"},"helm-install":{"n":"helm-install","t":"deploys a Helm chart to a Kubernetes cluster, creating a new release","s":[["TAGLINE","deploys a Helm chart to a Kubernetes cluster, creating a new release"],["TLDR","**Install a chart**\n\n```helm install [release-name] [chart]```\n\n**Install from repository**\n\n```helm install [my-nginx] bitnami/nginx```\n\n**Install with custom values file**\n\n```helm install [release] [chart] -f [values.yaml]```\n\n**Install with inline value**\n\n```helm install [release] [chart] --set [key]=[value]```\n\n**Install in specific namespace**\n\n```helm install [release] [chart] -n [namespace]```\n\n**Install and wait for pods**\n\n```helm install [release] [chart] --wait```\n\n**Dry run without installing**\n\n```helm install [release] [chart] --dry-run```"],["SYNOPSIS","**helm** **install** [_name_] _chart_ [_options_]"],["PARAMETERS","**-f**, **--values** _file_\n> Custom values YAML file.\n\n**--set** _key_=_value_\n> Override values inline.\n\n**-n**, **--namespace** _namespace_\n> Target namespace.\n\n**--wait**\n> Wait for pods to be ready.\n\n**--timeout** _duration_\n> Timeout for commands (default 5m0s).\n\n**--dry-run**\n> Simulate without installing.\n\n**--create-namespace**\n> Create namespace if missing."],["DESCRIPTION","**helm install** deploys a Helm chart to a Kubernetes cluster, creating a new release. A chart contains all resource definitions needed to run an application. Helm tracks releases for easy upgrades with **helm upgrade** and rollbacks with **helm rollback**. Use **--wait** to wait for all pods to be ready before completing."],["INSTALL","```dnf: sudo dnf install helm```\n\n```pacman: sudo pacman -S helm```\n\n```apk: sudo apk add helm```\n\n```zypper: sudo zypper install helm```\n\n```brew: brew install helm```\n\n```nix: nix profile install nixpkgs#helm```"],["SEE ALSO","[helm](/man/helm)(1), [kubectl](/man/kubectl)(1)"]],"e":["helm","kubectl"],"x":"helm install [release-name] [chart]"},"hledger-aregister":{"n":"hledger-aregister","t":"shows transactions and running balance for a single account in bank-statement format","s":[["TAGLINE","shows transactions and running balance for a single account in bank-statement format"],["TLDR","**Show account register**\n\n```hledger aregister [checking]```\n\n**Show with a specific journal file**\n\n```hledger aregister -f [journal.ledger] [assets:bank]```\n\n**Show monthly summary**\n\n```hledger aregister [checking] --monthly```\n\n**Include historical balance from before report period**\n\n```hledger aregister [checking] -H```\n\n**Limit output width**\n\n```hledger aregister [checking] -w [80]```\n\n**Include zero-change transactions**\n\n```hledger aregister [checking] -E```\n\n**Output as CSV**\n\n```hledger aregister [checking] -O csv```"],["SYNOPSIS","**hledger** **aregister** | **areg** [_options_] _account_"],["PARAMETERS","**-f**, **--file** _file_\n> Use specified journal file.\n\n**--daily**\n> Show daily summaries.\n\n**--weekly**\n> Show weekly summaries.\n\n**--monthly**\n> Show monthly summaries.\n\n**-H**, **--historical**\n> Include balance from before report period.\n\n**-E**, **--empty**\n> Show transactions making a net change of zero.\n\n**--txn-dates**\n> Filter by transaction date instead of posting date.\n\n**--depth** _n_\n> Limit account depth.\n\n**-w**, **--width** _n_\n> Set output width.\n\n**-O**, **--output-format** _fmt_\n> Output format: txt, csv, tsv, html, fods, json.\n\n**--quarterly**\n> Show quarterly summaries.\n\n**--yearly**\n> Show yearly summaries."],["DESCRIPTION","**hledger aregister** (alias **areg**) shows transactions and running balance for a single account in bank-statement format. Each transaction appears on one line with the date, description, other accounts involved, amount, and running balance. Unlike **register**, which shows one line per posting, aregister groups postings by transaction. The account can be specified by full name or a case-insensitive regular expression matching the first account alphabetically."],["INSTALL","```apt: sudo apt install hledger```\n\n```dnf: sudo dnf install hledger```\n\n```pacman: sudo pacman -S hledger```\n\n```apk: sudo apk add hledger```\n\n```zypper: sudo zypper install hledger```\n\n```brew: brew install hledger```\n\n```nix: nix profile install nixpkgs#hledger```"],["SEE ALSO","[hledger](/man/hledger)(1), [hledger-balance](/man/hledger-balance)(1), [hledger-print](/man/hledger-print)(1)"]],"e":["hledger","hledger-balance","hledger-print"],"x":"hledger aregister [checking]"},"holos":{"n":"holos","t":"Holistic Kubernetes platform manager using CUE","s":[["TAGLINE","Holistic Kubernetes platform manager using CUE"],["TLDR","**Show the installed version**\n\n```holos --version```\n\n**Initialize a new platform** in the current directory\n\n```holos init platform [v1alpha5]```\n\n**Render the whole platform** to deploy/ manifests\n\n```holos render platform```\n\n**Render a single component**\n\n```holos render component [path/to/component]```\n\n**Render using an alternate platform directory**\n\n```holos render platform --platform [path/to/platform]```\n\n**List build plans** the platform produces\n\n```holos show buildplans```"],["SYNOPSIS","**holos** _command_ [_subcommand_] [_options_]"],["DESCRIPTION","**Holos** is a configuration management tool for Kubernetes that implements the **rendered manifests pattern**. Instead of templating YAML, platforms and their components are described in **CUE**; **holos** combines CUE specs with **Helm** charts and **Kustomize** overlays and writes the resulting manifests to disk so they can be committed to Git and applied by any GitOps controller (Argo CD, Flux, etc.).\n\nA platform consists of a root directory containing **cue.mod/** and a generated **platform.gen.cue** describing a Platform resource. Each component lives in its own directory and exports a **BuildPlan** that **holos render** executes. The output is plain Kubernetes YAML under _deploy/_, which makes diffs reviewable and decouples rendering from cluster access."],["COMMANDS","**init platform** _version_\n> Scaffold a new platform using the given API version (e.g. _v1alpha5_). Creates _platform/_, _cue.mod/_, and _platform.gen.cue_.\n\n**render platform**\n> Render every component of the platform into _deploy/_.\n\n**render component** _path_\n> Render a single component directory.\n\n**show buildplans**\n> Print the BuildPlan resources Holos will execute.\n\n**generate**\n> Run code generators (e.g. regenerate CUE schemas from Kubernetes CRDs).\n\n**version**\n> Print build and API version information.\n\n**help** [_command_]\n> Show help for a command."],["PARAMETERS","**--platform** _dir_\n> Override the default platform directory (_platform/_) used as the rendering entry point.\n\n**--version**\n> Print the Holos version and exit.\n\n**--help**, **-h**\n> Show help for a command."],["CAVEATS","Holos is pre-1.0 and the API is versioned per directory (**v1alpha3**, **v1alpha4**, **v1alpha5**, ...); mixing versions in one platform is not supported, and upgrading usually requires regenerating **platform.gen.cue**. CUE has a learning curve if you are coming from Helm/Kustomize-only workflows. The tool itself only renders manifests — applying them to a cluster is left to your GitOps controller or **kubectl apply**."],["HISTORY","Started by **Jeff McCune** and the **holos-run** collective in **2023** to address drawbacks of pure templating in large multi-cluster Kubernetes platforms. Written in **Go** and **CUE** and released under **Apache 2.0**. Distributed via **Homebrew** (_brew install holos-run/tap/holos_) and as static binaries from GitHub releases."],["INSTALL","```zypper: sudo zypper install holos```\n\n```nix: nix profile install nixpkgs#holos```"],["SEE ALSO","[helm](/man/helm)(1), [kustomize](/man/kustomize)(1), [kubectl](/man/kubectl)(1), [argocd](/man/argocd)(1)"]],"e":["helm","kustomize","kubectl","argocd"],"x":"holos --version"},"httplab":{"n":"httplab","t":"Interactive web server for inspecting and mocking HTTP requests","s":[["TAGLINE","Interactive web server for inspecting and mocking HTTP requests"],["TLDR","**Start httplab** on the default port\n\n```httplab```\n\n**Start on a specific port**\n\n```httplab -p [8080]```\n\n**Start with an initial response status and body**\n\n```httplab -s [200] -b \"[Hello, World]\"```\n\n**Start with an initial response delay**\n\n```httplab -d [500]```\n\n**Enable CORS**\n\n```httplab --cors```\n\n**Specify custom config path**\n\n```httplab -c [path/to/.httplab]```"],["SYNOPSIS","**httplab** [_options_]"],["PARAMETERS","**-p**, **--port** _port_\n> Port to bind to (default: 10080).\n\n**-s**, **--status** _code_\n> Initial response status code (default: 200).\n\n**-b**, **--body** _string_\n> Initial response body (default: \"Hello, World\").\n\n**-d**, **--delay** _ms_\n> Initial response delay in milliseconds.\n\n**-H**, **--headers** _strings_\n> Initial response headers (default: X-Server:HTTPLab).\n\n**-a**, **--auto-update**\n> Auto-update response when fields change (default: true).\n\n**--cors**\n> Enable CORS.\n\n**--cors-display**\n> Display CORS requests (default: true).\n\n**-c**, **--config** _path_\n> Custom config path.\n\n**-v**, **--version**\n> Print version."],["DESCRIPTION","**httplab** is an interactive web server that lets you inspect incoming HTTP requests and forge responses in real time through a terminal UI. It runs a local HTTP server and displays each request's method, path, headers, and body, while allowing you to customize the response status code, headers, and body interactively.\n\nPre-built responses can be saved and loaded from a **.httplab** configuration file. The tool is useful for API development, webhook testing, and debugging HTTP client behavior."],["CAVEATS","Designed for development and testing only; not suitable for production use. Default port is 10080, which may conflict with other services."],["HISTORY","**httplab** was created by **Gustavo Chaín** (qustavo) and is written in **Go**. It was designed to provide developers with a quick way to inspect and mock HTTP interactions without configuring a full mock server."],["INSTALL","```nix: nix profile install nixpkgs#httplab```"],["SEE ALSO","[curl](/man/curl)(1), [nc](/man/nc)(1), [httpie](/man/httpie)(1)"]],"e":["curl","nc","httpie"],"x":"httplab"},"hub-browse":{"n":"hub-browse","t":"opens the GitHub repository page in your web browser","s":[["TAGLINE","opens the GitHub repository page in your web browser"],["TLDR","**Open repo in browser**\n\n```hub browse```\n\n**Open issues page**\n\n```hub browse -- issues```\n\n**Open pull requests**\n\n```hub browse -- pulls```\n\n**Open specific issue**\n\n```hub browse -- issues/[123]```\n\n**Open wiki**\n\n```hub browse -- wiki```\n\n**Open different repo**\n\n```hub browse [owner/repo]```\n\n**Print URL instead of opening browser**\n\n```hub browse -u```\n\n**Copy URL to clipboard**\n\n```hub browse -c```"],["SYNOPSIS","**hub** **browse** [_options_] [_owner/repo_] [-- _subpage_]"],["PARAMETERS","**-u**, **--url**\n> Print URL instead of opening browser.\n\n**-c**, **--copy**\n> Copy URL to clipboard.\n\n**--**\n> Separator for subpage path."],["DESCRIPTION","**hub browse** opens the GitHub repository page in your web browser. You can specify subpages like issues, pulls, wiki, commits, or specific file paths. Works with the current repository by default or specify another repo."],["CAVEATS","Hub is deprecated. Consider using **gh browse** instead."],["INSTALL","```apt: sudo apt install hub```\n\n```pacman: sudo pacman -S hub```\n\n```zypper: sudo zypper install hub```\n\n```brew: brew install hub```\n\n```nix: nix profile install nixpkgs#hub```"],["SEE ALSO","[hub](/man/hub)(1), [gh-browse](/man/gh-browse)(1)"]],"e":["hub","gh-browse"],"x":"hub browse"},"ibus":{"n":"ibus","t":"input method framework for Linux","s":[["TAGLINE","input method framework for Linux"],["TLDR","**Start IBus daemon**\n\n```ibus-daemon -drx```\n\n**List available input engines**\n\n```ibus list-engine```\n\n**Show current input engine**\n\n```ibus engine```\n\n**Set the current input engine**\n\n```ibus engine [engine_name]```\n\n**Restart the IBus daemon**\n\n```ibus restart```\n\n**Show IBus version**\n\n```ibus version```\n\n**Print current configuration**\n\n```ibus read-config```"],["SYNOPSIS","**ibus** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> IBus subcommand to execute.\n\n**list-engine**\n> Show available input engines.\n\n**engine** [_ENGINE_NAME_]\n> Show current engine, or set engine to ENGINE_NAME.\n\n**start**\n> Start the IBus daemon.\n\n**restart**\n> Restart the IBus daemon.\n\n**exit**\n> Exit the IBus daemon.\n\n**version**\n> Show the IBus version.\n\n**read-config**\n> Print configuration values.\n\n**reset-config**\n> Reset configuration to defaults.\n\n**read-cache**\n> Show the registry cache.\n\n**write-cache**\n> Save the registry cache.\n\n**emoji**\n> Launch IBus Emojier.\n\n**help**\n> Show the commands list."],["DESCRIPTION","**IBus** (Intelligent Input Bus) is an input method framework for Linux. It enables typing in languages requiring complex input like CJK.\n\nThe framework manages input method engines and provides a unified interface. It supports many languages and input systems."],["CAVEATS","Desktop integration needed. Engine selection required. X11/Wayland support varies."],["HISTORY","IBus was created by **Peng Huang** as the next-generation input framework for Linux, replacing SCIM."],["INSTALL","```apt: sudo apt install ibus```\n\n```dnf: sudo dnf install ibus```\n\n```pacman: sudo pacman -S ibus```\n\n```apk: sudo apk add ibus```\n\n```zypper: sudo zypper install ibus```\n\n```nix: nix profile install nixpkgs#ibus```"],["SEE ALSO","[fcitx](/man/fcitx)(1), [fcitx5](/man/fcitx5)(1)"]],"e":["fcitx","fcitx5"],"x":"ibus-daemon -drx"},"jira-project":{"n":"jira-project","t":"manages Jira projects from the command line","s":[["TAGLINE","manages Jira projects from the command line"],["TLDR","**List all projects**\n\n```jira project list```\n\n**View project details**\n\n```jira project view [PROJECT_KEY]```\n\n**List projects in plain table format**\n\n```jira project list --plain```"],["SYNOPSIS","**jira** **project** _subcommand_ [_options_]"],["PARAMETERS","**list**\n> List all accessible projects.\n\n**view** _key_\n> View project details by key (e.g., PROJ).\n\n**--plain**\n> Display output in plain table format without borders."],["DESCRIPTION","**jira project** manages Jira projects from the command line. Part of **jira-cli** (by ankitpokhrel), it allows listing and viewing project information including project keys, names, leads, and project types."],["SEE ALSO","[jira](/man/jira)(1), [jira-issue](/man/jira-issue)(1), [jira-sprint](/man/jira-sprint)(1)"]],"e":["jira","jira-issue","jira-sprint"],"x":"jira project list"},"jira":{"n":"jira","t":"Command-line client for Atlassian Jira","s":[["TAGLINE","Command-line client for Atlassian Jira"],["TLDR","**Initialize configuration**\n\n```jira init```\n\n**List recent issues**\n\n```jira issue list```\n\n**Filter issues** by JQL\n\n```jira issue list --jql \"[project = PROJ AND status = Open]\"```\n\n**Create an issue** interactively\n\n```jira issue create```\n\n**View an issue**\n\n```jira issue view [PROJ-123]```\n\n**Move an issue** to a status\n\n```jira issue move [PROJ-123] \"[In Progress]\"```\n\n**Assign an issue** to a user\n\n```jira issue assign [PROJ-123] [username]```\n\n**Add a comment**\n\n```jira issue comment add [PROJ-123] \"[comment body]\"```\n\n**Open issue in browser**\n\n```jira open [PROJ-123]```"],["SYNOPSIS","**jira** _command_ [_subcommand_] [_options_]"],["COMMANDS","**init**\n> Initialize the configuration file with server URL, login, and default project.\n\n**issue**\n> Issue management: create, view, list, edit, assign, move, comment, link, watch, etc.\n\n**project**\n> List and inspect projects.\n\n**sprint**\n> Manage sprints (Jira Cloud / Software).\n\n**epic**\n> Manage epics and their child issues.\n\n**board**\n> List boards in a project.\n\n**me**\n> Show the currently authenticated user.\n\n**open**\n> Open an issue in the default web browser.\n\n**completion**\n> Generate shell completion scripts."],["PARAMETERS","**-p**, **--project** _key_\n> Override the default project key for the current invocation.\n\n**--config** _file_\n> Path to a config file other than the default.\n\n**--debug**\n> Enable verbose debug logging.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jira** (jira-cli) is an open-source command-line client for Atlassian Jira Cloud and Data Center. It provides interactive TUI flows for the common workflow operations (create, transition, comment) and JQL-driven listings suitable for piping into shell scripts.\n\nAuthentication is handled via API tokens stored in **~/.config/.jira/.config.yml**. The tool supports multiple Jira instances, custom fields, and templates for issue creation."],["CONFIGURATION","**~/.config/.jira/.config.yml**\n> Stores server URL, default project, user/email, and API token.\n\n**JIRA_API_TOKEN**\n> Environment variable that overrides the stored API token."],["CAVEATS","Requires a Jira account and API token (Atlassian Cloud) or personal access token (Data Center). This is the third-party **jira-cli** tool by **ankitpokhrel**, not an official Atlassian product. Some features (e.g. sprint operations) require Jira Software, not just Jira Core/Work Management."],["HISTORY","**jira-cli** was created by **Ankit Pokhrel** to provide a fast, scriptable interface to Jira from the terminal. The project is written in Go."],["SEE ALSO","[gh](/man/gh)(1), [glab](/man/glab)(1), [git](/man/git)(1)"]],"e":["gh","glab","git"],"x":"jira init"},"koji-call":{"n":"koji-call","t":"executes an arbitrary XML-RPC call to the Koji hub","s":[["TAGLINE","executes an arbitrary XML-RPC call to the Koji hub"],["TLDR","Execute **arbitrary XML-RPC call** to Koji hub\n\n```koji call build '\"git+https://src.fedoraproject.org/rpms/pkg.git#commit\"' target```\n\nCall build with **scratch** option\n\n```koji call build '\"git+https://url#commit\"' target --kwargs '{\"opts\":{\"scratch\": True}}'```\n\nCall build with **arch override**\n\n```koji call build '\"git+https://url#commit\"' target --kwargs '{\"opts\":{\"arch_override\":\"x86_64\"}}'```\n\nCall build on **specific channel**\n\n```koji call build '\"git+https://url#commit\"' target --kwargs '{\"channel\":\"default\"}'```\n\nDisplay **help**\n\n```koji call --help```"],["SYNOPSIS","**koji call** [_options_] _function_ [_arguments_]"],["DESCRIPTION","**koji call** executes an arbitrary XML-RPC call to the Koji hub. This allows direct access to the Koji API for advanced operations not covered by standard subcommands.\n\nThe function signature follows the Koji API, such as `build(src, target, opts=None, priority=None, channel=None)`. Arguments are passed positionally, with complex options provided via `--kwargs`."],["PARAMETERS","**function**\n> The XML-RPC function name to call\n\n**--kwargs JSON**\n> Pass keyword arguments as JSON object\n\n**--json-output**\n> Output results in JSON format\n\n**-h, --help**\n> Display help information"],["CAVEATS","Requires deep knowledge of the Koji API. Incorrect calls can have unintended effects. JSON syntax must be properly quoted for shell escaping."],["SEE ALSO","[koji](/man/koji)(1), [koji-build](/man/koji-build)(1)"]],"e":["koji","koji-build"],"x":"koji call build '\"git+https://src.fedoraproject.org/rpms/pkg.git#commit\"' target"},"kubectl-taint":{"n":"kubectl-taint","t":"adds or removes node taints","s":[["TAGLINE","adds or removes node taints"],["TLDR","**Add taint to node**\n\n```kubectl taint nodes [node-name] [key=value:NoSchedule]```\n\n**Remove taint**\n\n```kubectl taint nodes [node-name] [key:NoSchedule-]```\n\n**Add NoExecute taint**\n\n```kubectl taint nodes [node-name] [key=value:NoExecute]```\n\n**Add PreferNoSchedule**\n\n```kubectl taint nodes [node-name] [key=value:PreferNoSchedule]```\n\n**Taint multiple nodes**\n\n```kubectl taint nodes [node1] [node2] [key=value:NoSchedule]```"],["SYNOPSIS","**kubectl taint** [_options_] _node_ _key=value:effect_"],["PARAMETERS","_NODE_\n> Node name.\n\n_KEY=VALUE:EFFECT_\n> Taint specification.\n\n**NoSchedule**\n> Prevent scheduling.\n\n**NoExecute**\n> Evict existing pods.\n\n**PreferNoSchedule**\n> Soft no-schedule.\n\n**-** (suffix on the taint key)\n> Remove a taint matching the given _key:effect_ pair (e.g. **node1 dedicated:NoSchedule-**).\n\n**--all**\n> Apply the taint operation to every node in the cluster.\n\n**-l**, **--selector** _SELECTOR_\n> Apply only to nodes matching the label selector (e.g. `--selector=role=worker`).\n\n**--overwrite**\n> Allow updating the value of an existing taint (without it, attempting to add a taint with the same key produces an error).\n\n**--dry-run** _client|server|none_\n> Print what would change without modifying the cluster.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kubectl taint** adds, updates, or removes node taints. A taint is a _key=value:effect_ tuple attached to a node; pods are scheduled or kept on the node only if they carry a matching **toleration** in their pod spec. The supported effects are **NoSchedule** (block new pods that don't tolerate it), **PreferNoSchedule** (best-effort avoid), and **NoExecute** (evict running pods that don't tolerate it).\n\nTaints are the standard mechanism for dedicating nodes to a workload class (e.g., GPU nodes), keeping pods off control-plane nodes, and gracefully draining nodes for maintenance in conjunction with **kubectl drain**."],["CAVEATS","**NoExecute** evicts already-scheduled pods that lack a matching toleration; ensure tolerations are in place before tainting a populated node. Overwriting an existing taint requires **--overwrite**. Removing a taint uses a trailing **-** on the taint key (no value needed)."],["HISTORY","kubectl taint provides taint management for **Kubernetes** advanced scheduling control."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-cordon](/man/kubectl-cordon)(1), [kubectl-label](/man/kubectl-label)(1)"]],"e":["kubectl","kubectl-cordon","kubectl-label"],"x":"kubectl taint nodes [node-name] [key=value:NoSchedule]"},"lightpanda":{"n":"lightpanda","t":"Headless browser designed for AI and automation","s":[["TAGLINE","Headless browser designed for AI and automation"],["TLDR","**Fetch a URL** and dump the rendered HTML to stdout\n\n```lightpanda fetch --dump html [url]```\n\n**Fetch a URL** and output as **markdown**\n\n```lightpanda fetch --dump markdown [url]```\n\n**Start a CDP server** on the default host and port\n\n```lightpanda serve```\n\n**Start a CDP server** on a specific host and port\n\n```lightpanda serve --host [127.0.0.1] --port [9222]```\n\n**Fetch a URL** while obeying robots.txt\n\n```lightpanda fetch --obey_robots --dump html [url]```\n\n**Fetch a URL** through an HTTP proxy\n\n```lightpanda fetch --http_proxy [proxy_host:port] --dump html [url]```"],["SYNOPSIS","**lightpanda** _command_ [_options_] [_url_]"],["PARAMETERS","**fetch**\n> Fetch a URL, execute JavaScript, and output the rendered page content to stdout.\n\n**serve**\n> Start a Chrome DevTools Protocol (CDP) server for remote automation via Playwright, Puppeteer, or raw CDP clients.\n\n**help**\n> Display available commands and options.\n\n**--dump** _html_|_markdown_\n> Output format for the fetched page. Use **html** for rendered HTML or **markdown** for a Markdown conversion. (fetch only)\n\n**--with_base**\n> Add a **\\<base\\>** tag in the dumped output. (fetch only)\n\n**--host** _address_\n> Host address for the CDP server. Default **127.0.0.1**. (serve only)\n\n**--port** _port_\n> Port for the CDP server. Default **9222**. (serve only)\n\n**--timeout** _seconds_\n> Inactivity timeout in seconds before disconnecting clients. Default **10**. (serve only)\n\n**--obey_robots**\n> Fetch and obey the robots.txt of the target web pages.\n\n**--http_proxy** _host:port_\n> HTTP proxy to use for all requests. Supports optional **username:password** for basic authentication.\n\n**--http_timeout** _milliseconds_\n> Maximum time in milliseconds the transfer is allowed to complete. **0** means no timeout. Default **10000**.\n\n**--log_level** _level_\n> Set logging verbosity. Default **info**. Use **debug** for detailed output.\n\n**--log_format** _format_\n> Set log output format. Use **pretty** for human-readable output."],["DESCRIPTION","**Lightpanda** is an open-source headless browser built from scratch in **Zig** with the **V8** JavaScript engine, purpose-built for AI agents, web scraping, LLM training, and test automation. Unlike headless modes of traditional browsers, Lightpanda skips graphical rendering entirely, focusing only on DOM construction and JavaScript execution.\n\nIt exposes a **Chrome DevTools Protocol** (CDP) endpoint, making it compatible with existing automation tools like **Playwright**, **Puppeteer**, and **chromedp** as a drop-in replacement for headless Chrome. The **fetch** command retrieves a single URL and dumps the rendered page, while **serve** launches a persistent CDP server for remote browser control.\n\nLightpanda achieves **9x less memory usage** and **11x faster execution** compared to headless Chrome, making it particularly suited for high-volume scraping and automation workloads."],["CAVEATS","Lightpanda is in **beta** and does not yet implement the full web platform. Complex web applications that rely on advanced CSS layout, canvas, WebGL, or certain newer Web APIs may not work correctly. Windows is not natively supported; use WSL instead. As a non-Chromium browser, some sites with browser fingerprinting may behave differently."],["HISTORY","Lightpanda was created by **Francis** and **Pierre**, who previously worked on large-scale web scraping using headless Chrome and found it too resource-heavy for high-volume workloads. They spent over **two years** building a headless browser from scratch in **Zig**, choosing the language for its low-level control and performance characteristics. The project was publicly announced on **Hacker News in early 2025** and released as open source under the **AGPL-3.0** license."],["INSTALL","```aur: yay -S lightpanda```"],["SEE ALSO","[chromium](/man/chromium)(1), [curl](/man/curl)(1), [wget](/man/wget)(1), [puppeteer](/man/puppeteer)(1)"]],"e":["chromium","curl","wget","puppeteer"],"x":"lightpanda fetch --dump html [url]"},"make":{"n":"make","t":"builds programs from source","s":[["TAGLINE","builds programs from source"],["TLDR","**Build the default target**\n\n```make```\n\n**Build a specific target**\n\n```make [target]```\n\n**Run parallel jobs**\n\n```make -j [4]```\n\n**Use a specific makefile**\n\n```make -f [Makefile.custom]```\n\n**Dry run (print commands without executing)**\n\n```make -n```\n\n**Override a variable**\n\n```make [VAR=value] [target]```\n\n**Change to a directory before building**\n\n```make -C [path/to/dir]```\n\n**Force rebuild of all targets**\n\n```make -B```"],["SYNOPSIS","**make** [_options_] [_target_] [_VAR=value_ ...]"],["PARAMETERS","_TARGET_\n> Target to build. Defaults to the first target in the Makefile.\n\n**-f** _FILE_\n> Read _FILE_ as the makefile.\n\n**-j** [_N_]\n> Run up to _N_ jobs in parallel. Without _N_, no limit is imposed.\n\n**-n**, **--dry-run**\n> Print commands that would be executed without running them.\n\n**-B**, **--always-make**\n> Unconditionally rebuild all targets.\n\n**-C** _DIR_\n> Change to _DIR_ before reading makefiles or doing anything else.\n\n**-k**, **--keep-going**\n> Continue building other targets even after an error.\n\n**-s**, **--silent**\n> Do not print commands as they are executed.\n\n**-p**\n> Print the internal database of rules and variables, then proceed normally.\n\n**-q**, **--question**\n> Return exit status 0 if targets are up to date, nonzero otherwise; execute nothing.\n\n**-r**, **--no-builtin-rules**\n> Disable built-in implicit rules.\n\n**-d**\n> Print verbose debugging information about files and rules considered.\n\n**-I** _DIR_\n> Search _DIR_ for included makefiles.\n\n**--help**\n> Display help information."],["DESCRIPTION","**make** is a build automation tool that reads instructions from a Makefile to compile and link programs from source code. Each Makefile defines targets (the files to build), their dependencies (the source files they rely on), and the shell commands needed to produce them. When invoked, make constructs a dependency graph and executes only the commands whose targets are out of date.\n\nThe tool determines what needs rebuilding by comparing file modification timestamps — if a source file is newer than its corresponding target, that target is rebuilt along with anything that depends on it. This incremental approach avoids redundant recompilation and can dramatically speed up large builds. The `-j` flag enables parallel execution of independent targets across multiple CPU cores, further reducing build times.\n\nMake's rule-based system also supports pattern rules, implicit rules, and variables, allowing concise Makefiles that scale from simple single-file projects to complex multi-directory software systems. Variables can be overridden on the command line (`make CC=clang`), and the special target `clean` is a conventional name for removing build artifacts."],["CAVEATS","Recipe lines in a Makefile must be indented with a real tab character, not spaces. Behavior may differ between GNU make, BSD make, and other implementations. Parallel builds (`-j`) can expose undeclared dependencies."],["HISTORY","make was created by **Stuart Feldman** at Bell Labs in **1976**, making it one of the oldest and most widely used build tools in software development."],["INSTALL","```apt: sudo apt install make```\n\n```dnf: sudo dnf install make```\n\n```pacman: sudo pacman -S make```\n\n```apk: sudo apk add make```\n\n```zypper: sudo zypper install make```\n\n```brew: brew install make```"],["SEE ALSO","[cmake](/man/cmake)(1), [ninja](/man/ninja)(1), [autoconf](/man/autoconf)(1)"]],"e":["cmake","ninja","autoconf"],"x":"make"},"meson":{"n":"meson","t":"Fast and user-friendly build system","s":[["TAGLINE","Fast and user-friendly build system"],["TLDR","**Setup build directory**\n\n```meson setup [builddir]```\n\n**Configure with options**\n\n```meson setup [builddir] -D[option=value]```\n\n**Compile project**\n\n```meson compile -C [builddir]```\n\n**Run tests**\n\n```meson test -C [builddir]```\n\n**Install project**\n\n```meson install -C [builddir]```\n\n**Reconfigure**\n\n```meson configure [builddir] -D[option=value]```\n\n**Wipe and reconfigure build directory**\n\n```meson setup --wipe [builddir]```\n\n**Create a release build**\n\n```meson setup [builddir] --buildtype=release```"],["SYNOPSIS","**meson** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Meson subcommand (see list below).\n\n**init**\n> Create a minimal meson.build for a new project in the current directory.\n\n**setup** _DIR_\n> Configure a build directory (the canonical first step).\n\n**compile** **-C** _DIR_\n> Build the project.\n\n**test** **-C** _DIR_\n> Run the project's test suite.\n\n**install** **-C** _DIR_\n> Install the built project into the configured prefix.\n\n**configure** _DIR_\n> Change build options on an existing build directory without wiping it.\n\n**dist**\n> Create a signed source distribution tarball.\n\n**introspect**\n> Print a JSON-formatted description of targets, tests, options, etc.\n\n**subprojects**, **wrap**\n> Manage meson subprojects and **.wrap** dependency files.\n\n**devenv**\n> Spawn a shell with the build environment (e.g. **PATH**, **PKG_CONFIG_PATH**) pre-set.\n\n**rewrite**\n> Programmatically modify **meson.build** files.\n\n**env2mfile**\n> Generate a cross- or native-file from current environment variables.\n\n**format**\n> Auto-format **meson.build** files (alias **fmt**).\n\n**-D** _OPT=VAL_\n> Set a build option (e.g. **-Dbuildtype=release**, **-Dprefix=/usr**).\n\n**--buildtype** _TYPE_\n> Build type: **plain**, **debug**, **debugoptimized**, **release**, **minsize**, **custom**.\n\n**--prefix** _PATH_\n> Installation prefix (default **/usr/local**).\n\n**--backend** _BACKEND_\n> Backend to generate: **ninja** (default), **vs**, **vs2017**, **xcode**, etc.\n\n**--default-library** _TYPE_\n> Default library type: **static**, **shared**, or **both**.\n\n**--cross-file** _FILE_\n> Use a cross-compilation definition file.\n\n**--native-file** _FILE_\n> Override native compiler/tool selection on the host.\n\n**--reconfigure**\n> Re-run configuration using the stored options without wiping the build directory.\n\n**--wipe**\n> Wipe and reconfigure an existing build directory.\n\n**--werror**, **--warnlevel** _N_\n> Treat warnings as errors, or set compiler warning level (0-3/everything).\n\n**--optimization** _LEVEL_\n> Optimization level (**0**, **g**, **1**, **2**, **3**, **s**).\n\n**--vsenv**\n> Activate a Visual Studio environment before building on Windows.\n\n**--help**\n> Display help information."],["DESCRIPTION","**meson** is a build system designed to be fast and user-friendly. It reads **meson.build** project definitions and generates backend build files (ninja by default) for actual compilation.\n\nMeson supports C, C++, Rust, Java, Fortran, and other languages. It features automatic dependency detection, cross-compilation support, and built-in unit test and coverage infrastructure. Used by projects like GNOME, systemd, X.Org, and Mesa."],["CAVEATS","Requires Python 3 and ninja backend by default. Build directories cannot be reused across different source directories. Out-of-source builds only (cannot build in the source tree). The --wipe flag destroys and recreates the build directory."],["HISTORY","Meson was created by **Jussi Pakkanen** in 2013 as a fast, user-friendly build system."],["INSTALL","```dnf: sudo dnf install meson```\n\n```pacman: sudo pacman -S meson```\n\n```apk: sudo apk add meson```\n\n```zypper: sudo zypper install meson```\n\n```brew: brew install meson```\n\n```nix: nix profile install nixpkgs#meson```"],["SEE ALSO","[ninja](/man/ninja)(1), [cmake](/man/cmake)(1), [make](/man/make)(1)"]],"e":["ninja","cmake","make"],"x":"meson setup [builddir]"},"mg":{"n":"mg","t":"micro GNU Emacs-style editor","s":[["TAGLINE","micro GNU Emacs-style editor"],["TLDR","**Open a file**\n\n```mg [file.txt]```\n\n**Open a file at a specific line**\n\n```mg +[10] [file.txt]```\n\n**Open in read-only mode**\n\n```mg -R [file.txt]```\n\n**Open multiple files** (switch with C-x b)\n\n```mg [file1.txt] [file2.txt]```"],["SYNOPSIS","**mg** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Files to edit.\n\n**+** _LINE_\n> Start at specified line number.\n\n**-R**\n> Read-only (view) mode.\n\n**-n**\n> Turn off backup file generation.\n\n**-f** _mode_\n> Run the given mode at startup (e.g., -f auto-fill-mode)."],["DESCRIPTION","**mg** is a micro GNU Emacs-style editor. It provides basic Emacs keybindings in a lightweight package, making it ideal for quick edits on minimal systems.\n\nCommon keybindings: **C-x C-s** (save), **C-x C-c** (quit), **C-x C-f** (open file), **C-s** (incremental search), **C-x b** (switch buffer), **C-x k** (kill buffer), **C-space** (set mark), **C-w** (cut region), **M-w** (copy region), **C-y** (paste), **C-x u** (undo).\n\nmg is the default editor on OpenBSD and is available on other BSD and Linux systems."],["CAVEATS","Much more limited than GNU Emacs. No Emacs Lisp extension support, no syntax highlighting, no Unicode support. Intended for basic text editing only."],["HISTORY","mg was originally named **MicroGNUEmacs** (later shortened to **mg**), created in **1986** by Dave Conroy for systems where full GNU Emacs was too large. It was subsequently maintained by various contributors and is now part of the OpenBSD base system."],["INSTALL","```apt: sudo apt install mg```\n\n```dnf: sudo dnf install mg```\n\n```pacman: sudo pacman -S mg```\n\n```apk: sudo apk add mg```\n\n```zypper: sudo zypper install mg```\n\n```brew: brew install mg```\n\n```nix: nix profile install nixpkgs#mg```"],["SEE ALSO","[emacs](/man/emacs)(1), [nano](/man/nano)(1), [vi](/man/vi)(1)"]],"e":["emacs","nano","vi"],"x":"mg [file.txt]"},"mh_lint":{"n":"mh_lint","t":"checks MATLAB code for issues","s":[["TAGLINE","checks MATLAB code for issues"],["TLDR","**Check MATLAB code**\n\n```mh_lint [file.m]```\n\n**Check directory**\n\n```mh_lint [src/]```\n\n**Output to file**\n\n```mh_lint [file.m] > [report.txt]```\n\n**Enable all checks**\n\n```mh_lint --all [file.m]```\n\n**Show available checks**\n\n```mh_lint --list```"],["SYNOPSIS","**mh_lint** [_options_] _files_"],["PARAMETERS","_FILES_\n> MATLAB files to check.\n\n**--all**\n> Enable all checks.\n\n**--list**\n> List available checks.\n\n**--fix**\n> Auto-fix issues.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mh_lint** checks MATLAB code for issues. It identifies style violations and potential bugs.\n\nThe tool is part of MISS_HIT. It enforces coding standards for MATLAB/Simulink projects."],["CAVEATS","Part of MISS_HIT. MATLAB specific. Python-based tool."],["HISTORY","mh_lint is part of **MISS_HIT**, a MATLAB/Simulink code quality tool suite."],["SEE ALSO","[mh_metric](/man/mh_metric)(1)"]],"e":["mh_metric"],"x":"mh_lint [file.m]"},"modetest":{"n":"modetest","t":"diagnostic tool for testing and debugging Kernel Mode Setting functionality","s":[["TAGLINE","diagnostic tool for testing and debugging Kernel Mode Setting functionality"],["TLDR","**List connectors** and their available modes for a specific driver\n\n```modetest -M [driver] -c```\n\n**List all DRM objects** (connectors, encoders, CRTCs, planes)\n\n```modetest -M [i915]```\n\n**Set resolution** of a connector\n\n```sudo modetest -M [driver] -s [connector_id]:[1920x1080]```\n\n**Test a specific plane** with format\n\n```sudo modetest -M [driver] -P [plane_id]@[crtc_id]:[WxH]+[X]+[Y]@[format]```\n\n**Set a DRM property** on an object\n\n```sudo modetest -M [driver] -w [obj_id]:[prop_name]:[value]```\n\n**Use atomic mode setting** with a connector\n\n```sudo modetest -M [driver] -a -s [connector_id]:[1920x1080]```"],["SYNOPSIS","**modetest** [**-M** _module_] [**-D** _device_] [**-c**] [**-e**] [**-f**] [**-p**] [**-s** _connector:mode_] [**-P** _plane_] [**-a**] [**-C**] [**-w** _prop_] [**-v**] [**-d**]"],["PARAMETERS","**-M _module_**\n> Use the given DRM driver module (e.g., i915, amdgpu, nouveau)\n\n**-D _device_**\n> Use the given DRM device node\n\n**-c**\n> List connectors and their available modes\n\n**-e**\n> List encoders\n\n**-f**\n> List framebuffers\n\n**-p**\n> List CRTCs and planes\n\n**-s _connector_id[,connector_id][@crtc_id]:mode[-vrefresh][@format]_**\n> Set a mode on one or more connectors\n\n**-P _plane_id@crtc_id:WxH[+X+Y][*scale][@format]_**\n> Test a plane with specified configuration\n\n**-C**\n> Test hardware cursor\n\n**-v**\n> Test vsynced page flipping\n\n**-w _obj_id:prop_name:value_**\n> Set a property on a DRM object\n\n**-a**\n> Use atomic mode setting API\n\n**-F _pattern1,pattern2_**\n> Specify fill patterns for test image\n\n**-d**\n> Drop DRM master after mode set"],["DESCRIPTION","**modetest** is a diagnostic tool for testing and debugging Kernel Mode Setting (KMS) functionality of Direct Rendering Manager (DRM) drivers. It enumerates DRM objects including connectors, encoders, CRTCs, and planes, and can set display modes directly.\n\nThe tool is part of **libdrm** and provides low-level access to the graphics subsystem for testing display configurations, pixel formats, and plane overlays. When no query or test options are given, it dumps all available DRM information. It displays an SMPTE color bar test pattern when setting modes."],["CAVEATS","Requires root privileges or membership in the render/video groups to access DRM devices. Running modetest on an active display can disrupt the current desktop environment (Xorg, Wayland), potentially causing blank screens or display corruption. Primarily intended for developers and diagnostic purposes."],["HISTORY","**modetest** is part of the **libdrm** project, which provides the userspace library for interacting with the Linux kernel's Direct Rendering Manager. KMS was introduced in Linux kernel 2.6.29 (2009) to move display mode setting from userspace X servers into the kernel."],["INSTALL","```apt: sudo apt install libdrm-tests```\n\n```apk: sudo apk add libdrm-tests```"],["SEE ALSO","[xrandr](/man/xrandr)(1)"]],"e":["xrandr"],"x":"modetest -M [driver] -c"},"needs":{"n":"needs","t":"Check if required executables are installed on the system","s":[["TAGLINE","Check if required executables are installed on the system"],["TLDR","**Check if tools are installed and show their versions**\n\n```needs [tool1] [tool2] [tool3]```\n\n**Check without retrieving version information**\n\n```needs --no-versions [tool1] [tool2]```\n\n**Show full version strings instead of short versions**\n\n```needs --full-versions [tool1] [tool2]```\n\n**Run quietly, only reporting via exit code**\n\n```needs -q [tool1] [tool2]```\n\n**Check with verbose logging**\n\n```needs -vvv [tool1] [tool2]```"],["SYNOPSIS","**needs** [_options_] [_executables_...]"],["PARAMETERS","**-n**, **--no-versions**\n> Skip version checks, only verify that binaries exist.\n\n**-f**, **--full-versions**\n> Show the full version string for each binary instead of a shortened version.\n\n**-q**, **--quiet**\n> Stay quiet, only communicate results via exit code (0 if all found, 1 if any missing).\n\n**-v**, **--verbosity**\n> Increase verbosity level. Can be repeated for more detail (e.g., -vvv for logs, -vvvv for timing info).\n\n**--help**\n> Display help information.\n\n**--version**\n> Show the version of needs itself."],["DESCRIPTION","**needs** is a CLI tool that verifies whether specified executables are installed on a system and optionally retrieves their version information. It runs checks using multi-threaded execution for speed, provides colored output, and supports pipe-friendly output.\n\nBinary names can be provided as command-line arguments or listed in a `needsfile` (one or more binaries per line).\n\nExit codes indicate whether all required tools are present (0 for success, 1 for missing), making it suitable for use in scripts and CI pipelines."],["CAVEATS","Version detection relies on each tool supporting a standard version flag (`--version` or `-v`). Some tools may not have their version detected correctly. The `--no-versions` and `--full-versions` flags are only available when compiled with the `version-retrieval` feature (enabled by default)."],["HISTORY","**needs** was created by **NQMVD** and is written in **Rust**."],["SEE ALSO","[which](/man/which)(1), [command](/man/command)(1), [has](/man/has)(1), [type](/man/type)(1), [whereis](/man/whereis)(1)"]],"e":["which","command","has","type","whereis"],"x":"needs [tool1] [tool2] [tool3]"},"netperf":{"n":"netperf","t":"measures network throughput and latency","s":[["TAGLINE","measures network throughput and latency"],["TLDR","**Run TCP throughput test**\n\n```netperf -H [hostname]```\n\n**Run UDP test**\n\n```netperf -H [hostname] -t UDP_STREAM```\n\n**Run latency test**\n\n```netperf -H [hostname] -t TCP_RR```\n\n**Run test for specific duration**\n\n```netperf -H [hostname] -l [30]```\n\n**Specify message size**\n\n```netperf -H [hostname] -- -m [1024]```\n\n**Run with multiple connections**\n\n```netperf -H [hostname] -C -c```\n\n**Start netserver** (on remote host)\n\n```netserver```"],["SYNOPSIS","**netperf** [_-H host_] [_-t test_] [_-l duration_] [_options_] [_-- test_options_]"],["PARAMETERS","**-H** _HOST_\n> Remote host running netserver.\n\n**-t** _TEST_\n> Test type (TCP_STREAM, UDP_STREAM, TCP_RR, etc.).\n\n**-l** _SECONDS_\n> Test duration.\n\n**-p** _PORT_\n> Port for control connection.\n\n**-P** _PORT_\n> Port for data connection.\n\n**-c**\n> Report local CPU utilization.\n\n**-C**\n> Report remote CPU utilization.\n\n**-f** _UNITS_\n> Output format (K, M, G for bits/bytes).\n\n**-v** _LEVEL_\n> Verbosity level.\n\n**-- -m** _SIZE_\n> Message size for test.\n\n**-- -s** _SIZE_\n> Local socket buffer size.\n\n**-- -S** _SIZE_\n> Remote socket buffer size."],["DESCRIPTION","**netperf** measures network throughput and latency. It consists of netperf (client) and netserver (server running on the remote host).\n\nTCP_STREAM tests bulk data transfer throughput. It measures how fast data can be pushed through a TCP connection, useful for evaluating network capacity.\n\nUDP_STREAM tests UDP throughput without TCP's overhead. Results show raw network capability but may include packet loss.\n\nTCP_RR (request/response) measures latency - how long round trips take. This is critical for interactive applications and databases.\n\nCPU utilization reporting helps identify whether the network or CPU limits performance. High CPU use during network tests suggests the endpoint is the bottleneck.\n\nSocket buffer sizes affect performance. Larger buffers improve throughput on high-latency links but increase memory use."],["CAVEATS","Requires netserver on remote host. Firewall may block connections. Results affected by other network traffic. CPU measurement needs privileges on some systems."],["HISTORY","**netperf** was developed by **Hewlett-Packard** starting in the **1990s** as a network benchmarking tool. It became a standard for measuring network performance, used in countless benchmarks and academic papers."],["INSTALL","```apt: sudo apt install netperf```\n\n```dnf: sudo dnf install netperf```\n\n```pacman: sudo pacman -S netperf```\n\n```nix: nix profile install nixpkgs#netperf```"],["SEE ALSO","[iperf3](/man/iperf3)(1), [nuttcp](/man/nuttcp)(1), [ping](/man/ping)(1), [netcat](/man/netcat)(1)"]],"e":["iperf3","nuttcp","ping","netcat"],"x":"netperf -H [hostname]"},"ntfs-read.py":{"n":"ntfs-read.py","t":"Impacket tool for reading NTFS volumes without mounting","s":[["TAGLINE","Impacket tool for reading NTFS volumes without mounting"],["TLDR","**Read a file from an NTFS partition**\n\n```ntfs-read.py [/dev/sda1] [/path/to/file]```\n\n**List contents of a directory**\n\n```ntfs-read.py [/dev/sda1] -l [/path/to/dir]```\n\n**Extract a file to a local output file**\n\n```ntfs-read.py [/dev/sda1] [/path/to/file] -o [output]```\n\n**List the root directory**\n\n```ntfs-read.py [/dev/sda1] -l /```"],["SYNOPSIS","**ntfs-read.py** [_options_] _device_ [_path_]"],["PARAMETERS","_DEVICE_\n> NTFS partition device (e.g., /dev/sda1).\n\n_PATH_\n> Path within the NTFS volume to read or list.\n\n**-l**\n> List directory contents instead of reading a file.\n\n**-o** _FILE_\n> Write output to the specified file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ntfs-read.py** is an Impacket tool that reads NTFS file systems directly from block devices without mounting them. It parses the NTFS structures at a low level, providing access to files and directories on NTFS partitions.\n\nThis is useful for forensic analysis and data recovery scenarios where mounting the filesystem is undesirable or not possible. The tool can list directories, read individual files, and extract their contents.\n\nPart of the **Impacket** security toolkit, which provides Python classes for working with network protocols and Windows-related structures."],["CAVEATS","Requires raw device access, so typically needs root privileges. As an Impacket Python script, it requires the Impacket library to be installed. Intended for security research and forensic use; not suitable as a general-purpose NTFS access tool."],["HISTORY","ntfs-read.py is part of **Impacket**, a collection of Python classes for working with network protocols originally developed by **SecureAuth** (now **Fortra**). It provides low-level NTFS access for security research and digital forensics."],["INSTALL","```apk: sudo apk add py3-impacket```"],["SEE ALSO","[ntfs-3g](/man/ntfs-3g)(1), [ntfsfix](/man/ntfsfix)(1), [ntfsclone](/man/ntfsclone)(1), [impacket](/man/impacket)(1)"]],"e":["ntfs-3g","ntfsfix","ntfsclone","impacket"],"x":"ntfs-read.py [/dev/sda1] [/path/to/file]"},"nxc-mssql":{"n":"nxc-mssql","t":"Test Microsoft SQL Server credentials and execute queries via NetExec","s":[["TAGLINE","Test Microsoft SQL Server credentials and execute queries via NetExec"],["TLDR","**Test MSSQL credentials against a target**\n\n```nxc mssql [192.168.1.100] -u [user] -p [password]```\n\n**Authenticate using local authentication instead of Windows auth**\n\n```nxc mssql [target] -u [sa] -p [password] --local-auth```\n\n**Execute a SQL query**\n\n```nxc mssql [target] -u [sa] -p [password] -q \"[SELECT @@version]\"```\n\n**Execute an OS command via xp_cmdshell**\n\n```nxc mssql [target] -u [sa] -p [password] -x \"[whoami]\"```\n\n**Check for privilege escalation paths**\n\n```nxc mssql [target] -u [user] -p [password] -M mssql_priv```\n\n**Attempt privilege escalation to sysadmin**\n\n```nxc mssql [target] -u [user] -p [password] -M mssql_priv -o ACTION=privesc```"],["SYNOPSIS","**nxc mssql** _target_ [**-u** _user_] [**-p** _pass_] [_options_]"],["PARAMETERS","**-u** _user_\n> Username for authentication.\n\n**-p** _pass_\n> Password for authentication.\n\n**-q** _query_\n> Execute a SQL query on the target.\n\n**-x** _cmd_\n> Execute an OS command via xp_cmdshell.\n\n**--local-auth**\n> Use local authentication instead of Windows domain authentication.\n\n**--port** _port_\n> MSSQL port (default: 1433).\n\n**-M** _module_\n> Run a specific module (e.g., mssql_priv).\n\n**-o** _KEY=VALUE_\n> Module options (e.g., ACTION=privesc).\n\n**--put-file** _src_ _dst_\n> Upload a file to the target system.\n\n**--get-file** _src_ _dst_\n> Download a file from the target system."],["DESCRIPTION","**nxc mssql** is the Microsoft SQL Server protocol module of **NetExec** (formerly CrackMapExec). It tests credentials, executes SQL queries, runs OS commands, and supports privilege escalation against MSSQL targets.\n\nThe tool supports Windows (NTLM/Kerberos) and local SQL Server authentication. It can spray credentials across multiple targets, execute queries, and use modules like **mssql_priv** to check for and exploit privilege escalation paths."],["CAVEATS","For authorized security assessments only. Requires proper authorization before use. OS command execution requires xp_cmdshell to be enabled on the target."],["SEE ALSO","[nxc](/man/nxc)(1), [sqlcmd](/man/sqlcmd)(1)"]],"e":["nxc","sqlcmd"],"x":"nxc mssql [192.168.1.100] -u [user] -p [password]"},"oha":{"n":"oha","t":"HTTP load generator written in Rust, inspired by hey and rakyll/hey","s":[["TAGLINE","HTTP load generator written in Rust, inspired by hey and rakyll/hey"],["TLDR","**Send 100 requests to URL**\n\n```oha -n [100] [https://example.com]```\n\n**Run load test for 30 seconds**\n\n```oha -z [30s] [https://example.com]```\n\n**Test with 50 concurrent connections**\n\n```oha -c [50] -n [1000] [https://example.com]```\n\n**POST request with JSON body**\n\n```oha -m POST -d '[{\"key\":\"value\"}]' -T application/json [https://example.com/api]```\n\n**Use custom headers**\n\n```oha -H \"Authorization: Bearer [token]\" [https://example.com]```\n\n**Set queries per second limit**\n\n```oha -q [100] -z [30s] [https://example.com]```\n\n**Disable TLS verification**\n\n```oha --insecure [https://example.com]```\n\n**Output results as JSON**\n\n```oha -n [100] --json [https://example.com]```"],["SYNOPSIS","**oha** [_options_] _url_"],["PARAMETERS","**-n** _num_\n> Number of requests to send. Default: 200.\n\n**-c** _num_\n> Number of concurrent connections. Default: 50.\n\n**-z** _duration_\n> Duration of test (e.g., 10s, 1m).\n\n**-q** _rate_\n> Rate limit in queries per second.\n\n**-m**, **--method** _method_\n> HTTP method: GET, POST, PUT, DELETE, etc.\n\n**-d**, **--data** _body_\n> Request body data.\n\n**-D**, **--data-file** _file_\n> Read request body from file.\n\n**-T**, **--content-type** _type_\n> Content-Type header value.\n\n**-H** _header_\n> Custom header (repeatable).\n\n**-A**, **--accept** _type_\n> Accept header value.\n\n**-t**, **--timeout** _secs_\n> Request timeout in seconds.\n\n**--latency-correction**\n> Enable coordinated omission correction.\n\n**--no-tui**\n> Disable real-time terminal UI.\n\n**--json**\n> Output results in JSON format.\n\n**--insecure**\n> Skip TLS certificate verification.\n\n**--http2**\n> Use HTTP/2.\n\n**--host** _host_\n> Override Host header.\n\n**-p**, **--redirect** _num_\n> Maximum redirects to follow."],["DESCRIPTION","**oha** is an HTTP load generator written in Rust, inspired by hey and rakyll/hey. It provides real-time TUI visualization during load tests and detailed latency statistics.\n\nBy default, oha displays a terminal interface showing request progress, latency distribution, and error rates. Use **--no-tui** for scripted execution or **--json** for machine-readable output.\n\nThe tool measures response time distribution including percentiles (p50, p90, p99), throughput, and error rates. With **--latency-correction**, it applies coordinated omission correction for accurate latency under load.\n\nRate limiting with **-q** enables controlled load testing without overwhelming targets. Combined with **-z** for duration-based tests, this enables realistic traffic simulation.\n\noha supports HTTP/1.1 and HTTP/2, custom headers for authentication, and various request body formats for API testing."],["CAVEATS","High concurrency settings may exhaust file descriptors or network resources. Use --insecure cautiously. Rate limiting accuracy depends on system capabilities. Results should be interpreted considering network conditions."],["HISTORY","oha was created by **Haruki Shimada** (hatoo) and first released around **2020**. The name means \"good morning\" in Japanese. Written in Rust using tokio for async I/O, it provides modern load testing with real-time visualization. The project gained popularity as a fast, user-friendly alternative to older tools like ab and wrk."],["INSTALL","```pacman: sudo pacman -S oha```\n\n```brew: brew install oha```\n\n```nix: nix profile install nixpkgs#oha```"],["SEE ALSO","[hey](/man/hey)(1), [wrk](/man/wrk)(1), [ab](/man/ab)(1), [curl](/man/curl)(1), [siege](/man/siege)(1)"]],"e":["hey","wrk","ab","curl","siege"],"x":"oha -n [100] [https://example.com]"},"opencubicplayer":{"n":"opencubicplayer","t":"Text-based audio player and music visualizer","s":[["TAGLINE","Text-based audio player and music visualizer"],["TLDR","**Play a music file**\n\n```ocp [path/to/file]```\n\n**Play a directory of files**\n\n```ocp [path/to/directory]```"],["SYNOPSIS","**ocp** [_options_] [_files_...]"],["DESCRIPTION","**opencubicplayer** (command: **ocp**) is a text-based audio player and music visualizer supporting tracked music formats (MOD, XM, S3M, IT), chiptunes, SID, MP3, OGG, FLAC, and more. It features FFT analyzer, oscilloscope, phase viewer, and channel viewer visualizations.\n\nOriginally from the demoscene era on DOS, the Unix/Linux fork maintains the classic text-mode interface with modern codec support."],["CAVEATS","Some visualization modes require a terminal with color support. Tracked format playback quality depends on available decoder libraries."],["HISTORY","The Unix fork of **Open Cubic Player** is maintained by **Stian Sebastian Skjelstad** (mywave82) and is written in **C**."],["SEE ALSO","[cmus](/man/cmus)(1), [mpv](/man/mpv)(1), [musikcube](/man/musikcube)(1)"]],"e":["cmus","mpv","musikcube"],"x":"ocp [path/to/file]"},"openrc":{"n":"openrc","t":"service manager for OpenRC-based systems, providing an alternative to systemd","s":[["TAGLINE","service manager for OpenRC-based systems, providing an alternative to systemd"],["TLDR","Change to a **specific runlevel**\n\n```sudo openrc [runlevel_name]```\n\nChange to a runlevel, but **don't stop any existing services**\n\n```sudo openrc -n [runlevel_name]```"],["SYNOPSIS","**openrc** [_options_] [_runlevel_]"],["PARAMETERS","**-n, --no-stop**\n> Don't stop any services when changing runlevels\n\n**-o, --override**\n> Override the next runlevel to go to when leaving single user\n\n**-s, --service**\n> Run the service specified with the rest of the arguments\n\n**-S, --sys**\n> Run the service as if in /etc/init.d"],["DESCRIPTION","**openrc** is the service manager for OpenRC-based systems, providing an alternative to systemd for managing system services and runlevels. It's the default init system on Gentoo, Alpine Linux, and other distributions.\n\nOpenRC uses the concept of runlevels (similar to SysV init) to group services that should run together. Common runlevels include **default** (normal operation), **boot** (essential services), **single** (single-user mode), and **shutdown**.\n\nServices are managed through scripts in **/etc/init.d/** and configured using **rc-update** to add or remove them from runlevels."],["CAVEATS","Runlevel changes may stop services in the current runlevel that aren't in the target. Use **-n** to preserve running services. OpenRC is not compatible with systemd; distributions use one or the other."],["INSTALL","```apt: sudo apt install openrc```\n\n```apk: sudo apk add openrc```"],["SEE ALSO","[rc-status](/man/rc-status)(8), [rc-update](/man/rc-update)(8), [rc-service](/man/rc-service)(8)"]],"e":["rc-status","rc-update","rc-service"],"x":"sudo openrc [runlevel_name]"},"pacman-f":{"n":"pacman-f","t":"short form of **pacman --files**, which queries the files database","s":[["TAGLINE","short form of **pacman --files**, which queries the files database"],["TLDR","**Refresh** the files database from the configured mirrors\n\n```sudo pacman -Fy```\n\n**Find which package** owns a given file path\n\n```pacman -F [bin/ls]```\n\n**List all files** installed by a given package\n\n```pacman -Fl [pacman]```\n\n**Search for files** matching a regular expression\n\n```pacman -Fx '[\\.so\\.1$]'```\n\n**Machine-readable output** suitable for scripts\n\n```pacman -F [pattern] --machinereadable```"],["SYNOPSIS","**pacman** **-F** [_options_] [_file_...]"],["PARAMETERS","**-y**, **--refresh**\n> Download a fresh copy of each repository's **.files** database. Pass twice to force a download even if the local copy looks current.\n\n**-l**, **--list**\n> List the files owned by the given package(s).\n\n**-x**, **--regex**\n> Treat the query as a POSIX regular expression.\n\n**-q**, **--quiet**\n> Show less output (useful for scripting).\n\n**--machinereadable**\n> Output results in a tab-separated, script-friendly format."],["DESCRIPTION","**pacman -F** (equivalently **pacman --files**) queries the Arch files database, which maps every file in a package to its owning package. It answers the classic \"which package provides **/usr/bin/foo**?\" question without having the package installed.\n\nThe files database must be downloaded first with **pacman -Fy**. A bare **pacman -F** _pattern_ implicitly performs a filename search, so an explicit **-s** is not required."],["INSTALL","```apt: sudo apt install pacman-package-manager```\n\n```dnf: sudo dnf install pacman```\n\n```pacman: sudo pacman -S pacman```\n\n```apk: sudo apk add pacman```\n\n```nix: nix profile install nixpkgs#pacman```"],["SEE ALSO","[pacman](/man/pacman)(8)"]],"e":["pacman"],"x":"sudo pacman -Fy"},"pacsift":{"n":"pacsift","t":"queries and filters packages from pacman databases with flexible criteria","s":[["TAGLINE","queries and filters packages from pacman databases with flexible criteria"],["TLDR","List **all available packages**\n\n```pacsift```\n\nFilter packages that **satisfy** a dependency\n\n```pacsift --satisfies [package]```\n\nSearch **installed packages** by name\n\n```pacsift --local --name [pattern]```\n\nFind **foreign** (AUR/manually installed) packages\n\n```pacsift --local --foreign```\n\nFind **explicitly installed** packages\n\n```pacsift --local --reason explicit```\n\nCombine filters with **exact** matching\n\n```pacsift --exact --repo [extra] --name [package]```"],["SYNOPSIS","**pacsift** [_options_] [_filters_]"],["PARAMETERS","**--satisfies _dep_**\n> Filter packages that satisfy a dependency\n\n**--repo _name_**\n> Filter by repository\n\n**--local**\n> Search only installed packages\n\n**--sync**\n> Search only sync databases\n\n**--name _pattern_**\n> Filter by package name\n\n**--group _name_**\n> Filter by group membership\n\n**--provides _pkg_**\n> Filter packages that provide something\n\n**--depends _pkg_**\n> Filter packages that depend on something\n\n**--exact**\n> Match values exactly\n\n**--regex**\n> Treat values as extended case-insensitive regular expressions\n\n**--invert**\n> Return packages that do NOT match\n\n**--any**\n> Return the union rather than the intersection of matches\n\n**--foreign**\n> Show packages not found in sync databases (e.g., AUR packages)\n\n**--native**\n> Show packages found in sync databases\n\n**--reason** _reason_\n> Filter by install reason (explicit or depend)\n\n**--null**[=_sep_]\n> Set alternate separator for stdin values (default: NUL)"],["DESCRIPTION","**pacsift** queries and filters packages from pacman databases with flexible criteria. Part of the **pacutils** collection, it provides more powerful filtering than basic pacman queries.\n\nThe tool can filter by various attributes: name patterns, repository, installation status, dependencies, groups, and more. Multiple filters can be combined to narrow down results.\n\nOutput is a list of package names suitable for piping to other pacutils commands or pacman."],["CAVEATS","Part of the pacutils package, not installed by default on Arch. Filter syntax differs from pacman's built-in search. For simple searches, **pacman -Ss** may be more convenient."],["SEE ALSO","[pacman](/man/pacman)(8), [paclog](/man/paclog)(1), [pactree](/man/pactree)(8), [pacinfo](/man/pacinfo)(1)"]],"e":["pacman","paclog","pactree","pacinfo"],"x":"pacsift"},"pam_permit":{"n":"pam_permit","t":"always returns success","s":[["TAGLINE","always returns success"],["TLDR","**Allow all authentication**\n\n```auth sufficient pam_permit.so```\n\n**Always succeed session**\n\n```session optional pam_permit.so```"],["SYNOPSIS","**pam_permit.so**"],["PARAMETERS","No parameters. Always returns success."],["DESCRIPTION","**pam_permit** always returns success. Permits all access unconditionally.\n\nThe module grants all requests. Security bypass module."],["CAVEATS","Security risk. Use only when appropriate. No authentication performed."],["HISTORY","pam_permit provides a **universal permit module** for PAM configurations."],["SEE ALSO","[pam](/man/pam)(8), [pam_deny](/man/pam_deny)(8)"]],"e":["pam","pam_deny"],"x":"auth sufficient pam_permit.so"},"pamshadedrelief":{"n":"pamshadedrelief","t":"generate shaded relief image from a PAM height map","s":[["TAGLINE","generate shaded relief image from a PAM height map"],["TLDR","**Create shaded relief** from a height map\n\n```pamshadedrelief [heightmap.pam] > [relief.pam]```\n\n**Apply gamma correction** to the rendered relief\n\n```pamshadedrelief -gamma [2.0] [heightmap.pam] > [relief.pam]```\n\n**Pipe to pamtopnm** to view as PPM\n\n```pamshadedrelief [heightmap.pam] | pamtopnm > [relief.ppm]```\n\n**Render and encode** as PNG\n\n```pamshadedrelief [heightmap.pam] | pnmtopng > [relief.png]```"],["SYNOPSIS","**pamshadedrelief** [**-gamma** _value_] [_pamfile_]"],["DESCRIPTION","**pamshadedrelief** reads a Netpbm PAM file containing a grayscale height map and produces a PAM image rendered as a shaded relief, simulating illumination of a 3D terrain by a virtual light source. Each output pixel encodes the brightness derived from the local slope of the height field, giving a hill-shading effect similar to topographic maps.\n\nThe input must be a one-channel (grayscale) PAM image; sample values are interpreted as elevations. The illumination angle is fixed by the program. Output samples are gamma-corrected with **-gamma** so the contrast of the relief can be tuned without re-rendering the source data."],["PARAMETERS","_PAMFILE_\n> Input grayscale PAM height map. If omitted, read from standard input.\n\n**-gamma** _VALUE_\n> Apply gamma correction to the output. Values greater than 1 increase contrast in dark areas; values less than 1 reduce it. Default is 1.0."],["CAVEATS","Part of **Netpbm**. The input must be a single-plane (grayscale) PAM image; PGM inputs should be promoted to PAM first. The light direction is hard-coded and cannot be changed from the command line. Output is a PAM image; convert to PPM or PNG for display with **pamtopnm** or **pnmtopng**."],["HISTORY","**pamshadedrelief** was added to **Netpbm** to support cartographic and terrain visualization tasks. Netpbm provides a long-lived family of small, composable image utilities operating on the PNM/PAM family of formats."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamtopnm](/man/pamtopnm)(1), [pnmtopng](/man/pnmtopng)(1), [pamflip](/man/pamflip)(1)"]],"e":["pamtopnm","pnmtopng","pamflip"],"x":"pamshadedrelief [heightmap.pam] > [relief.pam]"},"parenthesis":{"n":"parenthesis","t":"Shell parentheses for subshells, arrays, and arithmetic","s":[["TAGLINE","Shell parentheses for subshells, arrays, and arithmetic"],["TLDR","**Run commands in a subshell**\n\n```(cd [/tmp] && [command])```\n\n**Create an array**\n\n```array=([one] [two] [three])```\n\n**Command grouping without subshell**\n\n```{ [command1]; [command2]; }```\n\n**Function definition**\n\n```function_name() { [commands]; }```\n\n**Arithmetic evaluation**\n\n```(( count++ ))```"],["SYNOPSIS","**(** _commands_ **)**\n\n**((**_arithmetic_**))**"],["DESCRIPTION","Parentheses **( )** in shell have several distinct uses depending on context:\n\n**Subshell execution**: Commands in **(...)** run in a child shell. Environment changes (cd, variable assignments) don't affect the parent shell.\n\n**Array literals**: In bash/zsh, **array=(a b c)** creates an array.\n\n**Function definition**: **name() { ... }** defines a function (parentheses are part of syntax, not grouping).\n\n**Command substitution**: **$(...)** captures command output.\n\n**Arithmetic**: **((...))** performs arithmetic evaluation, **$((...))** expands to the result."],["SUBSHELL BEHAVIOR","```bash"],["Changes don't affect parent","(cd /tmp; pwd)  # prints /tmp\npwd             # still original directory"],["Variables don't leak","(x=5)\necho $x         # empty or original value\n```"],["ARITHMETIC (( ))","```bash\n(( x = 5 + 3 ))    # Assignment\n(( x++ ))          # Increment\n(( x > 5 )) && echo \"big\"  # Condition\nresult=$(( a * b ))         # Capture result\n```"],["CAVEATS","Subshells have overhead from process creation. For simple grouping without isolation, use braces **{ ...; }** instead.\n\n**{ }** grouping requires a space after **{** and semicolon before **}**; parentheses don't.\n\nSubshell exit status is visible to parent, but variable changes are not.\n\nNested parentheses may need careful quoting to avoid syntax issues."],["SEE ALSO","[bash](/man/bash)(1), [sh](/man/sh)(1), [test](/man/test)(1)"]],"e":["bash","sh","test"],"x":"(cd [/tmp] && [command])"},"pbmtowbmp":{"n":"pbmtowbmp","t":"converts PBM bitmaps to WBMP format used by WAP/WML mobile web pages","s":[["TAGLINE","converts PBM bitmaps to WBMP format used by WAP/WML mobile web pages"],["TLDR","**Convert PBM to WBMP format**\n\n```pbmtowbmp [input.pbm] > [output.wbmp]```"],["SYNOPSIS","**pbmtowbmp** [_file_]"],["DESCRIPTION","**pbmtowbmp** converts PBM bitmaps to WBMP (Wireless Bitmap) format used by WAP/WML mobile web pages. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[wbmptopbm](/man/wbmptopbm)(1), [pbmtonokia](/man/pbmtonokia)(1)"]],"e":["wbmptopbm","pbmtonokia"],"x":"pbmtowbmp [input.pbm] > [output.wbmp]"},"pbzip2":{"n":"pbzip2","t":"parallel implementation of bzip2 compression","s":[["TAGLINE","parallel implementation of bzip2 compression"],["TLDR","**Compress file**\n\n```pbzip2 [file]```\n\n**Decompress file**\n\n```pbzip2 -d [file.bz2]```\n\n**Keep original file**\n\n```pbzip2 -k [file]```\n\n**Set compression level**\n\n```pbzip2 -[9] [file]```\n\n**Use specific number of processors**\n\n```pbzip2 -p[4] [file]```\n\n**Compress from stdin**\n\n```cat [file] | pbzip2 > [file.bz2]```\n\n**Decompress to stdout**\n\n```pbzip2 -dc [file.bz2] > [file]```\n\n**Test compressed file**\n\n```pbzip2 -t [file.bz2]```"],["SYNOPSIS","**pbzip2** [_-d_] [_-k_] [_-p n_] [_-1..9_] [_options_] [_files_]"],["PARAMETERS","**-d**\n> Decompress.\n\n**-z**\n> Compress (default).\n\n**-k**\n> Keep original files.\n\n**-p** _N_\n> Number of processors to use.\n\n**-1** to **-9**\n> Compression level (9 = best).\n\n**-c**\n> Output to stdout.\n\n**-f**\n> Force overwrite.\n\n**-t**\n> Test integrity.\n\n**-q**\n> Quiet mode.\n\n**-v**\n> Verbose mode.\n\n**-m** _N_\n> Memory limit per thread (MB).\n\n**-r**\n> Read entire file into RAM."],["DESCRIPTION","**pbzip2** is a parallel implementation of bzip2 compression. It uses multiple CPU cores to compress and decompress files faster than single-threaded bzip2.\n\nThe tool produces files compatible with standard bzip2. Output can be decompressed by bzip2, bunzip2, or pbzip2 itself.\n\nParallelism scales with available processors. By default, all cores are used. The -p flag limits processor usage for background compression.\n\nCompression levels work like bzip2: higher numbers produce smaller files but take longer. The parallel implementation helps offset slower compression speeds.\n\nMemory usage scales with thread count. Each thread needs memory for its compression buffer. Limits can prevent memory exhaustion on large jobs.\n\nPipe mode supports streaming compression. Combined with tar, it enables parallel compressed archives."],["CAVEATS","Memory usage higher than bzip2. Best speedup on multi-core systems. Decompression speedup depends on how file was created."],["HISTORY","**pbzip2** was created by **Jeff Gilchrist** around **2005** to leverage multi-core processors for bzip2 compression. As multi-core CPUs became common, parallel compression tools like pbzip2 became essential for handling large data."],["INSTALL","```apt: sudo apt install pbzip2```\n\n```dnf: sudo dnf install pbzip2```\n\n```pacman: sudo pacman -S pbzip2```\n\n```zypper: sudo zypper install pbzip2```\n\n```brew: brew install pbzip2```\n\n```nix: nix profile install nixpkgs#pbzip2```"],["SEE ALSO","[bzip2](/man/bzip2)(1), [pigz](/man/pigz)(1), [lbzip2](/man/lbzip2)(1), [xz](/man/xz)(1)"]],"e":["bzip2","pigz","lbzip2","xz"],"x":"pbzip2 [file]"},"phar":{"n":"phar","t":"manages PHP Archive files, which bundle PHP applications into single","s":[["TAGLINE","manages PHP Archive files, which bundle PHP applications into single"],["TLDR","**Add** files to a PHAR archive\n\n```phar add -f [path/to/archive.phar] [files...]```\n\n**List** contents of archive\n\n```phar list -f [path/to/archive.phar]```\n\n**Delete** file from archive\n\n```phar delete -f [path/to/archive.phar] -e [file]```\n\n**Compress** archive contents\n\n```phar compress -f [path/to/archive.phar] -c [gzip|bzip2]```\n\n**Get info** about archive\n\n```phar info -f [path/to/archive.phar]```\n\n**Sign** with hash algorithm\n\n```phar sign -f [path/to/archive.phar] -h [sha256|sha512]```\n\nDisplay **help**\n\n```phar help```"],["SYNOPSIS","**phar** _command_ [**-f** _archive_] [_options_] [_files_...]"],["COMMANDS","**add**\n> Add files or directories to archive\n\n**list**\n> List contents of archive\n\n**delete**\n> Remove files from archive\n\n**compress**\n> Compress archive contents\n\n**info**\n> Display archive information\n\n**sign**\n> Sign archive with hash or key\n\n**extract**\n> Extract archive contents"],["PARAMETERS","**-f _archive_**\n> PHAR archive file to operate on\n\n**-c _algorithm_**\n> Compression algorithm (gzip, bzip2)\n\n**-h _algorithm_**\n> Hash algorithm for signing\n\n**-y _keyfile_**\n> Private key file for OpenSSL signing\n\n**-e _entry_**\n> Specific entry within archive"],["DESCRIPTION","**phar** manages PHP Archive (PHAR) files, which bundle PHP applications into single distributable files. PHAR files can include PHP code, assets, and metadata, and can be executed directly by PHP.\n\nThe tool supports creating, modifying, extracting, and signing PHAR archives. Signing ensures archive integrity and authenticity using hash algorithms or OpenSSL keys."],["CAVEATS","Requires phar.readonly=0 in php.ini for write operations. OpenSSL signing requires the OpenSSL extension. Large archives may consume significant memory during operations."],["HISTORY","**PHAR** (PHP Archive) was introduced in PHP 5.3 as a native way to distribute PHP applications. The phar command-line tool provides archive management capabilities outside of PHP scripts."],["INSTALL","```apk: sudo apk add php85-phar```"],["SEE ALSO","[php](/man/php)(1), [composer](/man/composer)(1)"]],"e":["php","composer"],"x":"phar add -f [path/to/archive.phar] [files...]"},"pnmtopclxl":{"n":"pnmtopclxl","t":"Convert PNM images to PCL-XL printer format","s":[["TAGLINE","Convert PNM images to PCL-XL printer format"],["TLDR","**Convert PNM to PCL-XL format**\n\n```pnmtopclxl [input.pnm] > [output.pclxl]```\n\n**Specify paper size** (A4)\n\n```pnmtopclxl -format=a4 [input.pnm] > [output.pclxl]```\n\n**High-resolution output, multiple copies**\n\n```pnmtopclxl -dpi=600 -copies=2 [input.pnm] > [output.pclxl]```\n\n**Duplex printing**\n\n```pnmtopclxl -duplex=vertical [input.pnm] > [output.pclxl]```"],["SYNOPSIS","**pnmtopclxl** [_options_] [_pnmfile_]"],["PARAMETERS","**-format=**_PAPER_\n> Paper size: **letter**, **legal**, **a3**-**a6**, **jb4**-**jb6**, **exec**, **ledger**, **b5envelope**, **c5envelope**, **com10envelope**, **monarchenvelope**, **dlenvelope**, **jpostcard**, **jdoublepostcard**.\n\n**-dpi=**_N_\n> Print resolution in dots per inch.\n\n**-xoffs=**_N_, **-yoffs=**_N_\n> Horizontal/vertical offset (in dots) of the image on the page.\n\n**-center**\n> Center the image on the page.\n\n**-duplex=**_vertical_|_horizontal_\n> Enable duplex printing with the given binding edge.\n\n**-feeder=**_N_\n> Numeric media source (input tray).\n\n**-copies=**_N_\n> Number of copies to print.\n\n**-rendergray**\n> Force grayscale rendering.\n\n**-colorok**\n> Allow color output even on printers that report no color support.\n\n**-jobsetup=**_FILE_\n> Prepend the contents of _FILE_ as a PCL-XL job-setup prologue.\n\n**-embedded**\n> Emit only the image stream, omitting the PCL-XL job wrapper (for embedding in larger jobs)."],["DESCRIPTION","**pnmtopclxl** converts PNM images into **PCL-XL** (also known as PCL 6) printer language, the native format of modern HP LaserJet printers. Part of the **Netpbm** toolkit. Options accept either a single or double hyphen and use **=** or whitespace to separate the value."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmtolj](/man/pbmtolj)(1), [pnmtops](/man/pnmtops)(1)"]],"e":["pbmtolj","pnmtops"],"x":"pnmtopclxl [input.pnm] > [output.pclxl]"},"prowler-kubernetes":{"n":"prowler-kubernetes","t":"Kubernetes cluster security assessment","s":[["TAGLINE","Kubernetes cluster security assessment"],["TLDR","**Run Kubernetes security assessment**\n\n```prowler kubernetes```\n\n**Run with specific context**\n\n```prowler kubernetes --context [my-cluster]```\n\n**Run specific checks**\n\n```prowler kubernetes --checks [pod_security_policy]```\n\n**Output to JSON**\n\n```prowler kubernetes -M json -o [results/]```"],["SYNOPSIS","**prowler kubernetes** [_options_]"],["PARAMETERS","**--checks** _checks_\n> Specific checks to run.\n\n**--context** _name_\n> Kubernetes context.\n\n**--namespace** _name_\n> Target namespace.\n\n**--compliance** _framework_\n> Compliance framework.\n\n**-M**, **--output-modes** _format_\n> Output format.\n\n**-o**, **--output-directory** _dir_\n> Output directory."],["DESCRIPTION","**prowler kubernetes** performs security assessment of Kubernetes clusters. It checks for misconfigurations, RBAC issues, pod security violations, and compliance against security best practices including the CIS Kubernetes Benchmark.\n\nThe tool connects to the cluster using the current kubeconfig context and evaluates resources across namespaces. Results can be filtered by specific checks, namespaces, or compliance frameworks."],["CAVEATS","Requires appropriate RBAC permissions to read cluster resources. Some checks need cluster-admin access. Results reflect the current state at scan time. The `--context` flag must match an existing kubeconfig context."],["INSTALL","```brew: brew install prowler```\n\n```nix: nix profile install nixpkgs#prowler```"],["SEE ALSO","[prowler](/man/prowler)(1), [kubectl](/man/kubectl)(1)"]],"e":["prowler","kubectl"],"x":"prowler kubernetes"},"purge-old-kernels":{"n":"purge-old-kernels","t":"Safely remove outdated kernel packages","s":[["TAGLINE","Safely remove outdated kernel packages"],["TLDR","**Remove old kernels, keeping the default 2 most recent**\n\n```sudo purge-old-kernels```\n\n**Keep a specific number of old kernels**\n\n```sudo purge-old-kernels --keep [3]```\n\n**Simulate removal without making changes**\n\n```sudo purge-old-kernels -s```\n\n**Remove old kernels without prompting for confirmation**\n\n```sudo purge-old-kernels -y```"],["SYNOPSIS","**purge-old-kernels** [_--keep N_] [_apt-get options..._]"],["PARAMETERS","**--keep** _N_\n> Number of old kernels to keep. Defaults to **2**.\n\n_apt-get options_\n> Any additional arguments are passed directly to **apt-get**(8). Common examples: **-y** (assume yes), **-s** (simulate/dry-run), **-q** (quiet)."],["DESCRIPTION","**purge-old-kernels** removes old Linux kernel and header packages from Ubuntu and Debian systems to reclaim disk space, particularly in the **/boot** partition. Over time, kernel upgrades accumulate previous versions that are no longer needed, and this utility automates their safe removal. It will never remove the currently running kernel.\n\nBy default the tool keeps the **2** most recent kernels, configurable with the **--keep** option. Any additional arguments are passed directly to **apt-get**(8), so **-s** can be used for dry-run simulation and **-y** to skip confirmation. The command is part of the **byobu** package on Ubuntu/Debian systems and requires root privileges.\n\n**Note:** This utility is now deprecated. Its functionality is expected to be integrated into **apt**(8)."],["CAVEATS","Ubuntu/Debian specific. Requires root. The currently running kernel is never removed. Now deprecated in favor of apt(8) integration."],["HISTORY","purge-old-kernels was created for **Ubuntu** kernel package cleanup."],["INSTALL","```dnf: sudo dnf install byobu```\n\n```apk: sudo apk add byobu```\n\n```zypper: sudo zypper install byobu```\n\n```brew: brew install byobu```\n\n```nix: nix profile install nixpkgs#byobu```"],["SEE ALSO","[apt](/man/apt)(8), [dpkg](/man/dpkg)(1), [uname](/man/uname)(1)"]],"e":["apt","dpkg","uname"],"x":"sudo purge-old-kernels"},"python-m-json.tool":{"n":"python-m-json.tool","t":"Validate and pretty-print JSON data","s":[["TAGLINE","Validate and pretty-print JSON data"],["TLDR","**Pretty print JSON**\n\n```python -m json.tool [file.json]```\n\n**Format from stdin**\n\n```echo '{\"key\":\"value\"}' | python -m json.tool```\n\n**Compact output**\n\n```python -m json.tool --compact [file.json]```\n\n**Sort keys**\n\n```python -m json.tool --sort-keys [file.json]```"],["SYNOPSIS","**python -m json.tool** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> JSON file to format.\n\n**--sort-keys**\n> Sort object keys.\n\n**--compact**\n> Compact output.\n\n**--indent** _N_\n> Indentation level.\n\n**--no-ensure-ascii**\n> Allow non-ASCII."],["DESCRIPTION","**python -m json.tool** is a built-in Python module that validates and pretty-prints JSON data. It reads JSON from a file argument or standard input, checks for syntax errors, and outputs a formatted version with proper indentation for readability.\n\nThe tool is useful for quickly inspecting API responses, configuration files, or any JSON output piped from other commands. Options control key sorting, indentation depth, and compact output mode. Since it ships with Python's standard library, it requires no additional installation."],["CAVEATS","Part of Python standard library. Basic functionality."],["HISTORY","json.tool is a **Python standard library** module for JSON formatting."],["SEE ALSO","[jq](/man/jq)(1), [python](/man/python)(1)"]],"e":["jq","python"],"x":"python -m json.tool [file.json]"},"rm":{"n":"rm","t":"Remove files and directories","s":[["TAGLINE","Remove files and directories"],["TLDR","**Remove a file**\n\n```rm [file]```\n\n**Remove multiple files**\n\n```rm [file1] [file2] [file3]```\n\n**Remove a file with confirmation prompt**\n\n```rm -i [file]```\n\n**Remove a directory and its contents recursively**\n\n```rm -r [directory]```\n\n**Force remove without confirmation**\n\n```rm -f [file]```\n\n**Force remove a directory recursively** (dangerous)\n\n```rm -rf [directory]```\n\n**Remove files verbosely** (show what's being deleted)\n\n```rm -v [file]```\n\n**Remove files matching a pattern**\n\n```rm *.tmp```"],["SYNOPSIS","**rm** [**-firvd**] _file_..."],["PARAMETERS","**-f**, **--force**\n> Ignore nonexistent files, never prompt\n\n**-i**\n> Prompt before every removal\n\n**-I**\n> Prompt once before removing more than 3 files or recursively\n\n**-r**, **-R**, **--recursive**\n> Remove directories and their contents recursively\n\n**-d**, **--dir**\n> Remove empty directories\n\n**-v**, **--verbose**\n> Explain what is being done\n\n**--no-preserve-root**\n> Do not treat '/' specially (dangerous)\n\n**--preserve-root**\n> Do not remove '/' (default)\n\n**--one-file-system**\n> When removing recursively, skip directories on different filesystems"],["DESCRIPTION","**rm** removes files and directories. By default, it does not remove directories; use **-r** to remove directories recursively.\n\nWithout **-f**, rm prompts for confirmation when removing write-protected files. With **-i**, it prompts for every file. The **-I** option provides a middle ground, prompting once for potentially dangerous operations.\n\nTo remove a file starting with a dash, use **--** to end option processing: **rm -- -filename** or use a path: **rm ./-filename**.\n\nThe command unlinks files from the filesystem. Data may be recoverable until overwritten. For secure deletion, use **shred**."],["CAVEATS","**rm -rf /** or similar commands can destroy entire systems. Modern systems protect against this, but variations may bypass protections.\n\nRemoved files bypass the trash. Use **trash-cli** or desktop trash if recovery might be needed.\n\nBe careful with wildcards. **rm \\*** in the wrong directory is destructive. Use **ls** to preview what would be deleted.\n\nRoot user should use **rm** with extreme caution. Consider aliases that add **-i** by default."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[rmdir](/man/rmdir)(1), [unlink](/man/unlink)(1), [shred](/man/shred)(1), [trash-cli](/man/trash-cli)(1)"]],"e":["rmdir","unlink","shred","trash-cli"],"x":"rm [file]"},"rmdir":{"n":"rmdir","t":"Remove empty directories","s":[["TAGLINE","Remove empty directories"],["TLDR","**Remove an empty directory**\n\n```rmdir [path/to/directory]```\n\n**Remove multiple empty directories**\n\n```rmdir [dir1] [dir2] [dir3]```\n\n**Remove directory and empty parents**\n\n```rmdir -p [path/to/directory]```\n\n**Remove with verbose output**\n\n```rmdir -v [path/to/directory]```\n\n**Ignore errors** for non-empty directories\n\n```rmdir --ignore-fail-on-non-empty [path/to/directory]```\n\n**Remove nested empty directories**\n\n```rmdir -p [parent/child/grandchild]```"],["SYNOPSIS","**rmdir** [_OPTION_]... _DIRECTORY_..."],["PARAMETERS","**-p**, **--parents**\n> Remove directory and its empty ancestor directories.\n\n**-v**, **--verbose**\n> Print a message for each removed directory.\n\n**--ignore-fail-on-non-empty**\n> Ignore failures caused solely by non-empty directories.\n\n**--help**\n> Display help message.\n\n**--version**\n> Display version information."],["DESCRIPTION","**rmdir** removes empty directories. Unlike **rm -r**, it will not remove directories that contain files or subdirectories, providing a safety check against accidental data loss.\n\nWith the **-p** option, rmdir removes the specified directory and then attempts to remove each parent directory in the path, stopping when a non-empty directory is encountered or removal fails.\n\nrmdir is useful in scripts where you want to clean up empty directories without risking removal of directories with content."],["CAVEATS","rmdir only removes empty directories. For directories with content, use **rm -r** (with caution). The directory must be empty of all files and subdirectories, including hidden files (those starting with .). The -p option removes parent directories only if they become empty after child removal."],["HISTORY","rmdir is part of GNU coreutils and has been a standard Unix command since the earliest versions of Unix. It follows the POSIX specification for directory removal. The separation between rmdir (empty directories only) and rm -r (recursive removal) is a deliberate safety feature in Unix design."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[rm](/man/rm)(1), [mkdir](/man/mkdir)(1), [find](/man/find)(1)"]],"e":["rm","mkdir","find"],"x":"rmdir [path/to/directory]"},"smbclient":{"n":"smbclient","t":"FTP-like client for SMB/CIFS shares","s":[["TAGLINE","FTP-like client for SMB/CIFS shares"],["TLDR","**Connect** to a share\n\n```smbclient //[server]/[share]```\n\nConnect with **username**\n\n```smbclient //[server]/[share] --user [username]```\n\nConnect with **workgroup** and username\n\n```smbclient //[server]/[share] --workgroup [domain] --user [username]```\n\nConnect with username and **password**\n\n```smbclient //[server]/[share] --user [username%password]```\n\n**Download** a file\n\n```smbclient //[server]/[share] --directory [path] --command \"get [file.txt]\"```\n\n**Upload** a file\n\n```smbclient //[server]/[share] --directory [path] --command \"put [file.txt]\"```\n\n**List shares** anonymously\n\n```smbclient --list=[server] --no-pass```"],["SYNOPSIS","**smbclient** _//server/share_ [_OPTIONS_]"],["PARAMETERS","**-U, --user** _username[%password]_\n> Specify username and optional password\n\n**-W, --workgroup** _domain_\n> Set SMB workgroup/domain\n\n**-N, --no-pass**\n> Suppress password prompt for anonymous access\n\n**-L, --list** _host_\n> List available shares on server\n\n**-D, --directory** _path_\n> Change to initial directory on connection\n\n**-c, --command** _commands_\n> Execute semicolon-separated commands\n\n**-I, --ip-address** _ip_\n> Specify server IP directly\n\n**-p, --port** _port_\n> TCP port (default: 139 or 445)\n\n**-m, --max-protocol** _level_\n> Maximum SMB protocol (NT1, SMB2, SMB3)\n\n**-A, --authentication-file** _file_\n> Read credentials from file\n\n**-T, --tar** _options_\n> Tar backup mode with flags\n\n**-d, --debuglevel** _level_\n> Debug verbosity (0-10)\n\n**-n, --netbiosname** _name_\n> Override client NetBIOS name"],["INTERACTIVE COMMANDS","**get** _remote_ [_local_]\n> Download file from share\n\n**put** _local_ [_remote_]\n> Upload file to share\n\n**ls** [_pattern_]\n> List directory contents\n\n**cd** _directory_\n> Change remote directory\n\n**lcd** _directory_\n> Change local directory\n\n**mkdir** _directory_\n> Create remote directory\n\n**rm** _file_\n> Delete remote file\n\n**exit**\n> Close connection"],["DESCRIPTION","**smbclient** is an FTP-like client for accessing SMB/CIFS network shares. It connects to Windows file servers and Samba shares, enabling file transfers, directory operations, and share browsing from the command line.\n\nThe tool supports interactive sessions with FTP-style commands or batch operations via **-c** flag. It can create tar backups of shares and browse available network resources."],["CAVEATS","Password on command line is visible in process listings; use **-A** for credentials file. Some features require specific SMB protocol versions. Firewall rules may block SMB ports (139, 445). NTLM authentication is being deprecated in favor of Kerberos."],["HISTORY","**smbclient** is part of **Samba**, created by **Andrew Tridgell** in **1992** after reverse-engineering the SMB protocol. Samba has evolved to support modern SMB2 and SMB3 protocols. The tool remains essential for cross-platform file sharing and accessing Windows shares from Linux."],["INSTALL","```apt: sudo apt install smbclient```\n\n```dnf: sudo dnf install samba-client```\n\n```pacman: sudo pacman -S smbclient```\n\n```apk: sudo apk add samba-client```\n\n```zypper: sudo zypper install samba-client```"],["SEE ALSO","[mount.cifs](/man/mount.cifs)(8), [smbcacls](/man/smbcacls)(1), [smbget](/man/smbget)(1), [nmblookup](/man/nmblookup)(1)"]],"e":["mount.cifs","smbcacls","smbget","nmblookup"],"x":"smbclient //[server]/[share]"},"steamos-dump-info":{"n":"steamos-dump-info","t":"Collect SteamOS system diagnostics","s":[["TAGLINE","Collect SteamOS system diagnostics"],["TLDR","**Dump all system information**\n\n```sudo steamos-dump-info```\n\n**List available sections**\n\n```steamos-dump-info --list-sections```\n\n**Dump with verbose output**\n\n```sudo steamos-dump-info --verbose```\n\n**Dump without compressing output**\n\n```sudo steamos-dump-info --no-compress```\n\n**Dump only a specific section**\n\n```sudo steamos-dump-info --section [network]```"],["SYNOPSIS","**steamos-dump-info** [_options_]"],["PARAMETERS","**-h**, **--help**\n> Display help message.\n\n**-L**, **--list-sections**\n> List all available sections that can be dumped individually.\n\n**--section** _NAME_\n> Dump only the specified section. Can be used multiple times.\n\n**-V**, **--version**\n> Display version information.\n\n**-v**, **--verbose**\n> Enable verbose output with more detail about the collection process.\n\n**-q**, **--quiet**\n> Suppress most output, showing only critical messages.\n\n**--no-compress**\n> Create an uncompressed directory instead of a compressed archive."],["DESCRIPTION","**steamos-dump-info** collects comprehensive system diagnostics from a SteamOS installation into a compressed archive. The output includes system logs (journalctl, /var/log), hardware information (lspci, lsusb, /proc entries), network configuration, Steam client logs, Xorg logs, and system configuration files.\n\nThe resulting archive is commonly requested by Valve support for troubleshooting Steam Deck and SteamOS issues, and can be uploaded to bug trackers or support tickets."],["CAVEATS","Requires root privileges for full system information. Specific to SteamOS (Steam Deck and other Valve devices). The output archive may contain sensitive system information."],["HISTORY","**steamos-dump-info** is part of the SteamOS system tools developed by **Valve** for the Steam Deck and other SteamOS-based devices."],["SEE ALSO","[uname](/man/uname)(1), [lshw](/man/lshw)(1), [dmidecode](/man/dmidecode)(8), [steamos-finalize-install](/man/steamos-finalize-install)(1)"]],"e":["uname","lshw","dmidecode","steamos-finalize-install"],"x":"sudo steamos-dump-info"},"systemctl-disable":{"n":"systemctl-disable","t":"Disable unit auto-start at boot","s":[["TAGLINE","Disable unit auto-start at boot"],["TLDR","**Disable** a service from running on boot\n\n```systemctl disable [unit]```\n\nDisable a service from running on boot **and stop** it immediately\n\n```systemctl disable --now [unit]```\n\nDisable a **user** service from running on login\n\n```systemctl --user disable [unit]```\n\nDisable **multiple** units at once\n\n```systemctl disable [unit1] [unit2]```\n\nDisable a unit **temporarily** until next reboot\n\n```systemctl disable --runtime [unit]```"],["SYNOPSIS","**systemctl** [_OPTIONS_...] **disable** _UNIT_..."],["PARAMETERS","**--now**\n> Also stop the unit after disabling\n\n**--user**\n> Disable user service manager units instead of system units\n\n**--no-reload**\n> Do not reload systemd configuration after disabling\n\n**--runtime**\n> Disable temporarily until next reboot only.\n\n**--force**\n> When disabling, remove symlinks even if the unit file does not exist.\n\n**--global**\n> Disable for all users (with --user-mode units)."],["DESCRIPTION","**systemctl disable** removes the symbolic links that cause units to start automatically at boot or login. This prevents the unit from starting in the future but does not stop a currently running instance unless **--now** is used.\n\nThe command reverses the effect of **systemctl enable** by removing symlinks from target.wants directories. The unit file itself is not modified or deleted."],["CAVEATS","Requires root privileges for system units. Disabling does not stop currently running units; use **--now** or **systemctl stop**. Some units are started by other means (socket activation, path triggers) and may need additional steps to fully disable."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-enable](/man/systemctl-enable)(1), [systemctl-stop](/man/systemctl-stop)(1), [systemctl-mask](/man/systemctl-mask)(1), [systemctl-is-enabled](/man/systemctl-is-enabled)(1)"]],"e":["systemctl","systemctl-enable","systemctl-stop","systemctl-mask","systemctl-is-enabled"],"x":"systemctl disable [unit]"},"systemctl-show":{"n":"systemctl-show","t":"Show unit properties in machine-readable format","s":[["TAGLINE","Show unit properties in machine-readable format"],["TLDR","Show **manager** properties\n\n```systemctl show```\n\nShow **user** manager properties\n\n```systemctl show --user```\n\nShow **unit** properties\n\n```systemctl show [unit]```\n\nShow **specific** properties\n\n```systemctl show [unit] -p [MainPID,ActiveState]```\n\nShow only the **value** of a specific property\n\n```systemctl show [unit] -p [MainPID] --value```\n\nInclude **empty** properties\n\n```systemctl show -a [unit]```"],["SYNOPSIS","**systemctl show** [_OPTIONS_] [_PATTERN_...|_JOB_...]"],["PARAMETERS","**--user**\n> Show user service manager properties\n\n**-a, --all**\n> Include empty properties\n\n**-p, --property=** _NAME_\n> Show only specified properties\n\n**--value**\n> Print only the value, skip property name and \"=\" (useful with -p)\n\n**--no-pager**\n> Disable pager"],["DESCRIPTION","**systemctl show** displays properties of units, jobs, or the service manager itself in a machine-readable format. Without arguments, it shows properties of the service manager.\n\nThe output is suitable for parsing by scripts. Use `systemctl status` for human-readable output."],["COMMON PROPERTIES","**ActiveState** - Current state (active, inactive, failed, activating, deactivating)\n\n**LoadState** - Load state (loaded, not-found, error, masked)\n\n**SubState** - More fine-grained state (running, dead, exited, etc.)\n\n**MainPID** - Main process ID\n\n**ExecMainStartTimestamp** - Start time\n\n**Wants, Requires, Conflicts** - Unit dependency properties\n\n**FragmentPath** - Path to the unit file"],["CAVEATS","Output format is key=value pairs. Some properties may be empty. For human-readable format, use `systemctl status` instead."],["HISTORY","The **show** subcommand provides machine-parseable property access, enabling scripted queries and monitoring integration."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl-status](/man/systemctl-status)(1), [systemctl-set-property](/man/systemctl-set-property)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-status","systemctl-set-property","systemctl"],"x":"systemctl show"},"tcpick":{"n":"tcpick","t":"TCP stream sniffer and analyzer","s":[["TAGLINE","TCP stream sniffer and analyzer"],["TLDR","Capture traffic on **interface**, host, and port\n\n```sudo tcpick -i [interface] -C -h [host] -p [port]```\n\nCapture **HTTP** traffic\n\n```sudo tcpick -i eth0 -C -h [192.168.1.100] -p 80```\n\nDisplay **help**\n\n```tcpick --help```"],["SYNOPSIS","**tcpick** [_OPTIONS_]"],["PARAMETERS","**-i, --interface** _INTERFACE_\n> Capture on specified network interface\n\n**-C, --colors**\n> Display output with colors\n\n**-h** _HOST_\n> Filter by host\n\n**-p** _PORT_\n> Filter by port\n\n**-r** _FILE_\n> Read from pcap file\n\n**-w** _FILE_\n> Write to pcap file\n\n**-a**\n> Display data as ASCII\n\n**-x**\n> Display data as hexadecimal"],["DESCRIPTION","**tcpick** is a packet sniffer and TCP stream analyzer that captures and displays TCP connections and their data. It can monitor network traffic on specific interfaces, hosts, or ports with colorized output.\n\nThe tool tracks TCP connection states and can reassemble TCP streams to show complete conversations."],["CAVEATS","Requires root privileges for packet capture. Uses libpcap for capture functionality. Connection tracking may consume memory on busy networks."],["HISTORY","**tcpick** provides network analysts with a focused tool for TCP traffic inspection, combining capture and analysis in a single utility."],["INSTALL","```apt: sudo apt install tcpick```\n\n```dnf: sudo dnf install tcpick```"],["SEE ALSO","[tcpdump](/man/tcpdump)(8), [tcpflow](/man/tcpflow)(1), [wireshark](/man/wireshark)(1)"]],"e":["tcpdump","tcpflow","wireshark"],"x":"sudo tcpick -i [interface] -C -h [host] -p [port]"},"termshark":{"n":"termshark","t":"Terminal-based Wireshark interface","s":[["TAGLINE","Terminal-based Wireshark interface"],["TLDR","Monitor **default** interface\n\n```sudo termshark```\n\nMonitor **specific** interface\n\n```sudo termshark -i [eth0]```\n\nRead a **pcap** file\n\n```termshark -r [path/to/capture.pcap]```\n\nApply a **display filter**\n\n```sudo termshark -i [eth0] -Y \"[http.request]\"```\n\nApply a **capture filter**\n\n```sudo termshark -i [eth0] -f \"[port 80]\"```"],["SYNOPSIS","**termshark** [_OPTIONS_] [_INTERFACE_|_FILE_]"],["PARAMETERS","**-i** _INTERFACE_\n> Capture on specified interface\n\n**-r** _FILE_\n> Read from pcap file\n\n**-Y** _FILTER_\n> Apply display filter\n\n**-f** _FILTER_\n> Apply capture filter\n\n**-d** _layer==selector,protocol_\n> Specify dissection of layer type\n\n**--pass-thru** _[auto|yes|no]_\n> Run tshark instead of UI (auto means if stdout is not a tty)\n\n**--debug**\n> Enable debug mode with profiling server on port 6060"],["DESCRIPTION","**termshark** is a terminal-based user interface for tshark that provides a Wireshark-like experience in the terminal. It offers packet list, packet details, and packet bytes views with interactive navigation.\n\nThe interface supports keyboard navigation, display filtering, and stream reassembly similar to the graphical Wireshark."],["KEYBOARD SHORTCUTS","**Tab** - Switch between views\n\n**/** - Filter packets\n\n**Enter** - Expand/collapse details\n\n**q** - Quit"],["CAVEATS","Requires tshark (Wireshark CLI) to be installed. Root privileges needed for live capture. Large capture files may be slow to load."],["HISTORY","**termshark** was created by **Graham Clark** to bring Wireshark's interface paradigm to the terminal, enabling packet analysis over SSH or in environments without a GUI."],["INSTALL","```apt: sudo apt install termshark```\n\n```pacman: sudo pacman -S termshark```\n\n```apk: sudo apk add termshark```\n\n```zypper: sudo zypper install termshark```\n\n```brew: brew install termshark```\n\n```nix: nix profile install nixpkgs#termshark```"],["SEE ALSO","[tshark](/man/tshark)(1), [wireshark](/man/wireshark)(1), [tcpdump](/man/tcpdump)(8)"]],"e":["tshark","wireshark","tcpdump"],"x":"sudo termshark"},"tlmgr-conf":{"n":"tlmgr-conf","t":"Display and modify TeX Live configuration","s":[["TAGLINE","Display and modify TeX Live configuration"],["TLDR","**Show general TeX Live configuration**\n\n```tlmgr conf```\n\n**Show all texmf.cnf settings**\n\n```tlmgr conf texmf```\n\n**Show the value of a single tlmgr setting**\n\n```tlmgr conf tlmgr [setting]```\n\n**Set a tlmgr configuration value**\n\n```tlmgr conf tlmgr [setting] [value]```\n\n**Delete a setting from the tlmgr config file**\n\n```tlmgr conf tlmgr --delete [setting]```\n\n**List auxiliary texmf trees**\n\n```tlmgr conf auxtrees show```\n\n**Add an auxiliary texmf tree**\n\n```tlmgr conf auxtrees add [/path/to/tree]```\n\n**Remove an auxiliary texmf tree**\n\n```tlmgr conf auxtrees remove [/path/to/tree]```"],["SYNOPSIS","**tlmgr conf** [texmf|tlmgr|updmap [**--conffile** _file_] [**--delete**] [_key_ [_value_]]]\n\n**tlmgr conf** auxtrees [**--conffile** _file_] [show|add|remove] [_value_]"],["PARAMETERS","**texmf**\n> Show or set values in texmf.cnf.\n\n**tlmgr**\n> Show or set values in the tlmgr configuration file.\n\n**updmap**\n> Show or set values in the first found updmap.cfg.\n\n**auxtrees**\n> Manage additional (auxiliary) texmf trees. Subcommands: show, add, remove.\n\n**--conffile** _file_\n> Use the specified configuration file instead of the default.\n\n**--delete**\n> Remove the key entirely from the configuration file (rather than commenting it out).\n\n_key_ _value_\n> Set the configuration key to value in the chosen config file."],["DESCRIPTION","**tlmgr conf** displays and modifies TeX Live configuration settings. With no arguments it shows general configuration information (active files, paths, and settings). With a system argument (texmf, tlmgr, or updmap) it shows all key/value pairs saved in the corresponding config file. Supplying a key shows only that key's value; also supplying a value sets it. The **--delete** flag removes a key entirely rather than overriding it.\n\nThe **auxtrees** subcommand manages additional texmf trees that are searched alongside the standard TeX Live tree.\n\nPart of the TeX Live Manager."],["SEE ALSO","[tlmgr-option](/man/tlmgr-option)(1), [tlmgr](/man/tlmgr)(1)"]],"e":["tlmgr-option","tlmgr"],"x":"tlmgr conf"},"tlmgr-recreate-tlpdb":{"n":"tlmgr-recreate-tlpdb","t":"Reconstruct TeX Live package database","s":[["TAGLINE","Reconstruct TeX Live package database"],["TLDR","**Recreate package database**\n\n```tlmgr recreate-tlpdb```"],["SYNOPSIS","**tlmgr recreate-tlpdb** [_options_]"],["DESCRIPTION","**tlmgr recreate-tlpdb** reconstructs the TeX Live package database from installed files. Recovery tool for corrupted installations. Part of TeX Live Manager maintenance."],["CAVEATS","Only use if database is corrupted. May not fully restore all package metadata."],["SEE ALSO","[tlmgr-check](/man/tlmgr-check)(1), [tlmgr](/man/tlmgr)(1)"]],"e":["tlmgr-check","tlmgr"],"x":"tlmgr recreate-tlpdb"},"transset":{"n":"transset","t":"Set X11 window transparency","s":[["TAGLINE","Set X11 window transparency"],["TLDR","**Set window transparency by clicking**\n\n```transset [0.8]```\n\n**Set transparency for specific window ID**\n\n```transset -i [0x1400003] [0.75]```\n\n**Toggle transparency on clicked window**\n\n```transset -t```\n\n**Increase transparency of clicked window**\n\n```transset --inc [0.1]```\n\n**Decrease transparency of clicked window**\n\n```transset --dec [0.1]```\n\n**Apply to all windows**\n\n```transset -a [0.9]```"],["SYNOPSIS","**transset** [_options_] [_opacity_]"],["PARAMETERS","**-i**, **--id** _id_\n> Select window by X window ID.\n\n**-n**, **--name** _name_\n> Select window by name.\n\n**-t**, **--toggle**\n> Toggle between current opacity and fully opaque.\n\n**--inc** _value_\n> Increase opacity by specified amount.\n\n**--dec** _value_\n> Decrease opacity by specified amount.\n\n**-a**, **--all**\n> Apply to all windows.\n\n**-p**, **--point**\n> Select window under pointer (default).\n\n**-v**, **--verbose**\n> Print verbose output.\n\n**--version**\n> Display version."],["DESCRIPTION","**transset** sets window transparency in X11 by modifying the _NET_WM_WINDOW_OPACITY property on windows. The opacity value ranges from 0 (fully transparent) to 1 (fully opaque), with 0.75 as the default.\n\nThe tool requires a compositing window manager (such as picom, xcompmgr, KWin, or Mutter) to actually render the transparency. Without a compositor, the property is set but no visual effect occurs.\n\nBy default, transset enters interactive mode where clicking on a window sets its transparency. Window ID or name options allow non-interactive scripted use."],["CAVEATS","Requires a running compositing manager for visible effects. X11 only; does not work on Wayland. Some applications may not respect transparency settings. Window IDs change between sessions."],["HISTORY","**transset** was developed as part of the X.Org project to demonstrate and utilize the COMPOSITE extension. The transset-df variant by Daniel Forchheimer added command-line options for scripted use without interactive clicking."],["INSTALL","```apt: sudo apt install x11-apps```\n\n```zypper: sudo zypper install transset```\n\n```nix: nix profile install nixpkgs#transset```"],["SEE ALSO","[xwininfo](/man/xwininfo)(1), [picom](/man/picom)(1), [xcompmgr](/man/xcompmgr)(1), [xprop](/man/xprop)(1)"]],"e":["xwininfo","picom","xcompmgr","xprop"],"x":"transset [0.8]"},"turbo":{"n":"turbo","t":"High-performance monorepo build system","s":[["TAGLINE","High-performance monorepo build system"],["TLDR","**Run task**\n\n```turbo run [build]```\n\n**Run multiple tasks**\n\n```turbo run [build] [test]```\n\n**Run with filter**\n\n```turbo run [build] --filter=[package-name]```\n\n**Run in parallel**\n\n```turbo run [build] --parallel```\n\n**Continue on error**\n\n```turbo run [build] --continue```\n\n**Show dependency graph**\n\n```turbo run [build] --graph```\n\n**Prune for deployment**\n\n```turbo prune [app]```\n\n**Prune with Docker-optimized output**\n\n```turbo prune [app] --docker```\n\n**Bypass cache and re-execute**\n\n```turbo run [build] --force```"],["SYNOPSIS","**turbo** _command_ [_--filter pattern_] [_options_]"],["PARAMETERS","**run** _TASKS_\n> Run specified tasks across packages.\n\n**prune** _package_\n> Create pruned subset of monorepo for a target package.\n\n**-F**, **--filter** _PATTERN_\n> Filter packages by name, directory, or git changes.\n\n**--affected**\n> Run only in packages changed since the base branch.\n\n**--continue**\n> Continue on error (never, dependencies-successful, always).\n\n**--graph** _file_\n> Output dependency graph (svg, html, mermaid, dot).\n\n**--force**\n> Bypass cache and re-execute all tasks.\n\n**--dry**\n> Display tasks without executing.\n\n**--cache-dir** _DIR_\n> Set filesystem cache directory.\n\n**--concurrency** _num_\n> Limit parallel task execution (number or percentage)."],["DESCRIPTION","**Turborepo** is a high-performance build system for JavaScript and TypeScript monorepos. It executes tasks like build, test, and lint across multiple packages while respecting inter-package dependencies and maintaining topological order automatically.\n\nThe tool uses content-aware hashing to cache task outputs, skipping work when inputs haven't changed. This caching works locally and can be shared across a team via remote caching, so developers don't rebuild what others have already built. The **--filter** flag targets specific packages for focused development.\n\nConfiguration is defined in **turbo.json** at the repository root, specifying task dependencies and caching behavior. The **--graph** flag visualizes the dependency structure, and **prune** creates minimal subsets of the monorepo for efficient deployment."],["CAVEATS","Requires turbo.json config. Monorepo setup needed. The **--parallel** and **--scope** flags are deprecated; use **--filter** and task configuration instead."],["HISTORY","**Turborepo** was created by **Jared Palmer** and acquired by **Vercel** in 2021. It focuses on making monorepo development fast."],["INSTALL","```pacman: sudo pacman -S turbo```\n\n```zypper: sudo zypper install turbo```\n\n```nix: nix profile install nixpkgs#turbo```"],["SEE ALSO","[npm](/man/npm)(1), [yarn](/man/yarn)(1), [pnpm](/man/pnpm)(1), [nx](/man/nx)(1)"]],"e":["npm","yarn","pnpm","nx"],"x":"turbo run [build]"},"tuxi":{"n":"tuxi","t":"Quick Google answers from the terminal","s":[["TAGLINE","Quick Google answers from the terminal"],["TLDR","**Search Google for an answer**\n\n```tuxi [search_terms]```\n\n**Display raw output without formatting**\n\n```tuxi -r [search_terms]```\n\n**Display all valid answers**\n\n```tuxi -a [search_terms]```\n\n**Print top URLs for a query**\n\n```tuxi -u [search_terms]```\n\n**Suppress suggestions and show results only**\n\n```tuxi -q [search_terms]```\n\n**Search in a specific language**\n\n```tuxi -l LANG_[language_code] [search_terms]```\n\n**Display version**\n\n```tuxi -v```"],["SYNOPSIS","**tuxi** [_OPTIONS_] [_SEARCH_TERMS_...]"],["PARAMETERS","**-r**\n> Raw format output (no colors or formatting).\n\n**-q**\n> Quiet mode (suppress \"Did you mean?\" suggestions and greeting).\n\n**-a**\n> Display all valid answers.\n\n**-u**\n> Print top URLs for the query.\n\n**-b**\n> Select best answer based on query keywords (experimental).\n\n**-l** _LANG_code_\n> Override search language (e.g., LANG_en_US, LANG_fr_FR).\n\n**-d**\n> Print debug information.\n\n**-s**\n> Save HTML results to cache.\n\n**-c**\n> Use most recent cached result instead of fetching.\n\n**-p**\n> Disable pipe support.\n\n**-v**\n> Show version information.\n\n**-h**\n> Display help information."],["DESCRIPTION","**tuxi** is a command-line tool that scrapes Google search results to provide instant, concise answers to queries. It extracts featured snippets and knowledge panel information to display answers directly in the terminal.\n\nThe tool is useful for quick fact-checking and getting answers without opening a web browser. It requires **pup** (HTML parser), **recode** (character set conversion), and **jq** (JSON processor) as dependencies."],["ENVIRONMENT","**TUXI_LANG**=_lang_\n> Set default search language.\n\n**TUXI_DELAY**=_int_\n> Adjust delay between answer detection (default: 250ms)."],["CAVEATS","Depends on Google's HTML structure which may change without notice. May be blocked by Google rate limiting or CAPTCHA. Requires internet connection. Results are scraped and may not always be accurate. Requires **pup**, **recode**, and **jq** to be installed."],["HISTORY","**tuxi** was created by **Bugswriter** as a fast, lightweight way to get answers from Google directly in the terminal without browser overhead."],["SEE ALSO","[ddgr](/man/ddgr)(1), [googler](/man/googler)(1), [curl](/man/curl)(1), [jq](/man/jq)(1)"]],"e":["ddgr","googler","curl","jq"],"x":"tuxi [search_terms]"},"unar":{"n":"unar","t":"Universal multi-format archive extractor","s":[["TAGLINE","Universal multi-format archive extractor"],["TLDR","**Extract archive**\n\n```unar [archive.zip]```\n\n**Extract to directory**\n\n```unar -o [output_dir] [archive.rar]```\n\n**Extract with password**\n\n```unar -p [password] [encrypted.zip]```\n\n**Force overwrite**\n\n```unar -f [archive.zip]```\n\n**Extract without directory**\n\n```unar -D [archive.tar.gz]```\n\n**List contents only**\n\n```lsar [archive.7z]```\n\n**Extract specific encoding**\n\n```unar -e [shift_jis] [japanese.zip]```"],["SYNOPSIS","**unar** [_-o dir_] [_-p password_] [_-e encoding_] [_options_] _archive_"],["PARAMETERS","**-o** _DIR_\n> Output directory.\n\n**-p** _PASSWORD_\n> Archive password.\n\n**-e** _ENCODING_\n> Filename encoding.\n\n**-f**, **--force**\n> Overwrite without asking.\n\n**-D**, **--no-directory**\n> Don't create containing directory.\n\n**-d**, **--directory**\n> Always create directory.\n\n**-k** _MODE_\n> How to handle existing files.\n\n**-q**, **--quiet**\n> Quiet mode.\n\n**-t**, **--test**\n> Test archive.\n\n**-nr**, **--no-recursion**\n> Don't extract nested archives.\n\n**-h**, **--help**\n> Show help."],["DESCRIPTION","**unar** extracts archives of many formats. It handles Zip, RAR, 7-Zip, tar, gzip, bzip2, ISO, and many legacy formats.\n\nFormat detection is automatic. The tool examines archive contents rather than relying solely on extensions.\n\nEncoding handling processes international filenames correctly. Japanese, Chinese, Korean, and other encodings are supported and auto-detected.\n\nPassword handling works for encrypted archives. Both header-encrypted and content-encrypted formats are supported.\n\nThe tool handles broken or non-standard archives that other extractors reject. It attempts to recover what it can.\n\nBy default, archives extract to a directory named after the archive. This can be controlled with -d or -D options."],["CAVEATS","Not installed by default. Some formats have limited metadata support. Very old formats may have quirks."],["HISTORY","**unar** is part of **The Unarchiver**, created by **Dag Ågren** starting around **2006**. Originally a macOS GUI application, command-line tools were added later for scripting."],["INSTALL","```apt: sudo apt install unar```\n\n```dnf: sudo dnf install unar```\n\n```zypper: sudo zypper install unar```\n\n```brew: brew install unar```\n\n```nix: nix profile install nixpkgs#unar```"],["SEE ALSO","[lsar](/man/lsar)(1), [unzip](/man/unzip)(1), [unrar](/man/unrar)(1), [7z](/man/7z)(1), [tar](/man/tar)(1)"]],"e":["lsar","unzip","unrar","7z","tar"],"x":"unar [archive.zip]"},"unclutter":{"n":"unclutter","t":"Auto-hide idle mouse cursor on X11","s":[["TAGLINE","Auto-hide idle mouse cursor on X11"],["TLDR","**Hide cursor after idle**\n\n```unclutter```\n\n**Custom timeout**\n\n```unclutter -idle [3]```\n\n**Ignore window**\n\n```unclutter -not [window-name]```\n\n**Run in background**\n\n```unclutter -b```\n\n**Don't touch root window**\n\n```unclutter -noevents```"],["SYNOPSIS","**unclutter** [_-idle seconds_] [_-b_] [_options_]"],["PARAMETERS","**-idle** _SEC_\n> Seconds before hide.\n\n**-b**\n> Background daemon.\n\n**-not** _NAME_\n> Ignore window.\n\n**-noevents**\n> No root events.\n\n**-display** _DPY_\n> X display."],["DESCRIPTION","**unclutter** automatically hides the mouse cursor on X11 desktops after a configurable period of inactivity. When the user moves the mouse, the cursor immediately reappears, making the hiding completely transparent during normal use.\n\nThe tool is particularly useful for presentations, kiosk displays, and keyboard-focused workflows where a stationary mouse pointer is a visual distraction. It can run as a background daemon and supports excluding specific windows from cursor hiding, allowing the cursor to remain visible in applications that need it.\n\nTwo main implementations exist: the original X11 version and **unclutter-xfixes**, which uses the XFixes extension for a more modern approach. Both serve the same purpose but differ in implementation details and compatibility with compositing window managers."],["CAVEATS","X11 only. Multiple versions exist. May conflict with apps."],["HISTORY","**unclutter** was created to hide the mouse cursor when it's not being used, cleaning up the X11 display."],["INSTALL","```pacman: sudo pacman -S unclutter```\n\n```zypper: sudo zypper install unclutter```\n\n```nix: nix profile install nixpkgs#unclutter```"],["SEE ALSO","[xdotool](/man/xdotool)(1), [xsetroot](/man/xsetroot)(1)"]],"e":["xdotool","xsetroot"],"x":"unclutter"},"urpmi.addmedia":{"n":"urpmi.addmedia","t":"Add package repositories in Mageia Linux","s":[["TAGLINE","Add package repositories in Mageia Linux"],["TLDR","**Add** a medium\n\n```sudo urpmi.addmedia [medium] [ftp://ftp.site.com/path/to/Mageia/RPMS]```\n\nAdd from **hard drive**\n\n```sudo urpmi.addmedia --distrib HD file://[path/to/repo]```\n\nAdd from **mirror**\n\n```sudo urpmi.addmedia --distrib ftp://[mirror_website]/mirror/mageia/distrib/[version]/[arch]```\n\nAdd from **mirror list**\n\n```sudo urpmi.addmedia --distrib --mirrorlist [mirrorlist]```"],["SYNOPSIS","**urpmi.addmedia** [_OPTIONS_] _NAME_ _URL_"],["PARAMETERS","**--distrib**\n> Add all standard media from a distribution\n\n**--mirrorlist** _URL_\n> Use mirror list for automatic selection\n\n**HD**\n> Specify local hard drive media"],["DESCRIPTION","**urpmi.addmedia** adds package repositories (media) in Mageia Linux. Media can be added from FTP servers, HTTP servers, local directories, or mirror lists.\n\nIn Mageia documentation, \"medium\" and \"repository\" are used synonymously."],["CAVEATS","Mageia specific. Local repositories require running genhdlist2 first. Requires root privileges."],["SEE ALSO","[urpmi](/man/urpmi)(8), [urpmi.removemedia](/man/urpmi.removemedia)(8), [urpmi.update](/man/urpmi.update)(8)"]],"e":["urpmi","urpmi.removemedia","urpmi.update"],"x":"sudo urpmi.addmedia [medium] [ftp://ftp.site.com/path/to/Mageia/RPMS]"},"vacuumdb":{"n":"vacuumdb","t":"Garbage-collect and analyze a PostgreSQL database","s":[["TAGLINE","Garbage-collect and analyze a PostgreSQL database"],["TLDR","**Vacuum a database**\n\n```vacuumdb [database]```\n\n**Vacuum all databases**\n\n```vacuumdb --all```\n\n**Vacuum and update optimizer statistics**\n\n```vacuumdb --analyze [database]```\n\n**Full vacuum** (rewrites tables and reclaims disk space)\n\n```vacuumdb --full [database]```\n\n**Vacuum a specific table**\n\n```vacuumdb -t [table_name] [database]```\n\n**Run vacuum in parallel** with multiple jobs\n\n```vacuumdb -j [4] [database]```\n\n**Freeze tuples aggressively** to prevent transaction ID wraparound\n\n```vacuumdb --freeze [database]```\n\n**Only update statistics** without vacuuming\n\n```vacuumdb --analyze-only [database]```\n\n**Vacuum on a remote server** with authentication\n\n```vacuumdb -h [hostname] -p [5432] -U [username] [database]```"],["SYNOPSIS","**vacuumdb** [_-a_] [_-z_] [_-f_] [_-F_] [_-t table_] [_-j jobs_] [_options_] [_dbname_]"],["PARAMETERS","**-a**, **--all**\n> Vacuum all databases.\n\n**-d** _DBNAME_, **--dbname** _DBNAME_\n> Database to clean or analyze.\n\n**-z**, **--analyze**\n> Also calculate statistics for use by the optimizer.\n\n**-Z**, **--analyze-only**\n> Only calculate statistics, do not vacuum.\n\n**--analyze-in-stages**\n> Analyze in three stages with increasing statistics targets. Useful for databases with no or wholly incorrect statistics.\n\n**-f**, **--full**\n> Perform full vacuuming (rewrites tables, reclaims disk space).\n\n**-F**, **--freeze**\n> Aggressively freeze tuples to prevent transaction ID wraparound.\n\n**-t** _TABLE_, **--table** _TABLE_\n> Vacuum specific table only. Can be specified multiple times.\n\n**-n** _SCHEMA_, **--schema** _SCHEMA_\n> Clean or analyze all tables in the specified schema only. Can be specified multiple times.\n\n**-N** _SCHEMA_, **--exclude-schema** _SCHEMA_\n> Exclude tables in the specified schema.\n\n**-j** _N_, **--jobs** _N_\n> Run vacuum or analyze commands in parallel by running N jobs simultaneously.\n\n**-P** _N_, **--parallel** _N_\n> Specify the number of parallel workers for each vacuum operation.\n\n**--skip-locked**\n> Skip relations that cannot be immediately locked for processing.\n\n**--no-index-cleanup**\n> Do not remove index entries pointing to dead tuples.\n\n**--no-truncate**\n> Do not truncate empty pages at the end of the table.\n\n**--disable-page-skipping**\n> Disable skipping pages based on the visibility map.\n\n**--min-xid-age** _AGE_\n> Only process tables with a transaction ID age of at least AGE.\n\n**--min-mxid-age** _AGE_\n> Only process tables with a multixact ID age of at least AGE.\n\n**-e**, **--echo**\n> Echo the commands sent to the server.\n\n**-q**, **--quiet**\n> Do not display progress messages.\n\n**-v**, **--verbose**\n> Print detailed information during processing.\n\n**-h** _HOST_, **--host** _HOST_\n> Database server host.\n\n**-p** _PORT_, **--port** _PORT_\n> Server port.\n\n**-U** _USER_, **--username** _USER_\n> Username to connect as.\n\n**-w**, **--no-password**\n> Never prompt for password.\n\n**-W**, **--password**\n> Force password prompt.\n\n**--maintenance-db** _DBNAME_\n> Database to connect to when using --all."],["DESCRIPTION","**vacuumdb** cleans and analyzes PostgreSQL databases. It is a wrapper around the SQL VACUUM and ANALYZE commands, providing convenient options for scripting and cron jobs.\n\nRegular vacuum marks dead tuples for reuse. It doesn't reclaim disk space but prevents table bloat from growing indefinitely.\n\nFull vacuum rewrites tables, reclaiming disk space. It requires an exclusive lock on the table and more time than regular vacuum.\n\nAnalyze updates statistics used by the query planner. Accurate statistics lead to better query plans and performance.\n\nParallel vacuuming with **-j** processes multiple tables simultaneously. The **-P** option controls the number of parallel workers within each vacuum operation."],["CAVEATS","Full vacuum locks tables exclusively and requires extra disk space for the rewrite. Regular vacuum should run frequently. The **--freeze** option is important for preventing transaction ID wraparound on busy databases."],["HISTORY","**vacuumdb** has been part of **PostgreSQL** since early versions. It provides command-line access to vacuum operations essential for database maintenance."],["INSTALL","```apk: sudo apk add postgresql17-client```"],["SEE ALSO","[psql](/man/psql)(1), [pg_dump](/man/pg_dump)(1), [reindexdb](/man/reindexdb)(1), [clusterdb](/man/clusterdb)(1), [createdb](/man/createdb)(1)"]],"e":["psql","pg_dump","reindexdb","clusterdb","createdb"],"x":"vacuumdb [database]"},"vagrant-ssh-config":{"n":"vagrant-ssh-config","t":"Output SSH configuration for Vagrant VMs","s":[["TAGLINE","Output SSH configuration for Vagrant VMs"],["TLDR","**Show SSH config**\n\n```vagrant ssh-config```\n\n**Output for specific host**\n\n```vagrant ssh-config --host [name]```\n\n**Append to SSH config**\n\n```vagrant ssh-config >> ~/.ssh/config```"],["SYNOPSIS","**vagrant** **ssh-config** [_options_] [_name_]"],["PARAMETERS","**--host** _name_\n> Host name for config."],["DESCRIPTION","**vagrant ssh-config** outputs SSH configuration for connecting to the VM. Displays host, user, port, and identity file settings. Can be appended to ~/.ssh/config for direct ssh access."],["SEE ALSO","[vagrant](/man/vagrant)(1), [vagrant-ssh](/man/vagrant-ssh)(1), [ssh](/man/ssh)(1)"]],"e":["vagrant","vagrant-ssh","ssh"],"x":"vagrant ssh-config"},"virsh-pool-delete":{"n":"virsh-pool-delete","t":"Delete storage pool data","s":[["TAGLINE","Delete storage pool data"],["TLDR","**Delete pool storage**\n\n```virsh pool-delete [pool]```"],["SYNOPSIS","**virsh** **pool-delete** _pool_"],["DESCRIPTION","**virsh pool-delete** destroys the underlying storage for a pool. Removes all data from the storage pool. Pool must be stopped first. Configuration remains until pool-undefine is run."],["CAVEATS","This permanently destroys all data in the pool. Cannot be undone."],["SEE ALSO","[virsh](/man/virsh)(1), [virsh-pool-undefine](/man/virsh-pool-undefine)(1)"]],"e":["virsh","virsh-pool-undefine"],"x":"virsh pool-delete [pool]"},"vnstat":{"n":"vnstat","t":"Console-based network traffic monitor","s":[["TAGLINE","Console-based network traffic monitor"],["TLDR","**Display** traffic summary for all interfaces\n\n```vnstat```\n\n**Display** traffic summary for a specific interface\n\n```vnstat -i [network_interface]```\n\n**Display** live stats for a specific interface\n\n```vnstat -l -i [network_interface]```\n\n**Show** hourly traffic statistics with bar graph\n\n```vnstat -hg```\n\n**Measure** and show average traffic for 30 seconds\n\n```vnstat -tr 30```"],["SYNOPSIS","**vnstat** [_options_] [_interface_]"],["PARAMETERS","**-i, --iface _interface_**\n> Select specific interface\n\n**-l, --live**\n> Display live traffic statistics\n\n**-hg, --hoursgraph**\n> Show hourly statistics as bar graph\n\n**-tr, --traffic _seconds_**\n> Measure traffic for specified duration\n\n**-d, --days**\n> Show daily statistics\n\n**-m, --months**\n> Show monthly statistics\n\n**-t, --top**\n> Show top traffic days\n\n**-s, --short**\n> Use short output format\n\n**--json**\n> Output in JSON format\n\n**--xml**\n> Output in XML format"],["DESCRIPTION","**vnstat** is a console-based network traffic monitor that keeps a log of network traffic for selected interfaces. It uses kernel interface statistics for minimal resource usage.\n\nThe daemon (vnstatd) runs in the background collecting data, while the vnstat command queries and displays the collected statistics. Data persists across reboots in a database."],["CAVEATS","Requires vnstatd daemon to be running for data collection. Initial statistics require time to accumulate. Traffic counters may reset if the interface is reconfigured. Statistics are based on kernel counters, not packet inspection."],["INSTALL","```apt: sudo apt install vnstat```\n\n```dnf: sudo dnf install vnstat```\n\n```pacman: sudo pacman -S vnstat```\n\n```apk: sudo apk add vnstat```\n\n```zypper: sudo zypper install vnstat```\n\n```brew: brew install vnstat```\n\n```nix: nix profile install nixpkgs#vnstat```"],["SEE ALSO","[vnstatd](/man/vnstatd)(8), [iftop](/man/iftop)(8), [nethogs](/man/nethogs)(8), [nload](/man/nload)(1)"]],"e":["vnstatd","iftop","nethogs","nload"],"x":"vnstat"},"wdctl":{"n":"wdctl","t":"Display hardware watchdog timer status","s":[["TAGLINE","Display hardware watchdog timer status"],["TLDR","**Display** the watchdog status\n\n```wdctl```\n\n**Display** the watchdog status in key-value format on one line\n\n```wdctl -O```\n\n**Display** only specific watchdog flags\n\n```wdctl -f [flag_list]```"],["SYNOPSIS","**wdctl** [_options_] [_device_]"],["PARAMETERS","**-O, --oneline**\n> Output in single-line key-value format\n\n**-f, --flags _list_**\n> Display only specified flags\n\n**-s, --settimeout _seconds_**\n> Set the watchdog timeout\n\n**-x, --flags-only**\n> Show flags only\n\n**-o, --output _list_**\n> Define output columns\n\n**-r, --raw**\n> Raw output format\n\n**-n, --noheadings**\n> Don't print headings"],["DESCRIPTION","**wdctl** shows the status of the hardware watchdog timer. A watchdog is a hardware or software timer that triggers a system reset if the main program fails to regularly reset it, preventing system lockups.\n\nThe output includes information about the watchdog device, timeout values, and supported features. Different hardware watchdogs support different capabilities."],["CAVEATS","Requires read access to the watchdog device (typically **/dev/watchdog**). Available flags are driver-specific. Not all systems have hardware watchdog support. Part of the util-linux package."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[watchdog](/man/watchdog)(8)"]],"e":["watchdog"],"x":"wdctl"},"weather":{"n":"weather","t":"Command-line weather forecasts","s":[["TAGLINE","Command-line weather forecasts"],["TLDR","**Current weather** for a location\n\n```weather [city]```\n\n**Use geolocation** / default location\n\n```weather```\n\n**JSON output** (if supported)\n\n```weather -j [city]```"],["SYNOPSIS","**weather** [*options*] [*location*]"],["DESCRIPTION","**weather** (also published historically as **darksky-weather** from genuinetools) prints concise weather conditions and forecasts in the terminal. Location can be a city name or coordinates depending on version and backend API configuration.\n\nMany builds require an API key from a weather data provider (originally Dark Sky; forks may use OpenWeatherMap or others). Check **weather --help** and environment variables documented by your package."],["PARAMETERS","*location*\n\n> City or query string for the forecast.\n\n**-j** / **--json**\n\n> Machine-readable output when available.\n\n**-h**, **--help**\n\n> Options for units, days ahead, and icons."],["CAVEATS","Upstream weather APIs change frequently; keys, quotas, and endpoints may break older binaries. Prefer a maintained package for your distribution. Not related to GNOME Weather GUI."],["INSTALL","```dnf: sudo dnf install expect```\n\n```pacman: sudo pacman -S expect```\n\n```apk: sudo apk add expect```\n\n```zypper: sudo zypper install expect```\n\n```brew: brew install expect```\n\n```nix: nix profile install nixpkgs#expect```"],["SEE ALSO","[curl](/man/curl)(1), [ansiweather](/man/ansiweather)(1), [wttr](/man/wttr)(1)"],["RESOURCES","```[Source code](https://github.com/genuinetools/weather)```"]],"e":["curl","ansiweather","wttr"],"x":"weather [city]"},"wifitui":{"n":"wifitui","t":"Terminal UI for managing WiFi connections","s":[["TAGLINE","Terminal UI for managing WiFi connections"],["TLDR","**Launch the interactive WiFi manager**\n\n```wifitui```\n\n**Scan and list available WiFi networks**\n\n```wifitui scan```\n\n**Connect to a specific network**\n\n```wifitui connect \"[SSID]\"```\n\n**Disconnect from current network**\n\n```wifitui disconnect```\n\n**Show known (saved) networks**\n\n```wifitui known```"],["SYNOPSIS","**wifitui** [_command_] [_options_]"],["PARAMETERS","**scan**\n> Scan and display available WiFi networks.\n\n**connect** _SSID_\n> Connect to the specified WiFi network.\n\n**disconnect**\n> Disconnect from the current WiFi network.\n\n**known**\n> List saved/known WiFi networks."],["DESCRIPTION","**wifitui** is a terminal-based WiFi manager for Linux that provides an interactive TUI for scanning, connecting, and managing wireless networks. It uses **NetworkManager** or **iwd** as the backend and provides fuzzy filtering for network selection.\n\nFeatures include QR code generation for sharing network credentials and both interactive and non-interactive (command-line) modes of operation."],["CAVEATS","Requires either **NetworkManager** or **iwd** to be running as the wireless backend. Root or appropriate group membership may be needed for scanning and connecting operations."],["HISTORY","**wifitui** was created by **shazow** and is written in **Rust**."],["INSTALL","```brew: brew install wifitui```\n\n```nix: nix profile install nixpkgs#wifitui```"],["SEE ALSO","[nmtui](/man/nmtui)(1), [nmcli](/man/nmcli)(1), [iwctl](/man/iwctl)(1)"]],"e":["nmtui","nmcli","iwctl"],"x":"wifitui"},"xxh":{"n":"xxh","t":"Portable shell environment over SSH","s":[["TAGLINE","Portable shell environment over SSH"],["TLDR","**Connect to a host** with your portable shell\n\n```xxh [hostname]```\n\n**Connect using a specific shell**\n\n```xxh [hostname] +s [zsh]```\n\n**Connect with SSH arguments**\n\n```xxh -i [~/.ssh/id_rsa] -p [2222] [hostname]```\n\n**Install shell on first connect** without prompts\n\n```xxh [hostname] +s [fish] +i```\n\n**Install a plugin** before connecting\n\n```xxh [hostname] +I [xxh-plugin-zsh-ohmyzsh]```\n\n**Connect in hermetic mode** (isolated home)\n\n```xxh [hostname] +hh```\n\n**List available shells**\n\n```xxh +LS```\n\n**Remove xxh from remote host**\n\n```xxh [hostname] +hc```"],["SYNOPSIS","**xxh** [_ssh_options_] _host_ [**+s** _shell_] [**+i**] [**+I** _plugin_] [**+hh**]"],["PARAMETERS","**+s**, **+shell** _shell_\n> Specify shell to use (zsh, bash, fish, xonsh, osquery)\n\n**+i**, **+install**\n> Install shell without confirmation prompts\n\n**+I**, **+install-plugin** _plugin_\n> Install a plugin before connecting\n\n**+hh**, **+hermetic-home**\n> Use hermetic home directory (~/.xxh isolated)\n\n**+hc**, **+host-clean**\n> Remove xxh directory from remote host\n\n**+LS**, **+list-shells**\n> List available portable shells\n\n**+LP**, **+list-plugins**\n> List available plugins\n\n**-i** _keyfile_\n> SSH identity file (passed to ssh)\n\n**-p** _port_\n> SSH port (passed to ssh)"],["DESCRIPTION","**xxh** brings your favorite shell and configurations to any remote host via SSH without requiring root access or system installations. It uploads portable versions of shells (zsh, fish, bash, xonsh) along with your plugins and configurations.\n\nWhen you run **xxh host**, it downloads a portable shell locally (cached for reuse), uploads it to the remote host's **~/.xxh** directory, then connects via SSH and runs that shell. Your aliases, themes, and plugins work immediately.\n\nThe tool is **hermetic**: removing the **~/.xxh** directory from the remote host completely restores the original environment. Different hermetic levels control how isolated the xxh environment is from the system.\n\nPlugins extend functionality, adding tools like oh-my-zsh, powerlevel10k, or command-line utilities. Plugins are installed with **+I plugin-name**."],["CAVEATS","xxh requires Python 3.6+ on the local machine. The first connection to a new host may be slower while uploading the portable shell. Some plugins may have additional dependencies. The remote host needs basic tools like tar and gzip."],["HISTORY","**xxh** was created to solve the problem of losing custom shell environments when connecting to remote servers. Written in Python, it provides a way to maintain consistent command-line experience across multiple machines. The project supports multiple shells through community-contributed portable shell packages."],["INSTALL","```brew: brew install xxh```\n\n```nix: nix profile install nixpkgs#xxh```"],["SEE ALSO","[ssh](/man/ssh)(1), [zsh](/man/zsh)(1), [fish](/man/fish)(1), [bash](/man/bash)(1)"]],"e":["ssh","zsh","fish","bash"],"x":"xxh [hostname]"},"zellij":{"n":"zellij","t":"Modern terminal workspace multiplexer","s":[["TAGLINE","Modern terminal workspace multiplexer"],["TLDR","**Start a new session**\n\n```zellij```\n\n**Start a named session**\n\n```zellij -s [session_name]```\n\n**List existing sessions**\n\n```zellij list-sessions```\n\n**Attach to existing session**\n\n```zellij attach [session_name]```\n\n**Run with specific layout**\n\n```zellij --layout [layout_name]```\n\n**Detach from session (keybinding)**\n\n```Ctrl+o d```"],["SYNOPSIS","**zellij** [_options_] [_command_]"],["PARAMETERS","**-s**, **--session** _name_\n> Name for the new session\n\n**--layout** _layout_\n> Use specified layout file\n\n**-l**, **--layout-path** _path_\n> Path to layout file\n\n**-c**, **--config** _file_\n> Path to config file\n\n**--config-dir** _dir_\n> Path to config directory\n\n**-n**, **--new-session-with-layout** _layout_\n> Create new session with layout"],["SUBCOMMANDS","**attach** _name_\n> Attach to an existing session\n\n**list-sessions**, **ls**\n> List active sessions\n\n**kill-session** _name_\n> Kill specified session\n\n**kill-all-sessions**\n> Kill all sessions\n\n**setup**\n> Setup wizard and shell integration\n\n**options**\n> Change session options"],["DESCRIPTION","**zellij** is a terminal workspace manager (multiplexer) similar to tmux and screen, but with a modern approach. It provides panes, tabs, and sessions with a discoverable keyboard-driven interface.\n\nKey features include floating panes, a plugin system (WebAssembly-based), built-in layouts for common workflows, and a status bar showing available keybindings.\n\nDefault keybindings use modes accessed via **Ctrl+key**: **Ctrl+p** for pane mode, **Ctrl+t** for tab mode, **Ctrl+n** for resize mode, **Ctrl+h** for move mode, **Ctrl+s** for scroll mode, **Ctrl+o** for session mode.\n\nConfiguration is in **~/.config/zellij/config.kdl** using KDL format. Layouts define pane arrangements for reusable workspace setups."],["CAVEATS","Some key combinations may conflict with shell or application bindings. Zellij's mode system helps avoid conflicts.\n\nPlugin development requires Rust and WebAssembly knowledge.\n\nSessions persist until explicitly killed, potentially consuming resources if forgotten."],["INSTALL","```pacman: sudo pacman -S zellij```\n\n```apk: sudo apk add zellij```\n\n```zypper: sudo zypper install zellij```\n\n```brew: brew install zellij```\n\n```nix: nix profile install nixpkgs#zellij```"],["SEE ALSO","[tmux](/man/tmux)(1), [screen](/man/screen)(1), [byobu](/man/byobu)(1)"]],"e":["tmux","screen","byobu"],"x":"zellij"},"zerobrew":{"n":"zerobrew","t":"Faster experimental Homebrew alternative written in Rust","s":[["TAGLINE","Faster experimental Homebrew alternative written in Rust"],["TLDR","**Install one or more packages**\n\n```zb install [ripgrep] [jq]```\n\n**Uninstall a package**\n\n```zb uninstall [ripgrep]```\n\n**Install packages from a Brewfile**\n\n```zb bundle```\n\n**Dump installed packages to a Brewfile**\n\n```zb bundle dump```\n\n**Run a package without linking it**\n\n```zbx [jq] --version```\n\n**Garbage collect unreferenced store entries**\n\n```zb gc```\n\n**Uninstall all packages (reset)**\n\n```zb reset```"],["SYNOPSIS","**zb** _command_ [_options_]\n\n**zbx** _package_ [_arguments_...]"],["PARAMETERS","**install** _PACKAGE_...\n> Install one or more packages.\n\n**uninstall** _PACKAGE_...\n> Remove packages.\n\n**bundle**\n> Install packages listed in a Brewfile.\n\n**bundle dump**\n> Export installed packages to a Brewfile.\n\n**gc**\n> Garbage collect unused store entries.\n\n**reset**\n> Uninstall all packages.\n\n**zbx**\n> Run a package without creating system links."],["DESCRIPTION","**zerobrew** (`zb`) is a high-performance reimplementation of Homebrew that uses content-addressable storage and APFS clonefiles for zero-overhead copying, giving 5-20x faster installs than Homebrew on macOS. It leverages Homebrew's formula definitions and pre-built bottles, bringing a uv-style architecture to Homebrew packages."],["CAVEATS","Experimental software; recommended to run alongside Homebrew rather than as a full replacement. Works primarily with bottles (pre-built binaries), not source builds. Best performance on macOS with APFS via clonefiles."],["HISTORY","**zerobrew** was created by **Lucas Gelfond** (lucasgelfond) and is written in **Rust**."],["INSTALL","```aur: yay -S zerobrew```"],["SEE ALSO","[brew](/man/brew)(1), [nix](/man/nix)(1)"]],"e":["brew","nix"],"x":"zb install [ripgrep] [jq]"},"zf":{"n":"zf","t":"Fuzzy finder optimized for filtering file paths","s":[["TAGLINE","Fuzzy finder optimized for filtering file paths"],["TLDR","**Fuzzy find files interactively**\n\n```find . -type f | zf```\n\n**Open selected file in vim**\n\n```vim $(find . -type f | zf)```"],["SYNOPSIS","**zf** [_options_]"],["DESCRIPTION","**zf** is a command-line fuzzy finder that prioritizes filename matches over full path matches. When the query resembles a file path, it applies path-aware heuristics for more accurate results. Also available as an allocation-free library."],["HISTORY","**zf** was created by **Nate Craddock** (natecraddock) and is written in **Zig**."],["INSTALL","```brew: brew install zf```\n\n```nix: nix profile install nixpkgs#zf```"],["SEE ALSO","[fzf](/man/fzf)(1)"]],"e":["fzf"],"x":"find . -type f | zf"}});
