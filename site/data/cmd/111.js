window.LCL_BUCKET(111,{"a2dismod":{"n":"a2dismod","t":"disable an Apache2 module","s":[["TAGLINE","disable an Apache2 module"],["TLDR","**Disable** a module\n\n```sudo a2dismod [module]```\n\nDon't show **informative messages**\n\n```sudo a2dismod -q [module]```\n\n**Force** disable a module even if it does not exist or is already disabled\n\n```sudo a2dismod -f [module]```\n\nDisable a module and **purge** all traces from the internal state database\n\n```sudo a2dismod -p [module]```"],["SYNOPSIS","**a2dismod** [_-q_|_--quiet_] [_-f_|_--force_] [_-m_|_--maintmode_] [_-p_|_--purge_] [_module_]"],["DESCRIPTION","**a2dismod** is a Debian-specific utility that disables Apache2 modules by removing symbolic links from **/etc/apache2/mods-enabled**. It manages both **.load** files and associated **.conf** files for each module.\n\nThe tool works alongside **a2enmod**, which enables modules. After disabling a module, Apache must be restarted or reloaded for changes to take effect."],["PARAMETERS","**-q, --quiet**\n> Don't show informative messages\n\n**-f, --force**\n> Do not complain if the specified module does not exist or is already disabled\n\n**-m, --maintmode**\n> Enables maintainer mode; the program invocation is effectuated automatically by a maintainer script\n\n**-p, --purge**\n> When disabling a module, purge all traces from the internal state database"],["CAVEATS","This utility is **Debian-specific** and not available on other Linux distributions (also works on Ubuntu and derivatives). After disabling a module, you must restart or reload Apache for changes to take effect."],["HISTORY","Created as part of the Apache2 package for **Debian GNU/Linux**. The manual page was authored by Daniel Stone and dates to **October 2006**."],["INSTALL","```apt: sudo apt install apache2```\n\n```zypper: sudo zypper install apache2```\n\n```brew: brew install apache2```"],["SEE ALSO","[a2enmod](/man/a2enmod)(8), [a2enconf](/man/a2enconf)(8), [a2disconf](/man/a2disconf)(8), [a2ensite](/man/a2ensite)(8), [a2dissite](/man/a2dissite)(8), [apache2ctl](/man/apache2ctl)(8)"]],"e":["a2enmod","a2enconf","a2disconf","a2ensite","a2dissite","apache2ctl"],"x":"sudo a2dismod [module]"},"airodump-ng":{"n":"airodump-ng","t":"Capture wireless packets and discover nearby networks","s":[["TAGLINE","Capture wireless packets and discover nearby networks"],["TLDR","**Scan** all wireless networks\n\n```sudo airodump-ng [wlan0mon]```\n\nCapture packets on **specific channel**\n\n```sudo airodump-ng -c [6] --bssid [00:11:22:33:44:55] -w [capture] [wlan0mon]```\n\nScan only **5GHz** networks\n\n```sudo airodump-ng --band a [wlan0mon]```\n\nScan with **manufacturer info**\n\n```sudo airodump-ng --manufacturer [wlan0mon]```\n\nScan showing **WPS** information\n\n```sudo airodump-ng --wps [wlan0mon]```"],["SYNOPSIS","**airodump-ng** [_-c channel_] [_--bssid mac_] [_-w prefix_] [_--band band_] [_options_] _interface_"],["DESCRIPTION","**airodump-ng** is a wireless packet capture and network discovery tool. It displays detected access points and clients in real-time, capturing packets to files for later analysis with aircrack-ng.\n\nThe tool shows network names, encryption types, signal strength, channel, MAC addresses, and associated clients, making it essential for wireless reconnaissance."],["PARAMETERS","**-c** _channel_\n> Lock to specific channel\n\n**--bssid** _mac_\n> Filter by access point MAC\n\n**-w** _prefix_\n> Output file prefix (creates .cap, .csv, .kismet.csv, .kismet.netxml)\n\n**--band** _band_\n> Band(s) to scan: a (5GHz), b and g (2.4GHz). Letters can be combined, e.g. abg\n\n**-a**\n> Filter out (hide) unassociated clients\n\n**--encrypt** _suite_\n> Filter access points by encryption (e.g. WEP, WPA, WPA2, OPN)\n\n**--essid** _essid_\n> Filter access points by ESSID\n\n**--write-interval** _seconds_\n> Time between file writes\n\n**--manufacturer**\n> Show manufacturer from OUI\n\n**--wps**\n> Show WPS information\n\n**--output-format** _formats_\n> Output formats: pcap, ivs, csv, gps, kismet, netxml\n\n**-r** _file_\n> Read from capture file instead of live capture"],["CAVEATS","For authorized security testing only. Requires monitor mode interface. Capture files can become very large. Some information requires being on the correct channel."],["HISTORY","**airodump-ng** is a core component of the aircrack-ng suite, first released in **2006**. It replaced the original airodump with improved features and stability."],["INSTALL","```apt: sudo apt install aircrack-ng```\n\n```dnf: sudo dnf install aircrack-ng```\n\n```pacman: sudo pacman -S aircrack-ng```\n\n```apk: sudo apk add aircrack-ng```\n\n```zypper: sudo zypper install aircrack-ng```\n\n```brew: brew install aircrack-ng```\n\n```nix: nix profile install nixpkgs#aircrack-ng```"],["SEE ALSO","[aircrack-ng](/man/aircrack-ng)(1), [aireplay-ng](/man/aireplay-ng)(1), [airmon-ng](/man/airmon-ng)(1), [airodump-ng-oui-update](/man/airodump-ng-oui-update)(1), [wireshark](/man/wireshark)(1)"],["RESOURCES","```[Source code](https://github.com/aircrack-ng/aircrack-ng)```\n\n```[Homepage](https://www.aircrack-ng.org/)```\n\n```[Documentation](https://www.aircrack-ng.org/documentation.html)```"]],"e":["aircrack-ng","aireplay-ng","airmon-ng","airodump-ng-oui-update","wireshark"],"x":"sudo airodump-ng [wlan0mon]"},"ascii-image-converter":{"n":"ascii-image-converter","t":"Transform images into ASCII art for terminal display","s":[["TAGLINE","Transform images into ASCII art for terminal display"],["TLDR","**Convert image to ASCII art**\n\n```ascii-image-converter [image.png]```\n\n**Convert with color output**\n\n```ascii-image-converter -C [image.png]```\n\n**Convert with specific width**\n\n```ascii-image-converter -W [80] [image.png]```\n\n**Convert using braille characters**\n\n```ascii-image-converter -b [image.png]```\n\n**Use a custom character map** (darkest to lightest)\n\n```ascii-image-converter -m \" .:-=+*#%@\" [image.png]```\n\n**Fit output to terminal width**\n\n```ascii-image-converter -f [image.png]```\n\n**Save output as PNG image**\n\n```ascii-image-converter -s [output_dir] -C [image.png]```"],["SYNOPSIS","**ascii-image-converter** [_options_] _image_"],["DESCRIPTION","**ascii-image-converter** transforms images into ASCII art. It analyzes pixel brightness and maps values to ASCII characters, creating text representations of images viewable in terminals.\n\nThe tool supports various image formats and can produce colored output using ANSI escape codes."],["PARAMETERS","**-C**, **--color**\n> Display with colors from the original image\n\n**-b**, **--braille**\n> Use Braille characters instead of ASCII\n\n**-g**, **--grayscale**\n> Display in grayscale colors\n\n**-n**, **--negative**\n> Invert colors\n\n**-c**, **--complex**\n> Use a wider range of ASCII characters for more detail\n\n**-m** _chars_, **--map** _chars_\n> Custom character string (darkest to lightest)\n\n**-d** _width_,_height_, **--dimensions** _width_,_height_\n> Set width and height in character lengths\n\n**-W** _width_, **--width** _width_\n> Set width; height calculated by aspect ratio\n\n**-H** _height_, **--height** _height_\n> Set height; width calculated by aspect ratio\n\n**-f**, **--full**\n> Fit to terminal width while maintaining aspect ratio\n\n**-x**, **--flipX**\n> Flip horizontally\n\n**-y**, **--flipY**\n> Flip vertically\n\n**--dither**\n> Apply dithering for braille art\n\n**--threshold** _value_\n> Threshold (0-255) for braille pixel comparison\n\n**--color-bg**\n> Apply color to character backgrounds instead of foregrounds\n\n**-s** _dir_, **--save-img** _dir_\n> Save output as PNG image\n\n**--save-txt** _dir_\n> Save output as TXT file\n\n**--save-gif** _dir_\n> Save converted GIF as ASCII art GIF\n\n**--only-save**\n> Suppress terminal output when saving\n\n**--formats**\n> Display supported input image formats"],["CAVEATS","Output quality depends on terminal font and size. Colored output requires a terminal with 24-bit or 8-bit ANSI color support. Braille characters require UTF-8 terminal support. Wide images need large terminal widths."],["HISTORY","**ascii-image-converter** is a modern Go-based tool for ASCII art generation, providing more features than traditional tools like jp2a."],["INSTALL","```aur: yay -S ascii-image-converter```\n\n```nix: nix profile install nixpkgs#ascii-image-converter```"],["SEE ALSO","[jp2a](/man/jp2a)(1), [img2txt](/man/img2txt)(1)"]],"e":["jp2a","img2txt"],"x":"ascii-image-converter [image.png]"},"asn":{"n":"asn","t":"ASN/IP lookup, network reconnaissance, and OSINT tool","s":[["TAGLINE","ASN/IP lookup, network reconnaissance, and OSINT tool"],["TLDR","Lookup an **IP address** with AS path trace\n\n```asn [8.8.8.8]```\n\nLookup an **autonomous system** by number\n\n```asn [AS15169]```\n\nLookup a **hostname** with trace\n\n```asn [github.com]```\n\nPerform a **detailed trace** with RPKI validation and hijack detection\n\n```asn -d [8.8.8.8]```\n\nLookup **without tracing**\n\n```asn -n [1.1.1.1]```\n\nSearch for all **ASNs matching** a name\n\n```asn -a [google]```\n\n**Shodan scan** multiple targets for CVEs, open ports, and hostnames\n\n```asn -s [1.1.1.1] [8.8.8.8]```\n\n**Bulk geolocate** IPs from a log file\n\n```asn -g < [access.log]```\n\nList all **CIDR blocks** allocated to a country\n\n```asn -c [germany]```\n\nOutput results as **pretty-printed JSON**\n\n```asn -J [AS15169]```"],["SYNOPSIS","**asn** [_options_] [_target_]\n\n**asn** [**-v**] **-l** [_server options_]"],["PARAMETERS","**-t**\n> Enable AS path trace to target (default behavior for IP targets).\n\n**-n**\n> Disable path tracing for IP targets. Disable additional INETNUM/origin lookups for AS targets.\n\n**-d**\n> Enable detailed trace mode with RPKI validation, route leak detection, and BGP hijack warnings.\n\n**-a**\n> ASN suggestion mode. Find all AS numbers and names matching the target string.\n\n**-u**\n> Transit/upstream lookup. Analyze BGP updates and AS paths to infer transit providers.\n\n**-c**\n> Country CIDR mode. Output all IPv4/IPv6 blocks allocated to the specified country.\n\n**-g**\n> Bulk geolocation mode. Extract and geolocate all IP addresses from input with occurrence statistics.\n\n**-s**\n> Shodan InternetDB scan. Query for CVEs, CPEs, tags, open ports, and hostnames. Supports multiple mixed targets and stdin.\n\n**-o**\n> Force interpretation of target as an organization name, bypassing automatic target type detection.\n\n**-j**\n> Compact JSON output for programmatic consumption.\n\n**-J**\n> Pretty-printed JSON output for readability.\n\n**-m**\n> Monochrome mode. Disable all colored output.\n\n**-4**\n> Force IPv4-only connectivity during traces and server mode.\n\n**-l** [_bind_address_] [_bind_port_]\n> Launch server mode. Starts a web interface for browser-based lookups and traceroutes. Default binding is 127.0.0.1:49200.\n\n**-v**\n> Verbose/debug mode. Display all queried URLs and log output to $HOME/asndebug.log.\n\n**-h**\n> Display usage information."],["SERVER OPTIONS","**--allow** _host[,host,...]_\n> Restrict connections to specified hosts.\n\n**--allowfile** _file_\n> Restrict connections using a host list file.\n\n**--deny** _host[,host,...]_\n> Block specified hosts from connecting.\n\n**--denyfile** _file_\n> Block hosts listed in a file.\n\n**-m**, **--max-conns** _n_\n> Maximum simultaneous connections (default: 100)."],["DESCRIPTION","**asn** is a comprehensive network reconnaissance and OSINT command-line tool written in Bash. It performs lookups on IP addresses, AS numbers, prefixes, hostnames, URLs, and organization names, combining data from numerous sources to produce detailed reports.\n\nFor AS numbers, the tool retrieves organization name, RIR allocation region, IXP presence, global AS ranking, BGP statistics, peering relationships, and BGP incident history including hijacks and route leaks over the past 12 months.\n\nFor IP addresses, it performs route lookups with ASN data, IP reputation scoring from multiple threat intelligence feeds, abuse contact information, and optional AS path tracing using mtr. The detailed trace mode adds RPKI route origin validation at each hop, detecting potential route leaks and BGP hijacks.\n\nAdditional modes support bulk IP geolocation from log files, country-level CIDR block enumeration, Shodan-based vulnerability scanning, transit provider identification through BGP update analysis, and organization-based network range discovery.\n\nThe tool can operate as a self-hosted web server, providing browser-based access and a JSON API for integration with other tools. Server mode supports access controls, custom bind addresses, and can be deployed as a systemd service.\n\nData sources include Team Cymru, PeeringDB, CAIDA ASRank, RIPEStat, ipinfo.io, ip-api, Shodan InternetDB, GreyNoise, IPQualityScore, Cloudflare Radar, ISC DSHIELD, NIST NVD, and the Prefix WhoIs project."],["CONFIGURATION","**$HOME/.asnrc**\n> Shell configuration file for overriding defaults. Supports settings such as MTR_ROUNDS, DETAILED_TRACE, FORCE_IPV4, MAX_CONCURRENT_SHODAN_REQUESTS, MONOCHROME_MODE, and server bind address/port.\n\n**$HOME/.asn/ipinfo_token**\n> ipinfo.io API token for enhanced geolocation data.\n\n**$HOME/.asn/iqs_token**\n> IPQualityScore API token for advanced threat intelligence.\n\n**$HOME/.asn/cloudflare_token**\n> Cloudflare Radar API token for BGP incident data.\n\nSystem-wide token files in /etc/asn/ are also supported and are preferred for server mode deployments."],["CAVEATS","Requires Bash v4.2 or later. Path tracing with mtr requires root privileges or CAP_NET_RAW capability. Several external dependencies must be installed: curl, whois, bind9-host (or equivalent), mtr, jq, ipcalc, grepcidr, nmap, ncat, and aha. Some features depend on optional API tokens for full functionality. The tool queries numerous external services, so an active internet connection is required."],["HISTORY","**asn** was created by **nitefood** as a quick OSINT command-line utility for investigating network data during incident response. It was featured in the Security Trails blog as a notable ASN lookup technique. The project is open source, licensed under MIT, and actively maintained with contributions from the community. It has grown from a simple lookup script into a comprehensive network reconnaissance platform with server mode, JSON API, and integration with major threat intelligence feeds."],["INSTALL","```apk: sudo apk add asn```\n\n```brew: brew install asn```\n\n```nix: nix profile install nixpkgs#asn```"],["SEE ALSO","[mtr](/man/mtr)(8), [whois](/man/whois)(1), [nmap](/man/nmap)(1), [dig](/man/dig)(1), [traceroute](/man/traceroute)(1), [jq](/man/jq)(1), [curl](/man/curl)(1)"]],"e":["mtr","whois","nmap","dig","traceroute","jq","curl"],"x":"asn [8.8.8.8]"},"aws-cloudfront":{"n":"aws-cloudfront","t":"Manage content delivery network distributions and caching.","s":[["TAGLINE","Manage content delivery network distributions and caching."],["TLDR","**Create a cache invalidation** for specific paths\n\n```aws cloudfront create-invalidation --distribution-id [EDFDVBD6EXAMPLE] --paths \"/[path/to/file.jpg]\" \"/[images/*]\"```\n\n**Invalidate all files** in a distribution\n\n```aws cloudfront create-invalidation --distribution-id [EDFDVBD6EXAMPLE] --paths \"/*\"```\n\n**List all distributions**\n\n```aws cloudfront list-distributions```\n\n**Get distribution details**\n\n```aws cloudfront get-distribution --id [EDFDVBD6EXAMPLE]```\n\n**Create a distribution** from config file\n\n```aws cloudfront create-distribution --distribution-config file://[distribution.json]```\n\n**List invalidations** for a distribution\n\n```aws cloudfront list-invalidations --distribution-id [EDFDVBD6EXAMPLE]```\n\n**Get invalidation status**\n\n```aws cloudfront get-invalidation --distribution-id [EDFDVBD6EXAMPLE] --id [I2J0I21PCUYOIK]```\n\n**Wait for a distribution to be deployed**\n\n```aws cloudfront wait distribution-deployed --id [EDFDVBD6EXAMPLE]```"],["SYNOPSIS","**aws cloudfront** _command_ [_options_]"],["PARAMETERS","**create-invalidation**\n> Invalidate cached objects in edge locations\n\n**list-invalidations**\n> List invalidation requests for a distribution\n\n**get-invalidation**\n> Get status of an invalidation request\n\n**create-distribution**\n> Create a new CloudFront distribution\n\n**get-distribution**\n> Get distribution configuration and status\n\n**list-distributions**\n> List all distributions in the account\n\n**update-distribution**\n> Modify distribution settings\n\n**delete-distribution**\n> Remove a distribution (must be disabled first)\n\n**create-origin-access-control**\n> Create OAC for secure S3 access.\n\n**get-distribution-config**\n> Get only the distribution configuration (without status metadata).\n\n**create-function**\n> Create a CloudFront Function for lightweight edge compute.\n\n**sign**\n> Sign CloudFront URLs or cookies for private content.\n\n**wait**\n> Wait for a distribution to reach a specific state (e.g., deployed).\n\n**--distribution-id** _id_\n> Distribution identifier (e.g., EDFDVBD6EXAMPLE)\n\n**--paths** _paths_\n> Space-separated paths to invalidate (supports wildcards with *)\n\n**--invalidation-batch** _json_\n> JSON with paths and caller reference\n\n**--distribution-config** _json_\n> Distribution configuration file\n\n**--id** _id_\n> Resource identifier (invalidation, distribution)\n\n**--if-match** _etag_\n> ETag for conditional updates/deletes"],["DESCRIPTION","**aws cloudfront** manages Amazon CloudFront, a content delivery network (CDN) that caches content at edge locations worldwide for low-latency delivery.\n\n**Invalidations** remove cached objects from edge locations before their TTL expires. Paths support wildcards (/images/*) to invalidate multiple files. The first 1,000 invalidation paths per month are free.\n\n**Distributions** define origins (S3, ALB, custom HTTP), cache behaviors, SSL certificates, and other delivery settings. Changes propagate globally and may take 15-30 minutes.\n\n**Origin Access Control (OAC)** secures S3 origins by restricting direct bucket access, requiring requests to go through CloudFront.\n\n**CloudFront Functions** and **Lambda@Edge** allow running code at edge locations to customize request/response handling.\n\nUse quotes around paths with wildcards in shell commands to prevent glob expansion."],["CAVEATS","Distribution deletion requires disabling first, then waiting for the status to change to `Deployed`. Invalidations have a cost after the first 1,000 paths per month. Distribution changes take time to propagate globally. Config updates require the current ETag value via **--if-match**."],["HISTORY","**Amazon CloudFront** launched in **November 2008** as AWS's content delivery network service. It has grown from basic static content delivery to support dynamic content, streaming, WebSocket, and serverless compute at the edge via Lambda@Edge (2016) and CloudFront Functions (2021)."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-s3](/man/aws-s3)(1), [aws-acm](/man/aws-acm)(1), [aws-route53](/man/aws-route53)(1)"]],"e":["aws","aws-s3","aws-acm","aws-route53"],"x":"aws cloudfront create-invalidation --distribution-id [EDFDVBD6EXAMPLE] --paths \"/[path/to/file.jpg]\" \"/[images/*]\""},"b4":{"n":"b4","t":"Work with patches and pull requests in mailing lists","s":[["TAGLINE","Work with patches and pull requests in mailing lists"],["TLDR","**Fetch a patch series** and apply it to the current branch\n\n```b4 shazam [message-id]```\n\n**Download a patch series** as a ready-to-apply mbox\n\n```b4 am [message-id]```\n\n**Start a new patch series** on a tracked branch\n\n```b4 prep -n [topical-branch-name]```\n\n**Send the prepared series** to its recipients\n\n```b4 send```\n\n**Retrieve a full thread** as an mbox file\n\n```b4 mbox [message-id]```\n\n**Compare two revisions** of a patch series\n\n```b4 diff [message-id]```\n\n**Generate thank-you notes** for merged contributions\n\n```b4 ty```"],["SYNOPSIS","**b4** [_-d_] [_-q_] [_--version_] _subcommand_ [_options_]"],["DESCRIPTION","**b4** is a helper tool for the distributed, patches-over-email development workflow used by the Linux kernel and many other projects hosted on public-inbox archives such as lore.kernel.org. It lets contributors prepare and send patch series, and lets maintainers retrieve, validate, and apply them without manually wrangling mailboxes.\n\nGiven a message-id or an archive URL, b4 finds the entire thread, picks the latest revision of a series, collects review trailers (Acked-by, Reviewed-by, Tested-by) scattered across replies, and produces a clean, correctly ordered mbox. It can also verify cryptographic patch attestation, guess the base commit, and apply everything directly to a working tree.\n\nOn the contributor side, the **prep** and **send** subcommands manage a series as a tracked branch, build the cover letter and recipient list, and dispatch the patches over SMTP or to a web submission endpoint."],["SUBCOMMANDS","**am**\n> Retrieve a series and write it to a ready-to-apply mbox.\n\n**shazam**\n> Retrieve a series and apply it straight to the current git branch.\n\n**prep**\n> Create and manage a patch series for submission.\n\n**send**\n> Send a prepared patch series to its recipients.\n\n**mbox**\n> Download an entire thread as an mbox file.\n\n**diff**\n> Generate an interdiff between two revisions of a series.\n\n**pr**\n> Work with pull requests sent to the list.\n\n**ty**\n> Generate thank-you notes for applied or merged series.\n\n**kr**\n> Manage contributor public keys for attestation."],["CAVEATS","b4 expects the upstream project to use a public-inbox archive (for example lore.kernel.org). Sending patches requires a working **git send-email** or SMTP configuration. Attestation verification depends on having contributor keys available locally."],["HISTORY","**b4** was written by **Konstantin Ryabitsev** at the Linux Foundation. It began life as a small script called **get-lore-mbox** and was renamed b4 (after the android character B-4 from Star Trek) as it grew into a full contributor and maintainer toolkit. It is widely used in Linux kernel subsystem workflows."],["INSTALL","```dnf: sudo dnf install b4```\n\n```pacman: sudo pacman -S b4```\n\n```apk: sudo apk add b4```\n\n```zypper: sudo zypper install b4```\n\n```brew: brew install b4```\n\n```nix: nix profile install nixpkgs#b4```"],["SEE ALSO","[git-am](/man/git-am)(1), [git-send-email](/man/git-send-email)(1), [git-format-patch](/man/git-format-patch)(1), [git](/man/git)(1)"],["RESOURCES","```[Source code](https://git.kernel.org/pub/scm/utils/b4/b4.git)```\n\n```[Documentation](https://b4.docs.kernel.org/)```"]],"e":["git-am","git-send-email","git-format-patch","git"],"x":"b4 shazam [message-id]"},"babo":{"n":"babo","t":"Run plain-language `.babo` scripts compiled by Claude Code","s":[["TAGLINE","Run plain-language `.babo` scripts compiled by Claude Code"],["TLDR","**Run** a Babo script (builds on first use, uses cache afterward)\n\n```babo hello.babo```\n\n**Force a rebuild** after editing the source description\n\n```babo build hello.babo```\n\n**Check** whether the cached build is still current\n\n```babo check hello.babo```\n\n**Show** cache metadata for a script\n\n```babo info hello.babo```\n\n**Remove** all cached builds\n\n```babo clean```"],["SYNOPSIS","**babo** [_run_] _file.babo_ [_args_...]\n\n**babo** _check_|_build_|_info_|_clean_ _file.babo_"],["DESCRIPTION","**babo** is a Python CLI that turns `.babo` files — short natural-language program descriptions — into runnable Python programs. On first run it sends the description to **Claude Code** (`claude -p`), which generates an entry point, optional `requirements.txt`, and an isolated virtual environment under `.baboc/<name>.baboc/` next to the source file (similar in spirit to Python's `__pycache__`).\n\nSubsequent runs skip the build when the `.babo` file is older than its cache directory. Arguments after the script path are forwarded to the generated program. A shebang `#!/usr/bin/env babo` lets `.babo` files execute directly once **babo** is on **PATH**.\n\nBabo scripts can call other `.babo` modules via a generated `runtime.call_babo()` helper, each module keeping its own venv so dependencies do not collide."],["PARAMETERS","**run**\n\n> Explicit run (default when the first argument is a `.babo` file).\n\n**build**\n\n> Force regeneration from the `.babo` source, ignoring a fresh cache.\n\n**check**\n\n> Print whether the cache is **FRESH** or **STALE**.\n\n**info**\n\n> Show build metadata (paths, timestamps, package list).\n\n**clean**\n\n> Delete every `.baboc/` cache directory under the current tree."],["CAVEATS","Requires **Python** 3.10+, the **claude** CLI installed and authenticated, and network access during builds. Builds are not offline. The tool is experimental and depends on Claude Code API availability."],["CONFIGURATION","No global config file. Each `.babo` file owns a `.baboc/<stem>.baboc/` cache with `venv/`, `requirements.txt`, `metadata.json`, and the generated `babo` executable entry point."],["SEE ALSO","[claude](/man/claude)(1), [python3](/man/python3)(1), [pip](/man/pip)(1)"]],"e":["claude","python3","pip"],"x":"babo hello.babo"},"beakerlib":{"n":"beakerlib","t":"Shell-level integration testing library","s":[["TAGLINE","Shell-level integration testing library"],["TLDR","**Create a basic test script**\n\n```rlJournalStart && rlPhaseStart FAIL \"Setup\" && rlRun \"command\" && rlPhaseEnd && rlJournalEnd```\n\n**Run a command and assert success**\n\n```rlRun \"[command]\" 0 \"Command should succeed\"```\n\n**Run a command and expect failure**\n\n```rlRun \"[command]\" 1 \"Command should fail with exit code 1\"```\n\n**Assert a file exists**\n\n```rlAssertExists [path/to/file]```\n\n**Assert strings are equal**\n\n```rlAssertEquals \"Test value\" \"[actual]\" \"[expected]\"```\n\n**Log informational message**\n\n```rlLog \"Informational message\"```\n\n**Backup and restore a file**\n\n```rlFileBackup [path/to/file] && rlRun \"[modify file]\" && rlFileRestore```"],["SYNOPSIS","Test scripts source BeakerLib and use its functions:\n\n```source /usr/share/beakerlib/beakerlib.sh```"],["FUNCTIONS","**rlJournalStart**\n> Initialize test journal; must be called first.\n\n**rlJournalEnd**\n> Finalize journal and generate report.\n\n**rlPhaseStart** _type_ _name_\n> Start a test phase (FAIL, WARN, or pass type).\n\n**rlPhaseEnd**\n> End the current test phase.\n\n**rlRun** _command_ [_expected_exit_] [_comment_]\n> Run command and check exit code.\n\n**rlAssertEquals** _comment_ _value1_ _value2_\n> Assert two values are equal.\n\n**rlAssertExists** _path_\n> Assert file or directory exists.\n\n**rlAssertNotExists** _path_\n> Assert file or directory does not exist.\n\n**rlAssertGrep** _pattern_ _file_\n> Assert pattern exists in file.\n\n**rlLog** _message_\n> Log an informational message.\n\n**rlFileBackup** _file_...\n> Backup files for later restoration.\n\n**rlFileRestore**\n> Restore previously backed up files.\n\n**rlServiceStart** / **rlServiceStop**\n> Start or stop a system service."],["DESCRIPTION","**BeakerLib** is a shell-level integration testing library used primarily for testing RPM packages and system components. It provides functions for organizing tests into phases, running commands with assertions, managing files and services, and generating structured test reports.\n\nTests are organized as journals containing phases. Each phase groups related assertions and logs. The library handles setup/cleanup, result collection, and report generation automatically.\n\nBeakerLib is designed for the **Beaker** test framework used in Red Hat and Fedora quality assurance, but can be used standalone for any shell-based integration testing. Tests produce logs in multiple formats including HTML and JUnit XML."],["CAVEATS","BeakerLib is primarily designed for RHEL/Fedora ecosystem; availability varies on other distributions. The shell-based approach can be slower than compiled test frameworks. Complex assertions require multiple function calls. The library must be sourced at the start of each test script."],["HISTORY","BeakerLib was developed at **Red Hat** as part of the **Beaker** integration testing infrastructure, originally designed for testing RHEL packages and system components. It grew from internal tools in the **mid-2000s** and was open-sourced along with Beaker. The library continues to be maintained as part of Red Hat's quality engineering toolchain."],["INSTALL","```dnf: sudo dnf install beakerlib```\n\n```aur: yay -S beakerlib```\n\n```brew: brew install beakerlib```"],["SEE ALSO","[bats](/man/bats)(1)"]],"e":["bats"],"x":"rlJournalStart && rlPhaseStart FAIL \"Setup\" && rlRun \"command\" && rlPhaseEnd && rlJournalEnd"},"cargo-owner":{"n":"cargo-owner","t":"Manage crate owners on a registry","s":[["TAGLINE","Manage crate owners on a registry"],["TLDR","**List crate owners**\n\n```cargo owner --list [crate_name]```\n\n**Add owner by username**\n\n```cargo owner --add [username] [crate_name]```\n\n**Remove owner**\n\n```cargo owner --remove [username] [crate_name]```\n\n**Add GitHub team as owner**\n\n```cargo owner --add github:[org]:[team] [crate_name]```"],["SYNOPSIS","**cargo owner** [_options_] [_crate_]"],["DESCRIPTION","**cargo owner** manages the owners of a crate on crates.io or another registry. Owners have the ability to publish new versions, yank existing releases, and add or remove other owners. All ownership modifications require authentication via an API token.\n\nThere are two types of owners: named users and teams. Named users have full rights including managing other owners, while teams can only publish and yank versions. GitHub teams can be added as owners using the `github:org:team` syntax, but only by a member of that team."],["PARAMETERS","**-a**, **--add** _login_\n> Add user or team as owner\n\n**-r**, **--remove** _login_\n> Remove user or team as owner\n\n**-l**, **--list**\n> List current owners\n\n**--registry** _name_\n> Registry to use\n\n**--index** _url_\n> Registry index URL\n\n**--token** _token_\n> API token for authentication\n\n**-v**, **--verbose**\n> Verbose output\n\n**-q**, **--quiet**\n> Suppress output"],["CONFIGURATION","**~/.cargo/credentials.toml**\n> Stores API tokens for registry authentication. Created by `cargo login`."],["OWNER TYPES","**Named owners (users)**\n> Full rights: publish, yank, add/remove owners\n\n**Team owners**\n> Limited rights: publish and yank only"],["TEAM SYNTAX","```\ngithub:organization:team-name\n```\n\nMust be a member of the team to add it."],["ENVIRONMENT","**CARGO_REGISTRY_TOKEN**\n> API token for authentication"],["CAVEATS","Only existing owners can modify ownership. Users must have logged into crates.io. Teams cannot add or remove other owners."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-publish](/man/cargo-publish)(1), [cargo-login](/man/cargo-login)(1)"],["RESOURCES","```[Source code](https://github.com/rust-lang/cargo)```\n\n```[Documentation](https://doc.rust-lang.org/cargo/commands/cargo-owner.html)```"]],"e":["cargo","cargo-publish","cargo-login"],"x":"cargo owner --list [crate_name]"},"cb":{"n":"cb","t":"C source code beautifier","s":[["TAGLINE","C source code beautifier"],["TLDR","**Beautify C code from a file**\n\n```cb < [path/to/source.c] > [path/to/output.c]```\n\n**Format using Kernighan & Ritchie style**\n\n```cb -s < [path/to/source.c]```\n\n**Set maximum line length**\n\n```cb -l [80] < [path/to/source.c]```\n\n**Join case labels onto single lines**\n\n```cb -j < [path/to/source.c]```\n\n**Display version information**\n\n```cb -V```"],["SYNOPSIS","**cb** [**-s**] [**-j**] [**-l** _length_] [**-V**] [_file_ ...]"],["DESCRIPTION","**cb** (C Beautifier) is a classic Unix utility that reformats C source code for improved readability. It reads from its arguments or standard input and writes beautified code to standard output, making it suitable for use in pipelines.\n\nThe tool performs lexical formatting without full syntactic analysis, adjusting indentation, spacing, and line breaks to display the structure of the code."],["PARAMETERS","**-s**\n> Write code in Kernighan & Ritchie style from The C Programming Language\n\n**-j**\n> Put split lines back together\n\n**-l** _length_\n> Split lines that are longer than _length_\n\n**-V**\n> Print version information to standard error"],["CAVEATS","The **cb** command performs only lexical analysis and may produce incorrect formatting with complex preprocessor macros that alter code structure. It supports only C code, not C++ or other languages.\n\nOn modern Linux distributions, **cb** is often not installed by default and is considered largely obsolete."],["HISTORY","The **cb** command originated in early Unix systems at Bell Labs as a simple tool for standardizing C code formatting. It has since been superseded by more sophisticated formatters like **indent**, **astyle**, and **clang-format** that provide fuller syntactic understanding and greater configurability."],["INSTALL","```apk: sudo apk add clipboard```\n\n```brew: brew install clipboard```"],["SEE ALSO","[indent](/man/indent)(1), [clang-format](/man/clang-format)(1), [astyle](/man/astyle)(1)"]],"e":["indent","clang-format","astyle"],"x":"cb < [path/to/source.c] > [path/to/output.c]"},"codemod":{"n":"codemod","t":"interactive regex-based codebase refactoring tool","s":[["TAGLINE","interactive regex-based codebase refactoring tool"],["TLDR","**Interactive regex replace in directory**\n\n```codemod -m -d [directory] '[old_pattern]' '[new_pattern]'```\n\n**Replace in specific file types**\n\n```codemod --extensions [php,html] '[pattern]' '[replacement]'```\n\n**Search without replacement (edit mode)**\n\n```codemod -m -d [directory] '[pattern]'```\n\n**Accept all changes without prompting**\n\n```codemod --accept-all '[pattern]' '[replacement]'```\n\n**Count matches only**\n\n```codemod --count '[pattern]' [directory]```"],["SYNOPSIS","**codemod** [_options_] _pattern_ [_replacement_] [_path_]"],["DESCRIPTION","**codemod** assists with large-scale codebase refactoring that requires human oversight and judgment. Unlike fully automated find-and-replace tools, codemod presents each potential change interactively, showing a colored diff and prompting whether to accept, reject, or manually edit the modification.\n\nThis interactive approach is ideal for complex refactoring tasks like renaming functions across hundreds of files, updating API calls, or modernizing code patterns where context matters. The tool uses Python regex for pattern matching and displays sufficient surrounding code to make informed decisions.\n\nDeveloped by Facebook and released as open source, codemod was designed for their massive PHP and JavaScript codebases. While the original project is now archived, it pioneered the interactive refactoring workflow later adopted by tools like fastmod and jscodeshift."],["PARAMETERS","**-m**\n> Show more context around matches\n\n**-d** _directory_\n> Start searching from directory\n\n**--extensions** _list_\n> Only process files with these extensions\n\n**--accept-all**\n> Accept all changes without prompting\n\n**--count**\n> Just count matches, don't modify\n\n**--editor** _editor_\n> Editor to use for manual edits\n\n**--exclude-paths** _pattern_\n> Exclude paths matching pattern\n\n**--include-extensionless**\n> Process files without extensions"],["INTERACTIVE PROMPTS","**y** - Accept change\n**n** - Reject change\n**e** - Edit in $EDITOR\n**q** - Quit\n**a** - Accept all remaining"],["RELATED TOOLS","**jscodeshift**\n> JavaScript/TypeScript AST-based codemod toolkit\n\n**fastmod**\n> Fast Rust-based alternative by Facebook"],["CAVEATS","Uses Python regex syntax. Complex transformations may require scripting via Python API. Archived project; consider fastmod or jscodeshift for new projects."],["SEE ALSO","[sed](/man/sed)(1), [perl](/man/perl)(1), [fastmod](/man/fastmod)(1)"],["RESOURCES","```[Source code](https://github.com/facebookarchive/codemod)```"]],"e":["sed","perl","fastmod"],"x":"codemod -m -d [directory] '[old_pattern]' '[new_pattern]'"},"cpdf":{"n":"cpdf","t":"Command-line PDF manipulation tool","s":[["TAGLINE","Command-line PDF manipulation tool"],["TLDR","**Merge PDF files**\n\n```cpdf [input1.pdf] [input2.pdf] -o [output.pdf]```\n\n**Extract pages**\n\n```cpdf [input.pdf] [1-10] -o [output.pdf]```\n\n**Rotate pages**\n\n```cpdf -rotate [90] [input.pdf] -o [output.pdf]```\n\n**Add password protection**\n\n```cpdf -encrypt [128bit] [owner_password] [user_password] [input.pdf] -o [output.pdf]```\n\n**Remove password**\n\n```cpdf -decrypt [input.pdf] owner=[password] -o [output.pdf]```\n\n**Split into single pages**\n\n```cpdf -split [input.pdf] -o [page%%%.pdf]```\n\n**Scale pages**\n\n```cpdf -scale-page \"[2.0 2.0]\" [input.pdf] -o [output.pdf]```"],["SYNOPSIS","**cpdf** [_options_] _input.pdf_ [_pages_] **-o** _output.pdf_"],["DESCRIPTION","**cpdf** (Coherent PDF) is a command-line tool for manipulating PDF files. It can merge, split, encrypt, decrypt, rotate, scale, and perform many other operations on PDF documents without requiring a GUI.\n\nThe tool provides precise control over PDF manipulation with support for page ranges, encryption levels, metadata editing, and transformations. It's designed for batch processing, automation, and scenarios where graphical PDF tools are impractical.\n\ncpdf is particularly useful for server-side PDF processing, shell scripts, and workflows requiring reliable, scriptable PDF operations. It handles complex operations like adding watermarks, stamping pages, and manipulating bookmarks."],["PARAMETERS","**-o** _file_\n> Output file path.\n\n**-encrypt** _level_ _owner_ _user_\n> Encrypt with specified bit level and passwords.\n\n**-decrypt**\n> Decrypt a PDF.\n\n**-rotate** _degrees_\n> Rotate pages by degrees.\n\n**-split**\n> Split into individual pages.\n\n**-scale-page** _\"x y\"_\n> Scale pages by factors.\n\n**-merge**\n> Merge multiple PDFs."],["INSTALL","```dnf: sudo dnf install cpdf```\n\n```apk: sudo apk add cpdf```\n\n```brew: brew install cpdf```"],["SEE ALSO","[pdftk](/man/pdftk)(1), [qpdf](/man/qpdf)(1), [gs](/man/gs)(1)"],["RESOURCES","```[Source code](https://github.com/johnwhitington/cpdf-source)```\n\n```[Homepage](https://www.coherentpdf.com/)```\n\n```[Documentation](https://www.coherentpdf.com/cpdfmanual.pdf)```"]],"e":["pdftk","qpdf","gs"],"x":"cpdf [input1.pdf] [input2.pdf] -o [output.pdf]"},"crane-push":{"n":"crane-push","t":"upload tarball as container image","s":[["TAGLINE","upload tarball as container image"],["TLDR","**Push tarball** as image\n\n```crane push [image.tar] [registry/repo:tag]```\n\n**Push** and write the resulting image digest\n\n```crane push [image.tar] [registry/repo:tag] --image-refs [refs.txt]```\n\n**Push** with a specific platform index\n\n```crane push [image.tar] [registry/repo:tag] --index```"],["SYNOPSIS","**crane** **push** [_options_] _tarball_ _image_"],["PARAMETERS","**--image-refs** _file_\n> Path to write the full image reference (including digest) by tag.\n\n**--index**\n> Push a collection of images as a single index (manifest list).\n\n**--platform** _os/arch_\n> Specify platform when pushing a multi-platform tarball."],["DESCRIPTION","**crane push** uploads a tarball as a container image to a registry. The tarball should be in OCI or Docker image format (as produced by **crane pull**, **docker save**, or **crane export**).\n\nThe command reads the image layers from the tarball and pushes them to the target repository, then uploads the manifest. Authentication uses the same Docker credentials configured in **~/.docker/config.json**."],["CAVEATS","The registry must be reachable and the user must have push access. For multi-platform images, use **--index**. Large tarballs stream from disk, so sufficient bandwidth is required."],["INSTALL","```pacman: sudo pacman -S crane```\n\n```apk: sudo apk add crane```\n\n```zypper: sudo zypper install crane```\n\n```brew: brew install crane```\n\n```nix: nix profile install nixpkgs#crane```"],["SEE ALSO","[crane](/man/crane)(1), [crane-pull](/man/crane-pull)(1), [docker](/man/docker)(1), [skopeo](/man/skopeo)(1)"]],"e":["crane","crane-pull","docker","skopeo"],"x":"crane push [image.tar] [registry/repo:tag]"},"cryfs":{"n":"cryfs","t":"cryptographic filesystem for cloud storage","s":[["TAGLINE","cryptographic filesystem for cloud storage"],["TLDR","**Create and mount** an encrypted filesystem\n\n```cryfs [path/to/cipher_dir] [path/to/mount_point]```\n\n**Mount with a specific configuration** file\n\n```cryfs --config [cryfs.config] [cipher_dir] [mount_point]```\n\n**Unmount** the encrypted filesystem\n\n```cryfs-unmount [path/to/mount_point]```\n\n**Change the encryption password**\n\n```cryfs-change-password [path/to/cipher_dir]```\n\n**Show filesystem information**\n\n```cryfs --show-ciphers```\n\n**Mount in foreground** for debugging\n\n```cryfs -f [cipher_dir] [mount_point]```"],["SYNOPSIS","**cryfs** [_options_] _cipher_dir_ _mount_point_"],["PARAMETERS","_CIPHER_DIR_\n> Directory where encrypted data is stored.\n\n_MOUNT_POINT_\n> Directory where the decrypted filesystem is mounted.\n\n**-c** _FILE_, **--config** _FILE_\n> Use specified configuration file.\n\n**-f**, **--foreground**\n> Run in foreground instead of daemonizing.\n\n**--allow-filesystem-upgrade**\n> Allow upgrading the filesystem format.\n\n**--show-ciphers**\n> Show available cipher options.\n\n**--unmount-idle** _MINUTES_\n> Automatically unmount after idle time.\n\n**--logfile** _FILE_\n> Write logs to specified file."],["DESCRIPTION","**CryFS** is a cryptographic filesystem designed specifically for cloud storage services like Dropbox, Google Drive, or OneDrive. Unlike other encrypted filesystems, CryFS encrypts not just file contents but also file sizes, metadata, and directory structure.\n\nFiles are split into fixed-size blocks that are individually encrypted and stored with random names. This prevents cloud providers and attackers from learning anything about your data, including which files changed and how large they are.\n\nThe encrypted data is stored in the cipher directory, which can be synced with cloud services. The mount point shows the decrypted view of your files. CryFS uses authenticated encryption with AES-256-GCM by default."],["CONFIGURATION","**~/.cryfs/config**\n> Stores filesystem configuration and cipher settings."],["CAVEATS","Performance is lower than unencrypted filesystems due to encryption overhead and block-based storage. The cipher directory should be synced, not the mount point. Forgetting the password means permanent data loss. FUSE must be available on the system."],["HISTORY","CryFS was created by Sebastian Messmer as his master's thesis project, with the first release in **2015**. It was designed to address privacy concerns with cloud storage, providing stronger confidentiality guarantees than traditional encrypted filesystems like EncFS."],["INSTALL","```apt: sudo apt install cryfs```\n\n```dnf: sudo dnf install cryfs```\n\n```pacman: sudo pacman -S cryfs```\n\n```zypper: sudo zypper install cryfs```\n\n```brew: brew install cryfs```"],["SEE ALSO","[encfs](/man/encfs)(1), [gocryptfs](/man/gocryptfs)(1), [veracrypt](/man/veracrypt)(1), [fusermount](/man/fusermount)(1)"],["RESOURCES","```[Source code](https://github.com/cryfs/cryfs)```\n\n```[Homepage](https://www.cryfs.org)```"]],"e":["encfs","gocryptfs","veracrypt","fusermount"],"x":"cryfs [path/to/cipher_dir] [path/to/mount_point]"},"docker-inspect":{"n":"docker-inspect","t":"return low-level information on Docker objects","s":[["TAGLINE","return low-level information on Docker objects"],["TLDR","**Inspect a container**\n\n```docker inspect [container]```\n\n**Inspect an image**\n\n```docker inspect [image]```\n\n**Get specific field**\n\n```docker inspect -f '{{.State.Status}}' [container]```\n\n**Get IP address**\n\n```docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' [container]```\n\n**Output as JSON**\n\n```docker inspect --format '{{json .Config}}' [container]```"],["SYNOPSIS","**docker** **inspect** [_options_] _name|id_ [_name|id..._]"],["PARAMETERS","**-f**, **--format** _string_\n> Format output using Go template.\n\n**-s**, **--size**\n> Display total file sizes (containers only).\n\n**--type** _string_\n> Return JSON for specified type (container or image)."],["DESCRIPTION","**docker inspect** returns low-level information on Docker objects including containers, images, volumes, and networks. Output is JSON by default, an array of result objects, one per argument.\n\nThe **--format** flag accepts a Go template for extracting specific fields, useful in shell scripts to capture values like IP addresses, mount points, or exit codes. The **{{json .Field}}** template emits raw JSON for a sub-object. The **--type** flag disambiguates when an image and container share the same name."],["CAVEATS","When multiple objects share a name, **--type** must be used to disambiguate. For containers, **--size** adds **SizeRw** and **SizeRootFs** fields but is expensive on large filesystems. The output schema differs between containers, images, networks, and volumes, so format templates are not portable across object types."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker](/man/docker)(1), [docker-ps](/man/docker-ps)(1), [docker-images](/man/docker-images)(1)"]],"e":["docker","docker-ps","docker-images"],"x":"docker inspect [container]"},"du":{"n":"du","t":"disk usage analyzer","s":[["TAGLINE","disk usage analyzer"],["TLDR","**Show total size of a directory**\n\n```du -sh [directory]```\n\n**Show sizes in human-readable format**\n\n```du -h [directory]```\n\n**Show sizes of subdirectories** (one level deep)\n\n```du -h --max-depth=1 [directory]```\n\n**Show largest subdirectories first**\n\n```du -h --max-depth=1 [directory] | sort -hr```\n\n**Show size of all files and directories**\n\n```du -ah [directory]```\n\n**Stay on one filesystem** (don't cross mount points)\n\n```du -shx [directory]```"],["SYNOPSIS","**du** [_options_] [_file_...]"],["DESCRIPTION","**du** (disk usage) estimates and reports file and directory space usage by recursively examining directory trees. Unlike df which shows filesystem-level free space, du focuses on individual files and directories, making it essential for identifying what's consuming disk space and where cleanup efforts should focus.\n\nThe tool walks directory hierarchies and sums the disk space used by each file, reporting sizes at each directory level. By default, it shows sizes for all directories in the tree, but options like --max-depth allow limiting output to higher-level summaries. The -h flag converts raw block counts to human-readable formats (KB, MB, GB).\n\ndu is commonly combined with sort to identify the largest space consumers. Patterns like \"du -h | sort -hr | head\" reveal the top disk space users, essential for troubleshooting full filesystems or planning cleanup operations.\n\nThe command reports disk usage (actual blocks consumed) rather than apparent file size, which matters for sparse files and filesystems with compression. Hard links to the same inode are counted only once by default (use -l to count each link separately). The -x option prevents crossing filesystem boundaries, useful for analyzing specific filesystems without including mounted subdirectories."],["PARAMETERS","**-h**, **--human-readable**\n> Human-readable sizes (K, M, G)\n\n**-s**, **--summarize**\n> Display total only\n\n**-c**, **--total**\n> Produce grand total\n\n**-a**, **--all**\n> Include files, not just directories\n\n**-d** _N_, **--max-depth=**_N_\n> Maximum directory depth\n\n**-b**, **--bytes**\n> Print sizes in bytes\n\n**-k**, **--kilobytes**\n> Print sizes in kilobytes\n\n**-m**, **--megabytes**\n> Print sizes in megabytes\n\n**-x**, **--one-file-system**\n> Skip different filesystems\n\n**-l**, **--count-links**\n> Count hard links multiple times\n\n**--apparent-size**\n> Print apparent sizes rather than disk usage\n\n**--si**\n> Like -h but use powers of 1000 (not 1024)\n\n**-L**, **--dereference**\n> Follow symbolic links\n\n**--exclude=**_pattern_\n> Exclude files matching pattern\n\n**--time**\n> Show last modification time"],["CAVEATS","Can be slow on large directory trees. Reports actual disk usage (blocks allocated), not apparent file size — use **--apparent-size** for file sizes. Crosses filesystem boundaries by default; use **-x** to stay on one filesystem. Hard links are counted only once by default. May require root for restricted directories."],["HISTORY","**du** has been part of Unix since the early **1970s**, one of the original Unix utilities for disk management."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[df](/man/df)(1), [ncdu](/man/ncdu)(1), [ls](/man/ls)(1), [find](/man/find)(1)"]],"e":["df","ncdu","ls","find"],"x":"du -sh [directory]"},"e2label":{"n":"e2label","t":"change ext filesystem labels","s":[["TAGLINE","change ext filesystem labels"],["TLDR","**Display** the current volume label\n\n```sudo e2label [/dev/sda1]```\n\nChange **volume label**\n\n```sudo e2label [/dev/sda1] \"[label_name]\"```\n\n**Clear** the volume label\n\n```sudo e2label [/dev/sda1] \"\"```"],["SYNOPSIS","**e2label** _device_ [_new-label_]"],["DESCRIPTION","**e2label** changes or displays the filesystem label on an ext2/ext3/ext4 filesystem. Labels provide human-readable names for filesystems and can be used to mount filesystems by label instead of device name, which improves portability when device names change.\n\nIf no label is specified, the current label is displayed. Labels are particularly useful in /etc/fstab entries, making system configuration more resilient to device renaming after hardware changes."],["PARAMETERS","_device_\n> The filesystem device\n\n_new-label_\n> New label (max 16 characters)"],["CAVEATS","Labels are limited to 16 characters and are silently truncated if longer. e2label is a thin wrapper around **tune2fs -L**, so the two are interchangeable. The filesystem may be mounted while the label is changed, but the new label is only picked up by udev and /dev/disk/by-label after the next scan. Part of the **e2fsprogs** package."],["INSTALL","```apt: sudo apt install e2fsprogs```\n\n```dnf: sudo dnf install e2fsprogs```\n\n```pacman: sudo pacman -S e2fsprogs```\n\n```apk: sudo apk add e2fsprogs-extra```\n\n```zypper: sudo zypper install e2fsprogs```\n\n```brew: brew install e2fsprogs```\n\n```nix: nix profile install nixpkgs#e2fsprogs```"],["SEE ALSO","[tune2fs](/man/tune2fs)(8), [blkid](/man/blkid)(8), [mount](/man/mount)(8), [e2undo](/man/e2undo)(8)"],["RESOURCES","```[Source code](https://git.kernel.org/pub/scm/fs/ext2/e2fsprogs.git)```\n\n```[Homepage](https://e2fsprogs.sourceforge.net/)```"]],"e":["tune2fs","blkid","mount","e2undo"],"x":"sudo e2label [/dev/sda1]"},"gdbus":{"n":"gdbus","t":"command-line tool for D-Bus introspection and interaction","s":[["TAGLINE","command-line tool for D-Bus introspection and interaction"],["TLDR","List all names on the **session bus**\n\n```gdbus list-names --session```\n\nList all names on the **system bus**\n\n```gdbus list-names --system```\n\n**Introspect** an object to see its interfaces and methods\n\n```gdbus introspect --session --dest [destination_bus_name] --object-path /[path/to/object]```\n\n**Call a method** on an object with arguments\n\n```gdbus call --session --dest [destination_bus_name] --object-path /[path/to/object] --method [interface.method_name] [args...]```\n\n**Emit a signal** from an object with arguments\n\n```gdbus emit --session --object-path /[path/to/object] --signal [interface.signal_name] [args...]```\n\n**Monitor** all messages on the session bus\n\n```gdbus monitor --session```"],["SYNOPSIS","**gdbus** _command_ [_options_]"],["PARAMETERS","**list-names**\n> List all names on a bus\n\n**introspect**\n> Introspect a remote object\n\n**call**\n> Invoke a method on a remote object\n\n**emit**\n> Emit a signal\n\n**monitor**\n> Monitor a remote object\n\n**wait**\n> Wait for a bus name to appear\n\n**--session**\n> Connect to the session bus (user-specific)\n\n**--system**\n> Connect to the system bus (system-wide)\n\n**--dest** _NAME_\n> Destination bus name\n\n**--object-path** _PATH_\n> D-Bus object path\n\n**--method** _INTERFACE.METHOD_\n> Method to call\n\n**--signal** _INTERFACE.SIGNAL_\n> Signal to emit"],["DESCRIPTION","**gdbus** is a command-line tool for interacting with D-Bus, the inter-process communication system used extensively in Linux desktop environments. It's part of GLib and provides a way to inspect, call methods on, and monitor D-Bus objects.\n\nThe session bus is used for user applications (desktop notifications, media players, etc.), while the system bus handles system-wide services (NetworkManager, UPower, etc.).\n\nUse **introspect** to discover available interfaces and methods on any D-Bus object before interacting with it."],["CAVEATS","D-Bus method signatures must match exactly. Complex types require proper GVariant formatting. Some system bus operations require elevated privileges or PolicyKit authorization."],["HISTORY","gdbus was introduced as part of GLib's GDBus implementation in **2010** (GLib 2.26), replacing the older dbus-glib bindings. It provides a more modern, GObject-based interface to D-Bus."],["INSTALL","```apt: sudo apt install libglib2.0-bin```\n\n```dnf: sudo dnf install glib```\n\n```apk: sudo apk add glib```\n\n```brew: brew install glib```\n\n```nix: nix profile install nixpkgs#glib```"],["SEE ALSO","[dbus-send](/man/dbus-send)(1), [busctl](/man/busctl)(1), [qdbus](/man/qdbus)(1)"],["RESOURCES","```[Documentation](https://docs.gtk.org/gio/gdbus.html)```\n\n```[Source code](https://gitlab.gnome.org/GNOME/glib)```"]],"e":["dbus-send","busctl","qdbus"],"x":"gdbus list-names --session"},"gixy":{"n":"gixy","t":"Nginx configuration security analyzer","s":[["TAGLINE","Nginx configuration security analyzer"],["TLDR","**Analyze nginx config**\n\n```gixy [/etc/nginx/nginx.conf]```\n\n**Run only specific tests**\n\n```gixy --tests [http_splitting,ssrf] [/etc/nginx/nginx.conf]```\n\n**Output in JSON format**\n\n```gixy -f json [/etc/nginx/nginx.conf]```\n\n**Skip specific tests**\n\n```gixy --skips [host_spoofing,low_keepalive_requests] [/etc/nginx/nginx.conf]```\n\n**Report only high severity issues**\n\n```gixy -lll [/etc/nginx/nginx.conf]```"],["SYNOPSIS","**gixy** [_options_] _config_"],["PARAMETERS","_CONFIG_\n> Nginx configuration file.\n\n**--tests** _TESTS_\n> Comma-separated list of tests to run.\n\n**--skips** _TESTS_\n> Comma-separated list of tests to skip.\n\n**-l, --level**\n> Report issues of a given severity level or higher. Use -l for LOW, -ll for MEDIUM, -lll for HIGH.\n\n**-f** _FORMAT_, **--format** _FORMAT_\n> Output format: text, json, console.\n\n**-o** _FILE_, **--output** _FILE_\n> Write report to a file.\n\n**-c** _FILE_, **--config** _FILE_\n> Path to configuration file.\n\n**-d, --debug**\n> Enable debug mode.\n\n**--disable-includes**\n> Disable processing of include directives.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gixy** analyzes nginx configuration files for security issues. It detects common misconfigurations that could lead to vulnerabilities like SSRF, host spoofing, and path traversal.\n\nThe tool includes plugins for various security checks and provides detailed explanations of issues found. It is used in security audits and CI pipelines to catch nginx misconfigurations before deployment."],["CAVEATS","Python tool. The original yandex/gixy project is unmaintained; gixy-next is a maintained fork. May need updates for latest nginx features. False positives possible. Some plugins expose additional options configurable via CLI flags or a configuration file."],["HISTORY","gixy was created by **Yandex** to automate nginx security configuration review."],["INSTALL","```brew: brew install gixy```\n\n```nix: nix profile install nixpkgs#gixy```"],["SEE ALSO","[nginx](/man/nginx)(1)"]],"e":["nginx"],"x":"gixy [/etc/nginx/nginx.conf]"},"glab-mr-create":{"n":"glab-mr-create","t":"Create a new GitLab merge request","s":[["TAGLINE","Create a new GitLab merge request"],["TLDR","**Create MR interactively**\n\n```glab mr create```\n\n**Create with title**\n\n```glab mr create -t \"[title]\"```\n\n**Create as draft**\n\n```glab mr create --draft -t \"[title]\"```\n\n**Create with reviewers**\n\n```glab mr create -t \"[title]\" --reviewer [user1],[user2]```\n\n**Create to specific branch**\n\n```glab mr create --target-branch [main]```\n\n**Fill from commits**\n\n```glab mr create --fill```\n\n**Skip prompts and push the branch**\n\n```glab mr create --fill --push --yes```\n\n**Create for a linked issue**\n\n```glab mr create --related-issue [issue-number]```"],["SYNOPSIS","**glab** **mr** **create** [_options_]"],["PARAMETERS","**-t**, **--title** _title_\n> MR title.\n\n**-d**, **--description** _text_\n> MR description. Use `-` to open an editor.\n\n**--draft**, **--wip**\n> Mark the merge request as a draft.\n\n**-a**, **--assignee** _users_\n> Assign the MR to users by username; comma-separate or repeat the flag.\n\n**--reviewer** _users_\n> Request review from users by username; comma-separate or repeat the flag.\n\n**-b**, **--target-branch** _branch_\n> Target/base branch to merge into.\n\n**-s**, **--source-branch** _branch_\n> Branch to create the MR from (default: current branch).\n\n**-f**, **--fill**\n> Don't prompt for title/description, use commit info instead. Implies **--push**.\n\n**--fill-commit-body**\n> When multiple commits exist, fill the description with every commit body. Requires **--fill**.\n\n**-l**, **--label** _labels_\n> Add labels by name; comma-separate or repeat the flag.\n\n**-m**, **--milestone** _milestone_\n> Assign a milestone by ID or title.\n\n**-i**, **--related-issue** _issue_\n> Create the MR for an issue; uses the issue title if **--title** is omitted.\n\n**--push**\n> Push local commits to the source branch after creating the MR.\n\n**--remove-source-branch**\n> Remove the source branch when the MR is merged.\n\n**--squash-before-merge**\n> Squash commits into one when merging.\n\n**-H**, **--head** _repo_\n> Use another head repository, as `OWNER/REPO`, `GROUP/NAMESPACE/REPO`, a project ID, or a full URL.\n\n**--template** _name_\n> Use a template from `.gitlab/merge_request_templates/` to pre-fill the description.\n\n**-y**, **--yes**\n> Skip the submission confirmation prompt. Use with **--fill** to skip all optional prompts.\n\n**-w**, **--web**\n> Continue MR creation in the browser."],["DESCRIPTION","**glab mr create** creates a new GitLab merge request from the current branch. It supports drafts, reviewer assignment, labels, milestones, and auto-filling title and description from commit messages.\n\nWhen run without flags, it launches an interactive prompt for composing the merge request."],["INSTALL","```apt: sudo apt install glab```\n\n```dnf: sudo dnf install glab```\n\n```pacman: sudo pacman -S glab```\n\n```apk: sudo apk add glab```\n\n```zypper: sudo zypper install glab```\n\n```brew: brew install glab```\n\n```nix: nix profile install nixpkgs#glab```"],["SEE ALSO","[glab-mr](/man/glab-mr)(1), [glab](/man/glab)(1)"],["RESOURCES","```[Documentation](https://docs.gitlab.com/cli/mr/create/)```\n\n```[Source code](https://gitlab.com/gitlab-org/cli)```"]],"e":["glab-mr","glab"],"x":"glab mr create"},"gocryptfs":{"n":"gocryptfs","t":"Encrypted overlay filesystem using FUSE","s":[["TAGLINE","Encrypted overlay filesystem using FUSE"],["TLDR","**Create encrypted directory**\n\n```gocryptfs -init [cipher-dir]```\n\n**Mount encrypted directory**\n\n```gocryptfs [cipher-dir] [mount-point]```\n\n**Unmount**\n\n```fusermount -u [mount-point]```\n\n**Change password**\n\n```gocryptfs -passwd [cipher-dir]```\n\n**Show info**\n\n```gocryptfs -info [cipher-dir]```"],["SYNOPSIS","**gocryptfs** [_options_] _cipherdir_ [_mountpoint_]"],["PARAMETERS","_CIPHERDIR_\n> Encrypted directory.\n\n_MOUNTPOINT_\n> Where to mount.\n\n**-init**\n> Initialize new volume.\n\n**-passwd**\n> Change password.\n\n**-info**\n> Show volume info.\n\n**-fg**\n> Run in foreground.\n\n**-allow_other**\n> Allow other users.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gocryptfs** is an encrypted overlay filesystem written in Go. It encrypts files individually using AES-256-GCM, storing ciphertext in a directory while presenting plaintext through a FUSE mount.\n\nThe tool provides file-level encryption with encrypted file names, enabling safe cloud backup of encrypted data. Metadata leakage is minimized compared to full-disk encryption alternatives."],["CONFIGURATION","**gocryptfs.conf**\n> Per-volume configuration file stored in the cipher directory, containing encryption parameters and the encrypted master key."],["CAVEATS","FUSE required. File sizes visible. Password recovery impossible without master key backup."],["HISTORY","gocryptfs was created as a modern, audited alternative to EncFS, using authenticated encryption and written in Go."],["INSTALL","```apt: sudo apt install gocryptfs```\n\n```dnf: sudo dnf install gocryptfs```\n\n```pacman: sudo pacman -S gocryptfs```\n\n```apk: sudo apk add gocryptfs```\n\n```zypper: sudo zypper install gocryptfs```\n\n```brew: brew install gocryptfs```\n\n```nix: nix profile install nixpkgs#gocryptfs```"],["SEE ALSO","[encfs](/man/encfs)(1), [cryptsetup](/man/cryptsetup)(1), [veracrypt](/man/veracrypt)(1)"],["RESOURCES","```[Source code](https://github.com/rfjakob/gocryptfs)```\n\n```[Homepage](https://nuetzlich.net/gocryptfs/)```"]],"e":["encfs","cryptsetup","veracrypt"],"x":"gocryptfs -init [cipher-dir]"},"gsettings":{"n":"gsettings","t":"GNOME desktop configuration command-line interface","s":[["TAGLINE","GNOME desktop configuration command-line interface"],["TLDR","**Set** the value of a key\n\n```gsettings set [org.example.schema] [example-key] [value]```\n\n**Get** the value of a key\n\n```gsettings get [org.example.schema] [example-key]```\n\n**Reset** a key to its schema default value\n\n```gsettings reset [org.example.schema] [example-key]```\n\nDisplay all schemas, keys, and **values recursively**\n\n```gsettings list-recursively```\n\nDisplay keys and values from **one schema**\n\n```gsettings list-recursively [org.example.schema]```\n\nDisplay **allowed values** for a key\n\n```gsettings range [org.example.schema] [example-key]```\n\nDisplay the **description** of a key\n\n```gsettings describe [org.example.schema] [example-key]```"],["SYNOPSIS","**gsettings** _command_ [_arguments_]"],["PARAMETERS","**get** _SCHEMA_ _KEY_\n> Get the value of a key\n\n**set** _SCHEMA_ _KEY_ _VALUE_\n> Set a key's value (must match schema type)\n\n**reset** _SCHEMA_ _KEY_\n> Reset key to schema default\n\n**list-schemas**\n> List installed schemas\n\n**list-keys** _SCHEMA_\n> List keys in a schema\n\n**list-recursively** [_SCHEMA_]\n> List all settings (optionally filtered by schema)\n\n**range** _SCHEMA_ _KEY_\n> Show valid values for a key\n\n**describe** _SCHEMA_ _KEY_\n> Show human-readable description\n\n**monitor** _SCHEMA_ [_KEY_]\n> Watch for changes"],["DESCRIPTION","**gsettings** is the command-line interface for GSettings, GNOME's configuration system. It provides schema-validated access to dconf settings, ensuring type safety and valid values.\n\nSettings are organized in schemas (like org.gnome.desktop.interface) containing typed keys. Unlike direct dconf access, gsettings validates values against the schema before applying them."],["COMMON SCHEMAS","**org.gnome.desktop.interface** - Desktop appearance settings\n**org.gnome.desktop.wm.preferences** - Window manager preferences\n**org.gnome.shell** - GNOME Shell settings"],["CAVEATS","Only works with applications using GSettings (primarily GNOME). Some settings require logout or restart to take effect. Schema must be installed for settings to work."],["HISTORY","GSettings was introduced in GLib 2.26 (**2010**) as a replacement for GConf. It uses dconf as its backend while providing schema validation, making configuration more robust and introspectable."],["INSTALL","```apt: sudo apt install libglib2.0-bin```\n\n```dnf: sudo dnf install glib```\n\n```apk: sudo apk add glib```\n\n```brew: brew install glib```\n\n```nix: nix profile install nixpkgs#glib```"],["SEE ALSO","[dconf](/man/dconf)(1)"]],"e":["dconf"],"x":"gsettings set [org.example.schema] [example-key] [value]"},"gummy":{"n":"gummy","t":"screen brightness and color temperature manager for Linux systems running X11","s":[["TAGLINE","screen brightness and color temperature manager for Linux systems running X11"],["TLDR","**Start the gummy daemon** (required before adjustments take effect)\n\n```gummy start```\n\nSet screen **temperature** to 3400K on all screens\n\n```gummy -t [3400]```\n\nSet **brightness** to 60% on a specific screen\n\n```gummy -b [60] -s [1]```\n\n**Enable automatic brightness** based on screen content\n\n```gummy -B [1]```\n\n**Enable automatic temperature** with sunrise/sunset times\n\n```gummy -T [1] -y [06:00] -u [16:30]```\n\n**Disable automatic brightness** on a specific screen\n\n```gummy -B [0] -s [1]```"],["SYNOPSIS","**gummy** **start**\n\n**gummy** [_options_]"],["PARAMETERS","**start**\n> Start the gummy background daemon that applies brightness/temperature adjustments.\n\n**-t** _KELVIN_\n> Set screen color temperature manually.\n\n**-T** _0|1_\n> Toggle automatic temperature adjustment based on sunrise/sunset times.\n\n**-b** _PERCENT_\n> Set screen brightness manually.\n\n**-B** _0|1|2_\n> Toggle automatic brightness: 0 = off, 1 = based on captured screen content, 2 = based on an ambient light sensor (if available).\n\n**-y** _HH:MM_\n> Set the sunrise time used for automatic temperature.\n\n**-u** _HH:MM_\n> Set the sunset time used for automatic temperature.\n\n**-s** _NUMBER_\n> Apply the given options to a specific screen (0-indexed) instead of all screens.\n\n**-h**\n> Print help information."],["DESCRIPTION","**gummy** is a CLI screen manager for X11 that adjusts brightness and color temperature, automatically or manually, per screen or across all screens. Brightness is applied via gamma (and, on supported embedded displays, backlight), while temperature adjustment reduces blue light for reduced eye strain in the evening.\n\nAutomatic brightness can follow either captured screen content (estimating ambient brightness from what's being displayed) or an ambient light sensor where present. Automatic temperature follows configurable sunrise/sunset times. The **gummy start** daemon must be running for either automatic mode, or for changes to persist."],["CAVEATS","Requires X11; does not work under Wayland. The upstream `Fushko/gummy` repository is no longer available on GitHub; forks/mirrors such as `Gitoffthelawn/gummy` carry the code forward. A separate configuration GUI, **gummy-conf**, exists for users who prefer not to use the CLI directly."],["HISTORY","**gummy** was created by **Francesco Fusco** as a lightweight, daemon-based alternative to Redshift-style tools for managing screen brightness and color temperature on Linux/X11."],["INSTALL","```nix: nix profile install nixpkgs#gummy```"],["SEE ALSO","[redshift](/man/redshift)(1), [xrandr](/man/xrandr)(1), [brightnessctl](/man/brightnessctl)(1)"],["RESOURCES","```[Source code](https://github.com/Gitoffthelawn/gummy)```"]],"e":["redshift","xrandr","brightnessctl"],"x":"gummy start"},"hardcover-tui":{"n":"hardcover-tui","t":"Terminal interface for Hardcover book tracking","s":[["TAGLINE","Terminal interface for Hardcover book tracking"],["TLDR","**Launch hardcover-tui**\n\n```hardcover-tui```"],["SYNOPSIS","**hardcover-tui** [_options_]"],["DESCRIPTION","**hardcover-tui** is an unofficial terminal user interface client for **Hardcover.app**, a social book tracking platform. It provides a TUI for browsing, searching, and managing your reading list directly from the terminal, including tracking reading progress, marking books as read, and exploring book recommendations."],["CAVEATS","Requires a Hardcover.app account and API token for authentication. As an unofficial client, feature availability depends on the Hardcover API."],["HISTORY","**hardcover-tui** was developed as a community project to bring Hardcover.app functionality to the terminal for users who prefer keyboard-driven workflows."],["SEE ALSO","[curl](/man/curl)(1)"]],"e":["curl"],"x":"hardcover-tui"},"iperf":{"n":"iperf","t":"measures network bandwidth between two endpoints","s":[["TAGLINE","measures network bandwidth between two endpoints"],["TLDR","**Start server** mode\n\n```iperf -s```\n\n**Connect to server** and run test\n\n```iperf -c [server_ip]```\n\n**Run test for specific** duration\n\n```iperf -c [server_ip] -t [30]```\n\n**Run UDP test** instead of TCP\n\n```iperf -c [server_ip] -u```\n\n**Test with multiple** parallel streams\n\n```iperf -c [server_ip] -P [4]```\n\n**Run bidirectional** test\n\n```iperf -c [server_ip] -d```\n\n**Set target bandwidth** for UDP\n\n```iperf -c [server_ip] -u -b [100M]```\n\n**Specify port**\n\n```iperf -c [server_ip] -p [5001]```\n\n**Report at intervals**\n\n```iperf -c [server_ip] -i [1]```"],["SYNOPSIS","**iperf** **-s** [_OPTIONS_]\n\n**iperf** **-c** _HOST_ [_OPTIONS_]"],["PARAMETERS","**-s**\n> Run in server mode.\n\n**-c** _HOST_\n> Run in client mode, connecting to server at HOST.\n\n**-p** _PORT_\n> Server port to listen on or connect to (default: 5001).\n\n**-u**\n> Use UDP instead of TCP.\n\n**-b** _BANDWIDTH_\n> Target bandwidth (e.g., 10M, 1G). Limits UDP and TCP rates.\n\n**-t** _TIME_\n> Test duration in seconds (default: 10).\n\n**-n** _BYTES_\n> Transmit specified number of bytes instead of using a time duration (e.g., 100M, 1G).\n\n**-i** _INTERVAL_\n> Report interval in seconds.\n\n**-P** _STREAMS_\n> Number of parallel client streams.\n\n**-d**\n> Bidirectional test using separate unidirectional sockets.\n\n**-r**\n> Bidirectional test (sequential: client-to-server, then reverse).\n\n**--full-duplex**\n> Full duplex test using a single socket for both directions.\n\n**-R**\n> Reverse traffic flow (server sends, client receives).\n\n**-f** _FORMAT_\n> Report format: a (adaptive), k/m/g (bits), K/M/G (bytes).\n\n**-w** _SIZE_\n> Socket buffer size (TCP window size).\n\n**-l** _LENGTH_\n> Read/write buffer length (TCP default 128K, UDP default 1470).\n\n**-M** _MSS_\n> Set TCP maximum segment size via TCP_MAXSEG.\n\n**-N**\n> Disable Nagle's algorithm (set TCP_NODELAY).\n\n**-B** _HOST_\n> Bind to a specific host, interface, or multicast address.\n\n**-D**\n> Run the server as a daemon.\n\n**-e**\n> Display enhanced output in reports.\n\n**-o** _FILE_\n> Write report output to specified file."],["DESCRIPTION","**iperf** measures network bandwidth between two endpoints. One host runs in server mode (-s), and another connects as a client (-c) to perform the test. By default, iperf measures TCP throughput.\n\nUDP mode (-u) tests UDP performance with configurable target bandwidth. This is useful for measuring packet loss and jitter at specific rates. Parallel streams (-P) can saturate high-bandwidth links that a single stream cannot fill.\n\nResults show bandwidth achieved, along with additional metrics depending on protocol (retransmits for TCP, packet loss and jitter for UDP)."],["CAVEATS","iperf (version 2) and iperf3 are separate projects with incompatible protocols. This page covers iperf2. Firewall rules may need to allow the iperf port (default 5001). Results can be affected by CPU limitations on either end, especially at high bandwidths."],["HISTORY","iperf was developed by NLANR/DAST as an open-source network testing tool. The original iperf (version 2) became widely used for network diagnostics. **iperf3** is a complete rewrite by ESnet with enhanced features but uses an incompatible protocol, requiring both endpoints to use the same version."],["INSTALL","```apt: sudo apt install iperf```\n\n```dnf: sudo dnf install iperf```\n\n```pacman: sudo pacman -S iperf```\n\n```apk: sudo apk add iperf```\n\n```zypper: sudo zypper install iperf```\n\n```brew: brew install iperf```\n\n```nix: nix profile install nixpkgs#iperf```"],["SEE ALSO","[iperf3](/man/iperf3)(1), [netcat](/man/netcat)(1), [speedtest-cli](/man/speedtest-cli)(1), [ping](/man/ping)(8)"]],"e":["iperf3","netcat","speedtest-cli","ping"],"x":"iperf -s"},"iptables-restore":{"n":"iptables-restore","t":"restores iptables IPv4 rules from a file created by iptables-save","s":[["TAGLINE","restores iptables IPv4 rules from a file created by iptables-save"],["TLDR","**Restore** from file\n\n```sudo iptables-restore /etc/iptables.rules```\n\n**Restore** from file without flushing existing rules\n\n```sudo iptables-restore -n /etc/iptables.rules```\n\n**Test** rules without applying them\n\n```sudo iptables-restore -t /etc/iptables.rules```\n\n**Restore** from stdin using iptables-save\n\n```sudo iptables-save | sudo iptables-restore```"],["SYNOPSIS","**iptables-restore** [_OPTIONS_] [_file_]"],["DESCRIPTION","**iptables-restore** restores iptables IPv4 rules from a file created by iptables-save. Use ip6tables-restore for IPv6 rules. Input can be from a file or stdin."],["PARAMETERS","**-c, --counters**\n> Restore packet and byte counter values\n\n**-n, --noflush**\n> Don't flush existing rules before restoring\n\n**-T, --table** _name_\n> Only restore the specified table\n\n**-t, --test**\n> Test mode - parse rules but don't apply\n\n**-v, --verbose**\n> Print additional debug info during ruleset processing\n\n**-w, --wait** [_seconds_]\n> Wait for xtables lock\n\n**-V, --version**\n> Print the program version number\n\n**-M, --modprobe** _modprobe_\n> Specify the path to the modprobe program"],["CAVEATS","By default, all existing rules are flushed before restore. Use -n to add rules without flushing. This only restores IPv4 rules; use ip6tables-restore for IPv6."],["HISTORY","**iptables-restore** is part of the **iptables** package for managing the Linux kernel firewall."],["INSTALL","```pacman: sudo pacman -S iptables```\n\n```apk: sudo apk add iptables```\n\n```zypper: sudo zypper install iptables```\n\n```brew: brew install iptables```\n\n```nix: nix profile install nixpkgs#iptables```"],["SEE ALSO","[iptables](/man/iptables)(8), [iptables-save](/man/iptables-save)(8), [ip6tables-restore](/man/ip6tables-restore)(8)"]],"e":["iptables","iptables-save","ip6tables-restore"],"x":"sudo iptables-restore /etc/iptables.rules"},"is-up":{"n":"is-up","t":"checks if websites are accessible","s":[["TAGLINE","checks if websites are accessible"],["TLDR","**Check if site is up**\n\n```is-up [example.com]```\n\n**Check multiple sites**\n\n```is-up [site1.com] [site2.com]```\n\n**Get exit code only**\n\n```is-up -q [example.com]```"],["SYNOPSIS","**is-up** [_options_] _url_..."],["PARAMETERS","_URL_\n> Website URL or domain to check.\n\n**-q**, **--quiet**\n> Quiet mode, exit code only.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**is-up** checks if websites are accessible. It performs HTTP requests to verify site availability.\n\nThe tool returns success if the site responds. Useful for quick uptime checks and scripting."],["CAVEATS","Checks from your location only. Network issues affect results. Simple HTTP check."],["HISTORY","is-up was created as a simple command-line tool to quickly check if websites are reachable and responding."],["SEE ALSO","[curl](/man/curl)(1), [wget](/man/wget)(1), [ping](/man/ping)(8), [httpie](/man/http)(1)"]],"e":["curl","wget","ping","httpie"],"x":"is-up [example.com]"},"jj-duplicate":{"n":"jj-duplicate","t":"creates copies of revisions","s":[["TAGLINE","creates copies of revisions"],["TLDR","**Duplicate current revision**\n\n```jj duplicate```\n\n**Duplicate specific revision**\n\n```jj duplicate [revision]```\n\n**Duplicate multiple revisions**\n\n```jj duplicate [rev1] [rev2]```"],["SYNOPSIS","**jj** **duplicate** [_revisions_...]"],["DESCRIPTION","**jj duplicate** creates copies of revisions. The duplicated commits have identical content but new change IDs. Useful for creating variations or preserving commits before modification."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-new](/man/jj-new)(1)"]],"e":["jj","jj-new"],"x":"jj duplicate"},"jqp":{"n":"jqp","t":"TUI playground to experiment with jq","s":[["TAGLINE","TUI playground to experiment with jq"],["TLDR","**Open a JSON file** in the playground\n\n```jqp -f [path/to/file.json]```\n\n**Pipe JSON from stdin**\n\n```cat [path/to/file.json] | jqp```\n\n**Start with an initial query**\n\n```jqp -f [path/to/file.json] -q '.[0]'```\n\n**Use a specific theme**\n\n```jqp -f [path/to/file.json] -t [monokai]```\n\n**Use a custom config file**\n\n```jqp -f [path/to/file.json] --config [path/to/.jqp.yaml]```"],["SYNOPSIS","**jqp** [_query_] [_flags_]"],["PARAMETERS","**-f**, **--file** _FILE_\n> Path to the JSON or NDJSON input file.\n\n**-q**, **--query** _QUERY_\n> Initial jq query to execute on startup.\n\n**-t**, **--theme** _THEME_\n> Color theme name. Light themes work best with light terminal backgrounds, dark themes with dark backgrounds.\n\n**--config** _FILE_\n> Path to config file (default: **$HOME/.jqp.yaml**).\n\n**-h**, **--help**\n> Display help information.\n\n**-v**, **--version**\n> Display version information."],["DESCRIPTION","**jqp** is a TUI playground for exploring and experimenting with **jq** queries. It provides an interactive interface where you can type jq filters and see results update in real time against your JSON data. The tool internally uses **gojq**, a Go implementation of jq, and supports both JSON and **newline-delimited JSON** (NDJSON) input.\n\nFeatures include query history, auto-completion, customizable themes (light and dark), and the ability to start with a pre-defined query. Configuration can be provided via **$HOME/.jqp.yaml** or the **--config** flag; command-line options take precedence over the config file."],["CAVEATS","Large JSON files may impact the responsiveness of live preview updates. Input must be valid JSON or NDJSON."],["HISTORY","**jqp** was created by **Noah Gorstein** and is written in **Go**. It was designed as a terminal-native alternative to web-based jq playgrounds."],["INSTALL","```apt: sudo apt install jqp```\n\n```brew: brew install jqp```\n\n```nix: nix profile install nixpkgs#jqp```"],["SEE ALSO","[jq](/man/jq)(1), [jnv](/man/jnv)(1), [fx](/man/fx)(1)"]],"e":["jq","jnv","fx"],"x":"jqp -f [path/to/file.json]"},"k9s":{"n":"k9s","t":"terminal UI for Kubernetes","s":[["TAGLINE","terminal UI for Kubernetes"],["TLDR","**Start k9s**\n\n```k9s```\n\n**Start in namespace**\n\n```k9s -n [namespace]```\n\n**Start with specific context**\n\n```k9s --context [context-name]```\n\n**Start read-only**\n\n```k9s --readonly```\n\n**Show specific resource**\n\n```k9s --command [pods|deployments|services]```\n\n**List all clusters**\n\n```k9s --all-namespaces```"],["SYNOPSIS","**k9s** [_options_]"],["PARAMETERS","**-n** _NAMESPACE_\n> Start in namespace.\n\n**--context** _NAME_\n> Kubernetes context.\n\n**--readonly**\n> Read-only mode.\n\n**--command** _RESOURCE_\n> Initial resource view.\n\n**-A**, **--all-namespaces**\n> All namespaces.\n\n**--help**\n> Display help information."],["DESCRIPTION","**k9s** is a terminal UI for Kubernetes. It provides real-time cluster management with keyboard navigation.\n\nThe tool displays pods, deployments, services, and more. It supports logs, exec, and resource editing."],["CAVEATS","Requires kubeconfig. Terminal-based. Keyboard shortcuts to learn."],["HISTORY","k9s was created by **Fernand Galiana** to provide a fast, terminal-based interface for managing Kubernetes clusters."],["INSTALL","```dnf: sudo dnf install k9s```\n\n```pacman: sudo pacman -S k9s```\n\n```apk: sudo apk add k9s```\n\n```zypper: sudo zypper install k9s```\n\n```brew: brew install k9s```\n\n```nix: nix profile install nixpkgs#k9s```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [stern](/man/stern)(1), [kubectx](/man/kubectx)(1)"]],"e":["kubectl","stern","kubectx"],"x":"k9s"},"kdocker":{"n":"kdocker","t":"allows any application to be docked to the system tray","s":[["TAGLINE","allows any application to be docked to the system tray"],["TLDR","**Select a window to dock by clicking on it**\n\n```kdocker```\n\n**Launch an application and dock it to the tray**\n\n```kdocker [application]```\n\n**Dock the currently focused window**\n\n```kdocker -f```\n\n**Dock with a custom icon**\n\n```kdocker -i [/path/to/icon.png] [application]```\n\n**Launch an application, dock it, and minimize on start**\n\n```kdocker -o [application]```\n\n**Dock a window and minimize it when it loses focus**\n\n```kdocker -l [application]```"],["SYNOPSIS","**kdocker** [_options_] [_command_] [_args_]"],["PARAMETERS","**-f**\n> Dock the currently focused window.\n\n**-i** _ICON_\n> Use a custom icon for the docked application.\n\n**-l**\n> Minimize to tray when the window loses focus.\n\n**-m**\n> Keep application window mapped (visible); don't hide when minimizing.\n\n**-o**\n> Minimize to tray immediately on start.\n\n**-p** _SECONDS_\n> Wait the specified number of seconds before docking.\n\n**-t**\n> Remove the tray icon when the application window is closed.\n\n**-q**\n> Disable notification balloon messages.\n\n**-w** _WINDOW_ID_\n> Dock the window with the specified X11 window ID.\n\n**--version**\n> Display version information."],["DESCRIPTION","**kdocker** allows any application to be docked to the system tray. When an application is docked, clicking its tray icon toggles the window visibility, effectively providing system tray functionality to applications that don't natively support it.\n\nThe tool works by selecting a window (interactively via mouse click or automatically) and managing its visibility through the system tray. It works with both Qt and GTK applications on any X11 desktop environment."],["CAVEATS","Requires a system tray (most desktop environments provide one). Only works with X11; does not support Wayland. Behavior may vary with different window managers. Some applications may not dock correctly due to window management quirks. The **-p** delay option can help when applications take time to create their window."],["HISTORY","KDocker was originally a KDE-specific tool but evolved to work with any X11 desktop environment. It provides universal system tray functionality regardless of toolkit."],["INSTALL","```apt: sudo apt install kdocker```\n\n```zypper: sudo zypper install kdocker```\n\n```nix: nix profile install nixpkgs#kdocker```"],["SEE ALSO","[alltray](/man/alltray)(1), [trayer](/man/trayer)(1)"]],"e":["alltray","trayer"],"x":"kdocker"},"kiterunner-wordlist":{"n":"kiterunner-wordlist","t":"manages wordlists for Kiterunner API endpoint discovery","s":[["TAGLINE","manages wordlists for Kiterunner API endpoint discovery"],["TLDR","**List available wordlists**\n\n```kr wordlist list```\n\n**Download wordlist**\n\n```kr wordlist download [wordlist-name]```\n\n**Convert wordlist to kite format**\n\n```kr wordlist convert [input.txt] -o [output.kite]```"],["SYNOPSIS","**kr** **wordlist** _subcommand_ [_options_]"],["PARAMETERS","**list**\n> List available wordlists.\n\n**download** _name_\n> Download wordlist.\n\n**convert** _input_\n> Convert to kite format.\n\n**-o** _file_\n> Output file path."],["DESCRIPTION","**kr wordlist** manages wordlists for Kiterunner API endpoint discovery. Provides access to curated API-specific wordlists and tools to convert custom wordlists to the optimized kite format."],["INSTALL","```nix: nix profile install nixpkgs#kiterunner```"],["SEE ALSO","[kiterunner-brute](/man/kiterunner-brute)(1)"]],"e":["kiterunner-brute"],"x":"kr wordlist list"},"koji-build":{"n":"koji-build","t":"builds an RPM package using the Koji build system","s":[["TAGLINE","builds an RPM package using the Koji build system"],["TLDR","Build from **src.rpm**\n\n```koji build target path/to/package.src.rpm```\n\nBuild from **SCM URL**\n\n```koji build target git+https://src.fedoraproject.org/rpms/pkg.git#commit```\n\nPerform **scratch** build\n\n```koji build target path/to/package.src.rpm --scratch```\n\nBuild and **wait** for completion\n\n```koji build target path/to/package.src.rpm --wait```\n\nBuild without **waiting**\n\n```koji build target path/to/package.src.rpm --nowait```\n\nDisplay **help**\n\n```koji build --help```"],["SYNOPSIS","**koji build** [_options_] _target_ _srpm_|_scm_url_"],["DESCRIPTION","**koji build** builds an RPM package using the Koji build system. It can build from a source RPM or from a SCM (Source Code Management) URL pointing to a package repository."],["PARAMETERS","**target**\n> Build target defining the build environment\n\n**--scratch**\n> Perform a scratch build (not imported into repos)\n\n**--wait**\n> Wait for build completion even if running in background\n\n**--nowait**\n> Submit build and return immediately\n\n**--arch-override ARCHES**\n> Override architectures to build for\n\n**--repo-id REPO**\n> Use a specific repo for the build\n\n**--background**\n> Run build at a lower priority\n\n**-h, --help**\n> Display help information"],["CAVEATS","Scratch builds are temporary and not imported into Koji's package database. SCM URLs must use a specific format including the commit hash. Requires proper Koji credentials and permissions."],["HISTORY","**koji** is Fedora's build system, used for building and tracking packages across multiple architectures and releases."],["INSTALL","```dnf: sudo dnf install koji```\n\n```brew: brew install koji```\n\n```nix: nix profile install nixpkgs#koji```"],["SEE ALSO","[koji](/man/koji)(1), [rpmbuild](/man/rpmbuild)(8), [mock](/man/mock)(1)"]],"e":["koji","rpmbuild","mock"],"x":"koji build target path/to/package.src.rpm"},"logstash":{"n":"logstash","t":"Server-side data processing pipeline for ingesting, transforming, and shipping data","s":[["TAGLINE","Server-side data processing pipeline for ingesting, transforming, and shipping data"],["TLDR","**Start Logstash with a config file**\n\n```logstash -f [/path/to/config.conf]```\n\n**Test configuration syntax**\n\n```logstash -t -f [/path/to/config.conf]```\n\n**Start with inline config**\n\n```logstash -e 'input { stdin {} } output { stdout {} }'```\n\n**Start with config directory**\n\n```logstash -f [/etc/logstash/conf.d/]```\n\n**Start with specific pipeline settings**\n\n```logstash -f [config.conf] --pipeline.workers [4]```\n\n**Reload config automatically**\n\n```logstash -f [config.conf] --config.reload.automatic```\n\n**List installed plugins**\n\n```logstash-plugin list```\n\n**Install a plugin**\n\n```logstash-plugin install [logstash-filter-json]```"],["SYNOPSIS","**logstash** [_-f config_] [_-e string_] [_-t_] [_--pipeline.workers num_] [_options_]"],["PARAMETERS","**-f**, **--path.config** _PATH_\n> Path to config file or directory.\n\n**-e**, **--config.string** _CONFIG_\n> Inline configuration string.\n\n**-t**, **--config.test_and_exit**\n> Test configuration and exit.\n\n**-r**, **--config.reload.automatic**\n> Automatically reload config on changes.\n\n**--config.reload.interval** _SECONDS_\n> Config reload check interval.\n\n**-w**, **--pipeline.workers** _NUM_\n> Number of pipeline worker threads.\n\n**-b**, **--pipeline.batch.size** _NUM_\n> Events per batch.\n\n**-u**, **--pipeline.batch.delay** _MS_\n> Batch delay in milliseconds.\n\n**-l**, **--path.logs** _PATH_\n> Log file directory.\n\n**--log.level** _LEVEL_\n> Log level: fatal, error, warn, info, debug, trace.\n\n**--path.data** _PATH_\n> Data directory path.\n\n**--path.plugins** _PATH_\n> Custom plugins directory.\n\n**-n**, **--node.name** _NAME_\n> Node name for cluster identification.\n\n**--api.http.host** _HOST_\n> Monitoring API host (default: 127.0.0.1).\n\n**--api.http.port** _PORT_\n> Monitoring API port (default: 9600).\n\n**--path.settings** _DIR_\n> Directory containing logstash.yml.\n\n**-V**, **--version**\n> Display version.\n\n**-h**, **--help**\n> Display help."],["DESCRIPTION","**Logstash** is a server-side data processing pipeline that ingests data from multiple sources, transforms it, and sends it to various destinations. It is part of the Elastic Stack (ELK: Elasticsearch, Logstash, Kibana).\n\nConfiguration uses three sections: **input** (data sources like files, beats, syslog), **filter** (transformations like grok, mutate, geoip), and **output** (destinations like Elasticsearch, file, stdout). Configs use a Ruby-like DSL.\n\nThe grok filter is particularly powerful for parsing unstructured log data into structured fields using pattern matching. Common patterns for web logs, syslog, and other formats are included.\n\nMultiple pipelines can run simultaneously with separate configurations defined in `pipelines.yml`. Pipeline-to-pipeline communication allows complex routing. Persistent queues provide durability across restarts.\n\nPlugins extend functionality: hundreds of input, filter, and output plugins are available via `logstash-plugin`. The codec system handles data serialization (JSON, multiline, etc.)."],["CAVEATS","JVM-based with significant memory requirements (1GB+ heap typical). Startup is slow. Complex grok patterns can be CPU-intensive. Configuration errors only surface at startup or reload. Plugin compatibility varies with Logstash versions."],["HISTORY","**Logstash** was created by **Jordan Sissel** in **2009** as a tool for managing logs and events. Originally independent, it was acquired by **Elastic** (then Elasticsearch) in **2013** and became a core part of the ELK Stack. The project evolved from a simple log shipper to a full data processing pipeline, with major rewrites including the move to a persistent queue architecture."],["INSTALL","```brew: brew install logstash```\n\n```nix: nix profile install nixpkgs#logstash```"],["SEE ALSO","[filebeat](/man/filebeat)(1), [elasticsearch](/man/elasticsearch)(1)"]],"e":["filebeat","elasticsearch"],"x":"logstash -f [/path/to/config.conf]"},"mate-about":{"n":"mate-about","t":"Display information about the MATE desktop environment","s":[["TAGLINE","Display information about the MATE desktop environment"],["TLDR","**Open the MATE About dialog**\n\n```mate-about```\n\n**Display the MATE version string**\n\n```mate-about --version```"],["SYNOPSIS","**mate-about** [_options_]"],["PARAMETERS","**--version**\n> Print the MATE desktop environment version to the terminal and exit.\n\n**--gtk-module** _module_\n> Load an additional GTK module.\n\n**--display** _display_\n> X display to use.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mate-about** displays information about the MATE desktop environment, including the version number, credits, and licensing information. MATE is a fork of GNOME 2 that provides a traditional desktop experience for Linux and other Unix-like systems.\n\nWhen run without options, it opens a graphical dialog showing MATE version details, contributors, and license. The **--version** flag outputs just the version string to the terminal without opening the GUI."],["HISTORY","MATE was started in **2011** as a continuation of the GNOME 2 desktop after GNOME 3 shifted to a new interface paradigm. The name comes from yerba mate, a South American plant."],["INSTALL","```apt: sudo apt install mate-desktop```\n\n```dnf: sudo dnf install mate-desktop```\n\n```pacman: sudo pacman -S mate-desktop```\n\n```apk: sudo apk add mate-desktop```\n\n```zypper: sudo zypper install mate-desktop```\n\n```nix: nix profile install nixpkgs#mate-desktop```"],["SEE ALSO","[mate-about](/man/mate-about)(1)"]],"e":["mate-about"],"x":"mate-about"},"ncu":{"n":"ncu","t":"identifies outdated dependencies in package","s":[["TAGLINE","identifies outdated dependencies in package"],["TLDR","**Check for updates**\n\n```ncu```\n\n**Update package.json**\n\n```ncu -u```\n\n**Check specific packages**\n\n```ncu [lodash] [react]```\n\n**Check packages matching pattern**\n\n```ncu \"/^@types/\"```\n\n**Exclude packages**\n\n```ncu --reject [typescript]```\n\n**Interactive mode**\n\n```ncu -i```\n\n**Check only minor updates**\n\n```ncu --target minor```\n\n**Show peer dependencies**\n\n```ncu --peer```"],["SYNOPSIS","**ncu** [_-u_] [_-i_] [_--target level_] [_--reject pattern_] [_options_] [_packages_]"],["PARAMETERS","**-u**, **--upgrade**\n> Update package.json.\n\n**-i**, **--interactive**\n> Interactive mode.\n\n**-t**, **--target** _LEVEL_\n> Update target (latest, minor, patch, semver).\n\n**-f**, **--filter** _PATTERN_\n> Only include matching packages.\n\n**-x**, **--reject** _PATTERN_\n> Exclude matching packages.\n\n**-g**, **--global**\n> Check global packages.\n\n**-p**, **--packageManager** _PM_\n> Package manager (npm, yarn, pnpm).\n\n**--peer**\n> Check peer dependencies.\n\n**-d**, **--deep**\n> Check workspaces.\n\n**--pre**\n> Include prerelease versions.\n\n**--format** _FMT_\n> Output format.\n\n**--doctor**\n> Test upgrades one by one."],["DESCRIPTION","**ncu** (npm-check-updates) identifies outdated dependencies in package.json. It shows available updates without installing them.\n\nThe tool compares installed versions against npm registry. Output shows current version, wanted version, and latest version for each package.\n\nUpdate mode (-u) modifies package.json with new versions. Run npm install afterward to actually install updates.\n\nInteractive mode lets you select which packages to update. This is useful when upgrading incrementally or avoiding certain updates.\n\nTarget levels control update scope: patch allows only patch updates (1.0.x), minor allows minor (1.x.x), and latest allows any version.\n\nFilter and reject patterns focus on specific packages. Regex patterns match package names."],["CAVEATS","Updates package.json but doesn't install. Breaking changes possible with major updates. Test after updating. Doesn't check actual compatibility."],["HISTORY","**npm-check-updates** was created around **2014** to fill a gap in npm's update workflow. npm update installs updates but doesn't modify package.json, while ncu updates the file for explicit version control."],["SEE ALSO","[npm](/man/npm)(1), [yarn](/man/yarn)(1), [pnpm](/man/pnpm)(1), [npm-outdated](/man/npm-outdated)(1)"]],"e":["npm","yarn","pnpm","npm-outdated"],"x":"ncu"},"nethack":{"n":"nethack","t":"classic roguelike dungeon exploration game","s":[["TAGLINE","classic roguelike dungeon exploration game"],["TLDR","**Start new game**\n\n```nethack```\n\n**Start with specific role**\n\n```nethack -p [Wizard]```\n\n**Start with specific race**\n\n```nethack -r [Elf]```\n\n**Restore saved game**\n\n```nethack -u [playername]```\n\n**Show version**\n\n```nethack --version```"],["SYNOPSIS","**nethack** [_options_]"],["DESCRIPTION","**nethack** is a classic roguelike dungeon exploration game. Players descend through procedurally generated dungeons to retrieve the Amulet of Yendor, facing monsters, traps, and challenges.\n\nThe game features permadeath, ASCII graphics, and an incredibly deep gameplay system with countless interactions between items, monsters, and the environment."],["PARAMETERS","**-p** _role_\n> Character role/class.\n\n**-r** _race_\n> Character race.\n\n**-u** _name_\n> Player name.\n\n**-s**\n> Show high scores.\n\n**-n**\n> Start with no stripes (simpler display).\n\n**-D**\n> Debug/wizard mode."],["ROLES","Archeologist, Barbarian, Caveman, Healer, Knight, Monk, Priest, Ranger, Rogue, Samurai, Tourist, Valkyrie, Wizard"],["KEY COMMANDS","**hjkl/arrows**: Move\n**o**: Open door\n**c**: Close door\n**s**: Search\n**e**: Eat\n**q**: Quaff\n**r**: Read\n**w**: Wield\n**W**: Wear\n**T**: Take off\n**,**: Pick up\n**d**: Drop"],["CAVEATS","Steep learning curve. Permadeath means careful play needed. ASCII interface takes adjustment. Very deep mechanics to learn."],["HISTORY","**NetHack** evolved from **Hack** (1985), itself derived from **Rogue** (1980). The NetHack DevTeam has maintained it since **1987**, making it one of the longest-developed games. Its open development model pioneered collaborative game development."],["INSTALL","```dnf: sudo dnf install nethack```\n\n```pacman: sudo pacman -S nethack```\n\n```apk: sudo apk add nethack```\n\n```zypper: sudo zypper install nethack```\n\n```brew: brew install nethack```\n\n```nix: nix profile install nixpkgs#nethack```"],["SEE ALSO","[rogue](/man/rogue)(6), [angband](/man/angband)(6), [crawl](/man/crawl)(6), [frotz](/man/frotz)(1)"]],"e":["rogue","angband","crawl","frotz"],"x":"nethack"},"netpbm":{"n":"netpbm","t":"toolkit for image manipulation","s":[["TAGLINE","toolkit for image manipulation"],["TLDR","**Convert JPEG to PPM**\n\n```jpegtopnm [image.jpg] > [image.ppm]```\n\n**Convert PNG to PPM**\n\n```pngtopam [image.png] > [image.ppm]```\n\n**Convert PPM to JPEG**\n\n```pnmtojpeg [image.ppm] > [image.jpg]```\n\n**Resize image**\n\n```pamscale -width [800] [input.ppm] > [output.ppm]```\n\n**Flip image**\n\n```pamflip -lr [input.ppm] > [output.ppm]```"],["SYNOPSIS","**netpbm** [_tool_] [_options_] [_files_]"],["COMMON TOOLS","**jpegtopnm**\n> Convert JPEG to PNM format.\n\n**pngtopam**\n> Convert PNG to PAM format.\n\n**pnmtojpeg**\n> Convert PNM to JPEG format.\n\n**pnmtopng**\n> Convert PNM to PNG format.\n\n**ppmtogif**\n> Convert PPM to GIF format.\n\n**giftopnm**\n> Convert GIF to PNM format.\n\n**pamscale**\n> Scale (resize) images.\n\n**pamflip**\n> Flip or rotate images.\n\n**pnmcrop**\n> Crop whitespace from edges.\n\n**ppmbrighten**\n> Adjust brightness and saturation.\n\n**pnmcat**\n> Concatenate images side by side or vertically."],["DESCRIPTION","**netpbm** is a toolkit of over 350 separate programs for manipulation of graphic images, including conversion between about 100 graphics formats. The programs use PNM (Portable Any Map) and PAM as intermediate formats for pipeline-based image processing.\n\nThe tools are designed for command-line use and pipeline composition. A typical workflow converts an input format to PNM, applies transformations, then converts to an output format. Each tool does one thing, and they are most powerful when combined in shell pipelines."],["CAVEATS","Not an interactive tool. Requires converting to/from PNM/PAM intermediate formats. Individual tools have separate man pages. Some tools have been superseded by newer equivalents (e.g., pnmtopng replaces ppmtogif for many use cases)."],["HISTORY","Netpbm evolved from **PBMPLUS**, a pioneering image manipulation package created by Jef Poskanzer in 1988."],["INSTALL","```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[imagemagick](/man/imagemagick)(1), [convert](/man/convert)(1), [gimp](/man/gimp)(1)"]],"e":["imagemagick","convert","gimp"],"x":"jpegtopnm [image.jpg] > [image.ppm]"},"ocr-grab":{"n":"ocr-grab","t":"X11 region screenshot tool with instant Tesseract OCR to clipboard (Flameshot-like)","s":[["TAGLINE","X11 region screenshot tool with instant Tesseract OCR to clipboard (Flameshot-like)"],["TLDR","**Launch** the selector and OCR the chosen region to clipboard\n\n```ocr-grab```\n\n**Build** from source\n\n```make```"],["SYNOPSIS","**ocr-grab**"],["DESCRIPTION","OCR Grab is a lightweight C/GTK3 + Tesseract utility for X11. It lets you interactively select a screen region (with adjustment mode) and immediately runs OCR on the selection, copying the extracted text to the clipboard (with xclip fallback for minimal WMs).\n\nIt is designed as a fast, keyboard-driven complement or replacement for Flameshot-style workflows when the goal is to capture text rather than an image. There are no CLI flags beyond launching the interactive selector."],["USAGE","1. Bind `ocr-grab` to a hotkey (e.g. Mod+Print in i3).\n2. Drag to select a region.\n3. Adjust with mouse if needed (edges, corners, or move).\n4. Press Enter to OCR and copy, or Escape to cancel.\n\nA popup shows the result; \"Copy & Close\" puts it on both primary and selection clipboards."],["DEPENDENCIES","Build requires development headers for gtk+-3.0, tesseract, leptonica, zbar, and x11, plus a C compiler and make. Runtime language data (e.g. `tesseract-ocr-eng`) and optionally `xclip` for bare WMs."],["CAVEATS","- X11 only (no native Wayland support mentioned).\n- Requires tesseract language data (e.g. tesseract-ocr-eng).\n- GPL-3.0 licensed."],["SEE ALSO","[flameshot](/man/flameshot)(1), [tesseract](/man/tesseract)(1), [xclip](/man/xclip)(1)"],["RESOURCES","```[Source code](https://github.com/talalalrwas/ocr-grab)```\n\n```[Documentation](https://github.com/talalalrwas/ocr-grab#readme)```"]],"e":["flameshot","tesseract","xclip"],"x":"ocr-grab"},"pam_time":{"n":"pam_time","t":"PAM module for time-based access control","s":[["TAGLINE","PAM module for time-based access control"],["TLDR","**Enable time-based access** in a PAM service configuration\n\n```account required pam_time.so```\n\n**Allow login only during business hours** (Mon-Fri 08:00-17:00)\n\n```echo \"login ; * ; * ; Wk0800-1700\" >> /etc/security/time.conf```\n\n**Restrict SSH access** for a specific user to weekdays\n\n```echo \"sshd ; * ; john ; Wk0000-2400\" >> /etc/security/time.conf```\n\n**Deny console login on weekends** for all users\n\n```echo \"login ; tty* ; !root ; !Wd0000-2400\" >> /etc/security/time.conf```"],["SYNOPSIS","**pam_time.so** [_debug_] [_noaudit_]"],["PARAMETERS","**debug**\n> Log verbose debugging information via syslog.\n\n**noaudit**\n> Do not report login denials to the audit subsystem."],["CONFIGURATION","Rules in **/etc/security/time.conf** use the format:\n\n_services_ ; _ttys_ ; _users_ ; _times_\n\n**services**\n> PAM service names (e.g., login, sshd, su). Use * for all.\n\n**ttys**\n> Terminal names (e.g., tty1, pts/*). Use * for all.\n\n**users**\n> Usernames or groups. Prefix with ! to negate.\n\n**times**\n> Day/time ranges. Days: Mo Tu We Th Fr Sa Su Wk Wd Al. Times in HHMM-HHMM format. Prefix with ! to negate."],["DESCRIPTION","**pam_time** is a PAM module that restricts access to a system and/or specific applications at various times of the day and on specific days of the week. It does not authenticate the user but instead denies or allows access based on time rules defined in /etc/security/time.conf.\n\nRules are evaluated in order. Each rule specifies which services, terminals, and users are affected, along with permitted time windows. The module is typically used as an **account** type in PAM configuration."],["CAVEATS","The module only enforces restrictions at login time; it does not terminate active sessions when the allowed window expires. Lines in time.conf beginning with # are treated as comments. White space is ignored and lines can be extended with backslash continuation."],["HISTORY","**pam_time** is part of **Linux-PAM** (Pluggable Authentication Modules), providing time-based access control for system logins and services."],["SEE ALSO","[pam](/man/pam)(8), [pam_limits](/man/pam_limits)(8)"]],"e":["pam","pam_limits"],"x":"account required pam_time.so"},"paxs":{"n":"paxs","t":"meta-package manager that unifies operations across Yay, Flatpak, and Snap","s":[["TAGLINE","meta-package manager that unifies operations across Yay, Flatpak, and Snap"],["TLDR","**Search** for a package\n\n```paxs [search_term]```\n\n**Upgrade** all packages\n\n```paxs -u```\n\n**Install** a package (prompts for source)\n\n```paxs -i [package]```\n\n**Remove** a package\n\n```paxs -r [package]```\n\n**Check** for updates\n\n```paxs -c```\n\nDisplay **help**\n\n```paxs -h```"],["SYNOPSIS","**paxs** [**-u**|**--upgrade-all**] [**-i**|**--install** _pkg_] [**-r**|**--remove** _pkg_] [**-c**|**--check-update**] [_search_term_]"],["PARAMETERS","**-u, --upgrade-all**\n> Upgrade all packages across all package managers\n\n**-i, --install _package_**\n> Install package (prompts for source if available in multiple)\n\n**-r, --remove _package_**\n> Remove package (prompts for source if in multiple)\n\n**-c, --check-update**\n> Check for updates across all managers\n\n**-h, --help**\n> Display help information"],["DESCRIPTION","**paxs** is a meta-package manager that unifies operations across Yay (AUR), Flatpak, and Snap. It allows searching, installing, removing, and upgrading packages from multiple sources through a single interface.\n\nWhen a package exists in multiple sources, paxs prompts the user to select which source to use. This simplifies package management on systems using multiple package formats."],["CAVEATS","Requires yay, flatpak, and/or snap to be installed. Only manages packages through supported package managers. Some operations may require root privileges depending on the underlying manager."],["HISTORY","**paxs** was created by **zamhedonia** to address the complexity of managing multiple package managers on modern Linux systems. It provides a unified interface without replacing the underlying package managers."],["INSTALL","```aur: yay -S paxs```"],["SEE ALSO","[yay](/man/yay)(1), [flatpak](/man/flatpak)(1), [snap](/man/snap)(1), [pacman](/man/pacman)(8)"]],"e":["yay","flatpak","snap","pacman"],"x":"paxs [search_term]"},"pico2wave":{"n":"pico2wave","t":"text-to-speech synthesizer using the SVOX Pico engine","s":[["TAGLINE","text-to-speech synthesizer using the SVOX Pico engine"],["TLDR","**Convert text to a WAV file using default language (en-US)**\n\n```pico2wave -w [output.wav] \"[Hello world]\"```\n\n**Specify a language for synthesis**\n\n```pico2wave --lang [de-DE] -w [output.wav] \"[Hallo Welt]\"```\n\n**Synthesize French text**\n\n```pico2wave -l fr-FR -w [output.wav] \"[Bonjour le monde]\"```\n\n**Synthesize text read from a file**\n\n```pico2wave -w [output.wav] \"$(cat [input.txt])\"```\n\n**Synthesize and play immediately**\n\n```pico2wave -w /tmp/speech.wav \"[text]\" && aplay /tmp/speech.wav```"],["SYNOPSIS","**pico2wave** [_options_] **-w** _wavfile_ _text_"],["PARAMETERS","**-w**, **--wave** _file_\n> Output WAV file path (required).\n\n**-l**, **--lang** _lang_\n> Language code (default: en-US). See LANGUAGES section for supported values."],["DESCRIPTION","**pico2wave** is a text-to-speech synthesizer using the SVOX Pico engine. It converts text supplied on the command line into a WAV audio file. The output is always 16-bit mono PCM at 16 kHz. Six languages are supported."],["LANGUAGES","```\nen-US  - English (US)\nen-GB  - English (UK)\nde-DE  - German\nes-ES  - Spanish\nfr-FR  - French\nit-IT  - Italian\n```"],["CAVEATS","Only six languages are supported. Output format is WAV only; use ffmpeg or sox to convert to other formats. Part of the libttspico-utils package. Text must be passed as a command-line argument, not via stdin."],["HISTORY","Pico TTS was developed by **SVOX** and released as open source for Android, later packaged for Linux."],["INSTALL","```apk: sudo apk add picotts```\n\n```nix: nix profile install nixpkgs#picotts```"],["SEE ALSO","[espeak](/man/espeak)(1), [espeak-ng](/man/espeak-ng)(1), [flite](/man/flite)(1), [festival](/man/festival)(1), [aplay](/man/aplay)(1), [sox](/man/sox)(1)"]],"e":["espeak","espeak-ng","flite","festival","aplay","sox"],"x":"pico2wave -w [output.wav] \"[Hello world]\""},"pls":{"n":"pls","t":"Prettier and powerful ls for the pros","s":[["TAGLINE","Prettier and powerful ls for the pros"],["TLDR","**List the current directory**\n\n```pls```\n\n**List with detailed metadata**\n\n```pls -d```\n\n**List specific files or directories**\n\n```pls [path/to/directory] [path/to/file]```"],["SYNOPSIS","**pls** [_options_] [_paths_...]"],["PARAMETERS","**-d**, **--det** _COLS_\n> Display detailed file metadata. Columns can be freely ordered based on specification."],["DESCRIPTION","**pls** is a prettier and more powerful replacement for **ls** designed for human consumption rather than scripting. The \"p\" in pls stands for pretty, powerful, performant, practical, petite, pliable, and personable. It features Nerd Font icons, rich formatting, flexible column ordering, and a configurable display. Files and directories can be passed as positional arguments."],["CAVEATS","Requires a Nerd Font for icon rendering. Not intended for scripting — use **ls** for machine-readable output."],["HISTORY","**pls** is maintained by the **pls-rs** organization and is written in **Rust**."],["INSTALL","```nix: nix profile install nixpkgs#pls```"],["SEE ALSO","[ls](/man/ls)(1), [eza](/man/eza)(1), [lsd](/man/lsd)(1), [lla](/man/lla)(1)"]],"e":["ls","eza","lsd","lla"],"x":"pls"},"podman-load":{"n":"podman-load","t":"Load container images from archives","s":[["TAGLINE","Load container images from archives"],["TLDR","**Load image from archive**\n\n```podman load -i [image.tar]```\n\n**Load from stdin**\n\n```cat [image.tar] | podman load```\n\n**Load with new name**\n\n```podman load -i [archive.tar]```"],["SYNOPSIS","**podman load** [_options_]"],["PARAMETERS","**-i**, **--input** _FILE_\n> Input archive file.\n\n**-q**, **--quiet**\n> Suppress output."],["DESCRIPTION","**podman load** restores container images from tar archives previously created by podman save or docker save. Unlike podman import, it preserves all image layers, metadata, tags, and build history.\n\nThe archive can be read from a file with **-i** or piped from stdin. This is the standard way to transfer complete images between systems without using a registry."],["CAVEATS","Use with podman save archives. OCI format compatible."],["HISTORY","podman load provides **image archive** loading functionality."],["INSTALL","```apt: sudo apt install podman```\n\n```dnf: sudo dnf install podman```\n\n```pacman: sudo pacman -S podman```\n\n```apk: sudo apk add podman```\n\n```zypper: sudo zypper install podman```\n\n```brew: brew install podman```\n\n```nix: nix profile install nixpkgs#podman```"],["SEE ALSO","[podman](/man/podman)(1), [podman-save](/man/podman-save)(1), [podman-import](/man/podman-import)(1)"]],"e":["podman","podman-save","podman-import"],"x":"podman load -i [image.tar]"},"pridecat":{"n":"pridecat","t":"Display files with pride flag colors","s":[["TAGLINE","Display files with pride flag colors"],["TLDR","Print file in **pride colors**\n\n```pridecat file.txt```\n\nPrint in **trans** pride colors\n\n```pridecat --trans file.txt```\n\nPrint in **bisexual** pride colors\n\n```pridecat --bi file.txt```\n\nPrint in **lesbian** pride colors\n\n```pridecat --lesbian file.txt```\n\nApply colors to **background**\n\n```pridecat -b file.txt```\n\nPipe command output through **pridecat**\n\n```ls | pridecat --rainbow```"],["SYNOPSIS","**pridecat** [_options_] [_file_]"],["DESCRIPTION","**pridecat** is a colorful alternative to cat that displays file contents with pride flag colors. It supports various pride flags and can colorize both foreground text and backgrounds."],["PARAMETERS","**--rainbow**\n> Use rainbow pride flag colors (default)\n\n**--trans, --transgender**\n> Use transgender pride flag colors\n\n**--bi, --bisexual**\n> Use bisexual pride flag colors\n\n**--lesbian**\n> Use lesbian pride flag colors\n\n**--gay**\n> Use gay pride flag colors\n\n**--pan, --pansexual**\n> Use pansexual pride flag colors\n\n**--nb, --nonbinary**\n> Use non-binary pride flag colors\n\n**--ace, --asexual**\n> Use asexual pride flag colors\n\n**-b, --background**\n> Apply colors to background instead of text\n\n**-f, --force**\n> Force color output even when not a terminal\n\n**-h, --help**\n> Display help information"],["CAVEATS","Colors are applied as ANSI escape sequences. Output may not display correctly in terminals without color support. Multiple flag options can be combined to alternate between flags."],["HISTORY","**pridecat** was created as a fun, colorful alternative to cat, allowing users to display file contents decorated with pride flag colors."],["INSTALL","```nix: nix profile install nixpkgs#pridecat```"],["SEE ALSO","[cat](/man/cat)(1), [lolcat](/man/lolcat)(1)"]],"e":["cat","lolcat"],"x":"pridecat file.txt"},"pumas":{"n":"pumas","t":"Power usage monitor for Apple Silicon","s":[["TAGLINE","Power usage monitor for Apple Silicon"],["TLDR","**Launch the power monitor TUI**\n\n```sudo pumas```\n\n**Stream metrics as JSON**\n\n```sudo pumas --json```"],["SYNOPSIS","**pumas** [_options_]"],["PARAMETERS","**--json**\n> Stream metrics to stdout as JSON instead of running the TUI."],["DESCRIPTION","**pumas** is an nvtop-inspired command-line tool for monitoring power usage and performance metrics on Apple Silicon Macs. It runs in two modes: a TUI mode displaying global metrics for utilization and power consumption, and a JSON mode for piping metrics to tools like jq or building Prometheus exporters. It uses the macOS powermetrics utility and the sysinfo crate."],["CAVEATS","Requires **sudo** because the underlying powermetrics utility needs root access. Apple Silicon Macs only."],["HISTORY","**pumas** was created by **graelo** and is written in **Rust**."],["SEE ALSO","[htop](/man/htop)(1), [macmon](/man/macmon)(1), [nvtop](/man/nvtop)(1)"]],"e":["htop","macmon","nvtop"],"x":"sudo pumas"},"pw-dot":{"n":"pw-dot","t":"Visualize PipeWire graph as Graphviz DOT","s":[["TAGLINE","Visualize PipeWire graph as Graphviz DOT"],["TLDR","Generate a graph to **pw.dot** file\n\n```pw-dot```\n\nRead objects from **pw-dump JSON** file\n\n```pw-dot -j [path/to/file.json]```\n\nSpecify **output file** and show all object types\n\n```pw-dot -o [path/to/file.dot] -a```\n\nPrint graph to **stdout** with object properties\n\n```pw-dot -o - -d```\n\nGenerate graph from **remote instance** showing linked objects\n\n```pw-dot -r [remote_name] -s```\n\nLay graph from **left to right**\n\n```pw-dot -L```\n\nUse **90-degree angles** in edges\n\n```pw-dot -9```\n\nGenerate graph and **render to PNG** image\n\n```pw-dot && dot -Tpng pw.dot -o [pw-graph.png]```"],["SYNOPSIS","**pw-dot** [_options_]"],["PARAMETERS","**-o**, **--output** _file_\n> Output file (default: pw.dot, use - for stdout)\n\n**-a**, **--all**\n> Show all object types\n\n**-s**, **--smart**\n> Show only linked objects\n\n**-d**, **--detail**\n> Show all object properties\n\n**-r**, **--remote** _name_\n> Connect to remote PipeWire instance\n\n**-j**, **--json** _file_\n> Read objects from pw-dump JSON file\n\n**-L**, **--lr**\n> Lay graph left to right\n\n**-9**, **--90**\n> Use 90-degree angles for edges\n\n**-h**, **--help**\n> Display help message"],["DESCRIPTION","**pw-dot** generates Graphviz DOT files from the PipeWire graph, visualizing the connections between nodes, ports, and devices. The output can be rendered to images using the **dot** command from Graphviz.\n\nThis is useful for understanding audio/video routing, debugging connection issues, and documenting system audio configurations."],["CAVEATS","Requires Graphviz to render the DOT file to images. Complex graphs with many objects can be difficult to read; use **--smart** to show only connected objects."],["HISTORY","Part of **PipeWire**, providing visualization of the multimedia graph. Useful for developers, system integrators, and users troubleshooting audio routing."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pw-dump](/man/pw-dump)(1), [pw-cli](/man/pw-cli)(1), [pw-top](/man/pw-top)(1), [pw-mon](/man/pw-mon)(1), [pipewire](/man/pipewire)(1), [dot](/man/dot)(1)"]],"e":["pw-dump","pw-cli","pw-top","pw-mon","pipewire","dot"],"x":"pw-dot"},"pwgen":{"n":"pwgen","t":"Generate memorable or random passwords","s":[["TAGLINE","Generate memorable or random passwords"],["TLDR","**Generate a password**\n\n```pwgen```\n\n**Generate password** of specific length\n\n```pwgen [16]```\n\n**Generate multiple passwords**\n\n```pwgen [16] [5]```\n\n**Generate secure password** (harder to memorize)\n\n```pwgen -s [16]```\n\n**Generate with at least one symbol**\n\n```pwgen -y [16]```\n\n**Generate without ambiguous characters**\n\n```pwgen -B [16]```\n\n**Generate single password** (no columns)\n\n```pwgen -1 [16]```\n\n**Generate completely random**\n\n```pwgen -sy [20] [1]```"],["SYNOPSIS","**pwgen** [_options_] [_length_] [_count_]"],["DESCRIPTION","**pwgen** generates random, pronounceable passwords. By default, it creates passwords designed to be easily memorized by humans while still being reasonably secure.\n\nThe tool can generate different styles of passwords: pronounceable (default), completely random, with or without special characters, and with various constraints. It outputs multiple passwords allowing users to choose one they find memorable.\n\npwgen is commonly used for creating initial passwords, generating secrets for scripts, and batch password creation."],["PARAMETERS","**-s**, **--secure**\n> Generate completely random passwords.\n\n**-y**, **--symbols**\n> Include at least one special character.\n\n**-n**, **--numerals**\n> Include at least one number.\n\n**-c**, **--capitalize**\n> Include at least one capital letter.\n\n**-A**, **--no-capitalize**\n> No capital letters.\n\n**-0**, **--no-numerals**\n> No numbers.\n\n**-B**, **--ambiguous**\n> Avoid ambiguous characters (0O1lI).\n\n**-1**\n> Print one password per line.\n\n**-v**, **--no-vowels**\n> No vowels (avoid offensive words).\n\n**-H** _file_, **--sha1=** _file_\n> Use SHA1 hash of file as seed.\n\n**-r** _chars_\n> Remove specified characters."],["CAVEATS","Default pronounceable passwords are less secure than random ones. Generated passwords should still meet your security requirements. Use -s for high-security applications. Entropy depends on password length and character set."],["HISTORY","**pwgen** was originally written by **Theodore Ts'o** in **2001**. It was designed to create passwords that users could actually remember, reducing the tendency to write them down. The tool has remained popular for both personal and administrative password generation."],["INSTALL","```apt: sudo apt install pwgen```\n\n```dnf: sudo dnf install pwgen```\n\n```pacman: sudo pacman -S pwgen```\n\n```apk: sudo apk add pwgen```\n\n```zypper: sudo zypper install pwgen```\n\n```brew: brew install pwgen```\n\n```nix: nix profile install nixpkgs#pwgen```"],["SEE ALSO","[openssl](/man/openssl)(1), [mkpasswd](/man/mkpasswd)(1), [apg](/man/apg)(1), [pass](/man/pass)(1)"]],"e":["openssl","mkpasswd","apg","pass"],"x":"pwgen"},"pyrig":{"n":"pyrig","t":"Python project scaffolding and maintenance tool","s":[["TAGLINE","Python project scaffolding and maintenance tool"],["TLDR","**Initialize** a new project after adding pyrig as a dev dependency\n\n```uv add pyrig --dev && uv run pyrig init```\n\n**Sync** all managed config files and test skeletons\n\n```uv run pyrig sync```\n\n**Scaffold a new CLI command** for the project\n\n```uv run pyrig mk cmd [command_name]```\n\n**Scaffold a shared CLI command** (available across pyrig-runtime projects)\n\n```uv run pyrig mk cmd [command_name] --shared```\n\n**Interactively create a subclass** to override pyrig behavior\n\n```uv run pyrig mk subcls```\n\n**Create missing** `__init__.py` files\n\n```uv run pyrig mk inits```\n\n**Remove all** `__pycache__` directories\n\n```uv run pyrig rm pyc```\n\n**Remove pyrig** and its footprint from the project\n\n```uv run pyrig rm pyrig```"],["SYNOPSIS","**pyrig** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**pyrig** is an opinionated command-line tool that **rigs up** a Python project: it scaffolds a complete, working layout and keeps configuration, CLI entry points, tests, and CI/CD in sync as the project evolves. It is designed to sit on top of **uv** as the package manager and build tool, and requires **Python 3.12+** and **Git**.\n\nTypical workflow: create a project with **uv init**, add pyrig as a development dependency (**uv add pyrig --dev**), then run **pyrig init**. That command regenerates managed project files (leaving **pyproject.toml**), sets up linters, formatters, type checkers, git hooks, GitHub Actions workflows, a working CLI, and related tooling, then creates an initial commit. After that, **pyrig sync** keeps config files and mirror-test skeletons aligned with the source tree, and **pyrig mk** helpers scaffold new commands, subclasses, and local ignore files.\n\nCustomization is inheritance-based: every managed config and tool is backed by a Python class. Subclass and override methods (or generate stubs with **pyrig mk subcls**); pyrig discovers custom classes automatically. Optional plugins (for example PyPI publishing, Codecov, standalone executables) are ordinary packages added as dependencies and picked up by **init** and **sync**.\n\nThe CLI entry point is the console script **pyrig** (registered via **pyproject.toml**). In a uv-managed project you usually invoke it with **uv run pyrig**. Projects scaffolded by pyrig also get their own project CLI (for example **my-project version**)."],["COMMANDS","**init**\n> Full project initialization: delete existing managed config files (except **pyproject.toml**), regenerate the scaffold, and create an initial commit. Prefer an empty GitHub repo (no README/license commits) when integrating remote CI/CD.\n\n**sync**\n> Create or update all managed project files and mirror-test skeletons to match the current source tree and installed plugins.\n\n**scratch**\n> Run the project's **.scratch.py** file.\n\n**rm pyc**\n> Remove all **__pycache__** directories under the project.\n\n**rm pyrig**\n> Remove pyrig and its managed footprint from the project entirely.\n\n**mk cmd** _name_\n> Scaffold a new CLI command stub in the project's subcommands module. Use **--shared** to register a command available in every pyrig-runtime-based project in the environment.\n\n**mk inits**\n> Create any missing **__init__.py** files in the package tree.\n\n**mk local**\n> Create or update version-control-ignored local config files.\n\n**mk subcls**\n> Interactively scaffold a subclass of any pyrig class for overrides."],["PARAMETERS","**-h**, **--help**\n> Show help for pyrig or a subcommand (**pyrig** _command_ **--help**).\n\n**--shared**\n> With **mk cmd**: place the stub in shared subcommands so it is discovered across projects using pyrig-runtime in the same environment."],["CAVEATS","Requires **Python 3.12+**, **Git**, and **uv**. Installation is typically via **uv add pyrig --dev** (or **pip install pyrig** / **uv tool** / **pipx** for a global CLI); distro packages may not ship it.\n\n**pyrig init** is opinionated and destructive toward managed files other than **pyproject.toml** (including placeholders from **uv init**). Undoing the scaffold is more involved than dropping a dependency; use **pyrig rm pyrig** and see upstream drawbacks docs if you need a full exit path.\n\nFor full GitHub CI/CD, the remote repo should start empty and a fine-grained **REPO_TOKEN** (and any plugin-specific secrets) must be configured before the first push. Empty projects may fail health-check workflows until tests exist."],["SEE ALSO","[uv](/man/uv)(1), [poetry](/man/poetry)(1), [cookiecutter](/man/cookiecutter)(1), [copier](/man/copier)(1), [git](/man/git)(1), [python](/man/python)(1)"],["RESOURCES","```[Source code](https://github.com/Winipedia/pyrig)```\n\n```[Documentation](https://Winipedia.github.io/pyrig)```"]],"e":["uv","poetry","cookiecutter","copier","git","python"],"x":"uv add pyrig --dev && uv run pyrig init"},"qm-clone":{"n":"qm-clone","t":"Clone a Proxmox virtual machine","s":[["TAGLINE","Clone a Proxmox virtual machine"],["TLDR","**Copy** a virtual machine\n\n```qm clone 100 101```\n\nCopy using a **specific name**\n\n```qm clone 100 101 --name new_vm```\n\nCopy with a specific **description**\n\n```qm clone 100 101 --description \"Clone of VM 100\"```\n\nCreate a **full copy** of all disks\n\n```qm clone 100 101 --full```\n\nFull copy with specific **format** (qcow2, raw, vmdk)\n\n```qm clone 100 101 --full --format qcow2```\n\nCopy and add to a specific **pool**\n\n```qm clone 100 101 --pool pool_name```"],["SYNOPSIS","**qm clone** _vmid_ _newid_ [_options_]"],["PARAMETERS","**--name** _name_\n> Set name for the cloned VM\n\n**--description** _text_\n> Set description for the cloned VM\n\n**--full**\n> Create full copy of all disks instead of linked clones\n\n**--format** _format_\n> Target format for disk images (qcow2, raw, vmdk)\n\n**--pool** _name_\n> Add cloned VM to specified pool"],["DESCRIPTION","**qm clone** creates a copy of a virtual machine on QEMU/KVM in Proxmox VE. By default, it creates linked clones that share base images with the original. The --full option creates independent copies.\n\nCloning is useful for creating VM templates and quickly deploying multiple similar machines."],["CAVEATS","Linked clones depend on the original VM's disks. Full clones require more storage but are independent. The original VM should be stopped or have snapshots for consistent cloning."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools for virtual machine administration."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-create](/man/qm-create)(1), [qm-destroy](/man/qm-destroy)(1)"]],"e":["qm","qm-create","qm-destroy"],"x":"qm clone 100 101"},"readpe":{"n":"readpe","t":"Display information about PE executable files","s":[["TAGLINE","Display information about PE executable files"],["TLDR","Display **all information** about a PE file\n\n```readpe path/to/executable```\n\nDisplay **all headers** in a PE file\n\n```readpe --all-headers path/to/executable```\n\nDisplay **all sections** in a PE file\n\n```readpe --all-sections path/to/executable```\n\nDisplay a **specific header** (dos, coff, optional)\n\n```readpe --header dos path/to/executable```\n\nList all **imported functions**\n\n```readpe --imports path/to/executable```\n\nList all **exported functions**\n\n```readpe --exports path/to/executable```"],["SYNOPSIS","**readpe** [_options_] _file_"],["PARAMETERS","**--all-headers**\n> Display all headers present in the PE file\n\n**--all-sections**\n> Display all sections in the PE file\n\n**--header** _type_\n> Display specific header (dos, coff, optional)\n\n**--imports**\n> List imported functions\n\n**--exports**\n> List exported functions"],["DESCRIPTION","**readpe** displays information about PE (Portable Executable) files, the executable format used by Windows. It can analyze headers, sections, imports, and exports from EXE, DLL, and other PE files.\n\nThis tool is useful for binary analysis, reverse engineering, and understanding executable structure."],["CAVEATS","Only works with valid PE format files. Malformed or corrupted PE files may not parse correctly."],["HISTORY","Part of **pev**, the PE file analysis toolkit for Unix-like systems."],["INSTALL","```apt: sudo apt install readpe```\n\n```apk: sudo apk add pev```\n\n```zypper: sudo zypper install readpe```\n\n```brew: brew install readpe```\n\n```nix: nix profile install nixpkgs#pev```"],["SEE ALSO","[objdump](/man/objdump)(1), [readelf](/man/readelf)(1), [file](/man/file)(1)"]],"e":["objdump","readelf","file"],"x":"readpe path/to/executable"},"rename.util":{"n":"rename.util","t":"Rename files using string substitution","s":[["TAGLINE","Rename files using string substitution"],["TLDR","Rename files using **simple substitutions**\n\n```rename [foo] [bar] *```\n\n**Dry-run** - display which renames would occur without performing them\n\n```rename -v -n [foo] [bar] *```\n\nDo **not overwrite** existing files\n\n```rename -o [foo] [bar] *```\n\nChange **file extensions**\n\n```rename .ext .bak *.ext```\n\n**Prepend** text to all filenames\n\n```rename '' '[prefix]' *```\n\n**Zero-pad** numbered files\n\n```rename foo foo00 foo? && rename foo foo0 foo??```"],["SYNOPSIS","**rename** [_options_] _expression_ _replacement_ _file_..."],["PARAMETERS","**-v**, **--verbose**\n> Show which files are being renamed\n\n**-n**, **--no-act**\n> Dry run; show what would be renamed without doing it\n\n**-o**, **--no-overwrite**\n> Do not overwrite existing files\n\n**-a**, **--all**\n> Replace all occurrences, not just the first\n\n**-l**, **--last**\n> Replace the last occurrence instead of the first\n\n**-s**, **--symlink**\n> Rename symbolic link target instead of link itself\n\n**-i**, **--interactive**\n> Prompt before overwriting\n\n**-V**, **--version**\n> Display version information\n\n**-h**, **--help**\n> Display help message"],["DESCRIPTION","**rename** from util-linux performs batch file renaming using simple string substitution. It replaces the first occurrence of _expression_ with _replacement_ in each filename. Unlike the Perl rename command, it does not use regular expressions.\n\nThe command processes all matching files, making it efficient for bulk operations like changing extensions or adding prefixes. Multiple rename operations can be chained together for more complex transformations."],["CAVEATS","**Overwrites files without prompting** by default; use **-n** for dry runs and **-o** to prevent overwrites. Only replaces the first occurrence unless **-a** is specified. Does not support regular expressions; use Perl rename for complex patterns."],["HISTORY","Part of **util-linux**, the standard Linux utility collection. Provides simple string-based renaming as opposed to the more powerful (but complex) Perl-based rename found on Debian systems. The two commands are often confused due to sharing the same name."],["SEE ALSO","[mv](/man/mv)(1), [rename](/man/rename)(1), [mmv](/man/mmv)(1)"]],"e":["mv","rename","mmv"],"x":"rename [foo] [bar] *"},"rfc":{"n":"rfc","t":"Read IETF RFCs from the terminal","s":[["TAGLINE","Read IETF RFCs from the terminal"],["TLDR","**Open** an RFC by number\n\n```rfc [791]```\n\n**Search** RFCs by keyword (if supported by build)\n\n```rfc [search-term]```"],["SYNOPSIS","**rfc** *number*|*query*"],["DESCRIPTION","**rfc** fetches and displays IETF Request for Comments documents in the terminal, so you can read protocol specifications without a browser. Implementations commonly page through text via **$PAGER**. The widely packaged tool from **bfontaine/rfc** is a small shell wrapper around remote RFC text sources."],["PARAMETERS","*number*\n\n> RFC number to retrieve (e.g. **7231**).\n\nBehavior for keyword search, local caching, and offline mirrors depends on the version—run **rfc -h** or read the script header."],["CAVEATS","Requires network access unless a local RFC collection is configured. Formatting is plain text; some RFCs are better read as PDF/HTML on the IETF site."],["INSTALL","```pacman: sudo pacman -S rfc```\n\n```nix: nix profile install nixpkgs#rfc```"],["SEE ALSO","[curl](/man/curl)(1), [less](/man/less)(1), [man](/man/man)(1)"],["RESOURCES","```[Source code](https://github.com/bfontaine/rfc)```"]],"e":["curl","less","man"],"x":"rfc [791]"},"rpcbind":{"n":"rpcbind","t":"RPC port mapper service daemon","s":[["TAGLINE","RPC port mapper service daemon"],["TLDR","**Start rpcbind**\n\n```sudo rpcbind```\n\n**Start in foreground**\n\n```sudo rpcbind -f```\n\n**Debug mode**\n\n```sudo rpcbind -d```\n\n**Check status**\n\n```rpcinfo -p```"],["SYNOPSIS","**rpcbind** [_options_]"],["PARAMETERS","**-d**\n> Debug mode.\n\n**-f**\n> Foreground.\n\n**-w**\n> Warm start (restore state).\n\n**-l**\n> Logging.\n\n**-h** _host_\n> Bind to host.\n\n**-i** _interface_\n> Bind to interface."],["DESCRIPTION","**rpcbind** is the RPC port mapper daemon. It converts RPC program numbers to network addresses, enabling clients to find RPC services. Required for NFS and NIS.\n\nReplacement for the older portmap daemon."],["EXAMPLES","```bash"],["Start service","sudo systemctl start rpcbind"],["Check registered programs","rpcinfo -p"],["Check specific host","rpcinfo -p hostname"],["List NFS services","rpcinfo -p | grep nfs"],["Debug mode","sudo rpcbind -df\n```"],["RPC INFO","```bash"],["List all registered programs","rpcinfo -p localhost"],["Test specific program","rpcinfo -u localhost nfs"],["Show statistics","rpcinfo -m\n```"],["CAVEATS","Required for NFS. Security concerns - limit access with firewall. Port 111 TCP/UDP."],["HISTORY","rpcbind replaces the older **portmap** from Sun RPC, providing improved security and IPv6 support."],["INSTALL","```apt: sudo apt install rpcbind```\n\n```dnf: sudo dnf install rpcbind```\n\n```pacman: sudo pacman -S rpcbind```\n\n```apk: sudo apk add rpcbind```\n\n```zypper: sudo zypper install rpcbind```\n\n```nix: nix profile install nixpkgs#rpcbind```"],["SEE ALSO","[rpcinfo](/man/rpcinfo)(8)"]],"e":["rpcinfo"],"x":"sudo rpcbind"},"sdkmanager":{"n":"sdkmanager","t":"Install and manage Android SDK packages","s":[["TAGLINE","Install and manage Android SDK packages"],["TLDR","**List** available and installed packages\n\n```sdkmanager --list```\n\n**List** only installed packages\n\n```sdkmanager --list_installed```\n\n**Install** a platform package\n\n```sdkmanager \"platforms;android-35\"```\n\n**Install** multiple packages at once\n\n```sdkmanager \"platforms;android-35\" \"build-tools;35.0.0\"```\n\n**Update** all installed packages\n\n```sdkmanager --update```\n\n**Uninstall** a package\n\n```sdkmanager --uninstall \"build-tools;34.0.0\"```\n\n**Accept** all pending licenses non-interactively\n\n```yes | sdkmanager --licenses```\n\n**Install** packages using a specific SDK root and channel\n\n```sdkmanager --sdk_root=[path] --channel=1 \"platform-tools\"```"],["SYNOPSIS","**sdkmanager** [_--list_|_--list_installed_|_--update_|_--licenses_|_--uninstall_] [_options_] [_packages..._]"],["PARAMETERS","**--list**\n> List all available and installed packages.\n\n**--list_installed**\n> List only installed packages.\n\n**--update**\n> Update all installed packages to the latest versions.\n\n**--uninstall** _PACKAGES_\n> Remove specified packages.\n\n**--licenses**\n> Review and accept SDK licenses. Required before installing packages.\n\n**--sdk_root** _PATH_\n> Specify the SDK installation directory (overrides ANDROID_HOME).\n\n**--channel** _N_\n> Include packages from the specified channel: 0 (stable, default), 1 (beta), 2 (dev), 3 (canary).\n\n**--no_https**\n> Use HTTP instead of HTTPS for downloads.\n\n**--verbose**\n> Enable verbose output showing errors, warnings, and all messages.\n\n**--proxy** _TYPE_\n> Connect via a proxy of the given type (http or socks).\n\n**--proxy_host** _HOST_\n> IP or DNS address of the proxy.\n\n**--proxy_port** _PORT_\n> Proxy port number."],["DESCRIPTION","**sdkmanager** manages Android SDK components from the command line, installing, updating, and removing platform packages, build tools, system images, and other SDK components. It is the headless counterpart to Android Studio's SDK Manager and is provided in the Android SDK Command-Line Tools package.\n\nPackages are identified by string paths like **platforms;android-35** for platform APIs, **build-tools;35.0.0** for compilation tools, and **system-images;android-35;google_apis;x86_64** for emulator images. Multiple packages can be installed in a single command by separating them with spaces, each wrapped in quotes.\n\nLicense acceptance is required before installing packages. Running **--licenses** interactively accepts all pending licenses, which is essential for automated CI/CD setups where the SDK needs to be provisioned non-interactively."],["CONFIGURATION","**ANDROID_HOME**\n> Environment variable specifying the root directory of the Android SDK installation.\n\n**ANDROID_SDK_ROOT**\n> Alternative to ANDROID_HOME for specifying the SDK location (deprecated in favor of ANDROID_HOME)."],["CAVEATS","Part of Android SDK Command-Line Tools, which must be downloaded separately. Requires ANDROID_HOME or --sdk_root to be set. Package downloads can be large. If Android Studio is installed, the IDE's built-in SDK Manager can be used instead."],["INSTALL","```nix: nix profile install nixpkgs#sdkmanager```"],["SEE ALSO","[adb](/man/adb)(1), [avdmanager](/man/avdmanager)(1), [emulator](/man/emulator)(1), [gradle](/man/gradle)(1)"]],"e":["adb","avdmanager","emulator","gradle"],"x":"sdkmanager --list"},"sfdk-config":{"n":"sfdk-config","t":"Manage Sailfish SDK configuration settings","s":[["TAGLINE","Manage Sailfish SDK configuration settings"],["TLDR","**Show current configuration**\n\n```sfdk config```\n\n**Set target device**\n\n```sfdk config --device [device_name]```\n\n**Set build target**\n\n```sfdk config --target [target_name]```"],["SYNOPSIS","**sfdk config** [_options_]"],["PARAMETERS","**--device** _name_\n> Set target device.\n\n**--target** _name_\n> Set build target.\n\n**--specfile** _file_\n> Set spec file."],["DESCRIPTION","**sfdk config** manages Sailfish SDK configuration. Sets build targets, devices, and project options. Part of Sailfish SDK."],["SEE ALSO","[sfdk](/man/sfdk)(1), [sfdk-device](/man/sfdk-device)(1)"]],"e":["sfdk","sfdk-device"],"x":"sfdk config"},"sighthound":{"n":"sighthound","t":"Tree-sitter based static application security testing (SAST) scanner","s":[["TAGLINE","Tree-sitter based static application security testing (SAST) scanner"],["TLDR","**Scan** a project with auto-detected language and bundled rules\n\n```sighthound /path/to/project```\n\n**Run** a taint analysis scan and output JSON\n\n```sighthound --taint-analysis --output-format json /path/to/project > findings.json```\n\n**Emit SARIF** for GitHub Code Scanning\n\n```sighthound --output-format sarif /path/to/project > results.sarif```\n\n**Scan** with an explicit language and custom rules path\n\n```sighthound /path/to/project python rules/python```\n\n**Build** from source\n\n```cargo build --release```"],["SYNOPSIS","**sighthound** [options] <ROOT_DIR> [LANGUAGE] [RULES_PATH]"],["DESCRIPTION","Sighthound is a fast, AST-aware static vulnerability scanner. It supports pattern matching (\"search\" mode) and taint tracking (\"taint\" mode) across many languages using Tree-sitter parsers. Rules are written in RON and bundled by default for common languages (Python, JavaScript/TypeScript, Java, PHP, C#, Go, Ruby, HTML/Django templates).\n\nIt can run in parallel, produces text/JSON/CSV/SARIF output, and supports custom rule packs."],["PARAMETERS","**--taint-analysis**\n> Enable taint (source → sink) analysis in addition to or instead of pattern search.\n\n**--output-format** text|json|csv|sarif\n> Choose report format (default text). SARIF 2.1.0 is suitable for GitHub Code Scanning.\n\n**--help**\n> Show full option list.\n\nLanguage and custom rules path are optional positional arguments after the root directory."],["CAVEATS","- Some runtime-only or heavily dynamic issues will be missed.\n- Multi-file taint is supported but may need tuning for complex codebases.\n- C/C++ and Razor (`.cshtml`) are not currently supported.\n- Best used as part of a broader security program."],["SEE ALSO","[semgrep](/man/semgrep)(1)"],["RESOURCES","```[Source code](https://github.com/Corgea/Sighthound)```\n\n```[Homepage](https://corgea.com/sighthound)```\n\n```[Documentation](https://github.com/Corgea/Sighthound#readme)```"]],"e":["semgrep"],"x":"sighthound /path/to/project"},"sqlite-utils":{"n":"sqlite-utils","t":"SQLite database manipulation CLI and library","s":[["TAGLINE","SQLite database manipulation CLI and library"],["TLDR","**Import JSON into SQLite**\n\n```sqlite-utils insert [database.db] [table] [data.json]```\n\n**Import CSV into SQLite**\n\n```sqlite-utils insert [database.db] [table] [data.csv] --csv```\n\n**Query with SQL**\n\n```sqlite-utils [database.db] \"[SELECT * FROM table]\"```\n\n**Query and output as JSON**\n\n```sqlite-utils [database.db] \"[SELECT * FROM table]\" --json```\n\n**List tables**\n\n```sqlite-utils tables [database.db]```\n\n**Show schema**\n\n```sqlite-utils schema [database.db]```\n\n**Create FTS search index**\n\n```sqlite-utils enable-fts [database.db] [table] [column1] [column2]```\n\n**Export table to JSON**\n\n```sqlite-utils rows [database.db] [table] > [data.json]```\n\n**Upsert data** (insert or update on conflict)\n\n```sqlite-utils upsert [database.db] [table] [data.json] --pk [id]```\n\n**Process data from stdin** with in-memory database\n\n```cat [data.json] | sqlite-utils memory - \"[SELECT * FROM stdin]\"```"],["SYNOPSIS","**sqlite-utils** _command_ [_database_] [_options_] [_arguments_]"],["PARAMETERS","**insert** _DB_ _TABLE_ [_FILE_]\n> Insert data from JSON, CSV, or stdin.\n\n**rows** _DB_ _TABLE_\n> Output rows as JSON.\n\n**tables** _DB_\n> List tables.\n\n**schema** _DB_\n> Show database schema.\n\n**query** _DB_ _SQL_\n> Run SQL query.\n\n**upsert** _DB_ _TABLE_ [_FILE_]\n> Insert or update data (requires --pk).\n\n**memory** _SQL_\n> Run SQL against in-memory database (reads from files or stdin).\n\n**enable-fts** _DB_ _TABLE_ _COLUMNS_\n> Enable full-text search.\n\n**search** _DB_ _TABLE_ _QUERY_\n> Full-text search.\n\n**create-table** _DB_ _TABLE_ _COLUMNS_\n> Create table with columns.\n\n**drop-table** _DB_ _TABLE_\n> Drop table.\n\n**add-column** _DB_ _TABLE_ _COL_ [_TYPE_]\n> Add column to table.\n\n**indexes** _DB_ [_TABLE_]\n> List indexes.\n\n**--csv**\n> Input is CSV.\n\n**--tsv**\n> Input is TSV.\n\n**--nl**\n> Input is newline-delimited JSON.\n\n**--pk** _COLUMN_\n> Primary key column.\n\n**--json**\n> Output as JSON.\n\n**--table**\n> Output as table.\n\n**-c**, **--csv**\n> Output as CSV.\n\n**--flatten**\n> Flatten nested JSON objects into columns.\n\n**--batch-size** _N_\n> Number of rows per insert batch."],["DESCRIPTION","**sqlite-utils** provides a CLI and Python library for working with SQLite databases. It simplifies common tasks: importing data, running queries, and managing schema.\n\nData import handles JSON, CSV, TSV, and newline-delimited JSON. Types are inferred automatically. Primary keys and foreign keys can be specified. Existing tables are updated or replaced as configured.\n\nQuerying supports SQL with multiple output formats. The memory subcommand runs queries against in-memory databases, useful for quick data processing without persistent files.\n\nFull-text search (FTS) enables fast text searching. Enable-fts creates virtual tables for specified columns. Search queries use SQLite's FTS5 syntax for relevance ranking.\n\nTable management includes creation, alteration, and inspection. Schema command shows CREATE statements. Columns can be added without recreating tables.\n\nThe tool integrates well with Unix pipelines. Data flows in from curl or other tools, gets processed, and outputs to further commands."],["CAVEATS","Large inserts may need --batch-size adjustment. FTS increases database size. Type inference may not always be correct. Some features require SQLite version 3.25+. Memory usage grows with large datasets."],["HISTORY","**sqlite-utils** was created by **Simon Willison** around **2019** as part of the Datasette ecosystem. It provides command-line access to SQLite manipulation that previously required Python scripting or separate tools. The library is also usable from Python for programmatic database work."],["INSTALL","```brew: brew install sqlite-utils```\n\n```nix: nix profile install nixpkgs#sqlite-utils```"],["SEE ALSO","[sqlite3](/man/sqlite3)(1), [jq](/man/jq)(1), [datasette](/man/datasette)(1), [csvkit](/man/csvkit)(1)"]],"e":["sqlite3","jq","datasette","csvkit"],"x":"sqlite-utils insert [database.db] [table] [data.json]"},"squid":{"n":"squid","t":"Caching HTTP proxy server","s":[["TAGLINE","Caching HTTP proxy server"],["TLDR","**Start squid**\n\n```squid```\n\n**Check configuration**\n\n```squid -k parse```\n\n**Reload configuration**\n\n```squid -k reconfigure```\n\n**Shutdown gracefully**\n\n```squid -k shutdown```\n\n**Show version**\n\n```squid -v```\n\n**Run in foreground**\n\n```squid -N```\n\n**Debug mode**\n\n```squid -d [5]```\n\n**Clear cache**\n\n```squid -k rotate && rm -rf [/var/spool/squid/*] && squid -z```"],["SYNOPSIS","**squid** [_-k command_] [_-f config_] [_-N_] [_options_]"],["PARAMETERS","**-k** _COMMAND_\n> Send command to running squid.\n\n**-f** _FILE_\n> Configuration file.\n\n**-N**\n> No daemon mode.\n\n**-d** _LEVEL_\n> Debug level.\n\n**-z**\n> Create swap directories.\n\n**-v**\n> Show version."],["COMMANDS","**parse** - Check config\n**reconfigure** - Reload config\n**shutdown** - Graceful stop\n**interrupt** - Immediate stop\n**rotate** - Rotate logs"],["DESCRIPTION","**squid** is a caching proxy server supporting HTTP, HTTPS, FTP, and other protocols. It operates as both a forward proxy for client internet access and a reverse proxy (accelerator) for web servers, caching frequently requested content to reduce bandwidth usage and improve response times.\n\nThe server uses Access Control Lists (ACLs) to filter traffic, authenticate users, restrict access to specific sites or content types, and enforce bandwidth limits. Cache storage is configurable with both memory and disk backends, supporting hierarchical caching with parent and sibling proxies.\n\nConfiguration is managed through a comprehensive configuration file that defines listening ports, ACL rules, cache policies, authentication schemes, and logging behavior. The **-k** flag sends management commands to a running instance for operations like configuration reload, log rotation, and graceful shutdown."],["CONFIGURATION","**/etc/squid/squid.conf**\n> Main configuration file defining listening ports, ACL rules, cache policies, authentication helpers, and logging settings.\n\n**/var/spool/squid/**\n> Default disk cache directory for cached web content.\n\n**/var/log/squid/**\n> Log directory containing access.log, cache.log, and store.log."],["CAVEATS","Configuration is complex. ACL order matters. HTTPS interception needs certificates."],["HISTORY","**Squid** originated from the **Harvest** project at the **University of Colorado** in **1996**. It became the most widely deployed caching proxy."],["INSTALL","```dnf: sudo dnf install squid```\n\n```pacman: sudo pacman -S squid```\n\n```apk: sudo apk add squid```\n\n```zypper: sudo zypper install squid```\n\n```brew: brew install squid```\n\n```nix: nix profile install nixpkgs#squid```"],["SEE ALSO","[nginx](/man/nginx)(1), [apache](/man/apache)(1), [privoxy](/man/privoxy)(1)"]],"e":["nginx","apache","privoxy"],"x":"squid"},"sreport":{"n":"sreport","t":"Slurm cluster usage report generator","s":[["TAGLINE","Slurm cluster usage report generator"],["TLDR","Show **cluster utilization** (parsable format)\n\n```sreport -p cluster utilization```\n\nShow **number of jobs** run\n\n```sreport job sizes printjobcount```\n\nShow users with **highest CPU usage**\n\n```sreport user topusage```\n\nShow **account utilization** by user\n\n```sreport cluster AccountUtilizationByUser```\n\nSpecify **time period**\n\n```sreport cluster utilization Start=[YYYY-MM-DD] End=[YYYY-MM-DD]```\n\nShow for **specific cluster**\n\n```sreport -M [cluster_name] cluster utilization```"],["SYNOPSIS","**sreport** [_OPTIONS_...] [_COMMAND_...]"],["PARAMETERS","**-a, --all_clusters**\n> Report on all clusters, not just local\n\n**-M, --cluster** _names_\n> Specify cluster(s) for reports\n\n**-n, --noheader**\n> Omit header from output\n\n**-p, --parsable**\n> Pipe-delimited output with trailing pipe\n\n**-P, --parsable2**\n> Pipe-delimited output without trailing pipe\n\n**-t** _time_format_\n> Set time format for output (e.g., Seconds, Minutes, Hours, Percent)\n\n**-T, --tres** _tres_names_\n> Specify trackable resources to report\n\n**--federation**\n> Report for entire federation\n\n**--local**\n> Report local cluster only\n\n**-Q, --quiet**\n> Show errors only\n\n**-v, --verbose**\n> Verbose output\n\n**-V, --version**\n> Print version"],["REPORT TYPES","**cluster**\n> AccountUtilizationByUser, UserUtilizationByAccount, Utilization, WCKeyUtilizationByUser\n\n**job**\n> SizesByAccount, SizesByAccountAndWckey, SizesByWckey\n\n**reservation**\n> Utilization\n\n**user**\n> TopUsage"],["REPORT OPTIONS","**Start** _date_\n> Report period start (default: previous day 00:00)\n\n**End** _date_\n> Report period end (default: previous day 23:59)\n\n**Accounts** _list_\n> Filter by accounts\n\n**Users** _list_\n> Filter by users"],["DESCRIPTION","**sreport** generates usage and utilization reports from Slurm accounting data stored in slurmdbd. It produces reports on job statistics, user usage, cluster utilization, and reservation usage based on hourly, daily, and monthly rollup tables.\n\nReports help administrators understand cluster usage patterns, identify top users, and track resource consumption across accounts and projects."],["CAVEATS","Requires slurmdbd to be configured and running. Report accuracy depends on proper accounting configuration. Historical data availability depends on database retention settings. Time periods use cluster local time."],["HISTORY","**sreport** is part of **Slurm**, developed at **Lawrence Livermore National Laboratory** starting in **2002**. The accounting and reporting features help HPC centers track usage for billing, capacity planning, and fair-share scheduling. Slurm is now maintained by **SchedMD**."],["INSTALL","```apt: sudo apt install slurm-client```"],["SEE ALSO","[sacct](/man/sacct)(1), [sshare](/man/sshare)(1), [sacctmgr](/man/sacctmgr)(1), [slurmdbd](/man/slurmdbd)(8)"]],"e":["sacct","sshare","sacctmgr","slurmdbd"],"x":"sreport -p cluster utilization"},"srgn":{"n":"srgn","t":"Syntax-aware grep and code editing tool","s":[["TAGLINE","Syntax-aware grep and code editing tool"],["TLDR","**Replace matching text**\n\n```echo 'Hello World!' | srgn '[wW]orld' -- 'there'```\n\n**Convert piped input to uppercase**\n\n```echo 'hello' | srgn --upper '.*'```\n\n**Delete matching characters**\n\n```echo 'Hello World!' | srgn --delete '[!]'```\n\n**Squeeze repeated characters**\n\n```echo 'Helloooo Woooorld!!!' | srgn --squeeze '(o|!)'```\n\n**Search within Python class definitions**\n\n```srgn --python 'class' '[age]'```\n\n**Convert to lowercase within Rust strings only**\n\n```srgn --rust 'string' --lower '.*'```"],["SYNOPSIS","**srgn** [_options_] [_scope_] [**--** _replacement_]"],["PARAMETERS","**--upper**\n> Convert matched text to uppercase\n\n**--lower**\n> Convert matched text to lowercase\n\n**--delete**\n> Delete matched text\n\n**--squeeze**\n> Squeeze consecutive repeated characters into single occurrences\n\n**--python** _QUERY_\n> Scope to Python syntax nodes (e.g., class, function, string)\n\n**--rust** _QUERY_\n> Scope to Rust syntax nodes\n\n**--go** _QUERY_\n> Scope to Go syntax nodes\n\n**--typescript** _QUERY_\n> Scope to TypeScript syntax nodes\n\n**--c** _QUERY_\n> Scope to C syntax nodes\n\n**--csharp** _QUERY_\n> Scope to C# syntax nodes\n\n**--hcl** _QUERY_\n> Scope to HCL syntax nodes\n\n**--glob** _PATTERN_\n> Process files matching glob pattern"],["DESCRIPTION","**srgn** is a grep-like tool that understands source code syntax via tree-sitter and allows manipulation in addition to search. It combines capabilities of tr, sed, and ripgrep with language grammar awareness, supporting Python, Rust, Go, TypeScript, C, C#, and HCL. It can perform bulk file edits, case transformations, and regex replacements scoped to specific syntactic constructs.\n\nActions are composable and applied in a fixed order regardless of flag order. Replacement (positional, after **--**) is applied first, then flags like **--upper**, **--lower**, **--delete**, and **--squeeze** are applied in sequence."],["HISTORY","**srgn** was created by **Alex Povel** and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S srgn```\n\n```brew: brew install srgn```\n\n```nix: nix profile install nixpkgs#srgn```"],["SEE ALSO","[sed](/man/sed)(1), [tr](/man/tr)(1), [ripgrep](/man/ripgrep)(1), [ast-grep](/man/ast-grep)(1)"]],"e":["sed","tr","ripgrep","ast-grep"],"x":"echo 'Hello World!' | srgn '[wW]orld' -- 'there'"},"st":{"n":"st","t":"","s":[["TLDR","This command may refer to different tools. See documentation for:\n\n**st (terminal emulator)**\n\n```tldr st.1```"],["DESCRIPTION","The command **st** most commonly refers to the simple terminal emulator for X11 from the suckless.org project. See **st.1** for its documentation.\n\nThe name **st** is also used by some unrelated command-line statistics utilities for analyzing numeric data, but those are separate, less common tools."],["INSTALL","```apt: sudo apt install stterm```\n\n```dnf: sudo dnf install st```\n\n```apk: sudo apk add st```\n\n```zypper: sudo zypper install st```\n\n```brew: brew install st```\n\n```nix: nix profile install nixpkgs#st```"],["SEE ALSO","[st.1](/man/st.1)(1), [xterm](/man/xterm)(1), [urxvt](/man/urxvt)(1)"],["RESOURCES","```[Homepage](https://st.suckless.org/)```"]],"e":["st.1","xterm","urxvt"],"x":"tldr st.1"},"systemctl-log-target":{"n":"systemctl-log-target","t":"Get or set systemd log destination","s":[["TAGLINE","Get or set systemd log destination"],["TLDR","Show **current** log target\n\n```systemctl log-target```\n\n**Set** log target\n\n```systemctl log-target [journal-or-kmsg|journal|kmsg|console|syslog|null|auto]```"],["SYNOPSIS","**systemctl log-target** [_TARGET_]"],["LOG TARGETS","**journal-or-kmsg** - Journal if available, else kernel ring buffer\n\n**journal** - systemd journal only\n\n**kmsg** - Kernel ring buffer (dmesg)\n\n**console** - System console\n\n**syslog** - Traditional syslog\n\n**null** - Discard all logs\n\n**auto** - Automatic selection"],["DESCRIPTION","**systemctl log-target** gets or sets where the systemd service manager sends its log messages. Without arguments, it displays the current target. With a target argument, it redirects logging at runtime.\n\nThis controls where systemd's own messages go, separate from where individual service logs are sent."],["CAVEATS","Changes are temporary and reset on reboot. Console logging can slow the system and flood the display. Null target discards all systemd diagnostic output."],["HISTORY","The **log-target** subcommand provides runtime control over systemd's log destination, useful for debugging boot issues or redirecting logs in specific environments."],["SEE ALSO","[systemctl-log-level](/man/systemctl-log-level)(1), [journalctl](/man/journalctl)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-log-level","journalctl","systemctl"],"x":"systemctl log-target"},"tc":{"n":"tc","t":"Linux traffic control and shaping","s":[["TAGLINE","Linux traffic control and shaping"],["TLDR","Add constant **delay**\n\n```sudo tc qdisc add dev eth0 root netem delay 100ms```\n\nAdd **variable** delay\n\n```sudo tc qdisc add dev eth0 root netem delay 100ms 20ms```\n\nAdd packet **loss**\n\n```sudo tc qdisc add dev eth0 root netem loss 5%```\n\n**Limit** bandwidth\n\n```sudo tc qdisc add dev eth0 root tbf rate 10mbit burst 32kbit latency 400ms```\n\n**Show** active policies\n\n```tc qdisc show dev eth0```\n\n**Delete** all rules\n\n```sudo tc qdisc delete dev eth0```\n\n**Change** rule\n\n```sudo tc qdisc change dev eth0 root netem delay 50ms```"],["SYNOPSIS","**tc** [_OPTIONS_] _OBJECT_ _COMMAND_"],["DESCRIPTION","**tc** (traffic control) configures kernel packet scheduling, shaping, and filtering. It can simulate network conditions like latency, packet loss, and bandwidth limits for testing or quality of service management."],["PARAMETERS","**qdisc**\n> Queuing discipline - manage packet queuing\n\n**add**\n> Add a new traffic control policy\n\n**delete**\n> Remove traffic control policy\n\n**change**\n> Modify existing policy\n\n**show**\n> Display current policies\n\n**netem**\n> Network emulation (delay, loss, corruption)\n\n**tbf**\n> Token bucket filter (bandwidth limiting)\n\n**delay** _ms_\n> Add latency to packets\n\n**loss** _percent_\n> Drop percentage of packets\n\n**rate** _bandwidth_\n> Maximum bandwidth rate"],["CAVEATS","Affects outgoing traffic only. For bidirectional control, configure on both ends. Changes are immediate and can disrupt network connectivity. Not persistent across reboots."],["HISTORY","**tc** is part of **iproute2**, the Linux networking toolkit, providing advanced traffic shaping and network emulation."],["INSTALL","```apt: sudo apt install iproute2```\n\n```pacman: sudo pacman -S iproute2```\n\n```apk: sudo apk add iproute2-tc```\n\n```zypper: sudo zypper install iproute2```\n\n```brew: brew install iproute2```\n\n```nix: nix profile install nixpkgs#iproute2```"],["SEE ALSO","[ip](/man/ip)(8), [ethtool](/man/ethtool)(8)"]],"e":["ip","ethtool"],"x":"sudo tc qdisc add dev eth0 root netem delay 100ms"},"termgraph":{"n":"termgraph","t":"Draw graphs in the terminal","s":[["TAGLINE","Draw graphs in the terminal"],["TLDR","**Create bar chart from file**\n\n```termgraph [data.txt]```\n\n**Create chart with custom title**\n\n```termgraph --title \"[Monthly Sales]\" [data.txt]```\n\n**Create colored chart**\n\n```termgraph --color [red] [data.txt]```\n\n**Create vertical bar chart**\n\n```termgraph --vertical [data.txt]```\n\n**Create stacked bar chart**\n\n```termgraph --stacked [data.txt]```\n\n**Create histogram**\n\n```termgraph --histogram --bins [10] [data.txt]```\n\n**Customize chart width**\n\n```termgraph --width [80] [data.txt]```\n\n**Create chart from stdin**\n\n```cat [data.txt] | termgraph```"],["SYNOPSIS","**termgraph** [_options_] [_filename_]"],["PARAMETERS","**--title** _title_\n> Title displayed above the graph.\n\n**--width** _n_\n> Width of graph in characters (default: 50).\n\n**--format** _format_\n> Format specifier for values.\n\n**--suffix** _suffix_\n> String appended to all data points.\n\n**--no-labels**\n> Hide label column.\n\n**--no-values**\n> Hide values at end of bars.\n\n**--space-between**\n> Print newline after each row.\n\n**--color** _color_\n> Bar color(s): red, blue, green, magenta, yellow, black, cyan.\n\n**--vertical**\n> Display vertical bars.\n\n**--stacked**\n> Display stacked bar chart.\n\n**--histogram**\n> Display histogram from raw data.\n\n**--bins** _n_\n> Number of histogram bins.\n\n**--different-scale**\n> Use different scales per category.\n\n**--calendar**\n> Display calendar heatmap.\n\n**--start-dt** _date_\n> Start date for calendar chart.\n\n**--custom-tick** _char_\n> Custom tick mark character (emoji supported).\n\n**--delim** _char_\n> Custom delimiter (default: comma or space).\n\n**--verbose**\n> Enable verbose output."],["DESCRIPTION","**termgraph** is a Python command-line tool that draws basic graphs in the terminal. It supports bar charts, stacked charts, histograms, calendar heatmaps, and can display graphs horizontally or vertically.\n\nData is provided in a simple format with labels in the first column and numeric values in subsequent columns, separated by commas or spaces. Multiple value columns create grouped or stacked charts.\n\nThe tool supports emoji characters as tick marks, enabling creative visualizations. It can also be used as a Python library for programmatic chart generation.\n\nInstall via pip: `pip install termgraph`"],["CAVEATS","Requires Python 3. Terminal width limits chart size. Color support depends on terminal capabilities. Complex charts may not render correctly in narrow terminals."],["HISTORY","**termgraph** was created by Marcus Kazmierczak as a simple way to visualize data directly in the terminal. It provides a quick alternative to graphical charting tools for command-line workflows and scripts."],["SEE ALSO","[gnuplot](/man/gnuplot)(1), [spark](/man/spark)(1)"]],"e":["gnuplot","spark"],"x":"termgraph [data.txt]"},"unopkg":{"n":"unopkg","t":"LibreOffice extension package manager","s":[["TAGLINE","LibreOffice extension package manager"],["TLDR","**Add** extension\n\n```unopkg add [path/to/extension]```\n\n**Remove** extension\n\n```unopkg remove [extension_id]```\n\n**List** extensions\n\n```unopkg list```\n\nOpen **GUI** dialog\n\n```unopkg gui```\n\n**Reinstall** all extensions\n\n```unopkg reinstall```"],["SYNOPSIS","**unopkg** _COMMAND_ [_OPTIONS_] [_EXTENSION_]"],["COMMANDS","**add**\n> Add and deploy an extension\n\n**remove**\n> Remove an extension\n\n**list**\n> List deployed extensions\n\n**gui**\n> Open extensions dialog\n\n**reinstall**\n> Reinstall all deployed extensions"],["PARAMETERS","**-h, --help**\n> Display help information"],["DESCRIPTION","**unopkg** is the LibreOffice extensions manager. It allows installing, removing, and managing extensions from the command line. Extensions add functionality to LibreOffice applications.\n\nExtensions can be downloaded from the official LibreOffice Extensions website and installed using this tool."],["CAVEATS","Some extensions require LibreOffice restart. Extension IDs are needed for removal. GUI mode requires display server."],["INSTALL","```apk: sudo apk add libreoffice-common```"],["SEE ALSO","[libreoffice](/man/libreoffice)(1)"]],"e":["libreoffice"],"x":"unopkg add [path/to/extension]"},"update-grub":{"n":"update-grub","t":"Regenerate GRUB bootloader configuration","s":[["TAGLINE","Regenerate GRUB bootloader configuration"],["TLDR","This command is an alias for **grub-mkconfig --output /boot/grub/grub.cfg**.\n\n**View** documentation for the original command\n\n```tldr grub-mkconfig```"],["SYNOPSIS","**update-grub**"],["DESCRIPTION","**update-grub** is a convenience wrapper that generates a new GRUB configuration file. It is equivalent to running **grub-mkconfig -o /boot/grub/grub.cfg** and is commonly found on Debian-based distributions.\n\nThe command reads configuration from **/etc/default/grub** and scripts in **/etc/grub.d/** to generate the bootloader configuration."],["CAVEATS","Requires root privileges. The actual path may vary (**/boot/grub2/grub.cfg** on some systems). Always review changes to GRUB configuration carefully."],["INSTALL","```apt: sudo apt install grub2-common```\n\n```dnf: sudo dnf install grub2-common```\n\n```pacman: sudo pacman -S grub```\n\n```apk: sudo apk add grub```\n\n```zypper: sudo zypper install grub2-common```"],["SEE ALSO","[grub-mkconfig](/man/grub-mkconfig)(8), [grub-install](/man/grub-install)(8)"]],"e":["grub-mkconfig","grub-install"],"x":"tldr grub-mkconfig"},"vboxmanage-startvm":{"n":"vboxmanage-startvm","t":"Start VirtualBox virtual machines","s":[["TAGLINE","Start VirtualBox virtual machines"],["TLDR","**Start VM with GUI** (default)\n\n```vboxmanage startvm [vmname]```\n\n**Start headless** (no window, for remote display)\n\n```vboxmanage startvm [vmname] --type headless```\n\n**Start with separate** detachable frontend\n\n```vboxmanage startvm [vmname] --type separate```\n\n**Start VM by UUID**\n\n```vboxmanage startvm [670e746d-abea-4ba6-ad02-2a3b043810a5]```\n\n**Set environment variable** when starting\n\n```vboxmanage startvm [vmname] --putenv \"[DISPLAY=:0.0]\"```"],["SYNOPSIS","**vboxmanage** **startvm** _vmname|uuid_ [**--type** _type_] [**--putenv** _name_=_value_]"],["PARAMETERS","**--type** _type_\n> Frontend type: gui (default, shows window), headless (no window, remote display only), separate (detachable UI).\n\n**--putenv** _name_=_value_\n> Set an environment variable for the VM process."],["DESCRIPTION","**vboxmanage startvm** starts a virtual machine that is currently in the Powered Off or Saved state. The VM can be specified by name or UUID. Supports GUI mode (default), headless (no window), and separate (detachable GUI). Use headless for server VMs."],["SEE ALSO","[vboxmanage](/man/vboxmanage)(1), [vboxmanage-controlvm](/man/vboxmanage-controlvm)(1), [vboxmanage-list](/man/vboxmanage-list)(1), [vboxmanage-showvminfo](/man/vboxmanage-showvminfo)(1)"]],"e":["vboxmanage","vboxmanage-controlvm","vboxmanage-list","vboxmanage-showvminfo"],"x":"vboxmanage startvm [vmname]"},"vivaldi":{"n":"vivaldi","t":"Customizable Chromium-based web browser","s":[["TAGLINE","Customizable Chromium-based web browser"],["TLDR","**Start browser**\n\n```vivaldi```\n\n**Open URL**\n\n```vivaldi [https://example.com]```\n\n**New window**\n\n```vivaldi --new-window [url]```\n\n**Incognito mode**\n\n```vivaldi --incognito```\n\n**With profile**\n\n```vivaldi --profile-directory=\"[Profile 1]\"```"],["SYNOPSIS","**vivaldi** [_--new-window_] [_--incognito_] [_options_] [_url_]"],["PARAMETERS","**--new-window**\n> Open a new browser window.\n\n**--incognito**\n> Open a new window in private browsing mode.\n\n**--profile-directory** _DIR_\n> Use the specified profile directory.\n\n**--app** _URL_\n> Launch URL in app mode (minimal UI, no tabs or address bar).\n\n**--no-sandbox**\n> Disable the security sandbox.\n\n**--user-data-dir** _DIR_\n> Use a custom user data directory.\n\n**--help**\n> Show help."],["DESCRIPTION","**vivaldi** is a feature-rich, Chromium-based web browser designed for power users who want extensive control over their browsing experience. It supports modern web standards through its Chromium engine while adding a highly customizable user interface on top.\n\nThe browser includes advanced tab management features such as tab stacking, tab tiling for side-by-side viewing, and a tab bar that can be placed on any edge of the window. Built-in productivity tools include a notes panel, email client, calendar, feed reader, and translation service, reducing the need for separate applications or extensions.\n\nNearly every aspect of the interface can be customized, from keyboard shortcuts and mouse gestures to toolbar layouts and color themes. The browser also supports command chaining, custom search engines, and a web panel sidebar for keeping frequently used sites accessible."],["CAVEATS","Contains proprietary UI components on top of open-source Chromium. Supports most Chromium command-line switches. Custom flags can be placed in `~/.config/vivaldi-stable.conf`."],["HISTORY","**Vivaldi** was created by **Jon von Tetzchner**, co-founder of Opera, for power users wanting customization."],["INSTALL","```pacman: sudo pacman -S vivaldi```\n\n```nix: nix profile install nixpkgs#vivaldi```"],["SEE ALSO","[vivaldi-stable](/man/vivaldi-stable)(1), [firefox](/man/firefox)(1), [chromium](/man/chromium)(1), [google-chrome](/man/google-chrome)(1)"]],"e":["vivaldi-stable","firefox","chromium","google-chrome"],"x":"vivaldi"},"x509":{"n":"x509","t":"X.509 certificate management utility","s":[["TAGLINE","X.509 certificate management utility"],["TLDR","**Display certificate information**\n\n```openssl x509 -in [certificate.pem] -noout -text```\n\n**Display subject and issuer**\n\n```openssl x509 -in [certificate.pem] -noout -subject -issuer```\n\n**Display certificate dates** (validity period)\n\n```openssl x509 -in [certificate.pem] -noout -dates```\n\n**Display certificate fingerprint**\n\n```openssl x509 -in [certificate.pem] -noout -fingerprint -sha256```\n\n**Convert PEM to DER** format\n\n```openssl x509 -in [cert.pem] -outform DER -out [cert.der]```\n\n**Convert DER to PEM** format\n\n```openssl x509 -in [cert.der] -inform DER -out [cert.pem]```\n\n**Extract public key** from certificate\n\n```openssl x509 -in [certificate.pem] -noout -pubkey```\n\n**Self-sign a certificate request**\n\n```openssl x509 -req -in [request.csr] -signkey [key.pem] -out [certificate.pem]```"],["SYNOPSIS","**openssl x509** [**-in** _file_] [**-out** _file_] [**-inform** _DER_|_PEM_] [**-outform** _DER_|_PEM_] [**-noout**] [**-text**] [_options_]"],["PARAMETERS","**-in** _file_\n> Input certificate file. Reads from stdin if not specified.\n\n**-out** _file_\n> Output file. Writes to stdout if not specified.\n\n**-inform** _format_\n> Input format: DER or PEM (default).\n\n**-outform** _format_\n> Output format: DER or PEM (default).\n\n**-noout**\n> Prevent output of the encoded certificate.\n\n**-text**\n> Print certificate in human-readable text form.\n\n**-subject**\n> Print the certificate subject name.\n\n**-issuer**\n> Print the certificate issuer name.\n\n**-dates**\n> Print the notBefore and notAfter dates.\n\n**-serial**\n> Print the certificate serial number.\n\n**-fingerprint**\n> Print certificate fingerprint (use with -sha256, -sha1, etc.).\n\n**-pubkey**\n> Output the certificate's public key.\n\n**-req**\n> Input is a certificate request, not a certificate.\n\n**-signkey** _file_\n> Self-sign using the specified private key.\n\n**-days** _n_\n> Validity period in days when signing.\n\n**-CA** _file_\n> CA certificate to use for signing.\n\n**-CAkey** _file_\n> CA private key for signing.\n\n**-CAcreateserial**\n> Create a serial-number file for the CA if one does not already exist.\n\n**-hash**\n> Print a hash of the certificate subject (useful for naming trust files).\n\n**-checkend** _sec_\n> Exit 0 if certificate will not expire within _sec_ seconds, otherwise exit 1.\n\n**-purpose**\n> Print out intended certificate purposes."],["DESCRIPTION","**openssl x509** is a multi-purpose certificate utility that displays certificate information, converts between formats, signs certificate requests, and modifies trust settings.\n\nThe command operates on X.509 certificates in PEM or DER format. Common operations include viewing certificate details (subject, issuer, validity, extensions), verifying signatures, converting between formats, and certificate signing.\n\nWhen signing certificates (acting as a mini-CA), it can self-sign using **-signkey** or sign using a CA certificate and key with **-CA** and **-CAkey** options. The **-days** option specifies the validity period.\n\nThe command is typically invoked as **openssl x509** rather than standalone **x509**."],["CAVEATS","PEM format is the default and more common for certificate exchange. DER format is binary and cannot contain multiple certificates. When converting formats, ensure the correct -inform is specified. Self-signed certificates may not be trusted by browsers without manual exception."],["HISTORY","The **x509** command has been part of **OpenSSL** since its early releases in the late **1990s**. X.509 is an ITU-T standard for public key infrastructure, first published in **1988** and revised multiple times. The OpenSSL implementation provides comprehensive support for certificate manipulation and has become the standard tool for certificate operations on Unix-like systems."],["SEE ALSO","[openssl](/man/openssl)(1), [openssl-req](/man/openssl-req)(1), [openssl-verify](/man/openssl-verify)(1), [openssl-ca](/man/openssl-ca)(1)"]],"e":["openssl","openssl-req","openssl-verify","openssl-ca"],"x":"openssl x509 -in [certificate.pem] -noout -text"},"yadm-bootstrap":{"n":"yadm-bootstrap","t":"Execute the yadm bootstrap program","s":[["TAGLINE","Execute the yadm bootstrap program"],["TLDR","**Run the bootstrap script**\n\n```yadm bootstrap```"],["SYNOPSIS","**yadm** **bootstrap**"],["DESCRIPTION","**yadm bootstrap** executes the bootstrap program at `$HOME/.config/yadm/bootstrap` if it exists. This is typically used for post-clone setup such as installing packages, setting permissions, or configuring applications. The bootstrap script must be executable.\n\nAfter `yadm clone` successfully clones a repository, yadm will prompt whether to run the bootstrap program if one is found. This prompt can be bypassed with the `--bootstrap` or `--no-bootstrap` options to `yadm clone`.\n\nThe bootstrap program can be any executable file, though bash scripts are common. It is recommended to make the bootstrap logic idempotent so it can be safely re-run when merging changes from other hosts. The bootstrap path can be overridden with the `--yadm-bootstrap` option."],["INSTALL","```pacman: sudo pacman -S yadm```\n\n```apk: sudo apk add yadm```\n\n```zypper: sudo zypper install yadm```\n\n```brew: brew install yadm```\n\n```nix: nix profile install nixpkgs#yadm```"],["SEE ALSO","[yadm](/man/yadm)(1), [yadm-clone](/man/yadm-clone)(1)"]],"e":["yadm","yadm-clone"],"x":"yadm bootstrap"}});
