window.LCL_BUCKET(112,{"adb-disconnect":{"n":"adb-disconnect","t":"wireless Android device disconnector","s":[["TAGLINE","wireless Android device disconnector"],["TLDR","**Disconnect** from a specific device\n\n```adb disconnect [192.168.1.100]:5555```\n\nDisconnect from **all** wireless devices\n\n```adb disconnect```"],["SYNOPSIS","**adb disconnect** [_host_[:_port_]]"],["DESCRIPTION","**adb disconnect** terminates wireless adb connections. When called without arguments, it disconnects all connected TCP/IP devices. With a specific host and port, it disconnects only that device.\n\nThis command only affects wireless connections; USB-connected devices remain connected until physically unplugged."],["PARAMETERS","**host**\n> IP address or hostname of the device to disconnect\n\n**port**\n> TCP port number (default: 5555)"],["CAVEATS","Does not affect USB connections. The device remains in TCP/IP mode after disconnection; you can reconnect using **adb connect** without reconfiguration."],["HISTORY","**adb disconnect** has been available since wireless debugging was introduced in the Android SDK. It provides a clean way to terminate network connections without killing the entire adb server."],["INSTALL","```apt: sudo apt install adb```\n\n```dnf: sudo dnf install android-tools```\n\n```pacman: sudo pacman -S android-tools```\n\n```apk: sudo apk add android-tools-adb```\n\n```zypper: sudo zypper install android-tools```\n\n```nix: nix profile install nixpkgs#android-tools```"],["SEE ALSO","[adb](/man/adb)(1), [adb-connect](/man/adb-connect)(1), [adb-pair](/man/adb-pair)(1), [adb-kill-server](/man/adb-kill-server)(1)"],["RESOURCES","```[Source code](https://android.googlesource.com/platform/packages/modules/adb/)```\n\n```[Documentation](https://developer.android.com/tools/adb)```"]],"e":["adb","adb-connect","adb-pair","adb-kill-server"],"x":"adb disconnect [192.168.1.100]:5555"},"aurpublish":{"n":"aurpublish","t":"Publish packages to the Arch User Repository","s":[["TAGLINE","Publish packages to the Arch User Repository"],["TLDR","**Publish** a package to the AUR\n\n```aurpublish [package_name]```\n\n**Add githooks** to the current repository\n\n```aurpublish setup```"],["SYNOPSIS","**aurpublish** [_options_] [_package_]"],["DESCRIPTION","**aurpublish** streamlines publishing packages to the Arch User Repository. It verifies PKGBUILD integrity, generates **.SRCINFO** files, creates commit message templates, and handles the git push to the AUR.\n\nThe tool is designed for AUR package maintainers to simplify the update and publishing workflow."],["SUBCOMMANDS","**setup**\n> Install git hooks in the current repository for automated checks"],["PARAMETERS","**-h, --help**\n> Display help information"],["CAVEATS","Requires AUR SSH access to be configured. The PKGBUILD must pass validation before publishing. Existing **.SRCINFO** files will be regenerated automatically."],["INSTALL","```pacman: sudo pacman -S aurpublish```"],["SEE ALSO","[makepkg](/man/makepkg)(8), [git](/man/git)(1), [pacman](/man/pacman)(8)"]],"e":["makepkg","git","pacman"],"x":"aurpublish [package_name]"},"autoflake":{"n":"autoflake","t":"Remove unused imports and variables from Python code","s":[["TAGLINE","Remove unused imports and variables from Python code"],["TLDR","**Preview** changes as a diff without modifying the file\n\n```autoflake [file.py]```\n\n**Remove** all unused imports in place\n\n```autoflake --in-place --remove-all-unused-imports [file.py]```\n\nRemove unused imports recursively from **all Python files**\n\n```autoflake --in-place --remove-all-unused-imports -r [.]```\n\n**Check** without modifying and exit non-zero if changes are needed\n\n```autoflake --check [file.py]```\n\nRemove unused imports and **variables** in place\n\n```autoflake --in-place --remove-unused-variables --remove-all-unused-imports [file.py]```"],["SYNOPSIS","**autoflake** [_--in-place_] [_--remove-unused-variables_] [_options_] _files_"],["DESCRIPTION","**autoflake** removes unused imports and variables from Python code. It helps keep code clean by eliminating dead code automatically.\n\nThe tool uses static analysis to identify unused elements and can modify files in place or report issues."],["PARAMETERS","**-i**, **--in-place**\n> Modify files directly instead of printing a diff\n\n**-s**, **--stdout**\n> Write the result to standard output\n\n**--remove-unused-variables**\n> Remove unused variables\n\n**--remove-all-unused-imports**\n> Remove all unused imports, not just standard-library ones\n\n**--ignore-init-module-imports**\n> Leave imports in __init__.py files untouched\n\n**--expand-star-imports**\n> Expand wildcard imports (from x import *) when names can be resolved\n\n**--remove-duplicate-keys**\n> Remove duplicate keys in dictionary literals\n\n**--check**\n> Exit non-zero if changes are needed, without modifying files (CI mode)\n\n**--check-diff**\n> Like --check, but also print the diff of proposed changes\n\n**-r**, **--recursive**\n> Process directories recursively\n\n**-j** _n_, **--jobs** _n_\n> Number of parallel jobs (0 uses all CPU cores)\n\n**--exclude** _globs_\n> Exclude files and directories matching the comma-separated globs\n\n**--imports** _modules_\n> Comma-separated list of additional modules whose unused imports should be removed"],["CAVEATS","May incorrectly identify imports used only in type hints. Does not handle all edge cases. Review changes before committing. Works well with other formatters like black."],["HISTORY","**autoflake** was created to automate cleanup of unused Python imports, complementing code formatters and linters."],["INSTALL","```nix: nix profile install nixpkgs#autoflake```"],["SEE ALSO","[black](/man/black)(1), [isort](/man/isort)(1), [flake8](/man/flake8)(1)"],["RESOURCES","```[Source code](https://github.com/PyCQA/autoflake)```"]],"e":["black","isort","flake8"],"x":"autoflake [file.py]"},"aws-cur":{"n":"aws-cur","t":"Manage AWS Cost and Usage Report definitions.","s":[["TAGLINE","Manage AWS Cost and Usage Report definitions."],["TLDR","**List all report definitions** in the account\n\n```aws cur describe-report-definitions```\n\n**Create a new cost and usage report** delivered to S3\n\n```aws cur put-report-definition --report-definition ReportName=[my-report],TimeUnit=DAILY,Format=textORcsv,Compression=GZIP,AdditionalSchemaElements=RESOURCES,S3Bucket=[my-bucket],S3Prefix=[reports/],S3Region=[us-east-1]```\n\n**Delete a report definition** by name\n\n```aws cur delete-report-definition --report-name [my-report]```\n\n**Modify an existing report** definition\n\n```aws cur modify-report-definition --report-name [my-report] --report-definition ReportName=[my-report],TimeUnit=HOURLY,Format=Parquet,Compression=Parquet,AdditionalSchemaElements=RESOURCES,S3Bucket=[my-bucket],S3Prefix=[reports/],S3Region=[us-east-1]```\n\n**List tags** for a report resource\n\n```aws cur list-tags-for-resource --resource-arn [arn:aws:cur:us-east-1:123456789012:definition/my-report]```"],["SYNOPSIS","**aws cur** _command_ [_options_]"],["DESCRIPTION","**aws cur** is the AWS CLI interface for AWS Cost and Usage Reports, enabling programmatic management of billing report definitions. Cost and Usage Reports provide comprehensive cost data about your AWS usage, delivered to an S3 bucket in CSV or Parquet format.\n\nReports can be configured with different time granularities (hourly, daily, monthly) and include resource-level detail. The data can be integrated with analytics services like Athena, Redshift, or QuickSight for cost analysis."],["COMMANDS","**put-report-definition**\n> Create a new cost and usage report definition\n\n**describe-report-definitions**\n> List all report definitions in the account\n\n**modify-report-definition**\n> Update an existing report definition\n\n**delete-report-definition**\n> Delete a report definition\n\n**tag-resource**\n> Add tags to a report resource\n\n**untag-resource**\n> Remove tags from a report resource\n\n**list-tags-for-resource**\n> List tags for a report resource"],["CAVEATS","Reports can take up to 24 hours to appear in S3 after creation. The API endpoint (cur.us-east-1.amazonaws.com) is only available in the **us-east-1** region. In **--report-definition**, the fields ReportName, TimeUnit, Format, Compression, AdditionalSchemaElements, S3Bucket, S3Prefix and S3Region are all required; valid Format values are textORcsv and Parquet, valid Compression values are ZIP, GZIP and Parquet. Report data may be delayed and is not suitable for real-time cost monitoring. Legacy reports created in the console may not be visible via the API."],["HISTORY","AWS Cost and Usage Reports launched in **2015** as an evolution of the Detailed Billing Reports. The CUR API was added in **2017** to enable programmatic management. Parquet format support was introduced in **2018** for better integration with analytics tools, and resource-level data became available for more services over time."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-ce](/man/aws-ce)(1), [aws-budgets](/man/aws-budgets)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/cur/index.html)```"]],"e":["aws","aws-ce","aws-budgets"],"x":"aws cur describe-report-definitions"},"aws-route53":{"n":"aws-route53","t":"Manage DNS hosted zones and records","s":[["TAGLINE","Manage DNS hosted zones and records"],["TLDR","**List all hosted zones** in your account\n\n```aws route53 list-hosted-zones```\n\n**Get details** of a specific hosted zone\n\n```aws route53 get-hosted-zone --id [/hostedzone/ZONE_ID]```\n\n**List DNS records** in a hosted zone\n\n```aws route53 list-resource-record-sets --hosted-zone-id [ZONE_ID]```\n\n**Create a new hosted zone** for a domain\n\n```aws route53 create-hosted-zone --name [example.com] --caller-reference [unique-string]```\n\n**Test a DNS answer** for a specific record\n\n```aws route53 test-dns-answer --hosted-zone-id [ZONE_ID] --record-name [www.example.com] --record-type [A]```\n\n**List all health checks**\n\n```aws route53 list-health-checks```"],["SYNOPSIS","**aws route53** _subcommand_ [_options_]"],["DESCRIPTION","**aws route53** is the AWS CLI interface for Amazon Route 53, a highly available and scalable Domain Name System (DNS) web service. It provides commands for domain registration, DNS routing configuration, and health checking of resources.\n\nRoute 53 connects user requests to infrastructure running in AWS (such as EC2 instances, Elastic Load Balancers, or S3 buckets) and can also route users to infrastructure outside of AWS. The service offers DNS failover, geolocation routing, latency-based routing, and weighted round-robin routing."],["SUBCOMMANDS","**Hosted Zones**\n> create-hosted-zone, delete-hosted-zone, get-hosted-zone, list-hosted-zones, list-hosted-zones-by-name, list-hosted-zones-by-vpc, update-hosted-zone-comment\n\n**Resource Records**\n> change-resource-record-sets, list-resource-record-sets, test-dns-answer\n\n**Health Checks**\n> create-health-check, delete-health-check, get-health-check, get-health-check-status, list-health-checks, update-health-check\n\n**Traffic Policies**\n> create-traffic-policy, delete-traffic-policy, get-traffic-policy, list-traffic-policies, create-traffic-policy-instance, update-traffic-policy-instance\n\n**DNSSEC**\n> enable-hosted-zone-dnssec, disable-hosted-zone-dnssec, get-dnssec, create-key-signing-key, delete-key-signing-key\n\n**VPC Association**\n> associate-vpc-with-hosted-zone, disassociate-vpc-from-hosted-zone, list-vpc-association-authorizations\n\n**Query Logging**\n> create-query-logging-config, delete-query-logging-config, get-query-logging-config, list-query-logging-configs\n\n**CIDR Collections** (for IP-based routing)\n> create-cidr-collection, delete-cidr-collection, change-cidr-collection, list-cidr-collections, list-cidr-blocks, list-cidr-locations\n\n**Tagging / Misc**\n> list-tags-for-resource, list-tags-for-resources, change-tags-for-resource, get-checker-ip-ranges, wait"],["CAVEATS","DNS changes made via **change-resource-record-sets** are not instantaneous; propagation can take up to 60 seconds. Use **get-change** with the change ID to check propagation status. Hosted zone IDs must include the **/hostedzone/** prefix in some commands. Route 53 has API rate limits that may affect bulk operations."],["HISTORY","Amazon Route 53 was launched in **December 2010** as part of AWS's infrastructure services. The name is a reference to TCP/UDP port 53, where DNS server requests are addressed. The AWS CLI support for Route 53 was introduced with the AWS CLI tool and has been continuously expanded to support features like DNSSEC (2020), CIDR collections, and traffic policies."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-s3](/man/aws-s3)(1), [dig](/man/dig)(1), [nslookup](/man/nslookup)(1)"]],"e":["aws","aws-s3","dig","nslookup"],"x":"aws route53 list-hosted-zones"},"bookshelf":{"n":"bookshelf","t":"Terminal book library and ambient quotes for coding-agent sessions","s":[["TAGLINE","Terminal book library and ambient quotes for coding-agent sessions"],["TLDR","**Open** the interactive terminal library\n\n```bookshelf```\n\n**Show a quote** on demand\n\n```bookshelf quote```\n\n**Show a quote** tagged for an intent (e.g. refactor)\n\n```bookshelf quote --intent [refactor]```\n\n**Enable ambient quotes** every N completed agent turns\n\n```bookshelf ambient enable --cadence [5] --intent [refactor]```\n\n**Check ambient** status\n\n```bookshelf ambient status```\n\n**Disable ambient** mode\n\n```bookshelf ambient disable```\n\n**Send feedback** on the last quote\n\n```bookshelf feedback [up|down]```"],["SYNOPSIS","**bookshelf** [_subcommand_] [_options_]"],["PARAMETERS","_(no subcommand)_\n> Open the interactive TUI library (browse books, search, favorites, reading lists).\n\n**quote**\n> Print an on-demand quote. **--intent** _theme_ maps to local tags (e.g. refactor, debug) for relevance ranking.\n\n**ambient** **enable** | **disable** | **status**\n> Control ambient delivery for coding-agent Stop hooks. **enable --cadence** _N_ sets how many completed turns between quotes. **enable --intent** _theme_ stores the ambient theme.\n\n**feedback** **up** | **down**\n> Record local feedback on the last delivered quote."],["DESCRIPTION","**bookshelf** is a local-first Python CLI that surfaces book quotes inside coding-agent workflows (Codex, Claude Code, and related hosts) and provides a full terminal library for browsing. Ambient mode is optional and off by default: when enabled via host plugins/hooks, a short quote can appear every few completed turns. Quotes and relevance ranking stay on-device—no prompts, code, transcripts, or network calls for selection.\n\nThe interactive **bookshelf** TUI supports navigation (**j**/**k**, arrows), genre tabs, search (**/**), reading lists (**c**), random pick (**r**), favorites (**f**), read/want-to-read marks (**m**/**w**), help (**?**), and quit (**q**). Favorites, lists, statistics, and ambient settings live under the platform application-data directory named **bookshelf** (or **BOOKSHELF_DATA_HOME**).\n\nInstall with **pipx** from the GitHub repository (**Python 3.10+**, no runtime dependencies). Host integrations are available as Codex/Claude Code plugins and other agent marketplaces; those hooks can deliver ambient quotes without a global **bookshelf** binary. Package name on PyPI is planned as **ambient-bookshelf**."],["CONFIGURATION","**BOOKSHELF_AMBIENT_ENABLED**\n> Override ambient on/off for the process (**1**/**0** or **true**/**false**). Wins over saved config.\n\n**BOOKSHELF_AMBIENT_CADENCE**\n> Positive integer: completed turns between ambient quotes.\n\n**BOOKSHELF_DATA_HOME**\n> Redirect config and ambient state to another directory."],["CAVEATS","Ambient delivery depends on host plugin/hook support and fails closed when no safe bounded quote is available. Ambient lines are capped (about **220** UTF-8 bytes / **32** words); on-demand **quote** allows a wider display budget. Catalog entries vary in verification status—see the project's **DATA.md**. Intent-based ranking is deterministic tag mapping, not a claim of deep semantic relevance."],["HISTORY","Created by **Amal David** as an MIT-licensed Python project combining a terminal library with optional ambient quotes for AI coding agents. Integrates with Codex, Claude Code, and similar hosts via plugins and hooks."],["SEE ALSO","[claude](/man/claude)(1), [codex](/man/codex)(1)"],["RESOURCES","```[Source code](https://github.com/Amal-David/bookshelf)```\n\n```[Homepage](https://bookshelf-8dz.pages.dev/)```\n\n```[Documentation](https://github.com/Amal-David/bookshelf#readme)```"]],"e":["claude","codex"],"x":"bookshelf"},"bq":{"n":"bq","t":"Google BigQuery command-line interface","s":[["TAGLINE","Google BigQuery command-line interface"],["TLDR","**Query** data with standard SQL\n\n```bq query --use_legacy_sql=false \"SELECT * FROM [dataset.table] LIMIT 10\"```\n\n**List** datasets in the current project\n\n```bq ls```\n\n**List** tables in a dataset\n\n```bq ls [dataset]```\n\n**Show** table schema and metadata\n\n```bq show [dataset.table]```\n\n**Load** a CSV file into a table\n\n```bq load --source_format=CSV [dataset.table] [data.csv] [schema.json]```\n\n**Extract** a table to Cloud Storage\n\n```bq extract [dataset.table] gs://[bucket]/[file.csv]```\n\n**Estimate** the bytes a query would process without running it\n\n```bq query --dry_run --use_legacy_sql=false \"[SELECT ...]\"```"],["SYNOPSIS","**bq** [_options_] _command_"],["DESCRIPTION","**bq** is the Python-based command-line interface for Google BigQuery, shipped as part of the Google Cloud CLI (gcloud). It enables querying massive datasets, managing tables and datasets, loading data, and controlling access through a unified interface.\n\nResources are addressed as **PROJECT:DATASET.TABLE**; the project prefix can be omitted when a default project is configured. Commands take the form **bq COMMAND [FLAGS] [ARGUMENTS]**, where global flags (such as **--project_id**) work across commands and command-specific flags apply to a single command."],["PARAMETERS","**--project_id** _id_\n> Project ID\n\n**--dataset_id** _id_\n> Dataset ID\n\n**--format** _format_\n> Output format (json, csv, prettyjson)\n\n**--max_rows** _n_\n> Maximum rows to return\n\n**--use_legacy_sql=false**\n> Use GoogleSQL (standard SQL) instead of the legacy dialect. Recommended.\n\n**--location** _region_\n> Region or multi-region for the job or resource (for example US, EU, asia-northeast1).\n\n**--dry_run**\n> Validate the query and report bytes processed without executing it.\n\n**--apilog** _file_\n> Log API requests and responses to a file (use - for stdout) for debugging."],["COMMON COMMANDS","**query** _sql_\n> Execute SQL query\n\n**ls** [_dataset_]\n> List datasets or tables\n\n**show** _resource_\n> Show resource details\n\n**mk** _dataset_\n> Create dataset\n\n**rm** _resource_\n> Delete resource\n\n**load** _table_ _source_\n> Load data into table\n\n**extract** _table_ _destination_\n> Export table data\n\n**cp** _source_ _destination_\n> Copy table"],["WORKFLOW","```bash"],["Run query","bq query --use_legacy_sql=false \\\n  'SELECT name, count FROM `project.dataset.table` LIMIT 10'"],["Create dataset","bq mk --dataset project:mydataset"],["Create table","bq mk --table mydataset.mytable schema.json"],["Load CSV","bq load --source_format=CSV \\\n  mydataset.mytable \\\n  gs://bucket/data.csv \\\n  name:STRING,age:INTEGER"],["Export to GCS","bq extract mydataset.mytable gs://bucket/export.csv\n```"],["FEATURES","- Interactive and batch queries\n- Dataset and table management\n- Data loading from multiple sources\n- Data export to Cloud Storage\n- Access control management\n- Scheduled queries\n- Data transfer service"],["CONFIGURATION","**~/.bigqueryrc**\n> Default configuration file for default project, dataset, format, and per-command flag values. Override its location with **--bigqueryrc**."],["CAVEATS","Requires Google Cloud project and authentication. Queries cost based on data processed. Large exports require Cloud Storage. Legacy SQL vs Standard SQL differences. Query costs can accumulate quickly."],["HISTORY","**bq** was released by Google with BigQuery around **2011** as the primary command-line interface for the data warehouse service."],["SEE ALSO","[gcloud](/man/gcloud)(1), [gsutil](/man/gsutil)(1), [psql](/man/psql)(1)"],["RESOURCES","```[Homepage](https://cloud.google.com/bigquery/docs/bq-command-line-tool)```\n\n```[Documentation](https://cloud.google.com/bigquery/docs/reference/bq-cli-reference)```"]],"e":["gcloud","gsutil","psql"],"x":"bq query --use_legacy_sql=false \"SELECT * FROM [dataset.table] LIMIT 10\""},"brew-cask":{"n":"brew-cask","t":"Install and manage macOS GUI applications via Homebrew","s":[["TAGLINE","Install and manage macOS GUI applications via Homebrew"],["TLDR","**Install an application**\n\n```brew install --cask [firefox]```\n\n**List installed casks**\n\n```brew list --cask```\n\n**Search for a cask**\n\n```brew search --cask [application-name]```\n\n**Get information** about a cask\n\n```brew info --cask [firefox]```\n\n**Upgrade a cask**\n\n```brew upgrade --cask [firefox]```\n\n**Upgrade all casks** including self-updating apps\n\n```brew upgrade --cask --greedy```\n\n**Uninstall a cask**\n\n```brew uninstall --cask [firefox]```\n\n**Uninstall and remove** all associated files\n\n```brew uninstall --cask --zap [firefox]```\n\n**Install to custom directory**\n\n```brew install --cask --appdir=[~/Applications] [firefox]```"],["SYNOPSIS","**brew** [_--cask_] _command_ [_cask-name_] [_options_]"],["DESCRIPTION","**brew --cask** (formerly Homebrew Cask) manages the installation of macOS applications distributed as binary packages (.dmg, .pkg, .app). It automates downloading, installing to /Applications, and removing GUI applications.\n\nCask extends Homebrew's command-line workflow to desktop applications like browsers, editors, and utilities that would normally require manual drag-and-drop installation."],["COMMANDS","**install --cask**\n> Install a cask application\n\n**uninstall --cask**\n> Remove a cask application\n\n**upgrade --cask**\n> Upgrade installed casks\n\n**list --cask**\n> List installed casks\n\n**info --cask**\n> Show information about a cask\n\n**search --cask**\n> Search available casks\n\n**outdated --cask**\n> List casks with available updates"],["PARAMETERS","**--appdir** _path_\n> Install applications to specified directory (default: /Applications)\n\n**--fontdir** _path_\n> Install fonts to specified directory\n\n**--greedy**\n> Include self-updating apps in upgrade\n\n**--zap**\n> Remove all files associated with cask (including preferences)\n\n**--force**\n> Force install even if already installed\n\n**--no-quarantine**\n> Disable macOS quarantine attribute"],["CAVEATS","Many applications auto-update and are excluded from **brew upgrade** by default; use **--greedy** to include them. The **--zap** option may remove files shared with other applications. Some casks require administrator privileges for installation."],["HISTORY","Homebrew Cask was created as a separate project in **2012** by **phinze** (Alfred Perlstein) to extend Homebrew to macOS GUI applications. It was integrated into core Homebrew in **2020**, changing the command from **brew cask install** to **brew install --cask**."],["SEE ALSO","[brew](/man/brew)(1), [brew-bundle](/man/brew-bundle)(1)"]],"e":["brew","brew-bundle"],"x":"brew install --cask [firefox]"},"buku":{"n":"buku","t":"Command-line bookmark manager with tagging and search","s":[["TAGLINE","Command-line bookmark manager with tagging and search"],["TLDR","**Add** bookmark\n\n```buku -a [https://example.com] [tag1,tag2]```\n\n**Search** bookmarks\n\n```buku [keyword]```\n\n**List** all bookmarks\n\n```buku -p```\n\n**Open** bookmark\n\n```buku -o [index]```\n\n**Delete** bookmark\n\n```buku -d [index]```\n\n**Export** to HTML\n\n```buku -e [bookmarks.html]```"],["SYNOPSIS","**buku** [_options_] [_keywords_]"],["DESCRIPTION","**buku** is a powerful command-line bookmark manager. It stores bookmarks in a SQLite database with support for tagging, searching, automatic title fetching, and browser integration.\n\nBookmarks can be organized with comma-separated tags and searched using keywords, regular expressions, or tag filters. The tool automatically fetches page titles and descriptions when adding URLs. Import and export is supported in HTML, Markdown, and JSON formats, enabling migration from browsers and other bookmark managers. An optional web interface (**bukuserver**) is available for graphical access to the same database."],["PARAMETERS","**-a**, **--add** _url_ [_tags_]\n> Add bookmark\n\n**-u**, **--update** _index_\n> Update bookmark\n\n**-d**, **--delete** _index_\n> Delete bookmark\n\n**-p**, **--print** [_index_]\n> List bookmarks\n\n**-o**, **--open** _index_\n> Open in browser\n\n**-s**, **--sany** _keywords_\n> Search (any keyword)\n\n**-S**, **--sall** _keywords_\n> Search (all keywords)\n\n**-e**, **--export** _file_\n> Export to HTML\n\n**-i**, **--import** _file_\n> Import from HTML/Markdown\n\n**-t**, **--tags**\n> List all tags"],["FEATURES","- Automatic title fetching\n- Full-text search\n- Tag-based organization\n- Browser integration\n- Import/export (HTML, Markdown, JSON)\n- Encryption support\n- Regex search\n- Portable database"],["WORKFLOW","```bash"],["Add bookmark","buku -a https://github.com programming,git"],["Search","buku python"],["Browse all","buku -p"],["Open in browser","buku -o 5"],["Update tags","buku -u 5 --tag +docker,-kubernetes"],["Export","buku -e bookmarks.html\n```"],["BROWSER INTEGRATION","Can import from:\n- Chrome/Chromium\n- Firefox\n- Safari"],["CAVEATS","Requires internet for title fetching. Browser integration needs configuration. Database is local (not synced). Command-line interface may not suit all users. GUI available separately (bukuserver)."],["HISTORY","**buku** was created by Arun Prakash Jana around **2015** to provide a fast, privacy-focused bookmark manager for the terminal."],["INSTALL","```dnf: sudo dnf install buku```\n\n```apk: sudo apk add buku```\n\n```brew: brew install buku```\n\n```nix: nix profile install nixpkgs#buku```"],["SEE ALSO","[urlview](/man/urlview)(1), [qutebrowser](/man/qutebrowser)(1)"],["RESOURCES","```[Source code](https://github.com/jarun/buku)```\n\n```[Documentation](https://github.com/jarun/buku/wiki)```"]],"e":["urlview","qutebrowser"],"x":"buku -a [https://example.com] [tag1,tag2]"},"chromium-browser":{"n":"chromium-browser","t":"Open-source web browser with extensive command-line controls","s":[["TAGLINE","Open-source web browser with extensive command-line controls"],["TLDR","**Open URL**\n\n```chromium-browser [https://example.com]```\n\n**Open in incognito mode**\n\n```chromium-browser --incognito```\n\n**Open with remote debugging**\n\n```chromium-browser --remote-debugging-port=[9222]```\n\n**Disable GPU acceleration**\n\n```chromium-browser --disable-gpu```\n\n**Start headless**\n\n```chromium-browser --headless --screenshot [https://example.com]```\n\n**Use specific profile**\n\n```chromium-browser --user-data-dir=[/path/to/profile]```"],["SYNOPSIS","**chromium-browser** [_options_] [_URL_...]"],["DESCRIPTION","**chromium-browser** is the open-source web browser project that forms the foundation of Google Chrome, Microsoft Edge, and many other browsers. It provides a full-featured browsing experience with support for modern web standards, extensions, and developer tools.\n\nThe browser supports an extensive set of command-line switches for customization, debugging, and automation. Headless mode enables server-side rendering, automated testing, and screenshot/PDF generation without a graphical display. Remote debugging via DevTools Protocol allows external tools to inspect and control browser sessions.\n\nChromium can be configured to use specific user profiles, proxy servers, and experimental features. It is commonly used in CI/CD pipelines, web scraping, and automated testing environments alongside tools like Puppeteer and Playwright."],["PARAMETERS","**--incognito**\n> Start in incognito mode\n\n**--headless**\n> Run without UI\n\n**--disable-gpu**\n> Disable GPU hardware acceleration\n\n**--remote-debugging-port** _port_\n> Enable remote debugging\n\n**--user-data-dir** _dir_\n> Custom profile directory\n\n**--no-sandbox**\n> Disable sandbox (for containers)\n\n**--disable-extensions**\n> Disable extensions\n\n**--start-maximized**\n> Start maximized\n\n**--kiosk**\n> Start in kiosk mode\n\n**--proxy-server** _server_\n> Use proxy server\n\n**--enable-features** _features_\n> Enable experimental features\n\n**--disable-features** _features_\n> Disable features\n\n**--screenshot** _file_\n> Take screenshot (headless)\n\n**--print-to-pdf** _file_\n> Print page to PDF (headless)"],["HEADLESS USAGE","```\nchromium-browser --headless --print-to-pdf=page.pdf https://example.com\n```"],["CONFIGURATION","**~/.config/chromium/**\n> User profile directory containing preferences, bookmarks, history, and extensions.\n\n**~/.config/chromium/Default/Preferences**\n> JSON file with per-profile browser settings."],["CAVEATS","Command-line switches may change between versions. Check about:version for active switches. Use chrome://flags for experimental features."],["INSTALL","```dnf: sudo dnf install chromium```\n\n```pacman: sudo pacman -S chromium```\n\n```apk: sudo apk add chromium```\n\n```zypper: sudo zypper install chromium```\n\n```nix: nix profile install nixpkgs#chromium```"],["SEE ALSO","[google-chrome](/man/google-chrome)(1), [firefox](/man/firefox)(1), [chromium](/man/chromium)(1)"],["RESOURCES","```[Source code](https://chromium.googlesource.com/chromium/src/)```\n\n```[Homepage](https://www.chromium.org/Home/)```"]],"e":["google-chrome","firefox","chromium"],"x":"chromium-browser [https://example.com]"},"col":{"n":"col","t":"reverse line feed filter for text processing","s":[["TAGLINE","reverse line feed filter for text processing"],["TLDR","Filter **reverse line feeds** from input\n\n```command | col```\n\nOutput with **spaces** instead of tabs\n\n```command | col -x```\n\nRemove **backspaces**, show final character only\n\n```command | col -b```\n\nSet **buffer size** in lines\n\n```command | col -l 256```\n\nFormat **man page** for viewing\n\n```man ls | col -b | less```"],["SYNOPSIS","**col** [_options_]"],["DESCRIPTION","**col** filters reverse line feeds and half-reverse line feeds from input text, ensuring output displays in the correct order on terminals that cannot handle these control sequences. It's a text processing filter that resolves line feed conflicts and optimizes whitespace representation.\n\nThe utility is particularly useful for processing output from nroff, tbl, and other text formatting commands that use reverse line feeds for underlining and overstriking. By default, col converts sequences of spaces to tabs for efficiency, though this can be disabled with the -x flag.\n\nOriginally designed for printing formatted text on line printers and simple terminals, col remains useful for converting man page output to plain text, preprocessing formatted documents, and handling legacy text formats. It reads from standard input and writes cleaned output to standard output, making it suitable for Unix pipelines."],["PARAMETERS","**-b, --no-backspaces**\n> Do not output backspaces; display only the final character at each position\n\n**-f, --fine**\n> Enable half-forward line feeds (normally deferred to the following line)\n\n**-h, --tabs**\n> Convert multiple spaces to tab characters\n\n**-l, --lines NUM**\n> Set memory buffer to at least NUM lines (default: 128)\n\n**-p, --pass**\n> Allow unrecognized control sequences to pass through unchanged\n\n**-x, --spaces**\n> Output multiple spaces instead of tabs\n\n**-H, --help**\n> Display help information and exit\n\n**-V, --version**\n> Display version information and exit"],["CAVEATS","All unrecognized control characters and escape sequences are discarded by default. The utility warns if input attempts to back up past the last flushed line. Input is assumed to be encoded in UTF-8."],["HISTORY","**col** is part of the **util-linux** package, originally from BSD Unix. It was designed to process output from nroff for display on terminals that could not handle reverse line feeds."],["INSTALL","```apt: sudo apt install bsdextrautils```"],["SEE ALSO","[nroff](/man/nroff)(1), [tbl](/man/tbl)(1), [colcrt](/man/colcrt)(1), [expand](/man/expand)(1)"],["RESOURCES","```[Source code](https://github.com/util-linux/util-linux)```\n\n```[Documentation](https://man7.org/linux/man-pages/man1/col.1.html)```"]],"e":["nroff","tbl","colcrt","expand"],"x":"command | col"},"createdb":{"n":"createdb","t":"Create a new PostgreSQL database","s":[["TAGLINE","Create a new PostgreSQL database"],["TLDR","**Create a database with the default name (current user)**\n\n```createdb```\n\n**Create a database with a specific name**\n\n```createdb [dbname]```\n\n**Create a database with a specific owner**\n\n```createdb -O [owner] [dbname]```\n\n**Create a database with a specific encoding**\n\n```createdb -E [UTF8] [dbname]```\n\n**Create a clean database from template0**\n\n```createdb -T [template0] [dbname]```\n\n**Create a database on a remote server**\n\n```createdb -h [hostname] -p [5432] -U [user] [dbname]```\n\n**Show the SQL command being executed**\n\n```createdb -e [dbname]```"],["SYNOPSIS","**createdb** [_options_] [_dbname_] [_description_]"],["PARAMETERS","**-D** _tablespace_, **--tablespace** _tablespace_\n> Default tablespace for the database.\n\n**-E** _encoding_, **--encoding** _encoding_\n> Character encoding for the database.\n\n**-l** _locale_, **--locale** _locale_\n> Locale for the database.\n\n**-O** _owner_, **--owner** _owner_\n> Database owner user.\n\n**-T** _template_, **--template** _template_\n> Template database to copy from (default template1).\n\n**-e**, **--echo**\n> Echo the SQL commands that createdb sends to the server.\n\n**--icu-locale** _locale_\n> ICU locale for the database.\n\n**--icu-rules** _rules_\n> ICU collation rules for the database.\n\n**--locale-provider** _provider_\n> Locale provider (libc or icu).\n\n**--strategy** _strategy_\n> Database creation strategy (wal_log or file_copy).\n\n**-V**, **--version**\n> Print the createdb version and exit.\n\n**-h** _host_, **--host** _host_\n> Server hostname or socket directory.\n\n**-p** _port_, **--port** _port_\n> Server port number.\n\n**-U** _user_, **--username** _user_\n> User name to connect as.\n\n**-w**, **--no-password**\n> Never prompt for password.\n\n**-W**, **--password**\n> Force password prompt.\n\n**--maintenance-db** _db_\n> Database to connect to when creating the new database. Defaults to postgres, or template1 if postgres does not exist."],["DESCRIPTION","**createdb** creates a new PostgreSQL database. It is a convenience wrapper around the SQL **CREATE DATABASE** command, providing a command-line interface that is often more convenient than invoking psql directly.\n\nThe database name defaults to the current system user name if not specified. An optional description (comment) can be provided as the second argument."],["CAVEATS","Requires **CREATEDB** privilege or superuser role. The default template is template1; use template0 for a clean database without local additions. Connection parameters can also be set via the standard PostgreSQL environment variables (**PGHOST**, **PGPORT**, **PGUSER**, **PGDATABASE**)."],["HISTORY","**createdb** has been part of the PostgreSQL distribution since its early versions, providing a simple command-line interface to the **CREATE DATABASE** SQL command."],["INSTALL","```apk: sudo apk add postgresql17-client```"],["SEE ALSO","[dropdb](/man/dropdb)(1), [psql](/man/psql)(1), [pg_dump](/man/pg_dump)(1)"]],"e":["dropdb","psql","pg_dump"],"x":"createdb"},"deemix":{"n":"deemix","t":"download tool for Deezer music streaming service","s":[["TAGLINE","download tool for Deezer music streaming service"],["TLDR","**Download a track** by URL\n\n```deemix [https://www.deezer.com/track/12345]```\n\n**Download an album**\n\n```deemix [https://www.deezer.com/album/12345]```\n\n**Download a playlist**\n\n```deemix [https://www.deezer.com/playlist/12345]```\n\n**Specify output directory**\n\n```deemix -p [/path/to/music] [url]```\n\n**Download in specific** quality\n\n```deemix -b [flac] [url]```\n\n**Download multiple URLs** at once\n\n```deemix [url1] [url2]```\n\n**Use a portable config folder** in the current directory\n\n```deemix --portable [url]```"],["SYNOPSIS","**deemix** [_options_] _url_..."],["PARAMETERS","_URL_\n> Deezer URL for track, album, playlist, or artist. Multiple URLs can be given.\n\n**-p**, **--path** _DIR_\n> Output directory for downloads.\n\n**-b**, **--bitrate** _QUALITY_\n> Audio quality: 128, 320, flac.\n\n**--portable**\n> Create the config folder in the current directory instead of the user config directory.\n\n**--help**\n> Display help information."],["CONFIGURATION","**~/.config/deemix/.arl**\n> Stores the Deezer ARL authentication token.\n\n**~/.config/deemix/config.json**\n> Configuration file for download quality, folder structure, and other settings."],["DESCRIPTION","**deemix** is a download tool for Deezer music streaming service. It downloads tracks, albums, playlists, and artist discographies from Deezer with options for audio quality up to FLAC lossless format.\n\nThe tool requires authentication via an ARL token from a Deezer account; on first run it prompts for the token and stores it in the config folder. Downloaded files include metadata and album artwork. Output can be organized in configurable folder structures via config.json.\n\nWith the optional Spotify plugin configured, deemix can also resolve Spotify links to their Deezer equivalents."],["CAVEATS","Requires valid Deezer ARL token from active subscription. FLAC quality requires Deezer HiFi subscription. Downloading copyrighted content may violate terms of service. ARL tokens expire periodically."],["HISTORY","deemix was developed by **RemixDev** as a continuation of earlier Deezer download tools like deezloader. The original author stopped development in **2022**; community forks continue to maintain the codebase."],["INSTALL","```aur: yay -S deemix```"],["SEE ALSO","[spotdl](/man/spotdl)(1), [youtube-dl](/man/youtube-dl)(1), [yt-dlp](/man/yt-dlp)(1)"],["RESOURCES","```[Source code](https://gitlab.com/RemixDev/deemix-py)```"]],"e":["spotdl","youtube-dl","yt-dlp"],"x":"deemix [https://www.deezer.com/track/12345]"},"dnsenum":{"n":"dnsenum","t":"DNS enumeration and reconnaissance tool","s":[["TAGLINE","DNS enumeration and reconnaissance tool"],["TLDR","**Enumerate DNS information** for domain\n\n```dnsenum [domain.com]```\n\n**Use specific DNS** server\n\n```dnsenum --dnsserver [8.8.8.8] [domain.com]```\n\n**Enumerate with brute** force\n\n```dnsenum --enum -f [wordlist.txt] [domain.com]```\n\n**Save output to file**\n\n```dnsenum -o [output.xml] [domain.com]```\n\n**Specify threads** for brute force\n\n```dnsenum --threads [10] [domain.com]```\n\n**Skip reverse** lookups\n\n```dnsenum --noreverse [domain.com]```"],["SYNOPSIS","**dnsenum** [_options_] _domain_"],["PARAMETERS","_DOMAIN_\n> Target domain to enumerate.\n\n**--dnsserver** _SERVER_\n> DNS server to query.\n\n**--enum**\n> Shortcut equivalent to --threads 5 -s 15 -w.\n\n**-f** _FILE_\n> Subdomain wordlist for brute force.\n\n**--threads** _N_\n> Number of threads for parallel queries.\n\n**-o** _FILE_\n> Write output to FILE in XML (magictree) format.\n\n**--noreverse**\n> Skip reverse lookups.\n\n**--private**\n> Show and save private (RFC 1918) IP addresses.\n\n**-p** _N_, **--pages** _N_\n> Number of Google search pages to scrape (default 5).\n\n**-s** _N_, **--scrap** _N_\n> Maximum number of subdomains scraped from Google.\n\n**-r**, **--recursion**\n> Recurse on discovered subdomains.\n\n**-w**, **--whois**\n> Perform whois queries on class C network ranges.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dnsenum** is a DNS enumeration tool that gathers information about a domain's DNS infrastructure. It performs multiple queries to discover hosts, name servers, mail servers, and potential subdomains.\n\nThe tool attempts zone transfers, performs reverse lookups, and can brute force subdomains using wordlists. It also scrapes search engines for additional subdomain information and integrates results from multiple sources.\n\ndnsenum is commonly used in penetration testing and security assessments to map an organization's DNS footprint and identify potential attack vectors."],["CAVEATS","Only perform on domains you have authorization to test. Zone transfers typically blocked on properly configured servers. Brute forcing may be detected and blocked. Search scraping may hit rate limits."],["HISTORY","dnsenum is a Perl-based tool developed for the security community. It became a standard reconnaissance tool in penetration testing distributions like Kali Linux, providing comprehensive DNS enumeration capabilities."],["INSTALL","```dnf: sudo dnf install dnsenum```\n\n```apk: sudo apk add dnsenum```\n\n```nix: nix profile install nixpkgs#dnsenum```"],["SEE ALSO","[dig](/man/dig)(1), [nslookup](/man/nslookup)(1), [fierce](/man/fierce)(1), [dnsrecon](/man/dnsrecon)(1)"],["RESOURCES","```[Source code](https://github.com/fwaeytens/dnsenum)```"]],"e":["dig","nslookup","fierce","dnsrecon"],"x":"dnsenum [domain.com]"},"docker-container-start":{"n":"docker-container-start","t":"start one or more stopped containers","s":[["TAGLINE","start one or more stopped containers"],["TLDR","**Start a stopped container**\n\n```docker container start [container]```\n\n**Start multiple containers**\n\n```docker container start [container1] [container2]```\n\n**Start with attached output**\n\n```docker container start -a [container]```\n\n**Start interactively**\n\n```docker container start -ai [container]```"],["SYNOPSIS","**docker** **container** **start** [_options_] _container_ [_container..._]"],["PARAMETERS","**-a**, **--attach**\n> Attach STDOUT/STDERR.\n\n**-i**, **--interactive**\n> Attach STDIN."],["DESCRIPTION","**docker container start** starts one or more stopped containers, resuming them from their previous state. Unlike **docker container run**, which creates a new container, this command operates on existing containers that have been stopped.\n\nThe container resumes with its original configuration, including environment variables, volumes, network settings, and the command that was initially specified. This makes it useful for restarting services or resuming work in development containers."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-start](/man/docker-start)(1)"]],"e":["docker-start"],"x":"docker container start [container]"},"docker-image-save":{"n":"docker-image-save","t":"save images to tar archives","s":[["TAGLINE","save images to tar archives"],["TLDR","**Save image to tar archive**\n\n```docker image save -o [image.tar] [image]```\n\n**Save to stdout**\n\n```docker image save [image] > [image.tar]```\n\n**Save multiple images**\n\n```docker image save -o [images.tar] [image1] [image2]```\n\n**Save with compression**\n\n```docker image save [image] | gzip > [image.tar.gz]```"],["SYNOPSIS","**docker** **image** **save** [_options_] _image_ [_image..._]"],["PARAMETERS","**-o**, **--output** _file_\n> Write to file instead of stdout."],["DESCRIPTION","**docker image save** saves one or more Docker images to a tar archive, packaging all image layers, configuration, and metadata into a single file. This command is designed for transferring images between systems without using a registry, backing up important images, or distributing images via offline media.\n\nThe resulting tar archive contains the complete image structure, including all filesystem layers, image manifests, and configuration JSON. Multiple images can be saved to a single archive, which is useful for bundling related images together.\n\nOutput can be directed to a file with **-o** or to stdout, allowing for flexible integration with compression tools like gzip or transmission over network connections. Use **docker image load** to restore images from these archives."],["CAVEATS","The archive contains the image's filesystem layers as raw tar files; it is **not** identical to a **docker export** archive (which dumps a running container's filesystem flat without history). Saving by image **ID** rather than name preserves no repository tags in the archive; pass a **name:tag** to retain them. Archives produced by newer Docker engines use the OCI manifest format and may not load cleanly into very old engines."],["HISTORY","**docker save** was part of the original Docker CLI from **2013**. The subcommand was reorganized under the **docker image** namespace in **Docker 1.13** (January 2017) while keeping the legacy short form as an alias."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-save](/man/docker-save)(1), [docker-image-load](/man/docker-image-load)(1), [docker-load](/man/docker-load)(1)"]],"e":["docker-save","docker-image-load","docker-load"],"x":"docker image save -o [image.tar] [image]"},"drill":{"n":"drill","t":"DNS lookup tool with DNSSEC support","s":[["TAGLINE","DNS lookup tool with DNSSEC support"],["TLDR","**Query A records**\n\n```drill [example.com]```\n\n**Query specific record** type\n\n```drill [example.com] [MX]```\n\n**Use specific DNS** server\n\n```drill @[8.8.8.8] [example.com]```\n\n**Trace delegation** path\n\n```drill -T [example.com]```\n\n**Show DNSSEC** information\n\n```drill -D [example.com]```\n\n**TCP instead of UDP**\n\n```drill -t [example.com]```\n\n**Reverse DNS lookup**\n\n```drill -x [8.8.8.8]```"],["SYNOPSIS","**drill** [_options_] _name_ [@_server_] [_type_]"],["PARAMETERS","_NAME_\n> Domain name to query.\n\n@_SERVER_\n> DNS server to query.\n\n_TYPE_\n> Record type: A, AAAA, MX, NS, TXT, etc.\n\n**-T**\n> Trace from root to answer.\n\n**-D**\n> Enable DNSSEC, setting the DO (DNSSEC OK) bit in the query.\n\n**-S**\n> Chase the signatures of the answer up to a known key or trust anchor (requires OpenSSL support).\n\n**-t**\n> Use TCP instead of UDP.\n\n**-x**\n> Reverse DNS lookup.\n\n**-k** _FILE_\n> Trust anchor file for DNSSEC.\n\n**-v**\n> Verbose output."],["DESCRIPTION","**drill** is a DNS lookup tool from the ldns library, designed as a modern alternative to dig. It performs DNS queries with particular emphasis on DNSSEC verification capabilities.\n\nThe tool provides detailed output about DNS responses including flags, sections, and timing. The trace option follows the delegation chain from root servers, showing how DNS resolution works.\n\ndrill's DNSSEC support includes signature validation and trust chain verification, making it valuable for debugging secure DNS configurations."],["CAVEATS","Less widely installed than dig. Some output format differences from dig. DNSSEC validation requires proper trust anchor configuration."],["HISTORY","drill was developed as part of the **ldns** library by **NLnet Labs**, creators of Unbound DNS. It was designed as a dig replacement with better DNSSEC support and cleaner output."],["INSTALL","```apt: sudo apt install ldnsutils```\n\n```apk: sudo apk add drill```\n\n```brew: brew install drill```\n\n```nix: nix profile install nixpkgs#drill```"],["SEE ALSO","[dig](/man/dig)(1), [nslookup](/man/nslookup)(1), [dog](/man/dog)(1), [unbound](/man/unbound)(8)"]],"e":["dig","nslookup","dog","unbound"],"x":"drill [example.com]"},"ego":{"n":"ego","t":"Funtoo Linux system personality manager","s":[["TAGLINE","Funtoo Linux system personality manager"],["TLDR","**Synchronize** the Portage tree\n\n```ego sync```\n\n**Update** bootloader configuration\n\n```ego boot update```\n\n**Read** a Funtoo wiki page\n\n```ego doc [wiki_page]```\n\n**Print** current profile\n\n```ego profile show```\n\n**Enable/Disable** mix-ins\n\n```ego profile mix-in +[gnome] -[kde-plasma-5]```\n\n**Query** Funtoo bugs for a package\n\n```ego query bug [package]```"],["SYNOPSIS","**ego** _command_ [_arguments_]"],["DESCRIPTION","**ego** is Funtoo's official system personality management tool. It provides a unified interface for synchronizing repositories, managing profiles, updating bootloader configuration, and accessing Funtoo documentation.\n\nAvailable modules include: **sync**, **profile**, **query**, **doc**, **boot**, **kit**, and **config**. Replaces several separate tools with a single coherent interface."],["PARAMETERS","**sync**\n> Synchronize Portage tree\n\n**boot update**\n> Update bootloader configuration\n\n**doc** _page_\n> Read wiki documentation\n\n**profile show**\n> Display current profile\n\n**profile mix-in** _changes_\n> Enable/disable mix-ins with +/-\n\n**query bug** _package_\n> Query bugs for package\n\n**kit**\n> Display kit information\n\n**config**\n> View and modify /etc/ego.conf settings\n\n**help** [_module_]\n> Show help for ego or a specific module."],["CONFIGURATION","**/etc/ego.conf**\n> Main configuration file for ego profiles and settings."],["CAVEATS","Funtoo Linux specific. Not available on stock Gentoo or other distributions. Requires proper /etc/ego.conf configuration. Ego is a wrapper around and intended eventual replacement for **eselect** on Funtoo; the shortcut `epro` is equivalent to `ego profile`."],["HISTORY","**ego** was introduced by the **Funtoo Linux** project, a Gentoo-derived distribution led by Daniel Robbins (the original Gentoo founder). It consolidates previously separate utilities (profile selection, Portage tree sync, bootloader generation) into a single modular command and replaces portions of eselect on Funtoo systems."],["INSTALL","```nix: nix profile install nixpkgs#ego```"],["SEE ALSO","[emerge](/man/emerge)(1), [eselect](/man/eselect)(1)"]],"e":["emerge","eselect"],"x":"ego sync"},"electrum":{"n":"electrum","t":"Lightweight Bitcoin wallet with SPV verification","s":[["TAGLINE","Lightweight Bitcoin wallet with SPV verification"],["TLDR","**Start Electrum GUI**\n\n```electrum```\n\n**Create a new wallet**\n\n```electrum create```\n\n**Get wallet balance**\n\n```electrum getbalance```\n\n**List all addresses** in the wallet\n\n```electrum listaddresses```\n\n**Generate a new receiving address**\n\n```electrum createnewaddress```\n\n**Send Bitcoin** to an address\n\n```electrum payto [address] [amount]```\n\n**Start the daemon** for background operation\n\n```electrum daemon start```\n\n**Load a specific wallet** in the daemon\n\n```electrum load_wallet -w [path/to/wallet]```"],["SYNOPSIS","**electrum** [_options_] [_command_] [_args_]\n\n**electrum** daemon [_start_|_stop_|_status_]\n\n**electrum** [_-w wallet_] _command_"],["PARAMETERS","**-w**, **--wallet** _PATH_\n> Use wallet from specified path\n\n**-o**, **--offline**\n> Run in offline mode without network access\n\n**-g**, **--gui** _GUI_\n> Select user interface (qt, text)\n\n**-s**, **--server** _HOST:PORT:PROTOCOL_\n> Set server address\n\n**-p**, **--proxy** _TYPE:HOST:PORT_\n> Set proxy server\n\n**-v**, **--verbose**\n> Show debugging information\n\n**-P**, **--portable**\n> Use portable wallet mode\n\n**-L**, **--lang** _LANGUAGE_\n> Set default language for GUI\n\n**--testnet**\n> Use Bitcoin testnet\n\n**--mainnet**\n> Use Bitcoin mainnet (default)\n\n**-a**, **--all**\n> Show all addresses\n\n**-h**, **--help**\n> Show help message and exit\n\n**--version**\n> Show program version"],["COMMANDS","**create**\n> Create a new wallet\n\n**restore**\n> Restore wallet from seed\n\n**getbalance**\n> Display wallet balance\n\n**listaddresses**\n> List all wallet addresses\n\n**createnewaddress**\n> Generate a new receiving address\n\n**getaddresshistory** _ADDRESS_\n> Show transaction history for an address\n\n**history**\n> Show wallet transaction history\n\n**payto** _ADDRESS_ _AMOUNT_\n> Create and send a transaction\n\n**broadcast** _TX_\n> Broadcast a signed transaction\n\n**signmessage** _ADDRESS_ _MESSAGE_\n> Sign a message with an address\n\n**verifymessage** _ADDRESS_ _SIGNATURE_ _MESSAGE_\n> Verify a signed message\n\n**commands**\n> List all available commands"],["DESCRIPTION","**electrum** is a lightweight Bitcoin wallet that connects to remote servers instead of downloading the entire blockchain. It supports both a graphical interface and command-line operations, making it suitable for desktop use and scripted automation.\n\nThe wallet uses hierarchical deterministic (HD) key generation from a mnemonic seed phrase, allowing full wallet recovery from the seed alone. It supports multi-signature wallets, hardware wallet integration (Trezor, Ledger), and watch-only wallets.\n\nFor command-line operations, the daemon can run in the background handling network communication while CLI commands interact with it. Some commands work offline without the daemon using the **-o** flag. The special argument **!** means \"maximum amount available\" and **-** reads from stdin."],["CONFIGURATION","**~/.electrum/config**\n> Main configuration file for wallet settings and server preferences.\n\n**~/.electrum/wallets/**\n> Directory containing encrypted wallet files.\n\n**~/.electrum/daemon**\n> Daemon socket and process information."],["CAVEATS","Electrum connects to third-party servers that can see your addresses and transaction history, though they cannot access your funds. For maximum privacy, run your own Electrum server. The wallet file is encrypted by default but should be backed up securely. Hardware wallet integration requires additional packages."],["HISTORY","Electrum was created by **Thomas Voegtlin** and first released in **November 2011**. It was one of the first lightweight Bitcoin wallets, pioneering the use of SPV (Simplified Payment Verification) with remote servers. The name comes from the Latin word for amber, chosen because amber produces static electricity when rubbed. Electrum introduced the concept of seed phrases for wallet backup, which became a standard across the cryptocurrency industry."],["INSTALL","```dnf: sudo dnf install electrum```\n\n```pacman: sudo pacman -S electrum```\n\n```apk: sudo apk add electrum```\n\n```nix: nix profile install nixpkgs#electrum```"],["SEE ALSO","[bitcoin-cli](/man/bitcoin-cli)(1), [trezorctl](/man/trezorctl)(1)"],["RESOURCES","```[Source code](https://github.com/spesmilo/electrum)```\n\n```[Homepage](https://electrum.org/)```\n\n```[Documentation](https://electrum.readthedocs.io/)```"]],"e":["bitcoin-cli","trezorctl"],"x":"electrum"},"firewall-cmd":{"n":"firewall-cmd","t":"dynamic firewall management interface","s":[["TAGLINE","dynamic firewall management interface"],["TLDR","**Check** firewalld daemon status\n\n```firewall-cmd --state```\n\n**List** all active zones with their interfaces\n\n```firewall-cmd --get-active-zones```\n\n**List** all rules in the default zone\n\n```firewall-cmd --list-all```\n\n**Add** a service permanently to a zone\n\n```firewall-cmd --permanent --zone public --add-service https```\n\n**Add** a port permanently to a zone\n\n```firewall-cmd --permanent --zone public --add-port 8080/tcp```\n\n**Remove** a service permanently\n\n```firewall-cmd --permanent --zone public --remove-service http```\n\n**Reload** configuration (apply permanent changes at runtime)\n\n```firewall-cmd --reload```\n\n**Save** runtime config to permanent\n\n```firewall-cmd --runtime-to-permanent```\n\n**Enable masquerade** (NAT) on a zone\n\n```firewall-cmd --permanent --zone public --add-masquerade```"],["SYNOPSIS","**firewall-cmd** [_OPTIONS_]"],["DESCRIPTION","**firewall-cmd** is the command-line interface for firewalld, providing dynamic management of the Linux firewall. It supports zones, services, port forwarding, masquerading, and rich rules with both runtime and permanent configurations."],["PARAMETERS","**--state**\n> Check whether the firewalld daemon is active (returns exit code 0 if running).\n\n**--reload**\n> Reload firewall rules while keeping state information. Applies permanent rules to the runtime configuration.\n\n**--complete-reload**\n> Reload the firewall completely, including netfilter kernel modules. May terminate active connections.\n\n**--runtime-to-permanent**\n> Save the current runtime configuration to permanent.\n\n**--get-zones**\n> List all predefined zones.\n\n**--get-default-zone**\n> Print the default zone for connections and interfaces.\n\n**--set-default-zone** _zone_\n> Set the default zone.\n\n**--get-active-zones**\n> Print currently active zones with their bound interfaces and sources.\n\n**--list-all-zones**\n> List all available zones with their complete settings.\n\n**--list-all**\n> List everything added or enabled in the current or specified zone.\n\n**--zone** _zone_\n> Specify the zone to operate on.\n\n**--get-services**\n> List all predefined services.\n\n**--add-service** _service_\n> Add a service to the zone.\n\n**--remove-service** _service_\n> Remove a service from the zone.\n\n**--add-port** _port/protocol_\n> Add a port (or port range) to the zone (e.g. `8080/tcp` or `5000-5100/tcp`).\n\n**--remove-port** _port/protocol_\n> Remove a previously added port from the zone.\n\n**--add-masquerade**\n> Enable IPv4 masquerade (NAT) on the zone. Useful when the machine is a router.\n\n**--remove-masquerade**\n> Disable IPv4 masquerade on the zone.\n\n**--query-masquerade**\n> Return whether IPv4 masquerade is enabled in the zone.\n\n**--add-rich-rule** _rule_\n> Add a rich language firewall rule.\n\n**--remove-rich-rule** _rule_\n> Remove a rich language firewall rule.\n\n**--permanent**\n> Make changes permanent (survives reboot). Requires `--reload` to take effect at runtime.\n\n**--panic-on** / **--panic-off**\n> Enable/disable panic mode (drops all incoming and outgoing traffic).\n\n**--change-interface** _interface_\n> Change the zone binding of a network interface."],["CONFIGURATION","**/etc/firewalld/zones/*.xml**\n> Zone definitions including allowed services, ports, and rich rules.\n\n**/etc/firewalld/services/*.xml**\n> Service definitions mapping service names to ports and protocols."],["CAVEATS","Changes without --permanent are lost on reload or reboot. After permanent changes, use --reload to apply them. Zone changes may disconnect active sessions."],["HISTORY","**firewall-cmd** is the client for **firewalld**, Red Hat's dynamic firewall management daemon used in RHEL, CentOS, and Fedora."],["INSTALL","```dnf: sudo dnf install firewalld```\n\n```pacman: sudo pacman -S firewalld```\n\n```apk: sudo apk add firewalld```\n\n```zypper: sudo zypper install firewalld```\n\n```nix: nix profile install nixpkgs#firewalld```"],["SEE ALSO","[firewalld](/man/firewalld)(1), [iptables](/man/iptables)(8), [nft](/man/nft)(8)"]],"e":["firewalld","iptables","nft"],"x":"firewall-cmd --state"},"flow":{"n":"flow","t":"static type checker for JavaScript","s":[["TAGLINE","static type checker for JavaScript"],["TLDR","**Type check project**\n\n```flow check```\n\n**Start Flow server**\n\n```flow server```\n\n**Check single file**\n\n```flow check-contents < [file.js]```\n\n**Initialize Flow config**\n\n```flow init```\n\n**Get type at** position\n\n```flow type-at-pos [file.js] [10] [5]```"],["SYNOPSIS","**flow** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> Operation: check, server, status, init, etc.\n\n**check**\n> Type check all files.\n\n**server**\n> Start background server.\n\n**status**\n> Show server status and errors.\n\n**init**\n> Create .flowconfig file.\n\n**stop**\n> Stop background server.\n\n**type-at-pos** _FILE_ _LINE_ _COL_\n> Get type at position.\n\n**--help**\n> Display help information."],["DESCRIPTION","**flow** is a static type checker for JavaScript from Facebook. It adds gradual typing through type annotations and inference, catching errors before runtime.\n\nThe tool runs as a background server for fast incremental checking. Type annotations use a superset of JavaScript syntax, stripped at build time.\n\nFlow integrates with editors for real-time type checking and provides detailed error messages with suggested fixes."],["CONFIGURATION","**.flowconfig**\n> Configuration file controlling which files Flow checks, library definitions, linting rules, and options for type checking behavior."],["CAVEATS","Requires .flowconfig file. Not compatible with all JavaScript patterns. Build step needed to strip annotations."],["HISTORY","Flow was created at **Facebook** (now Meta) in 2014 to add type safety to their JavaScript codebase. It competed with TypeScript, offering a different approach to gradual typing."],["INSTALL","```apt: sudo apt install libopm-simulators-bin```\n\n```brew: brew install flow```\n\n```nix: nix profile install nixpkgs#flow```"],["SEE ALSO","[tsc](/man/tsc)(1), [eslint](/man/eslint)(1), [node](/man/node)(1)"],["RESOURCES","```[Homepage](https://flow.org/)```\n\n```[Source code](https://github.com/facebook/flow)```"]],"e":["tsc","eslint","node"],"x":"flow check"},"from":{"n":"from","t":"Display mail sender information from a mailbox","s":[["TAGLINE","Display mail sender information from a mailbox"],["TLDR","**Show all mail senders** in your mailbox\n\n```from```\n\n**Show senders for a specific user**\n\n```from [username]```\n\n**Filter messages by sender** address/substring\n\n```from -s [alice@example.com]```\n\n**Count the number of messages** (GNU mailutils only)\n\n```from -c```\n\n**Check a specific mailbox file**\n\n```from -f [/var/mail/user]```"],["SYNOPSIS","**from** [_-f file_] [_-s sender_] [_user_]"],["PARAMETERS","_USER_\n> Username to check mail for (reads their system mailbox).\n\n**-s** _SENDER_\n> Only print headers from messages whose From address matches the given string. _Note:_ this does **not** show subject lines.\n\n**-f** _FILE_\n> Read from the specified mailbox file instead of the default system mailbox.\n\n**-c**\n> Print only the count of messages. _GNU mailutils only_; not available in BSD/OpenBSD **from**.\n\n**-d**, **--debug**\n> Print debugging information. _GNU mailutils only._\n\n**-V**, **--version**\n> Display version information. _GNU mailutils only._"],["DESCRIPTION","**from** displays the header of each message in a mailbox, showing who mail is from (and, depending on the implementation, subject and date). It reads the system mailbox (typically /var/mail/username) and prints one line per message.\n\nThe tool provides a quick overview of pending mail without opening a full mail client. With **-s**, output is filtered to messages whose sender matches the given address or substring. This is useful for checking mail status in shell scripts, cron jobs, or quick terminal checks."],["CAVEATS","Only works with mbox format mailboxes. May not work with modern mail delivery systems (Maildir, IMAP). Limited to local mail stored in traditional Unix mbox files."],["HISTORY","**from** is a classic **BSD utility** for checking local mail, originating from 4.2BSD. It predates modern mail systems and works with traditional Unix mbox format files."],["INSTALL","```pacman: sudo pacman -S mailutils```\n\n```apk: sudo apk add mailutils```\n\n```zypper: sudo zypper install mailutils```\n\n```brew: brew install mailutils```\n\n```nix: nix profile install nixpkgs#mailutils```"],["SEE ALSO","[mail](/man/mail)(1), [biff](/man/biff)(1), [mailx](/man/mailx)(1), [sendmail](/man/sendmail)(1)"]],"e":["mail","biff","mailx","sendmail"],"x":"from"},"getent":{"n":"getent","t":"Name Service Switch database query tool","s":[["TAGLINE","Name Service Switch database query tool"],["TLDR","Look up a **passwd** entry by UID\n\n```getent passwd [1000]```\n\nList every entry in the **group** database\n\n```getent group```\n\nShow members of one **group name**\n\n```getent group [group_name]```\n\nResolve a hostname via **hosts** (NSS)\n\n```getent hosts [hostname]```\n\nList known **services** from the services database\n\n```getent services```"],["SYNOPSIS","**getent** _database_ [_key_...]"],["PARAMETERS","**passwd**\n> User account database (/etc/passwd and NSS sources)\n\n**group**\n> Group database (/etc/group and NSS sources)\n\n**hosts**\n> Hostname/IP resolution (like /etc/hosts, DNS)\n\n**services**\n> Network services (/etc/services)\n\n**protocols**\n> Network protocols (/etc/protocols)\n\n**networks**\n> Network names and addresses\n\n**shadow**\n> Shadow password entries (requires root)\n\n**aliases**\n> Mail aliases"],["CONFIGURATION","**/etc/nsswitch.conf**\n> Name Service Switch configuration determining which sources to query for each database type."],["DESCRIPTION","**getent** queries entries from administrative databases configured in the Name Service Switch (NSS), including local files, LDAP, NIS, and other sources. It provides a unified interface to look up users, groups, hosts, and other system information regardless of where the data is stored.\n\nUnlike directly reading files like /etc/passwd, getent returns results from all configured NSS sources, making it essential for systems using centralized authentication (LDAP, Active Directory, etc.).\n\nWithout a key, getent lists all entries in the database. With a key, it returns only matching entries."],["CAVEATS","The shadow database requires root privileges. Some databases may not support enumeration (listing all entries). Results depend on NSS configuration in /etc/nsswitch.conf."],["HISTORY","getent has been part of the GNU C Library (glibc) since the implementation of the Name Service Switch in the early 1990s, providing a standard way to query system databases that may come from multiple sources."],["INSTALL","```apt: sudo apt install libc-bin```\n\n```apk: sudo apk add musl-utils```\n\n```nix: nix profile install nixpkgs#getent```"],["SEE ALSO","[id](/man/id)(1), [groups](/man/groups)(1), [host](/man/host)(1)"]],"e":["id","groups","host"],"x":"getent passwd [1000]"},"git-secret":{"n":"git-secret","t":"Encrypt and store secrets in a Git repository","s":[["TAGLINE","Encrypt and store secrets in a Git repository"],["TLDR","**Initialize git-secret**\n\n```git secret init```\n\n**Add user by email**\n\n```git secret tell [user@email.com]```\n\n**Add file to encrypt**\n\n```git secret add [secret.txt]```\n\n**Encrypt secrets**\n\n```git secret hide```\n\n**Decrypt secrets**\n\n```git secret reveal```\n\n**List users who can access secrets**\n\n```git secret whoknows```\n\n**Decrypt and print a single file to stdout**\n\n```git secret cat [secret.txt]```"],["SYNOPSIS","**git secret** _command_ [_options_]"],["PARAMETERS","**init**\n> Initialize git-secret in repo.\n\n**tell** _EMAIL_\n> Add a GPG key holder who can decrypt secrets. Use **-m** to use git config user.email.\n\n**add** _FILE_\n> Add a file to be encrypted. The plaintext file is automatically added to .gitignore.\n\n**hide**\n> Encrypt all added secret files. Use **-m** to delete plaintext after encrypting.\n\n**reveal**\n> Decrypt all secret files. Use **-p** _PASSWORD_ for non-interactive passphrase input.\n\n**remove** _FILE_\n> Remove a file from the secret list.\n\n**list**\n> List all secret files.\n\n**whoknows**\n> List users who have access to secrets.\n\n**changes** _FILE_\n> Show diff between plaintext and decrypted version of a file.\n\n**cat** _FILE_\n> Decrypt and print a single secret file to stdout.\n\n**removeperson** _EMAIL_\n> Remove a person from the secret access list.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git secret** encrypts sensitive files for safe storage in Git repositories. Files are encrypted with GPG for specified recipients, allowing secrets to be version-controlled safely.\n\nThe tool manages encryption, decryption, and recipient lists. Encrypted files (`.secret`) are tracked by Git while the original plaintext files are automatically added to `.gitignore`."],["CONFIGURATION","**.gitsecret/**\n> Directory storing git-secret configuration, key rings, and file mappings."],["ENVIRONMENT","**SECRETS_VERBOSE**\n> Sets verbose flag to show additional output for all commands.\n\n**SECRETS_GPG_COMMAND**\n> Sets an alternative to gpg (default: gpg).\n\n**SECRETS_GPG_ARMOR**\n> Enables gpg --armor mode for ASCII-armored output."],["CAVEATS","Requires GPG. Recipients need GPG keys. This is a separate tool from **gh secret** (GitHub CLI secrets)."],["HISTORY","git secret was created to solve the problem of storing secrets in git repositories, using GPG encryption to protect sensitive configuration."],["INSTALL","```dnf: sudo dnf install git-secret```\n\n```brew: brew install git-secret```\n\n```nix: nix profile install nixpkgs#git-secret```"],["SEE ALSO","[gpg](/man/gpg)(1), [gh-secret](/man/gh-secret)(1), [git](/man/git)(1)"]],"e":["gpg","gh-secret","git"],"x":"git secret init"},"glab":{"n":"glab","t":"Official GitLab CLI tool","s":[["TAGLINE","Official GitLab CLI tool"],["TLDR","**Authenticate with GitLab**\n\n```glab auth login```\n\n**Create a merge request**\n\n```glab mr create```\n\n**List merge requests**\n\n```glab mr list```\n\n**View a merge request**\n\n```glab mr view [number]```\n\n**Create an issue**\n\n```glab issue create```\n\n**Clone a repository**\n\n```glab repo clone [owner/repo]```\n\n**View CI/CD pipelines**\n\n```glab ci list```\n\n**Make a raw API request**\n\n```glab api [/projects/:id/issues]```"],["SYNOPSIS","**glab** _command_ [_options_]"],["PARAMETERS","**mr** _subcommand_\n> Merge request operations (create, list, view, merge, close, checkout, approve).\n\n**issue** _subcommand_\n> Issue operations (create, list, view, close, reopen, note).\n\n**repo** _subcommand_\n> Repository operations (clone, fork, view, archive).\n\n**ci** _subcommand_\n> CI/CD pipeline operations (list, view, run, trace, retry).\n\n**auth** _subcommand_\n> Authentication (login, status, logout).\n\n**release** _subcommand_\n> Manage releases (create, list, view, delete).\n\n**config** _subcommand_\n> Configuration management.\n\n**api** _endpoint_\n> Make authenticated API requests to GitLab.\n\n**snippet** _subcommand_\n> Create and manage code snippets.\n\n**variable** _subcommand_\n> Manage CI/CD variables.\n\n**label** _subcommand_\n> Manage project labels.\n\n**-R** _repo_, **--repo** _repo_\n> Target repository (owner/repo format)."],["DESCRIPTION","**glab** is the official GitLab CLI tool. It provides command-line access to GitLab features including merge requests, issues, CI/CD pipelines, and repository management.\n\nThe tool mirrors the `gh` CLI's approach for GitHub, providing a familiar interface for GitLab users. It supports both GitLab.com and self-hosted instances."],["CONFIGURATION","**~/.config/glab-cli/config.yml**\n> Configuration file storing authentication tokens, default settings, and host configurations."],["CAVEATS","Requires GitLab access token. Some features may require specific permissions. Self-hosted instances need hostname configuration. API rate limits apply."],["HISTORY","glab was created by **Clement Sam** and later became the official GitLab CLI. It was inspired by GitHub's gh CLI and provides similar functionality for GitLab users."],["INSTALL","```apt: sudo apt install glab```\n\n```dnf: sudo dnf install glab```\n\n```pacman: sudo pacman -S glab```\n\n```apk: sudo apk add glab```\n\n```zypper: sudo zypper install glab```\n\n```brew: brew install glab```\n\n```nix: nix profile install nixpkgs#glab```"],["SEE ALSO","[gh](/man/gh)(1), [git](/man/git)(1), [gitlab-runner](/man/gitlab-runner)(1)"]],"e":["gh","git","gitlab-runner"],"x":"glab auth login"},"groups":{"n":"groups","t":"display user group memberships","s":[["TAGLINE","display user group memberships"],["TLDR","**Show current user groups**\n\n```groups```\n\n**Show groups for user**\n\n```groups [username]```\n\n**Show groups for multiple users**\n\n```groups [user1] [user2]```"],["SYNOPSIS","**groups** [_options_] [_users_]"],["PARAMETERS","_USERS_\n> Usernames to check groups for.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version."],["DESCRIPTION","**groups** displays the group memberships for a user. Without arguments, it shows groups for the current user. The primary group is listed first, followed by supplementary groups.\n\nThe command reads from **/etc/group** and the user's supplementary group list. It is equivalent to **id -Gn** but with simpler output formatting."],["CAVEATS","Shows cached groups at login. New group memberships require re-login. Primary group shown first."],["HISTORY","groups is a standard Unix command, part of **GNU coreutils** on Linux systems."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add uutils-coreutils-groups```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[id](/man/id)(1), [groupmems](/man/groupmems)(8), [newgrp](/man/newgrp)(1)"]],"e":["id","groupmems","newgrp"],"x":"groups"},"grub-bios-setup":{"n":"grub-bios-setup","t":"write GRUB boot code to MBR for BIOS systems","s":[["TAGLINE","write GRUB boot code to MBR for BIOS systems"],["TLDR","Set up a device to **boot with GRUB**\n\n```grub-bios-setup /dev/sdX```\n\nInstall even if **problems are detected**\n\n```grub-bios-setup -f /dev/sdX```\n\nInstall GRUB in a **specific directory**\n\n```grub-bios-setup -d /boot/grub /dev/sdX```\n\nAlso make the drive **bootable as a floppy**\n\n```grub-bios-setup -a /dev/sdX```"],["SYNOPSIS","**grub-bios-setup** [_options_] _device_"],["PARAMETERS","**-b**, **--boot-image** _FILE_\n> Use _FILE_ as the boot image instead of boot.img\n\n**-c**, **--core-image** _FILE_\n> Use _FILE_ as the core image instead of core.img\n\n**-d**, **--directory** _DIR_\n> Use GRUB files from _DIR_ instead of default\n\n**-m**, **--device-map** _FILE_\n> Use device map from _FILE_\n\n**-f**, **--force**\n> Install even if problems are detected\n\n**-a**, **--allow-floppy**\n> Make the drive also bootable as a floppy (may break on some BIOSes)\n\n**-s**, **--skip-fs-probe**\n> Skip filesystem probing\n\n**--no-rs-codes**\n> Do not add Reed-Solomon error-correcting codes (x86 BIOS targets only)\n\n**-v**, **--verbose**\n> Enable verbose output"],["DESCRIPTION","**grub-bios-setup** is a low-level tool that writes the GRUB boot code to a disk's Master Boot Record (MBR) or BIOS Boot Partition for BIOS/legacy boot systems.\n\nIn most cases, you should use **grub-install** instead, which handles both copying GRUB files and running grub-bios-setup automatically. This tool is typically used for advanced scenarios like installing GRUB to a different device than where the GRUB files are located."],["CAVEATS","For BIOS systems only; UEFI systems use different methods. Incorrect use can make a system unbootable. Requires GRUB files to already be in place (use grub-install for normal installations)."],["HISTORY","grub-bios-setup is part of GRUB 2, providing the low-level MBR installation that was previously handled by a single grub-install command. The separation allows more flexibility in complex installation scenarios."],["INSTALL","```apt: sudo apt install grub-pc```\n\n```pacman: sudo pacman -S grub```\n\n```apk: sudo apk add grub```"],["SEE ALSO","[grub-install](/man/grub-install)(8), [grub-mkconfig](/man/grub-mkconfig)(8)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/grub/)```\n\n```[Source code](https://git.savannah.gnu.org/cgit/grub.git)```\n\n```[Documentation](https://www.gnu.org/software/grub/manual/grub/grub.html)```"]],"e":["grub-install","grub-mkconfig"],"x":"grub-bios-setup /dev/sdX"},"hakyll-init":{"n":"hakyll-init","t":"Create a new Hakyll static site project with a basic structure","s":[["TAGLINE","Create a new Hakyll static site project with a basic structure"],["TLDR","**Create a new Hakyll site**\n\n```hakyll-init [my-site]```\n\n**Create in current directory**\n\n```hakyll-init .```"],["SYNOPSIS","**hakyll-init** _directory_"],["DESCRIPTION","**hakyll-init** creates a new Hakyll static site project with a basic structure. Hakyll is a Haskell-based static site generator with Pandoc integration. The generated project includes site.hs (main configuration), templates, CSS, sample posts, and images. After initialization, compile site.hs with GHC and use the resulting binary to build and serve the site."],["INSTALL","```apt: sudo apt install libghc-hakyll-dev```"],["SEE ALSO","[hugo](/man/hugo)(1), [jekyll](/man/jekyll)(1), [ghc](/man/ghc)(1), [cabal](/man/cabal)(1), [stack](/man/stack)(1)"]],"e":["hugo","jekyll","ghc","cabal","stack"],"x":"hakyll-init [my-site]"},"halp":{"n":"halp","t":"CLI tool to get help with CLI tools","s":[["TAGLINE","CLI tool to get help with CLI tools"],["TLDR","**Check help flags** for a command\n\n```halp [command]```\n\n**Show the manual page** or cheat sheet for a command\n\n```halp plz [command]```\n\n**Check a specific argument for a command**\n\n```halp --check [--flag] [command]```\n\n**Set a custom timeout** for checking\n\n```halp -t [10] [command]```"],["SYNOPSIS","**halp** [_options_] _command_\n\n**halp** **plz** [_options_] _command_"],["PARAMETERS","**--check** _ARG_\n> Set the argument to check\n\n**--no-version**\n> Disable checking version information\n\n**--no-help**\n> Disable checking help information\n\n**-c**, **--config** _PATH_\n> Set the configuration file\n\n**-t**, **--timeout** _S_\n> Set the timeout for commands (default: 5 seconds)\n\n**-v**, **--verbose**\n> Enable verbose logging\n\n**-h**, **--help**\n> Print help\n\n**-V**, **--version**\n> Print version"],["DESCRIPTION","**halp** is a CLI tool that helps you find the correct help flags for command-line tools. Instead of manually trying **-h**, **--help**, **-?**, **help**, or other variations, halp checks a predefined list of commonly used help options and reports which ones work for the given command.\n\nThe **plz** subcommand goes further by providing quick access to the manual page or **cheat.sh** cheat sheet for a command, using your preferred pager."],["CAVEATS","Some commands may produce side effects when invoked with certain flags, even help-related ones. The default timeout of 5 seconds may be too short for slow commands. Cheat sheet lookups via **cheat.sh** require an internet connection."],["HISTORY","**halp** was created by **Orhun Parmaksiz** and is written in **Rust**. It was designed to eliminate the frustration of guessing which flag a command uses for its help output."],["INSTALL","```pacman: sudo pacman -S halp```\n\n```brew: brew install halp```\n\n```nix: nix profile install nixpkgs#halp```"],["SEE ALSO","[man](/man/man)(1), [tldr](/man/tldr)(1), [cheat](/man/cheat)(1)"]],"e":["man","tldr","cheat"],"x":"halp [command]"},"hey":{"n":"hey","t":"HTTP load testing tool","s":[["TAGLINE","HTTP load testing tool"],["TLDR","**Send 200 requests**\n\n```hey -n [200] [https://example.com]```\n\n**Set concurrency**\n\n```hey -n [1000] -c [50] [https://example.com]```\n\n**Set duration**\n\n```hey -z [30s] [https://example.com]```\n\n**POST with body**\n\n```hey -m POST -d \"[{\\\"key\\\":\\\"value\\\"}]\" [https://example.com/api]```\n\n**POST with body from file**\n\n```hey -m POST -D [data.json] -T \"[application/json]\" [https://example.com/api]```\n\n**Add header**\n\n```hey -H \"[Authorization: Bearer token]\" [https://example.com]```"],["SYNOPSIS","**hey** [_options_] _url_"],["PARAMETERS","**-n** _requests_\n> Number of requests to run. Default is 200.\n\n**-c** _concurrency_\n> Number of concurrent workers. Default is 50.\n\n**-z** _duration_\n> Duration to send requests (e.g., 10s, 3m). Overrides -n.\n\n**-m** _method_\n> HTTP method (GET, POST, PUT, DELETE, HEAD, OPTIONS). Default is GET.\n\n**-d** _body_\n> Request body.\n\n**-D** _file_\n> Request body from file.\n\n**-T** _content-type_\n> Content-Type header. Default is \"text/html\".\n\n**-H** _header_\n> Custom HTTP header. Can be repeated for multiple headers.\n\n**-t** _timeout_\n> Timeout for each request in seconds. Default is 20. Use 0 for infinite.\n\n**-A** _accept_\n> HTTP Accept header.\n\n**-a** _auth_\n> Basic authentication, username:password.\n\n**-x** _proxy_\n> HTTP Proxy address as host:port.\n\n**-q** _rate_\n> Rate limit in QPS per worker. No limit by default.\n\n**-o** _format_\n> Output format: csv.\n\n**-h2**\n> Enable HTTP/2.\n\n**-host** _header_\n> HTTP Host header.\n\n**-disable-compression**\n> Disable compression.\n\n**-disable-keepalive**\n> Disable keep-alive, prevents TCP connection reuse.\n\n**-disable-redirects**\n> Disable following of HTTP redirects.\n\n**-cpus** _n_\n> Number of CPU cores to use."],["DESCRIPTION","**hey** is a HTTP load testing tool. It sends concurrent requests to a URL and provides statistics including latency distribution, throughput, and status code breakdown.\n\nhey is designed to be simple and fast, written in Go. It's useful for quick performance testing and benchmarking web services."],["OUTPUT","Displays:\n- Total time and requests\n- Requests per second\n- Latency distribution (10%, 50%, 90%, 99%)\n- Status code distribution\n- Error summary"],["CAVEATS","Can overload target servers; use responsibly. May be blocked by rate limiters. Not suitable for distributed load testing. Single machine limitation."],["HISTORY","hey was created by **Jaana B. Dogan** (rakyll) at Google as a successor to the tool \"boom\". It's written in Go and designed for simplicity and ease of use."],["INSTALL","```apt: sudo apt install hey```\n\n```apk: sudo apk add hey```\n\n```zypper: sudo zypper install hey```\n\n```brew: brew install hey```\n\n```nix: nix profile install nixpkgs#hey```"],["SEE ALSO","[ab](/man/ab)(1), [wrk](/man/wrk)(1), [siege](/man/siege)(1), [k6](/man/k6)(1)"]],"e":["ab","wrk","siege","k6"],"x":"hey -n [200] [https://example.com]"},"hp-clean":{"n":"hp-clean","t":"Printhead cleaning utility for HPLIP-supported HP inkjet printers","s":[["TAGLINE","Printhead cleaning utility for HPLIP-supported HP inkjet printers"],["TLDR","**Clean print heads** for the default printer (opens the interactive cleaning wizard)\n\n```hp-clean```\n\n**Clean a specific CUPS printer** by queue name\n\n```hp-clean -p [printer-name]```\n\n**Clean a printer** by device URI\n\n```hp-clean -d [device-uri]```\n\n**Run interactively**, choosing the cleaning level (1-3) at the prompt\n\n```hp-clean -i```\n\n**Enable debug-level logging** for troubleshooting\n\n```hp-clean -g```"],["SYNOPSIS","**hp-clean** [_DEVICE-URI_|_PRINTER-NAME_] [_MODE_] [_OPTIONS_]"],["PARAMETERS","**-d** _device-uri_, **--device**=_device-uri_\n> Specify the target printer by CUPS device URI.\n\n**-p** _printer_, **--printer**=_printer_\n> Specify the target printer by CUPS queue name.\n\n**-i**, **--interactive**\n> Run in interactive mode.\n\n**-l** _level_, **--logging**=_level_\n> Set logging verbosity: none, info (default), error, warn, or debug.\n\n**-g**\n> Run with debug-level logging (equivalent to **-l debug**).\n\n**-h**, **--help**\n> Display help and exit."],["DESCRIPTION","**hp-clean** runs the printhead cleaning routine on HPLIP-supported HP inkjet printers, clearing dried ink and debris from the nozzles to restore print quality.\n\nWhen launched without a specific mode it walks through an interactive wizard that lets the user pick a cleaning level from 1 to 3; higher levels clean more aggressively and consume more ink, so level 1 is usually tried first. The target printer can be selected up front with **-p** or **-d**, otherwise hp-clean prompts for one if more than one HPLIP device is installed.\n\nhp-clean only works with inkjet printers whose HPLIP plugin exposes a cleaning routine; laser printers and some low-end inkjets do not support it."],["CAVEATS","Part of the HPLIP package. Cleaning consumes ink and should only be run when print quality actually degrades (streaks, missing lines). Not all HPLIP-supported models implement printhead cleaning."],["HISTORY","**hp-clean** is one of the device-maintenance utilities in **HPLIP** (HP Linux Imaging and Printing), HP's open-source printer/scanner driver suite for Linux, alongside **hp-align** and **hp-linefeedcal**."],["INSTALL","```apt: sudo apt install hplip```\n\n```dnf: sudo dnf install hplip```\n\n```pacman: sudo pacman -S hplip```\n\n```zypper: sudo zypper install hplip```\n\n```nix: nix profile install nixpkgs#hplip```"],["SEE ALSO","[hp-align](/man/hp-align)(1), [hp-check](/man/hp-check)(1), [hp-levels](/man/hp-levels)(1), [hp-info](/man/hp-info)(1)"],["RESOURCES","```[Source code](https://sourceforge.net/projects/hplip/)```\n\n```[Homepage](https://developers.hp.com/hp-linux-imaging-and-printing)```\n\n```[Documentation](https://developers.hp.com/hp-linux-imaging-and-printing/tech_docs/man_pages/index)```"]],"e":["hp-align","hp-check","hp-levels","hp-info"],"x":"hp-clean"},"hping":{"n":"hping","t":"Legacy TCP/IP packet crafting and analysis tool, the predecessor to hping3","s":[["TAGLINE","Legacy TCP/IP packet crafting and analysis tool, the predecessor to hping3"],["TLDR","**Send ICMP ping**\n\n```hping -1 [host]```\n\n**Send TCP SYN to a port**\n\n```hping -S -p [80] [host]```\n\n**Send UDP packets to a port**\n\n```hping -2 -p [53] [host]```\n\n**Traceroute mode**\n\n```hping --traceroute -1 [host]```\n\n**Send a fixed number of packets**\n\n```hping -c [5] -1 [host]```\n\n**Spoof the source IP address**\n\n```hping -a [source-ip] -S -p [80] [host]```\n\n**Scan a range of ports**\n\n```hping -8 [1-100] -S [host]```"],["SYNOPSIS","**hping** [_options_] _host_"],["PARAMETERS","**-0**, **--rawip**\n> Raw IP mode.\n\n**-1**, **--icmp**\n> ICMP mode.\n\n**-2**, **--udp**\n> UDP mode.\n\n**-8**, **--scan** _range_\n> Port scan mode.\n\n**-S**, **--syn**\n> Set TCP SYN flag.\n\n**-A**, **--ack**\n> Set TCP ACK flag.\n\n**-p**, **--destport** _port_\n> Destination port.\n\n**-s**, **--baseport** _port_\n> Source port.\n\n**-c**, **--count** _count_\n> Number of packets to send.\n\n**-i**, **--interval** _wait_\n> Interval between packets.\n\n**-a**, **--spoof** _host_\n> Spoof the source address.\n\n**-T**, **--traceroute**\n> Traceroute mode.\n\n**-C** _type_\n> Set ICMP type.\n\n**-K** _code_\n> Set ICMP code."],["DESCRIPTION","**hping** (also known as hping2) is a command-line tool that crafts and sends custom TCP, UDP, ICMP, or raw IP packets and displays the target's replies, similar to how **ping** shows ICMP echo replies. It supports fragmentation, arbitrary packet bodies, and can be used for firewall testing, port scanning, path MTU discovery, traceroute-like probing over arbitrary protocols, and TCP/IP stack auditing.\n\n**hping3** superseded this version by adding Tcl scripting support, but reuses the same underlying packet-generation code, so hping's command-line flags carry over almost unchanged. Most current Linux distributions only package **hping3**; the plain **hping** binary is largely a legacy name kept for compatibility and may not be installed by default."],["CAVEATS","Requires root privileges to craft raw packets. **hping** is unmaintained; new deployments should use **hping3** instead, which receives the (limited) ongoing upkeep. Spoofed packets and aggressive scanning may be illegal without authorization and can trigger firewalls or IDS systems."],["HISTORY","**hping** was created by **Salvatore Sanfilippo** (\"antirez\", also the creator of Redis) in the late 1990s as a packet generator and analyzer for TCP/IP protocols. Sanfilippo used it to develop the idle scan technique later adopted by Nmap. **hping3**, released in 2005, replaced it as the actively developed version by adding a Tcl scripting engine on top of the same core packet-crafting code."],["INSTALL","```pacman: sudo pacman -S hping```\n\n```nix: nix profile install nixpkgs#hping```"],["SEE ALSO","[hping3](/man/hping3)(8), [nmap](/man/nmap)(1), [ping](/man/ping)(1), [traceroute](/man/traceroute)(1)"],["RESOURCES","```[Source code](https://github.com/antirez/hping)```"]],"e":["hping3","nmap","ping","traceroute"],"x":"hping -1 [host]"},"hsetroot":{"n":"hsetroot","t":"sets the X11 root window background","s":[["TAGLINE","sets the X11 root window background"],["TLDR","**Set solid color background**\n\n```hsetroot -solid \"[#1a1b26]\"```\n\n**Set wallpaper (fill)**\n\n```hsetroot -fill [wallpaper.jpg]```\n\n**Set wallpaper (center)**\n\n```hsetroot -center [wallpaper.png]```\n\n**Set wallpaper (tile)**\n\n```hsetroot -tile [pattern.png]```\n\n**Set gradient**\n\n```hsetroot -add \"[#1a1b26]\" -add \"[#24283b]\" -gradient [0]```\n\n**Set with cover mode**\n\n```hsetroot -cover [wallpaper.jpg]```"],["SYNOPSIS","**hsetroot** [_options_]"],["PARAMETERS","**-solid** _color_\n> Solid color background.\n\n**-fill** _image_\n> Scale to fill screen.\n\n**-center** _image_\n> Center image.\n\n**-tile** _image_\n> Tile image.\n\n**-cover** _image_\n> Cover screen, preserve aspect.\n\n**-add** _color_\n> Add color to gradient.\n\n**-gradient** _angle_\n> Create gradient at angle.\n\n**-screens** _n_\n> Apply to n screens.\n\n**-brightness** _value_\n> Adjust brightness.\n\n**-contrast** _value_\n> Adjust contrast."],["DESCRIPTION","**hsetroot** sets the X11 root window background. It's commonly used with minimal window managers that don't include wallpaper functionality, like dwm, bspwm, or i3.\n\nThe tool supports solid colors, images with various scaling modes, and gradients. It's lightweight and scriptable."],["CAVEATS","X11 only; doesn't work with Wayland. Some compositors may override. Requires Imlib2. Color formats: #RRGGBB or names."],["HISTORY","hsetroot is a fork/rewrite of various root background setters. It's commonly used in the minimal/tiling window manager community where dedicated wallpaper handling is needed."],["INSTALL","```apt: sudo apt install hsetroot```\n\n```dnf: sudo dnf install hsetroot```\n\n```pacman: sudo pacman -S hsetroot```\n\n```zypper: sudo zypper install hsetroot```\n\n```nix: nix profile install nixpkgs#hsetroot```"],["SEE ALSO","[feh](/man/feh)(1), [nitrogen](/man/nitrogen)(1), [xwallpaper](/man/xwallpaper)(1), [xsetroot](/man/xsetroot)(1)"]],"e":["feh","nitrogen","xwallpaper","xsetroot"],"x":"hsetroot -solid \"[#1a1b26]\""},"isoinfo":{"n":"isoinfo","t":"examines and extracts information from ISO 9660 filesystem images","s":[["TAGLINE","examines and extracts information from ISO 9660 filesystem images"],["TLDR","**List** all files in an ISO image\n\n```isoinfo -f -i [path/to/image.iso]```\n\n**Extract** a file from ISO to stdout\n\n```isoinfo -i [path/to/image.iso] -x [/PATH/TO/FILE.EXT]```\n\nShow **header information** for an ISO\n\n```isoinfo -d -i [path/to/image.iso]```"],["SYNOPSIS","**isoinfo** [_options_] **-i** _filename_"],["PARAMETERS","**-i** _FILE_\n> Input ISO image file\n\n**-f**\n> List all files in the ISO\n\n**-d**\n> Display header information (volume descriptor)\n\n**-x** _PATH_\n> Extract file at specified path to stdout\n\n**-l**\n> Long listing format (like ls -l)\n\n**-R**\n> Use Rock Ridge extensions\n\n**-J**\n> Use Joliet extensions"],["DESCRIPTION","**isoinfo** examines and extracts information from ISO 9660 filesystem images. It can list files, display volume information, and extract individual files without mounting the image.\n\nThe tool supports ISO 9660 extensions including Rock Ridge (Unix permissions and long filenames) and Joliet (Windows long filenames). This is useful for inspecting ISO images before burning or mounting."],["CAVEATS","File paths inside ISO images are often uppercase. Rock Ridge or Joliet extensions may be needed to see original filenames. Extracted files go to stdout; redirect to save."],["HISTORY","isoinfo is part of the cdrtools/cdrkit package, originally developed by Joerg Schilling. ISO 9660 is the standard filesystem for CD-ROM media, defined in **1988**."],["INSTALL","```apt: sudo apt install genisoimage```\n\n```dnf: sudo dnf install genisoimage```\n\n```apk: sudo apk add cdrkit```\n\n```nix: nix profile install nixpkgs#cdrkit```"],["SEE ALSO","[genisoimage](/man/genisoimage)(1), [mount](/man/mount)(8)"]],"e":["genisoimage","mount"],"x":"isoinfo -f -i [path/to/image.iso]"},"iverilog":{"n":"iverilog","t":"icarus Verilog compiler","s":[["TAGLINE","icarus Verilog compiler"],["TLDR","**Compile Verilog file**\n\n```iverilog -o [output] [input.v]```\n\n**Compile multiple files**\n\n```iverilog -o [output] [file1.v] [file2.v]```\n\n**Specify top module**\n\n```iverilog -s [top_module] -o [output] [input.v]```\n\n**Generate VCD output**\n\n```iverilog -o [output] [input.v] && vvp [output]```\n\n**Include directory**\n\n```iverilog -I [include_dir] -o [output] [input.v]```\n\n**Define macro**\n\n```iverilog -D[MACRO=value] -o [output] [input.v]```"],["SYNOPSIS","**iverilog** [_options_] _file_..."],["PARAMETERS","**-o** _FILE_\n> Output file name.\n\n**-s** _MODULE_\n> Specify top-level module.\n\n**-I** _DIR_\n> Add include directory.\n\n**-D** _MACRO_\n> Define preprocessor macro.\n\n**-g** _STANDARD_\n> Verilog standard (2001, 2005, 2009, 2012).\n\n**-t** _TARGET_\n> Target format (vvp, null).\n\n**--help**\n> Display help information."],["DESCRIPTION","**iverilog** is the Icarus Verilog compiler. It compiles Verilog HDL to simulation executables.\n\nThe tool supports IEEE Verilog standards. Output runs with vvp simulator for verification and testing."],["CAVEATS","Simulation only, not synthesis. Some SystemVerilog limited. Output requires vvp runtime."],["HISTORY","Icarus Verilog was created by **Stephen Williams** as an open-source alternative to commercial Verilog simulators."],["INSTALL","```apt: sudo apt install iverilog```\n\n```dnf: sudo dnf install iverilog```\n\n```pacman: sudo pacman -S iverilog```\n\n```apk: sudo apk add iverilog```\n\n```zypper: sudo zypper install iverilog```\n\n```nix: nix profile install nixpkgs#iverilog```"],["SEE ALSO","[gtkwave](/man/gtkwave)(1), [verilator](/man/verilator)(1), [yosys](/man/yosys)(1)"]],"e":["gtkwave","verilator","yosys"],"x":"iverilog -o [output] [input.v]"},"jwt":{"n":"jwt","t":"encodes and decodes JSON Web Tokens","s":[["TAGLINE","encodes and decodes JSON Web Tokens"],["TLDR","**Decode JWT**\n\n```jwt decode [token]```\n\n**Encode JWT**\n\n```jwt encode --secret [secret] --payload '[{\"sub\":\"user\"}]'```\n\n**Decode without verification**\n\n```jwt decode --no-verify [token]```\n\n**Show header**\n\n```jwt decode --header [token]```\n\n**Encode with algorithm**\n\n```jwt encode --alg [HS256] --secret [key] --payload '[{}]'```"],["SYNOPSIS","**jwt** _command_ [_options_]"],["PARAMETERS","**decode** _TOKEN_\n> Decode JWT token.\n\n**encode**\n> Create JWT token.\n\n**--secret** _SECRET_\n> Signing secret/key.\n\n**--payload** _JSON_\n> Token payload.\n\n**--alg** _ALG_\n> Signing algorithm.\n\n**--no-verify**\n> Skip signature verification.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jwt** encodes and decodes JSON Web Tokens. It creates and verifies JWTs for authentication systems.\n\nThe tool supports various signing algorithms. It displays token contents for debugging."],["CAVEATS","Multiple implementations exist. Security sensitive. Verify algorithms carefully."],["HISTORY","jwt CLI tools provide command-line access to **JSON Web Token** encoding and decoding for testing and debugging."],["INSTALL","```apt: sudo apt install jwt```"],["SEE ALSO","[openssl](/man/openssl)(1), [jq](/man/jq)(1)"]],"e":["openssl","jq"],"x":"jwt decode [token]"},"lpstat":{"n":"lpstat","t":"displays status information about the current print service","s":[["TAGLINE","displays status information about the current print service"],["TLDR","**Show all printers**\n\n```lpstat -p```\n\n**Show default printer**\n\n```lpstat -d```\n\n**Show print jobs**\n\n```lpstat```\n\n**Show jobs for specific printer**\n\n```lpstat -P [printer]```\n\n**Show detailed printer info**\n\n```lpstat -l -p [printer]```\n\n**Show all status info**\n\n```lpstat -t```\n\n**Show accepting printers**\n\n```lpstat -a```"],["SYNOPSIS","**lpstat** [_options_]"],["DESCRIPTION","**lpstat** displays status information about the current print service. It shows printers, print jobs, and system configuration for CUPS.\n\nThe tool helps troubleshoot printing issues and monitor job queues."],["PARAMETERS","**-a** [_printers_]\n> Show accepting status.\n\n**-d**\n> Show default destination.\n\n**-o** [_dest_]\n> Show jobs.\n\n**-p** [_printers_]\n> Show printer status.\n\n**-r**\n> Show scheduler running.\n\n**-s**\n> Show summary.\n\n**-t**\n> Show all status.\n\n**-u** [_users_]\n> Show user jobs.\n\n**-l**\n> Long (detailed) output.\n\n**-W** _which_\n> Show completed/not-completed jobs."],["CAVEATS","Information depends on CUPS configuration. Some details need appropriate access. Network printers may have delayed status."],["HISTORY","**lpstat** follows the POSIX printing interface, with implementations in various Unix systems. The CUPS implementation by **Apple** and **Michael Sweet** became standard on macOS and Linux."],["INSTALL","```apt: sudo apt install lprng```\n\n```dnf: sudo dnf install cups-client```\n\n```apk: sudo apk add cups-client```\n\n```zypper: sudo zypper install cups-client```"],["SEE ALSO","[lp](/man/lp)(1), [lpr](/man/lpr)(1), [lpq](/man/lpq)(1), [cancel](/man/cancel)(1), [cupsd](/man/cupsd)(8)"]],"e":["lp","lpr","lpq","cancel","cupsd"],"x":"lpstat -p"},"mako":{"n":"mako","t":"Lightweight Wayland notification daemon","s":[["TAGLINE","Lightweight Wayland notification daemon"],["TLDR","Start the **notification daemon**\n\n```mako```\n\nStart with a **custom config file**\n\n```mako --config [path/to/config]```\n\nSet **max visible** notifications\n\n```mako --max-visible=5```\n\nSet **default timeout** in milliseconds (0 disables)\n\n```mako --default-timeout=2000```\n\n**Group** notifications by app name\n\n```mako --group-by=app-name```\n\nAnchor notifications to a **screen position**\n\n```mako --anchor=top-right```"],["SYNOPSIS","**mako** [_options_]"],["DESCRIPTION","**mako** is a lightweight notification daemon for Wayland compositors implementing the **org.freedesktop.Notifications** D-Bus specification. It is designed for use with **sway** and other wlroots-based compositors.\n\nAny option accepted in the configuration file can also be provided on the command line using **--key=value**. The running daemon is controlled at runtime using **makoctl**."],["PARAMETERS","**-c** _FILE_, **--config** _FILE_\n> Use the specified configuration file instead of the default\n\n**-h**, **--help**\n> Display help and exit\n\n**--key=value**\n> Any config-file style option (for example **--max-visible=5**, **--default-timeout=2000**, **--group-by=app-name**, **--sort=-time**, **--anchor=top-right**). See **mako(5)** for the full list."],["CAVEATS","Wayland only; requires a compositor that supports the **wlr-layer-shell** protocol. Configuration file location is **$XDG_CONFIG_HOME/mako/config** (usually **~/.config/mako/config**). Control a running instance with **makoctl**. For the full list of configuration keys and criteria, see **mako(5)**."],["INSTALL","```apt: sudo apt install mako-notifier```\n\n```dnf: sudo dnf install mako```\n\n```pacman: sudo pacman -S mako```\n\n```apk: sudo apk add mako```\n\n```zypper: sudo zypper install mako```\n\n```brew: brew install mako```\n\n```nix: nix profile install nixpkgs#mako```"],["SEE ALSO","[makoctl](/man/makoctl)(1), [dunst](/man/dunst)(1)"]],"e":["makoctl","dunst"],"x":"mako"},"mate-calc-cmd":{"n":"mate-calc-cmd","t":"command-line interface to the MATE calculator, providing an interactive","s":[["TAGLINE","command-line interface to the MATE calculator, providing an interactive"],["TLDR","Start an **interactive calculator** session\n\n```mate-calc-cmd```\n\nCalculate a specific expression (in interactive mode)\n\n```[2 + 5]```"],["SYNOPSIS","**mate-calc-cmd**"],["DESCRIPTION","**mate-calc-cmd** is the command-line interface to the MATE calculator, providing an interactive terminal-based calculator session. It uses the same calculation engine as the graphical **mate-calc** application.\n\nThe interactive session accepts mathematical expressions and displays results immediately. It supports the same functions and operators as the GUI version, including trigonometry, logarithms, and arbitrary precision arithmetic.\n\nType **quit** or press **Ctrl+D** to exit the interactive session."],["CAVEATS","Unlike **mate-calc --solve**, this command starts an interactive session rather than evaluating a single expression. For one-off calculations in scripts, use **mate-calc --solve** instead."],["INSTALL","```apt: sudo apt install mate-calc```\n\n```dnf: sudo dnf install mate-calc```\n\n```pacman: sudo pacman -S mate-calc```\n\n```apk: sudo apk add mate-calc```\n\n```zypper: sudo zypper install mate-calc```\n\n```nix: nix profile install nixpkgs#mate-calc```"],["SEE ALSO","[mate-calc](/man/mate-calc)(1), [bc](/man/bc)(1), [calc](/man/calc)(1)"]],"e":["mate-calc","bc","calc"],"x":"mate-calc-cmd"},"mixxx":{"n":"mixxx","t":"DJ mixing software","s":[["TAGLINE","DJ mixing software"],["TLDR","**Start Mixxx**\n\n```mixxx```\n\n**Open specific file**\n\n```mixxx [music.mp3]```\n\n**Full screen mode**\n\n```mixxx --fullScreen```\n\n**Specify settings path**\n\n```mixxx --settingsPath [/path/to/config]```\n\n**Debug mode**\n\n```mixxx --debugAssertBreak```"],["SYNOPSIS","**mixxx** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Music file to load.\n\n**--fullScreen**\n> Start full screen.\n\n**--settingsPath** _DIR_\n> Configuration directory.\n\n**--midiDebug**\n> Enable MIDI debugging.\n\n**--resourcePath** _DIR_\n> Resource path.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mixxx** is DJ mixing software. It provides digital DJ features with controller support.\n\nThe tool offers beat detection, effects, and library management. Supports MIDI controllers."],["CAVEATS","Audio latency varies. Controller mapping needed. Resource intensive."],["HISTORY","Mixxx started in 2001 as an open-source alternative to commercial **DJ software** like Traktor."],["INSTALL","```apt: sudo apt install mixxx```\n\n```dnf: sudo dnf install mixxx```\n\n```pacman: sudo pacman -S mixxx```\n\n```apk: sudo apk add mixxx```\n\n```zypper: sudo zypper install mixxx```\n\n```nix: nix profile install nixpkgs#mixxx```"],["SEE ALSO","[audacity](/man/audacity)(1), [ardour](/man/ardour)(1)"]],"e":["audacity","ardour"],"x":"mixxx"},"mmdc":{"n":"mmdc","t":"Mermaid CLI for generating diagrams from text","s":[["TAGLINE","Mermaid CLI for generating diagrams from text"],["TLDR","**Generate an SVG diagram from a file**\n\n```mmdc -i [input.mmd] -o [output.svg]```\n\n**Generate a PNG with a dark theme**\n\n```mmdc -i [input.mmd] -o [output.png] -t dark```\n\n**Generate a PDF scaled to fit the chart**\n\n```mmdc -i [input.mmd] -o [output.pdf] -f```\n\n**Set custom page dimensions and scale**\n\n```mmdc -i [input.mmd] -o [output.png] -w [1200] -H [800] -s [2]```\n\n**Use a custom Mermaid configuration file**\n\n```mmdc -i [input.mmd] -o [output.svg] -c [config.json]```\n\n**Read from stdin with transparent background**\n\n```cat [diagram.mmd] | mmdc -i - -o [output.svg] -b transparent```\n\n**Process a Markdown file** extracting embedded diagrams\n\n```mmdc -i [document.md] -o [document-out.md]```"],["SYNOPSIS","**mmdc** [_options_]"],["PARAMETERS","**-i**, **--input** _FILE_\n> Input file path. Use `-` for stdin. Files ending in .md or .markdown are treated as Markdown.\n\n**-o**, **--output** _FILE_\n> Output file path. Use `-` for stdout. Defaults to `{input}.svg`.\n\n**-e**, **--outputFormat** _FORMAT_\n> Output format: svg, png, or pdf. Inferred from output extension by default.\n\n**-t**, **--theme** _THEME_\n> Mermaid theme: default, dark, forest, or neutral.\n\n**-b**, **--backgroundColor** _COLOR_\n> Background color for PNG/SVG output (default: white).\n\n**-w**, **--width** _WIDTH_\n> Page width in pixels (default: 800).\n\n**-H**, **--height** _HEIGHT_\n> Page height in pixels (default: 600).\n\n**-s**, **--scale** _FACTOR_\n> Puppeteer scale factor for deviceScaleFactor (default: 1).\n\n**-f**, **--pdfFit**\n> Scale PDF output to fit chart dimensions.\n\n**-c**, **--configFile** _FILE_\n> JSON configuration file for Mermaid settings.\n\n**-C**, **--cssFile** _FILE_\n> Path to custom CSS file for injection into the page.\n\n**-I**, **--svgId** _ID_\n> ID attribute for the SVG element (default: my-svg).\n\n**-p**, **--puppeteerConfigFile** _FILE_\n> JSON configuration file for Puppeteer launch options.\n\n**-a**, **--artefacts** _PATH_\n> Output path for images when processing Markdown files.\n\n**-q**, **--quiet**\n> Suppress log output to stdout.\n\n**-V**, **--version**\n> Display version number.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**mmdc** is the CLI for the Mermaid diagramming library. It renders diagrams defined in Mermaid's text-based syntax into SVG, PNG, or PDF output using a headless Chromium browser via Puppeteer.\n\nSupported diagram types include flowcharts, sequence diagrams, class diagrams, state diagrams, Gantt charts, pie charts, ER diagrams, and more. When given a Markdown file as input, mmdc extracts and renders embedded Mermaid code blocks, replacing them with generated images.\n\nThe **--configFile** option accepts a JSON file that configures Mermaid behavior and is merged with the **--theme** option, with the config file taking precedence."],["CAVEATS","Requires **Node.js** and downloads a Chromium browser via **Puppeteer** for rendering. The first run may take time due to the browser download. The scale factor (**-s**) only affects PNG output resolution."],["HISTORY","**mmdc** is the CLI for **Mermaid**, a JavaScript-based diagramming and charting tool created by **Knut Sveidqvist**. The CLI package is published as **@mermaid-js/mermaid-cli** on npm."],["SEE ALSO","[graphviz](/man/graphviz)(1), [plantuml](/man/plantuml)(1), [dot](/man/dot)(1)"]],"e":["graphviz","plantuml","dot"],"x":"mmdc -i [input.mmd] -o [output.svg]"},"mons":{"n":"mons","t":"POSIX-compliant shell script for managing multi-monitor displays on X","s":[["TAGLINE","POSIX-compliant shell script for managing multi-monitor displays on X"],["TLDR","Enable only the **primary monitor**\n\n```mons -o```\n\nEnable only the **secondary monitor**\n\n```mons -s```\n\n**Duplicate** primary monitor onto secondary (primary resolution)\n\n```mons -d```\n\n**Mirror** primary monitor onto secondary (secondary resolution)\n\n```mons -m```\n\n**Extend** primary monitor to the right\n\n```mons -e right```\n\n**List** connected monitors\n\n```mons```\n\nEnable a **specific monitor** by ID\n\n```mons -O [id]```"],["SYNOPSIS","**mons** [**-o**] [**-s**] [**-d**] [**-m**] [**-e** _side_] [**-O** _id_] [**-S** _id1,id2:pos_] [**--dpi** _value_]"],["PARAMETERS","**-o**\n> Enable primary monitor only\n\n**-s**\n> Enable secondary monitor only\n\n**-d**\n> Duplicate primary monitor output onto secondary\n\n**-m**\n> Mirror primary display onto secondary\n\n**-e _side_**\n> Extend primary monitor; side: top, left, right, bottom\n\n**-n _side_**\n> Cycle through display modes sequentially\n\n**-O _id_**\n> Enable specific monitor by identifier\n\n**-S _id1,id2:pos_**\n> Enable two specific monitors; pos: R (right), T (top)\n\n**-a**\n> Daemon mode; auto-enable display on monitor changes\n\n**-x _script_**\n> Execute custom script on monitor count changes\n\n**--dpi _value_**\n> Set display DPI (0-27432 range)\n\n**--primary _name_**\n> Designate primary output monitor\n\n**-h**\n> Display help\n\n**-v**\n> Show version"],["DESCRIPTION","**mons** is a POSIX-compliant shell script for managing multi-monitor displays on X Window System. It simplifies switching between display configurations without extensive dependencies.\n\nThe tool wraps xrandr to provide intuitive commands for common dual-monitor setups: primary only, secondary only, duplicate, mirror, and extend modes. It supports daemon mode for automatic display management when monitors are connected or disconnected."],["CAVEATS","Requires X Window System and xrandr. Does not work with Wayland compositors. Limited to configurations that xrandr supports. The secondary monitor is determined by connection order; use -O or -S for explicit control with multiple monitors."],["HISTORY","**mons** was developed by **Thomas� Venturini** (Ventto) as a lightweight alternative to graphical display configuration tools. It provides a minimal, scriptable interface for common multi-monitor operations."],["INSTALL","```nix: nix profile install nixpkgs#mons```"],["SEE ALSO","[xrandr](/man/xrandr)(1), [autorandr](/man/autorandr)(1), [arandr](/man/arandr)(1)"]],"e":["xrandr","autorandr","arandr"],"x":"mons -o"},"nagios4":{"n":"nagios4","t":"current stable IT infrastructure monitoring system","s":[["TAGLINE","current stable IT infrastructure monitoring system"],["TLDR","**Verify configuration**\n\n```nagios4 -v [/etc/nagios4/nagios.cfg]```\n\n**Start Nagios**\n\n```nagios4 [/etc/nagios4/nagios.cfg]```\n\n**Run as daemon**\n\n```nagios4 -d [/etc/nagios4/nagios.cfg]```\n\n**Show version**\n\n```nagios4 -V```"],["SYNOPSIS","**nagios4** [_options_] _config_file_"],["PARAMETERS","**-v**\n> Verify configuration.\n\n**-d**\n> Run as daemon.\n\n**-s**\n> Show scheduling information.\n\n**-p**\n> Show pending events.\n\n**-V**\n> Show version.\n\n**-W**\n> Pre-cache objects."],["DESCRIPTION","**nagios4** (version 4) is the current stable IT infrastructure monitoring system. Monitors hosts, services, network devices, and applications. Provides alerting, reporting, and web interface."],["INSTALL","```apt: sudo apt install nagios4-core```"],["SEE ALSO","[icinga2](/man/icinga2)(1), [prometheus](/man/prometheus)(1)"]],"e":["icinga2","prometheus"],"x":"nagios4 -v [/etc/nagios4/nagios.cfg]"},"nslookup":{"n":"nslookup","t":"queries DNS servers for hostname and IP information","s":[["TAGLINE","queries DNS servers for hostname and IP information"],["TLDR","**Lookup IP address**\n\n```nslookup [example.com]```\n\n**Lookup using specific DNS server**\n\n```nslookup [example.com] [8.8.8.8]```\n\n**Reverse DNS lookup**\n\n```nslookup [192.168.1.1]```\n\n**Query specific record type**\n\n```nslookup -type=[MX] [example.com]```\n\n**Query all records**\n\n```nslookup -type=[ANY] [example.com]```\n\n**Use TCP instead of UDP**\n\n```nslookup -vc [example.com]```\n\n**Interactive mode**\n\n```nslookup```\n\n**Debug mode**\n\n```nslookup -debug [example.com]```"],["SYNOPSIS","**nslookup** [_options_] [_host_] [_server_]"],["DESCRIPTION","**nslookup** queries DNS servers for hostname and IP information. It can perform forward and reverse lookups, query specific record types, and diagnose DNS issues.\n\nThe tool operates in interactive or non-interactive mode, with interactive mode allowing multiple queries."],["PARAMETERS","**-type=**_type_\n> Record type (A, AAAA, MX, NS, TXT, SOA, PTR, ANY, CNAME, SRV).\n\n**-query=**_type_\n> Alias for -type.\n\n**-class=**_class_\n> DNS class (IN, CH, HS, ANY). Default is IN.\n\n**-debug**\n> Show debug information (full response packets).\n\n**-port=**_port_\n> DNS server port (default 53).\n\n**-timeout=**_seconds_\n> Query timeout.\n\n**-retry=**_n_\n> Number of retries before giving up.\n\n**-vc**\n> Use TCP instead of UDP for queries.\n\n**-nosearch**\n> Do not append the search list to unqualified names.\n\n**-norecurse**\n> Do not request recursive resolution from the server.\n\n**-version**\n> Print version and exit."],["INTERACTIVE COMMANDS","**server** _name_: Set default DNS server (uses current server to resolve it)\n**lserver** _name_: Set default server using the initial server\n**set type=**_type_: Set query type\n**set class=**_class_: Set query class\n**set debug** / **set nodebug**: Toggle debug output\n**set all**: Display current option settings\n**exit**: Quit"],["CAVEATS","Deprecated in favor of dig. Behavior varies by implementation. Interactive mode differs from command line. Some features implementation-specific."],["HISTORY","**nslookup** was written by **Andrew Cherenson** at UC Berkeley as part of BIND. While still included in most systems, the BIND documentation recommends using dig for DNS queries due to more consistent behavior."],["INSTALL","```apt: sudo apt install bind9-dnsutils```\n\n```apk: sudo apk add bind-tools```"],["SEE ALSO","[dig](/man/dig)(1), [host](/man/host)(1), [drill](/man/drill)(1), [getent](/man/getent)(1)"]],"e":["dig","host","drill","getent"],"x":"nslookup [example.com]"},"nxc-ldap":{"n":"nxc-ldap","t":"NetExec LDAP module for Active Directory enumeration","s":[["TAGLINE","NetExec LDAP module for Active Directory enumeration"],["TLDR","**Authenticate to LDAP**\n\n```nxc ldap [target] -u [user] -p [password]```\n\n**Authenticate with NTLM hash**\n\n```nxc ldap [target] -u [user] -H [hash]```\n\n**Enumerate users**\n\n```nxc ldap [target] -u [user] -p [password] --users```\n\n**Enumerate groups**\n\n```nxc ldap [target] -u [user] -p [password] --groups```\n\n**Kerberoasting**\n\n```nxc ldap [target] -u [user] -p [password] --kerberoasting [output.txt]```\n\n**AS-REP roasting**\n\n```nxc ldap [target] -u [user] -p [password] --asreproast [output.txt]```\n\n**Run custom LDAP query**\n\n```nxc ldap [target] -u [user] -p [password] --query \"([sAMAccountName=*])\" --base-dn \"[DC=domain,DC=com]\"```\n\n**Collect BloodHound data**\n\n```nxc ldap [target] -u [user] -p [password] --bloodhound -c All --dns-server [dc_ip]```"],["SYNOPSIS","**nxc** **ldap** _target_ [_options_]"],["PARAMETERS","_TARGET_\n> Domain controller IP or hostname.\n\n**-u** _USER_\n> Username for authentication.\n\n**-p** _PASSWORD_\n> Password for authentication.\n\n**-H** _HASH_\n> NTLM hash for pass-the-hash authentication.\n\n**-k**\n> Use Kerberos authentication.\n\n**--use-kcache**\n> Use Kerberos credential cache.\n\n**-d** _DOMAIN_\n> Domain to authenticate to.\n\n**--port** _PORT_\n> LDAP port (default: 389).\n\n**--users**\n> Enumerate domain users.\n\n**--active-users**\n> Enumerate active (non-disabled) domain users.\n\n**--groups**\n> Enumerate domain groups.\n\n**--computers**\n> Enumerate domain computers.\n\n**--dc-list**\n> List domain controllers.\n\n**--get-sid**\n> Get domain SID.\n\n**--pass-pol**\n> Get password policy.\n\n**--gmsa**\n> Enumerate Group Managed Service Accounts.\n\n**--kerberoasting** _FILE_\n> Perform Kerberoasting and save hashes to file.\n\n**--asreproast** _FILE_\n> Perform AS-REP roasting and save hashes to file.\n\n**--trusted-for-delegation**\n> Find accounts trusted for delegation.\n\n**--password-not-required**\n> Find accounts that do not require a password.\n\n**--admin-count**\n> Find accounts with adminCount=1.\n\n**--bloodhound**\n> Collect data for BloodHound.\n\n**--query** _FILTER_\n> Custom LDAP query filter.\n\n**--base-dn** _DN_\n> Base DN for LDAP queries.\n\n**-M** _MODULE_\n> Run a specific LDAP module.\n\n**--continue-on-success**\n> Continue on successful authentication (password spraying)."],["DESCRIPTION","**nxc ldap** is the LDAP protocol module of NetExec (formerly CrackMapExec). It performs Active Directory enumeration and attacks via the LDAP protocol against domain controllers.\n\nThe module supports authentication via passwords, NTLM hashes, and Kerberos tickets. It can enumerate users, groups, computers, policies, and delegation settings, as well as perform Kerberoasting and AS-REP roasting attacks."],["CAVEATS","Security/penetration testing tool. Authorized use only. Requires valid credentials or hashes for most operations. Some features require specific AD privilege levels."],["HISTORY","NetExec (nxc) is the successor to **CrackMapExec** (cme), maintained by the community after the original project was archived. The LDAP module provides Active Directory enumeration via the LDAP protocol."],["SEE ALSO","[nxc](/man/nxc)(1), [nxc-smb](/man/nxc-smb)(1), [nxc-winrm](/man/nxc-winrm)(1), [ldapsearch](/man/ldapsearch)(1), [bloodhound](/man/bloodhound)(1)"]],"e":["nxc","nxc-smb","nxc-winrm","ldapsearch","bloodhound"],"x":"nxc ldap [target] -u [user] -p [password]"},"ocamlopt":{"n":"ocamlopt","t":"OCaml native-code compiler","s":[["TAGLINE","OCaml native-code compiler"],["TLDR","**Compile to native code**\n\n```ocamlopt -o [program] [source.ml]```\n\n**Compile multiple files**\n\n```ocamlopt -o [program] [file1.ml] [file2.ml]```\n\n**Compile with optimization**\n\n```ocamlopt -O3 -o [program] [source.ml]```\n\n**Compile to object file**\n\n```ocamlopt -c [source.ml]```\n\n**Create native library**\n\n```ocamlopt -a -o [library.cmxa] [file1.ml] [file2.ml]```"],["SYNOPSIS","**ocamlopt** [_options_] _files_..."],["PARAMETERS","**-o** _file_\n> Specify the output filename.\n\n**-c**\n> Compile only; suppress the linking phase. Produces `.cmx` and `.o` files.\n\n**-a**\n> Build a library (`.cmxa` and `.a` files) from the given object files.\n\n**-shared**\n> Build a dynamically loadable plugin (`.cmxs` file).\n\n**-pack**\n> Combine multiple `.cmx` files into a single compilation unit.\n\n**-O** _level_\n> Optimization level: `0` disables all optimizations, `3` enables aggressive optimization including unboxing. Default is `1`.\n\n**-I** _dir_\n> Add _dir_ to the search path for `.cmi` and `.cmx` files.\n\n**-g**\n> Add debugging information to enable stack backtraces at runtime.\n\n**-S**\n> Keep the assembly code produced during compilation in a `.s` file.\n\n**-inline** _n_\n> Set the aggressiveness of inlining (default 10; higher means more inlining).\n\n**-compact**\n> Optimize the produced code for space rather than speed.\n\n**-unsafe**\n> Disable bounds checking on array/string accesses and zero-division checks; faster but unsafe.\n\n**-for-pack** _module-path_\n> Generate an object file that can later be included as a sub-module in a `-pack` compilation.\n\n**-linkall**\n> Force all modules contained in libraries to be linked in.\n\n**-pp** _command_\n> Pipe source files through the given preprocessor command.\n\n**-ppx** _command_\n> Pipe the abstract syntax tree through the given preprocessor.\n\n**-w** _warning-list_\n> Enable, disable, or mark as fatal specific compiler warnings.\n\n**-warn-error** _warning-list_\n> Turn the specified warnings into errors.\n\n**-open** _Module_\n> Open the named module before processing each source file.\n\n**-nostdlib**\n> Do not include the standard library directory in the search path.\n\n**-cc** _ccomp_\n> Use _ccomp_ as the C linker when building executables.\n\n**-cclib** _-llib_\n> Link with the given C library.\n\n**-ccopt** _option_\n> Pass _option_ to the C compiler and linker.\n\n**-verbose**\n> Print all external commands as they are executed.\n\n**-v**\n> Print the version number of the compiler and the location of the standard library.\n\n**-i**\n> Print inferred interface to stdout; do not produce object files.\n\n**-bin-annot**\n> Generate detailed type and location information in `.cmt` and `.cmti` files."],["DESCRIPTION","**ocamlopt** is the OCaml native-code compiler. It compiles OCaml source files directly to native machine code, producing significantly faster executables than the bytecode compiler **ocamlc**. The output is architecture-specific and not portable across platforms.\n\nCompilation proceeds in three phases: type-checking and compilation to `.cmx` / `.o` files (`-c`), optional library archiving (`-a`), and linking. Interface files (`.mli`) are compiled to `.cmi` files shared with the bytecode compiler."],["CAVEATS","Native-code compilation is slower than bytecode compilation. The resulting executables are platform-specific. Debugging native code is harder than debugging bytecode; **ocamldebug** does not support native executables (use `-g` and system debuggers such as `gdb`)."],["HISTORY","The OCaml native-code compiler was developed at **INRIA** alongside the bytecode compiler as part of the Caml/OCaml project to provide high-performance compilation while retaining the language's type safety guarantees."],["INSTALL","```apt: sudo apt install ocaml```\n\n```dnf: sudo dnf install ocaml```\n\n```pacman: sudo pacman -S ocaml```\n\n```apk: sudo apk add ocaml5```\n\n```zypper: sudo zypper install ocaml```\n\n```brew: brew install ocaml```\n\n```nix: nix profile install nixpkgs#ocaml```"],["SEE ALSO","[ocamlc](/man/ocamlc)(1), [ocamlfind](/man/ocamlfind)(1), [opam](/man/opam)(1), [dune](/man/dune)(1)"]],"e":["ocamlc","ocamlfind","opam","dune"],"x":"ocamlopt -o [program] [source.ml]"},"osmconvert":{"n":"osmconvert","t":"converts and filters OpenStreetMap data files","s":[["TAGLINE","converts and filters OpenStreetMap data files"],["TLDR","**Convert PBF to OSM XML**\n\n```osmconvert [input.pbf] -o=[output.osm]```\n\n**Convert OSM to PBF**\n\n```osmconvert [input.osm] -o=[output.pbf]```\n\n**Extract bounding box**\n\n```osmconvert [input.pbf] -b=[lon1,lat1,lon2,lat2] -o=[output.pbf]```\n\n**Filter by polygon**\n\n```osmconvert [input.pbf] -B=[polygon.poly] -o=[output.pbf]```\n\n**Merge files**\n\n```osmconvert [file1.pbf] [file2.pbf] -o=[merged.pbf]```\n\n**Show statistics**\n\n```osmconvert [input.pbf] --out-statistics```"],["SYNOPSIS","**osmconvert** [_options_] _input_... [-o=_output_]"],["PARAMETERS","**-o** _file_\n> Output file.\n\n**-b** _bbox_\n> Bounding box filter.\n\n**-B** _file_\n> Polygon file filter.\n\n**--drop-author**\n> Remove author info.\n\n**--drop-version**\n> Remove version info.\n\n**--complete-ways**\n> Keep complete ways.\n\n**--out-statistics**\n> Show statistics."],["DESCRIPTION","**osmconvert** converts and filters OpenStreetMap data files. It handles conversion between OSM XML, PBF, and O5M formats, and can extract regions by bounding box or polygon."],["BOUNDING BOX FORMAT","```\n-b=min_lon,min_lat,max_lon,max_lat\n-b=-74.1,40.5,-73.7,40.9  # NYC area\n```"],["CAVEATS","Memory-intensive for large files. Polygon files in .poly format. PBF format most efficient."],["HISTORY","osmconvert was developed by **Markus Weber** as a fast alternative to osmosis for common OSM data manipulation tasks."],["INSTALL","```apt: sudo apt install osmctools```\n\n```dnf: sudo dnf install osmctools```\n\n```nix: nix profile install nixpkgs#osmctools```"],["SEE ALSO","[osm2pgsql](/man/osm2pgsql)(1)"]],"e":["osm2pgsql"],"x":"osmconvert [input.pbf] -o=[output.osm]"},"pacman.conf":{"n":"pacman.conf","t":"configuration file for pacman, the Arch Linux package manager","s":[["TAGLINE","configuration file for pacman, the Arch Linux package manager"],["TLDR","**Enable multilib repository**\n\n```[multilib]\\nInclude = /etc/pacman.d/mirrorlist```\n\n**Set parallel downloads**\n\n```ParallelDownloads = 5```\n\n**Enable color output**\n\n```Color```\n\n**Add custom repository**\n\n```[custom]\\nServer = https://example.com/$repo/$arch```"],["SYNOPSIS","**/etc/pacman.conf**"],["PARAMETERS","**[options]**\n> General options section.\n\n**Color**\n> Enable color output.\n\n**ParallelDownloads**\n> Concurrent downloads.\n\n**CheckSpace**\n> Check disk space.\n\n**SigLevel**\n> Package signature level.\n\n**[repository]**\n> Repository definition.\n\n**Include**\n> Include mirror list.\n\n**Server**\n> Direct server URL."],["DESCRIPTION","**pacman.conf** is the configuration file for pacman, the Arch Linux package manager. It defines repositories, signature checking, and various options.\n\nThe file uses INI-style sections with [options] for general settings and [reponame] for repositories."],["EXAMPLE CONFIG","```ini\n[options]\nHoldPkg = pacman glibc\nArchitecture = auto\nColor\nCheckSpace\nParallelDownloads = 5\nSigLevel = Required DatabaseOptional\n\n[core]\nInclude = /etc/pacman.d/mirrorlist\n\n[extra]\nInclude = /etc/pacman.d/mirrorlist\n\n[multilib]\nInclude = /etc/pacman.d/mirrorlist\n```"],["CAVEATS","Repository order matters. Changes require pacman -Sy. Backup before editing."],["SEE ALSO","[pacman](/man/pacman)(8), [makepkg](/man/makepkg)(8), [libalpm](/man/libalpm)(3)"]],"e":["pacman","makepkg","libalpm"],"x":"[multilib]\\nInclude = /etc/pacman.d/mirrorlist"},"pamcomp":{"n":"pamcomp","t":"composites two images","s":[["TAGLINE","composites two images"],["TLDR","**Composite images**\n\n```pamcomp [overlay.pam] [base.pam] > [output.pam]```\n\n**With transparency**\n\n```pamcomp -alpha=[mask.pgm] [overlay.pam] [base.pam] > [output.pam]```\n\n**Specify position**\n\n```pamcomp -xoff=[100] -yoff=[50] [overlay.pam] [base.pam] > [output.pam]```"],["SYNOPSIS","**pamcomp** [_options_] _overlay_ _base_"],["PARAMETERS","_OVERLAY_\n> Foreground image.\n\n_BASE_\n> Background image.\n\n**-alpha** _FILE_\n> Alpha mask file.\n\n**-xoff** _PIXELS_\n> X offset.\n\n**-yoff** _PIXELS_\n> Y offset.\n\n**-align** _POS_\n> Alignment position."],["DESCRIPTION","**pamcomp** composites two images. Overlays with transparency support.\n\nThe tool combines PAM/PNM images. Part of Netpbm."],["CAVEATS","Part of Netpbm. PAM/PNM format. Alpha channel support."],["HISTORY","pamcomp is part of **Netpbm** for image compositing operations."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmcomp](/man/pnmcomp)(1), [pnmpaste](/man/pnmpaste)(1), [netpbm](/man/netpbm)(1)"]],"e":["pnmcomp","pnmpaste","netpbm"],"x":"pamcomp [overlay.pam] [base.pam] > [output.pam]"},"pastel":{"n":"pastel","t":"command-line tool for working with colors","s":[["TAGLINE","command-line tool for working with colors"],["TLDR","**Display color information**\n\n```pastel color [red]```\n\n**Show color from hex**\n\n```pastel color [\"#ff5500\"]```\n\n**Convert color format**\n\n```pastel format hsl [\"#ff5500\"]```\n\n**List color names**\n\n```pastel list```\n\n**Generate color palette**\n\n```pastel gradient [blue] [red] --number [5]```\n\n**Pick color interactively**\n\n```pastel pick```\n\n**Lighten a color**\n\n```pastel lighten [0.2] [blue]```\n\n**Mix two colors**\n\n```pastel mix [red] [blue]```"],["SYNOPSIS","**pastel** _command_ [_options_] [_arguments_]"],["PARAMETERS","**color** _COLOR_\n> Show color information.\n\n**list**\n> List named colors.\n\n**format** _FORMAT_ _COLOR_\n> Convert color to format.\n\n**pick**\n> Interactive color picker.\n\n**gradient** _COLOR1_ _COLOR2_\n> Generate gradient between colors.\n\n**mix** _COLOR1_ _COLOR2_\n> Mix two colors.\n\n**lighten** _AMOUNT_ _COLOR_\n> Lighten color (0.0-1.0).\n\n**darken** _AMOUNT_ _COLOR_\n> Darken color.\n\n**saturate** _AMOUNT_ _COLOR_\n> Increase saturation.\n\n**desaturate** _AMOUNT_ _COLOR_\n> Decrease saturation.\n\n**rotate** _DEGREES_ _COLOR_\n> Rotate hue.\n\n**complement** _COLOR_\n> Get complementary color.\n\n**distinct** _NUM_ [_COLORS_]\n> Generate distinct colors.\n\n**random**\n> Generate random color.\n\n**paint** _COLOR_ _TEXT_\n> Print colored text.\n\n**--number** _N_\n> Number of colors to generate.\n\n**--colorspace** _SPACE_\n> Color space for operations."],["DESCRIPTION","**pastel** is a command-line tool for working with colors. It displays color information, converts between formats, generates palettes, and manipulates colors programmatically.\n\nColor input accepts multiple formats: names (red, blue), hex (#ff0000), RGB (rgb(255,0,0)), HSL (hsl(0,100%,50%)), and more. The tool displays color blocks in terminal with accurate previews using true color.\n\nThe color command shows comprehensive information: RGB, HSL, CIELAB values, closest named color, and contrast information. This helps in design work and accessibility checking.\n\nManipulation functions work in perceptually uniform color spaces (CIELAB) for natural-looking results. Lightening, darkening, and saturation changes appear consistent across the spectrum.\n\nPalette generation creates harmonious color schemes. Gradient produces smooth transitions. Distinct generates maximally different colors for visualization. Random creates aesthetic random colors.\n\nThe paint command enables colored terminal output in scripts. Format conversion helps with CSS, code, and design tool integration."],["CAVEATS","Requires true color terminal for accurate display. Terminal color accuracy varies. Color perception depends on display calibration. Some operations may produce out-of-gamut results. Interactive picker requires terminal with mouse support."],["HISTORY","**pastel** was created by **David Peter** (sharkdp) around **2019**. Written in Rust, it's part of a suite of modern CLI tools including fd and bat. The tool addresses the need for color manipulation in terminal workflows, design, and accessibility testing. It emphasizes perceptually uniform color operations."],["INSTALL","```dnf: sudo dnf install pastel```\n\n```pacman: sudo pacman -S pastel```\n\n```apk: sudo apk add pastel```\n\n```zypper: sudo zypper install pastel```\n\n```brew: brew install pastel```\n\n```nix: nix profile install nixpkgs#pastel```"],["SEE ALSO","[color](/man/color)(1), [tput](/man/tput)(1), [convert](/man/convert)(1)"]],"e":["color","tput","convert"],"x":"pastel color [red]"},"pgbackrest":{"n":"pgbackrest","t":"Reliable PostgreSQL backup and restore tool","s":[["TAGLINE","Reliable PostgreSQL backup and restore tool"],["TLDR","**Initialize a new stanza** for a PostgreSQL cluster\n\n```pgbackrest --stanza=[name] stanza-create```\n\n**Verify** that database and archive configuration is correct\n\n```pgbackrest --stanza=[name] check```\n\n**Create a full backup**\n\n```pgbackrest --stanza=[name] --type=full backup```\n\n**Create an incremental backup**\n\n```pgbackrest --stanza=[name] --type=incr backup```\n\n**Restore the latest backup** to PGDATA\n\n```pgbackrest --stanza=[name] restore```\n\n**Restore to a specific point in time**\n\n```pgbackrest --stanza=[name] --type=time --target=\"[2026-04-27 12:00:00]\" restore```\n\n**Show information** about backups in the repository\n\n```pgbackrest --stanza=[name] info```\n\n**Expire** old backups and archived WAL according to retention policy\n\n```pgbackrest --stanza=[name] expire```"],["SYNOPSIS","**pgbackrest** [_options_] _command_"],["PARAMETERS","**backup**\n> Create a full, differential, or incremental backup of a PostgreSQL cluster.\n\n**restore**\n> Restore a backup, optionally to a specific time, name, xid, LSN, or named recovery target.\n\n**archive-push**\n> Push a WAL segment to the repository (called from PostgreSQL **archive_command**).\n\n**archive-get**\n> Retrieve a WAL segment from the repository during recovery (called from **restore_command**).\n\n**stanza-create**\n> Initialize a new stanza, creating the repository layout for a cluster.\n\n**stanza-upgrade**\n> Update stanza metadata after a major PostgreSQL upgrade.\n\n**stanza-delete**\n> Remove a stanza and all its backups and WAL.\n\n**check**\n> Verify configuration, archive command, and repository accessibility.\n\n**info**\n> Display backup and archive information for one or all stanzas.\n\n**verify**\n> Verify integrity of backups and WAL in the repository.\n\n**expire**\n> Apply retention policies, removing old backups and archived WAL.\n\n**repo-ls**, **repo-get**\n> List or fetch files directly from the repository.\n\n**server**, **server-ping**, **start**, **stop**\n> Manage the pgBackRest server (used with TLS-based remote repositories).\n\n**annotate**\n> Add, modify, or remove free-form annotations on a backup.\n\n**--stanza** _name_\n> Stanza (PostgreSQL cluster) to operate on.\n\n**--type** _full|diff|incr|time|name|xid|lsn|immediate|default|standby_\n> Backup type (full/diff/incr) or restore recovery target type.\n\n**--target** _value_\n> Target for **--type=time/name/xid/lsn** restores.\n\n**--config** _file_\n> Path to alternative pgbackrest.conf.\n\n**--config-path** _dir_\n> Base directory for configuration files.\n\n**--repo** _N_\n> Select a specific repository when multiple are configured.\n\n**--process-max** _N_\n> Maximum parallel processes for compression and transfer.\n\n**--compress-type** _none|gz|lz4|zst|bz2_\n> Compression algorithm for backups and WAL.\n\n**--log-level-console** _off|error|warn|info|detail|debug|trace_\n> Verbosity of console logging.\n\n**--delta**\n> Restore only files that differ from the backup (faster repeated restores)."],["DESCRIPTION","**pgBackRest** is a backup and restore solution for PostgreSQL designed for reliability at scale. It supports full, differential, and incremental backups, parallel processing, hardware-accelerated compression (gz, lz4, zstd), local and remote repositories, and storage backends including filesystem, S3, Azure Blob, and Google Cloud Storage. Backups can be verified end-to-end and restored to a point in time using PostgreSQL's recovery facilities.\n\nOperation is organized around a **stanza**, which represents a single PostgreSQL cluster and its backup repository. After **stanza-create**, the **archive_command** in **postgresql.conf** is set to **pgbackrest --stanza=name archive-push %p** so that every WAL segment is shipped to the repository. **backup** then captures the data directory and pairs it with the archived WAL to enable point-in-time recovery via **restore**.\n\npgBackRest is implemented in C with a small Perl helper layer historically; modern versions are pure C. It is widely deployed in PostgreSQL operator stacks (Crunchy Data, CloudNativePG, Zalando) and in production database environments where pg_basebackup is insufficient."],["CAVEATS","The **stanza** name must match the value used in **archive_command** and on every host that touches the repository. Forgetting to run **stanza-upgrade** after a major PostgreSQL upgrade leaves the stanza misaligned with the new cluster. Restore is destructive: by default it overwrites **PGDATA**, so use **--delta** or restore to an empty directory when in doubt. Repository encryption keys, if used, must be backed up out of band — losing them makes the backups unreadable."],["CONFIGURATION","**/etc/pgbackrest/pgbackrest.conf** (or **/etc/pgbackrest.conf**)\n> Default configuration file. Defines stanzas, repository locations, retention, encryption, compression, and PostgreSQL connection details.\n\n**[global]** section\n> Process-wide defaults: **repo1-path**, **repo1-cipher-type**, **process-max**, **log-level-***, **compress-type**.\n\n**[stanza-name]** section\n> Per-stanza settings: **pg1-path**, **pg1-host**, **pg1-port**, **pg1-user**, plus retention overrides such as **repo1-retention-full**."],["HISTORY","**pgBackRest** was created by **David Steele** at **Crunchy Data** and first released in **2013** as a more scalable alternative to **pg_basebackup**-based backup workflows. Originally written in Perl, it was progressively rewritten in **C** for performance, with the migration substantially complete by version **2.0**. It has become a de-facto standard for production PostgreSQL backups and is bundled with the major PostgreSQL Kubernetes operators."],["INSTALL","```apt: sudo apt install pgbackrest```\n\n```pacman: sudo pacman -S pgbackrest```\n\n```apk: sudo apk add pgbackrest```\n\n```zypper: sudo zypper install pgbackrest```\n\n```brew: brew install pgbackrest```\n\n```nix: nix profile install nixpkgs#pgbackrest```"],["SEE ALSO","[pg_basebackup](/man/pg_basebackup)(1), [pg_dump](/man/pg_dump)(1), [postgres](/man/postgres)(1)"]],"e":["pg_basebackup","pg_dump","postgres"],"x":"pgbackrest --stanza=[name] stanza-create"},"php-build":{"n":"php-build","t":"builds PHP versions from source for use with phpenv or standalone","s":[["TAGLINE","builds PHP versions from source for use with phpenv or standalone"],["TLDR","**List available PHP versions**\n\n```php-build --definitions```\n\n**Install PHP version**\n\n```php-build [8.2.0] [~/.phpenv/versions/8.2.0]```\n\n**Install with configure options**\n\n```PHP_BUILD_CONFIGURE_OPTS=\"--with-openssl\" php-build [8.2.0] [/path/to/install]```\n\n**Install specific version with debug**\n\n```php-build -v [8.2.0] [/path/to/install]```"],["SYNOPSIS","**php-build** [_options_] _version_ _install-path_"],["PARAMETERS","**--definitions**\n> List available versions.\n\n**-v**\n> Verbose mode.\n\n**-i** _file_\n> Read custom definitions.\n\n**--pear**\n> Install PEAR."],["DESCRIPTION","**php-build** builds PHP versions from source for use with phpenv or standalone. It handles downloading, configuring, compiling, and installing multiple PHP versions, including selecting the correct OpenSSL/curl/ICU dependencies and applying patches for older releases.\n\nIt is normally invoked via **phpenv install** but can also be run directly to install a PHP version into any prefix."],["ENVIRONMENT","**PHP_BUILD_CONFIGURE_OPTS**\n> Extra options passed to the PHP **./configure** script.\n\n**PHP_BUILD_EXTRA_MAKE_ARGUMENTS**\n> Additional arguments passed to **make** during compilation.\n\n**PHP_BUILD_ROOT**\n> Build root directory."],["CAVEATS","Compilation takes time. Requires build dependencies. Used with phpenv for version management."],["HISTORY","php-build is part of the **phpenv** ecosystem, inspired by ruby-build, for managing multiple PHP versions."],["INSTALL","```dnf: sudo dnf install php```\n\n```pacman: sudo pacman -S php```\n\n```apk: sudo apk add php85```\n\n```brew: brew install php```\n\n```nix: nix profile install nixpkgs#php85```"],["SEE ALSO","[phpenv](/man/phpenv)(1), [php](/man/php)(1), [pecl](/man/pecl)(1)"]],"e":["phpenv","php","pecl"],"x":"php-build --definitions"},"pip-install":{"n":"pip-install","t":"Install Python packages from PyPI and other sources","s":[["TAGLINE","Install Python packages from PyPI and other sources"],["TLDR","**Install package**\n\n```pip install [package]```\n\n**Install specific version**\n\n```pip install [package]==[version]```\n\n**Install from requirements file**\n\n```pip install -r requirements.txt```\n\n**Install in editable mode**\n\n```pip install -e [path]```\n\n**Upgrade package**\n\n```pip install --upgrade [package]```\n\n**Install to user directory**\n\n```pip install --user [package]```"],["SYNOPSIS","**pip** **install** [_options_] _packages_..."],["PARAMETERS","**-r**, **--requirement** _file_\n> Install from requirements file.\n\n**-e**, **--editable** _path_\n> Install in editable/develop mode.\n\n**-U**, **--upgrade**\n> Upgrade package to newest version.\n\n**--user**\n> Install to user site-packages.\n\n**--no-deps**\n> Don't install dependencies.\n\n**--pre**\n> Include pre-release versions.\n\n**-i**, **--index-url** _url_\n> Use custom package index.\n\n**--extra-index-url** _url_\n> Additional package index.\n\n**-t**, **--target** _dir_\n> Install to specific directory.\n\n**--force-reinstall**\n> Reinstall even if up-to-date."],["DESCRIPTION","**pip install** installs Python packages from the Python Package Index (PyPI) and other sources. It handles dependency resolution, downloading, building from source if needed, and placing packages into the target environment.\n\nPackages can be specified by name, version constraints (**==**, **>=**, **~=**), requirements files, local directories, Git URLs, or wheel files. Use **-e** for editable installs during development, which creates a link to the source directory rather than copying files. The **--user** flag installs to the user site-packages directory without requiring root privileges.\n\nBy default, pip resolves and installs all dependencies. Use **--no-deps** to skip dependency installation for tighter control. The **--pre** flag includes pre-release and development versions in the resolution process. Custom package indexes can be specified with **-i** for private registries."],["INSTALL","```apk: sudo apk add py3-pip```"],["SEE ALSO","[pip](/man/pip)(1), [pip-uninstall](/man/pip-uninstall)(1), [pip-download](/man/pip-download)(1)"]],"e":["pip","pip-uninstall","pip-download"],"x":"pip install [package]"},"pjtoppm":{"n":"pjtoppm","t":"Convert HP PaintJet format to PPM","s":[["TAGLINE","Convert HP PaintJet format to PPM"],["TLDR","**Convert an HP PaintJet file to PPM**\n\n```pjtoppm [input.pj] > [output.ppm]```\n\n**Read PaintJet data from stdin**\n\n```cat [input.pj] | pjtoppm > [output.ppm]```\n\n**Pipe into another Netpbm tool** (e.g. to PNG via pnmtopng)\n\n```pjtoppm [input.pj] | pnmtopng > [output.png]```"],["SYNOPSIS","**pjtoppm** [_paintjetfile_]"],["DESCRIPTION","**pjtoppm** reads an HP PaintJet printer file and writes a PPM (Portable Pixmap) image to standard output. If no filename is given, input is read from stdin.\n\nIt is a quick hack that handles only the subset of PaintJet escape sequences needed to decode most raster images; it is not a full PCL interpreter. The resulting PPM can be converted to other formats by the rest of the Netpbm toolchain (for example, **pnmtopng** or **pnmtojpeg**)."],["CAVEATS","Only a limited subset of PaintJet commands is supported, so complex PCL documents may not convert correctly. There are no command-line options other than the optional input file. **ppmtopj** performs the reverse conversion."],["HISTORY","**pjtoppm** was written by Christos Zoulas in 1991 and has shipped as part of the **Netpbm** package ever since. It references the HP PaintJet XL Color Graphics Printer User's Guide."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtopj](/man/ppmtopj)(1), [pcxtoppm](/man/pcxtoppm)(1), [pnmtops](/man/pnmtops)(1)"]],"e":["ppmtopj","pcxtoppm","pnmtops"],"x":"pjtoppm [input.pj] > [output.ppm]"},"pnmtofiasco":{"n":"pnmtofiasco","t":"Convert PNM to FIASCO wavelet format","s":[["TAGLINE","Convert PNM to FIASCO wavelet format"],["TLDR","**Convert PNM to FIASCO format**\n\n```pnmtofiasco [input.pnm] -o [output.fco]```\n\n**Specify quality** (1=worst, 100=best, default 20)\n\n```pnmtofiasco -q [50] [input.pnm] -o [output.fco]```\n\n**Set optimization level** (0=fastest, 3=slowest)\n\n```pnmtofiasco -z [2] [input.pnm] -o [output.fco]```\n\n**Verbose** compression statistics\n\n```pnmtofiasco -V [2] [input.pnm] -o [output.fco]```"],["SYNOPSIS","**pnmtofiasco** [_options_] [_file_]"],["PARAMETERS","**-o, --output-name=**_file_\n> Write FIASCO output to named file. Default is Standard Output.\n\n**-q, --quality=**_N_\n> Quality of compression (1=worst, 100=best). Default is 20.\n\n**-z, --optimize=**_N_\n> Optimization level (0=fastest, 3=slowest). Default is 0.\n\n**-b, --basis-name=**_name_\n> Preload compression basis (e.g., small.fco, medium.fco, large.fco).\n\n**-V, --verbose=**_N_\n> Verbosity level: 0 (no output), 1 (progress meter), 2 (detailed statistics). Default is 1.\n\n**-v, --version**\n> Print version number and exit.\n\n**-h, --info**\n> Print brief help.\n\n**-P, --prediction**\n> Enable predictive coding for video sequences."],["DESCRIPTION","**pnmtofiasco** compresses PBM, PGM, or PPM images, or Standard Input if no file is named, and produces a FIASCO (Fractal Image And Sequence COdec) file on Standard Output. FIASCO uses fractal and wavelet-based compression. Part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[fiascotopnm](/man/fiascotopnm)(1), [pnmtojpeg](/man/pnmtojpeg)(1)"]],"e":["fiascotopnm","pnmtojpeg"],"x":"pnmtofiasco [input.pnm] -o [output.fco]"},"portal":{"n":"portal","t":"Command-line file transfer between computers with end-to-end encryption","s":[["TAGLINE","Command-line file transfer between computers with end-to-end encryption"],["TLDR","**Send files**\n\n```portal send [file1] [file2] [folder1]```\n\n**Receive files using the password**\n\n```portal receive [1-inertia-elliptical-celestial]```\n\n**Run a relay server on a custom port**\n\n```portal serve --port [1337]```"],["SYNOPSIS","**portal** _command_ [_options_] [_args_...]"],["PARAMETERS","**send** _FILES_...\n> Send files or directories to another computer.\n\n**receive** _PASSWORD_\n> Receive files using the temporary password displayed by the sender.\n\n**serve**\n> Run a relay server.\n\n**-r**, **--relay** _ADDRESS_\n> Specify a custom relay server address.\n\n**-y**, **--yes**\n> Auto-overwrite existing files without prompting (receiver only)."],["DESCRIPTION","**portal** is a command-line file transfer utility for sending files between any two computers using temporary passwords. It uses PAKE2 for end-to-end encryption and parallel gzip compression. Portal attempts a direct peer-to-peer connection first, falling back to routing through a relay server when needed."],["HISTORY","**portal** was created by **Arvid Gotthard** (mellonnen) and **Zino Kader** (ZinoKader) and is written in **Go**."],["INSTALL","```brew: brew install portal```\n\n```nix: nix profile install nixpkgs#portal```"],["SEE ALSO","[croc](/man/croc)(1), [scp](/man/scp)(1)"]],"e":["croc","scp"],"x":"portal send [file1] [file2] [folder1]"},"prosodyctl":{"n":"prosodyctl","t":"Control and manage the Prosody XMPP server","s":[["TAGLINE","Control and manage the Prosody XMPP server"],["TLDR","**Start server**\n\n```prosodyctl start```\n\n**Stop server**\n\n```prosodyctl stop```\n\n**Check status**\n\n```prosodyctl status```\n\n**Add user**\n\n```prosodyctl adduser [user@domain.com]```\n\n**Delete user**\n\n```prosodyctl deluser [user@domain.com]```\n\n**Change password**\n\n```prosodyctl passwd [user@domain.com]```\n\n**Check configuration**\n\n```prosodyctl check```\n\n**Reload configuration**\n\n```prosodyctl reload```"],["SYNOPSIS","**prosodyctl** [_options_] _command_ [_args_]"],["PARAMETERS","**start**\n> Start Prosody.\n\n**stop**\n> Stop Prosody.\n\n**restart**\n> Restart Prosody.\n\n**status**\n> Show status.\n\n**reload**\n> Reload configuration.\n\n**adduser** _JID_\n> Create user account.\n\n**deluser** _JID_\n> Delete user account.\n\n**passwd** _JID_\n> Change password.\n\n**check**\n> Validate configuration.\n\n**about**\n> Version and paths.\n\n**cert** _subcommand_ _hosts_\n> Certificate management (request, generate, key, import).\n\n**register** _USER_ _HOST_ _PASSWORD_\n> Register a user non-interactively.\n\n**unregister** _USER_ _HOST_\n> Unregister a user non-interactively.\n\n**shell**\n> Open the Prosody admin shell for runtime inspection.\n\n**--config** _filename_\n> Use the specified config file instead of the default.\n\n**--verbose**\n> Increase log level to show debug messages.\n\n**--quiet**\n> Reduce log level to only show errors."],["DESCRIPTION","**prosodyctl** is the administration tool for the Prosody XMPP/Jabber server, providing service control, user management, and configuration validation from the command line. It can start, stop, restart, and reload the server, with reload applying configuration changes without disconnecting users.\n\nUser management commands handle XMPP account lifecycle: creating accounts with **adduser**, removing them with **deluser**, and resetting passwords with **passwd**. All user operations use JID (Jabber ID) format like **user@domain.com**.\n\nThe **check** command validates the Prosody configuration, DNS records, and TLS certificates before applying changes, catching common setup errors. Certificate management commands help import, generate, and inspect TLS certificates needed for secure XMPP connections."],["CAVEATS","Requires root or appropriate permissions for most operations. Commands like status, reload, and shell require Prosody to be running. JID format (user@domain) required for user management commands."],["HISTORY","**Prosody** was started by **Matthew Wild** in **2008** as a lightweight XMPP server. It's known for being modular, standards-compliant, and memory-efficient."],["INSTALL","```apt: sudo apt install prosody```\n\n```dnf: sudo dnf install prosody```\n\n```pacman: sudo pacman -S prosody```\n\n```apk: sudo apk add prosody```\n\n```zypper: sudo zypper install prosody```\n\n```nix: nix profile install nixpkgs#prosody```"],["SEE ALSO","[ejabberdctl](/man/ejabberdctl)(1), [prosody](/man/prosody)(8)"]],"e":["ejabberdctl","prosody"],"x":"prosodyctl start"},"pw-cli":{"n":"pw-cli","t":"Manage PipeWire objects from command line","s":[["TAGLINE","Manage PipeWire objects from command line"],["TLDR","**List** objects by type\n\n```pw-cli ls [Node|Link|Port|Device]```\n\nShow **info** for specific object\n\n```pw-cli info [4]```\n\nShow **all** objects info\n\n```pw-cli info all```\n\n**Monitor** for changes\n\n```pw-cli -m```\n\nDisplay **help**\n\n```pw-cli help```"],["SYNOPSIS","**pw-cli** [**-m**|**--monitor**] [_command_] [_args_...]"],["COMMANDS","**ls, list-objects _type_**\n> List objects of specified type\n\n**i, info _id_|_all_**\n> Display info for object or all\n\n**create-node _factory_ _props_**\n> Create a new node\n\n**destroy _id_**\n> Destroy an object\n\n**enum-params _id_ _param_**\n> Enumerate object parameters\n\n**set-param _id_ _param_ _value_**\n> Set object parameter"],["PARAMETERS","**-m, --monitor**\n> Monitor for object changes\n\n**-r, --remote _name_**\n> Connect to remote PipeWire instance\n\n**-h, --help**\n> Display help information"],["DESCRIPTION","**pw-cli** is the command-line interface for managing PipeWire. It can list, inspect, create, and destroy PipeWire objects including nodes, links, ports, and devices.\n\nThe tool provides low-level access to PipeWire's object model, useful for debugging, scripting, and understanding the audio graph."],["CAVEATS","Requires running PipeWire daemon. Object IDs may change between sessions. For simpler management use wpctl. Some operations require understanding PipeWire internals."],["HISTORY","**pw-cli** is part of **PipeWire**, the modern multimedia framework for Linux. It provides administrative capabilities similar to pactl for PulseAudio."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pw-top](/man/pw-top)(1), [pw-dump](/man/pw-dump)(1), [wpctl](/man/wpctl)(1), [pipewire](/man/pipewire)(1)"]],"e":["pw-top","pw-dump","wpctl","pipewire"],"x":"pw-cli ls [Node|Link|Port|Device]"},"qm-listsnapshot":{"n":"qm-listsnapshot","t":"List snapshots of a Proxmox VM","s":[["TAGLINE","List snapshots of a Proxmox VM"],["TLDR","**List all snapshots** of a virtual machine with VMID 100\n\n```qm listsnapshot [100]```"],["SYNOPSIS","**qm listsnapshot** _vmid_"],["PARAMETERS","_vmid_\n> The numeric ID of the virtual machine (100 or higher)."],["DESCRIPTION","**qm listsnapshot** lists all snapshots for a specific virtual machine in Proxmox VE. It displays a tree of snapshot names, creation times, and descriptions, showing the parent-child relationship between snapshots.\n\nThe output indicates the current snapshot with an arrow marker. This helps identify available restore points and manage snapshot storage before performing rollbacks or deletions."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-snapshot](/man/qm-snapshot)(1), [qm-delsnapshot](/man/qm-delsnapshot)(1), [qm-rollback](/man/qm-rollback)(1), [qm-clone](/man/qm-clone)(1)"]],"e":["qm","qm-snapshot","qm-delsnapshot","qm-rollback","qm-clone"],"x":"qm listsnapshot [100]"},"qoitopam":{"n":"qoitopam","t":"Convert QOI images to PAM format","s":[["TAGLINE","Convert QOI images to PAM format"],["TLDR","**Convert a QOI file to PAM**\n\n```qoitopam [input.qoi] > [output.pam]```\n\n**Convert from stdin**\n\n```qoitopam < [input.qoi] > [output.pam]```\n\n**Convert and pipe to another Netpbm tool**\n\n```qoitopam [input.qoi] | pamtopng > [output.png]```"],["SYNOPSIS","**qoitopam** [_qoifile_]"],["PARAMETERS","_qoifile_\n> Input QOI file. If omitted, reads from standard input.\n\n**-quiet**\n> Common libnetpbm option; suppress informational messages."],["DESCRIPTION","**qoitopam** converts images from QOI (Quite OK Image) format to PAM (Portable Arbitrary Map) format. QOI is a fast lossless image compression format designed for simplicity and speed, and PAM is the flexible container format used by the Netpbm image processing toolkit.\n\nThe tool reads a QOI file from the specified argument or from standard input if no file is given. It writes PAM data to standard output, making it easy to chain with other Netpbm tools for further image processing and format conversion.\n\nThis program is part of Netpbm."],["CAVEATS","Only format-agnostic libnetpbm options are accepted (notably **-quiet**). For conversion to common formats like PNG or JPEG, pipe through another Netpbm tool such as **pamtopng**."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamtoqoi](/man/pamtoqoi)(1), [pngtopam](/man/pngtopam)(1)"]],"e":["pamtoqoi","pngtopam"],"x":"qoitopam [input.qoi] > [output.pam]"},"rehash":{"n":"rehash","t":"Rebuild the command hash table","s":[["TAGLINE","Rebuild the command hash table"],["TLDR","**Rebuild the command hash table**\n\n```rehash```\n\n**Rehash after installing new software**\n\n```sudo apt install [package] && rehash```"],["SYNOPSIS","**rehash**"],["DESCRIPTION","**rehash** is a shell builtin that rebuilds the internal hash table of executable commands. The shell maintains a hash table mapping command names to their full paths to avoid repeated **$PATH** searches. When new programs are installed or removed, this table can become outdated, causing the shell to report \"command not found\" for newly installed programs or to run stale paths.\n\nRunning **rehash** forces the shell to discard the cached hash table and rediscover all executables in the directories listed in **$PATH**.\n\nIn **zsh**, rehash also regenerates the completion list so that tab completion reflects newly installed commands."],["CAVEATS","In **bash**, the equivalent command is **hash -r**. Most modern shells handle this automatically when **$PATH** is modified, but newly installed programs within existing **$PATH** directories may still require a manual rehash. The command only affects the current shell session."],["HISTORY","Command hashing was introduced in the **Bourne Shell** to improve performance by caching command locations. The **rehash** builtin appeared in **csh** (1978) and was adopted by **tcsh**, **zsh**, and other shells. Bash uses the **hash** builtin instead but supports the same concept."],["SEE ALSO","[hash](/man/hash)(1), [which](/man/which)(1), [type](/man/type)(1), [zsh](/man/zsh)(1), [csh](/man/csh)(1), [tcsh](/man/tcsh)(1)"]],"e":["hash","which","type","zsh","csh","tcsh"],"x":"rehash"},"rg":{"n":"rg","t":"Fast recursive text search with smart defaults","s":[["TAGLINE","Fast recursive text search with smart defaults"],["TLDR","**Search for a pattern** in current directory\n\n```rg \"[pattern]\"```\n\n**Search in specific files or directories**\n\n```rg \"[pattern]\" [path]```\n\n**Search case-insensitively**\n\n```rg -i \"[pattern]\"```\n\n**Search for whole words only**\n\n```rg -w \"[pattern]\"```\n\n**Show line numbers** with matches\n\n```rg -n \"[pattern]\"```\n\n**Show context lines** around matches\n\n```rg -C [3] \"[pattern]\"```\n\n**Search specific file types**\n\n```rg -t [py] \"[pattern]\"```\n\n**Search hidden files and directories**\n\n```rg --hidden \"[pattern]\"```\n\n**List files containing matches** (no content)\n\n```rg -l \"[pattern]\"```\n\n**Count matches per file**\n\n```rg -c \"[pattern]\"```"],["SYNOPSIS","**rg** [_options_] _pattern_ [_path_...]"],["PARAMETERS","**-i**, **--ignore-case**\n> Case-insensitive search\n\n**-S**, **--smart-case**\n> Case-insensitive unless pattern has uppercase\n\n**-w**, **--word-regexp**\n> Match whole words only\n\n**-x**, **--line-regexp**\n> Match whole lines only\n\n**-c**, **--count**\n> Show count of matches per file\n\n**-l**, **--files-with-matches**\n> Show only filenames with matches\n\n**-L**, **--files-without-match**\n> Show only filenames without matches\n\n**-n**, **--line-number**\n> Show line numbers (default when terminal)\n\n**-H**, **--with-filename**\n> Show filename with matches\n\n**-A** _num_, **--after-context**\n> Show num lines after match\n\n**-B** _num_, **--before-context**\n> Show num lines before match\n\n**-C** _num_, **--context**\n> Show num lines before and after match\n\n**-t** _type_, **--type**\n> Search only files of specified type\n\n**-T** _type_, **--type-not**\n> Exclude files of specified type\n\n**-g** _glob_, **--glob**\n> Include/exclude files matching glob\n\n**--hidden**\n> Search hidden files and directories\n\n**-u**, **--unrestricted**\n> Reduce filtering (repeatable: -uu, -uuu)\n\n**-F**, **--fixed-strings**\n> Treat pattern as literal string\n\n**-e** _pattern_\n> Specify pattern (allows multiple)\n\n**-r** _replacement_, **--replace**\n> Replace matches with text\n\n**-o**, **--only-matching**\n> Show only matched parts"],["DESCRIPTION","**rg** (ripgrep) is a fast, recursive grep alternative that respects gitignore rules by default. It searches directories for regex patterns, automatically skipping hidden files, binary files, and ignored paths.\n\nRipgrep uses Rust's regex engine for speed and supports most Perl-compatible regex syntax. It's designed for searching codebases, automatically detecting and skipping binary files and respecting .gitignore patterns.\n\nMultiple patterns can be specified with **-e**. File type filtering uses built-in definitions (use **rg --type-list** to see available types). Custom globs with **-g** provide flexible include/exclude patterns.\n\nOutput is colorized by default when writing to a terminal. Use **--color=never** for scripts or **--color=always** for piping to pagers."],["CAVEATS","By default, ripgrep skips hidden files, gitignored files, and binary files. Use **--hidden**, **--no-ignore**, or **-u** to search more comprehensively.\n\nRipgrep uses Rust regex syntax, which differs slightly from PCRE. Some advanced features like backreferences are not supported.\n\nFor very large result sets, consider **-l** (files only) or **-c** (counts) to reduce output volume."],["HISTORY","Ripgrep was created by **Andrew Gallant** (BurntSushi) and first released in **2016**. Written in Rust, it combines the usability of The Silver Searcher (ag) with the raw speed of GNU grep, while adding gitignore support. It has become one of the most popular grep alternatives."],["INSTALL","```apt: sudo apt install ripgrep```\n\n```dnf: sudo dnf install ripgrep```\n\n```pacman: sudo pacman -S ripgrep```\n\n```apk: sudo apk add ripgrep```\n\n```zypper: sudo zypper install ripgrep```\n\n```brew: brew install ripgrep```\n\n```nix: nix profile install nixpkgs#ripgrep```"],["SEE ALSO","[grep](/man/grep)(1), [ag](/man/ag)(1), [ack](/man/ack)(1), [git-grep](/man/git-grep)(1)"]],"e":["grep","ag","ack","git-grep"],"x":"rg \"[pattern]\""},"strace":{"n":"strace","t":"Trace system calls and signals","s":[["TAGLINE","Trace system calls and signals"],["TLDR","**Attach** to a running process\n\n```sudo strace -p 1234```\n\nTrace process with **filtered** system calls\n\n```sudo strace -p 1234 -e read,write```\n\nShow **summary** of calls and errors\n\n```sudo strace -p 1234 -c```\n\nShow **time spent** in each call\n\n```sudo strace -p 1234 -T -s 32```\n\n**Trace** a program from start\n\n```strace ./program```\n\nTrace **file operations** only\n\n```strace -e trace=file ./program```\n\nTrace **network** operations with child processes\n\n```strace -f -e trace=network -o trace.txt ./program```"],["SYNOPSIS","**strace** [_-e expr_] [_-o file_] [_-p pid_] [_-f_] [_-c_] [_command_ [_args_]]"],["DESCRIPTION","**strace** intercepts and records system calls made by a process and the signals received by it. It is invaluable for debugging, diagnostics, and understanding system behavior without requiring source code access."],["PARAMETERS","**-p, --attach pid**\n> Attach to running process with given PID\n\n**-f, --follow-forks**\n> Trace child processes created by fork/vfork/clone\n\n**-e trace=set**\n> Trace only specified system calls (file, network, process, etc.)\n\n**-e signal=set**\n> Trace only specified signals\n\n**-o, --output file**\n> Write trace output to file\n\n**-c, --summary-only**\n> Display summary statistics at exit\n\n**-C, --summary**\n> Display summary along with normal output\n\n**-t, -tt, -ttt**\n> Add timestamps (-t: HH:MM:SS, -tt: with microseconds, -ttt: epoch seconds)\n\n**-T, --syscall-times**\n> Show time spent in each system call\n\n**-s, --string-limit size**\n> Maximum string size to print (default 32)\n\n**-v, --no-abbrev**\n> Display unabbreviated output\n\n**-y, --decode-fds**\n> Print file paths associated with file descriptors\n\n**-z, --successful-only**\n> Show only successful system calls\n\n**-Z, --failed-only**\n> Show only failed system calls\n\n**-P, --trace-path path**\n> Trace only syscalls accessing given path\n\n**-k, --stack-traces**\n> Print stack trace for each system call\n\n**-u, --user username**\n> Run command as specified user"],["TRACE EXPRESSIONS","**%file**: File operations (open, stat, chmod, etc.)\n**%network**: Network operations (socket, connect, etc.)\n**%process**: Process operations (fork, exec, etc.)\n**%memory**: Memory mapping operations\n**%signal**: Signal-related calls\n**%ipc**: IPC operations (msgget, semop, etc.)\n**%desc**: File descriptor operations (read, write, select, etc.)"],["CAVEATS","Tracing significantly slows down the traced process. Do not use on production systems without care. Use **-f** to trace forked children or some calls may be missed."],["HISTORY","**strace** was originally written for SunOS by Paul Kranenburg in 1991. It has become an essential debugging tool on Linux systems."],["INSTALL","```apt: sudo apt install strace```\n\n```dnf: sudo dnf install strace```\n\n```pacman: sudo pacman -S strace```\n\n```apk: sudo apk add strace```\n\n```zypper: sudo zypper install strace```\n\n```brew: brew install strace```\n\n```nix: nix profile install nixpkgs#strace```"],["SEE ALSO","[ltrace](/man/ltrace)(1), [ptrace](/man/ptrace)(2), [perf](/man/perf)(1)"]],"e":["ltrace","ptrace","perf"],"x":"sudo strace -p 1234"},"supervisord":{"n":"supervisord","t":"Process control and auto-restart daemon","s":[["TAGLINE","Process control and auto-restart daemon"],["TLDR","**Start supervisord**\n\n```supervisord```\n\n**Use config file**\n\n```supervisord -c [/etc/supervisor/supervisord.conf]```\n\n**Run in foreground**\n\n```supervisord -n```\n\n**Debug mode**\n\n```supervisord -e debug```\n\n**Check configuration**\n\n```supervisord -c [supervisord.conf] -t```"],["SYNOPSIS","**supervisord** [_-c config_] [_-n_] [_-e level_] [_options_]"],["PARAMETERS","**-c** _FILE_\n> Configuration file.\n\n**-n**\n> Run in foreground.\n\n**-e** _LEVEL_\n> Log level.\n\n**-t**\n> Test configuration.\n\n**-d** _DIR_\n> Working directory.\n\n**-u** _USER_\n> Run as user.\n\n**-j** _FILE_\n> Pidfile path."],["DESCRIPTION","**supervisord** is a process control daemon that manages long-running processes, automatically restarting them if they crash or exit unexpectedly. It is widely used to run application servers, background workers, and other services that need reliable process supervision.\n\nThe configuration file defines programs with their commands, environment variables, user permissions, and restart policies. Programs can be organized into groups for coordinated management. The daemon captures stdout and stderr from managed processes, writing them to log files with configurable rotation.\n\nEvent listeners allow custom scripts to respond to process state changes such as crashes or transitions. The daemon communicates with supervisorctl through a Unix socket or TCP connection, and provides an optional web interface for process management."],["CONFIGURATION","**/etc/supervisor/supervisord.conf**\n> Main configuration file defining programs, groups, logging, and socket settings.\n\n**/etc/supervisor/conf.d/*.conf**\n> Drop-in directory for individual program configuration files."],["CAVEATS","Not a replacement for init systems. Single point of failure. Needs its own monitoring."],["HISTORY","**Supervisor** was created by **Chris McDonough** (Agendaless Consulting). It provides simple process management for Python applications and beyond."],["INSTALL","```dnf: sudo dnf install supervisor```\n\n```pacman: sudo pacman -S supervisor```\n\n```apk: sudo apk add supervisor```\n\n```zypper: sudo zypper install supervisor```\n\n```brew: brew install supervisor```"],["SEE ALSO","[supervisorctl](/man/supervisorctl)(1)"]],"e":["supervisorctl"],"x":"supervisord"},"systemd-growfs":{"n":"systemd-growfs","t":"Grow filesystem to fill partition","s":[["TAGLINE","Grow filesystem to fill partition"],["TLDR","**Grow root filesystem to fill partition**\n\n```/usr/lib/systemd/systemd-growfs /```\n\n**Grow filesystem at mount point**\n\n```/usr/lib/systemd/systemd-growfs [/home]```\n\n**Enable automatic grow at boot**\n\n```systemctl enable systemd-growfs-root.service```\n\n**Check service status**\n\n```systemctl status systemd-growfs-root.service```"],["SYNOPSIS","**/usr/lib/systemd/systemd-growfs** _mountpoint_"],["DESCRIPTION","**systemd-growfs** is a systemd utility that instructs the kernel to grow a mounted filesystem to fill its underlying block device. It is typically used after partition resizing to expand the filesystem to use the newly available space.\n\nThe utility is usually invoked automatically by systemd services based on the **x-systemd.growfs** mount option in /etc/fstab. It can also be triggered by GPT partition flag 59 as defined in the Discoverable Partitions Specification.\n\nSupported filesystems include ext4, btrfs, xfs, and dm-crypt encrypted partitions. The filesystem must already be mounted for the operation to succeed."],["FSTAB USAGE","Add x-systemd.growfs option to /etc/fstab:\n\n```\n/dev/sda2  /  ext4  defaults,x-systemd.growfs  0  1\n```"],["RELATED SERVICES","**systemd-growfs@.service**: Template service for arbitrary mount points.\n\n**systemd-growfs-root.service**: Service for growing the root filesystem.\n\n**systemd-makefs@.service**: Creates filesystems on first boot."],["CAVEATS","Only works with mounted filesystems. Only supports ext4, btrfs, xfs, and dm-crypt. The underlying partition must already be resized before growing the filesystem. Errors during filesystem growth produce warnings but don't fail the mount."],["HISTORY","**systemd-growfs** was introduced as part of systemd's image-based deployment and container features. It enables automatic filesystem expansion for cloud instances and embedded systems that need to grow their filesystem after initial provisioning."],["SEE ALSO","[systemctl](/man/systemctl)(1), [resize2fs](/man/resize2fs)(8), [systemd-repart](/man/systemd-repart)(8), [parted](/man/parted)(8)"]],"e":["systemctl","resize2fs","systemd-repart","parted"],"x":"/usr/lib/systemd/systemd-growfs /"},"thc-ipv6":{"n":"thc-ipv6","t":"IPv6 security testing toolkit","s":[["TAGLINE","IPv6 security testing toolkit"],["TLDR","**Scan for alive IPv6 hosts** on a network\n\n```atk6-alive6 [eth0]```\n\n**Trace route to IPv6 host**\n\n```atk6-trace6 [eth0] [target]```\n\n**Convert MAC address** to IPv6 link-local\n\n```atk6-address6 [00:11:22:33:44:55]```\n\n**Flood target with router advertisements**\n\n```atk6-flood_router6 [eth0] [target]```\n\n**Spoof neighbor advertisements** (MITM)\n\n```atk6-parasite6 [eth0]```\n\n**Announce as a fake router**\n\n```atk6-fake_router6 [eth0] [network/64]```\n\n**Test known IPv6 vulnerabilities**\n\n```atk6-exploit6 [eth0] [target]```"],["SYNOPSIS","**atk6-**_tool_ [_options_] [_interface_] [_arguments_]"],["TOOLS","**alive6**\n> Detect alive hosts on the local network segment\n\n**trace6**\n> Fast IPv6 traceroute with ICMP6 or TCP-SYN\n\n**address6**\n> Convert between MAC, IPv4, and IPv6 addresses\n\n**fake_router6**\n> Announce as a router on the network\n\n**flood_router6**\n> Flood target with random router advertisements\n\n**flood_advertise6**\n> Flood target with random neighbor advertisements\n\n**parasite6**\n> ICMP neighbor spoofer for MITM attacks\n\n**redir6**\n> ICMPv6 redirect spoofer for MITM\n\n**dos-new-ip6**\n> DoS by telling new devices their IP collides\n\n**denial6**\n> Prevent new IPv6 interfaces with DAD attacks\n\n**fake_mld6**\n> Join multicast groups on the network\n\n**exploit6**\n> Test known IPv6 implementation vulnerabilities\n\n**ndpexhaust26**\n> Exhaust NDP tables with ICMPv6 flooding\n\n**rsmurf6**\n> Remote smurf attack over IPv6"],["DESCRIPTION","**thc-ipv6** is a toolkit for testing IPv6 and ICMPv6 protocol weaknesses. It contains various tools for network reconnaissance, denial of service, man-in-the-middle attacks, and vulnerability testing on IPv6 networks.\n\nOn Debian-based systems, tools are prefixed with **atk6-** (e.g., **atk6-alive6**). Each tool has its own options; run with **-h** for help. The toolkit exploits features of IPv6 that differ from IPv4, including neighbor discovery, router advertisements, and multicast.\n\nThe library **thc-ipv6-lib.c** enables custom tool development with minimal code for crafting IPv6/ICMPv6 packets. Environment variables **THC_IPV6_PPPOE** and **THC_IPV6_6IN4** configure tunnel support."],["CAVEATS","These tools are for authorized security testing only. IPv6 attacks can disrupt network connectivity for all users on a segment. Many attacks require root privileges and direct network access. Some tools may crash vulnerable systems. Detection and logging of IPv6 attacks varies by network infrastructure."],["HISTORY","THC-IPv6 was created by **van Hauser** (Marc Heuse) of **The Hacker's Choice** (THC), a German hacker collective. The toolkit was developed to demonstrate IPv6 security weaknesses as organizations began IPv6 deployment. It highlights that IPv6 introduces new attack surfaces absent in IPv4 networks, particularly around neighbor discovery and router advertisement protocols."],["INSTALL","```dnf: sudo dnf install thc-ipv6```\n\n```pacman: sudo pacman -S thc-ipv6```\n\n```nix: nix profile install nixpkgs#thc-ipv6```"],["SEE ALSO","[nmap](/man/nmap)(1), [ip](/man/ip)(8), [ping6](/man/ping6)(8), [traceroute6](/man/traceroute6)(8)"]],"e":["nmap","ip","ping6","traceroute6"],"x":"atk6-alive6 [eth0]"},"tor-browser":{"n":"tor-browser","t":"Privacy-focused anonymous web browser","s":[["TAGLINE","Privacy-focused anonymous web browser"],["TLDR","**Launch Tor Browser**\n\n```tor-browser```\n\n**Launch with a specific profile**\n\n```tor-browser --profile [path/to/profile]```\n\n**Launch and open a URL**\n\n```tor-browser [url]```\n\n**Launch in offline mode**\n\n```tor-browser --offline```\n\n**Launch with new instance** (separate from existing)\n\n```tor-browser --new-instance```\n\n**Show version information**\n\n```tor-browser --version```"],["SYNOPSIS","**tor-browser** [_options_] [_url_]"],["PARAMETERS","**--profile** _path_\n> Start with the specified profile directory.\n\n**--new-instance**\n> Open a new browser instance instead of using an existing one.\n\n**--offline**\n> Start in offline mode.\n\n**--private-window**\n> Open a private browsing window.\n\n**--safe-mode**\n> Start with all extensions disabled.\n\n**--headless**\n> Run without a visible UI (for automation).\n\n**--version**\n> Display version information.\n\n**--help**\n> Display help message."],["DESCRIPTION","**Tor Browser** is a privacy-focused web browser based on Mozilla Firefox that routes all traffic through the Tor anonymity network. It is pre-configured with privacy protections including NoScript, HTTPS-Only mode, and anti-fingerprinting measures.\n\nThe browser is typically installed via **torbrowser-launcher** on Debian/Ubuntu systems or from the official tarball. When installed via the launcher, the command **torbrowser-launcher** handles downloading, verifying, updating, and starting the browser. From a manual installation, the browser is started using **./start-tor-browser.desktop** from the extracted directory.\n\nTor Browser automatically connects to the Tor network on startup, encrypting traffic through multiple relays to anonymize the user's location and browsing activity. It includes a circuit display showing the relay path and allows requesting new circuits for different exit nodes."],["CAVEATS","Tor Browser should not be used with other browsers simultaneously for activities requiring anonymity, as this can compromise privacy. Downloading files or enabling plugins may expose your real IP address. Performance is slower than regular browsers due to traffic routing through multiple relays. The browser is designed for anonymity, not just privacy."],["HISTORY","**Tor Browser** was first released by **The Tor Project** in **2008**, initially as a bundle combining Firefox with Tor. It evolved from earlier tools like Torbutton and became the officially recommended way to browse anonymously using Tor. The project continues active development with regular security updates and Firefox ESR rebases."],["INSTALL","```nix: nix profile install nixpkgs#tor-browser```"],["SEE ALSO","[tor](/man/tor)(1), [torbrowser-launcher](/man/torbrowser-launcher)(1), [torify](/man/torify)(1), [firefox](/man/firefox)(1)"]],"e":["tor","torbrowser-launcher","torify","firefox"],"x":"tor-browser"},"tune2fs":{"n":"tune2fs","t":"Adjust ext2/ext3/ext4 filesystem parameters","s":[["TAGLINE","Adjust ext2/ext3/ext4 filesystem parameters"],["TLDR","Set max **mount count** before check\n\n```sudo tune2fs -c 2 /dev/sdXN```\n\nSet filesystem **label**\n\n```sudo tune2fs -L 'MY_LABEL' /dev/sdXN```\n\nEnable **discard** and extended attributes\n\n```sudo tune2fs -o discard,user_xattr /dev/sdXN```\n\nEnable **journaling**\n\n```sudo tune2fs -o has_journal /dev/sdXN```\n\nAssign new **random UUID**\n\n```sudo tune2fs -U random /dev/sdXN```\n\nSet **reserved blocks** percentage\n\n```sudo tune2fs -m 1 /dev/sdXN```"],["SYNOPSIS","**tune2fs** [_options_] _device_"],["DESCRIPTION","**tune2fs** adjusts tunable filesystem parameters on ext2, ext3, or ext4 filesystems. Many parameters can be changed on mounted filesystems, but some require the filesystem to be unmounted."],["PARAMETERS","**-c MAX-MOUNT-COUNTS**\n> Set maximum mount count before fsck\n\n**-C MOUNT-COUNT**\n> Set current mount count\n\n**-e ERROR-BEHAVIOR**\n> Set error behavior (continue, remount-ro, panic)\n\n**-E EXTENDED-OPTIONS**\n> Set extended options\n\n**-f**\n> Force operation on filesystems with errors\n\n**-i INTERVAL**\n> Time between filesystem checks (d/w/m for days/weeks/months)\n\n**-j**\n> Add ext3 journal to ext2 filesystem\n\n**-J JOURNAL-OPTIONS**\n> Set journal options\n\n**-l**\n> List filesystem superblock contents\n\n**-L LABEL**\n> Set filesystem label\n\n**-m RESERVED-BLOCKS-PERCENTAGE**\n> Set percentage of reserved blocks\n\n**-o OPTIONS**\n> Set/clear default mount options\n\n**-O FEATURES**\n> Set/clear filesystem features\n\n**-r RESERVED-BLOCKS-COUNT**\n> Set number of reserved blocks\n\n**-T TIME**\n> Set time of last filesystem check\n\n**-u USER**\n> Set user who can use reserved blocks\n\n**-U UUID**\n> Set filesystem UUID"],["CAVEATS","Some changes require unmounting the filesystem. Changing certain features incorrectly can make the filesystem unmountable. Always backup important data before making changes."],["HISTORY","**tune2fs** is part of **e2fsprogs**, the ext2/3/4 filesystem utilities package."],["INSTALL","```apt: sudo apt install e2fsprogs```\n\n```dnf: sudo dnf install e2fsprogs```\n\n```pacman: sudo pacman -S e2fsprogs```\n\n```apk: sudo apk add e2fsprogs-extra```\n\n```zypper: sudo zypper install e2fsprogs```\n\n```brew: brew install e2fsprogs```\n\n```nix: nix profile install nixpkgs#e2fsprogs```"],["SEE ALSO","[e2fsck](/man/e2fsck)(8), [dumpe2fs](/man/dumpe2fs)(8), [mke2fs](/man/mke2fs)(8)"]],"e":["e2fsck","dumpe2fs","mke2fs"],"x":"sudo tune2fs -c 2 /dev/sdXN"},"up":{"n":"up","t":"Write Linux pipes interactively with live preview","s":[["TAGLINE","Write Linux pipes interactively with live preview"],["TLDR","**Interactively build a pipeline from command output**\n\n```lshw |& up```\n\n**Process CSV data interactively**\n\n```cat [data.csv] | up```"],["SYNOPSIS","_command_ | **up**"],["DESCRIPTION","**up** (Ultimate Plumber) lets you build complex shell pipelines interactively by showing instant live preview of command results as you type. The pipeline is passed to bash, so all bash features work. Press **Ctrl-X** to save the final pipeline to a script file. The input buffer is capped at 40 MB."],["HISTORY","**up** was created by **Mateusz Czaplinski** (akavel) and is written in **Go**."],["INSTALL","```pacman: sudo pacman -S up```\n\n```brew: brew install up```\n\n```nix: nix profile install nixpkgs#up```"],["SEE ALSO","[watch](/man/watch)(1), [fzf](/man/fzf)(1), [pv](/man/pv)(1)"]],"e":["watch","fzf","pv"],"x":"lshw |& up"},"uuidgen":{"n":"uuidgen","t":"Generate new UUID values","s":[["TAGLINE","Generate new UUID values"],["TLDR","**Generate** a random UUID (v4)\n\n```uuidgen -r```\n\n**Generate** a time-based UUID (v1)\n\n```uuidgen -t```\n\n**Generate** a time-based sortable UUID (v7)\n\n```uuidgen -7```\n\n**Generate** a SHA1-based UUID (v5) from a namespace and name\n\n```uuidgen -s -n @dns -N [example.com]```\n\n**Generate** a MD5-based UUID (v3) from a namespace and name\n\n```uuidgen -m -n @url -N [https://example.com]```\n\n**Generate** multiple time-based UUIDs at once\n\n```uuidgen -t -C [5]```"],["SYNOPSIS","**uuidgen** [_options_]"],["PARAMETERS","**-r, --random**\n> Generate random UUID (version 4)\n\n**-t, --time**\n> Generate time-based UUID (version 1)\n\n**-7, --time-v7**\n> Generate time-based UUID (version 7), lexicographically sortable by timestamp\n\n**-s, --sha1**\n> Generate SHA1-based UUID (version 5)\n\n**-m, --md5**\n> Generate MD5-based UUID (version 3)\n\n**-n, --namespace _ns_**\n> Namespace for name-based UUIDs (@dns, @url, @oid, @x500)\n\n**-N, --name _name_**\n> Name for name-based UUIDs\n\n**-x, --hex**\n> Interpret name as a hexadecimal string\n\n**-C, --count _num_**\n> Generate multiple UUIDs\n\n**-h, --help**\n> Display help text and exit\n\n**-V, --version**\n> Print version and exit"],["DESCRIPTION","**uuidgen** creates a new universally unique identifier (UUID). By default it generates a random UUID (version 4). Time-based (v1) UUIDs include timestamp and MAC address. Name-based UUIDs (v3/v5) are deterministic, generating the same UUID for the same namespace and name combination.\n\nUUIDs are 128-bit identifiers commonly used for unique identification in databases, distributed systems, and file systems."],["UUID VERSIONS","**v1**: Time and MAC address based\n**v3**: MD5 hash of namespace and name\n**v4**: Random\n**v5**: SHA1 hash of namespace and name\n**v7**: Time-based, lexicographically sortable (RFC 9562)"],["CAVEATS","Time-based UUIDs expose the MAC address of the generating machine. For privacy, prefer random UUIDs. Part of the util-linux package."],["INSTALL","```apt: sudo apt install uuid-runtime```\n\n```apk: sudo apk add uuidgen```"],["SEE ALSO","[uuid](/man/uuid)(1)"]],"e":["uuid"],"x":"uuidgen -r"},"valheim":{"n":"valheim","t":"Dedicated Valheim game server","s":[["TAGLINE","Dedicated Valheim game server"],["TLDR","**Start** the server\n\n```[path/to]/start_server.sh```\n\n**Stop** server gracefully\n\n```<Ctrl c>```\n\nStart with **Docker** script\n\n```[path/to/docker_start_server.sh] [path/to/start_server.sh]```"],["SYNOPSIS","**start_server.sh** [_OPTIONS_]"],["DESCRIPTION","**Valheim** dedicated server creates and runs a headless Valheim game server. Players can connect to the server for multiplayer survival gameplay.\n\nServer configuration is done by editing a copy of the start_server.sh script, where you can set the server name, world name, password, and port."],["CAVEATS","Requires a valid Steam account and Valheim dedicated server files. Server configuration requires editing the startup script. Adequate system resources needed for hosting multiple players."],["HISTORY","**Valheim** is a survival and exploration game developed by **Iron Gate AB**, released in early access in **2021**. The dedicated server allows hosting persistent worlds."],["SEE ALSO","[screen](/man/screen)(1), [tmux](/man/tmux)(1)"]],"e":["screen","tmux"],"x":"[path/to]/start_server.sh"},"vboxmanage-controlvm":{"n":"vboxmanage-controlvm","t":"Control running VirtualBox virtual machines","s":[["TAGLINE","Control running VirtualBox virtual machines"],["TLDR","**Pause VM**\n\n```vboxmanage controlvm [vmname] pause```\n\n**Resume VM**\n\n```vboxmanage controlvm [vmname] resume```\n\n**Reset VM**\n\n```vboxmanage controlvm [vmname] reset```\n\n**Power off VM**\n\n```vboxmanage controlvm [vmname] poweroff```\n\n**Save state**\n\n```vboxmanage controlvm [vmname] savestate```\n\n**Send ACPI shutdown** signal (graceful)\n\n```vboxmanage controlvm [vmname] acpipowerbutton```\n\n**Take screenshot**\n\n```vboxmanage controlvm [vmname] screenshotpng [file.png]```\n\n**Set shared clipboard mode**\n\n```vboxmanage controlvm [vmname] clipboard mode bidirectional```\n\n**Connect/disconnect network cable**\n\n```vboxmanage controlvm [vmname] setlinkstate1 [on|off]```"],["SYNOPSIS","**vboxmanage** **controlvm** _vmname_ _command_ [_options_]"],["PARAMETERS","**pause**\n> Suspend VM execution.\n\n**resume**\n> Resume paused VM.\n\n**reset**\n> Hard reset VM.\n\n**poweroff**\n> Force power off.\n\n**savestate**\n> Save and stop VM.\n\n**acpipowerbutton**\n> Send ACPI shutdown.\n\n**screenshotpng** _file_\n> Capture screen.\n\n**setvideomodehint** _w_ _h_ _bpp_\n> Set video mode.\n\n**clipboard mode** _disabled|hosttoguest|guesttohost|bidirectional_\n> Set shared clipboard mode.\n\n**setlinkstate**_N_ _on|off_\n> Connect or disconnect virtual network cable N.\n\n**nic**_N_ _null|nat|bridged|intnet|hostonly_\n> Change the type of network adapter N.\n\n**nicpromisc**_N_ _deny|allow-vms|allow-all_\n> Set promiscuous mode for network adapter N.\n\n**draganddrop** _disabled|hosttoguest|guesttohost|bidirectional_\n> Set drag and drop mode."],["DESCRIPTION","**vboxmanage controlvm** controls running virtual machines. Manage VM state, send input, modify settings on running VMs. Central command for VM runtime control."],["CAVEATS","The **poweroff** and **reset** commands are equivalent to pulling the power cord — use **acpipowerbutton** for graceful shutdown. The VM must be in a running state for most commands. Network-related changes (nic, setlinkstate) take effect immediately on the running VM."],["SEE ALSO","[vboxmanage](/man/vboxmanage)(1), [vboxmanage-startvm](/man/vboxmanage-startvm)(1), [vboxmanage-modifyvm](/man/vboxmanage-modifyvm)(1), [vboxmanage-showvminfo](/man/vboxmanage-showvminfo)(1)"]],"e":["vboxmanage","vboxmanage-startvm","vboxmanage-modifyvm","vboxmanage-showvminfo"],"x":"vboxmanage controlvm [vmname] pause"},"vboxmanage-movevm":{"n":"vboxmanage-movevm","t":"Relocate VirtualBox VM files","s":[["TAGLINE","Relocate VirtualBox VM files"],["TLDR","**Move a VM to a new folder**\n\n```VBoxManage movevm [vmname] --type basic --folder [/path/to/new/location]```\n\n**Move a VM identified by UUID**\n\n```VBoxManage movevm [uuid] --type basic --folder [/path/to/new/location]```"],["SYNOPSIS","**VBoxManage** **movevm** _vm_ **--type** basic [**--folder** _path_]"],["PARAMETERS","**--type** basic\n> Specifies the type of move operation. Currently only \"basic\" is supported.\n\n**--folder** _path_\n> Destination folder on the host file system. Can be a relative or absolute path."],["DESCRIPTION","**VBoxManage movevm** moves a virtual machine to a new location on the host. All associated files of the VM, including settings files and disk image files, are moved to the new location. The Oracle VM VirtualBox configuration is updated automatically. The VM can be identified by name or UUID. VM names containing spaces must be enclosed in double quotation marks."],["SEE ALSO","[vboxmanage](/man/vboxmanage)(1), [vboxmanage-clonevm](/man/vboxmanage-clonevm)(1)"]],"e":["vboxmanage","vboxmanage-clonevm"],"x":"VBoxManage movevm [vmname] --type basic --folder [/path/to/new/location]"},"winrm":{"n":"winrm","t":"Remote Windows management via WS-Management","s":[["TAGLINE","Remote Windows management via WS-Management"],["TLDR","**Run a command** on a Windows host (via pywinrm Python library)\n\n```python -c \"import winrm; s = winrm.Session('[host]', auth=('[user]', '[pass]')); print(s.run_cmd('ipconfig').std_out)\"```\n\n**Run PowerShell command** on remote Windows host\n\n```python -c \"import winrm; s = winrm.Session('[host]', auth=('[user]', '[pass]')); print(s.run_ps('Get-Process').std_out)\"```\n\n**Connect using HTTPS** (port 5986)\n\n```python -c \"import winrm; s = winrm.Session('[host]', auth=('[user]', '[pass]'), transport='ssl'); print(s.run_cmd('hostname').std_out)\"```\n\n**Connect with NTLM authentication**\n\n```python -c \"import winrm; s = winrm.Session('[host]', auth=('[domain\\\\user]', '[pass]'), transport='ntlm'); print(s.run_cmd('whoami').std_out)\"```"],["SYNOPSIS","**winrm** (via pywinrm Python library)\n\n```python\nimport winrm\nsession = winrm.Session(_host_, auth=(_user_, _password_), transport=_method_)\nresult = session.run_cmd(_command_, [_args_])\nresult = session.run_ps(_powershell_script_)\n```"],["PARAMETERS","**host**\n> Windows target hostname or IP address.\n\n**auth**\n> Tuple of (username, password) for authentication.\n\n**transport**\n> Authentication method: basic, ntlm, kerberos, ssl, or credssp.\n\n**run_cmd**(_command_, [_args_])\n> Execute a Windows command (cmd.exe).\n\n**run_ps**(_script_)\n> Execute a PowerShell script.\n\n**std_out**\n> Standard output from command execution.\n\n**std_err**\n> Standard error from command execution.\n\n**status_code**\n> Exit code of the executed command."],["DESCRIPTION","**pywinrm** is a Python library for Windows Remote Management (WinRM), enabling command execution on Windows machines from Linux or other systems. WinRM is Microsoft's implementation of WS-Management protocol, providing remote management capabilities similar to SSH.\n\nThe library supports multiple authentication methods: Basic (requires HTTPS or unencrypted setup), NTLM (works with domain and local accounts), Kerberos (for domain environments), and CredSSP (allows credential delegation). Connection uses HTTP port 5985 or HTTPS port 5986.\n\npywinrm is extensively used by automation tools like Ansible for Windows management. It can execute both cmd.exe commands (run_cmd) and PowerShell scripts (run_ps), returning output, errors, and exit codes.\n\nWindows hosts must have WinRM enabled (Enable-PSRemoting -Force) and configured to accept the chosen authentication method."],["CAVEATS","WinRM communication is unencrypted over HTTP by default; use HTTPS or NTLM/Kerberos transport for security. Basic authentication requires explicit enabling on Windows hosts. Kerberos requires proper domain configuration and kinit initialization. Firewall rules must allow WinRM ports (5985/5986)."],["HISTORY","**WinRM** (Windows Remote Management) was introduced by **Microsoft** in **Windows Vista** and **Server 2008** as part of the WS-Management standard. **pywinrm** was developed to enable WinRM access from Python on non-Windows systems. The library became essential for cross-platform Windows management, particularly gaining prominence through **Ansible's** Windows support starting around **2014**."],["SEE ALSO","[ssh](/man/ssh)(1), [ansible](/man/ansible)(1), [powershell](/man/powershell)(1)"]],"e":["ssh","ansible","powershell"],"x":"python -c \"import winrm; s = winrm.Session('[host]', auth=('[user]', '[pass]')); print(s.run_cmd('ipconfig').std_out)\""}});
