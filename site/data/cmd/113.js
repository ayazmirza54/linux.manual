window.LCL_BUCKET(113,{"aa-teardown":{"n":"aa-teardown","t":"Unload all AppArmor profiles and disable enforcement","s":[["TAGLINE","Unload all AppArmor profiles and disable enforcement"],["TLDR","**Disable** all AppArmor profiles and stop enforcement\n\n```sudo aa-teardown```"],["SYNOPSIS","**aa-teardown**"],["DESCRIPTION","**aa-teardown** tears down all AppArmor profiles and disables AppArmor enforcement. This command unloads all currently loaded profiles from the kernel, effectively removing AppArmor security restrictions from all confined processes.\n\nThis is typically used for troubleshooting when AppArmor profiles are causing issues, or when completely disabling AppArmor on a system."],["CAVEATS","Running this command removes **all AppArmor security protections** from the system. Applications will run without any AppArmor confinement until profiles are reloaded."],["HISTORY","Part of the **AppArmor** utilities package for managing application security profiles on Linux systems."],["INSTALL","```apt: sudo apt install apparmor```\n\n```pacman: sudo pacman -S apparmor```"],["SEE ALSO","[aa-status](/man/aa-status)(8), [aa-enforce](/man/aa-enforce)(8), [aa-complain](/man/aa-complain)(8), [apparmor](/man/apparmor)(7)"]],"e":["aa-status","aa-enforce","aa-complain","apparmor"],"x":"sudo aa-teardown"},"apt-extracttemplates":{"n":"apt-extracttemplates","t":"Extract debconf templates from Debian packages.","s":[["TAGLINE","Extract debconf templates from Debian packages."],["TLDR","**Extract** debconf templates from packages\n\n```apt-extracttemplates [package.deb]```\n\nExtract to **specific directory**\n\n```apt-extracttemplates -t [/tmp/templates] [package.deb]```\n\nExtract from **multiple packages**\n\n```apt-extracttemplates [package1.deb] [package2.deb]```"],["SYNOPSIS","**apt-extracttemplates** [_-t tempdir_] _packages_"],["DESCRIPTION","**apt-extracttemplates** extracts debconf templates and configuration scripts from Debian packages. It's used internally by APT during package installation to gather configuration information.\n\nFor each package, it outputs four lines: package name, version, template filename, and config script filename. The extracted templates contain questions that debconf uses to configure packages during installation."],["PARAMETERS","**-t** _dir_, **--tempdir** _dir_\n> Directory for extracted files\n\n**-c** _file_, **--config-file** _file_\n> Configuration file\n\n**-o** _option_\n> Set an arbitrary APT configuration option"],["CAVEATS","This is primarily an internal APT tool. Most users interact with debconf through frontend tools rather than directly extracting templates."],["HISTORY","**apt-extracttemplates** is part of the APT package management infrastructure, supporting the debconf configuration system."],["INSTALL","```apt: sudo apt install apt-utils```\n\n```dnf: sudo dnf install apt-utils```"],["SEE ALSO","[apt](/man/apt)(8), [apt-get](/man/apt-get)(8), [dpkg](/man/dpkg)(1), [dpkg-deb](/man/dpkg-deb)(1)"]],"e":["apt","apt-get","dpkg","dpkg-deb"],"x":"apt-extracttemplates [package.deb]"},"argocd":{"n":"argocd","t":"GitOps continuous delivery CLI for Kubernetes","s":[["TAGLINE","GitOps continuous delivery CLI for Kubernetes"],["TLDR","**Login** to Argo CD server\n\n```argocd login [argocd.example.com]```\n\n**List** applications\n\n```argocd app list```\n\n**Create** application\n\n```argocd app create [app-name] --repo [https://github.com/user/repo] --path [k8s/] --dest-server [https://kubernetes.default.svc]```\n\n**Sync** application\n\n```argocd app sync [app-name]```\n\n**Get** application details\n\n```argocd app get [app-name]```"],["SYNOPSIS","**argocd** _command_ [_options_]"],["DESCRIPTION","**argocd** is the CLI for Argo CD, a declarative GitOps continuous delivery tool for Kubernetes. It manages applications, clusters, repositories, and projects through the Argo CD server.\n\nThe tool enables GitOps workflows where Git repositories are the source of truth for Kubernetes application definitions."],["PARAMETERS","**login** _server_\n> Authenticate with Argo CD server\n\n**app** _command_\n> Application management commands\n\n**cluster** _command_\n> Cluster management commands\n\n**repo** _command_\n> Repository management commands\n\n**proj** _command_\n> Project management commands\n\n**account** _command_\n> Account management commands\n\n**--server** _url_\n> Argo CD server address\n\n**--auth-token** _token_\n> Authentication token\n\n**--grpc-web**\n> Use gRPC-Web for communication\n\n**--insecure**\n> Skip TLS verification"],["CONFIGURATION","**~/.config/argocd/config**\n> Stores authentication contexts for Argo CD servers including server addresses and auth tokens."],["CAVEATS","Requires running Argo CD server. Server must have access to Git repositories and Kubernetes clusters. RBAC controls access to applications."],["HISTORY","**Argo CD** was created by Intuit and open-sourced in **2018**. It became a CNCF incubating project, establishing itself as a leading GitOps solution for Kubernetes."],["INSTALL","```pacman: sudo pacman -S argocd```\n\n```brew: brew install argocd```\n\n```nix: nix profile install nixpkgs#argocd```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [helm](/man/helm)(1), [flux](/man/flux)(1)"],["RESOURCES","```[Source code](https://github.com/argoproj/argo-cd)```\n\n```[Documentation](https://argo-cd.readthedocs.io)```"]],"e":["kubectl","helm","flux"],"x":"argocd login [argocd.example.com]"},"aws-ce":{"n":"aws-ce","t":"Analyze cloud cost and usage data.","s":[["TAGLINE","Analyze cloud cost and usage data."],["TLDR","**Get cost and usage data** for the last month grouped by service\n\n```aws ce get-cost-and-usage --time-period Start=[YYYY-MM-DD],End=[YYYY-MM-DD] --granularity MONTHLY --metrics \"BlendedCost\" --group-by Type=DIMENSION,Key=SERVICE```\n\n**Get a cost forecast** for the next month\n\n```aws ce get-cost-forecast --time-period Start=[YYYY-MM-DD],End=[YYYY-MM-DD] --metric BLENDED_COST --granularity MONTHLY```\n\n**List all cost anomalies** detected in the last 30 days\n\n```aws ce get-anomalies --date-interval StartDate=[YYYY-MM-DD],EndDate=[YYYY-MM-DD]```\n\n**Get reservation utilization** for the current month\n\n```aws ce get-reservation-utilization --time-period Start=[YYYY-MM-DD],End=[YYYY-MM-DD]```\n\n**Get savings plans coverage** by service\n\n```aws ce get-savings-plans-coverage --time-period Start=[YYYY-MM-DD],End=[YYYY-MM-DD] --group-by Type=DIMENSION,Key=SERVICE```\n\n**Get rightsizing recommendations** for EC2 instances\n\n```aws ce get-rightsizing-recommendation --service AmazonEC2```"],["SYNOPSIS","**aws ce** _command_ [_options_]"],["DESCRIPTION","**aws ce** is the AWS CLI interface for AWS Cost Explorer, enabling programmatic access to cost and usage data. It allows querying aggregated data like total monthly costs or granular metrics such as daily operations for specific services.\n\nCost Explorer provides insights into spending patterns, forecasting, anomaly detection, and recommendations for reserved instances and savings plans. All requests go through the **us-east-1** regional endpoint."],["COMMANDS","**get-cost-and-usage**\n> Retrieve cost and usage metrics with filtering and grouping\n\n**get-cost-forecast**\n> Generate cost predictions based on historical data\n\n**get-anomalies**\n> List detected spending anomalies\n\n**get-reservation-utilization**\n> Check reserved instance utilization rates\n\n**get-savings-plans-coverage**\n> Analyze savings plans coverage across services\n\n**get-rightsizing-recommendation**\n> Get EC2 instance rightsizing suggestions\n\n**get-dimension-values**\n> List available values for a filter dimension (for example SERVICE)\n\n**get-usage-forecast**\n> Forecast usage quantity for a future period\n\n**create-anomaly-monitor**\n> Set up automatic anomaly detection monitoring\n\n**create-anomaly-subscription**\n> Subscribe to alerts for a detected anomaly monitor\n\n**create-cost-category-definition**\n> Define custom cost categorization rules"],["CAVEATS","Cost Explorer API has usage charges beyond the free tier. Data is typically delayed by 24-48 hours. Historical data is available for the last 13 months only. The **us-east-1** endpoint must be used regardless of where resources are deployed."],["HISTORY","AWS Cost Explorer launched in **2017** as a visualization tool in the AWS Console. The Cost Explorer API was introduced in **2018** to enable programmatic access. Anomaly detection was added in **2020**, and commitment purchase analysis features expanded through **2023-2024**."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-budgets](/man/aws-budgets)(1), [aws-pricing](/man/aws-pricing)(1), [aws-cur](/man/aws-cur)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/ce/)```"]],"e":["aws","aws-budgets","aws-pricing","aws-cur"],"x":"aws ce get-cost-and-usage --time-period Start=[YYYY-MM-DD],End=[YYYY-MM-DD] --granularity MONTHLY --metrics \"BlendedCost\" --group-by Type=DIMENSION,Key=SERVICE"},"az-storage-queue":{"n":"az-storage-queue","t":"Manage Azure Queue Storage and messages","s":[["TAGLINE","Manage Azure Queue Storage and messages"],["TLDR","**Create a queue**\n\n```az storage queue create --name [myqueue] --account-name [mystorageaccount]```\n\n**List all queues**\n\n```az storage queue list --account-name [mystorageaccount]```\n\n**Delete a queue**\n\n```az storage queue delete --name [myqueue] --account-name [mystorageaccount]```\n\n**Add a message** to a queue\n\n```az storage message put -q [myqueue] --content [Hello World] --account-name [mystorageaccount]```\n\n**Get (dequeue) messages**, hiding them for a visibility timeout\n\n```az storage message get -q [myqueue] --num-messages [5] --visibility-timeout [30] --account-name [mystorageaccount]```\n\n**Peek at messages** without removing or hiding them\n\n```az storage message peek -q [myqueue] --num-messages [5] --account-name [mystorageaccount]```\n\n**Delete a message** from a queue\n\n```az storage message delete --id [message-id] --pop-receipt [receipt] -q [myqueue] --account-name [mystorageaccount]```"],["SYNOPSIS","**az** **storage** **queue** _subcommand_ [_options_]\n\n**az** **storage** **message** _subcommand_ [_options_]"],["SUBCOMMANDS","**create**\n> Create a queue under the given account.\n\n**delete**\n> Delete a queue and any messages it contains.\n\n**list**\n> List queues in a storage account.\n\n**exists**\n> Return a boolean indicating whether the queue exists.\n\n**generate-sas**\n> Generate a shared access signature for the queue.\n\n**stats**\n> Retrieve replication statistics for the Queue service (requires RA-GRS).\n\n**metadata show**\n> Return all user-defined metadata for the queue.\n\n**metadata update**\n> Set user-defined metadata on the queue.\n\n**policy create / delete / list / show / update**\n> Manage stored access policies for the queue.\n\n**message put / get / peek / delete / update / clear**\n> Manage queue messages. These live under **az storage message**, not **az storage queue**."],["PARAMETERS","**--name** **-n**\n> Name of the queue (for queue subcommands).\n\n**--queue-name** **-q**\n> Name of the queue (for **az storage message** subcommands).\n\n**--account-name**\n> Storage account name. Env: AZURE_STORAGE_ACCOUNT.\n\n**--account-key**\n> Storage account key. Env: AZURE_STORAGE_KEY.\n\n**--connection-string**\n> Storage connection string. Env: AZURE_STORAGE_CONNECTION_STRING.\n\n**--auth-mode** _value_\n> Authentication mode: key (legacy account key) or login (Azure AD credentials).\n\n**--content**\n> Message content for **message put** and **message update**, up to 64 KB.\n\n**--visibility-timeout** _seconds_\n> Hide a retrieved message from other consumers for this many seconds (1 second to 7 days).\n\n**--time-to-live** _seconds_\n> Lifetime of a message for **message put**. Use -1 for infinity. Default 7 days.\n\n**--num-messages** _value_\n> Number of messages to get or peek, up to 32. Default 1."],["DESCRIPTION","**az storage queue** manages Azure Queue Storage, which provides cloud messaging between application components. Queues enable asynchronous communication and decoupled architectures.\n\nMessages can be up to 64 KB in size and have a default time-to-live of 7 days. Individual messages are managed with the **az storage message** command group (put, get, peek, delete, update, clear)."],["CAVEATS","The **az storage queue** and **az storage message** groups are in preview. Requires Azure CLI and valid storage credentials (account key, connection string, SAS token, or `--auth-mode login`). Queue names must be 3-63 characters, lowercase letters, numbers, and hyphens. After a **get**, a message is hidden for the visibility timeout; if not deleted before it elapses the message reappears, so consumers must delete with the returned message id and pop receipt."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [azure-cli](/man/azure-cli)(1), [az-storage](/man/az-storage)(1), [az-storage-table](/man/az-storage-table)(1), [az-storage-account](/man/az-storage-account)(1)"],["RESOURCES","```[Documentation](https://learn.microsoft.com/en-us/cli/azure/storage/queue)```\n\n```[Homepage](https://learn.microsoft.com/en-us/cli/azure/)```\n\n```[Source code](https://github.com/Azure/azure-cli)```"]],"e":["az","azure-cli","az-storage","az-storage-table","az-storage-account"],"x":"az storage queue create --name [myqueue] --account-name [mystorageaccount]"},"blurlock":{"n":"blurlock","t":"Lock the screen with a blurred screenshot background","s":[["TAGLINE","Lock the screen with a blurred screenshot background"],["TLDR","**Lock** the screen with a blurred screenshot\n\n```blurlock```\n\nLock screen and **disable unlock indicator**\n\n```blurlock -u```\n\nLock screen and **don't hide** the mouse pointer\n\n```blurlock -p default```\n\nLock screen and **show failed attempts**\n\n```blurlock -f```"],["SYNOPSIS","**blurlock** [_options_]"],["DESCRIPTION","**blurlock** is a wrapper around **i3lock** that takes a screenshot of the current screen, applies a blur effect, and uses it as the lock screen background. This provides a visually appealing lock screen while obscuring screen contents.\n\nThe tool is commonly used with the i3 window manager and related tiling window managers."],["PARAMETERS","**-u, --no-unlock-indicator**\n> Disable the unlock indicator (no feedback on keypress)\n\n**-p, --pointer** _mode_\n> Control mouse pointer visibility (default hides pointer)\n\n**-f, --show-failed-attempts**\n> Display the number of failed login attempts"],["CAVEATS","Requires i3lock and ImageMagick or similar for blur effects. Screenshot capture adds a slight delay before locking. May not work correctly with multiple monitors."],["SEE ALSO","[i3lock](/man/i3lock)(1), [xscreensaver](/man/xscreensaver)(1)"]],"e":["i3lock","xscreensaver"],"x":"blurlock"},"bwrap":{"n":"bwrap","t":"Create lightweight unprivileged sandboxes","s":[["TAGLINE","Create lightweight unprivileged sandboxes"],["TLDR","Run a program in a **read-only environment**\n\n```bwrap --ro-bind / / [/bin/bash]```\n\nGive access to **devices, process info, and tmpfs**\n\n```bwrap --dev-bind /dev /dev --proc /proc --ro-bind / / --tmpfs /tmp [/bin/bash]```\n\n**Run with no network access**\n\n```bwrap --unshare-net --ro-bind / / --dev /dev --proc /proc [/bin/bash]```\n\n**Run isolated process** that dies when parent exits\n\n```bwrap --unshare-all --die-with-parent --ro-bind / / --dev /dev --proc /proc [command]```"],["SYNOPSIS","**bwrap** [_options_] _command_ [_arguments_]"],["DESCRIPTION","**bwrap** (bubblewrap) creates lightweight sandboxes using Linux namespaces. It provides unprivileged containers for running applications with restricted access to the filesystem, network, and other system resources.\n\nThe tool is commonly used by Flatpak and other sandboxing systems to isolate applications from the host system."],["PARAMETERS","**--ro-bind** _src_ _dest_\n> Bind mount source read-only at destination\n\n**--bind** _src_ _dest_\n> Bind mount source read-write at destination\n\n**--dev-bind** _src_ _dest_\n> Bind mount source with device access\n\n**--proc** _dest_\n> Mount procfs at destination\n\n**--tmpfs** _dest_\n> Mount tmpfs at destination\n\n**--unshare-pid**\n> Create new PID namespace\n\n**--unshare-net**\n> Create new network namespace\n\n**--unshare-user**\n> Create new user namespace\n\n**--unshare-all**\n> Unshare all available namespaces\n\n**--unshare-ipc**\n> Create new IPC namespace\n\n**--unshare-uts**\n> Create new UTS namespace\n\n**--unshare-cgroup**\n> Create new cgroup namespace\n\n**--symlink** _SRC_ _DEST_\n> Create a symbolic link\n\n**--dev** _DEST_\n> Mount new devtmpfs at destination\n\n**--dir** _DEST_\n> Create a directory\n\n**--setenv** _VAR_ _VALUE_\n> Set environment variable\n\n**--unsetenv** _VAR_\n> Remove environment variable\n\n**--chdir** _DIR_\n> Change working directory\n\n**--hostname** _HOSTNAME_\n> Set custom hostname (requires --unshare-uts)\n\n**--die-with-parent**\n> Kill sandbox when parent process dies\n\n**--new-session**\n> Create new terminal session via setsid()\n\n**--cap-add** _CAP_\n> Add Linux capability\n\n**--cap-drop** _CAP_\n> Drop Linux capability\n\n**--seccomp** _FD_\n> Load seccomp rules from file descriptor"],["CAVEATS","Requires user namespaces to be enabled in the kernel. Not a complete security boundary; some attack vectors may still exist. Complex sandboxing requires careful bind mount configuration."],["INSTALL","```apt: sudo apt install bubblewrap```\n\n```dnf: sudo dnf install bubblewrap```\n\n```pacman: sudo pacman -S bubblewrap```\n\n```apk: sudo apk add bubblewrap```\n\n```zypper: sudo zypper install bubblewrap```\n\n```brew: brew install bubblewrap```\n\n```nix: nix profile install nixpkgs#bubblewrap```"],["SEE ALSO","[firejail](/man/firejail)(1), [flatpak](/man/flatpak)(1), [unshare](/man/unshare)(1)"]],"e":["firejail","flatpak","unshare"],"x":"bwrap --ro-bind / / [/bin/bash]"},"cargo":{"n":"cargo","t":"rust package manager and build system","s":[["TAGLINE","rust package manager and build system"],["TLDR","**Build** project\n\n```cargo build```\n\n**Run** project\n\n```cargo run```\n\n**Test** project\n\n```cargo test```\n\n**Create** new project\n\n```cargo new [project-name]```\n\n**Add** dependency\n\n```cargo add [dependency]```\n\n**Update** dependencies\n\n```cargo update```"],["SYNOPSIS","**cargo** _command_ [_options_]"],["DESCRIPTION","**cargo** is the Rust package manager and build system. It handles dependency management, compiling packages, running tests, generating documentation, and publishing crates to crates.io.\n\nThe tool is essential for Rust development and comes bundled with Rust."],["PARAMETERS","**new** _name_\n> Create new project\n\n**build** [**--release**]\n> Compile project\n\n**run**\n> Build and run\n\n**test**\n> Run tests\n\n**check**\n> Check compilation without building\n\n**add** _crate_\n> Add dependency\n\n**install** _crate_\n> Install binary crate\n\n**publish**\n> Publish to crates.io\n\n**doc** [**--open**]\n> Build documentation\n\n**clean**\n> Remove build artifacts\n\n**update**\n> Update dependencies\n\n**bench**\n> Run benchmarks (nightly or harness-provided).\n\n**fmt**\n> Format code via `rustfmt` (component).\n\n**clippy**\n> Run the Clippy linter (component)."],["FEATURES","- Dependency resolution\n- Build management\n- Test runner\n- Documentation generator\n- Benchmarking\n- Publishing to crates.io\n- Workspace support\n- Custom build scripts"],["PROJECT STRUCTURE","```\nmy_project/\n├── Cargo.toml      # Project manifest\n├── Cargo.lock      # Dependency lock file\n└── src/\n    └── main.rs     # Source code\n```"],["CARGO.TOML","```toml\n[package]\nname = \"myproject\"\nversion = \"0.1.0\"\nedition = \"2021\"\n\n[dependencies]\nserde = \"1.0\"\ntokio = { version = \"1.0\", features = [\"full\"] }\n```"],["WORKFLOW","```bash"],["Create new binary project","cargo new myapp"],["Create library","cargo new --lib mylib"],["Build debug version","cargo build"],["Build optimized release","cargo build --release"],["Run project","cargo run"],["Run tests","cargo test"],["Check without building","cargo check"],["Generate and open docs","cargo doc --open\n```"],["CAVEATS","First build downloads dependencies (slow). Release builds significantly slower than debug. Cargo.lock should be committed for binaries (not libraries). Large projects have long compile times. Target directory can grow large."],["HISTORY","**Cargo** was developed alongside Rust starting around **2013**, becoming the standard build tool and package manager for the Rust ecosystem."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[rustc](/man/rustc)(1), [rustup](/man/rustup)(1), [cargo-fmt](/man/cargo-fmt)(1), [cargo-clippy](/man/cargo-clippy)(1), [cargo-bench](/man/cargo-bench)(1)"]],"e":["rustc","rustup","cargo-fmt","cargo-clippy","cargo-bench"],"x":"cargo build"},"chatgpt":{"n":"chatgpt","t":"command-line interface for OpenAI language models","s":[["TAGLINE","command-line interface for OpenAI language models"],["TLDR","**Start an interactive chat** session\n\n```chatgpt```\n\n**Send a single prompt** and get a response\n\n```chatgpt \"[What is the capital of France?]\"```\n\n**Pipe input** to ChatGPT\n\n```cat [file.txt] | chatgpt \"[Summarize this text]\"```\n\n**Use a specific model**\n\n```chatgpt --model [gpt-4] \"[prompt]\"```\n\n**Continue a previous conversation**\n\n```chatgpt --continue \"[follow-up question]\"```\n\n**Set system prompt** for context\n\n```chatgpt --system \"[You are a helpful coding assistant]\" \"[prompt]\"```\n\n**Output response to a file**\n\n```chatgpt \"[prompt]\" > [response.txt]```\n\n**Set temperature** for response creativity\n\n```chatgpt --temperature [0.7] \"[prompt]\"```"],["SYNOPSIS","**chatgpt** [_options_] [_prompt_]"],["PARAMETERS","**--model**, **-m** _model_\n> Specify the model to use (gpt-4, gpt-3.5-turbo, etc.).\n\n**--system**, **-s** _prompt_\n> Set a system prompt to define assistant behavior.\n\n**--continue**, **-c**\n> Continue the previous conversation.\n\n**--temperature**, **-t** _value_\n> Control randomness (0.0-2.0, default: 1.0).\n\n**--max-tokens** _n_\n> Maximum tokens in the response.\n\n**--top-p** _value_\n> Nucleus sampling parameter.\n\n**--stream**\n> Stream the response as it's generated.\n\n**--no-stream**\n> Wait for complete response before displaying.\n\n**--api-key** _key_\n> OpenAI API key (or set OPENAI_API_KEY environment variable).\n\n**--config** _file_\n> Path to configuration file.\n\n**--list-models**\n> List available models.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**chatgpt** is a command-line interface for interacting with OpenAI's ChatGPT models. It provides terminal-based access to the GPT language models for text generation, coding assistance, analysis, and general conversation.\n\nThe tool supports both interactive mode for back-and-forth conversation and single-prompt mode for quick queries. Input can be piped from other commands or files, making it useful in shell pipelines for text processing tasks.\n\nConfiguration can be set via command-line flags, environment variables, or a config file. The **OPENAI_API_KEY** environment variable is the standard way to provide credentials. Different models offer varying capabilities, speed, and pricing.\n\nSystem prompts allow customizing the assistant's behavior and expertise area. Temperature controls response creativity — lower values produce more focused responses while higher values increase variety. The streaming option displays responses token-by-token as they're generated."],["CAVEATS","Requires an OpenAI API key with active billing. API usage incurs costs based on token consumption. Response quality and capabilities vary by model. Network connectivity required. Context length is limited by model constraints. Sensitive data should not be sent without considering privacy implications."],["HISTORY","Command-line interfaces for ChatGPT emerged shortly after OpenAI released the ChatGPT API in **March 2023**. Multiple CLI implementations exist across languages (Python, Go, Rust, etc.) with varying feature sets. These tools brought GPT capabilities to terminal-centric workflows, enabling integration with shell scripts and development pipelines."],["INSTALL","```nix: nix profile install nixpkgs#chatgpt```"],["SEE ALSO","[chatgpt-cli](/man/chatgpt-cli)(1), [curl](/man/curl)(1), [jq](/man/jq)(1), [claude](/man/claude)(1), [ollama](/man/ollama)(1)"]],"e":["chatgpt-cli","curl","jq","claude","ollama"],"x":"chatgpt"},"choose":{"n":"choose","t":"human-friendly alternative to cut and awk","s":[["TAGLINE","human-friendly alternative to cut and awk"],["TLDR","**Select first field**\n\n```echo \"one two three\" | choose 0```\n\n**Select third field**\n\n```echo \"a b c d e\" | choose 2```\n\n**Select range of fields**\n\n```echo \"a b c d e\" | choose 1:3```\n\n**Select from end (last field)**\n\n```echo \"a b c d e\" | choose -1```\n\n**Select last three fields**\n\n```echo \"a b c d e\" | choose -3:```\n\n**Custom field separator**\n\n```echo \"a,b,c\" | choose -f ',' 1```\n\n**Regex field separator**\n\n```echo \"a1b2c\" | choose -f '[0-9]' 0:```\n\n**Select exclusive range**\n\n```echo \"a b c d e\" | choose 1:4 --exclusive```"],["SYNOPSIS","**choose** [_options_] _field_[_:end_]"],["DESCRIPTION","**choose** is a human-friendly, fast alternative to **cut** and **awk** for selecting fields from text input. It uses Python-like slice syntax with zero-based indexing, negative indices for counting from the end, and inclusive ranges by default.\n\nUnlike **cut**, which requires exact delimiter specification and uses 1-based indexing, choose handles whitespace splitting automatically and supports regex-based field separators. Written in Rust, it is significantly faster than awk for simple field selection tasks."],["PARAMETERS","**-f**, **--field-separator** _sep_\n> Field separator (regex supported)\n\n**-o**, **--output-field-separator** _sep_\n> Output separator (default: space)\n\n**-x**, **--exclusive**\n> Exclude end index from range\n\n**-c**, **--character-wise**\n> Select characters instead of fields\n\n**-n**, **--non-greedy**\n> Use non-greedy field splitting"],["FIELD SYNTAX","**N**\n> Single field (0-indexed)\n\n**N:M**\n> Range from N to M (inclusive)\n\n**N:**\n> From N to end\n\n**:M**\n> From start to M\n\n**-N**\n> Nth field from end"],["CAVEATS","Zero-indexed (first field is 0). Faster than cut for long inputs, much faster than awk. Install via cargo or brew install choose-rust."],["INSTALL","```pacman: sudo pacman -S choose```\n\n```apk: sudo apk add choose```\n\n```zypper: sudo zypper install choose```\n\n```nix: nix profile install nixpkgs#choose```"],["SEE ALSO","[cut](/man/cut)(1), [awk](/man/awk)(1), [sed](/man/sed)(1)"],["RESOURCES","```[Source code](https://github.com/theryangeary/choose)```"]],"e":["cut","awk","sed"],"x":"echo \"one two three\" | choose 0"},"compgen":{"n":"compgen","t":"generate completion matches in bash","s":[["TAGLINE","generate completion matches in bash"],["TLDR","**List all commands**\n\n```compgen -c```\n\n**List commands starting with prefix**\n\n```compgen -c [ls]```\n\n**List all aliases**\n\n```compgen -a```\n\n**List all shell functions**\n\n```compgen -A function```\n\n**List all builtins**\n\n```compgen -b```\n\n**List all variables**\n\n```compgen -v```\n\n**Complete from word list**\n\n```compgen -W '[start stop restart]' -- [sta]```\n\n**List all users**\n\n```compgen -u```"],["SYNOPSIS","**compgen** [_options_] [_word_]"],["DESCRIPTION","**compgen** is a Bash builtin that generates possible completion matches for a word. It is primarily used inside programmable completion functions but is also useful interactively for discovering available commands, functions, variables, and other shell entities. When given a word argument, only matches starting with that word are shown."],["PARAMETERS","**-a**\n> Generate alias names.\n\n**-b**\n> Generate shell builtin names.\n\n**-c**\n> Generate command names (builtins, functions, and executables on PATH).\n\n**-d**\n> Generate directory names.\n\n**-e**\n> Generate names of exported shell variables.\n\n**-f**\n> Generate filenames.\n\n**-g**\n> Generate group names.\n\n**-j**\n> Generate job names.\n\n**-k**\n> Generate shell reserved words (keywords).\n\n**-s**\n> Generate service names.\n\n**-u**\n> Generate usernames.\n\n**-v**\n> Generate shell variable names.\n\n**-A** _action_\n> Generate completions using the given action. Valid actions include: alias, arrayvar, binding, builtin, command, directory, disabled, enabled, export, file, function, group, hostname, job, keyword, running, service, setopt, signal, stopped, user, variable.\n\n**-F** _function_\n> Call shell function to generate completions.\n\n**-C** _command_\n> Run command in a subshell and use its output as completions.\n\n**-W** _wordlist_\n> Split wordlist on IFS and generate matches from resulting words.\n\n**-G** _pattern_\n> Expand the glob pattern and use the results as completions.\n\n**-P** _prefix_\n> Prepend prefix to each generated completion.\n\n**-S** _suffix_\n> Append suffix to each generated completion.\n\n**-X** _pattern_\n> Filter completions using the pattern; a leading **!** negates the filter."],["CAVEATS","Bash builtin only, not available in other shells. Returns true unless invalid option or no matches. Used in completion scripts with complete builtin."],["SEE ALSO","[complete](/man/complete)(1), [bash](/man/bash)(1)"]],"e":["complete","bash"],"x":"compgen -c"},"conntrack":{"n":"conntrack","t":"network connection tracking management","s":[["TAGLINE","network connection tracking management"],["TLDR","**List** all connections\n\n```conntrack -L```\n\nShow **real-time** events\n\n```conntrack -E```\n\nEvents with **timestamps**\n\n```conntrack -E -o timestamp```\n\nEvents for specific **IP**\n\n```conntrack -E -s ip_address```\n\n**Delete** flows by source IP\n\n```conntrack -D -s ip_address```"],["SYNOPSIS","**conntrack** [_OPTIONS_]"],["DESCRIPTION","**conntrack** interacts with the Linux kernel's connection tracking system (conntrack). It allows viewing, searching, and modifying tracked network connections used by stateful firewalling."],["PARAMETERS","**-L, --dump**\n> List all currently tracked connections\n\n**-E, --event**\n> Display real-time connection events\n\n**-D, --delete**\n> Delete connections matching criteria\n\n**-U, --update**\n> Update connections matching criteria\n\n**-s, --orig-src** _address_\n> Filter by original source address\n\n**-d, --orig-dst** _address_\n> Filter by original destination address\n\n**-p, --proto** _protocol_\n> Filter by protocol (tcp, udp, icmp)\n\n**-o, --output** _type_\n> Output format (extended, timestamp, xml)\n\n**--src-nat**\n> Filter for SNAT connections\n\n**--dst-nat**\n> Filter for DNAT connections"],["CAVEATS","Requires root privileges and the nf_conntrack kernel module. Deleting connections may disrupt active sessions. High traffic systems may have thousands of tracked connections."],["HISTORY","**conntrack** is part of the **conntrack-tools** package for managing the Linux connection tracking system."],["INSTALL","```apt: sudo apt install conntrack```\n\n```dnf: sudo dnf install conntrack-tools```\n\n```pacman: sudo pacman -S conntrack-tools```\n\n```apk: sudo apk add conntrack-tools```\n\n```zypper: sudo zypper install conntrack-tools```\n\n```nix: nix profile install nixpkgs#conntrack-tools```"],["SEE ALSO","[iptables](/man/iptables)(8), [nft](/man/nft)(8), [ss](/man/ss)(8)"],["RESOURCES","```[Source code](https://git.netfilter.org/conntrack-tools/)```\n\n```[Homepage](https://conntrack-tools.netfilter.org/)```\n\n```[Documentation](https://www.netfilter.org/projects/conntrack-tools/)```"]],"e":["iptables","nft","ss"],"x":"conntrack -L"},"dcg":{"n":"dcg","t":"schema-driven code generator for multiple languages","s":[["TAGLINE","schema-driven code generator for multiple languages"],["TLDR","**Generate code** from specification\n\n```dcg [specification.dcg] -o [output_dir]```\n\n**Generate with specific** language target\n\n```dcg [specification.dcg] --lang [go]```\n\n**Generate with custom** templates\n\n```dcg [specification.dcg] --template [template_dir]```\n\n**Validate specification** without generating\n\n```dcg [specification.dcg] --validate```\n\n**List available generators**\n\n```dcg --list-generators```"],["SYNOPSIS","**dcg** [_options_] _specification_"],["PARAMETERS","_SPECIFICATION_\n> Input specification file to process.\n\n**-o**, **--output** _DIR_\n> Output directory for generated code.\n\n**--lang** _LANGUAGE_\n> Target programming language.\n\n**--template** _DIR_\n> Directory containing custom templates.\n\n**--validate**\n> Validate specification only.\n\n**--list-generators**\n> Show available code generators.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dcg** (Data Code Generator) is a code generation tool that transforms data specifications or schemas into code for various programming languages. It automates the creation of data structures, serialization code, and related boilerplate.\n\nThe tool reads a specification file describing data types and their relationships, then applies language-specific templates to generate corresponding source code. This approach ensures consistency between documentation and implementation.\n\nCode generators like dcg are valuable in projects with complex data models, multiple language implementations, or requirements for strict schema enforcement across system boundaries."],["CAVEATS","Generated code may require manual adjustments for edge cases. Custom templates require understanding of the template engine. Breaking changes in specifications may require regenerating all dependent code."],["HISTORY","dcg represents a category of code generation tools that emerged to address the challenge of maintaining consistent data representations across multiple languages and platforms. Various implementations exist with different specification formats and target languages."],["SEE ALSO","[protoc](/man/protoc)(1), [thrift](/man/thrift)(1)"]],"e":["protoc","thrift"],"x":"dcg [specification.dcg] -o [output_dir]"},"django-admin":{"n":"django-admin","t":"Django command-line utility for administrative tasks","s":[["TAGLINE","Django command-line utility for administrative tasks"],["TLDR","**Start a new project**\n\n```django-admin startproject [project_name]```\n\n**Start a new app**\n\n```django-admin startapp [app_name]```\n\n**Run development server**\n\n```django-admin runserver```\n\n**Create database migrations**\n\n```django-admin makemigrations```\n\n**Apply database migrations**\n\n```django-admin migrate```\n\n**Create superuser**\n\n```django-admin createsuperuser```\n\n**Open Django shell**\n\n```django-admin shell```"],["SYNOPSIS","**django-admin** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Django management command to execute.\n\n**startproject** _NAME_\n> Create new Django project.\n\n**startapp** _NAME_\n> Create new Django app.\n\n**runserver** [_PORT_]\n> Run development server.\n\n**makemigrations** [_APP_]\n> Create migration files.\n\n**migrate** [_APP_]\n> Apply migrations.\n\n**createsuperuser**\n> Create admin user.\n\n**shell**\n> Open interactive Python shell with Django context.\n\n**--settings** _MODULE_\n> Settings module to use.\n\n**--help**\n> Display help information."],["DESCRIPTION","**django-admin** is Django's command-line utility for administrative tasks. It provides commands for project setup, database management, testing, and development server operation.\n\nThe tool requires a Django settings module to function. When inside a project directory, it automatically discovers the settings; otherwise, the DJANGO_SETTINGS_MODULE environment variable or --settings flag specifies the configuration.\n\ndjango-admin is typically aliased or replaced by manage.py within projects, which automatically configures the settings module. Both provide identical functionality."],["CONFIGURATION","**settings.py**\n> Django project settings module controlling database, installed apps, middleware, and all configuration.\n\n**manage.py**\n> Project-specific wrapper around django-admin that sets the settings module automatically."],["CAVEATS","Requires Django installed. Some commands require configured settings and database. Development server not suitable for production. Migration commands require database access."],["HISTORY","django-admin is part of **Django**, created by **Adrian Holovaty** and **Simon Willison** at the Lawrence Journal-World newspaper. Django was released as open source in **2005** and has become one of the most popular Python web frameworks."],["INSTALL","```apk: sudo apk add py3-django```"],["SEE ALSO","[python](/man/python)(1), [pip](/man/pip)(1)"]],"e":["python","pip"],"x":"django-admin startproject [project_name]"},"dnf-reposync":{"n":"dnf-reposync","t":"synchronize remote repositories locally","s":[["TAGLINE","synchronize remote repositories locally"],["TLDR","**Synchronize** repository to local directory\n\n```dnf reposync --repoid [repo_name]```\n\nSync to **custom location**\n\n```dnf reposync --repoid [repo_name] -p [path/to/directory]```\n\nSync packages **and metadata**\n\n```dnf reposync --repoid [repo_name] --download-metadata```\n\nDownload only **newest packages**\n\n```dnf reposync --repoid [repo_name] -n```\n\n**Print URLs** only\n\n```dnf reposync --repoid [repo_name] -u```\n\nPreserve **remote timestamps**\n\n```dnf reposync --repoid [repo_name] --remote-time```"],["SYNOPSIS","**dnf reposync** [_options_]"],["DESCRIPTION","**dnf reposync** synchronizes packages and metadata from remote DNF repositories to a local directory. Part of dnf-plugins-core.\n\nUseful for creating local mirrors or offline repositories."],["PARAMETERS","**--repoid** _repo_\n> Repository to sync\n\n**-p, --download-path** _path_\n> Download destination\n\n**--download-metadata**\n> Also download repository metadata\n\n**-n, --newest-only**\n> Only download newest package versions\n\n**-u, --urls**\n> Print URLs instead of downloading\n\n**--remote-time**\n> Preserve remote timestamps"],["CAVEATS","Requires dnf-plugins-core. Large repositories require significant disk space and bandwidth. Use --newest-only to reduce size."],["SEE ALSO","[dnf](/man/dnf)(8), [createrepo](/man/createrepo)(8)"]],"e":["dnf","createrepo"],"x":"dnf reposync --repoid [repo_name]"},"drupal-check":{"n":"drupal-check","t":"Drupal code deprecation checker","s":[["TAGLINE","Drupal code deprecation checker"],["TLDR","**Check module for** deprecations\n\n```drupal-check [modules/custom/mymodule]```\n\n**Check with analysis** level\n\n```drupal-check -a [modules/custom/mymodule]```\n\n**Check for Drupal 10** compatibility\n\n```drupal-check --drupal-root=[.] [modules/custom/mymodule]```\n\n**Output in format**\n\n```drupal-check --format=[json] [path]```\n\n**Check multiple paths**\n\n```drupal-check [path1] [path2]```"],["SYNOPSIS","**drupal-check** [_options_] _paths_..."],["PARAMETERS","_PATH_...\n> One or more directories or files to analyze.\n\n**-d**, **--deprecations**\n> Check for deprecated API usage (default behavior).\n\n**-a**, **--analysis**\n> Run general static analysis checks.\n\n**-s**, **--style**\n> Run code style checks.\n\n**--php8**\n> Use PHPStan phpVersion 8.1 (Drupal 10 requirement).\n\n**--drupal-root** _PATH_\n> Path to the Drupal root.\n\n**--format** _FORMAT_\n> Output format: raw, table, checkstyle, json, or junit (default: table).\n\n**--memory-limit** _LIMIT_\n> Memory limit for analysis (e.g. 1G).\n\n**-e**, **--exclude-dir** _DIRS_\n> Comma-separated directories to exclude.\n\n**--no-progress**\n> Suppress the progress bar.\n\n**-v**, **-vv**, **-vvv**\n> Increase output verbosity.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**drupal-check** is a static analysis tool that checks Drupal code for deprecated API usage and other issues. It helps prepare modules for Drupal version upgrades by identifying code that needs updating.\n\nThe tool uses PHPStan for static analysis with Drupal-specific rules. It detects usage of deprecated functions, classes, and services that will be removed in future Drupal versions.\n\ndrupal-check is essential for maintaining Drupal modules and ensuring compatibility with upcoming releases."],["CAVEATS","Requires PHP and Composer. May have false positives. Complex dynamic code may not analyze correctly. Results depend on deprecation database currency."],["HISTORY","drupal-check was created by **Matt Glaman** to help the Drupal community prepare for Drupal 9 migration. It addresses the challenge of identifying deprecated code across large Drupal codebases."],["INSTALL","```pacman: sudo pacman -S drupal```\n\n```nix: nix profile install nixpkgs#drupal```"],["SEE ALSO","[drupal](/man/drupal)(1), [drush](/man/drush)(1), [phpstan](/man/phpstan)(1)"]],"e":["drupal","drush","phpstan"],"x":"drupal-check [modules/custom/mymodule]"},"fdfind":{"n":"fdfind","t":"fast and user-friendly file finder","s":[["TAGLINE","fast and user-friendly file finder"],["TLDR","**Find files by name**\n\n```fdfind [pattern]```\n\n**Find with extension**\n\n```fdfind -e [txt] [pattern]```\n\n**Find directories only**\n\n```fdfind -t d [pattern]```\n\n**Find files only**\n\n```fdfind -t f [pattern]```\n\n**Search hidden files**\n\n```fdfind -H [pattern]```\n\n**Search specific directory**\n\n```fdfind [pattern] [/path/to/search]```\n\n**Execute command on results**\n\n```fdfind [pattern] -x [command]```\n\n**Case-sensitive search**\n\n```fdfind -s [Pattern]```"],["SYNOPSIS","**fdfind** [_options_] [_pattern_] [_path_...]"],["PARAMETERS","**-e**, **--extension** _ext_\n> Filter by extension.\n\n**-t**, **--type** _type_\n> Filter by type (f, d, l, x).\n\n**-H**, **--hidden**\n> Include hidden files.\n\n**-I**, **--no-ignore**\n> Don't respect ignore files.\n\n**-s**, **--case-sensitive**\n> Case-sensitive search.\n\n**-x**, **--exec** _cmd_\n> Execute command on each result.\n\n**-X**, **--exec-batch** _cmd_\n> Execute command with all results.\n\n**-d**, **--max-depth** _num_\n> Maximum search depth.\n\n**-E**, **--exclude** _pattern_\n> Exclude pattern."],["DESCRIPTION","**fdfind** (or fd) is a simple, fast, and user-friendly alternative to find, written in Rust. It uses regex patterns by default and respects .gitignore files, making it ideal for searching code repositories.\n\nThe command provides colorful output, sensible defaults that ignore hidden and gitignored files, and parallel execution for better performance. On Debian-based systems, the binary is named fdfind to avoid conflicts with another package."],["CAVEATS","Uses regex patterns by default, not glob syntax. Ignores hidden and .gitignore files unless explicitly enabled. May not cover all find use cases."],["INSTALL","```apt: sudo apt install fd-find```\n\n```dnf: sudo dnf install fd-find```"],["SEE ALSO","[fd](/man/fd)(1), [find](/man/find)(1), [ripgrep](/man/ripgrep)(1)"],["RESOURCES","```[Source code](https://github.com/sharkdp/fd)```"]],"e":["fd","find","ripgrep"],"x":"fdfind [pattern]"},"gamemoderun":{"n":"gamemoderun","t":"Invoke a program with GameMode optimizations for better game performance","s":[["TAGLINE","Invoke a program with GameMode optimizations for better game performance"],["TLDR","**Run a program** with GameMode CPU and system optimizations enabled\n\n```gamemoderun ./mygame```\n\n**Run a game from Steam** by setting launch options\n\n```gamemoderun %command%```\n\n**Run with a custom wrapper** that receives the original command\n\n```GAMEMODERUNEXEC=\"env __GL_SYNC_TO_VBLANK=1\" gamemoderun ./game```\n\n**Use a hybrid-GPU launcher** via the same variable\n\n```GAMEMODERUNEXEC=optirun gamemoderun ./game```"],["SYNOPSIS","**gamemoderun** _program_ [_args_...]"],["DESCRIPTION","**gamemoderun** is a helper that launches the given program under GameMode. GameMode is a daemon and library combination for Linux that requests temporary system optimizations (such as CPU governor changes, I/O priority, process niceness, screensaver inhibiting, GPU performance modes, and optional custom scripts) while a game or other performance-sensitive application is running.\n\nIt works by preloading `libgamemodeauto.so.0`, which communicates with the `gamemoded` daemon. The design cleanly separates the client from the daemon so games can safely request mode without hard dependencies.\n\ngamemoderun is the recommended way to launch games that do not integrate the GameMode client library directly. It is commonly used in Steam launch options and desktop launchers."],["PARAMETERS","gamemoderun itself takes no flags; it executes its arguments under GameMode via `LD_PRELOAD`.\n\n**GAMEMODERUNEXEC**\n> Environment variable. If set, gamemoderun runs this command (or command prefix) and passes the original program and arguments to it. Useful for hybrid-GPU wrappers (`optirun`, `env DRI_PRIME=1`, NVIDIA prime offload env) or other launch helpers. GameMode is injected into the final game process, not into the wrapper itself. May be set globally (e.g. in `/etc/environment`)."],["CAVEATS","GameMode optimizations are best-effort and depend on the host configuration and privileges. On some systems the CPU governor changes may be restricted. The daemon must be installed and running (or activatable via user service) for optimizations to apply; missing daemon still allows the game to start."],["INSTALL","```dnf: sudo dnf install gamemode```\n\n```pacman: sudo pacman -S gamemode```\n\n```apk: sudo apk add gamemode```\n\n```zypper: sudo zypper install gamemode```\n\n```nix: nix profile install nixpkgs#gamemode```"],["RESOURCES","```[Source code](https://github.com/FeralInteractive/gamemode)```\n\n```[Documentation](https://github.com/FeralInteractive/gamemode#requesting-gamemode)```"]],"e":[],"x":"gamemoderun ./mygame"},"gdm":{"n":"gdm","t":"graphical login manager for GNOME","s":[["TAGLINE","graphical login manager for GNOME"],["TLDR","**Start GDM**\n\n```systemctl start gdm```\n\n**Stop GDM**\n\n```systemctl stop gdm```\n\n**Enable at boot**\n\n```systemctl enable gdm```\n\n**Restart GDM**\n\n```systemctl restart gdm```\n\n**Check status**\n\n```systemctl status gdm```"],["SYNOPSIS","**gdm** [_options_]"],["PARAMETERS","**--version**\n> Display version.\n\n**--help**\n> Display help information."],["CONFIGURATION","**/etc/gdm/custom.conf**\n> Main GDM configuration file controlling login behavior, auto-login, and session settings.\n\n**/etc/gdm3/custom.conf**\n> Alternative configuration path on Debian-based systems."],["DESCRIPTION","**gdm** (GNOME Display Manager) is the graphical login manager for GNOME. It provides the login screen, user authentication, and session management for desktop environments.\n\nGDM handles user selection, password authentication, and starting desktop sessions. It supports multiple sessions, accessibility features, and user switching.\n\ngdm is typically started by systemd and runs as a system service."],["CAVEATS","Requires X11 or Wayland. Configuration through dconf. Managed by systemd on modern systems."],["HISTORY","GDM was created as part of the **GNOME project** to provide a display manager integrated with the GNOME desktop environment. It replaced xdm for GNOME users."],["INSTALL","```dnf: sudo dnf install gdm```\n\n```pacman: sudo pacman -S gdm```\n\n```apk: sudo apk add gdm```\n\n```zypper: sudo zypper install gdm```\n\n```nix: nix profile install nixpkgs#gdm```"],["SEE ALSO","[ly](/man/ly)(1), [slim](/man/slim)(1), [xinit](/man/xinit)(1)"]],"e":["ly","slim","xinit"],"x":"systemctl start gdm"},"git-cherry-pick":{"n":"git-cherry-pick","t":"Apply changes from existing commits","s":[["TAGLINE","Apply changes from existing commits"],["TLDR","**Apply a commit to the current branch**\n\n```git cherry-pick [commit]```\n\n**Cherry-pick multiple commits**\n\n```git cherry-pick [commit1] [commit2]```\n\n**Cherry-pick an exclusive range (excludes start, includes end)**\n\n```git cherry-pick [start]..[end]```\n\n**Cherry-pick an inclusive range (includes both start and end)**\n\n```git cherry-pick [start]^..[end]```\n\n**Apply changes without committing**\n\n```git cherry-pick -n [commit]```\n\n**Continue after resolving conflicts**\n\n```git cherry-pick --continue```\n\n**Abort and return to pre-cherry-pick state**\n\n```git cherry-pick --abort```\n\n**Append a \"cherry picked from\" note to the commit message**\n\n```git cherry-pick -x [commit]```"],["SYNOPSIS","**git** **cherry-pick** [_options_] _commit_...\n\n**git** **cherry-pick** (**--continue** | **--skip** | **--abort** | **--quit**)"],["PARAMETERS","**-e**, **--edit**\n> Edit the commit message before committing.\n\n**-n**, **--no-commit**\n> Apply changes to the working tree and index without creating a commit.\n\n**-x**\n> Append a line saying \"(cherry picked from commit ...)\" to the original commit message. Useful for tracking backports on public branches.\n\n**-s**, **--signoff**\n> Add a `Signed-off-by` trailer to the commit message.\n\n**-m** _parent-number_, **--mainline** _parent-number_\n> Specify the parent number (starting from 1) to use as the mainline when cherry-picking a merge commit.\n\n**--ff**\n> If HEAD is the same as the parent of the commit being cherry-picked, fast-forward HEAD instead of creating a new commit.\n\n**--continue**\n> Continue the operation after resolving conflicts.\n\n**--skip**\n> Skip the current commit and continue with the rest of the sequence.\n\n**--abort**\n> Cancel the operation and return to the pre-cherry-pick state.\n\n**--quit**\n> Forget the current operation without restoring HEAD, leaving the working tree as-is."],["DESCRIPTION","**git cherry-pick** applies the changes introduced by existing commits onto the current branch, creating new commits with the same diff but different ancestry. This allows selective integration of specific changes without merging entire branches.\n\nThe command is essential for backporting bug fixes to maintenance branches, incorporating specific features from development branches, and recovering commits from abandoned branches. Each cherry-picked commit gets a new SHA-1 hash reflecting its new parent.\n\nWhen specifying a range with `..`, the start commit is excluded. To include both endpoints use `start^..end`. Cherry-picking can encounter conflicts when changes don't apply cleanly to the target branch's context. The operation can be paused to allow manual resolution before continuing with `--continue`."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-rebase](/man/git-rebase)(1), [git-revert](/man/git-revert)(1), [git-cherry](/man/git-cherry)(1)"]],"e":["git-rebase","git-revert","git-cherry"],"x":"git cherry-pick [commit]"},"git-undo":{"n":"git-undo","t":"Undo recent commits while preserving changes","s":[["TAGLINE","Undo recent commits while preserving changes"],["TLDR","**Undo last commit**\n\n```git undo```\n\n**Undo N commits**\n\n```git undo [3]```\n\n**Undo and keep changes staged**\n\n```git undo --soft```\n\n**Undo and discard changes**\n\n```git undo --hard```"],["SYNOPSIS","**git undo** [_count_] [_options_]"],["PARAMETERS","_COUNT_\n> Number of commits to undo (default: 1).\n\n**--soft**\n> Keep changes staged.\n\n**--hard**\n> Discard all changes.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git undo** removes the last commit(s) while keeping changes in the working directory (unstaged). It is a convenient wrapper around `git reset` that defaults to a mixed reset, preserving all work.\n\nWithout arguments, it undoes the most recent commit. Pass a number to undo multiple commits. Use **--soft** to keep changes staged, or **--hard** to discard them entirely."],["CAVEATS","Part of the **git-extras** package and must be installed separately. Only affects local commits that have not been pushed. **--hard** discards changes permanently and cannot be easily recovered."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-reset](/man/git-reset)(1), [git-revert](/man/git-revert)(1), [git-extras](/man/git-extras)(1)"]],"e":["git-reset","git-revert","git-extras"],"x":"git undo"},"glxinfo":{"n":"glxinfo","t":"OpenGL and GLX capability display tool","s":[["TAGLINE","OpenGL and GLX capability display tool"],["TLDR","**Show OpenGL information**\n\n```glxinfo```\n\n**Show brief summary**\n\n```glxinfo -B```\n\n**Show only vendor and version**\n\n```glxinfo | grep \"OpenGL\"```\n\n**List supported extensions**\n\n```glxinfo | grep \"GL_\"```\n\n**Display on specific screen**\n\n```DISPLAY=:0 glxinfo```\n\n**Show in-depth limits**\n\n```glxinfo -l```"],["SYNOPSIS","**glxinfo** [_options_]"],["PARAMETERS","**-B**\n> Brief output (summary only).\n\n**-l**\n> Display extended limits.\n\n**-v**\n> Verbose output for the visual list.\n\n**-t**\n> Produce a wider, more readable tabular format for the visual list.\n\n**-s**\n> Print a single extension per line.\n\n**-i**\n> Force indirect rendering.\n\n**-b**\n> Find the \"best\" visual and print its number.\n\n**-display** _dpy_\n> Specify X display.\n\n**-h**\n> Display help."],["DESCRIPTION","**glxinfo** displays information about the GLX implementation and OpenGL capabilities of the graphics system. It is useful for diagnosing graphics driver issues and checking hardware capabilities.\n\nThe tool queries the X server and graphics driver, reporting OpenGL version, renderer, vendor, and supported extensions."],["CAVEATS","Requires X server connection. Results depend on driver and configuration. Indirect rendering may show different capabilities. Wayland needs XWayland."],["HISTORY","**glxinfo** is part of the **mesa-utils** package, associated with the **Mesa** 3D Graphics Library. Mesa was started by **Brian Paul** in **1993**, and glxinfo has been a standard diagnostic tool for OpenGL on X11 systems since the early days of GLX."],["INSTALL","```apt: sudo apt install mesa-utils```\n\n```pacman: sudo pacman -S mesa-utils```\n\n```apk: sudo apk add mesa-utils```"],["SEE ALSO","[glxgears](/man/glxgears)(1), [xdpyinfo](/man/xdpyinfo)(1), [vulkaninfo](/man/vulkaninfo)(1), [nvidia-smi](/man/nvidia-smi)(1)"],["RESOURCES","```[Source code](https://gitlab.freedesktop.org/mesa/demos)```\n\n```[Manual page](https://manpages.debian.org/testing/mesa-utils/glxinfo.1.en.html)```"]],"e":["glxgears","xdpyinfo","vulkaninfo","nvidia-smi"],"x":"glxinfo"},"gnatmake":{"n":"gnatmake","t":"Ada program build utility","s":[["TAGLINE","Ada program build utility"],["TLDR","**Compile an Ada program**\n\n```gnatmake [main.adb]```\n\n**Compile with optimization**\n\n```gnatmake -O2 [main.adb]```\n\n**Compile with debugging info**\n\n```gnatmake -g [main.adb]```\n\n**Compile with parallel jobs**\n\n```gnatmake -j4 [main.adb]```\n\n**Specify output executable name**\n\n```gnatmake [main.adb] -o [program]```\n\n**Force recompilation of all sources**\n\n```gnatmake -f [main.adb]```"],["SYNOPSIS","**gnatmake** [_options_] _file_name_ [_options_]"],["PARAMETERS","**-jN**\n> Use N parallel jobs for compilation.\n\n**-g**\n> Generate debugging information.\n\n**-O**_level_\n> Optimization level (0, 1, 2, 3, or s for size).\n\n**-o** _name_\n> Output executable name.\n\n**-c**\n> Compile only, do not bind or link.\n\n**-f**\n> Force recompilation of all sources.\n\n**-q**\n> Quiet mode, less output.\n\n**-v**\n> Verbose mode.\n\n**-I**_dir_\n> Add directory to source search path.\n\n**--RTS=**_runtime_\n> Specify Ada runtime library."],["DESCRIPTION","**gnatmake** is the primary build utility for Ada programs in the GNAT (GNU Ada Toolset) environment. It automatically determines dependencies by analyzing Ada's with clauses, compiles modified source files, and performs binding and linking to create an executable. Unlike traditional make utilities, **gnatmake** always recomputes dependencies from sources, ensuring accurate tracking of changes."],["CAVEATS","Multiple main files can be specified to build several executables. Dependencies are computed from Ada source files rather than object file timestamps."],["INSTALL","```apt: sudo apt install gnat-12```\n\n```dnf: sudo dnf install gcc-gnat```\n\n```apk: sudo apk add gcc-gnat```"],["SEE ALSO","[gcc](/man/gcc)(1)"]],"e":["gcc"],"x":"gnatmake [main.adb]"},"gradle-init":{"n":"gradle-init","t":"Gradle project scaffolding generator","s":[["TAGLINE","Gradle project scaffolding generator"],["TLDR","**Create new project interactively**\n\n```gradle init```\n\n**Create Java application**\n\n```gradle init --type java-application```\n\n**Create with Kotlin DSL**\n\n```gradle init --type java-application --dsl kotlin```\n\n**Create library with test framework**\n\n```gradle init --type java-library --test-framework junit-jupiter```\n\n**Create Kotlin application**\n\n```gradle init --type kotlin-application```\n\n**Create basic empty project**\n\n```gradle init --type basic```"],["SYNOPSIS","**gradle** **init** [**--type** _type_] [**--dsl** _dsl_] [_options_]"],["PARAMETERS","**--type** _type_\n> Project type: java-application, java-library, kotlin-application, etc.\n\n**--dsl** _dsl_\n> Build script DSL: groovy or kotlin.\n\n**--test-framework** _framework_\n> Test framework: junit, junit-jupiter, spock, testng.\n\n**--project-name** _name_\n> Project name.\n\n**--package** _package_\n> Source package name."],["DESCRIPTION","**gradle init** creates a new Gradle project with a standard directory structure. It can run interactively, prompting for project type, language, and build script DSL, or non-interactively with command-line options. Supported types include applications and libraries for Java, Kotlin, Groovy, Scala, C++, and Swift."],["CAVEATS","Running in a non-empty directory may fail or skip generation if existing build files are detected. The **--incubating** APIs used for scaffolding new project types can change between Gradle versions."],["INSTALL","```pacman: sudo pacman -S gradle```\n\n```apk: sudo apk add gradle```\n\n```brew: brew install gradle```\n\n```nix: nix profile install nixpkgs#gradle```"],["SEE ALSO","[gradle](/man/gradle)(1), [gradle-build](/man/gradle-build)(1)"],["RESOURCES","```[Documentation](https://docs.gradle.org/current/userguide/build_init_plugin.html)```"]],"e":["gradle","gradle-build"],"x":"gradle init"},"ibtop":{"n":"ibtop","t":"Real-time TUI monitor for InfiniBand networks","s":[["TAGLINE","Real-time TUI monitor for InfiniBand networks"],["TLDR","**Launch ibtop** to monitor InfiniBand ports\n\n```ibtop```"],["SYNOPSIS","**ibtop** [_options_]"],["DESCRIPTION","**ibtop** is a terminal-based real-time monitor for **InfiniBand** high-speed interconnects, similar to htop but for network fabric monitoring. It displays live port statistics including throughput, packet rates, and errors with continuous updates. The interface is built with **Ratatui** for a modern TUI experience."],["CAVEATS","Only useful on systems with InfiniBand hardware. Requires appropriate permissions to read InfiniBand port counters."],["HISTORY","**ibtop** was created by **JannikSt** and is written in **Rust**."],["SEE ALSO","[htop](/man/htop)(1)"]],"e":["htop"],"x":"ibtop"},"iconv":{"n":"iconv","t":"converts text files from one character encoding to another","s":[["TAGLINE","converts text files from one character encoding to another"],["TLDR","**Convert file** from UTF-8 to ISO-8859-1\n\n```iconv -f UTF-8 -t ISO-8859-1 [input.txt] > [output.txt]```\n\n**Convert file** in place (with temp file)\n\n```iconv -f UTF-8 -t ASCII//TRANSLIT [input.txt] -o [output.txt]```\n\n**List available encodings**\n\n```iconv -l```\n\n**Convert with transliteration** (approximate characters)\n\n```iconv -f UTF-8 -t ASCII//TRANSLIT [input.txt]```\n\n**Convert ignoring errors**\n\n```iconv -f UTF-8 -t ASCII//IGNORE [input.txt]```\n\n**Convert from Windows codepage**\n\n```iconv -f CP1252 -t UTF-8 [windows.txt]```\n\n**Convert from stdin**\n\n```cat [file.txt] | iconv -f UTF-8 -t UTF-16```"],["SYNOPSIS","**iconv** [_options_] [**-f** _encoding_] [**-t** _encoding_] [_file_...]"],["DESCRIPTION","**iconv** converts text files from one character encoding to another. It reads input in the source encoding and writes output in the target encoding, handling the character mapping between different character sets.\n\nThe tool supports hundreds of encodings including UTF-8, ISO-8859 family, Windows code pages, Asian encodings, and legacy formats. Special suffixes //TRANSLIT (approximate conversion) and //IGNORE (skip unconvertible) modify conversion behavior.\n\niconv is essential for handling files from different systems, migrating legacy data, and ensuring proper text display across platforms with different default encodings."],["PARAMETERS","**-f** _encoding_, **--from-code=** _encoding_\n> Source encoding.\n\n**-t** _encoding_, **--to-code=** _encoding_\n> Target encoding.\n\n**-l**, **--list**\n> List available encodings.\n\n**-o** _file_, **--output=** _file_\n> Output file.\n\n**-c**\n> Silently discard unconvertible characters.\n\n**-s**, **--silent**\n> Suppress warnings.\n\n**--verbose**\n> Print progress information.\n\n**//TRANSLIT**\n> Transliterate unconvertible characters.\n\n**//IGNORE**\n> Skip unconvertible characters."],["CAVEATS","Not all character mappings are reversible. Some characters may be lost or approximated. UTF-8 is the recommended target for modern systems. Encoding detection is not automatic (use chardet first if unknown). Binary files should not be converted."],["HISTORY","**iconv** is part of the **POSIX standard** and has implementations in glibc (GNU libc) and other C libraries. The character conversion functionality has been standardized since **POSIX.2** in **1992**. The GNU implementation supports an extensive list of encodings and is the version found on most Linux systems."],["INSTALL","```apt: sudo apt install libc-bin```\n\n```apk: sudo apk add musl-utils```\n\n```nix: nix profile install nixpkgs#iconv```"],["SEE ALSO","[chardet](/man/chardet)(1), [file](/man/file)(1), [recode](/man/recode)(1), [uconv](/man/uconv)(1)"]],"e":["chardet","file","recode","uconv"],"x":"iconv -f UTF-8 -t ISO-8859-1 [input.txt] > [output.txt]"},"infotocap":{"n":"infotocap","t":"converts terminal descriptions from terminfo format to termcap format","s":[["TAGLINE","converts terminal descriptions from terminfo format to termcap format"],["TLDR","**Convert terminfo to termcap**\n\n```infotocap [terminfo_file]```\n\n**Convert specific entry**\n\n```infotocap -1 [term_name]```\n\n**Output to file**\n\n```infotocap [input] > [output.cap]```\n\n**Verbose mode**\n\n```infotocap -v [terminfo]```"],["SYNOPSIS","**infotocap** [_options_] _file_..."],["PARAMETERS","_FILE_\n> Terminfo source file to convert.\n\n**-1**\n> One entry per line.\n\n**-v**\n> Verbose output.\n\n**-V**\n> Display version.\n\n**-w** _N_\n> Set output width."],["DESCRIPTION","**infotocap** converts terminal descriptions from terminfo format to termcap format. It is implemented as a link to **tic**(1) with the **-C** option implied. It is the reverse of **captoinfo**.\n\nThe tool enables compatibility with older applications requiring termcap format. Since it invokes tic internally, other tic options such as **-1**, **-f**, **-v**, **-w**, and **-x** can also be used."],["CAVEATS","Some terminfo capabilities have no termcap equivalents and cannot be converted. Part of ncurses. Output may need manual adjustment for complex terminal descriptions."],["HISTORY","infotocap is part of **ncurses**, providing conversion between terminal description formats."],["INSTALL","```apt: sudo apt install ncurses-bin```\n\n```dnf: sudo dnf install ncurses```\n\n```pacman: sudo pacman -S ncurses```\n\n```apk: sudo apk add ncurses```\n\n```brew: brew install ncurses```\n\n```nix: nix profile install nixpkgs#ncurses```"],["SEE ALSO","[captoinfo](/man/captoinfo)(1), [tic](/man/tic)(1), [infocmp](/man/infocmp)(1)"]],"e":["captoinfo","tic","infocmp"],"x":"infotocap [terminfo_file]"},"javac":{"n":"javac","t":"compiles Java source files into bytecode class files that run on the Java","s":[["TAGLINE","compiles Java source files into bytecode class files that run on the Java"],["TLDR","**Compile a Java file**\n\n```javac [File.java]```\n\n**Compile multiple files**\n\n```javac [File1.java] [File2.java]```\n\n**Compile all Java files** in current directory\n\n```javac *.java```\n\n**Compile with classpath**\n\n```javac -cp [lib/*:classes] [File.java]```\n\n**Specify output directory** for class files\n\n```javac -d [out] [File.java]```\n\n**Compile with specific** Java version compatibility\n\n```javac --release [11] [File.java]```\n\n**Enable all warnings**\n\n```javac -Xlint:all [File.java]```\n\n**Compile with verbose output**\n\n```javac -verbose [File.java]```"],["SYNOPSIS","**javac** [_OPTIONS_] _SOURCE-FILES_\n\n**javac** [_OPTIONS_] **@**_ARGFILE_"],["PARAMETERS","**-d** _DIR_\n> Output directory for compiled class files.\n\n**-cp**, **-classpath** _PATH_\n> Classpath for finding user class files and libraries.\n\n**--module-path** _PATH_\n> Module path for finding application modules.\n\n**-sourcepath** _PATH_\n> Path for finding source files.\n\n**--release** _VERSION_\n> Compile for specific Java SE release.\n\n**-source** _VERSION_\n> Source code compatibility version.\n\n**-target** _VERSION_\n> Generate class files for specific VM version.\n\n**-Xlint**[**:**_WARNINGS_]\n> Enable warnings (all, deprecation, unchecked, etc.).\n\n**-g**\n> Include debugging information.\n\n**-verbose**\n> Output messages about what the compiler is doing.\n\n**-deprecation**\n> Show description of deprecated API usage.\n\n**-encoding** _ENCODING_\n> Source file character encoding.\n\n**-h** _DIR_\n> Generate native header files for JNI.\n\n**@**_FILE_\n> Read options and filenames from file."],["DESCRIPTION","**javac** compiles Java source files (.java) into bytecode class files (.class) that run on the Java Virtual Machine. It performs syntax and type checking, optimizes code, and generates platform-independent bytecode.\n\nThe compiler uses the classpath to locate referenced classes and libraries. For modular projects (Java 9+), the module path specifies module locations. The **--release** flag ensures compatibility with a specific Java version for both compilation and available APIs.\n\nWhen compiling many files, use @argfiles to avoid command-line length limits. Each line in the argfile can contain a source file or option."],["CAVEATS","Source and class files must match Java package structure with directory paths. Circular dependencies between source files require all files to be compiled together. The classpath separator is : on Unix and ; on Windows."],["HISTORY","javac has been part of the Java Development Kit since Java 1.0 in **1996**. The compiler has evolved significantly, adding generics (Java 5), modules (Java 9), and numerous language features. Modern versions support incremental compilation and annotation processing."],["SEE ALSO","[java](/man/java)(1), [jar](/man/jar)(1), [javadoc](/man/javadoc)(1)"]],"e":["java","jar","javadoc"],"x":"javac [File.java]"},"jhead":{"n":"jhead","t":"utility for displaying and manipulating EXIF metadata in JPEG images","s":[["TAGLINE","utility for displaying and manipulating EXIF metadata in JPEG images"],["TLDR","Show all **EXIF data**\n\n```jhead image.jpg```\n\nSet **file date to EXIF** date\n\n```jhead -ft image.jpg```\n\nSet **EXIF date to file** date\n\n```jhead -dsft image.jpg```\n\n**Rename** files based on EXIF date\n\n```jhead -n%Y_%m_%d-%H_%M_%S *.jpg```\n\n**Auto-rotate** based on EXIF orientation\n\n```jhead -autorot *.jpg```\n\n**Adjust** EXIF timestamps\n\n```jhead -ta-1:00:00 *.jpg```\n\n**Remove** all EXIF data\n\n```jhead -purejpg image.jpg```\n\n**Delete** thumbnail only\n\n```jhead -dt image.jpg```"],["SYNOPSIS","**jhead** [_options_] [_file_...]"],["DESCRIPTION","**jhead** is a utility for displaying and manipulating EXIF metadata in JPEG images from digital cameras. It can adjust timestamps, remove thumbnails, auto-rotate images, and transfer EXIF headers between files."],["PARAMETERS","**-ft**\n> Set file modification time to EXIF create date\n\n**-dsft**\n> Set EXIF timestamp to file modification time\n\n**-n FORMAT**\n> Rename files using strftime format based on EXIF date\n\n**-autorot**\n> Rotate images losslessly based on EXIF orientation tag\n\n**-ta OFFSET**\n> Adjust EXIF time by offset (format: +/-HH:MM:SS)\n\n**-da NEWDATE-OLDDATE**\n> Correct large date offsets\n\n**-purejpg**\n> Remove all non-essential sections and metadata\n\n**-de**\n> Delete entire EXIF header\n\n**-dt**\n> Delete thumbnail from EXIF header\n\n**-dc**\n> Delete comment field\n\n**-te FILE**\n> Transfer EXIF header from another file\n\n**-ce**\n> Edit comment field in text editor\n\n**-cl STRING**\n> Set comment from command line\n\n**-st FILE**\n> Save thumbnail to file\n\n**-rgt SIZE**\n> Regenerate thumbnail with specified size\n\n**-mkexif**\n> Create minimal EXIF header"],["CAVEATS","The -autorot option uses jpegtran for lossless rotation. Renaming with -n uses strftime format codes. Changes are made in-place to the original files unless otherwise specified."],["HISTORY","**jhead** was created by Matthias Wandel for manipulating EXIF data in JPEG files from digital cameras."],["INSTALL","```apt: sudo apt install jhead```\n\n```dnf: sudo dnf install jhead```\n\n```pacman: sudo pacman -S jhead```\n\n```brew: brew install jhead```\n\n```nix: nix profile install nixpkgs#jhead```"],["SEE ALSO","[exiftool](/man/exiftool)(1), [exiv2](/man/exiv2)(1), [jpegtran](/man/jpegtran)(1)"]],"e":["exiftool","exiv2","jpegtran"],"x":"jhead image.jpg"},"jira-sprints":{"n":"jira-sprints","t":"lists sprints for a Jira project","s":[["TAGLINE","lists sprints for a Jira project"],["TLDR","**List all sprints**\n\n```jira sprints -p [PROJECT]```\n\n**List active sprints**\n\n```jira sprints --state active -p [PROJECT]```\n\n**List closed sprints**\n\n```jira sprints --state closed -p [PROJECT]```\n\n**List with limit**\n\n```jira sprints -l [10] -p [PROJECT]```"],["SYNOPSIS","**jira sprints** [_options_]"],["PARAMETERS","**-p** _PROJECT_\n> Project key.\n\n**--state** _STATE_\n> Sprint state (active, closed, future).\n\n**-l** _LIMIT_\n> Maximum results.\n\n**--plain**\n> Plain text output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jira sprints** lists sprints for a Jira project. It shows sprint names, states, and dates.\n\nThe command provides an overview of sprint history and planning. It filters by state to focus on relevant sprints."],["CAVEATS","Subcommand of jira-cli. Requires Scrum board configured. Project permissions needed."],["HISTORY","jira sprints is part of **jira-cli**, providing sprint listing functionality for Agile project management."],["SEE ALSO","[jira](/man/jira)(1), [jira-sprint](/man/jira-sprint)(1), [jira-me](/man/jira-me)(1)"]],"e":["jira","jira-sprint","jira-me"],"x":"jira sprints -p [PROJECT]"},"jj-config-set":{"n":"jj-config-set","t":"sets configuration values in Jujutsu config files","s":[["TAGLINE","sets configuration values in Jujutsu config files"],["TLDR","**Set user config value**\n\n```jj config set --user [user.name] \"[John Doe]\"```\n\n**Set repo config value**\n\n```jj config set --repo [key] \"[value]\"```\n\n**Set user email**\n\n```jj config set --user user.email \"[email@example.com]\"```"],["SYNOPSIS","**jj** **config** **set** [_options_] _name_ _value_"],["PARAMETERS","**--user**\n> Set in user configuration.\n\n**--repo**\n> Set in repository configuration."],["DESCRIPTION","**jj config set** sets configuration values in Jujutsu config files. User config applies globally while repo config applies only to the current repository. Values are stored in TOML format."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-config-get](/man/jj-config-get)(1), [jj-config-unset](/man/jj-config-unset)(1)"]],"e":["jj","jj-config-get","jj-config-unset"],"x":"jj config set --user [user.name] \"[John Doe]\""},"komi-learn":{"n":"komi-learn","t":"Continuous memory and self-improvement layer for coding agents","s":[["TAGLINE","Continuous memory and self-improvement layer for coding agents"],["TLDR","**Install** komi-learn hooks for Claude Code\n\n```komi-learn install```\n\n**Install** for Codex instead of Claude Code\n\n```komi-learn install --host codex```\n\n**Run a health check** and surface issues\n\n```komi-learn doctor```\n\n**Show** current configuration and learning progress\n\n```komi-learn status```\n\n**Update** komi-learn and its agent hooks\n\n```komi-learn update```\n\n**Preview** an update without applying\n\n```komi-learn update --check```\n\n**Edit** a configuration value\n\n```komi-learn config set [key] [value]```\n\n**Sync** community-pool lessons\n\n```komi-learn sync```\n\n**Review** pending contributions before they are shared\n\n```komi-learn queue```\n\n**Forget** a previously learned item (archive by default)\n\n```komi-learn forget [topic]```\n\n**Permanently delete** a learned item instead of archiving\n\n```komi-learn forget [topic] --hard```\n\n**Uninstall** hooks (use `--purge` to also delete stored data)\n\n```komi-learn uninstall```"],["SYNOPSIS","**komi-learn** _subcommand_ [_options_]"],["DESCRIPTION","**komi-learn** is a Python CLI that adds a memory and self-improvement loop to coding agents such as **Claude Code** and **Codex**. It installs per-agent hooks that observe sessions and persist durable lessons across runs.\n\nThe learning loop has four stages. **Recall** activates relevant lessons at the start of a session. **Distill** analyzes the session transcript after it ends and extracts durable lessons. **Curate** merges overlapping items and archives outdated ones. **Share** optionally publishes user-approved lessons to a GitHub-hosted, cryptographically signed community pool so they can be pulled by other users with `komi-learn sync`.\n\nSecrets, absolute paths, and one-off failures are filtered before content is stored or shared. `forget` archives a learning by default and only deletes it with `--hard`. `uninstall` removes hooks; `--purge` also removes stored learnings."],["PARAMETERS","**install**\n> Set up the agent hooks. `--yes` accepts defaults. `--host` selects the agent (`claude`, `codex`).\n\n**doctor**\n> Verify the installation and report problems.\n\n**update**\n> Upgrade komi-learn and its hooks. `--check` previews only.\n\n**status**\n> Display configuration and learning progress.\n\n**config**\n> Adjust settings, either interactively or via `config set <key> <value>`.\n\n**sync**\n> Pull community-pool lessons.\n\n**queue**\n> Review pending shared contributions.\n\n**forget** _topic_\n> Archive a learned item. `--hard` deletes it permanently.\n\n**uninstall**\n> Remove hooks. `--purge` deletes all stored data."],["INSTALLATION","```pip install komi-learn```\n\nThen install hooks for the desired agent:\n\n```komi-learn install```"],["CAVEATS","komi-learn modifies the host agent's configuration during `install`. Run `komi-learn doctor` after an upgrade to confirm hooks are still valid. The community pool is opt-in, but be aware that lessons you queue for sharing may include excerpts of code or commands from your sessions; review the queue before syncing."],["SEE ALSO","[claude](/man/claude), [aislop](/man/aislop)"]],"e":["claude","aislop"],"x":"komi-learn install"},"kompose":{"n":"kompose","t":"converts Docker Compose files to Kubernetes resources","s":[["TAGLINE","converts Docker Compose files to Kubernetes resources"],["TLDR","**Convert to Kubernetes**\n\n```kompose convert```\n\n**Convert specific file**\n\n```kompose convert -f [docker-compose.yml]```\n\n**Convert to Helm chart**\n\n```kompose convert -c```\n\n**Convert with specific** controller type\n\n```kompose convert --controller [daemonSet]```\n\n**Output as JSON** to stdout\n\n```kompose convert --json --stdout```\n\n**Convert with replicas**\n\n```kompose convert --replicas [3]```\n\n**Apply directly to cluster**\n\n```kompose up```"],["SYNOPSIS","**kompose** _command_ [_options_]"],["PARAMETERS","**convert**\n> Convert compose to Kubernetes.\n\n**up**\n> Deploy to Kubernetes.\n\n**down**\n> Remove from Kubernetes.\n\n**-f** _FILE_\n> Compose file to convert.\n\n**-c**, **--chart**\n> Output Helm chart.\n\n**-o** _DIR_\n> Output directory.\n\n**--replicas** _N_\n> Set replica count (default 1).\n\n**--controller** _TYPE_\n> Set output controller type (deployment, daemonSet, replicationController).\n\n**--volumes** _TYPE_\n> Volume generation type (persistentVolumeClaim, emptyDir, hostPath, configMap). Default persistentVolumeClaim.\n\n**--json**\n> Generate resource files in JSON format.\n\n**--stdout**\n> Print converted objects to stdout.\n\n**--provider** _PROVIDER_\n> Target provider (kubernetes, openshift). Default kubernetes.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kompose** is a conversion tool that translates Docker Compose files into Kubernetes resource manifests. It parses `docker-compose.yml` definitions and generates corresponding Deployments, Services, PersistentVolumeClaims, and other Kubernetes objects, providing a migration path for applications originally designed to run with Docker Compose.\n\nThe tool supports multiple output formats including standard Kubernetes YAML, JSON, and Helm charts. It can also deploy converted resources directly to a cluster with `kompose up` or tear them down with `kompose down`. As a CNCF sandbox project, kompose handles common Compose directives such as port mappings, volumes, environment variables, and replica counts, though some Compose-specific features may require manual adjustment after conversion."],["CAVEATS","Not all Compose features translate. May need manual adjustment. CNCF project."],["HISTORY","kompose was created to help users migrate from Docker Compose to Kubernetes, becoming a CNCF sandbox project."],["INSTALL","```pacman: sudo pacman -S kompose```\n\n```zypper: sudo zypper install kompose```\n\n```brew: brew install kompose```\n\n```nix: nix profile install nixpkgs#kompose```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [docker-compose](/man/docker-compose)(1), [helm](/man/helm)(1)"]],"e":["kubectl","docker-compose","helm"],"x":"kompose convert"},"link":{"n":"link","t":"creates a hard link between files","s":[["TAGLINE","creates a hard link between files"],["TLDR","**Create hard link**\n\n```link [target] [linkname]```\n\n**Link file to directory**\n\n```link [file.txt] [dir/file.txt]```"],["SYNOPSIS","**link** _file1_ _file2_"],["PARAMETERS","_FILE1_\n> Target file (source).\n\n_FILE2_\n> Link name (destination)."],["DESCRIPTION","**link** creates a hard link between files. Unlike ln, it only creates hard links and takes exactly two arguments.\n\nThe tool creates an additional directory entry pointing to the same inode. Both names reference the same file data."],["CAVEATS","Cannot create symbolic links. Cannot link directories. Target must exist. Same filesystem only."],["HISTORY","link is a **POSIX** utility providing a simple interface to the link() system call for creating hard links."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[ln](/man/ln)(1), [unlink](/man/unlink)(1), [readlink](/man/readlink)(1)"]],"e":["ln","unlink","readlink"],"x":"link [target] [linkname]"},"lssh":{"n":"lssh","t":"TUI list-based SSH, SCP, and SFTP client","s":[["TAGLINE","TUI list-based SSH, SCP, and SFTP client"],["TLDR","**Launch the SSH connection list**\n\n```lssh```\n\n**Launch the SCP client**\n\n```lscp```\n\n**Launch the SFTP client**\n\n```lsftp```"],["SYNOPSIS","**lssh** [_options_]"],["DESCRIPTION","**lssh** is a TUI list-select client for SSH, SCP, and SFTP connections. It reads your OpenSSH config (~/.ssh/config) and presents servers in a selectable list for quick connection.\n\nFeatures include support for password, public key, certificate, and PKCS11 (Yubikey) authentication, the ability to use the local machine's bashrc at the SSH destination, and NFS forward/reverse forwarding support."],["CAVEATS","Requires an OpenSSH configuration file. Authentication methods depend on the server-side configuration."],["HISTORY","**lssh** was created by **blacknon** and is written in **Go**. It runs on Linux, macOS, and Windows."],["SEE ALSO","[ssh](/man/ssh)(1), [scp](/man/scp)(1), [sftp](/man/sftp)(1), [lazyssh](/man/lazyssh)(1)"]],"e":["ssh","scp","sftp","lazyssh"],"x":"lssh"},"lt":{"n":"lt","t":"lists directory contents as a tree","s":[["TAGLINE","lists directory contents as a tree"],["TLDR","**List files as tree**\n\n```lt```\n\n**List specific directory**\n\n```lt [directory/]```\n\n**Show hidden files**\n\n```lt -a```\n\n**Limit depth**\n\n```lt -L [2]```\n\n**Show file details**\n\n```lt -l```\n\n**Color output**\n\n```lt --color```"],["SYNOPSIS","**lt** [_options_] [_path_]"],["PARAMETERS","_PATH_\n> Directory to list.\n\n**-a**\n> Show hidden files.\n\n**-L** _DEPTH_\n> Maximum depth.\n\n**-l**\n> Long listing format.\n\n**--color**\n> Enable color output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lt** lists directory contents in a tree format. It is typically a shell alias rather than a standalone command, commonly configured as an alias for **lsd --tree**, **exa --tree**, or **eza --tree**.\n\nThe exact behavior and available options depend on the underlying command. Run **type lt** or **alias lt** to see the actual definition on your system."],["CAVEATS","Not a standard command. Behavior and available flags depend entirely on how the alias is configured. Options shown here correspond to common implementations like lsd or eza."],["HISTORY","**lt** is commonly configured as a shell alias for tree-style directory listing using modern ls alternatives like **lsd**, **exa**, or **eza**."],["INSTALL","```apt: sudo apt install looptools```"],["SEE ALSO","[tree](/man/tree)(1), [lsd](/man/lsd)(1), [exa](/man/exa)(1), [eza](/man/eza)(1), [ls](/man/ls)(1)"]],"e":["tree","lsd","exa","eza","ls"],"x":"lt"},"mashtree":{"n":"mashtree","t":"rapidly creates distance trees from genome sequences using MinHash sketching","s":[["TAGLINE","rapidly creates distance trees from genome sequences using MinHash sketching"],["TLDR","**Fastest method** to create a tree from fastq and/or fasta files using multiple threads\n\n```mashtree --numcpus [12] [*.fastq.gz] [*.fasta] > [mashtree.dnd]```\n\n**Most accurate method** to create a tree with mindepth 0\n\n```mashtree --mindepth 0 --numcpus [12] [*.fastq.gz] [*.fasta] > [mashtree.dnd]```\n\nMost accurate method with **confidence values** (bootstrap)\n\n```mashtree_bootstrap.pl --reps [100] --numcpus [12] [*.fastq.gz] -- --min-depth 0 > [mashtree.bootstrap.dnd]```"],["SYNOPSIS","**mashtree** [_options_] _genome_files_..."],["PARAMETERS","**--numcpus _n_**\n> Number of CPU threads to use for parallel processing\n\n**--mindepth _n_**\n> Minimum depth for k-mer counting (0 for most accuracy)\n\n**--genomesize _size_**\n> Estimated genome size for sketch calculations\n\n**--truncLength _n_**\n> Truncate sequence names at this length\n\n**--outtree _file_**\n> Output file for the tree (default: stdout)"],["DESCRIPTION","**mashtree** rapidly creates distance trees from genome sequences using MinHash sketching. It computes pairwise distances between genomes based on k-mer similarity and constructs a neighbor-joining tree.\n\nThe tool accepts FASTA, FASTQ, and compressed versions (.gz) of both formats. It uses the Mash algorithm internally for efficient sketch-based distance calculation, making it suitable for thousands of genomes.\n\nOutput is in Newick format (.dnd), compatible with tree visualization tools. Note that mashtree creates distance trees, not phylogenetic trees—it shows similarity relationships, not evolutionary history."],["CAVEATS","Not suitable for formal phylogenetic analysis; use proper phylogenetic methods for evolutionary studies. Accuracy depends on genome completeness and quality. Very divergent sequences may produce unreliable trees. Memory usage scales with the number of genomes being compared."],["HISTORY","Mashtree was developed by Lee Katz at the CDC (Centers for Disease Control and Prevention) for rapid outbreak analysis and genome clustering in public health microbiology."]],"e":[],"x":"mashtree --numcpus [12] [*.fastq.gz] [*.fasta] > [mashtree.dnd]"},"mcp-probe":{"n":"mcp-probe","t":"Interactive MCP protocol debugger and testing TUI","s":[["TAGLINE","Interactive MCP protocol debugger and testing TUI"],["TLDR","**Debug an MCP server** via stdio\n\n```mcp-probe debug --stdio [command]```\n\n**Debug via HTTP+SSE**\n\n```mcp-probe debug --http-sse [url]```\n\n**Debug via Streamable HTTP**\n\n```mcp-probe debug --streamable-http [url]```\n\n**Run compliance validation**\n\n```mcp-probe validate --http-sse [url] --suite all```"],["SYNOPSIS","**mcp-probe** _command_ [_options_]"],["PARAMETERS","**debug**\n> Launch interactive TUI debugger for an MCP server.\n\n**validate**\n> Run automated compliance tests against an MCP server.\n\n**--stdio** _COMMAND_\n> Connect to MCP server via stdio transport.\n\n**--http-sse** _URL_\n> Connect to MCP server via HTTP+SSE transport.\n\n**--streamable-http** _URL_\n> Connect to MCP server via Streamable HTTP transport.\n\n**--suite** _SUITE_\n> Compliance test suite to run (all, initialization, capabilities, security, performance).\n\n**--format** _FORMAT_\n> Output format for validation reports (html, markdown, json, yaml)."],["DESCRIPTION","**mcp-probe** is an interactive debugger and testing toolkit for Model Context Protocol (MCP) servers. The TUI provides automatic discovery of tools, resources, and prompts, advanced fuzzy search, and multiple view modes for inspecting server responses.\n\nIt supports compliance testing with report generation in HTML, Markdown, JSON, and YAML formats, making it suitable for CI/CD pipeline integration."],["CAVEATS","Requires a running MCP server to connect to. Transport support depends on the server's implementation."],["HISTORY","**mcp-probe** was created by **conikeec** and is written in **Rust** using the Ratatui library."],["INSTALL","```aur: yay -S mcp-probe```"],["SEE ALSO","[claude](/man/claude)(1)"]],"e":["claude"],"x":"mcp-probe debug --stdio [command]"},"mcpp":{"n":"mcpp","t":"C++23 module-first build tool and package manager","s":[["TAGLINE","C++23 module-first build tool and package manager"],["TLDR","**Create** a new modular C++ project\n\n```mcpp new [project_name]```\n\n**Build** the project in the current directory\n\n```mcpp build```\n\n**Build and run** the default target\n\n```mcpp run```\n\n**Run tests** (auto-discovered under tests/)\n\n```mcpp test```\n\n**Add a dependency** from the package index\n\n```mcpp add [package]```\n\n**Install a bundled toolchain** (e.g. GCC 16)\n\n```mcpp toolchain install gcc 16```\n\n**Package** a release tarball (vendored mode by default)\n\n```mcpp pack```\n\n**Diagnose** the local environment\n\n```mcpp self doctor```"],["SYNOPSIS","**mcpp** [_global-options_] _command_ [_args_]"],["DESCRIPTION","**mcpp** is a modern C++ build tool aimed at C++23 module-first development. It creates, builds, tests, and packages projects that use `import std`, module interface units, and module partitions, with automatic module dependency analysis and file-level incremental builds.\n\nProjects are described by a **mcpp.toml** manifest. By convention, `src/main.cpp` becomes a binary target and tests under `tests/` are discovered by **mcpp test**. The first build downloads a host-aware default toolchain (GCC or LLVM depending on platform) into an isolated sandbox under **~/.mcpp/**, so system compilers are not required.\n\nDependency management resolves SemVer constraints against package indices (default: mcpp-index / mcpplibs), writes a lockfile, and can pull modules from index, Git, or local paths. Multi-package workspaces share one lockfile and target directory. **mcpp pack** produces distributable artifacts (system, vendored, self-contained, or fully static musl builds on Linux).\n\nThe tool is early-stage and self-hosted (written in pure C++23 modules). Full option lists for each subcommand are available via **mcpp** _command_ **--help**."],["PARAMETERS","**new** _name_\n> Scaffold a modular project (optional **--template** for library-provided templates).\n\n**build**\n> Compile the project (incremental; Ninja backend).\n\n**run** [**--** _args_]\n> Build and run the default target.\n\n**test** [_pattern_]\n> Discover, build, and run tests; filter by name pattern.\n\n**clean**\n> Remove build outputs under the target directory.\n\n**add** / **remove** / **update**\n> Manage dependencies declared in mcpp.toml.\n\n**search**\n> Search configured package indices.\n\n**pack**\n> Bundle a release package (**--mode** system | vendored | self-contained | static).\n\n**publish**\n> Publish a package to a configured index.\n\n**toolchain** _install_|_list_|_default_ ...\n> Install, list, or select sandboxed compiler toolchains.\n\n**cache** / **index**\n> Manage BMI/build cache and package indices.\n\n**self** _doctor_|_version_|_env_|_config_|_explain_ ...\n> Environment checks and self-maintenance commands.\n\n**why** [_toolchain_|_runtime_|_deps_]\n> Explain resolved build decisions.\n\n**explain** _CODE_\n> Print a detailed explanation for an error code.\n\n**--offline**\n> Use only already available local state (no downloads or index refresh). Also **MCPP_OFFLINE=1**.\n\n**--version**\n> Print the mcpp version."],["CAVEATS","Name clash: many Linux/Homebrew distributions ship an unrelated **mcpp** C preprocessor. Package names for this build tool are often **mcpp-m** / **mcpp-bin** (AUR) or the Homebrew tap formula **mcpp-m**, while the installed command remains **mcpp**. Prefer the install paths from the project README if **which mcpp** points at the preprocessor.\n\nEarly-stage software: interfaces and behavior may change between releases. The first build can take a long time while toolchains download into **~/.mcpp/**."],["CONFIGURATION","Project manifest: **mcpp.toml** (package metadata, dependencies, toolchains, workspaces).\n\nLockfile: **mcpp.lock**.\n\nPer-user data and sandboxed toolchains: **~/.mcpp/** (registry, caches, install layout)."],["HISTORY","mcpp is a community project under mcpp-community focused on native C++23 modules and self-hosting. It builds on the xlings toolchain/package foundation and is inspired by tools such as Cargo, xmake, and Ninja."],["INSTALL","```aur: yay -S mcpp-bin```\n\n```brew: brew install mcpp-community/mcpp/mcpp-m```"],["SEE ALSO","[xmake](/man/xmake)(1), [cargo](/man/cargo)(1), [ninja](/man/ninja)(1), [cmake](/man/cmake)(1), [meson](/man/meson)(1)"],["RESOURCES","```[Source code](https://github.com/mcpp-community/mcpp)```\n\n```[Documentation](https://github.com/mcpp-community/mcpp/tree/main/docs)```"]],"e":["xmake","cargo","ninja","cmake","meson"],"x":"mcpp new [project_name]"},"ninja":{"n":"ninja","t":"small, fast build system","s":[["TAGLINE","small, fast build system"],["TLDR","**Build project**\n\n```ninja```\n\n**Build specific target**\n\n```ninja [target]```\n\n**Build with multiple jobs**\n\n```ninja -j [8]```\n\n**Build in a different directory**\n\n```ninja -C [build_dir]```\n\n**Clean build**\n\n```ninja -t clean```\n\n**Dry run**\n\n```ninja -n```\n\n**Verbose output**\n\n```ninja -v```\n\n**Keep going after errors**\n\n```ninja -k [0]```\n\n**Use specific build file**\n\n```ninja -f [build.ninja]```\n\n**Show build graph**\n\n```ninja -t graph [target]```"],["SYNOPSIS","**ninja** [_options_] [_targets_]"],["PARAMETERS","_TARGETS_\n> Build targets.\n\n**-j** _N_\n> Run N jobs in parallel (0 means infinity) [default=number of CPUs].\n\n**-f** _FILE_\n> Specify input build file [default=build.ninja].\n\n**-C** _DIR_\n> Change to DIR before doing anything else.\n\n**-v**\n> Show all command lines while building.\n\n**-n**\n> Dry run (don't run commands but act like they succeeded).\n\n**-k** _N_\n> Keep going until N jobs fail (0 means infinity) [default=1].\n\n**-l** _N_\n> Do not start new jobs if the load average is greater than N.\n\n**-t** _TOOL_\n> Run a subtool (use -t list to list subtools).\n\n**-d** _MODE_\n> Enable debugging (use -d list to list modes).\n\n**-w** _FLAG_\n> Adjust warnings (use -w list to list warnings).\n\n**--version**\n> Print ninja version.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ninja** is a small, fast build system closest in spirit to Make. It focuses on speed and correctness, taking a different approach by having its input files be generated by a higher-level build system rather than written by hand.\n\nBuild files (build.ninja) are typically generated by CMake, Meson, or GN. Ninja was designed to replace Make as the build executor for large projects like Chromium."],["CAVEATS","Build files are not typically hand-written but generated by a meta-build system. Ninja has minimal features compared to Make by design. Default parallelism is based on the number of available CPUs."],["HISTORY","Ninja was created by **Evan Martin** at Google for building Chrome, released in 2012."],["INSTALL","```apt: sudo apt install ninja-build```\n\n```dnf: sudo dnf install ninja-build```\n\n```pacman: sudo pacman -S samurai```\n\n```apk: sudo apk add samurai```\n\n```zypper: sudo zypper install samurai```\n\n```brew: brew install samurai```\n\n```nix: nix profile install nixpkgs#samurai```"],["SEE ALSO","[cmake](/man/cmake)(1), [meson](/man/meson)(1), [make](/man/make)(1)"]],"e":["cmake","meson","make"],"x":"ninja"},"nkt":{"n":"nkt","t":"Terminal note taking with journals, tasks, and Zettelkasten","s":[["TAGLINE","Terminal note taking with journals, tasks, and Zettelkasten"],["TLDR","**Add a journal entry**\n\n```nkt log [journal_name]```\n\n**Read notes**\n\n```nkt read [note_name]```\n\n**Edit a note**\n\n```nkt edit [note_name]```\n\n**List all notes**\n\n```nkt list```"],["SYNOPSIS","**nkt** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**nkt** is a terminal note-taking tool that combines journals, notes, task lists, and habit chains. It draws inspiration from Dendron, jrnl, vim-wiki, and methods like Zettelkasten and \"Don't break the chain.\"\n\nFeatures include timestamped journal entries, fuzzy finding across all notes (similar to fzf), tag-based organization with **@tag** syntax, task management, and linking between notes for easy navigation."],["CAVEATS","Notes are stored locally. The project has moved from GitHub to sourcehut."],["HISTORY","**nkt** was created by **fjebaker** and is written in **Zig**."],["INSTALL","```brew: brew install nkt```"],["SEE ALSO","[nb](/man/nb)(1), [jrnl](/man/jrnl)(1)"]],"e":["nb","jrnl"],"x":"nkt log [journal_name]"},"ntfs-3g.secaudit":{"n":"ntfs-3g.secaudit","t":"NTFS security data auditing tool","s":[["TAGLINE","NTFS security data auditing tool"],["TLDR","**Audit all security data** on a volume recursively\n\n```sudo ntfs-3g.secaudit -ar [/dev/sda1]```\n\n**Display permissions** for files in a directory\n\n```sudo ntfs-3g.secaudit [/dev/sda1] [/path/to/directory]```\n\n**Recursively display permissions** in a directory\n\n```sudo ntfs-3g.secaudit -r [/dev/sda1] [/path/to/directory]```\n\n**Backup ACLs** from a volume and directory to a file\n\n```sudo ntfs-3g.secaudit -b [/dev/sda1] [/path] > [backup.txt]```\n\n**Restore ACLs** from a backup file\n\n```sudo ntfs-3g.secaudit -s [/dev/sda1] < [backup.txt]```\n\n**Get a user mapping proposal**\n\n```sudo ntfs-3g.secaudit -u [/dev/sda1] [/path/to/windows/file]```"],["SYNOPSIS","**ntfs-3g.secaudit** **-a**[**rv**] _volume_\n**ntfs-3g.secaudit** **-b**[**v**] _volume_ [_directory_]\n**ntfs-3g.secaudit** **-s**[**ev**] _volume_ [_backup-file_]\n**ntfs-3g.secaudit** **-r**[**v**] _volume_ _directory_\n**ntfs-3g.secaudit** **-u**[**v**] _volume_ _mounted-file_\n**ntfs-3g.secaudit** **-h** _file_\n**ntfs-3g.secaudit** _volume_ _directory_"],["PARAMETERS","**-a**\n> Audit all global security data on the volume. Combine with -r to also scan all files and directories.\n\n**-b**\n> Recursively extract NTFS ACLs to standard output for backup purposes.\n\n**-s**\n> Set NTFS ACLs from a backup file or standard input. Combine with -e to also restore extra parameters (Windows attrib).\n\n**-e**\n> Used with -s to set extra backed-up parameters.\n\n**-r**\n> Recurse into subdirectories. With -a, checks file relations to global security data.\n\n**-u**\n> Display a proposed user mapping file based on Windows ownership of a mounted file.\n\n**-h _file_**\n> Display hexadecimal security descriptors from a saved file in human-readable form.\n\n**-v**\n> Verbose output. Use twice for very verbose output."],["DESCRIPTION","**ntfs-3g.secaudit** displays the ownership and permissions of files on an NTFS file system and checks their consistency. It can audit security data, backup and restore ACLs, and propose user mappings. The volume must be unmounted and the command must be run as root."],["CAVEATS","Requires root privileges. The volume must be unmounted before use. Output should be redirected to a file for large directory operations. Returns 0 on success and 1 when errors are detected."],["HISTORY","**ntfs-3g.secaudit** is part of the **ntfs-3g** project, providing security audit and ACL management functionality for NTFS filesystems on Linux."],["SEE ALSO","[ntfs-3g](/man/ntfs-3g)(8), [ntfs-3g.usermap](/man/ntfs-3g.usermap)(8)"]],"e":["ntfs-3g","ntfs-3g.usermap"],"x":"sudo ntfs-3g.secaudit -ar [/dev/sda1]"},"onefetch":{"n":"onefetch","t":"command-line Git information tool that displays repository summary with ASCII","s":[["TAGLINE","command-line Git information tool that displays repository summary with ASCII"],["TLDR","**Show repository info**\n\n```onefetch```\n\n**Show info for specific repository**\n\n```onefetch [/path/to/repo]```\n\n**Show info in ASCII art**\n\n```onefetch --ascii-language [python]```\n\n**Disable ASCII art**\n\n```onefetch --no-art```\n\n**Show specific info only**\n\n```onefetch --show-info [project,head,version,size]```\n\n**Hide specific info**\n\n```onefetch --hide-info [authors,commits]```\n\n**Output as JSON**\n\n```onefetch --output json```\n\n**Use custom image**\n\n```onefetch --image [/path/to/image.png]```"],["SYNOPSIS","**onefetch** [_path_] [_--ascii-language lang_] [_--image file_] [_--output format_] [_options_]"],["PARAMETERS","**-a**, **--ascii-language** _LANG_\n> Force ASCII art for specified language.\n\n**-i**, **--image** _FILE_\n> Use custom image instead of ASCII.\n\n**--image-protocol** _PROTOCOL_\n> Image protocol: sixel, kitty, iterm.\n\n**--no-art**\n> Disable ASCII/image art.\n\n**-o**, **--output** _FORMAT_\n> Output format: yaml, json.\n\n**--show-info** _FIELDS_\n> Show only specified info fields.\n\n**--hide-info** _FIELDS_\n> Hide specified info fields.\n\n**--no-bold**\n> Disable bold text.\n\n**--no-color-palette**\n> Hide color palette.\n\n**--number-of-authors** _NUM_\n> Number of authors to display.\n\n**--number-of-languages** _NUM_\n> Number of languages to display.\n\n**-e**, **--exclude** _PATTERNS_\n> Ignore files matching patterns.\n\n**--no-bots**\n> Exclude bot commits from statistics.\n\n**-T**, **--true-color** _WHEN_\n> True color: auto, always, never.\n\n**-d**, **--disabled-fields** _FIELDS_\n> Deprecated alias for --hide-info."],["DESCRIPTION","**onefetch** is a command-line Git information tool that displays repository summary with ASCII art representing the dominant programming language. It's designed for quick repository overview, similar to neofetch for system info.\n\nThe display includes: project name, description, HEAD reference, version tags, creation date, languages breakdown, authors, last change, repository size, lines of code, license, and dependencies.\n\nASCII art is automatically selected based on the dominant language. Over 100 language logos are included. Custom images can be displayed in supported terminals (kitty, iTerm2, terminals with Sixel support).\n\nAuthor statistics show commit counts and line contributions. Bot accounts can be filtered out. Language detection respects .gitignore and can exclude additional patterns.\n\nOutput formats (JSON, YAML) enable integration with other tools. Field selection allows customizing what information appears, useful for different use cases or terminal sizes."],["CAVEATS","Requires a Git repository. ASCII art needs sufficient terminal width. Image display requires compatible terminal. Large repositories may take time to analyze. Some statistics require full repository history. Language detection may misidentify some file types."],["HISTORY","**onefetch** was created by **Ossama Hjaji** (o2sh) starting around **2019**, inspired by neofetch and screenfetch. Written in Rust, it gained popularity for providing a visually appealing way to showcase repository information. The project has grown to support many languages and terminals."],["INSTALL","```dnf: sudo dnf install onefetch```\n\n```pacman: sudo pacman -S onefetch```\n\n```apk: sudo apk add onefetch```\n\n```zypper: sudo zypper install onefetch```\n\n```brew: brew install onefetch```\n\n```nix: nix profile install nixpkgs#onefetch```"],["SEE ALSO","[neofetch](/man/neofetch)(1), [tokei](/man/tokei)(1), [git](/man/git)(1), [cloc](/man/cloc)(1)"]],"e":["neofetch","tokei","git","cloc"],"x":"onefetch"},"openssl":{"n":"openssl","t":"robust toolkit for cryptographic operations, SSL/TLS protocols","s":[["TAGLINE","robust toolkit for cryptographic operations, SSL/TLS protocols"],["TLDR","**Generate a private key**\n\n```openssl genrsa -out [private.key] [2048]```\n\n**Generate a certificate signing request**\n\n```openssl req -new -key [private.key] -out [request.csr]```\n\n**Generate a self-signed certificate**\n\n```openssl req -x509 -newkey rsa:4096 -keyout [key.pem] -out [cert.pem] -days [365] -nodes```\n\n**View certificate details**\n\n```openssl x509 -in [certificate.crt] -text -noout```\n\n**Check certificate expiration**\n\n```openssl x509 -enddate -noout -in [certificate.crt]```\n\n**Verify certificate chain**\n\n```openssl verify -CAfile [ca.crt] [certificate.crt]```\n\n**Test SSL connection**\n\n```openssl s_client -connect [host:443]```\n\n**Encrypt a file**\n\n```openssl enc -aes-256-cbc -salt -in [plaintext.txt] -out [encrypted.enc]```\n\n**Decrypt a file**\n\n```openssl enc -aes-256-cbc -d -in [encrypted.enc] -out [decrypted.txt]```\n\n**Generate random bytes**\n\n```openssl rand -base64 [32]```"],["SYNOPSIS","**openssl** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**OpenSSL** is a robust toolkit for cryptographic operations, SSL/TLS protocols, and certificate management. It provides commands for generating keys, creating certificates, encrypting data, testing connections, and performing various cryptographic functions.\n\nThe toolkit supports numerous algorithms for encryption (AES, DES, ChaCha20), hashing (SHA, MD5), and public key cryptography (RSA, ECDSA, Ed25519). It can act as a client or server for testing SSL/TLS connections.\n\nOpenSSL is essential for system administrators managing certificates, developers implementing secure communications, and security professionals analyzing cryptographic configurations."],["COMMON COMMANDS","**genrsa**\n> Generate RSA private key.\n\n**req**\n> Certificate signing request operations.\n\n**x509**\n> Certificate operations.\n\n**rsa**\n> RSA key processing.\n\n**enc**\n> Symmetric encryption/decryption.\n\n**dgst**\n> Message digest (hashing).\n\n**s_client**\n> SSL/TLS client for testing.\n\n**s_server**\n> SSL/TLS server for testing.\n\n**verify**\n> Certificate verification.\n\n**rand**\n> Random number generation.\n\n**pkcs12**\n> PKCS#12 operations.\n\n**ca**\n> Certificate authority operations."],["PARAMETERS","**-in** _file_\n> Input file.\n\n**-out** _file_\n> Output file.\n\n**-noout**\n> No output (for viewing).\n\n**-text**\n> Human-readable output.\n\n**-nodes**\n> No DES encryption of private key.\n\n**-days** _n_\n> Certificate validity period.\n\n**-subj** _subject_\n> Certificate subject DN."],["CAVEATS","Old versions have known vulnerabilities (update regularly). Encryption commands prompt for passwords interactively. Self-signed certificates cause browser warnings. Some legacy algorithms (MD5, DES) are insecure."],["HISTORY","**OpenSSL** originated from **SSLeay**, created by **Eric Young** and **Tim Hudson** in **1995**. It was forked as OpenSSL in **1998** when development of SSLeay ended. The project gained critical importance for internet security, though the **Heartbleed** vulnerability in **2014** highlighted maintenance challenges. This led to increased funding and the **LibreSSL** fork by OpenBSD."],["INSTALL","```apt: sudo apt install openssl```\n\n```dnf: sudo dnf install openssl```\n\n```pacman: sudo pacman -S openssl```\n\n```apk: sudo apk add openssl```\n\n```zypper: sudo zypper install openssl```\n\n```brew: brew install openssl```\n\n```nix: nix profile install nixpkgs#openssl```"],["SEE ALSO","[ssh-keygen](/man/ssh-keygen)(1), [certbot](/man/certbot)(1), [gpg](/man/gpg)(1), [stunnel](/man/stunnel)(1)"]],"e":["ssh-keygen","certbot","gpg","stunnel"],"x":"openssl genrsa -out [private.key] [2048]"},"pamarith":{"n":"pamarith","t":"performs arithmetic operations on two PAM/PNM images","s":[["TAGLINE","performs arithmetic operations on two PAM/PNM images"],["TLDR","**Add two images**\n\n```pamarith -add [image1.pam] [image2.pam] > [result.pam]```\n\n**Subtract images**\n\n```pamarith -subtract [image1.pam] [image2.pam] > [result.pam]```\n\n**Multiply images**\n\n```pamarith -multiply [image1.pam] [image2.pam] > [result.pam]```\n\n**Find difference**\n\n```pamarith -difference [image1.pam] [image2.pam] > [result.pam]```\n\n**Minimum of images**\n\n```pamarith -minimum [image1.pam] [image2.pam] > [result.pam]```"],["SYNOPSIS","**pamarith** _-function_ _pamfile1_ _pamfile2_"],["PARAMETERS","**-add**\n> Add pixel values.\n\n**-subtract**\n> Subtract pixel values.\n\n**-multiply**\n> Multiply pixel values.\n\n**-divide**\n> Divide pixel values.\n\n**-difference**\n> Absolute difference.\n\n**-minimum**\n> Minimum of each pixel.\n\n**-maximum**\n> Maximum of each pixel.\n\n**-mean**\n> Average of pixels.\n\n**-and**, **-or**, **-xor**\n> Bitwise operations."],["DESCRIPTION","**pamarith** performs arithmetic operations on two PAM/PNM images. Images must have the same dimensions. Used for compositing, masking, and image comparison."],["EXAMPLE","```bash"],["Find difference between images","pamarith -difference old.ppm new.ppm > diff.ppm"],["Blend two images","pamarith -mean image1.ppm image2.ppm > blended.ppm\n```"],["CAVEATS","Images must be same size. PAM format preferred. Output may need scaling for display."],["HISTORY","pamarith is part of **Netpbm**, a toolkit for image manipulation dating back to the late 1980s."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmarith](/man/pnmarith)(1), [pamcomp](/man/pamcomp)(1), [netpbm](/man/netpbm)(1)"]],"e":["pnmarith","pamcomp","netpbm"],"x":"pamarith -add [image1.pam] [image2.pam] > [result.pam]"},"parallel":{"n":"parallel","t":"runs jobs in parallel, utilizing multiple CPU cores","s":[["TAGLINE","runs jobs in parallel, utilizing multiple CPU cores"],["TLDR","**Run command on multiple inputs**\n\n```parallel [gzip] ::: [*.txt]```\n\n**Pipe input to parallel jobs**\n\n```cat [urls.txt] | parallel [curl -O]```\n\n**Run with specific number of jobs**\n\n```parallel -j [4] [command] ::: [args]```\n\n**Use input as argument placeholder**\n\n```parallel [wget {}] ::: [url1] [url2]```\n\n**Multiple input sources**\n\n```parallel [convert {1} {2}] ::: [*.jpg] ::: [--resize 50%]```\n\n**Run on remote hosts**\n\n```parallel -S [host1,host2] [command] ::: [args]```\n\n**Show progress bar**\n\n```parallel --bar [command] ::: [args]```\n\n**Dry run** (show commands without executing)\n\n```parallel --dry-run [command] ::: [args]```"],["SYNOPSIS","**parallel** [_-j jobs_] [_-S hosts_] [_--bar_] [_options_] _command_ **:::** _arguments_"],["PARAMETERS","**-j**, **--jobs** _N_\n> Number of parallel jobs (N, N%, +N, -N).\n\n**-S**, **--sshlogin** _HOSTS_\n> Remote hosts for distributed execution.\n\n**-a**, **--arg-file** _FILE_\n> Read arguments from file.\n\n**:::**, **:::::**\n> Argument separators (inline or from file).\n\n**{}**\n> Replacement string for argument.\n\n**{#}**\n> Job number.\n\n**{%}**\n> Job slot number.\n\n**{.}**\n> Argument without extension.\n\n**{/}**\n> Basename of argument.\n\n**{//}**\n> Directory of argument.\n\n**--bar**\n> Show progress bar.\n\n**--eta**\n> Show estimated time of arrival.\n\n**--dry-run**\n> Show commands without executing.\n\n**--keep-order**, **-k**\n> Preserve output order.\n\n**--halt** _MODE_\n> When to halt: never, soon, now.\n\n**--results** _DIR_\n> Save output to files in directory.\n\n**--delay** _TIME_\n> Delay between job starts.\n\n**--retries** _N_\n> Retry failed jobs.\n\n**--timeout** _TIME_\n> Maximum runtime per job."],["DESCRIPTION","**GNU Parallel** runs jobs in parallel, utilizing multiple CPU cores. It's a more powerful replacement for xargs -P, designed for parallel processing of command-line tasks.\n\nArguments can come from the command line (:::), files (::::), or stdin. The {} placeholder is replaced with each argument. Variants like {.} (no extension), {/} (basename), and {//} (dirname) enable flexible filename manipulation.\n\nJob control includes limiting parallelism (-j), distributing across SSH hosts (-S), and handling failures (--halt). Output can be kept in order (--keep-order) or saved to separate files (--results).\n\nFor distributed computing, parallel copies necessary files to remote hosts, runs jobs, and retrieves output. This enables clusters without complex job schedulers.\n\nProgress tracking with --bar or --eta helps monitor long-running batch jobs. Logging options record what ran and what failed for later analysis."],["CAVEATS","Shell quoting can be tricky with complex commands. Different from moreutils parallel (simpler tool). Progress bar requires terminal. Distributed mode needs SSH key setup. Memory usage grows with job count. Command output interleaving without -k."],["HISTORY","**GNU Parallel** was written by **Ole Tange** starting around **2007**. It was designed to be a general parallelization tool, more flexible than xargs. The tool has grown to include distributed computing features and sophisticated job control. It's released under GPL and requests citation in academic work."],["INSTALL","```apt: sudo apt install moreutils```\n\n```dnf: sudo dnf install moreutils```\n\n```pacman: sudo pacman -S moreutils```\n\n```apk: sudo apk add parallel```\n\n```zypper: sudo zypper install moreutils```\n\n```brew: brew install moreutils```\n\n```nix: nix profile install nixpkgs#moreutils```"],["SEE ALSO","[xargs](/man/xargs)(1), [make](/man/make)(1), [sem](/man/sem)(1), [nohup](/man/nohup)(1)"]],"e":["xargs","make","sem","nohup"],"x":"parallel [gzip] ::: [*.txt]"},"pipetron":{"n":"pipetron","t":"PipeWire node manager for Electron audio streams","s":[["TAGLINE","PipeWire node manager for Electron audio streams"],["TLDR","**Start** the pipetron daemon\n\n```pipetron --daemon```\n\n**Start** the volume daemon only\n\n```pipetron --volume-daemon```\n\n**Start** the audio daemon (experimental)\n\n```pipetron --audio-daemon```\n\n**Show version** information\n\n```pipetron --version```"],["SYNOPSIS","**pipetron** [**-d**|**-vd**|**-ad**] [**-h**] [**-v**]"],["PARAMETERS","**-d**, **--daemon**\n> Start the daemon as specified in the configuration file.\n\n**-vd**, **--volume-daemon**\n> Start the volume daemon specifically.\n\n**-ad**, **--audio-daemon**\n> Start the audio daemon (experimental).\n\n**-h**, **--help**\n> Display help information.\n\n**-v**, **--version**\n> Show version details."],["DESCRIPTION","**pipetron** is a PipeWire node manager that fixes a long-standing issue with Electron applications showing all their audio streams as \"Chromium\" in PipeWire-based audio managers. This makes it impossible to distinguish between different Electron apps in volume controllers like **pavucontrol**.\n\nPipetron replicates each Electron audio stream with its actual application name and icon, then performs a one-way volume sync from the replicated stream to the corresponding Electron stream. As a result, **WirePlumber** correctly saves per-application volume settings.\n\nThe tool typically runs as a **systemd user service** in the background, starting automatically on login."],["CONFIGURATION","Configuration is stored in **~/.config/pipetron/config.toml**. The TOML file specifies which daemon type to run and related settings."],["CAVEATS","Requires **PipeWire** as the audio server. Only affects Electron-based applications. The audio daemon mode is experimental. The tool runs as a background service and provides no interactive terminal interface."],["HISTORY","**pipetron** (a portmanteau of PipeWire and Electron) was created to address the widespread issue of Electron applications being unable to set proper audio stream names under PipeWire. The project is available on **Codeberg** and mirrored on GitHub, and is packaged in the **Arch Linux AUR**."],["INSTALL","```aur: yay -S pipetron```"],["SEE ALSO","[pipewire](/man/pipewire)(1), [pavucontrol](/man/pavucontrol)(1), [wireplumber](/man/wireplumber)(1)"]],"e":["pipewire","pavucontrol","wireplumber"],"x":"pipetron --daemon"},"pkgadd":{"n":"pkgadd","t":"Install packages on CRUX Linux","s":[["TAGLINE","Install packages on CRUX Linux"],["TLDR","**Install** a local package\n\n```pkgadd [package]```\n\n**Update** an existing package\n\n```pkgadd -u [package]```"],["SYNOPSIS","**pkgadd** [**-u**] [**-f**] [**-r** _root_] _package_"],["PARAMETERS","**-u**\n> Upgrade mode, update existing package\n\n**-f**\n> Force installation, overwrite existing files\n\n**-r _root_**\n> Use alternate root directory"],["DESCRIPTION","**pkgadd** installs software packages on CRUX Linux systems. Packages are tar archives with a specific structure containing binaries, libraries, and metadata.\n\nThe tool extracts package contents to the filesystem and registers the package in the system database. It handles file conflicts and can upgrade existing installations."],["CAVEATS","CRUX-specific package manager. Packages must be built locally or downloaded. No automatic dependency resolution. Manual dependency management required."],["HISTORY","**pkgadd** is part of **pkgutils**, the native package management system for **CRUX Linux**. CRUX emphasizes simplicity with packages being simple tar archives and minimal tooling."],["SEE ALSO","[pkgrm](/man/pkgrm)(8), [pkginfo](/man/pkginfo)(1), [pkgmk](/man/pkgmk)(8)"]],"e":["pkgrm","pkginfo","pkgmk"],"x":"pkgadd [package]"},"pnmnoise":{"n":"pnmnoise","t":"Generate random noise PNM images (often an alias for pgmnoise)","s":[["TAGLINE","Generate random noise PNM images (often an alias for pgmnoise)"],["TLDR","**Create grayscale noise image** (via pgmnoise)\n\n```pgmnoise [width] [height] > [noise.pgm]```\n\n**Build color noise** by combining three grayscale noise channels\n\n```rgb3toppm <(pgmnoise 256 256) <(pgmnoise 256 256) <(pgmnoise 256 256) > noise.ppm```\n\n**Add noise to an existing image**\n\n```pamaddnoise -type=gaussian -lsigma=0.1 [image.pnm] > [noisy.pnm]```"],["SYNOPSIS","**pgmnoise** [**-randomseed** _n_] _width_ _height_"],["PARAMETERS","**width**\n> Image width in pixels.\n\n**height**\n> Image height in pixels.\n\n**-randomseed** _n_\n> Seed the pseudo-random number generator for reproducible output."],["DESCRIPTION","There is no standalone **pnmnoise** program in modern Netpbm. The name is sometimes used loosely to refer to Netpbm's family of noise generators.\n\nUse **pgmnoise** to generate a grayscale PGM of white noise, **pbmnoise** for a PBM (bitmap) of random black/white pixels, and combine three **pgmnoise** outputs with **rgb3toppm** to create a color PPM noise image. **pamaddnoise** adds several noise distributions (gaussian, impulse, multiplicative-gaussian, etc.) to an existing image."],["EXAMPLES","```bash"],["Grayscale noise 512x512","pgmnoise 512 512 > noise.pgm"],["Reproducible grayscale noise","pgmnoise -randomseed 12345 100 100 > noise.pgm"],["Color noise by merging three channels","rgb3toppm <(pgmnoise 256 256) <(pgmnoise 256 256) <(pgmnoise 256 256) > noise.ppm"],["Convert to PNG","pgmnoise 200 200 | pnmtopng > noise.png\n```"],["CAVEATS","**pnmnoise** is not a distinct binary; pgmnoise/pbmnoise produce single-channel noise only. Random distribution is uniform; for other distributions use pamaddnoise on a base image."],["HISTORY","The Netpbm noise generators are part of the **Netpbm** package (originally **pbmplus** by **Jef Poskanzer**, 1988). The older **pnmaddnoise** was renamed to **pamaddnoise** in Netpbm 10.30."],["SEE ALSO","[pgmnoise](/man/pgmnoise)(1), [ppmforge](/man/ppmforge)(1), [ppmmake](/man/ppmmake)(1), [netpbm](/man/netpbm)(1)"]],"e":["pgmnoise","ppmforge","ppmmake","netpbm"],"x":"pgmnoise [width] [height] > [noise.pgm]"},"podman-rm":{"n":"podman-rm","t":"Remove one or more containers","s":[["TAGLINE","Remove one or more containers"],["TLDR","**Remove container**\n\n```podman rm [container]```\n\n**Force remove running container**\n\n```podman rm -f [container]```\n\n**Remove multiple containers**\n\n```podman rm [container1] [container2]```\n\n**Remove all containers**\n\n```podman rm -a```\n\n**Remove container and its anonymous volumes**\n\n```podman rm -v [container]```\n\n**Remove containers matching a filter**\n\n```podman rm --filter status=exited```"],["SYNOPSIS","**podman rm** [_options_] _container_..."],["PARAMETERS","**-a**, **--all**\n> Remove all containers.\n\n**--cidfile** _file_\n> Read container ID from file and remove it. Can be specified multiple times.\n\n**--depend**\n> Remove selected container and recursively remove all containers that depend on it.\n\n**--filter** _filter_\n> Filter containers to remove (e.g., status, label, network, ancestor, name).\n\n**-f**, **--force**\n> Force removal of running and paused containers.\n\n**-i**, **--ignore**\n> Ignore errors when specified containers are not in the container store.\n\n**-l**, **--latest**\n> Use the last created container instead of specifying name or ID.\n\n**-t**, **--time** _seconds_\n> Seconds to wait before forcibly stopping the container (requires --force). Use -1 for infinite wait.\n\n**-v**, **--volumes**\n> Remove anonymous volumes associated with the container."],["DESCRIPTION","**podman rm** removes one or more containers. By default, only stopped containers can be removed. Use --force to remove running containers."],["EXAMPLES","```bash"],["Remove stopped container","podman rm mycontainer"],["Force remove running container","podman rm -f webserver"],["Remove all containers","podman rm -a"],["Remove all containers including running ones","podman rm -af"],["Remove with anonymous volumes","podman rm -v database"],["Remove containers filtered by status","podman rm --filter status=exited"],["Remove latest container","podman rm -l\n```"],["CAVEATS","Cannot remove running containers without --force. Anonymous volumes not removed unless -v specified. Named volumes are never removed by this command."],["HISTORY","podman rm is part of **Podman**, the daemonless container engine by **Red Hat** providing Docker-compatible container management."],["INSTALL","```apt: sudo apt install podman```\n\n```dnf: sudo dnf install podman```\n\n```pacman: sudo pacman -S podman```\n\n```apk: sudo apk add podman```\n\n```zypper: sudo zypper install podman```\n\n```brew: brew install podman```\n\n```nix: nix profile install nixpkgs#podman```"],["SEE ALSO","[podman](/man/podman)(1), [podman-rmi](/man/podman-rmi)(1), [podman-ps](/man/podman-ps)(1), [docker-rm](/man/docker-rm)(1)"]],"e":["podman","podman-rmi","podman-ps","docker-rm"],"x":"podman rm [container]"},"pppoe-connect":{"n":"pppoe-connect","t":"Manage a PPPoE DSL connection","s":[["TAGLINE","Manage a PPPoE DSL connection"],["TLDR","**Start PPPoE connection with default config**\n\n```sudo pppoe-connect```\n\n**Connect with a specific config file**\n\n```sudo pppoe-connect [/etc/ppp/pppoe.conf]```"],["SYNOPSIS","**pppoe-connect** [_config_file_]"],["PARAMETERS","_CONFIG_FILE_\n> Configuration file path. Defaults to /etc/ppp/pppoe.conf."],["DESCRIPTION","**pppoe-connect** is a shell script that manages a PPPoE (PPP over Ethernet) connection using the Roaring Penguin user-space PPPoE client. It reads a configuration file, brings up the connection, and automatically re-establishes it if it drops, logging reconnection events to syslog.\n\nEach time the connection drops, pppoe-connect executes /etc/ppp/pppoe-lost if it exists. Normally you should use **pppoe-start** instead of invoking pppoe-connect directly."],["CAVEATS","Requires configured pppoe.conf and pppd. Normally invoked via pppoe-start rather than directly. Multiple connections supported with different config files using different PIDFILE settings."],["HISTORY","pppoe-connect is part of the **Roaring Penguin PPPoE** client package."],["INSTALL","```apt: sudo apt install pppoe```\n\n```dnf: sudo dnf install rp-pppoe```\n\n```pacman: sudo pacman -S rp-pppoe```\n\n```apk: sudo apk add rp-pppoe```\n\n```zypper: sudo zypper install rp-pppoe```\n\n```nix: nix profile install nixpkgs#rp-pppoe```"],["SEE ALSO","[pppoe](/man/pppoe)(8), [pppoe-setup](/man/pppoe-setup)(8), [pppoe-stop](/man/pppoe-stop)(8), [pppd](/man/pppd)(8), [pppoe-discovery](/man/pppoe-discovery)(8)"]],"e":["pppoe","pppoe-setup","pppoe-stop","pppd","pppoe-discovery"],"x":"sudo pppoe-connect"},"printf":{"n":"printf","t":"Format and print data with specifiers","s":[["TAGLINE","Format and print data with specifiers"],["TLDR","**Print formatted output**\n\n```printf \"%s\\n\" \"Hello\"```\n\n**Print with variables**\n\n```printf \"Name: %s, Age: %d\\n\" \"[name]\" [25]```\n\n**Print hexadecimal**\n\n```printf \"%x\\n\" [255]```\n\n**Print with padding**\n\n```printf \"%10s\\n\" \"text\"```\n\n**Print floating point**\n\n```printf \"%.2f\\n\" [3.14159]```"],["SYNOPSIS","**printf** _format_ [_arguments_]"],["PARAMETERS","_FORMAT_\n> Format string.\n\n_ARGUMENTS_\n> Values to format.\n\n**%s**\n> String.\n\n**%d**\n> Integer.\n\n**%f**\n> Float.\n\n**%x**\n> Hexadecimal.\n\n**%o**\n> Octal."],["DESCRIPTION","**printf** formats and prints data according to a format string, similar to the C library function of the same name. The format string can contain literal text, escape sequences like **\\n** and **\\t**, and conversion specifiers such as **%s** for strings, **%d** for integers, **%f** for floating-point numbers, and **%x** for hexadecimal.\n\nUnlike **echo**, printf provides precise control over output formatting and behaves consistently across shells and platforms. If more arguments are provided than the format string consumes, the format string is reused, making it easy to process lists. The command does not append a trailing newline unless explicitly included with **\\n** in the format string.\n\nprintf is part of GNU coreutils and is also available as a shell built-in in most shells. It is the preferred tool for generating formatted output in portable shell scripts."],["CAVEATS","Format string required. More portable than echo."],["HISTORY","printf originated from **C programming language** formatting functions."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[echo](/man/echo)(1), [print](/man/print)(1), [fmt](/man/fmt)(1)"]],"e":["echo","print","fmt"],"x":"printf \"%s\\n\" \"Hello\""},"pw-config":{"n":"pw-config","t":"Inspect PipeWire configuration files and sections","s":[["TAGLINE","Inspect PipeWire configuration files and sections"],["TLDR","List all **configuration files** that will be used\n\n```pw-config```\n\nList configuration files for **PipeWire PulseAudio** server\n\n```pw-config -n pipewire-pulse.conf```\n\nList all configuration **sections** used by PipeWire PulseAudio\n\n```pw-config -n pipewire-pulse.conf list```\n\nList the **context.properties fragments** used by JACK clients\n\n```pw-config -n jack.conf list context.properties```\n\nList the **merged context.properties** used by JACK clients\n\n```pw-config -n jack.conf merge context.properties```\n\nList the merged **context.modules** and reformat\n\n```pw-config -n pipewire.conf -r merge context.modules```"],["SYNOPSIS","**pw-config** [_options_] [_command_] [_section_]"],["PARAMETERS","**-n**, **--name** _config_\n> Specify configuration file name\n\n**-r**, **--recurse**\n> Reformat/recurse into nested structures\n\n**list** [_section_]\n> List configuration fragments\n\n**merge** [_section_]\n> Show merged configuration\n\n**-h**, **--help**\n> Display help message"],["DESCRIPTION","**pw-config** displays configuration paths and sections used by PipeWire server and clients. It helps understand how PipeWire's split configuration files are combined, showing which files contribute to specific settings.\n\nThe tool can inspect different configuration contexts (pipewire.conf, pipewire-pulse.conf, jack.conf) and display both individual fragments and merged results."],["CAVEATS","Configuration files are searched in multiple locations (/etc/pipewire, ~/.config/pipewire, etc.) and merged by priority. Understanding the merge order is important for troubleshooting configuration issues."],["HISTORY","Part of **PipeWire**, the modern multimedia framework for Linux. Added to help administrators and users understand PipeWire's flexible configuration system."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-dev```"],["SEE ALSO","[pipewire](/man/pipewire)(1), [pw-cli](/man/pw-cli)(1), [wpctl](/man/wpctl)(1)"]],"e":["pipewire","pw-cli","wpctl"],"x":"pw-config"},"redis-server":{"n":"redis-server","t":"In-memory data structure store and cache server","s":[["TAGLINE","In-memory data structure store and cache server"],["TLDR","**Start Redis server** with default configuration\n\n```redis-server```\n\n**Start with a configuration file**\n\n```redis-server [/path/to/redis.conf]```\n\n**Start on a specific port**\n\n```redis-server --port [6380]```\n\n**Start with password protection**\n\n```redis-server --requirepass [password]```\n\n**Start in daemonized mode** (background)\n\n```redis-server --daemonize yes```\n\n**Start with a specific data directory**\n\n```redis-server --dir [/path/to/data]```\n\n**Test configuration file syntax**\n\n```redis-server [/path/to/redis.conf] --test-memory [megabytes]```"],["SYNOPSIS","**redis-server** [_configfile_] [_options_]"],["PARAMETERS","**--port** _number_\n> Listen on specified port (default: 6379)\n\n**--bind** _address_\n> Bind to specified IP address(es)\n\n**--daemonize** _yes|no_\n> Run as daemon in background\n\n**--requirepass** _password_\n> Require password for client connections\n\n**--dir** _path_\n> Set working directory for data files\n\n**--dbfilename** _file_\n> Set RDB snapshot filename\n\n**--appendonly** _yes|no_\n> Enable append-only file persistence\n\n**--maxmemory** _bytes_\n> Set maximum memory limit\n\n**--maxmemory-policy** _policy_\n> Eviction policy when maxmemory reached\n\n**--loglevel** _level_\n> Set log verbosity (debug, verbose, notice, warning)\n\n**--logfile** _path_\n> Log to specified file\n\n**--replica-of** _host_ _port_\n> Start as replica of specified master"],["DESCRIPTION","**redis-server** is the Redis server daemon, an in-memory data structure store supporting strings, lists, sets, sorted sets, hashes, streams, and more. It provides high-performance key-value storage with optional persistence.\n\nRedis supports two persistence modes: RDB snapshots (point-in-time snapshots at intervals) and AOF (append-only file logging every write). Both can be enabled simultaneously for maximum durability.\n\nThe server supports replication for high availability, with replicas automatically syncing from a master. Redis Sentinel provides automatic failover, while Redis Cluster enables horizontal scaling across multiple nodes.\n\nConfiguration can be specified via command-line options or a configuration file. Command-line options override config file settings."],["CONFIGURATION","**/etc/redis/redis.conf**\n> Main server configuration file controlling bind address, port, persistence mode, memory limits, replication, and security settings.\n\n**--save** _seconds_ _changes_\n> RDB snapshot trigger: save dataset if at least N changes occur within N seconds. Multiple save rules can be specified.\n\n**--appendonly yes**\n> Enable AOF (Append Only File) persistence, logging every write operation for stronger durability than RDB snapshots alone."],["CAVEATS","By default, Redis binds to all interfaces (0.0.0.0), which may expose it to the network. In production, use **--bind** to restrict to specific addresses and **--requirepass** for authentication.\n\nRedis stores data in memory. Without **--maxmemory**, it can consume all available RAM. Set appropriate limits and eviction policies for production use.\n\nWith default settings, Redis may lose up to 1 second of writes on crash (RDB mode). Enable AOF with **appendfsync always** for stronger durability guarantees at the cost of performance."],["HISTORY","Redis was created by **Salvatore Sanfilippo** (antirez) in **2009**. Originally standing for \"Remote Dictionary Server,\" it quickly gained popularity as a high-performance caching and data structure server. Redis became part of Redis Ltd. (formerly Redis Labs) while remaining open source under BSD license."],["INSTALL","```apt: sudo apt install redis-server```\n\n```apk: sudo apk add valkey-compat```"],["SEE ALSO","[redis-cli](/man/redis-cli)(1), [redis-benchmark](/man/redis-benchmark)(1), [redis-sentinel](/man/redis-sentinel)(1), [memcached](/man/memcached)(1)"]],"e":["redis-cli","redis-benchmark","redis-sentinel","memcached"],"x":"redis-server"},"rfkill":{"n":"rfkill","t":"Enable and disable wireless devices","s":[["TAGLINE","Enable and disable wireless devices"],["TLDR","**List** devices\n\n```rfkill```\n\nFilter **columns**\n\n```rfkill -o ID,TYPE,DEVICE```\n\n**Block** device type\n\n```rfkill block wifi```\n\n**Unblock** device type\n\n```rfkill unblock bluetooth```\n\nOutput **JSON**\n\n```rfkill -J```"],["SYNOPSIS","**rfkill** [_OPTIONS_] [_command_] [_identifier_]"],["DESCRIPTION","**rfkill** enables and disables wireless communication devices (WiFi, Bluetooth, GPS, NFC, etc.) through the kernel's rfkill subsystem. It can soft-block devices in software while respecting hardware kill switches."],["PARAMETERS","**list**\n> List all devices (default action)\n\n**block** _type|id_\n> Disable specified device type or ID\n\n**unblock** _type|id_\n> Enable specified device type or ID\n\n**toggle** _type|id_\n> Toggle the state of device\n\n**-o, --output** _columns_\n> Specify output columns\n\n**-J, --json**\n> Output in JSON format\n\n**-n, --noheadings**\n> Don't print column headers"],["CAVEATS","Hardware kill switches cannot be overridden by software. Blocking may affect multiple devices if they share the same rfkill switch. Some devices may require reboot to fully reset."],["HISTORY","**rfkill** is part of **util-linux**, providing control over the kernel's rfkill subsystem for wireless devices."],["INSTALL","```apt: sudo apt install rfkill```\n\n```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[iw](/man/iw)(8), [bluetoothctl](/man/bluetoothctl)(1)"]],"e":["iw","bluetoothctl"],"x":"rfkill"},"rustormy":{"n":"rustormy","t":"Neofetch-inspired weather CLI with ASCII art","s":[["TAGLINE","Neofetch-inspired weather CLI with ASCII art"],["TLDR","**Display weather for your current location**\n\n```rustormy```\n\n**Show weather for a specific city**\n\n```rustormy -c [London]```\n\n**Display weather in imperial units with colors**\n\n```rustormy -c [Austin] --colors -u imperial```\n\n**Output weather data as JSON**\n\n```rustormy -c [Tokyo] -o json```"],["SYNOPSIS","**rustormy** [_options_]"],["PARAMETERS","**-c**, **--city** _CITY_\n> Specify the city name.\n\n**-u**, **--units** _UNITS_\n> Unit system: metric or imperial.\n\n**-o**, **--format** _FORMAT_\n> Output format: text or json.\n\n**-m**, **--text-mode** _MODE_\n> Display mode: full, compact, or one_line.\n\n**-l**, **--live**\n> Enable live mode with periodic updates.\n\n**--colors**\n> Enable ANSI color output."],["DESCRIPTION","**rustormy** is a lightweight weather CLI tool that displays current conditions including temperature, wind, humidity, precipitation, pressure, and UV index with ASCII art weather icons. It supports multiple weather data providers (Open-Meteo by default, no API key required), multiple output formats, and automatic provider fallback on API limits."],["HISTORY","**rustormy** was created by **Tairesh** and is written in **Rust**."],["SEE ALSO","[stormy](/man/stormy)(1), [curl](/man/curl)(1), [zeitfetch](/man/zeitfetch)(1)"]],"e":["stormy","curl","zeitfetch"],"x":"rustormy"},"scriptreplay":{"n":"scriptreplay","t":"Replay recorded terminal session output","s":[["TAGLINE","Replay recorded terminal session output"],["TLDR","**Replay** a typescript at original speed\n\n```scriptreplay path/to/timing_file path/to/typescript```\n\nReplay at **double speed**\n\n```scriptreplay path/to/timing_file path/to/typescript 2```\n\nReplay at **half speed**\n\n```scriptreplay path/to/timing_file path/to/typescript 0.5```"],["SYNOPSIS","**scriptreplay** _timing_file_ _typescript_ [_divisor_]"],["PARAMETERS","_timing_file_\n> File containing timing information from script\n\n_typescript_\n> File containing recorded terminal output\n\n_divisor_\n> Speed multiplier (2 = double, 0.5 = half)"],["DESCRIPTION","**scriptreplay** replays a terminal session recorded by the script command. It displays the output with the original timing preserved, useful for demonstrations and reviewing sessions.\n\nThe timing file contains delay information between each output block, allowing accurate replay."],["CAVEATS","Only displays output; does not execute commands. Requires both timing and typescript files from script recording."],["HISTORY","Part of **util-linux** package, complementing script for terminal session recording and playback."],["INSTALL","```apt: sudo apt install bsdutils```\n\n```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[script](/man/script)(1), [scriptlive](/man/scriptlive)(1)"]],"e":["script","scriptlive"],"x":"scriptreplay path/to/timing_file path/to/typescript"},"setfacl":{"n":"setfacl","t":"Set file access control lists","s":[["TAGLINE","Set file access control lists"],["TLDR","Set **read/write** access for a user\n\n```setfacl -m u:username:rw path/to/file```\n\nSet **default ACL** for new files in a directory\n\n```setfacl -d -m u::rw path/to/directory```\n\n**Remove** ACL for a specific user\n\n```setfacl -x u:username path/to/file```\n\nRemove **all** extended ACL entries\n\n```setfacl -b path/to/file```\n\nApply ACL **recursively** to a directory\n\n```setfacl -R -m u:username:rx path/to/directory```\n\n**Copy** ACL from one file to another\n\n```getfacl file1 | setfacl --set-file=- file2```\n\nSet **group** permissions via ACL\n\n```setfacl -m g:groupname:r path/to/file```"],["SYNOPSIS","**setfacl** [_-bkndRLPvh_] [{_-m_|_-x_} _acl_spec_] [{_-M_|_-X_} _acl_file_] _file_..."],["DESCRIPTION","**setfacl** sets Access Control Lists (ACLs) of files and directories. ACLs provide fine-grained access control beyond the traditional Unix owner/group/other permission model, allowing specific permissions for individual users and groups."],["PARAMETERS","**-m, --modify**\n> Modify the ACL with the specified entries.\n\n**-x, --remove**\n> Remove specified ACL entries.\n\n**-M, --modify-file**\n> Read ACL entries to modify from a file.\n\n**-X, --remove-file**\n> Read ACL entries to remove from a file.\n\n**-b, --remove-all**\n> Remove all extended ACL entries.\n\n**-k, --remove-default**\n> Remove the default ACL.\n\n**-d, --default**\n> Apply operations to the default ACL.\n\n**-n, --no-mask**\n> Do not recalculate the effective rights mask.\n\n**--mask**\n> Force recalculation of the effective rights mask.\n\n**-R, --recursive**\n> Apply operations recursively.\n\n**-L, --logical**\n> Follow symbolic links to directories (with -R).\n\n**-P, --physical**\n> Do not follow symbolic links (with -R).\n\n**--restore=file**\n> Restore permissions from a getfacl backup.\n\n**--test**\n> Test mode - list resulting ACLs without modifying."],["ACL ENTRY FORMAT","**u[ser]:uid:perms**: Named user or owner permissions\n**g[roup]:gid:perms**: Named group or owning group permissions\n**m[ask]:perms**: Effective rights mask\n**o[ther]:perms**: Permissions for others\n\nPermissions: r (read), w (write), x (execute), or numeric (4, 2, 1)."],["CAVEATS","The effective rights mask limits permissions granted to named users/groups. Some filesystems (e.g., FAT, NFS without ACL support) do not support ACLs. Use **getfacl** to backup ACLs before modifying. Default ACLs only apply to newly created files within a directory."],["HISTORY","**setfacl** is part of the **acl** package implementing POSIX Access Control Lists (POSIX 1003.1e draft 17). ACLs extend the standard Unix permission model."],["INSTALL","```apt: sudo apt install acl```\n\n```dnf: sudo dnf install acl```\n\n```pacman: sudo pacman -S acl```\n\n```apk: sudo apk add acl```\n\n```zypper: sudo zypper install acl```\n\n```brew: brew install acl```\n\n```nix: nix profile install nixpkgs#acl```"],["SEE ALSO","[getfacl](/man/getfacl)(1), [chmod](/man/chmod)(1), [umask](/man/umask)(1), [acl](/man/acl)(5)"]],"e":["getfacl","chmod","umask","acl"],"x":"setfacl -m u:username:rw path/to/file"},"soxi":{"n":"soxi","t":"Display audio file format information","s":[["TAGLINE","Display audio file format information"],["TLDR","**Show file info**\n\n```soxi [audio.wav]```\n\n**Show duration**\n\n```soxi -D [audio.wav]```\n\n**Show sample rate**\n\n```soxi -r [audio.wav]```\n\n**Show channels**\n\n```soxi -c [audio.wav]```\n\n**Show bit depth**\n\n```soxi -b [audio.wav]```\n\n**Show encoding**\n\n```soxi -e [audio.wav]```\n\n**Show file type**\n\n```soxi -t [audio.wav]```\n\n**Show total duration of multiple files**\n\n```soxi -T [file1.wav] [file2.wav]```"],["SYNOPSIS","**soxi** [_-D_] [_-r_] [_-c_] [_-b_] [_options_] _files_"],["PARAMETERS","**-D**\n> Duration in seconds.\n\n**-d**\n> Duration in time format.\n\n**-r**\n> Sample rate.\n\n**-c**\n> Channel count.\n\n**-b**\n> Bits per sample.\n\n**-e**\n> Encoding type.\n\n**-t**\n> File type.\n\n**-s**\n> Sample count.\n\n**-V**[_level_]\n> Set verbosity level.\n\n**-T**\n> Total duration when multiple files are given."],["DESCRIPTION","**soxi** displays audio file information. It's part of SoX (Sound eXchange).\n\nDuration shows file length. Seconds or time format available.\n\nTechnical parameters show format details. Sample rate, bit depth, channels.\n\nMultiple files can be queried. Summary totals available.\n\nOutput is scriptable. Individual values returned for parsing."],["CAVEATS","Part of SoX suite. Not all formats supported. Metadata limited to audio properties."],["HISTORY","**soxi** is the info utility from **SoX** (Sound eXchange), the Swiss Army knife of audio processing. Created by Chris Bagwell and others."],["INSTALL","```apt: sudo apt install sox```\n\n```dnf: sudo dnf install sox```\n\n```pacman: sudo pacman -S sox```\n\n```apk: sudo apk add sox```\n\n```zypper: sudo zypper install sox```\n\n```brew: brew install sox```\n\n```nix: nix profile install nixpkgs#sox```"],["SEE ALSO","[sox](/man/sox)(1), [ffprobe](/man/ffprobe)(1), [mediainfo](/man/mediainfo)(1)"]],"e":["sox","ffprobe","mediainfo"],"x":"soxi [audio.wav]"},"streambed":{"n":"streambed","t":"Stream PostgreSQL changes to Apache Iceberg tables on S3","s":[["TAGLINE","Stream PostgreSQL changes to Apache Iceberg tables on S3"],["TLDR","**Sync** Postgres WAL to Iceberg and serve queries\n\n```streambed sync --source-url=[postgres://user:pass@host:5432/db] --s3-bucket=[bucket] --s3-endpoint=[https://s3] --s3-prefix=[path] --query-addr=:5433```\n\n**Backfill** an Iceberg table with a one-shot snapshot\n\n```streambed resync --source-url=[postgres://...] --s3-bucket=[bucket] --s3-prefix=[path]```\n\n**Serve queries** against existing Iceberg tables (no Postgres connection)\n\n```streambed query --s3-bucket=[bucket] --s3-prefix=[path] --query-addr=:5433```\n\n**Delete** S3 objects and state for a table\n\n```streambed cleanup --s3-bucket=[bucket] --s3-prefix=[path] --table=[name]```\n\n**Inspect** sync flags\n\n```streambed sync --help```"],["SYNOPSIS","**streambed** _sync_|_resync_|_query_|_cleanup_ [_flags_]"],["DESCRIPTION","**streambed** is a change data capture (CDC) tool written in Go. It tails the **PostgreSQL** write-ahead log using logical replication, writes the resulting changes as **Parquet** files to **S3**, and commits **Apache Iceberg** metadata so the data is queryable as a versioned analytical table.\n\nThe `sync` subcommand runs as a long-lived daemon and can optionally expose a query endpoint compatible with the **PostgreSQL wire protocol**, so existing Postgres clients can read Iceberg tables without changes. `resync` performs a one-shot backfill under a consistent snapshot using `COPY`. `query` runs the wire-protocol server alone against tables that were already populated. `cleanup` deletes S3 objects and tracking state for a given table.\n\nStreambed targets the use case of offloading analytical workloads from a production Postgres instance to an Iceberg-on-S3 lakehouse while still letting tools that speak the Postgres protocol query the result."],["PARAMETERS","**sync**\n> Stream WAL changes, write Iceberg, optionally serve queries.\n\n**resync**\n> One-shot backfill via `COPY` under a consistent snapshot.\n\n**query**\n> Postgres-wire query server for existing Iceberg tables.\n\n**cleanup**\n> Delete S3 objects and state for a table.\n\n**--source-url** _URL_\n> Postgres connection string (or `STREAMBED_SOURCE_URL`).\n\n**--s3-bucket** _NAME_\n> Destination S3 bucket (or `STREAMBED_S3_BUCKET`).\n\n**--s3-endpoint** _URL_\n> S3 endpoint (use MinIO or other S3-compatible storage).\n\n**--s3-prefix** _PATH_\n> Key prefix within the bucket.\n\n**--query-addr** _HOST:PORT_\n> Bind address for the Postgres-wire query server."],["CONFIGURATION","Every flag has an environment-variable equivalent with a `STREAMBED_` prefix (for example `STREAMBED_SOURCE_URL`)."],["INSTALLATION","The project is built from source:\n\n```go build -o streambed ./cmd/streambed```"],["CAVEATS","Streambed needs Postgres logical replication to be enabled (`wal_level = logical`) and a replication slot. Like any CDC pipeline it can fall behind if the WAL grows faster than it is consumed, which keeps WAL segments on the primary until they are processed."],["SEE ALSO","[pg_basebackup](/man/pg_basebackup)(1), [pg_recvlogical](/man/pg_recvlogical)(1)"]],"e":["pg_basebackup","pg_recvlogical"],"x":"streambed sync --source-url=[postgres://user:pass@host:5432/db] --s3-bucket=[bucket] --s3-endpoint=[https://s3] --s3-prefix=[path] --query-addr=:5433"},"systemd-notify":{"n":"systemd-notify","t":"Notify service manager about status changes","s":[["TAGLINE","Notify service manager about status changes"],["TLDR","**Notify** systemd that the service has completed initialization\n\n```systemd-notify --booted```\n\n**Signal** to systemd that the service is ready\n\n```systemd-notify --ready```\n\n**Provide** a custom status message to systemd\n\n```systemd-notify --status \"[Add custom status message here...]\"```"],["SYNOPSIS","**systemd-notify** [_options_] [_variable=value_...]"],["PARAMETERS","**--ready**\n> Signal service readiness to systemd\n\n**--booted**\n> Check if system was booted with systemd\n\n**--status _text_**\n> Send status text to service manager\n\n**--pid _pid_**\n> Inform about main PID of daemon\n\n**--uid _user_**\n> Specify the user to notify as\n\n**--reloading**\n> Signal that service is reloading configuration\n\n**--stopping**\n> Signal that service is stopping"],["DESCRIPTION","**systemd-notify** notifies the service manager about start-up completion and other daemon status changes. It is primarily used within systemd service scripts for services with **Type=notify**.\n\nThe status text provided with **--status** is displayed by **systemctl status**. This allows services to communicate their current state and progress to administrators."],["CAVEATS","Only useful within systemd service contexts. The service must be configured with **Type=notify** or **Type=notify-reload** in its unit file. Has no effect when run outside of systemd services. Part of the systemd suite."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl](/man/systemctl)(1)"]],"e":["systemctl"],"x":"systemd-notify --booted"},"taproom":{"n":"taproom","t":"Interactive TUI for managing Homebrew packages","s":[["TAGLINE","Interactive TUI for managing Homebrew packages"],["TLDR","**Launch the Homebrew TUI browser**\n\n```taproom```\n\n**Start with only installed packages shown**\n\n```taproom --filters installed```\n\n**Sort by name and hide the help bar**\n\n```taproom --sort-column name --hide-help```"],["SYNOPSIS","**taproom** [_options_]"],["DESCRIPTION","**taproom** is a terminal user interface for Homebrew that lets you browse, search, install, remove, and view dependencies of formulae and casks. It displays packages in searchable tables with real-time filtering, status indicators, and recursive dependency visualization."],["CAVEATS","Requires Homebrew to be installed."],["HISTORY","**taproom** was created by **Johnny Huang** (hzqtc) and is written in **Go**."],["INSTALL","```brew: brew install taproom```"],["SEE ALSO","[brew](/man/brew)(1), [stew](/man/stew)(1), [nix](/man/nix)(1)"]],"e":["brew","stew","nix"],"x":"taproom"},"tcsh":{"n":"tcsh","t":"Enhanced C shell with editing","s":[["TAGLINE","Enhanced C shell with editing"],["TLDR","**Start an interactive tcsh shell**\n\n```tcsh```\n\n**Execute a command**\n\n```tcsh -c \"[command]\"```\n\n**Run a script**\n\n```tcsh [script.csh]```\n\n**Start as a login shell**\n\n```tcsh -l```\n\n**Start without reading startup files** (fast start)\n\n```tcsh -f```\n\n**Parse commands without executing** (syntax check)\n\n```tcsh -n [script.csh]```\n\n**Enable verbose mode** to echo commands after history substitution\n\n```tcsh -v```"],["SYNOPSIS","**tcsh** [_-bcefinstvVxX_] [_-l_] [_command_]"],["PARAMETERS","**-c** _command_\n> Read commands from the following argument string.\n\n**-e**\n> Exit immediately if a command exits with non-zero status.\n\n**-f**\n> Fast start; do not read ~/.tcshrc or ~/.cshrc startup files.\n\n**-i**\n> Force interactive mode even if input is not a terminal.\n\n**-l**\n> Act as a login shell; read .login and .logout files.\n\n**-n**\n> Parse commands but do not execute them (for debugging scripts).\n\n**-s**\n> Read command input from standard input.\n\n**-t**\n> Read and execute a single line of input.\n\n**-v**\n> Set verbose mode; echo commands after history substitution.\n\n**-x**\n> Set echo mode; echo commands before execution.\n\n**-V**\n> Set verbose mode before reading ~/.tcshrc.\n\n**-X**\n> Set echo mode before reading ~/.tcshrc.\n\n**-b**\n> Force a break from option processing.\n\n**-m**\n> Load ~/.tcshrc even if it doesn't belong to the effective user.\n\n**-q**\n> Accept SIGQUIT; for use under a debugger with job control disabled.\n\n**--help**\n> Print help message and exit.\n\n**--version**\n> Print version information and exit."],["DESCRIPTION","**tcsh** is an enhanced version of the Berkeley C shell (csh). It provides a command-line editor, programmable word completion, spelling correction, history mechanism, job control, and C-like syntax.\n\nInteractive shells read **~/.tcshrc** (or ~/.cshrc) on startup. Login shells additionally read **~/.login** and execute **~/.logout** on exit. The shell supports command history with **!** syntax, aliases, and shell variables.\n\nFeatures include filename completion with Tab, command history editing, directory stack manipulation with **pushd/popd**, and programmable completions for commands and arguments."],["CONFIGURATION","**~/.tcshrc**\n> Primary startup file read by all interactive tcsh shells; falls back to ~/.cshrc if not found\n\n**~/.login**\n> Read by login shells after ~/.tcshrc; typically sets environment variables and runs login-time commands\n\n**~/.logout**\n> Executed when a login shell exits\n\n**~/.cshdirs**\n> Saved directory stack restored on startup when savedirs shell variable is set"],["CAVEATS","tcsh uses C shell syntax which differs from POSIX sh/bash. Scripts written for tcsh may not be portable. The C shell family has known issues with complex scripting, and POSIX-compatible shells are generally recommended for portable scripts."],["HISTORY","The C shell (csh) was created by **Bill Joy** at UC Berkeley in the late 1970s for BSD Unix. tcsh was developed as an enhanced version with additional features like command-line editing and completion. The name \"tcsh\" comes from the TENEX operating system which inspired some features. tcsh became the default shell on FreeBSD and was widely used on Unix systems before bash became dominant."],["INSTALL","```apt: sudo apt install tcsh```\n\n```dnf: sudo dnf install tcsh```\n\n```pacman: sudo pacman -S tcsh```\n\n```apk: sudo apk add tcsh```\n\n```zypper: sudo zypper install tcsh```\n\n```brew: brew install tcsh```\n\n```nix: nix profile install nixpkgs#tcsh```"],["SEE ALSO","[csh](/man/csh)(1), [bash](/man/bash)(1), [zsh](/man/zsh)(1), [sh](/man/sh)(1), [ksh](/man/ksh)(1), [fish](/man/fish)(1)"]],"e":["csh","bash","zsh","sh","ksh","fish"],"x":"tcsh"},"tsv-filter":{"n":"tsv-filter","t":"Filter TSV files by column values","s":[["TAGLINE","Filter TSV files by column values"],["TLDR","**Filter by column value**\n\n```tsv-filter --eq [1]:[value] [file.tsv]```\n\n**Greater than**\n\n```tsv-filter --gt [2]:[100] [file.tsv]```\n\n**String contains**\n\n```tsv-filter --str-in-fld [1]:[pattern] [file.tsv]```\n\n**Multiple conditions**\n\n```tsv-filter --ge [2]:[10] --le [2]:[100] [file.tsv]```\n\n**Invert filter**\n\n```tsv-filter --invert --eq [1]:[value] [file.tsv]```"],["SYNOPSIS","**tsv-filter** [_--eq col:val_] [_--gt col:val_] [_options_] [_file_]"],["PARAMETERS","**--eq** _COL:VAL_\n> Equal to value.\n\n**--gt** _COL:VAL_\n> Greater than.\n\n**--lt** _COL:VAL_\n> Less than.\n\n**--str-in-fld** _COL:PAT_\n> String contains.\n\n**--invert**\n> Invert match.\n\n**-H**\n> Has header."],["DESCRIPTION","**tsv-filter** selects rows from tab-separated value files based on column conditions. It supports both numeric comparisons (equal, greater than, less than) and string operations (contains, exact match), allowing precise filtering of structured data.\n\nMultiple conditions can be combined, and they are joined with AND logic by default — all conditions must match for a row to be included. The **--invert** flag reverses the selection to output non-matching rows instead.\n\nPart of the **tsv-utils** suite, tsv-filter is written in D for high performance on large datasets. Column numbers are 1-based, and the **-H** flag enables header-aware processing."],["CAVEATS","TSV format. Column numbers start at 1. Part of tsv-utils."],["HISTORY","**tsv-filter** is part of **tsv-utils**, a collection of high-performance command-line tools for TSV files."],["SEE ALSO","[awk](/man/awk)(1), [cut](/man/cut)(1), [miller](/man/miller)(1)"]],"e":["awk","cut","miller"],"x":"tsv-filter --eq [1]:[value] [file.tsv]"},"turbostat":{"n":"turbostat","t":"Report CPU frequency, power, and idle statistics","s":[["TAGLINE","Report CPU frequency, power, and idle statistics"],["TLDR","**Display** statistics every 5 seconds\n\n```sudo turbostat```\n\n**Display** statistics every specified amount of seconds\n\n```sudo turbostat -i [n_seconds]```\n\n**Display** statistics without header information\n\n```sudo turbostat --quiet```\n\n**Display** useful CPU information every second for specific CPUs\n\n```sudo turbostat --quiet -i 1 --cpu 0-[CPU_count] --show \"PkgWatt\",\"Busy%\",\"Core\",\"CoreTmp\"```"],["SYNOPSIS","**turbostat** [_options_] [_command_...]"],["PARAMETERS","**-i, --interval _seconds_**\n> Sampling interval (default: 5 seconds)\n\n**--quiet**\n> Don't print system configuration header\n\n**--cpu _list_**\n> Specify which CPUs to monitor\n\n**--show _columns_**\n> Select columns to display\n\n**--hide _columns_**\n> Hide specified columns\n\n**--out _file_**\n> Write output to file\n\n**--debug**\n> Enable debug output"],["DESCRIPTION","**turbostat** reports processor topology, frequency, temperature, power consumption, and idle statistics for Intel and AMD processors. It reads hardware performance counters and model-specific registers.\n\nThe tool is valuable for understanding CPU behavior under load, identifying thermal throttling, and analyzing power consumption. It shows per-core and per-package statistics."],["CAVEATS","Requires root privileges and access to MSR (Model Specific Registers). Works best on Intel processors; AMD support varies. Some features require specific CPU models. Part of the Linux kernel tools."],["INSTALL","```apt: sudo apt install linux-cpupower```\n\n```pacman: sudo pacman -S cpupower```\n\n```apk: sudo apk add cpupower```\n\n```zypper: sudo zypper install cpupower```"],["SEE ALSO","[cpupower](/man/cpupower)(1), [lscpu](/man/lscpu)(1), [sensors](/man/sensors)(1), [powertop](/man/powertop)(8)"]],"e":["cpupower","lscpu","sensors","powertop"],"x":"sudo turbostat"},"wash":{"n":"wash","t":"wasmCloud development and management CLI","s":[["TAGLINE","wasmCloud development and management CLI"],["TLDR","**Create new WebAssembly component project**\n\n```wash new component [project-name]```\n\n**Build a component**\n\n```wash build```\n\n**Start development server with hot reload**\n\n```wash dev```\n\n**Start local wasmCloud environment**\n\n```wash up```\n\n**Stop local wasmCloud environment**\n\n```wash down```\n\n**Push component to OCI registry**\n\n```wash push [registry.example.com/component:tag] [component.wasm]```\n\n**Inspect component interfaces**\n\n```wash inspect [component.wasm]```"],["SYNOPSIS","**wash** [_options_] _command_ [_arguments_]"],["COMMANDS","**new**: Create project from template (component, provider).\n\n**build**: Build and sign component or provider.\n\n**dev**: Start hot-reload development loop.\n\n**up**: Bootstrap local wasmCloud environment.\n\n**down**: Tear down local environment.\n\n**app**: Manage declarative applications (wadm).\n\n**start**: Start a component or provider.\n\n**stop**: Stop a running component or provider.\n\n**scale**: Scale component instances.\n\n**push**: Push artifact to OCI registry.\n\n**pull**: Pull artifact from OCI registry.\n\n**inspect**: Show component WIT interfaces and metadata.\n\n**lint**: Check component for common issues.\n\n**completions**: Generate shell completions."],["PARAMETERS","**-o** _format_\n> Output format: text, json.\n\n**--nats-host** _host_\n> NATS server host.\n\n**--nats-port** _port_\n> NATS server port.\n\n**--lattice** _id_\n> Lattice ID to connect to.\n\n**--ctl-host** _host_\n> Control interface host."],["DESCRIPTION","**wash** (wasmCloud Shell) is the comprehensive CLI for developing WebAssembly components and managing wasmCloud distributed applications. It provides tools for the complete development lifecycle from project scaffolding to deployment.\n\nThe CLI supports multi-language component development including Rust, Go, and TypeScript. Projects are built to WebAssembly components following the Component Model specification. The dev command provides a hot-reload development server for rapid iteration.\n\nwasmCloud enables distributed applications where components communicate through capability providers for networking, storage, and other system services. wash manages local development environments and interacts with remote wasmCloud lattices."],["CAVEATS","Requires language-specific toolchains (Rust, Go, TinyGo) for building components. Local development needs Docker or Podman for NATS messaging. Some features require wasmCloud host running."],["HISTORY","**wash** was created by the wasmCloud project as the primary interface for WebAssembly component development. The wasmCloud platform emerged from the Wasm community's effort to build distributed systems using the Component Model. wash consolidated multiple tools into a single CLI, becoming the standard interface for the wasmCloud ecosystem."],["INSTALL","```apt: sudo apt install reaver```\n\n```dnf: sudo dnf install reaver```\n\n```pacman: sudo pacman -S reaver```\n\n```brew: brew install reaver```"],["SEE ALSO","[wasmtime](/man/wasmtime)(1), [docker](/man/docker)(1), [podman](/man/podman)(1)"]],"e":["wasmtime","docker","podman"],"x":"wash new component [project-name]"},"xcursorgen":{"n":"xcursorgen","t":"Create X cursor files from images","s":[["TAGLINE","Create X cursor files from images"],["TLDR","Create cursor from **config**\n\n```xcursorgen [path/to/config.cursor] [path/to/output_file]```\n\nCreate with image **prefix**\n\n```xcursorgen --prefix [path/to/image_directory]/ [path/to/config.cursor] [path/to/output_file]```\n\nWrite to **stdout**\n\n```xcursorgen [path/to/config.cursor]```"],["SYNOPSIS","**xcursorgen** [_OPTIONS_] _CONFIG_ [_OUTPUT_]"],["PARAMETERS","**--prefix** _PATH_\n> Path to image directory"],["DESCRIPTION","**xcursorgen** creates X cursor files from a collection of PNG images. The configuration file specifies image sizes, hotspots, and frame delays for animated cursors.\n\nThe config file format defines each cursor frame with size, hotspot coordinates, filename, and optional delay."],["CAVEATS","Images must be PNG format. Configuration file format is specific. Without --prefix, images must be in current directory."],["INSTALL","```apt: sudo apt install x11-apps```\n\n```dnf: sudo dnf install xcursorgen```\n\n```apk: sudo apk add xcursorgen```\n\n```zypper: sudo zypper install xcursorgen```\n\n```brew: brew install xcursorgen```\n\n```nix: nix profile install nixpkgs#xcursorgen```"]],"e":[],"x":"xcursorgen [path/to/config.cursor] [path/to/output_file]"},"xpdf":{"n":"xpdf","t":"Lightweight X11 PDF viewer","s":[["TAGLINE","Lightweight X11 PDF viewer"],["TLDR","**Open PDF file**\n\n```xpdf [document.pdf]```\n\n**Open at specific page**\n\n```xpdf [document.pdf] [5]```\n\n**Set initial zoom** (percent, \"page\", \"width\")\n\n```xpdf -z [150] [document.pdf]```\n\n**Full screen mode**\n\n```xpdf -fullscreen [document.pdf]```\n\n**Open with owner password**\n\n```xpdf -opw [owner_password] [document.pdf]```\n\n**Reverse video** (dark background) mode\n\n```xpdf -rv [document.pdf]```\n\n**Remote control mode**\n\n```xpdf -remote [name] [document.pdf]```"],["SYNOPSIS","**xpdf** [_-z zoom_] [_-fullscreen_] [_-opw password_] [_options_] _file.pdf_ [_page_]"],["PARAMETERS","**-z** _ZOOM_\n> Initial zoom (percent, page, width).\n\n**-fullscreen**\n> Full screen mode.\n\n**-opw** _PASSWORD_\n> Owner password.\n\n**-upw** _PASSWORD_\n> User password.\n\n**-remote** _NAME_\n> Start in remote mode.\n\n**-g** _GEOMETRY_\n> Window geometry.\n\n**-rv**\n> Reverse video mode.\n\n**-papercolor** _COLOR_\n> Paper background color.\n\n**-mattecolor** _COLOR_\n> Matte color around page area.\n\n**-cfg** _FILE_\n> Config file.\n\n**-v**\n> Show version.\n\n**-h**\n> Show help."],["KEY BINDINGS","**n**, **Space**, **PgDn**\n> Next page.\n\n**p**, **Backspace**, **PgUp**\n> Previous page.\n\n**+**, **-**\n> Zoom in/out.\n\n**z**\n> Zoom to page.\n\n**w**\n> Zoom to width.\n\n**/**\n> Search forward.\n\n**?**\n> Search backward.\n\n**q**\n> Quit."],["DESCRIPTION","**xpdf** is a lightweight PDF viewer for X11. It renders PDF documents with minimal resource usage.\n\nThe viewer provides basic PDF functionality: navigation, zoom, search, and printing. It handles most standard PDF features.\n\nRemote mode enables control from scripts. Commands sent to the named instance open files, navigate, and control display.\n\nConfiguration through ~/.xpdfrc customizes appearance and behavior. Font paths, colors, and key bindings are adjustable.\n\nThe xpdf package includes command-line tools: [pdftotext](/man/pdftotext)(1), [pdftops](/man/pdftops)(1), [pdfinfo](/man/pdfinfo)(1), and [pdfimages](/man/pdfimages)(1). These are widely used independent of the viewer."],["CAVEATS","Fewer features than Evince or Okular. Some modern PDF features may not render. X11 only. Development slower than alternatives."],["HISTORY","**xpdf** was created by **Derek Noonburg** starting in **1995**. It was one of the first open-source PDF viewers and spawned the Poppler library used by many modern viewers."],["INSTALL","```apt: sudo apt install xpdf```\n\n```dnf: sudo dnf install xpdf```\n\n```pacman: sudo pacman -S xpdf```\n\n```apk: sudo apk add xpdf```\n\n```brew: brew install xpdf```\n\n```nix: nix profile install nixpkgs#xpdf```"],["SEE ALSO","[evince](/man/evince)(1), [okular](/man/okular)(1), [mupdf](/man/mupdf)(1), [zathura](/man/zathura)(1)"]],"e":["evince","okular","mupdf","zathura"],"x":"xpdf [document.pdf]"},"zerofree":{"n":"zerofree","t":"zero free blocks on ext2/3/4 filesystems","s":[["TAGLINE","zero free blocks on ext2/3/4 filesystems"],["TLDR","Zero the free blocks of an **unmounted** filesystem\n\n```sudo zerofree /dev/[sda1]```\n\nShow **progress** while clearing\n\n```sudo zerofree -v /dev/[sda1]```\n\n**Dry run**: count non-zero free blocks without writing\n\n```sudo zerofree -n /dev/[sda1]```\n\nRemount the **root** filesystem read-only first, then clear\n\n```sudo mount -o remount,ro / && sudo zerofree /dev/[sda1]```"],["SYNOPSIS","**zerofree** [_OPTIONS_] _device_"],["PARAMETERS","**-n**\n> Dry run: report how many free blocks are non-zero without writing anything.\n\n**-v**\n> Verbose: print progress as a percentage while clearing.\n\n**-f** _fillval_\n> Fill free blocks with the byte value fillval (0-255) instead of zero."],["DESCRIPTION","**zerofree** scans an ext2, ext3, or ext4 filesystem and overwrites every unallocated block that is not already zero with zeros (or a chosen fill byte). Data in allocated files is left untouched.\n\nZeroing free space is mainly useful before imaging or compressing a disk. A virtual machine image or sparse file shrinks dramatically once its free blocks are zero, and the unused space can then be reclaimed by the host or by sparse-aware tools.\n\nThe target filesystem must be unmounted or mounted read-only, since zerofree reads and writes the block device directly and cannot run safely while the kernel is also modifying it."],["CAVEATS","The filesystem must not be mounted read-write. Run zerofree from a live/rescue environment, or remount the device read-only first (the root filesystem usually needs single-user or recovery mode). Only ext2/3/4 are supported. Zeroing is not a secure erase; it only makes free space compress well. Use **shred** or **blkdiscard** to destroy previously written data."],["HISTORY","**zerofree** was written by Ron Yorston to help shrink User-Mode Linux and virtual machine disk images. It is packaged as **zerofree** on most distributions."],["INSTALL","```apt: sudo apt install zerofree```\n\n```dnf: sudo dnf install zerofree```\n\n```apk: sudo apk add zerofree```\n\n```zypper: sudo zypper install zerofree```\n\n```nix: nix profile install nixpkgs#zerofree```"],["SEE ALSO","[e2fsck](/man/e2fsck)(8), [fstrim](/man/fstrim)(8), [blkdiscard](/man/blkdiscard)(8), [shred](/man/shred)(1)"]],"e":["e2fsck","fstrim","blkdiscard","shred"],"x":"sudo zerofree /dev/[sda1]"}});
