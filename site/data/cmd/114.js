window.LCL_BUCKET(114,{"aafire":{"n":"aafire","t":"ASCII art fire animation","s":[["TAGLINE","ASCII art fire animation"],["TLDR","**Display ASCII fire** animation in the terminal\n\n```aafire```\n\n**Run with curses driver** (force terminal output)\n\n```aafire -driver curses```\n\n**Run with slang driver** for terminal display\n\n```aafire -driver slang```\n\n**Display with specific dimensions**\n\n```aafire -width [80] -height [25]```"],["SYNOPSIS","**aafire** [_aalib-options_]"],["PARAMETERS","**-driver** _name_\n> Select output driver (curses, slang, X11, etc.)\n\n**-width** _n_\n> Set display width in characters\n\n**-height** _n_\n> Set display height in characters\n\n**-bold**\n> Enable bold font for brighter output\n\n**-dim**\n> Use dim characters for darker appearance\n\n**-reverse**\n> Use reverse video mode\n\n**-normal**\n> Use normal rendering mode\n\n**-extended**\n> Use extended ASCII character set\n\n**-eight**\n> Use eight-bit ASCII characters\n\n**-help**\n> Display available options and drivers"],["DESCRIPTION","**aafire** displays an animated fire simulation rendered entirely in ASCII art characters. It is a demonstration program for the aalib library, which converts graphical output into text-based representations.\n\nThe animation renders flames that flicker and dance in real-time using only standard text characters, creating a visually interesting effect directly in the terminal or in an X11 window. The program demonstrates aalib's ability to represent complex visual information using nothing but ASCII characters and their brightness levels.\n\nBy default, aalib may attempt to use a graphical driver. To ensure output stays in the terminal, use the **curses** or **slang** driver explicitly."],["CAVEATS","The default driver may attempt to open an X11 window rather than display in the terminal. Use **-driver curses** or **-driver slang** to force terminal output. Performance depends on terminal emulator capabilities and may vary across different systems."],["HISTORY","**aafire** is part of the aalib (ASCII Art Library) created by **Jan Hubicka** in the late **1990s**. The library was developed to enable ASCII art rendering for various applications and became popular for its creative approach to displaying graphics in text-only environments. It gained wider recognition through projects like MPlayer which used aalib to play video as ASCII art."],["INSTALL","```apt: sudo apt install libaa-bin```"],["SEE ALSO","[bb](/man/bb)(1), [cmatrix](/man/cmatrix)(1), [sl](/man/sl)(1)"],["RESOURCES","```[Source code](https://sourceforge.net/projects/aa-project/)```\n\n```[Homepage](https://aa-project.sourceforge.net/aalib/)```"]],"e":["bb","cmatrix","sl"],"x":"aafire"},"acpid":{"n":"acpid","t":"ACPI event daemon","s":[["TAGLINE","ACPI event daemon"],["TLDR","**Start** the ACPI daemon\n\n```sudo acpid```\n\nStart in **foreground** for debugging\n\n```sudo acpid -f```\n\nStart with custom **configuration directory**\n\n```sudo acpid -c [/etc/acpi/events]```\n\nStart with increased **debug** output\n\n```sudo acpid -d```"],["SYNOPSIS","**acpid** [_-c confdir_] [_-d_] [_-e eventfile_] [_-f_] [_-g group_] [_-l logfile_] [_options_]"],["DESCRIPTION","**acpid** (ACPI Event Daemon) monitors ACPI events from the kernel and executes corresponding shell scripts or programs. It handles hardware events such as power button presses, lid closing, AC adapter changes, and battery status changes.\n\nThe daemon reads event configuration files from /etc/acpi/events/ and runs the associated scripts when matching events occur. This enables automatic responses to hardware events like suspending when the laptop lid closes or shutting down when the power button is pressed."],["PARAMETERS","**-c** _dir_\n> Configuration directory for event files (default: /etc/acpi/events)\n\n**-d**\n> Enable debug mode with increased verbosity\n\n**-e** _file_\n> Event source file (default: /proc/acpi/event or /dev/input/event*)\n\n**-f**\n> Run in foreground (don't daemonize)\n\n**-g** _group_\n> Set socket group ownership\n\n**-l** _file_\n> Log file location (default: /var/log/acpid)\n\n**-p** _file_\n> PID file location\n\n**-s** _file_\n> Socket file for client connections"],["CONFIGURATION","**/etc/acpi/events/**\n> Directory containing event rule files. Each file defines an event pattern and an action (script) to execute when that event occurs.\n\n**/etc/acpi/handler.sh**\n> Default event handler script invoked by acpid when events match. Receives the event type and parameters as arguments."],["CAVEATS","Requires root privileges or appropriate permissions to access ACPI events. Event scripts must be executable. Modern systems using systemd may handle ACPI events through logind instead. Misconfigured scripts can interfere with power management."],["HISTORY","**acpid** was developed in the early **2000s** as Linux transitioned from APM (Advanced Power Management) to ACPI. Tim Hockin was an early maintainer. The daemon became a standard component for laptop power management on Linux systems."],["INSTALL","```apt: sudo apt install acpid```\n\n```dnf: sudo dnf install acpid```\n\n```pacman: sudo pacman -S acpid```\n\n```apk: sudo apk add acpid```\n\n```zypper: sudo zypper install acpid```\n\n```nix: nix profile install nixpkgs#acpid```"],["SEE ALSO","[acpi](/man/acpi)(1), [acpi_listen](/man/acpi_listen)(8), [systemctl](/man/systemctl)(1), [loginctl](/man/loginctl)(1)"],["RESOURCES","```[Source code](https://sourceforge.net/projects/acpid2/)```"]],"e":["acpi","acpi_listen","systemctl","loginctl"],"x":"sudo acpid"},"atac":{"n":"atac","t":"Postman-like API client for the terminal","s":[["TAGLINE","Postman-like API client for the terminal"],["TLDR","**Launch the TUI** in the current directory\n\n```atac```\n\n**Specify a custom application directory**\n\n```atac -d [path/to/directory]```\n\n**Send a one-shot GET request**\n\n```atac try [https://api.example.com/endpoint]```\n\n**Send a request from a collection**\n\n```atac request [collection/request_name] send```\n\n**Send a request using an environment**\n\n```atac request [collection/request_name] send --env [my_env]```\n\n**Import a Postman collection**\n\n```atac import postman [path/to/collection.json]```\n\n**Import from a cURL command**\n\n```atac import curl \"[curl -X GET https://example.com]\"```\n\n**Generate shell completions**\n\n```atac completions [bash|zsh|fish]```"],["SYNOPSIS","**atac** [**-d** _directory_] [**--filter** _regex_] [**--dry-run**] [**--no-ansi-log**] [**-v**...] [_subcommand_]"],["PARAMETERS","**-d** _directory_, **--directory** _directory_\n> Main application directory containing collection files, the atac.toml config file, and the atac.log file\n\n**--filter** _regex_\n> Only parse collection files whose names match the given regular expression\n\n**--tui**\n> Run the TUI after executing a CLI subcommand\n\n**--dry-run**\n> Avoid saving data to collection and environment files\n\n**--no-ansi-log**\n> Avoid using ANSI formatting in log file output\n\n**-v**, **--verbose**\n> Increase verbosity level; can be repeated for more detail\n\n**collection**\n> Manage collections (list, create, delete, rename)\n\n**request**\n> Manage and send requests within a collection\n\n**try**\n> One-shot request sender for quick API calls without a saved collection\n\n**env**\n> Manage environment files and variables\n\n**import**\n> Import collections, requests, or environments from Postman v2.1.0, cURL, or OpenAPI formats\n\n**completions**\n> Generate shell completion scripts for bash, zsh, or fish\n\n**man**\n> Generate the ATAC man page"],["DESCRIPTION","**atac** (Arguably a Terminal API Client) is a feature-rich, terminal-based API testing tool written in Rust. It provides a Postman-like experience entirely within the terminal, supporting both an interactive TUI mode and a scriptable CLI mode.\n\nThe TUI presents a full interface for building and organizing HTTP requests, viewing responses with syntax highlighting, managing collections, and switching between environments. The CLI mode allows sending requests, managing collections, and importing data non-interactively, making it suitable for scripting and automation.\n\natac supports all standard HTTP methods including GET, POST, PUT, PATCH, DELETE, OPTIONS, HEAD, TRACE, and CONNECT. Authentication options include Basic, Bearer token, Digest, and JWT. Request bodies can be JSON, XML, HTML, plain text, multipart form, URL-encoded form, or file uploads. Full response details are available including status codes, headers, cookies, and timing.\n\nBeyond HTTP, atac includes a WebSocket client supporting text, binary, ping, pong, and close messages. A built-in JavaScript runtime enables pre-request and post-request scripting for dynamic request modification and response processing.\n\nCollections and environments are stored as human-readable JSON or YAML files, designed to be committed to version control. Environment variables are defined in .env files and referenced in requests using **{{variable_name}}** syntax. The tool can import from Postman v2.1.0 collections, OpenAPI specifications, and cURL commands, and can export requests to cURL, PHP Guzzle, Node.js Axios, Rust Reqwest, and raw HTTP formats."],["CONFIGURATION","**atac.toml** -- Main configuration file placed in the application directory. All keys are optional.\n\n**disable_syntax_highlighting**\n> Set to true to disable response syntax highlighting (default: false)\n\n**disable_cors**\n> Set to true to disable CORS handling (default: false)\n\n**[proxy] http_proxy**\n> HTTP proxy URL\n\n**[proxy] https_proxy**\n> HTTPS proxy URL\n\nKey bindings are configured via a separate TOML file specified by the **ATAC_KEY_BINDINGS** environment variable. Default and Vim-style key binding templates are provided.\n\nThe **ATAC_MAIN_DIR** environment variable can be used as an alternative to the **-d** flag for specifying the application directory."],["CAVEATS","Requires Rust 1.82 or later to build from source. On macOS, binaries downloaded from GitHub releases may need the quarantine attribute removed with **xattr -rd com.apple.quarantine**. OAuth and AWS authentication are not yet supported. Only Postman v2.1.0 collection format is supported for import."],["HISTORY","**atac** was created by **Julien Caposiena** (Julien-cpsn) and first published to crates.io in **April 2024**. The project is open source under the MIT license and hosted on GitHub. It was built to provide a free, account-less, offline-first alternative to graphical API clients like Postman and Insomnia."],["INSTALL","```apt: sudo apt install atac```\n\n```pacman: sudo pacman -S atac```\n\n```brew: brew install atac```\n\n```nix: nix profile install nixpkgs#atac```"],["SEE ALSO","[curl](/man/curl)(1), [httpie](/man/httpie)(1), [wget](/man/wget)(1), [websocat](/man/websocat)(1), [posting](/man/posting)(1)"]],"e":["curl","httpie","wget","websocat","posting"],"x":"atac"},"brew-leaves":{"n":"brew-leaves","t":"List top-level installed packages that are not dependencies","s":[["TAGLINE","List top-level installed packages that are not dependencies"],["TLDR","**List top-level packages (not dependencies)**\n\n```brew leaves```\n\n**List only manually installed leaves**\n\n```brew leaves -r```\n\n**List only dependency-installed leaves**\n\n```brew leaves -p```\n\n**Show dependency tree of leaves**\n\n```brew deps --tree $(brew leaves)```"],["SYNOPSIS","**brew leaves** [_options_]"],["DESCRIPTION","**brew leaves** lists installed formulae that are not dependencies of any other installed formula or cask. These are your \"top-level\" packages that you explicitly installed.\n\nThis command shows the leaves of Homebrew's dependency graph - packages that can be safely uninstalled without breaking other packages."],["PARAMETERS","**-r**, **--installed-on-request**\n> Only list leaves that were manually installed by the user\n\n**-p**, **--installed-as-dependency**\n> Only list leaves that were installed as dependencies of another formula"],["CAVEATS","Not the same as \"manually installed\" - shows packages with no dependents. A manually installed package might have dependents and won't appear. Use -r flag for truly manual installations."],["SEE ALSO","[brew](/man/brew)(1), [brew-list](/man/brew-list)(1), [brew-autoremove](/man/brew-autoremove)(1)"]],"e":["brew","brew-list","brew-autoremove"],"x":"brew leaves"},"bzcat":{"n":"bzcat","t":"Decompress bzip2 files to standard output","s":[["TAGLINE","Decompress bzip2 files to standard output"],["TLDR","**View** compressed file\n\n```bzcat [file.txt.bz2]```\n\n**View** multiple files\n\n```bzcat [file1.bz2] [file2.bz2]```\n\n**Pipe** to less\n\n```bzcat [file.txt.bz2] | less```"],["SYNOPSIS","**bzcat** [_file_...]"],["DESCRIPTION","**bzcat** decompresses bzip2 files to standard output without modifying the original compressed file. It's equivalent to `bzip2 -dc` and useful for viewing or piping compressed content.\n\nThe tool allows working with compressed files without extracting them to disk."],["WORKFLOW","```bash"],["View compressed file","bzcat log.txt.bz2"],["View with pager","bzcat log.txt.bz2 | less"],["Search in compressed file","bzcat log.txt.bz2 | grep \"error\""],["Concatenate compressed files","bzcat file1.bz2 file2.bz2 > combined.txt"],["Count lines in compressed file","bzcat data.txt.bz2 | wc -l\n```"],["FEATURES","- Preserves original compressed file\n- Streams output (low memory)\n- Supports multiple files\n- Pipeline-friendly"],["CAVEATS","Output is always to stdout. No options for compression level. For viewing only, consider bzless. Cannot handle corrupted files."],["HISTORY","**bzcat** has been part of the bzip2 suite since its creation in **1996**."],["INSTALL","```apt: sudo apt install bzip2```\n\n```dnf: sudo dnf install bzip2```\n\n```pacman: sudo pacman -S bzip2```\n\n```apk: sudo apk add bzip2```\n\n```zypper: sudo zypper install bzip2```\n\n```brew: brew install bzip2```\n\n```nix: nix profile install nixpkgs#bzip2```"],["SEE ALSO","[bzip2](/man/bzip2)(1), [bzless](/man/bzless)(1), [bzgrep](/man/bzgrep)(1), [zcat](/man/zcat)(1)"],["RESOURCES","```[Source code](https://gitlab.com/bzip2/bzip2)```\n\n```[Homepage](https://sourceware.org/bzip2/)```"]],"e":["bzip2","bzless","bzgrep","zcat"],"x":"bzcat [file.txt.bz2]"},"bzegrep":{"n":"bzegrep","t":"Search bzip2 files using extended regular expressions","s":[["TAGLINE","Search bzip2 files using extended regular expressions"],["TLDR","**Search for an extended regex pattern in a bzip2 compressed file**\n\n```bzegrep \"[pattern]\" [path/to/file.bz2]```\n\n**Search case-insensitively**\n\n```bzegrep -i \"[pattern]\" [path/to/file.bz2]```\n\n**Print line numbers with matches**\n\n```bzegrep -n \"[pattern]\" [path/to/file.bz2]```\n\n**Search multiple compressed files**\n\n```bzegrep \"[pattern]\" [file1.bz2] [file2.bz2]```\n\n**Count matching lines**\n\n```bzegrep -c \"[pattern]\" [path/to/file.bz2]```\n\n**Search from stdin**\n\n```bzcat [path/to/file.bz2] | bzegrep \"[pattern]\"```"],["SYNOPSIS","**bzegrep** [_egrep_options_] [**-e**] _pattern_ [_filename..._]"],["DESCRIPTION","**bzegrep** searches possibly bzip2-compressed files for lines matching an extended regular expression pattern. It transparently decompresses **.bz2** files before searching and passes all options directly to **egrep**.\n\nIf no file is specified, bzegrep reads from standard input, decompressing if necessary. This allows it to be used in pipelines with other bzip2 utilities.\n\nThe command is equivalent to running **bzgrep** but using **egrep** (extended grep) for pattern matching, supporting extended regular expression syntax including **+**, **?**, **|**, and parentheses for grouping."],["PARAMETERS","All **egrep** options are supported, including:\n\n**-i**\n> Case-insensitive matching\n\n**-n**\n> Print line numbers\n\n**-c**\n> Print only a count of matching lines\n\n**-v**\n> Invert match (show non-matching lines)\n\n**-l**\n> Print only names of files containing matches\n\n**-h**\n> Suppress the filename prefix on output (when searching multiple files)\n\n**-e** _pattern_\n> Specify the pattern (useful if pattern starts with -)"],["CAVEATS","The **GREP** environment variable can be set to specify an alternate grep program. The file must be bzip2-compressed for transparent decompression; uncompressed files are searched directly."],["INSTALL","```apt: sudo apt install bzip2```\n\n```dnf: sudo dnf install bzip2```\n\n```pacman: sudo pacman -S bzip2```\n\n```apk: sudo apk add bzip2```\n\n```zypper: sudo zypper install bzip2```\n\n```brew: brew install bzip2```\n\n```nix: nix profile install nixpkgs#bzip2```"],["SEE ALSO","[bzgrep](/man/bzgrep)(1), [bzfgrep](/man/bzfgrep)(1), [egrep](/man/egrep)(1), [grep](/man/grep)(1), [bzip2](/man/bzip2)(1), [bzcat](/man/bzcat)(1), [zegrep](/man/zegrep)(1)"]],"e":["bzgrep","bzfgrep","egrep","grep","bzip2","bzcat","zegrep"],"x":"bzegrep \"[pattern]\" [path/to/file.bz2]"},"cancel":{"n":"cancel","t":"cancel CUPS print jobs","s":[["TAGLINE","cancel CUPS print jobs"],["TLDR","**Cancel the current print job on the default printer**\n\n```cancel```\n\n**Cancel a specific print job by ID**\n\n```cancel [job_id]```\n\n**Cancel a print job on a specific printer**\n\n```cancel [printer_name]-[job_id]```\n\n**Cancel all print jobs on a specific printer**\n\n```cancel -a [printer_name]```\n\n**Cancel all print jobs on all printers**\n\n```cancel -a```\n\n**Cancel all jobs owned by a specific user**\n\n```cancel -u [username]```"],["SYNOPSIS","**cancel** [**-E**] [**-U** _username_] [**-a**] [**-h** _hostname_[**:**_port_]] [**-u** _username_] [**-x**] [_id_] [_destination_] [_destination-id_]"],["DESCRIPTION","**cancel** is a CUPS command that cancels print jobs. Without arguments, it cancels the currently printing job on the default destination. Jobs can be specified by job ID, destination printer, or both.\n\nJob IDs are assigned by the print system and can be viewed with **lpstat**. The destination-id format combines the printer name and job ID (e.g., \"myprinter-42\")."],["PARAMETERS","**-a**\n> Cancel all jobs on the specified destination, or all destinations if none specified\n\n**-E**\n> Force encryption when connecting to the server\n\n**-h** _hostname_[**:**_port_]\n> Specify alternate server\n\n**-u** _username_\n> Cancel jobs owned by the specified user\n\n**-U** _username_\n> Specify the username for authentication\n\n**-x**\n> Delete job data files in addition to canceling"],["CONFIGURATION","**/etc/cups/cupsd.conf**\n> CUPS server configuration including operation policies that control Cancel-Jobs and Cancel-My-Jobs permissions.\n\n**/etc/cups/printers.conf**\n> Printer definitions and queue settings."],["CAVEATS","The **-a** and **-u** options require administrative privileges. These restrictions are controlled by operation policies in **cupsd.conf** for \"Cancel-Jobs\" and \"Cancel-My-Jobs\" operations.\n\nCUPS printer names can contain any printable character except SPACE, TAB, \"/\", or \"#\", and are case-insensitive."],["INSTALL","```apt: sudo apt install lprng```\n\n```dnf: sudo dnf install cups-client```\n\n```apk: sudo apk add cups-client```\n\n```zypper: sudo zypper install cups-client```"],["SEE ALSO","[lp](/man/lp)(1), [lpq](/man/lpq)(1), [lpr](/man/lpr)(1), [lprm](/man/lprm)(1), [lpstat](/man/lpstat)(1)"]],"e":["lp","lpq","lpr","lprm","lpstat"],"x":"cancel"},"charmap":{"n":"charmap","t":"Unicode character map browser","s":[["TAGLINE","Unicode character map browser"],["TLDR","**Open character map**\n\n```charmap```\n\n**Search for character** by name\n\n```charmap --search \"[heart]\"```\n\n**Copy character to clipboard**\n\n```charmap --copy \"[character]\"```"],["SYNOPSIS","**charmap** [_options_]"],["DESCRIPTION","**charmap** is a Unicode character map application, typically part of GNOME desktop. It allows browsing and searching through all Unicode characters, viewing character details, and copying characters to the clipboard.\n\nThe application displays characters organized by Unicode block, with options to filter by script, language, or search term. Each character shows its code point, official name, and related information."],["PARAMETERS","**--search** _text_\n> Search for characters by name.\n\n**--copy** _char_\n> Copy character to clipboard.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["CAVEATS","Requires a graphical environment. Character availability depends on installed fonts. Some characters may display as boxes if fonts don't include them."],["HISTORY","**charmap** is part of the **GNOME** desktop environment. Character map utilities have existed since early GUI systems, helping users access characters not on their keyboard. The GNOME version, known as **gucharmap**, was created in the early 2000s."],["INSTALL","```apt: sudo apt install gucharmap```\n\n```dnf: sudo dnf install gucharmap```\n\n```pacman: sudo pacman -S gucharmap```\n\n```zypper: sudo zypper install gucharmap```\n\n```brew: brew install gucharmap```\n\n```nix: nix profile install nixpkgs#gucharmap```"],["SEE ALSO","[xdotool](/man/xdotool)(1), [xclip](/man/xclip)(1)"]],"e":["xdotool","xclip"],"x":"charmap"},"check_mk":{"n":"check_mk","t":"IT infrastructure monitoring tool","s":[["TAGLINE","IT infrastructure monitoring tool"],["TLDR","**Check a host manually**\n\n```cmk -nv [hostname]```\n\n**Discover services on a host**\n\n```cmk -I [hostname]```\n\n**Discover services on all hosts**\n\n```cmk -I```\n\n**Update configuration and reload**\n\n```cmk -O```\n\n**Show agent output from a host**\n\n```cmk -d [hostname]```\n\n**List all hosts**\n\n```cmk --list-hosts```\n\n**Activate changes**\n\n```cmk -R```"],["SYNOPSIS","**cmk** [_options_] [_hostname_]"],["PARAMETERS","**-I**, **--discover**\n> Discover new services on hosts.\n\n**-II**\n> Remove vanished services and discover new ones.\n\n**-n**\n> Simulate check without submitting results (dry run).\n\n**-v**\n> Verbose output. Commonly combined as **-nv** for testing.\n\n**-d**\n> Dump agent output from host.\n\n**-O**, **--reload**\n> Reload configuration without restart.\n\n**-R**, **--restart**\n> Restart monitoring core.\n\n**--list-hosts**\n> List all configured hosts.\n\n**--list-checks**\n> List all available check plugins.\n\n**--paths**\n> Show Checkmk directory paths.\n\n**-P**, **--package** _action_\n> Manage extension packages (install, remove, list).\n\n**--snmpwalk** _HOSTNAME_\n> Perform an SNMP walk on a host for diagnostics.\n\n**--debug**\n> Enable debug output."],["DESCRIPTION","**Checkmk** (formerly Check_MK) is a comprehensive IT monitoring solution built on Nagios monitoring core concepts. The **cmk** command is the central CLI tool for managing hosts, services, and configuration.\n\nThe system uses agents installed on monitored hosts that collect metrics and status information. The **cmk** command processes this data, discovers available services, performs checks, and manages the configuration.\n\nDiscovery (**cmk -I**) scans hosts for monitorable services. Configuration changes require activation with **cmk -O** or **cmk -R**. The tool supports distributed monitoring, agent bakery for deployment, and extensive plugin architecture."],["CONFIGURATION","**/omd/sites/&lt;site&gt;/etc/check_mk/main.mk**\n> Main configuration file for host definitions, check parameters, and rules.\n\n**/omd/sites/&lt;site&gt;/etc/check_mk/conf.d/**\n> Directory for additional configuration files, often managed by the web interface."],["CAVEATS","Changes to configuration must be activated before taking effect. The commercial editions (CEE, CME) have additional features not in Raw Edition. Large-scale discoveries can be resource-intensive. Agent-based monitoring requires agents deployed on target hosts."],["HISTORY","Check_MK was created by **Mathias Kettner** in **2008** as an extension to Nagios to simplify configuration and improve performance. It evolved from a plugin to a complete monitoring solution. In **2019**, the project rebranded to **Checkmk** and tribe29 GmbH (now Checkmk GmbH) was founded. The Raw Edition remains open source while commercial editions add enterprise features."],["SEE ALSO","[nagios](/man/nagios)(8), [icinga2](/man/icinga2)(8), [prometheus](/man/prometheus)(1)"]],"e":["nagios","icinga2","prometheus"],"x":"cmk -nv [hostname]"},"clusterctl":{"n":"clusterctl","t":"Kubernetes Cluster API management CLI","s":[["TAGLINE","Kubernetes Cluster API management CLI"],["TLDR","**Initialize management cluster**\n\n```clusterctl init --infrastructure [aws]```\n\n**Initialize with specific version**\n\n```clusterctl init --infrastructure [aws:v2.0.0]```\n\n**Generate cluster template**\n\n```clusterctl generate cluster [mycluster] --kubernetes-version [v1.28.0]```\n\n**Get workload cluster kubeconfig**\n\n```clusterctl get kubeconfig [mycluster]```\n\n**Upgrade providers**\n\n```clusterctl upgrade apply --contract [v1beta1]```\n\n**Move resources between clusters**\n\n```clusterctl move --to-kubeconfig [target.kubeconfig]```\n\n**Delete provider**\n\n```clusterctl delete --infrastructure [aws]```"],["SYNOPSIS","**clusterctl** _command_ [_options_]"],["DESCRIPTION","**clusterctl** is the command-line tool for managing Kubernetes Cluster API (CAPI) management clusters. It handles the full lifecycle of cluster infrastructure providers, from initial installation through upgrades and deletion.\n\nThe tool can initialize a Kubernetes cluster as a CAPI management cluster by installing infrastructure providers (AWS, Azure, vSphere, etc.), bootstrap providers, and control plane providers. Once initialized, it generates cluster templates for creating workload clusters and retrieves their kubeconfigs.\n\nclusterctl also supports moving Cluster API resources between management clusters, which is essential for upgrade scenarios and disaster recovery. The upgrade commands help keep providers in sync with the latest releases and API contract versions."],["COMMANDS","**init**\n> Install Cluster API components\n\n**generate cluster**\n> Generate cluster template\n\n**generate yaml**\n> Process YAML templates\n\n**get kubeconfig**\n> Get workload cluster kubeconfig\n\n**upgrade plan**\n> Show upgrade recommendations\n\n**upgrade apply**\n> Apply provider upgrades\n\n**move**\n> Move resources between clusters\n\n**delete**\n> Delete providers\n\n**describe cluster**\n> Describe cluster resources\n\n**config**\n> Display clusterctl configuration details.\n\n**version**\n> Print clusterctl version information.\n\n**completion**\n> Output shell completion code (bash, zsh, fish, powershell).\n\n**alpha rollout**\n> Manage the rollout of Cluster API resources."],["INIT OPTIONS","**--infrastructure** _provider_\n> Infrastructure provider (aws, azure, vsphere, etc.)\n\n**--bootstrap** _provider_\n> Bootstrap provider\n\n**--control-plane** _provider_\n> Control plane provider\n\n**--target-namespace** _ns_\n> Target namespace for providers\n\n**--core** _provider_\n> Core provider version"],["CONFIGURATION","**~/.cluster-api/clusterctl.yaml**\n> Main configuration file for providers, variables, and image overrides."],["CAVEATS","Set GITHUB_TOKEN to avoid API rate limiting. Requires kubectl configured for target cluster. Uses go proxy for version detection."],["INSTALL","```zypper: sudo zypper install clusterctl```\n\n```brew: brew install clusterctl```\n\n```nix: nix profile install nixpkgs#clusterctl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kind](/man/kind)(1), [kubeadm](/man/kubeadm)(1)"]],"e":["kubectl","kind","kubeadm"],"x":"clusterctl init --infrastructure [aws]"},"cmake":{"n":"cmake","t":"cross-platform build system generator","s":[["TAGLINE","cross-platform build system generator"],["TLDR","**Generate build files in a build directory**\n\n```cmake -B [build]```\n\n**Build the project**\n\n```cmake --build [build]```\n\n**Build with a specific build type**\n\n```cmake -B [build] -DCMAKE_BUILD_TYPE=[Release]```\n\n**Build with Ninja generator**\n\n```cmake -B [build] -G Ninja```\n\n**Build in parallel**\n\n```cmake --build [build] -j [8]```\n\n**Install the project**\n\n```cmake --install [build]```"],["SYNOPSIS","**cmake** [_options_] _source-dir_"],["DESCRIPTION","**cmake** is a cross-platform build system generator. It reads platform-independent CMakeLists.txt project descriptions and generates native build files for the user's chosen build tool, including Unix Makefiles, Ninja, Visual Studio solutions, and Xcode projects.\n\nThe typical workflow involves a configure step that generates build files in an out-of-source build directory, followed by a build step that invokes the native build tool. CMake handles dependency detection, compiler feature checks, and library discovery through its find-module system.\n\nCMake is the dominant build system for C and C++ projects, and also supports Fortran, CUDA, and other languages. It includes CTest for test automation and CPack for creating distributable packages."],["PARAMETERS","**-B** _dir_\n> Build directory\n\n**-S** _dir_\n> Source directory (default: current)\n\n**-G** _generator_\n> Build system generator (Unix Makefiles, Ninja, Xcode)\n\n**-D** _var_**=**_value_\n> Set CMake variable\n\n**--build** _dir_\n> Build project\n\n**--install** _dir_\n> Install project\n\n**--preset** _name_\n> Use configure preset\n\n**-j** _N_\n> Parallel build jobs (passed to the underlying build tool).\n\n**--target** _target_\n> Build a specific target instead of the default.\n\n**--verbose**\n> Enable verbose build output.\n\n**--clean-first**\n> Clean before building.\n\n**-DCMAKE_INSTALL_PREFIX=**_path_\n> Set installation prefix directory.\n\n**-DCMAKE_CXX_COMPILER=**_compiler_\n> Specify the C++ compiler.\n\n**-DCMAKE_BUILD_TYPE=**_type_\n> Build type: Debug, Release, RelWithDebInfo, or MinSizeRel."],["CAVEATS","Complex syntax for advanced features. Learning curve steep. Generated files can be large. Cache issues require cleaning. Version differences cause compatibility issues. Some find it overcomplicated for simple projects."],["HISTORY","**CMake** was created by Bill Hoffman and Ken Martin at Kitware in **2000** to address the complexity of cross-platform builds, becoming widely adopted by **2010**."],["INSTALL","```apt: sudo apt install cmake```\n\n```dnf: sudo dnf install cmake```\n\n```pacman: sudo pacman -S cmake```\n\n```apk: sudo apk add cmake```\n\n```zypper: sudo zypper install cmake```\n\n```brew: brew install cmake```\n\n```nix: nix profile install nixpkgs#cmake```"],["SEE ALSO","[make](/man/make)(1), [ninja](/man/ninja)(1), [meson](/man/meson)(1)"]],"e":["make","ninja","meson"],"x":"cmake -B [build]"},"cntlm":{"n":"cntlm","t":"NTLM/NTLMv2 authenticating HTTP proxy","s":[["TAGLINE","NTLM/NTLMv2 authenticating HTTP proxy"],["TLDR","**Start with config file**\n\n```cntlm -c [/etc/cntlm.conf]```\n\n**Run in foreground debug mode**\n\n```cntlm -v -f```\n\n**Detect NTLM settings**\n\n```cntlm -M http://test.com```\n\n**Generate password hash**\n\n```cntlm -H -d [DOMAIN] -u [username]```\n\n**Start SOCKS5 proxy**\n\n```cntlm -O [1080]```\n\n**Specify proxy and credentials**\n\n```cntlm -u [user] -d [DOMAIN] -p [pass] [proxy:port]```"],["SYNOPSIS","**cntlm** [_options_] [_proxy_host_:]_proxy_port_..."],["DESCRIPTION","**cntlm** is an NTLM/NTLMv2 authenticating HTTP proxy that sits between applications and a corporate proxy server. It handles the NTLM authentication handshake transparently, so applications only need to use a simple unauthenticated proxy connection.\n\nThe proxy caches authenticated connections for reuse, providing significant speed improvements over alternatives that re-authenticate on every request. It supports NTLM, NTLMv2, NTLM2SR, and basic authentication methods, with automatic detection of the strongest supported method via the **-M** flag.\n\nIn addition to HTTP proxying, cntlm provides SOCKS5 proxy support and TCP/IP tunneling through the corporate proxy, enabling protocols beyond HTTP to traverse the proxy infrastructure. Password hashes can be stored instead of plaintext passwords for improved security."],["PARAMETERS","**-u** _user_\n> Proxy username\n\n**-d** _domain_\n> Proxy domain\n\n**-p** _password_\n> Proxy password\n\n**-a** _auth_\n> Auth type: NTLMv2, NTLM2SR, NT, NTLM, LM\n\n**-M** _url_\n> Magic NTLM detection mode\n\n**-H**\n> Generate password hashes\n\n**-c** _file_\n> Configuration file\n\n**-l** _[addr:]port_\n> Listen on port\n\n**-O** _[addr:]port_\n> Enable SOCKS5 proxy\n\n**-N** _pattern_\n> No-proxy pattern list\n\n**-f**\n> Run in foreground\n\n**-v**\n> Verbose/debug mode\n\n**-g**\n> Allow gateway mode (non-local connections)\n\n**-P** _pidfile_\n> Create PID file"],["CONFIGURATION","**/etc/cntlm.conf**\n> Main configuration file for proxy address, credentials, listen port, and authentication settings."],["AUTH TYPES","**NTLMv2**: Strongest, recommended\n\n**NTLM2SR**: Strong\n\n**NTLM**: Legacy compatibility\n\n**LM**: Weakest, old servers only"],["CAVEATS","Use -M to auto-detect strongest working auth. Store password hashes instead of plaintext. Config file same syntax as OpenSSH."],["INSTALL","```apt: sudo apt install cntlm```\n\n```dnf: sudo dnf install cntlm```\n\n```apk: sudo apk add cntlm```\n\n```zypper: sudo zypper install cntlm```\n\n```brew: brew install cntlm```\n\n```nix: nix profile install nixpkgs#cntlm```"],["SEE ALSO","[squid](/man/squid)(8), [privoxy](/man/privoxy)(1), [tinyproxy](/man/tinyproxy)(8)"],["RESOURCES","```[Homepage](https://cntlm.sourceforge.net/)```"]],"e":["squid","privoxy","tinyproxy"],"x":"cntlm -c [/etc/cntlm.conf]"},"crunch":{"n":"crunch","t":"wordlist generator for password testing","s":[["TAGLINE","wordlist generator for password testing"],["TLDR","**Generate all combinations** of specified length using default charset\n\n```crunch [4] [6]```\n\n**Generate wordlist with custom** character set\n\n```crunch [4] [4] [abc123]```\n\n**Output wordlist to file**\n\n```crunch [6] [8] -o [wordlist.txt]```\n\n**Generate with pattern** (@ = lowercase, , = uppercase, % = number, ^ = symbol)\n\n```crunch [8] [8] -t [@@@@%%%%]```\n\n**Limit output file size** (e.g., 10MB per file)\n\n```crunch [6] [6] -b [10mb] -o [START]```\n\n**Generate using words** from a file\n\n```crunch [4] [4] -p [word1] [word2] [word3]```"],["SYNOPSIS","**crunch** _min-len_ _max-len_ [_charset_] [_options_]"],["PARAMETERS","_MIN-LEN_\n> Minimum length of generated strings.\n\n_MAX-LEN_\n> Maximum length of generated strings.\n\n_CHARSET_\n> Characters to use for generation (default: lowercase alphabet).\n\n**-o** _FILE_\n> Output to file instead of stdout.\n\n**-t** _PATTERN_\n> Specify pattern using placeholders (@ , % ^).\n\n**-b** _SIZE_\n> Split output into files of specified size.\n\n**-c** _COUNT_\n> Number of lines per output file.\n\n**-p** _WORDS_\n> Generate permutations of specified words.\n\n**-d** _NUM_\n> Limit consecutive duplicate characters.\n\n**-e** _STRING_\n> Stop generating words at the specified string.\n\n**-f** _FILE_ _CHARSET_\n> Read character set from a file (e.g., charset.lst).\n\n**-i**\n> Invert output so the first character changes most often.\n\n**-l** _CHARS_\n> Treat characters as literals in the -t pattern (overrides @,%^).\n\n**-s** _STRING_\n> Start at specified string (for resuming).\n\n**-u**\n> Disable the print-percentage thread.\n\n**-z** _COMP_\n> Compress output (gzip, bzip2, lzma, 7z)."],["DESCRIPTION","**crunch** is a wordlist generator that creates lists of all possible combinations based on specified criteria. It's commonly used in authorized penetration testing for creating password lists, brute-force dictionaries, and testing security measures.\n\nThe tool generates combinations efficiently, supporting custom character sets, patterns, and size limits. Patterns allow mixing character types (letters, numbers, symbols) in specific positions. The permutation mode creates all arrangements of given words.\n\nOutput can be piped to other tools or split into manageable file sizes for large wordlists. Compression options help manage disk space for extensive generations."],["CAVEATS","Large wordlists can consume significant disk space and generation time. Use responsibly and only for authorized security testing. Ensure adequate storage before generating large lists. Consider the exponential growth of combinations with length."],["HISTORY","crunch was developed for the security community as a flexible wordlist generator. It became a standard tool in penetration testing distributions like Kali Linux. The tool enables security professionals to test password policies and authentication systems."],["INSTALL","```apt: sudo apt install crunch```\n\n```brew: brew install crunch```\n\n```nix: nix profile install nixpkgs#crunch```"],["SEE ALSO","[john](/man/john)(1), [hashcat](/man/hashcat)(1), [hydra](/man/hydra)(1)"]],"e":["john","hashcat","hydra"],"x":"crunch [4] [6]"},"docker-buildx-du":{"n":"docker-buildx-du","t":"Show disk usage of Docker build cache","s":[["TAGLINE","Show disk usage of Docker build cache"],["TLDR","**Show build cache** disk usage\n\n```docker buildx du```\n\n**Show detailed** disk usage with all metadata\n\n```docker buildx du --verbose```\n\n**Show disk usage** for a specific builder\n\n```docker buildx du --builder [builder_name]```\n\n**Filter** cache records by age\n\n```docker buildx du --filter until=[24h]```\n\n**Filter** cache records by type\n\n```docker buildx du --filter type=[regular]```"],["SYNOPSIS","**docker buildx du** [_options_]"],["PARAMETERS","**--filter** _key=value_\n> Filter output using key-value selectors (e.g., `until=24h`, `type=...`).\n\n**--verbose**\n> Show detailed output with additional metadata.\n\n**--builder** _name_\n> Target a specific builder instance (overrides the default)."],["DESCRIPTION","**docker buildx du** displays disk usage information for the build cache of the currently selected (or specified) builder instance. The output lists cache records with their IDs, whether they are reclaimable, their size, and when they were last accessed.\n\nThis command is useful for understanding how much disk space the build cache consumes before deciding to prune it with **docker buildx prune**."],["CAVEATS","Asterisks in the output indicate mutable records (size may change) or shared storage that overlaps with other resources. The reported sizes may not reflect actual reclaimable space when records are shared between builds."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-buildx-prune](/man/docker-buildx-prune)(1), [docker-buildx-ls](/man/docker-buildx-ls)(1), [docker-build](/man/docker-build)(1), [docker](/man/docker)(1)"]],"e":["docker-buildx-prune","docker-buildx-ls","docker-build","docker"],"x":"docker buildx du"},"dolt-config":{"n":"dolt-config","t":"manage repository and global settings","s":[["TAGLINE","manage repository and global settings"],["TLDR","**Set global user** name\n\n```dolt config --global --add user.name \"[Your Name]\"```\n\n**Set global email**\n\n```dolt config --global --add user.email \"[email@example.com]\"```\n\n**List all config**\n\n```dolt config --list```\n\n**Get specific value**\n\n```dolt config --get user.name```\n\n**Set local config**\n\n```dolt config --local --set [key] [value]```\n\n**Unset config value**\n\n```dolt config --global --unset [key]```"],["SYNOPSIS","**dolt config** [**--global**|**--local**] **--list**\n\n**dolt config** [**--global**|**--local**] **--add**|**--set** _name_ _value_\n\n**dolt config** [**--global**|**--local**] **--get** _name_\n\n**dolt config** [**--global**|**--local**] **--unset** _name_..."],["PARAMETERS","**--global**\n> Operate on the global config, shared by every repository for this user.\n\n**--local**\n> Operate on the repository-local config.\n\n**--set** _name_ _value_\n> Set the value of one or more config parameters.\n\n**--add** _name_ _value_\n> Set the value of one or more config parameters (a synonym for **--set**).\n\n**--get** _name_\n> Print the value of one or more config parameters.\n\n**--unset** _name_...\n> Remove one or more config parameters.\n\n**--list**\n> List the values of all config parameters."],["DESCRIPTION","**dolt config** manages Dolt configuration settings at global and repository-local levels. Essential settings include user identity (name and email) used in commit authorship.\n\nGlobal configuration applies to all repositories for the user, while local configuration is repository-specific. Local settings override global ones when both exist.\n\nConfiguration mirrors Git's config system, including its precedence rules, but the values are stored as JSON rather than INI. Beyond `user.name` and `user.email`, common keys include `init.defaultbranch`, `sqlserver.global.*` for server defaults, and `metrics.disabled` to opt out of usage reporting."],["CONFIGURATION","**~/.dolt/config_global.json**\n> Global configuration for all Dolt repositories belonging to the current user.\n\n**.dolt/config.json**\n> Repository-local configuration, which overrides the global settings."],["CAVEATS","An identity (`user.name` and `user.email`) must be set before the first commit; Dolt will refuse to commit without one. Unknown keys are accepted silently, so a typo in a key name fails quietly rather than erroring. **--local** only works inside a Dolt database directory. Server-related settings under `sqlserver.global.*` are read at startup, so `dolt sql-server` must be restarted for changes to take effect."],["HISTORY","dolt config follows the **git config** conventions closely, deliberately so: Dolt's whole interface is designed to be guessable by anyone who already knows Git, and configuration was one of the first commands ported when Dolt was released in 2019."],["INSTALL","```pacman: sudo pacman -S dolt```\n\n```brew: brew install dolt```\n\n```nix: nix profile install nixpkgs#dolt```"],["SEE ALSO","[dolt](/man/dolt)(1), [dolt-commit](/man/dolt-commit)(1), [git-config](/man/git-config)(1)"],["RESOURCES","```[Source code](https://github.com/dolthub/dolt)```\n\n```[Documentation](https://www.dolthub.com/docs/cli-reference/cli/)```"]],"e":["dolt","dolt-commit","git-config"],"x":"dolt config --global --add user.name \"[Your Name]\""},"dysk":{"n":"dysk","t":"filesystem information in formatted tables","s":[["TAGLINE","filesystem information in formatted tables"],["TLDR","Get **standard overview** of disks\n\n```dysk```\n\n**Sort** by free size\n\n```dysk -s free```\n\nInclude only **HDD** disks\n\n```dysk -f 'disk = HDD'```\n\n**Exclude** SSD disks\n\n```dysk -f 'disk <> SSD'```\n\nDisplay disks with **high utilization or low free space**\n\n```dysk -f 'use > 65% | free < 50G'```"],["SYNOPSIS","**dysk** [_options_]"],["DESCRIPTION","**dysk** displays filesystem information in a formatted table. It shows mount points, disk types, sizes, usage, and filesystem types with color-coded output.\n\nProvides filtering and sorting capabilities for focused analysis."],["PARAMETERS","**-a, --all**\n> Show all mount points, including pseudo, removable and remote ones\n\n**-s, --sort** _field_\n> Sort by field (free, size, used, filesystem, etc.)\n\n**-f, --filter** _expr_\n> Filter expression\n\n**-c, --csv**\n> Output as CSV\n\n**-j, --json**\n> Output as JSON\n\n**--cols** _cols_\n> Choose which columns are shown\n\n**-l, --list-cols**\n> List the available columns\n\n**--units** _si|binary_\n> Size units: SI (1M = 1,000,000 B) or binary (1M = 1,048,576 B)\n\n**--color** _auto|yes|no_\n> Control colored output"],["FILTER EXPRESSIONS","**disk = HDD**\n> Only HDDs\n\n**disk = SSD**\n> Only SSDs\n\n**use > 65%**\n> Usage over 65%\n\n**free < 50G**\n> Less than 50GB free"],["CAVEATS","Linux only: dysk reads /proc/mounts and sysfs, so it does not run on macOS or BSD. Disk type detection (SSD vs HDD) depends on what the kernel exposes and can be blank for virtual or remote filesystems. The project was previously named **lfs**."],["INSTALL","```apt: sudo apt install dysk```\n\n```pacman: sudo pacman -S dysk```\n\n```apk: sudo apk add dysk```\n\n```zypper: sudo zypper install dysk```\n\n```brew: brew install dysk```\n\n```nix: nix profile install nixpkgs#dysk```"],["SEE ALSO","[df](/man/df)(1), [lsblk](/man/lsblk)(8), [duf](/man/duf)(1)"],["RESOURCES","```[Source code](https://github.com/Canop/dysk)```\n\n```[Homepage](https://dystroy.org/dysk/)```"]],"e":["df","lsblk","duf"],"x":"dysk"},"ecryptfs-setup-private":{"n":"ecryptfs-setup-private","t":"encrypted private directory setup","s":[["TAGLINE","encrypted private directory setup"],["TLDR","**Setup encrypted private directory**\n\n```ecryptfs-setup-private```\n\n**Setup with wrapped passphrase**\n\n```ecryptfs-setup-private --wrapping```\n\n**Setup without wrapped passphrase**\n\n```ecryptfs-setup-private --noautomount```\n\n**Force setup (overwrite existing)**\n\n```ecryptfs-setup-private --force```"],["SYNOPSIS","**ecryptfs-setup-private** [_options_]"],["PARAMETERS","**--wrapping**\n> Use login passphrase for wrapping.\n\n**--noautomount**\n> Don't configure automount on login.\n\n**--nopwcheck**\n> Don't verify login password.\n\n**--force**\n> Overwrite existing configuration.\n\n**-u**, **--username** _name_\n> Specify username (for root use).\n\n**-l**, **--loginpass** _pass_\n> Login passphrase (insecure, for scripting).\n\n**-m**, **--mountpass** _pass_\n> Mount passphrase (insecure, for scripting)."],["CONFIGURATION","**~/.ecryptfs/**\n> Directory containing encryption keys, wrapped passphrases, and mount configuration.\n\n**~/.ecryptfs/wrapped-passphrase**\n> Mount passphrase wrapped with login password for automatic decryption."],["DESCRIPTION","**ecryptfs-setup-private** creates an encrypted private directory for a user. It sets up ~/Private as an encrypted folder that is automatically mounted when the user logs in and unmounted on logout.\n\nThe setup creates the necessary encryption keys and wrapper, storing them in ~/.ecryptfs. The mount passphrase is wrapped with the login password, enabling automatic decryption on login."],["SETUP PROCESS","1. Prompts for login password\n2. Generates random mount passphrase\n3. Wraps mount passphrase with login password\n4. Creates ~/.Private (encrypted) and ~/Private (mount point)\n5. Configures PAM for auto-mount"],["CAVEATS","Requires eCryptfs kernel module. Swap should be encrypted for security. Login password changes require rewrapping. Recovery requires unwrapped passphrase. Deprecated in favor of fscrypt on modern systems."],["HISTORY","This utility was developed for Ubuntu's encrypted home directory feature. It was widely used for per-user encryption from **Ubuntu 8.10** through **18.04**, after which fscrypt became the recommended solution."],["INSTALL","```apt: sudo apt install ecryptfs-utils```\n\n```dnf: sudo dnf install ecryptfs-utils```\n\n```pacman: sudo pacman -S ecryptfs-utils```\n\n```apk: sudo apk add ecryptfs-utils```\n\n```zypper: sudo zypper install ecryptfs-utils```"]],"e":[],"x":"ecryptfs-setup-private"},"ern":{"n":"ern","t":"React Native integration platform for native apps","s":[["TAGLINE","React Native integration platform for native apps"],["TLDR","**Create a new MiniApp** project\n\n```ern create-miniapp [app-name]```\n\n**Run a MiniApp** on Android\n\n```ern run-android```\n\n**Run a MiniApp** on iOS\n\n```ern run-ios```\n\n**Create a new API** module\n\n```ern create-api [api-name]```\n\n**Add a dependency** to a MiniApp\n\n```ern add [dependency]```\n\n**Start the development** server for MiniApps\n\n```ern start```\n\n**Push an OTA update** via CodePush\n\n```ern code-push [miniapps] -d [descriptor]```"],["SYNOPSIS","**ern** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> ERN operation to perform.\n\n**create-miniapp** _NAME_\n> Create a new MiniApp project (equivalent to react-native init).\n\n**run-android**\n> Run MiniApp on Android emulator or device.\n\n**run-ios**\n> Run MiniApp on iOS simulator or device.\n\n**create-api** _NAME_\n> Create a new API with generated client code.\n\n**start**\n> Start the Electrode Native development server (packager).\n\n**add** _DEPENDENCY_\n> Add a JavaScript or native dependency to a MiniApp.\n\n**cauldron** _COMMAND_\n> Manage the Cauldron (version registry for native app containers).\n\n**code-push** _MINIAPPS_\n> Push OTA updates via CodePush.\n\n**link**\n> Link a MiniApp for local development and debugging.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ern** (Electrode React Native) is a platform for integrating React Native into existing native applications. It provides tools for building, managing, and deploying React Native \"MiniApps\" within native mobile apps.\n\nThe platform includes a Cauldron for version management, Container generation for bundling MiniApps with native dependencies, and APIs for communication between React Native and native code.\n\nERN enables incremental React Native adoption in existing mobile applications."],["CAVEATS","Requires native development setup (Android SDK and/or Xcode). Complex architecture learning curve. Integrates with existing apps differently than standalone React Native."],["HISTORY","Electrode React Native was developed by **Walmart Labs** to enable their gradual adoption of React Native in their existing mobile applications, addressing enterprise integration challenges."],["SEE ALSO","[react-native](/man/react-native)(1), [npm](/man/npm)(1), [yarn](/man/yarn)(1), [node](/man/node)(1)"]],"e":["react-native","npm","yarn","node"],"x":"ern create-miniapp [app-name]"},"fonttools-subset":{"n":"fonttools-subset","t":"generate optimized subsets of font files","s":[["TAGLINE","generate optimized subsets of font files"],["TLDR","Subset a font to **Basic Latin** code points\n\n```fonttools subset [path/to/font.ttf] --unicodes=U+0000-007F```\n\nWrite the subset with an explicit **output path**\n\n```fonttools subset [path/to/font.ttf] --unicodes=U+0000-007F --output-file=[path/to/subset.ttf]```\n\nEmit **WOFF2** instead of TTF\n\n```fonttools subset [path/to/font.ttf] --unicodes=U+0000-007F --flavor=woff2```\n\nKeep only selected OpenType **layout features**\n\n```fonttools subset [path/to/font.ttf] --unicodes=U+0000-007F --layout-features=onum,kern```"],["SYNOPSIS","**fonttools subset** [_options_] _font-file_"],["PARAMETERS","**--unicodes=**_RANGE_\n> Unicode code points to include (e.g., U+0000-007F for Basic Latin)\n\n**--flavor=**_FORMAT_\n> Output format: **woff**, **woff2**, or omit for original format\n\n**--layout-features=**_LIST_\n> Comma-separated OpenType features to keep (e.g., kern,liga,onum)\n\n**--output-file=**_PATH_\n> Specify output filename; defaults to input with .subset suffix\n\n**--text=**_STRING_\n> Include only glyphs needed for the given text\n\n**--text-file=**_FILE_\n> Include glyphs needed for text in the specified file\n\n**--no-hinting**\n> Remove hinting instructions to reduce file size\n\n**--desubroutinize**\n> Remove subroutines from CFF fonts"],["DESCRIPTION","**fonttools subset** generates optimized subsets of font files by including only specified glyphs, Unicode ranges, or OpenType features. This is essential for web font optimization, reducing file sizes significantly by removing unused characters.\n\nThe tool supports TrueType (.ttf), OpenType (.otf), WOFF, and WOFF2 formats. It can convert between formats during subsetting using the **--flavor** option.\n\nCommon use cases include creating language-specific subsets, web-optimized fonts with only Latin characters, or icon fonts with specific glyphs."],["CAVEATS","WOFF2 output requires the **brotli** Python package. Some complex OpenType features may not subset correctly if dependent glyphs are excluded. Always test subsetted fonts thoroughly."],["INSTALL","```dnf: sudo dnf install fonttools```\n\n```apk: sudo apk add py3-fonttools```\n\n```brew: brew install fonttools```"],["SEE ALSO","[fonttools](/man/fonttools)(1)"]],"e":["fonttools"],"x":"fonttools subset [path/to/font.ttf] --unicodes=U+0000-007F"},"git-daemon":{"n":"git-daemon","t":"Simple Git protocol server for repository access","s":[["TAGLINE","Simple Git protocol server for repository access"],["TLDR","**Start Git daemon**\n\n```git daemon --base-path=[/path/to/repos]```\n\n**Enable upload-pack**\n\n```git daemon --enable=upload-pack --base-path=[/repos]```\n\n**Allow push**\n\n```git daemon --enable=receive-pack --base-path=[/repos]```\n\n**Run in foreground**\n\n```git daemon --verbose --base-path=[/repos]```\n\n**Export all directories**\n\n```git daemon --export-all --base-path=[/repos]```"],["SYNOPSIS","**git** **daemon** [_options_] [_directory_...]"],["PARAMETERS","**--base-path** _path_\n> Base path for repositories.\n\n**--export-all**\n> Export all repositories.\n\n**--enable** _service_\n> Enable service.\n\n**--disable** _service_\n> Disable service.\n\n**--port** _port_\n> Listen port (default 9418).\n\n**--listen** _host_\n> Listen address.\n\n**--verbose**\n> Log details.\n\n**--inetd**\n> Run from inetd."],["DESCRIPTION","**git daemon** provides a simple, lightweight server for Git repositories using the git:// protocol. It enables anonymous read access to repositories by default, with optional write access when explicitly enabled, making it suitable for public repository hosting and internal network sharing.\n\nThe daemon listens on port 9418 by default and serves repositories from a specified base path. Unlike HTTP-based Git servers, git daemon requires no web server setup, offering minimal overhead for read-only repository access.\n\nSecurity is intentionally limited: repositories must be explicitly exported (via --export-all or git-daemon-export-ok file), and write access should only be enabled in trusted environments. For production systems requiring authentication and encryption, HTTPS or SSH protocols are recommended instead."],["INSTALL","```dnf: sudo dnf install git-daemon```\n\n```zypper: sudo zypper install git-daemon```"]],"e":[],"x":"git daemon --base-path=[/path/to/repos]"},"git-merge-tree":{"n":"git-merge-tree","t":"Perform merge without touching index or working tree","s":[["TAGLINE","Perform merge without touching index or working tree"],["TLDR","**Preview merge result** (modern mode, Git 2.38+)\n\n```git merge-tree [branch1] [branch2]```\n\n**Perform a three-way merge** with explicit base (deprecated mode)\n\n```git merge-tree [base] [branch1] [branch2]```\n\n**Check for merge conflicts and show informational messages**\n\n```git merge-tree --write-tree --messages [branch1] [branch2]```\n\n**Check if a branch can be cleanly merged** (exit code 0 = clean, 1 = conflicts)\n\n```git merge-tree --write-tree --no-messages [branch1] [branch2]```"],["SYNOPSIS","**git merge-tree** [**--write-tree**] [_options_] _branch1_ _branch2_\n\n**git merge-tree** [**--trivial-merge**] _base_ _branch1_ _branch2_"],["PARAMETERS","_BRANCH1_\n> First branch to merge.\n\n_BRANCH2_\n> Second branch to merge.\n\n_BASE_\n> Common ancestor commit (deprecated trivial-merge mode only).\n\n**--write-tree**\n> Modern mode (default since Git 2.38): automatically finds the merge base and writes a result tree object. Reports conflicts via exit code and stdout.\n\n**--trivial-merge**\n> Deprecated mode: reads three trees and outputs trivial merge results. Requires explicit base.\n\n**--[no-]messages**\n> Write informational messages such as \"Auto-merging\" and CONFLICT notices. Default: included when conflicts exist.\n\n**--name-only**\n> In the output, show only filenames instead of full merge details.\n\n**--allow-unrelated-histories**\n> Allow merging branches that share no common history.\n\n**--merge-base** _commit_\n> Specify a merge base instead of computing one automatically. With this option, branch arguments can be trees instead of commits. Incompatible with --stdin.\n\n**--stdin**\n> Read commits to merge from standard input, one pair per line. Implies -z.\n\n**-z**\n> Use NUL characters as line terminators instead of newlines (for scripting)."],["DESCRIPTION","**git merge-tree** performs a three-way merge without touching the index or working tree. It shows what a merge would produce, making it useful for previewing merges or performing scripted merge analysis.\n\nThe modern mode (default since Git 2.38) automatically finds the merge base, writes a result tree object, and reports conflicts. The exit code indicates success (0) or conflicts (1). This makes it ideal for CI pipelines and automation scripts that need to check mergeability without modifying any files.\n\nThe deprecated trivial-merge mode requires specifying the base commit explicitly and only handles trivial merges."],["CAVEATS","Plumbing command primarily intended for scripting. Output format differs between modern and deprecated modes. Modern mode requires Git 2.38 or later."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-merge](/man/git-merge)(1), [git-merge-file](/man/git-merge-file)(1)"]],"e":["git-merge","git-merge-file"],"x":"git merge-tree [branch1] [branch2]"},"git-remote-ext":{"n":"git-remote-ext","t":"External command transport helper","s":[["TAGLINE","External command transport helper"],["TLDR","**Clone via external command**\n\n```git clone \"ext::[command] [args]\"```\n\n**Use with custom transport**\n\n```git remote add myremote \"ext::[transport-cmd] %S [host]\"```"],["SYNOPSIS","**git-remote-ext** _url_"],["PARAMETERS","_URL_\n> ext:: URL with command template.\n\n**%s**\n> Replaced with service name (e.g. `upload-pack`, `receive-pack`, `upload-archive`) without the `git-` prefix.\n\n**%S**\n> Replaced with long service name (e.g. `git-upload-pack`) including the `git-` prefix.\n\n**%G**\n> Placed as the first argument; suppresses the service-name argument so the remote command can accept extra positional arguments.\n\n**%V**\n> Only valid as the second argument; injects the Git protocol version (e.g. `version=2`) as an environment setting.\n\n**%%**\n> Literal percent sign.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git-remote-ext** is a remote helper that allows arbitrary external commands to handle Git transport. The `ext::` URL scheme specifies a command to run for communication, enabling custom transport mechanisms like specific SSH commands or non-standard protocols.\n\nTemplate variables (such as `%s`, `%S`, `%G`) allow dynamic command construction based on the Git service being requested. This helper is typically not invoked directly but is used through `ext::` URLs in remote configurations."],["CAVEATS","Security sensitive: commands run from URLs. Internal helper, not typically used directly. Used through ext:: URLs."],["HISTORY","git-remote-ext is a **Git** remote helper enabling custom transport mechanisms through arbitrary command execution."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-remote](/man/git-remote)(1), [git-remote-fd](/man/git-remote-fd)(1)"]],"e":["git-remote","git-remote-fd"],"x":"git clone \"ext::[command] [args]\""},"gitlab-runner":{"n":"gitlab-runner","t":"CI/CD job execution agent for GitLab","s":[["TAGLINE","CI/CD job execution agent for GitLab"],["TLDR","**Register a new runner**\n\n```sudo gitlab-runner register```\n\n**List configured runners**\n\n```gitlab-runner list```\n\n**Start the runner service**\n\n```sudo gitlab-runner start```\n\n**Stop the runner service**\n\n```sudo gitlab-runner stop```\n\n**Run in foreground** (for debugging)\n\n```gitlab-runner run```\n\n**Verify runner connections**\n\n```gitlab-runner verify```\n\n**Unregister a runner**\n\n```sudo gitlab-runner unregister --name [runner_name]```\n\n**Check runner status**\n\n```gitlab-runner status```\n\n**Reset runner authentication token**\n\n```sudo gitlab-runner reset-token --name [runner_name]```"],["SYNOPSIS","**gitlab-runner** _command_ [_options_]"],["PARAMETERS","**register**\n> Register a new runner with GitLab instance.\n\n**list**\n> List all configured runners.\n\n**start**\n> Start the runner service.\n\n**stop**\n> Stop the runner service.\n\n**restart**\n> Restart the runner service.\n\n**status**\n> Show runner service status.\n\n**run**\n> Run the runner in foreground.\n\n**run-single**\n> Execute a single build from a GitLab instance.\n\n**verify**\n> Verify runner connections to GitLab.\n\n**unregister**\n> Remove a runner from GitLab.\n\n**reset-token**\n> Reset a runner's authentication token.\n\n**install**\n> Install as system service.\n\n**uninstall**\n> Remove system service.\n\n**--debug**\n> Enable debug logging.\n\n**--log-level** _level_\n> Log level: debug, info, warn, error, fatal, panic.\n\n**--log-format** _format_\n> Log format: runner, text, json.\n\n**--config** _file_\n> Use alternate config file.\n\n**--working-directory** _path_\n> Set custom working directory."],["DESCRIPTION","**gitlab-runner** is the agent that runs CI/CD jobs for GitLab. It connects to a GitLab instance, receives job requests, executes them in isolated environments, and reports results back. Runners can be shared across projects or dedicated to specific ones.\n\nThe runner supports multiple executors: **shell** (directly on host), **docker** (in containers), **docker-machine** (auto-scaled Docker), **kubernetes** (in K8s pods), **virtualbox**, **parallels**, and **ssh**. The Docker executor is most common for isolated, reproducible builds.\n\nRegistration requires a GitLab URL and registration token from the project/group/admin CI settings. Runners can operate as a system service or run in the foreground for debugging."],["CONFIGURATION","**/etc/gitlab-runner/config.toml**\n> Main configuration file where each registered runner appears as a `[[runners]]` section with its URL, token, executor, and settings."],["CAVEATS","Shell executor provides no isolation between jobs. Docker executor requires Docker installed. Registration tokens should be kept secret. Runners have access to whatever secrets are passed to jobs. Resource limits should be configured to prevent runaway jobs."],["HISTORY","**GitLab Runner** was developed by GitLab Inc. as part of their CI/CD platform, which became part of GitLab around **2015** when GitLab CI was integrated into the main product. The runner has evolved from simple shell execution to supporting multiple sophisticated executors, making it one of the most versatile CI job runners available."],["INSTALL","```pacman: sudo pacman -S gitlab-runner```\n\n```apk: sudo apk add gitlab-runner```\n\n```brew: brew install gitlab-runner```\n\n```nix: nix profile install nixpkgs#gitlab-runner```"],["SEE ALSO","[docker](/man/docker)(1), [gitlab-ctl](/man/gitlab-ctl)(8), [jenkins](/man/jenkins)(1), [kubectl](/man/kubectl)(1)"]],"e":["docker","gitlab-ctl","jenkins","kubectl"],"x":"sudo gitlab-runner register"},"gitlogue":{"n":"gitlogue","t":"Cinematic animated replay of Git commit history","s":[["TAGLINE","Cinematic animated replay of Git commit history"],["TLDR","**Replay** the current repository's history\n\n```gitlogue```\n\n**Replay** a commit range\n\n```gitlogue --commit [HEAD~10..HEAD]```\n\n**Replay** a specific commit\n\n```gitlogue --commit [abc123]```"],["SYNOPSIS","**gitlogue** [*options*]"],["DESCRIPTION","**gitlogue** turns Git history into an animated terminal story—typing effects, syntax highlighting, and file-tree transitions as commits are replayed. Fun for demos, onboarding, and visualizing how a codebase evolved. Install via install script, Homebrew, Cargo, or Nix."],["PARAMETERS","**--commit** *rev*|*range*\n\n> Start at a commit or animate a range.\n\nOther flags control speed and theme—**gitlogue --help**."],["CAVEATS","Large histories can take a long time to animate. Needs a git repository and a capable terminal."],["INSTALL","```pacman: sudo pacman -S gitlogue```\n\n```brew: brew install gitlogue```\n\n```nix: nix profile install nixpkgs#gitlogue```"],["SEE ALSO","[git-log](/man/git-log)(1), [gource](/man/gource)(1), [git-blame](/man/git-blame)(1)"],["RESOURCES","```[Source code](https://github.com/unhappychoice/gitlogue)```"]],"e":["git-log","gource","git-blame"],"x":"gitlogue"},"glance":{"n":"glance","t":"legacy command-line client for the OpenStack Image service (Glance)","s":[["TAGLINE","legacy command-line client for the OpenStack Image service (Glance)"],["TLDR","**List images**\n\n```glance image-list```\n\n**Show image details**\n\n```glance image-show [image-id]```\n\n**Create an image from a local file**\n\n```glance image-create --name [image-name] --disk-format [qcow2] --container-format [bare] --file [image.qcow2]```\n\n**Download image data**\n\n```glance image-download [image-id] --file [output.qcow2]```\n\n**Update an image**\n\n```glance image-update [image-id] --name [new-name]```\n\n**Delete an image**\n\n```glance image-delete [image-id]```"],["SYNOPSIS","**glance** [_options_] _command_ [_command-options_]"],["PARAMETERS","**image-list**\n> List available images.\n\n**image-show** _image-id_\n> Show details for an image.\n\n**image-create**\n> Create a new image record, optionally uploading data with **--file**.\n\n**image-update** _image-id_\n> Update an image's metadata.\n\n**image-download** _image-id_\n> Download image data to a file.\n\n**image-upload** _image-id_\n> Upload data for an existing image.\n\n**image-delete** _image-id_\n> Delete an image.\n\n**--file** _path_\n> Local file to upload or save downloaded data to.\n\n**--disk-format** _format_\n> Disk format (raw, qcow2, vmdk, vdi, iso, ami, ...).\n\n**--container-format** _format_\n> Container format (bare, ovf, aki, ari, ami, ...).\n\n**--os-auth-token** _token_\n> Authentication token, as an alternative to username/password.\n\n**--os-username** _user_, **--os-password** _pass_\n> OpenStack credentials, also settable via environment variables.\n\n**--os-image-url** _url_\n> Glance API endpoint, overriding the service catalog."],["DESCRIPTION","**glance** is the standalone command-line client shipped by **python-glanceclient** for the OpenStack Image service (Glance), which stores and serves the virtual machine images used to boot Nova instances.\n\nIt predates the unified **openstack** CLI and is now considered legacy: OpenStack deployments and documentation direct users toward `openstack image` subcommands for day-to-day image management (see [openstack-image](/man/openstack-image)(1)). The `glance` binary remains available for scripts and environments that still depend on it."],["CAVEATS","Superseded by `openstack image ...` in current OpenStack releases; new deployments should prefer the unified CLI. Requires OpenStack credentials (via `clouds.yaml`, environment variables, or `--os-*` flags). Large image uploads/downloads can take a long time and are subject to project storage quotas."],["HISTORY","Glance was one of the original OpenStack services, launched alongside **Nova** in the **2010** Austin release, and the `glance` CLI was its dedicated client for years before OpenStack consolidated per-service clients into the single `openstack` command."],["INSTALL","```nix: nix profile install nixpkgs#glance```"],["SEE ALSO","[openstack](/man/openstack)(1), [openstack-image](/man/openstack-image)(1), [nova](/man/nova)(1), [qemu-img](/man/qemu-img)(1)"],["RESOURCES","```[Source code](https://github.com/openstack/python-glanceclient)```\n\n```[CLI documentation](https://docs.openstack.org/python-glanceclient/latest/cli/details.html)```"]],"e":["openstack","openstack-image","nova","qemu-img"],"x":"glance image-list"},"gzip":{"n":"gzip","t":"compresses files using the Lempel-Ziv coding algorithm","s":[["TAGLINE","compresses files using the Lempel-Ziv coding algorithm"],["TLDR","**Compress a file**\n\n```gzip [file]```\n\n**Decompress a file**\n\n```gzip -d [file.gz]```\n\n**Compress keeping original file**\n\n```gzip -k [file]```\n\n**Compress with best compression**\n\n```gzip -9 [file]```\n\n**Compress with fastest speed**\n\n```gzip -1 [file]```\n\n**List compression information**\n\n```gzip -l [file.gz]```\n\n**Test compressed file integrity**\n\n```gzip -t [file.gz]```\n\n**Compress to stdout** (pipe)\n\n```gzip -c [file] > [file.gz]```\n\n**Decompress to stdout**\n\n```gzip -dc [file.gz]```"],["SYNOPSIS","**gzip** [_options_] [_files_...]"],["DESCRIPTION","**gzip** compresses files using the Lempel-Ziv coding (LZ77) algorithm. Each file is replaced by a compressed version with a .gz extension, preserving ownership, modes, and timestamps.\n\nBy default, gzip removes the original file after compression. Compressed files can be restored using gzip -d (or gunzip). The tool can also compress data from stdin and write to stdout for pipeline use.\n\ngzip is widely supported and commonly used for compressing individual files, creating archives with tar, and transferring data. The format is defined in RFC 1952."],["PARAMETERS","**-d**, **--decompress**\n> Decompress files.\n\n**-c**, **--stdout**\n> Write to stdout, keep original files.\n\n**-k**, **--keep**\n> Keep original files.\n\n**-l**, **--list**\n> List compression info.\n\n**-t**, **--test**\n> Test integrity.\n\n**-f**, **--force**\n> Force compression/decompression.\n\n**-r**, **--recursive**\n> Recurse into directories.\n\n**-n**, **--no-name**\n> Don't save original name/timestamp.\n\n**-N**, **--name**\n> Save/restore original name/timestamp.\n\n**-1** to **-9**\n> Compression level (fast to best).\n\n**--best**\n> Maximum compression (-9).\n\n**--fast**\n> Fastest compression (-1).\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-q**, **--quiet**\n> Suppress warnings."],["CAVEATS","Cannot compress directories directly (use tar first). Original file is removed by default unless -k is used. Links are followed (not compressed as links). gzip -r on directories compresses each file separately."],["HISTORY","**gzip** was written by **Jean-loup Gailly** and **Mark Adler** in **1992** as a free replacement for the Unix compress utility, which was encumbered by patents. The gzip format became the standard for file compression on Unix systems and is used by HTTP for content encoding. The underlying DEFLATE algorithm is also used in ZIP files and PNG images."],["INSTALL","```apt: sudo apt install gzip```\n\n```dnf: sudo dnf install gzip```\n\n```pacman: sudo pacman -S gzip```\n\n```apk: sudo apk add gzip```\n\n```zypper: sudo zypper install gzip```\n\n```brew: brew install gzip```\n\n```nix: nix profile install nixpkgs#gzip```"],["SEE ALSO","[gunzip](/man/gunzip)(1), [zcat](/man/zcat)(1), [bzip2](/man/bzip2)(1), [xz](/man/xz)(1), [tar](/man/tar)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/gzip/)```\n\n```[Source code](https://git.savannah.gnu.org/cgit/gzip.git)```\n\n```[Documentation](https://www.gnu.org/software/gzip/manual/gzip.html)```"]],"e":["gunzip","zcat","bzip2","xz","tar"],"x":"gzip [file]"},"handbrakecli":{"n":"handbrakecli","t":"command-line interface for HandBrake, a powerful open-source video transcoder","s":[["TAGLINE","command-line interface for HandBrake, a powerful open-source video transcoder"],["TLDR","**Convert a video** using a preset\n\n```HandBrakeCLI -i [input.mkv] -o [output.mp4] --preset=\"[Fast 1080p30]\"```\n\n**List available presets**\n\n```HandBrakeCLI --preset-list```\n\n**Convert with specific video codec**\n\n```HandBrakeCLI -i [input.mkv] -o [output.mp4] -e [x264]```\n\n**Set video quality** (lower = better, 18-28 typical)\n\n```HandBrakeCLI -i [input.mkv] -o [output.mp4] -q [22]```\n\n**Convert a DVD** or Blu-ray directory\n\n```HandBrakeCLI -i [/path/to/VIDEO_TS] -o [output.mp4] --preset=\"[Fast 1080p30]\"```\n\n**Extract a specific title** from disc\n\n```HandBrakeCLI -i [/dev/dvd] -t [1] -o [output.mp4]```\n\n**Convert with audio and subtitle** selection\n\n```HandBrakeCLI -i [input.mkv] -o [output.mp4] -a [1,2] -s [1]```\n\n**Scan input** to see titles and tracks\n\n```HandBrakeCLI -i [input.mkv] --scan```"],["SYNOPSIS","**HandBrakeCLI** [_-i input_] [_-o output_] [_--preset name_] [_-e encoder_] [_-q quality_] [_options_]"],["PARAMETERS","**-i**, **--input** _source_\n> Input file, directory, or device.\n\n**-o**, **--output** _file_\n> Output file path.\n\n**-Z**, **--preset** _name_\n> Use named preset (see --preset-list).\n\n**-z**, **--preset-list**\n> List available presets.\n\n**-t**, **--title** _number_\n> Select title number (for DVDs/Blu-rays).\n\n**--scan**\n> Scan input without encoding.\n\n**-e**, **--encoder** _codec_\n> Video encoder: x264, x265, nvenc_h264, nvenc_h265, vce_h264, vce_h265, svt_av1.\n\n**-q**, **--quality** _RF_\n> Constant quality (RF value, lower = better).\n\n**-b**, **--vb** _kbps_\n> Video bitrate in kbps.\n\n**-r**, **--rate** _fps_\n> Frame rate.\n\n**-a**, **--audio** _tracks_\n> Audio track(s) to include (comma-separated).\n\n**-E**, **--aencoder** _codec_\n> Audio encoder: copy, aac, ac3, mp3, opus, flac.\n\n**-s**, **--subtitle** _tracks_\n> Subtitle track(s) to include.\n\n**--crop** _top:bottom:left:right_\n> Crop values.\n\n**-w**, **--width** _pixels_\n> Output width.\n\n**-l**, **--height** _pixels_\n> Output height.\n\n**--multi-pass**\n> Enable multi-pass encoding (replaces the older --two-pass flag)."],["DESCRIPTION","**HandBrakeCLI** is the command-line interface for HandBrake, a powerful open-source video transcoder. It converts videos between formats, rips DVDs and Blu-rays, and can significantly reduce file sizes while maintaining quality.\n\nThe tool uses presets to simplify common encoding scenarios. Built-in presets target specific devices (Apple TV, Android, Roku) or quality levels (Fast 1080p30, HQ 1080p30 Surround). Custom presets can be exported from the GUI and imported into CLI workflows.\n\nFor quality-based encoding, the RF (Rate Factor) value controls the quality-to-size tradeoff. For x264/x265, values of 18-22 provide high quality, 23-28 provide smaller files with acceptable quality. Lower numbers mean higher quality and larger files.\n\nHandBrake supports hardware acceleration on supported systems: NVIDIA NVENC, AMD VCE/VCN, Intel QuickSync, and Apple VideoToolbox. Hardware encoders are faster but may produce larger files at equivalent quality.\n\nAudio and subtitle tracks can be selected, converted, or passed through. Multiple audio tracks can be included with different encodings. Subtitles can be burned in or kept as soft subs."],["CAVEATS","Cannot output to some patent-encumbered formats. DVD/Blu-ray decryption requires libdvdcss. Quality settings don't translate directly between encoders. Hardware encoding availability depends on GPU and drivers. Multi-pass encoding takes longer than a single pass."],["HISTORY","**HandBrake** was originally created by Eric Petit (titer) in **2003** as \"MediaFork\" for BeOS, then renamed and continued on other platforms. The project was revived by the open-source community after a period of inactivity. It has become one of the most popular open-source video transcoders, known for its balance of features and usability."],["SEE ALSO","[handbrake](/man/handbrake)(1), [ffmpeg](/man/ffmpeg)(1), [ffprobe](/man/ffprobe)(1), [mkvmerge](/man/mkvmerge)(1), [mediainfo](/man/mediainfo)(1)"],["RESOURCES","```[Source code](https://github.com/HandBrake/HandBrake)```\n\n```[Homepage](https://handbrake.fr/)```\n\n```[Documentation](https://handbrake.fr/docs/)```"]],"e":["handbrake","ffmpeg","ffprobe","mkvmerge","mediainfo"],"x":"HandBrakeCLI -i [input.mkv] -o [output.mp4] --preset=\"[Fast 1080p30]\""},"hp-align":{"n":"hp-align","t":"aligns print cartridges on HP printers","s":[["TAGLINE","aligns print cartridges on HP printers"],["TLDR","**Align print heads**\n\n```hp-align```\n\n**Specify printer**\n\n```hp-align -p [printer-name]```\n\n**Interactive mode**\n\n```hp-align -i```\n\n**Specify device URI**\n\n```hp-align -d [hp:/usb/Deskjet_Printer?serial=XYZ]```\n\n**Enable debug logging**\n\n```hp-align -g```"],["SYNOPSIS","**hp-align** [_options_]"],["PARAMETERS","**-p** _PRINTER_, **--printer=**_PRINTER_\n> CUPS printer name.\n\n**-d** _URI_, **--device=**_URI_\n> Device URI (e.g. `hp:/usb/...`).\n\n**-i**, **--interactive**\n> Run in interactive mode.\n\n**-l** _LEVEL_, **--logging=**_LEVEL_\n> Logging level: none, info, error, warn, debug.\n\n**-g**\n> Debug mode (equivalent to `-l debug`).\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**hp-align** aligns print cartridges on HP printers. It runs the alignment procedure to ensure optimal print quality.\n\nThe tool supports automatic and interactive alignment modes. Interactive mode displays alignment patterns and asks for best match selection."],["CAVEATS","Part of HPLIP package. Uses paper and ink. May require manual selection."],["HISTORY","hp-align is part of **HPLIP** (HP Linux Imaging and Printing), HP's official printer driver project for Linux."],["INSTALL","```apt: sudo apt install hplip```\n\n```dnf: sudo dnf install hplip```\n\n```pacman: sudo pacman -S hplip```\n\n```zypper: sudo zypper install hplip```\n\n```nix: nix profile install nixpkgs#hplip```"],["SEE ALSO","[hp-check](/man/hp-check)(1), [hp-clean](/man/hp-clean)(1), [hp-setup](/man/hp-setup)(1)"]],"e":["hp-check","hp-clean","hp-setup"],"x":"hp-align"},"hwdb":{"n":"hwdb","t":"systemd component that maps hardware properties to udev device attributes","s":[["TAGLINE","systemd component that maps hardware properties to udev device attributes"],["TLDR","**Update hardware database**\n\n```sudo systemd-hwdb update```\n\n**Query device properties**\n\n```systemd-hwdb query [modalias]```\n\n**Show hardware database path**\n\n```systemd-hwdb --help```"],["SYNOPSIS","**systemd-hwdb** [_options_] _command_"],["PARAMETERS","**update**\n> Compile hwdb files to binary.\n\n**query** _modalias_\n> Query properties for modalias.\n\n**--path** _path_\n> Custom hwdb path.\n\n**--root** _root_\n> Root directory.\n\n**--strict**\n> Strict parsing mode.\n\n**--usr**\n> Use /usr directory."],["DESCRIPTION","**hwdb** (Hardware Database) is a systemd component that maps hardware properties to udev device attributes. It matches hardware by modalias strings and sets properties like device names, permissions, and driver hints.\n\nThe database is compiled from text files in /usr/lib/udev/hwdb.d/ and /etc/udev/hwdb.d/ into a binary format for fast matching."],["HWDB FILE FORMAT","```"],["Match USB keyboard","usb:v04D9p1702*\n KEYBOARD_KEY_70039=leftctrl\n KEYBOARD_KEY_3a=capslock"],["Match by PCI ID","pci:v00001234d00005678*\n ID_VENDOR=Custom Vendor\n```"],["USAGE","```bash"],["Update after editing hwdb files","sudo systemd-hwdb update\nsudo udevadm trigger"],["Find modalias for device","udevadm info /dev/input/event0 | grep MODALIAS\n```"],["CAVEATS","Changes require hwdb update and udevadm trigger. Modalias format varies by bus type. Incorrect entries may cause device issues."],["SEE ALSO","[udevadm](/man/udevadm)(8)"]],"e":["udevadm"],"x":"sudo systemd-hwdb update"},"in2csv":{"n":"in2csv","t":"converts tabular data from various formats to CSV","s":[["TAGLINE","converts tabular data from various formats to CSV"],["TLDR","**Convert Excel to CSV**\n\n```in2csv [data.xlsx] > [output.csv]```\n\n**Convert specific sheet**\n\n```in2csv --sheet [Sheet1] [data.xlsx]```\n\n**Convert JSON to CSV**\n\n```in2csv [data.json] > [output.csv]```\n\n**Specify input format**\n\n```in2csv -f [xlsx|json|ndjson] [input]```\n\n**Convert fixed-width file**\n\n```in2csv -f fixed -s [schema.csv] [data.txt]```"],["SYNOPSIS","**in2csv** [_options_] _file_"],["PARAMETERS","**-f** _FORMAT_\n> Input format (csv, xlsx, xls, json, ndjson, fixed).\n\n**--sheet** _NAME_\n> Sheet name for Excel files.\n\n**-s** _SCHEMA_\n> Schema file for fixed-width input.\n\n**-H**, **--no-header-row**\n> Input has no header row.\n\n**-K** _N_\n> Skip N lines at start.\n\n**--help**\n> Display help information."],["DESCRIPTION","**in2csv** converts tabular data from various formats to CSV. It's part of the csvkit toolkit for working with CSV files.\n\nThe tool handles Excel, JSON, fixed-width, and other formats. It enables piping data to other csvkit tools for analysis."],["CAVEATS","Part of csvkit. Python-based. Large files may be slow."],["HISTORY","in2csv is part of **csvkit**, created by **Christopher Groskopf** for journalists and data analysts."],["SEE ALSO","[csvcut](/man/csvcut)(1), [csvlook](/man/csvlook)(1), [csvstat](/man/csvstat)(1), [csvsql](/man/csvsql)(1)"]],"e":["csvcut","csvlook","csvstat","csvsql"],"x":"in2csv [data.xlsx] > [output.csv]"},"kubecolor":{"n":"kubecolor","t":"Colorize your kubectl output","s":[["TAGLINE","Colorize your kubectl output"],["TLDR","**Run a kubectl command** with colorized output\n\n```kubecolor get pods```\n\n**Get resources with light background** theme\n\n```kubecolor get pods --light-background```\n\n**Describe a resource** with colors\n\n```kubecolor describe pod [pod_name]```"],["SYNOPSIS","**kubecolor** [_kubectl_arguments_...]"],["PARAMETERS","**--light-background**\n> Use color preset optimized for light terminal backgrounds.\n\n**--force-colors**\n> Force colorized output even when not writing to a terminal.\n\n**--kubecolor-theme** _THEME_\n> Select a color theme. Includes colorblind-adjusted themes for deuteranopia and tritanopia."],["DESCRIPTION","**kubecolor** is a kubectl wrapper that adds colors to kubectl output. It internally calls kubectl and colorizes the response, making it usable as a drop-in replacement by aliasing `kubectl` to `kubecolor`.\n\nColorization is automatically disabled when output is piped or redirected to a file, ensuring compatibility with scripts and other commands. Only READ commands (get, describe, etc.) are colorized; WRITE commands (apply, edit, exec) pass through without modification."],["CONFIGURATION","**Alias setup**\n> Add `alias kubectl=\"kubecolor\"` to your shell profile for seamless integration."],["CAVEATS","Requires kubectl to be installed and configured. Only colorizes output from read operations. Some custom output formats may not be colorized correctly."],["HISTORY","**kubecolor** was originally created by **Hidetatsu Yaginuma** (hidetatz) and is written in **Go**. The project is now maintained by the **kubecolor** organization on GitHub."],["INSTALL","```apt: sudo apt install kubecolor```\n\n```zypper: sudo zypper install kubecolor```\n\n```brew: brew install kubecolor```\n\n```nix: nix profile install nixpkgs#kubecolor```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [k9s](/man/k9s)(1)"]],"e":["kubectl","k9s"],"x":"kubecolor get pods"},"lvm_import_vdo":{"n":"lvm_import_vdo","t":"Convert a standalone VDO volume into an LVM-managed VDO logical volume","s":[["TAGLINE","Convert a standalone VDO volume into an LVM-managed VDO logical volume"],["TLDR","Import a VDO volume with **automatic names** for the VG/LV\n\n```lvm_import_vdo [/dev/mapper/vdo_volume]```\n\nImport and set the **destination VG/LV name**\n\n```lvm_import_vdo -n [vg_name/lv_name] [/dev/mapper/vdo_volume]```\n\nShow what would be done **without changing anything**\n\n```lvm_import_vdo --dry-run [/dev/mapper/vdo_volume]```\n\nConvert in place **without using a temporary snapshot** (less safe)\n\n```lvm_import_vdo --no-snapshot [/dev/mapper/vdo_volume]```\n\n**Verbose output** and automatically answer \"yes\" to prompts\n\n```lvm_import_vdo -v -y [/dev/mapper/vdo_volume]```\n\nUse a **VDO manager configuration file** during import\n\n```lvm_import_vdo --vdo-config [path/to/vdo.conf] [/dev/mapper/vdo_volume]```"],["SYNOPSIS","**lvm_import_vdo** [_options_] _device_"],["PARAMETERS","**-n, --name _vg/lv_**\n> Specify the destination volume group and logical volume name\n\n**--dry-run**\n> Preview the import without making changes\n\n**--no-snapshot**\n> Skip creating a temporary snapshot during conversion (faster but riskier)\n\n**-v, --verbose**\n> Enable verbose output\n\n**-y, --yes**\n> Automatically answer yes to all prompts\n\n**--vdo-config _file_**\n> Use a VDO manager configuration file during import"],["DESCRIPTION","**lvm_import_vdo** converts a VDO (Virtual Data Optimizer) volume created with the standalone VDO manager into an LVM-managed VDO logical volume. This allows integration with LVM's volume management capabilities while preserving the VDO deduplication and compression features.\n\nThe conversion is **irreversible** and migrates the VDO metadata format. By default, a temporary snapshot is created for safety during the conversion process.\n\nVDO provides inline deduplication and compression for block storage, reducing storage requirements for workloads with redundant data like virtual machines and containers."],["CAVEATS","This operation is **irreversible**; the original VDO volume format cannot be restored. The **--no-snapshot** option skips safety mechanisms and should only be used when snapshot creation fails. Back up important data before importing. Requires root privileges."],["INSTALL","```apk: sudo apk add lvm2-extra```"],["SEE ALSO","[lvm](/man/lvm)(8), [lvcreate](/man/lvcreate)(8)"]],"e":["lvm","lvcreate"],"x":"lvm_import_vdo [/dev/mapper/vdo_volume]"},"mapscii":{"n":"mapscii","t":"Braille and ASCII world map renderer for the terminal","s":[["TAGLINE","Braille and ASCII world map renderer for the terminal"],["TLDR","**Launch the interactive world map**\n\n```mapscii```\n\n**Connect via telnet** without installing\n\n```telnet mapscii.me```"],["SYNOPSIS","**mapscii** [_options_]"],["DESCRIPTION","**mapscii** is a node.js based vector tile to Braille and ASCII renderer that brings an interactive world map to your terminal using OpenStreetMap data. You can pan with arrow keys or mouse drag, zoom with **a**/**z** keys or mouse scroll, and switch between Unicode Braille and raw ASCII rendering with **c**.\n\nThe map supports Mapbox Styles for customizing layer styling and can connect to any public or private vector tile server. It also works offline with local VectorTile/MBTiles sources."],["CAVEATS","Braille rendering requires a Unicode-capable terminal and font. Detail level depends on terminal size and zoom level."],["HISTORY","**mapscii** was created by **rastapasta** and is written in **JavaScript** (Node.js). It can be accessed remotely via `telnet mapscii.me` without installation."],["INSTALL","```brew: brew install mapscii```\n\n```nix: nix profile install nixpkgs#mapscii```"],["SEE ALSO","[telnet](/man/telnet)(1)"]],"e":["telnet"],"x":"mapscii"},"migratepages":{"n":"migratepages","t":"moves the physical memory pages of a process from one set of NUMA","s":[["TAGLINE","moves the physical memory pages of a process from one set of NUMA"],["TLDR","**Migrate pages from one node to another**\n\n```migratepages [pid] [from_node] [to_node]```\n\n**Migrate all pages to specific node**\n\n```migratepages [pid] all [2]```\n\n**Migrate to closest node**\n\n```migratepages [pid] [0] best```"],["SYNOPSIS","**migratepages** _pid_ _from-nodes_ _to-nodes_"],["PARAMETERS","**pid**\n> Process ID.\n\n**from-nodes**\n> Source NUMA nodes (or 'all').\n\n**to-nodes**\n> Destination NUMA nodes (or 'best')."],["DESCRIPTION","**migratepages** moves the physical memory pages of a process from one set of NUMA nodes to another. This is useful for optimizing memory placement on NUMA systems.\n\nThe command works on a running process and can migrate pages to improve memory locality."],["Migrate PID 1234 from node 0 to node 1","migratepages 1234 0 1"],["Migrate from all nodes to node 2","migratepages 1234 all 2"],["Let kernel choose best destination","migratepages 1234 0 best\n```"],["CAVEATS","Requires numactl package. Process must be running. Migration can be slow. May require root. Only works on NUMA systems."],["HISTORY","migratepages is part of the **numactl** package, developed for controlling NUMA memory policies on Linux systems."],["SEE ALSO","[numactl](/man/numactl)(8), [numastat](/man/numastat)(8), [mbind](/man/mbind)(2)"],["INSTALL","```apt: sudo apt install numactl```\n\n```dnf: sudo dnf install numactl```\n\n```pacman: sudo pacman -S numactl```\n\n```apk: sudo apk add numactl-tools```\n\n```zypper: sudo zypper install numactl```\n\n```brew: brew install numactl```\n\n```nix: nix profile install nixpkgs#numactl```"]],"e":["numactl","numastat","mbind"],"x":"migratepages [pid] [from_node] [to_node]"},"mingle":{"n":"mingle","t":"fast edge bundling for graphs","s":[["TAGLINE","fast edge bundling for graphs"],["TLDR","**Bundle edges in a graph and write output**\n\n```mingle -o [output.gv] [input.gv]```\n\n**Bundle using force-directed method**\n\n```mingle -m 0 [input.gv]```\n\n**Set edge compatibility measure (distance-based)**\n\n```mingle -c 0 [input.gv]```\n\n**Set maximum turning angle in degrees**\n\n```mingle -a [40] [input.gv]```\n\n**Output in simple schematic format**\n\n```mingle -T simple -o [output.gv] [input.gv]```\n\n**Enable verbose tracing**\n\n```mingle -v [input.gv]```"],["SYNOPSIS","**mingle** [_options_] [_file_]"],["PARAMETERS","**-m** _k_\n> Bundling method: 0 = force-directed, 1 = agglomerative ink saving (default if available), 2 = cluster plus ink saving.\n\n**-a** _k_\n> Maximum turning angle in degrees (non-negative real, default 40).\n\n**-c** _v_\n> Compatibility measure: 0 = distance (default), 1 = full compatibility. Only used with force-directed bundling.\n\n**-i** _k_\n> Maximum iterative divisions in force-directed bundling.\n\n**-r** _k_\n> Maximum recursion level for agglomerative method (default 100).\n\n**-T** _fmt_\n> Output format: \"gv\" attaches drawing info to input graph, \"simple\" outputs schematic representation.\n\n**-v** [_k_]\n> Verbose level for tracing (default 1 if no value given).\n\n**-?**\n> Print usage and exit."],["DESCRIPTION","**mingle** performs fast edge bundling on graphs. It reduces visual clutter in dense graph visualizations by grouping similar edges together into bundles.\n\nPart of the Graphviz suite, mingle reads a graph in DOT format and produces output with bundled edges. It supports multiple bundling algorithms including force-directed and agglomerative ink saving methods."],["CAVEATS","Part of Graphviz. Output is always in DOT format. May obscure individual edge paths in dense graphs."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[dot](/man/dot)(1), [neato](/man/neato)(1), [sfdp](/man/sfdp)(1), [fdp](/man/fdp)(1)"]],"e":["dot","neato","sfdp","fdp"],"x":"mingle -o [output.gv] [input.gv]"},"msbuild":{"n":"msbuild","t":"microsoft Build Engine","s":[["TAGLINE","microsoft Build Engine"],["TLDR","**Build solution**\n\n```msbuild [solution.sln]```\n\n**Build specific configuration**\n\n```msbuild [project.csproj] /p:Configuration=[Release]```\n\n**Build specific target**\n\n```msbuild /t:[Clean]```\n\n**Restore and build**\n\n```msbuild /t:Restore;Build```\n\n**Set verbosity**\n\n```msbuild /v:[detailed]```\n\n**Build with parallelism**\n\n```msbuild /m:[4]```\n\n**Build to output directory**\n\n```msbuild /p:OutputPath=[bin/output]```"],["SYNOPSIS","**msbuild** [_options_] [_project_file_]"],["PARAMETERS","_PROJECT_FILE_\n> Solution or project file.\n\n**/t:** _TARGET_\n> Target to build.\n\n**/p:** _PROPERTY=VALUE_\n> Set property value.\n\n**/v:** _LEVEL_\n> Verbosity level.\n\n**/m** [_N_]\n> Maximum parallel builds.\n\n**/restore**\n> Run restore before build.\n\n**/?**\n> Display help information."],["DESCRIPTION","**msbuild** is the Microsoft Build Engine. It builds .NET applications and solutions.\n\nThe tool processes project files. Supports parallel builds and custom targets."],["CAVEATS","Requires .NET SDK. Windows-centric but cross-platform. XML-based project files."],["HISTORY","MSBuild was created by **Microsoft** as the build platform for .NET, first released with Visual Studio 2005."],["INSTALL","```nix: nix profile install nixpkgs#msbuild```"],["SEE ALSO","[dotnet](/man/dotnet)(1), [make](/man/make)(1)"]],"e":["dotnet","make"],"x":"msbuild [solution.sln]"},"npm-star":{"n":"npm-star","t":"Mark a package as favorite on the npm registry","s":[["TAGLINE","Mark a package as favorite on the npm registry"],["TLDR","**Star a package**\n\n```npm star [package]```\n\n**Star multiple packages**\n\n```npm star [package1] [package2]```\n\n**View your starred packages**\n\n```npm stars```\n\n**Unstar a package**\n\n```npm unstar [package]```"],["SYNOPSIS","**npm** **star** _package_..."],["DESCRIPTION","**npm star** marks one or more packages as favorites on the npm registry. It is a way to show appreciation for packages you find useful. Requires npm authentication via **npm login**.\n\nStarred packages can be listed later with **npm stars**. To remove a star, use **npm unstar**."],["PARAMETERS","**--registry** _url_\n> Use the specified registry URL instead of the default.\n\n**--otp** _code_\n> Provide a one-time password for two-factor authentication."],["CAVEATS","Requires being logged in to npm (**npm login**). Stars are public and associated with your npm account."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-unstar](/man/npm-unstar)(1), [npm-stars](/man/npm-stars)(1), [npm-login](/man/npm-login)(1)"]],"e":["npm","npm-unstar","npm-stars","npm-login"],"x":"npm star [package]"},"oauth2c":{"n":"oauth2c","t":"OAuth2 CLI client","s":[["TAGLINE","OAuth2 CLI client"],["TLDR","**Get access token**\n\n```oauth2c [client_id] [client_secret] --token-url [url]```\n\n**Authorization code flow**\n\n```oauth2c --auth-url [url] --token-url [url] --redirect-uri [uri]```\n\n**Client credentials flow**\n\n```oauth2c [client_id] [client_secret] --token-url [url] --grant-type client_credentials```\n\n**With scopes**\n\n```oauth2c [client_id] [client_secret] --token-url [url] --scope \"[scope1 scope2]\"```"],["SYNOPSIS","**oauth2c** [_options_]"],["PARAMETERS","**--auth-url** _URL_\n> Authorization endpoint.\n\n**--token-url** _URL_\n> Token endpoint.\n\n**--redirect-uri** _URI_\n> Redirect URI.\n\n**--grant-type** _TYPE_\n> OAuth2 grant type.\n\n**--scope** _SCOPES_\n> Requested scopes.\n\n**--help**\n> Display help information."],["DESCRIPTION","**oauth2c** is an OAuth2 CLI client that obtains access tokens using various OAuth2 grant types. It supports authorization code, client credentials, device code, and other standard flows.\n\nThe tool is useful for testing OAuth2-protected APIs, debugging authentication flows, and scripting token acquisition. It opens a browser for authorization code flows and displays the obtained token on stdout."],["CAVEATS","Requires valid OAuth2 client credentials (client ID and secret). The authorization code flow opens a browser and starts a local server for the redirect URI. Tokens are printed to stdout and should be handled securely. Some grant types require the redirect URI to be pre-registered with the authorization server."],["HISTORY","**oauth2c** was created by **Cloudentity** to simplify OAuth2 token acquisition from the command line, supporting the full range of OAuth2 grant types."],["INSTALL","```brew: brew install oauth2c```\n\n```nix: nix profile install nixpkgs#oauth2c```"],["SEE ALSO","[curl](/man/curl)(1), [httpie](/man/httpie)(1)"]],"e":["curl","httpie"],"x":"oauth2c [client_id] [client_secret] --token-url [url]"},"openstack-flavor":{"n":"openstack-flavor","t":"manages instance flavors in OpenStack","s":[["TAGLINE","manages instance flavors in OpenStack"],["TLDR","**List flavors**\n\n```openstack flavor list```\n\n**Show flavor details**\n\n```openstack flavor show [m1.small]```\n\n**Create flavor with specific resources**\n\n```openstack flavor create --vcpus [2] --ram [4096] --disk [40] [custom.medium]```\n\n**Create a private flavor**\n\n```openstack flavor create --vcpus [4] --ram [8192] --disk [80] --private [custom.large]```\n\n**Delete flavor**\n\n```openstack flavor delete [custom.medium]```\n\n**Set extra property on a flavor**\n\n```openstack flavor set --property [hw:cpu_policy=dedicated] [custom.medium]```"],["SYNOPSIS","**openstack flavor** _command_ [_options_]"],["PARAMETERS","**list**\n> List available flavors.\n\n**show** _name_\n> Show flavor details.\n\n**create** _name_\n> Create flavor.\n\n**delete** _name_\n> Delete flavor.\n\n**set** _name_\n> Set flavor properties.\n\n**--vcpus** _num_\n> Number of vCPUs.\n\n**--ram** _mb_\n> RAM in megabytes.\n\n**--disk** _gb_\n> Root disk size in gigabytes.\n\n**--ephemeral** _gb_\n> Ephemeral disk size in gigabytes (default: 0).\n\n**--swap** _mb_\n> Swap space size in megabytes (default: 0).\n\n**--id** _id_\n> Unique flavor ID ('auto' generates a UUID, default: auto).\n\n**--public**\n> Make flavor accessible to all projects (default).\n\n**--private**\n> Make flavor accessible only to specific projects.\n\n**--property** _key=value_\n> Set extra specification property on the flavor."],["DESCRIPTION","**openstack flavor** manages instance flavors (size templates) in OpenStack Nova. Flavors define compute, memory and storage resources for virtual machines. Properties set via **--property** (aka extra_specs) control scheduler and hypervisor behavior — common keys include `hw:cpu_policy`, `hw:mem_page_size`, `hw:numa_nodes`, `quota:disk_read_bytes_sec` and `aggregate_instance_extra_specs:*`. The **set** command can also add or remove tenant access for private flavors via `--project` / `--project-domain`.\n\nPart of OpenStack unified CLI (`python-openstackclient`), superseding the old `nova flavor-*` commands."],["CAVEATS","Modifying a flavor is not possible in-place: change a property via **set**/**unset**, or delete and recreate for size changes. Running instances continue to use the original flavor. Admin role is required for create/delete and for setting most properties."],["SEE ALSO","[openstack](/man/openstack)(1), [openstack-server](/man/openstack-server)(1), [openstack-image](/man/openstack-image)(1), [openstack-volume](/man/openstack-volume)(1)"]],"e":["openstack","openstack-server","openstack-image","openstack-volume"],"x":"openstack flavor list"},"pacman-sync":{"n":"pacman-sync","t":"synchronizes packages from remote repositories","s":[["TAGLINE","synchronizes packages from remote repositories"],["TLDR","**Install** a package\n\n```sudo pacman -S package```\n\n**Sync** database and **upgrade**\n\n```sudo pacman -Syu```\n\nUpgrade with **new package**\n\n```sudo pacman -Syu --noconfirm package```\n\n**Search** package database\n\n```pacman -Ss \"search_pattern\"```\n\nShow package **information**\n\n```pacman -Si package```\n\n**Overwrite** conflicting files\n\n```sudo pacman -Syu --overwrite path/to/file```\n\n**Clean** package cache\n\n```sudo pacman -Sc```\n\nInstall specific **version**\n\n```sudo pacman -S package=version```"],["SYNOPSIS","**pacman -S** [_OPTIONS_] [_PACKAGE_...]"],["DESCRIPTION","**pacman -S** (sync) synchronizes packages from remote repositories. It downloads and installs packages, updates the system, and manages the local package cache on Arch Linux systems."],["PARAMETERS","**-S, --sync**\n> Synchronize packages from remote repositories\n\n**-y, --refresh**\n> Download fresh package databases from the server\n\n**-u, --sysupgrade**\n> Upgrade all out-of-date packages\n\n**-s, --search** _regex_\n> Search package names and descriptions\n\n**-i, --info**\n> Display detailed package information\n\n**-c, --clean**\n> Remove old packages from the cache\n\n**--noconfirm**\n> Skip confirmation prompts\n\n**--overwrite** _glob_\n> Overwrite conflicting files\n\n**--downloadonly**\n> Download packages without installing"],["CAVEATS","Always run **-Syu** together to ensure database and packages are in sync. Running **-Sy** alone can cause partial upgrade issues. Use **-Syyu** to force refresh even if databases appear up to date."],["HISTORY","**pacman** is the package manager for **Arch Linux** and its derivatives, designed with simplicity and speed as primary goals."],["INSTALL","```apt: sudo apt install pacman-package-manager```\n\n```dnf: sudo dnf install pacman```\n\n```pacman: sudo pacman -S pacman```\n\n```apk: sudo apk add pacman```\n\n```nix: nix profile install nixpkgs#pacman```"],["SEE ALSO","[pacman](/man/pacman)(8), [pacman-query](/man/pacman-query)(8), [pacman-remove](/man/pacman-remove)(8)"]],"e":["pacman","pacman-query","pacman-remove"],"x":"sudo pacman -S package"},"pg":{"n":"pg","t":"displays text files one page at a time, similar to more and less","s":[["TAGLINE","displays text files one page at a time, similar to more and less"],["TLDR","**View** a file page by page\n\n```pg [path/to/file]```\n\nDisplay **help**\n\n```pg -h```"],["SYNOPSIS","**pg** [**-**_number_] [**-p** _prompt_] [**-cefnrs**] [_file_...]"],["PARAMETERS","**-_number_**\n> Number of lines per page\n\n**-c**\n> Clear screen before displaying each page\n\n**-e**\n> Do not pause at end of file\n\n**-f**\n> Do not split long lines\n\n**-n**\n> Accept commands without newline\n\n**-p _string_**\n> Use string as the prompt\n\n**-r**\n> Allow restricted shell escape\n\n**-s**\n> Display messages in standout mode"],["DESCRIPTION","**pg** displays text files one page at a time, similar to more and less. It allows backward and forward navigation through files and supports searching within the displayed content.\n\nThe pager accepts various commands at its prompt: pressing Enter advances one line, space advances one page, and search patterns can be entered with slash notation."],["CAVEATS","Less feature-rich than modern pagers like less. Not installed by default on many systems. Primarily useful for compatibility with older scripts and workflows."],["HISTORY","**pg** is an older Unix pager that predates **more** and **less**. It was originally developed for System V Unix. While largely superseded by more capable pagers, it remains available in util-linux for compatibility."],["SEE ALSO","[less](/man/less)(1), [more](/man/more)(1), [cat](/man/cat)(1)"]],"e":["less","more","cat"],"x":"pg [path/to/file]"},"pgmenhance":{"n":"pgmenhance","t":"Edge-enhance a PGM grayscale image","s":[["TAGLINE","Edge-enhance a PGM grayscale image"],["TLDR","**Enhance image sharpness** with default intensity (9)\n\n```pgmenhance [input.pgm] > [output.pgm]```\n\n**Enhance with custom intensity** (1 = lowest, 9 = highest)\n\n```pgmenhance -[3] [input.pgm] > [output.pgm]```\n\n**Read from stdin**\n\n```cat [input.pgm] | pgmenhance -[5] > [output.pgm]```"],["SYNOPSIS","**pgmenhance** [_-N_] [_pgmfile_]"],["PARAMETERS","**-** _N_\n> Enhancement intensity from **1** (lowest) to **9** (highest). Default is **9**."],["DESCRIPTION","**pgmenhance** reads a PGM image as input, enhances the edges, and writes a PGM image as output. The enhancement technique is derived from Philip R. Thompson's **xim** program. Part of the **Netpbm** toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pgmnorm](/man/pgmnorm)(1), [pgmedge](/man/pgmedge)(1), [pamedge](/man/pamedge)(1)"]],"e":["pgmnorm","pgmedge","pamedge"],"x":"pgmenhance [input.pgm] > [output.pgm]"},"php-cs-fixer":{"n":"php-cs-fixer","t":"Automatically fix PHP coding standards issues","s":[["TAGLINE","Automatically fix PHP coding standards issues"],["TLDR","**Fix current directory**\n\n```php-cs-fixer fix```\n\n**Fix specific file**\n\n```php-cs-fixer fix [file.php]```\n\n**Dry run** showing what would change\n\n```php-cs-fixer fix --dry-run```\n\n**Show diff** of changes\n\n```php-cs-fixer fix --diff```\n\n**Use specific rule set**\n\n```php-cs-fixer fix --rules=@PSR12```\n\n**Fix with verbose output**\n\n```php-cs-fixer fix -v```\n\n**List files that need fixing**\n\n```php-cs-fixer list-files```"],["SYNOPSIS","**php-cs-fixer** _command_ [_options_] [_path_]"],["PARAMETERS","**fix** [_PATH_]\n> Fix coding standards in the given path or current directory.\n\n**check** [_PATH_]\n> Shorthand for `fix --dry-run`. Analyze without modifying files.\n\n**--dry-run**\n> Don't modify files, only show what would change.\n\n**--diff**\n> Show a diff of applied changes.\n\n**--rules** _RULES_\n> Coding standard rules or rule sets (e.g., @PSR12, @Symfony).\n\n**--config** _FILE_\n> Path to configuration file.\n\n**--allow-risky** _yes|no_\n> Allow risky rules that may change code behavior.\n\n**--using-cache** _yes|no_\n> Enable or disable caching (default: yes).\n\n**--cache-file** _FILE_\n> Path to cache file (default: .php-cs-fixer.cache).\n\n**--format** _FORMAT_\n> Output format (txt, json, checkstyle, gitlab, junit, xml).\n\n**--stop-on-violation**\n> Stop execution on first violation.\n\n**--path-mode** _override|intersection_\n> How to treat paths from config vs command arguments (default: override)."],["DESCRIPTION","**PHP CS Fixer** automatically fixes PHP code to follow coding standards. It supports PSR-1, PSR-2, PSR-12, Symfony, and custom rule sets. The tool parses PHP files, applies configured fixers, and rewrites files with corrected formatting."],["CONFIGURATION","**.php-cs-fixer.php** or **.php-cs-fixer.dist.php**\n> PHP configuration file defining rules, finders, and project-specific settings. Searched in the current directory by default."],["CAVEATS","Risky fixers may change code behavior and must be explicitly allowed. Configuration via PHP file provides more flexibility than command-line rules."],["INSTALL","```dnf: sudo dnf install php-cs-fixer```\n\n```zypper: sudo zypper install php-cs-fixer```\n\n```brew: brew install php-cs-fixer```"],["SEE ALSO","[phpcs](/man/phpcs)(1), [phpcbf](/man/phpcbf)(1), [php](/man/php)(1)"]],"e":["phpcs","phpcbf","php"],"x":"php-cs-fixer fix"},"pio-system":{"n":"pio-system","t":"Manage PlatformIO system information and maintenance","s":[["TAGLINE","Manage PlatformIO system information and maintenance"],["TLDR","**Show system information**\n\n```pio system info```\n\n**Prune unused data**\n\n```pio system prune```\n\n**Prune with dry run**\n\n```pio system prune --dry-run```"],["SYNOPSIS","**pio system** _command_ [_options_]"],["PARAMETERS","**info**\n> Show system information.\n\n**prune**\n> Remove unused data.\n\n**--dry-run**\n> Show what would be removed.\n\n**--force**\n> Skip confirmation."],["DESCRIPTION","**pio system** provides system information and maintenance commands. Shows PlatformIO installation details and cleans up unused packages and cache data."],["INSTALL","```aur: yay -S pio```"],["SEE ALSO","[pio-settings](/man/pio-settings)(1), [pio-upgrade](/man/pio-upgrade)(1)"]],"e":["pio-settings","pio-upgrade"],"x":"pio system info"},"pnmremap":{"n":"pnmremap","t":"Remap image colors to a specified palette","s":[["TAGLINE","Remap image colors to a specified palette"],["TLDR","**Remap image to palette**\n\n```pnmremap -mapfile=[palette.pnm] [input.pnm] > [output.pnm]```\n\n**Use Floyd-Steinberg dithering**\n\n```pnmremap -floyd -mapfile=[palette.pnm] [input.pnm] > [output.pnm]```\n\n**No dithering**\n\n```pnmremap -nofloyd -mapfile=[palette.pnm] [input.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmremap** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Input PNM file.\n\n**-mapfile** _FILE_\n> Color palette image.\n\n**-floyd**\n> Use Floyd-Steinberg dithering.\n\n**-nofloyd**\n> No dithering.\n\n**-norandom**\n> Disable random dithering.\n\n**-firstisdefault**\n> Use first palette color as default."],["DESCRIPTION","**pnmremap** replaces each pixel's color in a PNM image with the closest matching color from a specified palette image. This is useful for forcing an image to use only a specific set of colors, such as when targeting a limited-color display format.\n\nThe **-floyd** option enables Floyd-Steinberg error diffusion dithering for smoother visual results. The palette is provided via **-mapfile** as a PNM image where each unique pixel color represents one allowed color. Part of the Netpbm toolkit."],["CAVEATS","Requires palette file. Part of Netpbm suite."],["HISTORY","pnmremap was created as part of **Netpbm** for color palette mapping."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmquant](/man/ppmquant)(1), [pnmcolormap](/man/pnmcolormap)(1), [ppmtogif](/man/ppmtogif)(1)"]],"e":["ppmquant","pnmcolormap","ppmtogif"],"x":"pnmremap -mapfile=[palette.pnm] [input.pnm] > [output.pnm]"},"poster":{"n":"poster","t":"Scale PostScript images to poster size","s":[["TAGLINE","Scale PostScript images to poster size"],["TLDR","**Scale to poster size**\n\n```poster -p [A0] -s [1.0] [input.ps] > [output.ps]```\n\n**Create 2x2 tile poster**\n\n```poster -mA4 -p 2x2A4 [input.ps] > [output.ps]```\n\n**Scale to specific dimensions**\n\n```poster -m[A4] -p[100x80cm] [input.ps] > [output.ps]```\n\n**Cut marks for assembly**\n\n```poster -c -m[letter] -p[2x2letter] [input.ps] > [output.ps]```"],["SYNOPSIS","**poster** [_options_] _infile_ > _outfile_"],["PARAMETERS","**-p** _size_\n> Poster size.\n\n**-m** _size_\n> Media (paper) size.\n\n**-s** _scale_\n> Scale factor.\n\n**-c**\n> Add cut marks.\n\n**-o** _offset_\n> Offset pages.\n\n**-w** _width_\n> White margin."],["DESCRIPTION","**poster** scales PostScript images to larger sizes, splitting them across multiple pages for printing on regular printers and assembling into posters. It calculates the optimal tiling of the target poster size onto the available media size, handling page overlap and alignment marks.\n\nThe tool supports standard paper sizes (A0-A4, letter, legal), grid specifications (2x2A4, 3x3letter), and custom dimensions in centimeters or inches. Cut marks can be added to guide physical assembly of the printed tiles. An overlap margin ensures clean joins between adjacent pages."],["EXAMPLES","```bash"],["Scale to A0 on A4 sheets","poster -mA4 -pA0 image.ps > poster.ps"],["2x2 poster from A4","poster -mA4 -p2x2A4 document.ps > tiled.ps"],["With cut marks and overlap","poster -c -mA4 -pA1 -o5mm image.ps > poster.ps"],["Custom dimensions","poster -mA4 -p100x70cm diagram.ps > big.ps\n```"],["SIZE FORMATS","```\nStandard: A0, A1, A2, A3, A4, letter, legal\nGrid: 2x2A4, 3x3letter\nCustom: 100x70cm, 40x30in\n```"],["CAVEATS","Input must be PostScript. Use tools like pdf2ps for PDF. Output may need assembly instructions."],["HISTORY","poster was written by **Jos van Eijndhoven** for creating large format prints from standard printers."],["INSTALL","```apt: sudo apt install poster```\n\n```dnf: sudo dnf install poster```\n\n```zypper: sudo zypper install poster```\n\n```brew: brew install poster```"],["SEE ALSO","[ps2pdf](/man/ps2pdf)(1)"]],"e":["ps2pdf"],"x":"poster -p [A0] -s [1.0] [input.ps] > [output.ps]"},"preconv":{"n":"preconv","t":"Convert encoding for groff input processing","s":[["TAGLINE","Convert encoding for groff input processing"],["TLDR","**Convert encoding for groff**\n\n```preconv [file]```\n\n**Specify input encoding**\n\n```preconv -e [utf-8] [file]```\n\n**Debug mode**\n\n```preconv -d [file]```"],["SYNOPSIS","**preconv** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Input files.\n\n**-e** _ENCODING_\n> Input encoding.\n\n**-d**\n> Debug output.\n\n**-r**\n> Do not add .lf lines."],["DESCRIPTION","**preconv** is a groff preprocessor that converts input files from various character encodings (UTF-8, Latin-1, etc.) into a format that groff can process. It translates non-ASCII characters into groff escape sequences, enabling groff to handle Unicode and other multi-byte encodings.\n\nThe tool is typically invoked automatically by groff when needed, but can also be used explicitly in preprocessing pipelines. It detects the input encoding from BOM markers, coding tags, or locale settings."],["CAVEATS","Groff preprocessor. Usually called automatically."],["HISTORY","preconv was created as a **groff preprocessor** for encoding conversion."],["INSTALL","```apt: sudo apt install groff-base```\n\n```dnf: sudo dnf install groff-base```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[groff](/man/groff)(1), [troff](/man/troff)(1), [iconv](/man/iconv)(1)"]],"e":["groff","troff","iconv"],"x":"preconv [file]"},"ptpython3":{"n":"ptpython3","t":"Enhanced interactive Python 3 REPL","s":[["TAGLINE","Enhanced interactive Python 3 REPL"],["TLDR","**Start Python 3 REPL**\n\n```ptpython3```\n\n**Start with vi mode**\n\n```ptpython3 --vi```\n\n**Execute file then REPL**\n\n```ptpython3 -i [script.py]```"],["SYNOPSIS","**ptpython3** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Python file to execute before entering the REPL.\n\n**--vi**\n> Use Vi key bindings instead of the default Emacs bindings.\n\n**-i**, **--interactive**\n> Drop into an interactive REPL after executing _FILE_.\n\n**--asyncio**\n> Run an asyncio event loop so top-level _await_ expressions work.\n\n**--light-bg**\n> Use a color scheme tuned for terminals with a light background.\n\n**--dark-bg**\n> Use a color scheme tuned for terminals with a dark background.\n\n**--config-file** _PATH_\n> Load configuration from a non-default file.\n\n**--history-file** _PATH_\n> Use the specified file for input history.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**ptpython3** is the Python 3 variant of ptpython, an enhanced interactive REPL with syntax highlighting, autocompletion, multi-line editing, and vi/emacs key bindings built on the **prompt_toolkit** library.\n\nOn modern systems where Python 3 is the default, **ptpython** and **ptpython3** are typically identical. The separate binary exists for systems where both Python 2 and Python 3 are installed side by side."],["CONFIGURATION","**$PTPYTHON_CONFIG_HOME**\n> Override the directory ptpython searches for its configuration. Defaults to a platform-specific user config directory (e.g. **~/.config/ptpython/** on Linux).\n\n**$PYTHONSTARTUP**\n> Standard Python variable; the referenced file runs before the interactive prompt."],["CAVEATS","Python 3 variant of ptpython; on modern systems where Python 3 is the default the two binaries are equivalent. Configuration files are honored only when ptpython is launched directly — not when embedded inside another application."],["HISTORY","ptpython3 is the **Python 3 variant** of ptpython."],["INSTALL","```dnf: sudo dnf install ptpython3```"],["SEE ALSO","[ptpython](/man/ptpython)(1), [python3](/man/python3)(1), [ipython3](/man/ipython3)(1)"]],"e":["ptpython","python3","ipython3"],"x":"ptpython3"},"pulumi-logout":{"n":"pulumi-logout","t":"Remove stored Pulumi backend credentials","s":[["TAGLINE","Remove stored Pulumi backend credentials"],["TLDR","**Logout from current backend**\n\n```pulumi logout```\n\n**Logout from specific backend**\n\n```pulumi logout [url]```\n\n**Logout from all backends**\n\n```pulumi logout --all```"],["SYNOPSIS","**pulumi** **logout** [_options_] [_url_]"],["PARAMETERS","**--all**\n> Logout from all backends.\n\n**--local**\n> Logout from local backend."],["DESCRIPTION","**pulumi logout** removes stored credentials for a Pulumi backend. Does not affect deployed resources or state, only local authentication."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-login](/man/pulumi-login)(1)"]],"e":["pulumi","pulumi-login"],"x":"pulumi logout"},"rbash":{"n":"rbash","t":"Restricted mode of the Bash shell","s":[["TAGLINE","Restricted mode of the Bash shell"],["TLDR","**Start restricted shell**\n\n```rbash```\n\n**Run command in restricted mode**\n\n```rbash -c \"[command]\"```\n\n**Run script restricted**\n\n```rbash [script.sh]```"],["SYNOPSIS","**rbash** [_options_] [_script_]"],["PARAMETERS","**-c** _STRING_\n> Execute command string.\n\n**-i**\n> Interactive mode.\n\n**-l**\n> Login shell.\n\n**-r**\n> Restricted mode (default)."],["DESCRIPTION","**rbash** is a restricted mode of the Bash shell that limits user actions to prevent unauthorized system access. When invoked as rbash (or via **bash -r**), it disables directory changes with cd, prevents modification of PATH, SHELL, ENV, and BASH_ENV variables, and blocks output redirection with >, >>, and related operators.\n\nThe restrictions also prevent commands containing slashes (blocking direct paths like /bin/command), use of the exec builtin, and importing of functions from the environment. This makes it useful for providing limited shell access to untrusted users when combined with a carefully controlled PATH containing only approved commands."],["RESTRICTIONS","- No cd command\n- No PATH changes\n- No output redirection\n- No exec command\n- No command substitution assignment"],["CAVEATS","Can be escaped via scripts. Not security boundary. Use with other controls."],["HISTORY","**rbash** is bash started as restricted shell. It provides a limited environment for untrusted users."],["INSTALL","```apt: sudo apt install bash```\n\n```dnf: sudo dnf install bash```\n\n```pacman: sudo pacman -S bash```\n\n```zypper: sudo zypper install bash```\n\n```brew: brew install bash```\n\n```nix: nix profile install nixpkgs#bash```"],["SEE ALSO","[bash](/man/bash)(1), [chroot](/man/chroot)(1), [sudo](/man/sudo)(1)"]],"e":["bash","chroot","sudo"],"x":"rbash"},"reek":{"n":"reek","t":"Detect code smells in Ruby source files","s":[["TAGLINE","Detect code smells in Ruby source files"],["TLDR","**Check Ruby code for smells**\n\n```reek [file.rb]```\n\n**Check entire project**\n\n```reek```\n\n**Check with specific format**\n\n```reek --format [yaml] [file.rb]```\n\n**Show available smell types**\n\n```reek --smell-types```\n\n**Exclude specific smells**\n\n```reek --except [TooManyStatements] [file.rb]```"],["SYNOPSIS","**reek** [_options_] [_files_...]"],["PARAMETERS","**-f**, **--format** _format_\n> Output format (text, yaml, json, html).\n\n**--smell-types**\n> List available smells.\n\n**--except** _smells_\n> Exclude smells.\n\n**--only** _smells_\n> Check only specified smells.\n\n**-c**, **--config** _file_\n> Configuration file."],["DESCRIPTION","**Reek** is a static analysis tool that detects code smells in Ruby source files, reporting potential design problems such as long methods, feature envy, data clumps, and excessive parameter lists. It examines code structure and naming patterns to identify areas that may benefit from refactoring, based on principles from Martin Fowler's refactoring catalog.\n\nOutput can be formatted as text, YAML, JSON, or HTML for integration with CI pipelines and code review workflows. Individual smell types can be included or excluded with **--only** and **--except**, and project-wide configuration in **.reek.yml** allows tuning thresholds and excluding paths."],["EXAMPLES","```bash"],["Check file","reek app/models/user.rb"],["Check directory","reek app/"],["JSON output","reek --format json app/ > smells.json"],["List smell types","reek --smell-types"],["Specific smells only","reek --only \"TooManyStatements,LongMethod\" app/"],["With configuration","reek -c .reek.yml\n```"],["SMELL TYPES","```\nTooManyStatements  - Method too long\nFeatureEnvy        - Wrong class responsibility\nDataClump          - Data always together\nLongParameterList  - Too many parameters\nDuplicateMethodCall - Repeated calls\n```"],["CONFIGURATION (.reek.yml)","```yaml\ndetectors:\n  TooManyStatements:\n    max_statements: 10\n  exclude_paths:\n    - spec/\n```"],["CAVEATS","Ruby-specific. Some smells may be false positives. Configure per project."],["HISTORY","Reek was created by **Kevin Rutherford** as a Ruby code smell detector based on Martin Fowler's refactoring concepts."],["SEE ALSO","[rubocop](/man/rubocop)(1), [ruby](/man/ruby)(1)"]],"e":["rubocop","ruby"],"x":"reek [file.rb]"},"sbctl":{"n":"sbctl","t":"Manage UEFI Secure Boot keys and signing","s":[["TAGLINE","Manage UEFI Secure Boot keys and signing"],["TLDR","Show current **secure boot status**\n\n```sbctl status```\n\n**Create** custom secure boot keys\n\n```sbctl create-keys```\n\n**Enroll** keys with Microsoft certificates\n\n```sbctl enroll-keys -m```\n\n**List** files in the signing database\n\n```sbctl list-files```\n\n**Sign** an EFI binary and save to database\n\n```sbctl sign -s path/to/efi_binary```\n\n**Re-sign** all saved files\n\n```sbctl sign-all```\n\n**Verify** all EFI executables are signed\n\n```sbctl verify```"],["SYNOPSIS","**sbctl** _command_ [_options_]"],["PARAMETERS","**status**\n> Show secure boot status\n\n**create-keys**\n> Generate custom secure boot keys\n\n**enroll-keys**\n> Enroll keys into UEFI firmware\n\n**-m**, **--microsoft**\n> Include Microsoft UEFI vendor certificates\n\n**sign**\n> Sign EFI binary\n\n**-s**, **--save**\n> Save file to database for re-signing\n\n**sign-all**\n> Re-sign all saved files\n\n**verify**\n> Verify EFI executables are signed\n\n**list-files**\n> List files in the signing database\n\n**remove-file** _file_\n> Remove a file from the signing database\n\n**setup**\n> Automated setup of secure boot (create keys, sign, enroll)"],["DESCRIPTION","**sbctl** is a user-friendly secure boot key manager. It simplifies creating, enrolling, and managing custom secure boot keys and signing EFI binaries.\n\nCustom keys allow booting only signed kernels and bootloaders, enhancing system security."],["CONFIGURATION","**/usr/share/secureboot/**\n> Default directory storing generated secure boot keys (PK, KEK, db) and the database of signed files."],["CAVEATS","Not enrolling Microsoft certificates (the **-m** flag) can brick some systems with Option ROMs that require Microsoft-signed drivers. Keys are stored in /usr/share/secureboot by default. Requires root privileges for all operations."],["HISTORY","Developed as a user-friendly alternative to manual secure boot key management tools."],["INSTALL","```pacman: sudo pacman -S sbctl```\n\n```apk: sudo apk add sbctl```\n\n```zypper: sudo zypper install sbctl```\n\n```nix: nix profile install nixpkgs#sbctl```"],["SEE ALSO","[mokutil](/man/mokutil)(1), [efibootmgr](/man/efibootmgr)(8)"]],"e":["mokutil","efibootmgr"],"x":"sbctl status"},"tex":{"n":"tex","t":"Original TeX typesetting engine","s":[["TAGLINE","Original TeX typesetting engine"],["TLDR","**Compile document**\n\n```tex [document.tex]```\n\n**Compile with jobname**\n\n```tex -jobname=[output] [document.tex]```\n\n**Interaction mode**\n\n```tex -interaction=nonstopmode [document.tex]```\n\n**Initialize format**\n\n```tex -ini [format.tex]```"],["SYNOPSIS","**tex** [_-interaction mode_] [_-jobname name_] [_options_] _file_"],["PARAMETERS","**-interaction** _MODE_\n> Set interaction mode: batchmode, nonstopmode, scrollmode, or errorstopmode.\n\n**-jobname** _NAME_\n> Set the job name instead of deriving it from the input file name.\n\n**-ini**\n> Start in INI mode for dumping formats; no format is preloaded.\n\n**-output-directory** _DIR_\n> Write output files in DIR instead of the current directory.\n\n**-halt-on-error**\n> Stop processing at the first error.\n\n**-file-line-error**\n> Print error messages in file:line:error format.\n\n**-recorder**\n> Enable the filename recorder, creating a .fls file listing opened files.\n\n**-fmt** _NAME_\n> Use NAME as the format instead of the default."],["DESCRIPTION","**tex** is the original TeX typesetting engine created by Donald Knuth. It reads plain TeX source files and produces DVI (Device Independent) output, which can then be converted to PostScript, PDF, or other formats using tools like dvips or dvipdfm.\n\nTeX uses the plain TeX macro format by default, which provides basic typesetting commands without the higher-level abstractions of LaTeX. It excels at mathematical typesetting, producing publication-quality output for equations, formulas, and technical documents. TeX serves as the foundation upon which LaTeX, ConTeXt, and other macro packages are built."],["CAVEATS","Produces DVI output only; use pdftex or pdflatex for direct PDF output. Uses plain TeX macros by default without higher-level LaTeX abstractions."],["HISTORY","**TeX** was created by **Donald Knuth** starting in 1977 for typesetting his books, especially mathematical content."],["INSTALL","```apt: sudo apt install texlive-binaries```\n\n```dnf: sudo dnf install texlive```\n\n```apk: sudo apk add texlive```\n\n```zypper: sudo zypper install texlive```\n\n```brew: brew install texlive```"],["SEE ALSO","[latex](/man/latex)(1), [pdftex](/man/pdftex)(1), [xetex](/man/xetex)(1), [dvips](/man/dvips)(1)"]],"e":["latex","pdftex","xetex","dvips"],"x":"tex [document.tex]"},"textql":{"n":"textql","t":"Execute SQL queries on CSV files","s":[["TAGLINE","Execute SQL queries on CSV files"],["TLDR","**Query CSV**\n\n```textql -sql \"[SELECT * FROM file]\" [data.csv]```\n\n**With header**\n\n```textql -header -sql \"[SELECT name FROM file]\" [data.csv]```\n\n**Custom delimiter**\n\n```textql -dlm=\";\" -sql \"[query]\" [data.csv]```\n\n**Save to SQLite**\n\n```textql -save-to [output.db] [data.csv]```\n\n**Output to file**\n\n```textql -output-file [result.csv] -sql \"[query]\" [data.csv]```"],["SYNOPSIS","**textql** [_-sql query_] [_-header_] [_-dlm delim_] [_options_] _files_"],["PARAMETERS","**-sql** _QUERY_\n> SQL query.\n\n**-header**\n> First row is header.\n\n**-dlm** _CHAR_\n> Field delimiter.\n\n**-save-to** _FILE_\n> Save to SQLite.\n\n**-output-file** _FILE_\n> Output file.\n\n**-output-dlm** _CHAR_\n> Output delimiter."],["DESCRIPTION","**textql** allows running SQL queries against structured text files such as CSV and TSV. It loads files into an in-memory SQLite database, treating each file as a table, and executes standard SQL queries against them.\n\nWhen **-header** is specified, the first row is used for column names; otherwise columns are named c0, c1, etc. Multiple files can be loaded simultaneously and joined using SQL JOIN syntax. The **-save-to** option persists the imported data to a SQLite database file for further analysis. Custom delimiters are supported with the **-dlm** flag."],["CAVEATS","Memory for large files. SQLite limitations. Go-based tool."],["HISTORY","**textql** was created to execute SQL queries against structured text files like CSV using SQLite."],["INSTALL","```apt: sudo apt install textql```\n\n```nix: nix profile install nixpkgs#textql```"],["SEE ALSO","[sqlite3](/man/sqlite3)(1), [csvq](/man/csvq)(1), [q](/man/q)(1), [miller](/man/miller)(1)"]],"e":["sqlite3","csvq","q","miller"],"x":"textql -sql \"[SELECT * FROM file]\" [data.csv]"},"touch":{"n":"touch","t":"Create files and update timestamps","s":[["TAGLINE","Create files and update timestamps"],["TLDR","**Create a new empty file**\n\n```touch [file]```\n\n**Create multiple files**\n\n```touch [file1] [file2] [file3]```\n\n**Update access and modification time** to current time\n\n```touch [existing_file]```\n\n**Set specific modification time**\n\n```touch -t [[CC]YY]MMDDhhmm[.ss] [file]```\n\n**Set time from another file**\n\n```touch -r [reference_file] [file]```\n\n**Change only access time**\n\n```touch -a [file]```\n\n**Change only modification time**\n\n```touch -m [file]```\n\n**Do not create file** if it doesn't exist\n\n```touch -c [file]```"],["SYNOPSIS","**touch** [_options_] _file_..."],["PARAMETERS","**-a**\n> Change only access time\n\n**-m**\n> Change only modification time\n\n**-c**, **--no-create**\n> Do not create file if it doesn't exist\n\n**-d** _string_, **--date**=_string_\n> Parse string and use instead of current time\n\n**-r** _file_, **--reference**=_file_\n> Use times from reference file\n\n**-t** _stamp_\n> Use timestamp [[CC]YY]MMDDhhmm[.ss] instead of current time\n\n**-h**, **--no-dereference**\n> Affect symbolic link instead of referenced file\n\n**--time**=_WORD_\n> Change the specified time: _access_, _atime_, _use_ are equivalent to **-a**; _modify_, _mtime_ are equivalent to **-m**\n\n**-f**\n> Ignored; provided for compatibility with BSD versions of touch"],["DESCRIPTION","**touch** updates file access and modification timestamps. If the file doesn't exist, touch creates an empty file (unless **-c** or **-h** is specified).\n\nCommon uses include creating empty files, updating timestamps to trigger make rebuilds, and setting specific times for testing or archival purposes.\n\nTime can be specified in several formats: **-t** for precise timestamps, **-d** for human-readable strings (\"yesterday\", \"2 hours ago\"), or **-r** to copy times from another file.\n\nWithout **-a** or **-m**, both access time (atime) and modification time (mtime) are updated. Change time (ctime) is always updated by the kernel when timestamps change."],["DATE STRING EXAMPLES","```touch -d \"2024-01-15 10:30:00\" file```\n```touch -d \"yesterday\" file```\n```touch -d \"2 weeks ago\" file```\n```touch -d \"next monday\" file```"],["CAVEATS","Creating files requires write permission in the directory. Updating timestamps requires write permission on the file or ownership.\n\nSome filesystems mount with **noatime** or **relatime**, which affects how access times are recorded. This doesn't affect touch but may affect programs relying on atime.\n\nTouch cannot change ctime (inode change time); it's always set to current time by the kernel when metadata changes."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[stat](/man/stat)(1), [date](/man/date)(1), [ls](/man/ls)(1), [mkdir](/man/mkdir)(1), [chmod](/man/chmod)(1)"]],"e":["stat","date","ls","mkdir","chmod"],"x":"touch [file]"},"uconv":{"n":"uconv","t":"ICU character encoding converter and transliterator","s":[["TAGLINE","ICU character encoding converter and transliterator"],["TLDR","**Convert file from one encoding to another**\n\n```uconv -f [ISO-8859-1] -t [UTF-8] [input.txt] -o [output.txt]```\n\n**List all available encodings**\n\n```uconv -l```\n\n**Convert and transliterate to ASCII**\n\n```uconv -f [UTF-8] -t [US-ASCII] -x [Any-Latin; Latin-ASCII] [input.txt]```\n\n**List available transliterators**\n\n```uconv -L```\n\n**Show default encoding**\n\n```uconv --default-code```\n\n**Convert with substitute callback for unmappable chars**\n\n```uconv -f [UTF-8] -t [ISO-8859-1] --callback substitute [input.txt]```"],["SYNOPSIS","**uconv** [_options_] [-f _from_] [-t _to_] [_file_...]"],["PARAMETERS","**-f**, **--from-code** _encoding_\n> Source encoding.\n\n**-t**, **--to-code** _encoding_\n> Target encoding.\n\n**-o**, **--output** _file_\n> Write output to file instead of stdout.\n\n**-x** _transliteration_\n> Apply transliteration rules.\n\n**-l**, **--list**\n> List all available encodings.\n\n**-L**, **--list-transliterators**\n> List available transliterators.\n\n**--default-code**\n> Show default system encoding.\n\n**--from-callback** _name_\n> Callback for errors when converting from source encoding to Unicode.\n\n**--to-callback** _name_\n> Callback for errors when converting from Unicode to target encoding.\n\n**--callback** _name_\n> Set both --from-callback and --to-callback to the same value. Values: substitute, skip, stop, escape-unicode.\n\n**-c**\n> Omit invalid characters from output (shorthand for --to-callback skip).\n\n**--add-signature**\n> Add a U+FEFF BOM to the output if the target encoding supports it.\n\n**--remove-signature**\n> Remove a U+FEFF BOM from the input.\n\n**-s**, **--silent**\n> Suppress messages during execution.\n\n**-v**, **--verbose**\n> Display extra informative messages during execution.\n\n**-h**, **--help**\n> Display help.\n\n**-V**, **--version**\n> Display version."],["DESCRIPTION","**uconv** converts text between character encodings using the ICU (International Components for Unicode) library. It supports over 200 encodings and more than 1000 aliases, making it suitable for complex internationalization tasks.\n\nBeyond simple transcoding, uconv provides transliteration capabilities to convert text between scripts (e.g., Cyrillic to Latin) without translation. Multiple transliterators can be chained with semicolons.\n\nError callbacks control handling of characters that cannot be mapped. Callbacks can be set independently for the from-encoding step (--from-callback) and the to-encoding step (--to-callback), or both at once with --callback. Options include substitute (replacement character), skip, stop (default), and escape-unicode."],["CAVEATS","Part of ICU developer tools, not installed by default on all systems. Transliteration rules use ICU format syntax. Some encoding conversions may lose information. Large files processed in memory."],["HISTORY","**uconv** is part of the ICU (International Components for Unicode) project, originally developed at IBM and now maintained by the Unicode Consortium. ICU provides comprehensive Unicode support for software internationalization, with uconv offering command-line access to its conversion capabilities."],["INSTALL","```apt: sudo apt install icu-devtools```\n\n```dnf: sudo dnf install icu```\n\n```pacman: sudo pacman -S icu```\n\n```apk: sudo apk add icu```\n\n```zypper: sudo zypper install icu```\n\n```nix: nix profile install nixpkgs#icu```"],["SEE ALSO","[iconv](/man/iconv)(1), [recode](/man/recode)(1), [file](/man/file)(1), [locale](/man/locale)(1)"]],"e":["iconv","recode","file","locale"],"x":"uconv -f [ISO-8859-1] -t [UTF-8] [input.txt] -o [output.txt]"},"unsetopt":{"n":"unsetopt","t":"Disable zsh shell options","s":[["TAGLINE","Disable zsh shell options"],["TLDR","**Disable option**\n\n```unsetopt [optionname]```\n\n**Disable glob**\n\n```unsetopt glob```\n\n**Disable extended glob**\n\n```unsetopt extendedglob```\n\n**Show all options**\n\n```unsetopt```"],["SYNOPSIS","**unsetopt** [_options_]"],["PARAMETERS","_option_\n> Option to disable."],["COMMON OPTIONS","- **glob** - Filename globbing\n- **extendedglob** - Extended patterns\n- **nomatch** - Error on no match\n- **correct** - Command correction"],["DESCRIPTION","**unsetopt** is a zsh builtin that disables shell options, serving as the counterpart to **setopt**. It controls various aspects of zsh behavior including filename globbing, command correction, history handling, and prompt expansion.\n\nWhen called without arguments, unsetopt displays all options that are currently unset. Option names are case-insensitive and underscores in option names are ignored, so **extendedglob**, **EXTENDED_GLOB**, and **ExtendedGlob** all refer to the same option.\n\nChanges only affect the current shell session unless added to zsh configuration files like ~/.zshrc. This command is specific to zsh and has no equivalent in bash, which uses **shopt -u** for a similar purpose with different option names."],["CAVEATS","Zsh only. Use setopt to enable. See zshoptions(1)."],["HISTORY","**unsetopt** is a zsh builtin for disabling shell options, counterpart to setopt."],["SEE ALSO","[setopt](/man/setopt)(1), [zsh](/man/zsh)(1)"]],"e":["setopt","zsh"],"x":"unsetopt [optionname]"},"vela":{"n":"vela","t":"KubeVela application delivery CLI","s":[["TAGLINE","KubeVela application delivery CLI"],["TLDR","**Deploy application** from file\n\n```vela up -f [app.yaml]```\n\n**Show application status**\n\n```vela status [appname]```\n\n**List applications**\n\n```vela ls```\n\n**Delete application**\n\n```vela delete [appname]```\n\n**Show logs** for an application\n\n```vela logs [appname]```\n\n**Port-forward** to an application\n\n```vela port-forward [appname]```\n\n**Execute command** in application container\n\n```vela exec [appname] -- [command]```\n\n**Install KubeVela** on a Kubernetes cluster\n\n```vela install```"],["SYNOPSIS","**vela** _command_ [_options_] [_args_]"],["PARAMETERS","**up**\n> Create or update an application from file or URL.\n\n**status**\n> Show application status and health.\n\n**ls**\n> List all applications.\n\n**delete**\n> Remove an application.\n\n**logs**\n> View application logs.\n\n**exec**\n> Execute a command in an application container.\n\n**port-forward**\n> Forward local ports to an application container or service.\n\n**install**\n> Install KubeVela on a Kubernetes cluster.\n\n**show**\n> Show reference documentation for components, traits, or workflow types.\n\n**components**\n> List available component types.\n\n**workflow**\n> Operate application delivery workflows (suspend, resume, restart, rollback).\n\n**version**\n> Print version information.\n\n**-n**, **--namespace** _NAMESPACE_\n> Specify the namespace scope for the request.\n\n**-e**, **--env** _ENV_\n> Specify environment name for the application.\n\n**-y**, **--yes**\n> Assume yes for all prompts."],["DESCRIPTION","**vela** is the command-line client for KubeVela, an application delivery platform built on the Open Application Model (OAM). It manages the full lifecycle of applications on Kubernetes, from deployment and scaling to monitoring and rollback.\n\nApplications are defined with components, traits, and policies that abstract away infrastructure complexity. The platform supports multi-cluster deployment, allowing applications to be distributed across different Kubernetes clusters and cloud providers from a single definition."],["CAVEATS","Requires a running Kubernetes cluster with KubeVela installed. Familiarity with Kubernetes concepts and the Open Application Model (OAM) is recommended. Multi-cluster features require additional setup."],["HISTORY","**KubeVela** was created by the Alibaba Cloud team and accepted as a **CNCF sandbox project** in 2021, later graduating to incubation level. It implements the Open Application Model (OAM) specification to simplify application delivery on Kubernetes."],["SEE ALSO","[kubectl](/man/kubectl)(1), [helm](/man/helm)(1), [argocd](/man/argocd)(1)"]],"e":["kubectl","helm","argocd"],"x":"vela up -f [app.yaml]"},"webtorrent":{"n":"webtorrent","t":"Streaming torrent client for the terminal","s":[["TAGLINE","Streaming torrent client for the terminal"],["TLDR","**Download torrent**\n\n```webtorrent download \"[magnet:?xt=...]\"```\n\n**Download to directory**\n\n```webtorrent download [file.torrent] -o [./downloads]```\n\n**Stream to player**\n\n```webtorrent download [magnet] --vlc```\n\n**Stream to Chromecast**\n\n```webtorrent download [magnet] --chromecast```\n\n**Seed file**\n\n```webtorrent seed [file.mp4]```\n\n**Show torrent info**\n\n```webtorrent info [file.torrent]```\n\n**Create torrent**\n\n```webtorrent create [file.mp4]```"],["SYNOPSIS","**webtorrent** _command_ [_--vlc_|_--mpv_] [_-o dir_] [_options_] _torrent_"],["PARAMETERS","**download** _TORRENT_\n> Download torrent.\n\n**seed** _FILE_\n> Seed file.\n\n**info** _TORRENT_\n> Show info.\n\n**create** _FILE_\n> Create torrent.\n\n**-o**, **--out** _DIR_\n> Output directory.\n\n**--vlc**\n> Open in VLC.\n\n**--mpv**\n> Open in mpv.\n\n**--chromecast**\n> Cast to Chromecast.\n\n**--airplay**\n> Cast to AirPlay.\n\n**-s**, **--select** _N_\n> Select file index."],["DESCRIPTION","**webtorrent** is a torrent client that can download files to disk or stream them directly to media players like VLC and mpv while the download is still in progress. It supports both traditional BitTorrent peers and WebRTC-based browser peers.\n\nThe download command saves torrent contents to a specified directory, while streaming options like --vlc or --mpv open the media in the corresponding player as data arrives. Casting to Chromecast and AirPlay devices is also supported for streaming to TV screens.\n\nThe seed command shares local files over the BitTorrent network, and the create command generates new .torrent files. The tool accepts magnet links, torrent files, and info hashes as input."],["CAVEATS","Some trackers not supported. Streaming needs sufficient bandwidth. Browser version is different."],["HISTORY","**WebTorrent** was created by **Feross Aboukhadijeh** to bring torrents to browsers. The CLI extends functionality to Node.js."],["SEE ALSO","[transmission-cli](/man/transmission-cli)(1), [aria2c](/man/aria2c)(1), [vlc](/man/vlc)(1)"]],"e":["transmission-cli","aria2c","vlc"],"x":"webtorrent download \"[magnet:?xt=...]\""},"winicontoppm":{"n":"winicontoppm","t":"Convert Windows icons to PPM format","s":[["TAGLINE","Convert Windows icons to PPM format"],["TLDR","**Convert Windows icon to PPM**\n\n```winicontoppm [input.ico] > [output.ppm]```\n\n**Extract specific image**\n\n```winicontoppm -image [0] [input.ico] > [output.ppm]```"],["SYNOPSIS","**winicontoppm** [_options_] [_file_]"],["PARAMETERS","**-image** _n_\n> Extract specific image index.\n\n**-verbose**\n> Print icon information."],["DESCRIPTION","**winicontoppm** converts Windows ICO files to PPM format. Does not preserve transparency; use winicontopam for alpha channel. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtowinicon](/man/ppmtowinicon)(1), [winicontopam](/man/winicontopam)(1)"]],"e":["ppmtowinicon","winicontopam"],"x":"winicontoppm [input.ico] > [output.ppm]"},"wp":{"n":"wp","t":"WordPress command-line management tool","s":[["TAGLINE","WordPress command-line management tool"],["TLDR","**Show WordPress info**\n\n```wp --info```\n\n**Update WordPress core**\n\n```wp core update```\n\n**Install plugin**\n\n```wp plugin install [plugin-name]```\n\n**Activate plugin**\n\n```wp plugin activate [plugin-name]```\n\n**Install theme**\n\n```wp theme install [theme-name]```\n\n**Create user**\n\n```wp user create [username] [email@example.com] --role=[editor]```\n\n**Search/replace in database**\n\n```wp search-replace [old-url] [new-url]```\n\n**Export database**\n\n```wp db export [backup.sql]```"],["SYNOPSIS","**wp** _command_ [_subcommand_] [_options_]"],["PARAMETERS","**core**\n> WordPress core installation and updates.\n\n**plugin**\n> Plugin management (install, activate, deactivate, update, delete).\n\n**theme**\n> Theme management (install, activate, update, delete).\n\n**user**\n> User management (create, list, update, delete).\n\n**db**\n> Database operations (export, import, query, optimize, repair).\n\n**search-replace** _old_ _new_\n> Search and replace strings in the database.\n\n**cache**\n> Object cache commands (flush, type).\n\n**config**\n> Configuration file management (create, get, set).\n\n**post**\n> Post management (create, list, update, delete).\n\n**media**\n> Media attachment management (import, regenerate).\n\n**--path=**_DIR_\n> Path to WordPress installation.\n\n**--url=**_URL_\n> Target site URL (for multisite).\n\n**--skip-plugins**\n> Skip loading all plugins.\n\n**--skip-themes**\n> Skip loading all themes.\n\n**--user=**_USER_\n> Run command as a specific user.\n\n**--allow-root**\n> Allow running as root (not recommended)."],["DESCRIPTION","**wp** (WP-CLI) is the official command-line interface for WordPress administration. It provides complete control over WordPress installations from the terminal, including core updates, plugin and theme management, user administration, and database operations.\n\nCommon workflows include installing and activating plugins, creating users with specific roles, exporting and importing databases, and performing search-replace operations across the database for tasks like domain migration. The tool can also manage posts, pages, comments, and virtually every aspect of a WordPress site without needing a web browser."],["CAVEATS","Requires an existing WordPress installation and PHP CLI. Some operations need direct database access. Running as root is discouraged unless **--allow-root** is specified. For multisite, use **--url** to target a specific site."],["HISTORY","**WP-CLI** was initiated by **Andreas Creten** in **2011** and later maintained by **Daniel Bachhuber**. It became the official command-line tool for WordPress, endorsed by the WordPress project. The tool is actively maintained by the open-source community."],["SEE ALSO","[php](/man/php)(1), [mysql](/man/mysql)(1)"]],"e":["php","mysql"],"x":"wp --info"},"xhost":{"n":"xhost","t":"Manage X server access control","s":[["TAGLINE","Manage X server access control"],["TLDR","**Display** the current access control list\n\n```xhost```\n\n**Allow** a specific host to connect\n\n```xhost +[hostname]```\n\n**Deny** a specific host from connecting\n\n```xhost -[hostname]```\n\n**Allow** all hosts to connect (disable access control - insecure)\n\n```xhost +```\n\n**Deny** all hosts except those explicitly allowed\n\n```xhost -```\n\n**Remove** a specific user or address using family prefix\n\n```xhost -[family:name]```"],["SYNOPSIS","**xhost** [[**+**|**-**]_name_...]"],["PARAMETERS","**+_hostname_**\n> Add host to access list\n\n**-_hostname_**\n> Remove host from access list\n\n**+**\n> Disable access control (allow all)\n\n**-**\n> Enable access control (restrict to list)\n\n**+si:localuser:_user_**\n> Allow local user\n\n**-si:localuser:_user_**\n> Deny local user\n\n**+inet:_hostname_**\n> Allow by IP family"],["DESCRIPTION","**xhost** manages the X server access control list. It controls which hosts and users can connect to the X display server.\n\nFamily prefixes include: **inet** (IPv4), **inet6** (IPv6), **si** (server interpreted), **localuser** (local user), and **local** (local connections)."],["CAVEATS","Using **xhost +** is a security risk as it allows any host to connect. For secure remote X access, use SSH X forwarding instead. Access changes are not persistent across X server restarts."],["HISTORY","**xhost** is one of the original X Window System utilities, providing basic access control since the early days of X11."],["INSTALL","```apt: sudo apt install x11-xserver-utils```\n\n```dnf: sudo dnf install xhost```\n\n```apk: sudo apk add xhost```\n\n```zypper: sudo zypper install xhost```\n\n```nix: nix profile install nixpkgs#xhost```"],["SEE ALSO","[xauth](/man/xauth)(1), [ssh](/man/ssh)(1)"]],"e":["xauth","ssh"],"x":"xhost"},"xzegrep":{"n":"xzegrep","t":"Search xz files with extended regex","s":[["TAGLINE","Search xz files with extended regex"],["TLDR","**Search** an xz/lzma file using extended regex\n\n```xzegrep \"[pattern]\" [file.xz]```\n\n**Case-insensitive** search\n\n```xzegrep -i \"[pattern]\" [file.xz]```\n\n**Search recursively** through compressed files\n\n```xzegrep -r \"[pattern]\" [directory]```\n\n**Show only matching files**, not lines\n\n```xzegrep -l \"[pattern]\" [*.xz]```\n\n**Show line numbers** of matches\n\n```xzegrep -n \"[pattern]\" [file.xz]```\n\n**Print N lines of context** around each match\n\n```xzegrep -C [3] \"[pattern]\" [file.xz]```"],["SYNOPSIS","**xzegrep** [_grep-options_] _pattern_ [_file_...]"],["PARAMETERS","**-i**, **--ignore-case**\n> Ignore case distinctions.\n\n**-l**, **--files-with-matches**\n> Print only the names of files containing matches.\n\n**-n**, **--line-number**\n> Prefix each line with its line number.\n\n**-c**, **--count**\n> Print only the count of matching lines per file.\n\n**-v**, **--invert-match**\n> Select non-matching lines.\n\n**-h**, **--no-filename**\n> Suppress filename prefix in output.\n\n**-H**, **--with-filename**\n> Always print filename prefix.\n\n**-r**, **--recursive**\n> Recursively search directories.\n\n**-A** _N_ / **-B** _N_ / **-C** _N_\n> Print N lines after / before / around each match.\n\n**-q**, **--quiet**\n> Suppress all normal output; exit 0 if any line matches."],["DESCRIPTION","**xzegrep** invokes **grep -E** on the decompressed contents of files compressed with **xz**, **lzma**, **gzip**, or **bzip2** (detected by extension). It is the **xz**-utils wrapper equivalent to **zegrep** for gzip files: a convenience shortcut for **xzgrep -E**, taking an extended regular expression and forwarding all other arguments to **grep**.\n\nIf no file is given, xzegrep reads from standard input. Most options accepted by **grep** can be passed through."],["CAVEATS","The whole file must be decompressed in memory or piped, so searching very large archives is not faster than running **xz -dc | grep -E** manually. On many distributions **xzegrep** is being deprecated in favour of **xzgrep -E**."],["INSTALL","```apt: sudo apt install xz-utils```\n\n```dnf: sudo dnf install xz```\n\n```pacman: sudo pacman -S xz```\n\n```apk: sudo apk add xz```\n\n```zypper: sudo zypper install xz```\n\n```brew: brew install xz```\n\n```nix: nix profile install nixpkgs#xz```"],["SEE ALSO","[xzgrep](/man/xzgrep)(1), [xz](/man/xz)(1), [egrep](/man/egrep)(1), [grep](/man/grep)(1)"]],"e":["xzgrep","xz","egrep","grep"],"x":"xzegrep \"[pattern]\" [file.xz]"},"yatto":{"n":"yatto","t":"Interactive Git-backed todo list for the terminal","s":[["TAGLINE","Interactive Git-backed todo list for the terminal"],["TLDR","**Launch the interactive TUI**\n\n```yatto```\n\n**Add a task**\n\n```yatto add \"[Fix login bug]\" --project [myapp] --priority high```\n\n**List open tasks**\n\n```yatto list --status open```"],["SYNOPSIS","**yatto** [_command_] [_options_]"],["DESCRIPTION","**yatto** is a terminal task manager that stores each task as a JSON file and manages the task directory as a Git or Jujutsu repository. Every change is versioned, enabling sync and collaboration via shared repositories. Supports due dates, priorities, statuses, and markdown descriptions."],["HISTORY","**yatto** was created by **handlebargh** and is written in **Go**."],["INSTALL","```nix: nix profile install nixpkgs#yatto```"],["SEE ALSO","[taskwarrior](/man/taskwarrior)(1), [todoman](/man/todoman)(1)"]],"e":["taskwarrior","todoman"],"x":"yatto"}});
