window.LCL_BUCKET(115,{"apt-cache":{"n":"apt-cache","t":"Query the APT package cache","s":[["TAGLINE","Query the APT package cache"],["TLDR","**Search** for a package in your current sources\n\n```apt-cache search [query]```\n\n**Search** only in package names\n\n```apt-cache search --names-only [query]```\n\nShow **information** about a package\n\n```apt-cache show [package]```\n\nShow whether a package is **installed and up to date**\n\n```apt-cache policy [package]```\n\nShow **dependencies** for a package\n\n```apt-cache depends [package]```\n\nShow packages that **depend on** a particular package\n\n```apt-cache rdepends [package]```\n\nShow **available versions** in a table format\n\n```apt-cache madison [package]```"],["SYNOPSIS","**apt-cache** [_options_] _command_ [_arguments_]"],["DESCRIPTION","**apt-cache** queries the local APT package cache to search for packages, display detailed package information, and analyze dependency relationships. It reads the package index files downloaded by **apt update** and provides fast, offline access to package metadata without contacting remote repositories.\n\nCommon operations include searching for packages by name or description, inspecting version and dependency details, checking installation policies, and listing reverse dependencies. The output is suitable for scripting and can help diagnose dependency conflicts or determine which repository provides a given package."],["PARAMETERS","**search** _regex_\n> Search for packages matching the POSIX regex in names and descriptions\n\n**show** _package_\n> Show detailed package record for a package\n\n**showpkg** _package_\n> Show general information including versions and dependencies\n\n**showsrc** _package_\n> Show all source package records for a package\n\n**policy** [_package_]\n> Show policy settings, installation status, and repository priorities\n\n**depends** _package_\n> Show dependencies for a package\n\n**rdepends** _package_\n> Show reverse dependencies (packages that depend on this one)\n\n**madison** _package_\n> Show available versions in a tabular format\n\n**pkgnames** [_prefix_]\n> List all package names in the cache, optionally filtered by prefix\n\n**stats**\n> Show cache statistics\n\n**dump**\n> Show a short listing of every package in the cache\n\n**unmet**\n> Show a summary of all unmet dependencies in the cache\n\n**-f, --full**\n> Print full package records when searching\n\n**-n, --names-only**\n> Only search package names, not descriptions\n\n**-i, --important**\n> Print only Depends and Pre-Depends relations (for use with depends and unmet)\n\n**--installed**\n> Limit output to currently installed packages\n\n**--recurse**\n> Make depends and rdepends recursive"],["CONFIGURATION","**/etc/apt/sources.list**\n> Main file listing configured package repositories.\n\n**/etc/apt/sources.list.d/**\n> Directory for additional repository source files.\n\n**/etc/apt/preferences.d/**\n> Directory for package pinning and priority preferences."],["CAVEATS","This tool operates on the local cache; run **apt update** first to ensure the cache is current."],["HISTORY","Part of the **APT** (Advanced Package Tool) suite developed for Debian-based systems in the late 1990s."],["INSTALL","```apt: sudo apt install apt```\n\n```dnf: sudo dnf install apt```\n\n```pacman: sudo pacman -S apt```\n\n```apk: sudo apk add apt```\n\n```brew: brew install apt```\n\n```nix: nix profile install nixpkgs#apt```"],["SEE ALSO","[apt](/man/apt)(8), [apt-get](/man/apt-get)(8), [aptitude](/man/aptitude)(8), [dpkg](/man/dpkg)(1)"]],"e":["apt","apt-get","aptitude","dpkg"],"x":"apt-cache search [query]"},"apt-cdrom":{"n":"apt-cdrom","t":"Add CD-ROM or DVD-ROM as an APT package source","s":[["TAGLINE","Add CD-ROM or DVD-ROM as an APT package source"],["TLDR","**Add** CD-ROM to sources\n\n```sudo apt-cdrom add```\n\nAdd without **mounting**\n\n```sudo apt-cdrom add -m```\n\n**Identify** CD-ROM contents\n\n```sudo apt-cdrom ident```\n\nAdd with **specific mount point**\n\n```sudo apt-cdrom add -d [/media/cdrom]```"],["SYNOPSIS","**apt-cdrom** [_-d mount_] [_-m_] [_-r_] _command_"],["DESCRIPTION","**apt-cdrom** adds CD-ROMs or DVD-ROMs to APT's list of available sources. It scans the disc, identifies packages, and adds appropriate entries to /etc/apt/sources.list.\n\nThis is useful for offline installations or when using distribution media without internet access."],["PARAMETERS","**add**\n> Add disc to sources list\n\n**ident**\n> Report disc identity without adding\n\n**-d** _path_\n> CD-ROM mount point\n\n**-m**, **--no-mount**\n> Don't mount/unmount the disc\n\n**-r**, **--rename**\n> Prompt for disc label\n\n**-f**, **--fast**\n> Fast copy (assume disc structure)\n\n**-a**, **--thorough**\n> Thorough scan for packages"],["CONFIGURATION","**/etc/apt/sources.list**\n> File where CD-ROM entries are added as package sources.\n\n**/etc/apt/apt.conf**\n> APT configuration file where CD-ROM mount point and behavior options can be set."],["CAVEATS","CD-ROM sources are lower priority than network repositories. Physical media can degrade over time. Most modern installations use network sources instead."],["HISTORY","**apt-cdrom** was essential when Linux distributions were primarily distributed on physical media. It remains available but is rarely used since broadband internet became common."],["INSTALL","```apt: sudo apt install apt```\n\n```dnf: sudo dnf install apt```\n\n```pacman: sudo pacman -S apt```\n\n```apk: sudo apk add apt```\n\n```brew: brew install apt```\n\n```nix: nix profile install nixpkgs#apt```"],["SEE ALSO","[apt](/man/apt)(8), [apt-get](/man/apt-get)(8)"]],"e":["apt","apt-get"],"x":"sudo apt-cdrom add"},"arpd":{"n":"arpd","t":"Userspace ARP cache daemon with persistent storage.","s":[["TAGLINE","Userspace ARP cache daemon with persistent storage."],["TLDR","**Start** the ARP daemon on an interface, using a database file\n\n```sudo arpd -b [/var/lib/arpd/arpd.db] [eth0]```\n\nAlso **actively resolve** addresses (send up to N broadcast queries)\n\n```sudo arpd -a [3] -b [/var/lib/arpd/arpd.db] [eth0]```\n\n**Dump** the ARP database to standard output and exit\n\n```arpd -l -b [/var/lib/arpd/arpd.db]```\n\n**Suppress** kernel broadcast queries (let arpd do the asking)\n\n```sudo arpd -k [eth0]```"],["SYNOPSIS","**arpd** [_-lkh?_] [_-a N_] [_-b dbase_] [_-B number_] [_-f file_] [_-p interval_] [_-n time_] [_-R rate_] [_interface ..._]"],["DESCRIPTION","**arpd** is a userspace ARP daemon that collects ARP information and stores it in a database. It can answer ARP requests from the kernel cache, reducing ARP traffic on large networks.\n\nThe daemon maintains a persistent ARP database that survives reboots, improving network performance and reducing broadcast traffic."],["PARAMETERS","**-l**\n> Dump the arpd database (interface index, IP address, MAC address) to standard output and exit\n\n**-a** _N_\n> Actively send broadcast ARP queries, making up to _N_ attempts before marking a host dead (otherwise arpd only listens passively)\n\n**-b** _file_\n> Location of the database file (default: **/var/lib/arpd/arpd.db**)\n\n**-f** _file_\n> Read and preload the database from a text file in the format produced by **-l**\n\n**-k**\n> Suppress broadcast ARP queries sent by the kernel, so arpd performs the negotiation itself\n\n**-n** _time_\n> Timeout of the negative cache in seconds (default: 60)\n\n**-p** _interval_\n> Interval in seconds between polls of the kernel ARP table (default: 30)\n\n**-R** _rate_\n> Maximum steady rate of broadcasts sent by arpd, in packets per second (default: 1)\n\n**-B** _number_\n> Number of broadcasts arpd may send back to back (default: 3)\n\n**interface**\n> Network interface(s) to monitor"],["CAVEATS","Rarely needed on modern networks. May cause issues if the ARP cache becomes stale. Typically only useful on very large layer-2 networks. To use arpd, the kernel must be configured to push unresolved addresses to it via **app_solicit** in **/proc/sys/net/ipv4/neigh/**_iface_**/**."],["HISTORY","**arpd** is part of the **iproute2** package, providing userspace ARP table management for specialized networking scenarios."],["INSTALL","```apt: sudo apt install iproute2```\n\n```pacman: sudo pacman -S iproute2```\n\n```zypper: sudo zypper install iproute2```\n\n```brew: brew install iproute2```\n\n```nix: nix profile install nixpkgs#iproute2```"],["SEE ALSO","[arp](/man/arp)(8), [ip-neighbour](/man/ip-neighbour)(8)"],["RESOURCES","```[Source code](https://git.kernel.org/pub/scm/network/iproute2/iproute2.git)```"]],"e":["arp","ip-neighbour"],"x":"sudo arpd -b [/var/lib/arpd/arpd.db] [eth0]"},"aws-logs":{"n":"aws-logs","t":"Manage CloudWatch log groups, streams, and queries.","s":[["TAGLINE","Manage CloudWatch log groups, streams, and queries."],["TLDR","**List all log groups**\n\n```aws logs describe-log-groups```\n\n**List log streams** in a group\n\n```aws logs describe-log-streams --log-group-name [/aws/lambda/function-name]```\n\n**Get log events** from a stream\n\n```aws logs get-log-events --log-group-name [group_name] --log-stream-name [stream_name]```\n\n**Tail logs** in real-time\n\n```aws logs tail [/aws/lambda/function-name] --follow```\n\n**Tail logs** from the last 10 minutes\n\n```aws logs tail [/aws/lambda/function-name] --since [10m]```\n\n**Filter logs** with a pattern\n\n```aws logs filter-log-events --log-group-name [group_name] --filter-pattern \"[ERROR]\"```\n\n**Create a log group**\n\n```aws logs create-log-group --log-group-name [group_name]```\n\n**Set retention policy** on a log group\n\n```aws logs put-retention-policy --log-group-name [group_name] --retention-in-days [30]```\n\n**Delete a log group**\n\n```aws logs delete-log-group --log-group-name [group_name]```"],["SYNOPSIS","**aws logs** _subcommand_ [_options_]"],["DESCRIPTION","**aws logs** is a subcommand of the AWS CLI that manages Amazon CloudWatch Logs, a service for monitoring, storing, and accessing log files from AWS resources and applications.\n\nLogs are organized into log groups (containers) and log streams (sequences of events from a single source). Log events contain a timestamp and raw message. Many AWS services like Lambda, ECS, and API Gateway automatically send logs to CloudWatch.\n\nCloudWatch Logs Insights enables SQL-like queries across log data. Metric filters extract metrics from log patterns. Subscription filters stream logs to Lambda, Kinesis, or Firehose in real-time."],["PARAMETERS","**describe-log-groups**\n> List log groups with metadata.\n\n**create-log-group**\n> Create a new log group.\n\n**delete-log-group**\n> Remove a log group and all its data.\n\n**describe-log-streams**\n> List log streams in a group.\n\n**get-log-events**\n> Retrieve log events from a stream.\n\n**filter-log-events**\n> Search logs with a filter pattern.\n\n**tail**\n> Stream logs in real-time (like tail -f).\n\n**put-log-events**\n> Send log events to a stream.\n\n**put-retention-policy**\n> Set log expiration period.\n\n**delete-retention-policy**\n> Set logs to never expire.\n\n**put-metric-filter**\n> Create a metric filter.\n\n**put-subscription-filter**\n> Stream logs to another service.\n\n**start-query**\n> Run a CloudWatch Logs Insights query; returns a query ID.\n\n**get-query-results**\n> Retrieve results for a query started with start-query.\n\n**--log-group-name** _name_\n> Name of the log group.\n\n**--log-stream-name** _name_\n> Name of the log stream.\n\n**--filter-pattern** _pattern_\n> Pattern to match log events.\n\n**--start-time** _timestamp_\n> Start time in milliseconds since epoch.\n\n**--end-time** _timestamp_\n> End time in milliseconds since epoch.\n\n**--follow**\n> Continuously stream new logs (tail command).\n\n**--since** _time_\n> tail from a relative time like 5m, 2h, or 1d, or an absolute timestamp.\n\n**--retention-in-days** _days_\n> Retention period (1, 3, 5, 7, 14, 30, 60, 90, 120, 150, 180, 365, 400, 545, 731, 1096, 1827, 2192, 2557, 2922, 3288, 3653, or infinite)."],["CAVEATS","Log groups have no default retention; logs are kept forever unless configured. The tail command requires aws-cli v2. Filter patterns have specific syntax different from regex. Stored log data incurs charges based on ingestion and storage volume. Cross-account log sharing requires subscription filters and destination policies."],["HISTORY","**Amazon CloudWatch Logs** launched in **July 2014** as an extension of CloudWatch monitoring. It initially supported EC2 logs via the CloudWatch agent. **CloudWatch Logs Insights** was introduced at **re:Invent 2018** for interactive log analysis. **Live Tail** was added in **2023** for real-time log streaming in the console. The service expanded to handle logs from virtually all AWS services."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-cloudwatch](/man/aws-cloudwatch)(1), [journalctl](/man/journalctl)(1), [tail](/man/tail)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/logs/index.html)```"]],"e":["aws","aws-cloudwatch","journalctl","tail"],"x":"aws logs describe-log-groups"},"aws-ssm":{"n":"aws-ssm","t":"Manage Systems Manager sessions, commands, and parameters.","s":[["TAGLINE","Manage Systems Manager sessions, commands, and parameters."],["TLDR","**List managed instances**\n\n```aws ssm describe-instance-information```\n\n**Start a Session Manager session** to an instance\n\n```aws ssm start-session --target [i-xxxxxxxxxxxxxxxx]```\n\n**Run a command** on instances\n\n```aws ssm send-command --document-name \"AWS-RunShellScript\" --targets \"Key=instanceids,Values=[i-xxxxxxxx]\" --parameters 'commands=[\"echo hello\"]'```\n\n**Get parameter** from Parameter Store\n\n```aws ssm get-parameter --name [/path/to/parameter]```\n\n**Get parameter with decryption** (for SecureString)\n\n```aws ssm get-parameter --name [/path/to/secret] --with-decryption```\n\n**Put a parameter** into Parameter Store\n\n```aws ssm put-parameter --name [/path/to/parameter] --value \"[value]\" --type String```\n\n**List parameters** by path\n\n```aws ssm get-parameters-by-path --path [/path/prefix/] --recursive```\n\n**List command invocations**\n\n```aws ssm list-command-invocations --command-id [command-id] --details```"],["SYNOPSIS","**aws ssm** _subcommand_ [_options_]"],["DESCRIPTION","**aws ssm** is a subcommand of the AWS CLI that manages AWS Systems Manager, a service for operational management of AWS resources. SSM provides capabilities for instance management, configuration, patching, and secure parameter storage.\n\n**Session Manager** enables secure shell access to EC2 instances without SSH keys or open inbound ports. It logs sessions and supports IAM-based access control.\n\n**Run Command** executes scripts and commands across fleets of instances without SSH. **Parameter Store** provides hierarchical storage for configuration data and secrets with optional encryption.\n\nThe SSM Agent must be installed on instances for most operations. It comes pre-installed on many AWS AMIs."],["PARAMETERS","**describe-instance-information**\n> List instances managed by SSM.\n\n**start-session**\n> Start interactive Session Manager session.\n\n**send-command**\n> Execute a command on targets.\n\n**list-commands**\n> List command history.\n\n**list-command-invocations**\n> Get command execution results.\n\n**get-parameter**\n> Retrieve a parameter value.\n\n**put-parameter**\n> Create or update a parameter.\n\n**get-parameters-by-path**\n> List parameters under a path.\n\n**delete-parameter**\n> Remove a parameter.\n\n**get-document**\n> Get SSM document contents.\n\n**describe-document**\n> Get SSM document details.\n\n**list-documents**\n> List available SSM documents.\n\n**describe-patches**\n> List information about patches.\n\n**--target** _instance-id_\n> Target instance for session.\n\n**--document-name** _name_\n> SSM document to execute.\n\n**--targets** _targets_\n> Target instances for commands.\n\n**--parameters** _params_\n> Document parameters.\n\n**--name** _name_\n> Parameter name (hierarchical path).\n\n**--value** _value_\n> Parameter value.\n\n**--type** _type_\n> Parameter type: String, StringList, SecureString.\n\n**--with-decryption**\n> Decrypt SecureString parameters.\n\n**--overwrite**\n> Overwrite existing parameter value.\n\n**--recursive**\n> Retrieve parameters recursively by path."],["CAVEATS","Session Manager requires the SSM Agent and proper IAM instance profile. Parameter names are case-sensitive and hierarchical (use /path/format). SecureString parameters use KMS and may incur charges. Run Command output truncates at 48000 characters; use S3 for large outputs. Default parameter quota is 10,000 standard parameters per account."],["HISTORY","**AWS Systems Manager** evolved from **EC2 Run Command** which launched in **October 2015**. It was unified under the Systems Manager brand in **2017**. **Session Manager** was introduced at **re:Invent 2018** as a secure alternative to SSH. **Parameter Store** expanded to include advanced parameters and higher throughput tiers. The service has grown to include Patch Manager, State Manager, OpsCenter, and Change Manager for comprehensive operations."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-ec2](/man/aws-ec2)(1), [ssh](/man/ssh)(1), [aws-secretsmanager](/man/aws-secretsmanager)(1)"]],"e":["aws","aws-ec2","ssh","aws-secretsmanager"],"x":"aws ssm describe-instance-information"},"awslocal":{"n":"awslocal","t":"AWS CLI wrapper for LocalStack","s":[["TAGLINE","AWS CLI wrapper for LocalStack"],["TLDR","**List Kinesis streams** on LocalStack\n\n```awslocal kinesis list-streams```\n\n**Same as aws with LocalStack endpoint**\n\n```awslocal s3 ls```\n\n**Create a local bucket**\n\n```awslocal s3 mb s3://[my-bucket]```\n\n**Invoke Lambda** locally\n\n```awslocal lambda list-functions```"],["SYNOPSIS","**awslocal** [*aws-cli-arguments*...]"],["DESCRIPTION","**awslocal** is a thin wrapper around the **aws** CLI for use with [LocalStack](https://github.com/localstack/localstack). Instead of passing **--endpoint-url=http://localhost:4566** on every call, run the same subcommands via **awslocal**.\n\nInstall with **pip install awscli-local** (wrapper only) or **pip install awscli-local[ver1]** to also pull AWS CLI v1. Usage matches **aws**; see **aws help**."],["PARAMETERS","All arguments are forwarded to the underlying **aws** CLI (services, subcommands, and flags). There is no separate command tree."],["CONFIGURATION","Environment variables:\n\n**AWS_ENDPOINT_URL**\n\n> Endpoint to use (takes precedence over deprecated vars below).\n\n**LOCALSTACK_HOST** (deprecated)\n\n> **hostname:port** for LocalStack (default **localhost:4566**).\n\n**USE_SSL** (deprecated)\n\n> Whether to use SSL when connecting (default false).\n\nStandard AWS credential and region variables still apply for the CLI; LocalStack often accepts test credentials."],["CAVEATS","AWS CLI v2 is not auto-installed from PyPI; **awslocal** can still work if v2 is already on PATH, with known gaps (for example some **cloudformation package** endpoint cases). Prefer LocalStack docs for service coverage and ports. Not a substitute for production **aws** against real AWS."],["SEE ALSO","[aws](/man/aws)(1), [localstack](/man/localstack)(1)"],["RESOURCES","```[Source code](https://github.com/localstack/awscli-local)```\n\n```[Homepage](https://localstack.cloud)```"]],"e":["aws","localstack"],"x":"awslocal kinesis list-streams"},"bpytop":{"n":"bpytop","t":"Terminal-based resource monitor with graphs","s":[["TAGLINE","Terminal-based resource monitor with graphs"],["TLDR","**Start** resource monitor\n\n```bpytop```\n\n**Start** with a low update interval (milliseconds)\n\n```bpytop --update [1000]```\n\n**Force** the TTY-friendly box-drawing mode\n\n```bpytop --low-color```\n\n**Open** the in-app help\n\n```bpytop --help```"],["SYNOPSIS","**bpytop** [_options_]"],["DESCRIPTION","**bpytop** is a resource monitor written in Python showing CPU, memory, disks, network, and processes in a visually appealing terminal UI. It's a Python rewrite of bashtop with improved performance and additional features.\n\nThe tool provides comprehensive system monitoring with mouse support and customizable themes."],["FEATURES","- CPU usage per core with graphs\n- Memory and swap statistics\n- Disk I/O and usage\n- Network bandwidth monitoring\n- Process management\n- Mouse support\n- Customizable themes\n- Configuration menu (ESC)\n- Process filtering and sorting\n- Tree view for processes"],["INTERFACE SECTIONS","**CPU**\n> Usage graphs per core, temperature\n\n**Memory**\n> RAM and swap with graphs\n\n**Disks**\n> I/O activity and space usage\n\n**Network**\n> Bandwidth by interface\n\n**Processes**\n> Detailed process list"],["KEYBINDINGS","- **ESC** - Configuration menu\n- **q** - Quit\n- **f** - Filter processes\n- **t** - Tree view toggle\n- **+/-** - Adjust update speed\n- **Mouse** - Click to interact\n- **Arrow keys** - Navigate"],["THEMES","Built-in themes:\n- Default\n- Default-light\n- Gruvbox dark/light\n- Nord\n- Monokai"],["CONFIGURATION","**~/.config/bpytop/bpytop.conf**\n> Main configuration file for themes, update interval, process sorting, and display options."],["CAVEATS","Requires Python 3.6+. Higher CPU usage than native tools. Superseded by btop++ (C++ rewrite). May not show all metrics on all systems. GPU monitoring limited."],["HISTORY","**bpytop** was created by Jakob P. Liljenberg (aristocratos) in **2020** as a Python rewrite of bashtop. The same author later began btop++ (a C++ rewrite, btop) in **2021**, which is now the actively developed successor; bpytop receives little ongoing development."],["INSTALL","```pacman: sudo pacman -S bpytop```\n\n```brew: brew install bpytop```"],["SEE ALSO","[btop](/man/btop)(1), [htop](/man/htop)(1), [bashtop](/man/bashtop)(1), [glances](/man/glances)(1), [top](/man/top)(1)"],["RESOURCES","```[Source code](https://github.com/aristocratos/bpytop)```\n\n```[Documentation](https://pypi.org/project/bpytop/)```"]],"e":["btop","htop","bashtop","glances","top"],"x":"bpytop"},"check-language-support":{"n":"check-language-support","t":"identify missing language support packages","s":[["TAGLINE","identify missing language support packages"],["TLDR","Display **missing language packages**\n\n```check-language-support```\n\nList packages for a **specific locale**\n\n```check-language-support -l [en]```\n\nDisplay **installed packages** as well as missing\n\n```check-language-support --show-installed```"],["SYNOPSIS","**check-language-support** [_options_]"],["DESCRIPTION","**check-language-support** identifies missing language support packages on Ubuntu systems. It analyzes installed software and enabled locales to recommend additional packages for complete language support.\n\nThis includes translations, input methods, fonts, and spelling dictionaries."],["PARAMETERS","**-l, --language** _locale_\n> Check for specific language/locale\n\n**--show-installed**\n> Also show already installed packages\n\n**-a, --all**\n> Check for all languages\n\n**--package** _pkg_\n> Check language support for specific package"],["CAVEATS","Ubuntu-specific tool; may not work on other distributions. Recommendations are based on installed applications and may suggest packages not strictly necessary."],["SEE ALSO","[apt](/man/apt)(8), [locale](/man/locale)(1), [localectl](/man/localectl)(1)"],["RESOURCES","```[Homepage](https://launchpad.net/language-selector)```"]],"e":["apt","locale","localectl"],"x":"check-language-support"},"crush":{"n":"crush","t":"Glamorous AI coding agent for the terminal","s":[["TAGLINE","Glamorous AI coding agent for the terminal"],["TLDR","**Start an interactive session**\n\n```crush```\n\n**Change to a specific working directory**\n\n```crush --cwd [path/to/project]```\n\n**Skip all permission prompts (use with caution)**\n\n```crush --yolo```\n\n**Enable debug logging**\n\n```crush --debug```\n\n**Use a custom data directory**\n\n```crush --data-dir [path/to/data]```\n\n**View recent logs**\n\n```crush logs```\n\n**Follow logs in real time**\n\n```crush logs --follow```\n\n**Run non-interactively**\n\n```crush run \"[fix the bug in main.go]\"```"],["SYNOPSIS","**crush** [_options_] [_command_]"],["PARAMETERS","**--cwd** _PATH_\n> Change to a specific directory before starting\n\n**--data-dir** _PATH_\n> Override the default data directory location\n\n**--debug**\n> Enable debug logging for troubleshooting\n\n**--yolo**\n> Skip all permission prompts (dangerous, use with caution)\n\n**--help**\n> Display help information\n\n**--version**\n> Show version number"],["COMMANDS","**run** _PROMPT_\n> Run Crush in non-interactive mode with a prompt\n\n**logs**\n> Print the last 1000 lines of logs\n\n**logs --tail** _N_\n> Print the last N lines of logs\n\n**logs --follow**\n> Follow logs in real time"],["DESCRIPTION","**Crush** is a glamorous AI coding agent for your terminal, developed by Charmbracelet. Built in Go for speed and efficiency, it provides a terminal user interface (TUI) for interacting with various AI models to help with coding tasks, debugging, and file operations.\n\nCrush supports multiple AI providers including OpenAI, Anthropic, Google Gemini, Groq, OpenRouter, AWS Bedrock, and local models via Ollama and LM Studio. It uses Language Server Protocol (LSP) for enhanced code context and is extensible via Model Context Protocol (MCP) servers with stdio, http, and sse transports.\n\nThe tool offers excellent cross-platform compatibility, supporting macOS, Linux, Windows (PowerShell and WSL), FreeBSD, OpenBSD, and NetBSD. Configuration is stored in JSON files with settings for providers, LSP, MCP servers, and permissions."],["CONFIGURATION","**~/.config/crush/crush.json**\n> Main configuration file containing AI provider settings, API keys, LSP configurations, MCP server definitions, and permission settings."],["CAVEATS","Requires API keys from supported providers set via environment variables (ANTHROPIC_API_KEY, OPENAI_API_KEY, etc.) or configured in crush.json. The **--yolo** flag bypasses all permission prompts and should be used with extreme caution. Debug mode with **--debug** generates verbose logging."],["HISTORY","Crush was developed by **Charmbracelet**, a company known for creating engaging terminal applications. It originated from a project by Kujtim Hoxha that was briefly associated with the OpenCode name before Charmbracelet rebranded their version as Crush, with the OpenCode name continuing as a separate fork. The project is released under the FSL-1.1-MIT license (MIT-compatible) and has gained over 8,900 GitHub stars. Version 0.4.0 was released in August 2025."],["INSTALL","```nix: nix profile install nixpkgs#crush```"],["SEE ALSO","[claude](/man/claude)(1), [opencode](/man/opencode)(1), [aider](/man/aider)(1), [codex](/man/codex)(1)"]],"e":["claude","opencode","aider","codex"],"x":"crush"},"declare":{"n":"declare","t":"bash builtin for declaring variables with attributes","s":[["TAGLINE","bash builtin for declaring variables with attributes"],["TLDR","**Declare an integer variable**\n\n```declare -i [number]=42```\n\n**Declare a read-only variable**\n\n```declare -r [CONSTANT]=\"value\"```\n\n**Declare an indexed array**\n\n```declare -a [myarray]=([element1] [element2])```\n\n**Declare an associative array**\n\n```declare -A [mymap]=([key1]=value1 [key2]=value2)```\n\n**Export a variable**\n\n```declare -x [ENVVAR]=\"value\"```\n\n**Declare lowercase variable**\n\n```declare -l [lower]=\"HELLO\"```\n\n**Declare uppercase variable**\n\n```declare -u [upper]=\"hello\"```\n\n**Display attributes and value of a variable**\n\n```declare -p [variable_name]```\n\n**List all functions**\n\n```declare -f```"],["SYNOPSIS","**declare** [_options_] [_name_[=_value_]...]"],["PARAMETERS","**-a**\n> Declare indexed array.\n\n**-A**\n> Declare associative array (bash 4+).\n\n**-f**\n> Display or declare functions.\n\n**-F**\n> Display function names only.\n\n**-g**\n> Create global variable (in functions).\n\n**-i**\n> Declare integer (arithmetic evaluation).\n\n**-l**\n> Convert to lowercase on assignment.\n\n**-n**\n> Name reference (bash 4.3+).\n\n**-r**\n> Make read-only (constant).\n\n**-t**\n> Give trace attribute (debugging).\n\n**-u**\n> Convert to uppercase on assignment.\n\n**-x**\n> Export to environment.\n\n**-I**\n> Inherit attributes and value from variable of same name at surrounding scope (bash 5.1+).\n\n**-p**\n> Display attributes and values.\n\n**+**_attr_\n> Remove attribute."],["DESCRIPTION","**declare** is a bash builtin that declares variables with specific attributes. While bash allows implicit variable creation, declare provides explicit type declarations and special behaviors.\n\nInteger variables (**-i**) perform arithmetic automatically: `declare -i x; x=5+3` sets x to 8. Arrays require explicit declaration for proper initialization. Read-only variables cannot be changed or unset.\n\nIn functions, variables are local by default with declare; use **-g** for global scope. The **-n** option creates references to other variables, enabling indirect variable access."],["CAVEATS","Associative arrays require bash 4+, and name references require bash 4.3+. Declare is bash-specific; POSIX shells use different mechanisms. The -g flag only works inside functions. Read-only variables persist until shell exit; they cannot be unset."],["HISTORY","The **declare** builtin originated in **ksh** (Korn Shell) and was adopted by bash. It provides a more explicit way to define variables than simple assignment. Bash has extended declare over versions, adding associative arrays in **bash 4.0** (2009) and name references in **bash 4.3** (2014)."],["SEE ALSO","[typeset](/man/typeset)(1), [local](/man/local)(1), [readonly](/man/readonly)(1), [export](/man/export)(1)"]],"e":["typeset","local","readonly","export"],"x":"declare -i [number]=42"},"docker-buildx-rm":{"n":"docker-buildx-rm","t":"Remove Docker Buildx builder instances","s":[["TAGLINE","Remove Docker Buildx builder instances"],["TLDR","**Remove a specific** builder instance\n\n```docker buildx rm [builder_name]```\n\n**Remove a builder** without confirmation prompt\n\n```docker buildx rm -f [builder_name]```\n\n**Remove all inactive** builders\n\n```docker buildx rm --all-inactive```\n\n**Remove a builder** but keep the BuildKit daemon running\n\n```docker buildx rm --keep-daemon [builder_name]```\n\n**Remove a builder** but preserve its state for reuse\n\n```docker buildx rm --keep-state [builder_name]```"],["SYNOPSIS","**docker buildx rm** [_options_] [_name..._]"],["PARAMETERS","**--all-inactive**\n> Remove all inactive builder instances.\n\n**-f**, **--force**\n> Do not prompt for confirmation.\n\n**--keep-daemon**\n> Keep the BuildKit daemon running after removing the builder. Supported by docker-container and kubernetes drivers only.\n\n**--keep-state**\n> Preserve BuildKit state so a new builder with the same name can reuse it. Supported by docker-container driver only.\n\n**--timeout** _duration_\n> Override default timeout for loading builder status (default: 20s)."],["DESCRIPTION","**docker buildx rm** removes the specified builder instance or, if no name is given, the currently selected builder. Removing the **default** builder is a no-op since it uses the Docker daemon's built-in build capabilities.\n\nThe command stops the associated BuildKit daemon and cleans up its state by default. Use **--keep-daemon** to leave the daemon running independently, or **--keep-state** to preserve build cache and state for a future builder with the same name."],["CAVEATS","The default builder cannot be removed. The **--keep-daemon** flag only works with docker-container and kubernetes drivers. The **--keep-state** flag only works with the docker-container driver."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-buildx-ls](/man/docker-buildx-ls)(1), [docker-buildx-prune](/man/docker-buildx-prune)(1), [docker-build](/man/docker-build)(1), [docker](/man/docker)(1)"]],"e":["docker-buildx-ls","docker-buildx-prune","docker-build","docker"],"x":"docker buildx rm [builder_name]"},"dolt-fetch":{"n":"dolt-fetch","t":"download objects and refs from remote","s":[["TAGLINE","download objects and refs from remote"],["TLDR","**Fetch from remote**\n\n```dolt fetch```\n\n**Fetch from specific remote**\n\n```dolt fetch [remote]```\n\n**Fetch specific branch**\n\n```dolt fetch [remote] [branch]```\n\n**Fetch all remotes**\n\n```dolt fetch --all```"],["SYNOPSIS","**dolt** **fetch** [_options_] [_remote_] [_refspec_]"],["PARAMETERS","**--all**\n> Fetch from all remotes.\n\n**--prune**\n> Remove stale remote tracking branches."],["DESCRIPTION","**dolt fetch** downloads objects, commits, and branch references from a remote Dolt repository. It synchronizes the local repository's knowledge of the remote's state without modifying the working set or local branches.\n\nThis command updates remote-tracking branches (e.g., origin/main) to reflect the current state of the remote repository. Unlike pull, fetch does not automatically merge changes, allowing you to review remote changes before integrating them into your local branches.\n\nThe --prune option removes remote-tracking branches that no longer exist on the remote, keeping the local repository clean. Fetching from all remotes simultaneously is useful in multi-remote workflows."],["INSTALL","```pacman: sudo pacman -S dolt```\n\n```brew: brew install dolt```\n\n```nix: nix profile install nixpkgs#dolt```"]],"e":[],"x":"dolt fetch"},"driftnet":{"n":"driftnet","t":"capture images from network traffic","s":[["TAGLINE","capture images from network traffic"],["TLDR","**Capture images** from network\n\n```driftnet -i [eth0]```\n\n**Save images to** directory\n\n```driftnet -i [eth0] -d [/tmp/images]```\n\n**Capture without display**\n\n```driftnet -i [eth0] -a -d [/tmp/images]```\n\n**Read from pcap** file\n\n```driftnet -f [capture.pcap]```\n\n**Verbose output**\n\n```driftnet -v -i [eth0]```"],["SYNOPSIS","**driftnet** [_options_]"],["PARAMETERS","**-i** _INTERFACE_\n> Network interface to capture on.\n\n**-d** _DIR_\n> Directory to save images.\n\n**-a**\n> Adjunct mode (no display).\n\n**-f** _FILE_\n> Read from pcap file.\n\n**-p**\n> Don't put interface in promiscuous mode.\n\n**-v**\n> Verbose mode.\n\n**-b**\n> Beep when image captured.\n\n**--help**\n> Display help information."],["DESCRIPTION","**driftnet** captures and displays images from network traffic in real-time. It extracts JPEG, GIF, and PNG images transmitted over unencrypted HTTP connections passing through the monitored network interface.\n\nThe tool is useful for network monitoring, security demonstrations, and forensics. In display mode, captured images appear in a window; in adjunct mode, they're saved to disk.\n\ndriftnet can also extract audio data from network streams, playing it in real-time or saving to files."],["CAVEATS","Only captures unencrypted traffic (HTTP). Requires root/promiscuous access. HTTPS traffic not visible. Intended for authorized monitoring only."],["HISTORY","driftnet was created by **Chris Lightfoot** as a demonstration of network traffic visibility. It highlights the privacy implications of unencrypted communications and is used in security awareness training."],["INSTALL","```apt: sudo apt install driftnet```\n\n```nix: nix profile install nixpkgs#driftnet```"],["SEE ALSO","[tcpdump](/man/tcpdump)(8), [wireshark](/man/wireshark)(1), [ettercap](/man/ettercap)(8)"]],"e":["tcpdump","wireshark","ettercap"],"x":"driftnet -i [eth0]"},"emaint":{"n":"emaint","t":"Portage system maintenance utility","s":[["TAGLINE","Portage system maintenance utility"],["TLDR","Synchronize repositories set to **auto-sync**\n\n```sudo emaint sync [-a|--auto]```\n\nSynchronize a **specific** repository\n\n```sudo emaint sync [-r|--repo] [repository]```\n\nSynchronize **all** repositories\n\n```sudo emaint sync [-A|--allrepos]```\n\n**Clear** the Portage resume list\n\n```sudo emaint cleanresume [-f|--fix]```\n\n**Clean** Portage logs\n\n```sudo emaint logs [-C|--clean]```"],["SYNOPSIS","**emaint** _module_ [_options_]"],["DESCRIPTION","**emaint** performs Portage maintenance tasks on Gentoo systems. It is organized into modules, each covering one area: repository synchronization, the resume list, log rotation, the binary package host, and consistency checks on Portage's own databases.\n\nMost modules follow the same shape: **-c/--check** reports problems without touching anything, and **-f/--fix** applies the repair. Running `emaint --help` lists the modules available on the installed Portage version.\n\n**emaint sync** is the modern front end for updating repositories and understands per-repository settings in repos.conf, which `emerge --sync` predates."],["PARAMETERS","**sync**\n> Synchronize repositories\n\n**-a, --auto**\n> Sync only repositories that have auto-sync enabled\n\n**-r, --repo** _name_\n> Sync a specific repository\n\n**-A, --allrepos**\n> Sync all repositories regardless of auto-sync\n\n**cleanresume**\n> Clear Portage's resume list (what `emerge --resume` would replay)\n\n**logs**\n> Rotate and clean the Portage log directory\n\n**merges**\n> Detect and repair packages left half-merged by an interrupted emerge\n\n**binhost**\n> Regenerate the Packages index for a binary package host\n\n**world**\n> Check and fix the world file for invalid entries\n\n**-c, --check**\n> Report problems without changing anything\n\n**-C, --clean**\n> Clean old entries (logs module)\n\n**-f, --fix**\n> Apply the repair"],["CONFIGURATION","**/etc/portage/repos.conf**\n> Repository configuration for Portage, controlling which repositories are synchronized."],["CAVEATS","Gentoo Linux specific. Requires root privileges. Part of the Portage package management system."],["SEE ALSO","[emerge](/man/emerge)(1), [eselect](/man/eselect)(1), [eselect-repository](/man/eselect-repository)(1)"],["RESOURCES","```[Source code](https://github.com/gentoo/portage)```\n\n```[Documentation](https://wiki.gentoo.org/wiki/Portage)```"]],"e":["emerge","eselect","eselect-repository"],"x":"sudo emaint sync [-a|--auto]"},"eselect-profile":{"n":"eselect-profile","t":"Gentoo system profile management","s":[["TAGLINE","Gentoo system profile management"],["TLDR","**List** available profile symlink targets\n\n```eselect profile list```\n\n**Set** the /etc/portage/make.profile symlink\n\n```eselect profile set [name|number]```\n\n**Show** the current system profile\n\n```eselect profile show```"],["SYNOPSIS","**eselect profile** _action_ [_options_]"],["DESCRIPTION","**eselect profile** manages the /etc/portage/make.profile symlink on Gentoo systems. This symlink determines the system profile, which affects default USE flags, masked packages, and other Portage settings.\n\nPart of the eselect framework for system configuration."],["PARAMETERS","**list**\n> List available profiles\n\n**set** _target_\n> Set profile symlink to target\n\n**show**\n> Display current profile\n\n**--force**\n> Allow setting a profile whose target does not currently exist"],["CAVEATS","Gentoo Linux specific. Changing profiles affects default USE flags, masks and compiler settings, so run `emerge --update --deep --newuse @world` afterwards to rebuild anything affected. The profile module is part of the base eselect package, unlike the separately packaged repository module."],["SEE ALSO","[eselect](/man/eselect)(1), [eselect-repository](/man/eselect-repository)(1), [emerge](/man/emerge)(1)"],["RESOURCES","```[Source code](https://gitweb.gentoo.org/proj/eselect.git/)```\n\n```[Documentation](https://wiki.gentoo.org/wiki/Profile_(Portage))```"]],"e":["eselect","eselect-repository","emerge"],"x":"eselect profile list"},"exo-compute-instance":{"n":"exo-compute-instance","t":"Exoscale virtual machine lifecycle management","s":[["TAGLINE","Exoscale virtual machine lifecycle management"],["TLDR","**List instances**\n\n```exo compute instance list```\n\n**Create an instance**\n\n```exo compute instance create [name] --zone [ch-gva-2] --template [Linux Ubuntu]```\n\n**Show instance details**\n\n```exo compute instance show [name]```\n\n**Start an instance**\n\n```exo compute instance start [name]```\n\n**Stop an instance**\n\n```exo compute instance stop [name]```\n\n**Delete an instance**\n\n```exo compute instance delete [name]```\n\n**SSH into instance**\n\n```exo compute instance ssh [name]```"],["SYNOPSIS","**exo** **compute** **instance** _command_ [_options_]"],["SUBCOMMANDS","**list**\n> List instances.\n\n**create**\n> Create an instance.\n\n**show**\n> Show instance details.\n\n**start**\n> Start instance.\n\n**stop**\n> Stop instance.\n\n**reboot**\n> Reboot instance.\n\n**delete**\n> Delete instance.\n\n**ssh**\n> SSH into instance.\n\n**resize**\n> Resize instance."],["DESCRIPTION","**exo compute instance** manages Exoscale compute instances (virtual machines). It handles the full lifecycle from creation through deletion, including power management and configuration.\n\nInstances run in specified availability zones with configurable templates, instance types, security groups, and SSH keys. The tool provides convenient SSH access and management without using the web console.\n\nCommands support all instance operations including resizing, rebooting, and attaching to networks and storage."],["INSTALL","```dnf: sudo dnf install exo```\n\n```pacman: sudo pacman -S exo```\n\n```apk: sudo apk add exoscale```\n\n```nix: nix profile install nixpkgs#exo```"],["SEE ALSO","[exo](/man/exo)(1), [exo-compute](/man/exo-compute)(1), [exo-compute-block-storage](/man/exo-compute-block-storage)(1)"],["RESOURCES","```[Source code](https://github.com/exoscale/cli)```\n\n```[Homepage](https://www.exoscale.com/)```"]],"e":["exo","exo-compute","exo-compute-block-storage"],"x":"exo compute instance list"},"fdupes":{"n":"fdupes","t":"find and remove duplicate files","s":[["TAGLINE","find and remove duplicate files"],["TLDR","**Find duplicate files** in a directory\n\n```fdupes [/path/to/directory]```\n\n**Find duplicates recursively**\n\n```fdupes -r [/path/to/directory]```\n\n**Delete duplicates** interactively\n\n```fdupes -d [/path/to/directory]```\n\n**Delete duplicates** keeping first file automatically\n\n```fdupes -dN [/path/to/directory]```\n\n**Show sizes** of duplicate files\n\n```fdupes -S [/path/to/directory]```\n\n**Summarize** duplicate statistics\n\n```fdupes -m [/path/to/directory]```\n\n**Compare multiple directories**\n\n```fdupes [/path/dir1] [/path/dir2]```\n\n**Find duplicates** only comparing specified directories\n\n```fdupes -r [/path/original] [/path/copy]```"],["SYNOPSIS","**fdupes** [_options_] _directory_..."],["DESCRIPTION","**fdupes** identifies duplicate files in specified directories by comparing file sizes and MD5 signatures, followed by a byte-by-byte comparison for verification. It groups duplicate files together for review or automatic deletion.\n\nThe tool first compares file sizes, then calculates partial and full MD5 hashes, and finally performs byte comparison to ensure accuracy. This multi-stage approach balances speed and accuracy.\n\nfdupes can operate across multiple directories, finding duplicates within and between them. It's useful for cleaning up backup copies, photo libraries, and reclaiming disk space."],["PARAMETERS","**-r**, **--recurse**\n> Search directories recursively.\n\n**-R**, **--recurse:**\n> Recursive for following directories only.\n\n**-d**, **--delete**\n> Prompt for files to delete.\n\n**-N**, **--noprompt**\n> With -d, delete without prompting.\n\n**-S**, **--size**\n> Show size of duplicate files.\n\n**-m**, **--summarize**\n> Show summary statistics.\n\n**-1**, **--sameline**\n> List sets on single lines.\n\n**-n**, **--noempty**\n> Exclude empty files.\n\n**-H**, **--hardlinks**\n> Treat hard links as duplicates.\n\n**-f**, **--omitfirst**\n> Omit first file in each set.\n\n**-q**, **--quiet**\n> Quiet mode (for -d operation).\n\n**-i**, **--reverse**\n> Reverse order (delete first file).\n\n**-L**, **--linkhard**\n> Replace duplicates with hard links."],["CAVEATS","Byte-by-byte comparison can be slow for large files. Delete operations are irreversible. Hard link mode changes file relationships. Cannot compare across different filesystems for hard linking."],["HISTORY","**fdupes** was created by **Adrian Lopez** in **1999** as a utility for finding duplicate files. It has been maintained and enhanced over the years, becoming a standard tool for duplicate detection on Unix-like systems. The tool influenced many similar utilities and remains actively used for disk cleanup operations."],["INSTALL","```apt: sudo apt install fdupes```\n\n```dnf: sudo dnf install fdupes```\n\n```pacman: sudo pacman -S fdupes```\n\n```apk: sudo apk add fdupes```\n\n```zypper: sudo zypper install fdupes```\n\n```brew: brew install fdupes```\n\n```nix: nix profile install nixpkgs#fdupes```"],["SEE ALSO","[rmlint](/man/rmlint)(1), [find](/man/find)(1), [md5sum](/man/md5sum)(1), [diff](/man/diff)(1)"],["RESOURCES","```[Source code](https://github.com/adrianlopezroche/fdupes)```"]],"e":["rmlint","find","md5sum","diff"],"x":"fdupes [/path/to/directory]"},"file-rename":{"n":"file-rename","t":"Perl regex-based file renaming","s":[["TAGLINE","Perl regex-based file renaming"],["TLDR","**Rename using Perl expression**\n\n```file-rename 's/old/new/' [files]```\n\n**Rename to lowercase**\n\n```file-rename 'y/A-Z/a-z/' [files]```\n\n**Dry run to preview changes**\n\n```file-rename -n 's/pattern/replacement/' [files]```\n\n**Replace spaces with underscores**\n\n```file-rename 's/ /_/g' [files]```\n\n**Add prefix to files**\n\n```file-rename 's/^/prefix_/' [files]```\n\n**Change file extension**\n\n```file-rename 's/\\.jpeg$/.jpg/' [*.jpeg]```\n\n**Remove numbering prefix from files**\n\n```file-rename 's/^\\d+_//' [files]```"],["SYNOPSIS","**file-rename** [_options_] _expression_ [_files_...]"],["PARAMETERS","**-n**, **--no-act**\n> Dry run, show what would happen.\n\n**-v**, **--verbose**\n> Print names of files renamed.\n\n**-f**, **--force**\n> Overwrite existing files.\n\n**-0**, **--null**\n> Expect null-terminated input from stdin (for use with find -print0).\n\n**-d**, **--filename**\n> Rename only the filename component, not the directory part.\n\n**-e** _expression_\n> Apply expression to filename. Multiple -e options can be chained."],["DESCRIPTION","**file-rename** (also known as rename or prename) renames multiple files using Perl regular expressions, providing powerful pattern-based transformation capabilities. Unlike simple mv operations, it can apply complex substitutions, case conversions, and pattern matching across multiple filenames simultaneously.\n\nThe tool supports the full Perl regex syntax including capture groups, look-ahead/look-behind assertions, and transliteration operations. Common uses include batch renaming, removing or replacing characters, changing file extensions, and standardizing filename formats.\n\nfile-rename's dry-run mode (-n) allows preview of changes before execution, preventing accidental destructive operations. The force flag (-f) enables overwriting existing files when name collisions occur."],["CAVEATS","Multiple implementations of rename exist. This documents the Perl version (prename), not the util-linux version."],["INSTALL","```nix: nix profile install nixpkgs#file-rename```"],["SEE ALSO","[rename](/man/rename)(1), [mv](/man/mv)(1), [mmv](/man/mmv)(1), [find](/man/find)(1), [perl](/man/perl)(1), [sed](/man/sed)(1)"]],"e":["rename","mv","mmv","find","perl","sed"],"x":"file-rename 's/old/new/' [files]"},"flake8":{"n":"flake8","t":"Python code linter and style checker","s":[["TAGLINE","Python code linter and style checker"],["TLDR","**Check Python file**\n\n```flake8 [file.py]```\n\n**Check directory**\n\n```flake8 [src/]```\n\n**Show specific errors**\n\n```flake8 --select=[E,W] [file.py]```\n\n**Ignore specific errors**\n\n```flake8 --ignore=[E501] [file.py]```\n\n**Set max line** length\n\n```flake8 --max-line-length=[120] [file.py]```"],["SYNOPSIS","**flake8** [_options_] [_files_...]"],["PARAMETERS","_FILES_\n> Python files or directories to check.\n\n**--select** _ERRORS_\n> Check only specific error codes.\n\n**--ignore** _ERRORS_\n> Ignore specific error codes.\n\n**--max-line-length** _N_\n> Maximum line length (default 79).\n\n**--exclude** _PATTERNS_\n> Exclude matching files/directories.\n\n**--config** _FILE_\n> Configuration file.\n\n**--format** _FORMAT_\n> Output format.\n\n**--help**\n> Display help information."],["CONFIGURATION","**setup.cfg**\n> Project-level flake8 configuration in the [flake8] section.\n\n**.flake8**\n> Dedicated flake8 configuration file for project settings.\n\n**~/.config/flake8**\n> User-level global configuration file."],["DESCRIPTION","**flake8** is a Python linting tool combining PyFlakes, pycodestyle, and McCabe complexity checker. It finds programming errors, style issues, and overly complex code.\n\nThe tool enforces PEP 8 style guidelines and catches common errors like undefined variables, unused imports, and syntax problems. Plugins extend checking for additional frameworks and patterns.\n\nflake8 integrates with editors, CI pipelines, and pre-commit hooks for automated code quality enforcement."],["CAVEATS","Strict defaults may require customization. Some checks overlap with other tools. Plugin ecosystem varies in quality."],["HISTORY","flake8 was created by **Tarek Ziadé** to unify Python linting tools. It wraps multiple checkers under a single interface with consistent configuration and output."],["INSTALL","```apk: sudo apk add py3-flake8```\n\n```brew: brew install flake8```"],["SEE ALSO","[pylint](/man/pylint)(1), [black](/man/black)(1), [ruff](/man/ruff)(1)"],["RESOURCES","```[Source code](https://github.com/PyCQA/flake8)```\n\n```[Documentation](https://flake8.pycqa.org/)```"]],"e":["pylint","black","ruff"],"x":"flake8 [file.py]"},"fresh":{"n":"fresh","t":"Dotfiles manager that sources shell configuration from multiple repos","s":[["TAGLINE","Dotfiles manager that sources shell configuration from multiple repos"],["TLDR","**Build shell configuration** and symlinks from freshrc\n\n```fresh```\n\n**Update all sources** from remote repositories and rebuild\n\n```fresh update```\n\n**Update only local dotfiles** repository\n\n```fresh update --local```\n\n**Update a specific GitHub user's** repositories\n\n```fresh update [username]```\n\n**Search the fresh directory** for configuration examples\n\n```fresh search [keyword]```\n\n**Edit your freshrc** configuration file\n\n```fresh edit```\n\n**Remove dead symlinks** and unused source repos\n\n```fresh clean```\n\n**Show source references** for freshrc lines\n\n```fresh show```"],["SYNOPSIS","**fresh** [_command_] [_options_]"],["PARAMETERS","**install**\n> Build shell configuration and create symlinks. This is the default when no command is given.\n\n**update** [_filter_]\n> Fetch latest changes from source repositories and rebuild. Optionally filter by **--local**, a GitHub _username_, or _username/repo_.\n\n**clean**\n> Remove dead symlinks and unused source repositories.\n\n**search** _keyword_\n> Query the fresh directory wiki for configuration examples matching _keyword_.\n\n**edit**\n> Open **~/.freshrc** in your default **$EDITOR**.\n\n**show**\n> Display freshrc lines with their matching source files.\n\n**help**\n> Show usage documentation."],["DESCRIPTION","**fresh** is a dotfiles manager that works like Bundler for your shell configuration. It aggregates aliases, functions, completions, and configuration files from your own dotfiles and from other users' GitHub repositories into a single built shell script and a set of symlinks.\n\nConfiguration is defined in **~/.freshrc** using a simple DSL. Each line specifies a source file to include, optionally from a GitHub repository. When **fresh** runs, it concatenates all shell sources into **~/.fresh/build/shell.sh** and creates symlinks for non-shell config files. You source the built file from your shell rc to activate everything.\n\nFresh supports three types of sourced content: **shell files** (concatenated into the build script), **config files** (symlinked via **--file**), and **bin files** (made executable via **--bin**)."],["FRESHRC OPTIONS","**--file**[**=**_path_]\n> Link or build a configuration file. With a path like **--file=~/.vimrc**, the file is symlinked to that location. Without a path, files are concatenated into **~/.fresh/build/** using their original name.\n\n**--bin**[**=**_path_]\n> Create an executable symlink in **~/bin/** (or a custom path). The source file is made executable automatically.\n\n**--ref=**_ref_\n> Lock a source to a specific git branch, tag, or commit hash.\n\n**--filter=**_command_\n> Pipe the source file through a shell command before including it (e.g. **--filter=\"gpg -d\"**).\n\n**--marker=**_string_\n> Add comment markers around sourced content for identification in built files.\n\n**--ignore-missing**\n> Suppress errors when a source file is not available."],["CONFIGURATION","Fresh uses **~/.freshrc** as its configuration file. Sources are specified as:\n\n```"],["Local file from ~/.dotfiles/","fresh aliases.sh"],["File from a GitHub repository","fresh jasoncodes/dotfiles shell/aliases/git.sh"],["Non-GitHub git URL","fresh git://example.com/repo.git file.conf --file=~/.config/app.conf"],["Executable script","fresh freshshell/fresh contrib/completion/fresh-completion.bash --bin\n```\n\nUse **fresh-options** blocks to apply consistent flags to multiple lines:\n\n```\nfresh-options --file=~/.vimrc --marker='\"'\n  fresh vim/mappings.vim\n  fresh vim/plugins.vim\nfresh-options\n```\n\nEnvironment variables for customization:\n\n**FRESH_RCFILE** — configuration file path (default: **~/.freshrc**)\n**FRESH_PATH** — installation directory (default: **~/.fresh**)\n**FRESH_LOCAL** — local dotfiles repository (default: **~/.dotfiles**)\n**FRESH_BIN_PATH** — binary installation directory (default: **~/bin**)\n**FRESH_NO_LOCAL_CHECK** — disable local sourcing warnings\n**FRESH_NO_PATH_EXPORT** — skip automatic PATH modification\n**FRESH_NO_BIN_CONFLICT_CHECK** — suppress multi-source binary warnings"],["CAVEATS","Fresh requires **git** and **bash** to be installed. All shell sources are concatenated into a single file, so naming conflicts between sourced functions or aliases from different repositories must be managed manually. The built shell script must be explicitly sourced from your **.bashrc** or **.zshrc** — fresh does not modify these files automatically."],["HISTORY","**fresh** was created by **Jason Weathered** (jasoncodes) and **Scott Barron** (twe4ked) as an open-source shell tool released under the **MIT license**. It was inspired by the concept of Bundler for Ruby, applying the same dependency-sourcing philosophy to dotfiles management across the shell ecosystem."],["SEE ALSO","[git](/man/git)(1), [bash](/man/bash)(1), [stow](/man/stow)(8), [dotbot](/man/dotbot)(1)"]],"e":["git","bash","stow","dotbot"],"x":"fresh"},"git-flow":{"n":"git-flow","t":"High-level branching model workflow commands","s":[["TAGLINE","High-level branching model workflow commands"],["TLDR","**Initialize git flow**\n\n```git flow init```\n\n**Initialize with default branch names**\n\n```git flow init -d```\n\n**Start feature branch**\n\n```git flow feature start [feature-name]```\n\n**Finish feature**\n\n```git flow feature finish [feature-name]```\n\n**Start release**\n\n```git flow release start [version]```\n\n**Finish release**\n\n```git flow release finish [version]```\n\n**Publish a feature branch to remote**\n\n```git flow feature publish [feature-name]```\n\n**Start a hotfix**\n\n```git flow hotfix start [version]```\n\n**Finish a hotfix**\n\n```git flow hotfix finish [version]```"],["SYNOPSIS","**git flow** _subcommand_ [_action_] [_name_]"],["PARAMETERS","**init**\n> Initialize repository for git-flow.\n\n**feature** _ACTION_ _NAME_\n> Manage feature branches.\n\n**release** _ACTION_ _VERSION_\n> Manage release branches.\n\n**hotfix** _ACTION_ _NAME_\n> Manage hotfix branches.\n\n**support** _ACTION_ _NAME_\n> Manage support branches.\n\n**start**\n> Begin a new branch.\n\n**finish**\n> Complete and merge branch.\n\n**publish**\n> Push branch to the remote repository.\n\n**track**\n> Track a remote branch locally.\n\n**pull**\n> Pull a remote branch (deprecated in favor of track).\n\n**delete**\n> Delete a finished branch.\n\n**list**\n> List existing branches of a given type.\n\n**-d**\n> Use default branch naming conventions during init.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git flow** implements Vincent Driessen's branching model, providing high-level commands for managing feature, release, and hotfix branches with consistent naming and merge strategies.\n\nThe workflow uses develop as the integration branch and main/master for releases. Features branch from develop, releases prepare for production, and hotfixes go directly to main. This standardizes team branching workflows with clear conventions."],["CAVEATS","Opinionated workflow. May not suit all projects. Requires initialization per repo."],["HISTORY","git flow was created by **Vincent Driessen** in **2010** based on his successful git branching model blog post."],["INSTALL","```apk: sudo apk add git-flow```\n\n```brew: brew install git-flow```"],["SEE ALSO","[git-branch](/man/git-branch)(1), [git-merge](/man/git-merge)(1), [git-checkout](/man/git-checkout)(1), [git-tag](/man/git-tag)(1)"]],"e":["git-branch","git-merge","git-checkout","git-tag"],"x":"git flow init"},"gotty":{"n":"gotty","t":"Share terminal as a web application","s":[["TAGLINE","Share terminal as a web application"],["TLDR","**Share terminal as web**\n\n```gotty [command]```\n\n**Share with write permission**\n\n```gotty -w [bash]```\n\n**Specify port**\n\n```gotty -p [8080] [command]```\n\n**With authentication**\n\n```gotty -c [user:pass] [command]```\n\n**Random URL path**\n\n```gotty --random-url [command]```"],["SYNOPSIS","**gotty** [_options_] _command_"],["PARAMETERS","_COMMAND_\n> Command to share.\n\n**-w**, **--permit-write**\n> Allow input from browser.\n\n**-p** _PORT_, **--port** _PORT_\n> Server port.\n\n**-c** _CRED_, **--credential** _CRED_\n> Basic authentication credentials in the form user:pass.\n\n**-r**, **--random-url**\n> Add a random string to the URL path.\n\n**-t**, **--tls**\n> Enable TLS/SSL.\n\n**--tls-crt** _PATH_\n> Path to TLS certificate file.\n\n**--tls-key** _PATH_\n> Path to TLS key file.\n\n**--reconnect**\n> Allow client to reconnect after the connection is closed.\n\n**--once**\n> Quit after the first client disconnects.\n\n**--config** _FILE_\n> Configuration file path.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gotty** shares a terminal command as a web application accessible through a browser. It runs a command and exposes it through HTTP, enabling remote terminal access, log viewing, and collaborative debugging.\n\nAuthentication and TLS are supported for secure deployments. The **-w** flag enables browser users to send input to the running command."],["CONFIGURATION","**~/.gotty**\n> Configuration file for default settings like port, TLS certificates, and credential options."],["CAVEATS","Security sensitive with -w flag. Use authentication in production. Firewall considerations."],["HISTORY","gotty was created by **Iwasaki Yudai** to share terminal sessions through web browsers."],["INSTALL","```nix: nix profile install nixpkgs#gotty```"],["SEE ALSO","[ttyd](/man/ttyd)(1), [tmux](/man/tmux)(1)"]],"e":["ttyd","tmux"],"x":"gotty [command]"},"ic":{"n":"ic","t":"context switcher for various tools","s":[["TAGLINE","context switcher for various tools"],["TLDR","**Show current context**\n\n```ic```\n\n**Switch context**\n\n```ic [context-name]```\n\n**List contexts**\n\n```ic -l```\n\n**Create context**\n\n```ic -c [name]```"],["SYNOPSIS","**ic** [_options_] [_context_]"],["PARAMETERS","_CONTEXT_\n> Context name to switch to.\n\n**-l**, **--list**\n> List contexts.\n\n**-c**, **--create** _NAME_\n> Create new context.\n\n**-d**, **--delete** _NAME_\n> Delete context.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ic** is a context switcher for various tools. Different implementations exist for kubectl contexts, AWS profiles, and other multi-environment tools.\n\nThe tool simplifies switching between configurations. It provides quick access to different environments or clusters."],["CAVEATS","Multiple tools use this name. Check which version installed. Context-specific behavior."],["HISTORY","ic is commonly used as a shorthand for interactive context switching in various CLI tools."],["SEE ALSO","[kubectx](/man/kubectx)(1), [aws-vault](/man/aws-vault)(1)"]],"e":["kubectx","aws-vault"],"x":"ic"},"icinga2":{"n":"icinga2","t":"monitoring system that checks availability of hosts and services","s":[["TAGLINE","monitoring system that checks availability of hosts and services"],["TLDR","**Check configuration**\n\n```icinga2 daemon -C```\n\n**Start Icinga 2**\n\n```sudo systemctl start icinga2```\n\n**Reload configuration**\n\n```sudo systemctl reload icinga2```\n\n**Show features**\n\n```icinga2 feature list```\n\n**Enable feature**\n\n```sudo icinga2 feature enable [api]```\n\n**List objects**\n\n```icinga2 object list --type Host```"],["SYNOPSIS","**icinga2** _command_ [_options_]"],["PARAMETERS","**daemon** [**-C**]\n> Run daemon (or check config with -C).\n\n**feature** _subcommand_\n> Manage features (list, enable, disable).\n\n**object** _subcommand_\n> Object management (list).\n\n**node** _subcommand_\n> Node/cluster management.\n\n**pki** _subcommand_\n> PKI/certificate management.\n\n**console**\n> Open debug console.\n\n**--version**\n> Show version."],["DESCRIPTION","**Icinga 2** is a monitoring system that checks availability of hosts and services. It supports distributed monitoring, advanced configurations, and integrates with various backends.\n\nIcinga 2 uses a domain-specific language (DSL) for configuration, supporting templates, apply rules, and runtime modifications through its API."],["FEATURES","- Host and service monitoring\n- Distributed/clustered setup\n- REST API\n- Graphite/InfluxDB integration\n- Notification system\n- Performance data"],["CAVEATS","Configuration syntax differs from Icinga 1/Nagios. Cluster setup requires PKI. Large installations need tuning. Web interface (Icinga Web 2) installed separately."],["HISTORY","Icinga was forked from **Nagios** in **2009** due to disagreements about project governance. Icinga 2, released in **2014**, was a complete rewrite with modern architecture and new configuration language."],["INSTALL","```apt: sudo apt install icinga2-bin```\n\n```apk: sudo apk add icinga2```\n\n```zypper: sudo zypper install icinga2-bin```\n\n```nix: nix profile install nixpkgs#icinga2```"],["SEE ALSO","[nagios](/man/nagios)(8), [prometheus](/man/prometheus)(1)"]],"e":["nagios","prometheus"],"x":"icinga2 daemon -C"},"ipset":{"n":"ipset","t":"Create and manage IP address sets for firewall rules","s":[["TAGLINE","Create and manage IP address sets for firewall rules"],["TLDR","**Create** an empty IP set\n\n```ipset create [set_name] hash:ip```\n\n**Add** an IP address to a set\n\n```ipset add [set_name] [192.168.1.25]```\n\n**Delete** an IP address from a set\n\n```ipset del [set_name] [192.168.1.25]```\n\n**Destroy** an IP set\n\n```ipset destroy [set_name]```\n\n**Save** an IP set to file\n\n```ipset save [set_name] > [path/to/ip_set]```\n\n**List** all IP sets\n\n```ipset list```\n\n**Test** if an IP is in a set\n\n```ipset test [set_name] [192.168.1.25]```\n\n**Restore** IP sets from file\n\n```ipset restore < [path/to/ip_set]```"],["SYNOPSIS","**ipset** _command_ [_options_]"],["PARAMETERS","**create** _NAME_ _TYPE_\n> Create a new IP set (types: hash:ip, hash:net, hash:ip,port, etc.)\n\n**add** _NAME_ _ENTRY_\n> Add entry to a set\n\n**del** _NAME_ _ENTRY_\n> Remove entry from a set\n\n**destroy** _NAME_\n> Delete a set\n\n**list** [_NAME_]\n> List set contents\n\n**save** [_NAME_]\n> Output sets in restorable format\n\n**restore**\n> Restore sets from saved output\n\n**test** _NAME_ _ENTRY_\n> Test if an entry is in a set\n\n**flush** [_NAME_]\n> Clear all entries from a set\n\n**-exist**\n> Ignore errors when adding already existing entries or deleting non-existing entries\n\n**-quiet**\n> Suppress output"],["DESCRIPTION","**ipset** creates and manages IP sets, a framework for storing IP addresses, networks, ports, and combinations thereof. Sets can be referenced in iptables/nftables rules for efficient matching against large lists.\n\nDifferent set types support different entry formats: hash:ip for individual addresses, hash:net for CIDR ranges, hash:ip,port for address-port combinations. Sets use hash tables for O(1) lookup performance."],["CAVEATS","Requires root privileges. Sets must be created before they can be referenced in firewall rules. Set contents are not persistent across reboots without explicit save/restore."],["HISTORY","ipset was developed by Jozsef Kadlecsik to provide efficient set-based matching for the Linux firewall. It became part of the standard Linux networking toolkit and is widely used for implementing blocklists and allowlists."],["INSTALL","```apt: sudo apt install ipset```\n\n```dnf: sudo dnf install ipset```\n\n```pacman: sudo pacman -S ipset```\n\n```apk: sudo apk add ipset```\n\n```zypper: sudo zypper install ipset```\n\n```nix: nix profile install nixpkgs#ipset```"],["SEE ALSO","[iptables](/man/iptables)(8), [nft](/man/nft)(8), [firewall-cmd](/man/firewall-cmd)(1)"]],"e":["iptables","nft","firewall-cmd"],"x":"ipset create [set_name] hash:ip"},"iptables-save":{"n":"iptables-save","t":"dumps the current iptables IPv4 configuration to stdout or a file","s":[["TAGLINE","dumps the current iptables IPv4 configuration to stdout or a file"],["TLDR","**Print** all rules\n\n```sudo iptables-save```\n\nPrint **specific table**\n\n```sudo iptables-save -t filter```\n\n**Save** to file\n\n```sudo iptables-save -f /etc/iptables.rules```"],["SYNOPSIS","**iptables-save** [_OPTIONS_]"],["DESCRIPTION","**iptables-save** dumps the current iptables IPv4 configuration to stdout or a file. The output can be restored using iptables-restore. Use ip6tables-save for IPv6 rules."],["PARAMETERS","**-t, --table** _table_\n> Only output rules for the specified table (filter, nat, mangle, raw, security)\n\n**-f, --file** _filename_\n> Write output to file instead of stdout\n\n**-c, --counters**\n> Include packet and byte counters in output"],["CAVEATS","This saves only IPv4 rules. Use ip6tables-save for IPv6. Rules are output in a format suitable for iptables-restore. Counter values change constantly on active systems."],["HISTORY","**iptables-save** is part of the **iptables** package for managing the Linux kernel firewall."],["INSTALL","```pacman: sudo pacman -S iptables```\n\n```apk: sudo apk add iptables```\n\n```zypper: sudo zypper install iptables```\n\n```brew: brew install iptables```\n\n```nix: nix profile install nixpkgs#iptables```"],["SEE ALSO","[iptables](/man/iptables)(8), [iptables-restore](/man/iptables-restore)(8), [ip6tables-save](/man/ip6tables-save)(8)"]],"e":["iptables","iptables-restore","ip6tables-save"],"x":"sudo iptables-save"},"jj-restore":{"n":"jj-restore","t":"restores file contents from another revision","s":[["TAGLINE","restores file contents from another revision"],["TLDR","**Restore files from parent**\n\n```jj restore```\n\n**Restore specific files**\n\n```jj restore [file1] [file2]```\n\n**Restore from specific revision**\n\n```jj restore --from [revision]```\n\n**Restore to specific revision**\n\n```jj restore --to [revision] --from [source]```\n\n**Restore files matching a revset**\n\n```jj restore -c [revision] [path]```\n\n**Restore and keep changes as a new commit**\n\n```jj restore --changes-in [revision]```"],["SYNOPSIS","**jj** **restore** [_options_] [_paths_...]"],["PARAMETERS","**--from** _revision_\n> Source revision for content.\n\n**--to** _revision_\n> Target revision to modify."],["DESCRIPTION","**jj restore** restores file contents from another revision. By default, restores from the parent revision, effectively discarding changes. Can restore specific files or entire working copy."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-revert](/man/jj-revert)(1)"]],"e":["jj","jj-revert"],"x":"jj restore"},"kops":{"n":"kops","t":"manages production Kubernetes clusters","s":[["TAGLINE","manages production Kubernetes clusters"],["TLDR","**Create cluster**\n\n```kops create cluster --name=[cluster.example.com] --zones=[us-east-1a]```\n\n**Update cluster**\n\n```kops update cluster [cluster.example.com] --yes```\n\n**Validate cluster**\n\n```kops validate cluster [cluster.example.com]```\n\n**Delete cluster**\n\n```kops delete cluster [cluster.example.com] --yes```\n\n**Edit cluster**\n\n```kops edit cluster [cluster.example.com]```\n\n**Export kubeconfig**\n\n```kops export kubeconfig [cluster.example.com]```"],["SYNOPSIS","**kops** _command_ [_options_]"],["PARAMETERS","**create cluster**\n> Create cluster configuration.\n\n**update cluster**\n> Apply cluster changes.\n\n**validate cluster**\n> Verify cluster health.\n\n**delete cluster**\n> Delete cluster.\n\n**--name** _NAME_\n> Cluster name.\n\n**--zones** _ZONES_\n> Availability zones.\n\n**--yes**\n> Confirm changes.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kops** manages production Kubernetes clusters. It creates, updates, and destroys clusters on cloud providers.\n\nThe tool handles networking, node groups, and add-ons. It supports AWS, GCE, and other clouds."],["CAVEATS","Cloud provider credentials needed. DNS configuration required. Production-focused tool."],["HISTORY","kops (Kubernetes Operations) was created to help manage production-grade Kubernetes clusters on cloud infrastructure."],["INSTALL","```brew: brew install kops```\n\n```nix: nix profile install nixpkgs#kops```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [eksctl](/man/eksctl)(1), [kubeadm](/man/kubeadm)(1)"]],"e":["kubectl","eksctl","kubeadm"],"x":"kops create cluster --name=[cluster.example.com] --zones=[us-east-1a]"},"kubectl-wait":{"n":"kubectl-wait","t":"blocks until conditions are met","s":[["TAGLINE","blocks until conditions are met"],["TLDR","**Wait for pod ready**\n\n```kubectl wait --for=condition=Ready pod/[pod-name]```\n\n**Wait for deletion**\n\n```kubectl wait --for=delete pod/[pod-name]```\n\n**Wait for creation**\n\n```kubectl wait --for=create secret/[name]```\n\n**Wait with timeout**\n\n```kubectl wait --for=condition=Ready pod/[pod-name] --timeout=[60s]```\n\n**Wait for deployment available**\n\n```kubectl wait --for=condition=Available deployment/[name]```\n\n**Wait with label selector across resources**\n\n```kubectl wait --for=condition=Ready pods -l [app=myapp]```\n\n**Wait for a JSONPath value**\n\n```kubectl wait --for=jsonpath='{.status.phase}'=Running pod/[name]```\n\n**Wait across all namespaces**\n\n```kubectl wait --for=condition=Ready pods --all -A```"],["SYNOPSIS","**kubectl wait** [_options_] _resource_"],["PARAMETERS","_RESOURCE_\n> Resource(s) to wait on, e.g. `pod/busybox`, `deployment/web`, or a type like `pods`.\n\n**--for** _EXPR_\n> Condition to wait on. Accepts `create`, `delete`, `condition=<name>[=<value>]`, or `jsonpath='{...}'[=<value>]`. Default condition value is `true`.\n\n**--timeout** _DURATION_\n> Maximum time to wait (e.g. 30s, 5m). Default is 30s. Zero means check once; a negative value waits up to a week.\n\n**--all**\n> Select all resources of the given type in the namespace.\n\n**-A**, **--all-namespaces**\n> Operate across all namespaces.\n\n**-l**, **--selector** _QUERY_\n> Label selector to filter resources.\n\n**--field-selector** _QUERY_\n> Field selector to filter resources.\n\n**-f**, **--filename** _FILE_\n> Identify the resource(s) via a manifest file or directory.\n\n**-n**, **--namespace** _NS_\n> Target namespace.\n\n**-o**, **--output** _FORMAT_\n> Output format (json, yaml, name, go-template, jsonpath, ...).\n\n**--help**\n> Display help information."],["DESCRIPTION","**kubectl wait** blocks execution until one or more Kubernetes resources reach a specified condition, making it an essential synchronization primitive for shell scripts, CI/CD pipelines, and automation workflows. Rather than polling with repeated `kubectl get` calls, it efficiently watches the resource and returns as soon as the condition is satisfied or the timeout expires.\n\nThe command supports built-in condition checks such as `condition=Ready` and `condition=Available`, resource deletion events via `--for=delete`, and arbitrary field matching through JSONPath expressions like `jsonpath='{.status.phase}'=Running`. It can target individual resources by name or groups of resources matched by label selectors, and always defaults to a 30-second timeout unless overridden with --timeout."],["CAVEATS","Subcommand of kubectl. Blocks execution. Set reasonable timeouts."],["HISTORY","kubectl wait provides synchronization for **Kubernetes** automation and scripting."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-get](/man/kubectl-get)(1), [kubectl-apply](/man/kubectl-apply)(1), [kubectl-rollout](/man/kubectl-rollout)(1), [kubectl-describe](/man/kubectl-describe)(1)"]],"e":["kubectl","kubectl-get","kubectl-apply","kubectl-rollout","kubectl-describe"],"x":"kubectl wait --for=condition=Ready pod/[pod-name]"},"liquidctl":{"n":"liquidctl","t":"controls liquid coolers and RGB devices","s":[["TAGLINE","controls liquid coolers and RGB devices"],["TLDR","**List devices**\n\n```liquidctl list```\n\n**Initialize device**\n\n```liquidctl initialize```\n\n**Set pump speed**\n\n```liquidctl set pump speed [50]```\n\n**Set fan curve**\n\n```liquidctl set fan speed 20 30 30 50 40 70 50 100```\n\n**Set LED color**\n\n```liquidctl set led color fixed [ff0000]```\n\n**Show device status**\n\n```liquidctl status```"],["SYNOPSIS","**liquidctl** [_options_] _command_ [_args_]"],["PARAMETERS","_COMMAND_\n> Operation to perform.\n\n**list**\n> List supported devices.\n\n**initialize**\n> Initialize device.\n\n**status**\n> Show device status.\n\n**set** _CHANNEL_ _MODE_ _VALUE_\n> Configure device settings.\n\n**--match** _PATTERN_\n> Match specific device.\n\n**--help**\n> Display help information."],["DESCRIPTION","**liquidctl** controls liquid coolers and RGB devices. It manages pump speed, fan curves, and LED lighting.\n\nThe tool supports many AIO coolers and RGB controllers from various manufacturers."],["CAVEATS","Requires root or udev rules. Device support varies. Some features hardware-specific."],["HISTORY","liquidctl was created to provide open source control for AIO liquid coolers and RGB peripherals on Linux."],["INSTALL","```apt: sudo apt install liquidctl```\n\n```dnf: sudo dnf install liquidctl```\n\n```pacman: sudo pacman -S liquidctl```\n\n```apk: sudo apk add liquidctl```\n\n```brew: brew install liquidctl```\n\n```nix: nix profile install nixpkgs#liquidctl```"],["SEE ALSO","[sensors](/man/sensors)(1), [fancontrol](/man/fancontrol)(1)"]],"e":["sensors","fancontrol"],"x":"liquidctl list"},"loadtest":{"n":"loadtest","t":"node","s":[["TAGLINE","node"],["TLDR","**Run load test**\n\n```loadtest -c [10] -n [1000] [http://example.com/api]```\n\n**Run with requests per second limit**\n\n```loadtest -c [10] --rps [100] [http://example.com/api]```\n\n**POST request with body**\n\n```loadtest -c [5] -P '[{\"key\":\"value\"}]' -T 'application/json' [http://example.com/api]```"],["SYNOPSIS","**loadtest** [_options_] _url_"],["PARAMETERS","**-c** _num_\n> Number of concurrent clients.\n\n**-n** _num_\n> Total number of requests.\n\n**--rps** _num_\n> Requests per second limit.\n\n**-t** _seconds_\n> Maximum run time.\n\n**-P** _body_\n> POST request body.\n\n**-T** _type_\n> Content-Type header.\n\n**-H** _header_\n> Custom header.\n\n**-k**\n> Keep-alive connections."],["DESCRIPTION","**loadtest** is a Node.js load testing tool for HTTP APIs. Generates configurable concurrent requests to measure server performance and identify bottlenecks. Reports latency percentiles, throughput, and error rates."],["SEE ALSO","[ab](/man/ab)(1), [wrk](/man/wrk)(1), [siege](/man/siege)(1)"]],"e":["ab","wrk","siege"],"x":"loadtest -c [10] -n [1000] [http://example.com/api]"},"logger":{"n":"logger","t":"adds entries to the system log","s":[["TAGLINE","adds entries to the system log"],["TLDR","Log a **message**\n\n```logger [message]```\n\nLog from **stdin**\n\n```echo [message] | logger```\n\nLog to **remote server**\n\n```echo [message] | logger -n [hostname] -P [port]```\n\nLog with **custom tag**\n\n```echo [message] | logger -t [tag]```\n\nLog with **priority**\n\n```echo [message] | logger -p user.warning```"],["SYNOPSIS","**logger** [_options_] [_message_]"],["DESCRIPTION","**logger** adds entries to the system log (syslog). It is useful for logging from shell scripts or adding custom messages to system logs for debugging and monitoring."],["PARAMETERS","**-n, --server HOST**\n> Send to remote syslog server\n\n**-P, --port PORT**\n> Remote server port (default: 514)\n\n**-t, --tag TAG**\n> Mark messages with tag (default: username)\n\n**-p, --priority PRIORITY**\n> Log priority (facility.level, default: user.notice)\n\n**-s, --stderr**\n> Also output to stderr\n\n**-f, --file FILE**\n> Log contents of file\n\n**-i**\n> Include process ID"],["PRIORITY LEVELS","Facilities: auth, cron, daemon, kern, local0-7, mail, news, syslog, user, uucp\nLevels: emerg, alert, crit, err, warning, notice, info, debug"],["INSTALL","```apt: sudo apt install bsdutils```\n\n```apk: sudo apk add logger```\n\n```nix: nix profile install nixpkgs#logger```"],["SEE ALSO","[journalctl](/man/journalctl)(1)"]],"e":["journalctl"],"x":"logger [message]"},"lsr":{"n":"lsr","t":"Colorized ls alternative with Git integration and Lua extensibility","s":[["TAGLINE","Colorized ls alternative with Git integration and Lua extensibility"],["TLDR","**List files** in the current directory\n\n```lsr```\n\n**List files recursively** as a tree\n\n```lsr -R```\n\n**List files with Git status**\n\n```lsr -g```\n\n**List all files including hidden ones**\n\n```lsr -a```\n\n**List files in a specific directory**\n\n```lsr [/path/to/directory]```"],["SYNOPSIS","**lsr** [_options_] [_path_...]"],["PARAMETERS","**-R**, **--recursive**\n> Display files in a recursive tree structure.\n\n**-g**, **--git**\n> Show Git status indicators for staged and unstaged changes.\n\n**-a**, **--all**\n> Show hidden files."],["DESCRIPTION","**lsr** (also known as Laser) is a command-line alternative to **ls** that prioritizes readability and usability. It provides colorized file listings with improved visual organization, recursive tree display, Git status indicators, and file/symlink identification through visual flags.\n\nColor schemes are customizable, and the tool is extensible through **Lua scripts** for displaying custom information beyond what standard ls offers."],["CAVEATS","Lua scripting extension requires Lua to be available on the system."],["HISTORY","**lsr** was created by **jmattaa** and is written in **C**. It supports macOS, Linux, BSD, and Windows."],["INSTALL","```brew: brew install lsr```\n\n```nix: nix profile install nixpkgs#lsr```"],["SEE ALSO","[ls](/man/ls)(1), [eza](/man/eza)(1), [tree](/man/tree)(1), [lsd](/man/lsd)(1), [exa](/man/exa)(1)"]],"e":["ls","eza","tree","lsd","exa"],"x":"lsr"},"lvmconfig":{"n":"lvmconfig","t":"displays and manipulates Logical Volume Manager configuration information","s":[["TAGLINE","displays and manipulates Logical Volume Manager configuration information"],["TLDR","Display the **effective configuration** in use (after merging all config sources)\n\n```lvmconfig --typeconfig current --mergedconfig```\n\nShow only settings that **differ from defaults**\n\n```lvmconfig --typeconfig diff```\n\n**List all configuration keys**\n\n```lvmconfig -l```\n\nPrint the **default configuration** with full comments and extra spacing\n\n```lvmconfig --typeconfig default --withcomments --withspaces```\n\n**Validate** the full merged configuration and report errors\n\n```lvmconfig --mergedconfig --validate```\n\n**Write** the current effective configuration to a file\n\n```lvmconfig --typeconfig current -f [path/to/output.conf]```"],["SYNOPSIS","**lvmconfig** [_options_] [_section_]"],["PARAMETERS","**--typeconfig _type_**\n> Specify configuration type: current, default, diff, full, list, missing, new, profilable, or profilable-command\n\n**--mergedconfig**\n> Merge all configuration sources before display\n\n**-l, --list**\n> List all configuration keys\n\n**--withcomments**\n> Include explanatory comments in output\n\n**--withspaces**\n> Add extra spacing for readability\n\n**--validate**\n> Check configuration for errors and report problems\n\n**-f, --file _path_**\n> Write output to a file instead of stdout\n\n**--config _string_**\n> Override configuration settings from the command line"],["DESCRIPTION","**lvmconfig** displays and manipulates Logical Volume Manager (LVM) configuration information. It reads configuration from multiple sources (command line, environment, config files) and can show the effective merged configuration.\n\nThe tool supports different configuration views: **current** shows active settings, **default** shows built-in defaults, **diff** shows only non-default values, and **full** shows everything. It can also validate configuration files for syntax errors and deprecated options.\n\nConfiguration sections include global settings, device filters, activation options, and allocation policies for volume groups and logical volumes."],["CAVEATS","Configuration changes made with lvmconfig are not persistent; edit **/etc/lvm/lvm.conf** directly for permanent changes. The **--validate** option may not catch all configuration issues, particularly those that depend on runtime conditions."],["INSTALL","```apt: sudo apt install lvm2```\n\n```dnf: sudo dnf install lvm2```\n\n```pacman: sudo pacman -S lvm2```\n\n```apk: sudo apk add lvm2```\n\n```zypper: sudo zypper install lvm2```\n\n```nix: nix profile install nixpkgs#lvm2```"],["SEE ALSO","[lvm](/man/lvm)(8), [pvcreate](/man/pvcreate)(8), [vgcreate](/man/vgcreate)(8), [lvcreate](/man/lvcreate)(8)"]],"e":["lvm","pvcreate","vgcreate","lvcreate"],"x":"lvmconfig --typeconfig current --mergedconfig"},"lzip":{"n":"lzip","t":"lossless data compressor using the LZMA algorithm","s":[["TAGLINE","lossless data compressor using the LZMA algorithm"],["TLDR","**Compress file**\n\n```lzip [file]```\n\n**Decompress file**\n\n```lzip -d [file.lz]```\n\n**Compress with maximum compression**\n\n```lzip -9 [file]```\n\n**Keep original file**\n\n```lzip -k [file]```\n\n**Test archive integrity**\n\n```lzip -t [file.lz]```\n\n**Compress to stdout**\n\n```lzip -c [file] > [output.lz]```"],["SYNOPSIS","**lzip** [_options_] [_file_...]"],["PARAMETERS","**-d**, **--decompress**\n> Decompress files.\n\n**-k**, **--keep**\n> Keep input files.\n\n**-c**, **--stdout**\n> Write to standard output.\n\n**-t**, **--test**\n> Test archive integrity.\n\n**-1** to **-9**\n> Compression level (default 6).\n\n**-v**, **--verbose**\n> Verbose mode."],["DESCRIPTION","**lzip** is a lossless data compressor using the LZMA algorithm. Provides high compression ratios similar to xz but with a simpler format. Files have .lz extension. Designed for long-term archival."],["INSTALL","```dnf: sudo dnf install lzip```\n\n```pacman: sudo pacman -S lzip```\n\n```apk: sudo apk add lzip```\n\n```zypper: sudo zypper install lzip```\n\n```brew: brew install lzip```\n\n```nix: nix profile install nixpkgs#lzip```"],["SEE ALSO","[xz](/man/xz)(1), [gzip](/man/gzip)(1), [lzop](/man/lzop)(1)"]],"e":["xz","gzip","lzop"],"x":"lzip [file]"},"macmon":{"n":"macmon","t":"Sudoless performance monitoring for Apple Silicon processors","s":[["TAGLINE","Sudoless performance monitoring for Apple Silicon processors"],["TLDR","**Launch the system monitor**\n\n```macmon```\n\n**Output metrics as JSON**\n\n```macmon pipe```\n\n**Set update interval** in milliseconds\n\n```macmon -i [ms]```"],["SYNOPSIS","**macmon** [_command_] [_options_]"],["PARAMETERS","**-i**, **--interval** _MS_\n> Update interval in milliseconds (default: 1000).\n\n**pipe**\n> Output metrics in JSON format for piping to other tools.\n\n**debug**\n> Print debug information."],["DESCRIPTION","**macmon** is a sudoless performance monitoring tool for Apple Silicon processors. It displays real-time CPU, GPU, and Apple Neural Engine (ANE) power usage, CPU utilization per cluster, RAM/Swap usage, temperature, and historical charts with average and max values.\n\nIt uses an undocumented Apple API (the same used by powermetrics) to access system metrics without requiring sudo. The tool includes 6 switchable color themes."],["CAVEATS","Only works on Apple Silicon Macs (M1 and later). Uses undocumented APIs that may change between macOS versions."],["HISTORY","**macmon** was created by **vladkens** and is written in **Rust**."],["INSTALL","```brew: brew install macmon```\n\n```nix: nix profile install nixpkgs#macmon```"],["SEE ALSO","[htop](/man/htop)(1), [btop](/man/btop)(1)"]],"e":["htop","btop"],"x":"macmon"},"mypy":{"n":"mypy","t":"static type checker for Python","s":[["TAGLINE","static type checker for Python"],["TLDR","**Type check file**\n\n```mypy [script.py]```\n\n**Type check directory**\n\n```mypy [src/]```\n\n**Strict mode**\n\n```mypy --strict [script.py]```\n\n**Ignore missing imports**\n\n```mypy --ignore-missing-imports [script.py]```\n\n**Show error codes**\n\n```mypy --show-error-codes [script.py]```\n\n**Check specific Python version**\n\n```mypy --python-version [3.10] [script.py]```\n\n**Generate HTML report**\n\n```mypy --html-report [report/] [script.py]```"],["SYNOPSIS","**mypy** [_options_] _files_"],["PARAMETERS","_FILES_\n> Python files to check.\n\n**--strict**\n> Enable all optional error-checking flags (strict mode).\n\n**--ignore-missing-imports**\n> Silence errors about imports that cannot be resolved.\n\n**--show-error-codes**\n> Display error codes in messages (default in recent versions).\n\n**--python-version** _X.Y_\n> Type check code as if running on the specified Python version.\n\n**--disallow-untyped-defs**\n> Disallow defining functions without type annotations.\n\n**--follow-imports** _MODE_\n> How to handle imports (normal, silent, skip, error).\n\n**--html-report** _DIR_\n> Generate HTML type-check coverage report (requires lxml).\n\n**--install-types**\n> Install missing third-party type stubs automatically.\n\n**--namespace-packages**\n> Support PEP 420 namespace packages.\n\n**--config-file** _FILE_\n> Use specific mypy configuration file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mypy** is a static type checker for Python. It validates type annotations.\n\nThe tool catches type errors before runtime. Supports gradual typing adoption."],["CAVEATS","Requires type annotations. Third-party stubs may be needed. Optional strict mode."],["HISTORY","mypy was created by **Jukka Lehtosalo** and is developed at Dropbox as the premier Python type checker."],["INSTALL","```pacman: sudo pacman -S mypy```\n\n```apk: sudo apk add py3-mypy```\n\n```brew: brew install mypy```\n\n```nix: nix profile install nixpkgs#mypy```"],["SEE ALSO","[python](/man/python)(1), [pyright](/man/pyright)(1), [pylint](/man/pylint)(1), [ruff](/man/ruff)(1), [black](/man/black)(1), [flake8](/man/flake8)(1)"]],"e":["python","pyright","pylint","ruff","black","flake8"],"x":"mypy [script.py]"},"neofetch":{"n":"neofetch","t":"command-line system information tool with ASCII art","s":[["TAGLINE","command-line system information tool with ASCII art"],["TLDR","**Display system information** with distro logo\n\n```neofetch```\n\n**Use a custom ASCII art** from a file\n\n```neofetch --ascii [path/to/ascii.txt]```\n\n**Use a specific distro's logo**\n\n```neofetch --ascii_distro [arch]```\n\n**Display only specific info** lines\n\n```neofetch --disable [gpu] [memory]```\n\n**Use an image** instead of ASCII art\n\n```neofetch --backend [kitty] --source [path/to/image.png]```\n\n**Display without ASCII art**\n\n```neofetch --off```\n\n**Save output to file**\n\n```neofetch --stdout > [neofetch.txt]```"],["SYNOPSIS","**neofetch** [_options_]"],["PARAMETERS","**--disable** _info_\n> Disable specific info lines (e.g., gpu, memory, uptime)\n\n**--ascii_distro** _distro_\n> Use specified distro's ASCII art\n\n**--ascii** _path_\n> Path to custom ASCII art file\n\n**--source** _path_\n> Path to image file (used with image backends)\n\n**--backend** _backend_\n> Image backend (ascii, kitty, iterm2, sixel, w3m, etc.)\n\n**--off**\n> Disable ASCII art entirely\n\n**--color_blocks** _on/off_\n> Enable or disable color blocks\n\n**--colors** _x x x x x x_\n> Change text colors (title, @, underline, subtitle, colon, info)\n\n**--ascii_colors** _x x x x x x_\n> Change ASCII art colors\n\n**--bold** _on/off_\n> Enable or disable bold text\n\n**--stdout**\n> Print output to stdout (useful for piping/saving)\n\n**--config** _path_\n> Use specified config file\n\n**--print_config**\n> Print current config to stdout\n\n**--help**\n> Display help message\n\n**--version**\n> Display version"],["DESCRIPTION","**neofetch** is a command-line system information tool that displays information about your operating system, software, and hardware in an aesthetically pleasing format alongside your distribution's ASCII logo or a custom image.\n\nThe information displayed includes the operating system, kernel version, uptime, packages installed, shell, resolution, desktop environment, window manager, terminal, CPU, GPU, and memory usage. All aspects of the output are highly configurable through command-line options or a configuration file.\n\nNeofetch is popular for showing off system setups in screenshots, particularly in the Unix and Linux customization communities. It supports a wide range of operating systems including Linux, BSD, macOS, iOS, and Windows."],["CAVEATS","Image display requires a compatible terminal emulator (kitty, iTerm2, or one supporting sixel/w3m). Some information may not be available on all systems or may require additional tools. First run creates a config file which can significantly alter default behavior."],["HISTORY","**neofetch** was created by **Dylan Araps** and first released in **2015** as a bash script. It quickly gained popularity as a more feature-rich alternative to **screenfetch**. The project became well-known in the Unix rice (customization) community for its extensive customization options and wide distribution support. In **April 2024**, the original developer archived the project, though community forks like **hyfetch** and **fastfetch** continue its legacy."],["INSTALL","```aur: yay -S neofetch```"],["SEE ALSO","[screenfetch](/man/screenfetch)(1), [fastfetch](/man/fastfetch)(1), [inxi](/man/inxi)(1), [hwinfo](/man/hwinfo)(1), [lshw](/man/lshw)(1)"]],"e":["screenfetch","fastfetch","inxi","hwinfo","lshw"],"x":"neofetch"},"nix-develop":{"n":"nix-develop","t":"enters a development shell","s":[["TAGLINE","enters a development shell"],["TLDR","**Enter development shell**\n\n```nix develop```\n\n**Develop with flake**\n\n```nix develop [path/to/flake]```\n\n**Run command in shell**\n\n```nix develop --command [bash -c \"make\"]```\n\n**Develop specific output**\n\n```nix develop [.#devShells.default]```\n\n**Develop with impure**\n\n```nix develop --impure```\n\n**Build first**\n\n```nix develop --build```"],["SYNOPSIS","**nix** **develop** [_options_] [_installable_]"],["PARAMETERS","_INSTALLABLE_\n> Flake reference or path.\n\n**--command** _CMD_\n> Run command in shell.\n\n**--impure**\n> Allow impure evaluation.\n\n**--build**\n> Build before entering.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nix develop** enters a development shell. It provides build dependencies.\n\nThe tool sets up environment variables. Based on flake devShell or shell.nix."],["CAVEATS","Requires flakes enabled. Or shell.nix compatibility. Environment isolation."],["HISTORY","nix develop is part of **Nix 2.4+** flakes, providing reproducible development environments."],["INSTALL","```apt: sudo apt install nix-bin```\n\n```dnf: sudo dnf install nix```\n\n```pacman: sudo pacman -S nix```\n\n```apk: sudo apk add nix```\n\n```zypper: sudo zypper install nix```\n\n```nix: nix profile install nixpkgs#nix```"],["SEE ALSO","[nix](/man/nix)(1), [nix-shell](/man/nix-shell)(1), [direnv](/man/direnv)(1)"]],"e":["nix","nix-shell","direnv"],"x":"nix develop"},"nohup":{"n":"nohup","t":"Run a command immune to hangups","s":[["TAGLINE","Run a command immune to hangups"],["TLDR","**Run command immune to hangups**\n\n```nohup [command] &```\n\n**Run with output to file**\n\n```nohup [command] > [output.log] 2>&1 &```\n\n**Run script in background**\n\n```nohup [./script.sh] &```\n\n**Run with custom output**\n\n```nohup [command] > [custom.out] &```"],["SYNOPSIS","**nohup** _command_ [_arguments_]\n\n**nohup** _option_"],["PARAMETERS","_COMMAND_\n> Command to run.\n\n_ARGUMENTS_\n> Command arguments.\n\n**--help**\n> Display help information.\n\n**--version**\n> Output version information."],["DESCRIPTION","**nohup** runs _command_ while ignoring the SIGHUP signal, so the process keeps running after the controlling terminal is closed or the user logs out.\n\nIf standard output is a terminal, it is appended to **nohup.out** in the current directory, or to **$HOME/nohup.out** if that is not writable. If standard error is a terminal, it is redirected to standard output. If standard input is a terminal, it is redirected from an unreadable file.\n\nNote: **nohup** does not itself put the process in the background; combine with **&** (or a shell job-control facility) to detach."],["EXIT STATUS","**125** nohup itself failed. **126** command found but could not be invoked. **127** command not found. Otherwise the exit status of _command_."],["CAVEATS","Combine with **&** to run in the background. Output is appended (not overwritten) to **nohup.out**. Many shells provide a built-in **nohup** that may differ from the GNU coreutils version. To fully detach a process from the shell, consider **disown** or **setsid**."],["HISTORY","nohup is a classic **Unix** command for running processes that survive terminal disconnection."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[screen](/man/screen)(1), [tmux](/man/tmux)(1), [disown](/man/disown)(1), [setsid](/man/setsid)(1)"]],"e":["screen","tmux","disown","setsid"],"x":"nohup [command] &"},"nvme":{"n":"nvme","t":"NVMe storage command line interface utility","s":[["TAGLINE","NVMe storage command line interface utility"],["TLDR","**List all NVMe devices**\n\n```sudo nvme list```\n\n**Show controller identification**\n\n```sudo nvme id-ctrl [/dev/nvme0]```\n\n**Show namespace identification**\n\n```sudo nvme id-ns [/dev/nvme0n1]```\n\n**Show SMART health information**\n\n```sudo nvme smart-log [/dev/nvme0]```\n\n**Show error log entries**\n\n```sudo nvme error-log [/dev/nvme0]```\n\n**Format a namespace (destroys all data)**\n\n```sudo nvme format [/dev/nvme0n1]```\n\n**Download firmware to a device**\n\n```sudo nvme fw-download [/dev/nvme0] --fw=[firmware.bin]```\n\n**Run a device self-test**\n\n```sudo nvme device-self-test [/dev/nvme0] --stc=[1]```"],["SYNOPSIS","**nvme** _command_ [_device_] [_options_]"],["PARAMETERS","**list**\n> List all NVMe devices and namespaces.\n\n**id-ctrl** _DEVICE_\n> Send an identify controller command and display the result.\n\n**id-ns** _DEVICE_\n> Send an identify namespace command and display the result.\n\n**smart-log** _DEVICE_\n> Retrieve SMART / health information log.\n\n**error-log** _DEVICE_\n> Retrieve error information log.\n\n**fw-log** _DEVICE_\n> Retrieve firmware slot information log.\n\n**format** _DEVICE_\n> Format namespace(s) with specified LBA format.\n\n**fw-download** _DEVICE_\n> Download firmware image to the device.\n\n**fw-activate** _DEVICE_\n> Activate a downloaded firmware image.\n\n**sanitize** _DEVICE_\n> Perform a sanitize operation (secure erase).\n\n**device-self-test** _DEVICE_\n> Run a device self-test (short or extended).\n\n**get-feature** _DEVICE_\n> Get a feature value from the controller.\n\n**set-feature** _DEVICE_\n> Set a feature value on the controller.\n\n**discover**\n> Discover NVMe-over-Fabrics subsystems.\n\n**connect** _DEVICE_\n> Connect to an NVMe-over-Fabrics controller.\n\n**help** _COMMAND_\n> Display help for a specific subcommand."],["DESCRIPTION","**nvme** is a user space utility providing standards-compliant tooling for NVM-Express (NVMe) drives on Linux. It has subcommands for all admin and I/O commands defined in the NVMe specification, as well as commands for displaying controller registers and retrieving various log pages.\n\nThe utility supports NVMe-over-Fabrics operations including discovery, connect, and disconnect for remote NVMe subsystems. It can also submit completely arbitrary vendor-specific commands via passthrough.\n\nAll commands return 0 on success and 1 on failure."],["CAVEATS","Root privileges are required for most operations. The **format** and **sanitize** commands destroy all data on the device and are irreversible. The **sanitize** command cannot be aborted once started. NVMe-over-Fabrics commands require appropriate kernel modules."],["HISTORY","**nvme-cli** is developed as an open-source project under the Linux NVMe project at https://github.com/linux-nvme/nvme-cli. It relies on Linux kernel NVMe IOCTLs and is Linux-specific."],["INSTALL","```apt: sudo apt install nvme-cli```\n\n```dnf: sudo dnf install nvme-cli```\n\n```pacman: sudo pacman -S nvme-cli```\n\n```apk: sudo apk add nvme-cli```\n\n```zypper: sudo zypper install nvme-cli```\n\n```nix: nix profile install nixpkgs#nvme-cli```"],["SEE ALSO","[smartctl](/man/smartctl)(1), [hdparm](/man/hdparm)(8), [lsblk](/man/lsblk)(1)"]],"e":["smartctl","hdparm","lsblk"],"x":"sudo nvme list"},"ostree":{"n":"ostree","t":"version control system for binary files, optimized for managing operating system root filesystems","s":[["TAGLINE","version control system for binary files, optimized for managing operating system root filesystems"],["TLDR","**Initialize** a repository\n\n```ostree init --repo [path/to/repo]```\n\nCreate a **commit** (snapshot) of the files\n\n```ostree commit --repo [path/to/repo] --branch [branch_name]```\n\n**Show files** in a commit\n\n```ostree ls --repo [path/to/repo] [commit_id]```\n\nShow **metadata** of a commit\n\n```ostree show --repo [path/to/repo] [commit_id]```\n\nShow **list of commits** (log)\n\n```ostree log --repo [path/to/repo] [branch_name]```\n\nShow **repo summary**\n\n```ostree summary --repo [path/to/repo] --view```\n\nShow available **refs** (branches)\n\n```ostree refs --repo [path/to/repo]```"],["SYNOPSIS","**ostree** [_global-options_] _command_ [_command-options_] [_arguments_]"],["PARAMETERS","**init**\n> Initialize a new repository\n\n**commit**\n> Create a new commit\n\n**checkout**\n> Check out a commit to a directory\n\n**ls**\n> List files in a commit\n\n**show**\n> Show commit metadata\n\n**log**\n> Show commit history\n\n**refs**\n> List available refs (branches)\n\n**summary**\n> Show or update repository summary\n\n**diff**\n> Show differences between two commits\n\n**cat**\n> Display file contents from a commit\n\n**prune**\n> Search for unreachable objects and delete them\n\n**remote**\n> Manage remote repository configuration\n\n**pull**\n> Pull from a remote repository\n\n**fsck**\n> Check repository for consistency\n\n**static-delta**\n> Manage static deltas between commits\n\n**admin**\n> Manage system deployments (for bootable trees)\n\n**--repo _path_**\n> Path to the repository\n\n**--verbose**\n> Enable verbose output\n\n**--version**\n> Print version and exit"],["DESCRIPTION","**ostree** is a version control system for binary files, optimized for managing operating system root filesystems. Unlike git (which tracks text files), OSTree efficiently handles the large binary files and hard links common in OS trees.\n\nOSTree is the foundation for immutable, image-based operating systems like Fedora Silverblue, Fedora CoreOS, and Endless OS. It enables atomic upgrades, rollbacks, and multiple parallel deployments of the operating system.\n\nThe system uses content-addressed storage (like git) with deduplication, making it efficient for storing multiple OS versions that share most files."],["CAVEATS","Repository format is not compatible with git. System deployments (bootable trees) require additional configuration through **ostree admin**. Large commits can take significant disk space initially before deduplication. Not designed for arbitrary binary files; optimized for filesystem trees."],["HISTORY","OSTree was developed by Colin Walters at Red Hat, initially for GNOME's continuous integration system. It evolved into the foundation for Fedora's immutable desktop variants and container-focused distributions."],["INSTALL","```apt: sudo apt install ostree```\n\n```dnf: sudo dnf install ostree```\n\n```pacman: sudo pacman -S ostree```\n\n```apk: sudo apk add ostree```\n\n```nix: nix profile install nixpkgs#ostree```"],["SEE ALSO","[rpm-ostree](/man/rpm-ostree)(1), [flatpak](/man/flatpak)(1), [podman](/man/podman)(1)"]],"e":["rpm-ostree","flatpak","podman"],"x":"ostree init --repo [path/to/repo]"},"pdfcrop":{"n":"pdfcrop","t":"Crop white margins from PDF pages using Ghostscript and pdfTeX","s":[["TAGLINE","Crop white margins from PDF pages using Ghostscript and pdfTeX"],["TLDR","**Automatically detect and remove** margins from each page\n\n```pdfcrop [path/to/input_file.pdf] [path/to/output_file.pdf]```\n\nSet **margins** to specific values for each page\n\n```pdfcrop [path/to/input_file.pdf] --margins '[left] [top] [right] [bottom]' [path/to/output_file.pdf]```\n\nSet **uniform margins** using the same value for all sides\n\n```pdfcrop [path/to/input_file.pdf] --margins [300] [path/to/output_file.pdf]```\n\nUse a **user-defined bounding box** for cropping\n\n```pdfcrop [path/to/input_file.pdf] --bbox '[left] [top] [right] [bottom]' [path/to/output_file.pdf]```\n\nUse different bounding boxes for **odd and even pages**\n\n```pdfcrop [path/to/input_file.pdf] --bbox-odd '[left] [top] [right] [bottom]' --bbox-even '[left] [top] [right] [bottom]' [path/to/output_file.pdf]```\n\nUse **lower resolution** for faster margin detection\n\n```pdfcrop [path/to/input_file.pdf] --resolution 72 [path/to/output_file.pdf]```"],["SYNOPSIS","**pdfcrop** [_options_] _input.pdf_ [_output.pdf_]"],["PARAMETERS","**--margins** _\"l t r b\"_\n> Add margins after cropping (in bp units)\n\n**--clip**\n> Clip content to bounding box\n\n**--bbox** _\"l t r b\"_\n> Use specified bounding box for all pages\n\n**--bbox-odd** _\"l t r b\"_\n> Bounding box for odd pages\n\n**--bbox-even** _\"l t r b\"_\n> Bounding box for even pages\n\n**--resolution** _dpi_\n> Resolution for bounding box detection (default: 72)\n\n**--restricted**\n> Turn on restricted mode (safer for web use)\n\n**--verbose**\n> Print additional processing information\n\n**--debug**\n> Enable debug mode with more output\n\n**--gscmd** _command_\n> Specify Ghostscript command to use\n\n**--pdftexcmd** _command_\n> Specify pdftex command to use\n\n**--help**\n> Display help message\n\n**--version**\n> Display version information"],["DESCRIPTION","**pdfcrop** automatically detects and removes margins from PDF pages using Ghostscript for boundary detection and pdfTeX for the actual cropping. It analyzes each page to find the bounding box of actual content and creates a new PDF with minimal whitespace.\n\nThe tool is particularly useful for preparing scanned documents, removing excess margins from journal articles, or optimizing PDFs for e-reader displays. It can process all pages automatically or apply custom bounding boxes."],["CAVEATS","Requires Ghostscript and pdfTeX to be installed. Automatic detection may not work well for pages with very light content near edges. Processing time increases with resolution; use lower resolution for faster results when precision is not critical."],["HISTORY","Originally developed as part of **texlive** by Heiko Oberdiek. Widely used in academic and publishing workflows for preparing documents. Now maintained as part of the ho-tex bundle of TeX utilities."],["INSTALL","```pacman: sudo pacman -S texlive-binextra```\n\n```apk: sudo apk add texlive-binextra```"],["SEE ALSO","[pdfjam](/man/pdfjam)(1), [pdfbook2](/man/pdfbook2)(1), [gs](/man/gs)(1), [pdflatex](/man/pdflatex)(1)"]],"e":["pdfjam","pdfbook2","gs","pdflatex"],"x":"pdfcrop [path/to/input_file.pdf] [path/to/output_file.pdf]"},"pirut":{"n":"pirut","t":"Graphical frontend for yum package manager","s":[["TAGLINE","Graphical frontend for yum package manager"],["TLDR","Launch **pirut** graphical package manager\n\n```pirut```"],["SYNOPSIS","**pirut**"],["DESCRIPTION","**pirut** is a graphical frontend for the yum package manager on older Fedora and Red Hat systems. It provides a user-friendly interface for browsing, searching, installing, and removing packages.\n\nThe tool presents packages organized by categories and allows users to manage system software without using command-line tools."],["CAVEATS","Legacy tool, not available on modern Fedora/RHEL systems. Replaced by GNOME Software, dnfdragora, or dnf command line. Requires X11 display."],["HISTORY","**pirut** was the default graphical package manager in early Fedora releases (Fedora Core 5-6 era). It was superseded by PackageKit-based tools and eventually GNOME Software as Fedora modernized its desktop software management."],["SEE ALSO","[yum](/man/yum)(8), [dnf](/man/dnf)(8), [gnome-software](/man/gnome-software)(1)"]],"e":["yum","dnf","gnome-software"],"x":"pirut"},"playwright":{"n":"playwright","t":"Cross-browser automation and testing framework","s":[["TAGLINE","Cross-browser automation and testing framework"],["TLDR","**Install browsers**\n\n```npx playwright install```\n\n**Run tests**\n\n```npx playwright test```\n\n**Run specific test**\n\n```npx playwright test [test.spec.ts]```\n\n**Run with UI mode**\n\n```npx playwright test --ui```\n\n**Generate code**\n\n```npx playwright codegen [url]```"],["SYNOPSIS","**playwright** _command_ [_options_]"],["PARAMETERS","**install** [_browser_]\n> Install browsers (chromium, firefox, webkit) and dependencies.\n\n**install-deps**\n> Install OS-level dependencies required by browsers (Linux).\n\n**test** [_files_]\n> Run Playwright tests.\n\n**codegen** [_url_]\n> Record user actions and generate test code.\n\n**show-report** [_dir_]\n> Open the HTML report in a browser.\n\n**open** [_url_]\n> Open a page in Playwright inspector.\n\n**--ui**\n> Launch interactive UI mode for running and debugging tests.\n\n**--debug**\n> Run tests in debug mode with Playwright Inspector.\n\n**--headed**\n> Run browsers in headed mode (visible window).\n\n**--project** _NAME_\n> Run only the given project (browser) from the config.\n\n**--workers** _N_\n> Number of parallel test worker processes.\n\n**--reporter** _NAME_\n> Reporter to use (list, dot, line, html, json, junit).\n\n**--grep** _PATTERN_\n> Only run tests matching the regex pattern.\n\n**--retries** _N_\n> Number of times to retry failing tests."],["DESCRIPTION","**Playwright** is a framework for browser automation and testing. It supports Chromium, Firefox, and WebKit with a unified API for cross-browser testing."],["EXAMPLES","```bash"],["Setup new project","npm init playwright@latest"],["Run all tests","npx playwright test"],["Run with specific browser","npx playwright test --project=firefox"],["Debug failing test","npx playwright test --debug"],["Generate code by recording","npx playwright codegen example.com"],["Show HTML report","npx playwright show-report\n```"],["TEST EXAMPLE","```typescript\nimport { test, expect } from '@playwright/test';\n\ntest('basic test', async ({ page }) => {\n  await page.goto('https://example.com');\n  await expect(page).toHaveTitle(/Example/);\n});\n```"],["CONFIGURATION","**playwright.config.ts** or **playwright.config.js**\n> Project configuration file defining browsers, test directories, timeouts, and reporter settings.\n\n**PLAYWRIGHT_BROWSERS_PATH**\n> Environment variable to override the browser binary download location."],["CAVEATS","Requires Node.js. Browser binaries are large. CI may need special setup."],["HISTORY","Playwright was developed by **Microsoft**, created by the team behind Puppeteer, released in 2020."],["INSTALL","```nix: nix profile install nixpkgs#playwright```"],["SEE ALSO","[puppeteer](/man/puppeteer)(1), [node](/man/node)(1), [npx](/man/npx)(1)"]],"e":["puppeteer","node","npx"],"x":"npx playwright install"},"posting":{"n":"posting","t":"Modern API client that lives in your terminal","s":[["TAGLINE","Modern API client that lives in your terminal"],["TLDR","**Launch the HTTP client TUI**\n\n```posting```\n\n**Open a specific collection directory**\n\n```posting --collection [path/to/collection]```\n\n**Load environment variable files**\n\n```posting --env [dev.env] --env [secrets.env]```\n\n**Import a Postman v2.1 collection**\n\n```posting import [postman_collection.json]```\n\n**Display help**\n\n```posting --help```"],["SYNOPSIS","**posting** [_options_] [_subcommand_]"],["PARAMETERS","**--collection** _PATH_\n> Use the given directory as the request collection.\n\n**--env** _FILE_\n> Load environment variables from a .env file (may be repeated).\n\n**--config** _FILE_\n> Use an alternate configuration file.\n\n**import** _FILE_\n> Import a Postman v2.1 collection into a Posting collection.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**posting** is a terminal-based HTTP client similar to Postman and Insomnia. Requests are stored locally as YAML files for easy version control. Features include jump-mode navigation, environments and variables, syntax highlighting with tree-sitter, Vim keys, customizable keybindings, pre/post-request Python scripts, curl import/export, and import from Postman and OpenAPI specs."],["CAVEATS","Designed for keyboard-centric workflows. Usable over SSH."],["HISTORY","**posting** was created by **Darren Burns** (darrenburns) and is written in **Python**."],["INSTALL","```brew: brew install posting```\n\n```nix: nix profile install nixpkgs#posting```"],["SEE ALSO","[curl](/man/curl)(1), [httpie](/man/httpie)(1), [slumber](/man/slumber)(1)"]],"e":["curl","httpie","slumber"],"x":"posting"},"postqueue":{"n":"postqueue","t":"Manage the Postfix mail queue","s":[["TAGLINE","Manage the Postfix mail queue"],["TLDR","**List mail queue**\n\n```postqueue -p```\n\n**Flush mail queue**\n\n```postqueue -f```\n\n**Flush specific site**\n\n```postqueue -s [example.com]```\n\n**Schedule immediate delivery of a specific message**\n\n```postqueue -i [queue_id]```\n\n**List in JSON format**\n\n```postqueue -j```"],["SYNOPSIS","**postqueue** [_options_]"],["PARAMETERS","**-p**\n> Print queue contents.\n\n**-f**\n> Flush queue (retry delivery).\n\n**-s** _site_\n> Flush specific site.\n\n**-i** _queue_id_\n> Schedule immediate delivery of deferred mail with the specified queue ID.\n\n**-j**\n> Produce queue listing in JSON format.\n\n**-v**\n> Enable verbose logging for debugging. Multiple -v options increase verbosity.\n\n**-c** _dir_\n> Use the specified configuration directory instead of the default."],["DESCRIPTION","**postqueue** manages the Postfix mail queue, providing safe access to queue operations for regular users without requiring root privileges. It displays queued messages with their IDs, sizes, arrival times, senders, and recipients, and can trigger delivery retry attempts.\n\nThe flush option forces immediate delivery attempts for all deferred mail or for a specific destination site. JSON output mode makes queue data easily parseable for monitoring scripts. For advanced queue management like deleting or holding individual messages, the privileged **postsuper** command is needed instead."],["EXAMPLES","```bash"],["View queue","postqueue -p"],["Flush all queued mail","postqueue -f"],["Retry specific domain","postqueue -s gmail.com"],["JSON output for scripting","postqueue -j"],["Retry delivery of a specific message","postqueue -i ABC123DEF"],["Count queued messages","postqueue -p | tail -1\n```"],["QUEUE OUTPUT","```\n-Queue ID-  --Size-- ----Arrival Time---- -Sender/Recipient-------\nABC123DEF     1234 Mon Jan 01 12:00:00  sender@example.com\n                                         recipient@dest.com\n```"],["QUEUE LOCATIONS","```\n/var/spool/postfix/incoming  - New mail\n/var/spool/postfix/active    - Being delivered\n/var/spool/postfix/deferred  - Temporary failures\n/var/spool/postfix/hold      - Manually held\n```"],["CAVEATS","Flushing may not help permanent failures. Use postsuper for advanced queue management."],["HISTORY","postqueue is part of **Postfix** by **Wietse Venema**, providing safe queue management for unprivileged users."],["INSTALL","```apt: sudo apt install postfix```\n\n```dnf: sudo dnf install postfix```\n\n```pacman: sudo pacman -S postfix```\n\n```apk: sudo apk add postfix```\n\n```zypper: sudo zypper install postfix```\n\n```nix: nix profile install nixpkgs#postfix```"],["SEE ALSO","[mailq](/man/mailq)(1), [postfix](/man/postfix)(1), [postconf](/man/postconf)(1), [postmap](/man/postmap)(1), [sendmail](/man/sendmail)(1)"]],"e":["mailq","postfix","postconf","postmap","sendmail"],"x":"postqueue -p"},"ranger":{"n":"ranger","t":"Terminal file manager with vim keybindings","s":[["TAGLINE","Terminal file manager with vim keybindings"],["TLDR","**Start ranger** in the current directory\n\n```ranger```\n\n**Start ranger** in a specific directory\n\n```ranger [path/to/directory]```\n\n**Show hidden files** on startup\n\n```ranger --show-hidden```\n\n**Generate default configuration files**\n\n```ranger --copy-config=all```\n\n**Print the selected files** on exit (for shell integration)\n\n```ranger --choosefile=[target_file]```"],["SYNOPSIS","**ranger** [**--version**] [**--help**] [**--copy-config**=_which_] [**--choosefile**=_target_] [_path_]"],["PARAMETERS","**--copy-config**=_which_\n> Copy default configuration files to ~/.config/ranger (rc, scope, rifle, commands, or all)\n\n**--choosefile**=_target_\n> Write selected file path to target file on exit (for shell integration)\n\n**--choosedir**=_target_\n> Write last visited directory to target file on exit\n\n**--show-hidden**\n> Show hidden files by default\n\n**--cmd**=_command_\n> Execute ranger command after startup\n\n**--version**\n> Display version information\n\n**--help**\n> Display help information"],["DESCRIPTION","**ranger** is a console-based file manager with Vi-style keybindings. It features a three-column layout showing the parent directory, current directory, and file preview simultaneously.\n\nThe interface provides instant navigation using hjkl keys (or arrow keys): **h** moves to the parent directory, **j/k** moves up/down in the file list, and **l** opens directories or files. Additional Vim-like commands include **gg** for top of list, **G** for bottom, and **/** for search.\n\nFile operations follow Vim patterns: **yy** yanks (copies), **dd** cuts, **pp** pastes, **dD** deletes. Rename with **cw**, create directories with **:mkdir**, and open files with **r** to select an application.\n\nRanger includes **rifle**, a file opener that automatically selects appropriate applications based on file type. It supports image previews (with appropriate terminal support), syntax-highlighted text previews, and archive content viewing."],["KEY BINDINGS","**h/j/k/l** or arrows: Navigate directories and files\n**gg/G**: Jump to top/bottom of list\n**zh**: Toggle hidden files\n**yy**: Copy (yank) file\n**dd**: Cut file\n**pp**: Paste file\n**dD**: Delete file\n**cw**: Rename file\n**Space**: Select file\n**:**: Enter command mode\n**q** or **ZZ**: Quit ranger\n**m**_x_: Set bookmark x\n**\\`**_x_: Jump to bookmark x\n**S**: Open shell in current directory"],["CONFIGURATION","**~/.config/ranger/rc.conf**\n> Main configuration file controlling keybindings, settings, and startup commands.\n\n**~/.config/ranger/rifle.conf**\n> File opener rules defining which applications handle each file type.\n\n**~/.config/ranger/scope.sh**\n> File preview script determining how files are previewed in the third column.\n\n**~/.config/ranger/commands.py**\n> Custom Python commands extending ranger's built-in functionality."],["CAVEATS","Image previews require a terminal with image support (kitty, iTerm2, w3m) and appropriate configuration. Text previews may require additional tools like **highlight** for syntax coloring.\n\nRanger is written in Python and may be slower than native file managers for directories with many files. Consider using **lf** or **nnn** for faster performance.\n\nConfiguration is stored in **~/.config/ranger/**. Run **ranger --copy-config=all** to generate editable configuration files."],["HISTORY","Ranger was created by **Roman Zimbelmann** with the first release in **2009**. It was designed to bring Vim's efficient keybinding philosophy to file management. The project is open source and continues to be actively maintained on GitHub."],["INSTALL","```dnf: sudo dnf install ranger```\n\n```pacman: sudo pacman -S ranger```\n\n```apk: sudo apk add ranger```\n\n```zypper: sudo zypper install ranger```\n\n```brew: brew install ranger```\n\n```nix: nix profile install nixpkgs#ranger```"],["SEE ALSO","[mc](/man/mc)(1), [vifm](/man/vifm)(1), [nnn](/man/nnn)(1), [lf](/man/lf)(1)"]],"e":["mc","vifm","nnn","lf"],"x":"ranger"},"rar2john":{"n":"rar2john","t":"Extract password hashes from RAR archives","s":[["TAGLINE","Extract password hashes from RAR archives"],["TLDR","**Extract hash from RAR file**\n\n```rar2john [archive.rar] > [hash.txt]```\n\n**Multiple archives**\n\n```rar2john [file1.rar] [file2.rar] > [hashes.txt]```\n\n**Then crack with John**\n\n```john [hash.txt]```"],["SYNOPSIS","**rar2john** _file_..."],["DESCRIPTION","**rar2john** extracts password hashes from encrypted RAR archives into a format suitable for offline password recovery with John the Ripper or Hashcat. It reads the archive's encryption metadata and outputs a hash string containing the cryptographic parameters needed to verify password guesses without repeatedly accessing the original archive file.\n\nThe tool supports both RAR3 and RAR5 encryption formats, which use different key derivation algorithms. Multiple archives can be processed in a single invocation, with each hash line prefixed by the source filename. It is part of the John the Ripper jumbo suite and is typically used in authorized security auditing and password recovery scenarios."],["EXAMPLES","```bash"],["Extract hash","rar2john encrypted.rar > rar.hash"],["Crack with wordlist","john --wordlist=/usr/share/wordlists/rockyou.txt rar.hash"],["Show cracked password","john --show rar.hash"],["Use hashcat instead (RAR5 mode 13000, RAR3 mode 12500)","rar2john archive.rar | cut -d: -f2 > hash.txt\nhashcat -m 13000 hash.txt wordlist.txt\n```"],["HASH FORMAT","```\narchive.rar:$rar5$16$abc123...$15$...\n```"],["RAR VERSIONS","```\nRAR3 - Older format, faster to crack\nRAR5 - Modern format, stronger encryption\n```"],["CAVEATS","Part of John the Ripper jumbo. Strong passwords may take very long. Use legally and ethically."],["HISTORY","rar2john is part of **John the Ripper** by **Solar Designer** and the Openwall community, providing RAR hash extraction."],["INSTALL","```dnf: sudo dnf install john```\n\n```pacman: sudo pacman -S john```\n\n```apk: sudo apk add john```\n\n```zypper: sudo zypper install john```\n\n```brew: brew install john```\n\n```nix: nix profile install nixpkgs#john```"],["SEE ALSO","[john](/man/john)(1), [zip2john](/man/zip2john)(1), [pdf2john](/man/pdf2john)(1), [hashcat](/man/hashcat)(1)"]],"e":["john","zip2john","pdf2john","hashcat"],"x":"rar2john [archive.rar] > [hash.txt]"},"react-native":{"n":"react-native","t":"Build native mobile apps using React","s":[["TAGLINE","Build native mobile apps using React"],["TLDR","**Create new project**\n\n```npx react-native init [ProjectName]```\n\n**Start Metro bundler**\n\n```npx react-native start```\n\n**Run on Android**\n\n```npx react-native run-android```\n\n**Run on iOS**\n\n```npx react-native run-ios```\n\n**Run on specific device**\n\n```npx react-native run-ios --device \"[iPhone 15]\"```\n\n**Clean build cache**\n\n```npx react-native start --reset-cache```\n\n**Link native dependencies**\n\n```npx react-native link```\n\n**Show doctor diagnostics**\n\n```npx react-native doctor```"],["SYNOPSIS","**npx react-native** _command_ [_options_]"],["PARAMETERS","**init** _NAME_\n> Create new project.\n\n**start**\n> Start Metro bundler.\n\n**run-android**\n> Build and run on Android.\n\n**run-ios**\n> Build and run on iOS.\n\n**doctor**\n> Check development environment.\n\n**link**\n> Link native dependencies.\n\n**upgrade**\n> Upgrade React Native.\n\n**--device** _NAME_\n> Target device.\n\n**--reset-cache**\n> Clear bundler cache."],["DESCRIPTION","**react-native** is the command-line interface for React Native, a framework that enables building native iOS and Android mobile applications using JavaScript and React. It manages the full development lifecycle from project creation through building, running, and debugging apps on simulators, emulators, and physical devices.\n\nThe CLI starts the Metro bundler, which serves JavaScript bundles to running apps and enables hot reloading for rapid development iteration. Platform-specific commands (**run-android**, **run-ios**) compile native code and deploy to target devices. The **doctor** command verifies that the development environment is correctly configured, checking for Xcode, Android SDK, Node.js, and required dependencies."],["CAVEATS","iOS development requires macOS. Android needs SDK and emulator. Native builds can be slow."],["HISTORY","**React Native** was created by **Facebook** and released in **2015**. It enables building mobile apps using React and JavaScript, compiling to native code."],["SEE ALSO","[expo](/man/expo)(1), [npm](/man/npm)(1), [metro](/man/metro)(1)"]],"e":["expo","npm","metro"],"x":"npx react-native init [ProjectName]"},"restorecond":{"n":"restorecond","t":"Daemon to restore SELinux contexts on file creation","s":[["TAGLINE","Daemon to restore SELinux contexts on file creation"],["TLDR","Start the **restorecond** daemon\n\n```sudo restorecond```\n\nRun in **verbose** mode to see restoration events\n\n```sudo restorecond -v```\n\nRun in **debug** mode\n\n```sudo restorecond -d```\n\nUse **alternative** configuration file\n\n```sudo restorecond -f [restorecond_file]```\n\nCheck the **status** of the restorecond service\n\n```sudo systemctl status restorecond```\n\n**Enable** restorecond to start at boot\n\n```sudo systemctl enable restorecond --now```"],["SYNOPSIS","**restorecond** [**-d**] [**-f** _restorecond_file_] [**-u**] [**-v**]"],["PARAMETERS","**-d**\n> Debug mode; run in foreground with verbose output\n\n**-f** _file_\n> Use alternate configuration file instead of /etc/selinux/restorecond.conf\n\n**-u**\n> Watch user home directory (~) for file creation\n\n**-v**\n> Verbose mode; show restoration events\n\n**-F**\n> Force mode; do not check device numbers"],["DESCRIPTION","**restorecond** is an SELinux daemon that monitors file creation events using inotify and automatically restores proper SELinux security contexts to newly created files. This is useful for directories where files are frequently created with incorrect contexts by applications that don't set contexts properly.\n\nThe daemon reads its configuration from /etc/selinux/restorecond.conf, which lists files and directories to watch. When a file matching the configuration is created or modified, restorecond applies the correct context based on SELinux policy."],["CONFIGURATION","**/etc/selinux/restorecond.conf**\n> Lists file paths and directories to watch for creation events. One path per line; created files matching these paths have their SELinux contexts automatically restored.\n\n**/etc/selinux/restorecond_user.conf**\n> Per-user watch list used when restorecond runs with the **-u** flag to monitor the user's home directory."],["CAVEATS","Requires SELinux to be enabled in enforcing or permissive mode. Must be run as root for the system-wide configuration. Only watches paths explicitly listed in the configuration file (no glob expansion). On modern systemd-based distros **restorecond** is largely deprecated in favor of **systemd file restore** rules and **restorecon -R** during package installation; check whether your distro still ships it before relying on the daemon."],["HISTORY","Part of **policycoreutils**, the SELinux policy core utilities package developed by Red Hat. Created to address the issue of applications creating files with incorrect security contexts, which could cause access denials under SELinux."],["INSTALL","```apt: sudo apt install restorecond```\n\n```zypper: sudo zypper install restorecond```"],["SEE ALSO","[restorecon](/man/restorecon)(8), [semanage-fcontext](/man/semanage-fcontext)(8), [semanage](/man/semanage)(8)"]],"e":["restorecon","semanage-fcontext","semanage"],"x":"sudo restorecond"},"rsh":{"n":"rsh","t":"Execute commands on remote hosts via remote shell","s":[["TAGLINE","Execute commands on remote hosts via remote shell"],["TLDR","Execute a **command** on a remote host\n\n```rsh remote_host ls -l```\n\nExecute with **specific username**\n\n```rsh remote_host -l username ls -l```\n\nExecute with **stdin redirected** to /dev/null\n\n```rsh remote_host --no-err ls -l```"],["SYNOPSIS","**rsh** [_options_] _host_ [_command_]"],["PARAMETERS","**-l**, **--user** _username_\n> Specify remote username\n\n**--no-err**\n> Redirect stdin from /dev/null"],["DESCRIPTION","**rsh** (remote shell) executes commands on a remote host using the RSH protocol. It provides basic remote command execution capabilities.\n\nNote: rsh transmits data unencrypted and is insecure. Use SSH for secure remote command execution."],["CAVEATS","Transmits credentials and data in plain text. Not recommended for use over untrusted networks. SSH is the secure alternative."],["HISTORY","Part of **GNU inetutils**, providing classic Unix networking utilities. Superseded by SSH for security reasons."],["INSTALL","```dnf: sudo dnf install rsh```"],["SEE ALSO","[ssh](/man/ssh)(1), [rlogin](/man/rlogin)(1), [rcp](/man/rcp)(1)"]],"e":["ssh","rlogin","rcp"],"x":"rsh remote_host ls -l"},"sbopkg":{"n":"sbopkg","t":"SlackBuilds.org package management tool","s":[["TAGLINE","SlackBuilds.org package management tool"],["TLDR","**Synchronize with the SlackBuilds.org repository**\n\n```sbopkg -r```\n\n**Launch the dialog interface**\n\n```sbopkg```\n\n**Install a package** from the command line\n\n```sbopkg -i [package]```\n\n**Install multiple packages** with dependencies in order\n\n```sbopkg -i \"[dep1] [dep2] [package]\"```\n\n**Check for updates** to installed SBo packages\n\n```sbopkg -c```\n\n**Search and inspect** a package\n\n```sbopkg -s [package]```\n\n**Build a package without installing**\n\n```sbopkg -b [package]```"],["SYNOPSIS","**sbopkg** [_-r_] [_-c_] [_-i package_] [_-b package_] [_-s package_] [_-g_]"],["PARAMETERS","**-r**\n> Synchronize with the remote SlackBuilds.org repository\n\n**-c**\n> Check for updates to installed SBo packages\n\n**-i** _package_\n> Install the specified package (quote multiple packages)\n\n**-b** _package_\n> Build a package without installing it\n\n**-s** _package_\n> Search and display package information (README, SlackBuild, .info)\n\n**-g**\n> Upgrade installed packages with available updates\n\n**-V**\n> Display version information\n\n**-h**\n> Display help and all command-line options"],["DESCRIPTION","**sbopkg** is a tool for Slackware Linux that interfaces with the SlackBuilds.org (SBo) repository. It synchronizes a local copy of the repository, browses available packages, and automates building and installing third-party software.\n\nThe tool features both a dialog-based interactive interface and command-line options. It can view README files, SlackBuild scripts, and .info files, download source code with MD5 verification, and build Slackware packages. Queue files (.sqf) enable batch processing of multiple packages.\n\nWhen the primary download URL fails, sbopkg can retrieve source from the SBo Source Archive as a fallback."],["CAVEATS","sbopkg does not handle dependencies automatically as this is not native to Slackware. Install dependencies manually in order, or specify all packages in quotes: **-i \"dep1 dep2 package\"**. Must be run as root."],["HISTORY","sbopkg was created to simplify access to SlackBuilds.org, a community repository of build scripts for Slackware packages. New versions are released to match each Slackware release, maintaining compatibility with both the distribution and the SBo repository."],["SEE ALSO","[slackpkg](/man/slackpkg)(8), [installpkg](/man/installpkg)(8), [upgradepkg](/man/upgradepkg)(8), [makepkg](/man/makepkg)(8)"]],"e":["slackpkg","installpkg","upgradepkg","makepkg"],"x":"sbopkg -r"},"shred":{"n":"shred","t":"Securely overwrite and delete files","s":[["TAGLINE","Securely overwrite and delete files"],["TLDR","**Securely delete a file**\n\n```shred [file]```\n\n**Shred and remove the file**\n\n```shred -u [file]```\n\n**Shred with specific number of passes**\n\n```shred -n [5] [file]```\n\n**Shred and zero-fill at end** (hide shredding)\n\n```shred -z [file]```\n\n**Shred verbosely** (show progress)\n\n```shred -v [file]```\n\n**Shred multiple files**\n\n```shred -u [file1] [file2] [file3]```\n\n**Shred a disk partition** (dangerous)\n\n```sudo shred -v /dev/[sdX]```"],["SYNOPSIS","**shred** [_options_] _file_..."],["PARAMETERS","**-u**, **--remove**\n> Truncate and remove file after overwriting\n\n**-n** _N_, **--iterations**=_N_\n> Overwrite N times (default: 3)\n\n**-z**, **--zero**\n> Add final overwrite with zeros to hide shredding\n\n**-v**, **--verbose**\n> Show progress\n\n**-f**, **--force**\n> Change permissions if necessary to allow writing\n\n**-s** _N_, **--size**=_N_\n> Shred only first N bytes (suffixes K, M, G accepted)\n\n**--random-source**=_FILE_\n> Get random bytes from FILE\n\n**-x**, **--exact**\n> Do not round file sizes up to full block"],["DESCRIPTION","**shred** overwrites files to make them harder to recover, then optionally deletes them. It writes random patterns multiple times over the file data, attempting to destroy information at the physical level.\n\nBy default, shred performs 3 passes of random overwrites. Adding **-z** performs a final pass with zeros, which makes the shredding less obvious (the file looks simply zeroed rather than deliberately destroyed).\n\nShred can be used on regular files or entire devices (disk partitions). For devices, it overwrites all data on the device.\n\nThe **-u** option removes the file after shredding, including attempting to rename it to disguise the original filename before unlinking."],["CAVEATS","**Journaling filesystems**: On ext3, ext4, ReiserFS, and others, shred may not be effective because the filesystem may write data elsewhere before overwriting. Only the current location is shredded.\n\n**Copy-on-write filesystems**: On Btrfs, ZFS, and similar, shred is ineffective as old data remains in previous snapshots.\n\n**SSD and flash storage**: Wear leveling means data isn't overwritten in place. Use manufacturer's secure erase commands or full-disk encryption instead.\n\n**RAID**: Striping and mirroring complicate secure deletion. Data may remain on individual disks.\n\n**For maximum security**, use full-disk encryption from the start; then destroying the key makes all data unrecoverable regardless of filesystem or storage type."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[rm](/man/rm)(1), [wipe](/man/wipe)(1), [srm](/man/srm)(1), [dd](/man/dd)(1)"]],"e":["rm","wipe","srm","dd"],"x":"shred [file]"},"spa-inspect":{"n":"spa-inspect","t":"Inspect PipeWire SPA plugin details","s":[["TAGLINE","Inspect PipeWire SPA plugin details"],["TLDR","Display information about a **SPA plugin**\n\n```spa-inspect [path/to/plugin.so]```\n\nInspect a **Bluetooth codec** plugin\n\n```spa-inspect /usr/lib64/spa-0.2/bluez5/libspa-codec-bluez5-sbc.so```\n\nInspect an **audio convert** plugin\n\n```spa-inspect /usr/lib64/spa-0.2/audioconvert/libspa-audioconvert.so```"],["SYNOPSIS","**spa-inspect** _FILE_"],["PARAMETERS","_FILE_\n> Path to a SPA plugin shared object file (.so)"],["DESCRIPTION","**spa-inspect** displays information about SPA (Simple Plugin API) plugins used by PipeWire. It lists the factories contained within a plugin and attempts to instantiate them, showing available interfaces and capabilities.\n\nSPA plugins are the low-level building blocks of PipeWire, handling audio/video processing, device access, and format conversion. This tool helps developers and administrators understand what functionality each plugin provides."],["CAVEATS","Plugin paths vary by distribution and architecture (lib vs lib64). Some plugins may fail to instantiate without proper hardware or dependencies. Primarily useful for debugging and development purposes."],["HISTORY","**spa-inspect** is part of **PipeWire**, developed by **Wim Taymans** at Red Hat. PipeWire was created starting around **2017** as a unified multimedia framework for Linux, combining the capabilities of PulseAudio and JACK. The SPA layer provides the plugin architecture that makes PipeWire modular and extensible."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-spa-tools```\n\n```zypper: sudo zypper install pipewire-spa-tools```"],["SEE ALSO","[pipewire](/man/pipewire)(1), [spa-acp-tool](/man/spa-acp-tool)(1), [pw-cli](/man/pw-cli)(1)"]],"e":["pipewire","spa-acp-tool","pw-cli"],"x":"spa-inspect [path/to/plugin.so]"},"speedread":{"n":"speedread","t":"Terminal speed reading with RSVP","s":[["TAGLINE","Terminal speed reading with RSVP"],["TLDR","Read text at **specific speed**\n\n```cat [path/to/file.txt] | speedread -w [250]```\n\n**Resume** from a specific line\n\n```cat [path/to/file.txt] | speedread -resume [5]```\n\nShow **multiple words** at a time\n\n```cat [path/to/file.txt] | speedread -multiword```"],["SYNOPSIS","**speedread** [_OPTIONS_]"],["PARAMETERS","**-w, -wpm** _words_\n> Set reading speed in words per minute (default: 250)\n\n**-resume** _line_\n> Resume from specific line number\n\n**-multiword**\n> Display multiple words at once"],["INTERACTIVE CONTROLS","**[**\n> Slow down by 10%\n\n**]**\n> Speed up by 10%\n\n**Space**\n> Pause and show context lines\n\n**q**\n> Quit"],["DESCRIPTION","**speedread** is a terminal-based speed reading tool using Rapid Serial Visual Presentation (RSVP). It displays text one word at a time, aligned on the optimal recognition point, enabling faster reading speeds while maintaining comprehension.\n\nThe technique reduces eye movement and subvocalization, allowing trained readers to achieve speeds of 400-1000+ words per minute. Text is piped through stdin, making it easy to integrate with other tools like cat, curl, or email clients."],["CAVEATS","Effectiveness varies by individual; speed reading is a skill that requires practice. Very high speeds may reduce comprehension. Complex or technical text may require slower speeds. Terminal must support the display formatting."],["HISTORY","**speedread** was created by **Petr Baudis** (pasky) and released as open-source software. The tool is inspired by **Spritz**, a commercial speed reading technology. RSVP techniques for speed reading have been studied since the **1970s**, with digital implementations becoming popular in the **2010s**."],["INSTALL","```brew: brew install speedread```\n\n```nix: nix profile install nixpkgs#speedread```"],["SEE ALSO","[less](/man/less)(1), [more](/man/more)(1), [cat](/man/cat)(1), [curl](/man/curl)(1)"]],"e":["less","more","cat","curl"],"x":"cat [path/to/file.txt] | speedread -w [250]"},"standardrb":{"n":"standardrb","t":"Zero-configuration Ruby style linter","s":[["TAGLINE","Zero-configuration Ruby style linter"],["TLDR","**Lint Ruby files** in current directory\n\n```standardrb```\n\n**Lint specific files**\n\n```standardrb [path/to/file.rb] [path/to/other.rb]```\n\n**Automatically fix** code style issues\n\n```standardrb --fix```\n\n**Fix including unsafe corrections**\n\n```standardrb --fix-unsafely```\n\n**Generate a todo file** for existing violations\n\n```standardrb --generate-todo```\n\n**Output in progress format**\n\n```standardrb --format progress```\n\n**Start language server** for IDE integration\n\n```standardrb --lsp```"],["SYNOPSIS","**standardrb** [_options_] [_files_...]"],["PARAMETERS","**--fix**\n> Automatically fix code style issues where possible.\n\n**--fix-unsafely**\n> Apply fixes including those marked as unsafe.\n\n**--generate-todo**\n> Create **.standard_todo.yml** to ignore existing violations.\n\n**--format** _format_\n> Output format: progress, simple, json, etc.\n\n**--lsp**\n> Run as Language Server Protocol server for IDE integration.\n\n**--no-fix**\n> Disable auto-fixing (default behavior).\n\n**--parallel**\n> Run in parallel mode for faster linting.\n\n**--config** _file_\n> Use a specific configuration file.\n\n**-v**, **--version**\n> Display version information.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**standardrb** is Ruby's opinionated linter and formatter, bringing the StandardJS philosophy to Ruby. It provides a zero-configuration approach to code style by wrapping RuboCop with a fixed set of rules, eliminating debates about code formatting.\n\nThe tool enforces consistent style across Ruby projects without requiring configuration files or team discussions about style preferences. It includes rules from RuboCop's built-in cops and rubocop-performance.\n\nStandard Ruby integrates with editors via LSP support and can be used in CI pipelines. The **--generate-todo** option helps adopt Standard in existing projects by creating an ignore file for current violations that can be addressed incrementally."],["CAVEATS","Standard is intentionally unconfigurable; if you need custom rules, use RuboCop directly. The **--fix-unsafely** option may change code behavior in rare cases. Standard forwards most CLI arguments to RuboCop, so RuboCop flags generally work. Requires Ruby 2.6 or later."],["HISTORY","**Standard Ruby** was created to bring the \"no configuration\" philosophy of StandardJS to the Ruby ecosystem. It was built on top of RuboCop to leverage its extensive rule set while providing an opinionated, zero-config experience. The project has gained significant adoption in the Ruby community as a way to avoid bikeshedding over code style."],["SEE ALSO","[rubocop](/man/rubocop)(1), [ruby](/man/ruby)(1), [bundle](/man/bundle)(1)"]],"e":["rubocop","ruby","bundle"],"x":"standardrb"},"systemctl-reset-failed":{"n":"systemctl-reset-failed","t":"Clear failed unit states","s":[["TAGLINE","Clear failed unit states"],["TLDR","Reset **all** failed units\n\n```systemctl reset-failed```\n\nReset a **specific** unit\n\n```systemctl reset-failed [unit]```\n\nReset **multiple** units\n\n```systemctl reset-failed [unit1 unit2 ...]```\n\n**List** failed units first, then reset them\n\n```systemctl --failed```"],["SYNOPSIS","**systemctl reset-failed** [_PATTERN_...]"],["DESCRIPTION","**systemctl reset-failed** clears the \"failed\" state from one or more units, resetting their state counters. Without arguments, it resets all failed units.\n\nWhen a unit fails (process exits with a non-zero code, terminates abnormally, or times out), it enters the \"failed\" state with its exit code and status recorded. This state persists until the unit is restarted or explicitly reset with this command.\n\nIn addition to clearing the failed state, this command also resets:\n\n- The **start rate limit counter** for all unit types (reset to zero).\n- The **restart counter** for service units (reset to zero).\n\nThis is particularly useful when a unit has hit its start rate limit and systemd refuses to restart it. Running **reset-failed** resets the counter and allows the unit to be started again immediately."],["CAVEATS","Does not fix the underlying cause of failure. The unit may fail again when started. Use after investigating and fixing the root cause of the failure."],["HISTORY","The **reset-failed** subcommand provides a way to acknowledge and clear failed unit states, enabling restart attempts and cleaning up the failure list after issues are resolved."],["SEE ALSO","[systemctl-status](/man/systemctl-status)(1), [systemctl-restart](/man/systemctl-restart)(1), [systemctl-start](/man/systemctl-start)(1), [systemctl](/man/systemctl)(1), [journalctl](/man/journalctl)(1)"]],"e":["systemctl-status","systemctl-restart","systemctl-start","systemctl","journalctl"],"x":"systemctl reset-failed"},"systemctl-service-log-target":{"n":"systemctl-service-log-target","t":"Get or set per-service log destination","s":[["TAGLINE","Get or set per-service log destination"],["TLDR","Show service **log target**\n\n```systemctl service-log-target [service]```\n\nSet to **console**\n\n```systemctl service-log-target [service] console```\n\nSet to **journal**\n\n```systemctl service-log-target [service] journal```\n\nSet to **syslog**\n\n```systemctl service-log-target [service] syslog```\n\nSet to **auto**\n\n```systemctl service-log-target [service] auto```\n\n**Disable** logging\n\n```systemctl service-log-target [service] null```"],["SYNOPSIS","**systemctl service-log-target** _SERVICE_ [_TARGET_]"],["LOG TARGETS","**console** - Send logs to stderr\n\n**journal** - Send logs to systemd-journald\n\n**syslog** - Send logs to /dev/log\n\n**auto** - Let systemd choose\n\n**null** - Disable all log output"],["DESCRIPTION","**systemctl service-log-target** gets or sets where a service sends its log messages via D-Bus. This allows redirecting service logs at runtime without restarting.\n\nThe service must support the standard D-Bus logging interface."],["CAVEATS","Only works with D-Bus integrated services. Changes are temporary and reset on service restart. Not all services support this feature."],["HISTORY","The **service-log-target** subcommand enables per-service log routing control for debugging and troubleshooting specific services."],["SEE ALSO","[systemctl-service-log-level](/man/systemctl-service-log-level)(1), [systemctl-log-target](/man/systemctl-log-target)(1), [journalctl](/man/journalctl)(1)"]],"e":["systemctl-service-log-level","systemctl-log-target","journalctl"],"x":"systemctl service-log-target [service]"},"systemd-firstboot":{"n":"systemd-firstboot","t":"Initialize basic system settings","s":[["TAGLINE","Initialize basic system settings"],["TLDR","**Operate** on a specified directory instead of the host system\n\n```sudo systemd-firstboot --root [path/to/root_directory]```\n\n**Set** the system keyboard layout\n\n```sudo systemd-firstboot --keymap [keymap]```\n\n**Set** the system hostname\n\n```sudo systemd-firstboot --hostname [hostname]```\n\n**Set** the root user's password\n\n```sudo systemd-firstboot --root-password [password]```\n\n**Prompt** the user interactively for a setting\n\n```sudo systemd-firstboot --prompt [setting]```\n\n**Force** writing configuration even if files exist\n\n```sudo systemd-firstboot --force```\n\n**Remove** all existing firstboot configuration files\n\n```sudo systemd-firstboot --reset```\n\n**Remove** the password of the root user\n\n```sudo systemd-firstboot --delete-root-password```"],["SYNOPSIS","**systemd-firstboot** [_options_]"],["PARAMETERS","**--root _path_**\n> Operate on specified root directory\n\n**--hostname _name_**\n> Set the system hostname\n\n**--keymap _map_**\n> Set the keyboard layout\n\n**--timezone _tz_**\n> Set the system timezone\n\n**--locale _locale_**\n> Set the system locale\n\n**--root-password _password_**\n> Set root's password\n\n**--root-shell _shell_**\n> Set root's login shell\n\n**--prompt**\n> Prompt interactively for settings\n\n**--force**\n> Overwrite existing configuration\n\n**--reset**\n> Remove all firstboot configuration files\n\n**--delete-root-password**\n> Remove root password"],["DESCRIPTION","**systemd-firstboot** initializes basic system settings on or before the first boot of a system. It is typically used when preparing OS images or system installations.\n\nThe tool can set hostname, locale, timezone, keyboard layout, and root password. When run without options on a live system, it interactively prompts for unconfigured settings."],["CAVEATS","Some settings require the **--force** flag to overwrite existing configuration. The **--root** option is essential when preparing disk images. Passwords should be provided securely. Part of the systemd suite."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[localectl](/man/localectl)(1), [timedatectl](/man/timedatectl)(1), [hostnamectl](/man/hostnamectl)(1)"]],"e":["localectl","timedatectl","hostnamectl"],"x":"sudo systemd-firstboot --root [path/to/root_directory]"},"timeshift":{"n":"timeshift","t":"System backup and restore using snapshots","s":[["TAGLINE","System backup and restore using snapshots"],["TLDR","**List** all snapshots\n\n```sudo timeshift --list```\n\n**Create** a snapshot with a description\n\n```sudo timeshift --create --comments \"[description]\"```\n\n**Create** a snapshot with a specific **tag**\n\n```sudo timeshift --create --tags D```\n\n**Check** and create a snapshot if scheduled\n\n```sudo timeshift --check```\n\n**Restore** a specific snapshot\n\n```sudo timeshift --restore --snapshot '[snapshot_name]'```\n\n**Restore** a snapshot to a specific **target device**\n\n```sudo timeshift --restore --snapshot '[snapshot_name]' --target-device [/dev/sda1]```\n\n**Delete** a specific snapshot\n\n```sudo timeshift --delete --snapshot '[snapshot_name]'```\n\n**Delete all** snapshots\n\n```sudo timeshift --delete-all```"],["SYNOPSIS","**timeshift** [_OPTIONS_]"],["PARAMETERS","**--list**\n> List all snapshots.\n\n**--list-devices**\n> List available devices.\n\n**--check**\n> Create snapshot if one is scheduled.\n\n**--create**\n> Create a new snapshot (even if not scheduled).\n\n**--restore**\n> Restore a snapshot.\n\n**--delete**\n> Delete a snapshot.\n\n**--delete-all**\n> Delete all snapshots.\n\n**--snapshot** _NAME_\n> Specify the snapshot to restore or delete.\n\n**--comments** _TEXT_\n> Set snapshot description.\n\n**--tags** _{O,B,H,D,W,M}_\n> Add tags to snapshot: Ondemand, Boot, Hourly, Daily, Weekly, Monthly (default: O).\n\n**--target-device** _DEVICE_\n> Specify target device for restore.\n\n**--grub-device** _DEVICE_\n> Specify device for installing GRUB2 bootloader.\n\n**--skip-grub**\n> Skip GRUB2 reinstallation during restore.\n\n**--snapshot-device** _DEVICE_\n> Specify backup device (default: from config).\n\n**--btrfs**\n> Switch to BTRFS mode.\n\n**--rsync**\n> Switch to RSYNC mode.\n\n**--yes**\n> Answer YES to all confirmation prompts.\n\n**--scripted**\n> Run in non-interactive mode."],["DESCRIPTION","**timeshift** is a system restore utility for Linux that creates incremental snapshots of the filesystem using rsync or btrfs snapshots. It allows rolling back system changes while preserving user data.\n\nSimilar to Windows System Restore or macOS Time Machine for system files."],["CAVEATS","Requires sufficient storage space. BTRFS snapshots are more space-efficient than rsync. User data in /home is excluded by default for safety. Restoring typically requires a reboot. Requires root privileges for all operations."],["HISTORY","**Timeshift** was created by **Tony George** as a GUI and CLI tool for system backup on Linux, originally released in **2014**."],["INSTALL","```apt: sudo apt install timeshift```\n\n```dnf: sudo dnf install timeshift```\n\n```pacman: sudo pacman -S timeshift```\n\n```zypper: sudo zypper install timeshift```\n\n```nix: nix profile install nixpkgs#timeshift```"],["SEE ALSO","[rsync](/man/rsync)(1), [btrfs](/man/btrfs)(8), [snapper](/man/snapper)(1), [btrbk](/man/btrbk)(1), [restic](/man/restic)(1)"]],"e":["rsync","btrfs","snapper","btrbk","restic"],"x":"sudo timeshift --list"},"tmuxinator":{"n":"tmuxinator","t":"Automate tmux session configuration","s":[["TAGLINE","Automate tmux session configuration"],["TLDR","**Start project**\n\n```tmuxinator start [project]```\n\n**Create new project**\n\n```tmuxinator new [project]```\n\n**Edit project**\n\n```tmuxinator edit [project]```\n\n**List projects**\n\n```tmuxinator list```\n\n**Delete project**\n\n```tmuxinator delete [project]```\n\n**Copy project**\n\n```tmuxinator copy [existing] [new_name]```\n\n**Stop project**\n\n```tmuxinator stop [project]```\n\n**Show project config**\n\n```tmuxinator debug [project]```"],["SYNOPSIS","**tmuxinator** [_start_] [_new_] [_edit_] [_list_] [_delete_] [_options_] [_project_]"],["PARAMETERS","**start** _PROJECT_\n> Start project session.\n\n**new** _PROJECT_\n> Create new project.\n\n**edit** _PROJECT_\n> Edit project config.\n\n**open** _PROJECT_\n> Alias for edit.\n\n**list**, **ls**\n> List projects.\n\n**delete** _PROJECT_\n> Delete project.\n\n**stop** _PROJECT_\n> Kill project session.\n\n**copy** _SRC_ _DEST_\n> Copy project.\n\n**debug** _PROJECT_\n> Show generated script.\n\n**doctor**\n> Check configuration.\n\n**version**\n> Show version.\n\n**-p** _FILE_\n> Use project file.\n\n**-n** _NAME_\n> Override session name."],["DESCRIPTION","**tmuxinator** automates tmux session setup. Project files define windows, panes, and commands that start with a single command.\n\nYAML configuration describes session layout. Windows have names, working directories, and pane configurations.\n\nCommands run automatically when panes open. Development servers, editors, and monitoring tools start in their designated locations.\n\nLayout presets handle common pane arrangements. Custom layouts specify exact pane dimensions.\n\nProject files live in ~/.config/tmuxinator. They're portable across machines and can be version controlled.\n\nStarting a project creates or attaches to the tmux session. Subsequent starts attach without recreating."],["CONFIGURATION","**~/.config/tmuxinator/*.yml**\n> Project configuration files in YAML format defining session layouts, windows, panes, and startup commands."],["CAVEATS","Requires Ruby. Complex layouts need careful configuration. Some tmux versions may have compatibility issues."],["HISTORY","**Tmuxinator** was created around **2010** to automate tmux session creation. It became the standard tool for managing development environment configurations."],["INSTALL","```apk: sudo apk add tmuxinator```\n\n```brew: brew install tmuxinator```\n\n```nix: nix profile install nixpkgs#tmuxinator```"],["SEE ALSO","[tmux](/man/tmux)(1), [screen](/man/screen)(1), [byobu](/man/byobu)(1)"]],"e":["tmux","screen","byobu"],"x":"tmuxinator start [project]"},"tslint":{"n":"tslint","t":"Deprecated TypeScript linter","s":[["TAGLINE","Deprecated TypeScript linter"],["TLDR","**Lint files**\n\n```tslint [file.ts]```\n\n**Lint with config**\n\n```tslint -c [tslint.json] [file.ts]```\n\n**Fix automatically**\n\n```tslint --fix [file.ts]```\n\n**Lint project**\n\n```tslint -p [tsconfig.json]```\n\n**Output as JSON**\n\n```tslint --format json [file.ts]```\n\n**Lint all TypeScript**\n\n```tslint -p . \"[src/**/*.ts]\"```"],["SYNOPSIS","**tslint** [_-c config_] [_-p project_] [_--fix_] [_options_] _files_"],["PARAMETERS","**-c** _FILE_\n> Config file.\n\n**-p** _FILE_\n> Project tsconfig.\n\n**--fix**\n> Auto-fix issues.\n\n**--format** _FMT_\n> Output format.\n\n**-e** _PATTERN_\n> Exclude pattern."],["DESCRIPTION","**TSLint** was a static analysis tool for TypeScript that checked code for readability, maintainability, and functional correctness. It enforced configurable rules covering style conventions, potential errors, and best practices.\n\nThe tool supported automatic fixing of many rule violations and integrated with editors and CI systems. Configuration was managed through a tslint.json file that specified which rules to enable and their severity levels.\n\nTSLint was deprecated in 2019 in favor of **ESLint** with the **typescript-eslint** plugin, which provides the same TypeScript linting capabilities within the more widely adopted ESLint ecosystem."],["CAVEATS","Deprecated in favor of ESLint. TypeScript-specific. Node.js required."],["HISTORY","**TSLint** was the standard TypeScript linter until 2019 when the team deprecated it in favor of **ESLint** with TypeScript support."],["INSTALL","```aur: yay -S tslint```"],["SEE ALSO","[eslint](/man/eslint)(1), [tsc](/man/tsc)(1), [prettier](/man/prettier)(1)"]],"e":["eslint","tsc","prettier"],"x":"tslint [file.ts]"},"unbound":{"n":"unbound","t":"Validating recursive caching DNS resolver","s":[["TAGLINE","Validating recursive caching DNS resolver"],["TLDR","**Start the unbound DNS server**\n\n```sudo unbound```\n\n**Start with a specific config file**\n\n```sudo unbound -c [path/to/unbound.conf]```\n\n**Start in foreground** (don't daemonize)\n\n```sudo unbound -d```\n\n**Start with verbose output**\n\n```sudo unbound -v```\n\n**Check configuration file** for errors\n\n```unbound-checkconf [path/to/unbound.conf]```\n\n**Show version**\n\n```unbound -V```"],["SYNOPSIS","**unbound** [**-c** _configfile_] [**-d**] [**-p**] [**-v**] [**-V**] [**-h**]"],["PARAMETERS","**-c** _configfile_\n> Use the specified configuration file instead of /etc/unbound/unbound.conf.\n\n**-d**\n> Debug mode. Do not fork into the background, stay in foreground.\n\n**-p**\n> Do not use chroot or drop privileges.\n\n**-v**\n> Increase verbosity level. Can be specified multiple times for more output.\n\n**-V**\n> Show version information.\n\n**-h**\n> Show help message."],["DESCRIPTION","**Unbound** is a validating, recursive, and caching DNS resolver designed for security and performance. It performs DNS lookups on behalf of clients, validates responses using DNSSEC, and caches results to improve response times for subsequent queries.\n\nUnlike forwarding resolvers, Unbound is a full recursive resolver that can query root DNS servers directly, eliminating dependency on upstream DNS providers. It supports DNSSEC validation by default, verifying the authenticity of DNS responses to protect against cache poisoning and other DNS attacks.\n\nConfiguration is done through /etc/unbound/unbound.conf, which controls listening interfaces, access control, DNSSEC settings, cache sizes, and performance tuning. Unbound can operate as a local resolver for a single machine or as a network-wide DNS server.\n\nRelated utilities include **unbound-control** for runtime management, **unbound-checkconf** for configuration validation, **unbound-anchor** for DNSSEC root key management, and **unbound-host** for DNS lookups."],["CONFIGURATION","**/etc/unbound/unbound.conf**\n> Main configuration file controlling listening interfaces, access control, DNSSEC settings, cache sizes, and forwarding rules.\n\n**/etc/unbound/unbound.conf.d/**\n> Directory for additional configuration fragments included by the main config.\n\n**/var/lib/unbound/root.key**\n> DNSSEC root trust anchor file managed by unbound-anchor."],["CAVEATS","Running as a public DNS resolver requires careful access control configuration to prevent abuse. DNSSEC validation may cause resolution failures for misconfigured domains. Cache size should be tuned based on expected query volume and available memory. Ensure root trust anchors are updated periodically with unbound-anchor."],["HISTORY","**Unbound** was developed by **NLnet Labs** in the Netherlands, with the first version released in **May 2008**. It was designed as a modern, secure alternative to BIND, focusing on code cleanliness and security. Unbound has become the default DNS resolver in several BSD operating systems and is widely used with Pi-hole and other DNS filtering solutions."],["INSTALL","```apt: sudo apt install unbound```\n\n```dnf: sudo dnf install unbound```\n\n```pacman: sudo pacman -S unbound```\n\n```apk: sudo apk add unbound```\n\n```zypper: sudo zypper install unbound```\n\n```brew: brew install unbound```\n\n```nix: nix profile install nixpkgs#unbound```"],["SEE ALSO","[unbound-control](/man/unbound-control)(8), [unbound-checkconf](/man/unbound-checkconf)(8), [unbound-host](/man/unbound-host)(1), [dig](/man/dig)(1), [nslookup](/man/nslookup)(1)"]],"e":["unbound-control","unbound-checkconf","unbound-host","dig","nslookup"],"x":"sudo unbound"},"update-mime-database":{"n":"update-mime-database","t":"Build shared MIME-Info database cache","s":[["TAGLINE","Build shared MIME-Info database cache"],["TLDR","**Update system MIME database**\n\n```sudo update-mime-database /usr/share/mime```\n\n**Update user MIME database**\n\n```update-mime-database ~/.local/share/mime```\n\n**Show version**\n\n```update-mime-database -v```\n\n**Display help**\n\n```update-mime-database -h```"],["SYNOPSIS","**update-mime-database** [_options_] _mime-dir_"],["PARAMETERS","**-h**\n> Display help and exit.\n\n**-v**\n> Display version and exit.\n\n**-V**\n> Verbose output during processing.\n\n**-n**\n> Only update if files have changed."],["DESCRIPTION","**update-mime-database** builds the shared MIME-Info database cache from XML source files. It scans the specified directory for MIME type definitions and compiles them into an optimized binary cache (mime.cache) for fast application access.\n\nThe database maps file patterns, magic bytes, and other characteristics to MIME types, enabling applications to correctly identify file types. System-wide definitions go in /usr/share/mime while user customizations use ~/.local/share/mime.\n\nPackage managers typically run this command automatically when installing software that provides new MIME type definitions. Manual execution is needed when adding custom MIME types."],["DIRECTORIES","**/usr/share/mime**: System-wide MIME database.\n\n**/usr/share/mime/packages/**: Source XML files for MIME types.\n\n**~/.local/share/mime**: User-specific MIME database."],["CAVEATS","Requires write permission to target directory. System directory updates need root privileges. Applications may need restart to recognize new types. Part of the shared-mime-info package."],["HISTORY","**update-mime-database** is part of the freedesktop.org shared-mime-info project, created to standardize MIME type handling across Linux desktop environments. Before this project, GNOME and KDE maintained separate incompatible MIME databases."],["INSTALL","```apt: sudo apt install shared-mime-info```\n\n```dnf: sudo dnf install shared-mime-info```\n\n```pacman: sudo pacman -S shared-mime-info```\n\n```apk: sudo apk add shared-mime-info```\n\n```zypper: sudo zypper install shared-mime-info```\n\n```brew: brew install shared-mime-info```\n\n```nix: nix profile install nixpkgs#shared-mime-info```"],["SEE ALSO","[xdg-mime](/man/xdg-mime)(1), [file](/man/file)(1), [mimetype](/man/mimetype)(1)"]],"e":["xdg-mime","file","mimetype"],"x":"sudo update-mime-database /usr/share/mime"},"urpmi":{"n":"urpmi","t":"Install packages on Mageia Linux","s":[["TAGLINE","Install packages on Mageia Linux"],["TLDR","**Install** package\n\n```sudo urpmi [package]```\n\nInstall from **local** RPM\n\n```sudo urpmi [path/to/file.rpm]```\n\n**Download** without installing\n\n```urpmi --no-install [package]```\n\n**Update** all packages\n\n```sudo urpmi --auto-select```\n\nInstall on **multiple** machines\n\n```sudo urpmi --parallel local [package]```"],["SYNOPSIS","**urpmi** [_OPTIONS_] _PACKAGE_..."],["PARAMETERS","**--no-install**\n> Download package without installing\n\n**--auto-select**\n> Update all installed packages\n\n**--parallel** _CONFIG_\n> Install on multiple machines\n\n**--auto**\n> Automatically confirm actions"],["DESCRIPTION","**urpmi** is the package installer for Mageia Linux. It can install packages from repositories or local RPM files, handling dependencies automatically.\n\nThe tool supports updating all installed packages and can manage packages across multiple machines using parallel configuration."],["CAVEATS","Mageia specific. Run urpmi.update -a first to get latest package lists. Requires root privileges for installation."],["SEE ALSO","[urpme](/man/urpme)(8), [urpmq](/man/urpmq)(8), [urpmi.update](/man/urpmi.update)(8), [urpmi.addmedia](/man/urpmi.addmedia)(8)"]],"e":["urpme","urpmq","urpmi.update","urpmi.addmedia"],"x":"sudo urpmi [package]"},"uv-init":{"n":"uv-init","t":"Initialize a new Python project with uv","s":[["TAGLINE","Initialize a new Python project with uv"],["TLDR","**Initialize new project**\n\n```uv init```\n\n**Initialize with name**\n\n```uv init [project_name]```\n\n**Initialize library project**\n\n```uv init --lib```\n\n**Initialize in directory**\n\n```uv init [path]```"],["SYNOPSIS","**uv** **init** [_options_] [_path_]"],["PARAMETERS","**--name** _name_\n> Project name.\n\n**--lib**\n> Create library structure.\n\n**--app**\n> Create application structure.\n\n**--no-workspace**\n> Don't add to workspace.\n\n**--python** _version_\n> Python version requirement."],["DESCRIPTION","**uv init** initializes a new Python project. Creates pyproject.toml with project metadata. Sets up project structure for applications or libraries."],["INSTALL","```dnf: sudo dnf install uv```\n\n```pacman: sudo pacman -S uv```\n\n```apk: sudo apk add uv```\n\n```brew: brew install uv```\n\n```nix: nix profile install nixpkgs#uv```"],["SEE ALSO","[uv](/man/uv)(1), [uv-add](/man/uv-add)(1), [uv-sync](/man/uv-sync)(1)"]],"e":["uv","uv-add","uv-sync"],"x":"uv init"},"vi":{"n":"vi","t":"Classic Unix visual text editor","s":[["TAGLINE","Classic Unix visual text editor"],["TLDR","**Open a file**\n\n```vi [file]```\n\n**Open at specific line**\n\n```vi +[line_number] [file]```\n\n**Open in read-only mode**\n\n```vi -R [file]```\n\n**Open multiple files**\n\n```vi [file1] [file2]```\n\n**Recover a file** from swap\n\n```vi -r [file]```"],["SYNOPSIS","**vi** [**-rR**] [**+**_command_] [_file_...]"],["PARAMETERS","**-R**\n> Read-only mode\n\n**-r** [_file_]\n> Recovery mode; list recoverable files or recover specific file\n\n**+** _command_\n> Execute command after loading\n\n**+** _line_\n> Start at specified line number\n\n**-c** _command_\n> Execute command after loading (same as +)"],["MODES","**Normal**: Navigation and commands (default)\n**Insert**: Text input (i, a, o)\n**Command**: Ex commands (:)\n**Visual**: Selection (v) - vim only\n\nPress **Esc** to return to Normal mode."],["ESSENTIAL COMMANDS","**Navigation**: h/j/k/l (left/down/up/right), 0/$ (line start/end), gg/G (file start/end)\n**Insert**: i (before cursor), a (after cursor), o (new line below), O (new line above)\n**Delete**: x (character), dd (line), dw (word)\n**Copy/Paste**: yy (copy line), p (paste after), P (paste before)\n**Undo**: u (undo), Ctrl+r (redo - vim only)\n**Save/Quit**: :w (save), :q (quit), :wq (save & quit), :q! (quit without saving), ZZ (save & quit)\n**Search**: /pattern (forward), ?pattern (backward), n/N (next/previous)"],["DESCRIPTION","**vi** is the classic Unix visual text editor, present on virtually all Unix-like systems. It uses modal editing where keys have different functions depending on the current mode.\n\nOn most modern Linux systems, **vi** is actually a link to **vim** (Vi IMproved) running in compatibility mode, or a minimal vi clone like **nvi**. True original vi is rare outside of commercial Unix systems.\n\nThe modal design allows powerful text manipulation without modifier keys, using the home row for navigation. While the learning curve is steep, proficiency enables very efficient editing.\n\nConfiguration is stored in **~/.exrc** for traditional vi or **~/.vimrc** for vim."],["CAVEATS","Vi has a steep learning curve. Use **vimtutor** (if vim is installed) to learn interactively.\n\nWithout a proper terminal, vi may not function correctly. Set the TERM environment variable appropriately.\n\nOn minimal systems, only basic vi may be available without vim's enhancements. Know the common subset of commands."],["HISTORY","Vi was created by **Bill Joy** at UC Berkeley in **1976** as the visual mode for the ex line editor. It became the standard Unix editor and influenced countless text editors. The name stands for \"visual\" as it was the visual interface to ex."],["INSTALL","```apk: sudo apk add neatvi-vi```"],["SEE ALSO","[vim](/man/vim)(1), [nvim](/man/nvim)(1), [nano](/man/nano)(1), [emacs](/man/emacs)(1), [ed](/man/ed)(1)"]],"e":["vim","nvim","nano","emacs","ed"],"x":"vi [file]"},"wtfis":{"n":"wtfis","t":"Passive hostname, domain and IP lookup tool","s":[["TAGLINE","Passive hostname, domain and IP lookup tool"],["TLDR","**Look up a domain**\n\n```wtfis [example.com]```\n\n**Look up an IP with Shodan enrichment**\n\n```wtfis -s [8.8.8.8]```\n\n**Enable all enrichments**\n\n```wtfis -A [example.com]```"],["SYNOPSIS","**wtfis** [_options_] _hostname_|_domain_|_IP_"],["DESCRIPTION","**wtfis** gathers and presents information about domains, hostnames, and IP addresses using OSINT services like VirusTotal, IP2Whois, and Shodan in a colorized, human-readable format. It minimizes API calls to avoid hitting free-tier rate limits."],["HISTORY","**wtfis** was created by **pirxthepilot** (Joon) and is written in **Python**."],["INSTALL","```brew: brew install wtfis```\n\n```nix: nix profile install nixpkgs#wtfis```"],["SEE ALSO","[whois](/man/whois)(1), [nslookup](/man/nslookup)(1), [dig](/man/dig)(1)"]],"e":["whois","nslookup","dig"],"x":"wtfis [example.com]"},"xsp":{"n":"xsp","t":"Mono ASP.NET development web server","s":[["TAGLINE","Mono ASP.NET development web server"],["TLDR","**Start server**\n\n```xsp```\n\n**Specify port**\n\n```xsp --port [8080]```\n\n**Set root directory**\n\n```xsp --root [/var/www]```\n\n**Enable HTTPS**\n\n```xsp --https --p12file [server.p12] --pkpwd [password]```\n\n**Run ASP.NET 4**\n\n```xsp4```\n\n**Bind to address**\n\n```xsp --address [0.0.0.0]```"],["SYNOPSIS","**xsp** [_--port port_] [_--root path_] [_--address ip_] [_--https_] [_options_]"],["PARAMETERS","**--port** _PORT_\n> Listen port (default 8080).\n\n**--root** _PATH_\n> Application root directory.\n\n**--address** _IP_\n> Bind address.\n\n**--https**\n> Enable HTTPS.\n\n**--p12file** _FILE_\n> PKCS#12 certificate file.\n\n**--pkpwd** _PASSWORD_\n> Certificate password.\n\n**--nonstop**\n> Don't stop on ENTER."],["DESCRIPTION","**xsp** is a lightweight standalone web server included with the Mono project for hosting ASP.NET applications on Linux and other Unix-like systems. It serves as a development and testing server, providing a quick way to run ASP.NET WebForms and MVC applications without requiring a full web server setup.\n\nThe server supports HTTPS via PKCS#12 certificate files and can bind to specific addresses and ports. For production deployments, xsp should be replaced with mod_mono behind Apache or a similar reverse proxy configuration that provides better performance and security."],["CAVEATS","Development server. Mono required. Use with mod_mono for production."],["HISTORY","**xsp** is part of the **Mono** project, providing a standalone ASP.NET web server for testing and development on Linux."],["INSTALL","```dnf: sudo dnf install xsp```\n\n```zypper: sudo zypper install xsp```"],["SEE ALSO","[mono](/man/mono)(1), [mcs](/man/mcs)(1), [apache2](/man/apache2)(1)"]],"e":["mono","mcs","apache2"],"x":"xsp"},"z_jail":{"n":"z_jail","t":"Multi-layer Linux sandbox for native code execution","s":[["TAGLINE","Multi-layer Linux sandbox for native code execution"],["TLDR","**Run a program** inside an isolated root filesystem with seccomp enforcement\n\n```sudo z_jail --root=[path/to/rootfs] --seccomp-enforce -- [program] [args...]```\n\n**Verify binary integrity** before execution\n\n```sudo z_jail --root=[path/to/rootfs] --seccomp-enforce --self-hash=[blake2b_hex] -- [program]```\n\n**Suppress audit JSON** output\n\n```sudo z_jail --root=[path/to/rootfs] --quiet -- [program]```\n\n**Build** the ~130 KiB PIE binary from source\n\n```make```"],["SYNOPSIS","**z_jail** **--root=**_dir_ [_options_] **--** _program_ [_args_]"],["PARAMETERS","**--root=**_dir_\n> Sandbox root directory containing the target binary and dependencies (required).\n\n**--seccomp-enforce**\n> Enable seccomp-BPF whitelist (**whitelist-v1**, 15 syscalls).\n\n**--self-hash=**_hex_\n> Refuse to run unless the **z_jail** binary matches the given BLAKE2b-256 hash.\n\n**--quiet**\n> Suppress JSON audit output.\n\n**--verbose**\n> Enable debug logging.\n\n**--version**\n> Print build ID and exit.\n\n**--help**\n> Show usage and exit."],["DESCRIPTION","**z_jail** is a lightweight C99 sandbox for executing untrusted native binaries on Linux. It combines seven defence layers with no runtime dependencies beyond the standard C toolchain: Truthimatics verdict engine, mount/PID/net/IPC/UTS namespaces, **pivot_root**, capability dropping with locked securebits, **PR_SET_NO_NEW_PRIVS**, seccomp-BPF syscall whitelisting, and JSON audit logging with BLAKE2b content fingerprints.\n\nThe parent process clones isolated namespaces, applies resource limits, pivots into **--root**, installs the seccomp filter, and **execve**s the target. After the child exits, an audit record is written describing verdict, duration, sandbox settings, and executable hash.\n\nDesigned for CI pipelines, CTF jails, and lightweight code evaluation, **z_jail** sits between minimal tools like **bwrap** and heavier sandboxes such as **nsjail** or **gVisor**."],["CAVEATS","**z_jail** requires **CAP_SYS_ADMIN** (typically root) to create namespaces and **pivot_root**. The default seccomp whitelist allows only 15 syscalls — many programs need a custom policy or will be killed. Dynamic binaries require libraries inside **--root**; static binaries need only the executable. Kernel exploits and hardware side channels are outside the stated threat model."],["HISTORY","**Z-Jail** was developed by **Division-36** and released as **v1** in **2026**, targeting Linux kernel **5.4+** with a single **make** build and no external libraries."],["SEE ALSO","[bwrap](/man/bwrap)(1), [firejail](/man/firejail)(1), [chroot](/man/chroot)(8)"],["RESOURCES","```[Source code](https://github.com/Division-36/Z-Jail)```\n\n```[Documentation](https://github.com/Division-36/Z-Jail/tree/main/docs)```"]],"e":["bwrap","firejail","chroot"],"x":"sudo z_jail --root=[path/to/rootfs] --seccomp-enforce -- [program] [args...]"},"zstyle":{"n":"zstyle","t":"Define and look up zsh styles by context","s":[["TAGLINE","Define and look up zsh styles by context"],["TLDR","**Define a style** for a pattern (e.g. enable case-insensitive completion matching)\n\n```zstyle ':completion:*' matcher-list 'm:{a-zA-Z}={A-Za-z}'```\n\n**List all** style definitions in zstyle form\n\n```zstyle -L```\n\n**List styles** matching a meta-pattern\n\n```zstyle -L ':completion:*'```\n\n**Delete a style** for a pattern\n\n```zstyle -d ':completion:*' menu```\n\n**Look up a style** value into a scalar variable\n\n```zstyle -s ':completion:*' completer REPLY```\n\n**Look up a style** as an array\n\n```zstyle -a ':completion:*:descriptions' format DESCR```\n\n**Test a boolean style** (returns 0 if set to yes/true/on/1)\n\n```zstyle -t ':completion:*' verbose```\n\n**Define a dynamically-evaluated** style with `-e`\n\n```zstyle -e ':completion:*' max-errors 'reply=($((($#PREFIX+$#SUFFIX)/3))numeric)'```"],["SYNOPSIS","**zstyle** [ **-L** [ _metapattern_ [ _style_ ] ] ]\n\n**zstyle** [ **-** | **--** | **-e** ] _pattern_ _style_ _string_ ...\n\n**zstyle** **-d** [ _pattern_ [ _style_ ... ] ]\n\n**zstyle** **-g** _name_ [ _pattern_ [ _style_ ] ]\n\n**zstyle** **-{a|b|s}** _context_ _style_ _name_ [ _sep_ ]\n\n**zstyle** **-{T|t}** _context_ _style_ [ _string_ ... ]\n\n**zstyle** **-m** _context_ _style_ _pattern_"],["PARAMETERS","**-L** [_metapattern_ [_style_]]\n> List style definitions as **zstyle** commands. Optional _metapattern_ filters by pattern, _style_ filters by style name.\n\n**-e**\n> Treat the value as code to be evaluated when the style is looked up; the parameter **reply** must be set to the resulting strings.\n\n**-d** [_pattern_ [_style_ ...]]\n> Delete styles. Without arguments deletes all definitions. With a _pattern_, deletes that pattern; with _styles_, deletes only those.\n\n**-g** _name_ [_pattern_ [_style_]]\n> Get definitions into the array _name_. Returns patterns, styles for a pattern, or values for a (pattern, style) pair.\n\n**-s** _context_ _style_ _name_ [_sep_]\n> Read the style as a scalar into _name_, joining multiple values with spaces or _sep_. Returns 0 if set, 1 otherwise.\n\n**-b** _context_ _style_ _name_\n> Read the style as a boolean (`yes` if the single value is **yes**, **true**, **on**, or **1**; otherwise `no`).\n\n**-a** _context_ _style_ _name_\n> Read the style as an array; if _name_ is associative, alternate strings become keys and values.\n\n**-t** _context_ _style_ [_string_ ...]\n> Test a style. Status 0 = matches/true, 1 = defined but not matching, 2 = undefined.\n\n**-T** _context_ _style_ [_string_ ...]\n> Like **-t** but returns 0 (instead of 2) when the style is undefined for any matching pattern.\n\n**-m** _context_ _style_ _pattern_\n> Pattern-match a style value. Returns 0 if _pattern_ matches at least one of the value's strings."],["DESCRIPTION","**zstyle** is a builtin from the **zsh/zutil** module used to define and look up styles. A style is a name/value pair stored against a pattern; lookups supply a colon-separated **context** string that is matched against those patterns. The most specific matching pattern wins.\n\nPattern specificity is determined by the number of colon-separated components and how literal each component is. Literal strings beat patterns, complex patterns beat the wildcard `*`, and ties are broken in favor of the pattern defined first. A `*` matches any number of characters including colons.\n\nStyles are most prominently used by zsh's shell-function-based completion system, where contexts follow the form **:completion:_function_:_completer_:_command_:_argument_:_tag_**. They are also used by prompt themes (e.g. **vcs_info**), zsh plugins, and arbitrary user code that calls **zstyle -s/-a/-b/-t** to read user preferences."],["CAVEATS","A common gotcha with **-L**: the meta-pattern is matched against the stored pattern, so `zstyle -L \":completion:*\"` matches any stored pattern beginning with `:completion:`, not just the literal `:completion:*`. Use `:completion:\\*` to match the literal asterisk. The **-L** option cannot be combined with other options.\n\nThe **-e** form re-evaluates its value on every lookup; if **reply** is left unset by the evaluation, the style is treated as undefined."],["CONFIGURATION","Styles are typically set in **~/.zshrc** after **autoload -U compinit && compinit**. There is no on-disk configuration file dedicated to styles; they live in shell memory until the shell exits.\n\nCommon contexts include **:completion:\\***, **:vcs_info:\\***, **:zle:\\***, and plugin-specific namespaces such as **:weather:\\*** or **:zsh-autosuggest**."],["HISTORY","The **zstyle** builtin was introduced as part of the **zsh/zutil** module to support the new function-based completion system that replaced the older **compctl** approach in **zsh 4.0** (2001). It became the standard configuration mechanism for completion behavior, prompt themes such as **vcs_info**, and many third-party zsh plugins."],["SEE ALSO","[zsh](/man/zsh)(1)"]],"e":["zsh"],"x":"zstyle ':completion:*' matcher-list 'm:{a-zA-Z}={A-Za-z}'"}});
