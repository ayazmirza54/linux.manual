window.LCL_BUCKET(116,{"afconvert":{"n":"afconvert","t":"macOS audio file format converter","s":[["TAGLINE","macOS audio file format converter"],["TLDR","**Convert** audio file to AAC\n\n```afconvert -f m4af -d aac [input.wav] [output.m4a]```\n\nConvert to **ALAC** (Apple Lossless)\n\n```afconvert -f m4af -d alac [input.wav] [output.m4a]```\n\nConvert to **AIFF**\n\n```afconvert -f AIFF -d BEI16 [input.mp3] [output.aiff]```\n\nSet **bit rate** for AAC encoding\n\n```afconvert -f m4af -d aac -b [128000] [input.wav] [output.m4a]```\n\nConvert with VBR **strategy**\n\n```afconvert -f m4af -d aac -s 3 -b [128000] [input.wav] [output.m4a]```\n\nList supported **formats**\n\n```afconvert -hf```"],["SYNOPSIS","**afconvert** [_options_] _input_ [_output_]"],["DESCRIPTION","**afconvert** is a macOS command-line tool for converting between audio file formats. It supports all audio formats handled by Core Audio, including WAV, AIFF, MP3, AAC, ALAC, CAF, and more. If no output file is specified, the name is generated automatically and saved in the same directory as the input.\n\nThe tool provides access to Apple's audio codecs and can perform sample rate conversion, bit depth conversion, and channel remapping along with format conversion."],["PARAMETERS","**-f**, **--file** _format_\n> Output file format (m4af, WAVE, AIFF, caff, etc.).\n\n**-d**, **--data** _format_\n> Audio data format (aac, alac, BEI16, LEF32, etc.). Can include sample rate and format flags.\n\n**-b**, **--bitrate** _rate_\n> Total bit rate in bits per second for compressed formats.\n\n**-c**, **--channels** _number_\n> Number of output channels.\n\n**-l**, **--channellayout** _tag_\n> Channel layout tag.\n\n**-q**, **--quality** _quality_\n> Codec quality (0-127, higher is better).\n\n**-r**, **--src-quality** _quality_\n> Sample rate converter quality (0-127, default: 127).\n\n**--src-complexity** _complexity_\n> Sample rate converter complexity (line, norm, bats).\n\n**-s**, **--strategy** _strategy_\n> Bitrate allocation strategy (0=CBR, 1=ABR, 2=VBR_constrained, 3=VBR).\n\n**-o** _filename_\n> Specify output file path.\n\n**--dither** _algorithm_\n> Dither algorithm (1 or 2).\n\n**--mix**\n> Enable channel downmixing.\n\n**-v**, **--verbose**\n> Display progress information.\n\n**-h**, **--help**\n> Display help information.\n\n**-hf**, **--help-formats**\n> List all supported file and data formats."],["CAVEATS","This tool is macOS-only. Some format conversions may be lossy. AAC encoding quality varies with bit rate settings. Not all format combinations are valid. Use **-hf** to see which formats are supported on your system."],["HISTORY","**afconvert** has been part of macOS since Mac OS X, providing command-line access to the same audio conversion capabilities used by iTunes and other Apple applications."],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [sox](/man/sox)(1), [lame](/man/lame)(1)"]],"e":["ffmpeg","sox","lame"],"x":"afconvert -f m4af -d aac [input.wav] [output.m4a]"},"ani-cli":{"n":"ani-cli","t":"Search and stream anime from the terminal","s":[["TAGLINE","Search and stream anime from the terminal"],["TLDR","**Search** and watch anime\n\n```ani-cli [anime_name]```\n\n**Continue** watching from history\n\n```ani-cli -c```\n\n**Download** instead of streaming\n\n```ani-cli -d [anime_name]```\n\nSelect specific **quality**\n\n```ani-cli -q [720] [anime_name]```\n\nUse **VLC** player\n\n```ani-cli -v [anime_name]```\n\nWatch **dubbed** version\n\n```ani-cli --dub [anime_name]```\n\nWatch specific **episode** range\n\n```ani-cli -e [1-5] [anime_name]```"],["SYNOPSIS","**ani-cli** [_-cdevN_] [_-q quality_] [_-e episodes_] [_--dub_] [_search_query_]"],["DESCRIPTION","**ani-cli** is a command-line tool for searching and streaming anime from various sources. It provides a terminal-based interface for selecting anime, choosing episodes, and playing them in your preferred video player.\n\nThe tool scrapes anime streaming sites to find available content, presenting options in an interactive menu format."],["PARAMETERS","**-c**, **--continue**\n> Continue watching from history\n\n**-d**, **--download**\n> Download instead of stream\n\n**-e**, **--episode** _range_\n> Specify episode number or range (e.g., 1-5)\n\n**-q** _quality_\n> Specify video quality (best, worst, 360, 480, 720, 1080). Default is best.\n\n**-v**, **--vlc**\n> Use VLC player instead of mpv\n\n**-D**, **--delete**\n> Delete watch history\n\n**-s**, **--syncplay**\n> Watch together with friends using Syncplay (mpv only)\n\n**-N**, **--non-interactive**\n> Disable the interactive menu\n\n**-S** _index_, **--select-nth** _index_\n> Select nth search result without prompting\n\n**--dub**\n> Play the dubbed version instead of subbed\n\n**--skip**\n> Use ani-skip to skip episode intros (mpv only)\n\n**--no-detach**\n> Don't detach the player (useful for in-terminal playback)\n\n**--rofi**\n> Use rofi instead of fzf for the interactive menu\n\n**-U**, **--update**\n> Update ani-cli\n\n**-h**, **--help**\n> Show help information"],["CONFIGURATION","**ANI_CLI_MODE**\n> Controls media mode: sub or dub. Default is sub.\n\n**ANI_CLI_QUALITY**\n> Controls video quality (best, worst, 360, 480, 720, 1080). Default is best.\n\n**ANI_CLI_DOWNLOAD_DIR**\n> Download directory. Default is the current directory.\n\n**ANI_CLI_EXTERNAL_MENU**\n> Set to 1 to use rofi dmenu instead of fzf. Default is 0.\n\n**ANI_CLI_SKIP_INTRO**\n> Set to 1 to use ani-skip for intro skipping (mpv only). Default is 0."],["CAVEATS","Depends on external streaming sources that may change or become unavailable. Quality and availability vary by title. Requires mpv or vlc for playback. May not work in all regions. Requires curl, sed, grep, and fzf as dependencies."],["HISTORY","**ani-cli** was created as a community project for anime fans who prefer command-line tools. It has been actively maintained with updates to support changing streaming sources."],["INSTALL","```aur: yay -S ani-cli```\n\n```nix: nix profile install nixpkgs#ani-cli```"],["SEE ALSO","[mpv](/man/mpv)(1), [vlc](/man/vlc)(1), [yt-dlp](/man/yt-dlp)(1), [fzf](/man/fzf)(1)"]],"e":["mpv","vlc","yt-dlp","fzf"],"x":"ani-cli [anime_name]"},"apache":{"n":"apache","t":"Control the Apache HTTP web server","s":[["TAGLINE","Control the Apache HTTP web server"],["TLDR","**Start Apache** daemon\n\n```apachectl start```\n\n**Stop Apache** gracefully (finish current requests)\n\n```apachectl graceful-stop```\n\n**Gracefully restart** Apache (finish current requests, then restart)\n\n```apachectl graceful```\n\n**Test configuration** for syntax errors\n\n```apachectl configtest```\n\n**Show Apache version**\n\n```apachectl -v```\n\n**Show compiled-in modules**\n\n```apachectl -l```\n\n**Show all loaded modules** (static and shared)\n\n```apachectl -M```\n\n**Show parsed virtual host settings**\n\n```apachectl -S```"],["SYNOPSIS","**apachectl** _command_\n\n**httpd** [**-d** _serverroot_] [**-f** _config_] [**-D** _parameter_]"],["PARAMETERS","**start**\n> Start the Apache httpd daemon.\n\n**stop**\n> Stop the daemon immediately.\n\n**restart**\n> Restart the daemon (aborts current connections).\n\n**graceful**\n> Gracefully restart (finish current requests, then restart).\n\n**graceful-stop**\n> Gracefully stop (finish current requests, then stop).\n\n**configtest**\n> Check configuration file syntax.\n\n**fullstatus**\n> Display full server status (requires **mod_status** and a text-based browser).\n\n**-v**\n> Show version number.\n\n**-V**\n> Show version and build parameters.\n\n**-l**\n> List compiled-in modules.\n\n**-M**\n> List all loaded modules (static and shared).\n\n**-t**\n> Test configuration syntax (same as configtest).\n\n**-S**\n> Show parsed virtual host settings.\n\n**-f** _file_\n> Specify an alternate configuration file.\n\n**-D** _name_\n> Define a parameter for use in configuration conditionals."],["DESCRIPTION","**Apache HTTP Server** (httpd) is one of the most widely used web server software packages. It serves static and dynamic content over HTTP/HTTPS, supporting virtual hosting, URL rewriting, authentication, and extensive module-based extensibility.\n\nThe **apachectl** script provides a convenient interface for controlling the Apache daemon. It can operate as a simple front-end to the **httpd** command, or act as a SysV init script translating one-word arguments like **start**, **restart**, and **stop** into appropriate signals.\n\nConfiguration is primarily done through **httpd.conf** and related files. The modular architecture allows enabling features like SSL/TLS (mod_ssl), URL rewriting (mod_rewrite), proxying (mod_proxy), and dynamic content via CGI, PHP, or mod_wsgi.\n\nApache supports multiple Multi-Processing Modules: **prefork** (process-based) for compatibility, **worker** (threaded), and **event** (threaded with improved keep-alive handling) for higher concurrency."],["CAVEATS","Configuration changes require a restart or reload. Syntax errors in configuration files prevent startup; always run **configtest** first. The **graceful** restart is preferred for production to avoid dropping active connections. Log files can grow large and require rotation. The **apachectl** script returns exit code 0 on success and >0 on error."],["HISTORY","The Apache HTTP Server originated from the **NCSA HTTPd** server at the University of Illinois. In **1995**, a group of developers began collaborating on patches, leading to the \"patchy server\" name that became Apache. The **Apache Software Foundation** was formed in **1999** to support its development. Apache dominated web server market share for over two decades, though nginx has gained significant ground since **2010**."],["INSTALL","```pacman: sudo pacman -S apache```"],["SEE ALSO","[nginx](/man/nginx)(8), [httpd](/man/httpd)(8), [htpasswd](/man/htpasswd)(1), [ab](/man/ab)(1)"]],"e":["nginx","httpd","htpasswd","ab"],"x":"apachectl start"},"b4n":{"n":"b4n","t":"Terminal user interface (TUI) for Kubernetes API inspired by k9s","s":[["TAGLINE","Terminal user interface (TUI) for Kubernetes API inspired by k9s"],["TLDR","**Launch b4n** with the default kubeconfig and context\n\n```b4n```\n\n**Start with a specific resource** kind\n\n```b4n [pods]```\n\n**Use a specific kubeconfig** context\n\n```b4n --context [my-cluster]```\n\n**Focus on a specific namespace** at startup\n\n```b4n --namespace [kube-system]```\n\n**Start with a cluster-wide view** across all namespaces\n\n```b4n --all-namespaces```\n\n**Use a custom kubeconfig** file\n\n```b4n --kube-config [path/to/kubeconfig]```\n\n**Skip TLS certificate verification**\n\n```b4n --insecure```"],["SYNOPSIS","**b4n** [_options_] [_resource_]"],["PARAMETERS","**--kube-config** _path_\n> Path to the kubeconfig file. Defaults to **$HOME/.kube/config**. Can also be set via the **KUBECONFIG** environment variable.\n\n**--context** _name_\n> Kubernetes context to use from the kubeconfig file.\n\n**-n**, **--namespace** _name_\n> Namespace to focus on at startup.\n\n**-A**, **--all-namespaces**\n> Start with a cluster-wide view showing resources across all namespaces.\n\n**--insecure**\n> Skip TLS certificate verification when connecting to the cluster.\n\n**-V**, **--version**\n> Print version information.\n\n**-h**, **--help**\n> Print help information."],["DESCRIPTION","**b4n** is an interactive terminal user interface for managing Kubernetes clusters, written in Rust using the **kube-rs** and **ratatui** libraries. It provides a keyboard-driven interface for performing common Kubernetes operations without leaving the terminal.\n\nThe TUI supports full CRUD operations on Kubernetes resources. Users can list, create, edit, and delete resources through the interactive interface. Additional capabilities include viewing resource events with **e**, streaming pod and container logs with **l**, launching interactive shell sessions in containers with **s**, inspecting resource YAML with **y**, decoding secrets with **x**, and configuring port forwarding with **f**.\n\nNavigation uses a command palette accessible via **:** or **>**, along with dedicated keys for namespace selection (**left arrow**) and resource kind selection (**right arrow**). Resources can be filtered with **/**, and columns can be sorted with **Alt+[0-9]**. Resources are selected with **Space** and deleted with **Ctrl+D** after confirmation. Mouse support can be toggled with **Ctrl+N**.\n\nThe command palette supports configurable aliases for quick resource access. Default aliases include **pp** for pods, **svc** for services, **cm** for configmaps, **ds** for daemonsets, **ns** for namespaces, **pvc** for persistentvolumeclaims, and **sts** for statefulsets, among others."],["CONFIGURATION","Configuration is stored in **$HOME/.b4n/** with the following structure:\n\n**config.yaml**\n> Main application settings including log preferences, mouse support, active theme, context-specific highlight colors, resource aliases, and custom key bindings. Auto-generated with defaults if missing.\n\n**themes/**\n> Directory containing TUI theme files in YAML format. A default theme is auto-generated on first run.\n\n**history.yaml**\n> Stores filter and search history per Kubernetes context, as well as the last used namespace and resource kind.\n\n**logs/**\n> Daily application log files for debugging.\n\nKey configuration options in **config.yaml** include **logs.lines** (number of retrieved log lines, default 800), **logs.timestamps** (show timestamps in logs, default true), **mouse** (enable mouse support, default true), **theme** (active theme name), **contexts** (per-cluster highlight colors), **aliases** (command palette shortcuts), and **key_bindings** (custom key mappings supporting Ctrl, Alt, and function keys)."],["CAVEATS","Requires a valid kubeconfig file and network access to a Kubernetes cluster. A terminal font with Nerd Font symbols (such as Cascadia Code) is recommended for proper UI rendering. Building from source requires Rust 1.88 or newer. The project is under active development and was created primarily as a Rust learning exercise."],["INSTALL","```zypper: sudo zypper install b4n```"],["SEE ALSO","[k9s](/man/k9s)(1), [kubectl](/man/kubectl)(1), [helm](/man/helm)(1)"]],"e":["k9s","kubectl","helm"],"x":"b4n"},"bcrypt":{"n":"bcrypt","t":"Encrypt and decrypt files using Blowfish","s":[["TAGLINE","Encrypt and decrypt files using Blowfish"],["TLDR","**Encrypt** a file (creates file.txt.bfe, removes the original)\n\n```bcrypt [file.txt]```\n\n**Decrypt** a file (any .bfe input is decrypted)\n\n```bcrypt [file.txt.bfe]```\n\n**Encrypt** but keep the original file\n\n```bcrypt -r [file.txt]```\n\n**Encrypt** without compressing first\n\n```bcrypt -c [file.txt]```"],["SYNOPSIS","**bcrypt** [_-orc_] [_-s_ _N_] _file_..."],["DESCRIPTION","**bcrypt** is a cross-platform file encryption utility using the Blowfish cipher. Files are encrypted with a passphrase and saved with a **.bfe** (Blowfish Encrypted) extension. Any file ending in .bfe is assumed to be encrypted and is decrypted; all other input files are encrypted.\n\nBy default bcrypt compresses each input file before encryption, removes the input file after it is processed successfully, and overwrites the original with random data first to hinder recovery. Passphrases may be 8 to 56 characters; the key is always hashed internally to the 448-bit maximum supported by Blowfish."],["PARAMETERS","**-o**\n> Print output to standard out (implies **-r**).\n\n**-r**\n> Do not remove input files after processing.\n\n**-c**\n> Do not compress files before encryption.\n\n**-s** _N_\n> Overwrite input files with random data _N_ times before deleting (default 3)."],["WORKFLOW","```bash"],["Encrypt (creates file.txt.bfe, removes file.txt)","bcrypt file.txt"],["Decrypt (restores file.txt)","bcrypt file.txt.bfe"],["Encrypt but keep the original","bcrypt -r file.txt\n```"],["CAVEATS","Older tool with limited maintenance. The passphrase resides in memory during operation. Blowfish has largely been superseded by AES, so consider **gpg** or **ccrypt** for modern needs. Forgetting the passphrase makes the data unrecoverable."],["HISTORY","**bcrypt** was written by **Johnny Shelley** as a lightweight, portable file encryption tool. It uses Paul Kocher's implementation of the Blowfish cipher, which was designed by **Bruce Schneier** in **1993**. This file utility is unrelated to the **bcrypt** password-hashing function of the same name."],["INSTALL","```dnf: sudo dnf install bcrypt```\n\n```brew: brew install bcrypt```"],["SEE ALSO","[gpg](/man/gpg)(1), [openssl](/man/openssl)(1), [ccrypt](/man/ccrypt)(1), [age](/man/age)(1)"],["RESOURCES","```[Homepage](https://bcrypt.sourceforge.net/)```"]],"e":["gpg","openssl","ccrypt","age"],"x":"bcrypt [file.txt]"},"bitcoin-qt":{"n":"bitcoin-qt","t":"Graphical interface for Bitcoin Core","s":[["TAGLINE","Graphical interface for Bitcoin Core"],["TLDR","**Start Bitcoin Core** with graphical interface\n\n```bitcoin-qt```\n\n**Start on testnet**\n\n```bitcoin-qt -testnet```\n\n**Start with specific data directory**\n\n```bitcoin-qt -datadir=[/path/to/data]```\n\n**Start with limited connections**\n\n```bitcoin-qt -maxconnections=[8]```\n\n**Start in prune mode** to save disk space\n\n```bitcoin-qt -prune=[550]```\n\n**Start minimized** to system tray\n\n```bitcoin-qt -min```\n\n**Start without splash screen**\n\n```bitcoin-qt -splash=0```\n\n**Reset GUI settings**\n\n```bitcoin-qt -resetguisettings```"],["SYNOPSIS","**bitcoin-qt** [_options_]"],["DESCRIPTION","**bitcoin-qt** is the graphical user interface for Bitcoin Core, the reference implementation of the Bitcoin protocol. It provides a full Bitcoin node with wallet functionality and a user-friendly interface for sending, receiving, and managing Bitcoin.\n\nRunning bitcoin-qt downloads and verifies the entire Bitcoin blockchain (several hundred gigabytes). It connects to the Bitcoin peer-to-peer network, validates transactions and blocks, and relays them to other nodes.\n\nThe wallet features include address generation, transaction history, coin control, and fee estimation. It supports multiple wallets, watch-only addresses, and hardware wallet integration."],["PARAMETERS","**-datadir=** _path_\n> Specify data directory.\n\n**-testnet**\n> Use the test network.\n\n**-regtest**\n> Enter regression test mode.\n\n**-prune=** _n_\n> Reduce storage by deleting old blocks (MB).\n\n**-maxconnections=** _n_\n> Limit peer connections.\n\n**-proxy=** _ip:port_\n> Connect through SOCKS5 proxy.\n\n**-onion=** _ip:port_\n> Use separate proxy for Tor.\n\n**-listen**\n> Accept incoming connections.\n\n**-server**\n> Accept JSON-RPC commands.\n\n**-rpcuser=** _user_\n> RPC username.\n\n**-rpcpassword=** _pass_\n> RPC password.\n\n**-min**\n> Start minimized.\n\n**-splash=** _0|1_\n> Show splash screen on startup.\n\n**-resetguisettings**\n> Reset GUI settings to defaults.\n\n**-choosedatadir**\n> Show data directory chooser on startup.\n\n**-lang=** _lang_\n> Set interface language."],["CONFIGURATION","**~/.bitcoin/bitcoin.conf**\n> Bitcoin Core configuration file for network, RPC, wallet, and GUI settings."],["CAVEATS","Initial blockchain sync takes hours to days depending on hardware and network. Full node requires 500+ GB disk space (or use pruning). High bandwidth usage during sync and operation. Wallet file (wallet.dat) should be backed up securely. Running on mainnet involves real money."],["HISTORY","**Bitcoin Core** was originally written by **Satoshi Nakamoto** and released in **January 2009** as the first Bitcoin implementation. The graphical interface (bitcoin-qt) was added later using the Qt framework. After Satoshi's departure, development continued under lead maintainers including Gavin Andresen, Wladimir van der Laan, and others. It remains the reference implementation that defines Bitcoin protocol rules."],["INSTALL","```pacman: sudo pacman -S bitcoin-qt```\n\n```apk: sudo apk add bitcoin-qt```"],["SEE ALSO","[bitcoind](/man/bitcoind)(1), [bitcoin-cli](/man/bitcoin-cli)(1)"]],"e":["bitcoind","bitcoin-cli"],"x":"bitcoin-qt"},"bundletool-validate":{"n":"bundletool-validate","t":"Validate Android App Bundle files","s":[["TAGLINE","Validate Android App Bundle files"],["TLDR","**Validate an Android App Bundle**\n\n```bundletool validate --bundle=[app.aab]```"],["SYNOPSIS","**bundletool** **validate** **--bundle**=_path_"],["PARAMETERS","**--bundle** _path_\n> Path to the Android App Bundle (.aab) file to validate."],["DESCRIPTION","**bundletool validate** checks if an Android App Bundle (.aab) file is structurally valid and conforms to the App Bundle format specification. It verifies the bundle's internal structure, manifest, and configuration.\n\nThis is useful for catching issues before uploading to Google Play or other app distribution stores."],["CAVEATS","Requires Java Runtime Environment (JRE) version 11 or higher. Validation is structural; it does not verify runtime behavior or business logic."],["INSTALL","```brew: brew install bundletool```\n\n```nix: nix profile install nixpkgs#bundletool```"],["SEE ALSO","[bundletool](/man/bundletool)(1), [bundletool-dump](/man/bundletool-dump)(1)"],["RESOURCES","```[Source code](https://github.com/google/bundletool)```\n\n```[Documentation](https://developer.android.com/tools/bundletool)```"]],"e":["bundletool","bundletool-dump"],"x":"bundletool validate --bundle=[app.aab]"},"cargo-msrv":{"n":"cargo-msrv","t":"Find and manage the minimum supported Rust version","s":[["TAGLINE","Find and manage the minimum supported Rust version"],["TLDR","**Find minimum supported Rust version**\n\n```cargo msrv find```\n\n**Find using linear search**\n\n```cargo msrv find --linear```\n\n**Verify current MSRV works**\n\n```cargo msrv verify```\n\n**Show declared MSRV**\n\n```cargo msrv show```\n\n**Set MSRV in Cargo.toml**\n\n```cargo msrv set [1.56.0]```\n\n**List dependency MSRVs**\n\n```cargo msrv list```\n\n**Find for specific path**\n\n```cargo msrv --path [/path/to/project] find```"],["SYNOPSIS","**cargo msrv** [_options_] _command_"],["DESCRIPTION","**cargo msrv** finds and manages the Minimum Supported Rust Version (MSRV) for Rust projects. It determines the oldest Rust compiler version that can successfully build a crate by testing against available toolchains. By default it uses binary search to efficiently narrow down the compatible version range.\n\nMaintaining an accurate MSRV is important for library authors who want to support users on older Rust versions. The tool can automatically set the `rust-version` field in Cargo.toml after finding the minimum version, and verify that the declared MSRV remains valid as the codebase evolves."],["COMMANDS","**find**\n> Discover the MSRV by testing versions\n\n**verify**\n> Check if project works with declared MSRV\n\n**show**\n> Display MSRV from Cargo.toml\n\n**set** _version_\n> Update rust-version in Cargo.toml\n\n**list**\n> Show MSRVs of dependencies"],["PARAMETERS","**--path** _dir_\n> Project directory path\n\n**--linear**\n> Use linear search instead of binary\n\n**--min** _version_\n> Minimum version to consider\n\n**--max** _version_\n> Maximum version to consider\n\n**--target** _triple_\n> Target platform\n\n**--manifest-path** _path_\n> Path to Cargo.toml\n\n**--write-msrv**\n> Write found MSRV to Cargo.toml after find\n\n**--ignore-lockfile**\n> Ignore the lockfile during MSRV determination"],["REQUIREMENTS","Requires rustup for toolchain management."],["CAVEATS","Binary search is much faster than linear for the many Rust minor versions. Requires rustup for downloading and managing toolchains. The `find` command compiles the project with each candidate version, which can be slow for large projects. The `rust-version` field in Cargo.toml only supports two-component versions (e.g., 1.56) since Rust 1.56."],["INSTALL","```pacman: sudo pacman -S cargo-msrv```\n\n```brew: brew install cargo-msrv```\n\n```nix: nix profile install nixpkgs#cargo-msrv```"],["SEE ALSO","[cargo](/man/cargo)(1), [rustup](/man/rustup)(1)"]],"e":["cargo","rustup"],"x":"cargo msrv find"},"cgset":{"n":"cgset","t":"set cgroup parameters","s":[["TAGLINE","set cgroup parameters"],["TLDR","**Set CPU quota** for a cgroup\n\n```cgset -r cpu.cfs_quota_us=[50000] [/mygroup]```\n\n**Set memory limit** for a cgroup\n\n```cgset -r memory.limit_in_bytes=[512M] [/mygroup]```\n\n**Set multiple parameters** at once\n\n```cgset -r cpu.shares=[512] -r memory.limit_in_bytes=[1G] [/mygroup]```\n\n**Copy settings** from one cgroup to another\n\n```cgset --copy-from [/source_group] [/dest_group]```"],["SYNOPSIS","**cgset** [**-r** _name=value_]... _cgroup_\n\n**cgset** **--copy-from** _source_ _destination_"],["DESCRIPTION","**cgset** modifies parameters of existing control groups (cgroups). It allows setting resource limits and controls for CPU, memory, I/O, and other subsystems on groups of processes.\n\nCommon parameters include CPU shares (relative priority), CPU quotas (hard limits), memory limits, and I/O weights. The available parameters depend on which controllers are mounted and the cgroup version.\n\ncgset is part of libcgroup-tools for managing cgroups v1. For cgroups v2, systemd or direct filesystem writes are typically used instead."],["PARAMETERS","**-r** _name=value_\n> Set a parameter to a value.\n\n**--copy-from** _cgroup_\n> Copy parameters from another cgroup.\n\n**-h**, **--help**\n> Display help information."],["CAVEATS","Requires appropriate permissions (typically root). Parameter names and values are controller-specific. Invalid values may be rejected by the kernel. Only works with cgroups v1 hierarchy. Some parameters cannot be changed while processes are in the cgroup."],["HISTORY","**cgset** is part of **libcgroup**, developed by **Red Hat** starting around **2008** to provide user-space cgroup management. The tool provided a way to configure resource limits without direct filesystem manipulation. As cgroups v2 and systemd became prevalent, direct management through these tools became less common."],["INSTALL","```apt: sudo apt install cgroup-tools```\n\n```apk: sudo apk add cgroup-tools```"],["SEE ALSO","[cgcreate](/man/cgcreate)(1), [cgdelete](/man/cgdelete)(1), [cgget](/man/cgget)(1), [cgexec](/man/cgexec)(1)"],["RESOURCES","```[Source code](https://github.com/libcgroup/libcgroup)```"]],"e":["cgcreate","cgdelete","cgget","cgexec"],"x":"cgset -r cpu.cfs_quota_us=[50000] [/mygroup]"},"cloudrift":{"n":"cloudrift","t":"Read-only AWS cost waste and hygiene scanner","s":[["TAGLINE","Read-only AWS cost waste and hygiene scanner"],["TLDR","**Install** and launch the interactive wizard\n\n```npm install -g @cloudrift/cli && cloudrift```\n\n**Analyze** waste in one or more regions\n\n```cloudrift analyze -r us-east-1 eu-west-1```\n\n**Export** a PDF report\n\n```cloudrift analyze -r us-east-1 --pdf```\n\n**Scan** dead/unused resources (often $0 cost hygiene)\n\n```cloudrift dead-resources -r us-east-1```\n\n**Scan** security misconfigurations\n\n```cloudrift resource-security -r us-east-1```\n\n**Compare** monthly spend or chart trends (Cost Explorer — billed)\n\n```cloudrift cost```\n\n```cloudrift trend --months 12```"],["SYNOPSIS","**cloudrift** [_command_] [_options_]"],["DESCRIPTION","**cloudrift** is an open-source, read-only AWS cost optimization CLI. It discovers idle, orphaned, and overprovisioned resources, estimates monthly waste, and never deletes, modifies, or stops anything — reports only. With no subcommand in an interactive terminal it starts a guided wizard.\n\nCore domains:\n\n- **analyze** — cost waste and optimization opportunities (EBS, EIPs, idle NAT, underutilized EC2/RDS, and many more scanners).\n- **dead-resources** — abandoned assets that may cost $0 but clutter the account (unused key pairs, empty S3, inactive IAM, ...).\n- **resource-security** — configuration risks (open security groups, public S3, missing MFA, unencrypted volumes, ...).\n- **cost** / **trend** — Cost Explorer spend comparison and monthly charts (**$0.01 per CE request**; confirmation required unless **-y**).\n- **history** — local SQLite snapshots under **~/.cloudrift/trends/**.\n- **mcp** — stdio MCP server for agent integrations.\n\nRequires Node.js 20+ and AWS credentials with the documented read-only IAM policy. Also installable via Homebrew (**elleVas/cloudrift/cloudrift**)."],["COMMANDS","**analyze** [**-r** _region_...] [**--pdf**] [other flags]\n\n> Run waste/optimization scanners. Default region often **us-east-1**.\n\n**dead-resources** [**-r** _region_...] [**--scanners** _id_]\n\n> Hygiene scan for dead or unused resources.\n\n**resource-security** [**-r** _region_...] [**--scanners** _id_]\n\n> Security posture checks.\n\n**cost** / **trend**\n\n> Bill comparison and multi-month trend (Cost Explorer charges apply).\n\n**history** [**--domain** _name_] [**--limit** _n_]\n\n> Read local prior scan snapshots.\n\n**mcp**\n\n> Run as a local Model Context Protocol server over stdio."],["CAVEATS","Findings are estimates — validate before acting. Rightsizing heuristics (e.g. CPU-only underutilization) are not a substitute for AWS Compute Optimizer. **cost**/**trend** are the only commands that intentionally incur AWS API charges. Exclusion tag **cloudrift:ignore** is a trust boundary, not a security control."],["SEE ALSO","[aws](/man/aws)(1), [aws-cli](/man/aws-cli)(1)"],["RESOURCES","```[Source code](https://github.com/elleVas/cloudrift)```\n\n```[Homepage](https://www.npmjs.com/package/@cloudrift/cli)```\n\n```[Documentation](https://github.com/elleVas/cloudrift/tree/main/docs/en)```"]],"e":["aws","aws-cli"],"x":"npm install -g @cloudrift/cli && cloudrift"},"create-image":{"n":"create-image","t":"CPIO firmware image creator","s":[["TAGLINE","CPIO firmware image creator"],["TLDR","Create a **CPIO archive** from current directory\n\n```create-image -o [path/to/output.cpio]```\n\nDisplay **help**\n\n```create-image -h```"],["SYNOPSIS","**create-image** [_options_]"],["DESCRIPTION","**create-image** creates CPIO images from the current directory. It is part of the BlueZ Bluetooth stack tools and is used for creating firmware images.\n\nThe tool packages directory contents into the CPIO archive format commonly used for initramfs and firmware images."],["PARAMETERS","**-o, --output** _file_\n> Output file path\n\n**-h, --help**\n> Display help information"],["CAVEATS","Part of BlueZ tools. Works on the current directory contents. CPIO format is uncompressed; use gzip separately if needed."],["SEE ALSO","[cpio](/man/cpio)(1), [find](/man/find)(1)"],["RESOURCES","```[Source code](https://github.com/bluez/bluez)```"]],"e":["cpio","find"],"x":"create-image -o [path/to/output.cpio]"},"dm-tool":{"n":"dm-tool","t":"LightDM display manager control","s":[["TAGLINE","LightDM display manager control"],["TLDR","Show **greeter** (login screen)\n\n```dm-tool switch-to-greeter```\n\n**Lock** the current session\n\n```dm-tool lock```\n\n**Switch** to a specific user\n\n```dm-tool switch-to-user [username] [session]```\n\n**Add** a dynamic seat\n\n```dm-tool add-seat [xlocal] [name]=[value]```"],["SYNOPSIS","**dm-tool** _command_ [_arguments_]"],["DESCRIPTION","**dm-tool** is a command-line utility for interacting with the LightDM display manager. It provides session management capabilities including locking the current session, switching between users, and displaying the greeter (login screen) without logging out.\n\nThe tool enables multi-user workflows by allowing quick user switching while preserving all open sessions. It can also manage display seats, which are independent workstations consisting of a display, keyboard, and mouse.\n\ndm-tool communicates with LightDM via D-Bus, sending commands that control the display manager's behavior. This makes it useful for scripting and custom session management workflows on systems using LightDM."],["PARAMETERS","**switch-to-greeter**\n> Show login screen, keep current session\n\n**lock**\n> Lock current session\n\n**switch-to-user** _user_ [_session_]\n> Switch to specified user\n\n**add-seat** _type_ _options_\n> Add dynamic seat\n\n**list-seats**\n> List available seats"],["CAVEATS","Only works with LightDM display manager. Other display managers (GDM, SDDM) have different tools. Session type must be valid."],["INSTALL","```apt: sudo apt install lightdm```\n\n```dnf: sudo dnf install lightdm```\n\n```pacman: sudo pacman -S lightdm```\n\n```apk: sudo apk add lightdm```\n\n```zypper: sudo zypper install lightdm```\n\n```nix: nix profile install nixpkgs#lightdm```"],["SEE ALSO","[loginctl](/man/loginctl)(1)"]],"e":["loginctl"],"x":"dm-tool switch-to-greeter"},"dnswalk":{"n":"dnswalk","t":"DNS zone validator and debugger","s":[["TAGLINE","DNS zone validator and debugger"],["TLDR","**Check DNS zone** for errors\n\n```dnswalk [domain.com.]```\n\n**Check with verbose** output\n\n```dnswalk -r [domain.com.]```\n\n**Check specific zone** file\n\n```dnswalk -l [domain.com.]```\n\n**Debug mode**\n\n```dnswalk -d [domain.com.]```\n\n**Force zone transfer** attempt\n\n```dnswalk -F [domain.com.]```"],["SYNOPSIS","**dnswalk** [_options_] _domain_"],["PARAMETERS","_DOMAIN_\n> Domain to check (must end with dot).\n\n**-r**\n> Recursively descend subdomains.\n\n**-l**\n> Check lame delegations.\n\n**-d**\n> Debug mode (verbose).\n\n**-F**\n> Force zone transfer even if normally blocked.\n\n**-i**\n> Suppress check of invalid characters.\n\n**-a**\n> Turn on all warning flags.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dnswalk** is a DNS debugger that checks zone configurations for common errors and inconsistencies. It performs zone transfers and analyzes the resulting data for problems like lame delegations, missing PTR records, and invalid data.\n\nThe tool identifies issues including orphaned CNAME records, duplicate A records, missing reverse entries, and records pointing to non-existent hosts. It helps DNS administrators maintain zone health and catch configuration errors.\n\ndnswalk traverses the zone hierarchy, optionally recursing into subdomains to provide comprehensive validation of DNS infrastructure."],["CAVEATS","Requires zone transfer access (often restricted). Domain name must end with trailing dot. May report false positives on valid but unusual configurations. Perl dependencies required."],["HISTORY","dnswalk was written by **David Barr** in the early 1990s as a DNS debugging and maintenance tool. It remains useful for validating zone configurations and has been included in DNS administrator toolkits for decades."],["INSTALL","```aur: yay -S dnswalk```"],["SEE ALSO","[dig](/man/dig)(1), [nslookup](/man/nslookup)(1)"]],"e":["dig","nslookup"],"x":"dnswalk [domain.com.]"},"dphys-swapfile":{"n":"dphys-swapfile","t":"swap file management for Debian-based systems","s":[["TAGLINE","swap file management for Debian-based systems"],["TLDR","**Disable** the swap file\n\n```sudo dphys-swapfile swapoff```\n\n**Enable** the swap file\n\n```sudo dphys-swapfile swapon```\n\n**Create or resize** the swap file to match the config\n\n```sudo dphys-swapfile setup```\n\n**Remove** the swap file entirely\n\n```sudo dphys-swapfile uninstall```\n\n**Resize the swap** (the standard three-step sequence)\n\n```sudo dphys-swapfile swapoff && sudo dphys-swapfile setup && sudo dphys-swapfile swapon```\n\n**Check the result**\n\n```swapon --show```"],["SYNOPSIS","**dphys-swapfile** _command_"],["DESCRIPTION","**dphys-swapfile** creates and manages a swap *file*, as opposed to a swap partition. It exists because a file can be resized after the fact while a partition cannot, which matters on systems installed from a fixed disk image, and it is best known as the swap mechanism on **Raspberry Pi OS**.\n\nBy default it computes the swap size from the amount of installed RAM, capped by `CONF_MAXSWAP`, so a machine gets sensible swap without anyone choosing a number. Setting `CONF_SWAPSIZE` overrides that and pins the size in megabytes.\n\nIt is normally driven by its init script or systemd unit at boot rather than run by hand. The manual invocations matter when changing the size: the file must be swapped off before it can be rebuilt, so **swapoff**, **setup**, **swapon** in that order."],["PARAMETERS","**setup**\n> Create the swap file, or resize it if the configured size has changed. Does not activate it.\n\n**swapon**\n> Activate the swap file, creating it first if it does not exist.\n\n**swapoff**\n> Deactivate the swap file, leaving it on disk.\n\n**uninstall**\n> Deactivate the swap file and delete it."],["CONFIGURATION","**/etc/dphys-swapfile**\n> Shell-style configuration file, read by every subcommand.\n\n**CONF_SWAPFILE**\n> Path to the swap file. Defaults to `/var/swap`.\n\n**CONF_SWAPSIZE**\n> Fixed size in megabytes. Leave it empty to compute the size from RAM instead. Raspberry Pi OS ships with a small default (commonly 100 or 200).\n\n**CONF_MAXSWAP**\n> Upper bound in megabytes on the computed size. Defaults to 2048, and it caps `CONF_SWAPSIZE` too, so raising the swap beyond 2 GB means raising this as well."],["CAVEATS","`CONF_MAXSWAP` silently limits `CONF_SWAPSIZE`. Setting `CONF_SWAPSIZE=4096` without also raising `CONF_MAXSWAP` gives you a 2 GB swap file and no warning, which is a frequent source of confusion.\n\nEditing the config alone does nothing: the file is only rebuilt by **setup**, and it cannot be rebuilt while in use, hence the swapoff/setup/swapon sequence.\n\nOn a Raspberry Pi the swap file lives on the SD card, and swapping writes heavily to flash that has limited erase cycles and no wear levelling worth the name. Heavy swap use is a well-known way to kill an SD card, and it is also painfully slow. If a workload genuinely needs swap, put it on a USB SSD, or use **zram** to compress pages in RAM instead, which most people find is the better answer on a small board."],["SEE ALSO","[swapon](/man/swapon)(8), [swapoff](/man/swapoff)(8), [mkswap](/man/mkswap)(8), [free](/man/free)(1), [zramctl](/man/zramctl)(8)"]],"e":["swapon","swapoff","mkswap","free","zramctl"],"x":"sudo dphys-swapfile swapoff"},"euse":{"n":"euse","t":"Gentoo USE flag management utility","s":[["TAGLINE","Gentoo USE flag management utility"],["TLDR","List **active global** USE flags\n\n```euse -a -g```\n\nList **active local** USE flags\n\n```euse -a -l```\n\n**Enable** a global USE flag\n\n```sudo euse -E useflag```\n\n**Disable** a global USE flag\n\n```sudo euse -D useflag```\n\n**Remove** a global USE flag\n\n```sudo euse -P useflag```\n\nShow **info** about a USE flag\n\n```euse -i useflag```"],["SYNOPSIS","**euse** [_options_] [_useflag_]"],["DESCRIPTION","**euse** is a Gentoo utility to enable, disable, and obtain information about USE flags. USE flags control which optional features are compiled into packages during installation, allowing fine-grained control over software capabilities, dependencies, and binary size.\n\nThe tool simplifies USE flag management by providing a command-line interface to modify system and package-specific flags without manually editing configuration files. This is central to Gentoo's philosophy of compile-time customization."],["PARAMETERS","**-a, --active**\n> Show active USE flags\n\n**-g, --global**\n> Operate on global USE flags (make.conf)\n\n**-l, --local**\n> Operate on local USE flags (package.use)\n\n**-E, --enable**\n> Enable the specified USE flag\n\n**-D, --disable**\n> Disable the specified USE flag (adds '-' prefix)\n\n**-P, --prune**\n> Remove the specified USE flag from configuration\n\n**-i, --info**\n> Show information about a USE flag (description, status, type).\n\n**-I, --info-installed**\n> Like **--info**, but also list currently installed packages that use the flag.\n\n**-h, --help**\n> Display help information.\n\n**-v, --version**\n> Display version information."],["CONFIGURATION","**/etc/portage/make.conf**\n> Global USE flags for the entire system set via the USE variable.\n\n**/etc/portage/package.use/**\n> Per-package USE flag settings organized in files within this directory."],["CAVEATS","Global USE flags are set in /etc/portage/make.conf. Local USE flags are set in /etc/portage/package.use/. After changing USE flags, affected packages should be rebuilt with emerge --changed-use."],["HISTORY","**euse** is part of **gentoolkit**, a collection of administration scripts for Gentoo Linux."],["SEE ALSO","[emerge](/man/emerge)(1), [equery](/man/equery)(1), [eselect](/man/eselect)(1)"]],"e":["emerge","equery","eselect"],"x":"euse -a -g"},"ffmpeg-devices":{"n":"ffmpeg-devices","t":"hardware capture device support for ffmpeg","s":[["TAGLINE","hardware capture device support for ffmpeg"],["TLDR","**List available devices**\n\n```ffmpeg -devices```\n\n**Capture from webcam**\n\n```ffmpeg -f v4l2 -i [/dev/video0] [output.mp4]```\n\n**Record screen** (X11)\n\n```ffmpeg -f x11grab -i [:0.0] [screen.mp4]```\n\n**Capture audio device**\n\n```ffmpeg -f alsa -i [hw:0] [audio.wav]```\n\n**List webcam formats**\n\n```ffmpeg -f v4l2 -list_formats all -i [/dev/video0]```"],["SYNOPSIS","**ffmpeg** **-f** _device_ [_options_] **-i** _source_ _output_"],["PARAMETERS","**-f** _DEVICE_\n> Device type: v4l2, x11grab, alsa, pulse, dshow.\n\n**-i** _SOURCE_\n> Device path or identifier.\n\n**v4l2**\n> Video4Linux2 webcam capture.\n\n**x11grab**\n> X11 screen capture.\n\n**alsa**\n> ALSA audio capture.\n\n**pulse**\n> PulseAudio capture.\n\n**dshow**\n> DirectShow (Windows) capture.\n\n**-list_devices**\n> List available devices."],["DESCRIPTION","**ffmpeg devices** enable capturing from hardware sources like webcams, microphones, and screens. Device-specific input formats provide access to system multimedia hardware.\n\nDifferent platforms have different device types: v4l2 and x11grab on Linux, avfoundation on macOS, and dshow on Windows. Each device type has specific options for resolution, framerate, and format.\n\nDevice capture enables recording, streaming, and real-time processing of live input sources."],["CAVEATS","Device names are platform-specific. Permissions may be required. Screen capture performance depends on system. Audio sync can be challenging."],["HISTORY","ffmpeg device support evolved with multimedia hardware APIs. V4L2 support came with Linux video devices, while screen grabbing capabilities were added for recording and streaming use cases."],["INSTALL","```apt: sudo apt install ffmpeg```\n\n```pacman: sudo pacman -S ffmpeg```\n\n```apk: sudo apk add ffmpeg```\n\n```brew: brew install ffmpeg```\n\n```nix: nix profile install nixpkgs#ffmpeg```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [ffprobe](/man/ffprobe)(1), [v4l2-ctl](/man/v4l2-ctl)(1)"],["RESOURCES","```[Homepage](https://ffmpeg.org/)```\n\n```[Documentation](https://ffmpeg.org/ffmpeg-devices.html)```"]],"e":["ffmpeg","ffprobe","v4l2-ctl"],"x":"ffmpeg -devices"},"fmql":{"n":"fmql","t":"Query and manage files using a SQL-like language","s":[["TAGLINE","Query and manage files using a SQL-like language"],["TLDR","**Find** every text file in a directory\n\n```fmql sql \"[SELECT * FROM ~/Documents WHERE extension = 'txt']\"```\n\n**Recurse** into subdirectories\n\n```fmql sql \"[WITH RECURSIVE SELECT name, size FROM ~/projects WHERE extension = 'rs']\"```\n\n**Find** large recently-modified images\n\n```fmql sql \"[SELECT * FROM ~/Pictures WHERE (extension = 'jpg' OR extension = 'png') AND size > 1000000 AND modified > '2026-01-01']\"```\n\n**Sort** results by size, descending\n\n```fmql sql \"[SELECT name, size FROM ~/Downloads ORDER BY size DESC]\"```\n\n**Match** filenames with a regular expression\n\n```fmql sql \"[SELECT * FROM ~/logs WHERE name REGEXP '^app-[0-9]+\\\\.log$']\"```\n\n**Update** permissions on every shell script under a directory\n\n```fmql sql \"[UPDATE ~/scripts SET permissions = '755' WHERE extension = 'sh']\"```"],["SYNOPSIS","**fmql** **sql** \"_QUERY_\""],["PARAMETERS","**sql** _QUERY_\n> Execute a SQL-like query against the filesystem."],["QUERY LANGUAGE","**SELECT** _columns_ **FROM** _path_ [**WHERE** _condition_] [**ORDER BY** _column_ [ASC|DESC]]\n> Match files in _path_ and return selected attributes.\n\n**WITH RECURSIVE SELECT** _columns_ **FROM** _path_ ...\n> Same as **SELECT** but descends into subdirectories.\n\n**UPDATE** _path_ **SET** _attribute_ = _value_ **WHERE** _condition_\n> Modify file attributes (currently permissions and similar mutable metadata) on every matching file."],["QUERYABLE ATTRIBUTES","**name**\n> Filename, including extension.\n\n**extension**\n> File extension without the dot.\n\n**size**\n> Size in bytes.\n\n**modified**\n> Last-modified timestamp; comparable to ISO-8601 date strings.\n\n**permissions**\n> Unix permission bits as an octal or symbolic string.\n\n**executable**\n> Boolean: whether the file has the executable bit set."],["OPERATORS","```\n=    !=    <    >    <=    >=\nLIKE        SQL-style wildcard match (% and _)\nREGEXP      Regular-expression match\nAND   OR   NOT\n```"],["CAVEATS","**fmql** is a small Rust experiment — its SQL dialect is \"sort of\" SQL, not a full query engine. Queries that look valid in a real RDBMS may be rejected. Operations are performed serially on the local filesystem, so recursive queries against very large trees can be slow. **UPDATE** modifies real files; test with **SELECT** first and back up before changing permissions in bulk."],["HISTORY","**fmql** was written by **Chris Mann** (**chriswmann**) in **Rust** and published at **github.com/chriswmann/fmql** and on **crates.io** as the **fmql** crate. It was created as a learning exercise around combining SQL parsing with filesystem operations."],["SEE ALSO","[find](/man/find)(1), [fd](/man/fd)(1), [stat](/man/stat)(1), [chmod](/man/chmod)(1), [sqlite3](/man/sqlite3)(1)"]],"e":["find","fd","stat","chmod","sqlite3"],"x":"fmql sql \"[SELECT * FROM ~/Documents WHERE extension = 'txt']\""},"fq":{"n":"fq","t":"jq for binary formats","s":[["TAGLINE","jq for binary formats"],["TLDR","**Inspect** a binary file\n\n```fq . [file]```\n\n**Decode** specific format\n\n```fq -d [mp4|flac|jpeg|...] . [file]```\n\n**Query** specific fields\n\n```fq '.tracks[0].duration' [file]```\n\n**Open** interactive REPL\n\n```fq -i [file]```"],["SYNOPSIS","**fq** [_options_] [_query_] [_file_]"],["PARAMETERS","**-d, --decode** _FORMAT_\n> Decode as specific format\n\n**-i, --interactive**\n> Start interactive REPL\n\n**-n, --null-input**\n> Use null as input value\n\n**-r, --raw-output**\n> Output raw strings\n\n**-c, --compact-output**\n> Compact output (no pretty print)\n\n**--help**\n> Display help and exit\n\n**--version**\n> Display version and exit"],["DESCRIPTION","**fq** is a tool for inspecting binary files using a jq-like query language. It can decode and query over 100 different binary formats including MP4, FLAC, JPEG, PCAP, ELF, and many more.\n\nThe tool provides a REPL with autocomplete, allowing interactive exploration of binary structures. It's useful for debugging, reverse engineering, and understanding file formats."],["USAGE","Basic inspection:\n\n```fq . file.mp4```\n\nQuery specific field:\n\n```fq '.meta.moov.tracks[0].mdia.minf.stbl.sample_descriptions[0].width' video.mp4```\n\nDecode as raw bytes:\n\n```fq -d raw '.[0:10]' file```\n\nList supported formats:\n\n```fq -h```"],["CAVEATS","Large files may consume significant memory. Some formats have incomplete support. Binary data output may require terminal with proper encoding support. Complex nested queries can be slow."],["HISTORY","**fq** was created by **Mattias Wadman** and first released in **2021**, bringing the power of jq to binary data and making it easier to inspect and understand binary file formats without specialized tools."],["INSTALL","```apt: sudo apt install fq```\n\n```pacman: sudo pacman -S fq```\n\n```zypper: sudo zypper install fq```\n\n```brew: brew install fq```\n\n```nix: nix profile install nixpkgs#fq```"],["SEE ALSO","[jq](/man/jq)(1), [xxd](/man/xxd)(1), [hexdump](/man/hexdump)(1), [binwalk](/man/binwalk)(1)"]],"e":["jq","xxd","hexdump","binwalk"],"x":"fq . [file]"},"gdalwarp":{"n":"gdalwarp","t":"image mosaicing, reprojection, and warping utility","s":[["TAGLINE","image mosaicing, reprojection, and warping utility"],["TLDR","**Reproject a raster** to a different coordinate system\n\n```gdalwarp -t_srs [EPSG:4326] [input.tif] [output.tif]```\n\n**Reproject with source and target** coordinate systems\n\n```gdalwarp -s_srs [EPSG:32611] -t_srs [EPSG:4326] [input.tif] [output.tif]```\n\n**Change resolution** while reprojecting\n\n```gdalwarp -tr [0.001] [0.001] -t_srs [EPSG:4326] [input.tif] [output.tif]```\n\n**Use bilinear resampling** method\n\n```gdalwarp -r bilinear -t_srs [EPSG:4326] [input.tif] [output.tif]```\n\n**Mosaic multiple rasters** into one output\n\n```gdalwarp [input1.tif] [input2.tif] [input3.tif] [output.tif]```\n\n**Clip raster using a shapefile** cutline\n\n```gdalwarp -cutline [boundary.shp] -crop_to_cutline [input.tif] [output.tif]```\n\n**Specify output format** and data type\n\n```gdalwarp -of GTiff -ot Float32 -t_srs [EPSG:4326] [input.tif] [output.tif]```"],["SYNOPSIS","**gdalwarp** [_OPTIONS_] _SRCFILE_... _DSTFILE_"],["PARAMETERS","**-s_srs** _SRS_\n> Source spatial reference system (EPSG code, PROJ.4, or WKT).\n\n**-t_srs** _SRS_\n> Target spatial reference system for output.\n\n**-r** _METHOD_\n> Resampling method: near, bilinear, cubic, cubicspline, lanczos, average, mode, max, min, med, q1, q3, sum.\n\n**-tr** _XRES_ _YRES_\n> Target resolution in georeferenced units.\n\n**-ts** _WIDTH_ _HEIGHT_\n> Target size in pixels.\n\n**-te** _XMIN_ _YMIN_ _XMAX_ _YMAX_\n> Target extent in georeferenced coordinates.\n\n**-of** _FORMAT_\n> Output format (GTiff, PNG, JPEG, etc.).\n\n**-ot** _TYPE_\n> Output band data type (Byte, UInt16, Float32, etc.).\n\n**-cutline** _FILE_\n> Vector file for clipping (shapefile, GeoJSON, etc.).\n\n**-crop_to_cutline**\n> Crop output extent to cutline boundary.\n\n**-overwrite**\n> Overwrite existing output file.\n\n**-multi**\n> Use multithreaded warping.\n\n**-co** _NAME_=_VALUE_\n> Creation option for output format."],["DESCRIPTION","**gdalwarp** is an image mosaicing, reprojection, and warping utility from the GDAL library. It transforms raster datasets between coordinate reference systems, resamples to different resolutions, mosaics multiple inputs into a single output, and clips rasters using vector boundaries.\n\nThe utility supports all GDAL raster formats and can apply ground control points (GCPs) stored with images. Various resampling algorithms are available, from fast nearest-neighbor to high-quality Lanczos windowed sinc interpolation.\n\ngdalwarp is essential for GIS workflows requiring coordinate system transformations, data integration from multiple sources, or preparing raster data for specific analysis requirements."],["CAVEATS","Large rasters may require significant memory; use **-wm** to limit memory usage. Reprojection can introduce artifacts at high compression or with inappropriate resampling methods. Source coordinate system must be defined or specified with -s_srs. NoData values should be handled explicitly with -srcnodata and -dstnodata."],["HISTORY","gdalwarp is part of **GDAL** (Geospatial Data Abstraction Library), originally developed by Frank Warmerdam starting in **1998**. GDAL became an OSGeo project and has grown into the fundamental library for geospatial raster operations, used by virtually all GIS software including QGIS, ArcGIS, and countless specialized tools."],["INSTALL","```apt: sudo apt install gdal-bin```\n\n```apk: sudo apk add gdal-tools```"],["SEE ALSO","[gdal_translate](/man/gdal_translate)(1), [gdalinfo](/man/gdalinfo)(1), [ogr2ogr](/man/ogr2ogr)(1), [gdalbuildvrt](/man/gdalbuildvrt)(1)"],["RESOURCES","```[Documentation](https://gdal.org/programs/gdalwarp.html)```\n\n```[Source code](https://github.com/OSGeo/gdal)```"]],"e":["gdal_translate","gdalinfo","ogr2ogr","gdalbuildvrt"],"x":"gdalwarp -t_srs [EPSG:4326] [input.tif] [output.tif]"},"genid":{"n":"genid","t":"random identifier and UUID generator","s":[["TAGLINE","random identifier and UUID generator"],["TLDR","**Generate random ID**\n\n```genid```\n\n**Generate UUID**\n\n```genid --uuid```\n\n**Generate specific length**\n\n```genid -l [32]```\n\n**Alphanumeric only**\n\n```genid --alphanumeric```"],["SYNOPSIS","**genid** [_options_]"],["PARAMETERS","**-l** _LENGTH_\n> ID length.\n\n**--uuid**\n> Generate UUID format.\n\n**--alphanumeric**\n> Use only letters and numbers.\n\n**--hex**\n> Hexadecimal output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**genid** generates random identifiers suitable for various purposes. It produces unique strings for use as IDs, tokens, or keys.\n\nThe tool supports multiple formats including UUID, alphanumeric strings, and custom lengths. Output is cryptographically suitable for non-security-critical uses."],["CAVEATS","Not for cryptographic keys. Uniqueness not guaranteed. May vary between implementations."],["HISTORY","genid is a utility for generating random identifiers, providing convenient access to random string generation for scripting and development purposes."],["SEE ALSO","[uuid](/man/uuid)(1), [uuidgen](/man/uuidgen)(1), [pwgen](/man/pwgen)(1)"]],"e":["uuid","uuidgen","pwgen"],"x":"genid"},"gh-repo":{"n":"gh-repo","t":"Manage GitHub repositories from the command line","s":[["TAGLINE","Manage GitHub repositories from the command line"],["TLDR","**Clone a repository**\n\n```gh repo clone [owner]/[repo]```\n\n**Create a new repository**\n\n```gh repo create [name] --public```\n\n**Fork a repository**\n\n```gh repo fork [owner]/[repo]```\n\n**View repository**\n\n```gh repo view```\n\n**List your repositories**\n\n```gh repo list```"],["SYNOPSIS","**gh repo** _command_ [_options_]"],["PARAMETERS","**clone** _REPO_\n> Clone a repository locally.\n\n**create** _NAME_\n> Create a new repository.\n\n**fork** _REPO_\n> Fork a repository.\n\n**view** _REPO_\n> View repository details.\n\n**list**\n> List accessible repositories.\n\n**delete** _REPO_\n> Delete a repository.\n\n**rename** _NEW-NAME_\n> Rename current repository.\n\n**sync**\n> Sync fork with upstream.\n\n**--public**\n> Make repository public.\n\n**--private**\n> Make repository private.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gh repo** manages GitHub repositories from the command line. It provides repository creation, cloning, forking, and management without visiting github.com.\n\nThe command supports both HTTPS and SSH cloning based on configuration. Repository creation includes options for visibility, description, license, and gitignore templates. Forking creates linked copies for contribution workflows."],["CAVEATS","Delete requires confirmation. Fork operations need network access. Some operations require owner permissions."],["HISTORY","gh repo is a core command in the **GitHub CLI**, handling repository operations that were previously only available through the web interface or API."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-pr](/man/gh-pr)(1), [git-clone](/man/git-clone)(1)"],["RESOURCES","```[Documentation](https://cli.github.com/manual/gh_repo)```\n\n```[Source code](https://github.com/cli/cli)```"]],"e":["gh","gh-pr","git-clone"],"x":"gh repo clone [owner]/[repo]"},"ghostscript":{"n":"ghostscript","t":"Interpreter for PostScript and PDF file manipulation","s":[["TAGLINE","Interpreter for PostScript and PDF file manipulation"],["TLDR","**Convert PDF to images**\n\n```gs -dNOPAUSE -dBATCH -sDEVICE=png16m -r300 -sOutputFile=[page-%d.png] [input.pdf]```\n\n**Merge PDFs**\n\n```gs -dNOPAUSE -dBATCH -sDEVICE=pdfwrite -sOutputFile=[merged.pdf] [file1.pdf] [file2.pdf]```\n\n**Compress PDF**\n\n```gs -dNOPAUSE -dBATCH -sDEVICE=pdfwrite -dPDFSETTINGS=/ebook -sOutputFile=[small.pdf] [large.pdf]```\n\n**Convert PS to PDF**\n\n```gs -dNOPAUSE -dBATCH -sDEVICE=pdfwrite -sOutputFile=[output.pdf] [input.ps]```\n\n**Extract pages from PDF**\n\n```gs -dNOPAUSE -dBATCH -sDEVICE=pdfwrite -dFirstPage=[2] -dLastPage=[5] -sOutputFile=[extract.pdf] [input.pdf]```"],["SYNOPSIS","**gs** [_options_] [_files_]"],["PARAMETERS","**-dNOPAUSE**\n> Don't pause between pages.\n\n**-dBATCH**\n> Exit after processing.\n\n**-sDEVICE** _device_\n> Output device (pdfwrite, png16m, jpeg, etc.).\n\n**-sOutputFile** _file_\n> Output filename (use %d for page numbers).\n\n**-r** _resolution_\n> Resolution in DPI.\n\n**-dPDFSETTINGS** _setting_\n> PDF quality: /screen, /ebook, /printer, /prepress.\n\n**-dFirstPage** _n_\n> Start page.\n\n**-dLastPage** _n_\n> End page.\n\n**-dCompatibilityLevel** _ver_\n> PDF version (1.4, 1.5, etc.).\n\n**-q**\n> Quiet mode; suppress startup messages.\n\n**-dSAFER**\n> Restrict file operations for safety (default in recent versions)."],["DESCRIPTION","**Ghostscript** (gs) is an interpreter for PostScript and PDF. It renders documents to various output formats, converts between formats, and processes print jobs. It is the engine behind many PDF tools and print systems on Unix/Linux.\n\nThe tool supports a wide range of output devices for rasterization, format conversion, and print processing. PDF manipulation includes merging, splitting, compressing, and converting between quality levels."],["CONFIGURATION","**/etc/ghostscript/**\n> System-wide configuration directory for font mappings, resource files, and initialization scripts.\n\n**~/.ghostscript**\n> User-specific Ghostscript resources and configuration overrides."],["CAVEATS","Complex command-line syntax. Some operations require specific device drivers. Font handling may need configuration. Security settings restrict file operations."],["HISTORY","Ghostscript was started by **L. Peter Deutsch** in **1988** as a free PostScript interpreter. It's now developed by Artifex Software and remains fundamental to Unix/Linux printing and PDF processing."],["INSTALL","```apt: sudo apt install ghostscript```\n\n```dnf: sudo dnf install ghostscript```\n\n```pacman: sudo pacman -S ghostscript```\n\n```zypper: sudo zypper install ghostscript```\n\n```brew: brew install ghostscript```\n\n```nix: nix profile install nixpkgs#ghostscript```"],["SEE ALSO","[pdf2ps](/man/pdf2ps)(1), [ps2pdf](/man/ps2pdf)(1), [pdftk](/man/pdftk)(1), [convert](/man/convert)(1)"]],"e":["pdf2ps","ps2pdf","pdftk","convert"],"x":"gs -dNOPAUSE -dBATCH -sDEVICE=png16m -r300 -sOutputFile=[page-%d.png] [input.pdf]"},"grex":{"n":"grex","t":"Regex generator from example strings","s":[["TAGLINE","Regex generator from example strings"],["TLDR","**Generate regex from examples**\n\n```grex [foo] [bar] [baz]```\n\n**Convert digits to** `\\d`\n\n```grex -d [test1] [test2]```\n\n**Convert whitespace to** `\\s` **and words to** `\\w`\n\n```grex -s -w [example1] [example2]```\n\n**Case insensitive matching**\n\n```grex -i [example1] [example2]```\n\n**Generate with repetition detection**\n\n```grex -r [aaab] [aaac]```\n\n**Generate without start/end anchors**\n\n```grex --no-anchors [examples...]```\n\n**Read examples from file** (one per line)\n\n```grex -f [examples.txt]```"],["SYNOPSIS","**grex** [_options_] _test-strings_"],["PARAMETERS","_TEST-STRINGS_\n> Example strings to generate regex from.\n\n**-d**, **--digits**\n> Convert Unicode decimal digits to \\d.\n\n**-D**, **--non-digits**\n> Convert non-digit characters to \\D.\n\n**-s**, **--spaces**\n> Convert Unicode whitespace to \\s.\n\n**-S**, **--non-spaces**\n> Convert non-whitespace characters to \\S.\n\n**-w**, **--words**\n> Convert Unicode word characters to \\w.\n\n**-W**, **--non-words**\n> Convert non-word characters to \\W.\n\n**-i**, **--ignore-case**\n> Generate case-insensitive regex.\n\n**-e**, **--escape**\n> Replace all non-ASCII characters with unicode escape sequences.\n\n**-r**, **--repetitions**\n> Detect repeated substrings and convert to quantifier notation.\n\n**--no-anchors**\n> Remove ^ and $ anchors (anchors are added by default).\n\n**--no-start-anchor**\n> Remove the ^ start anchor only.\n\n**--no-end-anchor**\n> Remove the $ end anchor only.\n\n**-f**, **--file** _FILE_\n> Read test strings from a file (one per line).\n\n**--help**\n> Display help information."],["DESCRIPTION","**grex** generates regular expressions from user-provided test strings. It analyzes the input examples and produces a regex that matches all of them. By default, the generated regex includes ^ and $ anchors.\n\nThe tool supports various character class conversions (\\d, \\s, \\w and their inverses), repetition detection, and case-insensitive matching. It is useful for quickly creating regexes without manual pattern analysis."],["CAVEATS","Generated regex may be overly specific or general. Manual refinement often needed. Complex patterns may require additional examples."],["HISTORY","grex was created by **Peter M. Stahl** as a command-line tool to simplify regex creation through example-based learning."],["INSTALL","```pacman: sudo pacman -S grex```\n\n```zypper: sudo zypper install grex```\n\n```brew: brew install grex```\n\n```nix: nix profile install nixpkgs#grex```"],["SEE ALSO","[grep](/man/grep)(1), [sed](/man/sed)(1), [regex](/man/regex)(7)"]],"e":["grep","sed","regex"],"x":"grex [foo] [bar] [baz]"},"gvfs-rm":{"n":"gvfs-rm","t":"removes files using GVFS","s":[["TAGLINE","removes files using GVFS"],["TLDR","**Remove file**\n\n```gvfs-rm [file]```\n\n**Remove remote file**\n\n```gvfs-rm smb://server/share/file.txt```\n\n**Force removal**\n\n```gvfs-rm -f [file]```"],["SYNOPSIS","**gvfs-rm** [_options_] _location_"],["PARAMETERS","_LOCATION_\n> File path or URI to remove.\n\n**-f**, **--force**\n> Ignore nonexistent and non-deletable files.\n\n**-?**, **--help**\n> Print a short help text and exit."],["DESCRIPTION","**gvfs-rm** removes files using GVFS. It can delete local files and files on remote servers accessed through GVFS backends.\n\nThe tool handles deletion across different protocols transparently. It works with SMB, FTP, SSH, and other GVFS-supported systems."],["CAVEATS","Part of the gvfs-bin command-line tools, deprecated in gvfs 1.31 (2016) and removed entirely in gvfs 1.38 (2018) in favor of the unified **gio** command (`gio remove`). Modern distributions no longer ship gvfs-rm. Deletes permanently rather than sending to trash; no recursive directory removal. Use **gvfs-trash** for recoverable deletion."],["HISTORY","**gvfs-rm** was part of GVFS's original gvfs-bin utilities, introduced for GNOME alongside GVFS in **2008** (GNOME 2.22). It has since been superseded by **gio remove**."],["SEE ALSO","[gio](/man/gio)(1), [rm](/man/rm)(1), [gvfs-trash](/man/gvfs-trash)(1)"],["RESOURCES","```[Source code](https://gitlab.gnome.org/GNOME/gvfs)```\n\n```[Homepage](https://wiki.gnome.org/Projects/gvfs)```"]],"e":["gio","rm","gvfs-trash"],"x":"gvfs-rm [file]"},"helmfile":{"n":"helmfile","t":"Declarative Helm chart deployment for Kubernetes","s":[["TAGLINE","Declarative Helm chart deployment for Kubernetes"],["TLDR","**Apply** the desired state from **helmfile.yaml**\n\n```helmfile apply```\n\n**Sync** releases to match the spec (install missing, upgrade changed)\n\n```helmfile sync```\n\n**Diff** pending changes before applying\n\n```helmfile diff```\n\n**Destroy** all releases defined in the helmfile\n\n```helmfile destroy```\n\n**Lint** the helmfile and referenced charts\n\n```helmfile lint```\n\n**Generate** a new project scaffold\n\n```helmfile create [my-project]```"],["SYNOPSIS","**helmfile** [_global_options_] **apply** | **sync** | **diff** | **destroy** | **lint** | **template** | **build** | **list** | **status** | **delete** | **init** | **create** [_command_options_]"],["PARAMETERS","**-f**, **--file** _path_\n> Path to the helmfile (default: **helmfile.yaml**). Repeatable.\n\n**-e**, **--environment** _name_\n> Environment name for layered values (for example **staging**, **production**).\n\n**--selector** _label=value_\n> Limit operations to releases matching the label selector.\n\n**-l**, **--selector** _label=value_\n> Alias for **--selector**.\n\n**-n**, **--namespace** _namespace_\n> Kubernetes namespace override.\n\n**--kube-context** _context_\n> Kubernetes context to use.\n\n**--debug**\n> Enable verbose debug output.\n\n**--quiet**\n> Suppress non-essential output.\n\n**--no-color**\n> Disable ANSI colour codes."],["DESCRIPTION","**helmfile** is a declarative spec for deploying Helm charts to Kubernetes. You describe repositories, releases, values, and hooks in YAML (typically **helmfile.yaml**), version-control that file, and run **helmfile apply** or **helmfile sync** to converge the cluster to the declared state.\n\nThe executable delegates chart operations to **helm** and expects the **helm-diff** plugin for the **diff** subcommand. Run **helmfile init** once after installation to install required plugins.\n\nHelmfile supports modular helmfiles (importing shared fragments from Git or S3), environment-specific value layers, JSON and strategic-merge patches on rendered manifests before install, and mixing Helm charts with raw Kubernetes manifests and Kustomize bases. Releases can declare dependencies so Helmfile installs them in the correct order.\n\nCommon workflows: **helmfile diff** to preview changes, **helmfile apply** to install and upgrade in one step, **helmfile template** or **helmfile build** to render manifests for GitOps tools such as Argo CD, and **helmfile destroy** to tear down every release managed by the file."],["CONFIGURATION","**helmfile.yaml**\n> Main declarative spec. Defines **repositories**, **releases**, optional **environments**, **helmDefaults**, and **templates**.\n\n**helmfile.d/**\n> Directory of additional fragment YAML files merged into the main spec.\n\n**.helmfile/**\n> Optional local state and cache directory created during operation.\n\n**helmfile init**\n> Installs the **helm-diff** plugin and other dependencies expected by Helmfile."],["CAVEATS","Helmfile v1.x introduced breaking changes from v0.x; review the v1 migration guide before upgrading legacy projects. **helm** and **helm-diff** must be installed separately and kept compatible with the Helmfile version in use. The **diff** subcommand requires a working **helm-diff** plugin."],["INSTALL","```pacman: sudo pacman -S helmfile```\n\n```zypper: sudo zypper install helmfile```\n\n```brew: brew install helmfile```\n\n```nix: nix profile install nixpkgs#helmfile```"],["SEE ALSO","[helm](/man/helm)(1), [helm-install](/man/helm-install)(1), [kubectl](/man/kubectl)(1)"]],"e":["helm","helm-install","kubectl"],"x":"helmfile apply"},"i2cset":{"n":"i2cset","t":"writes a value to a specified register of an I2C device","s":[["TAGLINE","writes a value to a specified register of an I2C device"],["TLDR","**Write** to a register of an I2C device\n\n```i2cset [i2cbus] [device_address] [register_address] [value]```\n\nWrite without **confirmation** prompt\n\n```i2cset -y [i2cbus] [device_address] [register_address] [value]```\n\nWrite using a **specific mode**\n\n```i2cset [i2cbus] [device_address] [register_address] [value] [b|w|c|s|i]```"],["SYNOPSIS","**i2cset** [_options_] _i2cbus_ _device_address_ _register_address_ _value_ [_mode_]"],["PARAMETERS","**-y**\n> Disable interactive confirmation (dangerous on some hardware)\n\n**b**\n> Byte mode - write single byte (default)\n\n**w**\n> Word mode - write 16-bit value\n\n**c**\n> Consecutive byte mode\n\n**s**\n> SMBus block mode\n\n**i**\n> I2C block mode"],["DESCRIPTION","**i2cset** writes a value to a specified register of an I2C device. This allows configuration of I2C peripherals, setting parameters, or controlling device behavior.\n\nAll addresses and values should be specified in hexadecimal notation. The command will prompt for confirmation before writing unless the -y flag is used."],["CAVEATS","Writing to I2C registers can cause hardware damage or unexpected behavior. Always verify device addresses and register meanings before writing. Some devices may become unresponsive or require power cycling after incorrect writes. Requires root privileges or i2c group membership."],["HISTORY","i2cset is part of the i2c-tools package, providing userspace write access to I2C devices. The I2C protocol was developed by Philips (now NXP) in **1982** for low-speed peripheral communication."],["INSTALL","```apt: sudo apt install i2c-tools```\n\n```dnf: sudo dnf install i2c-tools```\n\n```pacman: sudo pacman -S i2c-tools```\n\n```apk: sudo apk add i2c-tools```\n\n```zypper: sudo zypper install i2c-tools```\n\n```brew: brew install i2c-tools```\n\n```nix: nix profile install nixpkgs#i2c-tools```"],["SEE ALSO","[i2cdetect](/man/i2cdetect)(8), [i2cdump](/man/i2cdump)(8), [i2cget](/man/i2cget)(8)"]],"e":["i2cdetect","i2cdump","i2cget"],"x":"i2cset [i2cbus] [device_address] [register_address] [value]"},"idevicediagnostics":{"n":"idevicediagnostics","t":"Interact with the diagnostics interface of an iOS device","s":[["TAGLINE","Interact with the diagnostics interface of an iOS device"],["TLDR","**Restart device**\n\n```idevicediagnostics restart```\n\n**Shutdown device**\n\n```idevicediagnostics shutdown```\n\n**Put device to sleep**\n\n```idevicediagnostics sleep```\n\n**Get diagnostics info**\n\n```idevicediagnostics diagnostics```\n\n**Get battery info**\n\n```idevicediagnostics diagnostics GasGauge```\n\n**Target a specific device** by UDID\n\n```idevicediagnostics -u [UDID] restart```\n\n**Print IORegistry** for a specific plane\n\n```idevicediagnostics ioregistry IODeviceTree```"],["SYNOPSIS","**idevicediagnostics** [_options_] _command_"],["SUBCOMMANDS","**diagnostics** [_type_]\n> Print diagnostics information, optionally by type: All (default), WiFi, GasGauge, or NAND.\n\n**mobilegestalt** _key_ [_key_ ...]\n> Print values of MobileGestalt keys.\n\n**ioregistry** [_plane_]\n> Print IORegistry of device, optionally by plane (IODeviceTree, IOPower, IOService). iOS 5+ only.\n\n**restart**\n> Restart the device.\n\n**shutdown**\n> Shut down the device.\n\n**sleep**\n> Put device to sleep."],["PARAMETERS","**-u**, **--udid** _UDID_\n> Target specific device by its UDID.\n\n**-n**, **--network**\n> Connect to network device.\n\n**-d**, **--debug**\n> Enable communication debugging.\n\n**-h**, **--help**\n> Print usage information.\n\n**-v**, **--version**\n> Print version information."],["DESCRIPTION","**idevicediagnostics** interacts with the diagnostics interface of an iOS device to retrieve diagnostics data, MobileGestalt data, and IORegistry information, as well as perform actions like restart, shutdown, and sleep. Part of the **libimobiledevice** suite. Available for iOS 4 and later; IORegistry access requires iOS 5 or later."],["INSTALL","```apt: sudo apt install libimobiledevice-utils```\n\n```dnf: sudo dnf install libimobiledevice-utils```\n\n```apk: sudo apk add libimobiledevice-progs```"],["SEE ALSO","[ideviceinfo](/man/ideviceinfo)(1), [idevicecrashreport](/man/idevicecrashreport)(1), [idevicepair](/man/idevicepair)(1), [idevicebackup2](/man/idevicebackup2)(1), [idevicesyslog](/man/idevicesyslog)(1), [idevicescreenshot](/man/idevicescreenshot)(1)"]],"e":["ideviceinfo","idevicecrashreport","idevicepair","idevicebackup2","idevicesyslog","idevicescreenshot"],"x":"idevicediagnostics restart"},"ilbmtoppm":{"n":"ilbmtoppm","t":"Convert an IFF ILBM file into a PPM image","s":[["TAGLINE","Convert an IFF ILBM file into a PPM image"],["TLDR","**Convert an ILBM file to PPM**\n\n```ilbmtoppm [file.ilbm] > [output.ppm]```\n\n**Convert from stdin**\n\n```cat [file.ilbm] | ilbmtoppm > [output.ppm]```\n\n**Convert only the color map, not the image**\n\n```ilbmtoppm -cmaponly [file.ilbm] > [output.ppm]```\n\n**Convert with verbose output**\n\n```ilbmtoppm -verbose [file.ilbm] > [output.ppm]```\n\n**Force interpretation as HAM format**\n\n```ilbmtoppm -isham [file.ilbm] > [output.ppm]```"],["SYNOPSIS","**ilbmtoppm** [_options_] [_ilbmfile_]"],["PARAMETERS","**-verbose**\n> Display information about the ILBM file during conversion.\n\n**-ignore** _chunkID_\n> Skip the specified 4-letter IFF chunk during processing.\n\n**-isham**\n> Treat the input as a HAM (Hold-And-Modify) picture, even if the CAMG chunk does not indicate this.\n\n**-isnotham**\n> Do not treat the input as HAM, even if the CAMG chunk indicates it.\n\n**-isehb**\n> Treat the input as Extra_Halfbrite (EHB), even if not indicated in the CAMG chunk.\n\n**-isnotehb**\n> Do not treat the input as EHB.\n\n**-isdeep**\n> Treat the input as a deep (direct color) image.\n\n**-isnotdeep**\n> Do not treat the input as a deep image.\n\n**-cmaponly**\n> Output a PPM of the color map only, not the image itself.\n\n**-adjustcolors**\n> Scale the color map to 8 bits per channel.\n\n**-transparent** _color_\n> Set the transparency color.\n\n**-maskfile** _filename_\n> Write the transparency mask to the specified PBM file."],["DESCRIPTION","**ilbmtoppm** converts an IFF ILBM (Interleaved Bitmap) file to PPM (Portable Pixmap) format. ILBM was the standard image format on Amiga computers.\n\nThe program handles normal ILBMs with 1-16 planes, Amiga Extra_Halfbrite (EHB), Amiga HAM with 3-16 planes, Multipalette pictures (normal or HAM), color map only images, and unofficial direct color formats. It is part of the **Netpbm** package of graphics manipulation tools."],["CAVEATS","Some ILBM files use non-standard or unofficial extensions that may not be correctly auto-detected. Use the **-isham**, **-isehb**, or **-isdeep** flags to force the correct interpretation when auto-detection fails."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtoilbm](/man/ppmtoilbm)(1), [netpbm](/man/netpbm)(1)"]],"e":["ppmtoilbm","netpbm"],"x":"ilbmtoppm [file.ilbm] > [output.ppm]"},"impacket-rpcdump":{"n":"impacket-rpcdump","t":"enumerates RPC endpoints on a target system","s":[["TAGLINE","enumerates RPC endpoints on a target system"],["TLDR","**Dump RPC endpoints**\n\n```impacket-rpcdump [target]```\n\n**Dump with authentication**\n\n```impacket-rpcdump [domain]/[user]:[password]@[target]```\n\n**Specify port**\n\n```impacket-rpcdump -port [135] [target]```"],["SYNOPSIS","**impacket-rpcdump** [_options_] _target_"],["PARAMETERS","**-port** _PORT_\n> RPC endpoint port (default _135_).\n\n**-target-ip** _IP_\n> Override the IP used for the connection (useful when the target is given as a hostname).\n\n**-hashes** _LMHASH:NTHASH_\n> Authenticate via Pass-the-Hash instead of supplying a password.\n\n**-no-pass**\n> Don't prompt for a password (use **-hashes** or **-k** alone).\n\n**-k**\n> Use Kerberos authentication. Credentials are read from **ccache** (set by **kinit**).\n\n**-aesKey** _HEX_\n> AES key for Kerberos authentication.\n\n**-dc-ip** _IP_\n> IP of the domain controller for Kerberos.\n\n**-debug**\n> Print verbose protocol-level output."],["DESCRIPTION","**impacket-rpcdump** queries the Microsoft RPC endpoint mapper (port 135 by default) and prints every registered endpoint, its UUID, and the bindings (named pipes, TCP/UDP ports) it speaks. It is the Python/Impacket equivalent of Microsoft's classic **rpcdump.exe** and is commonly used to enumerate exposed Active Directory services during authorized engagements.\n\nWhen credentials are supplied (clear-text password, NTLM hash, or Kerberos ticket), additional services that require authentication may appear in the listing; an unauthenticated dump usually returns only the small set of endpoints reachable anonymously."],["CAVEATS","For **authorized testing only**. Some services restrict anonymous endpoint enumeration on modern Windows builds (e.g., **RestrictRemoteSAM**), so empty output does not mean no services are running."],["INSTALL","```pacman: sudo pacman -S impacket```"],["SEE ALSO","[impacket-rpcmap](/man/impacket-rpcmap)(1), [rpcclient](/man/rpcclient)(1)"]],"e":["impacket-rpcmap","rpcclient"],"x":"impacket-rpcdump [target]"},"init":{"n":"init","t":"parent of all processes on the system, responsible for starting and stopping","s":[["TAGLINE","parent of all processes on the system, responsible for starting and stopping"],["TLDR","Set system to **graphical** environment (runlevel 5)\n\n```sudo init 5```\n\nSet system to **multiuser terminal** (runlevel 3)\n\n```sudo init 3```\n\nSet system to **single user** mode (runlevel 1)\n\n```sudo init 1```\n\n**Shut down** the system\n\n```init 0```\n\n**Reboot** the system\n\n```init 6```"],["SYNOPSIS","**init** [_runlevel_]"],["PARAMETERS","**0**\n> Halt the system\n\n**1** or **S**\n> Single user mode (maintenance)\n\n**2**\n> Multi-user mode without NFS (Debian default multi-user with GUI)\n\n**3**\n> Multi-user mode with networking, text console only\n\n**4**\n> Unused/custom (available for user-defined purposes)\n\n**5**\n> Multi-user mode with networking and graphical desktop (Red Hat default GUI)\n\n**6**\n> Reboot the system"],["DESCRIPTION","**init** is the parent of all processes on the system, responsible for starting and stopping services at different runlevels. It is the traditional System V init daemon that manages the system state.\n\nRunlevels define different system states, from single-user maintenance mode to full graphical desktop. Changing runlevels causes init to start or stop services as defined in /etc/rc.d or /etc/init.d scripts.\n\nOn modern systemd-based systems, init may be a compatibility wrapper that translates runlevel commands to systemd targets."],["CAVEATS","Behavior varies between distributions. On systemd-based systems, **init** is a symlink to systemd and runlevel commands are translated to systemd targets (e.g., runlevel 3 maps to multi-user.target, runlevel 5 to graphical.target). Direct runlevel changes can disrupt running services. Use **systemctl** on modern systems."],["HISTORY","System V init originated in AT&T UNIX System V in **1983**. It was the standard init system on most Linux distributions until systemd began replacing it around **2011**."],["INSTALL","```apt: sudo apt install runit-init```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [telinit](/man/telinit)(8), [runlevel](/man/runlevel)(8), [halt](/man/halt)(8), [reboot](/man/reboot)(8), [shutdown](/man/shutdown)(8), [service](/man/service)(8)"]],"e":["systemctl","telinit","runlevel","halt","reboot","shutdown","service"],"x":"sudo init 5"},"ip-netconf":{"n":"ip-netconf","t":"displays network configuration parameters for interfaces","s":[["TAGLINE","displays network configuration parameters for interfaces"],["TLDR","Show **all** network configuration\n\n```ip netconf```\n\nShow config for **specific interface**\n\n```ip netconf show dev eth0```\n\nShow only **IPv4** configuration\n\n```ip -4 netconf```\n\nShow only **IPv6** configuration\n\n```ip -6 netconf```"],["SYNOPSIS","**ip** [_OPTIONS_] **netconf** { **show** [ **dev** _NAME_ ] | **help** }"],["DESCRIPTION","**ip netconf** monitors and displays IPv4 and IPv6 network configuration parameters from /proc/sys/net/ipv[4|6]/conf/. It shows settings like forwarding status, RPF (Reverse Path Filtering) mode, MC forwarding, and proxy ARP/NDP status. If no interface is specified, the **all** entry is displayed."],["PARAMETERS","**show**\n> Show network configuration (default if omitted)\n\n**dev NAME**\n> Show configuration for specific interface\n\n**help**\n> Display help information"],["OUTPUT FIELDS","**forwarding**\n> Whether IP forwarding is enabled\n\n**rp_filter**\n> Reverse Path Filtering mode (0=off, 1=strict, 2=loose)\n\n**mc_forwarding**\n> Whether multicast forwarding is enabled\n\n**proxy_neigh**\n> Whether proxy neighbor (ARP/NDP) is enabled\n\n**ignore_routes_with_linkdown**\n> Ignore routes when link is down"],["CAVEATS","Use -4 or -6 to filter by address family. Configuration can only be changed through sysctl or interface-specific tools, not through ip netconf directly."],["SEE ALSO","[ip](/man/ip)(8), [ip-link](/man/ip-link)(8), [ip-address](/man/ip-address)(8), [sysctl](/man/sysctl)(8)"]],"e":["ip","ip-link","ip-address","sysctl"],"x":"ip netconf"},"jira-open":{"n":"jira-open","t":"opens Jira resources in the default web browser","s":[["TAGLINE","opens Jira resources in the default web browser"],["TLDR","**Open issue in browser**\n\n```jira open [PROJ-123]```\n\n**Open project board**\n\n```jira open -p [PROJECT]```\n\n**Open current sprint**\n\n```jira open --sprint```\n\n**Open backlog**\n\n```jira open --backlog -p [PROJECT]```"],["SYNOPSIS","**jira open** [_options_] [_issue_]"],["PARAMETERS","_ISSUE_\n> Issue key to open.\n\n**-p** _PROJECT_\n> Project to open.\n\n**--sprint**\n> Open active sprint.\n\n**--backlog**\n> Open backlog.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jira open** opens Jira resources in the default web browser. It provides quick access to issues, boards, and backlogs.\n\nThe command translates issue keys or project identifiers to web URLs. It's faster than navigating through the web interface."],["CAVEATS","Subcommand of jira-cli. Requires configured server. Uses default browser."],["HISTORY","jira open is a convenience command in **jira-cli** for quickly accessing Jira web interface from terminal."],["SEE ALSO","[jira](/man/jira)(1), [jira-me](/man/jira-me)(1), [open](/man/open)(1)"]],"e":["jira","jira-me","open"],"x":"jira open [PROJ-123]"},"jj-abandon":{"n":"jj-abandon","t":"abandon revisions in Jujutsu","s":[["TAGLINE","abandon revisions in Jujutsu"],["TLDR","**Abandon the current working-copy** change\n\n```jj abandon```\n\n**Abandon a specific** revision\n\n```jj abandon [revision]```\n\n**Abandon multiple revisions** at once\n\n```jj abandon [rev1] [rev2]```\n\n**Abandon a revset** (e.g. all descendants of main)\n\n```jj abandon \"descendants(main)\"```\n\n**Abandon without** rebasing descendants\n\n```jj abandon --restore-descendants [revision]```\n\n**Abandon and keep bookmarks** pointing at the parent\n\n```jj abandon --retain-bookmarks [revision]```"],["SYNOPSIS","**jj** **abandon** [_options_] [_revsets_...]"],["PARAMETERS","_revsets_\n> One or more revsets to abandon. Defaults to the working-copy commit (`@`) when omitted.\n\n**--restore-descendants**\n> Do not rewrite descendants of the abandoned commits. Their parent pointers stay the same and their contents are preserved.\n\n**--retain-bookmarks**\n> Move bookmarks pointing at abandoned commits to the parent revision instead of deleting them."],["DESCRIPTION","**jj abandon** marks revisions as abandoned in Jujutsu. Abandoned commits are hidden from default log output, and any descendants are normally rebased onto the abandoned commit's parent so the visible history remains linear. The change IDs survive until garbage collection, so abandoned work can still be recovered via `jj op log` and `jj op restore`.\n\nIf the working-copy commit is abandoned, Jujutsu automatically creates a new empty working-copy commit on the parent so the workspace is never left without a current change."],["CAVEATS","Abandoning does not delete commits immediately. They remain reachable via the operation log until garbage collected. With **--restore-descendants**, descendants keep their original tree but their parent links are not updated to skip the abandoned commit, which can produce duplicated content."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-squash](/man/jj-squash)(1), [jj-git-push](/man/jj-git-push)(1)"]],"e":["jj","jj-squash","jj-git-push"],"x":"jj abandon"},"ldapmodify":{"n":"ldapmodify","t":"modifies LDAP directory entries","s":[["TAGLINE","modifies LDAP directory entries"],["TLDR","**Modify entry from LDIF**\n\n```ldapmodify -x -D \"[cn=admin,dc=example,dc=com]\" -W -f [changes.ldif]```\n\n**Add entry (like ldapadd)**\n\n```ldapmodify -a -x -D \"[binddn]\" -W -f [entry.ldif]```\n\n**Interactive mode**\n\n```ldapmodify -x -D \"[binddn]\" -W```\n\n**Delete attribute**\n\n```ldapmodify -x -D \"[binddn]\" -W -f [delete.ldif]```\n\n**Using LDAPS**\n\n```ldapmodify -x -H ldaps://[server] -D \"[binddn]\" -W -f [file.ldif]```"],["SYNOPSIS","**ldapmodify** [_options_] [**-f** _file_]"],["PARAMETERS","**-x**\n> Simple authentication.\n\n**-D** _binddn_\n> Bind DN.\n\n**-W**\n> Prompt for password.\n\n**-w** _password_\n> Password (insecure).\n\n**-H** _URI_\n> LDAP server URI.\n\n**-f** _file_\n> LDIF file with modifications.\n\n**-a**\n> Add mode (like ldapadd).\n\n**-c**\n> Continue on errors.\n\n**-M**\n> Enable manage DSA IT control.\n\n**-n**\n> Dry run."],["DESCRIPTION","**ldapmodify** modifies LDAP directory entries. It supports adding, replacing, and deleting attributes based on LDIF input."],["LDIF MODIFICATION TYPES","```ldif"],["Replace attribute","dn: uid=jdoe,ou=users,dc=example,dc=com\nchangetype: modify\nreplace: mail\nmail: newemail@example.com"],["Add attribute","dn: uid=jdoe,ou=users,dc=example,dc=com\nchangetype: modify\nadd: telephoneNumber\ntelephoneNumber: 555-1234"],["Delete attribute","dn: uid=jdoe,ou=users,dc=example,dc=com\nchangetype: modify\ndelete: telephoneNumber\n```"],["CAVEATS","Schema must allow modifications. Use correct changetype. Hyphen (-) separates multiple operations."],["INSTALL","```apt: sudo apt install ldap-utils```\n\n```dnf: sudo dnf install openldap-clients```\n\n```apk: sudo apk add openldap-clients```"],["SEE ALSO","[ldapadd](/man/ldapadd)(1), [ldapsearch](/man/ldapsearch)(1)"]],"e":["ldapadd","ldapsearch"],"x":"ldapmodify -x -D \"[cn=admin,dc=example,dc=com]\" -W -f [changes.ldif]"},"lstopo":{"n":"lstopo","t":"displays hardware topology including CPUs, caches, memory, and I/O devices","s":[["TAGLINE","displays hardware topology including CPUs, caches, memory, and I/O devices"],["TLDR","**Show hardware topology**\n\n```lstopo```\n\n**Display in terminal** (text mode)\n\n```lstopo-no-graphics```\n\n**Output as image**\n\n```lstopo [topology.png]```\n\n**Output as PDF**\n\n```lstopo [topology.pdf]```\n\n**Output as XML**\n\n```lstopo [topology.xml]```\n\n**Show only CPU info**\n\n```lstopo --only core```\n\n**Show with PCI devices**\n\n```lstopo --whole-io```\n\n**Show physical indexes**\n\n```lstopo -p```"],["SYNOPSIS","**lstopo** [_--of format_] [_--only type_] [_--whole-io_] [_options_] [_output_]"],["PARAMETERS","**--of** _FORMAT_\n> Output format (png, pdf, svg, xml, txt, console).\n\n**--only** _TYPE_\n> Show only specific object types.\n\n**--whole-io**\n> Show all I/O devices.\n\n**--no-io**\n> Hide I/O devices.\n\n**-p**, **--physical**\n> Show physical/OS indexes.\n\n**-l**, **--logical**\n> Show logical indexes.\n\n**--merge**\n> Merge identical objects.\n\n**--no-legend**\n> Hide the legend.\n\n**--no-caches**\n> Hide cache information.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**--input** _FILE_\n> Read topology from XML file.\n\n**--version**\n> Print version."],["DESCRIPTION","**lstopo** displays hardware topology including CPUs, caches, memory, and I/O devices. It visualizes the hierarchical structure of the system from machine level down to individual cores and threads.\n\nThe output shows NUMA nodes, packages (sockets), cores, and processing units (threads). Cache levels (L1, L2, L3) are displayed with their sizes and sharing between cores.\n\nGraphical output creates images showing the topology tree. Text mode (lstopo-no-graphics) works in terminals without graphics support.\n\nUnderstanding topology is important for performance optimization. It shows which cores share caches, which are on the same NUMA node, and how memory is organized.\n\nThe tool can load topology from XML files, enabling offline analysis or comparison between systems."],["CAVEATS","Graphical output requires Cairo/X11 libraries. Some virtual environments may show incorrect topology. Root access may be needed for complete I/O device information."],["HISTORY","**lstopo** is part of **hwloc** (Hardware Locality), developed by the **Open MPI** project starting around **2009**. hwloc provides a portable abstraction for hardware topology across different operating systems. lstopo evolved from earlier tools in the project."],["INSTALL","```apt: sudo apt install hwloc```\n\n```dnf: sudo dnf install hwloc```\n\n```pacman: sudo pacman -S hwloc```\n\n```apk: sudo apk add hwloc-tools```\n\n```zypper: sudo zypper install hwloc```\n\n```brew: brew install hwloc```\n\n```nix: nix profile install nixpkgs#hwloc```"],["SEE ALSO","[lscpu](/man/lscpu)(1), [numactl](/man/numactl)(8)"]],"e":["lscpu","numactl"],"x":"lstopo"},"metaflac":{"n":"metaflac","t":"command-line FLAC metadata editor","s":[["TAGLINE","command-line FLAC metadata editor"],["TLDR","**List all metadata**\n\n```metaflac --list [file.flac]```\n\n**Show all tags**\n\n```metaflac --export-tags-to=- [file.flac]```\n\n**Set tag value**\n\n```metaflac --set-tag=\"[ARTIST=Name]\" [file.flac]```\n\n**Remove tag**\n\n```metaflac --remove-tag=\"[COMMENT]\" [file.flac]```\n\n**Import tags from file**\n\n```metaflac --import-tags-from=[tags.txt] [file.flac]```\n\n**Export cover art**\n\n```metaflac --export-picture-to=[cover.jpg] [file.flac]```\n\n**Import cover art**\n\n```metaflac --import-picture-from=[cover.jpg] [file.flac]```"],["SYNOPSIS","**metaflac** [_options_] _files_..."],["PARAMETERS","**--list**\n> List metadata blocks.\n\n**--export-tags-to** _file_\n> Export tags.\n\n**--import-tags-from** _file_\n> Import tags.\n\n**--set-tag** _tag=value_\n> Set tag.\n\n**--remove-tag** _tag_\n> Remove tag.\n\n**--remove-all-tags**\n> Remove all tags.\n\n**--export-picture-to** _file_\n> Export the embedded picture (cover art) to file.\n\n**--import-picture-from** _file_\n> Import a picture into the file (PNG/JPEG; specification syntax allows _TYPE\\|MIME\\|DESC\\|WIDTHxHEIGHTxDEPTH/COLORS\\|FILE_).\n\n**--show-md5sum**\n> Print the unencoded MD5 sum of the audio data.\n\n**--show-min-blocksize** / **--show-max-blocksize** / **--show-sample-rate** / **--show-channels** / **--show-bps** / **--show-total-samples**\n> Print individual STREAMINFO fields useful for scripting.\n\n**--add-replay-gain**\n> Compute and add ReplayGain tags for one or more files (treated as one album when multiple files are passed).\n\n**--remove-replay-gain**\n> Remove ReplayGain tags.\n\n**--block-type** / **--except-block-type** _types_\n> Restrict subsequent operations to specific metadata block types (e.g. _VORBIS\\_COMMENT_, _PICTURE_, _SEEKTABLE_)."],["DESCRIPTION","**metaflac** is the command-line FLAC metadata editor. It can list, add, remove, and modify metadata in FLAC files including Vorbis comments and embedded pictures."],["TAG FORMAT","```"],["tags.txt","ARTIST=Artist Name\nALBUM=Album Name\nTITLE=Track Title\nTRACKNUMBER=01\n```"],["CAVEATS","Only works with FLAC files. Changes are immediate (no undo). Case-insensitive tag names. Picture imports need proper MIME type."],["HISTORY","metaflac is part of the **FLAC** project created by **Josh Coalson** in **2000** as a free lossless audio codec."],["INSTALL","```apt: sudo apt install flac```\n\n```dnf: sudo dnf install flac```\n\n```pacman: sudo pacman -S flac```\n\n```apk: sudo apk add flac```\n\n```zypper: sudo zypper install flac```\n\n```brew: brew install flac```\n\n```nix: nix profile install nixpkgs#flac```"],["SEE ALSO","[flac](/man/flac)(1), [ffmpeg](/man/ffmpeg)(1), [mid3v2](/man/mid3v2)(1)"]],"e":["flac","ffmpeg","mid3v2"],"x":"metaflac --list [file.flac]"},"microsoft-edge":{"n":"microsoft-edge","t":"edge browser for Linux","s":[["TAGLINE","edge browser for Linux"],["TLDR","**Open browser**\n\n```microsoft-edge```\n\n**Open URL**\n\n```microsoft-edge [https://example.com]```\n\n**Open in private mode**\n\n```microsoft-edge --inprivate```\n\n**Open file**\n\n```microsoft-edge [file.html]```\n\n**New window**\n\n```microsoft-edge --new-window [url]```\n\n**Profile directory**\n\n```microsoft-edge --user-data-dir=[/path/to/profile]```"],["SYNOPSIS","**microsoft-edge** [_options_] [_url_]"],["PARAMETERS","_URL_\n> URL to open.\n\n**--inprivate**\n> Private browsing mode.\n\n**--new-window**\n> Open in new window.\n\n**--user-data-dir** _DIR_\n> Profile directory.\n\n**--app** _URL_\n> Open the URL in app mode (no toolbar or address bar).\n\n**--incognito**\n> Alias for **--inprivate** on some builds.\n\n**--proxy-server** _HOST:PORT_\n> Use the specified proxy server for connections.\n\n**--disable-extensions**\n> Start with all extensions disabled.\n\n**--version**\n> Display browser version.\n\n**--help**\n> Display help information."],["DESCRIPTION","**microsoft-edge** is Microsoft Edge browser for Linux. It's based on Chromium.\n\nThe tool provides web browsing with Microsoft services integration. Supports extensions and sync."],["CAVEATS","Chromium-based. Microsoft account for sync. Proprietary software."],["HISTORY","Microsoft Edge for Linux was released in **2020**, bringing the Chromium-based browser to Linux."],["INSTALL","```nix: nix profile install nixpkgs#microsoft-edge```"],["SEE ALSO","[google-chrome](/man/google-chrome)(1), [firefox](/man/firefox)(1), [chromium](/man/chromium)(1)"]],"e":["google-chrome","firefox","chromium"],"x":"microsoft-edge"},"mkfs.vfat":{"n":"mkfs.vfat","t":"Create a FAT filesystem (symbolic link to mkfs.fat)","s":[["TAGLINE","Create a FAT filesystem (symbolic link to mkfs.fat)"],["TLDR","This command is an alias of **mkfs.fat**\n\nCreate a **FAT filesystem** inside partition Y on device X\n\n```sudo mkfs.vfat [/dev/sdXY]```\n\nCreate a FAT filesystem with a **volume name**\n\n```sudo mkfs.vfat -n [volume_name] [/dev/sdXY]```\n\nSpecify the **FAT type** (12, 16, or 32 bit)\n\n```sudo mkfs.vfat -F [12|16|32] [/dev/sdXY]```\n\nCreate a FAT32 filesystem with a **custom cluster size**\n\n```sudo mkfs.vfat -F 32 -s [8] [/dev/sdXY]```\n\nSet the **number of sectors** per cluster\n\n```sudo mkfs.vfat -F 32 -S [512] [/dev/sdXY]```"],["SYNOPSIS","**mkfs.vfat** [_options_] _device_ [_block-count_]"],["DESCRIPTION","**mkfs.vfat** is a symbolic link to **mkfs.fat**. It creates FAT (File Allocation Table) filesystems, commonly used for USB drives, SD cards, and EFI system partitions.\n\nThe \"vfat\" name refers to the Virtual FAT extension that enables long filename support (up to 255 characters) on FAT filesystems while maintaining compatibility with the original 8.3 filename format."],["INSTALL","```apt: sudo apt install dosfstools```\n\n```dnf: sudo dnf install dosfstools```\n\n```pacman: sudo pacman -S dosfstools```\n\n```apk: sudo apk add dosfstools```\n\n```zypper: sudo zypper install dosfstools```\n\n```brew: brew install dosfstools```\n\n```nix: nix profile install nixpkgs#dosfstools```"],["SEE ALSO","[mkfs.fat](/man/mkfs.fat)(8), [mkfs](/man/mkfs)(8), [fsck](/man/fsck)(8), [fatlabel](/man/fatlabel)(8), [mount](/man/mount)(8)"]],"e":["mkfs.fat","mkfs","fsck","fatlabel","mount"],"x":"sudo mkfs.vfat [/dev/sdXY]"},"modprobe":{"n":"modprobe","t":"intelligently adds or removes modules from the Linux kernel","s":[["TAGLINE","intelligently adds or removes modules from the Linux kernel"],["TLDR","**Dry run** loading a module\n\n```sudo modprobe -n module_name```\n\n**Load** a module into kernel\n\n```sudo modprobe module_name```\n\n**Remove** a module from kernel\n\n```sudo modprobe -r module_name```\n\nRemove module and **dependents**\n\n```sudo modprobe -r --remove-holders module_name```\n\nShow module **dependencies**\n\n```sudo modprobe -D module_name```\n\nShow **configuration**\n\n```modprobe -c```"],["SYNOPSIS","**modprobe** [_-v_] [_-n_] [_-r_] [_modulename_] [_parameters_...]"],["DESCRIPTION","**modprobe** intelligently adds or removes modules from the Linux kernel. It handles module dependencies automatically using modules.dep and searches for modules in /lib/modules/$(uname -r)."],["PARAMETERS","**-v, --verbose**\n> Print detailed information about operations\n\n**-n, --dry-run**\n> Preview actions without executing them\n\n**-r, --remove**\n> Remove module instead of inserting\n\n**-f, --force**\n> Force loading despite version mismatch\n\n**-q, --quiet**\n> Suppress errors for missing modules\n\n**-b, --use-blacklist**\n> Apply blacklist rules from configuration\n\n**-c, --showconfig**\n> Display effective configuration\n\n**-D, --show-depends**\n> List module dependencies\n\n**--first-time**\n> Fail if module already loaded/not loaded\n\n**-i, --ignore-install**\n> Ignore install commands in configuration\n\n**-w, --wait=TIMEOUT**\n> Wait and retry if module is busy (on removal)\n\n**-S, --set-version**\n> Use specified kernel version\n\n**--remove-holders**\n> Also remove dependent modules (with -r)"],["CONFIGURATION","Configuration files in /etc/modprobe.d/ control module behavior including aliases, options, blacklists, and install/remove commands."],["CAVEATS","Module names treat underscores and dashes interchangeably. Some modules require parameters for proper operation. Blacklisted modules can still be loaded explicitly."],["HISTORY","**modprobe** is part of **kmod**, replacing the older module-init-tools. It provides intelligent module management with automatic dependency resolution."],["INSTALL","```apt: sudo apt install kmod```\n\n```dnf: sudo dnf install kmod```\n\n```pacman: sudo pacman -S kmod```\n\n```apk: sudo apk add kmod```\n\n```zypper: sudo zypper install kmod```\n\n```brew: brew install kmod```\n\n```nix: nix profile install nixpkgs#kmod```"],["SEE ALSO","[lsmod](/man/lsmod)(8), [rmmod](/man/rmmod)(8), [insmod](/man/insmod)(8), [modinfo](/man/modinfo)(8), [depmod](/man/depmod)(8)"]],"e":["lsmod","rmmod","insmod","modinfo","depmod"],"x":"sudo modprobe -n module_name"},"mqtt-sn-sub":{"n":"mqtt-sn-sub","t":"Subscribe to MQTT-SN topics from the command line","s":[["TAGLINE","Subscribe to MQTT-SN topics from the command line"],["TLDR","**Subscribe** to a topic\n\n```mqtt-sn-sub -t [topic/name]```\n\n**Subscribe** with verbose topic names\n\n```mqtt-sn-sub -t [sensors/#] -v```\n\n**Subscribe** and exit after one message\n\n```mqtt-sn-sub -t [topic] -1```\n\n**Remote gateway**\n\n```mqtt-sn-sub -h [192.168.1.10] -p [1883] -t [topic]```\n\n**QoS 1** subscription\n\n```mqtt-sn-sub -t [topic] -q [1]```\n\n**Verbose with timestamps**\n\n```mqtt-sn-sub -t [topic] -V```"],["SYNOPSIS","**mqtt-sn-sub** [*opts*] **-t** *topic*"],["DESCRIPTION","**mqtt-sn-sub** is the subscriber from **mqtt-sn-tools**, a C toolkit for MQTT-SN (MQTT for Sensor Networks). It connects to an MQTT-SN gateway over UDP and prints received messages. Topic arguments may be repeated; pre-defined topic IDs are supported with **-T**.\n\nBuild with **make** from the upstream repository. Companion tools include **mqtt-sn-pub**, **mqtt-sn-dump**, and **mqtt-sn-serial-bridge**."],["PARAMETERS","**-t** *topic*\n\n> Topic to subscribe to (repeatable). May include MQTT-SN wildcards where supported by the gateway.\n\n**-T** *topicid*\n\n> Pre-defined topic ID to subscribe to (repeatable).\n\n**-1**\n\n> Exit after receiving a single message.\n\n**-c**\n\n> Disable clean session (store subscription/pending messages across disconnect).\n\n**-h** *host* / **-p** *port*\n\n> Gateway host (default **127.0.0.1**) and port (default **1883**).\n\n**-q** *qos*\n\n> Subscribe QoS **0** or **1** (default 0).\n\n**-k** *seconds*\n\n> Keepalive (default 10).\n\n**-i** *clientid*\n\n> Client ID (default **mqtt-sn-tools-** plus pid).\n\n**-v** / **-V**\n\n> Verbose output: topic name, or time plus topic name.\n\n**-d**\n\n> Increase debug level (repeatable).\n\n**--fe** / **--wlnid** / **--cport**\n\n> Forwarder encapsulation, wireless node id, and source port options."],["CAVEATS","Requires an MQTT-SN gateway, not a plain MQTT broker. Packet size limit 255 bytes; no QoS 2. Wildcard behaviour depends on gateway support."],["SEE ALSO","[mqtt-sn-pub](/man/mqtt-sn-pub)(1), [mqtt-sn-dump](/man/mqtt-sn-dump)(1), [mosquitto_sub](/man/mosquitto_sub)(1)"],["RESOURCES","```[Source code](https://github.com/njh/mqtt-sn-tools)```"]],"e":["mqtt-sn-pub","mqtt-sn-dump","mosquitto_sub"],"x":"mqtt-sn-sub -t [topic/name]"},"mumble":{"n":"mumble","t":"open-source voice chat application","s":[["TAGLINE","open-source voice chat application"],["TLDR","**Start Mumble**\n\n```mumble```\n\n**Connect to server**\n\n```mumble [mumble://server.example.com]```\n\n**Connect with username**\n\n```mumble [mumble://user@server.example.com]```\n\n**Connect to specific port**\n\n```mumble [mumble://server.example.com:64738]```\n\n**Start minimized**\n\n```mumble --minimized```"],["SYNOPSIS","**mumble** [_options_] [_url_]"],["PARAMETERS","_URL_\n> Mumble server URL.\n\n**--minimized**\n> Start minimized to tray.\n\n**--nooverlay**\n> Disable overlay.\n\n**--nospeech**\n> Disable text-to-speech.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mumble** is an open-source voice chat application. It provides low-latency voice communication.\n\nThe tool is designed for gaming. Features positional audio and low latency."],["CAVEATS","Requires Mumble server (murmur). Needs audio setup. Gaming-focused but general use."],["HISTORY","Mumble was created in **2005** as an open-source alternative to proprietary voice chat for gamers."],["INSTALL","```apt: sudo apt install mumble```\n\n```dnf: sudo dnf install mumble```\n\n```pacman: sudo pacman -S mumble```\n\n```apk: sudo apk add mumble```\n\n```zypper: sudo zypper install mumble```\n\n```nix: nix profile install nixpkgs#mumble```"],["SEE ALSO","[discord](/man/discord)(1), [signal-desktop](/man/signal-desktop)(1)"]],"e":["discord","signal-desktop"],"x":"mumble"},"netmiko":{"n":"netmiko","t":"multi-vendor SSH library for Python that simplifies CLI interactions","s":[["TAGLINE","multi-vendor SSH library for Python that simplifies CLI interactions"],["TLDR","**Connect and send command (Python)**\n\n```from netmiko import ConnectHandler; net_connect = ConnectHandler(**device); print(net_connect.send_command(\"show ip int brief\"))```\n\n**Send config commands**\n\n```net_connect.send_config_set([\"interface gi0/1\", \"description WAN\"])```\n\n**Save configuration**\n\n```net_connect.save_config()```\n\n**Enable mode**\n\n```net_connect.enable()```"],["SYNOPSIS","**netmiko** - Python library for SSH to network devices"],["DESCRIPTION","**Netmiko** is a multi-vendor SSH library for Python that simplifies CLI interactions with network devices. It handles connection setup, privilege escalation, and output parsing.\n\nNetmiko supports Cisco, Juniper, Arista, HP, and many other vendors."],["EXAMPLE","```python\nfrom netmiko import ConnectHandler\n\ndevice = {\n    'device_type': 'cisco_ios',\n    'host': '192.168.1.1',\n    'username': 'admin',\n    'password': 'password',\n}\n\nnet_connect = ConnectHandler(**device)\noutput = net_connect.send_command(\"show version\")\nnet_connect.disconnect()\n```"],["SUPPORTED PLATFORMS","```\ncisco_ios, cisco_xe, cisco_nxos\njuniper_junos, arista_eos\nhp_procurve, fortinet\nlinux, generic_termserver\n```"],["CAVEATS","SSH must be enabled on devices. Some devices need special handling. Blocking operations by default."],["HISTORY","Netmiko was created by **Kirk Byers** in **2014** to simplify Python-based network automation over SSH."],["SEE ALSO","[napalm](/man/napalm)(1), [ansible](/man/ansible)(1), [ssh](/man/ssh)(1)"]],"e":["napalm","ansible","ssh"],"x":"from netmiko import ConnectHandler; net_connect = ConnectHandler(**device); print(net_connect.send_command(\"show ip int brief\"))"},"nix-why-depends":{"n":"nix-why-depends","t":"shows why a package depends on another","s":[["TAGLINE","shows why a package depends on another"],["TLDR","**Show dependency path**\n\n```nix why-depends nixpkgs#[hello] nixpkgs#[glibc]```\n\n**Show all paths**\n\n```nix why-depends --all nixpkgs#[package] nixpkgs#[dep]```\n\n**From derivation files**\n\n```nix why-depends [/nix/store/...package] [/nix/store/...dep]```"],["SYNOPSIS","**nix** **why-depends** [_options_] _package_ _dependency_"],["PARAMETERS","_PACKAGE_\n> Package to analyze.\n\n_DEPENDENCY_\n> Dependency to find.\n\n**--all**\n> Show all dependency paths.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nix why-depends** shows why a package depends on another. Traces dependency paths.\n\nThe tool debugs closures. Helps understand and reduce dependencies."],["CAVEATS","Part of Nix CLI. Requires evaluation. Closure analysis."],["HISTORY","nix why-depends is part of the **new Nix CLI** for debugging dependency relationships."],["SEE ALSO","[nix](/man/nix)(1), [nix-store](/man/nix-store)(1), [nix-build](/man/nix-build)(1)"]],"e":["nix","nix-store","nix-build"],"x":"nix why-depends nixpkgs#[hello] nixpkgs#[glibc]"},"nokogiri":{"n":"nokogiri","t":"ruby HTML/XML parser CLI","s":[["TAGLINE","ruby HTML/XML parser CLI"],["TLDR","**Parse HTML file**\n\n```nokogiri [file.html]```\n\n**Fetch and parse URL**\n\n```nokogiri [https://example.com]```\n\n**Parse with CSS selector**\n\n```nokogiri [file.html] -e \"[doc.css('h1').text]\"```\n\n**Parse XML**\n\n```nokogiri [file.xml] --type xml```\n\n**Drop into an IRB session** with the document bound to `doc`\n\n```nokogiri [file.html]```\n\n**Validate against a RelaxNG schema**\n\n```nokogiri [file.xml] --rng [schema.rng]```"],["SYNOPSIS","**nokogiri** [_options_] [_file_or_url_]"],["PARAMETERS","_FILE_OR_URL_\n> HTML/XML file path or URL to parse. If absent, the document is read from stdin.\n\n**-e** _CODE_\n> Execute Ruby _CODE_ against the parsed document (which is bound to **doc**).\n\n**--type** _TYPE_\n> Document type: **xml** or **html**. Defaults to autodetection by content type / extension.\n\n**-C** _FILE_\n> Load a custom Ruby initialization file. Default: **~/.nokogirirc**.\n\n**-E**, **--encoding** _ENCODING_\n> Read input using the named character encoding (e.g. UTF-8, ISO-8859-1).\n\n**--rng** _URI_OR_PATH_\n> Validate the document against the given RelaxNG schema.\n\n**-v**, **--version**\n> Show the Nokogiri version.\n\n**-?**, **--help**\n> Display help."],["DESCRIPTION","**nokogiri** is the command-line front-end for the Nokogiri Ruby gem, a fast HTML/XML parser backed by libxml2 and libxslt. The CLI parses a file, URL, or stdin into a **Nokogiri::HTML::Document** or **Nokogiri::XML::Document** (bound as **doc**) and either drops you into an IRB session or runs the Ruby snippet supplied with **-e** so you can query it with CSS selectors (**doc.css**) or XPath (**doc.xpath**)."],["CAVEATS","Requires Ruby and the **nokogiri** gem (`gem install nokogiri`). The **-i** interactive flag is not part of the modern CLI — running **nokogiri** _file_ on a TTY drops into IRB by default; pass **-e** to run non-interactively. Fetching URLs uses **open-uri**, so HTTPS sites need OpenSSL support in the underlying Ruby build."],["HISTORY","**Nokogiri** (Japanese for \"saw\") was created by **Aaron Patterson** and **Mike Dalessio** in **2008** as a faster, libxml2-backed alternative to Hpricot. It is one of the most-installed Ruby gems and ships a small CLI for ad-hoc parsing and validation."],["INSTALL","```apt: sudo apt install ruby-nokogiri```\n\n```pacman: sudo pacman -S ruby-nokogiri```"],["SEE ALSO","[xmllint](/man/xmllint)(1), [pup](/man/pup)(1), [xidel](/man/xidel)(1)"]],"e":["xmllint","pup","xidel"],"x":"nokogiri [file.html]"},"noping":{"n":"noping","t":"Ncurses-based visual ping tool for multiple hosts","s":[["TAGLINE","Ncurses-based visual ping tool for multiple hosts"],["TLDR","**Ping multiple hosts** with visual display\n\n```noping [host1] [host2] [host3]```\n\n**Ping with a specific interval** (in seconds, supports fractions)\n\n```noping -i [seconds] [host]```\n\n**Send a specific number of pings** then exit\n\n```noping -c [count] [host]```\n\n**Force IPv4 or IPv6**\n\n```noping -4 [host]```\n\n**Read hosts from a file**\n\n```noping -f [hosts.txt]```\n\n**Write measurements to CSV file**\n\n```noping -O [output.csv] [host1] [host2]```\n\n**Select graph type** (none, prettyping, boxplot, histogram)\n\n```noping -g [prettyping] [host]```"],["SYNOPSIS","**noping** [_-4_ | _-6_] [_-c count_] [_-i interval_] _host_ [_host_...]\n**noping** [_-4_ | _-6_] [_-c count_] [_-i interval_] **-f** _filename_"],["PARAMETERS","**-4**\n> Force the use of IPv4.\n\n**-6**\n> Force the use of IPv6.\n\n**-c** _COUNT_\n> Send and receive COUNT ICMP packets, then stop and exit.\n\n**-i** _INTERVAL_\n> Send one ICMP packet per host each INTERVAL seconds. Supports floating-point for sub-second precision.\n\n**-w** _TIMEOUT_\n> Time to wait for an ECHO REPLY before giving up, in seconds. Defaults to 1.0.\n\n**-t** _TTL_\n> Set IP Time to Live (1-255). Defaults to 64.\n\n**-I** _ADDRESS_\n> Set the source address (IP or hostname).\n\n**-D** _DEVICE_\n> Set the outgoing network device.\n\n**-f** _FILENAME_\n> Read hostnames from FILENAME instead of the command line. Use - for stdin.\n\n**-O** _FILENAME_\n> Write measurements in CSV format to FILENAME.\n\n**-g** _TYPE_\n> Select graph type: none, prettyping, boxplot, or histogram.\n\n**-Q** _QOS_\n> Specify Quality of Service for outgoing packets.\n\n**-b**\n> Audible bell; print ASCII BEL when a packet is received.\n\n**-P** _PERCENT_\n> Configure the latency percentile to report (0-100, exclusive).\n\n**-Z** _PERCENT_\n> Exit with non-zero status if any host's drop rate exceeds this percentage.\n\n**-u**\n> Force UTF-8 output.\n\n**-U**\n> Disable UTF-8 output."],["DESCRIPTION","**noping** is an ncurses-based front-end to liboping that pings multiple hosts in parallel using IPv4 and/or IPv6 and displays statistics live in the terminal. Round-trip times are color-coded: green for normal, yellow for moderately unusual, and red for significantly aberrant values.\n\nMultiple graph types are available and can be toggled with **g**. New hosts can be added at any time with **a**. Display modes include a time-based graph and a box plot showing RTT distribution."],["CAVEATS","May require root privileges or CAP_NET_RAW capability for ICMP access."],["HISTORY","**noping** is part of the **liboping** project by **Florian Forster** (octo) and is written in **C**."],["INSTALL","```apt: sudo apt install oping```\n\n```dnf: sudo dnf install liboping```\n\n```apk: sudo apk add liboping```\n\n```zypper: sudo zypper install oping```\n\n```brew: brew install liboping```\n\n```nix: nix profile install nixpkgs#liboping```"],["SEE ALSO","[ping](/man/ping)(8), [mtr](/man/mtr)(8), [fping](/man/fping)(8), [traceroute](/man/traceroute)(8)"]],"e":["ping","mtr","fping","traceroute"],"x":"noping [host1] [host2] [host3]"},"npm-stars":{"n":"npm-stars","t":"displays packages starred by a user","s":[["TAGLINE","displays packages starred by a user"],["TLDR","**List starred packages**\n\n```npm stars```\n\n**List user's stars**\n\n```npm stars [username]```"],["SYNOPSIS","**npm** **stars** [_user_]"],["PARAMETERS","**--registry** _URL_\n> npm registry URL (default: https://registry.npmjs.org/)."],["DESCRIPTION","**npm stars** displays packages starred by a user. Shows your starred packages by default. View another user's starred packages by providing username."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-star](/man/npm-star)(1), [npm-unstar](/man/npm-unstar)(1)"]],"e":["npm","npm-star","npm-unstar"],"x":"npm stars"},"ogrinfo":{"n":"ogrinfo","t":"displays vector data information","s":[["TAGLINE","displays vector data information"],["TLDR","**List layers and summary of a data source**\n\n```ogrinfo [file.shp]```\n\n**Summary of a specific layer only (no features)**\n\n```ogrinfo -so [file.shp] [layer_name]```\n\n**Dump all layers and all features**\n\n```ogrinfo -al [file.shp]```\n\n**Show a single feature by FID**\n\n```ogrinfo [file.shp] [layer_name] -fid [feature_id]```\n\n**Filter features with a WHERE clause**\n\n```ogrinfo -al -where \"[field = 'value']\" [file.shp]```\n\n**Run an SQL query**\n\n```ogrinfo [file.shp] -sql \"[SELECT * FROM layer]\"```\n\n**Output as JSON**\n\n```ogrinfo -json [file.shp]```"],["SYNOPSIS","**ogrinfo** [_options_] _datasource_ [_layer_ ...]"],["PARAMETERS","_DATASOURCE_\n> OGR-supported vector data source (file, directory, connection string, URL).\n\n_LAYER_\n> One or more layer names to report on. If omitted with **-al**, all layers are reported.\n\n**-al**\n> List all features of all layers (opposite of **-so**).\n\n**-so**\n> Summary only: omit feature-by-feature reporting.\n\n**-fl**\n> Enable listing of features (opposite of **-so**).\n\n**-json**\n> Output the report in JSON format.\n\n**-fid** _ID_\n> Only report the feature with the given FID.\n\n**-sql** _STATEMENT_\n> Execute an SQL statement and return the result. Can also be a @filename reference.\n\n**-dialect** _DIALECT_\n> SQL dialect to use (e.g., OGRSQL, SQLITE).\n\n**-where** _QUERY_\n> Attribute filter applied to each layer.\n\n**-spat** _xmin_ _ymin_ _xmax_ _ymax_\n> Spatial filter: only features intersecting this bounding box are reported.\n\n**-geom** _{YES|NO|SUMMARY|WKT|ISO_WKT}_\n> How geometry is reported (default: YES).\n\n**-limit** _N_\n> Limit the number of features per layer.\n\n**-if** _FORMAT_\n> Force a specific driver when opening the input.\n\n**-oo** _NAME=VALUE_\n> Pass a dataset open option to the driver.\n\n**-ro**\n> Open the data source in read-only mode.\n\n**-nomd**\n> Suppress metadata printing.\n\n**-listmdd**\n> List all metadata domains available for the dataset.\n\n**-mdd** _domain_\n> Report metadata for the specified domain.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ogrinfo** lists information about an OGR-supported vector data source to stdout. It reports layers, schema, coordinate reference system, extents, feature counts, and optionally the features themselves. By executing SQL statements via **-sql** it can also edit data in supported drivers."],["CAVEATS","Part of the GDAL suite. Format support depends on which OGR drivers were compiled into your GDAL build; check with `ogrinfo --formats`. Using **-al** on very large datasets can produce huge output."],["HISTORY","**ogrinfo** is distributed with **GDAL/OGR**, the open-source geospatial data library maintained by the **OSGeo** foundation. OGR originated as a separate library for vector data and was unified into GDAL in version 2.0."],["INSTALL","```apt: sudo apt install gdal-bin```\n\n```apk: sudo apk add gdal-tools```"],["SEE ALSO","[ogr2ogr](/man/ogr2ogr)(1), [gdalinfo](/man/gdalinfo)(1)"]],"e":["ogr2ogr","gdalinfo"],"x":"ogrinfo [file.shp]"},"openssl-prime":{"n":"openssl-prime","t":"tests numbers for primality or generates prime numbers","s":[["TAGLINE","tests numbers for primality or generates prime numbers"],["TLDR","**Check if number is prime**\n\n```openssl prime [number]```\n\n**Check hex number**\n\n```openssl prime -hex [hex-number]```\n\n**Generate random prime**\n\n```openssl prime -generate -bits [256]```\n\n**Generate safe prime**\n\n```openssl prime -generate -bits [256] -safe```"],["SYNOPSIS","**openssl** **prime** [_options_] [_number_]"],["PARAMETERS","**-hex**\n> Input/output in hexadecimal.\n\n**-generate**\n> Generate a prime number.\n\n**-bits** _n_\n> Number of bits for generation.\n\n**-safe**\n> Generate safe prime (p where (p-1)/2 is also prime).\n\n**-checks** _n_\n> Number of Miller-Rabin tests."],["DESCRIPTION","**openssl prime** tests numbers for primality or generates prime numbers. Uses Miller-Rabin primality testing. Useful for cryptographic applications requiring prime numbers."],["INSTALL","```apt: sudo apt install openssl```\n\n```dnf: sudo dnf install openssl```\n\n```pacman: sudo pacman -S openssl```\n\n```apk: sudo apk add openssl```\n\n```zypper: sudo zypper install openssl```\n\n```brew: brew install openssl```\n\n```nix: nix profile install nixpkgs#openssl```"],["SEE ALSO","[openssl](/man/openssl)(1)"]],"e":["openssl"],"x":"openssl prime [number]"},"orca-c":{"n":"orca-c","t":"livecoding environment","s":[["TAGLINE","livecoding environment"],["TLDR","**Start Orca**\n\n```orca-c```\n\n**Open file**\n\n```orca-c [file.orca]```\n\n**Set BPM**\n\n```orca-c --bpm [120] [file.orca]```\n\n**Set MIDI output**\n\n```orca-c --portmidi-list```"],["SYNOPSIS","**orca-c** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Orca file to open.\n\n**--bpm** _BPM_\n> Beats per minute.\n\n**--portmidi-list**\n> List MIDI devices.\n\n**--help**\n> Display help information."],["DESCRIPTION","**orca-c** is Orca livecoding environment. Creates music through visual programming.\n\nThe tool provides a grid-based sequencer. Generates MIDI output."],["CAVEATS","Terminal-based interface. Requires MIDI setup. Unique programming paradigm."],["HISTORY","Orca was created by **Hundred Rabbits** as an esoteric livecoding environment."],["INSTALL","```nix: nix profile install nixpkgs#orca-c```"],["SEE ALSO","[supercollider](/man/supercollider)(1)"]],"e":["supercollider"],"x":"orca-c"},"pambrighten":{"n":"pambrighten","t":"adjusts brightness and saturation of Netpbm images using HSV color model","s":[["TAGLINE","adjusts brightness and saturation of Netpbm images using HSV color model"],["TLDR","**Increase image brightness by 50%**\n\n```pambrighten -value 50 [input.pam] > [output.pam]```\n\n**Decrease saturation by 30%**\n\n```pambrighten -saturation -30 [input.pam] > [output.pam]```\n\n**Adjust both brightness and saturation**\n\n```pambrighten -value [20] -saturation [10] [input.pam] > [output.pam]```\n\n**Read from stdin**\n\n```cat [input.pam] | pambrighten -value [50] > [output.pam]```"],["SYNOPSIS","**pambrighten** [**-value** _n_] [**-saturation** _n_] [_file_]"],["PARAMETERS","**-value** _n_\n> Change Value (brightness) by percentage. May be negative. Default is 0.\n\n**-saturation** _n_\n> Change Saturation by percentage. May be negative. Default is 0."],["DESCRIPTION","**pambrighten** increases or decreases the Saturation and Value (from the HSV color space) of each pixel of a Netpbm image by a specified percentage. Positive values increase, negative values decrease. If no file is specified, reads from stdin.\n\nMinimum unique abbreviation of option names is acceptable. Double hyphens may be used instead of single hyphens. Part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamfunc](/man/pamfunc)(1), [ppmbrighten](/man/ppmbrighten)(1)"]],"e":["pamfunc","ppmbrighten"],"x":"pambrighten -value 50 [input.pam] > [output.pam]"},"pamtopng":{"n":"pamtopng","t":"Convert PAM images to PNG format","s":[["TAGLINE","Convert PAM images to PNG format"],["TLDR","**Convert PAM to PNG**\n\n```pamtopng [input.pam] > [output.png]```\n\n**Convert with maximum compression**\n\n```pamtopng -compression [9] [input.pam] > [output.png]```\n\n**Create interlaced PNG**\n\n```pamtopng -interlace [input.pam] > [output.png]```\n\n**Convert with transparency**\n\n```pamtopng -transparent [white] [input.pam] > [output.png]```"],["SYNOPSIS","**pamtopng** [_options_] [_file_]"],["PARAMETERS","**-compression** _n_\n> Compression level (0-9, default: 6).\n\n**-interlace**\n> Create interlaced (Adam7) PNG for progressive loading.\n\n**-transparent** _color_\n> Set fully transparent color.\n\n**-background** _color_\n> Background color for alpha blending.\n\n**-gamma** _value_\n> Gamma value for the image.\n\n**-srgbintent** _intent_\n> sRGB rendering intent: absolute, relative, perceptual, or saturation.\n\n**-text** _file_\n> Add tEXt chunks from a text file.\n\n**-verbose**\n> Print detailed progress information to standard error."],["DESCRIPTION","**pamtopng** converts Netpbm PAM images to PNG format. It is the preferred tool over **pnmtopng** for images with alpha channels, as it properly handles PAM's native alpha support. Part of the Netpbm toolkit.\n\nInput is read from the specified file or standard input. Output is written to standard output."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pngtopam](/man/pngtopam)(1), [pnmtopng](/man/pnmtopng)(1)"]],"e":["pngtopam","pnmtopng"],"x":"pamtopng [input.pam] > [output.png]"},"pbmtoybm":{"n":"pbmtoybm","t":"converts PBM bitmaps to Bennet Yee's \"face\" format used by the YBM face","s":[["TAGLINE","converts PBM bitmaps to Bennet Yee's \"face\" format used by the YBM face"],["TLDR","**Convert PBM to Bennet Yee \"face\" format**\n\n```pbmtoybm [input.pbm] > [output.ybm]```"],["SYNOPSIS","**pbmtoybm** [_file_]"],["DESCRIPTION","**pbmtoybm** converts PBM bitmaps to Bennet Yee's \"face\" format used by the YBM face display program. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ybmtopbm](/man/ybmtopbm)(1), [pbmtoicon](/man/pbmtoicon)(1)"]],"e":["ybmtopbm","pbmtoicon"],"x":"pbmtoybm [input.pbm] > [output.ybm]"},"pgmkernel":{"n":"pgmkernel","t":"generates convolution kernels as PGM images for use with pnmconvol","s":[["TAGLINE","generates convolution kernels as PGM images for use with pnmconvol"],["TLDR","**Generate convolution kernel**\n\n```pgmkernel [7] [7] > [kernel.pgm]```\n\n**Generate with weight**\n\n```pgmkernel -weight [6.0] [5] [5] > [kernel.pgm]```"],["SYNOPSIS","**pgmkernel** [_options_] _width_ _height_"],["PARAMETERS","_width_\n> Kernel width.\n\n_height_\n> Kernel height.\n\n**-weight** _n_\n> Center pixel weight."],["DESCRIPTION","**pgmkernel** generates convolution kernels as PGM images for use with pnmconvol. Creates Gaussian-like weighting patterns. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmconvol](/man/pnmconvol)(1), [pgmtexture](/man/pgmtexture)(1)"]],"e":["pnmconvol","pgmtexture"],"x":"pgmkernel [7] [7] > [kernel.pgm]"},"pipes.sh":{"n":"pipes.sh","t":"Animated pipes terminal screensaver","s":[["TAGLINE","Animated pipes terminal screensaver"],["TLDR","**Run pipes animation**\n\n```pipes.sh```\n\n**Set pipe count**\n\n```pipes.sh -p [5]```\n\n**Set frame rate** (default: 75)\n\n```pipes.sh -f [75]```\n\n**Set pipe colors** (0-7, multiple allowed)\n\n```pipes.sh -c 1 2 3```\n\n**Set pipe type** (0=heavy, 1=arc, 2=light, 3=double, 4=ASCII)\n\n```pipes.sh -t [0]```\n\n**No bold colors**\n\n```pipes.sh -B```\n\n**Reset screen**\n\n```pipes.sh -r [2000]```"],["SYNOPSIS","**pipes.sh** [_options_...]"],["PARAMETERS","**-p** _N_\n> Number of pipes (default: 1).\n\n**-f** _FPS_\n> Frame rate (default: 75).\n\n**-c** _COLOR_\n> Colors of pipes as space-separated values 0-7 (default: 1 2 3 4 5 6 7 0).\n\n**-t** _TYPE_\n> Pipe type: 0=heavy, 1=arc, 2=light, 3=double, 4=ASCII box, 5=slashes, 6=dots, 7=circles, 8=railway, 9=knobby.\n\n**-s** _N_\n> Probability of straight fitting, 5-15 (default: 13).\n\n**-r** _LIMIT_\n> Reset drawing after LIMIT characters (default: 2000).\n\n**-R**\n> Randomize starting position and direction.\n\n**-B**\n> Disable bold effect.\n\n**-C**\n> Disable color.\n\n**-K**\n> No change of colors and types when hitting screen edges.\n\n**-v**\n> Print version number.\n\n**-h**\n> Show help."],["DESCRIPTION","**pipes.sh** displays animated pipes growing across the terminal as a screensaver-style animation. Pipes start from screen edges, extend in random directions, and turn at random intervals, creating increasingly complex patterns.\n\nMultiple pipes can run simultaneously, each in a different color. Various character sets provide different visual styles, from simple ASCII box-drawing to Unicode block characters. Color modes range from monochrome to full rainbow cycling.\n\nThe frame rate flag controls animation smoothness and CPU usage. A periodic reset clears the screen to prevent visual clutter from accumulated pipe trails."],["CAVEATS","CPU usage increases with pipe count. Terminal must support ANSI colors. Best in large terminals."],["HISTORY","**pipes.sh** is a bash implementation of the classic \"pipes\" screensaver effect. It has become a popular terminal entertainment and desktop decoration script."],["INSTALL","```aur: yay -S pipes.sh```"],["SEE ALSO","[cmatrix](/man/cmatrix)(1), [sl](/man/sl)(1), [asciiquarium](/man/asciiquarium)(1)"]],"e":["cmatrix","sl","asciiquarium"],"x":"pipes.sh"},"ps2epsi":{"n":"ps2epsi","t":"Convert PostScript to Encapsulated PostScript","s":[["TAGLINE","Convert PostScript to Encapsulated PostScript"],["TLDR","**Convert PS to EPSI**\n\n```ps2epsi [input.ps] [output.epsi]```\n\n**Convert with preview**\n\n```ps2epsi [input.ps]```"],["SYNOPSIS","**ps2epsi** _input_ [_output_]"],["PARAMETERS","_INPUT_\n> Input PostScript file.\n\n_OUTPUT_\n> Output EPSI file."],["DESCRIPTION","**ps2epsi** converts PostScript files to Encapsulated PostScript Interchange (EPSI) format by adding an ASCII preview image header. The EPSI format embeds a low-resolution bitmap preview that applications can display without a PostScript interpreter, making it useful for document layout programs that need to show a visual placeholder.\n\nThe tool is part of the Ghostscript suite and uses Ghostscript internally to render the preview image. If no output filename is given, the output file is named after the input with an **.epsi** extension."],["CAVEATS","Part of Ghostscript. Creates bitmap preview."],["HISTORY","ps2epsi is part of **Ghostscript** for EPSI conversion."],["INSTALL","```apt: sudo apt install ghostscript```\n\n```dnf: sudo dnf install ghostscript```\n\n```pacman: sudo pacman -S ghostscript```\n\n```apk: sudo apk add ghostscript```\n\n```zypper: sudo zypper install ghostscript```\n\n```brew: brew install ghostscript```\n\n```nix: nix profile install nixpkgs#ghostscript```"],["SEE ALSO","[gs](/man/gs)(1), [ps2pdf](/man/ps2pdf)(1), [ps2ps](/man/ps2ps)(1)"]],"e":["gs","ps2pdf","ps2ps"],"x":"ps2epsi [input.ps] [output.epsi]"},"pvenode-config":{"n":"pvenode-config","t":"get or set Proxmox VE node configuration options","s":[["TAGLINE","get or set Proxmox VE node configuration options"],["TLDR","**Get** all node configuration options\n\n```pvenode config get```\n\nGet a **specific property**\n\n```pvenode config get --property description```\n\nSet the node **description** (notes panel)\n\n```pvenode config set --description \"primary cluster node\"```\n\nConfigure **ACME** domains for TLS certificates\n\n```pvenode config set --acme domains=pve.example.com```\n\nSet **Wake-on-LAN** MAC address\n\n```pvenode config set --wakeonlan XX:XX:XX:XX:XX:XX```\n\nSet first guest **boot delay** in seconds\n\n```pvenode config set --startall-onboot-delay 10```\n\nSet RAM **ballooning target** percent\n\n```pvenode config set --ballooning-target 90```"],["SYNOPSIS","**pvenode config get** [_OPTIONS_]\n\n**pvenode config set** [_OPTIONS_]"],["PARAMETERS","**--property** _name_\n> (get) Return only a specific property: acme, acmedomain0–5, ballooning-target, description, location, startall-onboot-delay, wakeonlan (default: all)\n\n**--description** _text_\n> (set) Node description shown in the web UI notes panel\n\n**--acme** _settings_\n> (set) Node-specific ACME settings (account, domains)\n\n**--acmedomain**_n_ _settings_\n> (set) ACME domain and validation plugin for domain index n\n\n**--ballooning-target** _percent_\n> (set) RAM usage target for ballooning, 0–100 (default 80)\n\n**--startall-onboot-delay** _seconds_\n> (set) Delay before starting on-boot guests after host boot, 0–300 (default 0)\n\n**--wakeonlan** _settings_\n> (set) Wake-on-LAN MAC and optional bind interface / broadcast address\n\n**--location** _settings_\n> (set) Node location (latitude, longitude, optional name)\n\n**--delete** _list_\n> (set) Settings to remove\n\n**--digest** _hash_\n> (set) Prevent concurrent modifications if config digest differs"],["DESCRIPTION","**pvenode config** reads and writes node-level configuration for a Proxmox VE host. Settings include notes/description, ACME certificate domains, Wake-on-LAN, automatic guest start delay, memory ballooning target, and geographic location metadata.\n\nThese options affect the local node (not cluster-wide datacenter config). Certificate-related ACME domain settings work together with **pvenode acme** commands to order and renew TLS certificates used by the API and web UI."],["CAVEATS","Changing ACME domains does not by itself issue a certificate; you still need **pvenode acme cert order** (and typically a **pveproxy** restart). Concurrent edits can race; use **--digest** when scripting. Incorrect Wake-on-LAN settings will prevent remote power-on of sleeping nodes."],["HISTORY","**pvenode config** is part of the **Proxmox VE** **pvenode** node management tool."],["SEE ALSO","[pvesubscription](/man/pvesubscription)(1), [pveversion](/man/pveversion)(1), [pvesh](/man/pvesh)(1)"],["RESOURCES","```[Documentation](https://pve.proxmox.com/pve-docs/pvenode.1.html)```\n\n```[Homepage](https://www.proxmox.com)```"]],"e":["pvesubscription","pveversion","pvesh"],"x":"pvenode config get"},"pw-link":{"n":"pw-link","t":"Create and manage PipeWire port connections","s":[["TAGLINE","Create and manage PipeWire port connections"],["TLDR","List **input and output** ports with IDs\n\n```pw-link -oiI```\n\n**Create** link between ports\n\n```pw-link [output_port] [input_port]```\n\n**Disconnect** two ports\n\n```pw-link -d [output_port] [input_port]```\n\nList all **links** with IDs\n\n```pw-link -lI```\n\nDisplay **help**\n\n```pw-link -h```"],["SYNOPSIS","**pw-link** [**-o**] [**-i**] [**-l**] [**-I**] [**-d**] [_output_port_ _input_port_]"],["PARAMETERS","**-o, --output**\n> List output ports\n\n**-i, --input**\n> List input ports\n\n**-l, --links**\n> List existing links\n\n**-I, --id**\n> Show object IDs\n\n**-d, --disconnect**\n> Disconnect ports instead of connecting\n\n**-h, --help**\n> Display help information"],["DESCRIPTION","**pw-link** manages connections between PipeWire ports. It can list available ports, create new links between audio sources and sinks, and remove existing connections.\n\nThe tool enables routing audio between applications and devices, useful for creating complex audio setups like virtual cables or recording application output."],["CAVEATS","Port names can be long and complex. Links may be recreated by session manager. Use IDs for scripting stability. Some links managed by WirePlumber."],["HISTORY","**pw-link** is part of **PipeWire**, providing port connection management similar to jack_connect for JACK. It enables manual control over the audio routing graph."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pw-cli](/man/pw-cli)(1), [wpctl](/man/wpctl)(1), [jack_connect](/man/jack_connect)(1)"]],"e":["pw-cli","wpctl","jack_connect"],"x":"pw-link -oiI"},"qm-guest-exec":{"n":"qm-guest-exec","t":"Run commands inside a Proxmox VM","s":[["TAGLINE","Run commands inside a Proxmox VM"],["TLDR","**Execute** a command via guest agent\n\n```qm guest exec 100 command arg1 arg2```\n\nExecute **asynchronously**\n\n```qm guest exec 100 command --synchronous 0```\n\nExecute with **timeout** of 10 seconds\n\n```qm guest exec 100 command --timeout 10```\n\nExecute and **forward stdin** to guest\n\n```qm guest exec 100 command --pass-stdin 1```"],["SYNOPSIS","**qm guest exec** _vmid_ _command_ [_arguments_...] [_options_]"],["PARAMETERS","**--synchronous** _0|1_\n> Wait for the command to complete (default _1_). With _0_, **qm guest exec** returns a PID immediately; collect output later with **qm guest exec-status**.\n\n**--timeout** _seconds_\n> Maximum time to wait for synchronous completion (default _30_). Forces an asynchronous run when exceeded.\n\n**--pass-stdin** _0|1_\n> Forward the host's stdin to the guest command (default _0_)."],["DESCRIPTION","**qm guest exec** executes commands inside a virtual machine through the QEMU guest agent. This allows host-level control over guest operations without network access to the VM.\n\nThe guest agent must be installed and running inside the VM for this to work."],["CAVEATS","Requires QEMU guest agent to be installed and running in the VM. Some commands may require specific permissions within the guest OS."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools. Guest agent communication provides out-of-band management capabilities."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-agent](/man/qm-agent)(1)"]],"e":["qm","qm-agent"],"x":"qm guest exec 100 command arg1 arg2"},"quickemu":{"n":"quickemu","t":"Create and run optimized QEMU virtual machines","s":[["TAGLINE","Create and run optimized QEMU virtual machines"],["TLDR","**Run** VM from config file\n\n```quickemu --vm [file.conf]```\n\nRun without **committing changes**\n\n```quickemu --status-quo --vm [file.conf]```\n\nRun in **fullscreen** with display backend\n\n```quickemu --fullscreen --display [sdl] --vm [file.conf]```\n\nCreate/restore/delete **snapshot**\n\n```quickemu --snapshot [create|apply|delete] [tag] --vm [file.conf]```\n\nList **snapshots**\n\n```quickemu --snapshot info --vm [file.conf]```\n\n**Delete** VM\n\n```quickemu --delete-vm --vm [file.conf]```"],["SYNOPSIS","**quickemu** [**--vm** _config_] [**--display** _backend_] [**--snapshot** _action_] [_options_]"],["PARAMETERS","**--vm _config_**\n> VM configuration file\n\n**--status-quo**\n> Don't commit changes to disk\n\n**--fullscreen**\n> Start in fullscreen mode\n\n**--display _backend_**\n> Display: sdl, gtk, spice, spice-app, none\n\n**--sound-card _card_**\n> Audio: intel-hda, ac97, es1370, sb16, none\n\n**--snapshot _action_ _tag_**\n> Snapshot operations: create, apply, delete, info\n\n**--delete-vm**\n> Delete VM and configuration\n\n**--delete-disk**\n> Delete disk image and EFI vars\n\n**--shortcut**\n> Create desktop shortcut"],["DESCRIPTION","**quickemu** creates and manages optimized QEMU virtual machines with minimal configuration. It automatically configures display, audio, USB passthrough, and shared folders based on the guest OS.\n\nVMs are defined in simple configuration files that specify the OS type and disk image. The tool handles the complex QEMU command-line options internally."],["CAVEATS","Requires QEMU and related packages. Some features need KVM support. Guest additions may be needed for optimal integration. macOS guests have specific requirements."],["HISTORY","**quickemu** was created by **Martin Wimpress** to simplify QEMU VM management. It grew from scripts used to test Linux distributions and evolved into a comprehensive VM tool."],["INSTALL","```apt: sudo apt install quickemu```\n\n```dnf: sudo dnf install quickemu```\n\n```nix: nix profile install nixpkgs#quickemu```"],["SEE ALSO","[quickget](/man/quickget)(1), [qemu](/man/qemu)(1), [virt-manager](/man/virt-manager)(1)"]],"e":["quickget","qemu","virt-manager"],"x":"quickemu --vm [file.conf]"},"rcp":{"n":"rcp","t":"Copy files between hosts over the network","s":[["TAGLINE","Copy files between hosts over the network"],["TLDR","Copy a **file** to a remote host\n\n```rcp path/to/local_file username@remote_host:/path/to/destination/```\n\nCopy a **directory** recursively\n\n```rcp -r path/to/local_directory username@remote_host:/path/to/destination/```\n\n**Preserve** file attributes\n\n```rcp -p path/to/local_file username@remote_host:/path/to/destination/```\n\n**Force** copy without confirmation\n\n```rcp -f path/to/local_file username@remote_host:/path/to/destination/```"],["SYNOPSIS","**rcp** [_options_] _source_ _destination_"],["PARAMETERS","**-r**, **--recursive**\n> Copy directories recursively\n\n**-p**, **--preserve**\n> Preserve file attributes (mode, ownership, timestamps)\n\n**-f**, **--from**\n> Force copy without confirmation"],["DESCRIPTION","**rcp** copies files between local and remote systems. It mimics the behavior of the cp command but operates across different machines using the RSH protocol.\n\nThe source or destination can be specified as user@host:path for remote locations."],["CAVEATS","rcp transmits data unencrypted and is considered insecure. Use scp or rsync over SSH for secure file transfers instead."],["HISTORY","Part of **GNU inetutils**, providing classic Unix networking utilities. Largely replaced by secure alternatives like scp and rsync."],["INSTALL","```nix: nix profile install nixpkgs#rcp```"],["SEE ALSO","[scp](/man/scp)(1), [rsync](/man/rsync)(1), [cp](/man/cp)(1), [rsh](/man/rsh)(1)"]],"e":["scp","rsync","cp","rsh"],"x":"rcp path/to/local_file username@remote_host:/path/to/destination/"},"resticprofile-schedule":{"n":"resticprofile-schedule","t":"Schedule restic backup profile jobs","s":[["TAGLINE","Schedule restic backup profile jobs"],["TLDR","**Schedule backup profile**\n\n```resticprofile schedule [profile]```\n\n**Schedule all profiles**\n\n```resticprofile schedule --all```\n\n**Schedule with specific command**\n\n```resticprofile schedule [profile] --[backup]```"],["SYNOPSIS","**resticprofile schedule** [_options_] [_profile_]"],["PARAMETERS","_profile_\n> Profile name to schedule.\n\n**--all**\n> Schedule all profiles.\n\n**--backup**\n> Schedule backup command.\n\n**--check**\n> Schedule check command.\n\n**--prune**\n> Schedule prune command."],["DESCRIPTION","**resticprofile schedule** creates scheduled jobs for restic backup profiles. Integrates with systemd timers, launchd, or Windows Task Scheduler. Part of resticprofile wrapper for restic backup."],["INSTALL","```brew: brew install resticprofile```\n\n```nix: nix profile install nixpkgs#resticprofile```"],["SEE ALSO","[resticprofile](/man/resticprofile)(1), [restic](/man/restic)(1), [resticprofile-unschedule](/man/resticprofile-unschedule)(1)"]],"e":["resticprofile","restic","resticprofile-unschedule"],"x":"resticprofile schedule [profile]"},"setopt":{"n":"setopt","t":"Enable zsh shell options","s":[["TAGLINE","Enable zsh shell options"],["TLDR","**Enable an option** in zsh\n\n```setopt [autocd]```\n\n**Enable multiple options**\n\n```setopt [autocd] [extendedglob] [noclobber]```\n\n**Enable an option** using uppercase naming\n\n```setopt AUTO_CD```\n\n**List all currently set options**\n\n```setopt```\n\n**Enable options matching a pattern**\n\n```setopt -m '[a-z]*glob*'```\n\n**Disable an option** by prefixing with NO\n\n```setopt noautocd```"],["SYNOPSIS","**setopt** [**-m**] [_option_ ...]"],["PARAMETERS","**-m**\n> Treat arguments as patterns and set all options matching those patterns. Patterns should be quoted to prevent filename expansion."],["DESCRIPTION","**setopt** is a zsh shell builtin command that enables shell options. These options control various aspects of shell behavior including command completion, globbing, history, and compatibility with other shells.\n\nOption names are case insensitive and underscores are ignored, so **AUTO_CD**, **autocd**, and **auto_cd** all refer to the same option. Options can be negated by prefixing the name with **no** (e.g., **noautocd** disables the autocd option).\n\nWhen called without arguments, setopt lists all options that are currently set. The command **unsetopt** performs the inverse operation, disabling options."],["COMMON OPTIONS","**autocd**\n> Change to a directory by typing its name without cd.\n\n**extendedglob**\n> Enable extended pattern matching operators.\n\n**noclobber**\n> Prevent overwriting existing files with > redirection.\n\n**histignorealldups**\n> Remove older duplicate entries from history.\n\n**sharehistory**\n> Share command history between all running zsh sessions.\n\n**correct**\n> Try to correct spelling of commands.\n\n**autopushd**\n> Make cd push the old directory onto the directory stack.\n\n**promptsubst**\n> Enable parameter and command substitution in prompts."],["CAVEATS","Unlike **set -o**, a bad option name does not abort execution of subsequent shell code. This is because **set** is a POSIX special builtin while **setopt** is not. Options set with setopt only affect the current shell session unless added to **.zshrc** for persistence."],["HISTORY","**setopt** has been part of zsh since its early development. Zsh was created by Paul Falstad in **1990** while a student at Princeton University. The extensive option system distinguishes zsh from other shells, providing fine-grained control over shell behavior and compatibility modes for sh, ksh, and csh."],["SEE ALSO","[unsetopt](/man/unsetopt)(1), [zsh](/man/zsh)(1), [set](/man/set)(1)"]],"e":["unsetopt","zsh","set"],"x":"setopt [autocd]"},"sfdk-build-init":{"n":"sfdk-build-init","t":"Initialize Sailfish OS build environment","s":[["TAGLINE","Initialize Sailfish OS build environment"],["TLDR","**Initialize build environment**\n\n```sfdk build-init```\n\n**Initialize for a specific build target**\n\n```sfdk -c target=[SailfishOS-4.5.0.24-armv7hl] build-init```\n\n**Persist the target** for subsequent commands\n\n```sfdk config --push target [SailfishOS-4.5.0.24-armv7hl]; sfdk build-init```"],["SYNOPSIS","**sfdk** [_-c target=name_] **build-init** [_options_]"],["PARAMETERS","**-c** _target=name_\n> Global sfdk configuration option selecting the build target. The target is not specific to build-init; it is set with the **-c** option (or persisted with **sfdk config --push target** _name_) and reused by the other build subcommands."],["DESCRIPTION","**sfdk build-init** prepares (initializes) the build directory for a Sailfish OS project against the configured build target. It sets up a build engine snapshot and the build-time dependencies, after which commands like **sfdk build-shell**, **sfdk build**, or **sfdk make** can be run. Part of the Sailfish SDK command-line tool **sfdk**."],["SEE ALSO","[sfdk](/man/sfdk)(1), [sfdk-build-requires](/man/sfdk-build-requires)(1)"]],"e":["sfdk","sfdk-build-requires"],"x":"sfdk build-init"},"spectacle":{"n":"spectacle","t":"KDE Plasma screenshot capture tool","s":[["TAGLINE","KDE Plasma screenshot capture tool"],["TLDR","**Take screenshot**\n\n```spectacle```\n\n**Capture full screen**\n\n```spectacle -f```\n\n**Capture active window**\n\n```spectacle -a```\n\n**Capture rectangular region**\n\n```spectacle -r```\n\n**Capture window under cursor**\n\n```spectacle -u```\n\n**Capture after delay** (in seconds)\n\n```spectacle -d [5]```\n\n**Save to file** in background mode\n\n```spectacle -b -o [screenshot.png]```\n\n**Copy to clipboard** in background mode\n\n```spectacle -b -c```"],["SYNOPSIS","**spectacle** [_-f_] [_-a_] [_-r_] [_-d delay_] [_-o file_] [_options_]"],["PARAMETERS","**-f**, **--fullscreen**\n> Full screen capture.\n\n**-a**, **--activewindow**\n> Active window.\n\n**-r**, **--region**\n> Rectangular region.\n\n**-d**, **--delay** _SECS_\n> Delay in seconds.\n\n**-o**, **--output** _FILE_\n> Save to file.\n\n**-c**, **--clipboard**\n> Copy to clipboard.\n\n**-b**, **--background**\n> No GUI.\n\n**-u**, **--windowundercursor**\n> Capture window under cursor (including pop-up parents)\n\n**-t**, **--transientonly**\n> Capture window under cursor (excluding pop-up parents)\n\n**-m**, **--currentmonitor**\n> Capture the current monitor\n\n**-p**, **--pointer**\n> Include pointer in screenshot (background mode)\n\n**-w**, **--nowindow**\n> Launch without taking a screenshot\n\n**-n**, **--nonotify**\n> Suppress notification"],["DESCRIPTION","**spectacle** is KDE's screenshot tool. It captures screen content with various options.\n\nMode selection chooses the capture area: full screen, current monitor, active window, window under cursor, or a rectangular region.\n\nThe delay option allows time to position windows or open menus before capture. Background mode enables scripted captures without showing the GUI."],["CAVEATS","KDE/Plasma application. May not work on other desktops. Wayland has limitations."],["HISTORY","**Spectacle** is the default screenshot application for **KDE Plasma**. It replaced KSnapshot with a modernized interface."],["INSTALL","```apt: sudo apt install kde-spectacle```\n\n```dnf: sudo dnf install spectacle```\n\n```pacman: sudo pacman -S spectacle```\n\n```apk: sudo apk add spectacle```\n\n```zypper: sudo zypper install spectacle```"],["SEE ALSO","[scrot](/man/scrot)(1), [gnome-screenshot](/man/gnome-screenshot)(1), [flameshot](/man/flameshot)(1)"]],"e":["scrot","gnome-screenshot","flameshot"],"x":"spectacle"},"sqlit":{"n":"sqlit","t":"Lazygit-style TUI for SQL databases","s":[["TAGLINE","Lazygit-style TUI for SQL databases"],["TLDR","**Launch the interactive TUI**\n\n```sqlit```\n\n**Connect to a saved connection**\n\n```sqlit -c \"[MyConnection]\"```\n\n**Run a query from the CLI**\n\n```sqlit query -c \"[MyConnection]\" -q \"[SELECT * FROM Users]\" --format csv```"],["SYNOPSIS","**sqlit** [**-c** _connection_name_]\n\n**sqlit** **query** [**-c** _connection_name_] [**-q** _query_] [**--format** _format_]"],["PARAMETERS","**-c** _CONNECTION_\n> Connect using a saved connection name.\n\n**-q** _QUERY_\n> SQL query to execute (used with the **query** subcommand).\n\n**--format** _FORMAT_\n> Output format for query results: **table**, **csv**, **json** (used with the **query** subcommand)."],["DESCRIPTION","**sqlit** is a lightweight, keyboard-driven TUI for connecting to and querying SQL databases from the terminal. It supports 20+ database systems including PostgreSQL, MySQL, SQLite, DuckDB, BigQuery, ClickHouse, and Snowflake. Features include query history, autocomplete, Vim-style editing, SSH tunneling, Docker container auto-discovery, and secure credential storage."],["HISTORY","**sqlit** was created by **Maxteabag** (Peter Adams) and is written in **Python**."],["INSTALL","```aur: yay -S sqlit```"],["SEE ALSO","[litecli](/man/litecli)(1), [harlequin](/man/harlequin)(1), [usql](/man/usql)(1), [pgcli](/man/pgcli)(1), [mycli](/man/mycli)(1)"]],"e":["litecli","harlequin","usql","pgcli","mycli"],"x":"sqlit"},"st.1":{"n":"st.1","t":"Suckless simple terminal emulator","s":[["TAGLINE","Suckless simple terminal emulator"],["TLDR","**Open** a terminal\n\n```st```\n\nOpen with specific **title**\n\n```st -T [title]```\n\n**Execute command** and write output to file\n\n```st -o [path/to/file] -e [command] [args]```\n\n**Execute command** in terminal\n\n```st -e [command] [args]```\n\nSet **geometry**\n\n```st -g [columns]x[rows]```"],["KEYBOARD SHORTCUTS","**Ctrl+Shift+PageUp/PageDown**\n> Increase/decrease font size\n\n**Ctrl+Shift+c**\n> Copy to clipboard\n\n**Ctrl+Shift+v**\n> Paste from clipboard\n\n**Ctrl+Shift+y**\n> Paste from selection"],["SYNOPSIS","**st** [_OPTIONS_] [**-e** _command_ [_args_...]]"],["PARAMETERS","**-T** _title_\n> Set window title\n\n**-t** _title_\n> Set window title (alias for -T)\n\n**-e** _command_ [_args_]\n> Execute command instead of shell\n\n**-o** _file_\n> Write terminal output to file\n\n**-g** _geometry_\n> Set window geometry (WxH+X+Y)\n\n**-f** _font_\n> Set font (X11 font string)\n\n**-c** _class_\n> Set window class\n\n**-n** _name_\n> Set window name\n\n**-w** _windowid_\n> Embed in given window ID\n\n**-a**\n> Disable alternate screen buffer\n\n**-i**\n> Fix screen on insert mode\n\n**-v**\n> Print version"],["DESCRIPTION","**st** is a simple terminal emulator for X11 from the suckless project. It aims to be lightweight and follows the suckless philosophy of simplicity, providing a minimal terminal with core functionality and configuration through source code modification.\n\nFeatures include UTF-8 support, true color, clipboard handling, and configurable keybindings. Unlike feature-rich terminals, st is designed to be patched and recompiled for customization rather than configured through files."],["CAVEATS","Configuration requires editing config.h and recompiling. No scrollback by default; requires a patch or external tool like **scroll** or **tmux**. No tabs or splits; use a multiplexer. Requires X11; does not work on Wayland without XWayland."],["HISTORY","**st** was created by the **suckless.org** community, known for minimalist software including **dwm** window manager. The project follows the suckless philosophy emphasizing simplicity, clarity, and frugality in software design. st has been developed since **2012** as an alternative to bloated terminal emulators."],["SEE ALSO","[xterm](/man/xterm)(1), [urxvt](/man/urxvt)(1), [alacritty](/man/alacritty)(1), [tmux](/man/tmux)(1)"]],"e":["xterm","urxvt","alacritty","tmux"],"x":"st"},"swaybg":{"n":"swaybg","t":"Wayland wallpaper setter for Sway","s":[["TAGLINE","Wayland wallpaper setter for Sway"],["TLDR","**Set wallpaper**\n\n```swaybg -i [wallpaper.jpg]```\n\n**Specific output**\n\n```swaybg -o [HDMI-A-1] -i [wallpaper.jpg]```\n\n**Fill mode**\n\n```swaybg -i [wallpaper.jpg] -m fill```\n\n**Tile mode**\n\n```swaybg -i [wallpaper.jpg] -m tile```\n\n**Solid color**\n\n```swaybg -c \"[#1a1a2e]\"```\n\n**Center image**\n\n```swaybg -i [wallpaper.jpg] -m center```"],["SYNOPSIS","**swaybg** [_-o output_] [_-i image_] [_-m mode_] [_-c color_]"],["PARAMETERS","**-i**, **--image** _FILE_\n> Wallpaper image.\n\n**-o**, **--output** _OUTPUT_\n> Target output.\n\n**-m**, **--mode** _MODE_\n> Scaling mode.\n\n**-c**, **--color** _COLOR_\n> Background color."],["MODES","**stretch** - Stretch to fill\n**fill** - Fill, crop excess\n**fit** - Fit, may letterbox\n**center** - Center, no scaling\n**tile** - Tile image"],["DESCRIPTION","**swaybg** sets desktop wallpapers for Sway and other wlroots-based Wayland compositors. It supports displaying image files with various scaling modes (stretch, fill, fit, center, tile) or setting a solid background color using hex codes.\n\nDifferent wallpapers can be assigned to individual outputs by specifying the output name, enabling per-monitor backgrounds in multi-display setups. The tool runs as a persistent background process; to change the wallpaper, the running instance must be killed and a new one started with the desired settings.\n\nSwaybg is typically launched from the Sway configuration file using an `exec` directive, ensuring the wallpaper is set automatically when the compositor starts."],["CAVEATS","Wayland only. Sway/wlroots compositors. One instance per wallpaper."],["HISTORY","**swaybg** is part of the **Sway** project, a Wayland compositor. It provides simple background management for wlroots-based compositors."],["INSTALL","```apt: sudo apt install swaybg```\n\n```dnf: sudo dnf install swaybg```\n\n```pacman: sudo pacman -S swaybg```\n\n```apk: sudo apk add swaybg```\n\n```zypper: sudo zypper install swaybg```\n\n```nix: nix profile install nixpkgs#swaybg```"],["SEE ALSO","[sway](/man/sway)(1), [swaylock](/man/swaylock)(1), [feh](/man/feh)(1)"]],"e":["sway","swaylock","feh"],"x":"swaybg -i [wallpaper.jpg]"},"systemctl-add-requires":{"n":"systemctl-add-requires","t":"Add hard systemd unit dependencies","s":[["TAGLINE","Add hard systemd unit dependencies"],["TLDR","Add **Requires** dependency to a target\n\n```systemctl add-requires [target] [unit]```\n\nAdd **multiple** dependencies\n\n```systemctl add-requires [target] [unit1 unit2 ...]```\n\nAdd **user-level** dependency\n\n```systemctl add-requires [target] [unit] --user```"],["SYNOPSIS","**systemctl add-requires** _TARGET_ _UNIT_..."],["PARAMETERS","**--system**\n> Operate on system configuration (default)\n\n**--user**\n> Operate on user configuration\n\n**--runtime**\n> Make temporary changes until reboot\n\n**--global**\n> Apply globally for all user logins"],["DESCRIPTION","**systemctl add-requires** appends `Requires=` dependencies to a specified systemd target for one or more units. A `Requires=` relationship establishes a hard dependency: if any of the listed units fail to start, the target itself will also fail to activate.\n\nThis command provides a programmatic way to establish unit dependencies without manually editing unit files or creating symlinks in `.requires/` directories."],["CAVEATS","Changes made without --runtime are persistent and survive reboots. Hard dependencies (Requires) can cause cascading failures if a unit cannot start. Consider using add-wants for softer dependencies where failure should not propagate."],["HISTORY","The **add-requires** subcommand was added to **systemctl** in systemd version **217**. Systemd was created by **Lennart Poettering** and **Kay Sievers** at Red Hat and has been the default init system for most major Linux distributions since **2015**."],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-add-wants](/man/systemctl-add-wants)(1)"]],"e":["systemctl","systemctl-add-wants"],"x":"systemctl add-requires [target] [unit]"},"systemctl-list-timers":{"n":"systemctl-list-timers","t":"List scheduled timer units","s":[["TAGLINE","List scheduled timer units"],["TLDR","List **active** timers\n\n```systemctl list-timers```\n\nList **all** timers\n\n```systemctl list-timers -a```\n\nFilter by **pattern**\n\n```systemctl list-timers [pattern]```\n\nFilter by **state**\n\n```systemctl list-timers --state [active|inactive|failed]```"],["SYNOPSIS","**systemctl list-timers** [_OPTIONS_] [_PATTERN_...]"],["PARAMETERS","**-a, --all**\n> Include inactive timers\n\n**--state=** _STATE_\n> Filter by state\n\n**--no-legend**\n> Suppress header and footer\n\n**--no-pager**\n> Disable pager"],["DESCRIPTION","**systemctl list-timers** displays timer units currently active in memory. The output shows next activation time, time until activation, last trigger time, time since last trigger, the timer unit, and the unit it activates.\n\nTimer units are systemd's replacement for cron, providing scheduled activation of services with more flexibility and better logging integration."],["OUTPUT COLUMNS","**NEXT** - Next scheduled activation\n\n**LEFT** - Time until next activation\n\n**LAST** - Previous trigger time\n\n**PASSED** - Time since last trigger\n\n**UNIT** - Timer unit name\n\n**ACTIVATES** - Unit triggered by this timer"],["CAVEATS","Times are relative to the current time. Transient timers created with `systemd-run` also appear here. Some system timers run very infrequently (weekly, monthly)."],["HISTORY","The **list-timers** subcommand provides an overview of scheduled tasks, replacing traditional `crontab -l` listings with richer information about timing and triggered services."],["SEE ALSO","[systemctl](/man/systemctl)(1), [crontab](/man/crontab)(1)"]],"e":["systemctl","crontab"],"x":"systemctl list-timers"},"tint":{"n":"tint","t":"Terminal-based Tetris clone","s":[["TAGLINE","Terminal-based Tetris clone"],["TLDR","**Start the game**\n\n```tint```\n\n**Start at specific level**\n\n```tint -l [5]```\n\n**Show high scores**\n\n```tint -s```\n\n**Display version**\n\n```tint -v```"],["SYNOPSIS","**tint** [_options_]"],["PARAMETERS","**-l** _level_\n> Start at specified level (1-9).\n\n**-s**\n> Show high score table.\n\n**-n**\n> Disable colors.\n\n**-d**\n> Draw dots inside blocks.\n\n**-h**\n> Display help.\n\n**-v**\n> Display version."],["CONTROLS","**j**: Move piece left.\n\n**l**: Move piece right.\n\n**k**: Rotate piece clockwise.\n\n**Space**: Drop piece instantly.\n\n**p**: Pause game.\n\n**q**: Quit game."],["DESCRIPTION","**tint** (TINT Is Not Tetris) is a terminal-based clone of the classic Tetris game using ncurses. It aims to recreate the original gameplay as faithfully as possible while running in a text terminal.\n\nThe game features nine difficulty levels, with higher levels increasing piece drop speed. It includes a high score system that saves scores locally with player names. Statistics track the number of each tetromino type that has appeared during play.\n\nUnlike some Tetris variants, tint does not show a preview of the next piece, maintaining closer fidelity to the original game design."],["CAVEATS","Requires ncurses library. High scores are stored locally and not shared. No next-piece preview available. Terminal must support the required display dimensions."],["HISTORY","**tint** was created as a faithful terminal-based recreation of the original Tetris game by Alexey Pajitnov. The recursive acronym name (TINT Is Not Tetris) is a nod to GNU naming conventions while acknowledging trademark considerations. It became a popular choice for Tetris on minimal Linux systems and servers."],["INSTALL","```zypper: sudo zypper install tint```\n\n```nix: nix profile install nixpkgs#tint```"],["SEE ALSO","[tetrinet](/man/tetrinet)(1), [bastet](/man/bastet)(1), [vitetris](/man/vitetris)(1), [moon-buggy](/man/moon-buggy)(1)"]],"e":["tetrinet","bastet","vitetris","moon-buggy"],"x":"tint"},"tty":{"n":"tty","t":"Print terminal device file name","s":[["TAGLINE","Print terminal device file name"],["TLDR","**Print the terminal device** name\n\n```tty```\n\n**Check silently** if stdin is a terminal (exit status only)\n\n```tty -s```\n\n**Use in a script** to check for interactive mode\n\n```if tty -s; then echo \"Interactive\"; fi```\n\n**Show version**\n\n```tty --version```"],["SYNOPSIS","**tty** [_options_]"],["PARAMETERS","**-s**, **--silent**, **--quiet**\n> Print nothing; only return exit status\n\n**--help**\n> Display help and exit\n\n**--version**\n> Output version information and exit"],["DESCRIPTION","**tty** prints the file name of the terminal connected to standard input. The output is typically a device path like **/dev/pts/0** (pseudo-terminal) or **/dev/tty1** (virtual console).\n\nIf standard input is not connected to a terminal (e.g., when input is piped or redirected from a file), tty prints \"not a tty\" and exits with status 1.\n\nThe **-s** option suppresses output entirely, making it useful in scripts where only the exit status matters. This allows testing whether a script is running interactively or in a batch/piped context."],["EXIT STATUS","**0**\n> Standard input is a terminal\n\n**1**\n> Standard input is not a terminal\n\n**2**\n> Invalid option specified"],["EXAMPLES","**Check if running interactively**\n```\nif tty -s; then\n    echo \"Running in terminal\"\nelse\n    echo \"Running non-interactively\"\nfi\n```\n\n**Get the terminal device path**\n```\nMYTERM=$(tty)\necho \"Connected to: $MYTERM\"\n```"],["CAVEATS","The tty command only checks standard input. If stdin is redirected but stdout/stderr are still connected to a terminal, tty will report \"not a tty\". For more detailed terminal checks, consider using **test -t** or examining **/proc/self/fd/**."],["HISTORY","The **tty** command dates back to the earliest versions of Unix at Bell Labs in the **1970s**. The name comes from \"teletypewriter,\" the original terminal devices. The GNU coreutils version was written by **David MacKenzie**. The command is specified in POSIX."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[stty](/man/stty)(1), [who](/man/who)(1), [mesg](/man/mesg)(1), [write](/man/write)(1)"]],"e":["stty","who","mesg","write"],"x":"tty"},"unbuffer":{"n":"unbuffer","t":"Disable output buffering for commands","s":[["TAGLINE","Disable output buffering for commands"],["TLDR","**Run command with unbuffered output**\n\n```unbuffer [command]```\n\n**Unbuffer in a pipeline**\n\n```[command1] | unbuffer -p [command2] | [command3]```\n\n**Unbuffer grep in a pipeline**\n\n```tail -f [logfile] | unbuffer -p grep [pattern]```\n\n**Unbuffer output of a script**\n\n```unbuffer ./[script.sh] | tee [output.log]```"],["SYNOPSIS","**unbuffer** [**-p**] _program_ [_args_]"],["PARAMETERS","**-p**\n> Pipeline mode: read from stdin and pass to program.\n\n_program_\n> The command to run with unbuffered output.\n\n_args_\n> Arguments to pass to the program."],["DESCRIPTION","**unbuffer** disables output buffering that occurs when program output is redirected. Many programs buffer their output when not connected to a terminal, which can cause delays or issues in pipelines.\n\nThe tool works by connecting the program to a pseudo-terminal (pty), making it behave as if running interactively. This forces line-buffered or unbuffered output even when redirected to files or pipes.\n\nCommon use cases include watching log files through grep, capturing colored output, and ensuring real-time output in scripts.\n\nunbuffer is part of the Expect package and uses Expect's pty handling capabilities."],["CAVEATS","Does not read from stdin by default; use -p for pipeline mode. In pipeline mode with -p, unbuffer exits immediately when it encounters EOF. May affect programs that detect terminal capabilities. Part of the expect package, not installed by default on all systems."],["HISTORY","**unbuffer** was created as part of the Expect package by Don Libes at NIST. Expect is documented in \"Exploring Expect: A Tcl-Based Toolkit for Automating Interactive Programs\" (O'Reilly, 1995). The tool addresses a common Unix problem of output buffering in pipelines."],["INSTALL","```apt: sudo apt install expect```\n\n```dnf: sudo dnf install expect```\n\n```pacman: sudo pacman -S expect```\n\n```apk: sudo apk add expect```\n\n```zypper: sudo zypper install expect```\n\n```brew: brew install expect```\n\n```nix: nix profile install nixpkgs#expect```"],["SEE ALSO","[expect](/man/expect)(1), [stdbuf](/man/stdbuf)(1), [script](/man/script)(1), [tee](/man/tee)(1)"]],"e":["expect","stdbuf","script","tee"],"x":"unbuffer [command]"},"updpkgsums":{"n":"updpkgsums","t":"Update source checksums in Arch PKGBUILD files","s":[["TAGLINE","Update source checksums in Arch PKGBUILD files"],["TLDR","**Update checksums in PKGBUILD** in the current directory\n\n```updpkgsums```\n\n**Update checksums in a specific PKGBUILD** file\n\n```updpkgsums [path/to/PKGBUILD]```\n\n**Update checksums without colored output**\n\n```updpkgsums -m```\n\nDisplay **help**\n\n```updpkgsums -h```"],["SYNOPSIS","**updpkgsums** [_OPTIONS_] [_build file_]"],["PARAMETERS","**-h**, **--help**\n> Display help information.\n\n**-m**, **--nocolor**\n> Do not colorize output.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**updpkgsums** performs an in-place update of the checksums in a PKGBUILD file. It downloads the sources (if needed) and calculates new checksums, updating the PKGBUILD file. Defaults to the PKGBUILD in the current directory if no file is specified.\n\nUnless a preexisting hashing algorithm is already specified in the PKGBUILD, SHA256 will be used for the new checksums."],["CAVEATS","Arch Linux specific. Must be run in directory containing PKGBUILD. Downloads sources if not already cached. Network access required for remote sources."],["SEE ALSO","[makepkg](/man/makepkg)(8), [pacman](/man/pacman)(8)"]],"e":["makepkg","pacman"],"x":"updpkgsums"},"vtcode":{"n":"vtcode","t":"Semantic AI coding agent with multi-provider LLM support","s":[["TAGLINE","Semantic AI coding agent with multi-provider LLM support"],["TLDR","**Launch** the interactive coding agent in the current directory\n\n```vtcode```\n\n**Ask** the agent a one-off question and exit\n\n```vtcode ask \"[explain the project structure]\"```\n\n**Execute** a shell command through the agent's safe runner\n\n```vtcode exec \"[cargo test]\"```\n\n**Use** a specific model provider and model\n\n```vtcode --provider [anthropic] --model [claude-sonnet-4-5]```\n\n**Run** against an alternate workspace directory\n\n```vtcode --workspace [path/to/repo]```\n\n**Open** the configuration file for editing\n\n```vtcode config edit```\n\n**Print** the resolved configuration\n\n```vtcode config show```\n\n**Authenticate** with a provider via OAuth\n\n```vtcode /login```"],["SYNOPSIS","**vtcode** [_global-flags_] [_subcommand_] [_args_]"],["PARAMETERS","**ask** _prompt_\n> Send a single prompt to the configured model and print the response.\n\n**exec** _command_\n> Run _command_ through the agent's policy-aware shell runner with **tree-sitter-bash** validation.\n\n**config** [**show**|**edit**|**path**]\n> Inspect or edit the **vtcode.toml** configuration.\n\n**--provider** _name_\n> Override the default LLM provider (e.g. **openai**, **anthropic**, **deepseek**, **gemini**, **ollama**, **lmstudio**).\n\n**--model** _id_\n> Override the default model id for the selected provider.\n\n**--workspace** _path_\n> Set the working directory the agent operates in (defaults to **$PWD**).\n\n**--config** _file_\n> Use _file_ instead of the default **vtcode.toml**.\n\n**--no-tools**\n> Disable tool execution; the agent only emits text.\n\n**--yes**\n> Auto-approve tool calls instead of prompting per action.\n\n**-v**, **--verbose**\n> Increase log verbosity.\n\n**--version**\n> Print the version and exit.\n\n**-h**, **--help**\n> Show usage information."],["SLASH COMMANDS","**/login**\n> Run an OAuth flow to authenticate with the active provider.\n\n**/agent**\n> Inspect delegated subagents in the current session.\n\n**/agents active**\n> List currently running subagents.\n\n**/subprocesses**\n> Open the local agents drawer (also bound to **Alt+S**)."],["DESCRIPTION","**vtcode** is an open-source semantic coding agent written in **Rust**. It connects to one or more LLM providers and exposes a tool-using assistant that can read, edit, and execute code inside a workspace. A Tree-sitter-based parser gives the agent structural understanding of source files in many languages, enabling targeted edits rather than blind text rewrites.\n\nTool execution is gated by a configurable policy: each tool can be set to **allow**, **deny**, or **prompt**, and shell commands are statically validated with **tree-sitter-bash** before being run. Sandboxing keeps file operations within the configured workspace boundary.\n\nMulti-provider support — **OpenAI**, **Anthropic**, **DeepSeek**, **Gemini**, **Z.AI**, **Moonshot**, **OpenRouter**, **MiniMax**, **Ollama**, **LM Studio** — is wired through a single configuration file with automatic failover, prompt caching, and context-window-aware truncation."],["CONFIGURATION","User configuration lives in **vtcode.toml** (project-level) or **~/.config/vtcode/vtcode.toml** (global). Common keys:\n\n> **[providers.<name>]** — API base URL and credential source for each provider.\n> **[oauth]** — OAuth client settings; tokens are stored in the OS keychain.\n> **[tools]** — per-tool allow/deny/prompt policies.\n> **[workspace]** — root path and ignore globs.\n> **[security]** — shell sandbox, command allow/deny lists, max output size.\n> **[context]** — context window budget and truncation strategy.\n> **[hooks]** — shell commands to run on lifecycle events (session start, tool call, etc.).\n\nSecrets are typically read from environment variables (e.g. **OPENAI_API_KEY**, **ANTHROPIC_API_KEY**, **GEMINI_API_KEY**) or from the OS-native credential store."],["KEYBOARD SHORTCUTS","```\nCtrl+B   Toggle background subagent\nAlt+S    Open Local Agents drawer\nCtrl+C   Cancel current request\n```"],["CAVEATS","vtcode is **pre-1.0** — flags, configuration keys, and slash commands change between releases. LLM providers may charge per token; review **[security]** policies before enabling auto-approve (**--yes**) on a real codebase. Some features (Zed editor integration, A2A protocol, sandbox isolation) require additional setup beyond the default install."],["HISTORY","**vtcode** is developed by **Vinh Nguyen** (**vinhnx**) and published at **github.com/vinhnx/vtcode**. It is distributed via **Cargo** (**cargo install vtcode**), **Homebrew** (**brew install vtcode**), and an install script. Releases are published under an open-source license on GitHub."],["INSTALL","```brew: brew install vtcode```"],["SEE ALSO","[claude](/man/claude)(1), [aider](/man/aider)(1), [gh](/man/gh)(1), [cargo](/man/cargo)(1)"]],"e":["claude","aider","gh","cargo"],"x":"vtcode"},"witr":{"n":"witr","t":"Explain why a process, port, or container is running","s":[["TAGLINE","Explain why a process, port, or container is running"],["TLDR","**Explain** a process by name\n\n```witr [name]```\n\n**Explain** what is listening on a port\n\n```witr --port [port]```\n\n**Explain** a process by PID\n\n```witr --pid [pid]```\n\n**Show only the ancestry chain**\n\n```witr [name] --short```\n\n**Show ancestry as a tree**\n\n```witr --pid [pid] --tree```\n\n**Look up a container** by name or image\n\n```witr --container [name]```\n\n**Open interactive TUI** dashboard\n\n```witr -i```"],["SYNOPSIS","**witr** [*options*] [*name*...]"],["DESCRIPTION","**witr** (\"why is this running?\") answers the causal question behind a running process: where it came from, how it was started, and what chain of supervisors, shells, services, or containers is keeping it alive. Unlike **ps**, **lsof**, or **systemctl** alone, it correlates those layers into one human-readable explanation (or a tree / JSON view).\n\nTargets can be process names (substring match by default), PIDs, ports, open files, or containers. Container lookup spans Docker, Podman, nerdctl, Kubernetes/crictl, Incus, LXC/LXD, and FreeBSD jails. With no targets, or with **-i**, **witr** opens an interactive TUI."],["PARAMETERS","**-p**, **--pid** *pid*\n\n> Look up one or more process IDs (repeatable).\n\n**-o**, **--port** *port*\n\n> Look up process(es) bound to port(s) (repeatable).\n\n**-f**, **--file** *path*\n\n> Look up the process holding a file open (repeatable).\n\n**-c**, **--container** *name*\n\n> Look up container by name, image, command, or compose labels (repeatable).\n\n**-x**, **--exact**\n\n> Exact process name match (disable substring search).\n\n**-s**, **--short**\n\n> Print only the ancestry chain.\n\n**-t**, **--tree**\n\n> Print ancestry as a tree (includes children of the target).\n\n**-i**, **--interactive**\n\n> Interactive TUI mode.\n\n**--json**\n\n> Machine-readable JSON output.\n\n**--env**\n\n> Include environment variables for the process.\n\n**--verbose**\n\n> Extended process information (mounts, networks, compose metadata for containers).\n\n**--no-color**\n\n> Disable colorized output.\n\n**--warnings**\n\n> Show only warnings.\n\n**-h**, **--help**\n\n> Show help.\n\n**-v**, **--version**\n\n> Show version."],["CAVEATS","Some details (especially containers and remote runtimes) require appropriate privileges and installed client tools. Substring name matching can return multiple hits; re-run with **--pid** or **--exact**. Output quality depends on what the local process tree and supervisors expose."],["INSTALL","```brew: brew install witr```\n\n```nix: nix profile install nixpkgs#witr```"],["SEE ALSO","[ps](/man/ps)(1), [lsof](/man/lsof)(1), [ss](/man/ss)(8), [systemctl](/man/systemctl)(1), [docker](/man/docker)(1)"],["RESOURCES","```[Source code](https://github.com/pranshuparmar/witr)```"]],"e":["ps","lsof","ss","systemctl","docker"],"x":"witr [name]"},"xfreerdp":{"n":"xfreerdp","t":"Free RDP remote desktop client","s":[["TAGLINE","Free RDP remote desktop client"],["TLDR","**Connect** to server\n\n```xfreerdp /u:[username] /p:[password] /v:[ip_address]```\n\nConnect with **audio**\n\n```xfreerdp /u:[username] /p:[password] /v:[ip_address] /sound:sys:alsa```\n\nConnect with **dynamic** resolution\n\n```xfreerdp /v:[ip_address] /u:[username] /p:[password] /dynamic-resolution```\n\nConnect with **clipboard**\n\n```xfreerdp /v:[ip_address] /u:[username] /p:[password] +clipboard```\n\n**Ignore** certificate\n\n```xfreerdp /v:[ip_address] /u:[username] /p:[password] /cert:ignore```\n\nConnect with **shared** directory\n\n```xfreerdp /v:[ip_address] /u:[username] /p:[password] /drive:[path/to/directory],[share_name]```"],["SYNOPSIS","**xfreerdp** [_OPTIONS_]"],["PARAMETERS","**/v:** _HOST_\n> Server hostname or IP address\n\n**/u:** _USERNAME_\n> Username for authentication\n\n**/p:** _PASSWORD_\n> Password for authentication\n\n**/sound:** _DEVICE_\n> Enable audio redirection\n\n**/dynamic-resolution**\n> Enable dynamic resolution updates\n\n**+clipboard**\n> Enable clipboard redirection\n\n**/cert:ignore**\n> Ignore certificate warnings\n\n**/drive:** _PATH_,_NAME_\n> Share local directory\n\n**/f**\n> Fullscreen mode"],["DESCRIPTION","**xfreerdp** is a free Remote Desktop Protocol (RDP) client. It connects to Windows Remote Desktop servers and other RDP-compatible systems.\n\nPart of the FreeRDP project providing open-source RDP implementation."],["CAVEATS","Certificate warnings should not be ignored in production environments. Password visible in process list if specified on command line."],["INSTALL","```apk: sudo apk add freerdp2```\n\n```zypper: sudo zypper install freerdp2```"],["SEE ALSO","[rdesktop](/man/rdesktop)(1), [vinagre](/man/vinagre)(1), [vncviewer](/man/vncviewer)(1)"]],"e":["rdesktop","vinagre","vncviewer"],"x":"xfreerdp /u:[username] /p:[password] /v:[ip_address]"},"zapier-analytics":{"n":"zapier-analytics","t":"View and configure Zapier CLI analytics collection","s":[["TAGLINE","View and configure Zapier CLI analytics collection"],["TLDR","**Show current analytics status**\n\n```zapier analytics```\n\n**Enable full analytics collection**\n\n```zapier analytics -m enabled```\n\n**Set anonymous analytics mode**\n\n```zapier analytics -m anonymous```\n\n**Disable analytics collection**\n\n```zapier analytics -m disabled```"],["SYNOPSIS","**zapier** **analytics** [_-m mode_]"],["PARAMETERS","**-m**, **--mode** _MODE_\n> Set analytics collection mode: enabled (default, sends command info, OS type, and user ID), anonymous (sends command info only), or disabled (no data collected)."],["DESCRIPTION","**zapier analytics** shows the status of analytics collection for the Zapier CLI and allows changing what data is collected. By default, analytics are enabled and include information about the integration, CLI commands, operating system, and Zapier user ID.\n\nIn anonymous mode, OS type and user ID are omitted but command information is still sent. Analytics can also be disabled entirely by running zapier analytics -m disabled or by setting the DISABLE_ZAPIER_ANALYTICS environment variable to 1.\n\nTo see exactly what is being collected at runtime, prefix any command with DEBUG=zapier:analytics."],["SEE ALSO","[zapier](/man/zapier)(1), [zapier-history](/man/zapier-history)(1), [zapier-push](/man/zapier-push)(1), [zapier-validate](/man/zapier-validate)(1)"]],"e":["zapier","zapier-history","zapier-push","zapier-validate"],"x":"zapier analytics"},"zfs-receive":{"n":"zfs-receive","t":"Receive ZFS snapshot replication streams","s":[["TAGLINE","Receive ZFS snapshot replication streams"],["TLDR","**Receive snapshot from file**\n\n```zfs receive [pool/dataset] < [backup.zfs]```\n\n**Receive from remote system**\n\n```ssh [host] zfs send [pool/data@snap] | zfs receive [pool/newdata]```\n\n**Receive with force rollback**\n\n```zfs receive -F [pool/dataset] < [backup.zfs]```\n\n**Receive using full path from stream**\n\n```zfs receive -d [pool] < [backup.zfs]```\n\n**Dry run to test receive**\n\n```zfs receive -nv [pool/dataset] < [backup.zfs]```\n\n**Receive and override properties**\n\n```zfs receive -o mountpoint=[/mnt/data] [pool/dataset] < [backup.zfs]```\n\n**Receive excluding property**\n\n```zfs receive -x compression [pool/dataset] < [backup.zfs]```\n\n**Resume interrupted receive**\n\n```zfs receive -s [pool/dataset] < [backup.zfs]```"],["SYNOPSIS","**zfs receive** [_options_] _filesystem_|_volume_|_snapshot_"],["PARAMETERS","**-F**\n> Force rollback to most recent snapshot before receive.\n\n**-d**\n> Use all but first element of sent snapshot path.\n\n**-e**\n> Use only last element of sent snapshot path.\n\n**-u**\n> Don't mount received filesystem.\n\n**-o** _property=value_\n> Set property on received dataset.\n\n**-x** _property_\n> Exclude (inherit) property on received dataset.\n\n**-s**\n> Enable resumable receive.\n\n**-n**\n> Dry run, don't actually receive.\n\n**-v**\n> Verbose output.\n\n**-h**\n> Skip hold related errors (with -F)."],["DESCRIPTION","**zfs receive** (or **zfs recv**) creates a snapshot from a stream provided on stdin. The stream is typically generated by **zfs send**. Full streams create new datasets; incremental streams update existing ones.\n\nFor incremental receives, the destination must have the base snapshot. The -F flag forces rollback to enable the receive. Replication streams (-R from send) can destroy extra snapshots at destination.\n\nProperties can be overridden with -o or inherited with -x during receive. This is useful for setting different mount points or compression on the destination.\n\nThe -s flag enables resumable receives, allowing interrupted transfers to continue from where they stopped."],["CAVEATS","Incremental stream requires base snapshot at destination. Force (-F) destroys intermediate snapshots. Encrypted raw streams must be received as-is. Property overrides apply to entire subtree for replication streams."],["HISTORY","**zfs receive** has been integral to ZFS since Solaris 10, enabling ZFS replication workflows. OpenZFS enhanced it with resumable receives, property overrides, and better handling of encrypted datasets."],["INSTALL","```apt: sudo apt install zfs-fuse```\n\n```apk: sudo apk add zfs```\n\n```nix: nix profile install nixpkgs#zfs```"],["SEE ALSO","[zfs-send](/man/zfs-send)(8), [zfs](/man/zfs)(8), [zpool](/man/zpool)(8)"]],"e":["zfs-send","zfs","zpool"],"x":"zfs receive [pool/dataset] < [backup.zfs]"}});
