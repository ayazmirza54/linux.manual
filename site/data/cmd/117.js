window.LCL_BUCKET(117,{"addcomputer.py":{"n":"addcomputer.py","t":"Create machine accounts in Active Directory","s":[["TAGLINE","Create machine accounts in Active Directory"],["TLDR","**Add** computer to Active Directory\n\n```addcomputer.py [domain]/[user]:[password] -computer-name [hostname]```\n\nSpecify **domain controller** IP\n\n```addcomputer.py [domain]/[user]:[password] -dc-ip [192.168.1.1] -computer-name [hostname]```\n\nUse **pass-the-hash** authentication\n\n```addcomputer.py [domain]/[user] -hashes :[NTHASH] -computer-name [hostname]```\n\nAdd computer with a **specific password** using LDAP method\n\n```addcomputer.py [domain]/[user]:[password] -computer-name [hostname] -computer-pass [password] -method LDAPS```"],["SYNOPSIS","**addcomputer.py** _domain_/_user_:_password_ -computer-name _name_ [_options_]"],["DESCRIPTION","**addcomputer.py** (also known as add-computer.py) is an Impacket script for creating machine accounts in Active Directory. It leverages the default AD permission that allows any authenticated user to add computer accounts to the domain.\n\nThis capability is useful in penetration testing for setting up resource-based constrained delegation attacks or gaining additional footholds in the domain."],["PARAMETERS","**-dc-ip** _ip_\n> IP address of the domain controller\n\n**-computer-name** _name_\n> Name of the computer account to create\n\n**-computer-pass** _password_\n> Password for the computer account\n\n**-method** _SAMR|LDAPS_\n> Protocol to use for adding the computer (default: SAMR)\n\n**-hashes** _LMHASH:NTHASH_\n> Authenticate using NT hash\n\n**-k**, **--kerberos**\n> Use Kerberos authentication\n\n**-no-pass**\n> Don't prompt for password\n\n**-dc-host** _hostname_\n> Hostname of the domain controller"],["CAVEATS","For authorized security testing only. The default ms-DS-MachineAccountQuota of 10 can be modified or set to 0 by administrators. Computer accounts created this way are visible to domain administrators."],["HISTORY","Part of the Impacket suite maintained by SecureAuth, this tool gained prominence in security testing as resource-based constrained delegation attacks became more widely understood after **2018**."],["INSTALL","```apk: sudo apk add py3-impacket```"],["SEE ALSO","[add-computer.py](/man/add-computer.py)(1), [impacket-getnpusers](/man/impacket-getnpusers)(1)"]],"e":["add-computer.py","impacket-getnpusers"],"x":"addcomputer.py [domain]/[user]:[password] -computer-name [hostname]"},"ante":{"n":"ante","t":"Self-contained terminal coding agent with multi-provider and offline support","s":[["TAGLINE","Self-contained terminal coding agent with multi-provider and offline support"],["TLDR","**Launch interactive terminal UI**\n\n```ante```\n\n**Run a one-shot headless task**\n\n```ante -p \"[add error handling to src/main.rs]\"```\n\n**Pipe a diff for review**\n\n```git diff | ante -p \"[review this for security issues]\"```\n\n**Use a specific provider and model**\n\n```ante --provider [openai] --model [gpt-5.5] -p \"[refactor the database module]\"```\n\n**Run fully offline with a local GGUF model**\n\n```ante --offline-model [~/.ante/models/model.gguf] -p \"[add tests]\"```\n\n**Resume a saved session**\n\n```ante --resume [ses_...] -p \"[now add tests]\"```\n\n**Update to the latest release**\n\n```ante update```"],["SYNOPSIS","**ante** [_options_] [**-p** _prompt_]\n\n**ante** **serve**\n\n**ante** **gateway**\n\n**ante** **update** [_options_]"],["PARAMETERS","**-p**, **--prompt** _PROMPT_\n> Run in headless mode with the given prompt and exit when done\n\n**-m**, **--model** _MODEL_\n> Override the model name\n\n**--provider** _PROVIDER_\n> Override the API provider (for example anthropic, openai, gemini, xai, openrouter, local)\n\n**--profile** _NAME_\n> Use **~/.ante/**_NAME_**.settings.json** instead of the default settings; **ANTE_PROFILE** is equivalent\n\n**--effort** _LEVEL_\n> Override model effort: min, low, medium, high, xhigh, or max\n\n**--yolo**\n> Skip all interactive tool approval prompts\n\n**--output-format** _FORMAT_\n> Headless output format: json, human, or minimal (default: minimal)\n\n**--offline-model** _PATH_\n> Path to a local GGUF model file; boots built-in local inference and uses the local provider\n\n**-r**, **--resume** _SESSION_ID_\n> Resume a previously saved session by ID\n\n**--no-session-save**\n> Do not persist a transcript or resumable snapshot\n\n**--check**\n> After the main task, run a verification pass on the work\n\n**--system-prompt** _PROMPT_\n> Replace the default system prompt entirely\n\n**--system-prompt-file** _PATH_\n> Read a replacement system prompt from a UTF-8 file\n\n**--append-system-prompt** _TEXT_\n> Append text to the system prompt\n\n**--no-skills**\n> Skip skill discovery; no skills are advertised or invocable\n\n**--tools** _TOOLS_...\n> Replace the default tool set with exactly these tools\n\n**--include-tools** _TOOLS_...\n> Add tools on top of the default or **--tools** set\n\n**--exclude-tools** _TOOLS_...\n> Remove tools after **--tools** and **--include-tools**"],["COMMANDS","**serve**\n> Run as a long-lived daemon over a structured JSONL protocol for editor plugins and integrations\n\n**gateway**\n> Run Ante as a Slack or Discord bot\n\n**update**\n> Install the latest release; supports **--channel** and **--version**"],["DESCRIPTION","**ante** is a self-contained coding agent from Antigma Labs that runs in the terminal. It is distributed as a single Rust binary (~15MB) with no runtime language dependencies. Day-to-day use is an interactive TUI (**ante** with no arguments); headless one-shots use **-p** / **--prompt**.\n\nIt works with many cloud providers (Anthropic, OpenAI, Gemini, Grok/xAI, Open Router, and OpenAI-compatible endpoints) via API keys or OAuth, and can also run fully offline against a local GGUF model through a built-in inference engine. Configuration lives under **~/.ante/** (settings, catalog, and optional named profiles).\n\nSubcommands cover programmatic embedding (**ante serve**), chat-platform bots (**ante gateway**), and self-updates (**ante update**). Features include multi-agent orchestration, skills, MCP servers, and persistent memory across sessions."],["CAVEATS","Ante is in alpha/preview: expect breaking changes and incomplete functionality. Official support is macOS and Linux (Windows users are directed to WSL). Headless mode implies auto-approval of tool calls (yolo). Cloud providers need credentials (for example **ANTHROPIC_API_KEY**, **OPENAI_API_KEY**); offline mode needs a local GGUF. The prebuilt binary is free to use during the alpha under separate binary terms; open-source parts of the repository are Apache-2.0. Install via the official script (**curl -fsSL https://ante.run/install.sh | bash**); distro packages named **ante** are typically a different project (the Ante programming language)."],["CONFIGURATION","Default settings: **~/.ante/settings.json**\n\nNamed profiles: **~/.ante/**_name_**.settings.json** (select with **--profile** or **ANTE_PROFILE**)\n\nProvider/model catalog overrides: **~/.ante/catalog.json**\n\nBinary install location defaults to **~/.ante/bin** (override with **ANTE_INSTALL_DIR** during install)"],["SEE ALSO","[claude](/man/claude)(1), [codex](/man/codex)(1), [opencode](/man/opencode)(1), [aider](/man/aider)(1), [goose](/man/goose)(1)"],["RESOURCES","```[Source code](https://github.com/AntigmaLabs/ante)```\n\n```[Homepage](https://antigma.ai)```\n\n```[Documentation](https://docs.antigma.ai)```"]],"e":["claude","codex","opencode","aider","goose"],"x":"ante"},"bluetooth-sendto":{"n":"bluetooth-sendto","t":"Send files over Bluetooth via a graphical dialog","s":[["TAGLINE","Send files over Bluetooth via a graphical dialog"],["TLDR","**Open file transfer dialog to select device and files**\n\n```bluetooth-sendto```\n\n**Send a file to a specific Bluetooth device**\n\n```bluetooth-sendto --device=[XX:XX:XX:XX:XX:XX] [path/to/file]```\n\n**Send a file to a device with a specified name**\n\n```bluetooth-sendto --device=[XX:XX:XX:XX:XX:XX] --name=\"[Device Name]\" [path/to/file]```\n\n**Send multiple files to a device**\n\n```bluetooth-sendto --device=[XX:XX:XX:XX:XX:XX] [file1] [file2] [file3]```"],["SYNOPSIS","**bluetooth-sendto** [**--device=**_XX:XX:XX:XX:XX:XX_] [**--name=**_NAME_] [_file..._]"],["DESCRIPTION","**bluetooth-sendto** is a GTK application for transferring files over Bluetooth. It is part of the **gnome-bluetooth** package and provides a graphical dialog for selecting devices and files to transfer.\n\nWhen run without arguments, it displays a file chooser and device selection dialog. When a device address is specified, it connects directly to that device. Multiple files can be sent in a single operation."],["PARAMETERS","**--device=**_XX:XX:XX:XX:XX:XX_\n> Specify the Bluetooth MAC address of the target device. If omitted, a device chooser dialog is displayed\n\n**--name=**_NAME_\n> Specify the device name. If omitted, the name is auto-detected from the device address\n\n**file...**\n> One or more files to send. If omitted, a file chooser dialog is displayed"],["CAVEATS","Requires Bluetooth to be enabled and the target device to be paired and within range. The receiving device must accept the incoming file transfer request. Part of the GNOME Bluetooth stack, so a GTK environment is required."],["INSTALL","```apt: sudo apt install gnome-bluetooth-sendto```\n\n```dnf: sudo dnf install gnome-bluetooth```\n\n```apk: sudo apk add gnome-bluetooth```\n\n```zypper: sudo zypper install gnome-bluetooth```\n\n```nix: nix profile install nixpkgs#gnome-bluetooth```"],["SEE ALSO","[bluetoothctl](/man/bluetoothctl)(1)"],["RESOURCES","```[Source code](https://gitlab.gnome.org/GNOME/gnome-bluetooth)```\n\n```[Documentation](https://manpages.debian.org/testing/gnome-bluetooth/bluetooth-sendto.1.en.html)```"]],"e":["bluetoothctl"],"x":"bluetooth-sendto"},"cdrecord":{"n":"cdrecord","t":"burn data to CD, DVD, and Blu-ray discs","s":[["TAGLINE","burn data to CD, DVD, and Blu-ray discs"],["TLDR","**Burn an ISO image** to CD/DVD\n\n```cdrecord -v dev=[/dev/sr0] [image.iso]```\n\n**Burn with specific speed**\n\n```cdrecord -v speed=[16] dev=[/dev/sr0] [image.iso]```\n\n**Burn audio CD** from WAV files\n\n```cdrecord -v -audio dev=[/dev/sr0] [track1.wav] [track2.wav]```\n\n**Scan for available devices**\n\n```cdrecord -scanbus```\n\n**Erase a CD-RW**\n\n```cdrecord -v blank=fast dev=[/dev/sr0]```\n\n**Burn multi-session disc**\n\n```cdrecord -v -multi dev=[/dev/sr0] [image.iso]```\n\n**Test burn** without writing (simulation)\n\n```cdrecord -v -dummy dev=[/dev/sr0] [image.iso]```\n\n**Burn with buffer underrun protection**\n\n```cdrecord -v driveropts=burnfree dev=[/dev/sr0] [image.iso]```"],["SYNOPSIS","**cdrecord** [_options_] _dev=device_ [_track-options_] _track_..."],["DESCRIPTION","**cdrecord** is a command-line tool for burning data to CD, DVD, and Blu-ray discs. It supports various disc formats including CD-R, CD-RW, DVD-R, DVD+R, DVD-RW, DVD+RW, and dual-layer variants.\n\nThe tool can write ISO images, audio CDs from WAV files, and multi-session discs. It includes features like burn-free (buffer underrun protection), FIFO buffering, and SCSI/ATAPI device support.\n\nNote that on many Linux systems, cdrecord has been replaced or supplemented by wodim from the cdrkit project, which provides compatible functionality."],["PARAMETERS","**dev=** _device_\n> Recording device (e.g., /dev/sr0 or SCSI address).\n\n**speed=** _n_\n> Recording speed multiplier.\n\n**-v**\n> Verbose output.\n\n**-audio**\n> Write audio CD from WAV files.\n\n**-data**\n> Write data track (default).\n\n**-multi**\n> Enable multi-session recording.\n\n**-dummy**\n> Simulation mode, don't actually write.\n\n**-eject**\n> Eject disc after recording.\n\n**blank=** _type_\n> Blank a CD-RW (fast, all, session).\n\n**-scanbus**\n> Scan for available SCSI devices.\n\n**-toc**\n> Show table of contents.\n\n**-dao**\n> Write in Disc At Once mode with no gaps between tracks.\n\n**-checkdrive**\n> Check drive capabilities.\n\n**-immed**\n> Set SCSI IMMED flag for background operation during load/eject/blank.\n\n**driveropts=** _opts_\n> Driver-specific options (e.g., burnfree for buffer underrun protection).\n\n**fs=** _size_\n> FIFO buffer size (default 4 MB).\n\n**gracetime=** _secs_\n> Wait time in seconds before starting burn (default 2)."],["CAVEATS","Requires write access to the optical device (usually root or cdrom group membership). Buffer underruns can ruin discs without burnfree support. CD-R discs cannot be erased. Audio CDs require specific WAV format (44.1kHz, 16-bit, stereo). Device naming varies by system."],["HISTORY","**cdrecord** was developed by **J\\u00f6rg Schilling** starting in **1995**. It became the standard Unix CD burning tool. In **2006**, licensing disputes led to the creation of **cdrkit**, a fork that includes **wodim** as a cdrecord replacement. Most Linux distributions switched to cdrkit/wodim, though some still include original cdrecord. The tools remain largely command-compatible."],["INSTALL","```apt: sudo apt install wodim```\n\n```dnf: sudo dnf install wodim```\n\n```apk: sudo apk add cdrkit```\n\n```zypper: sudo zypper install cdrecord```\n\n```nix: nix profile install nixpkgs#cdrkit```"],["SEE ALSO","[wodim](/man/wodim)(1), [genisoimage](/man/genisoimage)(1), [mkisofs](/man/mkisofs)(1), [growisofs](/man/growisofs)(1)"]],"e":["wodim","genisoimage","mkisofs","growisofs"],"x":"cdrecord -v dev=[/dev/sr0] [image.iso]"},"chroma":{"n":"chroma","t":"general purpose syntax highlighter","s":[["TAGLINE","general purpose syntax highlighter"],["TLDR","**Highlight source file**\n\n```chroma [file.go]```\n\n**Specify language**\n\n```chroma -l python [file.py]```\n\n**HTML output**\n\n```chroma --html [file.js] > [output.html]```\n\n**List available lexers**\n\n```chroma --list```\n\n**Use specific style**\n\n```chroma -s monokai [file.rb]```\n\n**HTML with inline styles**\n\n```chroma --html --html-inline-styles [file.c]```"],["SYNOPSIS","**chroma** [_options_] [_file_...]"],["DESCRIPTION","**chroma** is a general-purpose syntax highlighter supporting approximately 250 languages. It converts source code to syntax-highlighted HTML, ANSI-colored terminal output, SVG, and other formats.\n\nThe tool is based on the Pygments highlighting engine but reimplemented in Go for speed and easy embedding. It supports numerous color styles (monokai, dracula, github, etc.) and can autodetect the language from filenames and content. Chroma is also available as a Go library, used by tools like Hugo for code highlighting."],["PARAMETERS","**-l**, **--lexer** _name_\n> Lexer to use (default: autodetect)\n\n**-s**, **--style** _name_\n> Highlighting style\n\n**-f**, **--formatter** _name_\n> Output formatter: terminal, terminal256, terminal16m, html\n\n**--html**\n> Enable HTML mode\n\n**--html-only**\n> Output HTML fragment only\n\n**--html-inline-styles**\n> Inline CSS styles (no classes)\n\n**--list**\n> List lexers, styles, formatters\n\n**--fail**\n> Exit with status 1 if lexer not found"],["USAGE WITH LESS","```bash\nexport LESSOPEN='| chroma --fail \"$1\" || cat \"$1\"; \"%s\"'\n```"],["CAVEATS","Autodetects language from filename and content. Use --fail for fallback pipelines."],["INSTALL","```apt: sudo apt install golang-chroma```\n\n```brew: brew install chroma```\n\n```nix: nix profile install nixpkgs#chroma```"],["SEE ALSO","[highlight](/man/highlight)(1), [pygmentize](/man/pygmentize)(1), [bat](/man/bat)(1)"],["RESOURCES","```[Source code](https://github.com/alecthomas/chroma)```"]],"e":["highlight","pygmentize","bat"],"x":"chroma [file.go]"},"corepack":{"n":"corepack","t":"Node.js package manager manager","s":[["TAGLINE","Node.js package manager manager"],["TLDR","**Enable Corepack**\n\n```corepack enable```\n\n**Disable Corepack**\n\n```corepack disable```\n\n**Prepare specific package manager version**\n\n```corepack prepare [yarn@4.0.0] --activate```\n\n**Update package.json with package manager**\n\n```corepack use [pnpm@9.x]```\n\n**Install from downloaded archive**\n\n```corepack hydrate [./corepack.tgz]```\n\n**Pack package manager for offline use**\n\n```corepack pack [yarn@4.0.0]```"],["SYNOPSIS","**corepack** _command_ [_arguments_]"],["DESCRIPTION","**corepack** is Node.js's package manager manager. It transparently manages Yarn, npm, and pnpm versions per project, using the \"packageManager\" field in package.json. This ensures that each project uses its specified package manager version without requiring global installations or manual version switching.\n\nIncluded with Node.js from version 14.19.0 to 24.x, Corepack acts as a shim that intercepts package manager commands and automatically downloads and uses the correct version specified in the project's configuration. This eliminates \"works on my machine\" issues caused by package manager version mismatches across development teams.\n\nThe tool maintains a local cache of package manager versions and can operate in offline mode using packed archives. Zero runtime dependencies make it lightweight and reliable for CI/CD environments."],["COMMANDS","**enable**\n> Install shims for package managers in PATH\n\n**disable**\n> Remove shims, restore direct access\n\n**prepare**\n> Download and cache package manager version\n\n**use**\n> Update package.json with packageManager field\n\n**hydrate**\n> Install package manager from archive\n\n**pack**\n> Create archive for offline installation"],["PARAMETERS","**--activate**\n> Set as default after preparing"],["PACKAGE.JSON INTEGRATION","```json\n{\n  \"packageManager\": \"pnpm@9.0.0\"\n}\n```\n\nCorepack reads this field and ensures the correct version is used."],["ENVIRONMENT","**COREPACK_HOME**\n> Cache directory for package managers\n\n**COREPACK_ENABLE_DOWNLOAD_PROMPT**\n> Prompt before downloading\n\n**COREPACK_ENV_FILE**\n> Custom env file path (set to 0 to disable)"],["OFFLINE USAGE","```\ncorepack pack yarn@4.0.0           # Create archive\ncorepack hydrate ./yarn.tgz        # Install from archive\n```"],["CAVEATS","Must be enabled before use. Respects packageManager in nearest package.json. Zero runtime dependencies."],["INSTALL","```pacman: sudo pacman -S corepack```\n\n```brew: brew install corepack```\n\n```nix: nix profile install nixpkgs#corepack```"],["SEE ALSO","[npm](/man/npm)(1), [yarn](/man/yarn)(1), [pnpm](/man/pnpm)(1), [node](/man/node)(1)"],["RESOURCES","```[Source code](https://github.com/nodejs/corepack)```"]],"e":["npm","yarn","pnpm","node"],"x":"corepack enable"},"ecryptfs-find":{"n":"ecryptfs-find","t":"match eCryptfs encrypted filenames to their decrypted counterparts","s":[["TAGLINE","match eCryptfs encrypted filenames to their decrypted counterparts"],["TLDR","**Find the encrypted form** of a decrypted filename\n\n```ecryptfs-find [cleartext-filename]```\n\n**Find the decrypted form** of an encrypted filename\n\n```ecryptfs-find [ECRYPTFS_FNEK_ENCRYPTED.fwBGx...]```"],["SYNOPSIS","**ecryptfs-find** _filename_"],["PARAMETERS","_FILENAME_\n> A cleartext or eCryptfs-encrypted filename to look up. The tool resolves the corresponding counterpart using inode numbers."],["DESCRIPTION","**ecryptfs-find** uses inode numbers to match encrypted filenames to their decrypted counterparts, and decrypted filenames to their encrypted counterparts. It calls **ls**(1) to determine the inode and **find**(1) to locate the inode on the filesystem.\n\nThe eCryptfs filesystem must be mounted for the command to function, since it needs both the encrypted and decrypted views of the directory to perform the match.\n\nThis is primarily a diagnostic tool for resolving filename mappings in eCryptfs deployments."],["CAVEATS","The eCryptfs filesystem must be mounted before running this command. The tool does not decrypt file contents, only filename mappings. Requires read access to the directories being searched."],["HISTORY","ecryptfs-find is part of the **ecryptfs-utils** package. eCryptfs is a POSIX-compliant stacked cryptographic filesystem included in the Linux kernel since version **2.6.19** (released **2006**)."],["INSTALL","```apt: sudo apt install ecryptfs-utils```\n\n```dnf: sudo dnf install ecryptfs-utils```\n\n```pacman: sudo pacman -S ecryptfs-utils```\n\n```apk: sudo apk add ecryptfs-utils```\n\n```zypper: sudo zypper install ecryptfs-utils```"],["SEE ALSO","[ecryptfs-setup-private](/man/ecryptfs-setup-private)(1), [ecryptfs-verify](/man/ecryptfs-verify)(1), [ecryptfs](/man/ecryptfs)(1), [encfs](/man/encfs)(1), [fscrypt](/man/fscrypt)(1)"]],"e":["ecryptfs-setup-private","ecryptfs-verify","ecryptfs","encfs","fscrypt"],"x":"ecryptfs-find [cleartext-filename]"},"fswatch":{"n":"fswatch","t":"cross-platform file change monitor","s":[["TAGLINE","cross-platform file change monitor"],["TLDR","**Watch directory** for changes\n\n```fswatch [/path/to/directory]```\n\n**Watch and execute command** on change\n\n```fswatch -o [/path/to/directory] | xargs -n1 -I{} [command]```\n\n**Watch with recursive monitoring**\n\n```fswatch -r [/path/to/directory]```\n\n**Watch specific file types**\n\n```fswatch --include \".*\\\\.txt$\" --exclude \".*\" [/path/to/directory]```\n\n**Watch with one event per change**\n\n```fswatch -1 [/path/to/directory]```\n\n**Watch multiple paths**\n\n```fswatch [/path/dir1] [/path/dir2]```\n\n**Verbose output** with timestamps\n\n```fswatch -t [/path/to/directory]```\n\n**Watch with latency** (debounce)\n\n```fswatch -l [2] [/path/to/directory]```"],["SYNOPSIS","**fswatch** [_options_] _paths_..."],["DESCRIPTION","**fswatch** is a cross-platform file change monitor that detects filesystem changes and outputs affected paths. It uses native platform APIs (inotify on Linux, FSEvents on macOS, kqueue on BSD) for efficient monitoring.\n\nThe tool outputs changed file paths to stdout, which can be piped to other commands for automated workflows like rebuilding projects, running tests, or reloading services. It supports recursive monitoring and filtering by file patterns.\n\nfswatch is useful for development workflows, build automation, backup triggers, and any scenario where actions should be triggered by file modifications."],["PARAMETERS","**-o**, **--one-per-batch**\n> Print single event per batch (for counting).\n\n**-r**, **--recursive**\n> Recurse into subdirectories.\n\n**-t**, **--timestamp**\n> Print timestamps with events.\n\n**-l** _seconds_, **--latency=** _seconds_\n> Set latency (debounce period).\n\n**-1**, **--one-event**\n> Exit after first event.\n\n**-0**, **--print0**\n> Separate paths with NUL character.\n\n**--event** _type_\n> Filter by event type.\n\n**--include** _regex_\n> Include only matching files.\n\n**--exclude** _regex_\n> Exclude matching files.\n\n**-m** _monitor_, **--monitor=** _monitor_\n> Use specific monitor backend.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-n**, **--numeric**\n> Print numeric event codes."],["CAVEATS","System limits on inotify watches may affect large directory monitoring (adjustable via /proc/sys/fs/inotify/max_user_watches). Different backends have different capabilities. Latency affects responsiveness vs. batching trade-off."],["HISTORY","**fswatch** was created by **Enrico M. Crisostomo** in **2014** to provide a unified, cross-platform file monitoring solution. It was designed to replace platform-specific tools with a single utility that uses native APIs. The project continues to be maintained and supports Linux, macOS, Windows, and BSD systems."],["INSTALL","```apt: sudo apt install fswatch```\n\n```dnf: sudo dnf install fswatch```\n\n```zypper: sudo zypper install fswatch```\n\n```brew: brew install fswatch```\n\n```nix: nix profile install nixpkgs#fswatch```"],["SEE ALSO","[inotifywait](/man/inotifywait)(1), [entr](/man/entr)(1), [watchexec](/man/watchexec)(1), [watchman](/man/watchman)(1)"],["RESOURCES","```[Source code](https://github.com/emcrisostomo/fswatch)```"]],"e":["inotifywait","entr","watchexec","watchman"],"x":"fswatch [/path/to/directory]"},"fvwm":{"n":"fvwm","t":"highly configurable X11 window manager","s":[["TAGLINE","highly configurable X11 window manager"],["TLDR","**Start FVWM**\n\n```fvwm```\n\n**Start with specific config**\n\n```fvwm -f [~/.fvwm/config]```\n\n**Replace running window manager**\n\n```fvwm --replace```\n\n**Show version**\n\n```fvwm --version```"],["SYNOPSIS","**fvwm** [_options_]"],["PARAMETERS","**-f** _file_\n> Configuration file.\n\n**-c** _command_\n> Execute command on startup.\n\n**-d** _display_\n> X display to use.\n\n**--replace**\n> Replace running window manager.\n\n**-s**\n> Single screen mode.\n\n**-v**, **--version**\n> Show version."],["DESCRIPTION","**FVWM** (F Virtual Window Manager) is a highly configurable window manager for X11. It provides virtual desktops, extensive theming, and a powerful configuration language.\n\nFVWM is known for its flexibility and low resource usage. Configuration is done through a text file with commands for window decoration, menus, keybindings, and module loading."],["CONFIGURATION","**~/.fvwm/config**\n> Main configuration file for window styles, keybindings, menus, and modules."],["CONFIGURATION EXAMPLE","```"],["~/.fvwm/config","Style * BorderWidth 5\nStyle * HandleWidth 5\nStyle * MWMButtons\n\nKey Tab A M WindowList Root c c\n\nMouse 1 R A Menu MainMenu\nMouse 3 R A WindowList\n\nAddToMenu MainMenu \"Main\" Title\n+ \"Terminal\" Exec exec xterm\n+ \"Exit\" Quit\n```"],["CAVEATS","Configuration has learning curve. Documentation can be sparse. FVWM3 is the current major version. Some features require external modules."],["HISTORY","FVWM was derived from TWM by **Robert Nation** in **1993**. The name originally meant \"Feeble Virtual Window Manager\" but the meaning has been changed multiple times. FVWM2 was a major rewrite in 1996, and FVWM3 was released in 2020."],["INSTALL","```dnf: sudo dnf install fvwm```\n\n```apk: sudo apk add fvwm```\n\n```nix: nix profile install nixpkgs#fvwm```"],["SEE ALSO","[openbox](/man/openbox)(1), [i3](/man/i3)(1), [twm](/man/twm)(1)"]],"e":["openbox","i3","twm"],"x":"fvwm"},"geth":{"n":"geth","t":"official Go implementation of Ethereum protocol","s":[["TAGLINE","official Go implementation of Ethereum protocol"],["TLDR","**Start Ethereum node**\n\n```geth```\n\n**Sync mainnet**\n\n```geth --syncmode [snap]```\n\n**Start with console**\n\n```geth console```\n\n**Attach to running** node\n\n```geth attach```\n\n**Create new account**\n\n```geth account new```"],["SYNOPSIS","**geth** [_options_] [_command_]"],["PARAMETERS","_COMMAND_\n> Subcommand: console, attach, account, etc.\n\n**--syncmode** _MODE_\n> Sync mode: full, snap (light mode is deprecated post-Merge).\n\n**--datadir** _PATH_\n> Data directory.\n\n**--http**\n> Enable HTTP RPC.\n\n**--ws**\n> Enable WebSocket RPC.\n\n**console**\n> Interactive JavaScript console.\n\n**attach**\n> Attach to running node.\n\n**account** _CMD_\n> Account management.\n\n**--port** _PORT_\n> Network listening port (default: 30303).\n\n**--maxpeers** _N_\n> Maximum number of network peers.\n\n**--networkid** _ID_\n> Network identifier (1=mainnet, 5=goerli).\n\n**--goerli**\n> Connect to Goerli test network.\n\n**--sepolia**\n> Connect to Sepolia test network.\n\n**--help**\n> Display help information."],["DESCRIPTION","**geth** (go-ethereum) is the official Go implementation of the Ethereum protocol. It runs a full Ethereum node, enabling blockchain synchronization, transaction validation, mining operations, and smart contract deployment.\n\nThe client provides multiple sync modes: \"snap\" for fast syncing with state snapshots and \"full\" for complete blockchain validation. Light sync mode is deprecated and non-functional since the Merge to proof-of-stake. It exposes JSON-RPC APIs over HTTP, WebSocket, and IPC for application integration.\n\ngeth includes an interactive JavaScript console for direct blockchain interaction, account management for creating and managing Ethereum wallets, and support for both mainnet and test networks. It's the most widely used Ethereum client for running production nodes and development environments."],["CAVEATS","Full sync requires significant disk space. Resource intensive. Mainnet sync takes time."],["HISTORY","geth is developed by the **Ethereum Foundation** as the official Go implementation. It was one of the original Ethereum clients and remains the most popular for running nodes."],["INSTALL","```apk: sudo apk add geth```\n\n```brew: brew install geth```"],["SEE ALSO","[solc](/man/solc)(1)"]],"e":["solc"],"x":"geth"},"gh":{"n":"gh","t":"GitHub official command line interface","s":[["TAGLINE","GitHub official command line interface"],["TLDR","**Clone a repository**\n\n```gh repo clone [owner]/[repo]```\n\n**Create a pull request**\n\n```gh pr create --title \"[title]\" --body \"[description]\"```\n\n**List open issues**\n\n```gh issue list```\n\n**View pull request status**\n\n```gh pr status```\n\n**Create a new repo**\n\n```gh repo create [name] --public```\n\n**View and check out a pull request**\n\n```gh pr checkout [pr_number]```\n\n**View workflow run status**\n\n```gh run list```\n\n**Open repo in browser**\n\n```gh browse```"],["SYNOPSIS","**gh** _command_ [_subcommand_] [_flags_]"],["PARAMETERS","**repo**\n> Manage repositories.\n\n**pr**\n> Manage pull requests.\n\n**issue**\n> Manage issues.\n\n**auth**\n> Authenticate with GitHub.\n\n**browse**\n> Open repository in browser.\n\n**gist**\n> Manage gists.\n\n**workflow**\n> Manage GitHub Actions workflows.\n\n**run**\n> View and manage workflow runs.\n\n**release**\n> Manage releases.\n\n**codespace**\n> Manage GitHub Codespaces.\n\n**search**\n> Search repositories, issues, and pull requests across GitHub.\n\n**project**\n> Manage GitHub Projects.\n\n**cache**\n> Manage GitHub Actions caches.\n\n**secret**\n> Manage secrets for GitHub Actions.\n\n**variable**\n> Manage GitHub Actions variables.\n\n**label**\n> Manage issue and PR labels.\n\n**extension**\n> Manage gh CLI extensions.\n\n**api**\n> Make authenticated GitHub API requests.\n\n**status**\n> Display status of relevant issues, pull requests, and notifications.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**gh** is GitHub's official command line tool. It brings GitHub features to the terminal, allowing you to manage repositories, issues, pull requests, and more without leaving your development environment.\n\nThe tool integrates deeply with git workflows, enabling seamless transitions between local development and GitHub collaboration. It supports interactive prompts for complex operations and scripted automation for CI/CD pipelines.\n\ngh provides a unified interface for GitHub's REST and GraphQL APIs with built-in authentication and configuration management."],["CONFIGURATION","**~/.config/gh/config.yml**\n> User configuration including default settings, authentication tokens, and preferences.\n\n**~/.config/gh/hosts.yml**\n> Authentication credentials for GitHub.com and GitHub Enterprise instances."],["CAVEATS","Requires authentication via **gh auth login**. Some features need appropriate repository permissions. API rate limits apply."],["HISTORY","gh was released by **GitHub** in **2020** as the successor to hub. It's written in Go and designed to be the official CLI companion to GitHub, supporting both github.com and GitHub Enterprise."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[git](/man/git)(1), [hub](/man/hub)(1), [gh-auth](/man/gh-auth)(1), [gh-pr](/man/gh-pr)(1), [gh-issue](/man/gh-issue)(1), [gh-repo](/man/gh-repo)(1), [gh-run](/man/gh-run)(1), [gh-release](/man/gh-release)(1), [gh-copilot](/man/gh-copilot)(1)"]],"e":["git","hub","gh-auth","gh-pr","gh-issue","gh-repo","gh-run","gh-release","gh-copilot"],"x":"gh repo clone [owner]/[repo]"},"git-continue":{"n":"git-continue","t":"Resume the in-progress rebase, merge, cherry-pick, or revert","s":[["TAGLINE","Resume the in-progress rebase, merge, cherry-pick, or revert"],["TLDR","**Continue whichever operation is currently paused**\n\n```git continue```"],["SYNOPSIS","**git continue**"],["DESCRIPTION","**git continue** is a **git-extras** command that resumes whichever operation Git currently has paused, without you needing to remember or look up the exact command.\n\nIt checks the repository's git-dir for the state files Git leaves behind mid-operation (**CHERRY_PICK_HEAD**, **MERGE_HEAD**, **REBASE_HEAD**, **REVERT_HEAD**) to work out whether a cherry-pick, merge, rebase, or revert is in progress, then runs the matching **--continue** for it, e.g. **git rebase --continue**.\n\nThe same script also implements **git-abort** (installed as a second name pointing at the same file): it picks its behavior by inspecting its own invoked name, running **--abort** instead of **--continue** for whichever operation is detected."],["CAVEATS","Errors out with \"No active operation found\" if nothing is paused, and lists all of them if it somehow detects more than one. Conflicts must still be resolved and staged yourself before continuing; this command only saves you from typing the operation-specific continue command. Requires **git-extras** to be installed."],["HISTORY","git continue is part of **git-extras**, the community-maintained collection of Git utility scripts."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-abort](/man/git-abort)(1), [git-rebase](/man/git-rebase)(1), [git-merge](/man/git-merge)(1), [git-cherry-pick](/man/git-cherry-pick)(1), [git-revert](/man/git-revert)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-continue)```"]],"e":["git-abort","git-rebase","git-merge","git-cherry-pick","git-revert","git-extras"],"x":"git continue"},"gitweb":{"n":"gitweb","t":"Web interface for browsing Git repositories","s":[["TAGLINE","Web interface for browsing Git repositories"],["TLDR","**Start a quick local gitweb** server with git instaweb\n\n```git instaweb --httpd=[webrick]```\n\n**Stop the instaweb** server\n\n```git instaweb --stop```\n\n**Configure gitweb** project root\n\n```git config --global instaweb.httpd [webrick]```"],["SYNOPSIS","**gitweb** _cgi_"],["DESCRIPTION","**gitweb** is a web interface for Git repositories, implemented as a Perl CGI script. It provides browsing of repository contents, commit history, branches, tags, diffs, blame annotations, and search functionality through a web browser.\n\n**gitweb** is typically deployed behind a web server (Apache, nginx, lighttpd) for shared repository browsing. For quick local browsing, use **git instaweb** which starts a temporary web server automatically. The configuration file uses Perl syntax to set variables controlling display and behavior."],["CONFIGURATION","**/etc/gitweb.conf**\n> System-wide configuration file specifying project root, site name, and display options.\n\n**gitweb_config.perl**\n> Per-instance configuration file (in same directory as gitweb CGI script), takes precedence over /etc/gitweb.conf."],["CAVEATS","CGI script, requires a web server for production use. No authentication built-in; rely on web server authentication. Read-only interface; cannot push or modify repositories."],["HISTORY","gitweb is part of the **Git** distribution, providing web repository browsing since Git's early versions. It is the predecessor to modern git hosting interfaces like Gitea and GitLab."],["INSTALL","```dnf: sudo dnf install gitweb```\n\n```nix: nix profile install nixpkgs#gitweb```"],["SEE ALSO","[git-instaweb](/man/git-instaweb)(1), [gitea](/man/gitea)(1)"]],"e":["git-instaweb","gitea"],"x":"git instaweb --httpd=[webrick]"},"gotp":{"n":"gotp","t":"TOTP/MFA authenticator for command line","s":[["TAGLINE","TOTP/MFA authenticator for command line"],["TLDR","**Show** current TOTP code\n\n```gotp show [account-name]```\n\n**Add** new TOTP account\n\n```gotp add [name] [secret-key]```\n\n**Generate** QR code for setup\n\n```gotp qr [account-name]```\n\n**List** all accounts\n\n```gotp list```"],["SYNOPSIS","**gotp** [_options_] _command_"],["PARAMETERS","**show** _NAME_\n> Show current TOTP code for account\n\n**add** _NAME_ _SECRET_\n> Add new TOTP account\n\n**remove** _NAME_\n> Remove account\n\n**list**\n> List all accounts\n\n**qr** _NAME_\n> Generate QR code for account\n\n**export**\n> Export accounts (encrypted)\n\n**import** _FILE_\n> Import accounts from file\n\n**-c, --config** _FILE_\n> Configuration file path\n\n**--clip**\n> Copy code to clipboard\n\n**-v, --version**\n> Display version and exit\n\n**-h, --help**\n> Display help and exit"],["DESCRIPTION","**gotp** is a command-line TOTP (Time-based One-Time Password) authenticator. It stores and generates 2FA/MFA codes for accounts without requiring a phone or browser extension.\n\nThe tool encrypts stored secrets and can generate QR codes for easy mobile app setup."],["SECURITY","Secrets are encrypted at rest using a master password. Clipboard integration for secure code copying. No network access required after setup."],["CAVEATS","System clock must be accurate for valid codes. Secrets are only as secure as the encryption password. Backup codes recommended for account recovery."],["HISTORY","**gotp** was created for users preferring terminal-based 2FA management or needing MFA access on headless servers."],["INSTALL","```aur: yay -S gotp```"],["SEE ALSO","[oathtool](/man/oathtool)(1), [pass](/man/pass)(1), [op](/man/op)(1)"]],"e":["oathtool","pass","op"],"x":"gotp show [account-name]"},"hatch":{"n":"hatch","t":"modern Python project manager","s":[["TAGLINE","modern Python project manager"],["TLDR","**Create new project**\n\n```hatch new [project-name]```\n\n**Build package**\n\n```hatch build```\n\n**Run tests**\n\n```hatch run test```\n\n**Enter environment**\n\n```hatch shell```\n\n**Publish to PyPI**\n\n```hatch publish```\n\n**Show or bump project version**\n\n```hatch version```"],["SYNOPSIS","**hatch** [_options_] _command_ [_args_]"],["PARAMETERS","**new** _name_\n> Create new project.\n\n**build**\n> Build distributions.\n\n**publish**\n> Publish to package index.\n\n**run** _cmd_\n> Run command in environment.\n\n**shell**\n> Enter project environment.\n\n**env** _subcommand_\n> Manage environments.\n\n**test**\n> Run tests across environments.\n\n**fmt**\n> Format code.\n\n**version**\n> Show/bump version.\n\n**clean**\n> Remove build artifacts."],["DESCRIPTION","**Hatch** is a modern, extensible Python project manager. It handles project creation, building, publishing, environment management, and testing in a unified tool. It is the official build backend recommended by the Python Packaging Authority (PyPA).\n\nHatch uses pyproject.toml for configuration and supports current PEP standards. It provides reproducible environments and multi-environment testing via configurable matrices."],["PYPROJECT.TOML","```toml\n[build-system]\nrequires = [\"hatchling\"]\nbuild-backend = \"hatchling.build\"\n\n[project]\nname = \"myproject\"\nversion = \"0.1.0\"\n\n[tool.hatch.envs.default]\ndependencies = [\"pytest\"]\n\n[tool.hatch.envs.default.scripts]\ntest = \"pytest {args}\"\n```"],["CAVEATS","Relatively new; ecosystem still growing. Different workflow from pip/setuptools. Environment management differs from virtualenv. Some plugins may be needed."],["HISTORY","Hatch was created by **Ofek Lev** as a modern replacement for traditional Python packaging tools. It follows current PEP standards and aims to simplify the Python project lifecycle."],["INSTALL","```dnf: sudo dnf install hatch```\n\n```brew: brew install hatch```\n\n```nix: nix profile install nixpkgs#hatch```"],["SEE ALSO","[pip](/man/pip)(1), [poetry](/man/poetry)(1), [flit](/man/flit)(1), [pdm](/man/pdm)(1)"]],"e":["pip","poetry","flit","pdm"],"x":"hatch new [project-name]"},"helm":{"n":"helm","t":"package manager for Kubernetes","s":[["TAGLINE","package manager for Kubernetes"],["TLDR","**Install chart** as a named release\n\n```helm install [release-name] [chart]```\n\n**Install with custom values** file\n\n```helm install [release-name] [chart] -f [values.yaml]```\n\n**Add repository**\n\n```helm repo add [name] [url]```\n\n**Update** repository indexes\n\n```helm repo update```\n\n**List releases** in current namespace\n\n```helm list```\n\n**Upgrade release** to new chart version\n\n```helm upgrade [release-name] [chart]```\n\n**Uninstall release**\n\n```helm uninstall [release-name]```\n\n**Show configurable values** for a chart\n\n```helm show values [chart]```"],["SYNOPSIS","**helm** _command_ [_options_]"],["PARAMETERS","**install** _release_ _chart_\n> Install a chart as a named release.\n\n**upgrade** _release_ _chart_\n> Upgrade a release to a new chart version or values.\n\n**uninstall** _release_\n> Uninstall a release and its resources.\n\n**list**\n> List deployed releases.\n\n**repo add** _name_ _url_\n> Add a chart repository.\n\n**repo update**\n> Update chart repository indexes.\n\n**search repo** _keyword_\n> Search repositories for charts.\n\n**show values** _chart_\n> Show configurable values for a chart.\n\n**template** _release_ _chart_\n> Render chart templates locally without installing.\n\n**rollback** _release_ _revision_\n> Roll back a release to a previous revision.\n\n**-n**, **--namespace** _NS_\n> Kubernetes namespace for the operation.\n\n**-f**, **--values** _FILE_\n> Specify values file for chart configuration.\n\n**--set** _key=value_\n> Set individual values on the command line.\n\n**--dry-run**\n> Simulate an install/upgrade without applying changes.\n\n**--wait**\n> Wait until all resources are ready before marking release as successful."],["DESCRIPTION","**Helm** is the package manager for Kubernetes. It manages charts, which are packages of pre-configured Kubernetes resources. Charts define, install, and upgrade complex Kubernetes applications.\n\nHelm simplifies application deployment, versioning, and upgrades. It uses Go templating to customize deployments for different environments. Values files and **--set** flags override chart defaults for environment-specific configuration."],["CAVEATS","Requires kubectl configured with cluster access. Chart versions should be pinned for reproducible deployments. Helm 3 removed the server-side Tiller component, operating entirely client-side."],["HISTORY","Helm was created by **Deis** and is now a **CNCF** graduated project, becoming the standard for Kubernetes package management."],["INSTALL","```dnf: sudo dnf install helm```\n\n```pacman: sudo pacman -S helm```\n\n```apk: sudo apk add helm```\n\n```zypper: sudo zypper install helm```\n\n```brew: brew install helm```\n\n```nix: nix profile install nixpkgs#helm```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kustomize](/man/kustomize)(1)"]],"e":["kubectl","kustomize"],"x":"helm install [release-name] [chart]"},"himalaya":{"n":"himalaya","t":"CLI to manage emails","s":[["TAGLINE","CLI to manage emails"],["TLDR","**List email envelopes** in the inbox\n\n```himalaya envelope list```\n\n**Read a specific email**\n\n```himalaya message read [id]```\n\n**Compose and send a new email**\n\n```himalaya message write```\n\n**Reply to an email**\n\n```himalaya message reply [id]```\n\n**Forward an email**\n\n```himalaya message forward [id]```\n\n**List folders**\n\n```himalaya folder list```\n\n**Download attachments**\n\n```himalaya attachment download [id]```\n\n**Use a specific account**\n\n```himalaya -a [account_name] envelope list```"],["SYNOPSIS","**himalaya** [_options_] _command_ _subcommand_ [_arguments_]"],["DESCRIPTION","**himalaya** is a command-line interface for managing emails. Unlike TUI email clients like **mutt** or **aerc**, himalaya is a pure CLI with no event loop — you interact with your emails using shell commands in a stateless way. This makes it composable with pipes and scripts, and freely integrable with other CLI tools.\n\nIt supports multiple backends including **IMAP**, **SMTP**, **Maildir**, and **Notmuch**, with features like multi-account support, **PGP encryption**, and **OAuth 2.0** authentication. Himalaya is designed to extract email logic into a simple CLI API that can be used directly from the terminal, from scripts, and from UIs."],["PARAMETERS","**-a**, **--account** _name_\n> Use a specific email account.\n\n**envelope list**\n> List email envelopes in the current folder.\n\n**envelope watch**\n> Watch for new envelope changes in real time.\n\n**message read** _id_\n> Read a specific email message.\n\n**message write**\n> Compose a new email message.\n\n**message reply** _id_\n> Reply to an email message.\n\n**message forward** _id_\n> Forward an email message.\n\n**message delete** _id_\n> Delete an email message.\n\n**folder list**\n> List mail folders.\n\n**folder create** _name_\n> Create a new mail folder.\n\n**attachment download** _id_\n> Download attachments from a message."],["CONFIGURATION","Configuration is stored in **$XDG_CONFIG_HOME/himalaya/config.toml** (or **~/.config/himalaya/config.toml**) and defines accounts with backend settings for IMAP, SMTP, Maildir, or Notmuch. Run **himalaya account configure** for an interactive setup wizard."],["CAVEATS","Initial configuration requires manual setup of mail accounts. OAuth 2.0 setup may require additional steps depending on the provider. PGP encryption requires GPG to be installed and configured."],["HISTORY","**himalaya** was created by **Clément DOUIN** as part of the **Pimalaya** project and is written in **Rust**. It was designed as a Unix-philosophy approach to email, providing simple composable commands rather than a monolithic TUI application."],["INSTALL","```pacman: sudo pacman -S himalaya```\n\n```apk: sudo apk add himalaya```\n\n```zypper: sudo zypper install himalaya```\n\n```brew: brew install himalaya```\n\n```nix: nix profile install nixpkgs#himalaya```"],["SEE ALSO","[mutt](/man/mutt)(1), [aerc](/man/aerc)(1), [neomutt](/man/neomutt)(1), [mail](/man/mail)(1)"]],"e":["mutt","aerc","neomutt","mail"],"x":"himalaya envelope list"},"i3exit":{"n":"i3exit","t":"script that provides system power management and session control for the i3","s":[["TAGLINE","script that provides system power management and session control for the i3"],["TLDR","**Log out** of i3\n\n```i3exit logout```\n\n**Lock** the screen\n\n```i3exit lock```\n\n**Suspend** the system\n\n```i3exit suspend```\n\n**Hibernate** the system\n\n```i3exit hibernate```\n\n**Reboot** the system\n\n```i3exit reboot```\n\n**Shut down** the system\n\n```i3exit shutdown```\n\n**Switch user** (return to login screen)\n\n```i3exit switch_user```"],["SYNOPSIS","**i3exit** _command_"],["PARAMETERS","**logout**\n> Exit i3 and return to display manager\n\n**lock**\n> Lock the screen\n\n**suspend**\n> Suspend the system to RAM\n\n**hibernate**\n> Hibernate the system to disk\n\n**reboot**\n> Reboot the system\n\n**shutdown**\n> Power off the system\n\n**switch_user**\n> Return to login screen for user switching"],["DESCRIPTION","**i3exit** is a script that provides system power management and session control for the i3 window manager. It wraps common actions like locking, suspending, hibernating, and logging out into a single command.\n\nThe script is typically bound to keyboard shortcuts or used in i3 mode menus, providing consistent behavior for power management operations regardless of the underlying display manager or systemd configuration."],["CAVEATS","Requires appropriate permissions for power management operations. Behavior may vary depending on the display manager and systemd configuration. Some commands may require polkit authentication."],["HISTORY","i3exit was developed for Manjaro Linux's i3 edition to provide a convenient power management interface for i3 users. It abstracts the differences between various power management backends."],["INSTALL","```aur: yay -S i3exit```"],["SEE ALSO","[i3](/man/i3)(1), [systemctl](/man/systemctl)(1), [loginctl](/man/loginctl)(1)"]],"e":["i3","systemctl","loginctl"],"x":"i3exit logout"},"i3lock":{"n":"i3lock","t":"Improved screen locker","s":[["TAGLINE","Improved screen locker"],["TLDR","**Lock the screen with a white background**\n\n```i3lock```\n\n**Lock with a solid color background**\n\n```i3lock -c [000000]```\n\n**Lock with a background image**\n\n```i3lock -i [path/to/image.png]```\n\n**Lock with a tiled background image**\n\n```i3lock -t -i [path/to/image.png]```\n\n**Lock without showing the unlock indicator**\n\n```i3lock -u```\n\n**Lock and show failed authentication attempts**\n\n```i3lock -f```\n\n**Lock without forking to the background**\n\n```i3lock -n```"],["SYNOPSIS","**i3lock** [_options_]"],["PARAMETERS","**-v**, **--version**\n> Display the version of i3lock.\n\n**-n**, **--nofork**\n> Do not fork after starting. Useful for combining with other commands.\n\n**-b**, **--beep**\n> Enable beeping on authentication failure.\n\n**-c** _RRGGBB_, **--color** _RRGGBB_\n> Set background color in 6-byte hex format (default: white).\n\n**-i** _PATH_, **--image** _PATH_\n> Display the given PNG image as background instead of a blank screen.\n\n**-t**, **--tiling**\n> Tile the image across the entire screen.\n\n**-u**, **--no-unlock-indicator**\n> Disable the unlock indicator circle shown when typing.\n\n**-f**, **--show-failed-attempts**\n> Show the number of failed authentication attempts.\n\n**-p** _win|default_, **--pointer** _win|default_\n> Control mouse pointer visibility. \"default\" shows the pointer, \"win\" shows a Windows-style pointer.\n\n**-e**, **--ignore-empty-password**\n> Do not validate an empty password.\n\n**--raw** _FORMAT_\n> Read the image given by --image as raw bytes instead of PNG.\n\n**--debug**\n> Enable debug logging. Note: this logs the password to stdout."],["DESCRIPTION","**i3lock** is an improved screen locker based on slock. It displays a blank screen or image and requires PAM authentication (typically a password) to unlock. After a configurable timeout, the screen turns off via DPMS.\n\nAn unlock indicator circle appears when typing: it shows a green fill while verifying, a red fill on failure, and clears when idle. The indicator provides visual feedback without revealing password length.\n\nThe locker was designed for the i3 window manager but works with any X11 session."],["CAVEATS","X11 only; for Wayland, use **swaylock** instead. Only PNG images are supported (unless using --raw). The **i3lock-color** fork adds many additional features such as custom ring colors and clock display."],["HISTORY","**i3lock** was created by Michael Stapelberg as the screen locker for the **i3** window manager, focusing on simplicity and security. It is a significantly improved version of **slock** with features like an unlock indicator and PAM support."],["INSTALL","```apt: sudo apt install i3lock```\n\n```dnf: sudo dnf install i3lock```\n\n```pacman: sudo pacman -S i3lock```\n\n```apk: sudo apk add i3lock```\n\n```zypper: sudo zypper install i3lock```\n\n```nix: nix profile install nixpkgs#i3lock```"],["SEE ALSO","[i3](/man/i3)(1), [swaylock](/man/swaylock)(1), [xlock](/man/xlock)(1), [xdotool](/man/xdotool)(1)"]],"e":["i3","swaylock","xlock","xdotool"],"x":"i3lock"},"infocmp":{"n":"infocmp","t":"Compare or print out terminfo descriptions","s":[["TAGLINE","Compare or print out terminfo descriptions"],["TLDR","**Show current terminal capabilities**\n\n```infocmp```\n\n**Show a specific terminal type**\n\n```infocmp [xterm-256color]```\n\n**Compare two terminal descriptions** (show differences)\n\n```infocmp -d [xterm] [xterm-256color]```\n\n**Output in terminfo source format** (can be compiled with tic)\n\n```infocmp -I [xterm]```\n\n**Show capabilities one per line**\n\n```infocmp -1 [xterm]```\n\n**Output in termcap format**\n\n```infocmp -C [xterm]```\n\n**Generate relative description** using use= fields\n\n```infocmp -u [xterm-256color] [xterm]```"],["SYNOPSIS","**infocmp** [_options_] [_termname_...]"],["PARAMETERS","_termname_\n> Terminal type(s) to display or compare.\n\n**-d**\n> Show differences between two terminal descriptions.\n\n**-c**\n> Show common capabilities between two terminals.\n\n**-n**\n> Show capabilities common to both, listing those that differ.\n\n**-u**\n> Produce a terminfo source relative to other entries using **use=** fields.\n\n**-I**\n> Output in terminfo source format.\n\n**-C**\n> Output in termcap format.\n\n**-L**\n> Use long C variable names in output.\n\n**-1**\n> Print one capability per line.\n\n**-r**\n> Show resolved paths for terminfo entries.\n\n**-E**\n> Dump capabilities as C initializer tables for a TERMTYPE structure.\n\n**-e**\n> Dump capabilities as a C initializer for a TERMTYPE structure.\n\n**-F**\n> Compare terminfo files, reporting matches and differences between entries.\n\n**-A** _dir_\n> Use the specified directory for the first terminal's terminfo database.\n\n**-B** _dir_\n> Use the specified directory for the second terminal's terminfo database."],["DESCRIPTION","**infocmp** compares or prints out terminal capability descriptions from the terminfo database. It can rewrite descriptions to use the **use=** field for sharing common capabilities, compare binary entries between different terminal types, and output in several formats including terminfo source and termcap.\n\nThe command reads compiled terminfo entries and outputs them in a readable format. It is useful for debugging terminal issues, understanding terminal features, and preparing terminfo source files for compilation with **tic**."],["CAVEATS","Terminfo database location varies by system (commonly **/usr/share/terminfo** or **/usr/lib/terminfo**). Some capabilities may be missing from terminal entries. Modern terminals support many extensions beyond the standard terminfo set. In compare mode (**-d**), only differing capabilities are shown."],["HISTORY","**infocmp** is part of the **ncurses** library, which implements the terminfo database. The terminfo system replaced the older termcap format, providing a more extensible binary format for terminal capabilities."],["INSTALL","```apt: sudo apt install ncurses-bin```\n\n```dnf: sudo dnf install ncurses```\n\n```pacman: sudo pacman -S ncurses```\n\n```apk: sudo apk add ncurses```\n\n```brew: brew install ncurses```\n\n```nix: nix profile install nixpkgs#ncurses```"],["SEE ALSO","[tic](/man/tic)(1), [tput](/man/tput)(1), [toe](/man/toe)(1)"]],"e":["tic","tput","toe"],"x":"infocmp"},"jj-tag-list":{"n":"jj-tag-list","t":"displays all tags in the repository","s":[["TAGLINE","displays all tags in the repository"],["TLDR","**List all tags**\n\n```jj tag list```\n\n**List tags matching a glob pattern**\n\n```jj tag list \"[v*]\"```\n\n**List tags sorted by author date** (descending)\n\n```jj tag list --sort author-date-```\n\n**List tags with a custom template**\n\n```jj tag list -T '[name ++ \"\\n\"]'```"],["SYNOPSIS","**jj** **tag** **list** [_options_] [_pattern_]"],["PARAMETERS","**-T**, **--template** _TEMPLATE_\n> Render output using a custom template.\n\n**--sort** _KEY_\n> Sort by key: name, author-name, author-email, author-date, committer-name, committer-email, committer-date. Suffix with - for descending."],["DESCRIPTION","**jj tag list** displays all tags in the repository. Optionally filter by a glob pattern. Shows the tag name and the commit it references. Multiple sort keys can be specified, with the first key being the most significant. Default sort order can be configured via `ui.tag-list-sort-keys` in the config."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-tag](/man/jj-tag)(1), [jj-tag-set](/man/jj-tag-set)(1), [jj-tag-delete](/man/jj-tag-delete)(1)"]],"e":["jj","jj-tag","jj-tag-set","jj-tag-delete"],"x":"jj tag list"},"llvm-cat":{"n":"llvm-cat","t":"Concatenate LLVM bitcode files into a single bitcode file","s":[["TAGLINE","Concatenate LLVM bitcode files into a single bitcode file"],["TLDR","**Concatenate bitcode files** into a single output\n\n```llvm-cat -o [output.bc] [file1.bc] [file2.bc]```\n\n**Concatenate multiple bitcode files** using a glob\n\n```llvm-cat -o [combined.bc] [*.bc]```\n\n**Produce a module containing embedded modules** (binary bundle)\n\n```llvm-cat -b -o [bundle.bc] [file1.bc] [file2.bc]```"],["SYNOPSIS","**llvm-cat** [_-o_ _output_] [_-b_] _files_..."],["PARAMETERS","**-o** _file_\n> Write output bitcode to _file_ (defaults to standard output).\n\n**-b**\n> Produce a binary bundle. Embeds each input module as a separate module inside the output, instead of linking them together.\n\n**--help**\n> Display help information."],["DESCRIPTION","**llvm-cat** concatenates LLVM bitcode files. By default it produces a single bitcode module that contains the linked contents of all inputs. With **-b**, it produces a bundle in which each input remains a distinct embedded module, useful for tools that operate on multiple modules without merging them.\n\nIt is part of the LLVM toolchain and is typically used in build systems that need to aggregate bitcode prior to linking, optimization, or analysis with **llvm-link**, **opt**, or **llc**."],["INSTALL","```apt: sudo apt install llvm```\n\n```dnf: sudo dnf install llvm```\n\n```pacman: sudo pacman -S llvm```\n\n```apk: sudo apk add llvm```\n\n```zypper: sudo zypper install llvm```\n\n```brew: brew install llvm```\n\n```nix: nix profile install nixpkgs#llvm```"],["SEE ALSO","[llvm-dis](/man/llvm-dis)(1), [llvm-as](/man/llvm-as)(1), [llvm-bcanalyzer](/man/llvm-bcanalyzer)(1)"]],"e":["llvm-dis","llvm-as","llvm-bcanalyzer"],"x":"llvm-cat -o [output.bc] [file1.bc] [file2.bc]"},"lzop":{"n":"lzop","t":"compresses files using the LZO library","s":[["TAGLINE","compresses files using the LZO library"],["TLDR","**Compress file**\n\n```lzop [file]```\n\n**Decompress file**\n\n```lzop -d [file.lzo]```\n\n**Keep original file**\n\n```lzop -k [file]```\n\n**Compress with maximum compression**\n\n```lzop -9 [file]```\n\n**Test archive integrity**\n\n```lzop -t [file.lzo]```\n\n**Compress to stdout**\n\n```lzop -c [file]```\n\n**Compress and delete original file**\n\n```lzop -U [file]```"],["SYNOPSIS","**lzop** [_options_] [_file_...]"],["PARAMETERS","**-d**, **--decompress**\n> Decompress files.\n\n**-k**, **--keep**\n> Keep input files.\n\n**-c**, **--stdout**\n> Write to standard output.\n\n**-t**, **--test**\n> Test archive integrity.\n\n**-1** to **-9**\n> Compression level (default: -3).\n\n**-f**, **--force**\n> Force overwrite of existing files.\n\n**-U**\n> Delete input files after successful compression or decompression.\n\n**-v**, **--verbose**\n> Display the name for each file compressed or decompressed.\n\n**-o** _FILE_, **--output=**_FILE_\n> Write output to the specified file.\n\n**-p** _DIR_, **--path=**_DIR_\n> Write output files into the specified directory.\n\n**-F**, **--no-checksum**\n> Do not store or verify a checksum of the uncompressed data."],["DESCRIPTION","**lzop** compresses files using the LZO library. Emphasizes speed over compression ratio. Very fast compression and decompression, suitable for real-time applications. Files have .lzo extension."],["INSTALL","```apt: sudo apt install lzop```\n\n```dnf: sudo dnf install lzop```\n\n```pacman: sudo pacman -S lzop```\n\n```apk: sudo apk add lzop```\n\n```zypper: sudo zypper install lzop```\n\n```brew: brew install lzop```\n\n```nix: nix profile install nixpkgs#lzop```"],["SEE ALSO","[gzip](/man/gzip)(1), [bzip2](/man/bzip2)(1), [lzip](/man/lzip)(1), [xz](/man/xz)(1), [zstd](/man/zstd)(1)"]],"e":["gzip","bzip2","lzip","xz","zstd"],"x":"lzop [file]"},"mpc":{"n":"mpc","t":"command-line client for MPD","s":[["TAGLINE","command-line client for MPD"],["TLDR","**Show current song**\n\n```mpc```\n\n**Play/pause toggle**\n\n```mpc toggle```\n\n**Next track**\n\n```mpc next```\n\n**Previous track**\n\n```mpc prev```\n\n**Add file to playlist**\n\n```mpc add [path/to/song.mp3]```\n\n**Clear playlist**\n\n```mpc clear```\n\n**Set volume**\n\n```mpc volume [80]```\n\n**Update database**\n\n```mpc update```\n\n**Seek within current song**\n\n```mpc seek [+10]```\n\n**Toggle repeat mode**\n\n```mpc repeat```"],["SYNOPSIS","**mpc** [_options_] [_command_] [_arguments_]"],["PARAMETERS","**-f**, **--format** _FORMAT_\n> Configure song display format using metadata delimiters like %artist%, %album%, %title%, %track%, %time%, %file%.\n\n**-h**, **--host** _HOST_\n> Host to connect to. Use password@host for authentication. An absolute path connects via Unix Domain Socket.\n\n**-p**, **--port** _PORT_\n> Port to connect to (default: MPD_PORT env or 6600).\n\n**-q**, **--quiet**, **--no-status**\n> Suppress current song status after commands.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**--wait**\n> Wait for operation to finish (e.g., database update)."],["COMMANDS","**play** [_position_]\n> Start playback, optionally at a playlist position.\n\n**pause**\n> Pause playback.\n\n**toggle**\n> Toggle play/pause.\n\n**stop**\n> Stop playback.\n\n**next**\n> Skip to next track.\n\n**prev**\n> Go to previous track.\n\n**seek** _value_\n> Seek within current song (e.g., +10, -5, 50%).\n\n**volume** _LEVEL_\n> Set volume percentage (0-100), or relative (+5, -5).\n\n**repeat** [_on|off_]\n> Toggle or set repeat mode.\n\n**random** [_on|off_]\n> Toggle or set random mode.\n\n**shuffle**\n> Shuffle the current playlist.\n\n**crossfade** [_seconds_]\n> Get or set crossfade between songs (0 disables).\n\n**add** _URI_\n> Add a song to the playlist.\n\n**clear**\n> Clear the playlist.\n\n**playlist**\n> Print the current playlist.\n\n**search** _type_ _query_\n> Search for songs in the database.\n\n**update** [_path_]\n> Scan for updated files in the music directory.\n\n**status**\n> Display current playback status."],["DESCRIPTION","**mpc** is a command-line client for MPD (Music Player Daemon). It connects to a running MPD instance and controls playback, playlists, volume, and playback state according to commands and arguments passed to it.\n\nIf no command is given, the current status is printed (same as **mpc status**)."],["CAVEATS","Requires a running MPD instance. The client does not play audio itself. Connection parameters default to localhost:6600 or can be set via MPD_HOST and MPD_PORT environment variables."],["HISTORY","mpc was created as the reference **command-line client** for the Music Player Daemon project."],["INSTALL","```apt: sudo apt install mpc```\n\n```dnf: sudo dnf install mpc```\n\n```pacman: sudo pacman -S mpc```\n\n```apk: sudo apk add mpc```\n\n```brew: brew install mpc```\n\n```nix: nix profile install nixpkgs#mpc```"],["SEE ALSO","[mpd](/man/mpd)(1), [ncmpcpp](/man/ncmpcpp)(1), [mpv](/man/mpv)(1)"]],"e":["mpd","ncmpcpp","mpv"],"x":"mpc"},"musikcube":{"n":"musikcube","t":"Cross-platform terminal-based music player and server","s":[["TAGLINE","Cross-platform terminal-based music player and server"],["TLDR","**Launch the music player TUI**\n\n```musikcube```\n\n**Start the headless audio server daemon**\n\n```musikcubed```"],["SYNOPSIS","**musikcube**\n\n**musikcubed**"],["DESCRIPTION","**musikcube** is a fully functional terminal-based music player, library manager, and streaming audio server written in C++. It runs natively on Windows, macOS, and Linux and provides a TUI for browsing and playing your music library.\n\nThe companion daemon **musikcubed** runs as a headless audio server that can stream music to remote musikcube clients. The library automatically indexes and organizes music files with metadata.\n\nThe interface is keyboard-driven. Press **ESC** to enter command mode, **Ctrl+D** to quit, and **Alt+Enter** to open context menus. Keyboard shortcuts can be customized by editing **~/.musikcube/hotkeys.json**."],["CAVEATS","Requires audio output support on the system. Library indexing may take time for large collections. The application is primarily interactive and does not accept traditional command-line arguments."],["HISTORY","**musikcube** was created by **clangen** as an open-source, cross-platform terminal music player."],["INSTALL","```brew: brew install musikcube```\n\n```nix: nix profile install nixpkgs#musikcube```"],["SEE ALSO","[cmus](/man/cmus)(1), [mpd](/man/mpd)(1), [ncmpcpp](/man/ncmpcpp)(1), [mocp](/man/mocp)(1), [termusic](/man/termusic)(1)"]],"e":["cmus","mpd","ncmpcpp","mocp","termusic"],"x":"musikcube"},"mvn-site":{"n":"mvn-site","t":"generates project documentation website","s":[["TAGLINE","generates project documentation website"],["TLDR","**Generate project site**\n\n```mvn site```\n\n**Generate and deploy site**\n\n```mvn site-deploy```\n\n**Generate site in specific directory**\n\n```mvn site -DoutputDirectory=[docs]```\n\n**Run site with reports**\n\n```mvn site -DgenerateReports=true```\n\n**Stage site locally**\n\n```mvn site:stage```\n\n**Clean and regenerate**\n\n```mvn clean site```"],["SYNOPSIS","**mvn** **site** [_options_]"],["PARAMETERS","**site**\n> Generate project website.\n\n**site-deploy**\n> Deploy site to server.\n\n**site:stage**\n> Stage site locally.\n\n**-DoutputDirectory** _DIR_\n> Output location.\n\n**-DgenerateReports** _BOOL_\n> Include reports.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mvn site** generates project documentation website. It creates HTML from project metadata.\n\nThe goal produces reports, JavaDocs, and info pages. Configured via pom.xml reporting section."],["CAVEATS","Requires site configuration. Heavy dependency download. Plugins control content."],["HISTORY","The Maven Site Plugin is part of **Apache Maven** for generating project documentation sites."],["SEE ALSO","[mvn](/man/mvn)(1), [javadoc](/man/javadoc)(1), [jekyll](/man/jekyll)(1)"]],"e":["mvn","javadoc","jekyll"],"x":"mvn site"},"oc":{"n":"oc","t":"openShift CLI","s":[["TAGLINE","openShift CLI"],["TLDR","**Login to cluster**\n\n```oc login [https://api.cluster:6443]```\n\n**Get resources**\n\n```oc get [pods|services|deployments]```\n\n**Create from YAML**\n\n```oc apply -f [manifest.yaml]```\n\n**Switch project**\n\n```oc project [project_name]```\n\n**View logs**\n\n```oc logs [pod_name]```\n\n**Execute in pod**\n\n```oc exec -it [pod_name] -- [command]```"],["SYNOPSIS","**oc** [_command_] [_options_]"],["PARAMETERS","**login** _URL_\n> Authenticate to cluster.\n\n**get** _RESOURCE_\n> List resources.\n\n**apply** **-f** _FILE_\n> Apply configuration.\n\n**project** _NAME_\n> Switch project/namespace.\n\n**logs** _POD_\n> View pod logs.\n\n**exec** _POD_\n> Execute in pod.\n\n**--help**\n> Display help information."],["DESCRIPTION","**oc** is the OpenShift CLI. Manages OpenShift/Kubernetes clusters.\n\nThe tool extends kubectl with OpenShift features. Primary interface for OpenShift."],["CAVEATS","OpenShift specific extensions. Compatible with kubectl. Requires cluster access."],["HISTORY","oc was developed by **Red Hat** as the command-line interface for OpenShift Container Platform."],["INSTALL","```zypper: sudo zypper install oc```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [docker](/man/docker)(1), [podman](/man/podman)(1)"]],"e":["kubectl","docker","podman"],"x":"oc login [https://api.cluster:6443]"},"openvpn":{"n":"openvpn","t":"open-source VPN solution using SSL/TLS for key exchange","s":[["TAGLINE","open-source VPN solution using SSL/TLS for key exchange"],["TLDR","**Connect using config file**\n\n```sudo openvpn --config [client.ovpn]```\n\n**Connect with authentication file**\n\n```sudo openvpn --config [client.ovpn] --auth-user-pass [credentials.txt]```\n\n**Start as daemon**\n\n```sudo openvpn --config [client.ovpn] --daemon```\n\n**Generate static key**\n\n```openvpn --genkey secret [static.key]```\n\n**Test config without connecting**\n\n```openvpn --config [client.ovpn] --verb 3 --pull```\n\n**Show version and compile options**\n\n```openvpn --version```"],["SYNOPSIS","**openvpn** [_--config file_] [_--remote host_] [_--dev tun_|_tap_] [_--ca file_] [_--cert file_] [_--key file_] [_options_]"],["PARAMETERS","**--config** _file_\n> Read configuration from file.\n\n**--remote** _host_ [_port_]\n> Remote VPN server address.\n\n**--port** _num_\n> Local and remote port number.\n\n**--dev** _type_\n> TUN/TAP device type (tun, tap).\n\n**--proto** _proto_\n> Protocol: udp, tcp-client, tcp-server.\n\n**--ca** _file_\n> Certificate authority file.\n\n**--cert** _file_\n> Local certificate file.\n\n**--key** _file_\n> Local private key file.\n\n**--auth-user-pass** [_file_]\n> Username/password authentication.\n\n**--tls-auth** _file_ [_direction_]\n> HMAC authentication on control channel.\n\n**--cipher** _alg_\n> Encryption cipher.\n\n**--auth** _alg_\n> HMAC digest algorithm.\n\n**--compress** [_alg_]\n> Compression algorithm.\n\n**--daemon** [_name_]\n> Daemonize after initialization.\n\n**--log** _file_\n> Log to file.\n\n**--verb** _level_\n> Verbosity level (0-11).\n\n**--ping** _n_\n> Ping remote every n seconds.\n\n**--ping-restart** _n_\n> Restart if no ping response in n seconds.\n\n**--script-security** _level_\n> Script execution policy."],["DESCRIPTION","**OpenVPN** is an open-source VPN solution using SSL/TLS for key exchange. It can create secure point-to-point or site-to-site connections, operating over UDP or TCP.\n\nTwo modes exist: routed (TUN) and bridged (TAP). TUN mode creates point-to-point IP tunnels, efficient for most VPN needs. TAP mode bridges Ethernet frames, required for non-IP protocols.\n\nConfiguration typically uses .ovpn files containing all connection parameters. These files can embed certificates or reference external files. Commercial VPN providers distribute pre-configured .ovpn files.\n\nSecurity features include: certificate-based authentication, perfect forward secrecy, HMAC packet authentication, and optional username/password verification. TLS-auth adds an additional HMAC layer for DoS protection.\n\nClient and server modes use the same binary. Server configurations include routing, client management, and multi-client handling. Scripts can execute on connection events for dynamic routing and firewall rules."],["CAVEATS","Requires root/administrator for TUN/TAP creation. Firewalls must allow VPN traffic. Certificate management adds complexity. Performance depends on cipher choice and hardware. MTU issues can cause connectivity problems. Some networks block OpenVPN signatures."],["HISTORY","**OpenVPN** was created by **James Yonan** and first released in **2001**. It was designed as an open-source alternative to IPsec, using standard TLS for the control channel. The project has become one of the most widely deployed VPN solutions, used by both commercial providers and enterprises. OpenVPN 3 introduced a new core library in C++."],["INSTALL","```apt: sudo apt install openvpn```\n\n```dnf: sudo dnf install openvpn```\n\n```pacman: sudo pacman -S openvpn```\n\n```apk: sudo apk add openvpn```\n\n```zypper: sudo zypper install openvpn```\n\n```brew: brew install openvpn```\n\n```nix: nix profile install nixpkgs#openvpn```"],["SEE ALSO","[ipsec](/man/ipsec)(8), [stunnel](/man/stunnel)(8), [ssh](/man/ssh)(1)"]],"e":["ipsec","stunnel","ssh"],"x":"sudo openvpn --config [client.ovpn]"},"packemon":{"n":"packemon","t":"TUI tool for generating and monitoring network packets","s":[["TAGLINE","TUI tool for generating and monitoring network packets"],["TLDR","**Launch the packet monitor**\n\n```sudo packemon monitor```\n\n**Launch the packet generator**\n\n```sudo packemon generator```\n\n**Monitor on a specific interface**\n\n```sudo packemon monitor --interface [eth0]```\n\n**List available network interfaces**\n\n```packemon interfaces```\n\n**Show the packemon version**\n\n```packemon version```"],["SYNOPSIS","**packemon** _command_ [_options_]"],["PARAMETERS","**monitor**, **mon**, **m**\n> Monitor packets received and sent on the specified interface.\n\n**generator**, **gen**, **g**\n> Generate and send arbitrary network packets.\n\n**interfaces**, **intfs**, **i**\n> List available network interfaces.\n\n**debugging**, **debug**, **d**\n> Debugging mode intended for development.\n\n**version**, **v**\n> Display version information.\n\n**--interface** _NAME_\n> Network interface to use (default: `eth0`).\n\n**-h**, **--help**\n> Show help."],["DESCRIPTION","**packemon** is a TUI tool for generating custom network packets and monitoring traffic on network interfaces. It supports Ethernet, ARP, IPv4/IPv6, ICMP, TCP, UDP, TLS 1.2/1.3, QUIC, DNS, and HTTP protocols. The monitor mode can capture packets and export them to pcapng format for analysis in Wireshark."],["CAVEATS","Requires root privileges. On Linux, depends on eBPF for TCP RST suppression. On Windows, requires Npcap."],["HISTORY","**packemon** was created by **ddddddO** and is written in **Go**."],["INSTALL","```aur: yay -S packemon```"],["SEE ALSO","[tcpdump](/man/tcpdump)(1), [wireshark](/man/wireshark)(1), [nmap](/man/nmap)(1)"]],"e":["tcpdump","wireshark","nmap"],"x":"sudo packemon monitor"},"pbmtoascii":{"n":"pbmtoascii","t":"converts PBM bitmap images to ASCII art","s":[["TAGLINE","converts PBM bitmap images to ASCII art"],["TLDR","**Convert PBM to ASCII art**\n\n```pbmtoascii [input.pbm]```\n\n**Convert with grid size**\n\n```pbmtoascii -[2x4] [input.pbm]```"],["SYNOPSIS","**pbmtoascii** [_options_] [_file_]"],["PARAMETERS","**-1x2**\n> 1x2 cell size.\n\n**-2x4**\n> 2x4 cell size (default)."],["DESCRIPTION","**pbmtoascii** converts PBM bitmap images to ASCII art. Renders images using text characters. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtoascii](/man/ppmtoascii)(1), [asciitopgm](/man/asciitopgm)(1)"]],"e":["ppmtoascii","asciitopgm"],"x":"pbmtoascii [input.pbm]"},"pbmtomda":{"n":"pbmtomda","t":"Convert PBM to Microdesign MDA format","s":[["TAGLINE","Convert PBM to Microdesign MDA format"],["TLDR","**Convert PBM to Microdesign Area format**\n\n```pbmtomda [input.pbm] > [output.mda]```\n\n**Invert colors**\n\n```pbmtomda -i [input.pbm] > [output.mda]```\n\n**Produce double-resolution MDA output**\n\n```pbmtomda -d [input.pbm] > [output.mda]```"],["SYNOPSIS","**pbmtomda** [**-d**] [**-i**] [_pbmfile_]"],["PARAMETERS","**-d**\n> Produce double-resolution MDA output instead of the default single resolution.\n\n**-i**\n> Invert the image so black pixels in the PBM become white in the MDA output.\n\n_pbmfile_\n> Input PBM file. If omitted, the image is read from standard input."],["DESCRIPTION","**pbmtomda** reads a PBM (Portable Bitmap) image and writes it to standard output in Microdesign Area (MDA) format, the native graphics format used by the Microdesign graphics program on the Amstrad PCW CP/M-based computer. The output is a raw MDA file without a header, suitable for pasting into a Microdesign document.\n\nPart of the Netpbm toolkit."],["CAVEATS","Generates only uncompressed MDA data; a Microdesign file header must be added separately when producing a standalone file."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[mdatopbm](/man/mdatopbm)(1), [pbmtolj](/man/pbmtolj)(1), [pbm](/man/pbm)(5)"]],"e":["mdatopbm","pbmtolj","pbm"],"x":"pbmtomda [input.pbm] > [output.mda]"},"pdftotext":{"n":"pdftotext","t":"converts Portable Document Format files to plain text","s":[["TAGLINE","converts Portable Document Format files to plain text"],["TLDR","**Extract text** from a PDF to stdout\n\n```pdftotext [file.pdf] -```\n\n**Extract text** to a text file\n\n```pdftotext [file.pdf] [output.txt]```\n\n**Extract text preserving layout**\n\n```pdftotext -layout [file.pdf] [output.txt]```\n\n**Extract text from specific pages**\n\n```pdftotext -f [1] -l [5] [file.pdf] [output.txt]```\n\n**Extract raw text** without formatting\n\n```pdftotext -raw [file.pdf] [output.txt]```\n\n**Extract text from a password-protected PDF**\n\n```pdftotext -upw [password] [file.pdf] [output.txt]```"],["SYNOPSIS","**pdftotext** [_options_] _PDF-file_ [_text-file_]"],["PARAMETERS","**-f** _number_\n> First page to convert (default: 1)\n\n**-l** _number_\n> Last page to convert (default: last page)\n\n**-layout**\n> Maintain original physical layout of the text\n\n**-simple**\n> Simple one-column page layout\n\n**-table**\n> Table mode, similar to layout but optimized for tables\n\n**-lineprinter**\n> Line printer mode with fixed-pitch font metrics\n\n**-raw**\n> Keep strings in content stream order\n\n**-fixed** _number_\n> Assume fixed-pitch font with specified character width\n\n**-enc** _encoding_\n> Output text encoding (Latin1, UTF-8, etc.)\n\n**-nopgbrk**\n> Don't insert page breaks between pages\n\n**-opw** _password_\n> Owner password for encrypted PDF\n\n**-upw** _password_\n> User password for encrypted PDF\n\n**-q**\n> Quiet mode, suppress messages and errors\n\n**-v**\n> Print version information\n\n**-h**\n> Print usage information"],["DESCRIPTION","**pdftotext** converts Portable Document Format (PDF) files to plain text. It extracts the text content from PDF documents while optionally attempting to preserve the visual layout of the original document.\n\nThe program is part of the **poppler-utils** package (or **xpdf-utils** on older systems) and handles most PDF text extraction needs. It can process encrypted PDFs when provided with the appropriate password and supports various output encodings.\n\nCommon use cases include making PDF content searchable, extracting text for further processing, creating accessible versions of documents, and feeding PDF content into text analysis pipelines."],["CAVEATS","Cannot extract text from scanned documents or image-based PDFs (use OCR tools like **tesseract** for those). Layout preservation may not be perfect for complex multi-column documents. Text in embedded fonts without Unicode mappings may not extract correctly. Ligatures and special characters may not render properly in all output encodings."],["HISTORY","**pdftotext** was originally developed as part of the **Xpdf** project by **Derek Noonburg** in the late 1990s. The tool has since been incorporated into the **Poppler** library, a fork of Xpdf that has become the standard PDF rendering library on many Linux distributions. Both versions continue to be maintained, with Poppler receiving more active development and becoming the default on most modern systems."],["INSTALL","```apt: sudo apt install poppler-utils```\n\n```dnf: sudo dnf install poppler-utils```\n\n```apk: sudo apk add poppler-utils```\n\n```nix: nix profile install nixpkgs#poppler-utils```"],["SEE ALSO","[pdfinfo](/man/pdfinfo)(1), [pdfimages](/man/pdfimages)(1), [pdftops](/man/pdftops)(1), [tesseract](/man/tesseract)(1), [mutool](/man/mutool)(1)"]],"e":["pdfinfo","pdfimages","pdftops","tesseract","mutool"],"x":"pdftotext [file.pdf] -"},"pi":{"n":"pi","t":"computes and displays decimal digits of Archimedes' constant Pi","s":[["TAGLINE","computes and displays decimal digits of Archimedes' constant Pi"],["TLDR","Display **100 digits** of Pi\n\n```pi```\n\nDisplay **specific number** of digits\n\n```pi [1000]```\n\nDisplay **bibliography**\n\n```pi --bibliography```\n\nDisplay **help**\n\n```pi --help```"],["SYNOPSIS","**pi** [_digits_] [**--bibliography**] [**--help**] [**--version**]"],["PARAMETERS","**digits**\n> Number of decimal digits to compute (default: 100)\n\n**--bibliography**\n> Display recommended readings about Pi computation\n\n**--help**\n> Display help information\n\n**--version**\n> Display version information"],["DESCRIPTION","**pi** computes and displays decimal digits of Archimedes' constant Pi (π). It uses efficient algorithms to calculate Pi to arbitrary precision, limited only by available memory and time.\n\nThe tool is useful for testing system performance, generating Pi digits for verification purposes, or educational exploration of the mathematical constant."],["CAVEATS","Very large digit counts require significant computation time and memory. Results are computed fresh each run (not cached). Part of the GNU coreutils on some systems."],["HISTORY","**pi** implementations have existed in various forms across Unix systems. The constant Pi has been computed to trillions of digits using specialized algorithms like the Chudnovsky algorithm, though command-line tools typically compute fewer digits for practical use."],["INSTALL","```apt: sudo apt install pi```\n\n```dnf: sudo dnf install cln```\n\n```pacman: sudo pacman -S cln```\n\n```apk: sudo apk add cln```\n\n```zypper: sudo zypper install cln```\n\n```brew: brew install cln```\n\n```nix: nix profile install nixpkgs#cln```"],["SEE ALSO","[bc](/man/bc)(1), [dc](/man/dc)(1), [factor](/man/factor)(1)"]],"e":["bc","dc","factor"],"x":"pi"},"pon":{"n":"pon","t":"Initiate PPP dial-up connections","s":[["TAGLINE","Initiate PPP dial-up connections"],["TLDR","**Start PPP connection**\n\n```pon```\n\n**Start specific provider**\n\n```pon [provider]```\n\n**Start with debugging**\n\n```pon [provider] debug```"],["SYNOPSIS","**pon** [_provider_] [_options_]"],["PARAMETERS","_PROVIDER_\n> PPP provider name.\n\n**debug**\n> Enable debug output."],["DESCRIPTION","**pon** initiates a PPP (Point-to-Point Protocol) connection using a named provider configuration. Provider configurations are stored in **/etc/ppp/peers/** and define the connection parameters including phone number, authentication, and modem settings.\n\nWithout a provider argument, it uses the default provider. The **debug** option enables verbose logging to syslog. Use **poff** to terminate the connection."],["CAVEATS","PPP-specific. Paired with poff command. Legacy dial-up."],["HISTORY","pon was created for **PPP connection** initiation on Linux systems."],["INSTALL","```apt: sudo apt install ppp```\n\n```dnf: sudo dnf install ppp```\n\n```pacman: sudo pacman -S ppp```\n\n```apk: sudo apk add ppp-daemon```\n\n```zypper: sudo zypper install ppp```\n\n```nix: nix profile install nixpkgs#ppp```"],["SEE ALSO","[poff](/man/poff)(1), [pppd](/man/pppd)(8), [pppconfig](/man/pppconfig)(8)"]],"e":["poff","pppd","pppconfig"],"x":"pon"},"postgres":{"n":"postgres","t":"Run PostgreSQL database server process","s":[["TAGLINE","Run PostgreSQL database server process"],["TLDR","**Start PostgreSQL server**\n\n```postgres -D [/var/lib/postgresql/data]```\n\n**Start in foreground with logging**\n\n```postgres -D [/var/lib/postgresql/data] -c log_statement=all```\n\n**Start on specific port**\n\n```postgres -D [/var/lib/postgresql/data] -p [5433]```\n\n**Start single-user mode**\n\n```postgres --single -D [/var/lib/postgresql/data] [database]```\n\n**Show version**\n\n```postgres --version```"],["SYNOPSIS","**postgres** [_-D datadir_] [_-p port_] [_-c name=value_] [_options_]"],["PARAMETERS","**-D** _DIR_\n> Data directory.\n\n**-p** _PORT_\n> Port number.\n\n**-c** _NAME=VALUE_\n> Set runtime parameter.\n\n**-h** _HOST_\n> Listen addresses.\n\n**-k** _DIR_\n> Unix socket directory.\n\n**-l** _FILE_\n> Send server log output to a file.\n\n**-d** _LEVEL_\n> Set debug level (1-5); higher values produce more output.\n\n**-B** _BUFFERS_\n> Shared buffers.\n\n**-N** _N_\n> Max connections.\n\n**--single**\n> Single-user mode. Must be the first argument on the command line.\n\n**-e**\n> Echo all commands to standard output (single-user mode).\n\n**-F**\n> Disable fsync for faster operation (risk of data corruption).\n\n**--describe-config**\n> Describe configuration parameters and exit.\n\n**--version**\n> Show version."],["DESCRIPTION","**postgres** is the PostgreSQL database server process. It manages database files, handles connections, and executes SQL queries.\n\nThe server is typically started through pg_ctl or system service managers rather than directly. Direct invocation is useful for debugging or non-standard configurations.\n\nConfiguration parameters can be set on command line or in postgresql.conf. Runtime parameters control memory, connections, logging, and behavior.\n\nSingle-user mode bypasses normal startup for maintenance. It's used for recovery operations when the database won't start normally.\n\nThe data directory contains all database files. It must be initialized with initdb before first use and is not portable between major versions.\n\nLogging options help diagnose problems. Statement logging shows all SQL. Connection logging tracks client access."],["CONFIGURATION","**postgresql.conf**\n> Main configuration file in the data directory controlling memory allocation, connection limits, logging, query planner settings, WAL behavior, and replication.\n\n**pg_hba.conf**\n> Host-based authentication file controlling which users can connect from which hosts using which authentication methods (trust, md5, scram-sha-256, cert).\n\n**pg_ident.conf**\n> User name mapping file for external authentication systems, mapping OS usernames to PostgreSQL roles.\n\n**PGDATA**\n> Environment variable specifying the data directory path, used as default when **-D** is not provided."],["CAVEATS","Running directly bypasses service management. Data directory must be secure. Configuration affects performance significantly. Major version upgrades need pg_upgrade."],["HISTORY","**PostgreSQL** development began at **UC Berkeley** in **1986** as POSTGRES (Post-Ingres). The open-source PostgreSQL project started in **1996**. It's now one of the most advanced open-source databases."],["INSTALL","```dnf: sudo dnf install postgresql17```\n\n```apk: sudo apk add postgresql17```\n\n```zypper: sudo zypper install postgresql17```\n\n```brew: brew install postgres```"],["SEE ALSO","[pg_ctl](/man/pg_ctl)(1), [initdb](/man/initdb)(1), [psql](/man/psql)(1), [pg_dump](/man/pg_dump)(1), [pg_upgrade](/man/pg_upgrade)(1), [pg_isready](/man/pg_isready)(1)"]],"e":["pg_ctl","initdb","psql","pg_dump","pg_upgrade","pg_isready"],"x":"postgres -D [/var/lib/postgresql/data]"},"pprof":{"n":"pprof","t":"Visualize and analyze profiling data","s":[["TAGLINE","Visualize and analyze profiling data"],["TLDR","**Start web interface**\n\n```pprof -http=[localhost:8080] [profile.pb.gz]```\n\n**Show top functions**\n\n```pprof -top [profile.pb.gz]```\n\n**Show call graph as text**\n\n```pprof -text [profile.pb.gz]```\n\n**Show call graph as PDF**\n\n```pprof -pdf [profile.pb.gz] > [profile.pdf]```\n\n**Profile from running server**\n\n```pprof -http=[localhost:8080] [http://localhost:6060/debug/pprof/profile]```\n\n**Compare two profiles**\n\n```pprof -base [baseline.pb.gz] [current.pb.gz]```\n\n**Focus on specific function**\n\n```pprof -focus=[FunctionName] [profile.pb.gz]```"],["SYNOPSIS","**pprof** [_-http addr_] [_-top_] [_-text_] [_-pdf_] [_-focus pattern_] [_options_] _source_"],["PARAMETERS","**-http** _ADDR_\n> Start web server.\n\n**-top**\n> Show top functions.\n\n**-text**\n> Text report.\n\n**-pdf**\n> PDF output.\n\n**-svg**\n> SVG output.\n\n**-png**\n> PNG output.\n\n**-web**\n> Open in browser.\n\n**-focus** _REGEX_\n> Focus on matching functions.\n\n**-ignore** _REGEX_\n> Ignore matching functions.\n\n**-base** _PROFILE_\n> Compare against baseline.\n\n**-seconds** _N_\n> Profile duration.\n\n**-sample_index** _IDX_\n> Sample type (cpu, alloc, etc.).\n\n**-lines**\n> Show line-level info."],["DESCRIPTION","**pprof** analyzes and visualizes performance profiles from Go programs and other sources. It shows where programs spend time and memory.\n\nThe web interface provides interactive exploration. Flame graphs show call hierarchies. Graph views display caller/callee relationships.\n\nProfiles capture CPU usage, memory allocations, goroutine counts, and custom metrics. Go's runtime/pprof package generates these profiles.\n\nLive profiling connects to running servers through HTTP endpoints. The standard /debug/pprof/ path provides various profile types.\n\nComparison mode highlights changes between profiles. This identifies performance regressions or improvements after code changes.\n\nFocus and ignore filters narrow analysis to specific areas. Regular expressions match function names for targeted investigation."],["CAVEATS","Requires graphviz for graph generation. CPU profiles need sufficient sample time. Memory profiles may need multiple collections. Profile size grows with sampling."],["HISTORY","**pprof** was developed at **Google** for profiling production systems. The Go version is maintained as part of the Go project. It originated from Google's internal profiling tools and gperftools."],["INSTALL","```zypper: sudo zypper install pprof```\n\n```nix: nix profile install nixpkgs#pprof```"],["SEE ALSO","[perf](/man/perf)(1), [flamegraph](/man/flamegraph)(1)"]],"e":["perf","flamegraph"],"x":"pprof -http=[localhost:8080] [profile.pb.gz]"},"pysetup3.4":{"n":"pysetup3.4","t":"Deprecated Python 3.4 package installer","s":[["TAGLINE","Deprecated Python 3.4 package installer"],["TLDR","**Install package (deprecated)**\n\n```pysetup3.4 install [package]```\n\n**Run setup command**\n\n```pysetup3.4 run [command]```"],["SYNOPSIS","**pysetup3.4** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> Setup command.\n\n**install**\n> Install package.\n\n**run**\n> Run command."],["DESCRIPTION","**pysetup3.4** was a package installation and management tool included with Python 3.4 as part of the distutils2 packaging effort. It provided commands for installing, removing, and managing Python packages before pip became the standard package manager.\n\nThe tool has been deprecated and replaced entirely by **pip**. It should not be used for new projects, and legacy systems still relying on it should migrate to pip for package management."],["CAVEATS","Deprecated. Use pip instead."],["HISTORY","pysetup3.4 was part of **deprecated distutils2** packaging effort."],["SEE ALSO","[pip](/man/pip)(1), [python3.4](/man/python3.4)(1)"]],"e":["pip","python3.4"],"x":"pysetup3.4 install [package]"},"rdfproc":{"n":"rdfproc","t":"Process and query RDF data stores","s":[["TAGLINE","Process and query RDF data stores"],["TLDR","**Query RDF store**\n\n```rdfproc [store] query sparql - \"[SELECT * WHERE { ?s ?p ?o }]\"```\n\n**Parse RDF file**\n\n```rdfproc [store] parse [file.rdf]```\n\n**Serialize to format**\n\n```rdfproc [store] serialize ntriples```\n\n**Add statement**\n\n```rdfproc [store] add [subject] [predicate] [object]```\n\n**Print all statements**\n\n```rdfproc [store] print```"],["SYNOPSIS","**rdfproc** [_options_] _store_ _command_ [_args_]"],["PARAMETERS","**parse** _file_\n> Parse and load RDF file.\n\n**query** _lang_ _uri_ _query_\n> Execute query.\n\n**serialize** _format_\n> Output in format.\n\n**print**\n> Print all statements.\n\n**add** _s_ _p_ _o_\n> Add triple.\n\n**remove** _s_ _p_ _o_\n> Remove triple."],["DESCRIPTION","**rdfproc** is a command-line utility for processing RDF data using the Redland RDF library. It can parse, store, query, and serialize RDF data."],["EXAMPLES","```bash"],["Create store and parse file","rdfproc mystore parse data.rdf"],["SPARQL query","rdfproc mystore query sparql - \"SELECT * WHERE { ?s ?p ?o } LIMIT 10\""],["Serialize as N-Triples","rdfproc mystore serialize ntriples"],["Add triple","rdfproc mystore add \"http://example.org/s\" \"http://example.org/p\" \"value\""],["Print all","rdfproc mystore print\n```"],["FORMATS","```\nrdfxml   - RDF/XML\nntriples - N-Triples\nturtle   - Turtle\n```"],["CAVEATS","Part of Redland RDF libraries. Stores are persistent by default. Use - for stdin/stdout."],["HISTORY","rdfproc is part of **Redland** RDF Libraries by **Dave Beckett**, providing RDF processing since 2000."],["INSTALL","```apt: sudo apt install redland-utils```\n\n```dnf: sudo dnf install redland```\n\n```pacman: sudo pacman -S redland```\n\n```apk: sudo apk add redland```\n\n```zypper: sudo zypper install redland```\n\n```brew: brew install redland```\n\n```nix: nix profile install nixpkgs#redland```"],["SEE ALSO","[rapper](/man/rapper)(1), [roqet](/man/roqet)(1), [redland](/man/redland)(3)"]],"e":["rapper","roqet","redland"],"x":"rdfproc [store] query sparql - \"[SELECT * WHERE { ?s ?p ?o }]\""},"rnano":{"n":"rnano","t":"View files in read-only nano editor","s":[["TAGLINE","View files in read-only nano editor"],["TLDR","**View file read-only**\n\n```rnano [file]```\n\n**View with line numbers**\n\n```rnano -l [file]```\n\n**View at specific line**\n\n```rnano +[42] [file]```\n\n**View with smooth scrolling**\n\n```rnano -S [file]```"],["SYNOPSIS","**rnano** [_-l_] [_+line_] [_options_] _file_"],["PARAMETERS","**-l**\n> Show line numbers.\n\n**-S**\n> Smooth scrolling.\n\n**+**_LINE_\n> Start at line.\n\n**-v**\n> View mode (default).\n\n**-h**\n> Show help."],["DESCRIPTION","**rnano** is the read-only mode of the nano text editor, typically implemented as a symlink that invokes nano with the **-v** (view) flag. It provides a familiar nano interface for viewing files while preventing any accidental modifications, making it suitable for inspecting configuration files and logs.\n\nAll of nano's navigation features remain available, including scrolling, line jumping with **+LINE**, syntax highlighting, line number display with **-l**, and smooth scrolling with **-S**. The editor simply refuses any modification commands, so users can browse safely without risk of unintended changes.\n\nThis is analogous to the relationship between **vi** and **view**, providing a read-only variant of a familiar editor."],["CAVEATS","Just links to nano -v. Limited compared to less/more. No search history."],["HISTORY","**rnano** is typically a symlink to **nano** that invokes it in read-only (view) mode, similar to how view relates to vim."],["INSTALL","```apt: sudo apt install nano```\n\n```dnf: sudo dnf install nano```\n\n```pacman: sudo pacman -S nano```\n\n```apk: sudo apk add nano```\n\n```zypper: sudo zypper install nano```\n\n```brew: brew install nano```\n\n```nix: nix profile install nixpkgs#nano```"],["SEE ALSO","[nano](/man/nano)(1), [less](/man/less)(1), [view](/man/view)(1), [cat](/man/cat)(1)"]],"e":["nano","less","view","cat"],"x":"rnano [file]"},"rpmconf":{"n":"rpmconf","t":"Handle RPM configuration file conflicts","s":[["TAGLINE","Handle RPM configuration file conflicts"],["TLDR","List leftover files and **interactively choose** actions\n\n```sudo rpmconf -a```\n\n**Delete orphaned** .rpmnew and .rpmsave files\n\n```sudo rpmconf -a -c```"],["SYNOPSIS","**rpmconf** [_options_]"],["PARAMETERS","**-a**, **--all**\n> Process all configuration files\n\n**-c**, **--clean**\n> Delete orphaned configuration backup files"],["DESCRIPTION","**rpmconf** handles .rpmnew, .rpmsave, and .rpmorig files left over by RPM package upgrades. These files are created when package updates contain configuration changes that might conflict with local modifications.\n\nThe tool provides an interactive interface to merge, replace, or keep configuration files."],["CAVEATS","Review changes carefully before applying. Incorrect choices may overwrite important customizations or leave outdated configurations."],["HISTORY","Part of **rpmconf** package for Fedora/RHEL systems. Simplifies post-upgrade configuration management."],["INSTALL","```dnf: sudo dnf install rpmconf```\n\n```zypper: sudo zypper install rpmconf```"],["SEE ALSO","[rpm](/man/rpm)(8), [dnf](/man/dnf)(8), [yum](/man/yum)(8)"]],"e":["rpm","dnf","yum"],"x":"sudo rpmconf -a"},"rspamc":{"n":"rspamc","t":"Rspamd spam filtering client","s":[["TAGLINE","Rspamd spam filtering client"],["TLDR","Train as **spam**\n\n```rspamc learn_spam path/to/email```\n\nTrain as **ham** (not spam)\n\n```rspamc learn_ham path/to/email```\n\nGenerate **report** on email\n\n```rspamc symbols path/to/email```\n\nShow server **statistics**\n\n```rspamc stat```\n\n**Scan** an email\n\n```rspamc < path/to/email```\n\nCheck **fuzzy hash**\n\n```rspamc fuzzy_check path/to/email```"],["SYNOPSIS","**rspamc** [_options_] [_command_] [_arguments_]"],["DESCRIPTION","**rspamc** is a command-line client for the rspamd spam filtering system. It can train the bayesian filter, scan emails, and query server statistics."],["PARAMETERS","**learn_spam FILE**\n> Train the filter to recognize the email as spam\n\n**learn_ham FILE**\n> Train the filter to recognize the email as legitimate\n\n**symbols FILE**\n> Generate a detailed spam analysis report\n\n**stat**\n> Show server statistics\n\n**fuzzy_add FILE**\n> Add email to fuzzy hash storage\n\n**fuzzy_check FILE**\n> Check if email matches fuzzy hashes\n\n**fuzzy_del FILE**\n> Remove email from fuzzy hash storage\n\n**-h, --host HOST**\n> Connect to specified rspamd host\n\n**-p, --port PORT**\n> Connect on specified port\n\n**-P, --pass PASSWORD**\n> Use password for authentication\n\n**-j, --json**\n> Output results in JSON format"],["CAVEATS","Requires a running rspamd server. Training effectiveness improves with more samples. Fuzzy hashes are used for matching similar spam patterns."],["HISTORY","**rspamc** is part of **rspamd**, a fast, free, and open-source spam filtering system designed for high-volume mail servers."],["INSTALL","```apt: sudo apt install rspamd```\n\n```pacman: sudo pacman -S rspamd```\n\n```apk: sudo apk add rspamd-client```\n\n```zypper: sudo zypper install rspamd```\n\n```nix: nix profile install nixpkgs#rspamd```"],["SEE ALSO","[rspamd](/man/rspamd)(8), [rspamadm](/man/rspamadm)(1)"]],"e":["rspamd","rspamadm"],"x":"rspamc learn_spam path/to/email"},"shntool-split":{"n":"shntool-split","t":"Alias for shnsplit audio splitter","s":[["TAGLINE","Alias for shnsplit audio splitter"],["TLDR","This command is an alias of **shnsplit**\n\nView documentation for the original command\n\n```tldr shnsplit```"],["SYNOPSIS","**shntool split** [_OPTIONS_] [_file_]"],["DESCRIPTION","**shntool split** is an alternative invocation of the **shnsplit** command. Both commands are functionally identical and split audio files according to CUE sheet definitions."],["SEE ALSO","[shnsplit](/man/shnsplit)(1)"]],"e":["shnsplit"],"x":"tldr shnsplit"},"signal-desktop":{"n":"signal-desktop","t":"Encrypted messaging desktop application","s":[["TAGLINE","Encrypted messaging desktop application"],["TLDR","**Start Signal Desktop**\n\n```signal-desktop```\n\n**Start minimized** to system tray\n\n```signal-desktop --start-in-tray```\n\n**Use a custom profile** with separate user data\n\n```signal-desktop --user-data-dir=[~/.config/SignalProfile]```\n\n**Enable system tray** icon\n\n```signal-desktop --use-tray-icon```\n\n**Start with DevTools open** for debugging\n\n```signal-desktop --enable-dev-tools```"],["SYNOPSIS","**signal-desktop** [_options_]"],["PARAMETERS","**--start-in-tray**\n> Start Signal with the window minimized to the system tray.\n\n**--use-tray-icon**\n> Display an icon in the system tray. Allows minimizing to tray instead of exiting the app.\n\n**--user-data-dir=**_PATH_\n> Use a custom user data directory. Useful for running multiple Signal profiles or managing multiple phone numbers.\n\n**--enable-dev-tools**\n> Start with Chromium DevTools panel open for debugging.\n\n**--enable-gpu**\n> Enable GPU acceleration.\n\n**--no-sandbox**\n> Disable Chromium sandboxing (not recommended for security).\n\n**--proxy-server=**_URL_\n> Use a proxy server for network connections."],["DESCRIPTION","**signal-desktop** is the official Signal Private Messenger desktop application for Linux. It provides end-to-end encrypted messaging, voice calls, and video calls synchronized with the Signal mobile app. The desktop client requires Signal to first be installed and set up on a mobile phone, which acts as the primary device.\n\nThe application is built on Electron and provides a full-featured messaging interface including group chats, disappearing messages, reactions, and file sharing. Messages are synchronized across linked devices using Signal's secure protocol.\n\nSignal Desktop can be installed via the official APT repository on Debian-based distributions, as a Snap package, or as a Flatpak."],["CAVEATS","Signal Desktop requires linking to an existing Signal account on a mobile device. The desktop app cannot be used as a standalone client. When using **--user-data-dir** for multiple profiles, each profile must be linked to a different phone number. Some features may require the mobile app to be online periodically for synchronization."],["HISTORY","**Signal Desktop** was originally released in **2015** as a Chrome app before transitioning to a standalone Electron application in **2017**. Signal itself evolved from TextSecure, developed by Open Whisper Systems (founded by Moxie Marlinspike in **2013**). The Signal Protocol for end-to-end encryption has been adopted by numerous other messaging platforms. In **2018**, the Signal Foundation was established as a non-profit to support Signal's development. The desktop client continues active development with regular security updates."],["INSTALL","```pacman: sudo pacman -S signal-desktop```\n\n```nix: nix profile install nixpkgs#signal-desktop```"],["SEE ALSO","[telegram-desktop](/man/telegram-desktop)(1), [gpg](/man/gpg)(1)"]],"e":["telegram-desktop","gpg"],"x":"signal-desktop"},"streamlink":{"n":"streamlink","t":"Pipe live streams to media players","s":[["TAGLINE","Pipe live streams to media players"],["TLDR","**Open stream in default player**\n\n```streamlink [URL] [best]```\n\n**Open stream in specific player**\n\n```streamlink --player [mpv] [URL] [best]```\n\n**List available qualities**\n\n```streamlink [URL]```\n\n**Record stream to file**\n\n```streamlink -o [output.ts] [URL] [best]```\n\n**Open with specific quality**\n\n```streamlink [URL] [720p]```\n\n**Open Twitch stream**\n\n```streamlink [twitch.tv/streamername] [best]```\n\n**Pass player arguments**\n\n```streamlink --player [mpv] --player-args \"[--no-border]\" [URL] [best]```"],["SYNOPSIS","**streamlink** [_--player player_] [_-o file_] [_options_] _URL_ [_quality_]"],["PARAMETERS","**-p** _PLAYER_, **--player** _PLAYER_\n> Player to use.\n\n**-a** _ARGS_, **--player-args** _ARGS_\n> Arguments for player.\n\n**-o** _FILE_, **--output** _FILE_\n> Record to file.\n\n**-r** _FILE_, **--record** _FILE_\n> Record while also playing.\n\n**-f**, **--force**\n> Overwrite output file.\n\n**--default-stream** _QUALITY_\n> Default stream quality.\n\n**--stream-url**\n> Print stream URL only.\n\n**--json**\n> Output as JSON.\n\n**--retry-streams** _SEC_\n> Retry if stream offline.\n\n**--retry-max** _NUM_\n> Maximum retry attempts.\n\n**--retry-open** _NUM_\n> Retry opening stream.\n\n**--hls-live-edge** _SEG_\n> Segments from live edge.\n\n**--twitch-disable-ads**\n> Disable Twitch ads.\n\n**--twitch-low-latency**\n> Enable low latency mode.\n\n**--http-header** _HEADER_\n> Add HTTP header.\n\n**--http-cookie** _COOKIE_\n> Add HTTP cookie."],["DESCRIPTION","**streamlink** extracts streams from websites and pipes them to media players. It bypasses browser-based players, enabling use of lightweight, efficient players like mpv or VLC.\n\nQuality selection uses identifiers like best, worst, or specific resolutions (720p, 1080p60). Listing qualities shows available options for a stream. Fallback selection (720p,480p,best) tries multiple qualities.\n\nRecording saves streams to files. Combined with a player, streams can be both watched and archived. Output format depends on stream type (usually TS or MP4).\n\nPlatform support includes Twitch, YouTube, and hundreds of other sites. Plugin architecture enables community-contributed support for additional platforms.\n\nTwitch-specific features include ad skipping, low-latency mode, and OAuth authentication for subscriber streams. Configuration can be saved in ~/.config/streamlink/config.\n\nHTTP options enable accessing streams behind authentication or with custom headers, handling various DRM and geo-restriction scenarios."],["CONFIGURATION","**~/.config/streamlink/config**\n> Main configuration file for default options, player settings, and plugin parameters.\n\n**~/.config/streamlink/plugins/**\n> Directory for custom or third-party stream extraction plugins."],["CAVEATS","Site support depends on plugins and may break with site changes. Some sites block third-party access. DRM content usually not supported. Quality options vary by platform. Ad blocking may violate ToS."],["HISTORY","**streamlink** is a fork of **livestreamer**, which was created around **2012**. After livestreamer's development stalled, streamlink forked in **2016** with active maintenance. The project supports an extensive list of streaming sites and continues community-driven development."],["INSTALL","```pacman: sudo pacman -S streamlink```\n\n```apk: sudo apk add streamlink```\n\n```zypper: sudo zypper install streamlink```\n\n```brew: brew install streamlink```\n\n```nix: nix profile install nixpkgs#streamlink```"],["SEE ALSO","[youtube-dl](/man/youtube-dl)(1), [yt-dlp](/man/yt-dlp)(1), [mpv](/man/mpv)(1), [vlc](/man/vlc)(1)"]],"e":["youtube-dl","yt-dlp","mpv","vlc"],"x":"streamlink [URL] [best]"},"strings":{"n":"strings","t":"Extract printable text from binary files","s":[["TAGLINE","Extract printable text from binary files"],["TLDR","**Find printable strings** in a file\n\n```strings [file]```\n\n**Find strings** of minimum length\n\n```strings -n [10] [file]```\n\n**Print filename** with each string\n\n```strings -f [file1] [file2]```\n\n**Print offset** of each string\n\n```strings -t d [file]```\n\n**Print offset in hexadecimal**\n\n```strings -t x [file]```\n\n**Search entire file** (not just data sections)\n\n```strings -a [file]```\n\n**Search for specific encoding**\n\n```strings -e l [file]```\n\n**Scan multiple files**\n\n```strings [*.bin]```"],["SYNOPSIS","**strings** [_options_] _file_..."],["DESCRIPTION","**strings** finds and prints sequences of printable characters in files, typically binary files. By default, it looks for sequences of at least 4 printable characters followed by an unprintable character.\n\nThe tool is commonly used to examine binaries, extract text from executables, find embedded messages, and perform basic forensic analysis. It can reveal version strings, error messages, and other embedded text.\n\nstrings ignores the structure of the file and simply scans for character sequences, making it useful when file format isn't known or important."],["PARAMETERS","**-a**, **--all**\n> Scan entire file, not just data sections.\n\n**-f**, **--print-file-name**\n> Print filename before each string.\n\n**-n** _min_, **--bytes=** _min_\n> Minimum string length (default 4).\n\n**-t** _format_, **--radix=** _format_\n> Print offset (o=octal, d=decimal, x=hex).\n\n**-e** _encoding_\n> Character encoding (s=7-bit, S=8-bit, b/l=16-bit, B/L=32-bit).\n\n**-o**\n> Print offset in octal (like -t o).\n\n**-T** _bfdname_, **--target=** _bfdname_\n> Specify binary format."],["CAVEATS","Results include any character sequences, not necessarily meaningful text. False positives are common in binary data. Cannot determine context or purpose of found strings. Some strings may be partial or misleading."],["HISTORY","**strings** has been part of Unix since early versions, originating in **Version 6 Unix** at **Bell Labs** in the **1970s**. The GNU version is part of **binutils**. The tool remains essential for binary analysis and is commonly used in security research and reverse engineering."],["INSTALL","```apt: sudo apt install binutils```\n\n```dnf: sudo dnf install binutils```\n\n```pacman: sudo pacman -S binutils```\n\n```apk: sudo apk add binutils```\n\n```zypper: sudo zypper install binutils```\n\n```brew: brew install binutils```\n\n```nix: nix profile install nixpkgs#binutils```"],["SEE ALSO","[file](/man/file)(1), [hexdump](/man/hexdump)(1), [od](/man/od)(1), [xxd](/man/xxd)(1)"]],"e":["file","hexdump","od","xxd"],"x":"strings [file]"},"systemctl-edit":{"n":"systemctl-edit","t":"Edit systemd unit override files","s":[["TAGLINE","Edit systemd unit override files"],["TLDR","**Overlay** unit file non-destructively\n\n```sudo systemctl edit [unit]```\n\n**Edit** full unit file\n\n```sudo systemctl edit [unit] --full```\n\n**Create** new unit file\n\n```sudo systemctl edit [unit] --full --force```\n\nEdit **user** unit file\n\n```systemctl edit [unit] --user```"],["SYNOPSIS","**systemctl edit** [_OPTIONS_] _UNIT_..."],["PARAMETERS","**-l, --full**\n> Edit the main unit file instead of creating a drop-in override\n\n**--force**\n> Create a new unit file if it doesn't exist (with --full)\n\n**--user**\n> Edit user unit files\n\n**--system**\n> Edit system unit files (default)\n\n**--runtime**\n> Make changes temporary (cleared on reboot)"],["DESCRIPTION","**systemctl edit** opens a text editor to modify systemd unit files. By default, it creates a drop-in override file in `/etc/systemd/system/<unit>.d/override.conf` that extends the original unit without modifying it directly.\n\nWith `--full`, the entire unit file is opened for editing. With `--full --force`, a new unit file can be created. After editing, systemd automatically reloads the configuration. The editor is determined by `SYSTEMD_EDITOR`, `EDITOR`, or `VISUAL` environment variables."],["CAVEATS","Drop-in files only add or override specific settings; they cannot remove existing configuration. The daemon reloads automatically after edit, but running units may need manual restart. Syntax errors in unit files can prevent services from starting."],["HISTORY","The **edit** subcommand was added to **systemctl** to simplify unit customization. Systemd was created by **Lennart Poettering** and **Kay Sievers** at Red Hat and has been the default init system for most major Linux distributions since **2015**. The drop-in override pattern allows safe customization that persists across package updates."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-revert](/man/systemctl-revert)(1)"]],"e":["systemctl","systemctl-revert"],"x":"sudo systemctl edit [unit]"},"systemctl-whoami":{"n":"systemctl-whoami","t":"Show unit owning a process","s":[["TAGLINE","Show unit owning a process"],["TLDR","Show **current** shell's unit\n\n```systemctl whoami```\n\nShow for **user** service manager\n\n```systemctl whoami --user```\n\nShow unit for **specific PID**\n\n```systemctl whoami [pid]```\n\nShow units for **multiple** PIDs\n\n```systemctl whoami [pid1 pid2 ...]```"],["SYNOPSIS","**systemctl whoami** [_PID_...]"],["PARAMETERS","**--user**\n> Query user service manager\n\n**--system**\n> Query system service manager (default)"],["DESCRIPTION","**systemctl whoami** displays which systemd unit a process belongs to. Without arguments, it shows the unit of the current shell (the process running systemctl).\n\nWith PIDs specified, it shows the units for those processes. This helps identify which service or scope owns a particular process."],["CAVEATS","Processes not managed by systemd may show as belonging to the root slice or have no associated unit. Container processes may appear differently."],["HISTORY","The **whoami** subcommand provides a quick way to identify which unit a process is running under, useful for debugging and understanding cgroup membership."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl-status](/man/systemctl-status)(1), [systemctl](/man/systemctl)(1), [ps](/man/ps)(1)"]],"e":["systemctl-status","systemctl","ps"],"x":"systemctl whoami"},"tinyproxy":{"n":"tinyproxy","t":"Lightweight HTTP and HTTPS proxy daemon","s":[["TAGLINE","Lightweight HTTP and HTTPS proxy daemon"],["TLDR","**Start tinyproxy** with the default configuration\n\n```tinyproxy```\n\n**Start with a custom configuration file**\n\n```tinyproxy -c [path/to/tinyproxy.conf]```\n\n**Run in foreground** (don't daemonize)\n\n```tinyproxy -d```\n\n**Run in foreground with custom config** for debugging\n\n```tinyproxy -d -c [path/to/tinyproxy.conf]```\n\n**Show version information**\n\n```tinyproxy -v```\n\n**Show help**\n\n```tinyproxy -h```"],["SYNOPSIS","**tinyproxy** [**-c** _config-file_] [**-d**] [**-h**] [**-l**] [**-v**]"],["PARAMETERS","**-c** _config-file_\n> Use an alternate configuration file instead of the default /etc/tinyproxy/tinyproxy.conf.\n\n**-d**\n> Do not daemonize. Stay in the foreground, useful for debugging.\n\n**-h**\n> Display a short help screen and exit.\n\n**-l**\n> Display the licensing agreement and exit.\n\n**-v**\n> Display version information and exit."],["DESCRIPTION","**Tinyproxy** is a lightweight HTTP/HTTPS proxy daemon designed for minimal resource consumption. It listens on a configurable TCP port and handles HTTP proxy requests, making it ideal for embedded systems, containers, or environments where a full-featured proxy would be too resource-intensive.\n\nThe daemon supports basic HTTP proxying, HTTPS via CONNECT method, upstream proxy configuration, access control lists, and URL-based filtering. It can also function as a reverse proxy, forwarding requests to backend servers.\n\nConfiguration is done through a configuration file (typically /etc/tinyproxy/tinyproxy.conf) that specifies the listening port, allowed clients, log settings, and filtering rules. Tinyproxy provides a built-in statistics page accessible by requesting the stathost (default: tinyproxy.stats).\n\nSignals can control the running daemon: SIGHUP triggers garbage collection on connections, and standard signals handle graceful shutdown."],["CONFIGURATION","**/etc/tinyproxy/tinyproxy.conf**\n> Main configuration file specifying listening port, allowed clients, log settings, upstream proxies, filtering rules, and access control lists.\n\n**-c** _file_\n> Use an alternate configuration file at runtime."],["CAVEATS","Tinyproxy does not support caching, authentication schemes beyond basic, or SOCKS proxying. For high-traffic production environments, consider more full-featured proxies like Squid. The default configuration may allow connections from any host, so access control should be configured appropriately."],["HISTORY","**Tinyproxy** was originally written by **Steven Young** in **1999** as a lightweight alternative to heavier proxy servers. The project has been maintained by various contributors and continues development on GitHub. Its small footprint and simplicity have made it popular for embedded Linux systems, Docker containers, and privacy-focused setups."],["INSTALL","```apt: sudo apt install tinyproxy-bin```\n\n```dnf: sudo dnf install tinyproxy```\n\n```pacman: sudo pacman -S tinyproxy```\n\n```apk: sudo apk add tinyproxy```\n\n```zypper: sudo zypper install tinyproxy```\n\n```brew: brew install tinyproxy```\n\n```nix: nix profile install nixpkgs#tinyproxy```"],["SEE ALSO","[squid](/man/squid)(8), [privoxy](/man/privoxy)(1), [nginx](/man/nginx)(8), [curl](/man/curl)(1), [wget](/man/wget)(1)"]],"e":["squid","privoxy","nginx","curl","wget"],"x":"tinyproxy"},"tripwire":{"n":"tripwire","t":"Host-based filesystem integrity monitoring","s":[["TAGLINE","Host-based filesystem integrity monitoring"],["TLDR","**Initialize the database** (create baseline)\n\n```sudo tripwire --init```\n\n**Run an integrity check**\n\n```sudo tripwire --check```\n\n**Run integrity check** in interactive mode\n\n```sudo tripwire --check --interactive```\n\n**Update the database** after legitimate changes\n\n```sudo tripwire --update --twrfile [path/to/report.twr]```\n\n**Update the policy file**\n\n```sudo tripwire --update-policy [path/to/policy.txt]```\n\n**Test the configuration** without modifying\n\n```sudo tripwire --test```"],["SYNOPSIS","**tripwire** {**--init** | **--check** | **--update** | **--update-policy** | **--test**} [_options_]"],["PARAMETERS","**-m i**, **--init**\n> Database initialization mode. Create baseline database from current filesystem state.\n\n**-m c**, **--check**\n> Integrity checking mode. Compare current filesystem against baseline database.\n\n**-m u**, **--update**\n> Database update mode. Update database to reflect legitimate changes.\n\n**-m p**, **--update-policy**\n> Policy update mode. Apply new policy and synchronize database.\n\n**-m t**, **--test**\n> Test mode. Verify configuration without making changes.\n\n**-I**, **--interactive**\n> Open report in editor after integrity check for easy database update selection.\n\n**-r** _file_, **--twrfile** _file_\n> Specify report file for database update mode.\n\n**-p** _file_, **--polfile** _file_\n> Specify policy file to use.\n\n**-c** _file_, **--cfgfile** _file_\n> Specify configuration file location.\n\n**-v**, **--verbose**\n> Enable verbose output.\n\n**-s**, **--silent**\n> Suppress output except errors.\n\n**-n**, **--no-tty-output**\n> Suppress report from being printed to console.\n\n**-E**, **--signed-report**\n> Create cryptographically signed report."],["DESCRIPTION","**Tripwire** is a host-based intrusion detection system (HIDS) that monitors filesystem integrity. It creates a baseline database of file attributes and periodically compares the current state against this baseline to detect unauthorized additions, deletions, or modifications.\n\nThe tool operates in several modes: initialization creates the baseline database, integrity checking compares current files against the baseline, database update incorporates legitimate changes, and policy update modifies monitoring rules. All database and policy files are cryptographically signed to prevent tampering.\n\nTripwire uses two encryption keys: a site key for organization-wide policies and a local key unique to each server. This allows centralized policy management while maintaining per-server database security.\n\nExit codes from integrity checking indicate what changed: 0 (no changes), 1 (files added), 2 (files modified), 4 (files deleted), 8 (errors)."],["CONFIGURATION","**/etc/tripwire/tw.cfg**\n> Main configuration file specifying database location, report directory, and mail settings.\n\n**/etc/tripwire/tw.pol**\n> Policy file defining which files and directories to monitor and what attributes to check.\n\n**/var/lib/tripwire/$(HOSTNAME).twd**\n> Baseline database file containing recorded filesystem state.\n\n**TRIPWIRE_SITE_KEY**\n> Path to the site key used for signing policy and configuration files.\n\n**TRIPWIRE_LOCAL_KEY**\n> Path to the local key used for signing the database and reports."],["CAVEATS","The baseline database must be created after a known-clean system state. Running with verbose output can expose sensitive file paths. Reports should be reviewed promptly as attackers could modify files and then restore them between checks. The database file should be stored securely, preferably on read-only media."],["HISTORY","**Tripwire** was originally developed by **Gene Kim** and **Gene Spafford** at **Purdue University** in **1992** as an academic research project. It became one of the first file integrity monitoring tools widely adopted for Unix security. The open-source version was later maintained by Tripwire Inc., with a commercial enterprise version also available. It remains a foundational tool in host-based intrusion detection."],["INSTALL","```apt: sudo apt install tripwire```\n\n```dnf: sudo dnf install tripwire```\n\n```zypper: sudo zypper install tripwire```"],["SEE ALSO","[aide](/man/aide)(1), [samhain](/man/samhain)(8), [md5sum](/man/md5sum)(1), [sha256sum](/man/sha256sum)(1)"]],"e":["aide","samhain","md5sum","sha256sum"],"x":"sudo tripwire --init"},"virsh-pool-undefine":{"n":"virsh-pool-undefine","t":"Remove storage pool definition","s":[["TAGLINE","Remove storage pool definition"],["TLDR","**Undefine storage pool**\n\n```virsh pool-undefine [pool]```"],["SYNOPSIS","**virsh** **pool-undefine** _pool_"],["DESCRIPTION","**virsh pool-undefine** removes the libvirt configuration for a storage pool, making it transient. If the pool is currently running, the runtime instance continues until stopped, but no longer persists across libvirtd restarts. Pool contents on disk are untouched; use `virsh pool-delete` to destroy the underlying storage."],["PARAMETERS","_pool_\n> Pool name or UUID."],["CAVEATS","Does not delete files on the backing storage. Running pools become transient and disappear on the next daemon restart."],["SEE ALSO","[virsh](/man/virsh)(1), [virsh-pool-destroy](/man/virsh-pool-destroy)(1), [virsh-pool-delete](/man/virsh-pool-delete)(1)"]],"e":["virsh","virsh-pool-destroy","virsh-pool-delete"],"x":"virsh pool-undefine [pool]"},"virt-viewer":{"n":"virt-viewer","t":"Display VM graphical console","s":[["TAGLINE","Display VM graphical console"],["TLDR","**Connect to VM**\n\n```virt-viewer [vmname]```\n\n**Connect via URI**\n\n```virt-viewer -c [qemu:///system] [vmname]```\n\n**Connect to remote**\n\n```virt-viewer -c [qemu+ssh://host/system] [vmname]```\n\n**Full screen**\n\n```virt-viewer --full-screen [vmname]```\n\n**Wait for VM**\n\n```virt-viewer --wait [vmname]```"],["SYNOPSIS","**virt-viewer** [_-c uri_] [_--full-screen_] [_options_] _vmname_"],["PARAMETERS","**-c** _URI_\n> Connection URI.\n\n**--full-screen**\n> Full screen mode.\n\n**--wait**\n> Wait for VM.\n\n**--reconnect**\n> Auto reconnect.\n\n**--hotkeys** _KEYS_\n> Custom hotkeys."],["DESCRIPTION","**virt-viewer** is a lightweight graphical console client for viewing the display of virtual machines managed by libvirt. It connects to running VMs and renders their graphical output in a native window, providing a simple alternative to the full virt-manager application when only console access is needed.\n\nThe tool supports both SPICE and VNC display protocols, automatically detecting the appropriate connection method from the VM's configuration. Remote connections are supported through SSH tunneling, allowing users to securely access VM consoles on remote hypervisors without exposing display ports directly.\n\nAdditional features include full-screen mode for dedicated display use, USB device redirection for passing local hardware to the guest, shared clipboard support, and automatic screen resolution adjustment. The viewer can also wait for a VM to start before connecting, making it useful in scripted or automated workflows."],["CAVEATS","libvirt required. VM must be running. X11/Wayland needed."],["HISTORY","**virt-viewer** is part of the **virt-manager** project, providing a minimal tool for viewing VM graphical consoles."],["INSTALL","```apt: sudo apt install virt-viewer```\n\n```dnf: sudo dnf install virt-viewer```\n\n```pacman: sudo pacman -S virt-viewer```\n\n```apk: sudo apk add virt-viewer```\n\n```zypper: sudo zypper install virt-viewer```\n\n```nix: nix profile install nixpkgs#virt-viewer```"],["SEE ALSO","[virsh](/man/virsh)(1), [virt-manager](/man/virt-manager)(1), [remote-viewer](/man/remote-viewer)(1)"]],"e":["virsh","virt-manager","remote-viewer"],"x":"virt-viewer [vmname]"},"virt-xml-validate":{"n":"virt-xml-validate","t":"Validate libvirt XML files","s":[["TAGLINE","Validate libvirt XML files"],["TLDR","Validate against **specific** schema\n\n```virt-xml-validate [path/to/file.xml] [schema]```\n\nValidate **domain** XML\n\n```virt-xml-validate [path/to/domain.xml] domain```\n\nValidate **network** XML\n\n```virt-xml-validate [path/to/network.xml] network```\n\nValidate **storage pool** XML\n\n```virt-xml-validate [path/to/pool.xml] storagepool```"],["SYNOPSIS","**virt-xml-validate** _FILE_ [_SCHEMA_]"],["PARAMETERS","**FILE**\n> XML file to validate\n\n**SCHEMA**\n> Schema name to validate against (auto-detected from root element if omitted). Valid schemas include: domain, domainsnapshot, domaincheckpoint, domainbackup, domaincaps, network, networkport, nwfilter, nwfilterbinding, storagepool, storagepoolcaps, storagevol, nodedev, capability, interface, secret, cpu.\n\n**-h**, **--help**\n> Display command-line help then exit.\n\n**-V**, **--version**\n> Display version information then exit."],["DESCRIPTION","**virt-xml-validate** validates libvirt XML files against their schemas. If no schema is specified, it is determined automatically from the root element in the XML file.\n\nThis tool helps verify that XML configuration files are valid before using them with libvirt."],["CAVEATS","Valid XML doesn't guarantee the configuration will work. Some schema errors may not be detected. Requires libvirt schemas to be installed."],["INSTALL","```apt: sudo apt install libvirt-clients```\n\n```dnf: sudo dnf install libvirt-client```\n\n```apk: sudo apk add libvirt-client```\n\n```zypper: sudo zypper install libvirt-client```"],["SEE ALSO","[virt-xml](/man/virt-xml)(1), [virsh](/man/virsh)(1)"]],"e":["virt-xml","virsh"],"x":"virt-xml-validate [path/to/file.xml] [schema]"},"wdiff":{"n":"wdiff","t":"Word-by-word file comparison tool","s":[["TAGLINE","Word-by-word file comparison tool"],["TLDR","**Compare files word-by-word**\n\n```wdiff [file1.txt] [file2.txt]```\n\n**Statistics only**\n\n```wdiff -s [file1.txt] [file2.txt]```\n\n**Ignore case**\n\n```wdiff -i [file1.txt] [file2.txt]```\n\n**Use color output**\n\n```wdiff -w $'\\033[1;31m' -x $'\\033[0m' -y $'\\033[1;32m' -z $'\\033[0m' [file1] [file2]```\n\n**Show only differences (suppress common words)**\n\n```wdiff -3 [file1.txt] [file2.txt]```\n\n**Avoid wrapping markers across lines**\n\n```wdiff -n [file1.txt] [file2.txt]```"],["SYNOPSIS","**wdiff** [_-s_] [_-i_] [_options_] _file1_ _file2_"],["PARAMETERS","**-s**, **--statistics**\n> Show word count statistics and percentage changes.\n\n**-i**, **--ignore-case**\n> Ignore case differences.\n\n**-3**, **--no-common**\n> Suppress common (unchanged) words; show only differences.\n\n**-1**, **--no-deleted**\n> Suppress deleted words from output.\n\n**-2**, **--no-inserted**\n> Suppress inserted words from output.\n\n**-n**, **--avoid-wraps**\n> Prevent markup from spanning line boundaries.\n\n**-a**, **--auto-pager**\n> Automatically pipe output through a pager.\n\n**-t**, **--terminal**\n> Use termcap strings for terminal emphasis.\n\n**-l**, **--less-mode**\n> Overstrike formatting optimized for the less pager.\n\n**-p**, **--printer**\n> Overstrike emphasis for printer output.\n\n**-d**, **--diff-input**\n> Accept unified diff format as input.\n\n**-w** _STRING_\n> Start delete marker (default `[-`).\n\n**-x** _STRING_\n> End delete marker (default `-]`).\n\n**-y** _STRING_\n> Start insert marker (default `{+`).\n\n**-z** _STRING_\n> End insert marker (default `+}`)."],["DESCRIPTION","**wdiff** compares two files on a word-by-word basis rather than line-by-line like traditional diff. This makes it particularly useful for comparing prose and text documents where changes often involve individual words within paragraphs rather than entire lines.\n\nBy default, deleted words are enclosed in `[-...-]` markers and inserted words in `{+...+}` markers, making changes easy to spot in the output. These markers can be customized with ANSI escape codes to produce colored output highlighting additions and deletions visually.\n\nThe statistics mode shows counts of deleted, inserted, and common words along with percentage changes. Case-insensitive comparison is available for situations where capitalization differences should be ignored."],["CAVEATS","Large files may be slow. Complex formatting lost. Line structure ignored."],["HISTORY","**wdiff** was created as part of GNU and is maintained as a standalone GNU package. It provides word-level differences suited for prose and natural language text where line-based diff output is hard to read."],["INSTALL","```apt: sudo apt install wdiff```\n\n```dnf: sudo dnf install wdiff```\n\n```pacman: sudo pacman -S wdiff```\n\n```zypper: sudo zypper install wdiff```\n\n```brew: brew install wdiff```\n\n```nix: nix profile install nixpkgs#wdiff```"],["SEE ALSO","[diff](/man/diff)(1), [colordiff](/man/colordiff)(1), [dwdiff](/man/dwdiff)(1)"]],"e":["diff","colordiff","dwdiff"],"x":"wdiff [file1.txt] [file2.txt]"},"xkbcli":{"n":"xkbcli","t":"Query, compile and test XKB keymaps from the command line","s":[["TAGLINE","Query, compile and test XKB keymaps from the command line"],["TLDR","**List** all available XKB rules, models, layouts, variants and options\n\n```xkbcli list```\n\n**Compile** the active system keymap and dump it to stdout\n\n```xkbcli compile-keymap```\n\n**Compile a keymap** with a specific layout/variant\n\n```xkbcli compile-keymap --layout [us] --variant [dvorak]```\n\n**Show how to type** a particular Unicode codepoint\n\n```xkbcli how-to-type [0x00e9]```\n\n**Interactively debug** XKB keymaps under **Wayland**\n\n```xkbcli interactive-wayland```\n\n**Interactively debug** XKB keymaps under **X11**\n\n```xkbcli interactive-x11```\n\n**Interactively debug** XKB keymaps reading from **evdev** (requires root)\n\n```sudo xkbcli interactive-evdev```\n\n**Print** the version of libxkbcommon\n\n```xkbcli --version```"],["SYNOPSIS","**xkbcli** _command_ [_arguments_]\n\n**xkbcli** [**--help** | **--version**]"],["PARAMETERS","**--help**\n> Show usage and exit.\n\n**--version**\n> Print the libxkbcommon version and exit."],["SUBCOMMANDS","**compile-keymap**\n> Compile an RMLVO (rules + model + layout + variant + options) keymap source into the keymap text format and print it. Accepts **--rules**, **--model**, **--layout**, **--variant**, **--options** and **--keymap**.\n\n**compile-compose**\n> Compile a Compose file and print the resulting Compose table.\n\n**how-to-type** _codepoint_\n> Show every keysym/key combination that produces the given Unicode codepoint (decimal, or **0x** hex).\n\n**interactive-evdev**\n> Interactive XKB debugger reading raw events from **/dev/input/eventN**; usually requires root or membership in the **input** group.\n\n**interactive-x11**\n> Interactive XKB debugger using the live X11 keymap.\n\n**interactive-wayland**\n> Interactive XKB debugger using the live Wayland keymap.\n\n**list**\n> Print all rules, models, layouts, variants and options known to libxkbcommon. Optionally filter with **--ruleset**."],["DESCRIPTION","**xkbcli** is the user-facing front-end shipped with **libxkbcommon**, the keymap library used by Wayland compositors, the X.Org server (since 1.20), GTK, Qt, EFL and most modern toolkits. It exposes the library's compilation, lookup and inspection APIs as a single multiplexed binary with sub-commands.\n\nIt is most often used in three ways. First, **xkbcli list** answers the question \"which layouts/variants does my system know about?\" — the same data shown by graphical keyboard pickers but in a machine-parseable form. Second, **xkbcli compile-keymap** reproduces exactly what a compositor or X server does when it compiles a keymap, which is invaluable when diagnosing why a custom layout is not being applied. Third, the **interactive-*** sub-commands print the live keysym, modifier state, group and Unicode codepoint of every keypress, making **xkbcli** the equivalent of **xev**(1) for the libxkbcommon era.\n\nThe tool relies on the system's **xkeyboard-config** data files (typically under **/usr/share/X11/xkb**), so its output reflects the keymaps that any libxkbcommon-based program would see."],["CAVEATS","**interactive-evdev** reads raw kernel input events and therefore needs read access to **/dev/input/event\\***; on most distributions this means **root** or membership of the **input** group. **interactive-wayland** and **interactive-x11** are only built when their respective headers are available at compile time, so they may be missing on minimal installations. The **list** sub-command shows what is shipped on the system, not what is currently active — use the compositor or **setxkbmap -query** for that."],["HISTORY","**libxkbcommon** was forked from **libxkbfile**/**xkbcomp** in **2012** by **Daniel Stone** and **Ran Benita** to provide a modern, X-server-independent XKB implementation usable by Wayland compositors. The **xkbcli** umbrella tool was added later as a single entry point for the family of debugging utilities (replacing the per-tool **xkbcompose**, **xkbgetkeymap**, etc.). It is now distributed by every major Linux distribution as part of the **libxkbcommon-tools** / **libxkbcommon-utils** package."],["INSTALL","```apt: sudo apt install libxkbcommon-tools```\n\n```apk: sudo apk add xkbcli```\n\n```zypper: sudo zypper install libxkbcommon-tools```"],["SEE ALSO","[xkbcomp](/man/xkbcomp)(1), [setxkbmap](/man/setxkbmap)(1), [xev](/man/xev)(1), [localectl](/man/localectl)(1)"]],"e":["xkbcomp","setxkbmap","xev","localectl"],"x":"xkbcli list"},"xml-canonic":{"n":"xml-canonic","t":"Produce canonical XML form","s":[["TAGLINE","Produce canonical XML form"],["TLDR","**Canonicalize XML (W3C C14N)**\n\n```xml canonic [file.xml]```\n\n**Exclusive canonicalization**\n\n```xml canonic --exc-c14n [file.xml]```\n\n**Canonicalize without comments**\n\n```xml canonic --without-comments [file.xml]```\n\n**Read from stdin**\n\n```cat file.xml | xml canonic```"],["SYNOPSIS","**xml** **canonic** [_options_] [_file_]"],["DESCRIPTION","**xml canonic** produces a canonical XML form of the input document as defined by the W3C XML Canonicalization (C14N) specification. Canonicalization normalizes whitespace, attribute order, namespace declarations, and character encoding so that two logically equivalent documents produce identical byte streams.\n\nIt is part of the **xmlstarlet** command-line XML toolkit. Canonical XML is commonly used as a preparation step before computing cryptographic digests for XML digital signatures (XML-DSig), and for reliable document comparison."],["PARAMETERS","**--exc-c14n**\n> Use Exclusive XML Canonicalization (EXC-C14N) instead of the default Canonical XML 1.0.\n\n**--without-comments**\n> Omit comments from canonical output."],["CAVEATS","Canonicalization is lossy: comments may be stripped, whitespace normalized, and namespace declarations rewritten. Non-canonical formatting differences are intentionally erased. Cryptographic signatures over canonicalized XML require both parties to use the same C14N variant."],["INSTALL","```dnf: sudo dnf install xmlstarlet```\n\n```pacman: sudo pacman -S xmlstarlet```\n\n```apk: sudo apk add xmlstarlet```\n\n```zypper: sudo zypper install xmlstarlet```\n\n```brew: brew install xmlstarlet```\n\n```nix: nix profile install nixpkgs#xmlstarlet```"],["SEE ALSO","[xml-c14n](/man/xml-c14n)(1), [xml-format](/man/xml-format)(1)"]],"e":["xml-c14n","xml-format"],"x":"xml canonic [file.xml]"},"yolo":{"n":"yolo","t":"Ultralytics YOLO object detection and computer vision CLI","s":[["TAGLINE","Ultralytics YOLO object detection and computer vision CLI"],["TLDR","**Run object detection** on an image\n\n```yolo detect predict model=[yolo11n.pt] source=[image.jpg]```\n\n**Train a model** on a custom dataset\n\n```yolo detect train data=[data.yaml] model=[yolo11n.pt] epochs=[100] imgsz=[640]```\n\n**Validate model** accuracy\n\n```yolo detect val model=[best.pt] data=[data.yaml]```\n\n**Export model** to ONNX format\n\n```yolo export model=[best.pt] format=[onnx]```\n\n**Track objects** in video\n\n```yolo detect track model=[yolo11n.pt] source=[video.mp4]```\n\n**Run pose estimation**\n\n```yolo pose predict model=[yolo11n-pose.pt] source=[image.jpg]```\n\n**Benchmark** model performance across export formats\n\n```yolo benchmark model=[yolo11n.pt] imgsz=[640]```"],["SYNOPSIS","**yolo** _TASK_ _MODE_ [_arg=value_...]"],["PARAMETERS","## Tasks\n\n**detect**\n> Object detection.\n\n**segment**\n> Instance segmentation.\n\n**classify**\n> Image classification.\n\n**pose**\n> Pose estimation.\n\n**obb**\n> Oriented bounding box detection.\n\n## Modes\n\n**train**\n> Train a model on a dataset.\n\n**val**\n> Validate model accuracy.\n\n**predict**\n> Run inference on images, video, or streams.\n\n**export**\n> Convert model to deployment formats (ONNX, TensorRT, CoreML, etc.).\n\n**track**\n> Multi-object tracking on video.\n\n**benchmark**\n> Benchmark model speed and accuracy across export formats.\n\n## Common Arguments\n\n**model=**_path_\n> Model file path (e.g., yolo11n.pt).\n\n**data=**_path_\n> Dataset configuration YAML file.\n\n**source=**_path_\n> Input source: image, video, directory, URL, or webcam (0).\n\n**epochs=**_n_\n> Number of training epochs.\n\n**imgsz=**_size_\n> Input image size (default: 640).\n\n**batch=**_n_\n> Batch size.\n\n**device=**_id_\n> Device: GPU id (0, 0,1) or cpu.\n\n**format=**_fmt_\n> Export format: onnx, engine, coreml, tflite, etc.\n\n**conf=**_threshold_\n> Confidence threshold for predictions."],["DESCRIPTION","**yolo** is the CLI for Ultralytics YOLO, a state-of-the-art computer vision framework. It provides commands for object detection, instance segmentation, image classification, pose estimation, and oriented bounding box detection from the terminal.\n\nThe tool supports a complete workflow: **train** builds models from datasets, **val** evaluates model accuracy, **predict** runs inference on images or video, **export** converts models to deployment formats like ONNX and TensorRT, **track** performs multi-object tracking on video streams, and **benchmark** tests model performance across formats.\n\nEach command accepts an optional task type (detect, segment, classify, pose, obb) and a required mode. Arguments are passed as key=value pairs. Pre-trained models can be used directly for inference or fine-tuned on custom datasets. GPU acceleration is supported through PyTorch."],["CAVEATS","Requires Python and PyTorch. GPU recommended for training. Pre-trained model weights are downloaded automatically on first use. Use `yolo cfg` to view all available configuration arguments."],["SEE ALSO","[python](/man/python)(1), [pip](/man/pip)(1), [nvidia-smi](/man/nvidia-smi)(1)"]],"e":["python","pip","nvidia-smi"],"x":"yolo detect predict model=[yolo11n.pt] source=[image.jpg]"},"zeroclaw":{"n":"zeroclaw","t":"Autonomous AI agent runtime built in Rust","s":[["TAGLINE","Autonomous AI agent runtime built in Rust"],["TLDR","**Initialize** configuration with interactive setup wizard\n\n```zeroclaw onboard --interactive```\n\n**Start an interactive** CLI agent session\n\n```zeroclaw agent```\n\n**Send a single message** without entering interactive mode\n\n```zeroclaw agent -m \"[message]\"```\n\n**Start the full autonomous daemon** with channels and scheduler\n\n```zeroclaw daemon```\n\n**Check system health** and run diagnostics\n\n```zeroclaw doctor```\n\n**List all supported** LLM providers\n\n```zeroclaw providers```"],["SYNOPSIS","**zeroclaw** [_command_] [_subcommand_] [_options_]"],["PARAMETERS","**onboard**\n> Initialize or reconfigure the workspace. Creates **~/.zeroclaw/config.toml** and scaffold files.\n\n**onboard --interactive**\n> Run the full 9-step configuration wizard.\n\n**onboard --api-key** _KEY_ **--provider** _PROVIDER_ [**--model** _MODEL_]\n> Non-interactive setup with API key and provider in one command.\n\n**onboard --channels-only**\n> Repair or reconfigure channels and allowlists only.\n\n**agent** [**-m** _MESSAGE_]\n> Run interactive CLI agent or send a single message with **-m**.\n\n**agent --provider** _PROVIDER_\n> Override the default provider for this session.\n\n**gateway** [**--port** _PORT_]\n> Start the HTTP/WebSocket server for external integrations. Default port: 42617.\n\n**daemon**\n> Start the full autonomous runtime with gateway, channels, heartbeat, and scheduler.\n\n**status**\n> Display comprehensive system status including provider, memory, channels, and security.\n\n**doctor** [**models** | **traces**]\n> Run system diagnostics. Optionally check model catalogs or inspect runtime traces.\n\n**service install** | **start** | **stop** | **status** | **restart**\n> Manage zeroclaw as a background system service (systemd or OpenRC).\n\n**channel list** | **doctor** | **start**\n> List, health-check, or start configured messaging channels.\n\n**channel add** _TYPE_ _JSON_CONFIG_\n> Add a new messaging channel configuration.\n\n**channel bind-telegram** _USER_ID_\n> Add a Telegram user to the allowlist.\n\n**auth login** _PROVIDER_ [_PROFILE_]\n> Authenticate via OAuth or device code flow.\n\n**auth status** | **logout** _PROFILE_ID_\n> Show or remove authentication profiles.\n\n**memory stats** | **list** | **search** _QUERY_ | **delete** _KEY_ | **prune**\n> Manage the built-in memory system.\n\n**cron add** _NAME_ _SCHEDULE_ _MESSAGE_ [**--tz** _TIMEZONE_]\n> Schedule a recurring task with a cron expression.\n\n**cron list** | **remove** _NAME_\n> List or remove scheduled tasks.\n\n**skills list** | **install** _NAME_ | **remove** _NAME_\n> Manage agent skills and extensions.\n\n**migrate openclaw** [**--dry-run**]\n> Import memory from an OpenClaw runtime.\n\n**completions** _SHELL_\n> Generate shell completions for bash, zsh, fish, powershell, or nushell.\n\n**providers**\n> List all supported LLM providers and aliases.\n\n**estop** [**--resume**]\n> Engage or resume from emergency stop."],["DESCRIPTION","**zeroclaw** is a lightweight, security-first autonomous AI agent runtime built entirely in Rust. It serves as infrastructure for agentic workflows, abstracting models, tools, memory, and execution into a single binary that can be deployed across ARM, x86, and RISC-V architectures.\n\nThe runtime compiles to an approximately 9MB binary with cold start under 10 milliseconds and less than 5MB RAM usage at idle. It uses a trait-driven architecture where providers, channels, memory backends, and tools are swappable through configuration without code changes.\n\nZeroClaw includes a built-in hybrid search memory system combining vector embeddings with keyword search (SQLite-backed), requiring no external dependencies. It supports 28+ LLM providers including OpenRouter, Anthropic, OpenAI, Gemini, Ollama, and any OpenAI-compatible endpoint. Messaging integrations cover 70+ channels including Telegram, Discord, Slack, iMessage, Matrix, Signal, and WhatsApp.\n\nThe agent supports multi-turn conversations with context preservation, tool execution (shell, file, git, browser), scheduled tasks via cron expressions, and hardware peripheral control for IoT devices."],["CONFIGURATION","Configuration is stored in **~/.zeroclaw/config.toml**. Run **zeroclaw onboard --interactive** for guided setup.\n\n```\n[providers]\ndefault = \"openrouter\"\n\n[providers.openrouter]\napi_key = \"sk-or-v1-xxx\"\nmodel = \"anthropic/claude-opus-4-5\"\n\n[memory]\nbackend = \"sqlite\"\n\n[runtime]\nkind = \"native\"\n\n[channels.telegram]\nbot_token = \"123:ABC...\"\nallowed_users = [\"123456789\"]\n```\n\nSupported memory backends: **sqlite** (default, hybrid search), **postgres**, **markdown**, and **none** (stateless).\n\nEnvironment variables override config values: **ZEROCLAW_API_KEY**, **ZEROCLAW_PROVIDER**, **ZEROCLAW_MODEL**, **ZEROCLAW_WORKSPACE**."],["CAVEATS","ZeroClaw executes shell commands and file operations with your user permissions. The agent can modify files and run arbitrary commands when tool use is enabled. API keys are stored in **config.toml** and **auth-profiles.json** (encrypted at rest if secrets encryption is enabled). Channel integrations require the daemon to be running. Compilation from source requires at least 2GB RAM and 6GB disk space due to the Rust toolchain."],["HISTORY","ZeroClaw was created by **ZeroClaw Labs** and first released in **2025** as a Rust-based alternative to existing AI agent runtimes. It was designed around the principle of zero overhead and zero lock-in, targeting deployment on resource-constrained hardware while maintaining production-grade extensibility. The project introduced a trait-driven architecture allowing providers, channels, and tools to be swapped without code changes, and gained attention for achieving sub-10ms cold start times with a single-binary deployment model."],["INSTALL","```brew: brew install zeroclaw```\n\n```nix: nix profile install nixpkgs#zeroclaw```"],["SEE ALSO","[nanobot](/man/nanobot)(1), [picoclaw](/man/picoclaw)(1), [claude](/man/claude)(1), [openclaw](/man/openclaw)(1)"]],"e":["nanobot","picoclaw","claude","openclaw"],"x":"zeroclaw onboard --interactive"}});
