window.LCL_BUCKET(119,{"aa-disable":{"n":"aa-disable","t":"Disable AppArmor security profiles","s":[["TAGLINE","Disable AppArmor security profiles"],["TLDR","**Disable** a profile\n\n```sudo aa-disable [path/to/profile]```\n\nDisable **multiple profiles**\n\n```sudo aa-disable [path/to/profile1] [path/to/profile2]```\n\nDisable profiles in a **directory**\n\n```sudo aa-disable --dir [path/to/profiles]```\n\nDisable a profile **without reloading** the policy\n\n```sudo aa-disable --no-reload [path/to/profile]```"],["SYNOPSIS","**aa-disable** _executable_ [_executable_ ...] [_-d /path/to/profiles_] [_--no-reload_]"],["DESCRIPTION","**aa-disable** is used to disable one or more AppArmor profiles. This command will unload the profile from the kernel and prevent the profile from being loaded on AppArmor startup.\n\nA symbolic link to the profile is created in **/etc/apparmor.d/disable/**, which keeps the profile from being loaded automatically when AppArmor is restarted. The profile file itself remains in **/etc/apparmor.d** but is not active. Use **aa-enforce** or **aa-complain** to re-enable a disabled profile."],["PARAMETERS","**-d, --dir /path/to/profiles**\n> Specifies the directory containing AppArmor profiles; defaults to /etc/apparmor.d\n\n**--no-reload**\n> Do not reload the profile after modifying it"],["CAVEATS","Disabling a profile removes security protections for the associated application. The application will run without AppArmor confinement until the profile is re-enabled."],["HISTORY","Part of the **AppArmor** utilities package for managing application security profiles on Linux systems."],["INSTALL","```apk: sudo apk add apparmor-utils```\n\n```zypper: sudo zypper install apparmor-utils```\n\n```nix: nix profile install nixpkgs#apparmor-utils```"],["SEE ALSO","[aa-enforce](/man/aa-enforce)(8), [aa-complain](/man/aa-complain)(8), [aa-remove-unknown](/man/aa-remove-unknown)(8), [aa-status](/man/aa-status)(8), [apparmor](/man/apparmor)(7)"],["RESOURCES","```[Source code](https://gitlab.com/apparmor/apparmor)```\n\n```[Homepage](https://apparmor.net/)```\n\n```[Documentation](https://gitlab.com/apparmor/apparmor/-/wikis/home)```"]],"e":["aa-enforce","aa-complain","aa-remove-unknown","aa-status","apparmor"],"x":"sudo aa-disable [path/to/profile]"},"airshare":{"n":"airshare","t":"Share files between devices on the local network","s":[["TAGLINE","Share files between devices on the local network"],["TLDR","**Send** a file with a code\n\n```airshare [mycode] [file.txt]```\n\n**Receive** files using a code\n\n```airshare [mycode]```\n\n**Send** multiple files\n\n```airshare [mycode] [file1.txt] [file2.txt]```\n\n**Send text** content directly\n\n```airshare [mycode] -t \"[Hello, World!]\"```\n\n**Host a receiving** (upload) server\n\n```airshare [mycode] -u```\n\n**Send clipboard** contents as text\n\n```airshare [mycode] -cs```\n\n**Specify a custom port**\n\n```airshare [mycode] [file.txt] -p [9000]```"],["SYNOPSIS","**airshare** [_options_] _code_ [_files_]"],["DESCRIPTION","**airshare** is a Python-based cross-platform file sharing tool that transfers files between devices on the same local network. It uses mDNS for device discovery and identifies transfers with a simple code word. Recipients access shared content by using the same code.\n\nThe tool provides a simple alternative to complex file sharing setups, requiring no server configuration or account creation. Shared content can also be accessed via a web browser at `http://<code>.local:8000`."],["PARAMETERS","_CODE_\n> An identifying code word for the sharing session.\n\n_FILES_\n> File(s) or directories to send.\n\n**-p**, **--port** _INTEGER_\n> Specify the port number for the server (default: 8000).\n\n**-t**, **--text** _TEXT_\n> Send text content directly. Enclose multiple words in quotes.\n\n**-u**, **--upload**\n> Host a receiving server to accept uploaded files.\n\n**-cs**, **--clip-send**\n> Send clipboard contents as text.\n\n**-cr**, **--clip-receive**\n> Receive content and copy it to the clipboard.\n\n**-fp**, **--file-path**\n> Send files whose paths have been copied to the clipboard.\n\n**--version**\n> Display version information."],["CAVEATS","Both devices must be on the same local network. Large files are processed in chunks but may take time depending on network speed. The tool requires mDNS support on the network."],["HISTORY","**airshare** was developed as a Python-based alternative to AirDrop for cross-platform local file sharing, leveraging mDNS for zero-configuration networking."],["INSTALL","```brew: brew install airshare```"],["SEE ALSO","[croc](/man/croc)(1), [wormhole](/man/wormhole)(1), [scp](/man/scp)(1)"]],"e":["croc","wormhole","scp"],"x":"airshare [mycode] [file.txt]"},"apksigner":{"n":"apksigner","t":"Sign and verify Android APK files","s":[["TAGLINE","Sign and verify Android APK files"],["TLDR","**Sign** an APK\n\n```apksigner sign --ks [keystore.jks] [app.apk]```\n\nSign with **specific key alias**\n\n```apksigner sign --ks [keystore.jks] --ks-key-alias [alias] [app.apk]```\n\n**Verify** APK signature\n\n```apksigner verify [app.apk]```\n\nVerify with **details**\n\n```apksigner verify --verbose --print-certs [app.apk]```\n\nSign with a **PKCS #8 key and certificate** (no keystore)\n\n```apksigner sign --key [key.pk8] --cert [cert.x509.pem] [app.apk]```\n\nSign targeting a **minimum SDK version**\n\n```apksigner sign --ks [keystore.jks] --min-sdk-version [21] [app.apk]```"],["SYNOPSIS","**apksigner** **sign** [_options_] _apk_\n\n**apksigner** **verify** [_options_] _apk_\n\n**apksigner** **rotate** [_options_]\n\n**apksigner** **lineage** [_options_]"],["DESCRIPTION","**apksigner** is the official Android SDK tool for signing APK files. It supports multiple signature schemes (v1/JAR, v2, v3, v4) and can sign aligned APKs produced by zipalign.\n\nThe tool ensures APKs meet Android's signing requirements for installation and updates, with newer schemes providing better security and verification speed."],["PARAMETERS","**sign**\n> Sign an APK.\n\n**verify**\n> Verify APK signatures.\n\n**rotate**\n> Rotate a signing key, producing a new lineage from an old key to a new one.\n\n**lineage**\n> Inspect or update a signing certificate lineage.\n\n**--ks** _file_\n> Keystore file. Use **NONE** for a keystore type that needs no file (e.g. some PKCS #11).\n\n**--ks-key-alias** _alias_\n> Key alias in keystore.\n\n**--ks-pass** _spec_\n> Keystore password (pass:, env:, file:, or stdin).\n\n**--key-pass** _spec_\n> Key password (same input formats as --ks-pass).\n\n**--key** _file_\n> PKCS #8 private key file (alternative to --ks).\n\n**--cert** _file_\n> X.509 certificate chain file (used with --key).\n\n**--out** _file_\n> Signed APK output path.\n\n**--min-sdk-version** _int_\n> Lowest API level for which the signature is verified. Defaults to the manifest minSdkVersion.\n\n**--max-sdk-version** _int_\n> Highest API level for which the signature is verified.\n\n**--v1-signing-enabled** _bool_\n> Enable JAR signing (v1).\n\n**--v2-signing-enabled** _bool_\n> Enable APK Signature Scheme v2.\n\n**--v3-signing-enabled** _bool_\n> Enable APK Signature Scheme v3.\n\n**--v4-signing-enabled** _bool|only_\n> Enable APK Signature Scheme v4, producing a separate .idsig file.\n\n**--verbose**, **-v**\n> Verbose output.\n\n**--print-certs**\n> Print certificate details (verify only)."],["CAVEATS","APK must be zipaligned before signing with v2+. v1-only signing is deprecated for Android 11+. Key rotation requires v3 signing. Private key security is critical."],["HISTORY","**apksigner** was introduced in Android SDK Build Tools 24.0.3 (**2016**) alongside APK Signature Scheme v2, replacing jarsigner as the recommended signing tool."],["INSTALL","```nix: nix profile install nixpkgs#apksigner```"],["SEE ALSO","[zipalign](/man/zipalign)(1), [keytool](/man/keytool)(1), [jarsigner](/man/jarsigner)(1), [aapt](/man/aapt)(1)"],["RESOURCES","```[Source code](https://android.googlesource.com/platform/tools/apksig/)```\n\n```[Documentation](https://developer.android.com/tools/apksigner)```"]],"e":["zipalign","keytool","jarsigner","aapt"],"x":"apksigner sign --ks [keystore.jks] [app.apk]"},"argospm":{"n":"argospm","t":"Argos Translate package manager for offline translation models","s":[["TAGLINE","Argos Translate package manager for offline translation models"],["TLDR","**Update** the remote package index\n\n```argospm update```\n\n**Search** for a translation package\n\n```argospm search --from-lang [en] --to-lang [es]```\n\n**Install** a translation package\n\n```argospm install [translate-en_es]```\n\n**List** installed translation packages\n\n```argospm list```\n\n**Remove** an installed translation package\n\n```argospm remove [translate-en_es]```"],["SYNOPSIS","**argospm** [**-h**] {**update**,**search**,**install**,**list**,**remove**} [_args_]"],["PARAMETERS","**update**\n> Download the remote package index from the Argos Translate package repository.\n\n**search**\n> Search for available translation packages in the remote index. Use **--from-lang** and **--to-lang** to filter by language pair.\n\n**install**\n> Install a translation package by name (e.g., translate-en_es).\n\n**list**\n> List all currently installed translation packages.\n\n**remove**\n> Remove an installed translation package by name.\n\n**-h**, **--help**\n> Display help message and exit."],["DESCRIPTION","**argospm** is the package management CLI for **Argos Translate**, an open-source offline translation library. It manages the download and installation of translation language packages (neural machine translation models) that enable offline text translation between language pairs.\n\nEach package provides translation between a specific source and target language. Packages are downloaded from the Argos Translate package index and stored locally for use by the **argos-translate** Python library and **LibreTranslate** web interface."],["CAVEATS","Translation packages can be large as they contain neural network models. The package index must be updated with **argospm update** before searching or installing new packages. Quality varies between language pairs — well-resourced language pairs like English-Spanish have better translation quality than less common pairs."],["HISTORY","**argospm** is part of the **Argos Translate** project, an open-source neural machine translation library built in **Python** using **OpenNMT**. The project was created by **Argos Open Technologies** to provide free, offline translation capabilities as an alternative to cloud-based services. It also powers **LibreTranslate**, a self-hosted translation API."],["SEE ALSO","[argos-translate](/man/argos-translate)(1), [trans](/man/trans)(1), [pip](/man/pip)(1)"]],"e":["argos-translate","trans","pip"],"x":"argospm update"},"austin-tui":{"n":"austin-tui","t":"Top-like TUI for the Austin Python frame stack sampler","s":[["TAGLINE","Top-like TUI for the Austin Python frame stack sampler"],["TLDR","**Profile a Python script**\n\n```austin-tui python3 [path/to/script.py]```\n\n**Attach to a running Python process** by PID\n\n```sudo austin-tui -p [pid]```\n\n**Attach to a process and its child processes**\n\n```sudo austin-tui -Cp [pid]```\n\n**Profile memory usage** instead of CPU time\n\n```austin-tui -m python3 [path/to/script.py]```\n\n**Profile with a custom sampling interval** (in microseconds)\n\n```austin-tui -i [200] python3 [path/to/script.py]```\n\n**Sample for a limited duration** (in seconds)\n\n```austin-tui -x [10] python3 [path/to/script.py]```"],["SYNOPSIS","**austin-tui** [_options_] [_command_ [_args..._]]\n\n**austin-tui** [_options_] **-p** _pid_"],["PARAMETERS","**-p** _pid_, **--pid** _pid_\n> Attach to an existing Python process by its process ID.\n\n**-C**, **--children**\n> Also sample child Python processes. Navigate between them with the arrow keys.\n\n**-m**, **--memory**\n> Profile memory usage instead of wall-clock time.\n\n**-c**, **--cpu**\n> Sample on-CPU stacks only, ignoring idle time.\n\n**-i** _microseconds_, **--interval** _microseconds_\n> Set the sampling interval in microseconds (default is 100).\n\n**-x** _seconds_, **--exposure** _seconds_\n> Sample for the given number of seconds only, then stop.\n\n**-t** _milliseconds_, **--timeout** _milliseconds_\n> Approximate startup wait time; increase on slow machines (default is 100 ms)."],["DESCRIPTION","**austin-tui** is a top-like text-based user interface for **Austin**, the frame stack sampler for CPython. It provides a real-time terminal view of where a Python application spends its time or allocates memory, displaying CPU and memory usage plots alongside per-frame statistics.\n\nAustin works by reading the CPython interpreter's virtual memory to sample frame stacks, requiring zero instrumentation and imposing minimal overhead on the target application. austin-tui wraps this sampling data in a curses-based interface inspired by the Unix **top** command.\n\nThe default view shows the last-seen stack for the current process and thread, refreshing approximately once per second. Pressing **F** switches to full mode, which displays a hierarchical tree of all collected frame statistics. Pressing **G** activates a live flame graph visualization. Thread navigation is done with the left and right arrow keys, with the current PID and TID shown in the header.\n\nAdditional interactive keys include **P** to pause the display while sampling continues in the background, **S** to save collected profiling data to a timestamped file, and **+**/**-** to adjust the minimum percentage threshold for displayed frames.\n\nWhen running in a VS Code terminal, source file paths in the output support Ctrl+Click (Cmd+Click on macOS) navigation. Saved data can be loaded in the Austin VS Code extension for further flame graph analysis."],["CONFIGURATION","austin-tui does not use a configuration file. All options are passed via command-line arguments. The underlying Austin binary must be installed separately and be discoverable on the system PATH.\n\nOn Linux, to avoid running with **sudo**, grant the ptrace capability to the Austin binary:\n\n```sudo setcap cap_sys_ptrace+ep `which austin```"],["CAVEATS","Requires the **Austin 3** binary to be installed and available on the PATH. On macOS, System Integrity Protection prevents profiling the system Python at /usr/bin/python3; use a virtual environment or a Homebrew-installed interpreter instead. On Windows, the standard Python curses module is broken and a replacement wheel must be installed manually."],["HISTORY","**austin-tui** was created by **Gabriele N. Tornetta** (P403n1x87) with the first release candidate (v1.1.0rc1) published in **November 2019**. The flame graph mode was added in **v1.2.0** (April 2021), and support for the MOJO binary format arrived in **v1.4.0** (November 2024). The project is licensed under GPL-3.0."],["SEE ALSO","[top](/man/top)(1), [htop](/man/htop)(1), [py-spy](/man/py-spy)(1), [perf](/man/perf)(1)"]],"e":["top","htop","py-spy","perf"],"x":"austin-tui python3 [path/to/script.py]"},"bats":{"n":"bats","t":"Automated testing framework for bash scripts","s":[["TAGLINE","Automated testing framework for bash scripts"],["TLDR","**Run** test file\n\n```bats [test.bats]```\n\n**Run** all tests in directory\n\n```bats [tests/]```\n\n**Tap output**\n\n```bats --tap [test.bats]```\n\n**Pretty** formatting\n\n```bats --pretty [test.bats]```\n\n**Filter** by test name\n\n```bats --filter [\"pattern\"] [test.bats]```"],["SYNOPSIS","**bats** [_options_] _test-file_..."],["DESCRIPTION","**bats** (Bash Automated Testing System) is a TAP-compliant testing framework for Bash scripts. It provides a simple syntax for writing tests and executing them with readable output.\n\nThe tool enables test-driven development and continuous integration for shell scripts."],["PARAMETERS","**-c**, **--count**\n> Count tests without running\n\n**-f**, **--filter** _pattern_\n> Filter tests by name pattern\n\n**-t**, **--tap**\n> Output in TAP format\n\n**-p**, **--pretty**\n> Pretty print output\n\n**-r**, **--recursive**\n> Run tests recursively\n\n**-j**, **--jobs** _num_\n> Number of parallel jobs (requires GNU parallel)\n\n**-F**, **--formatter** _name_\n> Output formatter (pretty, tap, tap13, junit)\n\n**-T**, **--timing**\n> Add timing information to tests\n\n**-x**, **--trace**\n> Print test commands as they are executed (like set -x)\n\n**--filter-tags** _tags_\n> Only run tests matching all comma-separated tags\n\n**--filter-status** _status_\n> Run tests by status from last run (failed, missed)\n\n**-o**, **--output** _dir_\n> Directory to write report files\n\n**--no-tempdir-cleanup**\n> Preserve test output temporary directory\n\n**--print-output-on-failure**\n> Automatically print $output on failed tests\n\n**-v**, **--version**\n> Display version number"],["TEST SYNTAX","```bash\n#!/usr/bin/env bats\n\n@test \"addition works\" {\n  result=\"$(echo 2+2 | bc)\"\n  [ \"$result\" -eq 4 ]\n}\n\n@test \"files exist\" {\n  [ -f \"/etc/hosts\" ]\n}\n```"],["ASSERTIONS","Uses standard bash test operators:\n- **[ -f file ]** - File exists\n- **[ \"$a\" = \"$b\" ]** - String equality\n- **[ $n -eq 4 ]** - Numeric equality"],["HELPER FUNCTIONS","**run** _command_\n> Run command and capture output/status\n\n**load** _file_\n> Load helper library\n\n**skip** [_reason_]\n> Skip current test"],["CAVEATS","Tests run in subshells (limited side effects). No built-in assertion library (use test-helper). Parallel execution may have ordering issues. Debugging can be tricky."],["HISTORY","**bats** was created by Sam Stephenson in **2011** to provide a simple testing framework for bash scripts, inspired by similar tools in other languages. The project was later forked and maintained as **bats-core** by the community."],["INSTALL","```dnf: sudo dnf install bats```\n\n```pacman: sudo pacman -S bats```\n\n```apk: sudo apk add bats-core```\n\n```zypper: sudo zypper install bats```\n\n```brew: brew install bats-core```\n\n```nix: nix profile install nixpkgs#bats```"],["SEE ALSO","[bash](/man/bash)(1), [shellcheck](/man/shellcheck)(1)"]],"e":["bash","shellcheck"],"x":"bats [test.bats]"},"biff":{"n":"biff","t":"Toggle mail arrival notification at the terminal","s":[["TAGLINE","Toggle mail arrival notification at the terminal"],["TLDR","**Enable mail notification** for the current terminal session\n\n```biff y```\n\n**Disable mail notification** for the current terminal session\n\n```biff n```\n\n**Display the current notification status**\n\n```biff```"],["SYNOPSIS","**biff** [**y** | **n**]"],["DESCRIPTION","**biff** is a mail notification utility that informs you at the command line when new mail arrives during your terminal session. When enabled, it displays the sender, subject, and the first few lines of incoming messages along with a terminal beep.\n\nThe notification works asynchronously through the **comsat**(8) service. If comsat is not enabled on the system, biff will have no effect. For synchronous notification, use the **MAIL** variable in **sh**(1) or the **mail** variable in **csh**(1) instead.\n\nA common practice is to include **biff y** in **~/.login** or **~/.profile** to enable notifications at each login."],["PARAMETERS","**y**\n> Enable mail notification for the current terminal\n\n**n**\n> Disable mail notification for the current terminal\n\nIf no argument is given, biff displays the current notification status."],["CAVEATS","The **biff** command requires the **comsat**(8) daemon to be running on the system. Without it, mail notifications will not work.\n\nUsing **su**(1) can cause permission issues with biff due to terminal ownership remaining with the original user, resulting in \"Permission denied\" errors when changing notification status."],["HISTORY","The command appeared in **4.0BSD** (released 1980), written by **John Foderaro** at the University of California, Berkeley. It is named after **Biff**, a dog belonging to fellow Berkeley student Heidi Stettner. The popular story that Biff barked at the mail carrier is disputed by Stettner herself, but the name stuck as a fitting choice for a mail notification utility.\n\nOn modern systems the notification daemon is part of **GNU inetutils** (as in.comsat), and the biff client typically ships in a dedicated **biff** package on Debian and Ubuntu."],["INSTALL","```apt: sudo apt install biff```"],["SEE ALSO","[mail](/man/mail)(1), [sh](/man/sh)(1), [csh](/man/csh)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/inetutils/)```"]],"e":["mail","sh","csh"],"x":"biff y"},"bmaptool":{"n":"bmaptool","t":"Flash disk images efficiently using block maps","s":[["TAGLINE","Flash disk images efficiently using block maps"],["TLDR","**Copy an image to a device** using bmap\n\n```sudo bmaptool copy [image.img] [/dev/sdX]```\n\n**Copy a compressed image**\n\n```sudo bmaptool copy [image.img.xz] [/dev/sdX]```\n\n**Copy with explicit bmap file**\n\n```sudo bmaptool copy --bmap [image.bmap] [image.img] [/dev/sdX]```\n\n**Copy without bmap** (falls back to dd-like behavior)\n\n```sudo bmaptool copy --nobmap [image.img] [/dev/sdX]```\n\n**Create a bmap file** for an image\n\n```bmaptool create -o [image.bmap] [image.img]```\n\n**Copy from a URL**\n\n```sudo bmaptool copy [https://example.com/image.img.xz] [/dev/sdX]```"],["SYNOPSIS","**bmaptool** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**bmaptool** is a utility for creating block maps (bmap) and copying images to block devices efficiently. It only writes data blocks, skipping empty or zero-filled areas, making it significantly faster than **dd** for sparse images.\n\nThe tool provides integrity verification using SHA256 checksums and includes safety features to prevent accidental writes to mounted devices. It supports compressed images and can decompress on-the-fly during copy operations."],["SUBCOMMANDS","**copy**\n> Copy an image file to a block device using bmap\n\n**create**\n> Generate a bmap file for an image"],["PARAMETERS","**--bmap** _file_\n> Path to the bmap file (auto-detected if not specified)\n\n**--nobmap**\n> Copy without using bmap (entire image is written)\n\n**-o, --output** _file_\n> Output file for create command\n\n**--no-verify**\n> Skip SHA256 checksum verification\n\n**--no-sig-verify**\n> Skip OpenPGP signature verification"],["CAVEATS","Requires root privileges for writing to block devices. The bmap file must match the image file; mismatches cause verification failures. Protection mechanisms prevent writing to mounted devices. Without a bmap file, the tool falls back to copying the entire image like dd."],["HISTORY","bmaptool was originally developed by **Intel** for efficient embedded Linux image deployment, addressing the slow and unsafe nature of using **dd** for flashing large disk images, particularly those with significant empty space. The project moved to the **Yocto Project** in **2024**; the original Intel repository is now archived."],["INSTALL","```nix: nix profile install nixpkgs#bmaptool```"],["SEE ALSO","[dd](/man/dd)(1), [pv](/man/pv)(1), [xzcat](/man/xzcat)(1)"],["RESOURCES","```[Source code](https://github.com/yoctoproject/bmaptool)```"]],"e":["dd","pv","xzcat"],"x":"sudo bmaptool copy [image.img] [/dev/sdX]"},"broot":{"n":"broot","t":"Interactive directory tree navigator","s":[["TAGLINE","Interactive directory tree navigator"],["TLDR","**Install** the br shell function\n\n```broot --install```\n\nStart **interactive** navigation\n\n```broot```\n\nShow file **sizes**\n\n```broot -s```\n\nShow **permissions**\n\n```broot -p```\n\nShow **only directories**\n\n```broot -f```\n\nShow **hidden** files\n\n```broot -h```\n\nStart in **specific directory**\n\n```broot path/to/directory```"],["SYNOPSIS","**broot** [_options_] [_path_]"],["DESCRIPTION","**broot** is an interactive directory tree navigator that provides a better way to navigate directories. It offers fuzzy searching, file previews, and can integrate with the shell via the br function."],["PARAMETERS","**--install**\n> Install or reinstall the br shell function\n\n**-s, --sizes**\n> Display file and directory sizes\n\n**-p, --permissions**\n> Display file permissions\n\n**-f, --only-folders**\n> Show only directories (no files)\n\n**-h, --hidden**\n> Show hidden files and directories\n\n**-d, --dates**\n> Display file modification dates\n\n**-g, --git-status**\n> Show git status of files\n\n**-w, --whale-hierarchical**\n> Show hierarchical directory sizes\n\n**--cmd COMMAND**\n> Execute command on startup\n\n**-c, --conf PATH**\n> Use specified configuration file\n\n**--outcmd PATH**\n> Write command to file for shell integration"],["CONFIGURATION","**~/.config/broot/conf.hjson**\n> Main configuration file in Hjson format. Controls key bindings, verbs, skin, and display settings.\n\n**~/.config/broot/conf.toml**\n> Alternative configuration file in TOML format. Used instead of conf.hjson if present."],["CAVEATS","The br shell function is recommended over direct broot invocation for changing directories. Install it with --install and use br instead of broot for full shell integration."],["HISTORY","**broot** was created as a modern alternative to tree and cd, providing fuzzy search, visual navigation, and efficient handling of large directory trees."],["INSTALL","```apt: sudo apt install broot```\n\n```pacman: sudo pacman -S broot```\n\n```apk: sudo apk add broot```\n\n```zypper: sudo zypper install broot```\n\n```brew: brew install broot```\n\n```nix: nix profile install nixpkgs#broot```"],["SEE ALSO","[tree](/man/tree)(1), [ranger](/man/ranger)(1), [lf](/man/lf)(1), [nnn](/man/nnn)(1)"],["RESOURCES","```[Source code](https://github.com/Canop/broot)```\n\n```[Homepage](https://dystroy.org/broot/)```"]],"e":["tree","ranger","lf","nnn"],"x":"broot --install"},"bun-info":{"n":"bun-info","t":"Display system and project diagnostic information","s":[["TAGLINE","Display system and project diagnostic information"],["TLDR","**Display system and project info**\n\n```bun info```\n\n**Display info** in JSON format\n\n```bun info --json```"],["SYNOPSIS","**bun** **info** [_options_]"],["PARAMETERS","**--json**\n> Output information in JSON format."],["DESCRIPTION","**bun info** displays diagnostic information about your Bun installation, system environment, and current project. This includes Bun version, operating system, CPU architecture, memory, and project configuration.\n\nUseful for troubleshooting issues or providing context when reporting bugs."],["INSTALL","```pacman: sudo pacman -S bun```\n\n```brew: brew install bun```\n\n```nix: nix profile install nixpkgs#bun```"],["SEE ALSO","[bun](/man/bun)(1), [bun-upgrade](/man/bun-upgrade)(1)"],["RESOURCES","```[Source code](https://github.com/oven-sh/bun)```\n\n```[Homepage](https://bun.sh)```\n\n```[Documentation](https://bun.sh/docs)```"]],"e":["bun","bun-upgrade"],"x":"bun info"},"cifs.idmap":{"n":"cifs.idmap","t":"Translate Windows SIDs to Linux UIDs/GIDs for CIFS mounts","s":[["TAGLINE","Translate Windows SIDs to Linux UIDs/GIDs for CIFS mounts"],["TLDR","**Display help information**\n\n```cifs.idmap --help```\n\n**Display version information**\n\n```cifs.idmap --version```"],["SYNOPSIS","**cifs.idmap** [**-h**] [**-t** _timeout_] [**-v**] _key_id_"],["DESCRIPTION","**cifs.idmap** is a userspace helper program for the Linux CIFS client filesystem. It translates Windows Security Identifiers (SIDs) to Linux UIDs/GIDs and vice versa, ensuring correct file ownership and permissions when mounting CIFS/SMB shares.\n\nThis program is not intended to be run directly from the command line. The kernel calls it via **request-key**(8) when a share is mounted with the **cifsacl** mount option.\n\nThe utility relies on a plugin at **/etc/cifs-utils/idmap-plugin** to perform actual ID mapping. Supported backends include sfu, rid, nss, and tdb, often using services like **sssd** or **winbind**."],["PARAMETERS","**-h, --help**\n> Print usage message and exit\n\n**-t, --timeout** _seconds_\n> Set key expiration timer in seconds (default: 600). Use 0 for no expiration\n\n**-v, --version**\n> Print version number and exit\n\n**key_id**\n> The key identifier provided by the kernel upcall"],["CONFIGURATION","**/etc/cifs-utils/idmap-plugin**\n> Symlink or plugin that performs the actual SID-to-UID/GID mapping. Must point to a supported backend (sfu, rid, nss, or tdb).\n\n**/etc/request-key.conf**\n> Kernel keyring configuration that directs upcalls to cifs.idmap."],["CAVEATS","If cifs.idmap or its plugin is unavailable, file objects are assigned the UID/GID of the process that mounted the share. Use the **uid** and **gid** mount options to specify defaults in this case.\n\nA plugin (or symlink) must exist at **/etc/cifs-utils/idmap-plugin** for the utility to function."],["HISTORY","Support for upcalls to cifs.idmap was introduced in **Linux kernel 3.0**. The program was written by Shirish Pargaonkar as part of the **cifs-utils** suite."],["INSTALL","```apt: sudo apt install cifs-utils```\n\n```dnf: sudo dnf install cifs-utils```\n\n```pacman: sudo pacman -S cifs-utils```\n\n```apk: sudo apk add cifs-utils```\n\n```zypper: sudo zypper install cifs-utils```\n\n```nix: nix profile install nixpkgs#cifs-utils```"],["SEE ALSO","[mount.cifs](/man/mount.cifs)(8), [cifscreds](/man/cifscreds)(1)"]],"e":["mount.cifs","cifscreds"],"x":"cifs.idmap --help"},"composer-audit":{"n":"composer-audit","t":"scan PHP dependencies for security vulnerabilities","s":[["TAGLINE","scan PHP dependencies for security vulnerabilities"],["TLDR","**Audit dependencies** for security vulnerabilities\n\n```composer audit```\n\n**Audit in JSON format**\n\n```composer audit --format=json```\n\n**Audit only production dependencies**\n\n```composer audit --no-dev```\n\n**Audit the locked packages and report abandoned ones as failures**\n\n```composer audit --locked --abandoned=fail```"],["SYNOPSIS","**composer** **audit** [_options_]"],["PARAMETERS","**--format** _format_\n> Output format: `table` (default), `plain`, `json`, or `summary`.\n\n**--locked**\n> Audit the exact versions recorded in composer.lock instead of the installed packages.\n\n**--no-dev**\n> Ignore development (require-dev) dependencies.\n\n**--abandoned** _mode_\n> How to treat abandoned packages: `ignore`, `report`, or `fail` (Composer 2.8+).\n\n**--ignore** _package_\n> Ignore the advisories for the given package. Repeatable."],["DESCRIPTION","**composer audit** performs security vulnerability scanning for PHP dependencies by querying the official Packagist security advisories database. Introduced in Composer 2.4, it provides a built-in mechanism to identify packages with known security issues, eliminating the need for third-party security scanning tools.\n\nThe command analyzes both composer.json requirements and the locked versions in composer.lock, reporting any packages that have published Common Vulnerabilities and Exposures (CVE) entries or security advisories. Results include the vulnerability severity, affected versions, and recommended remediation steps.\n\nThis functionality mirrors npm audit for JavaScript and bundle audit for Ruby, providing PHP developers with a standardized way to maintain secure dependency chains. It's particularly valuable in CI/CD pipelines where automated security checks help prevent vulnerable code from reaching production."],["CAVEATS","Requires Composer 2.4 or later. Only detects known, published vulnerabilities."],["INSTALL","```dnf: sudo dnf install composer```\n\n```pacman: sudo pacman -S composer```\n\n```apk: sudo apk add composer```\n\n```brew: brew install composer```"],["SEE ALSO","[composer](/man/composer)(1), [composer-require-checker](/man/composer-require-checker)(1)"]],"e":["composer","composer-require-checker"],"x":"composer audit"},"cwebp":{"n":"cwebp","t":"Google WebP image encoder","s":[["TAGLINE","Google WebP image encoder"],["TLDR","**Convert an image to WebP** format\n\n```cwebp [input.png] -o [output.webp]```\n\n**Convert with specific quality** (0-100)\n\n```cwebp -q [80] [input.jpg] -o [output.webp]```\n\n**Create lossless WebP**\n\n```cwebp -lossless [input.png] -o [output.webp]```\n\n**Resize during conversion**\n\n```cwebp -resize [800] [600] [input.jpg] -o [output.webp]```\n\n**Convert with alpha channel** preservation\n\n```cwebp -alpha_q [100] [input.png] -o [output.webp]```\n\n**Use near-lossless** compression\n\n```cwebp -near_lossless [60] [input.png] -o [output.webp]```\n\n**Maximum compression effort** (slowest, smallest)\n\n```cwebp -m 6 -q [80] [input.jpg] -o [output.webp]```\n\n**Use a preset** tuned for photos\n\n```cwebp -preset photo -q [80] [input.jpg] -o [output.webp]```"],["SYNOPSIS","**cwebp** [_options_] _input_file_ **-o** _output_file.webp_"],["PARAMETERS","**-o** _FILE_\n> Output WebP file path.\n\n**-q** _QUALITY_\n> Compression quality (0-100, default 75). Higher is better quality.\n\n**-lossless**\n> Encode image losslessly.\n\n**-near_lossless** _LEVEL_\n> Near-lossless preprocessing (0-100, default 100 = no preprocessing; 60 is typical). Triggers lossless mode.\n\n**-alpha_q** _QUALITY_\n> Alpha channel quality (0-100, default 100 = lossless alpha).\n\n**-resize** _WIDTH_ _HEIGHT_\n> Resize to the given dimensions. A value of 0 for one dimension preserves the aspect ratio.\n\n**-crop** _X_ _Y_ _W_ _H_\n> Crop the input image to the rectangle at (_X_,_Y_) with width _W_ and height _H_.\n\n**-preset** _NAME_\n> Preset: default, photo, picture, drawing, icon, text. Must appear before other options.\n\n**-m** _METHOD_\n> Compression method (0-6, default 4; higher is slower but produces smaller files).\n\n**-z** _LEVEL_\n> Lossless compression preset (0-9, 0 fastest / 9 slowest; a good default is 6).\n\n**-exact**\n> Preserve RGB values in fully transparent areas (lossless only).\n\n**-mt**\n> Use multi-threading if available.\n\n**-v**\n> Verbose output."],["DESCRIPTION","**cwebp** is Google's encoder for the WebP image format. WebP provides superior compression compared to JPEG and PNG while supporting both lossy and lossless modes, as well as animation and alpha transparency.\n\nThe encoder accepts PNG, JPEG, TIFF, and WebP inputs (animated PNG and WebP are not supported). For lossy compression, the **-q** parameter controls the quality-size tradeoff. For lossless, the **-z** parameter controls compression effort. Using **-o -** sends the encoded output to stdout.\n\nWebP is widely supported in web browsers and offers 25-34% smaller file sizes than JPEG at equivalent quality. The lossless mode typically achieves 26% smaller files than PNG."],["CAVEATS","Very high quality settings may produce files larger than the source. Some older software doesn't support WebP. Lossy WebP introduces artifacts similar to JPEG. Near-lossless mode modifies pixels slightly for better compression."],["HISTORY","WebP was developed by **Google** and released in **2010**, based on the VP8 video codec. The cwebp encoder is part of the libwebp reference implementation. WebP achieved widespread browser support by **2020** and is recommended for web image optimization."],["INSTALL","```apt: sudo apt install webp```\n\n```dnf: sudo dnf install libwebp-tools```\n\n```apk: sudo apk add libwebp-tools```\n\n```zypper: sudo zypper install libwebp-tools```\n\n```brew: brew install webp```"],["SEE ALSO","[dwebp](/man/dwebp)(1), [gif2webp](/man/gif2webp)(1), [cjpeg](/man/cjpeg)(1), [convert](/man/convert)(1)"]],"e":["dwebp","gif2webp","cjpeg","convert"],"x":"cwebp [input.png] -o [output.webp]"},"delpart":{"n":"delpart","t":"ask kernel to forget about a partition","s":[["TAGLINE","ask kernel to forget about a partition"],["TLDR","**Forget partition 1 of /dev/sda**\n\n```sudo delpart [/dev/sda] [1]```\n\n**Forget an NVMe partition**\n\n```sudo delpart [/dev/nvme0n1] [3]```\n\n**Show help**\n\n```delpart --help```"],["SYNOPSIS","**delpart** _device_ _partition_"],["DESCRIPTION","**delpart** asks the Linux kernel to forget about the specified partition (by number) on the given block device. It is a thin wrapper around the `BLKPG_DEL_PARTITION` ioctl and updates only the kernel's in-memory view — the on-disk partition table is not touched.\n\nTypical use is after manually editing a partition table with **fdisk** or **parted** while other partitions on the same disk remain busy: **delpart** removes the kernel node for a single partition so it can be recreated with **addpart**, avoiding a full re-read of the table (as **partprobe** or `blockdev --rereadpt` would attempt)."],["PARAMETERS","_device_\n> The block device containing the partition (e.g., /dev/sda, /dev/nvme0n1).\n\n_partition_\n> The partition number to remove from the kernel's view.\n\n**-h**, **--help**\n> Display help and exit.\n\n**-V**, **--version**\n> Display version information and exit."],["CAVEATS","Requires root privileges. The partition must not be in use — unmount filesystems and stop any swap, LVM, or RAID components on it first, or the ioctl will fail with **EBUSY**. Only the kernel's view is updated; on reboot the kernel re-reads the on-disk table, so the partition will reappear unless the table was also edited. Part of the util-linux package."],["HISTORY","**delpart** ships as part of **util-linux**, which is maintained by Karel Zak and distributed via the Linux Kernel Archive. It complements **addpart** and **resizepart** as minimal ioctl wrappers intended for scripts that modify partitions on live systems."],["INSTALL","```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[addpart](/man/addpart)(8), [partx](/man/partx)(8), [partprobe](/man/partprobe)(8), [fdisk](/man/fdisk)(8), [parted](/man/parted)(8)"]],"e":["addpart","partx","partprobe","fdisk","parted"],"x":"sudo delpart [/dev/sda] [1]"},"dvd-ram-control":{"n":"dvd-ram-control","t":"check or set features of DVD-RAM discs","s":[["TAGLINE","check or set features of DVD-RAM discs"],["TLDR","**Show current settings** of a DVD-RAM disc\n\n```dvd-ram-control [/dev/dvd]```\n\n**Make the disc read-only** (persistent write protection)\n\n```dvd-ram-control -rdonly [/dev/dvd]```\n\n**Re-enable read/write** access\n\n```dvd-ram-control -rdwr [/dev/dvd]```\n\n**Enable automatic write relocation** (AWRE)\n\n```dvd-ram-control -awre [/dev/dvd]```\n\n**Enable automatic read relocation** (ARRE)\n\n```dvd-ram-control -arre [/dev/dvd]```"],["SYNOPSIS","**dvd-ram-control** [**-arre**] [**-awre**] [**-rdonly**|**-rdwr**] _/dev/dvd_"],["PARAMETERS","_/dev/dvd_\n> The DVD-RAM drive device to operate on.\n\n**-rdonly**\n> Turn on persistent write protection (read-only).\n\n**-rdwr**\n> Turn off persistent write protection (read-write).\n\n**-awre**\n> Toggle Automatic Write Relocation Enable.\n\n**-arre**\n> Toggle Automatic Read Relocation Enable."],["DESCRIPTION","**dvd-ram-control** is part of the **dvd+rw-tools** suite and lets you inspect or change low-level features of a DVD-RAM disc. Running it with no option flags prints the current state of the disc's Persistent Write Protection and AWRE/ARRE flags. Adding a flag toggles or sets the corresponding feature in the drive's defect management data.\n\nPersistent Write Protection (**-rdonly** / **-rdwr**) turns the whole disc into a read-only or read-write medium; AWRE and ARRE control automatic sector reallocation on write or read errors."],["CAVEATS","Only works with DVD-RAM media (not DVD±R, DVD±RW, or Blu-ray). Needs access to the raw device (usually as root or as a member of the `cdrom`/`disk` group). Persistent Write Protection can outlive the current session — re-enable read/write explicitly before attempting to burn."],["HISTORY","**dvd-ram-control** ships with **dvd+rw-tools** by **Andy Polyakov**, a suite of utilities for working with DVD±RW, DVD±R, and DVD-RAM media on Unix."],["INSTALL","```apt: sudo apt install dvd+rw-tools```\n\n```dnf: sudo dnf install dvd+rw-tools```\n\n```pacman: sudo pacman -S dvd+rw-tools```\n\n```apk: sudo apk add dvd+rw-tools```\n\n```zypper: sudo zypper install dvd+rw-tools```\n\n```brew: brew install dvd+rw-tools```"],["SEE ALSO","[dvd+rw-mediainfo](/man/dvd+rw-mediainfo)(1), [growisofs](/man/growisofs)(1)"]],"e":["dvd+rw-mediainfo","growisofs"],"x":"dvd-ram-control [/dev/dvd]"},"eww":{"n":"eww","t":"custom desktop widget system","s":[["TAGLINE","custom desktop widget system"],["TLDR","Start the **daemon**\n\n```eww daemon```\n\n**Open** a widget\n\n```eww [-c|--config] [path/to/source_code_directory] open [window_name]```\n\n**Close** a widget\n\n```eww [-c|--config] [path/to/source_code_directory] close [window_name]```\n\n**Reload** configuration\n\n```eww reload```\n\n**Kill** the daemon\n\n```eww kill```\n\n**Get** the value of a variable\n\n```eww get [variable_name]```\n\n**Update** a variable\n\n```eww update [variable_name]=\"[value]\"```\n\nPrint and **watch logs**\n\n```eww logs```"],["SYNOPSIS","**eww** [_options_] _command_ [_arguments_]"],["PARAMETERS","**daemon**\n> Start the daemon process.\n\n**open** _window_\n> Open a widget window.\n\n**open-many** _windows..._\n> Open multiple widget windows at once.\n\n**close** _window_\n> Close a widget window.\n\n**close-all**\n> Close all open widget windows.\n\n**reload**\n> Reload configuration files.\n\n**kill**\n> Stop the daemon.\n\n**logs**\n> Display and watch logs.\n\n**get** _variable_\n> Print the current value of a variable.\n\n**update** _var=value_\n> Update a variable value.\n\n**state**\n> Display the current state of all variables.\n\n**inspect**\n> Open the GTK debugger.\n\n**shell-completions** _shell_\n> Generate shell completions (bash, fish, zsh).\n\n**-c**, **--config** _path_\n> Specify config directory.\n\n**--no-daemonize**\n> Don't fork the daemon process to the background."],["DESCRIPTION","**eww** (ElKowar's Wacky Widgets) creates custom desktop widgets for any window manager. It uses a configuration language to define widgets with dynamic content and styling.\n\nPopular for creating status bars, sidebars, and other desktop elements."],["CONFIGURATION","**~/.config/eww/eww.yuck**\n> Widget definitions and structure written in the eww configuration language.\n\n**~/.config/eww/eww.scss**\n> Widget styling using CSS/SCSS syntax."],["CAVEATS","Requires configuration files in eww format. Daemon must be running for widgets to display. Works with X11 and Wayland."],["INSTALL","```zypper: sudo zypper install eww```\n\n```nix: nix profile install nixpkgs#eww```"],["SEE ALSO","[polybar](/man/polybar)(1), [conky](/man/conky)(1), [waybar](/man/waybar)(1)"]],"e":["polybar","conky","waybar"],"x":"eww daemon"},"frugon":{"n":"frugon","t":"Free, local, open-source LLM cost analyzer","s":[["TAGLINE","Free, local, open-source LLM cost analyzer"],["TLDR","**Analyze** LLM usage logs and see potential savings by routing to cheaper models\n\n```frugon analyze ./logs.jsonl```\n\n**Capture** LLM calls locally via a proxy shim to generate logs\n\n```frugon capture --out ./logs.jsonl```\n\n**Run** a demo analysis on bundled sample data\n\n```frugon analyze --demo```\n\n**List** available models for routing candidates\n\n```frugon models```\n\n**Generate** a shareable HTML or Markdown report of the analysis\n\n```frugon analyze ./logs.jsonl --report savings.html```\n\n**Install** via pipx for permanent use\n\n```pipx install frugon```"],["SYNOPSIS","**frugon** <command> [options]"],["DESCRIPTION","Frugon is a local-first tool that analyzes your real LLM API call logs (in OpenAI-compatible JSONL format) to show exactly where your spend is going and how much you could save by routing easier calls to cheaper models while keeping hard calls on capable ones.\n\nIt runs entirely on your machine: no logs or keys are sent anywhere. It uses tokenizers and current pricing data locally. Optional `--measure` mode samples your traffic against candidate models using your own provider keys (directly, never via Frugon) to give quality estimates.\n\nFrugon can act as a transparent local proxy (`frugon capture`) that logs calls while forwarding them unchanged to the real provider."],["COMMANDS","**analyze** [file]\n> Analyze a JSONL log file (or `--demo`) and recommend routing.\n\n**capture**\n> Run a local proxy that logs requests/responses to a JSONL file.\n\n**models** [filter]\n> List models available for `--candidates` with pricing/quality tiers.\n\n**update**\n> Refresh local pricing and quality data.\n\n**pricing**, **quality**\n> Inspect local pricing and quality datasets."],["PARAMETERS","**analyze**\n> `--candidates` comma list of models to consider for routing\n> `--measure` sample real traffic for quality comparison (requires `pip install 'frugon[measure]'` + provider keys)\n> `--judge` score candidate quality when used with `--measure`\n> `--report` path to write .html or .md report\n> `--demo` use bundled sample instead of a file\n> `--verbose` extra accounting detail\n\n**capture**\n> `--out` output JSONL file (default capture.jsonl)\n> `--port` listen port (default 8787)\n> `--upstream` override the forwarding target\n> `--proxy` opt in to ambient HTTP(S)_PROXY for upstream calls (ignored by default so keys stay direct)\n> `--verbose` log one line per captured call"],["CAVEATS","- Logs must be in the specific JSONL shape (model + request/response + optional usage).\n- `--measure` makes real API calls to your providers and incurs their cost.\n- Quality tiers are estimates; always verify with `--measure --judge` on your data before switching production traffic.\n- Routing recommendations are offline math; they do not automatically change your application."],["SEE ALSO","[pipx](/man/pipx)(1), [uvx](/man/uvx)(1)"],["RESOURCES","```[Source code](https://github.com/Rodiun/frugon)```\n\n```[Homepage](https://frugon.rodiun.io)```\n\n```[Documentation](https://github.com/Rodiun/frugon#readme)```"]],"e":["pipx","uvx"],"x":"frugon analyze ./logs.jsonl"},"git-ignore":{"n":"git-ignore","t":"Manage gitignore entries","s":[["TAGLINE","Manage gitignore entries"],["TLDR","**Add to gitignore**\n\n```git ignore [pattern]```\n\n**Add multiple patterns**\n\n```git ignore \"*.log\" \"*.tmp\"```\n\n**Show current ignores**\n\n```git ignore```\n\n**Add to global gitignore**\n\n```git ignore --global [pattern]```\n\n**Add to private exclude file**\n\n```git ignore --private [pattern]```"],["SYNOPSIS","**git ignore** [_options_] [_patterns_]"],["PARAMETERS","_PATTERNS_\n> Patterns to add to .gitignore.\n\n**-l**, **--local**\n> Add to local .gitignore (default).\n\n**-g**, **--global**\n> Add to global gitignore.\n\n**-p**, **--private**\n> Add to private exclude file (.git/info/exclude).\n\n**--help**\n> Display help information."],["DESCRIPTION","**git ignore** manages .gitignore entries, adding patterns to the repository's .gitignore or the global gitignore file. Without arguments, it displays the current .gitignore contents.\n\nPart of the git-extras suite, the command handles proper formatting and avoids duplicates. It creates .gitignore if the file does not exist."],["CONFIGURATION","**.gitignore**\n> Repository-level ignore patterns managed by this command.\n\n**.git/info/exclude**\n> Private exclude patterns for the repository when using --private.\n\n**~/.gitignore_global**\n> Global ignore patterns applied to all repositories when using --global."],["CAVEATS","Part of git-extras package. Creates .gitignore if missing. Already tracked files need manual untracking."],["HISTORY","git ignore is part of **git-extras**, providing a command-line interface for the common task of editing gitignore files."],["INSTALL","```brew: brew install git-ignore```\n\n```nix: nix profile install nixpkgs#git-ignore```"],["SEE ALSO","[git-check-ignore](/man/git-check-ignore)(1), [git-extras](/man/git-extras)(1), [gibo](/man/gibo)(1)"]],"e":["git-check-ignore","git-extras","gibo"],"x":"git ignore [pattern]"},"git-init":{"n":"git-init","t":"Create empty repository","s":[["TAGLINE","Create empty repository"],["TLDR","**Initialize repository**\n\n```git init```\n\n**Initialize in directory**\n\n```git init [path]```\n\n**Initialize bare repository**\n\n```git init --bare```\n\n**Initialize with specific branch**\n\n```git init -b [main]```\n\n**Initialize with template**\n\n```git init --template=[template_dir]```"],["SYNOPSIS","**git** **init** [_options_] [_directory_]"],["PARAMETERS","**--bare**\n> Create bare repository.\n\n**-b**, **--initial-branch** _name_\n> Initial branch name.\n\n**--template** _dir_\n> Template directory.\n\n**--shared** _permissions_\n> Set repository permissions.\n\n**--separate-git-dir** _dir_\n> Store the `.git` directory at _dir_ instead of inside the working tree.\n\n**--object-format** _format_\n> Hash algorithm for objects: sha1 (default) or sha256.\n\n**-q**, **--quiet**\n> Quiet output."],["DESCRIPTION","**git init** creates an empty Git repository or reinitializes an existing one. It creates the `.git` directory structure with subdirectories for objects, refs/heads, refs/tags, and template files, establishing the foundation for version control.\n\nRunning git init in an existing repository is safe and won't overwrite existing history. It can be used to pick up newly added templates or move the repository to another place if `--separate-git-dir` is given. The command can create either a working repository (with a working directory) or a bare repository (no working directory, typically used as a central server).\n\nThe `--bare` option creates a repository optimized for sharing without a working directory. The `--initial-branch` option sets the name of the first branch, useful for starting with \"main\" instead of \"master\". Template directories allow customization of the initial repository structure, including hooks and configuration."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-clone](/man/git-clone)(1), [git-config](/man/git-config)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-init)```"]],"e":["git-clone","git-config"],"x":"git init"},"git-sh-setup":{"n":"git-sh-setup","t":"Common shell library for Git scripts","s":[["TAGLINE","Common shell library for Git scripts"],["TLDR","**Source for git scripts**\n\n```. \"$(git --exec-path)/git-sh-setup\"```"],["SYNOPSIS","**git-sh-setup**"],["DESCRIPTION","**git-sh-setup** is an internal shell library that provides common functions for Git scripts. It includes utilities for repository validation, directory navigation, color output, and error handling.\n\nKey functions include `require_work_tree`, `cd_to_toplevel`, `die`, and `git_pager`. Scripts source this library to get consistent behavior across Git shell commands."],["CAVEATS","Internal library, not a command. Must be sourced, not executed. Used by git shell scripts."],["HISTORY","git-sh-setup is part of **Git's** internal infrastructure, providing shared functionality for shell-based git commands and hooks."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-sh-i18n](/man/git-sh-i18n)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-sh-setup)```"]],"e":["git-sh-i18n"],"x":". \"$(git --exec-path)/git-sh-setup\""},"git-stage":{"n":"git-stage","t":"Alias for git add to stage changes","s":[["TAGLINE","Alias for git add to stage changes"],["TLDR","**Stage files**\n\n```git stage [file]```\n\n**Stage all changes**\n\n```git stage -A```\n\n**Interactive staging**\n\n```git stage -p```"],["SYNOPSIS","**git** **stage** [_options_] [_files_...]"],["DESCRIPTION","**git stage** is a built-in alias for `git add`. It stages changes for the next commit. The term \"stage\" more clearly describes the action of adding changes to the index than \"add\" does."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-add](/man/git-add)(1), [git-commit](/man/git-commit)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-add)```"]],"e":["git-add","git-commit"],"x":"git stage [file]"},"groff_font":{"n":"groff_font","t":"Groff font file format description","s":[["TAGLINE","Groff font file format description"],["TLDR","**List available fonts**\n\n```groff -Tps -Z -f[fontname] < /dev/null```\n\n**View font directory**\n\n```ls /usr/share/groff/current/font/```\n\n**Check font files**\n\n```cat /usr/share/groff/current/font/devps/DESC```"],["DESCRIPTION","**groff_font** describes the format of font files used by GNU troff (groff). Font files define character metrics, kerning pairs, and other typographic information needed for text layout.\n\nFont files are stored in device-specific subdirectories under /usr/share/groff/. Each output device (ps, pdf, html) has its own font definitions with the DESC file specifying device parameters."],["FONT FILE FORMAT","```\nname fontname\nspacewidth n\nslant n\nligatures ...\ncharset\nchar width type code\n...\n```"],["FONT DIRECTORIES","```\n/usr/share/groff/current/font/devps/   PostScript fonts\n/usr/share/groff/current/font/devpdf/  PDF fonts\n/usr/share/groff/current/font/devutf8/ UTF-8 terminal\n/usr/share/groff/current/font/devhtml/ HTML output\n```"],["STANDARD FONTS","```\nR   - Roman (Times)\nI   - Italic\nB   - Bold\nBI  - Bold Italic\nCR  - Courier Roman\nH   - Helvetica\n```"],["CAVEATS","Font availability depends on device. Custom fonts need proper installation. PostScript fonts require AFM files. Modern use may prefer pdfgroff."],["SEE ALSO","[groff](/man/groff)(1), [troff](/man/troff)(1), [afmtodit](/man/afmtodit)(1)"]],"e":["groff","troff","afmtodit"],"x":"groff -Tps -Z -f[fontname] < /dev/null"},"idevicesyslog":{"n":"idevicesyslog","t":"streams the system log from iOS devices","s":[["TAGLINE","streams the system log from iOS devices"],["TLDR","**Stream system log** from the attached device\n\n```idevicesyslog```\n\n**Include only** messages from specific processes (pipe-separated)\n\n```idevicesyslog -p [Safari|WebKit]```\n\n**Target a specific device** by UDID\n\n```idevicesyslog -u [udid]```\n\n**Print only** lines containing a pattern\n\n```idevicesyslog -m \"[error]\"```\n\n**Exclude processes** (pipe-separated)\n\n```idevicesyslog -e \"[SpringBoard|backboardd]\"```\n\n**Stream over network** instead of USB\n\n```idevicesyslog -n```\n\n**Save output** to a file\n\n```idevicesyslog -o [device.log]```"],["SYNOPSIS","**idevicesyslog** [_options_]\n\n**idevicesyslog** **pidlist**\n\n**idevicesyslog** **archive** _PATH_ [_options_]"],["PARAMETERS","**-u**, **--udid** _UDID_\n> Target a specific device by UDID.\n\n**-n**, **--network**\n> Connect to a network-attached device instead of USB.\n\n**-p**, **--process** _PROCESS_\n> Include messages from the given process names (**|** separated).\n\n**-e**, **--exclude** _PROCESS_\n> Exclude messages from the given process names (**|** separated).\n\n**-m**, **--match** _STRING_\n> Print only messages containing _STRING_.\n\n**-M**, **--unmatch** _STRING_\n> Print only messages **not** containing _STRING_.\n\n**-t**, **--trigger** _STRING_\n> Start logging when a message containing _STRING_ is seen.\n\n**-T**, **--untrigger** _STRING_\n> Stop logging when a message containing _STRING_ is seen.\n\n**-k**, **--kernel**, **-K**, **--no-kernel**\n> Include or exclude kernel messages.\n\n**-o**, **--output** _FILE_\n> Write the log stream to _FILE_ in addition to the terminal.\n\n**-x**, **--exit**\n> Exit when the device disconnects.\n\n**--no-colors**, **--colors**\n> Disable or force ANSI color output.\n\n**-q**, **--quiet**\n> Suppress banner / status output.\n\n**-d**, **--debug**\n> Enable debug output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**idevicesyslog** streams the system log from iOS devices. It provides real-time access to device logs for debugging.\n\nThe tool shows messages from all processes or filtered subsets. It's essential for iOS app development and debugging."],["CAVEATS","Part of libimobiledevice. Continuous stream. May need developer mode on iOS 16+."],["HISTORY","idevicesyslog is part of **libimobiledevice** for iOS log streaming."],["INSTALL","```apt: sudo apt install libimobiledevice-utils```\n\n```dnf: sudo dnf install libimobiledevice-utils```\n\n```apk: sudo apk add libimobiledevice-progs```"],["SEE ALSO","[idevice_id](/man/idevice_id)(1), [ideviceinfo](/man/ideviceinfo)(1), [idevicepair](/man/idevicepair)(1), [adb](/man/adb)(1)"]],"e":["idevice_id","ideviceinfo","idevicepair","adb"],"x":"idevicesyslog"},"impacket-getst":{"n":"impacket-getst","t":"Request a Kerberos Service Ticket and save as ccache","s":[["TAGLINE","Request a Kerberos Service Ticket and save as ccache"],["TLDR","**Request a service ticket** for a specific SPN\n\n```impacket-getst -spn '[cifs/target.domain.com]' '[domain]/[user]:[password]'```\n\n**Request a service ticket** using an NTLM hash\n\n```impacket-getst -spn '[cifs/target.domain.com]' -hashes ':[nthash]' '[domain]/[user]'```\n\n**Impersonate a user** via S4U2Self and S4U2Proxy (delegation attack)\n\n```impacket-getst -spn '[cifs/target.domain.com]' -impersonate '[administrator]' '[domain]/[user]:[password]'```\n\n**Request a service ticket** using Kerberos authentication from ccache\n\n```impacket-getst -spn '[cifs/target.domain.com]' -k -no-pass '[domain]/[user]'```\n\n**Request a service ticket** using an AES key\n\n```impacket-getst -spn '[cifs/target.domain.com]' -aesKey '[aes256key]' '[domain]/[user]'```\n\n**Request a User-to-User ticket**\n\n```impacket-getst -u2u -spn '[cifs/target.domain.com]' '[domain]/[user]:[password]'```"],["SYNOPSIS","**impacket-getst** [_-spn SPN_] [_-impersonate USER_] [_-altservice SPN_] [_-additional-ticket FILE_] [_-hashes LMHASH:NTHASH_] [_-aesKey KEY_] [_-k_] [_-no-pass_] [_-dc-ip IP_] _identity_"],["PARAMETERS","**-spn** _SPN_\n> Service Principal Name (service/server) of the target service.\n\n**-altservice** _SPN_\n> Set a new sname/SPN in the resulting ticket.\n\n**-impersonate** _USER_\n> Target username to impersonate via S4U2Self. Requires delegation privileges.\n\n**-additional-ticket** _FILE_\n> Include a forwardable service ticket in an S4U2Proxy request for RBCD + KCD.\n\n**-dmsa**\n> Use Delegated Managed Service Accounts.\n\n**-u2u**\n> Request a User-to-User ticket.\n\n**-self**\n> Only perform S4U2Self, skip S4U2Proxy.\n\n**-force-forwardable**\n> Force the S4U2Self service ticket to be forwardable.\n\n**-renew**\n> Renew the TGT used for authentication.\n\n**-ts**\n> Add timestamp to every logging output.\n\n**-debug**\n> Turn DEBUG output on.\n\n**-hashes** _LMHASH:NTHASH_\n> Use NTLM hashes for authentication instead of password.\n\n**-no-pass**\n> Do not prompt for password (useful with -k).\n\n**-k**\n> Use Kerberos authentication from ccache file (KRB5CCNAME).\n\n**-aesKey** _KEY_\n> AES key for Kerberos authentication (128 or 256 bit).\n\n**-dc-ip** _IP_\n> IP address of the domain controller."],["DESCRIPTION","**impacket-getst** requests a Kerberos Service Ticket (ST) from an Active Directory domain controller and saves it as a ccache file. It supports standard ticket requests, S4U2Self/S4U2Proxy delegation attacks for user impersonation, and User-to-User authentication.\n\nThis tool is commonly used in penetration testing for Kerberos delegation abuse (constrained delegation, resource-based constrained delegation) where an attacker with delegation privileges can impersonate any user to a target service. The resulting ccache file can be used with other tools via the **KRB5CCNAME** environment variable."],["CAVEATS","Requires a valid TGT or credentials. Impersonation via S4U2Self/S4U2Proxy requires the identity account to have delegation privileges configured in Active Directory. Time synchronization between client and KDC must be within 5 minutes. The **-force-forwardable** flag exploits CVE-2020-17049 (Bronze Bit) and may not work on patched domain controllers."],["HISTORY","Part of the **Impacket** library by **SecureAuth** (now **Fortra**). The S4U extensions (S4U2Self and S4U2Proxy) were originally designed by Microsoft for constrained delegation scenarios. Security researchers discovered that these mechanisms could be abused for privilege escalation, making getST a key tool in Kerberos-based attack chains."],["INSTALL","```pacman: sudo pacman -S impacket```"],["SEE ALSO","[impacket-gettgt](/man/impacket-gettgt)(1), [impacket-getuserspns](/man/impacket-getuserspns)(1), [impacket-secretsdump](/man/impacket-secretsdump)(1)"]],"e":["impacket-gettgt","impacket-getuserspns","impacket-secretsdump"],"x":"impacket-getst -spn '[cifs/target.domain.com]' '[domain]/[user]:[password]'"},"jwt-ui":{"n":"jwt-ui","t":"Terminal UI for decoding and encoding JSON Web Tokens","s":[["TAGLINE","Terminal UI for decoding and encoding JSON Web Tokens"],["TLDR","**Decode a JWT interactively**\n\n```jwt-ui [token]```\n\n**Decode from stdin**\n\n```echo \"[token]\" | jwt-ui```\n\n**Decode with a secret**\n\n```jwt-ui -S [secret] [token]```\n\n**Decode with a secret from file**\n\n```jwt-ui -S @[path/to/key.pem] [token]```"],["SYNOPSIS","**jwt-ui** [**-S** _secret_] [_token_]"],["DESCRIPTION","**jwt-ui** is a terminal UI for decoding and encoding JSON Web Tokens, inspired by **jwt.io** and **jwt-cli**. It provides an interactive interface showing the header, payload, and signature verification status of a JWT.\n\nSupported algorithms include **HMAC** (HS256/384/512), **RSA** (RS256/384/512, PS256/384/512), **ECDSA** (ES256/384), and **EdDSA**. Secrets can be provided as plain text, file paths (prefixed with **@**), or base64-encoded strings (prefixed with **b64:**)."],["CAVEATS","Only supports standard JWT formats. Encrypted JWTs (JWE) are not supported. Secret handling in command-line arguments may expose secrets in shell history."],["HISTORY","**jwt-ui** was created by the **jwt-rs** organization and is written in **Rust**. It was designed to bring the convenience of web-based JWT tools like jwt.io to the terminal."],["INSTALL","```pacman: sudo pacman -S jwt-ui```\n\n```brew: brew install jwt-ui```"],["SEE ALSO","[openssl](/man/openssl)(1)"]],"e":["openssl"],"x":"jwt-ui [token]"},"kdig":{"n":"kdig","t":"advanced DNS lookup utility from the Knot DNS project","s":[["TAGLINE","advanced DNS lookup utility from the Knot DNS project"],["TLDR","**Query A record**\n\n```kdig [example.com] A```\n\n**Query specific server**\n\n```kdig [example.com] @[8.8.8.8]```\n\n**Query using TCP**\n\n```kdig +tcp [example.com]```\n\n**Query using TLS (DoT)**\n\n```kdig +tls [example.com] @[1.1.1.1]```\n\n**Perform zone transfer**\n\n```kdig [example.com] AXFR @[ns1.example.com]```\n\n**Query with DNSSEC validation**\n\n```kdig +dnssec [example.com]```\n\n**Reverse DNS lookup**\n\n```kdig -x [192.0.2.1]```"],["SYNOPSIS","**kdig** [_common-settings_] [_query_...]"],["PARAMETERS","**@**_server_\n> Nameserver to query.\n\n**-t** _type_\n> Query type (A, AAAA, MX, NS, etc.).\n\n**-x** _address_\n> Reverse lookup for IP address.\n\n**+tcp**\n> Use TCP instead of UDP.\n\n**+tls**\n> Use TLS (DNS over TLS).\n\n**+https**\n> Use HTTPS (DNS over HTTPS).\n\n**+dnssec**\n> Request DNSSEC records.\n\n**+short**\n> Display short output."],["DESCRIPTION","**kdig** is an advanced DNS lookup utility from the Knot DNS project. It supports modern DNS protocols including DNS over TLS (DoT) and DNS over HTTPS (DoH). Provides more features than traditional dig for DNS troubleshooting and testing."],["INSTALL","```apt: sudo apt install knot-dnsutils```\n\n```dnf: sudo dnf install knot-utils```\n\n```apk: sudo apk add knot-utils```\n\n```zypper: sudo zypper install knot-utils```"],["SEE ALSO","[dig](/man/dig)(1), [host](/man/host)(1), [nslookup](/man/nslookup)(1)"]],"e":["dig","host","nslookup"],"x":"kdig [example.com] A"},"keybench":{"n":"keybench","t":"Scriptable benchmark for sorted key-value stores","s":[["TAGLINE","Scriptable benchmark for sorted key-value stores"],["TLDR","**Run a workload** against the default in-memory engine\n\n```keybench [path/to/workload.lua]```\n\n**Benchmark several engines** in one run (persistent engines need a data directory)\n\n```keybench --backend [skiplist,rocksdb,tidesdb] --data-dir [path/to/data] [path/to/workload.lua]```\n\n**Sweep a range of thread counts** to test scaling\n\n```keybench --threads [1,2,4,8] [path/to/workload.lua]```\n\n**Run for a fixed duration** instead of a fixed number of operations\n\n```keybench --secs [30] [path/to/workload.lua]```\n\n**Repeat each measurement** and report the median\n\n```keybench --repeat [5] [path/to/workload.lua]```\n\n**Write results** to the console and a TSV file\n\n```keybench --report [console,tsv:results.tsv] [path/to/workload.lua]```"],["SYNOPSIS","**keybench** [_options_] _workload_file_ [_workload_file ..._]"],["DESCRIPTION","**keybench** is a benchmarking harness that measures throughput and latency across multiple **sorted key-value storage engines**. Workloads are written once in **Lua** and run unchanged against every backend, so engines can be compared fairly under identical access patterns.\n\nEach workload is a Lua table with a required **run** function (the measured unit of work) and an optional **load** function that seeds the store before timing begins. A workload may also declare a **sweep** to repeat the run across a list of parameter values. The harness reports two throughput figures, workload units per second (**wu/s**) and primitive operations per second (**ops/s**), along with p50, p99, p99.9 and max latency per operation type.\n\nkeybench is compiled with **make**; the resulting binary embeds an in-memory skiplist engine by default. Persistent engines are enabled at build time, for example **make ROCKSDB=1 TIDESDB=1**."],["OPTIONS","**--ops** _N_\n> Total workload units to run across all threads (default 200000).\n\n**--secs** _S_\n> Run for _S_ seconds instead of a fixed operation count.\n\n**--users** _N_\n> User population size passed to the workload context.\n\n**--items** _N_\n> Catalog or keyspace size passed to the workload context.\n\n**--seed** _N_\n> Base RNG seed; thread _t_ uses seed + _t_.\n\n**--backend** _LIST_\n> Comma-separated list of engines to test (for example skiplist,rocksdb,tidesdb).\n\n**--threads** _LIST_\n> Comma-separated thread counts to sweep for scaling tests.\n\n**--seed-once**\n> Seed the dataset once per engine and reuse it across the thread sweep.\n\n**--repeat** _N_\n> Run each grid point _N_ times and report the median.\n\n**--data-dir** _DIR_\n> Directory for persistent engines such as rocksdb and tidesdb (required for them).\n\n**--report** _LIST_\n> Output sinks: console, tsv:file, or timeline:file.\n\n**--report-dir** _DIR_\n> Create a timestamped directory and bundle all run artifacts in it.\n\n**--auto-plot**\n> Generate plots after the run completes.\n\n**--config** _FILE_\n> Load run parameters from an INI file.\n\n**--save-config** _FILE_\n> Write the effective run parameters out as a replayable config."],["STORAGE ENGINES","**skiplist**\n> In-memory probabilistic skiplist with a reader-writer lock. The reference engine; no persistence.\n\n**rocksdb**\n> Persistent LSM-tree store, configured via a [rocksdb] section in the INI config.\n\n**tidesdb**\n> Persistent, transactional LSM-tree store, configured via a [tidesdb] section."],["CAVEAT","Persistent engines (rocksdb, tidesdb) are only available when compiled in (**make ROCKSDB=1 TIDESDB=1**) and require **--data-dir**. Results depend heavily on hardware, allocator and build flags, so compare runs only on the same machine and build."],["HISTORY","**keybench** is an open-source benchmarking tool written mainly in **C** (with Lua workloads and Python helpers) by developer **guycipher**. It is distributed under the **GPL-2.0** license."],["SEE ALSO","[redis-benchmark](/man/redis-benchmark)(1), [memtier_benchmark](/man/memtier_benchmark)(1), [sysbench](/man/sysbench)(1), [fio](/man/fio)(1), [hyperfine](/man/hyperfine)(1)"]],"e":["redis-benchmark","memtier_benchmark","sysbench","fio","hyperfine"],"x":"keybench [path/to/workload.lua]"},"kjv":{"n":"kjv","t":"command-line Bible reader providing access to the King James Version text","s":[["TAGLINE","command-line Bible reader providing access to the King James Version text"],["TLDR","**List** all books\n\n```kjv -l```\n\nOpen a specific **book**\n\n```kjv [Genesis]```\n\nOpen a specific **chapter**\n\n```kjv [Genesis] [2]```\n\nOpen a specific **verse**\n\n```kjv [John] [3]:[16]```\n\nOpen a **range of verses**\n\n```kjv [Proverbs] [3]:[1-6]```\n\nDisplay verses across **multiple chapters**\n\n```kjv [Matthew] [1]:[7]-[2]:[6]```\n\n**Search** for a pattern\n\n```kjv /[Plagues]```\n\nSearch within a **specific book**\n\n```kjv [1Jn]/[antichrist]```"],["SYNOPSIS","**kjv** [_options_] [_reference_]"],["PARAMETERS","**-l**\n> List all books\n\n_BOOK_\n> Display entire book\n\n_BOOK CHAPTER_\n> Display specific chapter\n\n_BOOK CHAPTER:VERSE_\n> Display specific verse\n\n_BOOK CHAPTER:START-END_\n> Display verse range\n\n**/**_PATTERN_\n> Search all text for pattern\n\n_BOOK_**/**_PATTERN_\n> Search within specific book"],["DESCRIPTION","**kjv** is a command-line Bible reader providing access to the King James Version text. It supports flexible reference formats for looking up books, chapters, verses, and ranges.\n\nThe tool also includes search functionality to find verses containing specific words or patterns across the entire text or within specific books."],["CAVEATS","Only includes King James Version text. Book names can use various abbreviations. Search is case-insensitive by default."],["HISTORY","kjv was created as a simple, terminal-based Bible reader following Unix philosophy. It provides quick access to Biblical text without requiring a graphical interface or web browser."],["INSTALL","```nix: nix profile install nixpkgs#kjv```"],["SEE ALSO","[less](/man/less)(1), [grep](/man/grep)(1)"]],"e":["less","grep"],"x":"kjv -l"},"kubectl":{"n":"kubectl","t":"kubernetes command-line tool","s":[["TAGLINE","kubernetes command-line tool"],["TLDR","**Get resources**\n\n```kubectl get [pods|deployments|services|nodes]```\n\n**Describe resource**\n\n```kubectl describe [pod] [name]```\n\n**Apply manifest**\n\n```kubectl apply -f [manifest.yaml]```\n\n**Delete resource**\n\n```kubectl delete [pod] [name]```\n\n**View logs**\n\n```kubectl logs [pod-name]```\n\n**Execute in pod**\n\n```kubectl exec -it [pod-name] -- [/bin/bash]```\n\n**Port forward**\n\n```kubectl port-forward [pod-name] [8080:80]```"],["SYNOPSIS","**kubectl** [_options_] _command_ [_type_] [_name_] [_flags_]"],["PARAMETERS","**get** _TYPE_ [_NAME_]\n> List resources of specified type.\n\n**describe** _TYPE_ _NAME_\n> Show detailed information about a resource.\n\n**apply** **-f** _FILE_\n> Apply configuration from file or directory.\n\n**delete** _TYPE_ _NAME_\n> Delete a resource.\n\n**logs** _POD_ [**-c** _container_]\n> Show pod logs, optionally for a specific container.\n\n**exec** [**-it**] _POD_ **--** _COMMAND_\n> Execute command in a container.\n\n**port-forward** _POD_ _LOCAL:REMOTE_\n> Forward local port to pod port.\n\n**scale** _TYPE/NAME_ **--replicas**=_N_\n> Scale a resource to N replicas.\n\n**create** _TYPE_ _NAME_\n> Create a resource imperatively.\n\n**edit** _TYPE_ _NAME_\n> Edit a resource in your default editor.\n\n**-n**, **--namespace** _NAMESPACE_\n> Target namespace.\n\n**-o**, **--output** _FORMAT_\n> Output format: json, yaml, wide, name.\n\n**-l**, **--selector** _LABEL_\n> Filter by label selector.\n\n**--context** _CONTEXT_\n> Kubeconfig context to use.\n\n**-A**, **--all-namespaces**\n> List resources across all namespaces."],["DESCRIPTION","**kubectl** is the official command-line interface for Kubernetes, used to communicate with the cluster's API server to manage and inspect resources. It supports the full lifecycle of Kubernetes objects, including creating, reading, updating, and deleting resources such as pods, deployments, services, configmaps, and more through declarative manifests or imperative commands.\n\nThe tool reads connection details from a kubeconfig file, which defines clusters, users, and contexts that determine which cluster and namespace kubectl targets. Beyond basic resource management, kubectl provides capabilities for debugging with log retrieval and exec sessions, scaling workloads, rolling out updates, port-forwarding to pods, and applying configuration changes from YAML or JSON manifests. Role-based access control (RBAC) on the cluster side governs what operations each authenticated user may perform."],["CAVEATS","Requires kubeconfig. Context determines cluster. RBAC affects access."],["HISTORY","kubectl is the official CLI for **Kubernetes**, developed as the primary cluster management interface."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubeadm](/man/kubeadm)(1), [k9s](/man/k9s)(1), [helm](/man/helm)(1), [kubectx](/man/kubectx)(1)"]],"e":["kubeadm","k9s","helm","kubectx"],"x":"kubectl get [pods|deployments|services|nodes]"},"lando":{"n":"lando","t":"provides Docker-based development environments","s":[["TAGLINE","provides Docker-based development environments"],["TLDR","**Initialize project**\n\n```lando init```\n\n**Start environment**\n\n```lando start```\n\n**Stop environment**\n\n```lando stop```\n\n**Rebuild environment**\n\n```lando rebuild```\n\n**Run command in container**\n\n```lando [npm|composer|php] [command]```\n\n**SSH into container**\n\n```lando ssh```\n\n**View logs**\n\n```lando logs```"],["SYNOPSIS","**lando** _command_ [_options_]"],["PARAMETERS","**init**\n> Initialize Lando project.\n\n**start**\n> Start containers.\n\n**stop**\n> Stop containers.\n\n**rebuild**\n> Rebuild containers.\n\n**ssh** [_SERVICE_]\n> SSH into service.\n\n**logs** [_SERVICE_]\n> View logs.\n\n**destroy**\n> Remove containers.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Lando** provides Docker-based development environments. It creates consistent local development setups.\n\nThe tool supports many frameworks with recipes. It handles networking, services, and tooling automatically."],["CAVEATS","Requires Docker. Memory intensive. Recipe-based configuration."],["HISTORY","Lando was created by **Tandem** to simplify Docker-based development environments for various tech stacks."],["INSTALL","```aur: yay -S lando```"],["SEE ALSO","[docker-compose](/man/docker-compose)(1), [ddev](/man/ddev)(1), [docker](/man/docker)(1)"]],"e":["docker-compose","ddev","docker"],"x":"lando init"},"light":{"n":"light","t":"controls the backlight brightness of laptop screens and other devices","s":[["TAGLINE","controls the backlight brightness of laptop screens and other devices"],["TLDR","Get current **backlight** value\n\n```light```\n\n**Set** backlight to 50%\n\n```light -S 50```\n\n**Increase** backlight by 20%\n\n```light -A 20```\n\n**Decrease** backlight by 20%\n\n```light -U 20```"],["SYNOPSIS","**light** [_options_]"],["DESCRIPTION","**light** controls the backlight brightness of laptop screens and other controllable backlights. It works without X or Wayland and can save and restore brightness levels."],["PARAMETERS","**-S PERCENT**\n> Set brightness to specified percent\n\n**-A PERCENT**\n> Add percent to current brightness\n\n**-U PERCENT**\n> Subtract percent from current brightness\n\n**-G**\n> Get current brightness (default)\n\n**-O**\n> Save current brightness\n\n**-I**\n> Restore previously saved brightness\n\n**-N PERCENT**\n> Set minimum brightness cap\n\n**-P**\n> Get minimum brightness cap\n\n**-L**\n> List available devices\n\n**-s DEVICE**\n> Select specific device\n\n**-r**\n> Interpret values in raw mode (device-specific values instead of percent)"],["CAVEATS","May require udev rules for non-root access. Works with devices exposed through /sys/class/backlight. The minimum brightness cap (-N) prevents the display from going completely dark on controllers that turn off at 0."],["INSTALL","```apt: sudo apt install light```\n\n```dnf: sudo dnf install light```"],["SEE ALSO","[xbacklight](/man/xbacklight)(1), [brightnessctl](/man/brightnessctl)(1)"]],"e":["xbacklight","brightnessctl"],"x":"light"},"lpoptions":{"n":"lpoptions","t":"display or set printer options and defaults","s":[["TAGLINE","display or set printer options and defaults"],["TLDR","**Show default options**\n\n```lpoptions```\n\n**Show printer options**\n\n```lpoptions -p [printer] -l```\n\n**Set default printer**\n\n```lpoptions -d [printer]```\n\n**Set printer option**\n\n```lpoptions -p [printer] -o [option=value]```\n\n**Remove option**\n\n```lpoptions -p [printer] -r [option]```\n\n**List options for the default printer**\n\n```lpoptions -l```\n\n**Remove all options for a printer instance**\n\n```lpoptions -x [printer/instance]```"],["SYNOPSIS","**lpoptions** [_options_]"],["PARAMETERS","**-d** _destination[/instance]_\n> Set the user default printer to the named destination.\n\n**-p** _destination[/instance]_\n> Set the destination and instance for any options that follow.\n\n**-l**\n> List printer-specific options and their current settings.\n\n**-o** _option[=value]_\n> Set a new option for the named destination.\n\n**-r** _option_\n> Remove the specified option from the named destination.\n\n**-x** _destination[/instance]_\n> Remove all options for the named destination and instance.\n\n**-E**\n> Enable encryption when communicating with the CUPS server.\n\n**-h** _server[:port]_\n> Connect to the specified server."],["DESCRIPTION","**lpoptions** displays or sets printer options and defaults. When run with no arguments, it shows the default printer options.\n\nUser-specific settings are stored in **~/.cups/lpoptions**. When run by root, system-wide defaults in **/etc/cups/lpoptions** are used instead. Options like paper size, media type, and print quality can be configured per printer or per instance."],["CAVEATS","Options are printer-specific and may vary between devices. System-wide options in /etc/cups/lpoptions require root access. Instances allow defining named sets of options for a single printer."],["HISTORY","lpoptions is part of **CUPS** for configuring default printer options and destinations."],["INSTALL","```apt: sudo apt install cups-client```\n\n```dnf: sudo dnf install cups-client```\n\n```apk: sudo apk add cups-client```\n\n```zypper: sudo zypper install cups-client```"],["SEE ALSO","[lp](/man/lp)(1), [lpadmin](/man/lpadmin)(8), [lpstat](/man/lpstat)(1), [lpr](/man/lpr)(1), [lpq](/man/lpq)(1), [cups](/man/cups)(1)"]],"e":["lp","lpadmin","lpstat","lpr","lpq","cups"],"x":"lpoptions"},"mc":{"n":"mc","t":"Midnight Commander, a visual file manager","s":[["TAGLINE","Midnight Commander, a visual file manager"],["TLDR","**Start Midnight Commander**\n\n```mc```\n\n**Start with two directories in panels**\n\n```mc [dir1] [dir2]```\n\n**Black and white mode**\n\n```mc -b```\n\n**View a file using the internal viewer**\n\n```mc -v [file]```\n\n**Edit a file using the internal editor**\n\n```mc -e [file]```\n\n**Disable mouse support**\n\n```mc -d```"],["SYNOPSIS","**mc** [_options_] [_path1_] [_path2_]"],["PARAMETERS","_PATH1_ _PATH2_\n> Directories for left and right panels.\n\n**-a**, **--stickchars**\n> Disable graphic characters for line drawing.\n\n**-b**, **--nocolor**\n> Force black and white display.\n\n**-c**, **--color**\n> Force color mode.\n\n**-C** _arg_, **--colors=**_arg_\n> Specify a different color set.\n\n**-d**, **--nomouse**\n> Disable mouse support.\n\n**-e** _FILE_\n> Start the internal editor on the specified file.\n\n**-v** _FILE_\n> Start the internal viewer on the specified file.\n\n**-s**, **--slow**\n> Slow terminal mode.\n\n**-u**, **--nosubshell**\n> Disable concurrent subshell.\n\n**-S** _arg_, **--skin=**_arg_\n> Specify skin name.\n\n**-P** _FILE_, **--printwd=**_FILE_\n> Write the last working directory of the panel to _FILE_ on exit (used by the **mc** wrapper script so that quitting with **F10** cd's the outer shell).\n\n**-F**, **--datadir-info**\n> Print the compiled-in data-directory paths and exit.\n\n**-g**, **--oldmouse**\n> Use \"normal tracking\" mouse mode (for terminals that do not support the modern protocol).\n\n**-X**, **--no-x11**\n> Do not use X11 to query modifier-key state.\n\n**--help**\n> Display help information."],["KEYBOARD COMMANDS","**F1**\n> Help.\n\n**F3**\n> View file.\n\n**F4**\n> Edit file.\n\n**F5**\n> Copy file or directory.\n\n**F6**\n> Move/rename file or directory.\n\n**F7**\n> Create directory.\n\n**F8**\n> Delete file or directory.\n\n**F9**\n> Access top menu bar.\n\n**F10**\n> Quit.\n\n**Tab**\n> Switch between panels.\n\n**Ctrl+O**\n> Toggle between panels and shell.\n\n**Alt+Enter**\n> Copy selected filename to command line."],["DESCRIPTION","**mc** (Midnight Commander) is a visual file manager with a dual-pane interface. It provides file operations like copying, moving, renaming, and deleting through function keys and menus.\n\nSupports browsing archives (tar, zip, rpm, etc.) as virtual filesystems, remote connections via FTP, SFTP, and SMB, and a built-in text editor and file viewer.\n\nThe subshell integration (Ctrl+O) allows switching between the file manager and a full command line without leaving mc."],["CAVEATS","Function keys are used extensively and may conflict with terminal emulator shortcuts. Some terminals may need key remapping. The subshell requires a compatible shell (bash, zsh, or fish)."],["HISTORY","Midnight Commander was created in **1994** by **Miguel de Icaza** as a Unix clone of Norton Commander."],["INSTALL","```apt: sudo apt install mc```\n\n```dnf: sudo dnf install mc```\n\n```pacman: sudo pacman -S mc```\n\n```apk: sudo apk add mc```\n\n```zypper: sudo zypper install mc```\n\n```brew: brew install mc```\n\n```nix: nix profile install nixpkgs#mc```"],["SEE ALSO","[ranger](/man/ranger)(1), [nnn](/man/nnn)(1), [lf](/man/lf)(1)"]],"e":["ranger","nnn","lf"],"x":"mc"},"mkfile":{"n":"mkfile","t":"creates files of specified size","s":[["TAGLINE","creates files of specified size"],["TLDR","**Create file of size**\n\n```mkfile [100m] [filename]```\n\n**Create sparse file**\n\n```mkfile -n [1g] [filename]```\n\n**Create file in bytes**\n\n```mkfile [1048576] [filename]```\n\n**Create multiple files**\n\n```mkfile [10m] [file1] [file2]```\n\n**Verbose output**\n\n```mkfile -v [100m] [filename]```"],["SYNOPSIS","**mkfile** [_options_] _size_ _file_"],["PARAMETERS","_SIZE_\n> File size (b, k, m, g suffixes).\n\n_FILE_\n> Output filename.\n\n**-n**\n> Create sparse file.\n\n**-v**\n> Verbose output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mkfile** creates files of specified size. It can create empty files or sparse files.\n\nThe tool is useful for testing and creating disk images. Sparse files don't allocate full space."],["CAVEATS","macOS/Solaris utility. Linux uses truncate/fallocate. Sparse file support varies."],["HISTORY","mkfile originates from **Solaris** and is also available on macOS for creating files of arbitrary size."],["SEE ALSO","[truncate](/man/truncate)(1), [fallocate](/man/fallocate)(1), [dd](/man/dd)(1)"]],"e":["truncate","fallocate","dd"],"x":"mkfile [100m] [filename]"},"mufetch":{"n":"mufetch","t":"Neofetch-style music info display for the terminal","s":[["TAGLINE","Neofetch-style music info display for the terminal"],["TLDR","**Show current track info**\n\n```mufetch```\n\n**Search for a track**\n\n```mufetch search -t \"[track name]\"```\n\n**Search for an artist**\n\n```mufetch search -a \"[artist name]\"```"],["SYNOPSIS","**mufetch** [_command_] [_options_]"],["DESCRIPTION","**mufetch** is a CLI tool that displays music metadata in your terminal, inspired by neofetch's design aesthetic. It shows track details, album information, and artist metadata with album artwork and artist photos rendered directly in the terminal.\n\nFeatures include duration, popularity, genres, and follower count metrics, clickable Spotify links, search commands with type filtering (track, album, artist), and adaptive image resizing for terminal display."],["CAVEATS","Requires Spotify API credentials for music data. Image display depends on terminal capabilities."],["HISTORY","**mufetch** was created by **Ashish Kumar** (ashish0kumar) and is written in **Go**."],["INSTALL","```brew: brew install mufetch```\n\n```nix: nix profile install nixpkgs#mufetch```"],["SEE ALSO","[neofetch](/man/neofetch)(1)"]],"e":["neofetch"],"x":"mufetch"},"mult":{"n":"mult","t":"Run a command multiple times and compare outputs","s":[["TAGLINE","Run a command multiple times and compare outputs"],["TLDR","**Run a command 5 times**\n\n```mult -n 5 \"[command]\"```\n\n**Run with delay between executions**\n\n```mult -n 5 -d 1s \"[command]\"```\n\n**Stop on first failure**\n\n```mult -n 10 --fail-fast \"[command]\"```"],["SYNOPSIS","**mult** [_options_] _command_"],["PARAMETERS","**-n** _COUNT_\n> Number of times to run the command.\n\n**-d**, **--delay** _DURATION_\n> Delay between executions.\n\n**--fail-fast**\n> Stop execution on the first failure."],["DESCRIPTION","**mult** is a TUI tool that executes a shell command multiple times and displays each run's output for comparison. It is useful for identifying inconsistent server behavior, examining unreliable test outcomes, and conducting quick performance assessments.\n\nThe TUI provides side-by-side output comparison of each run."],["CAVEATS","Commands with side effects will execute those effects on every run. Use with caution for destructive commands."],["HISTORY","**mult** was created by **dhth** and is written in **Go**."],["INSTALL","```aur: yay -S mult```"],["SEE ALSO","[watch](/man/watch)(1)"]],"e":["watch"],"x":"mult -n 5 \"[command]\""},"nest":{"n":"nest","t":"CLI for NestJS, a progressive Node","s":[["TAGLINE","CLI for NestJS, a progressive Node"],["TLDR","**Create new project**\n\n```nest new [project-name]```\n\n**Generate controller**\n\n```nest generate controller [name]```\n\n**Generate service**\n\n```nest generate service [name]```\n\n**Generate module**\n\n```nest generate module [name]```\n\n**Generate resource** (full CRUD)\n\n```nest generate resource [name]```\n\n**Start development server**\n\n```nest start --watch```\n\n**Build for production**\n\n```nest build```\n\n**Show help**\n\n```nest --help```"],["SYNOPSIS","**nest** [_new_] [_generate_] [_start_] [_build_] [_options_] [_args_]"],["PARAMETERS","**new** _NAME_\n> Create new project.\n\n**generate**, **g** _TYPE_ _NAME_\n> Generate component.\n\n**start**\n> Start application.\n\n**build**\n> Build application.\n\n**info**\n> Display project info.\n\n**add** _LIBRARY_\n> Add NestJS library.\n\n**--watch**, **-w**\n> Watch mode for development.\n\n**--prod**\n> Build for production.\n\n**--flat**\n> Don't create directory for generated file.\n\n**--spec**\n> Generate spec file (default true).\n\n**--no-spec**\n> Skip spec file generation.\n\n**--dry-run**, **-d**\n> Report actions without writing.\n\n**--skip-git**\n> Skip git initialization."],["DESCRIPTION","**nest** is the CLI for NestJS, a progressive Node.js framework for building server-side applications. It generates projects and components following NestJS conventions.\n\nNew projects include TypeScript configuration, testing setup, and the NestJS module structure. The generator creates controllers, services, modules, and other components.\n\nThe resource generator creates complete CRUD functionality including controller, service, DTOs, and entities. It can generate REST, GraphQL, or WebSocket resources.\n\nDevelopment server includes hot reload. Code changes trigger automatic restart. Production builds optimize for deployment.\n\nThe modular architecture separates concerns into modules, each containing related controllers, services, and providers. The CLI maintains this structure.\n\nNestJS supports Express or Fastify as underlying HTTP servers, with decorators providing routing and dependency injection."],["CAVEATS","Requires Node.js 16+. Generated code uses TypeScript. Some features need additional packages. Learning curve for decorators and dependency injection."],["HISTORY","**NestJS** was created by **Kamil Myśliwiec** in **2017**, inspired by Angular's architecture. It brought Angular-style dependency injection and decorators to Node.js backend development. The CLI provides Rails-like code generation."],["INSTALL","```dnf: sudo dnf install nest```\n\n```brew: brew install nest```\n\n```nix: nix profile install nixpkgs#nest```"],["SEE ALSO","[node](/man/node)(1), [npm](/man/npm)(1), [express](/man/express)(1)"]],"e":["node","npm","express"],"x":"nest new [project-name]"},"noglob":{"n":"noglob","t":"Zsh precommand modifier to disable filename globbing","s":[["TAGLINE","Zsh precommand modifier to disable filename globbing"],["TLDR","**Install a package without shell expansion** of wildcards\n\n```noglob apt install linux-*```\n\n**Pass a URL with special characters** without quoting\n\n```noglob curl https://example.com/api?key=value&page=2```\n\n**Run find without expanding glob patterns** in the shell\n\n```noglob find . -name *.txt```\n\n**Use with package managers** that have glob-like syntax in names\n\n```noglob pip install package[extra]```"],["SYNOPSIS","**noglob** _simple command_"],["DESCRIPTION","**noglob** is a zsh precommand modifier that disables filename generation (globbing) for all words in the command that follows. When used, pattern matching characters like **\\***, **?**, and **[...]** are treated literally rather than being expanded to match filenames.\n\nThis is useful when passing arguments that contain glob characters to commands without needing to quote each argument individually. Common use cases include URLs with query strings, package names with brackets, and commands where wildcard characters should be passed through verbatim.\n\n**noglob** must appear before any other precommand modifier, as it is interpreted immediately before any parsing is done."],["CAVEATS","Only available in **zsh**; not a POSIX shell feature. Has no effect in non-interactive shells. In bash, the equivalent is **set -o noglob** (or **set -f**) which applies globally rather than per-command. Cannot be combined after other precommand modifiers — it must come first."],["HISTORY","**noglob** has been part of **zsh** since its early versions, originating from the csh family of shells. The C shell (**csh**) introduced the concept of selectively disabling globbing, and zsh adopted and refined it as a precommand modifier alongside **nocorrect**, **builtin**, **command**, and **exec**."],["SEE ALSO","[zsh](/man/zsh)(1), [bash](/man/bash)(1)"]],"e":["zsh","bash"],"x":"noglob apt install linux-*"},"npm-whoami":{"n":"npm-whoami","t":"displays the logged-in username","s":[["TAGLINE","displays the logged-in username"],["TLDR","**Show current user**\n\n```npm whoami```\n\n**Check specific registry**\n\n```npm whoami --registry=[https://registry.example.com]```"],["SYNOPSIS","**npm** **whoami** [_options_]"],["PARAMETERS","**--registry** _URL_\n> Registry to check.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npm whoami** displays the logged-in username. Verifies authentication.\n\nThe command shows current user. Useful for debugging auth issues."],["CAVEATS","Requires authentication. Per-registry check. Empty if not logged in."],["HISTORY","npm whoami provides **authentication verification** for npm registry access."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-login](/man/npm-login)(1), [npm-logout](/man/npm-logout)(1)"]],"e":["npm","npm-login","npm-logout"],"x":"npm whoami"},"nroff":{"n":"nroff","t":"formats text for terminal display","s":[["TAGLINE","formats text for terminal display"],["TLDR","**Format document**\n\n```nroff [file.roff]```\n\n**Format for terminal**\n\n```nroff -Tascii [file]```\n\n**Format man page**\n\n```nroff -man [page.1]```\n\n**Format with macros**\n\n```nroff -ms [file.ms]```\n\n**Preview output**\n\n```nroff [file] | less```"],["SYNOPSIS","**nroff** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Input files.\n\n**-T** _DEV_\n> Output device.\n\n**-man**\n> Use man macros.\n\n**-ms**\n> Use ms macros.\n\n**-me**\n> Use me macros.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nroff** formats text for terminal display. Part of troff/groff system.\n\nThe tool processes roff documents. Outputs plain text formatting."],["CAVEATS","Part of groff. Terminal output. Use groff for typeset output."],["HISTORY","nroff originated in **Unix V7** for formatting text on line printers and terminals."],["INSTALL","```apt: sudo apt install groff-base```\n\n```dnf: sudo dnf install groff-base```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[groff](/man/groff)(1), [troff](/man/troff)(1), [man](/man/man)(1)"]],"e":["groff","troff","man"],"x":"nroff [file.roff]"},"oh-my-posh":{"n":"oh-my-posh","t":"prompt theme engine","s":[["TAGLINE","prompt theme engine"],["TLDR","**Initialize for shell**\n\n```eval \"$(oh-my-posh init [bash|zsh|fish])\"```\n\n**Use specific theme**\n\n```eval \"$(oh-my-posh init bash --config [theme.json])\"```\n\n**List available themes**\n\n```oh-my-posh get themes```\n\n**Print current config**\n\n```oh-my-posh print primary```\n\n**Debug prompt**\n\n```oh-my-posh debug```"],["SYNOPSIS","**oh-my-posh** [_command_] [_options_]"],["PARAMETERS","**init** _SHELL_\n> Generate shell initialization script (bash, zsh, fish, powershell, cmd, nu).\n\n**get themes**\n> List available themes.\n\n**get shell**\n> Display current shell information.\n\n**config export**\n> Export current theme configuration.\n\n**config migrate**\n> Migrate configuration to current version format.\n\n**font install**\n> Install a Nerd Font.\n\n**cache clear**\n> Clear the prompt cache.\n\n**upgrade**\n> Update oh-my-posh to the latest version.\n\n**toggle** _segment_\n> Enable or disable a specific prompt segment.\n\n**debug**\n> Show prompt rendering diagnostics.\n\n**--config** _FILE_\n> Path to configuration file (JSON, YAML, or TOML)."],["DESCRIPTION","**oh-my-posh** is a cross-platform prompt theme engine that renders customizable shell prompts using configurable segments for git status, language versions, execution time, and more. It works with bash, zsh, fish, PowerShell, cmd, and nu."],["CAVEATS","Requires Nerd Font for icons. Cross-platform. Config in JSON/YAML/TOML."],["HISTORY","oh-my-posh was created for **customizable shell prompts** across different shells."],["INSTALL","```dnf: sudo dnf install oh-my-posh```\n\n```brew: brew install oh-my-posh```\n\n```nix: nix profile install nixpkgs#oh-my-posh```"],["SEE ALSO","[starship](/man/starship)(1), [powerline](/man/powerline)(1), [p10k](/man/p10k)(1)"]],"e":["starship","powerline","p10k"],"x":"eval \"$(oh-my-posh init [bash|zsh|fish])\""},"pactree":{"n":"pactree","t":"visualizes package dependencies in a tree format","s":[["TAGLINE","visualizes package dependencies in a tree format"],["TLDR","Print **dependency tree** of a package\n\n```pactree [package]```\n\nPrint **reverse** dependencies (what depends on package)\n\n```pactree -r [package]```\n\nList dependencies **one per line**, skip duplicates\n\n```pactree -u [package]```\n\nInclude **optional** dependencies with color\n\n```pactree -co [package]```\n\nLimit tree **depth**\n\n```pactree -d [depth] [package]```\n\nDisplay **help**\n\n```pactree```"],["SYNOPSIS","**pactree** [**-c**|**--color**] [**-d** _depth_] [**-o**|**--optional**] [**-r**|**--reverse**] [**-u**|**--unique**] _package_"],["PARAMETERS","**-c, --color**\n> Colorize output for better readability\n\n**-d, --depth _num_**\n> Limit recursion depth\n\n**-o, --optional**\n> Include optional dependencies in tree\n\n**-r, --reverse**\n> Show reverse dependencies (packages depending on target)\n\n**-s, --sync**\n> Query sync databases instead of local\n\n**-u, --unique**\n> List each dependency only once, one per line\n\n**--config _file_**\n> Use alternate pacman configuration"],["DESCRIPTION","**pactree** visualizes package dependencies in a tree format. It queries pacman's database to show what packages a given package depends on, or with the reverse option, what packages depend on it.\n\nThe tool is useful for understanding package relationships, identifying orphaned packages, and planning package removal to avoid breaking dependencies."],["CAVEATS","Only shows dependencies for installed packages by default. Use -s flag for sync database queries. Large dependency trees can produce extensive output. Optional dependencies are hidden unless -o is specified."],["HISTORY","**pactree** was originally a standalone script that became part of the official **pacman-contrib** package. It provides visual dependency information that pacman's standard query options don't offer in an easy-to-read format."],["SEE ALSO","[pacman](/man/pacman)(8), [pactrans](/man/pactrans)(1), [pkgfile](/man/pkgfile)(1)"]],"e":["pacman","pactrans","pkgfile"],"x":"pactree [package]"},"pdf2json":{"n":"pdf2json","t":"extracts PDF content to JSON format","s":[["TAGLINE","extracts PDF content to JSON format"],["TLDR","**Convert PDF to JSON**\n\n```pdf2json [input.pdf] [output.json]```\n\n**Convert first page only**\n\n```pdf2json -f [1] -l [1] [input.pdf] [output.json]```\n\n**Include form fields**\n\n```pdf2json -form [input.pdf] [output.json]```\n\n**Split pages to separate files**\n\n```pdf2json -split [input.pdf] [output_prefix]```"],["SYNOPSIS","**pdf2json** [_options_] _input.pdf_ [_output.json_]"],["PARAMETERS","**-f** _num_\n> First page.\n\n**-l** _num_\n> Last page.\n\n**-form**\n> Include form data.\n\n**-split**\n> One file per page.\n\n**-enc** _encoding_\n> Text encoding."],["DESCRIPTION","**pdf2json** extracts PDF content to JSON format. It captures text, positions, fonts, and form fields, enabling programmatic access to PDF data."],["OUTPUT STRUCTURE","```json\n{\n  \"pages\": [\n    {\n      \"width\": 612,\n      \"height\": 792,\n      \"texts\": [\n        {\"x\": 72, \"y\": 720, \"text\": \"Hello\"}\n      ]\n    }\n  ]\n}\n```"],["CAVEATS","Text extraction quality varies. Complex layouts may not preserve structure. Images not extracted."],["HISTORY","pdf2json is based on PDF.js or similar libraries, providing JSON export for PDF processing pipelines."],["INSTALL","```brew: brew install pdf2json```"],["SEE ALSO","[pdftotext](/man/pdftotext)(1), [pdftohtml](/man/pdftohtml)(1), [poppler-utils](/man/poppler-utils)(1)"]],"e":["pdftotext","pdftohtml","poppler-utils"],"x":"pdf2json [input.pdf] [output.json]"},"pee":{"n":"pee","t":"tees stdin to multiple commands","s":[["TAGLINE","tees stdin to multiple commands"],["TLDR","**Pipe to multiple commands**\n\n```echo \"data\" | pee [cmd1] [cmd2]```\n\n**Tee to processes**\n\n```cat [file] | pee \"wc -l\" \"wc -w\"```\n\n**Process and save**\n\n```cat [log] | pee \"grep error\" \"cat > copy.log\"```"],["SYNOPSIS","**pee** [_command_...]"],["PARAMETERS","_COMMAND_\n> Commands to receive input.\n\nEach command receives a copy of stdin."],["DESCRIPTION","**pee** tees stdin to multiple commands. Like tee but to processes.\n\nThe tool duplicates input to commands. Part of moreutils."],["CAVEATS","Part of moreutils. Each command gets full input. Parallel execution."],["HISTORY","pee is part of **moreutils** for piping to multiple processes."],["INSTALL","```apt: sudo apt install moreutils```\n\n```dnf: sudo dnf install moreutils```\n\n```pacman: sudo pacman -S moreutils```\n\n```apk: sudo apk add moreutils```\n\n```zypper: sudo zypper install moreutils```\n\n```brew: brew install moreutils```\n\n```nix: nix profile install nixpkgs#moreutils```"],["SEE ALSO","[tee](/man/tee)(1), [moreutils](/man/moreutils)(7)"]],"e":["tee","moreutils"],"x":"echo \"data\" | pee [cmd1] [cmd2]"},"pg_basebackup":{"n":"pg_basebackup","t":"creates a base backup of a PostgreSQL cluster","s":[["TAGLINE","creates a base backup of a PostgreSQL cluster"],["TLDR","**Create backup**\n\n```pg_basebackup -D [backup_dir] -h [host] -U [user]```\n\n**Backup with progress**\n\n```pg_basebackup -D [backup_dir] -P```\n\n**Backup as tar**\n\n```pg_basebackup -D [backup_dir] -Ft```\n\n**Backup with WAL streaming**\n\n```pg_basebackup -D [backup_dir] -X stream```"],["SYNOPSIS","**pg_basebackup** [_options_]"],["PARAMETERS","**-D**, **--pgdata** _dir_\n> Backup destination.\n\n**-h**, **--host** _host_\n> Database server host.\n\n**-U**, **--username** _user_\n> Database user.\n\n**-P**, **--progress**\n> Show progress.\n\n**-F**, **--format** _fmt_\n> Output format (p=plain, t=tar).\n\n**-X**, **--wal-method** _method_\n> WAL inclusion (fetch, stream, none).\n\n**-z**\n> Compress tar output.\n\n**-Z** _level_\n> Compression level."],["DESCRIPTION","**pg_basebackup** creates a base backup of a PostgreSQL cluster. Foundation for point-in-time recovery and streaming replication setup. Includes all database files."],["INSTALL","```apk: sudo apk add postgresql17-client```"],["SEE ALSO","[pg_dump](/man/pg_dump)(1), [pg_restore](/man/pg_restore)(1)"]],"e":["pg_dump","pg_restore"],"x":"pg_basebackup -D [backup_dir] -h [host] -U [user]"},"pgmnorm":{"n":"pgmnorm","t":"Normalize the contrast in a Netpbm image","s":[["TAGLINE","Normalize the contrast in a Netpbm image"],["TLDR","**Normalize image contrast**\n\n```pgmnorm [input.pgm] > [output.pgm]```\n\n**Normalize with custom percentile cutoffs**\n\n```pgmnorm -bpercent [2] -wpercent [1] [input.pgm] > [output.pgm]```\n\n**Specify exact pixel values for black and white mapping**\n\n```pgmnorm -bvalue [30] -wvalue [220] [input.pgm] > [output.pgm]```\n\n**Normalize while preserving hues** in color images\n\n```pgmnorm -keephues [input.ppm] > [output.ppm]```"],["SYNOPSIS","**pgmnorm** [_options_] [_file_]"],["PARAMETERS","**-bpercent** _n_\n> Percentage of darkest pixels to map to black. Default: 2.\n\n**-wpercent** _n_\n> Percentage of lightest pixels to map to white. Default: 1.\n\n**-bvalue** _n_\n> Exact pixel value to map to black (overrides -bpercent if it produces less change).\n\n**-wvalue** _n_\n> Exact pixel value to map to white (overrides -wpercent if it produces less change).\n\n**-keephues**\n> Keep each pixel the same hue; only adjust brightness. Without this, components are normalized independently, which may shift hues.\n\n**-brightmax**\n> Use the most intense RGB component as the pixel's brightness instead of luminosity."],["DESCRIPTION","**pgmnorm** reads a PNM image (PBM, PGM, or PPM), normalizes the contrast by forcing the darkest pixels to black and the lightest pixels to white, and linearly rescales the values in between. It outputs the same kind of image. This is an older name for **pnmnorm**.\n\nBy default, the darkest 2 percent of pixels are mapped to black and the lightest 1 percent to white. These thresholds can be changed with **-bpercent** and **-wpercent**, or exact cutoff values can be specified with **-bvalue** and **-wvalue**. If both percentage and value options are given for the same end, the one producing the least change is used.\n\nPart of the **Netpbm** toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmnorm](/man/pnmnorm)(1), [pgmhist](/man/pgmhist)(1), [pnmhisteq](/man/pnmhisteq)(1), [pgmenhance](/man/pgmenhance)(1)"]],"e":["pnmnorm","pgmhist","pnmhisteq","pgmenhance"],"x":"pgmnorm [input.pgm] > [output.pgm]"},"picoclaw":{"n":"picoclaw","t":"Ultra-lightweight AI assistant for resource-constrained devices","s":[["TAGLINE","Ultra-lightweight AI assistant for resource-constrained devices"],["TLDR","**Initialize** configuration and set up credentials\n\n```picoclaw onboard```\n\n**Start an interactive** CLI chat session with the AI agent\n\n```picoclaw agent```\n\n**Send a one-shot message** without entering interactive mode\n\n```picoclaw agent -m \"[question or task]\"```\n\n**Start the multi-channel gateway** for chat platform integrations\n\n```picoclaw gateway```"],["SYNOPSIS","**picoclaw** [_command_] [_options_]"],["PARAMETERS","**onboard**\n> Initialize configuration and set up the environment for first-time use. Creates config at **~/.picoclaw/config.json**.\n\n**agent**\n> Start an interactive CLI chat session with the AI agent.\n\n**agent -m** _message_\n> Send a single message to the agent and receive a response without entering interactive mode.\n\n**gateway**\n> Start the multi-channel gateway for chat platform integrations including Telegram, Discord, WhatsApp, QQ, Slack, Matrix, IRC, DingTalk, LINE, and WeCom."],["DESCRIPTION","**picoclaw** is an ultra-lightweight personal AI assistant written in Go, designed to run on extremely resource-constrained hardware. It uses less than 10MB of RAM and ships as a single self-contained binary with sub-second boot times.\n\nPicoClaw supports RISC-V, ARM64, ARM32, MIPS, LoongArch, and x86 architectures natively, making it deployable on devices ranging from $10 RISC-V boards to powerful servers. Despite the minimal footprint, it provides full agent capabilities including planning, web search integration, command execution, and automation workflows.\n\nThe tool supports 30+ LLM providers including OpenAI, Anthropic (Claude), Google Gemini, DeepSeek, Mistral, Groq, Qwen, Ollama, and Zhipu. It can integrate with chat platforms via the gateway command for bot deployments on Telegram, Discord, WhatsApp, Slack, Matrix, IRC, and other messaging services."],["CONFIGURATION","Configuration is stored in **~/.picoclaw/config.json**. Run **picoclaw onboard** for interactive setup.\n\nKey configuration fields:\n\n```\n{\n  \"agents\": {\n    \"defaults\": {\n      \"workspace\": \"/path/to/workspace\",\n      \"model_name\": \"claude-opus-4-5\",\n      \"max_tokens\": 8192,\n      \"temperature\": 0.7,\n      \"max_tool_iterations\": 10\n    }\n  },\n  \"model_list\": [\n    {\n      \"model_name\": \"claude\",\n      \"model\": \"claude-opus-4-5\",\n      \"api_key\": \"sk-ant-xxx\"\n    }\n  ]\n}\n```\n\nWeb search can be enabled via **tools** configuration with support for Brave, Tavily, and DuckDuckGo search providers."],["CAVEATS","PicoClaw executes commands on your machine with your user permissions. In newer versions, sensitive data such as API keys can be stored separately in **.security.yml**. The project was largely AI-bootstrapped with approximately 95% of the core system generated by an AI agent, which may affect code quality and maintainability. Being Go-based, it requires downloading precompiled binaries or building from source rather than installing via a package manager."],["HISTORY","PicoClaw was developed by **Sipeed**, a company known for RISC-V hardware, as an AI assistant optimized for their low-cost development boards. First released in **2025**, it was written in Go through a self-bootstrapping process where the AI agent itself drove the architectural design and code optimization. The project gained attention for running full agent capabilities on hardware costing as little as $10."],["INSTALL","```brew: brew install picoclaw```\n\n```nix: nix profile install nixpkgs#picoclaw```"],["SEE ALSO","[nanobot](/man/nanobot)(1), [claude](/man/claude)(1)"]],"e":["nanobot","claude"],"x":"picoclaw onboard"},"pidstat":{"n":"pidstat","t":"Monitor individual tasks managed by the Linux kernel","s":[["TAGLINE","Monitor individual tasks managed by the Linux kernel"],["TLDR","Show **CPU statistics** at 2 second interval, 10 reports\n\n```pidstat 2 10```\n\nShow **page faults** and memory utilization\n\n```pidstat -r```\n\nShow **I/O** usage per process\n\n```pidstat -d```\n\nShow information for a **specific PID**\n\n```pidstat -p 1234```\n\nShow **memory** statistics for processes matching pattern\n\n```pidstat -C \"pattern\" -r -p ALL```\n\nShow **context switches**\n\n```pidstat -w```\n\n**Execute and monitor** a specific program\n\n```pidstat -e [program] [args] 1```\n\nShow output in **JSON** format\n\n```pidstat -o JSON 1 5```"],["SYNOPSIS","**pidstat** [_-d_] [_-H_] [_-h_] [_-I_] [_-l_] [_-R_] [_-r_] [_-s_] [_-t_] [_-U_ [_username_]] [_-u_] [_-v_] [_-w_] [_-C comm_] [_-G process_name_] [_--human_] [_-o JSON_] [_-p_ {_pid_|SELF|ALL}] [_-T_ {TASK|CHILD|ALL}] [_interval_] [_count_] [_-e program args_]"],["DESCRIPTION","**pidstat** is used for monitoring individual tasks currently being managed by the Linux kernel. It reports CPU usage, memory utilization, I/O statistics, context switches, and other resource usage for selected processes or all tasks."],["PARAMETERS","**-d**\n> Display I/O statistics (disk reads/writes, I/O delays)\n\n**-r**\n> Report page faults and memory utilization metrics\n\n**-u**\n> Show CPU usage percentages (default if no option specified)\n\n**-w**\n> Display voluntary and involuntary context switches\n\n**-s**\n> Report stack memory usage\n\n**-t**\n> Include thread statistics for selected tasks\n\n**-p {pid|SELF|ALL}**\n> Select specific process(es), current process, or all tasks\n\n**-T {TASK|CHILD|ALL}**\n> Monitor individual tasks, children, or both\n\n**-C comm**\n> Filter by command name using regex patterns\n\n**-G process_name**\n> Display processes matching name pattern\n\n**-l**\n> Show full command names with arguments\n\n**--human**\n> Format output in human-readable sizes (1.0k, 1.2M)\n\n**-e program args**\n> Execute and monitor a specific program\n\n**-h**\n> Show all activities horizontally on a single line; omits average statistics at end of report\n\n**-H**\n> Display timestamp in seconds since epoch\n\n**-I**\n> In SMP environments, divide CPU usage by total number of processors\n\n**-o JSON**\n> Output statistics in JSON format\n\n**-R**\n> Report realtime priority and scheduling policy information\n\n**-U** [_username_]\n> Display real username of the tasks; optionally filter by specified user\n\n**-v**\n> Report values of kernel tables: number of threads and file descriptors\n\n**--dec**={0|1|2}\n> Specify the number of decimal places to use (default: 2)"],["CAVEATS","When using **-e** to run and monitor a program, a non-zero **interval** value must be specified. The **interval** and **count** parameters control reporting frequency and duration.\n\nThe `/proc` filesystem must be mounted for pidstat to work. Specifying an interval of 0 or omitting it reports statistics accumulated since system startup."],["HISTORY","**pidstat** is part of the **sysstat** package, developed by Sebastien Godard. It provides per-process statistics that complement system-wide tools like mpstat and iostat."],["INSTALL","```apt: sudo apt install sysstat```\n\n```dnf: sudo dnf install sysstat```\n\n```pacman: sudo pacman -S sysstat```\n\n```apk: sudo apk add sysstat```\n\n```zypper: sudo zypper install sysstat```\n\n```brew: brew install sysstat```\n\n```nix: nix profile install nixpkgs#sysstat```"],["SEE ALSO","[sar](/man/sar)(1), [top](/man/top)(1), [ps](/man/ps)(1), [mpstat](/man/mpstat)(1), [iostat](/man/iostat)(1), [vmstat](/man/vmstat)(1)"]],"e":["sar","top","ps","mpstat","iostat","vmstat"],"x":"pidstat 2 10"},"pio-device":{"n":"pio-device","t":"manages connected development boards","s":[["TAGLINE","manages connected development boards"],["TLDR","**List connected devices**\n\n```pio device list```\n\n**Open serial monitor**\n\n```pio device monitor```\n\n**Monitor with specific baud rate**\n\n```pio device monitor -b [115200]```\n\n**Monitor specific port**\n\n```pio device monitor -p [/dev/ttyUSB0]```"],["SYNOPSIS","**pio device** _command_ [_options_]"],["PARAMETERS","**list**\n> List connected devices.\n\n**monitor**\n> Open serial monitor.\n\n**-p**, **--port** _port_\n> Serial port.\n\n**-b**, **--baud** _rate_\n> Baud rate (default 9600).\n\n**-f**, **--filter** _filter_\n> Apply output filter.\n\n**--raw**\n> Disable input/output filters."],["DESCRIPTION","**pio device** manages connected development boards. Lists serial devices and provides serial monitor for debugging embedded applications. Supports filtering and custom baud rates."],["INSTALL","```aur: yay -S pio```"],["SEE ALSO","[pio-run](/man/pio-run)(1), [pio-debug](/man/pio-debug)(1)"]],"e":["pio-run","pio-debug"],"x":"pio device list"},"pkgctl-release":{"n":"pkgctl-release","t":"Release Arch Linux package build artifacts","s":[["TAGLINE","Release Arch Linux package build artifacts"],["TLDR","**Release a package with a commit message**\n\n```pkgctl release -m \"[commit message]\"```\n\n**Release and automatically update the pacman database**\n\n```pkgctl release --db-update -m \"[commit message]\"```\n\n**Release to the staging repository**\n\n```pkgctl release --staging -m \"[commit message]\"```\n\n**Release to the testing repository**\n\n```pkgctl release --testing -m \"[commit message]\"```"],["SYNOPSIS","**pkgctl release** [_options_]"],["PARAMETERS","**-m**, **--message** _message_\n> Commit message for the release.\n\n**-u**, **--db-update**\n> Automatically update the pacman database as the last action.\n\n**-s**, **--staging**\n> Release to the staging repository.\n\n**-t**, **--testing**\n> Release to the testing repository.\n\n**-h**, **--help**\n> Show help text."],["DESCRIPTION","**pkgctl release** performs the release step to commit, tag, and upload build artifacts to Arch Linux repositories. It automates the workflow of finalizing package releases.\n\nThe command handles version tagging, source commits, and artifact upload as a single coordinated operation. By default, artifacts are uploaded to the user's staging directory on repos.archlinux.org and a signed tag is created on the packages git repository. To also update the binary package repository, pass `--db-update` or use `pkgctl db update` separately."],["CAVEATS","Requires authentication via `pkgctl auth`. Ensure build artifacts are properly tested before release."],["SEE ALSO","[pkgctl](/man/pkgctl)(1), [pkgctl-auth](/man/pkgctl-auth)(1), [pkgctl-build](/man/pkgctl-build)(1), [pkgctl-db-update](/man/pkgctl-db-update)(1)"]],"e":["pkgctl","pkgctl-auth","pkgctl-build","pkgctl-db-update"],"x":"pkgctl release -m \"[commit message]\""},"poweroff":{"n":"poweroff","t":"Shut down and power off the system","s":[["TAGLINE","Shut down and power off the system"],["TLDR","**Power off** the system\n\n```poweroff```\n\n**Halt** the system\n\n```poweroff --halt```\n\n**Reboot** the system\n\n```poweroff --reboot```\n\nPower off **immediately** (force)\n\n```poweroff -f```\n\nWrite wtmp entry **only**\n\n```poweroff -w```"],["SYNOPSIS","**poweroff** [**-f**|**--force**] [**-w**|**--wtmp-only**] [**--halt**] [**--reboot**]"],["PARAMETERS","**-f, --force**\n> Force immediate shutdown without system manager\n\n**-w, --wtmp-only**\n> Write wtmp record without actual shutdown\n\n**--halt**\n> Halt instead of power off\n\n**--reboot**\n> Reboot instead of power off\n\n**-n, --no-wtmp**\n> Don't write wtmp record"],["DESCRIPTION","**poweroff** shuts down the system and turns off power. On systems with ACPI or APM support, it signals hardware to cut power after shutdown completes.\n\nThe command is typically a symlink to systemctl or the init system's shutdown mechanism. It ensures clean system shutdown with proper service termination."],["CAVEATS","Force option may cause data loss. Requires root privileges. Behavior varies by init system. Virtual machines may not honor power-off signal."],["HISTORY","**poweroff** has been a standard Unix command for system shutdown. Modern implementations integrate with systemd or other init systems while maintaining the traditional interface."],["INSTALL","```apt: sudo apt install runit-init```"],["SEE ALSO","[shutdown](/man/shutdown)(8), [halt](/man/halt)(8), [reboot](/man/reboot)(8), [systemctl](/man/systemctl)(1)"]],"e":["shutdown","halt","reboot","systemctl"],"x":"poweroff"},"ppmtopict":{"n":"ppmtopict","t":"Convert PPM to Macintosh PICT format","s":[["TAGLINE","Convert PPM to Macintosh PICT format"],["TLDR","**Convert PPM to Macintosh PICT**\n\n```ppmtopict [input.ppm] > [output.pict]```"],["SYNOPSIS","**ppmtopict** [_file_]"],["DESCRIPTION","**ppmtopict** converts PPM images to Macintosh PICT format for classic Mac OS. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[picttoppm](/man/picttoppm)(1), [ppmtoapplevol](/man/ppmtoapplevol)(1)"]],"e":["picttoppm","ppmtoapplevol"],"x":"ppmtopict [input.ppm] > [output.pict]"},"ppmtoxpm":{"n":"ppmtoxpm","t":"Convert PPM to X11 pixmap format","s":[["TAGLINE","Convert PPM to X11 pixmap format"],["TLDR","**Convert PPM to XPM**\n\n```ppmtoxpm [input.ppm] > [output.xpm]```\n\n**Convert with custom name**\n\n```ppmtoxpm -name [icon] [input.ppm] > [icon.xpm]```"],["SYNOPSIS","**ppmtoxpm** [_options_] [_file_]"],["PARAMETERS","**-name** _name_\n> Variable name in output.\n\n**-hexonly**\n> Use hex color codes only.\n\n**-rgb** _file_\n> RGB color name file."],["DESCRIPTION","**ppmtoxpm** converts PPM images to X11 pixmap format (.xpm). XPM is a C source format for color images in X11. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[xpmtoppm](/man/xpmtoppm)(1), [pbmtoxbm](/man/pbmtoxbm)(1)"]],"e":["xpmtoppm","pbmtoxbm"],"x":"ppmtoxpm [input.ppm] > [output.xpm]"},"prefect":{"n":"prefect","t":"Python workflow orchestration platform","s":[["TAGLINE","Python workflow orchestration platform"],["TLDR","**Start Prefect server**\n\n```prefect server start```\n\n**Deploy a flow**\n\n```prefect deploy [flow.py:flow_name]```\n\n**Run a flow locally**\n\n```prefect flow-run create [flow-name]```\n\n**List deployments**\n\n```prefect deployment ls```\n\n**Start a worker**\n\n```prefect worker start -p [work-pool]```\n\n**Create a work pool**\n\n```prefect work-pool create [pool-name] -t [process]```\n\n**View flow runs**\n\n```prefect flow-run ls```\n\n**Login to Prefect Cloud**\n\n```prefect cloud login```"],["SYNOPSIS","**prefect** _command_ [_options_]"],["COMMANDS","**server** start\n> Start local Prefect server.\n\n**deploy** _path_\n> Deploy a flow.\n\n**flow-run** create|ls|cancel\n> Manage flow runs.\n\n**deployment** ls|run|delete|build|apply\n> Manage deployments.\n\n**worker** start\n> Start a worker process.\n\n**work-pool** create|ls|delete\n> Manage work pools.\n\n**block** register|ls|create\n> Manage storage and infrastructure blocks.\n\n**cloud** login|logout|workspace\n> Prefect Cloud authentication.\n\n**profile** ls|create|use\n> Manage configuration profiles.\n\n**config** set|unset|view\n> Configuration management."],["PARAMETERS","**-p**, **--pool** _name_\n> Work pool name.\n\n**-t**, **--type** _type_\n> Work pool type: process, docker, kubernetes.\n\n**-n**, **--name** _name_\n> Deployment or resource name.\n\n**--cron** _schedule_\n> Cron schedule for deployment.\n\n**--interval** _seconds_\n> Interval schedule for deployment.\n\n**--limit** _count_\n> Limit results."],["DESCRIPTION","**prefect** is the CLI for Prefect, a Python workflow orchestration framework. It manages deployments, workers, and infrastructure for running data pipelines.\n\nFlows are Python functions decorated with @flow. **prefect deploy** packages flows for remote execution with schedules, parameters, and infrastructure settings.\n\nWorkers poll work pools and execute flow runs. **prefect worker start** launches a worker for a specific pool. Work pool types define execution environment: process (local), Docker, or Kubernetes.\n\n**prefect server start** runs the API server and UI locally. For production, use Prefect Cloud or self-hosted with PostgreSQL. The UI shows flow runs, logs, and task states.\n\nBlocks store configuration like credentials, storage locations, and infrastructure templates. Register blocks with **block register** and reference them in deployments."],["CAVEATS","Prefect 2.x is significantly different from Prefect 1.x. Workers replace agents from 1.x. Deployments require accessible code (Git, Docker, storage). Cloud features require account."],["HISTORY","Prefect was founded by **Jeremiah Lowin** in **2018** as a modern alternative to Airflow. Prefect 1.0 (2019) introduced the Hybrid model with local execution. Prefect 2.0 (2022) was a complete rewrite emphasizing simplicity with native Python code as workflows. The company offers Prefect Cloud for managed orchestration. Prefect has grown popular for data engineering and ML pipelines."],["INSTALL","```nix: nix profile install nixpkgs#prefect```"],["SEE ALSO","[dagster](/man/dagster)(1), [airflow](/man/airflow)(1), [luigi](/man/luigi)(1)"]],"e":["dagster","airflow","luigi"],"x":"prefect server start"},"pulumi-login":{"n":"pulumi-login","t":"Authenticate with a Pulumi state backend","s":[["TAGLINE","Authenticate with a Pulumi state backend"],["TLDR","**Login to Pulumi Cloud**\n\n```pulumi login```\n\n**Login to local backend**\n\n```pulumi login --local```\n\n**Login to specific cloud**\n\n```pulumi login [url]```\n\n**Login to S3 backend**\n\n```pulumi login s3://[bucket]```"],["SYNOPSIS","**pulumi** **login** [_options_] [_url_]"],["PARAMETERS","**--local**\n> Use local file backend.\n\n**--cloud-url** _url_\n> Specify backend URL.\n\n**-c**, **--cloud**\n> Login to Pulumi Cloud."],["DESCRIPTION","**pulumi login** authenticates with a Pulumi backend. Supports Pulumi Cloud, self-hosted backends, and cloud storage backends (S3, Azure Blob, GCS). Backend stores stack state and history."],["INSTALL","```pacman: sudo pacman -S pulumi```\n\n```apk: sudo apk add pulumi```\n\n```brew: brew install pulumi```\n\n```nix: nix profile install nixpkgs#pulumi```"],["SEE ALSO","[pulumi](/man/pulumi)(1), [pulumi-logout](/man/pulumi-logout)(1), [pulumi-whoami](/man/pulumi-whoami)(1)"]],"e":["pulumi","pulumi-logout","pulumi-whoami"],"x":"pulumi login"},"pw-metadata":{"n":"pw-metadata","t":"Monitor and manage PipeWire metadata","s":[["TAGLINE","Monitor and manage PipeWire metadata"],["TLDR","Show metadata in **default** name\n\n```pw-metadata```\n\nShow metadata with **ID 0** in settings\n\n```pw-metadata -n settings 0```\n\nList all **available metadata** objects\n\n```pw-metadata -l```\n\nKeep running and **log changes** to metadata\n\n```pw-metadata -m```\n\n**Delete** all metadata\n\n```pw-metadata -d```\n\n**Set** log.level to 1 in settings\n\n```pw-metadata -n settings 0 log.level 1```"],["SYNOPSIS","**pw-metadata** [_options_] [_id_] [_key_] [_value_] [_type_]"],["PARAMETERS","**-n**, **--name** _name_\n> Metadata name to operate on (default: \"default\")\n\n**-l**, **--list**\n> List all available metadata objects\n\n**-m**, **--monitor**\n> Monitor metadata changes continuously\n\n**-d**, **--delete**\n> Delete metadata entries\n\n**-r**, **--remote** _NAME_\n> Name of the remote instance to connect to (default: default PipeWire instance).\n\n**--version**\n> Show version information.\n\n**-h**, **--help**\n> Display help message."],["DESCRIPTION","**pw-metadata** monitors, sets, and deletes metadata on PipeWire objects. Metadata provides key-value pairs associated with PipeWire graph objects, used for configuration and runtime state management.\n\nThe tool can operate on different metadata namespaces including \"default\" for general metadata and \"settings\" for PipeWire configuration values like log levels."],["CAVEATS","Modifying metadata in the \"settings\" namespace can affect PipeWire behavior immediately. Use caution when deleting metadata as it may reset configuration to defaults."],["HISTORY","Part of **PipeWire**, the modern multimedia framework for Linux. Provides runtime inspection and modification of PipeWire's metadata system."],["INSTALL","```apt: sudo apt install pipewire-bin```\n\n```apk: sudo apk add pipewire-tools```\n\n```zypper: sudo zypper install pipewire-tools```"],["SEE ALSO","[pipewire](/man/pipewire)(1), [pw-mon](/man/pw-mon)(1), [pw-cli](/man/pw-cli)(1)"]],"e":["pipewire","pw-mon","pw-cli"],"x":"pw-metadata"},"qemu":{"n":"qemu","t":"Open-source machine emulator and virtualizer","s":[["TAGLINE","Open-source machine emulator and virtualizer"],["TLDR","**Run virtual machine**\n\n```qemu-system-x86_64 -hda [disk.img]```\n\n**Run with memory**\n\n```qemu-system-x86_64 -m [2048] -hda [disk.img]```\n\n**Boot from ISO**\n\n```qemu-system-x86_64 -cdrom [installer.iso] -boot d```\n\n**Enable KVM acceleration**\n\n```qemu-system-x86_64 -enable-kvm -hda [disk.img]```\n\n**Network with port forwarding**\n\n```qemu-system-x86_64 -hda [disk.img] -netdev user,id=net0,hostfwd=tcp::2222-:22 -device e1000,netdev=net0```"],["SYNOPSIS","**qemu-system-***_arch_ [_options_]"],["PARAMETERS","**-hda** _FILE_\n> Primary hard disk.\n\n**-m** _SIZE_\n> Memory in MB.\n\n**-cdrom** _FILE_\n> CD-ROM image.\n\n**-enable-kvm**\n> KVM acceleration.\n\n**-boot** _ORDER_\n> Boot order.\n\n**-smp** _CPUS_\n> CPU count."],["DESCRIPTION","**QEMU** (Quick EMUlator) is an open-source machine emulator and virtualizer that can run operating systems for any supported architecture on any host. It provides full system emulation including CPU, memory, storage, and peripheral devices, supporting architectures like x86, ARM, RISC-V, MIPS, PowerPC, and others.\n\nWhen combined with KVM (Kernel-based Virtual Machine) on Linux, QEMU achieves near-native performance by using hardware virtualization extensions. Without KVM, it uses dynamic binary translation for full software emulation, which is slower but works across platforms. QEMU disk images support multiple formats (qcow2, raw, vmdk) with features like snapshots, thin provisioning, and backing files."],["CAVEATS","Performance varies by mode. KVM for best performance."],["HISTORY","QEMU was created by **Fabrice Bellard** for processor emulation."],["INSTALL","```dnf: sudo dnf install qemu```\n\n```zypper: sudo zypper install qemu```\n\n```brew: brew install qemu```\n\n```nix: nix profile install nixpkgs#qemu```"],["SEE ALSO","[qemu-img](/man/qemu-img)(1), [kvm](/man/kvm)(1), [virsh](/man/virsh)(1)"]],"e":["qemu-img","kvm","virsh"],"x":"qemu-system-x86_64 -hda [disk.img]"},"recset":{"n":"recset","t":"Update field values in GNU recutils databases","s":[["TAGLINE","Update field values in GNU recutils databases"],["TLDR","**Update field value**\n\n```recset -f [Field] -v \"[new_value]\" -e \"[condition]\" [file.rec]```\n\n**Set field in all records**\n\n```recset -f [Status] -v \"[active]\" [file.rec]```\n\n**Update specific type**\n\n```recset -t [Type] -f [Field] -v \"[value]\" -e \"[condition]\" [file.rec]```\n\n**Add field if missing**\n\n```recset -a -f [NewField] -v \"[value]\" [file.rec]```"],["SYNOPSIS","**recset** [_options_] [_file_]"],["PARAMETERS","**-t**, **--type** _type_\n> Record type.\n\n**-f**, **--field** _name_\n> Field to set.\n\n**-v**, **--value** _value_\n> New value.\n\n**-e**, **--expression** _expr_\n> Selection expression.\n\n**-a**, **--add**\n> Add field if missing.\n\n**-n**, **--num** _n_\n> Update first n matches."],["DESCRIPTION","**recset** modifies field values in GNU recutils plain-text database files, updating existing fields or adding new ones based on selection expressions. It uses the same expression syntax as **recsel** to target specific records, then sets the specified field to a new value.\n\nThe **-a** flag adds the field only to records where it does not already exist, and **-n** limits updates to the first N matching records. Without a selection expression, all records in the file are updated. The file is modified in place. Part of the GNU recutils toolkit."],["EXAMPLES","```bash"],["Update matching records","recset -f Status -v \"completed\" -e \"Status = 'pending'\" tasks.rec"],["Update all records","recset -f Updated -v \"$(date)\" data.rec"],["Update specific type","recset -t Contact -f Phone -v \"555-0000\" -e \"Name = 'John'\" contacts.rec"],["Add field if not exists","recset -a -f Category -v \"General\" -e \"!Category\" items.rec"],["Update first match","recset -n 1 -f Priority -v \"high\" -e \"Status = 'new'\" tasks.rec\n```"],["CAVEATS","Modifies file in place. Use selection expressions carefully. Part of GNU recutils."],["HISTORY","recset is part of **GNU recutils** by **Jose E. Marchesi** for managing text databases."],["INSTALL","```dnf: sudo dnf install recutils```\n\n```apk: sudo apk add recutils```\n\n```brew: brew install recutils```\n\n```nix: nix profile install nixpkgs#recutils```"],["SEE ALSO","[recsel](/man/recsel)(1), [recins](/man/recins)(1), [recdel](/man/recdel)(1)"]],"e":["recsel","recins","recdel"],"x":"recset -f [Field] -v \"[new_value]\" -e \"[condition]\" [file.rec]"},"repquota":{"n":"repquota","t":"Report filesystem disk quota usage","s":[["TAGLINE","Report filesystem disk quota usage"],["TLDR","**Report** stats for all quotas in use\n\n```sudo repquota -a```\n\n**Report** quota stats for all users, even those not using any quota\n\n```sudo repquota -v [filesystem]```\n\n**Report** on quotas for users only\n\n```repquota -u [filesystem]```\n\n**Report** on quotas for groups only\n\n```sudo repquota -g [filesystem]```\n\n**Report** on used quota and limits in human-readable format\n\n```sudo repquota -s [filesystem]```\n\n**Report** on all quotas for users and groups in human-readable format\n\n```sudo repquota -augs```"],["SYNOPSIS","**repquota** [_options_] [_filesystem_...]"],["PARAMETERS","**-a, --all**\n> Report on all filesystems with quotas enabled\n\n**-u, --user**\n> Report on user quotas (default)\n\n**-g, --group**\n> Report on group quotas\n\n**-v, --verbose**\n> Include users/groups with no storage used\n\n**-s, --human-readable**\n> Display sizes in human-readable format (KB, MB, GB)\n\n**-p, --raw-grace**\n> Report grace times in seconds since epoch\n\n**-n, --no-names**\n> Show numeric UIDs/GIDs instead of names\n\n**-c, --batch-translation**\n> Cache name lookups for better performance"],["DESCRIPTION","**repquota** displays a summary of disk quotas for specified filesystems. It shows the current usage, soft limits, hard limits, and grace period status for users and/or groups.\n\nThe output includes columns for used space, soft/hard limits, used files (inodes), and whether the user is over quota. The grace period indicates how long a user can exceed the soft limit."],["CAVEATS","Requires filesystem quota support and quota utilities to be installed. Must typically be run as root to view all users' quotas. Quotas must be enabled on the filesystem for meaningful output."],["INSTALL","```apt: sudo apt install quota```\n\n```dnf: sudo dnf install quota```\n\n```pacman: sudo pacman -S quota-tools```\n\n```apk: sudo apk add quota-tools```\n\n```zypper: sudo zypper install quota```\n\n```nix: nix profile install nixpkgs#quota```"],["SEE ALSO","[quota](/man/quota)(1), [quotacheck](/man/quotacheck)(8), [quotaon](/man/quotaon)(8), [edquota](/man/edquota)(8), [setquota](/man/setquota)(8)"]],"e":["quota","quotacheck","quotaon","edquota","setquota"],"x":"sudo repquota -a"},"ri":{"n":"ri","t":"View Ruby API documentation","s":[["TAGLINE","View Ruby API documentation"],["TLDR","**View class documentation**\n\n```ri [Array]```\n\n**View method documentation**\n\n```ri [Array#map]```\n\n**View instance method**\n\n```ri [String#split]```\n\n**View class method**\n\n```ri [File.open]```\n\n**List all classes**\n\n```ri -l```"],["SYNOPSIS","**ri** [_options_] [_name_...]"],["PARAMETERS","**-l**, **--list**\n> List classes.\n\n**-f**, **--format** _name_\n> Output format.\n\n**-i**, **--interactive**\n> Interactive mode.\n\n**-T**, **--no-pager**\n> Don't use pager.\n\n**--doc-dir** _dir_\n> Documentation directory."],["DESCRIPTION","**ri** (Ruby Information) displays Ruby documentation from the command line. It reads documentation generated by RDoc and provides quick access to class and method information."],["EXAMPLES","```bash"],["Class documentation","ri Array\nri String"],["Instance method (use #)","ri Array#each\nri String#gsub"],["Class method (use .)","ri File.read\nri Dir.glob"],["Search for method","ri -l | grep map"],["Interactive mode","ri -i\n```"],["NOTATION","```\nClass          - ri Array\nClass#method   - Instance method (ri Array#map)\nClass.method   - Class method (ri File.open)\nClass::Const   - Constant (ri File::SEPARATOR)\n```"],["CAVEATS","Documentation must be installed. Use gem rdoc --all to generate gem docs. Part of Ruby standard library."],["HISTORY","ri is part of **Ruby's** RDoc system, providing command-line documentation access since early Ruby versions."],["INSTALL","```apt: sudo apt install ruby```\n\n```dnf: sudo dnf install ruby```\n\n```pacman: sudo pacman -S ruby```\n\n```apk: sudo apk add ruby-rdoc```\n\n```zypper: sudo zypper install ruby```\n\n```brew: brew install ruby```\n\n```nix: nix profile install nixpkgs#ruby```"],["SEE ALSO","[rdoc](/man/rdoc)(1), [ruby](/man/ruby)(1), [irb](/man/irb)(1)"]],"e":["rdoc","ruby","irb"],"x":"ri [Array]"},"rmarkdown":{"n":"rmarkdown","t":"Render R Markdown documents to various formats","s":[["TAGLINE","Render R Markdown documents to various formats"],["TLDR","**Render R Markdown to HTML**\n\n```Rscript -e \"rmarkdown::render('[file.Rmd]')\"```\n\n**Render to PDF**\n\n```Rscript -e \"rmarkdown::render('[file.Rmd]', output_format='pdf_document')\"```\n\n**Render to Word**\n\n```Rscript -e \"rmarkdown::render('[file.Rmd]', output_format='word_document')\"```\n\n**Render with parameters**\n\n```Rscript -e \"rmarkdown::render('[file.Rmd]', params=list(year=2024))\"```"],["SYNOPSIS","R Markdown document processing"],["DESCRIPTION","**R Markdown** combines R code with Markdown text to create dynamic documents. It can produce HTML, PDF, Word documents, presentations, dashboards, and more."],["YAML HEADER","```yaml\n---\ntitle: \"My Report\"\nauthor: \"Name\"\ndate: \"`r Sys.Date()`\"\noutput:\n  html_document:\n    toc: true\n    theme: united\n---\n```"],["EXAMPLES","```r"],["In R","library(rmarkdown)\nrender(\"report.Rmd\")"],["Specify output","render(\"report.Rmd\", output_format = \"pdf_document\")"],["With parameters","render(\"report.Rmd\", params = list(data = \"sales.csv\"))\n```"],["CODE CHUNKS","````markdown\n```{r setup, include=FALSE}\nknitr::opts_chunk$set(echo = TRUE)\n```\n\n```{r plot-example, fig.width=8}\nplot(cars)\n```\n````"],["OUTPUT FORMATS","```\nhtml_document\npdf_document (requires LaTeX)\nword_document\nioslides_presentation\nbeamer_presentation\nflexdashboard\n```"],["CAVEATS","Requires R and rmarkdown package. PDF needs LaTeX (tinytex). Processing can be slow."],["HISTORY","R Markdown was developed by **RStudio** (now Posit) building on knitr by **Yihui Xie**."],["SEE ALSO","[pandoc](/man/pandoc)(1), [quarto](/man/quarto)(1)"]],"e":["pandoc","quarto"],"x":"Rscript -e \"rmarkdown::render('[file.Rmd]')\""},"rss2email":{"n":"rss2email","t":"Forward RSS feed items to email","s":[["TAGLINE","Forward RSS feed items to email"],["TLDR","**Initialize configuration**\n\n```r2e new [email@example.com]```\n\n**Add feed**\n\n```r2e add [feed_name] [https://example.com/feed.xml]```\n\n**Run (send emails)**\n\n```r2e run```\n\n**List feeds**\n\n```r2e list```\n\n**Delete feed**\n\n```r2e delete [feed_name]```"],["SYNOPSIS","**r2e** _command_ [_options_]"],["COMMANDS","**new** _email_\n> Initialize with default email.\n\n**add** _name_ _url_\n> Add feed.\n\n**run**\n> Process feeds and send.\n\n**list**\n> List feeds.\n\n**delete** _name_\n> Remove feed.\n\n**reset** _name_\n> Reset feed date.\n\n**opmlimport** _file_\n> Import OPML."],["DESCRIPTION","**rss2email** (r2e) converts RSS/Atom feeds to email. It checks feeds and sends new items to your email, allowing you to read feeds in your email client."],["EXAMPLES","```bash"],["Initialize","r2e new user@example.com"],["Add feeds","r2e add hackernews https://news.ycombinator.com/rss\nr2e add lwn https://lwn.net/headlines/rss"],["List feeds","r2e list"],["Run manually","r2e run"],["Reset feed (resend all)","r2e reset hackernews"],["Import OPML","r2e opmlimport feeds.opml\n```"],["CONFIGURATION","```ini"],["~/.config/rss2email.cfg","[DEFAULT]\nfrom = rss2email@example.com\nto = user@example.com\nsmtp-server = localhost\n```"],["CRON","```bash"],["Run every hour","0 * * * * r2e run\n```"],["CAVEATS","Requires working mail setup (SMTP or local). Stores state in ~/.local/share/rss2email/. Run periodically via cron."],["HISTORY","rss2email was created by **Aaron Swartz** in 2004, now maintained by the community."],["INSTALL","```dnf: sudo dnf install rss2email```\n\n```pacman: sudo pacman -S rss2email```\n\n```zypper: sudo zypper install rss2email```\n\n```nix: nix profile install nixpkgs#rss2email```"],["SEE ALSO","[fetchmail](/man/fetchmail)(1), [procmail](/man/procmail)(1), [mutt](/man/mutt)(1)"]],"e":["fetchmail","procmail","mutt"],"x":"r2e new [email@example.com]"},"runuser":{"n":"runuser","t":"Run commands as a different user","s":[["TAGLINE","Run commands as a different user"],["TLDR","Run command as **different user**\n\n```runuser [user] -c '[command]'```\n\nRun as user and **group**\n\n```runuser [user] -g [group] -c '[command]'```\n\nStart **login shell**\n\n```runuser [user] -l```\n\nUse **specific shell**\n\n```runuser [user] -s /bin/bash```\n\n**Preserve** environment\n\n```runuser [user] -p -c '[command]'```"],["SYNOPSIS","**runuser** [**-l**] [**-c** _command_] [**-g** _group_] [**-s** _shell_] [**-p**] _user_"],["PARAMETERS","**-c, --command _command_**\n> Command to execute\n\n**-g, --group _group_**\n> Specify group\n\n**-l, --login**\n> Start login shell\n\n**-s, --shell _shell_**\n> Use specific shell\n\n**-p, --preserve-environment**\n> Keep environment variables\n\n**-w, --whitelist-environment**\n> Preserve specific variables"],["DESCRIPTION","**runuser** runs commands as another user without requiring password authentication. Unlike su, it's designed for use by root in system scripts and services where interactive authentication isn't appropriate.\n\nThe tool is commonly used in init scripts and system services to drop privileges to a specific user before executing commands."],["CAVEATS","Requires root to run. No password prompt (unlike su). Environment handling depends on options. Login shell resets most environment."],["HISTORY","**runuser** is part of **util-linux**, providing a non-interactive way for root to run commands as other users. It's preferred over su in scripts due to its predictable behavior."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add runuser```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[su](/man/su)(1), [sudo](/man/sudo)(8), [runcon](/man/runcon)(1)"]],"e":["su","sudo","runcon"],"x":"runuser [user] -c '[command]'"},"s3scanner":{"n":"s3scanner","t":"Scan for misconfigured S3 buckets across cloud providers","s":[["TAGLINE","Scan for misconfigured S3 buckets across cloud providers"],["TLDR","**Scan a list of bucket names from a file**\n\n```s3scanner -bucket-file [names.txt]```\n\n**Scan and enumerate objects in a bucket**\n\n```s3scanner -bucket [my-bucket] -enumerate```\n\n**Scan a GCP bucket**\n\n```s3scanner -provider gcp -bucket [my-bucket]```\n\n**Scan with JSON output**\n\n```s3scanner -bucket [my-bucket] -json```"],["SYNOPSIS","**s3scanner** [_options_]"],["PARAMETERS","**-bucket** _NAME_\n> Single bucket name to scan.\n\n**-bucket-file** _FILE_\n> File containing bucket names to scan.\n\n**-enumerate**\n> List all objects found in accessible buckets.\n\n**-provider** _PROVIDER_\n> Storage provider: aws, gcp, digitalocean, dreamhost, linode, or scaleway.\n\n**-threads** _N_\n> Number of concurrent scan workers. Default: 4.\n\n**-json**\n> Output results in JSON format."],["DESCRIPTION","**s3scanner** checks permissions on object storage buckets to detect public access misconfigurations. It supports AWS and multiple S3-compatible providers, multi-threaded scanning, database persistence via PostgreSQL, and RabbitMQ integration for scaled operations."],["CAVEATS","Object enumeration is limited to 5,000 pages per bucket. Non-AWS providers support anonymous-user permissions only. Intended for authorized security testing only."],["HISTORY","**s3scanner** was created by **Dan Salmon** (sa7mon) and is written in **Go**."],["INSTALL","```brew: brew install s3scanner```\n\n```nix: nix profile install nixpkgs#s3scanner```"],["SEE ALSO","[aws](/man/aws)(1), [rclone](/man/rclone)(1), [trivy](/man/trivy)(1)"]],"e":["aws","rclone","trivy"],"x":"s3scanner -bucket-file [names.txt]"},"salt":{"n":"salt","t":"Remote execution and configuration management system","s":[["TAGLINE","Remote execution and configuration management system"],["TLDR","**Ping all minions**\n\n```salt '*' test.ping```\n\n**Run command on minions**\n\n```salt '*' cmd.run '[command]'```\n\n**Target specific minion**\n\n```salt '[minion-id]' test.ping```\n\n**Apply state**\n\n```salt '*' state.apply```\n\n**Apply specific state**\n\n```salt '*' state.apply [webserver]```\n\n**Install package**\n\n```salt '*' pkg.install [nginx]```\n\n**Show grains**\n\n```salt '[minion]' grains.items```\n\n**Copy file**\n\n```salt '*' cp.get_file [salt://file.txt] [/tmp/file.txt]```"],["SYNOPSIS","**salt** [_target_] _function_ [_args_]"],["PARAMETERS","_TARGET_\n> Minion pattern.\n\n_FUNCTION_\n> Module.function to execute.\n\n**-G** _GRAIN_\n> Target by grain.\n\n**-E**\n> Regex target.\n\n**-L**\n> List target.\n\n**-C**\n> Compound target.\n\n**--async**\n> Async execution.\n\n**--timeout** _SECS_\n> Command timeout.\n\n**-v**, **--verbose**\n> Verbose output."],["DESCRIPTION","**salt** is the primary command-line interface for SaltStack, a remote execution and configuration management system. It sends commands from the Salt master to minions (managed nodes), executing them in parallel across potentially thousands of systems simultaneously and returning results.\n\nTargeting determines which minions receive commands, using glob patterns (default), regular expressions (**-E**), grain-based matching (**-G**) for targeting by OS or hardware properties, or compound expressions (**-C**) combining multiple criteria. Execution modules provide hundreds of functions covering package management, file operations, service control, and system administration.\n\nStates define the desired configuration of systems declaratively using YAML files. The **state.apply** function ensures minions converge to the defined state, making Salt suitable for both ad-hoc command execution and systematic infrastructure management."],["CAVEATS","Requires Salt master/minion setup. Large infrastructure needs tuning. State complexity grows."],["HISTORY","**Salt** was created by **Thomas Hatch** and released in **2011**. It became **SaltStack**, later acquired by **VMware** in 2020."],["INSTALL","```dnf: sudo dnf install salt```\n\n```apk: sudo apk add salt-lts-master```\n\n```zypper: sudo zypper install salt```\n\n```nix: nix profile install nixpkgs#salt```"],["SEE ALSO","[salt-master](/man/salt-master)(1), [salt-minion](/man/salt-minion)(1), [salt-key](/man/salt-key)(1)"]],"e":["salt-master","salt-minion","salt-key"],"x":"salt '*' test.ping"},"scalac":{"n":"scalac","t":"Scala source code compiler","s":[["TAGLINE","Scala source code compiler"],["TLDR","**Compile a Scala source file**\n\n```scalac [Hello.scala]```\n\n**Compile multiple files** to a specific directory\n\n```scalac -d [classes/] [*.scala]```\n\n**Compile with classpath** dependencies\n\n```scalac -classpath [lib/*] [Source.scala]```\n\n**Compile with verbose output**\n\n```scalac -verbose [Source.scala]```\n\n**Compile with additional warnings**\n\n```scalac -Xlint [Source.scala]```\n\n**Treat warnings as errors**\n\n```scalac -Werror [Source.scala]```\n\n**Show compiler version**\n\n```scalac -version```"],["SYNOPSIS","**scalac** [_options_] [_source files_]"],["PARAMETERS","**-d** _directory|jar_\n> Specify where to place generated class files\n\n**-classpath** _path_\n> Specify where to find user class files (colon-separated on Unix)\n\n**-bootclasspath** _path_\n> Override location of bootstrap class files\n\n**-sourcepath** _path_\n> Specify where to find input source files\n\n**-verbose**\n> Output messages about what the compiler is doing\n\n**-deprecation**\n> Emit warning and location for usages of deprecated APIs\n\n**-unchecked**\n> Enable additional warnings for type erasure\n\n**-Werror**\n> Fail compilation if there are any warnings\n\n**-Xlint**\n> Enable recommended additional warnings\n\n**-version**\n> Print product version and exit\n\n**-help**\n> Print synopsis of standard options"],["DESCRIPTION","**scalac** is the compiler for the Scala programming language. It reads Scala source files (.scala) and compiles them into Java bytecode class files that run on the JVM.\n\nBy default, class files are placed in the same directory as their source files. Use **-d** to specify an alternate output directory or JAR file. The compiler integrates with Java classes via the classpath mechanism.\n\nOptions are categorized by prefix: **-W** for warnings, **-V** for verbose output, **-X** for extended options, and **-Y** for private/experimental options."],["CONFIGURATION","**JAVA_HOME**\n> Path to the JDK installation used by the Scala compiler.\n\n**JAVA_OPTS**\n> Additional JVM options passed to the compiler process (e.g., memory settings)."],["CAVEATS","The JAVA_HOME environment variable must point to a valid JDK installation. JAVA_OPTS can pass additional options to the underlying JVM. Scala 3 uses a different compiler with some incompatible options."],["HISTORY","Scala was designed by **Martin Odersky** starting in **2001** at EPFL, with the first public release in **2004**. The name stands for \"scalable language.\" Scala 3 (Dotty) was released in **2021** with significant language improvements."],["SEE ALSO","[scala](/man/scala)(1), [javac](/man/javac)(1), [sbt](/man/sbt)(1), [java](/man/java)(1)"]],"e":["scala","javac","sbt","java"],"x":"scalac [Hello.scala]"},"schroot":{"n":"schroot","t":"Run commands in managed chroot environments","s":[["TAGLINE","Run commands in managed chroot environments"],["TLDR","**List** available chroots\n\n```schroot --list```\n\nRun **command** in specific chroot\n\n```schroot --chroot chroot_name command```\n\nRun command with **options** in chroot\n\n```schroot --chroot chroot_name -- command command_options```\n\nRun command in **all** chroots\n\n```schroot --all command```\n\nStart **interactive shell** as specific user\n\n```schroot --chroot chroot_name --user user```\n\n**Begin** new session\n\n```schroot --begin-session --chroot chroot_name```\n\n**Connect** to existing session\n\n```schroot --run-session --chroot session_id```\n\n**End** a session\n\n```schroot --end-session --chroot session_id```"],["SYNOPSIS","**schroot** [_options_] [_command_]"],["PARAMETERS","**-l**, **--list**\n> List available chroots.\n\n**-c** _NAME_, **--chroot** _NAME_\n> Specify chroot environment.\n\n**-u** _USER_, **--user** _USER_\n> Run as specified user.\n\n**-a**, **--all**\n> Run in all available chroots.\n\n**--begin-session**\n> Start new session.\n\n**--run-session**\n> Connect to existing session.\n\n**--end-session**\n> Terminate session.\n\n**-i**, **--info**\n> Display detailed information about specified chroots.\n\n**-d** _DIR_, **--directory** _DIR_\n> Change to directory inside the chroot before running command.\n\n**-p**, **--preserve-environment**\n> Preserve the user's environment inside the chroot.\n\n**-q**, **--quiet**\n> Show less output.\n\n**-v**, **--verbose**\n> Show more output."],["DESCRIPTION","**schroot** runs commands or starts interactive shells in different root directory environments. It provides more features and customization than basic chroot, including session management and user mapping.\n\nConfigured via /etc/schroot/schroot.conf, it supports multiple chroot types and configurations."],["CONFIGURATION","**/etc/schroot/schroot.conf**\n> Main configuration file defining available chroot environments, their types, locations, and access permissions.\n\n**/etc/schroot/chroot.d/**\n> Directory for individual chroot definition files, allowing modular configuration.\n\n**/etc/schroot/setup.d/**\n> Directory containing setup scripts executed when sessions begin and end, handling mount points, networking, and environment preparation."],["CAVEATS","Requires proper chroot configuration. Sessions must be ended to release resources."],["HISTORY","Developed for **Debian** as an improved chroot solution with better security and usability features."],["INSTALL","```apt: sudo apt install schroot```\n\n```pacman: sudo pacman -S schroot```\n\n```nix: nix profile install nixpkgs#schroot```"],["SEE ALSO","[chroot](/man/chroot)(8), [debootstrap](/man/debootstrap)(8)"]],"e":["chroot","debootstrap"],"x":"schroot --list"},"sfdk-apply":{"n":"sfdk-apply","t":"Apply configuration patches to Sailfish projects","s":[["TAGLINE","Apply configuration patches to Sailfish projects"],["TLDR","**Apply configuration patch**\n\n```sfdk apply [patch_file]```"],["SYNOPSIS","**sfdk apply** [_options_] _patch_"],["DESCRIPTION","**sfdk apply** applies configuration patches to Sailfish OS projects. Part of the Sailfish SDK (sfdk) command-line tools for developing Sailfish OS applications."],["SEE ALSO","[sfdk](/man/sfdk)(1), [sfdk-config](/man/sfdk-config)(1)"]],"e":["sfdk","sfdk-config"],"x":"sfdk apply [patch_file]"},"siege":{"n":"siege","t":"HTTP load testing and benchmarking tool","s":[["TAGLINE","HTTP load testing and benchmarking tool"],["TLDR","**Test a URL** with default settings (15 concurrent users)\n\n```siege [https://example.com/]```\n\n**Benchmark with specific concurrency** and duration\n\n```siege -c [50] -t [1M] [https://example.com/]```\n\n**Run benchmark mode** (no delays between requests)\n\n```siege -b -c [100] -t [30S] [https://example.com/]```\n\n**Test multiple URLs** from a file\n\n```siege -f [urls.txt]```\n\n**Simulate internet traffic** (random URL selection)\n\n```siege -i -f [urls.txt] -c [25] -t [5M]```\n\n**Run with custom delay** between requests\n\n```siege -d [2] -c [10] [https://example.com/]```\n\n**Display headers** for a single request\n\n```siege -g [https://example.com/]```\n\n**Show current configuration**\n\n```siege -C```"],["SYNOPSIS","**siege** [_options_] [_URL_]"],["PARAMETERS","**-c**, **--concurrent** _num_\n> Number of concurrent simulated users (default: 15)\n\n**-r**, **--reps** _num_|**once**\n> Number of repetitions per user; \"once\" runs each URL once\n\n**-t**, **--time** _NUMm_\n> Run duration with modifier: S (seconds), M (minutes), H (hours)\n\n**-d**, **--delay** _num_\n> Random delay (0 to num seconds) between requests per user\n\n**-b**, **--benchmark**\n> Benchmark mode; removes delays for maximum throughput\n\n**-f**, **--file** _file_\n> Read URLs from a text file\n\n**-i**, **--internet**\n> Simulate internet traffic by randomizing URL order\n\n**-g**, **--get** _URL_\n> Display HTTP headers for a single request\n\n**-p**, **--print** _URL_\n> Display headers and response body\n\n**-A**, **--agent** _string_\n> Override the User-Agent header\n\n**-H**, **--header** _\"header: value\"_\n> Add custom request header\n\n**-T**, **--content-type** _type_\n> Override Content-Type header\n\n**-v**, **--verbose**\n> Show detailed output with color-coded responses\n\n**-q**, **--quiet**\n> Suppress output; useful for scripts\n\n**-L**, **--log** _file_\n> Write results to specified log file\n\n**-m**, **--mark** _\"string\"_\n> Log a marker message before stats; useful for identifying test runs\n\n**-R**, **--rc** _file_\n> Use alternative configuration file\n\n**--no-follow**\n> Do not follow 3xx redirects\n\n**-C**, **--config**\n> Display current configuration settings"],["DESCRIPTION","**siege** is a multi-threaded HTTP/HTTPS load testing and benchmarking utility. It simulates multiple concurrent users accessing a web server to measure performance under stress. Metrics reported include response time, transaction rate, throughput, concurrency, and availability.\n\nThe tool operates in three modes: **regression** (sequential URL testing), **internet simulation** (randomized access patterns with **-i**), and **brute force benchmarking** (maximum throughput with **-b**).\n\nBy default, each simulated user pauses randomly between requests to simulate realistic traffic. The **-d** option controls this delay range, while **-b** eliminates it entirely for pure throughput testing.\n\nConfiguration is stored in **~/.siege/siege.conf**. URLs can be specified on the command line or in a file with **-f**, supporting POST data and custom headers."],["CONFIGURATION","**~/.siege/siege.conf**\n> User configuration file for default concurrency, delay, logging, protocol version, and other settings. Generated with **siege.config**.\n\n**~/.siege/urls.txt**\n> Default URL file used when no **-f** option or URL argument is provided."],["CAVEATS","High concurrency levels can overwhelm target servers and may constitute a denial-of-service attack. Always obtain permission before load testing servers you do not own. Some hosting providers prohibit load testing in their terms of service."],["HISTORY","**siege** was created by **Jeffrey Fulmer** (JoeDog) and first released around **2000**. It was designed to help web developers measure their code's performance under load. The tool has been actively maintained and updated to support modern HTTP features including HTTPS and HTTP/1.1."],["INSTALL","```apt: sudo apt install siege```\n\n```dnf: sudo dnf install siege```\n\n```pacman: sudo pacman -S siege```\n\n```zypper: sudo zypper install siege```\n\n```brew: brew install siege```\n\n```nix: nix profile install nixpkgs#siege```"],["SEE ALSO","[ab](/man/ab)(1), [wrk](/man/wrk)(1), [curl](/man/curl)(1), [hey](/man/hey)(1), [vegeta](/man/vegeta)(1)"]],"e":["ab","wrk","curl","hey","vegeta"],"x":"siege [https://example.com/]"},"skipfish":{"n":"skipfish","t":"Web application security scanner","s":[["TAGLINE","Web application security scanner"],["TLDR","**Scan a website** with default settings\n\n```skipfish -o [output_dir] [https://example.com]```\n\n**Scan with authentication**\n\n```skipfish -o [output_dir] -A [user]:[password] [https://example.com]```\n\n**Scan with custom wordlist**\n\n```skipfish -o [output_dir] -W [wordlist.txt] [https://example.com]```\n\n**Limit scan depth**\n\n```skipfish -o [output_dir] -d [5] [https://example.com]```\n\n**Exclude URLs matching pattern**\n\n```skipfish -o [output_dir] -X [/logout] [https://example.com]```\n\n**Limit requests per second**\n\n```skipfish -o [output_dir] -l [10] [https://example.com]```\n\n**Scan with cookies**\n\n```skipfish -o [output_dir] -C \"[session=abc123]\" [https://example.com]```"],["SYNOPSIS","**skipfish** [_options_] -W _wordlist_ -o _output_dir_ _url_ [_url_...]"],["PARAMETERS","**-o** _DIR_\n> Output directory for report (required, must not exist)\n\n**-W** _FILE_\n> Wordlist file for dictionary-based probing\n\n**-S** _FILE_\n> Load additional scope rules\n\n**-A** _USER:PASS_\n> HTTP authentication credentials\n\n**-C** _NAME=VAL_\n> Add custom cookie to all requests\n\n**-H** _NAME=VAL_\n> Add custom HTTP header\n\n**-b** _i|f|p_\n> Browser headers (MSIE, Firefox, iPhone)\n\n**-d** _DEPTH_\n> Maximum crawl depth (default: 16)\n\n**-c** _NUM_\n> Maximum children per node (default: 512)\n\n**-r** _NUM_\n> Maximum total requests\n\n**-l** _NUM_\n> Maximum requests per second\n\n**-I** _STRING_\n> Only crawl URLs containing string\n\n**-X** _STRING_\n> Exclude URLs containing string\n\n**-D** _DOMAIN_\n> Add domain to scan scope\n\n**-K** _PARAM_\n> Skip fuzzing specified parameter\n\n**-N**\n> Do not accept new cookies\n\n**-M**\n> Log mixed content (HTTP in HTTPS)\n\n**-E**\n> Log cache mismatches\n\n**-U**\n> Log external URLs found\n\n**-Q**\n> Suppress duplicate nodes in report\n\n**-u**\n> Quiet mode; suppress console output\n\n**-v**\n> Verbose mode"],["DESCRIPTION","**skipfish** is a high-performance web application security scanner that creates an interactive sitemap through recursive crawling and dictionary-based probing. It performs active security checks and generates an HTML report highlighting potential vulnerabilities.\n\nThe scanner detects issues including XSS, SQL injection, shell injection, directory traversal, and various server misconfigurations. It uses adaptive techniques to minimize false positives and handles modern web applications with AJAX and complex state management.\n\nOutput is an interactive HTML report with a sitemap showing discovered paths, parameters, and identified security issues. Each finding includes severity rating, description, and evidence. The report serves as a foundation for manual security assessment."],["CAVEATS","Skipfish is resource-intensive on both the scanner and target server. Always obtain explicit authorization before scanning. The aggressive crawling can trigger DoS protections or generate large amounts of log data. Some dynamic applications may not be fully covered. Excluded patterns (**-X**) should include logout URLs to prevent session termination."],["HISTORY","Skipfish was developed by **Michal Zalewski** (lcamtuf) at **Google** and released in **2010**. Zalewski is renowned for security research including the AFL fuzzer. Skipfish was designed for speed and accuracy, using optimized HTTP handling and intelligent crawling algorithms. While development has slowed, it remains a useful tool for web application reconnaissance and automated security testing."],["INSTALL","```aur: yay -S skipfish```"],["SEE ALSO","[nikto](/man/nikto)(1), [wpscan](/man/wpscan)(1), [sqlmap](/man/sqlmap)(1), [burpsuite](/man/burpsuite)(1)"]],"e":["nikto","wpscan","sqlmap","burpsuite"],"x":"skipfish -o [output_dir] [https://example.com]"},"swayidle":{"n":"swayidle","t":"Idle management daemon for Sway","s":[["TAGLINE","Idle management daemon for Sway"],["TLDR","**Lock screen on idle**\n\n```swayidle timeout [300] 'swaylock -f'```\n\n**Turn off display**\n\n```swayidle timeout [600] 'swaymsg \"output * dpms off\"' resume 'swaymsg \"output * dpms on\"'```\n\n**Lock before sleep**\n\n```swayidle before-sleep 'swaylock -f'```\n\n**Multiple timeouts**\n\n```swayidle timeout [300] 'swaylock -f' timeout [600] 'systemctl suspend'```\n\n**With lock event**\n\n```swayidle lock 'swaylock -f'```"],["SYNOPSIS","**swayidle** [_options_] [_events_]"],["PARAMETERS","**timeout** _SECONDS_ _COMMAND_\n> Run after idle time.\n\n**resume** _COMMAND_\n> Run on activity.\n\n**before-sleep** _COMMAND_\n> Run before suspend.\n\n**after-resume** _COMMAND_\n> Run after wake.\n\n**lock** _COMMAND_\n> Run on lock event.\n\n**unlock** _COMMAND_\n> Run on unlock.\n\n**-w**\n> Wait for command to finish before continuing\n\n**-d**\n> Enable debug output\n\n**-C** _path_\n> Path to config file (default: $XDG_CONFIG_HOME/swayidle/config)\n\n**-S** _seat_\n> Specify seat name to use"],["DESCRIPTION","**swayidle** is an idle management daemon compatible with any Wayland compositor implementing the ext-idle-notify protocol. It triggers commands based on user inactivity, supporting multiple timeout events that execute commands after specified periods of idle time, enabling staged power management such as dimming the screen, locking, and eventually suspending.\n\nResume actions execute when user activity is detected after a timeout, allowing reversal of timeout effects like turning displays back on. The before-sleep and after-resume events integrate with systemd sleep/wake cycles, commonly used to ensure the screen is locked before the system suspends.\n\nLock and unlock events respond to session lock signals from logind, enabling coordination with external lock requests such as those from the system settings or other applications."],["CAVEATS","Requires a Wayland compositor with ext-idle-notify support. Commands run as shell. Audio playback may prevent idle detection on some compositors."],["HISTORY","**swayidle** is the idle manager for **Sway** compositor. It handles power management and screen locking triggers."],["INSTALL","```apt: sudo apt install swayidle```\n\n```dnf: sudo dnf install swayidle```\n\n```pacman: sudo pacman -S swayidle```\n\n```apk: sudo apk add swayidle```\n\n```zypper: sudo zypper install swayidle```\n\n```nix: nix profile install nixpkgs#swayidle```"],["SEE ALSO","[sway](/man/sway)(1), [swaylock](/man/swaylock)(1), [swaymsg](/man/swaymsg)(1)"]],"e":["sway","swaylock","swaymsg"],"x":"swayidle timeout [300] 'swaylock -f'"},"systemd-confext":{"n":"systemd-confext","t":"Configuration extension for /etc","s":[["TAGLINE","Configuration extension for /etc"],["TLDR","This command is an alias of [systemd-sysext](/man/systemd-sysext).\n\nUnlike systemd-sysext which extends `/usr` and `/opt`, confext extends only `/etc` with configuration extensions."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemd-sysext](/man/systemd-sysext)(8)"]],"e":["systemd-sysext"],"x":""},"tiup":{"n":"tiup","t":"TiDB component and cluster manager","s":[["TAGLINE","TiDB component and cluster manager"],["TLDR","**Start local TiDB playground**\n\n```tiup playground```\n\n**Start with specific version**\n\n```tiup playground v[7.1.0]```\n\n**Start with multiple components**\n\n```tiup playground --db [3] --pd [3] --kv [3]```\n\n**Deploy a cluster**\n\n```tiup cluster deploy [cluster-name] v[7.1.0] [topology.yaml]```\n\n**Start a cluster**\n\n```tiup cluster start [cluster-name]```\n\n**Show cluster status**\n\n```tiup cluster display [cluster-name]```\n\n**Connect to TiDB** (MySQL client)\n\n```mysql -h [127.0.0.1] -P [4000] -u root```\n\n**Install a component**\n\n```tiup install tidb:v[7.1.0]```"],["SYNOPSIS","**tiup** _component_ [_args_...]\n\n**tiup** [_command_] [_options_]"],["COMMANDS","**playground**\n> Start local test cluster.\n\n**cluster** deploy|start|stop|destroy|display|scale-out|scale-in\n> Manage production clusters.\n\n**install** _component_\n> Install a TiUP component.\n\n**update** _component_\n> Update a component.\n\n**list**\n> List available components.\n\n**status**\n> Show running components.\n\n**clean**\n> Clean component data.\n\n**uninstall** _component_\n> Remove a component.\n\n**--version**\n> Show TiUP version."],["PLAYGROUND OPTIONS","**--db** _count_\n> Number of TiDB instances.\n\n**--pd** _count_\n> Number of PD instances.\n\n**--kv** _count_\n> Number of TiKV instances.\n\n**--tiflash** _count_\n> Number of TiFlash instances.\n\n**--host** _address_\n> Bind address.\n\n**--db.port** _port_\n> TiDB port. Default: 4000.\n\n**--pd.port** _port_\n> PD port. Default: 2379."],["CLUSTER OPTIONS","**deploy** _name_ _version_ _topology_\n> Deploy cluster from topology file.\n\n**start** _name_\n> Start cluster.\n\n**stop** _name_\n> Stop cluster.\n\n**destroy** _name_\n> Remove cluster.\n\n**scale-out** _name_ _topology_\n> Add nodes.\n\n**scale-in** _name_ --node _host:port_\n> Remove nodes.\n\n**upgrade** _name_ _version_\n> Upgrade cluster."],["DESCRIPTION","**tiup** is the component manager for TiDB, a MySQL-compatible distributed database. It manages installation, deployment, and operations of TiDB clusters.\n\n**tiup playground** starts a local development cluster with TiDB, PD (placement driver), and TiKV (storage) components. Connect using any MySQL client on port 4000.\n\nProduction deployments use **tiup cluster** with YAML topology files. The topology defines hosts, ports, directories, and component placement. **deploy** creates the cluster; **start** runs it.\n\nTiDB is MySQL-compatible; use standard MySQL clients and drivers. TiFlash provides columnar storage for analytics workloads.\n\nTiUP components include monitoring (Prometheus, Grafana), diagnostic tools, and utilities. **tiup list** shows available components."],["CAVEATS","TiDB is mostly MySQL-compatible but some features differ or are unsupported. Minimum 3-node PD for production high availability. TiKV requires SSD storage. Resource requirements scale with data size."],["HISTORY","TiDB was created by **PingCAP** and first released in **2015**. Inspired by Google Spanner and F1, it combines MySQL compatibility with horizontal scalability. TiKV, the storage layer, joined CNCF and graduated in **2020**. TiDB is widely used for HTAP (hybrid transactional/analytical) workloads, especially in Asia. Major versions release annually with LTS support."],["SEE ALSO","[mysql](/man/mysql)(1), [cockroach](/man/cockroach)(1)"]],"e":["mysql","cockroach"],"x":"tiup playground"},"tome":{"n":"tome","t":"Roguelike dungeon crawler in Middle Earth","s":[["TAGLINE","Roguelike dungeon crawler in Middle Earth"],["TLDR","**Start the game**\n\n```tome```\n\n**Start with specific character file**\n\n```tome -u [savefile]```\n\n**Start in graphics mode**\n\n```tome -g```\n\n**Start in text/ASCII mode**\n\n```tome -m```\n\n**Start with specific display size**\n\n```tome -- -geometry [80x50]```"],["SYNOPSIS","**tome** [_options_]"],["PARAMETERS","**-u** _file_\n> Use specific save file.\n\n**-g**\n> Force graphics mode.\n\n**-m**\n> Force monochrome/ASCII mode.\n\n**-n**\n> Start a new character.\n\n**-r**\n> Restore from save file.\n\n**-s**\n> Show high scores.\n\n**-v**\n> Display version."],["CONTROLS","**?**: Display help and key bindings.\n\n**=**: Set game options.\n\n**Movement**: Arrow keys or vi keys (hjklyubn).\n\n**>**: Go down stairs.\n\n**<**: Go up stairs.\n\n**i**: View inventory.\n\n**d**: Drop item.\n\n**e**: Eat food.\n\n**q**: Quaff potion.\n\n**r**: Read scroll.\n\n**w**: Wear/wield equipment.\n\n**Ctrl+S**: Save and exit."],["DESCRIPTION","**tome** (Tales of Middle Earth) is a roguelike dungeon crawling game set in Tolkien's Middle Earth. Players explore procedurally generated dungeons, fight monsters, collect items, and develop their character through experience and equipment.\n\nThe game features permadeath, meaning character death is permanent and save files are deleted upon death. It includes extensive character customization with multiple races, classes, and skills. The world includes themed locations from the Middle Earth setting.\n\nToME descended from Angband and introduced many innovations including overworld travel, multiple dungeons, and expanded skill systems. The interface combines traditional roguelike keyboard commands with optional graphical tiles."],["CAVEATS","Permadeath means lost progress on death. Games can last many hours. Some versions require specific terminal sizes. The learning curve is steep for new roguelike players. Save files are deleted upon character death."],["HISTORY","**ToME** began as PernAngband in 1994, evolving through several name changes before becoming Tales of Middle Earth. It forked from Angband to add story elements and a connected world. The name later changed to Tales of Maj'Eyal (ToME 4) after 2010, moving to an original fantasy setting while the Middle Earth version continued separately. ToME won the ASCII Dreams Roguelike of the Year award multiple times."],["SEE ALSO","[angband](/man/angband)(6), [nethack](/man/nethack)(6), [crawl](/man/crawl)(6), [rogue](/man/rogue)(6), [moria](/man/moria)(6)"]],"e":["angband","nethack","crawl","rogue","moria"],"x":"tome"},"vagrant-validate":{"n":"vagrant-validate","t":"Validate Vagrantfile syntax","s":[["TAGLINE","Validate Vagrantfile syntax"],["TLDR","**Validate the Vagrantfile in the current directory**\n\n```vagrant validate```\n\n**Validate while ignoring provider-specific configuration**\n\n```vagrant validate -p```"],["SYNOPSIS","**vagrant** **validate** [_options_]"],["PARAMETERS","**-p**, **--ignore-provider**\n> Ignore provider-specific configuration options during validation."],["DESCRIPTION","**vagrant validate** checks the Vagrantfile for syntax errors. It validates the configuration without creating or modifying machines, making it useful for CI/CD pipelines and pre-commit checks. If valid, it prints \"Vagrantfile validated successfully.\""],["INSTALL","```apt: sudo apt install vagrant```\n\n```dnf: sudo dnf install vagrant```\n\n```nix: nix profile install nixpkgs#vagrant```"],["SEE ALSO","[vagrant](/man/vagrant)(1), [vagrant-init](/man/vagrant-init)(1)"]],"e":["vagrant","vagrant-init"],"x":"vagrant validate"},"winecfg":{"n":"winecfg","t":"Wine graphical configuration tool","s":[["TAGLINE","Wine graphical configuration tool"],["TLDR","**Open Wine configuration**\n\n```winecfg```\n\n**Configure a specific Wine prefix**\n\n```WINEPREFIX=[~/.wine32] winecfg```\n\n**Create and configure a new 32-bit Wine prefix**\n\n```WINEPREFIX=[~/.wine32] WINEARCH=win32 winecfg```\n\n**Set Windows version to Windows 10** via command line\n\n```WINEPREFIX=[~/.wine] wine reg add \"HKCU\\\\Software\\\\Wine\" /v Version /d win10```"],["SYNOPSIS","**winecfg** [_options_]"],["DESCRIPTION","**winecfg** is Wine's graphical configuration tool. It allows adjusting Wine settings without manually editing the registry, including Windows version emulation, DLL overrides, display options, and audio configuration.\n\nThe Applications tab lets you configure different Windows versions per application for compatibility. The Libraries tab is crucial for applications requiring specific DLL versions or overrides.\n\nEach Wine prefix can have independent settings. Use WINEPREFIX to manage multiple isolated Wine environments for different applications."],["ENVIRONMENT","**WINEPREFIX**\n> Directory containing Wine configuration. Defaults to ~/.wine.\n\n**WINEARCH**\n> Architecture for new prefixes: win32 or win64. Only takes effect when creating a new prefix.\n\n**WINEDEBUG**\n> Debug channels for troubleshooting (e.g., +all, -all, +relay)."],["TABS","**Applications**: Per-application settings and Windows version.\n\n**Libraries**: DLL override settings (native vs builtin).\n\n**Graphics**: Display settings, virtual desktop, screen resolution.\n\n**Desktop Integration**: Theme and folder integration.\n\n**Drives**: Configure virtual drive mappings.\n\n**Audio**: Sound driver selection and configuration.\n\n**Staging**: Wine Staging experimental features (if available)."],["CAVEATS","Changes affect the entire Wine prefix unless application-specific rules are set. DLL overrides can break applications if misconfigured. Some settings require Wine restart to take effect. WINEARCH only has effect when creating a new prefix for the first time; it cannot change an existing prefix's architecture."],["HISTORY","**winecfg** was created as part of Wine to provide a user-friendly configuration interface. Wine (Wine Is Not an Emulator) has been in development since 1993, providing a compatibility layer for running Windows applications on Unix-like systems."],["INSTALL","```dnf: sudo dnf install wine```\n\n```pacman: sudo pacman -S wine```\n\n```apk: sudo apk add wine```\n\n```zypper: sudo zypper install wine```\n\n```nix: nix profile install nixpkgs#wine```"],["SEE ALSO","[wine](/man/wine)(1), [regedit](/man/regedit)(1), [winetricks](/man/winetricks)(1)"]],"e":["wine","regedit","winetricks"],"x":"winecfg"},"wtype":{"n":"wtype","t":"Wayland keyboard input simulator","s":[["TAGLINE","Wayland keyboard input simulator"],["TLDR","**Type** text\n\n```wtype \"[Hello World]\"```\n\nType specific **key**\n\n```wtype -k [Left]```\n\n**Press and release** a named key\n\n```wtype -P [Left] -p [Left]```\n\n**Press** modifier\n\n```wtype -M [shift|ctrl]```\n\n**Release** modifier\n\n```wtype -m [ctrl]```\n\n**Delay** between keystrokes\n\n```wtype -d [500] \"[text]\"```\n\nRead from **stdin**\n\n```echo \"[text]\" | wtype -```"],["SYNOPSIS","**wtype** [_OPTIONS_] [_TEXT_]"],["PARAMETERS","**-k** _KEY_\n> Type specific key by name\n\n**-M** _MOD_\n> Press modifier key\n\n**-m** _MOD_\n> Release modifier key\n\n**-P** _KEY_\n> Press a named key\n\n**-p** _KEY_\n> Release a named key\n\n**-d** _MS_\n> Delay between keystrokes in milliseconds\n\n**-s** _MS_\n> Sleep before next option (for complex sequences)\n\n**-**\n> Read text from stdin"],["DESCRIPTION","**wtype** simulates keyboard input on Wayland compositors, similar to xdotool type for X11. It can type text, press specific keys, and manage modifier states.\n\nThe tool is useful for automation, testing, and scripting on Wayland systems."],["CAVEATS","Wayland only. Requires appropriate permissions for input simulation. Some applications may not receive simulated input due to security restrictions."],["INSTALL","```apt: sudo apt install wtype```\n\n```dnf: sudo dnf install wtype```\n\n```pacman: sudo pacman -S wtype```\n\n```apk: sudo apk add wtype```\n\n```zypper: sudo zypper install wtype```\n\n```brew: brew install wtype```\n\n```nix: nix profile install nixpkgs#wtype```"],["SEE ALSO","[ydotool](/man/ydotool)(1), [xdotool](/man/xdotool)(1)"]],"e":["ydotool","xdotool"],"x":"wtype \"[Hello World]\""},"xidel":{"n":"xidel","t":"HTML/XML/JSON data extraction tool","s":[["TAGLINE","HTML/XML/JSON data extraction tool"],["TLDR","**Extract with XPath**\n\n```xidel [file.html] -e \"//title\"```\n\n**Extract from URL**\n\n```xidel [https://example.com] -e \"//h1\"```\n\n**CSS selector**\n\n```xidel [file.html] --css \"div.content\"```\n\n**Extract JSON**\n\n```xidel [file.json] -e \"$json/key\"```\n\n**Multiple extractions**\n\n```xidel [file.html] -e \"//title\" -e \"//h1\"```\n\n**Output JSON**\n\n```xidel [file.html] -e \"//a/@href\" --output-format=json```\n\n**Follow links**\n\n```xidel [url] -f \"//a/@href\" -e \"//title\"```"],["SYNOPSIS","**xidel** [_-e expression_] [_--css selector_] [_options_] _input_"],["PARAMETERS","**-e**, **--extract** _EXPR_\n> XPath/XQuery expression.\n\n**--css** _SELECTOR_\n> CSS selector.\n\n**-f**, **--follow** _EXPR_\n> Follow links.\n\n**--output-format** _FORMAT_\n> Output format.\n\n**--input-format** _FORMAT_\n> Input format.\n\n**-s**, **--silent**\n> Suppress status.\n\n**--user-agent** _UA_\n> User agent."],["DESCRIPTION","**xidel** is a command-line tool for extracting and querying data from HTML, XML, and JSON documents. It supports multiple query languages including XPath, XQuery, and CSS selectors, making it versatile for a wide range of data extraction tasks from both local files and remote URLs.\n\nXPath and XQuery expressions allow precise navigation of document structure, while CSS selectors provide a familiar syntax for those accustomed to web development. For JSON documents, xidel uses a path-based syntax to navigate object hierarchies. Multiple extraction expressions can be combined in a single invocation for complex data gathering.\n\nThe tool includes a link-following mode that enables web spidering, where xidel can traverse links on pages and apply extraction expressions to each visited page. Output can be formatted as plain text, JSON, or other structured formats, making it suitable for integration into data processing pipelines."],["CAVEATS","Complex syntax learning curve. Large documents may be slow. Encoding issues possible."],["HISTORY","**xidel** was created as a powerful command-line data extraction tool. It combines multiple query languages in one utility."],["INSTALL","```brew: brew install xidel```\n\n```nix: nix profile install nixpkgs#xidel```"],["SEE ALSO","[xmllint](/man/xmllint)(1), [jq](/man/jq)(1), [pup](/man/pup)(1)"]],"e":["xmllint","jq","pup"],"x":"xidel [file.html] -e \"//title\""},"xsel":{"n":"xsel","t":"Manipulate X11 clipboard selections","s":[["TAGLINE","Manipulate X11 clipboard selections"],["TLDR","**Copy** to clipboard\n\n```echo 123 | xsel -ib```\n\nCopy **file** to clipboard\n\n```cat [path/to/file] | xsel -ib```\n\n**Output** clipboard\n\n```xsel -ob```\n\nOutput to **file**\n\n```xsel -ob > [path/to/file]```\n\n**Clear** clipboard\n\n```xsel -cb```\n\nOutput **primary** selection\n\n```xsel -op```"],["SYNOPSIS","**xsel** [_OPTIONS_]"],["PARAMETERS","**-i, --input**\n> Read from stdin\n\n**-o, --output**\n> Write to stdout\n\n**-c, --clear**\n> Clear selection\n\n**-b, --clipboard**\n> Use clipboard selection (Ctrl+C/V)\n\n**-p, --primary**\n> Use primary selection (middle click)\n\n**-s, --secondary**\n> Use secondary selection\n\n**-a, --append**\n> Append to selection"],["DESCRIPTION","**xsel** manipulates X11 clipboard and selection buffers. It can read, write, clear, and exchange content between primary selection, secondary selection, and clipboard.\n\nSimilar to xclip but with different syntax and additional features."],["CAVEATS","X11 only. Content may be lost when X server restarts. Use wl-copy/wl-paste for Wayland."],["INSTALL","```apt: sudo apt install xsel```\n\n```dnf: sudo dnf install xsel```\n\n```pacman: sudo pacman -S xsel```\n\n```apk: sudo apk add xsel```\n\n```zypper: sudo zypper install xsel```\n\n```brew: brew install xsel```\n\n```nix: nix profile install nixpkgs#xsel```"],["SEE ALSO","[xclip](/man/xclip)(1), [wl-copy](/man/wl-copy)(1), [wl-paste](/man/wl-paste)(1)"]],"e":["xclip","wl-copy","wl-paste"],"x":"echo 123 | xsel -ib"},"zmv":{"n":"zmv","t":"Batch rename files using zsh patterns","s":[["TAGLINE","Batch rename files using zsh patterns"],["TLDR","**Rename files lowercase to uppercase**\n\n```zmv '(*)' '${(U)1}'```\n\n**Add prefix to all files**\n\n```zmv '(*)' 'prefix_$1'```\n\n**Change file extension**\n\n```zmv '(*).txt' '$1.md'```\n\n**Rename with sequential numbers**\n\n```zmv -n '*.jpg' 'photo_$((++n)).jpg'```\n\n**Dry run (show what would happen)**\n\n```zmv -n '(*)' 'new_$1'```\n\n**Move files to subdirectory**\n\n```zmv '(*.txt)' 'subdir/$1'```"],["SYNOPSIS","**zmv** [**-finqQsvwW**] [**-C**|**-L**|**-M**|**-p** _program_] [**--**] _srcpat_ _dest_"],["PARAMETERS","**-n**\n> Dry run, show what would be done\n\n**-f**\n> Force overwrite without prompting\n\n**-i**\n> Interactive, prompt before overwriting\n\n**-q**\n> Quiet, don't report errors\n\n**-Q**\n> Force bare glob qualifiers on pattern\n\n**-s**\n> Symbolic link instead of move\n\n**-v**\n> Verbose, print each command\n\n**-w**\n> Pick out wildcard parts of pattern\n\n**-W**\n> Same as -w but with pattern replacement\n\n**-C**\n> Copy instead of move\n\n**-L**\n> Hard link instead of move\n\n**-M**\n> Move (default)\n\n**-p** _program_\n> Use specified program instead of mv"],["DESCRIPTION","**zmv** is a Zsh builtin for batch renaming files using pattern matching and replacement. It's more powerful than basic mv for complex renaming operations.\n\nThe source pattern uses Zsh extended globbing. Parenthesized groups are captured and available as **$1**, **$2**, etc. in the destination pattern. Zsh parameter expansion modifiers can transform captured text.\n\nCommon modifiers: **${(U)1}** uppercase, **${(L)1}** lowercase, **${(C)1}** capitalize, **${1:t}** tail (basename), **${1:h}** head (directory).\n\nzmv must be loaded first: **autoload -U zmv**"],["CAVEATS","zmv is Zsh-specific. It won't work in bash or other shells.\n\nAlways use **-n** (dry run) first to verify the operation before executing.\n\nPatterns use Zsh globbing, which differs from regular expressions. Enable extended_glob (**setopt extended_glob**) for full pattern support."],["SEE ALSO","[mv](/man/mv)(1), [rename](/man/rename)(1), [mmv](/man/mmv)(1), [zsh](/man/zsh)(1)"]],"e":["mv","rename","mmv","zsh"],"x":"zmv '(*)' '${(U)1}'"}});
