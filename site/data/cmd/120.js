window.LCL_BUCKET(120,{"anbox":{"n":"anbox","t":"Run Android applications on Linux","s":[["TAGLINE","Run Android applications on Linux"],["TLDR","Launch Anbox into the **app manager**\n\n```anbox launch --package=org.anbox.appmgr --component=org.anbox.appmgr.AppViewActivity```"],["SYNOPSIS","**anbox** [_command_] [_options_]"],["DESCRIPTION","**anbox** runs Android applications on any Linux operating system. It puts the Android operating system into a container, abstracts hardware access, and integrates core system services into a Linux environment."],["PARAMETERS","**launch**\n> Launch an Android application\n\n**session-manager**\n> Start the session manager that runs the Android container and graphics stack\n\n**container-manager**\n> Start the privileged container manager (usually run as a system service)\n\n**system-info**\n> Print information about the host system and Anbox configuration\n\n**--package**\n> Specify the Android package name to launch\n\n**--component**\n> Specify the component/activity to launch"],["CAVEATS","Anbox requires kernel modules (ashmem, binder) and a properly configured container environment. Not all Android applications are compatible due to the lack of Google Play Services. The upstream project was archived in February 2024 and is no longer maintained; Waydroid is the recommended successor for desktop Linux."],["HISTORY","Anbox (Android in a Box) is an open-source compatibility layer that allows Android apps to run on Linux. Development began around **2017** as an alternative to Android emulators. The project was archived as read-only on **February 13, 2024**."],["SEE ALSO","[waydroid](/man/waydroid)(1), [lxc](/man/lxc)(1)"],["RESOURCES","```[Source code](https://github.com/anbox/anbox)```\n\n```[Homepage](https://anbox.io)```"]],"e":["waydroid","lxc"],"x":"anbox launch --package=org.anbox.appmgr --component=org.anbox.appmgr.AppViewActivity"},"bcal":{"n":"bcal","t":"REPL CLI calculator for storage expressions and byte conversions","s":[["TAGLINE","REPL CLI calculator for storage expressions and byte conversions"],["TLDR","**Evaluate an arithmetic expression** with storage units\n\n```bcal \"(5kb+2mb)/3\"```\n\n**Convert a value** to all storage units\n\n```bcal 0xaabbcc kb```\n\n**Show binary, decimal and hex** representations of a number\n\n```bcal -c 20140115```\n\n**Perform bitwise operations**\n\n```bcal -b '0xFF & 0x0F'```\n\n**Use as a general-purpose calculator**\n\n```bcal -b '3.5 * 2.1 + 5.7'```\n\n**Convert LBA to CHS**\n\n```bcal -f l500```\n\n**Convert CHS to LBA**\n\n```bcal -f c10-10-10```\n\n**Start interactive REPL mode**\n\n```bcal```"],["SYNOPSIS","**bcal** [**-b** [_expr_]] [**-c** _N_] [**-p** _N_] [**-f** _loc_] [**-s** _bytes_] [_expr_] [_N_ [_unit_]] [**-m**] [**-d**] [**-h**]"],["PARAMETERS","**-b** [_expr_]\n> Start general-purpose REPL mode. If an expression is provided, evaluate it and quit.\n\n**-c** _N_\n> Show decimal, binary and hex representation of positive integer N.\n\n**-p** _N_\n> Show bit positions and bit values of a number.\n\n**-f** _loc_\n> Convert CHS to LBA or LBA to CHS. The location is a hyphen-separated representation.\n\n**-s** _bytes_\n> Sector size in bytes. Default value is 512.\n\n**-m**\n> Show minimal output (e.g. decimal bytes only).\n\n**-d**\n> Enable debug information and logs.\n\n**-h**\n> Show help, storage sizes on the system, and exit."],["DESCRIPTION","**bcal** (Byte CALculator) is a command-line utility for calculations and expressions involving binary prefixes, SI/IEC conversion, byte addressing, base conversion, and LBA/CHS calculation. It also supports general-purpose arithmetic, bitwise operations, and mathematical functions.\n\nStorage expressions support the operators **+**, **-**, **\\***, **/**, **%** with units such as B, KiB, MiB, GiB, TiB, kB, MB, GB, and TB. Units are case-insensitive. Hex values are prefixed with **0x** and binary values with **0b**.\n\nIn general-purpose mode (**-b**), bcal supports bitwise operations: AND (**&**), OR (**|**), XOR (**^**), complement (**~**), left shift (**<<**), and right shift (**>>**). Mathematical functions include **exp**(n), **log**(base, n), **ln**(n), **pow**(n, exponent), and **root**(radical, n).\n\nWhen started without arguments, bcal enters an interactive REPL mode. The last valid result is stored in the variable **r** and can be reused in subsequent expressions. In the REPL, press **b** to toggle between storage and general-purpose modes, **c N** for base conversion, **p N** for bit positions, **s** for storage type sizes, and **q** or double Enter to quit.\n\nbcal works with piped input and file redirection, making it suitable for scripted workflows. Precision is 128-bit when **__uint128_t** is available, otherwise 64-bit. Floating-point operations use **long double** arithmetic."],["CONFIGURATION","**History file**\n> REPL history is stored at **$XDG_CONFIG_HOME/bcal/history**, or **$HOME/.config/bcal/history** if XDG_CONFIG_HOME is unset.\n\n**BCAL_BIT_ANSI_COLOR_CODE**\n> Environment variable to set the ANSI escape code for colorizing bit value 1 in **-p** output. Set to empty to disable coloring."],["CAVEATS","Only 64-bit operating systems are supported. Fractional bytes do not exist and cannot be addressed; bcal shows the floor value of non-integer byte results. Negative values in storage expressions are unsupported.\n\nLBA and CHS format conversion arguments must be hyphen-separated. LBA values are prefixed with **l** and CHS values with **c**. Any unspecified value defaults to 0.\n\nDefault disk geometry values are: sector size 512, max heads per cylinder 16, max sectors per track 63."],["HISTORY","**bcal** was created by **Arun Prakash Jana** and released in **2016** as open-source software under the **GPLv3+** license. It is written in C with minimal dependencies (libc and optionally GNU Readline or BSD Editline)."],["INSTALL","```apt: sudo apt install bcal```\n\n```dnf: sudo dnf install bcal```\n\n```aur: yay -S bcal```\n\n```zypper: sudo zypper install bcal```\n\n```brew: brew install bcal```\n\n```nix: nix profile install nixpkgs#bcal```"],["SEE ALSO","[bc](/man/bc)(1), [dc](/man/dc)(1), [calc](/man/calc)(1), [units](/man/units)(1), [numfmt](/man/numfmt)(1)"]],"e":["bc","dc","calc","units","numfmt"],"x":"bcal \"(5kb+2mb)/3\""},"browsr":{"n":"browsr","t":"File explorer TUI with cloud storage support","s":[["TAGLINE","File explorer TUI with cloud storage support"],["TLDR","**Launch** in the current directory\n\n```browsr```\n\n**Browse** a specific directory\n\n```browsr [path/to/directory]```\n\n**Browse** a GitHub repository\n\n```browsr [github://owner/repo]```\n\n**Browse** an S3 bucket\n\n```browsr [s3://bucket-name]```"],["SYNOPSIS","**browsr** [_options_] [_path_]"],["PARAMETERS","**--theme** _THEME_\n> Set the color theme.\n\n**--debug**\n> Enable debug mode."],["DESCRIPTION","**browsr** is a terminal-based file explorer built with **Textual** (Python). It provides a rich TUI for browsing files and directories with syntax-highlighted file previews. Beyond local filesystems, it supports browsing remote sources including **GitHub repositories**, **AWS S3** buckets, and other cloud storage backends.\n\nThe interface features a directory tree panel and a file preview panel with syntax highlighting for source code files. It supports keyboard and mouse navigation."],["CAVEATS","Cloud storage access requires appropriate credentials and authentication to be configured (e.g., AWS credentials for S3, GitHub token for private repos). Performance depends on network speed for remote sources. Large files may be slow to preview."],["HISTORY","**browsr** was created by **Juftin** as a Textual-based file explorer with cloud storage integration. It was designed to bring rich file browsing with syntax highlighting to the terminal."],["INSTALL","```nix: nix profile install nixpkgs#browsr```"],["SEE ALSO","[ranger](/man/ranger)(1), [nnn](/man/nnn)(1), [lf](/man/lf)(1), [mc](/man/mc)(1)"]],"e":["ranger","nnn","lf","mc"],"x":"browsr"},"cewl":{"n":"cewl","t":"custom wordlist generator from website content","s":[["TAGLINE","custom wordlist generator from website content"],["TLDR","Create a **wordlist** from URL with 2 links depth\n\n```cewl -d 2 -w [path/to/wordlist.txt] [url]```\n\nOutput **alphanumeric** wordlist with minimum 5 characters\n\n```cewl --with-numbers -m 5 [url]```\n\nOutput wordlist with **verbose** output and **email** addresses\n\n```cewl -v -e [url]```\n\nUse HTTP **authentication**\n\n```cewl --auth_type [basic|digest] --auth_user [username] --auth_pass [password] [url]```\n\nOutput wordlist with **word count**\n\n```cewl -c [url]```\n\nUse a **proxy**\n\n```cewl --proxy_host [host] --proxy_port [port] [url]```"],["SYNOPSIS","**cewl** [_options_] _url_"],["DESCRIPTION","**cewl** (Custom Word List generator) spiders a website and creates a wordlist from the content found. This wordlist can be used for password cracking, especially when targeting organizations where passwords may be based on company-specific terms.\n\nThe tool follows links to a configurable depth and extracts unique words meeting length requirements."],["PARAMETERS","**-d**, **--depth** _n_\n> Spider to link depth n (default: 2)\n\n**-w**, **--write** _file_\n> Write wordlist to file\n\n**-m**, **--min_word_length** _n_\n> Minimum word length (default: 3)\n\n**--with-numbers**\n> Include words with numbers\n\n**-c**, **--count**\n> Show the count for each word found\n\n**-e**, **--email**\n> Include email addresses found in the page\n\n**-a**, **--meta**\n> Include metadata from the page\n\n**-u**, **--ua** _AGENT_\n> Set the user agent string\n\n**-v**, **--verbose**\n> Enable verbose output\n\n**--auth_type** _type_\n> Authentication type: basic or digest\n\n**--auth_user** _user_\n> Authentication username\n\n**--auth_pass** _pass_\n> Authentication password\n\n**--proxy_host** _host_\n> Proxy host\n\n**--proxy_port** _port_\n> Proxy port"],["CAVEATS","Only use on websites you have authorization to spider. Aggressive spidering may trigger rate limiting or blocking. Large sites may produce very large wordlists."],["INSTALL","```nix: nix profile install nixpkgs#cewl```"],["SEE ALSO","[john](/man/john)(1), [hashcat](/man/hashcat)(1), [crunch](/man/crunch)(1)"]],"e":["john","hashcat","crunch"],"x":"cewl -d 2 -w [path/to/wordlist.txt] [url]"},"cron":{"n":"cron","t":"time-based job scheduler daemon","s":[["TAGLINE","time-based job scheduler daemon"],["TLDR","**Edit crontab**\n\n```crontab -e```\n\n**List cron jobs**\n\n```crontab -l```\n\n**Remove crontab**\n\n```crontab -r```\n\n**Edit another user's crontab**\n\n```sudo crontab -u [user] -e```\n\n**Install crontab from file**\n\n```crontab [crontab.txt]```"],["SYNOPSIS","**crontab** [_options_] [_file_]\n\n**cron** [_options_]"],["DESCRIPTION","**cron** is the daemon that executes scheduled commands. **crontab** manages per-user cron tables. Together they provide time-based job scheduling for automated system maintenance, backups, log rotation, and recurring tasks.\n\nThe cron daemon starts at boot and runs continuously, waking up every minute to check all crontab files for jobs that need execution. Each user can have their own crontab file, and the system has crontabs for system-wide maintenance. Jobs run in the background without user interaction, making cron essential for Unix/Linux system administration."],["CRONTAB OPTIONS","**-e**\n> Edit crontab\n\n**-l**\n> List crontab\n\n**-r**\n> Remove crontab\n\n**-u** _user_\n> Operate on user's crontab (requires root)\n\n**-i**\n> Prompt before removal"],["CRON DAEMON OPTIONS","**-n**\n> Run in foreground\n\n**-m** _command_\n> Mail command (or \"off\" to disable)\n\n**-s**\n> Log to syslog\n\n**-p**\n> Accept any user crontab"],["CRONTAB SYNTAX","```\n* * * * * command\n│ │ │ │ │\n│ │ │ │ └─ Day of week (0-7, Sun=0 or 7)\n│ │ │ └─── Month (1-12)\n│ │ └───── Day of month (1-31)\n│ └─────── Hour (0-23)\n└───────── Minute (0-59)\n```"],["SPECIAL STRINGS","**@reboot**: Run at startup\n\n**@hourly**: 0 * * * *\n\n**@daily**: 0 0 * * *\n\n**@weekly**: 0 0 * * 0\n\n**@monthly**: 0 0 1 * *\n\n**@yearly**: 0 0 1 1 *"],["CONFIGURATION","**/etc/crontab**\n> System-wide crontab file with additional user field for running jobs as specific users.\n\n**/etc/cron.d/**\n> Directory containing system crontab files in the same format as /etc/crontab.\n\n**/var/spool/cron/**\n> Directory containing per-user crontab files managed via the crontab command."],["CAVEATS","Jobs run with limited environment. Set PATH explicitly in crontab. Output mailed to user unless redirected. Use MAILTO=\"\" to disable mail."],["INSTALL","```apt: sudo apt install cron```\n\n```zypper: sudo zypper install cron```\n\n```nix: nix profile install nixpkgs#cron```"],["SEE ALSO","[crontab](/man/crontab)(5), [at](/man/at)(1)"]],"e":["crontab","at"],"x":"crontab -e"},"dbus-launch":{"n":"dbus-launch","t":"utility to start a D-Bus session bus instance","s":[["TAGLINE","utility to start a D-Bus session bus instance"],["TLDR","**Start a D-Bus session** bus\n\n```dbus-launch```\n\n**Start session and export** environment variables\n\n```eval $(dbus-launch --sh-syntax)```\n\n**Start with auto-launch** option\n\n```dbus-launch --autolaunch=[machine-id]```\n\n**Start a specific program** with D-Bus\n\n```dbus-launch [program] [arguments]```\n\n**Exit when program terminates**\n\n```dbus-launch --exit-with-session [program]```\n\n**Close bus when X session** ends\n\n```dbus-launch --exit-with-x11```"],["SYNOPSIS","**dbus-launch** [_options_] [_program_] [_args_]"],["PARAMETERS","_PROGRAM_\n> Program to run with the D-Bus session.\n\n**--sh-syntax**\n> Output Bourne shell compatible syntax.\n\n**--csh-syntax**\n> Output C shell compatible syntax.\n\n**--auto-syntax**\n> Detect shell type from SHELL variable and use appropriate syntax.\n\n**--binary-syntax**\n> Output bus address and PID in binary format.\n\n**--autolaunch** _MACHINEID_\n> Enable autolaunch with specified machine ID.\n\n**--exit-with-session**\n> Exit when launched program terminates.\n\n**--exit-with-x11**\n> Exit when X11 connection closes.\n\n**--close-stderr**\n> Close stderr before starting the daemon.\n\n**--config-file** _FILE_\n> Use specified configuration file instead of --session.\n\n**--version**\n> Print the version of dbus-launch."],["DESCRIPTION","**dbus-launch** starts a session instance of the D-Bus message bus daemon and provides the connection address to client applications. It's typically used to ensure a D-Bus session is available for desktop applications.\n\nWhen invoked without arguments, it prints environment variables (DBUS_SESSION_BUS_ADDRESS and DBUS_SESSION_BUS_PID) that clients need to connect. With shell syntax options, the output can be directly evaluated to set these variables.\n\nWhen a program is specified, dbus-launch starts the D-Bus daemon, sets up the environment, and then executes the program. The --exit-with-session flag ensures the daemon terminates when the program exits."],["CAVEATS","Multiple invocations without proper session management can start multiple daemons. The daemon must be properly terminated to avoid resource leaks. X11 integration requires an active display connection."],["HISTORY","dbus-launch is part of the **D-Bus** reference implementation, created by **Red Hat** and the freedesktop.org project around **2003**. It became essential infrastructure for Linux desktop environments requiring inter-process communication."],["INSTALL","```apt: sudo apt install dbus-x11```\n\n```dnf: sudo dnf install dbus-x11```\n\n```apk: sudo apk add dbus-x11```"],["SEE ALSO","[dbus-daemon](/man/dbus-daemon)(1), [dbus-send](/man/dbus-send)(1), [dbus-monitor](/man/dbus-monitor)(1), [dbus-cleanup-sockets](/man/dbus-cleanup-sockets)(1)"]],"e":["dbus-daemon","dbus-send","dbus-monitor","dbus-cleanup-sockets"],"x":"dbus-launch"},"deluser":{"n":"deluser","t":"remove users from the system","s":[["TAGLINE","remove users from the system"],["TLDR","**Remove** a user\n\n```sudo deluser [username]```\n\nRemove user and **home directory**\n\n```sudo deluser --remove-home [username]```\n\nRemove user, **backup files**, and remove home\n\n```sudo deluser --backup-to [path/to/backup_directory] --remove-home [username]```\n\nRemove user and **all owned files**\n\n```sudo deluser --remove-all-files [username]```\n\nRemove user **from a group**\n\n```sudo deluser [username] [group]```"],["SYNOPSIS","**deluser** [_options_] _user_ [_group_]"],["DESCRIPTION","**deluser** removes users from the system. It can optionally remove the user's home directory, mail spool, and other files. It's the Debian/Ubuntu counterpart to userdel.\n\nWhen given both a user and group name, it removes the user from that group without deleting the user account."],["PARAMETERS","**--remove-home**\n> Remove user's home directory\n\n**--remove-all-files**\n> Remove all files owned by user\n\n**--backup**\n> Backup files before removal\n\n**--backup-to** _dir_\n> Backup destination directory\n\n**--system**\n> Only delete if system user"],["CONFIGURATION","**/etc/deluser.conf**\n> Controls default behavior including removal of home directories and backup settings."],["CAVEATS","Requires root privileges. Use --backup when removing important users. The user must not be logged in. On non-Debian systems, use userdel instead."],["SEE ALSO","[userdel](/man/userdel)(8), [adduser](/man/adduser)(8), [delgroup](/man/delgroup)(8)"],["RESOURCES","```[Source code](https://salsa.debian.org/debian/adduser)```\n\n```[Documentation](https://manpages.debian.org/deluser.8)```"]],"e":["userdel","adduser","delgroup"],"x":"sudo deluser [username]"},"dnf-versionlock":{"n":"dnf-versionlock","t":"Lock package versions to prevent updates","s":[["TAGLINE","Lock package versions to prevent updates"],["TLDR","**List** versionlock entries\n\n```dnf versionlock list```\n\n**Add** a versionlock for a package at its current version\n\n```dnf versionlock add [package]```\n\n**Add** a versionlock without resolving to a specific version\n\n```dnf versionlock add --raw [package-name-spec]```\n\n**Exclude** a specific package version from being installed\n\n```dnf versionlock exclude [package]```\n\n**Delete** a specific versionlock entry\n\n```dnf versionlock delete [package]```\n\n**Clear** all versionlock entries\n\n```dnf versionlock clear```"],["SYNOPSIS","**dnf versionlock** [_options_] [_add_|_exclude_|_list_|_delete_|_clear_] [_package-name-spec_]"],["DESCRIPTION","**dnf versionlock** protects packages from being updated to newer versions. It is part of the dnf-plugins-core package.\n\nThe plugin walks each line of the versionlock file, parses the name and version, and excludes any package by that name that does not match a listed version. It can also work in reverse by prefixing a '!' to exclude a specific version.\n\nUseful for keeping specific package versions that are known to work or avoiding problematic updates."],["PARAMETERS","**add** _package-name-spec_\n> Lock package at its currently installed version. Only matching versions will be available for transactions.\n\n**exclude** _package-name-spec_\n> Exclude packages matching the spec from being installed or updated to.\n\n**list**\n> Display the current versionlock entries (default when no subcommand given).\n\n**delete** _package-name-spec_\n> Remove specific versionlock entries matching the spec.\n\n**clear**\n> Remove all versionlock entries.\n\n**--raw**\n> Do not resolve package-name-spec to specific NEVRAs. Use the spec as-is, allowing locks on versions not yet available."],["CONFIGURATION","**/etc/dnf/plugins/versionlock.list**\n> Stores package version lock entries. Each line contains a package specification in NEVRA format. Lines prefixed with '!' act as excludes."],["CAVEATS","Requires the dnf-plugins-core package. Locked packages will not receive security updates unless the lock is removed or updated. Review locks periodically with `dnf versionlock list`. The lock file uses NEVRA format (name-epoch:version-release.arch) for precise version matching."],["SEE ALSO","[dnf](/man/dnf)(8), [yum-versionlock](/man/yum-versionlock)(8), [rpm](/man/rpm)(8), [yum](/man/yum)(8)"]],"e":["dnf","yum-versionlock","rpm","yum"],"x":"dnf versionlock list"},"docker-rm":{"n":"docker-rm","t":"remove stopped containers from the system","s":[["TAGLINE","remove stopped containers from the system"],["TLDR","**Remove a container**\n\n```docker rm [container]```\n\n**Remove multiple containers**\n\n```docker rm [container1] [container2]```\n\n**Force remove running container**\n\n```docker rm -f [container]```\n\n**Remove container and volumes**\n\n```docker rm -v [container]```\n\n**Remove all stopped containers**\n\n```docker rm $(docker ps -aq)```"],["SYNOPSIS","**docker** **rm** [_options_] _container_ [_container..._]"],["PARAMETERS","**-f**, **--force**\n> Force removal of running container.\n\n**-l**, **--link**\n> Remove specified link.\n\n**-v**, **--volumes**\n> Remove anonymous volumes attached to container."],["DESCRIPTION","**docker rm** removes one or more containers. Containers must be stopped unless -f is used. Does not remove images; use docker rmi for that. Container removal frees up disk space and cleans up container metadata from the Docker daemon. Named volumes are preserved unless explicitly removed with -v."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-container-rm](/man/docker-container-rm)(1), [docker-rmi](/man/docker-rmi)(1)"],["RESOURCES","```[Source code](https://github.com/docker/cli)```\n\n```[Documentation](https://docs.docker.com/reference/cli/docker/)```"]],"e":["docker-container-rm","docker-rmi"],"x":"docker rm [container]"},"doctl-databases-sql-mode":{"n":"doctl-databases-sql-mode","t":"manage MySQL SQL mode settings","s":[["TAGLINE","manage MySQL SQL mode settings"],["TLDR","**Get current SQL mode** for a database cluster\n\n```doctl databases sql-mode get [cluster_id]```\n\n**Set SQL mode** for a database cluster\n\n```doctl databases sql-mode set [cluster_id] [ANSI_QUOTES] [STRICT_TRANS_TABLES]```\n\n**Set SQL mode with output format**\n\n```doctl databases sql-mode get [cluster_id] --format [SQLMode]```"],["SYNOPSIS","**doctl** **databases** **sql-mode** _command_ [_options_]"],["SUBCOMMANDS","**get**\n> Get current SQL mode for a MySQL database cluster.\n\n**set**\n> Set SQL mode for a MySQL database cluster. Provide one or more modes as space-separated arguments."],["PARAMETERS","**--format** _columns_\n> Columns for output in a comma-separated list.\n\n**--no-header**\n> Return raw data with no headers."],["DESCRIPTION","**doctl databases sql-mode** manages SQL mode settings for MySQL managed database clusters on DigitalOcean. SQL modes control how MySQL handles query syntax validation, data type conversions, and error conditions.\n\nCommon SQL modes include STRICT_TRANS_TABLES (reject invalid data), ANSI_QUOTES (treat double quotes as identifier quotes), NO_ZERO_DATE (disallow '0000-00-00' dates), and ONLY_FULL_GROUP_BY (require GROUP BY to include all non-aggregated columns).\n\nSetting SQL modes replaces the existing configuration entirely rather than appending to it. To add a mode, first retrieve the current modes with **get**, then include them all in the **set** command."],["SEE ALSO","[doctl-databases](/man/doctl-databases)(1), [doctl](/man/doctl)(1), [mysql](/man/mysql)(1)"]],"e":["doctl-databases","doctl","mysql"],"x":"doctl databases sql-mode get [cluster_id]"},"dvc-config":{"n":"dvc-config","t":"manage DVC configuration settings","s":[["TAGLINE","manage DVC configuration settings"],["TLDR","**Set a configuration value**\n\n```dvc config [core.remote] [myremote]```\n\n**Get a configuration value**\n\n```dvc config [core.remote]```\n\n**Set a global configuration value**\n\n```dvc config --global [core.autostage] true```\n\n**Set a project-level (local) configuration value**\n\n```dvc config --local [core.remote] [myremote]```\n\n**List all configuration entries**\n\n```dvc config --list```\n\n**Unset a configuration value**\n\n```dvc config --unset [core.remote]```"],["SYNOPSIS","**dvc config** [_options_] [_name_] [_value_]"],["PARAMETERS","_NAME_\n> Configuration key.\n\n_VALUE_\n> Configuration value.\n\n**--global**\n> Use global configuration.\n\n**--system**\n> Use system configuration.\n\n**--local**\n> Use project-level configuration (.dvc/config.local, Git-ignored).\n\n**--project**\n> Use project-level configuration (.dvc/config, tracked by Git).\n\n**--list**, **-l**\n> List all configuration entries.\n\n**--unset**\n> Remove a configuration entry.\n\n**-h**, **--help**\n> Display help information."],["CONFIGURATION","**.dvc/config**\n> Repository-level DVC configuration file.\n\n**~/.config/dvc/config**\n> Global user configuration file."],["DESCRIPTION","**dvc config** manages DVC configuration at project, local, global, and system levels. Configuration controls behavior including default remote storage, caching, and automation settings.\n\nConfiguration precedence from highest to lowest: local (Git-ignored), project (Git-tracked), global (user), system. Common configuration sections include `core` (remote, autostage), `cache` (type, dir), and `remote` (url, credentials).\n\nSetting `core.autostage` to true automatically stages .dvc file changes with Git, reducing manual steps. The default remote is set with `core.remote`."],["CAVEATS","Some options need repository context. Invalid keys may be accepted. Requires appropriate permissions for system config."],["HISTORY","dvc config follows conventions from **Git config**, providing familiar configuration management for DVC settings."],["INSTALL","```brew: brew install dvc```\n\n```nix: nix profile install nixpkgs#dvc```"],["SEE ALSO","[dvc](/man/dvc)(1), [dvc-init](/man/dvc-init)(1), [git-config](/man/git-config)(1)"]],"e":["dvc","dvc-init","git-config"],"x":"dvc config [core.remote] [myremote]"},"ebtables":{"n":"ebtables","t":"Ethernet bridge frame filtering firewall","s":[["TAGLINE","Ethernet bridge frame filtering firewall"],["TLDR","**List all rules**\n\n```sudo ebtables -L```\n\n**List rules with line numbers**\n\n```sudo ebtables -L --Ln```\n\n**Block MAC address**\n\n```sudo ebtables -A INPUT -s [00:11:22:33:44:55] -j DROP```\n\n**Allow only specific MAC**\n\n```sudo ebtables -A INPUT -s ! [00:11:22:33:44:55] -j DROP```\n\n**Drop packets by protocol**\n\n```sudo ebtables -A FORWARD -p IPv4 -j DROP```\n\n**Flush all rules**\n\n```sudo ebtables -F```\n\n**Set policy for a chain**\n\n```sudo ebtables -P FORWARD DROP```\n\n**Save rules to file**\n\n```sudo ebtables-save > [rules.txt]```"],["SYNOPSIS","**ebtables** [_options_] [_command_] [_rule_]"],["PARAMETERS","**-L**, **--list**\n> List all rules.\n\n**-A** _chain_\n> Append rule to chain.\n\n**-D** _chain_\n> Delete rule from chain.\n\n**-I** _chain_ [_num_]\n> Insert rule at position.\n\n**-F** [_chain_]\n> Flush rules (all or specific chain).\n\n**-P** _chain_ _target_\n> Set chain policy.\n\n**-p** _protocol_\n> Match protocol (IPv4, IPv6, ARP).\n\n**-s** _mac_\n> Source MAC address.\n\n**-d** _mac_\n> Destination MAC address.\n\n**-i** _interface_\n> Input interface.\n\n**-o** _interface_\n> Output interface.\n\n**-j** _target_\n> Jump to target (ACCEPT, DROP, CONTINUE, RETURN).\n\n**-t** _table_\n> Table to operate on (filter, nat, broute). Default is filter.\n\n**--Ln**\n> Show line numbers when listing rules.\n\n**-N** _chain_\n> Create a new user-defined chain.\n\n**-X** [_chain_]\n> Delete a user-defined chain."],["DESCRIPTION","**ebtables** filters Ethernet frames at the link layer (Layer 2) in Linux bridges. It controls traffic passing through a bridge based on MAC addresses, VLAN tags, and Ethernet protocols, complementing iptables which operates at Layer 3.\n\nEbtables uses chains (INPUT, OUTPUT, FORWARD) and tables (filter, nat, broute) similar to iptables. It's essential for bridge firewall configurations and MAC-based access control."],["CAVEATS","Only applies to bridged traffic. Requires kernel bridge-nf support. Legacy tool; nftables provides unified filtering for modern systems. Must be used in conjunction with iptables for IP-level filtering. Rules don't persist across reboots without save/restore."],["HISTORY","ebtables was developed for the Linux kernel in the early **2000s** to provide Ethernet frame filtering for bridges. It was modeled after iptables to provide familiar syntax. The project is now considered legacy, with nftables recommended for new deployments."],["INSTALL","```pacman: sudo pacman -S iptables```\n\n```apk: sudo apk add iptables```\n\n```zypper: sudo zypper install iptables```\n\n```brew: brew install iptables```\n\n```nix: nix profile install nixpkgs#iptables```"],["SEE ALSO","[iptables](/man/iptables)(8), [nft](/man/nft)(8), [brctl](/man/brctl)(8), [bridge](/man/bridge)(8)"]],"e":["iptables","nft","brctl","bridge"],"x":"sudo ebtables -L"},"for":{"n":"for","t":"shell loop construct for iteration","s":[["TAGLINE","shell loop construct for iteration"],["TLDR","**Iterate** over an explicit list\n\n```for item in [a b c]; do echo \"$item\"; done```\n\n**Loop** over matching files\n\n```for file in *.txt; do cat \"$file\"; done```\n\n**Loop** over a numeric range with brace expansion\n\n```for i in {1..10}; do echo \"$i\"; done```\n\n**Loop** with a C-style counter (bash, ksh, zsh)\n\n```for ((i=0; i<10; i++)); do echo \"$i\"; done```\n\n**Iterate** over the positional parameters\n\n```for arg do echo \"$arg\"; done```\n\n**Iterate** over array elements (bash, ksh, zsh)\n\n```for x in \"${[array][@]}\"; do echo \"$x\"; done```\n\n**Loop** over command output, one word per iteration\n\n```for user in $(cut -d: -f1 [/etc/passwd]); do echo \"$user\"; done```"],["SYNOPSIS","**for** _name_ [**in** _words_]; **do** _commands_; **done**\n\n**for** **((** _init_; _condition_; _update_ **))**; **do** _commands_; **done**"],["PARAMETERS","_name_\n> Variable assigned each item in turn. It stays set to the last value after the loop.\n\n_words_\n> The list to iterate over, after all expansions. If **in** _words_ is omitted, the loop iterates over the positional parameters, as if **in \"$@\"** had been written.\n\n_commands_\n> The body, executed once per item.\n\n**in**\n> Introduces the word list.\n\n**do**\n> Begins the loop body.\n\n**done**\n> Ends the loop."],["DESCRIPTION","**for** iterates over a list of words, assigning each to a variable and running the body once per word. It is a reserved word handled by the shell's parser, not an external command.\n\nThe word list is produced by the shell's normal expansions, which is where its flexibility comes from: brace expansion (**{1..10}**) generates sequences, pathname expansion (***.txt**) generates filenames, command substitution (**$(...)**) turns another program's output into items, and **\"${array[@]}\"** yields array elements. Crucially the list is fully expanded before the first iteration, so it is a fixed set of words rather than a stream.\n\nOmitting **in** entirely is a useful shorthand: **for arg do ... done** loops over **\"$@\"**, the script's or function's arguments, correctly preserving arguments that contain spaces.\n\nThe C-style form **for ((init; condition; update))** is a separate construct that does arithmetic rather than word iteration. It is not POSIX, but is available in bash, ksh and zsh, and is the clearer choice when you need a counter with a computed bound.\n\nThe loop's exit status is that of the last command executed, or **0** if the body never ran. **break** exits the loop and **continue** skips to the next item."],["CAVEATS","An unquoted **$(command)** in the word list is split on whitespace, not on lines, and each resulting word is then glob-expanded. This makes **for line in $(cat file)** wrong for any file whose lines contain spaces or wildcards: use **while IFS= read -r line; do ...; done < file** to read line by line.\n\nAlways quote the variable inside the body (**\"$item\"**), or filenames with spaces will be split again when you use them.\n\nIf a glob matches nothing, the shell leaves the pattern itself in the list by default, so **for f in *.txt** runs once with the literal string ***.txt** in an empty directory. Guard with **[ -e \"$f\" ]**, or set **shopt -s nullglob** in bash.\n\nBecause the list is expanded up front, a **for** loop over a very large glob or command output builds the entire list in memory first. For huge inputs prefer a **while read** loop or **find -exec** / **xargs**.\n\nThe loop variable leaks: it remains set after **done** and is not scoped to the loop."],["HISTORY","**for** dates to the **Bourne shell** (**1977**) and is standardized by POSIX. The arithmetic **for ((...))** form came from the **Korn shell** and was adopted by bash in version **2.04**, offering C programmers a familiar counting loop."],["SEE ALSO","[while](/man/while)(1), [until](/man/until)(1), [do](/man/do)(1), [done](/man/done)(1), [break](/man/break)(1), [continue](/man/continue)(1), [seq](/man/seq)(1), [xargs](/man/xargs)(1), [bash](/man/bash)(1)"],["RESOURCES","```[Documentation](https://www.gnu.org/software/bash/manual/bash.html#Looping-Constructs)```"]],"e":["while","until","do","done","break","continue","seq","xargs","bash"],"x":"for item in [a b c]; do echo \"$item\"; done"},"getsebool":{"n":"getsebool","t":"SELinux boolean state query tool","s":[["TAGLINE","SELinux boolean state query tool"],["TLDR","Show the **current setting** of a boolean\n\n```getsebool httpd_can_connect_ftp```\n\nShow the current setting of **all** booleans\n\n```getsebool -a```\n\nShow all booleans with **explanations**\n\n```sudo semanage boolean -l```"],["SYNOPSIS","**getsebool** [_-a_] [_boolean_]"],["PARAMETERS","**-a**\n> List all SELinux booleans and their current values\n\n**boolean**\n> Name of specific boolean to query"],["DESCRIPTION","**getsebool** retrieves the current value of SELinux booleans. SELinux booleans are on/off switches that modify SELinux policy behavior at runtime without requiring policy recompilation.\n\nBooleans control specific policy features like whether Apache can connect to external networks (httpd_can_network_connect), whether users can run unconfined processes, or whether certain services can access home directories.\n\nOutput shows the boolean name and its current state (on/off). Use **setsebool** to change values."],["COMMON BOOLEANS","**httpd_can_network_connect**\n> Allow Apache to initiate network connections\n\n**httpd_can_connect_ftp**\n> Allow Apache to connect to FTP servers\n\n**allow_user_exec_content**\n> Allow users to execute content in home/tmp\n\n**virt_use_nfs**\n> Allow virtual machines to use NFS\n\n**ssh_chroot_rw_homedirs**\n> Allow SSH to write to chrooted home directories\n\n**samba_enable_home_dirs**\n> Allow Samba to share user home directories"],["CAVEATS","Requires SELinux to be enabled. Boolean names and availability vary by distribution and installed policy modules. Use **semanage boolean -l** for descriptions of what each boolean controls."],["HISTORY","SELinux booleans were introduced to provide flexibility in the policy without requiring policy source modifications. getsebool is part of the libselinux-utils package, providing command-line access to SELinux functionality."],["INSTALL","```apt: sudo apt install selinux-utils```\n\n```dnf: sudo dnf install libselinux-utils```\n\n```apk: sudo apk add libselinux-utils```"],["SEE ALSO","[setsebool](/man/setsebool)(8), [semanage](/man/semanage)(8), [getenforce](/man/getenforce)(1), [sestatus](/man/sestatus)(8)"]],"e":["setsebool","semanage","getenforce","sestatus"],"x":"getsebool httpd_can_connect_ftp"},"gfortran":{"n":"gfortran","t":"GNU Fortran compiler","s":[["TAGLINE","GNU Fortran compiler"],["TLDR","**Compile Fortran file**\n\n```gfortran [source.f90] -o [output]```\n\n**Compile with optimization**\n\n```gfortran -O3 [source.f90] -o [output]```\n\n**Enable warnings**\n\n```gfortran -Wall [source.f90] -o [output]```\n\n**Debug build**\n\n```gfortran -g [source.f90] -o [output]```\n\n**Link math library**\n\n```gfortran [source.f90] -o [output] -lm```"],["SYNOPSIS","**gfortran** [_options_] _files_..."],["PARAMETERS","_FILES_\n> Fortran source files (.f, .f90, .f95).\n\n**-o** _FILE_\n> Output filename.\n\n**-c**\n> Compile only, no linking.\n\n**-g**\n> Generate debug info.\n\n**-O** _LEVEL_\n> Optimization level (0-3).\n\n**-Wall**\n> Enable warnings.\n\n**-l** _LIBRARY_\n> Link library.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gfortran** is the GNU Fortran compiler, part of GCC. It compiles Fortran 77, 90, 95, 2003, and 2008 standards to native executables.\n\nThe compiler provides extensive optimization, debugging support, and interoperability with C code. It benefits from GCC's mature infrastructure.\n\ngfortran is the standard open source Fortran compiler for scientific computing."],["CAVEATS","Standard compliance varies by feature. Array bounds checking optional. Legacy code may need adaptation."],["HISTORY","gfortran replaced g77 as GCC's Fortran compiler starting with GCC 4.0. It supports modern Fortran standards while maintaining compatibility with older code."],["INSTALL","```apt: sudo apt install gfortran```\n\n```apk: sudo apk add gfortran```\n\n```brew: brew install gfortran```\n\n```nix: nix profile install nixpkgs#gfortran```"],["SEE ALSO","[gcc](/man/gcc)(1), [f2py](/man/f2py)(1)"]],"e":["gcc","f2py"],"x":"gfortran [source.f90] -o [output]"},"gifview":{"n":"gifview","t":"Display GIF images and animations in an X11 window","s":[["TAGLINE","Display GIF images and animations in an X11 window"],["TLDR","**View a GIF**\n\n```gifview [image.gif]```\n\n**View with animation**\n\n```gifview -a [image.gif]```\n\n**Specify minimum frame delay** (in centiseconds)\n\n```gifview --min-delay [10] [image.gif]```\n\n**View with unoptimized frames** for faithful display\n\n```gifview -U [image.gif]```\n\n**Disable interactive controls**\n\n```gifview +e [image.gif]```"],["SYNOPSIS","**gifview** [_options_] _files_"],["PARAMETERS","_FILES_\n> GIF files to display.\n\n**-a**, **--animate**\n> Animate multi-image GIFs (default behavior in most builds).\n\n**+a**, **--no-animate**\n> Display frames as a slideshow instead of animating.\n\n**-U**, **--unoptimize**\n> Display multi-image GIFs as unoptimized for faithful frame display.\n\n**--min-delay** _DELAY_\n> Minimum delay between frames in centiseconds (default: 0).\n\n**--fallback-delay** _DELAY_\n> Delay for frames with unspecified delay, in centiseconds.\n\n**+e**, **--no-interactive**\n> Ignore mouse and keyboard input.\n\n**-w** _WINDOW_, **--window** _WINDOW_\n> Display in an existing X window (ID or 'root').\n\n**--new-window** _WINDOW_\n> Display in a new child of an existing X window.\n\n**--geometry** _GEOMETRY_\n> Set window size and position.\n\n**--title** _TITLE_\n> Set the window title.\n\n**--bg** _COLOR_, **--background** _COLOR_\n> Background color for transparent pixels.\n\n**-i**, **--install-colormap**\n> Use a private colormap per window.\n\n**--memory-limit** _LIM_\n> Cache up to LIM megabytes of images (default: 40).\n\n**-d** _DISPLAY_, **--display** _DISPLAY_\n> Use the given X display.\n\n**--name** _NAME_\n> Set application name for resource lookup.\n\n**--version**\n> Print version number and exit.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gifview** displays GIF images and animations in an X11 window. It is part of the gifsicle package and provides simple GIF viewing with animation support and configurable playback speed.\n\nThe viewer can display specific frames for inspection and supports basic window management options. It handles animated GIFs with proper frame timing."],["CAVEATS","Requires X11 display. Part of gifsicle package. Limited to GIF format only."],["HISTORY","gifview is part of **gifsicle**, the GIF manipulation toolkit created by **Eddie Kohler**."],["INSTALL","```apt: sudo apt install gifsicle```\n\n```dnf: sudo dnf install gifsicle```\n\n```pacman: sudo pacman -S gifsicle```\n\n```apk: sudo apk add gifsicle```\n\n```zypper: sudo zypper install gifsicle```\n\n```brew: brew install gifsicle```\n\n```nix: nix profile install nixpkgs#gifsicle```"],["SEE ALSO","[gifsicle](/man/gifsicle)(1), [feh](/man/feh)(1), [display](/man/display)(1)"]],"e":["gifsicle","feh","display"],"x":"gifview [image.gif]"},"git-describe":{"n":"git-describe","t":"Generate human-readable names for commits","s":[["TAGLINE","Generate human-readable names for commits"],["TLDR","**Describe current commit**\n\n```git describe```\n\n**Describe with all tags**\n\n```git describe --tags```\n\n**Describe specific commit**\n\n```git describe [commit]```\n\n**Long format always**\n\n```git describe --long```\n\n**Match tag pattern**\n\n```git describe --match \"v*\"```\n\n**Mark dirty working tree**\n\n```git describe --dirty```"],["SYNOPSIS","**git describe** [_options_] [_commit_]"],["PARAMETERS","_COMMIT_\n> Commit to describe (default: HEAD).\n\n**--tags**\n> Use any tag, not just annotated.\n\n**--long**\n> Always output long format.\n\n**--match** _PATTERN_\n> Only consider matching tags.\n\n**--abbrev** _N_\n> Abbreviation length (default: 7, or as many digits as needed for uniqueness).\n\n**--always**\n> Show commit abbrev if no tag found.\n\n**--dirty**[=_mark_]\n> Append **-dirty** if the working tree has local modifications.\n\n**--contains**\n> Find the tag that comes after (contains) the commit, instead of the closest preceding one. Implies **--tags**.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git describe** gives a human-readable name to a commit based on available tags. It finds the most recent tag reachable from the commit and describes the distance from it.\n\nOutput format is tag-distance-gSHA, like \"v1.0.2-14-g2414721\" meaning 14 commits after v1.0.2. If the commit is directly on a tag, just the tag name is returned. This is commonly used for generating version strings in build systems."],["CAVEATS","Requires tags in history. Annotated tags preferred by default. Distance counting can be slow."],["HISTORY","git describe is a core **Git** command, essential for generating version strings from repository state."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-tag](/man/git-tag)(1), [git-log](/man/git-log)(1), [git-rev-parse](/man/git-rev-parse)(1)"]],"e":["git-tag","git-log","git-rev-parse"],"x":"git describe"},"glib":{"n":"glib","t":"general-purpose C utility library for GTK and GNOME","s":[["TAGLINE","general-purpose C utility library for GTK and GNOME"],["TLDR","**Compile with GLib**\n\n```gcc [program.c] $(pkg-config --cflags --libs glib-2.0) -o [program]```\n\n**Get GLib version**\n\n```pkg-config --modversion glib-2.0```\n\n**Generate enum types**\n\n```glib-mkenums --template [enum.h.template] [header.h] > [enum-types.h]```\n\n**Compile GLib resources**\n\n```glib-compile-resources [resources.gresource.xml] --target=[resources.c]```"],["DESCRIPTION","**GLib** is a general-purpose utility library used by GTK and GNOME. It provides data structures, portability wrappers, event loops, threads, and many utilities for C programming.\n\nGLib is not a command but a library. Related tools include glib-compile-resources, glib-mkenums, and glib-genmarshal."],["KEY FEATURES","- **GList/GSList**: Linked lists\n- **GHashTable**: Hash tables\n- **GString**: Dynamic strings\n- **GMainLoop**: Event loop\n- **GThread**: Threading\n- **GObject**: Object system\n- **GSettings**: Configuration storage"],["RELATED TOOLS","```\nglib-compile-resources  Compile resources\nglib-compile-schemas    Compile GSettings schemas\nglib-mkenums           Generate enum types\nglib-genmarshal        Generate marshallers\ngdbus                  D-Bus tool\ngsettings              GSettings CLI\n```"],["PARAMETERS","**glib-mkenums --template** _file_\n> Text template controlling the generated enum descriptions.\n\n**glib-genmarshal --header** / **--body**\n> Generate the marshaller header or C source respectively.\n\n**glib-compile-resources --target** _file_\n> Output file for the compiled resource bundle (C source or binary `.gresource`).\n\n**glib-compile-resources --generate-source** / **--generate-header**\n> Generate only the C source or only the header, instead of both.\n\n**glib-compile-resources --sourcedir** _dir_\n> Directory to search for referenced resource files."],["CAVEATS","C library, not a command. Requires understanding of C and GLib conventions. Memory management follows GLib patterns. Documentation at docs.gtk.org."],["HISTORY","GLib was originally part of **GTK+**, extracted as a separate library in **1998** to enable non-GUI programs to use its utilities. It's maintained by the GNOME project and used throughout the Linux desktop ecosystem."],["INSTALL","```dnf: sudo dnf install glib```\n\n```brew: brew install glib```\n\n```nix: nix profile install nixpkgs#glib```"],["SEE ALSO","[pkg-config](/man/pkg-config)(1), [gdbus](/man/gdbus)(1), [gsettings](/man/gsettings)(1)"],["RESOURCES","```[Source code](https://gitlab.gnome.org/GNOME/glib)```\n\n```[Documentation](https://docs.gtk.org/glib/)```"]],"e":["pkg-config","gdbus","gsettings"],"x":"gcc [program.c] $(pkg-config --cflags --libs glib-2.0) -o [program]"},"gpg-card":{"n":"gpg-card","t":"OpenPGP and PIV smartcard management","s":[["TAGLINE","OpenPGP and PIV smartcard management"],["TLDR","**List available smartcards**\n\n```gpg-card list```\n\n**Display card information**\n\n```gpg-card info```\n\n**Change PIN**\n\n```gpg-card passwd```\n\n**Set cardholder name**\n\n```gpg-card name```\n\n**Fetch public key from card URL**\n\n```gpg-card fetch```\n\n**Generate keys on card**\n\n```gpg-card generate```\n\n**Reset card to factory defaults**\n\n```gpg-card factory-reset```"],["SYNOPSIS","**gpg-card** [_options_] [_command_ [_args_]]"],["SUBCOMMANDS","**list**\n> List available cards and readers.\n\n**info**\n> Display card information.\n\n**name**\n> Set cardholder name.\n\n**lang**\n> Set language preference.\n\n**url**\n> Set public key URL.\n\n**fetch**\n> Fetch key from URL on card.\n\n**passwd**\n> Change PIN, Admin PIN, or Reset Code.\n\n**generate**\n> Generate keys on card.\n\n**keytocard**\n> Transfer key to card.\n\n**forcesig**\n> Toggle signature PIN requirement.\n\n**factory-reset**\n> Reset card to factory defaults."],["DESCRIPTION","**gpg-card** is a GnuPG tool for managing OpenPGP and PIV smartcards. It provides an interactive interface for configuring cards, managing PINs, generating keys, and transferring keys to smartcards.\n\nIntroduced in GnuPG 2.3 as a replacement for the older **gpg --card-edit** workflow, it offers a more streamlined command-line experience for smartcard operations."],["INSTALL","```apt: sudo apt install scdaemon```\n\n```apk: sudo apk add gnupg-utils```"],["SEE ALSO","[gpg](/man/gpg)(1)"]],"e":["gpg"],"x":"gpg-card list"},"grub-mkdevicemap":{"n":"grub-mkdevicemap","t":"create GRUB device mapping file","s":[["TAGLINE","create GRUB device mapping file"],["TLDR","**Generate device map**\n\n```sudo grub-mkdevicemap```\n\n**Specify output file**\n\n```sudo grub-mkdevicemap -m /boot/grub/device.map```\n\n**Verbose output**\n\n```sudo grub-mkdevicemap -v```"],["SYNOPSIS","**grub-mkdevicemap** [_options_]"],["PARAMETERS","**-m**, **--device-map** _FILE_\n> Output device map file (default: /boot/grub/device.map).\n\n**-n**, **--no-floppy**\n> Skip floppy detection.\n\n**-s**, **--probe-second-floppy**\n> Also probe the second floppy drive.\n\n**-v**, **--verbose**\n> Verbose mode.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**grub-mkdevicemap** creates a device map file for GRUB that maps BIOS drive numbers to Linux device names. The device map helps GRUB understand disk ordering.\n\nThe map is automatically generated but can be customized for special configurations where disk ordering needs to be explicitly controlled."],["CAVEATS","Mostly unnecessary today since GRUB generates a device map on the fly and identifies disks by UUID or label in grub.cfg. Still useful to override detection, such as when a partition or logical volume is used as a virtual machine's disk."],["HISTORY","grub-mkdevicemap was developed as part of **GNU GRUB** for device mapping between BIOS and Linux."],["INSTALL","```apt: sudo apt install grub-common```"],["SEE ALSO","[grub-install](/man/grub-install)(8), [grub-mkconfig](/man/grub-mkconfig)(8), [grub](/man/grub)(8)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/grub/)```\n\n```[Source code](https://git.savannah.gnu.org/cgit/grub.git)```\n\n```[Documentation](https://www.gnu.org/software/grub/manual/grub/grub.html)```"]],"e":["grub-install","grub-mkconfig","grub"],"x":"sudo grub-mkdevicemap"},"hive":{"n":"hive","t":"data warehouse system for Hadoop","s":[["TAGLINE","data warehouse system for Hadoop"],["TLDR","**Start Hive shell**\n\n```hive```\n\n**Execute query**\n\n```hive -e \"SELECT * FROM [table]\"```\n\n**Run script file**\n\n```hive -f [script.hql]```\n\n**Set configuration**\n\n```hive --hiveconf [key=value]```\n\n**Silent mode**\n\n```hive -S -e \"[query]\"```"],["SYNOPSIS","**hive** [_options_]"],["PARAMETERS","**-e** _QUERY_\n> Execute query.\n\n**-f** _FILE_\n> Execute script file.\n\n**-S**, **--silent**\n> Silent mode.\n\n**--hiveconf** _KEY=VALUE_\n> Set configuration.\n\n**--database** _DB_\n> Use database.\n\n**-i** _FILE_\n> Initialization file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Hive** is a data warehouse system for Hadoop. It provides SQL-like query language (HiveQL) for querying large datasets stored in HDFS.\n\nThe tool translates queries to MapReduce, Tez, or Spark jobs. It's used for data analysis and ETL on big data platforms."],["CAVEATS","Requires Hadoop cluster. Query latency higher than RDBMS. Schema on read."],["HISTORY","Apache Hive was developed at **Facebook** and contributed to the **Apache** project for SQL-based big data analytics."],["INSTALL","```brew: brew install hive```"],["SEE ALSO","[beeline](/man/beeline)(1), [yarn](/man/yarn)(1), [impala](/man/impala)(1)"]],"e":["beeline","yarn","impala"],"x":"hive"},"hud":{"n":"hud","t":"Compact heads-up display for coding-agent CLIs","s":[["TAGLINE","Compact heads-up display for coding-agent CLIs"],["TLDR","**Install** globally and wire agent handbacks\n\n```npm install -g adrida/hud-mode && hud install```\n\n**Start** with the default agent engine\n\n```hud```\n\n**Start with a prompt**\n\n```hud \"[fix the failing CI]\"```\n\n**Resume** the last session in this directory\n\n```hud -r```\n\n**Drive Claude Code** under the same deck\n\n```hud claude \"[fix the CI]\"```\n\n**Drive Codex** and resume the last session\n\n```hud codex -r```\n\n**Set** the default engine for bare `hud`\n\n```hud default claude```"],["SYNOPSIS","**hud** [_options_] [_prompt_]\n\n**hud** **claude**|**codex**|**opencode** [_options_] [_prompt_]\n\n**hud** **install** | **uninstall** | **default** _engine_"],["DESCRIPTION","**hud** is a zero-dependency terminal front end for coding agents (**OpenCode**, **Claude Code**, and **Codex**). It drives each CLI headless through its JSON event stream and shows a compact instrument panel (model, tokens, status, activity) instead of a scrolling wall of tool calls. The prompt bar stays writable mid-turn so follow-ups queue; when the agent finishes, the full answer lands with rendered markdown.\n\n`/hud` toggles between this deck and the engine's full TUI for the same session (hook / AGENTS.md rule / custom command, depending on the engine). Quitting a full TUI returns to the hud. Instruments and preferences persist under `~/.claude/hud/`.\n\nRequires **Node.js ≥ 18** and at least one of `opencode`, `claude`, or `codex` on `PATH`."],["PARAMETERS","**-r** [_session-id_]\n> Resume the last session in the current directory, or a specific session id.\n\n**-m**, **--model** _model_\n> Engine-specific model id.\n\n**-e**, **--effort** _level_\n> Engine-specific reasoning effort.\n\n**--danger**\n> Skip approvals / sandboxing where the engine supports it.\n\n**claude**, **codex**, **opencode**\n> Select which agent binary to drive for this run.\n\n**install**\n> Interactive setup: default agent, OpenCode echo, handback wiring into Claude/Codex/OpenCode configs.\n\n**uninstall**\n> Remove hooks, skills, prompts, and config blocks written by **install**.\n\n**default** _engine_\n> Persist which engine bare **hud** launches."],["CONFIGURATION","**~/.claude/hud/config.json**\n> Default engine and instrument (`/gauges`) preferences.\n\n**~/.claude/hud/links/**\n> Per-session ledger of URLs the agent shared (OSC 8 hyperlinks in supporting terminals).\n\n**~/.claude/hud/handoff.json**\n> Sentinel file used when toggling to/from a full engine TUI.\n\n**hud install** also writes engine-specific pieces under `~/.claude/skills/hud/`, `~/.claude/settings.json` (hook; backup `.hud-backup`), `~/.codex/prompts/hud.md`, a marked block in `~/.codex/AGENTS.md`, and `~/.config/opencode/commands/hud.md`."],["CAVEATS","Not a fork of the agent CLIs — behavior depends on each engine's headless JSON stream and resume APIs. Codex handback may use a plain `hud` AGENTS.md rule rather than a zero-token slash command. Permission approvals inside the hud itself remain on the roadmap."],["HISTORY","**hud** (package **hud-mode**) is an MIT-licensed terminal UI by the Tracer / adrida authors, aimed at making multi-turn agent sessions scannable without replacing Claude Code, Codex, or OpenCode."],["SEE ALSO","[claude](/man/claude)(1), [codex](/man/codex)(1), [opencode](/man/opencode)(1)"],["RESOURCES","```[Source code](https://github.com/adrida/hud-mode)```"]],"e":["claude","codex","opencode"],"x":"npm install -g adrida/hud-mode && hud install"},"ibmcloud":{"n":"ibmcloud","t":"CLI for managing resources on IBM Cloud","s":[["TAGLINE","CLI for managing resources on IBM Cloud"],["TLDR","**Log in interactively**\n\n```ibmcloud login```\n\n**Log in with single sign-on**\n\n```ibmcloud login --sso```\n\n**Log in non-interactively** with an API key\n\n```ibmcloud login --apikey [API_KEY]```\n\n**Set the target region and resource group**\n\n```ibmcloud target -r [us-south] -g [default]```\n\n**List resource instances**\n\n```ibmcloud resource service-instances```\n\n**Show account information**\n\n```ibmcloud account show```\n\n**Install a plugin** from the IBM Cloud repo\n\n```ibmcloud plugin install [plugin-name]```\n\n**List installed plugins**\n\n```ibmcloud plugin list```"],["SYNOPSIS","**ibmcloud** _command_ [_subcommand_] [_options_]"],["PARAMETERS","**login** [**--sso**] [**--apikey** _KEY_] [**-r** _REGION_] [**-g** _GROUP_] [**-c** _ACCOUNT_ID_]\n> Authenticate against IBM Cloud. `--sso` uses single sign-on, `--apikey` uses a platform API key.\n\n**logout**\n> End the current session.\n\n**target** [**-r** _REGION_] [**-g** _GROUP_] [**-o** _ORG_] [**-s** _SPACE_]\n> Set or show the current target region, resource group, Cloud Foundry org, or space.\n\n**account** _subcommand_\n> Manage accounts (e.g. `account show`, `account users`, `account list`).\n\n**resource** _subcommand_\n> Manage resource groups and service instances (e.g. `resource groups`, `resource service-instances`, `resource service-instance-create`).\n\n**plugin** _subcommand_\n> Manage plugins: `install`, `uninstall`, `list`, `update`, `repo-plugins`.\n\n**cf** _subcommand_\n> Cloud Foundry commands (requires the `cloud-foundry` plugin).\n\n**iam** _subcommand_\n> Manage users, service IDs, API keys, and access policies.\n\n**config** [**--http-timeout** _N_] [**--color** _on|off_] [**--locale** _LOCALE_] [**--check-version** _true|false_]\n> Configure CLI behavior (HTTP timeout, output color, locale, version check).\n\n**--help**, **-h**\n> Display help.\n\n**--version**\n> Print the CLI version."],["DESCRIPTION","**ibmcloud** is the official command-line interface for IBM Cloud. It wraps IBM Cloud's REST APIs for account, IAM, and resource management, and exposes service-specific functionality through installable plugins.\n\nMost workflows begin with `ibmcloud login` followed by `ibmcloud target` to select the region and resource group. From there, `resource`, `iam`, and plugin commands create and manage services, clusters, and functions. Plugins such as `kubernetes-service`, `code-engine`, `container-registry`, and `cloud-databases` add service-specific subcommands."],["CONFIGURATION","**~/.bluemix/config.json**\n> Stores session, target, and configuration information.\n\n**IBMCLOUD_API_KEY**\n> If set, used by `ibmcloud login` to authenticate non-interactively.\n\n**IBMCLOUD_HOME**\n> Overrides the default `~/.bluemix` configuration directory."],["CAVEATS","Requires an IBM Cloud account and network access to `cloud.ibm.com`. Many service commands live in plugins, which must be installed first. Some regions require specific resource groups; running commands before `ibmcloud target` often yields \"no target\" errors."],["HISTORY","**ibmcloud** is the official CLI for **IBM Cloud** (formerly **Bluemix**). The current binary unified and replaced the older `bluemix` (`bx`) CLI around 2018, adopting the `ibmcloud` command name along with IBM's Bluemix-to-IBM-Cloud rebrand."],["SEE ALSO","[aws](/man/aws)(1), [gcloud](/man/gcloud)(1), [az](/man/az)(1), [oc](/man/oc)(1), [kubectl](/man/kubectl)(1)"]],"e":["aws","gcloud","az","oc","kubectl"],"x":"ibmcloud login"},"initramfs-tools":{"n":"initramfs-tools","t":"manages the initial RAM filesystem on Debian-based systems","s":[["TAGLINE","manages the initial RAM filesystem on Debian-based systems"],["TLDR","**Update initramfs for current kernel**\n\n```sudo update-initramfs -u```\n\n**Create new initramfs**\n\n```sudo update-initramfs -c -k [$(uname -r)]```\n\n**Update all initramfs images**\n\n```sudo update-initramfs -u -k all```\n\n**Delete initramfs for kernel**\n\n```sudo update-initramfs -d -k [5.10.0-1]```\n\n**Show verbose output**\n\n```sudo update-initramfs -u -v```"],["SYNOPSIS","**update-initramfs** [_options_]"],["PARAMETERS","**-c**\n> Create new initramfs.\n\n**-u**\n> Update existing initramfs.\n\n**-d**\n> Delete initramfs.\n\n**-k** _version_\n> Kernel version (or \"all\").\n\n**-v**\n> Verbose output.\n\n**-b** _directory_\n> Set boot directory."],["CONFIGURATION","```\n/etc/initramfs-tools/initramfs.conf    Main config\n/etc/initramfs-tools/modules           Modules to include\n/etc/initramfs-tools/hooks/            Hook scripts\n/etc/initramfs-tools/scripts/          Boot scripts\n```"],["DESCRIPTION","**initramfs-tools** manages the initial RAM filesystem (initramfs) on Debian-based systems. The initramfs contains the minimal filesystem loaded before the real root filesystem is mounted.\n\nIt includes drivers, scripts, and tools needed for early boot, such as filesystem drivers, LVM, RAID, or encryption setup."],["CAVEATS","Debian/Ubuntu specific. Incorrect configuration may prevent boot. Always keep a working backup. Custom modules need manual configuration."],["HISTORY","initramfs-tools replaced the older initrd mechanism on Debian. It provides a modular, scriptable framework for building the initial filesystem, developed by the Debian project."],["SEE ALSO","[mkinitramfs](/man/mkinitramfs)(8), [dracut](/man/dracut)(8)"]],"e":["mkinitramfs","dracut"],"x":"sudo update-initramfs -u"},"jq":{"n":"jq","t":"command-line JSON processor","s":[["TAGLINE","command-line JSON processor"],["TLDR","**Pretty print JSON**\n\n```cat [data.json] | jq '.'```\n\n**Extract field**\n\n```cat [data.json] | jq '.name'```\n\n**Array indexing**\n\n```cat [data.json] | jq '.[0]'```\n\n**Filter array**\n\n```cat [data.json] | jq '.[] | select(.age > 30)'```\n\n**Map transformation**\n\n```cat [data.json] | jq '[.[] | {name, age}]'```\n\n**Raw string output**\n\n```cat [data.json] | jq -r '.name'```\n\n**Compact output**\n\n```cat [data.json] | jq -c '.'```"],["SYNOPSIS","**jq** [_options_] _filter_ [_file_...]"],["PARAMETERS","_FILTER_\n> jq filter expression.\n\n_FILE_\n> Input JSON file(s).\n\n**-r**, **--raw-output**\n> Output raw strings.\n\n**-c**, **--compact-output**\n> Compact output.\n\n**-s**, **--slurp**\n> Read entire input as array.\n\n**-n**, **--null-input**\n> No input.\n\n**--arg** _NAME_ _VAL_\n> Set variable.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jq** is a lightweight, command-line JSON processor often described as \"sed for JSON.\" It reads JSON input from files or standard input, applies a filter expression, and writes the transformed result to standard output. Filters can be chained together with the pipe operator (`|`), enabling multi-step transformations that extract fields, restructure objects, compute values, and aggregate arrays in a single invocation.\n\nThe filter language supports object and array indexing (`.foo`, `.[0]`), iteration (`.[]`), conditionals (`if-then-else`), comparison and logic operators, built-in functions like `map`, `select`, `group_by`, and `sort_by`, as well as string interpolation and regular expressions. This makes jq well suited for tasks like extracting nested values from API responses, converting JSON records into CSV, or reshaping data for downstream tools. Output can be pretty-printed (the default), compacted with `-c` for piping, or emitted as raw strings with `-r` for shell integration."],["CAVEATS","Learning curve for complex queries. Streaming mode for large files. Null handling can be tricky."],["HISTORY","jq was created by **Stephen Dolan** as a sed/awk equivalent for JSON, becoming the standard CLI JSON tool."],["INSTALL","```apt: sudo apt install jq```\n\n```dnf: sudo dnf install jq```\n\n```pacman: sudo pacman -S jq```\n\n```apk: sudo apk add jq```\n\n```zypper: sudo zypper install jq```\n\n```brew: brew install jq```\n\n```nix: nix profile install nixpkgs#jq```"],["SEE ALSO","[jo](/man/jo)(1), [jc](/man/jc)(1), [yq](/man/yq)(1), [gron](/man/gron)(1)"]],"e":["jo","jc","yq","gron"],"x":"cat [data.json] | jq '.'"},"kwrite":{"n":"kwrite","t":"simple text editor from the KDE project","s":[["TAGLINE","simple text editor from the KDE project"],["TLDR","**Open** a text file\n\n```kwrite [path/to/file]```\n\nOpen **multiple** files\n\n```kwrite [file1] [file2]```\n\nOpen with specific **encoding**\n\n```kwrite --encoding [UTF-8] [path/to/file]```\n\nOpen and navigate to specific **line and column**\n\n```kwrite --line [line_number] --column [column_number] [path/to/file]```"],["SYNOPSIS","**kwrite** [_options_] [_file_...]"],["PARAMETERS","**--encoding** _ENCODING_\n> Open file with specified character encoding\n\n**--line** _LINE_\n> Navigate to specified line number\n\n**--column** _COLUMN_\n> Navigate to specified column number\n\n**--stdin**\n> Read document from stdin\n\n**-h**, **--help**\n> Display help message"],["DESCRIPTION","**kwrite** is a simple text editor from the KDE project. It provides syntax highlighting for many programming languages, code folding, and integration with KDE services.\n\nKWrite shares the same editor component (KTextEditor) as Kate, making it a lightweight single-document alternative when full IDE features aren't needed."],["CAVEATS","Requires KDE Frameworks libraries. Single-document editor only; for multiple documents and sessions, use **kate** instead. Available in the KDE Applications package."],["HISTORY","KWrite has been part of KDE since the early versions, providing a simple but capable text editor for the KDE desktop environment."],["INSTALL","```apt: sudo apt install kwrite```\n\n```dnf: sudo dnf install kwrite```\n\n```apk: sudo apk add kwrite```\n\n```zypper: sudo zypper install kwrite```"],["SEE ALSO","[kate](/man/kate)(1), [gedit](/man/gedit)(1), [nano](/man/nano)(1)"]],"e":["kate","gedit","nano"],"x":"kwrite [path/to/file]"},"memcached":{"n":"memcached","t":"high-performance, distributed memory caching system","s":[["TAGLINE","high-performance, distributed memory caching system"],["TLDR","**Start memcached**\n\n```memcached```\n\n**Start with specific memory**\n\n```memcached -m [256]```\n\n**Listen on specific port**\n\n```memcached -p [11211]```\n\n**Run as daemon**\n\n```memcached -d -u [memcache]```\n\n**Verbose output**\n\n```memcached -vv```\n\n**Limit connections**\n\n```memcached -c [1024]```"],["SYNOPSIS","**memcached** [_options_]"],["PARAMETERS","**-p** _port_\n> TCP port (default 11211).\n\n**-m** _MB_\n> Memory limit in megabytes.\n\n**-c** _connections_\n> Max simultaneous connections.\n\n**-d**\n> Run as daemon.\n\n**-u** _user_\n> Run as user.\n\n**-l** _ip_\n> Listen address.\n\n**-v**, **-vv**\n> Verbose output.\n\n**-t** _threads_\n> Number of threads."],["DESCRIPTION","**memcached** is a high-performance, distributed memory caching system. It stores key-value pairs in RAM to reduce database load and speed up dynamic web applications.\n\nmemcached uses a simple protocol and is widely used for caching database queries, API responses, and session data."],["PROTOCOL COMMANDS","```\nset key 0 3600 5\\r\\nvalue\\r\\n  # Store\nget key\\r\\n                     # Retrieve\ndelete key\\r\\n                  # Remove\nincr key 1\\r\\n                  # Increment\nstats\\r\\n                       # Statistics\n```"],["CAVEATS","No persistence (RAM only). No authentication by default. LRU eviction when full. Not for large values (1MB limit)."],["HISTORY","memcached was developed by **Brad Fitzpatrick** at **LiveJournal** in **2003** to reduce database load. It became foundational for web scaling."],["INSTALL","```apt: sudo apt install memcached```\n\n```dnf: sudo dnf install memcached```\n\n```pacman: sudo pacman -S memcached```\n\n```apk: sudo apk add memcached```\n\n```zypper: sudo zypper install memcached```\n\n```brew: brew install memcached```\n\n```nix: nix profile install nixpkgs#memcached```"],["SEE ALSO","[redis-cli](/man/redis-cli)(1)"]],"e":["redis-cli"],"x":"memcached"},"minetestserver":{"n":"minetestserver","t":"runs a dedicated Minetest game server","s":[["TAGLINE","runs a dedicated Minetest game server"],["TLDR","**Start Minetest server**\n\n```minetestserver```\n\n**Start with specific world**\n\n```minetestserver --world [/path/to/world]```\n\n**Start on specific port**\n\n```minetestserver --port [30000]```\n\n**Start with game ID**\n\n```minetestserver --gameid [minetest_game] --world [/path/to/world]```"],["SYNOPSIS","**minetestserver** [_options_]"],["PARAMETERS","**--world** _path_\n> World directory path.\n\n**--port** _port_\n> Server port (default 30000).\n\n**--gameid** _id_\n> Game ID to use.\n\n**--config** _file_\n> Configuration file.\n\n**--logfile** _file_\n> Log output file.\n\n**--terminal**\n> Run in terminal mode.\n\n**--migrate** _backend_\n> Migrate map data to backend."],["DESCRIPTION","**minetestserver** runs a dedicated Minetest game server. Hosts multiplayer worlds without a graphical client. Supports mods, custom games, and player management."],["INSTALL","```pacman: sudo pacman -S luanti-server```\n\n```apk: sudo apk add luanti-server```\n\n```nix: nix profile install nixpkgs#luanti-server```"],["SEE ALSO","[minetest](/man/minetest)(1)"]],"e":["minetest"],"x":"minetestserver"},"mktorrent":{"n":"mktorrent","t":"creates BitTorrent metainfo files","s":[["TAGLINE","creates BitTorrent metainfo files"],["TLDR","**Create torrent from file**\n\n```mktorrent -a [http://tracker.example.com/announce] [file]```\n\n**Create torrent from directory**\n\n```mktorrent -a [http://tracker.example.com/announce] [directory/]```\n\n**Create with multiple trackers**\n\n```mktorrent -a [tracker1] -a [tracker2] [file]```\n\n**Create private torrent**\n\n```mktorrent -p -a [http://tracker.example.com/announce] [file]```\n\n**Specify piece length**\n\n```mktorrent -l [20] [file]```\n\n**Add web seed**\n\n```mktorrent -w [http://example.com/file] -a [tracker] [file]```\n\n**Create with comment**\n\n```mktorrent -c \"[My torrent]\" -a [tracker] [file]```\n\n**Specify output filename**\n\n```mktorrent -o [output.torrent] -a [tracker] [file]```\n\n**Verbose output**\n\n```mktorrent -v -a [tracker] [file]```"],["SYNOPSIS","**mktorrent** [_-a tracker_] [_-p_] [_-l piece_length_] [_-o output.torrent_] [_options_] _path_"],["PARAMETERS","**-a** _URL_\n> Tracker announce URL.\n\n**-o** _FILE_\n> Output torrent file.\n\n**-p**\n> Private torrent (no DHT/PEX).\n\n**-l** _N_\n> Piece length as power of 2 (default 18, i.e. 256KB).\n\n**-c** _TEXT_\n> Comment.\n\n**-n** _NAME_\n> Name in torrent (default: path basename).\n\n**-w** _URL_\n> Web seed URL.\n\n**-s** _TEXT_\n> Source tag for private trackers.\n\n**-v**\n> Verbose output.\n\n**-t** _THREADS_\n> Number of threads for hashing.\n\n**-d**\n> Don't write creation date.\n\n**-h**\n> Show help screen."],["DESCRIPTION","**mktorrent** creates BitTorrent metainfo (.torrent) files. It hashes the content and packages file information with tracker details.\n\nThe tool calculates piece hashes for file verification. Piece length affects swarm efficiency - larger pieces reduce overhead but delay initial sharing. Auto-selection usually works well.\n\nMultiple trackers provide redundancy. When one tracker is down, clients try others. Private torrents disable DHT and peer exchange, requiring tracker-only operation.\n\nWeb seeds provide HTTP fallback when peers are unavailable. They're useful for initial seeding or low-popularity torrents.\n\nThe source tag helps private trackers identify torrents from their site. It's embedded in the info dictionary, becoming part of the info hash.\n\nCreation is fast for small files but may take time for large datasets due to hashing requirements."],["CAVEATS","Changing any content after creation invalidates the torrent. Private tracker torrents not interoperable with public. Large piece sizes delay initial piece completion."],["HISTORY","**mktorrent** was written as a lightweight, fast torrent creator. It provides command-line access to torrent creation without requiring a full BitTorrent client."],["INSTALL","```apt: sudo apt install mktorrent```\n\n```dnf: sudo dnf install mktorrent```\n\n```pacman: sudo pacman -S mktorrent```\n\n```apk: sudo apk add mktorrent```\n\n```zypper: sudo zypper install mktorrent```\n\n```brew: brew install mktorrent```\n\n```nix: nix profile install nixpkgs#mktorrent```"],["SEE ALSO","[transmission-create](/man/transmission-create)(1), [rtorrent](/man/rtorrent)(1), [aria2c](/man/aria2c)(1)"]],"e":["transmission-create","rtorrent","aria2c"],"x":"mktorrent -a [http://tracker.example.com/announce] [file]"},"ndiswrapper":{"n":"ndiswrapper","t":"allows Windows WiFi drivers on Linux","s":[["TAGLINE","allows Windows WiFi drivers on Linux"],["TLDR","**Install Windows driver**\n\n```ndiswrapper -i [driver.inf]```\n\n**List installed drivers**\n\n```ndiswrapper -l```\n\n**Remove driver**\n\n```ndiswrapper -r [drivername]```\n\n**Write modprobe config**\n\n```ndiswrapper -m```\n\n**Show hardware**\n\n```ndiswrapper -v```\n\n**Load module**\n\n```modprobe ndiswrapper```"],["SYNOPSIS","**ndiswrapper** [_options_]"],["PARAMETERS","**-i** _INF_\n> Install driver from given .inf file (also copies referenced .sys/.bin into /etc/ndiswrapper).\n\n**-l**\n> List installed drivers and the matching hardware/PCI ID.\n\n**-r** _NAME_\n> Remove an installed driver.\n\n**-m**\n> Write a modprobe alias for the wireless interface (writes to /etc/modprobe.d).\n\n**-ma**\n> Write module alias configuration.\n\n**-mi**\n> Write module install configuration.\n\n**-v**\n> Show driver and hardware (device ID) information.\n\n**-d** _DEVID_:_DRIVER_\n> Use DRIVER for the device with the given PCI/USB id.\n\n**-e** _DRIVER_\n> Remove a driver from the system.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ndiswrapper** allows Windows WiFi drivers on Linux. It wraps NDIS drivers for Linux use.\n\nThe tool enables unsupported WiFi cards. Loads Windows .sys and .inf files."],["CAVEATS","Legacy tool. Most cards now have native drivers. Requires Windows driver files."],["HISTORY","ndiswrapper was created to use **Windows network drivers** on Linux when native drivers weren't available."],["INSTALL","```pacman: sudo pacman -S ndiswrapper```"],["SEE ALSO","[iwconfig](/man/iwconfig)(1), [modprobe](/man/modprobe)(1), [lspci](/man/lspci)(1)"]],"e":["iwconfig","modprobe","lspci"],"x":"ndiswrapper -i [driver.inf]"},"octotype":{"n":"octotype","t":"Terminal typing trainer inspired by Monkeytype","s":[["TAGLINE","Terminal typing trainer inspired by Monkeytype"],["TLDR","**Start a typing test**\n\n```octotype```"],["SYNOPSIS","**octotype** [_options_]"],["DESCRIPTION","**octotype** is a TUI typing trainer inspired by the popular Monkeytype web application. Built with Ratatui and powered by Gladius, it provides a customizable typing practice experience directly in the terminal.\n\nIt measures typing speed and accuracy, allowing you to practice and improve your typing skills without leaving the command line."],["CAVEATS","Terminal must support Unicode for proper character rendering."],["HISTORY","**octotype** is written in **Rust** using the Ratatui framework."],["SEE ALSO","[ttyper](/man/ttyper)(1)"]],"e":["ttyper"],"x":"octotype"},"ogr2ogr":{"n":"ogr2ogr","t":"Convert vector geospatial data between formats","s":[["TAGLINE","Convert vector geospatial data between formats"],["TLDR","**Convert a shapefile to GeoJSON**\n\n```ogr2ogr -f \"GeoJSON\" [output.json] [input.shp]```\n\n**Convert GeoJSON to GeoPackage**\n\n```ogr2ogr -f \"GPKG\" [output.gpkg] [input.json]```\n\n**Reproject data to WGS84**\n\n```ogr2ogr -t_srs EPSG:4326 [output.shp] [input.shp]```\n\n**Import a shapefile into PostGIS**\n\n```ogr2ogr -f \"PostgreSQL\" PG:\"dbname=[db]\" [input.shp]```\n\n**Filter features with a WHERE clause**\n\n```ogr2ogr -where \"[population > 10000]\" [output.shp] [input.shp]```\n\n**Clip features to a bounding box**\n\n```ogr2ogr -spat [xmin] [ymin] [xmax] [ymax] [output.shp] [input.shp]```\n\n**Append data to an existing PostGIS layer**\n\n```ogr2ogr -append -f \"PostgreSQL\" PG:\"dbname=[db]\" [input.shp]```\n\n**Select specific fields and rename the output layer**\n\n```ogr2ogr -select [name,population] -nln [cities] [output.gpkg] [input.shp]```"],["SYNOPSIS","**ogr2ogr** [_options_] _dst_datasource_ _src_datasource_ [_layer_]"],["PARAMETERS","_DST_DATASOURCE_\n> Destination dataset (file path, database connection string, etc.).\n\n_SRC_DATASOURCE_\n> Source dataset.\n\n**-f** _FORMAT_\n> Output format name (e.g., \"GeoJSON\", \"ESRI Shapefile\", \"PostgreSQL\", \"GPKG\").\n\n**-t_srs** _SRS_\n> Target spatial reference system (e.g., EPSG:4326).\n\n**-s_srs** _SRS_\n> Source spatial reference system (override if not defined in source).\n\n**-a_srs** _SRS_\n> Assign a spatial reference system to the output without reprojecting.\n\n**-select** _FIELDS_\n> Comma-separated list of fields to copy from the source.\n\n**-where** _EXPR_\n> SQL WHERE clause to filter features from the source.\n\n**-sql** _STATEMENT_\n> SQL statement to execute against the source for feature selection.\n\n**-spat** _XMIN_ _YMIN_ _XMAX_ _YMAX_\n> Spatial filter: only select features intersecting this bounding box.\n\n**-clipsrc** _XMIN_ _YMIN_ _XMAX_ _YMAX_\n> Clip geometries to the specified bounding box or WKT geometry.\n\n**-overwrite**\n> Delete and recreate the output layer if it already exists.\n\n**-append**\n> Append to an existing layer instead of creating a new one.\n\n**-update**\n> Open existing output datasource in update mode.\n\n**-nln** _NAME_\n> Assign a new name to the output layer.\n\n**-nlt** _TYPE_\n> Define the geometry type for the output layer (e.g., POINT, POLYGON, MULTILINESTRING).\n\n**-lco** _NAME=VALUE_\n> Layer creation option (format specific).\n\n**-dsco** _NAME=VALUE_\n> Dataset creation option (format specific).\n\n**-skipfailures**\n> Continue processing after a failure, skipping the failed feature.\n\n**-progress**\n> Display a progress bar on the terminal.\n\n**-gt** _N_\n> Group N features per transaction (default 20000). Increase for better performance with database drivers."],["DESCRIPTION","**ogr2ogr** converts vector geospatial data between file formats, databases, and web services. It is part of the **GDAL/OGR** library and supports over 80 vector formats including Shapefile, GeoJSON, GeoPackage, PostGIS, KML, and GML.\n\nBeyond simple format conversion, ogr2ogr can reproject coordinates between spatial reference systems, filter features by attribute or spatial extent, clip geometries, select specific fields, and transform geometry types."],["CAVEATS","Part of the GDAL suite and must be installed separately. Format support depends on how GDAL was compiled. Coordinate system reprojection requires correct SRS definitions. The **-skipfailures** option forces transaction grouping to 1, which can severely slow database inserts."],["INSTALL","```apt: sudo apt install gdal-bin```\n\n```apk: sudo apk add gdal-tools```"],["SEE ALSO","[ogrinfo](/man/ogrinfo)(1), [gdal_translate](/man/gdal_translate)(1), [gdalwarp](/man/gdalwarp)(1), [gdalinfo](/man/gdalinfo)(1)"]],"e":["ogrinfo","gdal_translate","gdalwarp","gdalinfo"],"x":"ogr2ogr -f \"GeoJSON\" [output.json] [input.shp]"},"openstack-volume":{"n":"openstack-volume","t":"manages block storage volumes in OpenStack Cinder","s":[["TAGLINE","manages block storage volumes in OpenStack Cinder"],["TLDR","**List volumes**\n\n```openstack volume list```\n\n**Create volume**\n\n```openstack volume create --size [10] [volume_name]```\n\n**Attach volume to server**\n\n```openstack server add volume [server] [volume]```\n\n**Delete volume**\n\n```openstack volume delete [volume_name]```"],["SYNOPSIS","**openstack volume** _command_ [_options_]"],["PARAMETERS","**list**\n> List volumes.\n\n**create** _name_\n> Create volume.\n\n**delete** _name_\n> Delete volume.\n\n**show** _name_\n> Show volume details.\n\n**--size** _gb_\n> Volume size in gigabytes.\n\n**--image** _image_\n> Create from image.\n\n**--snapshot** _snapshot_\n> Create from snapshot.\n\n**--type** _type_\n> Volume type."],["DESCRIPTION","**openstack volume** manages block storage volumes in OpenStack Cinder. Create persistent storage volumes to attach to instances. Part of OpenStack unified CLI."],["SEE ALSO","[openstack-server](/man/openstack-server)(1), [openstack](/man/openstack)(1)"]],"e":["openstack-server","openstack"],"x":"openstack volume list"},"pambackground":{"n":"pambackground","t":"creates a bitmap mask identifying background pixels in a PAM/PNM image","s":[["TAGLINE","creates a bitmap mask identifying background pixels in a PAM/PNM image"],["TLDR","**Create background mask from image**\n\n```pambackground [input.pam] > [output.pbm]```"],["SYNOPSIS","**pambackground** [_options_] [_file_]"],["PARAMETERS","**-verbose**\n> Print processing information."],["DESCRIPTION","**pambackground** creates a bitmap mask identifying background pixels in a PAM/PNM image. Uses corner pixels to determine background color and flood-fills to find connected background regions. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmcolormask](/man/ppmcolormask)(1), [pamcut](/man/pamcut)(1)"]],"e":["ppmcolormask","pamcut"],"x":"pambackground [input.pam] > [output.pbm]"},"paperkey":{"n":"paperkey","t":"extracts printable GPG key data","s":[["TAGLINE","extracts printable GPG key data"],["TLDR","**Extract secret from GPG key**\n\n```paperkey --secret-key [secret.gpg] --output [paperkey.txt]```\n\n**Restore from paper backup**\n\n```paperkey --pubring [public.gpg] --secrets [paperkey.txt] --output [restored.gpg]```\n\n**Output in base16**\n\n```paperkey --secret-key [secret.gpg] --output-type base16```"],["SYNOPSIS","**paperkey** [_options_]"],["PARAMETERS","**--secret-key** _FILE_\n> Secret key file.\n\n**--pubring** _FILE_\n> Public keyring for restore.\n\n**--secrets** _FILE_\n> Paper backup file.\n\n**--output** _FILE_\n> Output file.\n\n**--output-type** _TYPE_\n> Output format (raw, base16)."],["DESCRIPTION","**paperkey** extracts printable GPG key data. Creates paper backups.\n\nThe tool enables offline key storage. Requires public key to restore."],["CAVEATS","Requires public key for restore. Print and store securely."],["HISTORY","paperkey was created for **paper-based GPG key** backup and recovery."],["INSTALL","```apt: sudo apt install paperkey```\n\n```dnf: sudo dnf install paperkey```\n\n```pacman: sudo pacman -S paperkey```\n\n```zypper: sudo zypper install paperkey```\n\n```brew: brew install paperkey```\n\n```nix: nix profile install nixpkgs#paperkey```"],["SEE ALSO","[gpg](/man/gpg)(1), [gpg2](/man/gpg2)(1)"]],"e":["gpg","gpg2"],"x":"paperkey --secret-key [secret.gpg] --output [paperkey.txt]"},"phpbu":{"n":"phpbu","t":"PHP Backup Utility","s":[["TAGLINE","PHP Backup Utility"],["TLDR","**Run backup**\n\n```phpbu```\n\n**Use specific config**\n\n```phpbu --configuration=[phpbu.xml]```\n\n**Simulate backup**\n\n```phpbu --simulate```\n\n**Verbose output**\n\n```phpbu -v```"],["SYNOPSIS","**phpbu** [_options_]"],["PARAMETERS","**--configuration=**_FILE_\n> Path to a phpbu XML configuration file.\n\n**--bootstrap=**_FILE_\n> Bootstrap PHP file included before running the backup.\n\n**--simulate**\n> Perform a trial run without making any changes.\n\n**--limit=**_SUBSET_\n> Limit backup execution to the specified subset.\n\n**--restore**\n> Print a restore guide for the configured backups.\n\n**--generate-configuration**\n> Create a new configuration skeleton interactively.\n\n**--colors**\n> Enable colored output.\n\n**--debug**\n> Display debugging information during backup generation.\n\n**-v**, **--verbose**\n> Output more verbose information.\n\n**-V**, **--version**\n> Output version information and exit.\n\n**-h**, **--help**\n> Display usage information."],["DESCRIPTION","**phpbu** is PHP Backup Utility. Automates backup creation.\n\nThe tool handles databases, files, and syncing. XML configuration."],["CAVEATS","Requires configuration. Supports MySQL, files, etc."],["HISTORY","PHPBU was created for **automated PHP application** backup workflows."],["SEE ALSO","[mysqldump](/man/mysqldump)(1), [rsync](/man/rsync)(1)"]],"e":["mysqldump","rsync"],"x":"phpbu"},"pnmcolormap":{"n":"pnmcolormap","t":"Generate color palette from PNM images","s":[["TAGLINE","Generate color palette from PNM images"],["TLDR","**Generate color map with N colors**\n\n```pnmcolormap [256] [image.ppm] > [colormap.ppm]```\n\n**Use median cut algorithm**\n\n```pnmcolormap -center [16] [image.ppm] > [colormap.ppm]```\n\n**Spread colors evenly**\n\n```pnmcolormap -spreadluminosity [256] [image.ppm] > [colormap.ppm]```\n\n**From multiple images**\n\n```pnmcolormap [256] [image1.ppm] [image2.ppm] > [colormap.ppm]```"],["SYNOPSIS","**pnmcolormap** [_options_] _ncolors_ [_pnmfile_...]"],["PARAMETERS","**ncolors**\n> Maximum colors in map.\n\n**-center**\n> Use center of cluster.\n\n**-meancolor**\n> Use mean color of cluster.\n\n**-meanpixel**\n> Use mean weighted by pixel count.\n\n**-spreadbrightness**\n> Spread by brightness.\n\n**-spreadluminosity**\n> Spread by luminosity."],["DESCRIPTION","**pnmcolormap** creates a color map (palette) from an image using median cut quantization. The output is a small image where each pixel is a color from the palette.\n\nUsed with pnmremap for color reduction."],["EXAMPLES","```bash"],["Create 256-color palette","pnmcolormap 256 photo.ppm > palette.ppm"],["Reduce colors using map","pnmcolormap 16 image.ppm > map.ppm\npnmremap -mapfile=map.ppm image.ppm > reduced.ppm"],["Combined color reduction","pnmcolormap 256 image.ppm | pnmremap -mapfile=- image.ppm > reduced.ppm\n```"],["CAVEATS","Output is the colormap, not the remapped image. Use pnmremap to apply. Part of Netpbm."],["HISTORY","pnmcolormap is part of **Netpbm**, implementing median-cut color quantization algorithm."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmremap](/man/pnmremap)(1), [ppmquant](/man/ppmquant)(1), [ppmquantall](/man/ppmquantall)(1), [netpbm](/man/netpbm)(1)"]],"e":["pnmremap","ppmquant","ppmquantall","netpbm"],"x":"pnmcolormap [256] [image.ppm] > [colormap.ppm]"},"pnmrotate":{"n":"pnmrotate","t":"Rotate PNM images by an arbitrary angle","s":[["TAGLINE","Rotate PNM images by an arbitrary angle"],["TLDR","**Rotate image**\n\n```pnmrotate [45] [input.pnm] > [output.pnm]```\n\n**Rotate with background color**\n\n```pnmrotate -background [white] [30] [input.pnm] > [output.pnm]```\n\n**Rotate without anti-aliasing**\n\n```pnmrotate -noantialias [30] [input.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmrotate** [_options_] _angle_ [_file_]"],["PARAMETERS","_angle_\n> Rotation angle in degrees (must be between -90 and 90). Measured counter-clockwise.\n\n**-background** _color_\n> Background fill color. By default, pnmrotate selects what appears to be the background color of the original image.\n\n**-noantialias**\n> Disable anti-aliasing. Simply moves pixels instead of synthesizing output pixels from multiple input pixels."],["DESCRIPTION","**pnmrotate** rotates PNM images by an arbitrary angle specified in degrees (between -90 and 90, measured counter-clockwise). The output image is enlarged as needed to contain the full rotated result, with new areas filled by the background color.\n\nAnti-aliasing is applied by default for smooth edges; use **-noantialias** to disable it for faster processing or when working with binary images. For rotations that are multiples of 90 degrees, use **pamflip** instead. Part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamflip](/man/pamflip)(1), [pnmflip](/man/pnmflip)(1), [pnmshear](/man/pnmshear)(1)"]],"e":["pamflip","pnmflip","pnmshear"],"x":"pnmrotate [45] [input.pnm] > [output.pnm]"},"poedit":{"n":"poedit","t":"Graphical editor for gettext PO translations","s":[["TAGLINE","Graphical editor for gettext PO translations"],["TLDR","**Open a PO file for editing**\n\n```poedit [file.po]```\n\n**Create a new translation from a POT template**\n\n```poedit --new [template.pot]```\n\n**Open a file and jump to a specific line**\n\n```poedit --line [10] [file.po]```"],["SYNOPSIS","**poedit** [_options_] [_files_]"],["PARAMETERS","**--new** _file_\n> Create new catalog from POT.\n\n**--line** _num_\n> Open file at the specified line number.\n\n**--help**\n> Display usage information and exit.\n\n**--version**\n> Display version information and exit."],["DESCRIPTION","**Poedit** is a graphical editor for gettext PO translation files. It provides an intuitive interface for translators with features like translation memory, glossary, and machine translation integration."],["CAVEATS","Poedit is a GUI application with minimal command-line interface. For command-line translation management, use gettext tools directly (msgfmt, xgettext, msginit, msgmerge)."],["HISTORY","Poedit was created by **Václav Slavík** as an open-source tool for translators working with gettext."],["INSTALL","```dnf: sudo dnf install poedit```\n\n```pacman: sudo pacman -S poedit```\n\n```apk: sudo apk add poedit```\n\n```zypper: sudo zypper install poedit```\n\n```nix: nix profile install nixpkgs#poedit```"],["SEE ALSO","[msgfmt](/man/msgfmt)(1), [xgettext](/man/xgettext)(1), [msginit](/man/msginit)(1), [msgmerge](/man/msgmerge)(1), [gettext](/man/gettext)(1)"]],"e":["msgfmt","xgettext","msginit","msgmerge","gettext"],"x":"poedit [file.po]"},"pwdx":{"n":"pwdx","t":"Show working directory of a process","s":[["TAGLINE","Show working directory of a process"],["TLDR","Print **current working directory** of a process\n\n```pwdx 1234```\n\nPrint working directory of **multiple processes**\n\n```pwdx 1234 5678```"],["SYNOPSIS","**pwdx** [_option_...] _pid_..."],["DESCRIPTION","**pwdx** reports the current working directory of a process. It reads this information from /proc/[pid]/cwd and displays the directory path for each specified process ID."],["PARAMETERS","**-V, --version**\n> Output version information and exit\n\n**-h, --help**\n> Output help screen and exit"],["CAVEATS","The process must be accessible to the user running pwdx. For processes owned by other users, root privileges may be required to access the working directory information."],["HISTORY","**pwdx** was created by Nicholas Miell in 2004 and is part of the **procps-ng** package. It resembles a SunOS command of the same name."],["INSTALL","```apt: sudo apt install procps```\n\n```dnf: sudo dnf install procps-ng```\n\n```pacman: sudo pacman -S procps-ng```\n\n```apk: sudo apk add procps-ng```\n\n```zypper: sudo zypper install procps```\n\n```brew: brew install procps```\n\n```nix: nix profile install nixpkgs#procps```"],["SEE ALSO","[ps](/man/ps)(1), [pgrep](/man/pgrep)(1), [lsof](/man/lsof)(8), [readlink](/man/readlink)(1), [proc](/man/proc)(5)"]],"e":["ps","pgrep","lsof","readlink","proc"],"x":"pwdx 1234"},"pyinfra":{"n":"pyinfra","t":"Automate infrastructure deployment with Python","s":[["TAGLINE","Automate infrastructure deployment with Python"],["TLDR","**Deploy to servers**\n\n```pyinfra [inventory] [deploy.py]```\n\n**Run command on servers**\n\n```pyinfra [inventory] exec -- [command]```\n\n**Check what would change**\n\n```pyinfra --dry [inventory] [deploy.py]```\n\n**Deploy with facts**\n\n```pyinfra [inventory] fact [server.Hostname]```"],["SYNOPSIS","**pyinfra** [_options_] _inventory_ _operations_"],["PARAMETERS","_INVENTORY_\n> Target hosts file.\n\n_OPERATIONS_\n> Deploy script or commands.\n\n**--dry**\n> Dry run mode; print operations without executing.\n\n**--limit** _HOSTS_\n> Filter execution to specific hosts by name or glob pattern.\n\n**--retry** _N_\n> Retry failed operations up to N times.\n\n**--debug-inventory**\n> Print inventory hosts, groups, and data.\n\n**--debug-facts**\n> Show facts after generating operations and exit.\n\n**--debug-operations**\n> Show operations after generating and exit.\n\n**exec**\n> Execute an arbitrary shell command on hosts.\n\n**fact**\n> Gather facts from target hosts.\n\n**-v**, **-vv**, **-vvv**\n> Increase verbosity (facts, shell input, shell output)."],["DESCRIPTION","**pyinfra** automates infrastructure provisioning and configuration management using Python. Deploy scripts are written as regular Python files that describe desired system state through operations like installing packages, managing files, configuring services, and executing commands across groups of servers.\n\nThe tool connects to targets over SSH without requiring agents on remote machines, making it lightweight to adopt. An inventory file defines target hosts and groups, while deploy scripts specify what operations to run. The **--dry** flag previews changes before applying them, and the **fact** command gathers system information from targets for conditional logic in deployments."],["CONFIGURATION","**inventory.py**\n> Default inventory file defining target hosts, groups, and connection details for deployments.\n\n**deploy.py**\n> Default deploy script containing operations to execute on target hosts.\n\n**group_data/*.py**\n> Group-specific data files providing variables for host groups defined in the inventory.\n\n**config.py**\n> Project-level configuration for SSH settings, sudo behavior, and operation defaults."],["CAVEATS","Python required. SSH-based deployment."],["HISTORY","pyinfra was created for **Python-based** infrastructure automation."],["INSTALL","```nix: nix profile install nixpkgs#pyinfra```"],["SEE ALSO","[ansible](/man/ansible)(1), [fabric](/man/fabric)(1), [salt](/man/salt)(1)"]],"e":["ansible","fabric","salt"],"x":"pyinfra [inventory] [deploy.py]"},"readonly":{"n":"readonly","t":"Mark shell variables as read-only","s":[["TAGLINE","Mark shell variables as read-only"],["TLDR","**Make variable readonly**\n\n```readonly [VAR]=\"[value]\"```\n\n**Make existing variable readonly**\n\n```readonly [VAR]```\n\n**List readonly variables**\n\n```readonly```\n\n**Make function readonly**\n\n```readonly -f [function_name]```\n\n**Make array readonly**\n\n```readonly -a [ARRAY]```"],["SYNOPSIS","**readonly** [_options_] [_name_[=_value_]...]"],["PARAMETERS","**-p**\n> Print readonly variables.\n\n**-f**\n> Make functions readonly.\n\n**-a**\n> Make arrays readonly.\n\n**-A**\n> Make associative arrays readonly."],["DESCRIPTION","**readonly** is a shell builtin that marks variables or functions as read-only, preventing modification or unsetting. Useful for constants and protecting configuration values."],["EXAMPLES","```bash"],["Create readonly variable","readonly PI=3.14159"],["Attempting to change fails","PI=3  # bash: PI: readonly variable"],["Make existing readonly","CONFIG_FILE=\"/etc/app.conf\"\nreadonly CONFIG_FILE"],["Readonly function","myfunc() { echo \"Hello\"; }\nreadonly -f myfunc"],["List all readonly","readonly -p"],["Readonly array","readonly -a COLORS=(\"red\" \"green\" \"blue\")\n```"],["IN SCRIPTS","```bash\n#!/bin/bash\nreadonly SCRIPT_DIR=\"$(cd \"$(dirname \"$0\")\" && pwd)\"\nreadonly CONFIG=\"${SCRIPT_DIR}/config.ini\"\nreadonly VERSION=\"1.0.0\"\n```"],["CAVEATS","Cannot unset readonly variables (until shell exits). Subshells don't inherit readonly status. Bash specific."],["HISTORY","readonly is a **POSIX** shell builtin, with extended options in bash and other modern shells."],["SEE ALSO","[declare](/man/declare)(1), [export](/man/export)(1), [typeset](/man/typeset)(1), [bash](/man/bash)(1)"]],"e":["declare","export","typeset","bash"],"x":"readonly [VAR]=\"[value]\""},"reflector":{"n":"reflector","t":"Retrieve and filter Arch Linux mirror lists","s":[["TAGLINE","Retrieve and filter Arch Linux mirror lists"],["TLDR","Sort by **speed** and save\n\n```sudo reflector --sort rate --save /etc/pacman.d/mirrorlist```\n\nGet mirrors from **country** using **HTTPS**\n\n```reflector -c [Germany] -p https```\n\nGet **recently synced** mirrors\n\n```reflector -l [10]```\n\nUse **config file**\n\n```sudo reflector @/etc/xdg/reflector/reflector.conf```\n\nDisplay **help**\n\n```reflector -h```"],["SYNOPSIS","**reflector** [**-c** _country_] [**-p** _protocol_] [**-l** _num_] [**--sort** _method_] [**--save** _file_]"],["PARAMETERS","**-c, --country _country_**\n> Filter by country\n\n**-p, --protocol _proto_**\n> Filter by protocol (http, https, ftp)\n\n**-l, --latest _num_**\n> Get N most recently synced mirrors\n\n**--sort _method_**\n> Sort by: rate, country, score, delay\n\n**--save _file_**\n> Save output to file\n\n**--age _hours_**\n> Only mirrors synced within N hours\n\n**-f, --fastest _num_**\n> Return N fastest mirrors"],["DESCRIPTION","**reflector** retrieves and filters the latest Arch Linux mirrorlist. It can test mirror speed and sort by various criteria, then save an optimized mirrorlist for pacman.\n\nThe tool is useful for finding fast, reliable mirrors for your location. It can be run periodically via systemd timer to keep the mirrorlist current."],["CONFIGURATION","**/etc/xdg/reflector/reflector.conf**\n> Default configuration file with persistent options for country, protocol, sort method, and output path. Referenced with **@** prefix on the command line.\n\n**reflector.service** / **reflector.timer**\n> Systemd units for running reflector automatically on a schedule to keep the mirrorlist current.\n\n**/etc/pacman.d/mirrorlist**\n> Output destination for the generated mirror list, used by pacman for package downloads."],["CAVEATS","Arch Linux specific. Speed tests take time. Network conditions vary. Consider running periodically for best results. Requires internet access."],["HISTORY","**reflector** was created for Arch Linux to automate mirror selection. It replaces manual editing of the mirrorlist with intelligent, automated selection based on speed and sync status."],["INSTALL","```pacman: sudo pacman -S reflector```"],["SEE ALSO","[pacman](/man/pacman)(8), [rankmirrors](/man/rankmirrors)(1), [pacman-mirrors](/man/pacman-mirrors)(1)"]],"e":["pacman","rankmirrors","pacman-mirrors"],"x":"sudo reflector --sort rate --save /etc/pacman.d/mirrorlist"},"renice":{"n":"renice","t":"Alter process scheduling priority","s":[["TAGLINE","Alter process scheduling priority"],["TLDR","**Set the absolute priority of a running process**\n\n```renice -n [3] -p [pid]```\n\n**Increase the priority of a running process** (requires root)\n\n```sudo renice -n [-4] -p [pid]```\n\n**Decrease the priority of all processes owned by a user**\n\n```renice -n [4] -u [user]```\n\n**Set the priority of all processes in a process group**\n\n```sudo renice -n [-5] -g [process_group]```"],["SYNOPSIS","**renice** [**-n**] _priority_ [**-g**|**-p**|**-u**] _identifier_..."],["PARAMETERS","**-n** _priority_\n> Specify the scheduling priority to set. Values range from -20 (highest) to 19 (lowest).\n\n**-p** _pid_\n> Interpret arguments as process IDs (default).\n\n**-u** _user_\n> Interpret arguments as usernames or UIDs.\n\n**-g** _pgrp_\n> Interpret arguments as process group IDs."],["DESCRIPTION","**renice** alters the scheduling priority (niceness) of running processes. Niceness values range from **-20** (highest priority, most favorable to the process) to **19** (lowest priority, least favorable).\n\nRegular users can only increase niceness (lower priority) of their own processes. The superuser can decrease niceness (raise priority) and modify any process."],["CAVEATS","Lowering niceness (increasing priority) requires root privileges. The actual scheduling effect depends on the system's scheduler and load. Setting extreme priorities can impact system responsiveness."],["HISTORY","The **renice** command originated in 4.0BSD and has been a standard Unix utility since. It complements the **nice** command, which sets priority when launching new processes."],["INSTALL","```apt: sudo apt install bsdutils```\n\n```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[nice](/man/nice)(1), [top](/man/top)(1), [ps](/man/ps)(1), [ionice](/man/ionice)(1)"]],"e":["nice","top","ps","ionice"],"x":"renice -n [3] -p [pid]"},"rpi-otp-private-key":{"n":"rpi-otp-private-key","t":"Read or write a private key in Raspberry Pi OTP memory","s":[["TAGLINE","Read or write a private key in Raspberry Pi OTP memory"],["TLDR","**Read the OTP private key**\n\n```sudo rpi-otp-private-key```\n\n**Write a private key** from a binary file (irreversible!)\n\n```sudo rpi-otp-private-key -w [keyfile.bin]```\n\n**Specify which row** of OTP memory to read or write\n\n```sudo rpi-otp-private-key -y [56]```"],["SYNOPSIS","**rpi-otp-private-key** [**-w** _file_] [**-y** _row_] [**-c**]"],["PARAMETERS","**-w** _file_\n> Write the contents of _file_ (must be 32 bytes / 256 bits) to OTP memory. Permanent.\n\n**-y** _row_\n> Override the default OTP row used to store the key.\n\n**-c**\n> Check whether the OTP key has been programmed without printing it."],["DESCRIPTION","**rpi-otp-private-key** reads or writes the customer-controlled 256-bit private key stored in the One-Time Programmable (OTP) memory of a Raspberry Pi's SoC. The key is used to sign or decrypt material as part of the Raspberry Pi secure boot chain (sometimes called \"Customer OTP\").\n\nWhen invoked with no flags, it prints the currently programmed key as a 64-character hex string (or all zeros if it has not been programmed). With **-w**, it burns the supplied 32-byte file into OTP — this is **permanent and cannot be undone**, including after factory reset."],["CAVEATS","OTP programming is **irreversible**: once burned, the bits cannot be cleared. A bad write can permanently brick secure-boot deployments. The displayed private key should be treated as sensitive and never shared. Requires running as root because it accesses the OTP via the VideoCore mailbox interface (vcgencmd)."],["HISTORY","Part of the **rpi-eeprom** package shipped with Raspberry Pi OS, providing tools for managing EEPROM and OTP on Raspberry Pi 4 and later. Used by Raspberry Pi's secure-boot infrastructure introduced with the Raspberry Pi 4 boot ROM updates."],["SEE ALSO","[rpi-eeprom-update](/man/rpi-eeprom-update)(1), [vcgencmd](/man/vcgencmd)(1)"]],"e":["rpi-eeprom-update","vcgencmd"],"x":"sudo rpi-otp-private-key"},"setarch":{"n":"setarch","t":"Change reported architecture for program execution","s":[["TAGLINE","Change reported architecture for program execution"],["TLDR","**Run** a command as if the machine architecture is i686\n\n```setarch i686 [command]```\n\n**Run** a shell with x86_64 architecture\n\n```setarch x86_64 [bash]```\n\n**Disable** randomization of the virtual address space\n\n```setarch [linux32] -R [command]```\n\n**List** supported architectures\n\n```setarch --list```"],["SYNOPSIS","**setarch** [_arch_] [_options_] [_program_ [_arguments_]]"],["PARAMETERS","**arch**\n> Target architecture (i686, x86_64, linux32, linux64, etc.)\n\n**-R, --addr-no-randomize**\n> Disable address space layout randomization (ASLR).\n\n**-F, --fdpic-funcptrs**\n> Treat user-space function pointers as pointers to function descriptors.\n\n**-Z, --mmap-page-zero**\n> Turn on MMAP_PAGE_ZERO for legacy programs.\n\n**-L, --addr-compat-layout**\n> Use legacy virtual address space layout.\n\n**-X, --read-implies-exec**\n> Turn on READ_IMPLIES_EXEC.\n\n**-B, --32bit**\n> Turn on ADDR_LIMIT_32BIT.\n\n**-I, --short-inode**\n> Turn on SHORT_INODE.\n\n**-S, --whole-seconds**\n> Turn on WHOLE_SECONDS.\n\n**-T, --sticky-timeouts**\n> Turn on STICKY_TIMEOUTS.\n\n**-3, --3gb**\n> Use a 3GB memory layout.\n\n**--uname-2.6**\n> Report a 2.6 kernel version in uname.\n\n**--list**\n> List supported architectures and exit.\n\n**-v, --verbose**\n> Be verbose.\n\n**-h, --help**\n> Display help."],["DESCRIPTION","**setarch** changes the reported architecture for a program's execution environment. It modifies what **uname -m** reports and can affect how programs behave based on architecture detection.\n\nThe command is useful for running 32-bit applications on 64-bit systems, compatibility testing, and debugging programs that behave differently based on architecture. The **linux32** and **linux64** symlinks are shortcuts for common uses."],["CAVEATS","Does not enable actual architecture emulation; the underlying CPU must support the requested mode. Disabling ASLR (**-R**) reduces security and should only be used for debugging. Part of the util-linux package."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add setarch```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[linux32](/man/linux32)(8), [linux64](/man/linux64)(8), [uname](/man/uname)(1), [personality](/man/personality)(2)"]],"e":["linux32","linux64","uname","personality"],"x":"setarch i686 [command]"},"sfdk-qmake":{"n":"sfdk-qmake","t":"Run qmake in Sailfish build environment","s":[["TAGLINE","Run qmake in Sailfish build environment"],["TLDR","**Run qmake in build environment**\n\n```sfdk qmake```\n\n**Configure with debug option**\n\n```sfdk qmake CONFIG+=debug```\n\n**Display qmake help**\n\n```sfdk qmake --help```"],["SYNOPSIS","**sfdk qmake** [_qmake_options_]"],["DESCRIPTION","**sfdk qmake** runs qmake inside the Sailfish OS build environment with correct Qt configuration. Together with **sfdk make**, it allows running rpmbuild so that just the corresponding part of the %build section of the SPEC file is executed. Part of Sailfish SDK."],["SEE ALSO","[sfdk](/man/sfdk)(1), [sfdk-cmake](/man/sfdk-cmake)(1), [sfdk-make](/man/sfdk-make)(1), [qmake](/man/qmake)(1)"]],"e":["sfdk","sfdk-cmake","sfdk-make","qmake"],"x":"sfdk qmake"},"soupault":{"n":"soupault","t":"HTML-native static site generator","s":[["TAGLINE","HTML-native static site generator"],["TLDR","**Build site**\n\n```soupault```\n\n**Build with config**\n\n```soupault --config [soupault.toml]```\n\n**Force rebuild**\n\n```soupault --force```\n\n**Verbose output**\n\n```soupault --verbose```\n\n**Show version**\n\n```soupault --version```\n\n**Init project**\n\n```soupault --init```"],["SYNOPSIS","**soupault** [_--config file_] [_--force_] [_options_]"],["PARAMETERS","**--config** _FILE_\n> Config file path.\n\n**--force**\n> Force rebuild all.\n\n**--verbose**\n> Verbose output.\n\n**--init**\n> Initialize project.\n\n**--site-dir** _DIR_\n> Site directory.\n\n**--build-dir** _DIR_\n> Output directory."],["DESCRIPTION","**soupault** is a static site generator and HTML processor that works directly with HTML files rather than using a template language. It takes plain HTML as input, manipulates the DOM tree using CSS selectors, and outputs modified HTML, making it compatible with any existing HTML content.\n\nThe tool uses a widget system configured in TOML to perform transformations like inserting content into pages, generating tables of contents, and adding metadata. Custom widgets can be written in Lua for project-specific processing needs.\n\nUnlike most static site generators, soupault makes no assumptions about site structure or content format. It can process existing HTML sites without conversion, making it suitable for both new projects and migrating legacy websites."],["CAVEATS","Learning curve unique. HTML-centric approach. OCaml written."],["HISTORY","**soupault** was created as a site generator and HTML processor that works with real HTML rather than template languages."],["INSTALL","```apk: sudo apk add soupault```\n\n```nix: nix profile install nixpkgs#soupault```"],["SEE ALSO","[hugo](/man/hugo)(1), [jekyll](/man/jekyll)(1), [pandoc](/man/pandoc)(1)"]],"e":["hugo","jekyll","pandoc"],"x":"soupault"},"speedometer":{"n":"speedometer","t":"Real-time network traffic ASCII graphs","s":[["TAGLINE","Real-time network traffic ASCII graphs"],["TLDR","Show **network traffic** graph for an interface\n\n```speedometer -r [eth0] -t [eth0]```\n\nMonitor **received** bytes only\n\n```speedometer -r [eth0]```\n\nMonitor **transmitted** bytes only\n\n```speedometer -t [eth0]```\n\nTrack **file download** progress\n\n```speedometer -f [path/to/file] [expected_size]```\n\nUse **multiple columns** for different sources\n\n```speedometer -r [eth0] -c -t [eth0]```\n\nSet **refresh interval**\n\n```speedometer -i [seconds] -r [eth0]```"],["SYNOPSIS","**speedometer** [_OPTIONS_] _tap_ [_tap_...]"],["PARAMETERS","**-r** _interface_\n> Monitor received bytes on network interface\n\n**-t** _interface_\n> Monitor transmitted bytes on network interface\n\n**-f** _filename_ [_size_]\n> Track file download with optional progress bar\n\n**-c**\n> Start a new display column\n\n**-i** _seconds_\n> Set refresh interval (default: 1)\n\n**-b**\n> Use blocky display instead of smooth characters\n\n**-l**\n> Use linear scale instead of logarithmic\n\n**-s**\n> Show speeds in bits/second instead of bytes/second\n\n**-m** _max_\n> Set maximum bytes/second for chart\n\n**-n** _min_\n> Set minimum bytes/second for chart\n\n**-k** _colors_\n> Terminal color support (1, 16, 88, or 256)\n\n**-p**\n> Plain text display mode\n\n**-x**\n> Exit when file reaches expected size\n\n**-z**\n> Report zero for non-existent files"],["DESCRIPTION","**speedometer** displays real-time network traffic or file transfer speeds as ASCII graphs in the terminal. It reads data from network interfaces or monitors file growth, presenting the information visually with customizable scales and refresh rates.\n\nMultiple data sources (taps) can be displayed simultaneously in columns, allowing comparison of upload/download speeds or monitoring multiple interfaces. The tool is useful for monitoring transfers, debugging network issues, and visualizing bandwidth usage."],["CAVEATS","Requires access to network interface statistics in /proc or /sys. Interface names vary by system (eth0, enp0s3, wlan0, etc.). Large refresh intervals may miss traffic spikes. Terminal must support the display characters being used."],["HISTORY","**speedometer** was created as a Python script for visualizing network bandwidth. The tool provides a lightweight alternative to graphical network monitors, suitable for remote SSH sessions and headless servers. It has been available in Linux distribution repositories since the mid-**2000s**."],["INSTALL","```aur: yay -S speedometer```"],["SEE ALSO","[iftop](/man/iftop)(8), [nethogs](/man/nethogs)(8), [bmon](/man/bmon)(1), [vnstat](/man/vnstat)(1)"]],"e":["iftop","nethogs","bmon","vnstat"],"x":"speedometer -r [eth0] -t [eth0]"},"standard-version":{"n":"standard-version","t":"Automated semantic versioning from commits","s":[["TAGLINE","Automated semantic versioning from commits"],["TLDR","**Release new version**\n\n```standard-version```\n\n**First release**\n\n```standard-version --first-release```\n\n**Pre-release version**\n\n```standard-version --prerelease alpha```\n\n**Dry run**\n\n```standard-version --dry-run```\n\n**Specific version bump**\n\n```standard-version --release-as [major|minor|patch]```\n\n**Skip changelog**\n\n```standard-version --skip.changelog```"],["SYNOPSIS","**standard-version** [_--release-as type_] [_--prerelease tag_] [_options_]"],["PARAMETERS","**--first-release**\n> First version.\n\n**--release-as** _TYPE_\n> Version bump type.\n\n**--prerelease** _TAG_\n> Pre-release tag.\n\n**--dry-run**\n> Preview only.\n\n**--skip.changelog**\n> Skip changelog.\n\n**-s**, **--sign**\n> Sign commits and tags with GPG.\n\n**-a**, **--commit-all**\n> Commit all staged files, not just package/changelog.\n\n**-t**, **--tag-prefix** _prefix_\n> Customize tag prefix (default: v).\n\n**--no-verify**\n> Bypass pre-commit/commit-msg git hooks."],["DESCRIPTION","**standard-version** automates semantic versioning and changelog generation for Node.js projects based on the Conventional Commits specification. It analyzes commit messages (feat, fix, BREAKING CHANGE) to determine whether to bump the major, minor, or patch version.\n\nThe tool updates the version in package.json, generates or updates a CHANGELOG.md from commit messages, creates a git commit with the version bump, and tags it with the new version number. A dry run mode previews changes without modifying files. Pre-release tags (alpha, beta, rc) are supported for staged releases."],["CAVEATS","**Deprecated** - the project is no longer maintained. The authors recommend **release-please** (GitHub users) or the fork **commit-and-tag-version** for continued support. Requires conventional commits, a Node.js project, and a git repository."],["HISTORY","**standard-version** was created for automated semantic versioning based on Conventional Commits specification."],["INSTALL","```brew: brew install standard```"],["SEE ALSO","[npm-version](/man/npm-version)(1), [semantic-release](/man/semantic-release)(1), [git](/man/git)(1)"]],"e":["npm-version","semantic-release","git"],"x":"standard-version"},"subliminal":{"n":"subliminal","t":"Automatic subtitle downloader for videos","s":[["TAGLINE","Automatic subtitle downloader for videos"],["TLDR","**Download subtitles for video**\n\n```subliminal download -l [en] [video.mkv]```\n\n**Download for directory**\n\n```subliminal download -l [en] [videos/]```\n\n**Multiple languages**\n\n```subliminal download -l [en] -l [es] [video.mkv]```\n\n**Specific provider**\n\n```subliminal download -l [en] -p [opensubtitles] [video.mkv]```\n\n**Force download**\n\n```subliminal download -l [en] -f [video.mkv]```\n\n**With authentication**\n\n```subliminal --opensubtitles [user] [pass] download -l [en] [video.mkv]```"],["SYNOPSIS","**subliminal** [_--provider auth_] **download** [_-l lang_] [_-p provider_] [_options_] _paths_"],["PARAMETERS","**download**\n> Download subtitles.\n\n**-l**, **--language** _LANG_\n> Language code.\n\n**-p**, **--provider** _NAME_\n> Subtitle provider.\n\n**-f**, **--force**\n> Force re-download.\n\n**-a**, **--age** _AGE_\n> File age filter.\n\n**-d**, **--directory** _DIR_\n> Output directory.\n\n**-e**, **--encoding** _ENC_\n> Subtitle encoding."],["PROVIDERS","**opensubtitles** - OpenSubtitles.org\n**addic7ed** - Addic7ed\n**podnapisi** - Podnapisi\n**thesubdb** - TheSubDB\n**tvsubtitles** - TVSubtitles"],["DESCRIPTION","**subliminal** automatically downloads subtitles for video files by searching multiple online providers simultaneously. It uses video file hashing to accurately identify content, matching the exact release version for well-synchronized subtitles rather than relying solely on filename matching.\n\nThe tool queries multiple subtitle providers including OpenSubtitles, Addic7ed, Podnapisi, and others, increasing the likelihood of finding subtitles. If one provider fails, it falls back to others automatically. Languages are specified using ISO 639-1 codes, and multiple languages can be requested in a single command.\n\nSubliminal can recursively scan entire directories to process media libraries in batch. It skips videos that already have subtitles unless forced to re-download. Provider authentication can be configured for access to premium subtitle sources with higher download limits."],["CAVEATS","Some providers need accounts. Rate limits apply. Not all videos have subtitles."],["HISTORY","**Subliminal** is a Python library and CLI for subtitle downloading. It simplifies finding and downloading subtitles for video files."],["INSTALL","```brew: brew install subliminal```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [vlc](/man/vlc)(1)"]],"e":["ffmpeg","vlc"],"x":"subliminal download -l [en] [video.mkv]"},"switch_root":{"n":"switch_root","t":"Switch to new root filesystem during boot","s":[["TAGLINE","Switch to new root filesystem during boot"],["TLDR","**Switch** to a new root filesystem and start init\n\n```switch_root [new_root] [/sbin/init]```"],["SYNOPSIS","**switch_root** [_options_] _newroot_ _init_ [_arguments_]"],["PARAMETERS","**newroot**\n> Path to the new root filesystem\n\n**init**\n> Path to the init program to execute (relative to new root)\n\n**-h, --help**\n> Display help information\n\n**-V, --version**\n> Display version information"],["DESCRIPTION","**switch_root** moves special filesystems (**/proc**, **/dev**, **/sys**, **/run**) to a new root directory and makes it the new root of the mount tree. It then executes the specified init program.\n\nThis command is primarily used in initramfs to transition from the initial RAM filesystem to the real root filesystem. The old root is deleted to free memory before the switch occurs."],["CAVEATS","The new root must be the root of a mount; the command will fail otherwise. Use bind-mounting as a workaround if needed. Must be run as root. Should only be used during early boot from initramfs. Part of the util-linux package."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[chroot](/man/chroot)(1), [pivot_root](/man/pivot_root)(8), [mount](/man/mount)(8), [init](/man/init)(8)"]],"e":["chroot","pivot_root","mount","init"],"x":"switch_root [new_root] [/sbin/init]"},"sysctl":{"n":"sysctl","t":"View and modify kernel parameters","s":[["TAGLINE","View and modify kernel parameters"],["TLDR","**Show** all available variables and their values\n\n```sysctl -a```\n\n**Set** a changeable kernel state variable\n\n```sysctl -w [section.tunable]=[value]```\n\n**Get** currently open file handlers\n\n```sysctl fs.file-nr```\n\n**Get** limit for simultaneous open files\n\n```sysctl fs.file-max```\n\n**Apply** changes from /etc/sysctl.conf\n\n```sysctl -p```"],["SYNOPSIS","**sysctl** [_options_] [_variable_[=_value_]] ..."],["PARAMETERS","**-a, --all**\n> Display all available variables\n\n**-w, --write**\n> Write value to variable\n\n**-p, --load [_file_]**\n> Load settings from file (default: /etc/sysctl.conf)\n\n**-n, --values**\n> Print only values, not variable names\n\n**-e, --ignore**\n> Ignore unknown variables\n\n**-q, --quiet**\n> Suppress error messages\n\n**--system**\n> Load settings from all system configuration files"],["DESCRIPTION","**sysctl** is used to examine and modify kernel parameters at runtime. Parameters available are listed under **/proc/sys/** and can be read or written using this tool.\n\nCommon categories include **kernel.** (core kernel settings), **net.** (networking), **vm.** (virtual memory), and **fs.** (filesystem). Changes made with sysctl are temporary unless persisted to configuration files."],["CONFIGURATION","**/etc/sysctl.conf**\n> Main configuration file for persistent kernel parameter settings, loaded at boot.\n\n**/etc/sysctl.d/*.conf**\n> Drop-in directory for modular kernel parameter configuration files."],["CAVEATS","Changes are lost on reboot unless saved to **/etc/sysctl.conf** or files in **/etc/sysctl.d/**. Some parameters are read-only. Incorrect values can destabilize the system. Requires root privileges to modify values."],["HISTORY","**sysctl** is part of **procps** (now procps-ng). It provides a convenient interface to the /proc/sys virtual filesystem for reading and writing kernel parameters at runtime."],["INSTALL","```apt: sudo apt install procps```\n\n```dnf: sudo dnf install procps-ng```\n\n```pacman: sudo pacman -S procps-ng```\n\n```apk: sudo apk add procps-ng```\n\n```zypper: sudo zypper install procps```\n\n```brew: brew install procps```\n\n```nix: nix profile install nixpkgs#procps```"],["SEE ALSO","[proc](/man/proc)(5)"]],"e":["proc"],"x":"sysctl -a"},"systemd-machine-id-setup":{"n":"systemd-machine-id-setup","t":"Initialize the machine ID","s":[["TAGLINE","Initialize the machine ID"],["TLDR","**Initialize** the machine ID in /etc/machine-id\n\n```sudo systemd-machine-id-setup```\n\n**Print** the generated or committed machine ID after setup\n\n```sudo systemd-machine-id-setup --print```\n\n**Commit** a transient machine ID to persistent storage on disk\n\n```sudo systemd-machine-id-setup --commit```\n\n**Operate** on a specific root directory\n\n```sudo systemd-machine-id-setup --root /[path/to/root]```\n\n**Operate** on a disk image\n\n```sudo systemd-machine-id-setup --image /[path/to/image]```"],["SYNOPSIS","**systemd-machine-id-setup** [_options_]"],["PARAMETERS","**--print**\n> Print the machine ID after the setup operation completes\n\n**--commit**\n> Commit a transient machine ID to persistent disk storage. Has no effect if /etc/machine-id is not mounted from memory or if /etc/ is read-only. Primarily used by systemd-machine-id-commit.service\n\n**--root _path_**\n> Operate on the specified root directory instead of the real root. All paths including /etc/machine-id are prefixed with the given path\n\n**--image _path_**\n> Operate on the specified disk image (device node or regular file) instead of a directory tree\n\n**--image-policy _policy_**\n> Specify an image mounting policy string when using --image. Defaults to the \"*\" policy (all recognized file systems are used)\n\n**-h**, **--help**\n> Show brief help and exit\n\n**--version**\n> Show version information and exit"],["DESCRIPTION","**systemd-machine-id-setup** initializes the machine ID stored in **/etc/machine-id**. It is intended for use by system installer tools at install time, or when /etc/machine-id is empty or missing.\n\nWhen invoked without **--commit**, the tool initializes the machine ID using the first available source in this order:\n\n1. A valid ID from /run/machine-id\n2. An existing D-Bus machine ID\n3. A machine ID from the system.machine_id credential\n4. A UUID from the KVM virtual machine configuration\n5. A UUID from the container environment configuration\n6. A newly generated random ID as a fallback\n\nThe **--commit** option is used to convert a transient machine ID (mounted into memory during early boot) into a persistent one written to disk."],["CAVEATS","Requires root privileges. Should only be run once during system installation or when **/etc/machine-id** is missing or empty. Changing the machine ID can break services that depend on it for stable identification. Part of the systemd suite."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemd-id128](/man/systemd-id128)(1), [machine-id](/man/machine-id)(5), [systemd-firstboot](/man/systemd-firstboot)(1)"]],"e":["systemd-id128","machine-id","systemd-firstboot"],"x":"sudo systemd-machine-id-setup"},"tfjournal":{"n":"tfjournal","t":"Record Terraform runs with git context and timing","s":[["TAGLINE","Record Terraform runs with git context and timing"],["TLDR","**Record a Terraform apply**\n\n```tfjournal -- terraform apply```\n\n**Record with a workspace label**\n\n```tfjournal -w [prod] -- tofu plan```\n\n**List recent failed runs**\n\n```tfjournal list --since 7d --status failed```"],["SYNOPSIS","**tfjournal** [_options_] **--** _terraform-command_"],["DESCRIPTION","**tfjournal** wraps terraform, tofu, and terragrunt commands to capture output, timing, exit codes, and git context (commit, branch, user, CI provider). It parses resource-level events and can display them as Gantt charts, with an optional S3 backend for sharing run history across teams."],["HISTORY","**tfjournal** was created by **Owloops** and is written in **Go**."],["SEE ALSO","[terraform](/man/terraform)(1), [tofu](/man/tofu)(1), [terragrunt](/man/terragrunt)(1)"]],"e":["terraform","tofu","terragrunt"],"x":"tfjournal -- terraform apply"},"tgsend":{"n":"tgsend","t":"Send Telegram messages via bot API","s":[["TAGLINE","Send Telegram messages via bot API"],["TLDR","**Send message**\n\n```tgsend \"[message]\"```\n\n**Send to specific chat**\n\n```tgsend -c [chat_id] \"[message]\"```\n\n**Send file**\n\n```tgsend -f [document.pdf]```\n\n**Send image**\n\n```tgsend -p [image.jpg]```\n\n**Send with caption**\n\n```tgsend -p [image.jpg] -m \"[caption]\"```\n\n**Silent message**\n\n```tgsend -s \"[message]\"```"],["SYNOPSIS","**tgsend** [_-c chat_id_] [_-f file_] [_-p photo_] [_options_] [_message_]"],["PARAMETERS","**-c** _ID_\n> Chat ID.\n\n**-f** _FILE_\n> Send file.\n\n**-p** _FILE_\n> Send photo.\n\n**-m** _TEXT_\n> Caption text.\n\n**-s**\n> Silent mode.\n\n**-t** _TOKEN_\n> Bot token."],["DESCRIPTION","**tgsend** sends messages, files, and photos to Telegram chats using the Telegram Bot API. It is designed for automation and scripting, enabling notifications, alerts, and file delivery from the command line or cron jobs.\n\nMessages can be sent to specific chats using their ID, and media attachments can include captions via the **-m** flag. The **-s** flag sends messages silently without triggering a notification on the recipient's device. A bot token is required and can be passed via the **-t** flag or environment configuration."],["CAVEATS","Bot token required. Chat ID needed. Rate limits apply."],["HISTORY","**tgsend** is a lightweight CLI tool for sending messages via the Telegram Bot API, commonly used for alerting and automation in DevOps workflows."],["SEE ALSO","[telegram-desktop](/man/telegram-desktop)(1), [curl](/man/curl)(1)"]],"e":["telegram-desktop","curl"],"x":"tgsend \"[message]\""},"theHarvester":{"n":"theHarvester","t":"OSINT email and subdomain gatherer","s":[["TAGLINE","OSINT email and subdomain gatherer"],["TLDR","**Search for emails and subdomains** of a domain using all sources\n\n```theHarvester -d [example.com] -b all```\n\n**Search using a specific source**\n\n```theHarvester -d [example.com] -b [google]```\n\n**Limit the number of results**\n\n```theHarvester -d [example.com] -b google -l [100]```\n\n**Save results to a file**\n\n```theHarvester -d [example.com] -b all -f [report]```\n\n**Perform DNS lookup** on discovered hosts\n\n```theHarvester -d [example.com] -b all --dns-lookup```\n\n**Take screenshots** of discovered web pages\n\n```theHarvester -d [example.com] -b all --screenshot [path/to/output]```\n\n**Check for subdomain takeover** vulnerabilities\n\n```theHarvester -d [example.com] -b all --take-over```"],["SYNOPSIS","**theHarvester** **-d** _domain_ **-b** _source_ [_options_]"],["PARAMETERS","**-d** _domain_\n> Target domain to search (required).\n\n**-b** _source_\n> Data source to use: google, bing, yahoo, duckduckgo, github-code, linkedin, shodan, virustotal, certspotter, crtsh, dnsdumpster, hunter, securityTrails, **all**, and others.\n\n**-l** _limit_\n> Limit the number of search results.\n\n**-f** _filename_\n> Output filename to save results (HTML/XML format).\n\n**-S** _start_\n> Start result number for search pagination.\n\n**-e** _server_\n> Use a specific DNS server for lookups.\n\n**-p**\n> Route requests through a proxy server.\n\n**-s**\n> Use Shodan to query discovered hosts.\n\n**-c**\n> Verify discovered hosts via HTTP/HTTPS.\n\n**-n**\n> Enable DNS server lookup.\n\n**-r** [_file_]\n> Perform reverse DNS lookups on discovered ranges.\n\n**-w** _wordlist_\n> DNS brute force using a wordlist for subdomain discovery.\n\n**--take-over**\n> Check for possible subdomain takeover vulnerabilities.\n\n**--screenshot** _path_\n> Take screenshots of discovered web pages.\n\n**-v**\n> Show version information."],["DESCRIPTION","**theHarvester** is an open-source intelligence (OSINT) tool used during reconnaissance in penetration testing and red team assessments. It gathers publicly available information about a target domain including email addresses, subdomains, hostnames, employee names, and open ports.\n\nThe tool queries multiple data sources including search engines, certificate databases, DNS databases, and security-focused services. Results can be saved for further analysis. Many advanced sources require API keys configured in the **api-keys.yaml** file within the theHarvester installation directory."],["CAVEATS","Many data sources require valid API keys (Shodan, Hunter.io, Censys, SecurityTrails, etc.) for full functionality. Without proper API keys, searches will return limited or no results. Rate limiting may apply when querying certain sources extensively."],["HISTORY","theHarvester was created by **Christian Martorella** (laramies) and first released around **2007**. It has become a standard reconnaissance tool included in penetration testing distributions like **Kali Linux**. The tool is actively maintained and has been updated to support Python 3.12+ and numerous new data sources over the years."],["SEE ALSO","[nmap](/man/nmap)(1), [subfinder](/man/subfinder)(1), [amass](/man/amass)(1), [recon-ng](/man/recon-ng)(1)"]],"e":["nmap","subfinder","amass","recon-ng"],"x":"theHarvester -d [example.com] -b all"},"tox":{"n":"tox","t":"Automate Python testing across versions","s":[["TAGLINE","Automate Python testing across versions"],["TLDR","**Run all test environments**\n\n```tox```\n\n**Run specific environment**\n\n```tox -e [py39]```\n\n**Run multiple environments**\n\n```tox -e [py38,py39,lint]```\n\n**List available environments**\n\n```tox -l```\n\n**Recreate environments**\n\n```tox -r```\n\n**Run in parallel**\n\n```tox -p```\n\n**Pass arguments to test command**\n\n```tox -- [--verbose]```\n\n**Recreate and run**\n\n```tox -r -e [py39]```"],["SYNOPSIS","**tox** [_-e envs_] [_-r_] [_-p_] [_-l_] [_options_] [_-- test_args_]"],["PARAMETERS","**-e** _ENVLIST_, **--envlist** _ENVLIST_\n> Environments to run.\n\n**-r**, **--recreate**\n> Recreate virtual environments.\n\n**-p**, **--parallel**\n> Run environments in parallel.\n\n**-l**, **--list**\n> List environments.\n\n**-a**, **--listenvs-all**\n> List all environments including generated.\n\n**--devenv** _PATH_\n> Create development environment.\n\n**-c** _FILE_, **--conf** _FILE_\n> Configuration file path.\n\n**--skip-missing-interpreters**\n> Skip environments without interpreter.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-q**, **--quiet**\n> Quiet output.\n\n**--notest**\n> Install but don't test.\n\n**--sdistonly**\n> Only create source distribution.\n\n**--result-json** _FILE_\n> Write results as JSON.\n\n**--**\n> Pass remaining args to test command."],["DESCRIPTION","**tox** automates testing Python packages across multiple Python versions and environments. It creates isolated virtualenvs, installs packages, and runs tests.\n\nConfiguration in tox.ini or pyproject.toml defines environments. Each environment specifies: Python version, dependencies, and commands to run. Common setups test multiple Python versions (py38, py39, py310) plus linting environments.\n\nEnvironment creation installs the package in development mode plus test dependencies. This catches installation issues early. Recreate mode (-r) rebuilds environments from scratch.\n\nParallel mode (-p) runs environments simultaneously, speeding up multi-version testing on machines with multiple cores. Output is collected and displayed after completion.\n\nBeyond testing, tox runs any command: linting (flake8, mypy), documentation builds (sphinx), formatting checks. Multiple commands chain in sequence.\n\nThe double-dash (--) passes arguments to the test command, enabling pytest flags or test selection without modifying tox.ini."],["CAVEATS","Python versions must be installed for their environments. Initial environment creation downloads dependencies. Parallel mode may hit resource limits. Complex configurations can be hard to debug. Some CI systems prefer simpler approaches."],["HISTORY","**tox** was created by **Holger Krekel** (creator of pytest) around **2010** to solve multi-Python testing. It became the standard tool for Python package testing, used by major projects. Version 4 (2022) brought significant improvements and pyproject.toml support."],["INSTALL","```dnf: sudo dnf install tox```\n\n```apk: sudo apk add py3-tox```\n\n```brew: brew install tox```"],["SEE ALSO","[pytest](/man/pytest)(1), [nox](/man/nox)(1), [virtualenv](/man/virtualenv)(1), [hatch](/man/hatch)(1)"]],"e":["pytest","nox","virtualenv","hatch"],"x":"tox"},"twitch-tui":{"n":"twitch-tui","t":"Twitch chat client in the terminal","s":[["TAGLINE","Twitch chat client in the terminal"],["TLDR","**Launch the Twitch chat TUI**\n\n```twt```\n\n**Display help**\n\n```twt --help```\n\n**Display version**\n\n```twt --version```\n\n**Connect to a specific channel**\n\n```twt -c [channel_name]```\n\n**Use a custom config file**\n\n```twt --config [path/to/config.toml]```"],["SYNOPSIS","**twt** [_options_]"],["PARAMETERS","**-c** _CHANNEL_, **--channel** _CHANNEL_\n> Channel to connect to on launch (overrides config).\n\n**-C** _PATH_, **--config** _PATH_\n> Path to a custom configuration file.\n\n**-t** _TOKEN_, **--token** _TOKEN_\n> Twitch OAuth token (overrides config token).\n\n**-h**, **--help**\n> Display help information.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**twitch-tui** is a terminal user interface for reading and interacting with Twitch chat. It features Vim and Emacs inspired keybindings and is designed to pair with streamlink, eliminating the need for a browser while watching Twitch streams.\n\nConfiguration is managed through a TOML config file generated on first run. The config controls channels, appearance, and keybindings."],["CONFIGURATION","**~/.config/twt/config.toml**\n> Main configuration file for channels, appearance, and keybindings."],["HISTORY","**twitch-tui** was created by **Xithrius** and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S twitch-tui```\n\n```nix: nix profile install nixpkgs#twitch-tui```"],["SEE ALSO","[streamlink](/man/streamlink)(1), [irssi](/man/irssi)(1), [weechat](/man/weechat)(1)"]],"e":["streamlink","irssi","weechat"],"x":"twt"},"ugrep":{"n":"ugrep","t":"Ultra-fast grep with interactive query mode","s":[["TAGLINE","Ultra-fast grep with interactive query mode"],["TLDR","**Search pattern**\n\n```ugrep \"[pattern]\" [file]```\n\n**Recursive search**\n\n```ugrep -r \"[pattern]\"```\n\n**Interactive mode**\n\n```ugrep -Q \"[pattern]\"```\n\n**Search archives**\n\n```ugrep -z \"[pattern]\" [archive.zip]```\n\n**Boolean query**\n\n```ugrep \"[word1] AND [word2]\"```\n\n**Fuzzy matching**\n\n```ugrep -Z \"[pattern]\" [file]```"],["SYNOPSIS","**ugrep** [_-r_] [_-Q_] [_-z_] [_-Z_] [_options_] _pattern_ [_files_]"],["PARAMETERS","**-r**\n> Recursive.\n\n**-Q**\n> Interactive TUI.\n\n**-z**\n> Search archives.\n\n**-Z**\n> Fuzzy matching.\n\n**-i**\n> Case insensitive.\n\n**--bool**\n> Boolean queries."],["DESCRIPTION","**ugrep** is a high-performance text search tool designed as a modern replacement for grep with extended capabilities. It provides an interactive query mode with real-time results, Boolean search operators (AND, OR, NOT), fuzzy matching for approximate patterns, and the ability to search inside compressed archives and binary files.\n\nThe tool is grep-compatible and supports POSIX, Perl, and fixed-string pattern syntax. It can search recursively through directories with file type filtering, output results in various formats including JSON and CSV, and display context around matches.\n\nArchive searching handles ZIP, tar, gzip, bzip2, and other compressed formats transparently. The interactive query UI (invoked via the **ug** alias) allows refining searches in real time."],["CAVEATS","Different from grep. More features. C++ implementation."],["HISTORY","**ugrep** was created as a fast, feature-rich grep replacement with interactive and fuzzy search capabilities."],["INSTALL","```apt: sudo apt install ugrep```\n\n```dnf: sudo dnf install ugrep```\n\n```pacman: sudo pacman -S ugrep```\n\n```apk: sudo apk add ugrep```\n\n```zypper: sudo zypper install ugrep```\n\n```brew: brew install ugrep```\n\n```nix: nix profile install nixpkgs#ugrep```"],["SEE ALSO","[grep](/man/grep)(1), [rg](/man/rg)(1), [ag](/man/ag)(1)"]],"e":["grep","rg","ag"],"x":"ugrep \"[pattern]\" [file]"},"wacli-messages-show":{"n":"wacli-messages-show","t":"Display a specific WhatsApp message by chat and ID","s":[["TAGLINE","Display a specific WhatsApp message by chat and ID"],["TLDR","**Show a message** by chat JID and message ID in JSON\n\n```wacli messages show --chat 491234567890@s.whatsapp.net --id ABC123 --json```\n\n**Show using a custom store directory**\n\n```wacli messages show --chat 491234567890@s.whatsapp.net --id ABC123 --json --store /path/to/store```"],["SYNOPSIS","**wacli** **messages** **show** **--chat** _jid_ **--id** _msg_id_ [--json] [--store _dir_]"],["DESCRIPTION","**wacli messages show** fetches and prints the full record for a single message identified by chat JID and message ID from the local store.\n\nUseful for inspecting details, media references, or scripting when you already have an ID (e.g. from a previous search or list). Deleted messages kept as local tombstones may still appear for direct `show` even when hidden from list/search.\n\nDefault store path is `~/.local/state/wacli` on Linux and `~/.wacli` elsewhere."],["PARAMETERS","**--chat** _jid_\n> Chat JID that contains the message.\n\n**--id** _msg_id_\n> Message identifier within that chat.\n\n**--json**\n> Output as JSON (recommended for further processing).\n\n**--store** _dir_\n> Use an alternate store location (default `~/.local/state/wacli` on Linux, `~/.wacli` elsewhere)."],["SEE ALSO","[wacli](/man/wacli)(1), [wacli-messages-list](/man/wacli-messages-list)(1), [wacli-messages-search](/man/wacli-messages-search)(1), [wacli-messages-context](/man/wacli-messages-context)(1)"],["RESOURCES","```[Source code](https://github.com/openclaw/wacli)```\n\n```[Documentation](https://wacli.sh/messages.html)```"]],"e":["wacli","wacli-messages-list","wacli-messages-search","wacli-messages-context"],"x":"wacli messages show --chat 491234567890@s.whatsapp.net --id ABC123 --json"},"watchdog":{"n":"watchdog","t":"System health monitoring and reset daemon","s":[["TAGLINE","System health monitoring and reset daemon"],["TLDR","**Start watchdog daemon**\n\n```sudo systemctl start watchdog```\n\n**Enable at boot**\n\n```sudo systemctl enable watchdog```\n\n**Test configuration**\n\n```sudo watchdog -v```\n\n**Run in foreground**\n\n```sudo watchdog -f```\n\n**Force test mode**\n\n```sudo watchdog -F```"],["SYNOPSIS","**watchdog** [_options_]"],["PARAMETERS","**-f**, **--foreground**\n> Run in foreground, don't fork.\n\n**-F**, **--force**\n> Force test mode.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-c** _file_\n> Use specified configuration file.\n\n**-s**, **--sync**\n> Sync before reboot.\n\n**--help**\n> Display help."],["CONFIGURATION","Config file: /etc/watchdog.conf\n\nKey settings: watchdog-device, interval, max-load-1, min-memory, and test commands."],["DESCRIPTION","**watchdog** is a daemon that monitors system health and resets the system if problems are detected. It periodically writes to /dev/watchdog to prevent the kernel from triggering a hardware reset.\n\nThe daemon can monitor various system conditions: load average, memory usage, file presence, process existence, temperature, and custom test scripts. If any check fails, the system reboots after logging the failure reason.\n\nHardware watchdog support requires appropriate kernel modules (e.g., iTCO_wdt for Intel). Software watchdog via the softdog module provides fallback when hardware isn't available."],["CAVEATS","Requires appropriate kernel watchdog driver. Improper configuration can cause unexpected reboots. Test thoroughly before production use. Software watchdog less reliable than hardware."],["HISTORY","**watchdog** was developed for Linux systems requiring high availability without human intervention. The concept dates to embedded systems where hardware watchdog timers ensure recovery from system hangs. The Linux implementation combines hardware timer support with software monitoring."],["INSTALL","```apt: sudo apt install watchdog```\n\n```dnf: sudo dnf install watchdog```\n\n```zypper: sudo zypper install watchdog```\n\n```nix: nix profile install nixpkgs#watchdog```"],["SEE ALSO","[dmesg](/man/dmesg)(1)"]],"e":["dmesg"],"x":"sudo systemctl start watchdog"},"wfuzz":{"n":"wfuzz","t":"Web application fuzzing tool","s":[["TAGLINE","Web application fuzzing tool"],["TLDR","**Fuzz URL parameter**\n\n```wfuzz -c -z file,[wordlist.txt] \"[http://target/?param=FUZZ]\"```\n\n**Fuzz directory**\n\n```wfuzz -c -z file,[dirs.txt] \"[http://target/FUZZ]\"```\n\n**POST data fuzzing**\n\n```wfuzz -c -z file,[wordlist.txt] -d \"user=admin&pass=FUZZ\" \"[http://target/login]\"```\n\n**Filter by response code**\n\n```wfuzz -c --hc 404 -z file,[wordlist.txt] \"[http://target/FUZZ]\"```\n\n**Filter by response size**\n\n```wfuzz -c --hl 0 -z file,[wordlist.txt] \"[http://target/FUZZ]\"```\n\n**Multiple fuzz points**\n\n```wfuzz -c -z file,[users.txt] -z file,[passwords.txt] \"[http://target/?u=FUZ2Z&p=FUZZ]\"```"],["SYNOPSIS","**wfuzz** [_-c_] [_-z type,data_] [_--hc codes_] [_options_] _url_"],["PARAMETERS","**-z** _TYPE,DATA_\n> Payload specification.\n\n**-c**\n> Color output.\n\n**--hc** _CODES_\n> Hide response codes.\n\n**--hl** _LINES_\n> Hide by line count.\n\n**--hw** _WORDS_\n> Hide by word count.\n\n**--hh** _CHARS_\n> Hide by char count.\n\n**-d** _DATA_\n> POST data.\n\n**-H** _HEADER_\n> HTTP header.\n\n**-t** _N_\n> Threads."],["DESCRIPTION","**wfuzz** is a web application security testing tool that performs brute-force discovery of directories, parameters, forms, and other injection points. It replaces the FUZZ keyword in URLs, headers, or POST data with entries from wordlists or other payload sources.\n\nMultiple injection points can be used simultaneously with FUZ2Z, FUZ3Z markers, allowing combined username and password attacks or multi-parameter fuzzing. Response filtering by status code, line count, word count, or character count hides uninteresting results and highlights anomalies that may indicate vulnerabilities.\n\nPayload sources include wordlist files, numeric ranges, and encoded variants. Multi-threading speeds up testing, and colored output makes results easier to scan. The tool is commonly used for directory enumeration, parameter discovery, and authentication testing in authorized security assessments."],["CAVEATS","For authorized testing only. Rate limiting may be needed. Large wordlists are slow."],["HISTORY","**wfuzz** was created by **Christian Martorella** (Edge-Security) for web application testing. It's a standard tool in penetration testing."],["INSTALL","```nix: nix profile install nixpkgs#wfuzz```"],["SEE ALSO","[ffuf](/man/ffuf)(1), [gobuster](/man/gobuster)(1), [dirb](/man/dirb)(1)"]],"e":["ffuf","gobuster","dirb"],"x":"wfuzz -c -z file,[wordlist.txt] \"[http://target/?param=FUZZ]\""},"x86_64":{"n":"x86_64","t":"Run programs in x86_64 architecture mode","s":[["TAGLINE","Run programs in x86_64 architecture mode"],["TLDR","This command is an alias for **setarch x86_64**.\n\n**View** documentation for the original command\n\n```tldr setarch```"],["SYNOPSIS","**x86_64** [_options_] [_program_ [_arguments_]]"],["DESCRIPTION","**x86_64** is a symlink to **setarch** that runs a program with the reported architecture set to x86_64. It is equivalent to running **setarch x86_64**.\n\nThis is typically used on 64-bit systems to ensure programs detect and run in 64-bit mode, though on native x86_64 systems this is usually the default behavior."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[setarch](/man/setarch)(8), [linux32](/man/linux32)(8), [linux64](/man/linux64)(8)"]],"e":["setarch","linux32","linux64"],"x":"tldr setarch"},"xml-pyx":{"n":"xml-pyx","t":"Convert XML to PYX line-oriented format","s":[["TAGLINE","Convert XML to PYX line-oriented format"],["TLDR","**Convert an XML file to PYX format**\n\n```xml pyx [file.xml]```\n\n**Convert XML from stdin**\n\n```cat [file.xml] | xml pyx```\n\n**Strip all attributes from an XML file using PYX filtering**\n\n```xml pyx [file.xml] | grep -v \"^A\" | xml depyx```"],["SYNOPSIS","**xml pyx** [_xml-file_]"],["PARAMETERS","_xml-file_\n> Input XML file. If omitted, reads from stdin."],["DESCRIPTION","**xml pyx** converts XML documents to PYX notation, a line-oriented representation derived from the SGML ESIS format (ISO 8879). It is part of the **XMLStarlet** toolkit (also invoked as `xmlstarlet pyx`).\n\nPYX represents each XML construct on a single line using prefix notation: `(` for opening tags, `)` for closing tags, `A` for attributes, `-` for text content, and `?` for processing instructions. This line-oriented format makes it easy to process XML with standard Unix text tools like **grep**, **sed**, and **awk** without requiring an XML parser.\n\nThe companion command **xml depyx** (or **xml p2x**) converts PYX back into XML."],["CAVEATS","PYX is a simplified representation and may not preserve all XML features such as comments or CDATA sections."],["INSTALL","```dnf: sudo dnf install xmlstarlet```\n\n```pacman: sudo pacman -S xmlstarlet```\n\n```apk: sudo apk add xmlstarlet```\n\n```zypper: sudo zypper install xmlstarlet```\n\n```brew: brew install xmlstarlet```\n\n```nix: nix profile install nixpkgs#xmlstarlet```"],["SEE ALSO","[xml-depyx](/man/xml-depyx)(1), [xml-p2x](/man/xml-p2x)(1), [xmlstarlet](/man/xmlstarlet)(1), [xml](/man/xml)(1)"]],"e":["xml-depyx","xml-p2x","xmlstarlet","xml"],"x":"xml pyx [file.xml]"},"yuvsplittoppm":{"n":"yuvsplittoppm","t":"Combine YUV component files into a PPM image","s":[["TAGLINE","Combine YUV component files into a PPM image"],["TLDR","**Combine YUV component files into a PPM image**\n\n```yuvsplittoppm [base] [width] [height] > [output.ppm]```\n\n**Combine using CCIR 601 (MPEG) colour scaling**\n\n```yuvsplittoppm -ccir601 [base] [width] [height] > [output.ppm]```"],["SYNOPSIS","**yuvsplittoppm** [_-ccir601_] _basename_ _width_ _height_"],["PARAMETERS","_basename_\n> Base name of the component files. Reads _basename_.Y, _basename_.U, and _basename_.V.\n\n_width_\n> Image width in pixels.\n\n_height_\n> Image height in pixels.\n\n**-ccir601**\n> Interpret YUV values using the CCIR 601 (MPEG) range rather than the default JFIF (JPEG) range."],["DESCRIPTION","**yuvsplittoppm** reads three raw files containing the Y, U, and V colour components and produces a PPM image on standard output. The filenames are derived from _basename_ by appending `.Y`, `.U`, and `.V`. Because the files are raw with no header, the image dimensions must be given on the command line.\n\nIt is the inverse of [ppmtoyuvsplit](/man/ppmtoyuvsplit)(1) and is part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtoyuvsplit](/man/ppmtoyuvsplit)(1), [yuvtoppm](/man/yuvtoppm)(1), [rgb3toppm](/man/rgb3toppm)(1)"]],"e":["ppmtoyuvsplit","yuvtoppm","rgb3toppm"],"x":"yuvsplittoppm [base] [width] [height] > [output.ppm]"},"zpaq":{"n":"zpaq","t":"Journaling archiver with high compression","s":[["TAGLINE","Journaling archiver with high compression"],["TLDR","**Create archive**\n\n```zpaq add [archive.zpaq] [files...]```\n\n**Extract archive**\n\n```zpaq extract [archive.zpaq]```\n\n**List archive contents**\n\n```zpaq list [archive.zpaq]```\n\n**Add with maximum compression**\n\n```zpaq add [archive.zpaq] [files] -method 5```\n\n**Extract to specific directory**\n\n```zpaq extract [archive.zpaq] -to [/output/dir]```\n\n**Incremental backup**\n\n```zpaq add [backup.zpaq] [directory] -all```"],["SYNOPSIS","**zpaq** _command_ _archive_[**.zpaq**] [_files_...] [_options_]"],["COMMANDS","**add**\n> Add or update files in archive\n\n**extract**\n> Extract files from archive\n\n**list**\n> List archive contents\n\n**compare**\n> Compare archive with filesystem\n\n**test**\n> Test archive integrity"],["PARAMETERS","**-method** _level_\n> Compression level (0-5, higher = better compression)\n\n**-to** _path_\n> Destination directory for extraction\n\n**-all**\n> Include hidden files and empty directories\n\n**-not** _pattern_\n> Exclude files matching pattern\n\n**-only** _pattern_\n> Include only matching files\n\n**-until** _date_\n> Extract version as of date\n\n**-threads** _n_\n> Number of threads to use\n\n**-key** _password_\n> Encrypt/decrypt with password"],["DESCRIPTION","**zpaq** is an archiver with high compression ratios and journaling capability. It uses context mixing and neural network-based prediction for compression, achieving ratios better than most conventional archivers.\n\nZPAQ's journaling feature makes it excellent for incremental backups. Each **add** creates a new version, and previous versions remain accessible. The **-until** flag extracts files as they existed at a specific time.\n\nArchives are append-only by default - new versions add to the archive without modifying previous data. This provides some protection against corruption and allows versioned backups.\n\nCompression levels range from 0 (store only) to 5 (maximum compression, slow)."],["CAVEATS","High compression levels are CPU-intensive and slow. Level 5 can be orders of magnitude slower than level 1.\n\nZPAQ format is not widely supported outside the zpaq tool itself. Consider compatibility needs.\n\nJournaling archives can grow large over time. Use **zpaq add -method 0** to consolidate.\n\nEncryption is optional and must be specified during archive creation."],["INSTALL","```apt: sudo apt install zpaq```\n\n```dnf: sudo dnf install zpaq```\n\n```zypper: sudo zypper install zpaq```\n\n```brew: brew install zpaq```\n\n```nix: nix profile install nixpkgs#zpaq```"],["SEE ALSO","[tar](/man/tar)(1), [7z](/man/7z)(1), [gzip](/man/gzip)(1), [zstd](/man/zstd)(1)"]],"e":["tar","7z","gzip","zstd"],"x":"zpaq add [archive.zpaq] [files...]"}});
