window.LCL_BUCKET(121,{"apport-cli":{"n":"apport-cli","t":"Command-line interface for crash reporting and bug filing.","s":[["TAGLINE","Command-line interface for crash reporting and bug filing."],["TLDR","**Process pending crash reports** interactively\n\n```apport-cli```\n\n**Report a bug** against a specific package\n\n```apport-cli --file-bug --package [package-name]```\n\n**Report a bug** against a running process\n\n```apport-cli --file-bug --pid [1234]```\n\n**Report a problem** using a symptom script\n\n```apport-cli --file-bug --symptom [audio]```\n\n**Upload an existing crash report** file\n\n```apport-cli --crash-file [/path/to/report.crash]```\n\n**Update an existing bug report** with more information\n\n```apport-cli --update-report [bug-number]```\n\n**Save report to file** instead of submitting\n\n```apport-cli --file-bug --package [package-name] --save [report.apport]```"],["SYNOPSIS","**apport-cli** [_options_] [_symptom_|_package_|_pid_|_crash-file_]"],["PARAMETERS","**-f**, **--file-bug**\n> Report a non-crash problem; prompts for symptom if package/pid not specified\n\n**-p** _package_, **--package=**_package_\n> Specify the package to report the problem against\n\n**-P** _pid_, **--pid=**_pid_\n> Report a problem against a running process by its PID\n\n**-s** _symptom_, **--symptom=**_symptom_\n> Use a symptom script to guide problem reporting (audio, video, network, etc.)\n\n**-c** _report_, **--crash-file=**_report_\n> Upload a stored crash report from a file\n\n**-u** _number_, **--update-report=**_number_\n> Collect additional information for an existing bug report\n\n**--save** _filename_\n> Save the report to a file instead of submitting it\n\n**-w**, **--window**\n> Click on an application window to report a bug against it"],["DESCRIPTION","**apport-cli** is a command-line interface for Ubuntu's Apport crash reporting system. It processes crash reports collected in **/var/crash/** and allows users to submit them to the bug tracking system with relevant debugging information.\n\nWhen invoked without arguments, it processes pending crash reports interactively. It can also file new bug reports against packages, running processes, or using symptom scripts that guide users through diagnosing specific problem types.\n\nApport automatically collects system information, package versions, stack traces, and other debugging data. This comprehensive information helps developers diagnose and fix issues more effectively than manual bug reports.\n\nThe tool is primarily intended for server environments where graphical interfaces like **apport-gtk** are unavailable."],["CAVEATS","Apport refuses to create reports if the package or dependencies are outdated; set **APPORT_IGNORE_OBSOLETE_PACKAGES** to override. Submitting reports requires network connectivity and authentication with Ubuntu's bug tracker (Launchpad). Core dumps and logs may contain sensitive information."],["HISTORY","**Apport** was developed by **Martin Pitt** at Canonical and introduced in **Ubuntu 6.10** (Edgy Eft) in **2006**. It was designed to improve bug report quality by automatically collecting relevant debugging information. The system has become a standard component of Ubuntu's quality assurance infrastructure and has been adopted by other Debian-based distributions."],["SEE ALSO","[apport-unpack](/man/apport-unpack)(1), [ubuntu-bug](/man/ubuntu-bug)(1)"]],"e":["apport-unpack","ubuntu-bug"],"x":"apport-cli"},"ascii":{"n":"ascii","t":"Display ASCII character table and aliases","s":[["TAGLINE","Display ASCII character table and aliases"],["TLDR","Show **aliases** for character\n\n```ascii a```\n\n**Script-friendly** output\n\n```ascii -t a```\n\nShow for **multiple** characters\n\n```ascii -s tldr```\n\nTable in **decimal**\n\n```ascii -d```\n\nTable in **hexadecimal**\n\n```ascii -x```\n\nTable in **binary**\n\n```ascii -b```"],["SYNOPSIS","**ascii** [_OPTIONS_] [_characters_]"],["DESCRIPTION","**ascii** displays ASCII character information including aliases, numeric codes in various bases, and provides quick ASCII table lookups for character encoding reference."],["PARAMETERS","**-t**\n> Terse mode for script-friendly output\n\n**-s**\n> Process multiple characters in string\n\n**-d**\n> Display ASCII table in decimal\n\n**-x**\n> Display ASCII table in hexadecimal\n\n**-o**\n> Display ASCII table in octal\n\n**-b**\n> Display ASCII table in binary\n\n**-h**, **-?**\n> Display a summary of options and a simple ASCII table\n\n**-v**\n> Display program version"],["CAVEATS","Without arguments, shows an options summary and the complete ASCII table. The base flags (**-d**, **-x**, **-o**, **-b**) have dual behavior: with no argument they print the full table in that base; with arguments they interpret those values in that base and print only the matching character records. Character aliases include common names like NUL, TAB, LF, CR, ESC, DEL for control characters."],["HISTORY","**ascii** was written by Eric S. Raymond as a handy ASCII reference utility."],["INSTALL","```apt: sudo apt install ascii```\n\n```dnf: sudo dnf install ascii```\n\n```pacman: sudo pacman -S ascii```\n\n```zypper: sudo zypper install ascii```\n\n```brew: brew install ascii```\n\n```nix: nix profile install nixpkgs#ascii```"],["SEE ALSO","[man](/man/man)(1), [charmap](/man/charmap)(1)"],["RESOURCES","```[Source code](https://gitlab.com/esr/ascii)```\n\n```[Homepage](http://www.catb.org/~esr/ascii/)```\n\n```[Documentation](http://www.catb.org/~esr/ascii/ascii.html)```"]],"e":["man","charmap"],"x":"ascii a"},"automake":{"n":"automake","t":"Generate Makefile.in templates from Makefile.am","s":[["TAGLINE","Generate Makefile.in templates from Makefile.am"],["TLDR","**Generate** Makefile.in\n\n```automake --add-missing```\n\nGenerate with **copy** instead of symlink\n\n```automake --add-missing --copy```\n\nGenerate for **specific version**\n\n```automake-1.16```"],["SYNOPSIS","**automake** [_--add-missing_] [_--copy_] [_options_] [_Makefile_...]"],["DESCRIPTION","**automake** generates Makefile.in templates from Makefile.am files. It works with autoconf to create the complete GNU Build System, handling compilation, installation, and distribution tasks.\n\nAutomake provides portable makefiles that work across Unix-like systems."],["PARAMETERS","**-a**, **--add-missing**\n> Add missing standard auxiliary files (install-sh, missing, etc.).\n\n**-c**, **--copy**\n> Copy auxiliary files instead of symlinking them.\n\n**--force-missing**\n> Replace standard auxiliary files even if they already exist.\n\n**-W** _category_, **--warnings=**_category_\n> Report warnings for the given category (e.g. all, none, error, portability).\n\n**--gnu**\n> Require conformance to GNU coding standards (the default).\n\n**--foreign**\n> Relax checks; allow files that GNU standards require to be absent.\n\n**--gnits**\n> Apply the stricter GNU Gnits standards.\n\n**-i**, **--ignore-deps**\n> Disable generation of automatic dependency tracking."],["WORKFLOW","1. Write Makefile.am\n2. Run **automake** to generate Makefile.in\n3. Run **autoconf** to generate configure\n4. Users run ./configure to generate Makefile"],["CAVEATS","Steep learning curve. Generated Makefiles are complex. Requires understanding of autotools conventions. Many projects now use CMake or Meson instead."],["HISTORY","**Automake** was created by David MacKenzie and Tom Tromey, first released in **1994** to complement autoconf with makefile generation."],["INSTALL","```dnf: sudo dnf install automake```\n\n```pacman: sudo pacman -S automake```\n\n```apk: sudo apk add automake```\n\n```zypper: sudo zypper install automake```\n\n```brew: brew install automake```\n\n```nix: nix profile install nixpkgs#automake```"],["SEE ALSO","[autoconf](/man/autoconf)(1), [autoreconf](/man/autoreconf)(1), [make](/man/make)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/automake/)```\n\n```[Documentation](https://www.gnu.org/software/automake/manual/automake.html)```"]],"e":["autoconf","autoreconf","make"],"x":"automake --add-missing"},"autopkgtest":{"n":"autopkgtest","t":"Run DEP-8 tests for Debian packages","s":[["TAGLINE","Run DEP-8 tests for Debian packages"],["TLDR","Build the package and **run all tests** directly on the system\n\n```autopkgtest -- null```\n\nRun a **specific test** for the package\n\n```autopkgtest --test-name=[test_name] -- null```\n\nDownload a **specific package** and run all tests\n\n```autopkgtest [package] -- null```\n\nTest using a **new root directory** (chroot)\n\n```autopkgtest -- chroot [path/to/new_root]```\n\nTest the package **without rebuilding** it\n\n```autopkgtest -B -- null```"],["SYNOPSIS","**autopkgtest** [_options_] _source_ **--** _virtualization_"],["DESCRIPTION","**autopkgtest** runs tests defined in Debian source packages according to the DEP-8 (autopkgtest) specification. It supports various virtualization backends for isolated testing environments.\n\nThe tool separates the test specification from the execution environment using a **--** separator. The virtualization backend (null, chroot, schroot, lxc, qemu, etc.) determines how tests are isolated."],["PARAMETERS","**--test-name=**_name_\n> Run only the named test.\n\n**--skip-test=**_name_\n> Skip the named test.\n\n**-B, --no-built-binaries**\n> Do not build the package before testing; use the provided binaries.\n\n**--setup-commands=**_commands_\n> Run the given shell commands in the testbed before the tests.\n\n**-U, --apt-upgrade**\n> Run apt-get update and dist-upgrade in the testbed before testing.\n\n**-s, --shell-fail**\n> Open a shell in the testbed if a test fails, for debugging.\n\n**--shell**\n> Open a shell in the testbed after every test.\n\n**-o** _directory_, **--output-dir=**_directory_\n> Write test artifacts and logs to the specified directory."],["VIRTUALIZATION BACKENDS","**null**\n> Run tests directly on the current system (no isolation)\n\n**chroot** _path_\n> Use a chroot environment\n\n**schroot** _name_\n> Use a schroot environment\n\n**lxc** _container_\n> Use an LXC container\n\n**qemu** _image_\n> Use a QEMU virtual machine"],["CAVEATS","The **null** backend modifies the system directly and may leave changes behind. Chroot and other backends provide better isolation. Test definitions must follow the DEP-8 specification in **debian/tests/control**."],["INSTALL","```aur: yay -S autopkgtest```"],["SEE ALSO","[dpkg-buildpackage](/man/dpkg-buildpackage)(1), [lxc](/man/lxc)(1), [schroot](/man/schroot)(1)"],["RESOURCES","```[Source code](https://salsa.debian.org/ci-team/autopkgtest)```\n\n```[Documentation](https://wiki.debian.org/ContinuousIntegration/autopkgtest)```"]],"e":["dpkg-buildpackage","lxc","schroot"],"x":"autopkgtest -- null"},"aws-firehose":{"n":"aws-firehose","t":"Manage Amazon Data Firehose delivery streams","s":[["TAGLINE","Manage Amazon Data Firehose delivery streams"],["TLDR","**List all delivery streams**\n\n```aws firehose list-delivery-streams```\n\n**Describe a delivery stream**\n\n```aws firehose describe-delivery-stream --delivery-stream-name [stream_name]```\n\n**Create a delivery stream** to S3\n\n```aws firehose create-delivery-stream --delivery-stream-name [stream_name] --s3-destination-configuration RoleARN=[role_arn],BucketARN=[bucket_arn]```\n\n**Put a single record** to a delivery stream\n\n```aws firehose put-record --delivery-stream-name [stream_name] --record Data=[base64_encoded_data]```\n\n**Put multiple records** in a batch\n\n```aws firehose put-record-batch --delivery-stream-name [stream_name] --records Data=[data1] Data=[data2]```\n\n**Update delivery stream destination**\n\n```aws firehose update-destination --delivery-stream-name [stream_name] --current-delivery-stream-version-id [version] --destination-id [dest_id] --s3-destination-update BucketARN=[new_bucket_arn]```\n\n**Delete a delivery stream**\n\n```aws firehose delete-delivery-stream --delivery-stream-name [stream_name]```"],["SYNOPSIS","**aws firehose** _subcommand_ [_options_]"],["DESCRIPTION","**aws firehose** is a subcommand of the AWS CLI that manages Amazon Data Firehose (formerly Kinesis Data Firehose), a service for loading streaming data into data stores and analytics services.\n\nFirehose automatically batches, compresses, transforms, and encrypts data before delivering it to destinations like S3, Redshift, OpenSearch, Splunk, or HTTP endpoints. It handles scaling automatically without provisioning.\n\nData can be sent directly via put-record APIs or ingested from Kinesis Data Streams. Firehose buffers records based on size (1-128 MB) or time (60-900 seconds) before delivery."],["PARAMETERS","**list-delivery-streams**\n> List all delivery streams in the account.\n\n**describe-delivery-stream**\n> Get detailed configuration of a stream.\n\n**create-delivery-stream**\n> Create a new delivery stream.\n\n**delete-delivery-stream**\n> Remove a delivery stream.\n\n**put-record**\n> Send a single data record.\n\n**put-record-batch**\n> Send multiple records in one request.\n\n**update-destination**\n> Modify destination configuration.\n\n**start-delivery-stream-encryption**\n> Enable server-side encryption.\n\n**stop-delivery-stream-encryption**\n> Disable encryption.\n\n**--delivery-stream-name** _name_\n> Name of the delivery stream.\n\n**--record** _data_\n> Single record with Data field (base64).\n\n**--records** _records_\n> Array of records for batch put.\n\n**--s3-destination-configuration** _config_\n> S3 destination settings.\n\n**--redshift-destination-configuration** _config_\n> Redshift destination settings.\n\n**--extended-s3-destination-configuration** _config_\n> S3 with data transformation settings."],["CAVEATS","Record data must be base64-encoded when using the CLI. Maximum record size is 1 MB. Batch put accepts up to 500 records or 4 MB per request. Delivery streams cannot be renamed; create a new one instead. Buffer intervals cause delivery latency. Failed records to Redshift are written to S3 as a backup."],["HISTORY","**Amazon Kinesis Firehose** launched in **October 2015** as the easiest way to load streaming data into AWS. It was renamed to **Amazon Data Firehose** in **February 2024** to reflect its broader scope beyond Kinesis integration. Over time, destinations expanded from S3 and Redshift to include OpenSearch, Splunk, and custom HTTP endpoints. Dynamic partitioning was added in **2021** to enable efficient data lake patterns."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-kinesis](/man/aws-kinesis)(1), [aws-s3](/man/aws-s3)(1), [aws-redshift](/man/aws-redshift)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/firehose/index.html)```"]],"e":["aws","aws-kinesis","aws-s3","aws-redshift"],"x":"aws firehose list-delivery-streams"},"bcd":{"n":"bcd","t":"Reformat input as a punched card","s":[["TAGLINE","Reformat input as a punched card"],["TLDR","**Print text** as a punched card\n\n```bcd [Hello]```\n\n**Read text** from standard input\n\n```echo [hello world] | bcd```\n\n**Decode** a punched card back to text\n\n```bcd -d < [card.txt]```"],["SYNOPSIS","**bcd** [_-l_] [_string_...]\n\n**bcd** **-d** [_-l_]"],["DESCRIPTION","**bcd** reads the given input and reformats it as a visual representation of an IBM-style punched card, drawn with ASCII art. It is one of the classic novelty filters from the BSD games collection, alongside **ppt** (paper tape) and **morse** (Morse code).\n\nInput may be supplied as command-line arguments or on standard input. With no decode flag, the text is encoded into a punch card; only the first line is read when reading from standard input."],["PARAMETERS","**-d**\n> Decode a punched card on standard input back into ASCII text.\n\n**-l**\n> Produce or decode 80-column cards. The default is 48 columns."],["CAVEATS","This is a novelty program, not a data tool. Only the first line of input is encoded, and characters outside the supported set are ignored. It does not perform binary, decimal, or hexadecimal base conversion: for that use **bc** or **dc**."],["HISTORY","**bcd** first appeared in Version 6 AT&T UNIX and emulates the punched cards used by the GE 635 and GE 645 computers at Bell Labs in the late 1960s and early 1970s. It is distributed today as part of the **bsdgames** package."],["INSTALL","```dnf: sudo dnf install bcd```"],["SEE ALSO","[ppt](/man/ppt)(6), [morse](/man/morse)(6), [number](/man/number)(6), [bsdgames](/man/bsdgames)(6)"]],"e":["ppt","morse","number","bsdgames"],"x":"bcd [Hello]"},"bsdgames":{"n":"bsdgames","t":"Collection of classic BSD Unix text-based games","s":[["TAGLINE","Collection of classic BSD Unix text-based games"],["TLDR","**Play the Colossal Cave text adventure**\n\n```bsdgames-adventure```\n\n**Play Tetris clone**\n\n```tetris-bsd```\n\n**Play Snake game**\n\n```snake```\n\n**Play Worm (snake variant)**\n\n```worm```\n\n**Play hangman**\n\n```hangman```\n\n**Play text-based dungeon crawler**\n\n```hack```\n\n**Play backgammon against computer**\n\n```backgammon```\n\n**Show the answer to \"wtf\"** (abbreviation lookup)\n\n```wtf [lol]```"],["INCLUDED GAMES","**bsdgames-adventure**\n> Classic Colossal Cave text adventure (renamed from **adventure** to avoid clashes).\n\n**tetris-bsd**\n> Terminal-based Tetris clone.\n\n**snake** / **worm**\n> Classic snake games.\n\n**hangman**\n> Word guessing game.\n\n**hack**\n> Precursor to NetHack roguelike.\n\n**backgammon**\n> Classic board game.\n\n**battlestar**\n> Space-themed text adventure.\n\n**boggle**\n> Word search puzzle game.\n\n**canfield** / **cribbage**\n> Card games.\n\n**gomoku**\n> Five-in-a-row strategy game.\n\n**mille**\n> Card-based racing game.\n\n**phantasia**\n> Multiplayer fantasy game.\n\n**quiz**\n> Educational trivia game.\n\n**robots**\n> Strategy game escaping from robots.\n\n**sail**\n> Age of sail naval combat.\n\n**trek**\n> Star Trek themed strategy game.\n\n**wtf**\n> Look up common internet abbreviations.\n\n**wargames**\n> Nod to the 1983 film; prints a classic prompt on launch.\n\n**morse**, **caesar**, **rot13**, **primes**, **arithmetic**, **bcd**, **ppt**\n> Classic tiny Unix utilities shipped alongside the games."],["DESCRIPTION","**bsdgames** is a collection of classic text-based games originating from BSD Unix systems. These games represent decades of Unix gaming history, ranging from the earliest text adventures to terminal-based arcade games.\n\nThe collection includes puzzle games, card games, board games, text adventures, and action games - all playable in a terminal. Many influenced later games: Adventure pioneered interactive fiction, Hack led to NetHack and roguelikes, and Worm was an early snake game.\n\nGames typically use keyboard controls and ASCII graphics. Most include built-in help accessible via **?** or man pages. Configuration and high scores are often stored in the user's home directory."],["CAVEATS","Some games require a capable terminal emulator. Multiplayer games may not work without additional configuration. The exact games included vary by distribution packaging. Some games have different names on different systems (tetris vs tetris-bsd)."],["HISTORY","The BSD games collection evolved alongside Unix at **UC Berkeley** from the **1970s** onward. Many games were contributed by students and staff, becoming part of BSD Unix distributions. The collection represents a snapshot of early computer gaming when terminals were the primary interface. These games were open source before the term existed and have been preserved by the Linux community."],["INSTALL","```nix: nix profile install nixpkgs#bsdgames```"],["SEE ALSO","[nethack](/man/nethack)(6), [angband](/man/angband)(6), [cowsay](/man/cowsay)(1)"]],"e":["nethack","angband","cowsay"],"x":"bsdgames-adventure"},"cc":{"n":"cc","t":"system C compiler","s":[["TAGLINE","system C compiler"],["TLDR","This command is an alias of **gcc**."],["SYNOPSIS","**cc** [_options_] _files_..."],["DESCRIPTION","**cc** is traditionally the system C compiler command. On most Linux systems, it is a symbolic link or alias to **gcc** (GNU Compiler Collection).\n\nUsing **cc** in build scripts provides portability across systems where different compilers may be the default."],["INSTALL","```dnf: sudo dnf install gcc```\n\n```pacman: sudo pacman -S gcc```\n\n```apk: sudo apk add gcc```\n\n```zypper: sudo zypper install gcc```\n\n```brew: brew install gcc```\n\n```nix: nix profile install nixpkgs#gcc```"],["SEE ALSO","[gcc](/man/gcc)(1), [clang](/man/clang)(1), [make](/man/make)(1)"],["RESOURCES","```[Documentation](https://gcc.gnu.org/onlinedocs/)```"]],"e":["gcc","clang","make"],"x":""},"cockpit-desktop":{"n":"cockpit-desktop","t":"secure desktop access to Cockpit pages","s":[["TAGLINE","secure desktop access to Cockpit pages"],["TLDR","**Open** a Cockpit page\n\n```cockpit-desktop [url] [SSH_host]```\n\nOpen the **storage** page\n\n```cockpit-desktop /cockpit/@localhost/storage/index.html```"],["SYNOPSIS","**cockpit-desktop** [_url_] [_ssh_host_]"],["DESCRIPTION","**cockpit-desktop** provides secure access to Cockpit server management pages from within a local desktop session without exposing any network ports. It launches cockpit-ws and a web browser inside an isolated network namespace, along with cockpit-bridge in the user's session.\n\nThe network namespace isolation ensures that the Cockpit web service is only accessible to the locally spawned browser, eliminating the security surface of a listening network socket. This is ideal for single-user workstations where full Cockpit network service is unnecessary.\n\nAn optional SSH host argument allows managing remote machines through the local desktop interface, with the SSH connection providing the transport layer instead of a direct network service."],["PARAMETERS","_url_\n> The Cockpit page URL to open\n\n_ssh_host_\n> Optional SSH host for remote connection"],["CAVEATS","Part of the Cockpit suite. Requires a graphical desktop environment. Uses network namespaces for isolation, which requires appropriate kernel support."],["SEE ALSO","[cockpit-ws](/man/cockpit-ws)(8), [cockpit-bridge](/man/cockpit-bridge)(1)"],["RESOURCES","```[Source code](https://github.com/cockpit-project/cockpit)```\n\n```[Documentation](https://cockpit-project.org/guide/latest/)```"]],"e":["cockpit-ws","cockpit-bridge"],"x":"cockpit-desktop [url] [SSH_host]"},"configuration.nix":{"n":"configuration.nix","t":"NixOS system configuration file","s":[["TAGLINE","NixOS system configuration file"],["TLDR","**Edit system configuration**\n\n```sudo nano /etc/nixos/configuration.nix```\n\n**Rebuild system after changes**\n\n```sudo nixos-rebuild switch```\n\n**Test configuration without making it default**\n\n```sudo nixos-rebuild test```\n\n**Build but don't activate**\n\n```sudo nixos-rebuild build```\n\n**Rollback to previous generation**\n\n```sudo nixos-rebuild switch --rollback```\n\n**Check configuration syntax**\n\n```nix-instantiate --parse /etc/nixos/configuration.nix```\n\n**Basic configuration example**\n\n```nix\n{ config, pkgs, ... }:\n{\n  boot.loader.systemd-boot.enable = true;\n  networking.hostName = \"myhost\";\n  time.timeZone = \"America/New_York\";\n\n  users.users.alice = {\n    isNormalUser = true;\n    extraGroups = [ \"wheel\" ];\n  };\n\n  environment.systemPackages = with pkgs; [\n    vim git firefox\n  ];\n\n  services.openssh.enable = true;\n}\n```"],["SYNOPSIS","**/etc/nixos/configuration.nix**"],["COMMON OPTIONS","**boot.loader.systemd-boot.enable**\n> Enable systemd-boot bootloader.\n\n**networking.hostName**\n> Set system hostname.\n\n**time.timeZone**\n> Set system timezone.\n\n**users.users.<name>**\n> Define user accounts.\n\n**environment.systemPackages**\n> List of system-wide packages.\n\n**services.<name>.enable**\n> Enable system services.\n\n**programs.<name>.enable**\n> Enable system programs.\n\n**nixpkgs.config.allowUnfree**\n> Allow proprietary packages."],["DESCRIPTION","**configuration.nix** is the main system configuration file for NixOS, a Linux distribution built on the Nix package manager. It declaratively defines the entire system state: installed packages, enabled services, user accounts, and system settings.\n\nThe file uses the Nix expression language to describe system configuration. Changes are applied atomically via **nixos-rebuild**, which builds a new system generation that can be activated immediately or on next boot.\n\nNixOS stores previous configurations as generations, enabling easy rollback to any prior state. This approach ensures reproducible system configurations that can be version-controlled and shared."],["CAVEATS","Syntax errors prevent system rebuild; always test with **nixos-rebuild test** first. The Nix language has a learning curve. Some proprietary software requires explicit allowUnfree setting. Large configuration changes may require significant download and build time."],["HISTORY","NixOS was created by **Eelco Dolstra** based on his PhD research on the Nix package manager, with the first release in **2003**. The declarative configuration model was revolutionary, treating system configuration as code that produces reproducible results. NixOS has gained popularity in DevOps and development environments where reproducibility is valued."],["SEE ALSO","[nixos-rebuild](/man/nixos-rebuild)(8), [nix-env](/man/nix-env)(1), [nix](/man/nix)(1), [home-manager](/man/home-manager)(1)"],["RESOURCES","```[Source code](https://github.com/NixOS/nixpkgs)```\n\n```[Homepage](https://nixos.org)```\n\n```[Documentation](https://nixos.org/manual/nixos/stable/)```"]],"e":["nixos-rebuild","nix-env","nix","home-manager"],"x":"sudo nano /etc/nixos/configuration.nix"},"darktable-cli":{"n":"darktable-cli","t":"command-line interface for batch RAW photo processing","s":[["TAGLINE","command-line interface for batch RAW photo processing"],["TLDR","**Export a RAW file to JPEG**\n\n```darktable-cli [input.raw] [output.jpg]```\n\n**Export with specific XMP sidecar**\n\n```darktable-cli [input.raw] [preset.xmp] [output.jpg]```\n\n**Export as 16-bit TIFF**\n\n```darktable-cli [input.raw] [output.tiff] --bpp 16```\n\n**Export with specific dimensions**\n\n```darktable-cli [input.raw] [output.jpg] --width [1920] --height [1080]```\n\n**Export all RAWs in directory**\n\n```for f in *.raw; do darktable-cli \"$f\" \"${f%.raw}.jpg\"; done```\n\n**Apply style during export**\n\n```darktable-cli [input.raw] [output.jpg] --style \"[style_name]\"```"],["SYNOPSIS","**darktable-cli** [_options_] _input_ [_xmp_] _output_"],["PARAMETERS","**--width** _pixels_\n> Maximum output width.\n\n**--height** _pixels_\n> Maximum output height.\n\n**--bpp** _bits_\n> Bits per pixel (8, 16, 32).\n\n**--hq** _true_|_false_\n> High quality resampling.\n\n**--upscale** _true_|_false_\n> Allow upscaling.\n\n**--style** _name_\n> Apply named darktable style.\n\n**--style-overwrite**\n> Overwrite existing history with style.\n\n**--out-ext** _ext_\n> Output format extension (jpg, tif, png, etc.).\n\n**--core**\n> Pass options to darktable core.\n\n**--verbose**\n> Enable verbose output."],["DESCRIPTION","**darktable-cli** is the command-line interface for darktable, enabling batch processing of RAW photographs without the graphical interface. It applies darktable's non-destructive edits stored in XMP sidecar files to produce final images.\n\nThe tool uses the same processing engine as the GUI application, ensuring identical results. If an XMP file exists alongside the input (same name with .xmp extension), its edits are automatically applied. Explicit XMP files can also be specified.\n\nCommon uses include batch export of edited photos, automated processing pipelines, server-side image processing, and generating different output sizes from the same RAW file."],["CAVEATS","Requires OpenCL for GPU acceleration (same as GUI). Processing is single-threaded per image; use parallel for batch processing. Large RAW files require significant memory. XMP edits must be compatible with the darktable version used."],["HISTORY","darktable was created by **Johannes Hanika** and first released in **2009** as an open-source photography workflow application, inspired by Adobe Lightroom. The CLI interface was added to enable scripted processing and integration with other tools. darktable has grown to become one of the premier open-source RAW processing applications."],["INSTALL","```apt: sudo apt install darktable```\n\n```dnf: sudo dnf install darktable```\n\n```pacman: sudo pacman -S darktable```\n\n```apk: sudo apk add darktable```\n\n```zypper: sudo zypper install darktable```\n\n```nix: nix profile install nixpkgs#darktable```"],["SEE ALSO","[rawtherapee-cli](/man/rawtherapee-cli)(1), [dcraw](/man/dcraw)(1), [exiftool](/man/exiftool)(1)"]],"e":["rawtherapee-cli","dcraw","exiftool"],"x":"darktable-cli [input.raw] [output.jpg]"},"devenv":{"n":"devenv","t":"declarative development environments with Nix","s":[["TAGLINE","declarative development environments with Nix"],["TLDR","**Initialize a new project**\n\n```devenv init```\n\n**Enter the development shell**\n\n```devenv shell```\n\n**Start background processes** (databases, servers, etc.)\n\n```devenv up```\n\n**Start processes detached** in the background\n\n```devenv processes up```\n\n**Run a command in the environment**\n\n```devenv shell -- [command]```\n\n**Search for a package** in nixpkgs\n\n```devenv search [package_name]```\n\n**Update inputs and lockfile**\n\n```devenv update```\n\n**Run project tests**\n\n```devenv test```"],["SYNOPSIS","**devenv** _command_ [_options_]"],["SUBCOMMANDS","**init**\n> Initialize a new devenv project.\n\n**shell**\n> Enter the development shell.\n\n**up**\n> Start background processes.\n\n**processes up**\n> Start processes in the background.\n\n**processes down**\n> Stop background processes.\n\n**update**\n> Update devenv inputs and lockfile.\n\n**search** _name_\n> Search for packages in nixpkgs.\n\n**test**\n> Run project tests defined in devenv.nix.\n\n**container**\n> Build, copy, or run a container.\n\n**build**\n> Build any attribute in devenv.nix.\n\n**gc**\n> Garbage collect old shell generations.\n\n**info**\n> Show environment information.\n\n**repl**\n> Launch interactive REPL for inspecting configuration."],["DESCRIPTION","**devenv** is a development environment management tool built on Nix. It provides declarative, reproducible development environments with automatic dependency management.\n\nConfiguration is defined in devenv.nix, specifying packages, services, environment variables, and scripts. devenv simplifies Nix usage for development environments with a user-friendly API and pre-configured services like databases, language runtimes, and development tools.\n\nThe tool supports running services in the background, automatic environment activation with direnv integration, and deterministic builds through Nix's reproducibility guarantees."],["CONFIGURATION","**devenv.nix**\n> Main configuration file defining packages, languages, services, environment variables, scripts, and shell hooks.\n\n**devenv.yaml**\n> Configuration for Nix inputs (nixpkgs version, additional flakes) and project imports.\n\n**devenv.lock**\n> Lock file pinning exact input versions for reproducibility."],["CAVEATS","Requires Nix to be installed. First-time setup may take time to download dependencies. Nix store can consume significant disk space. Some proprietary software may not be available in Nix packages."],["INSTALL","```nix: nix profile install nixpkgs#devenv```"],["SEE ALSO","[nix](/man/nix)(1), [nix-shell](/man/nix-shell)(1), [direnv](/man/direnv)(1)"]],"e":["nix","nix-shell","direnv"],"x":"devenv init"},"env":{"n":"env","t":"run programs with modified environment variables","s":[["TAGLINE","run programs with modified environment variables"],["TLDR","**Show** all environment variables\n\n```env```\n\n**Run** command with environment\n\n```env VAR=value [command]```\n\n**Clear** environment\n\n```env -i [command]```\n\n**Unset** variable\n\n```env -u [VAR] [command]```"],["SYNOPSIS","**env** [_options_] [_VAR=value_]... [_command_]"],["DESCRIPTION","**env** runs a program in a modified environment. It can display current environment variables, set new ones, unset existing ones, or run commands with a clean environment.\n\nThe command is useful for setting variables temporarily or debugging environment issues."],["PARAMETERS","**-i**, **--ignore-environment**\n> Start with empty environment\n\n**-u** _var_, **--unset=**_var_\n> Remove variable from environment\n\n**-0**, **--null**\n> End lines with NUL, not newline\n\n**-C** _dir_, **--chdir=**_dir_\n> Change directory before running command\n\n**-S** _string_\n> Process and split string into arguments (useful in shebang lines)\n\n**-v**, **--verbose**\n> Print verbose information for each processing step"],["WORKFLOW","```bash"],["Display all environment variables","env"],["Set variable for one command","env DEBUG=1 ./program"],["Multiple variables","env VAR1=value1 VAR2=value2 command"],["Clear environment","env -i command"],["Clean environment with specific variables","env -i PATH=/usr/bin HOME=/home/user command"],["Unset variable","env -u DISPLAY command"],["Change directory first","env -C /tmp ls"],["In shebang for portability","#!/usr/bin/env python3\n```"],["COMMON USES","**Shebang lines:**\n```bash\n#!/usr/bin/env bash\n#!/usr/bin/env python3\n#!/usr/bin/env node\n```\n\n**Temporary variables:**\n```bash\nenv NODE_ENV=production npm start\nenv DATABASE_URL=postgres://... rails console\n```\n\n**Clean environment:**\n```bash\nenv -i PATH=\"$PATH\" command\n```\n\n**Debugging:**\n```bash\nenv | grep PATH\nenv | sort\n```"],["VARIABLE FORMAT","Variables shown as:\n```\nNAME=value\nPATH=/usr/bin:/bin\nHOME=/home/user\n```"],["COMPARISON WITH EXPORT","**env**\n> Runs command with modified environment\n\n**export**\n> Sets variables for current shell and children\n\n```bash"],["env (one command only)","env VAR=value command"],["export (persistent)","export VAR=value\ncommand  # VAR is available\n```"],["CAVEATS","Variables set with env don't persist after command exits. Shell built-ins (like `cd`, `alias`) cannot be run via env since it uses `execvp`. When using `-i`, PATH must be explicitly set for commands to be found. Values containing spaces must be quoted. The `-C` and `-S` options are GNU extensions not available on all platforms."],["HISTORY","**env** has been part of Unix since the early days, included in POSIX standards for environment manipulation."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils-env```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[export](/man/export)(1), [printenv](/man/printenv)(1), [set](/man/set)(1)"]],"e":["export","printenv","set"],"x":"env"},"envx":{"n":"envx","t":"Environment variable management tool","s":[["TAGLINE","Environment variable management tool"],["TLDR","**Load** environment from .env file\n\n```envx load [.env]```\n\n**Export** current environment to file\n\n```envx export [filename.env]```\n\n**Run** command with specific environment\n\n```envx run -e [KEY=VALUE] -- [command]```\n\n**Check** for required variables\n\n```envx check [REQUIRED_VAR1] [REQUIRED_VAR2]```"],["SYNOPSIS","**envx** [_command_] [_options_] [_arguments_]"],["PARAMETERS","**load** [_FILE_]\n> Load environment variables from file\n\n**export** [_FILE_]\n> Export current environment to file\n\n**run** [_options_] -- _COMMAND_\n> Run command with modified environment\n\n**check** _VARIABLES_\n> Verify required variables are set\n\n**-e, --env** _KEY=VALUE_\n> Set environment variable\n\n**-f, --file** _FILE_\n> Specify environment file\n\n**--expand**\n> Expand variable references in values\n\n**--override**\n> Override existing variables\n\n**-v, --version**\n> Display version and exit\n\n**-h, --help**\n> Display help and exit"],["DESCRIPTION","**envx** is an environment variable management tool that provides utilities for loading, exporting, and manipulating environment configurations. It supports .env files, variable expansion, and temporary environment modifications.\n\nThe tool is useful for managing environment configurations across different environments (development, staging, production) and ensuring required variables are present before running applications."],["CAVEATS","Variable expansion may have security implications with untrusted input. .env file format may vary between tools. System environment variables are protected from modification in some cases."],["HISTORY","**envx** was created to provide a consistent way to manage environment variables across projects and environments."],["INSTALL","```aur: yay -S envx```"],["SEE ALSO","[env](/man/env)(1), [direnv](/man/direnv)(1)"]],"e":["env","direnv"],"x":"envx load [.env]"},"faketime":{"n":"faketime","t":"system time manipulation for testing","s":[["TAGLINE","system time manipulation for testing"],["TLDR","Fake time to **this evening**\n\n```faketime '[today 23:30]' [date]```\n\nOpen shell with **yesterday** as current date\n\n```faketime '[yesterday]' [bash]```\n\nSimulate how program would act **next Friday**\n\n```faketime '[next Friday 1 am]' [path/to/program]```"],["SYNOPSIS","**faketime** [_options_] _timestamp_ _command_"],["DESCRIPTION","**faketime** runs a command with a fake system time by intercepting time-related system calls. It uses LD_PRELOAD to wrap functions like time(), gettimeofday(), and clock_gettime(), returning a user-specified time instead of the real system time.\n\nThis is useful for testing time-dependent software, debugging date-related bugs, simulating future or past conditions, and testing expiration or scheduling logic. The timestamp accepts absolute dates (\"YYYY-MM-DD hh:mm:ss\") as well as natural-language and relative descriptions parsed via GNU date.\n\nGiving an absolute timestamp freezes the clock at that instant: repeated time queries return the same value. The advanced format (**-f**) instead lets time advance, run faster or slower, or step forward on each call."],["PARAMETERS","_timestamp_\n> Time specification (absolute date, natural language, or, with -f, an advanced offset/speed spec).\n\n_command_\n> Command to run with the faked time.\n\n**-f**\n> Use the advanced timestamp specification format (offsets, per-call increments, and clock-speed multipliers).\n\n**-m**\n> Use the multi-threading variant of libfaketime.\n\n**--exclude-monotonic**\n> Do not fake CLOCK_MONOTONIC queries.\n\n**--date-prog** _path_\n> Use an alternate GNU date-compatible program to parse the timestamp."],["CAVEATS","Uses LD_PRELOAD, so it may not work with statically linked binaries or setuid programs. To freeze time, give an absolute timestamp; to let it advance or scale, use the -f advanced format."],["INSTALL","```apt: sudo apt install faketime```\n\n```dnf: sudo dnf install libfaketime```\n\n```pacman: sudo pacman -S libfaketime```\n\n```apk: sudo apk add libfaketime```\n\n```zypper: sudo zypper install libfaketime```\n\n```brew: brew install libfaketime```\n\n```nix: nix profile install nixpkgs#libfaketime```"],["SEE ALSO","[date](/man/date)(1), [timedatectl](/man/timedatectl)(1)"],["RESOURCES","```[Source code](https://github.com/wolfcw/libfaketime)```"]],"e":["date","timedatectl"],"x":"faketime '[today 23:30]' [date]"},"firejail":{"n":"firejail","t":"application sandboxing with Linux namespaces","s":[["TAGLINE","application sandboxing with Linux namespaces"],["TLDR","**Integrate** firejail with desktop environment\n\n```sudo firecfg```\n\nOpen a **restricted** Firefox\n\n```firejail [firefox]```\n\nStart restricted Apache on specific **network**\n\n```firejail --net=[eth0] --ip=[192.168.1.244] [/etc/init.d/apache2] [start]```\n\n**List** running sandboxes\n\n```firejail --list```\n\nList **network activity** from sandboxes\n\n```firejail --netstats```\n\n**Shutdown** a running sandbox\n\n```firejail --shutdown=[7777]```\n\nRun **highly restricted** Firefox session\n\n```firejail --seccomp --private --private-dev --private-tmp --protocol=inet firefox --new-instance --no-remote --safe-mode --private-window```\n\nUse **custom hosts** file\n\n```firejail --hosts-file=[~/myhosts] [curl http://mysite.arpa]```"],["SYNOPSIS","**firejail** [_options_] [_program_] [_arguments_]"],["DESCRIPTION","**firejail** securely sandboxes processes using Linux namespaces, seccomp-bpf, and capabilities. It isolates applications from the rest of the system for security.\n\nProvides per-application profiles for common programs like Firefox and VLC."],["PARAMETERS","**--list**\n> List running sandboxes\n\n**--netstats**\n> Show network activity\n\n**--shutdown** _pid_\n> Stop sandbox by PID\n\n**--net** _interface_\n> Use network namespace\n\n**--ip** _address_\n> Assign IP address\n\n**--seccomp**\n> Enable seccomp filtering\n\n**--private**\n> Use private home directory\n\n**--private-tmp**\n> Use private /tmp\n\n**--private-dev**\n> Use private /dev"],["CONFIGURATION","**/etc/firejail/[application].profile**\n> Application-specific sandboxing profiles defining restrictions and permissions.\n\n**~/.config/firejail/[application].profile**\n> User-level custom profiles that override system defaults."],["CAVEATS","Some applications may not work correctly in sandbox. Profiles in /etc/firejail/. Use firecfg to integrate with desktop."],["INSTALL","```apt: sudo apt install firejail```\n\n```dnf: sudo dnf install firejail```\n\n```pacman: sudo pacman -S firejail```\n\n```zypper: sudo zypper install firejail```\n\n```nix: nix profile install nixpkgs#firejail```"],["SEE ALSO","[bubblewrap](/man/bubblewrap)(1), [systemd-nspawn](/man/systemd-nspawn)(1)"],["RESOURCES","```[Source code](https://github.com/netblue30/firejail)```"]],"e":["bubblewrap","systemd-nspawn"],"x":"sudo firecfg"},"getopt":{"n":"getopt","t":"command-line option parser for shell scripts","s":[["TAGLINE","command-line option parser for shell scripts"],["TLDR","Parse optional **verbose/version flags** with shorthands\n\n```getopt -o vV -l verbose,version -- --version --verbose```\n\nAdd a **--file option** with a required argument\n\n```getopt -o f: -l file: -- --file=somefile```\n\nAdd a **--verbose option** with an optional argument and pass a non-option parameter\n\n```getopt -o v:: -l verbose:: -- --verbose arg```\n\nParse **multiple options** with various argument requirements\n\n```getopt -o rv::t: -l verbose,source::,target: -- -v --target target```"],["SYNOPSIS","**getopt** [_options_] [**--**] _parameters_"],["PARAMETERS","**-o**, **--options** _SHORT_\n> Short options to recognize (single letters). Colon after letter means required argument, double colon means optional.\n\n**-l**, **--longoptions** _LONG_\n> Long options to recognize (comma-separated). Colon rules same as short options.\n\n**-n**, **--name** _NAME_\n> Name to use in error messages\n\n**-q**, **--quiet**\n> Suppress error messages\n\n**-a**, **--alternative**\n> Allow long options with single dash\n\n**-T**, **--test**\n> Test for enhanced getopt"],["DESCRIPTION","**getopt** parses command-line options for shell scripts, handling both short (-v) and long (--verbose) option formats. It normalizes option order, handles bundled short options (-abc), and separates options from arguments.\n\nThe enhanced getopt (util-linux version) supports long options, optional arguments, and proper handling of arguments with spaces. Use **getopt -T** to test if the enhanced version is available.\n\nOutput is typically evaluated with **eval set --** to update the script's positional parameters with the normalized options."],["OPTION ARGUMENT SYNTAX","In the options string:\n- **f** - flag with no argument\n- **f:** - option with required argument\n- **f::** - option with optional argument"],["CAVEATS","The original BSD getopt is limited and doesn't support long options. Always test for enhanced getopt with **-T**. Optional arguments must be attached directly (--opt=arg, not --opt arg)."],["HISTORY","getopt dates back to early Unix systems. The enhanced version in util-linux was developed to overcome limitations of the original, adding long option support and proper quoting for arguments with special characters."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[getopts](/man/getopts)(1), [bash](/man/bash)(1)"],["RESOURCES","```[Source code](https://github.com/util-linux/util-linux)```\n\n```[Documentation](https://www.man7.org/linux/man-pages/man1/getopt.1.html)```"]],"e":["getopts","bash"],"x":"getopt -o vV -l verbose,version -- --version --verbose"},"gh-pr":{"n":"gh-pr","t":"Manage GitHub pull requests from the command line","s":[["TAGLINE","Manage GitHub pull requests from the command line"],["TLDR","**Create a pull request**\n\n```gh pr create```\n\n**List open pull requests**\n\n```gh pr list```\n\n**Check out a PR branch**\n\n```gh pr checkout [pr-number]```\n\n**View PR in browser**\n\n```gh pr view --web```\n\n**Merge a pull request**\n\n```gh pr merge [pr-number]```"],["SYNOPSIS","**gh pr** _command_ [_options_]"],["PARAMETERS","**create**\n> Create a new pull request.\n\n**list**\n> List pull requests.\n\n**checkout**\n> Check out a PR branch locally.\n\n**view**\n> View pull request details.\n\n**merge**\n> Merge a pull request.\n\n**close**\n> Close a pull request.\n\n**reopen**\n> Reopen a pull request.\n\n**ready**\n> Mark PR as ready for review.\n\n**checks**\n> Show CI status checks.\n\n**review**\n> Add a review to a PR.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gh pr** manages GitHub pull requests from the command line. It provides full pull request lifecycle management including creation, review, merging, and CI status monitoring.\n\nThe command integrates with git workflows, automatically detecting the current branch's pull request context. It supports interactive creation with editor integration and automated creation for scripted workflows."],["CAVEATS","Requires repository write access for most operations. PR creation needs branch pushed to remote. Merge may require approvals or passing checks."],["HISTORY","gh pr is a core command in the **GitHub CLI**, providing the pull request workflows that are central to GitHub's collaboration model."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-pr-merge](/man/gh-pr-merge)(1), [gh-pr-checks](/man/gh-pr-checks)(1)"],["RESOURCES","```[Documentation](https://cli.github.com/manual/gh_pr)```\n\n```[Source code](https://github.com/cli/cli)```"]],"e":["gh","gh-pr-merge","gh-pr-checks"],"x":"gh pr create"},"git-diff-files":{"n":"git-diff-files","t":"Compare working tree files to index","s":[["TAGLINE","Compare working tree files to index"],["TLDR","**Show unstaged changes**\n\n```git diff-files```\n\n**Show for specific file**\n\n```git diff-files [file]```\n\n**Raw output**\n\n```git diff-files --raw```\n\n**Check for changes**\n\n```git diff-files --quiet```"],["SYNOPSIS","**git** **diff-files** [_options_] [_files_...]"],["PARAMETERS","**--raw**\n> Raw diff format (default).\n\n**--quiet**\n> Exit with 1 if there are differences, 0 otherwise. Disables output.\n\n**-p**, **--patch**\n> Generate patch output.\n\n**--stat**\n> Show diffstat summary.\n\n**--name-only**\n> Show only names of changed files.\n\n**--name-status**\n> Show names and status (added, modified, deleted) of changed files.\n\n**-0**\n> Omit diff output for unmerged entries, just show \"Unmerged\"."],["DESCRIPTION","**git diff-files** compares the working tree with the index (staging area), operating as a low-level plumbing command that shows unstaged changes. It is used internally by git diff when invoked without arguments and provides the foundation for higher-level diff operations.\n\nThis command is part of Git's plumbing layer, designed for scripting and automation rather than daily interactive use. It can produce various output formats including raw diff data, patch format, or simple file lists, making it suitable for integration into build tools, pre-commit hooks, or custom Git workflows."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-diff](/man/git-diff)(1), [git-diff-index](/man/git-diff-index)(1), [git-diff-tree](/man/git-diff-tree)(1)"]],"e":["git-diff","git-diff-index","git-diff-tree"],"x":"git diff-files"},"git-fork":{"n":"git-fork","t":"Fork GitHub repository and clone locally","s":[["TAGLINE","Fork GitHub repository and clone locally"],["TLDR","**Fork and clone repository by URL**\n\n```git fork [https://github.com/owner/repo]```\n\n**Fork and clone repository by slug**\n\n```git fork [owner/repo]```\n\n**Fork the current repo (run inside an existing clone)**\n\n```git fork```"],["SYNOPSIS","**git fork** [_url_]"],["PARAMETERS","_URL_\n> Repository URL to fork.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git fork** forks a GitHub repository and clones it locally, automating the common workflow of forking a project for contribution. Given a URL or `owner/repo` slug, it forks the repo via the GitHub API, clones the new fork into the current directory, and adds the original as a remote named **upstream**. Run with no argument inside an existing clone of a GitHub repo, it forks that repo, renames the existing **origin** remote to **upstream**, and adds the fork as the new **origin**. Remotes use SSH if configured, otherwise HTTPS.\n\nPart of the git-extras suite, this command streamlines the fork-and-clone pattern that is fundamental to open-source collaboration on GitHub."],["CONFIGURATION","**git-extras.github-personal-access-token**\n> Personal access token used to call the GitHub API; set globally with **git config --global --add git-extras.github-personal-access-token \\<token\\>**, or per-repo to override for a different account."],["CAVEATS","Part of git-extras package. Requires a GitHub personal access token with the appropriate OAuth scopes. GitHub-specific functionality only."],["HISTORY","git fork is part of **git-extras**, created to automate the common GitHub fork workflow for contributing to projects."],["INSTALL","```nix: nix profile install nixpkgs#git-fork```"],["SEE ALSO","[git-clone](/man/git-clone)(1), [gh-repo](/man/gh-repo)(1), [git-remote](/man/git-remote)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/man/git-fork.md)```"]],"e":["git-clone","gh-repo","git-remote"],"x":"git fork [https://github.com/owner/repo]"},"git-sync":{"n":"git-sync","t":"Hard-reset a branch to match its remote, cleaning untracked files","s":[["TAGLINE","Hard-reset a branch to match its remote, cleaning untracked files"],["TLDR","**Sync current branch** with its upstream\n\n```git sync```\n\n**Sync with a specific remote and branch**\n\n```git sync [origin] [main]```\n\n**Sync but keep untracked/ignored files**\n\n```git sync --soft```\n\n**Sync without a confirmation prompt**\n\n```git sync --force```"],["SYNOPSIS","**git** **sync** [_remote_ _branch_] [_-s_ | _--soft_] [_-f_ | _--force_]"],["PARAMETERS","_REMOTE_ _BRANCH_\n> Remote and branch to sync with; defaults to the current branch's configured upstream.\n\n**-s**, **--soft**\n> Skip `git clean -d -f -x` after resetting, keeping untracked and ignored files.\n\n**-f**, **--force**\n> Skip the confirmation prompt.\n\n**-h**, **--help**\n> Display usage information."],["DESCRIPTION","**git sync** is a git-extras command that fetches the given (or upstream-tracked) remote branch and then runs `git reset --hard` to make the current branch match it exactly. Unless `-s`/`--soft` is given, it also runs `git clean -d -f -x` afterward, deleting every untracked and ignored file in the working tree.\n\nIt asks for confirmation before running unless `-f`/`--force` is passed."],["CAVEATS","This is destructive: local commits not on the remote, uncommitted changes, and (without `-s`) untracked/ignored files are all discarded. It is not a fetch-and-rebase or fetch-and-merge; unlike `git pull`, divergent local work is thrown away rather than integrated."],["INSTALL","```zypper: sudo zypper install git-sync```\n\n```brew: brew install git-sync```\n\n```nix: nix profile install nixpkgs#git-sync```"],["SEE ALSO","[git-fetch](/man/git-fetch)(1), [git-reset](/man/git-reset)(1), [git-clean](/man/git-clean)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-sync)```"]],"e":["git-fetch","git-reset","git-clean","git-extras"],"x":"git sync"},"gofumpt":{"n":"gofumpt","t":"Stricter Go code formatter","s":[["TAGLINE","Stricter Go code formatter"],["TLDR","**Format a Go file and print result to stdout**\n\n```gofumpt [file.go]```\n\n**Format a file in place**\n\n```gofumpt -w [file.go]```\n\n**Format all Go files in the current directory recursively**\n\n```gofumpt -w .```\n\n**Show a diff of formatting changes without applying**\n\n```gofumpt -d [file.go]```\n\n**Format with extra opinionated rules**\n\n```gofumpt -extra [file.go]```\n\n**Read from stdin and format**\n\n```cat [file.go] | gofumpt```"],["SYNOPSIS","**gofumpt** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Go files to format.\n\n**-w**\n> Write result to the source file instead of stdout.\n\n**-d**\n> Display diffs instead of rewriting files.\n\n**-e**\n> Report all errors (not just the first 10 per file).\n\n**-extra**\n> Enable extra formatting rules (e.g., enforce short case bodies on one line).\n\n**-l**\n> List files whose formatting differs from gofumpt's.\n\n**-version**\n> Print the version and exit."],["DESCRIPTION","**gofumpt** is a stricter alternative to gofmt that applies additional formatting rules beyond the standard Go style. It removes unnecessary blank lines, enforces consistent grouping, and applies other style preferences while remaining a superset of gofmt.\n\nThe **-extra** flag enables even more opinionated rules for tighter code formatting."],["CAVEATS","Stricter than gofmt -- code formatted by gofumpt is always valid gofmt output, but not vice versa. May conflict with project conventions. Most editors support gofumpt as a drop-in replacement for gofmt via settings."],["HISTORY","gofumpt was created by **Daniel Marti** to provide stricter formatting than standard gofmt."],["INSTALL","```pacman: sudo pacman -S gofumpt```\n\n```brew: brew install gofumpt```\n\n```nix: nix profile install nixpkgs#gofumpt```"],["SEE ALSO","[gofmt](/man/gofmt)(1), [go-fmt](/man/go-fmt)(1)"]],"e":["gofmt","go-fmt"],"x":"gofumpt [file.go]"},"gotelemetry":{"n":"gotelemetry","t":"Go toolchain telemetry management","s":[["TAGLINE","Go toolchain telemetry management"],["TLDR","**View telemetry config**\n\n```gotelemetry```\n\n**Enable telemetry**\n\n```gotelemetry on```\n\n**Disable telemetry**\n\n```gotelemetry off```\n\n**Set to local mode**\n\n```gotelemetry local```\n\n**View collected data**\n\n```gotelemetry view```"],["SYNOPSIS","**gotelemetry** [_command_]"],["PARAMETERS","**on**\n> Enable telemetry uploads.\n\n**off**\n> Disable telemetry.\n\n**local**\n> Collect locally only.\n\n**view**\n> Start a local web viewer showing charts of collected telemetry data.\n\n**env**\n> Print the current telemetry environment (mode and data directory).\n\n**clean**\n> Remove all local telemetry data.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gotelemetry** manages Go toolchain telemetry settings. It controls whether usage statistics and crash reports are collected and, optionally, uploaded to help the Go team prioritize improvements to the toolchain.\n\nRunning the command with no arguments shows the current mode. Three modes are supported: **local** (default) collects counters and reports on disk but never uploads them, **on** additionally uploads approved counters to telemetry.go.dev, and **off** disables collection entirely. The **view** command renders the locally stored reports as charts in a browser.\n\nSince Go 1.23, the same functionality is also exposed via **go telemetry** [_on|off|local_] as a subcommand of the go tool, without needing the separate gotelemetry binary."],["CAVEATS","Telemetry is opt-in for uploading; only counters approved through a public proposal process are ever uploaded, and stack traces or other sensitive data are excluded. Introduced with Go 1.21 (local-only), with upload support added in Go 1.23."],["HISTORY","gotelemetry was introduced with **Go 1.21** to enable opt-in telemetry collection for improving the Go toolchain, with the **go telemetry** subcommand and upload capability landing in **Go 1.23**."],["SEE ALSO","[go](/man/go)(1)"],["RESOURCES","```[Source code](https://go.googlesource.com/telemetry)```\n\n```[Documentation](https://go.dev/doc/telemetry)```"]],"e":["go"],"x":"gotelemetry"},"gprolog":{"n":"gprolog","t":"GNU Prolog compiler and interpreter","s":[["TAGLINE","GNU Prolog compiler and interpreter"],["TLDR","**Start GNU Prolog interpreter**\n\n```gprolog```\n\n**Load and run file**\n\n```gprolog --consult-file [program.pl]```\n\n**Compile to executable**\n\n```gplc [program.pl] -o [program]```\n\n**Run query from command line**\n\n```gprolog --query-goal \"[goal]\"```"],["SYNOPSIS","**gprolog** [_options_]\n\n**gplc** [_options_] _files_"],["PARAMETERS","**--consult-file** _file_\n> Load Prolog file at startup.\n\n**--query-goal** _goal_\n> Execute goal after loading.\n\n**--init-goal** _goal_\n> Execute initialization goal.\n\n**--entry-goal** _goal_\n> Entry point goal.\n\n**--no-top-level**\n> Exit after executing goals."],["DESCRIPTION","**GNU Prolog** is a Prolog compiler with constraint solving over finite domains. It compiles Prolog to native code and provides an interactive interpreter.\n\nGNU Prolog supports ISO Prolog with extensions for constraint logic programming, making it suitable for combinatorial problems, scheduling, and AI applications."],["INTERPRETER COMMANDS","```prolog\n?- consult('file.pl').    % Load file\n?- listing.               % Show predicates\n?- halt.                  % Exit\n?- trace.                 % Enable tracing\n```"],["COMPILING","```bash"],["Compile to executable","gplc program.pl -o program"],["Produce WAM file","gplc -W program.pl"],["Strip executable","gplc program.pl -o program -s"],["Compile without top-level","gplc --no-top-level program.pl -o program\n```"],["CAVEATS","Syntax differs slightly from SWI-Prolog. Native compilation adds startup complexity. Constraint solving requires finite domain declarations. Interactive debugging requires trace mode."],["HISTORY","GNU Prolog was created by **Daniel Diaz** at INRIA, first released in **1996**. It emphasizes native code compilation and constraint solving, distinguishing it from interpreted Prolog systems."],["INSTALL","```dnf: sudo dnf install gprolog```\n\n```nix: nix profile install nixpkgs#gprolog```"],["SEE ALSO","[swipl](/man/swipl)(1), [sicstus](/man/sicstus)(1)"]],"e":["swipl","sicstus"],"x":"gprolog"},"hcitool":{"n":"hcitool","t":"bluetooth Host Controller Interface tool for monitoring, configuring","s":[["TAGLINE","bluetooth Host Controller Interface tool for monitoring, configuring"],["TLDR","**Scan** for Bluetooth devices\n\n```hcitool scan```\n\nScan for **Bluetooth Low Energy (BLE)** devices\n\n```hcitool lescan```\n\nOutput the **name** of a device by MAC address\n\n```hcitool name [bdaddr]```\n\nFetch **information** about a remote Bluetooth device\n\n```hcitool info [bdaddr]```\n\nCheck the **link quality** to a Bluetooth device\n\n```hcitool lq [bdaddr]```\n\nModify the **transmit power** level\n\n```hcitool tpl [bdaddr] [0|1]```\n\nDisplay the **link policy**\n\n```hcitool lp```\n\nRequest **authentication** with a specific device\n\n```hcitool auth [bdaddr]```\n\nDisplay **local devices**\n\n```hcitool dev```"],["SYNOPSIS","**hcitool** [_options_] _command_ [_command-parameters_]"],["PARAMETERS","**-i** _hciX_\n> Use the specified HCI device instead of the first available one.\n\n**scan**\n> Scan for discoverable Bluetooth devices\n\n**lescan**\n> Scan for Bluetooth Low Energy (BLE) devices\n\n**dev**\n> List local Bluetooth adapters\n\n**name** _bdaddr_\n> Get remote device name\n\n**info** _bdaddr_\n> Get remote device information\n\n**lq** _bdaddr_\n> Get link quality\n\n**tpl** _bdaddr_ _type_\n> Get/set transmit power level\n\n**lp**\n> Display link policy\n\n**auth** _bdaddr_\n> Request authentication\n\n**con**\n> Display active connections\n\n**cc** _bdaddr_\n> Create connection\n\n**dc** _bdaddr_\n> Disconnect"],["DESCRIPTION","**hcitool** is a Bluetooth Host Controller Interface tool for monitoring, configuring connections, and sending commands to Bluetooth devices. It provides low-level access to Bluetooth functionality for diagnostics and advanced configuration.\n\nThe tool can discover nearby devices, query device information, manage connections, and configure link parameters. It operates at the HCI layer, below the higher-level profiles like A2DP or HFP."],["CAVEATS","Deprecated in favor of bluetoothctl in modern BlueZ versions. May require root privileges for some operations. Some features depend on Bluetooth adapter capabilities."],["HISTORY","hcitool is part of the BlueZ Bluetooth protocol stack for Linux. While functional, it's considered legacy and bluetoothctl from BlueZ 5 is recommended for most operations on modern systems."],["INSTALL","```apt: sudo apt install bluez```\n\n```dnf: sudo dnf install bluez```\n\n```pacman: sudo pacman -S bluez```\n\n```apk: sudo apk add bluez-deprecated```\n\n```zypper: sudo zypper install bluez```\n\n```brew: brew install bluez```\n\n```nix: nix profile install nixpkgs#bluez```"],["SEE ALSO","[bluetoothctl](/man/bluetoothctl)(1), [hciconfig](/man/hciconfig)(1), [rfkill](/man/rfkill)(8)"],["RESOURCES","```[Source code](https://github.com/bluez/bluez/blob/master/tools/hcitool.c)```"]],"e":["bluetoothctl","hciconfig","rfkill"],"x":"hcitool scan"},"ical":{"n":"ical","t":"command-line tool for working with iCalendar files","s":[["TAGLINE","command-line tool for working with iCalendar files"],["TLDR","**Display calendar**\n\n```ical [calendar.ics]```\n\n**List events**\n\n```ical -l [calendar.ics]```\n\n**Show events for today**\n\n```ical -t [calendar.ics]```\n\n**Export to different format**\n\n```ical -o [output.txt] [calendar.ics]```"],["SYNOPSIS","**ical** [_options_] [_file_]"],["PARAMETERS","**-l**\n> List events.\n\n**-t**\n> Show today's events.\n\n**-o** _file_\n> Output to file."],["DESCRIPTION","**ical** is a command-line tool for working with iCalendar (.ics) files. It can display, parse, and manipulate calendar data in the standard iCalendar format used by most calendar applications."],["INSTALL","```apt: sudo apt install itools```\n\n```nix: nix profile install nixpkgs#itools```"],["SEE ALSO","[calcurse](/man/calcurse)(1), [khal](/man/khal)(1), [remind](/man/remind)(1)"]],"e":["calcurse","khal","remind"],"x":"ical [calendar.ics]"},"ifconfig":{"n":"ifconfig","t":"configures network interfaces","s":[["TAGLINE","configures network interfaces"],["TLDR","**Show all interfaces**\n\n```ifconfig```\n\n**Show specific interface**\n\n```ifconfig [eth0]```\n\n**Set IP address**\n\n```sudo ifconfig [eth0] [192.168.1.100]```\n\n**Enable interface**\n\n```sudo ifconfig [eth0] up```\n\n**Disable interface**\n\n```sudo ifconfig [eth0] down```\n\n**Set netmask**\n\n```sudo ifconfig [eth0] netmask [255.255.255.0]```"],["SYNOPSIS","**ifconfig** [_interface_] [_options_]"],["PARAMETERS","_INTERFACE_\n> Network interface name.\n\n**up**\n> Activate interface.\n\n**down**\n> Deactivate interface.\n\n**netmask** _MASK_\n> Set network mask.\n\n**broadcast** _ADDR_\n> Set broadcast address.\n\n**mtu** _SIZE_\n> Set MTU size.\n\n**-a**\n> Display all interfaces, even if down.\n\n**hw** _class_ _address_\n> Set hardware (MAC) address (e.g. `hw ether AA:BB:...`).\n\n**promisc** / **-promisc**\n> Enable or disable promiscuous mode.\n\n**arp** / **-arp**\n> Enable or disable ARP protocol on the interface.\n\n**multicast** / **-multicast**\n> Enable or disable multicast flag.\n\n**add** _ADDR_/_PREFIX_\n> Add an IPv6 address to the interface.\n\n**del** _ADDR_/_PREFIX_\n> Remove an IPv6 address from the interface.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ifconfig** configures network interfaces. It displays and modifies IP addresses, netmasks, and interface status. Without arguments, it shows all active interfaces. With **-a**, it shows all interfaces including those that are down.\n\nThe tool is part of **net-tools** and is being replaced by the **ip** command from iproute2, but remains widely used on many systems."],["CAVEATS","Deprecated for ip command. Part of net-tools. Root needed for changes."],["HISTORY","**ifconfig** has been part of Unix networking since **4.2BSD** (1983). On Linux it is part of the **net-tools** package, now largely superseded by **iproute2**."],["INSTALL","```apt: sudo apt install net-tools```\n\n```dnf: sudo dnf install net-tools```\n\n```pacman: sudo pacman -S net-tools```\n\n```apk: sudo apk add net-tools```\n\n```zypper: sudo zypper install net-tools```\n\n```brew: brew install net-tools```\n\n```nix: nix profile install nixpkgs#net-tools```"],["SEE ALSO","[ip](/man/ip)(8), [route](/man/route)(8), [netstat](/man/netstat)(8)"]],"e":["ip","route","netstat"],"x":"ifconfig"},"ifne":{"n":"ifne","t":"runs a command only if there is input","s":[["TAGLINE","runs a command only if there is input"],["TLDR","**Run command if input**\n\n```command1 | ifne command2```\n\n**Delete file if results**\n\n```find . -empty | ifne xargs rm```\n\n**Invert condition**\n\n```command | ifne -n echo \"no output\"```\n\n**Mail if errors**\n\n```make 2>&1 | ifne mail -s \"errors\" [admin]```"],["SYNOPSIS","**ifne** [_options_] _command_ [_args_]"],["PARAMETERS","_COMMAND_\n> Command to run.\n\n_ARGS_\n> Command arguments.\n\n**-n**\n> Run if NO input.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ifne** (\"if not empty\") reads from standard input and runs the given command only if that input is non-empty. The buffered input is then re-emitted on the command's stdin, so the consumer sees the original stream unchanged. With **-n**, the behavior is inverted: the command runs only if standard input is empty (useful for handling \"nothing to do\" branches in scripts).\n\nThis makes **ifne** the natural complement to filters that may produce no output: pipelines like **find ... -print0 | ifne xargs -0 rm** or **make 2>&1 | ifne mail -s \"errors\" admin** stay silent when there is nothing to do."],["CAVEATS","Part of **moreutils**. Input must fit in memory or in the pipe buffer; the entire stream is read before the command is started, so very large inputs may be slow or memory-hungry. Stdin is only forwarded to the wrapped command when input was non-empty; in the **-n** case the wrapped command inherits an empty stdin."],["HISTORY","ifne is part of **moreutils**, a collection of Unix tools by **Joey Hess**."],["INSTALL","```apt: sudo apt install moreutils```\n\n```dnf: sudo dnf install moreutils```\n\n```pacman: sudo pacman -S moreutils```\n\n```apk: sudo apk add moreutils```\n\n```zypper: sudo zypper install moreutils```\n\n```brew: brew install moreutils```\n\n```nix: nix profile install nixpkgs#moreutils```"],["SEE ALSO","[xargs](/man/xargs)(1), [moreutils](/man/moreutils)(7), [test](/man/test)(1)"]],"e":["xargs","moreutils","test"],"x":"command1 | ifne command2"},"ifquery":{"n":"ifquery","t":"queries network interface configuration","s":[["TAGLINE","queries network interface configuration"],["TLDR","**Query configuration** for an interface\n\n```ifquery [eth0]```\n\n**List** all configured interfaces\n\n```ifquery --list```\n\nList interfaces that **are currently up** (from state file)\n\n```ifquery --state```\n\nQuery configuration for a specific **class** of interfaces (e.g. hotplug)\n\n```ifquery --list --allow [hotplug]```\n\nCheck the **running state** of an interface against its configuration\n\n```ifquery --check [eth0]```"],["SYNOPSIS","**ifquery** [_options_] _interface_"],["PARAMETERS","_INTERFACE_\n> Network interface name to query.\n\n**-a**, **--all**\n> Query all interfaces marked **auto** in the configuration.\n\n**--list**\n> List matching interface names instead of printing their configuration. Combine with **--all** or **--allow** to filter.\n\n**--state**\n> Query the state file (**/run/network/ifstate**) for currently active interfaces rather than the configuration file.\n\n**--check**\n> Compare running interface state against its configuration and report differences (ifupdown2 only).\n\n**--allow** _CLASS_\n> Only match interfaces in the given allow class (e.g. **auto**, **hotplug**).\n\n**-i**, **--interfaces** _FILE_\n> Read configuration from _FILE_ instead of **/etc/network/interfaces**.\n\n**--force**\n> Force the operation (used with some queries in ifupdown2).\n\n**-V**, **--version**\n> Display version information.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**ifquery** queries network interface configuration as defined in **/etc/network/interfaces** and its **interfaces.d/** drop-in directory. It is read-only and never modifies interface state.\n\nWhen called without **--state**, it parses the configuration file and prints the stanzas matching the given interface(s). With **--state**, it reads **/run/network/ifstate** to show which interfaces are currently marked as up.\n\nOn systems using **ifupdown2** (common on newer Debian/Ubuntu), additional features are available including **--check** to compare running state against configuration and JSON output."],["CAVEATS","Debian/Ubuntu specific; part of the **ifupdown** (or **ifupdown2**) package. Only reads configuration and state files; never changes anything. The **--check** flag is only available in ifupdown2. Systems using NetworkManager or systemd-networkd may not use **/etc/network/interfaces** at all."],["INSTALL","```apt: sudo apt install ifupdown```\n\n```apk: sudo apk add ifupdown-ng```\n\n```nix: nix profile install nixpkgs#ifupdown-ng```"],["SEE ALSO","[ifup](/man/ifup)(8), [ifdown](/man/ifdown)(8)"]],"e":["ifup","ifdown"],"x":"ifquery [eth0]"},"impacket-getadusers":{"n":"impacket-getadusers","t":"Enumerate Active Directory user accounts via LDAP","s":[["TAGLINE","Enumerate Active Directory user accounts via LDAP"],["TLDR","**Enumerate all domain users** with their details\n\n```impacket-getadusers -all '[domain]/[user]:[password]'```\n\n**Query users** specifying the domain controller IP\n\n```impacket-getadusers -all -dc-ip [192.168.1.100] '[domain]/[user]:[password]'```\n\n**Get users using NTLM hash** authentication\n\n```impacket-getadusers -all -hashes ':[nthash]' '[domain]/[user]'```\n\n**Enumerate users via Kerberos** authentication\n\n```impacket-getadusers -all -k -no-pass '[domain]/[user]'```\n\n**Output user information** in a specific format\n\n```impacket-getadusers -all -dc-ip [192.168.1.100] '[domain]/[user]:[password]' 2>/dev/null```"],["SYNOPSIS","**impacket-getadusers** [_-h_] [_-user USERNAME_] [_-all_] [_-dc-ip IP_] [_-dc-host HOSTNAME_] [_-hashes LMHASH:NTHASH_] [_-no-pass_] [_-k_] [_-aesKey KEY_] _target_"],["PARAMETERS","**-all**\n> Return all users in the domain\n\n**-user** _USERNAME_\n> Query information for a specific user only\n\n**-dc-ip** _IP_\n> IP address of the domain controller\n\n**-dc-host** _HOSTNAME_\n> Hostname of the domain controller (used for Kerberos)\n\n**-hashes** _LMHASH:NTHASH_\n> Use NTLM hashes for authentication instead of password\n\n**-no-pass**\n> Don't ask for password (useful with -k or -hashes)\n\n**-k**\n> Use Kerberos authentication from ccache file\n\n**-aesKey** _KEY_\n> AES key to use for Kerberos authentication"],["DESCRIPTION","**impacket-getadusers** queries Active Directory via LDAP to enumerate user accounts and their attributes. It retrieves information such as usernames, last logon times, password last set dates, and account status flags.\n\nThe tool is useful for reconnaissance during penetration tests to identify potential targets, find accounts with old passwords, or discover service accounts. Output includes the SAM account name, badPwdCount, and other relevant security attributes."],["CAVEATS","Requires valid domain credentials. Output may be verbose; consider redirecting stderr to /dev/null for cleaner output. The -all flag is typically required to see results for more than just the authenticated user."],["HISTORY","Part of the **Impacket** library developed by SecureAuth. The tool leverages LDAP queries against Active Directory, implementing Microsoft's directory service protocols in Python for cross-platform compatibility."],["INSTALL","```pacman: sudo pacman -S impacket```"],["SEE ALSO","[impacket-addcomputer](/man/impacket-addcomputer)(1), [impacket-secretsdump](/man/impacket-secretsdump)(1), [ldapsearch](/man/ldapsearch)(1), [net](/man/net)(1)"]],"e":["impacket-addcomputer","impacket-secretsdump","ldapsearch","net"],"x":"impacket-getadusers -all '[domain]/[user]:[password]'"},"install":{"n":"install","t":"copies files while setting permissions and ownership","s":[["TAGLINE","copies files while setting permissions and ownership"],["TLDR","**Copy file with specific permissions**\n\n```install -m [755] [source] [dest]```\n\n**Copy files into a target directory**\n\n```install -t [/usr/local/bin/] [file1] [file2]```\n\n**Create directories (including parents)**\n\n```install -d [/path/to/dir]```\n\n**Set owner and group**\n\n```install -o [user] -g [group] [file] [dest]```\n\n**Copy preserving timestamps**\n\n```install -p [file] [dest]```\n\n**Strip binaries during install**\n\n```install -s [binary] [/usr/local/bin/]```\n\n**Copy only if source is different (avoids unnecessary writes)**\n\n```install -C [file] [dest]```\n\n**Create parent directories then copy**\n\n```install -D [source] [/path/to/new/dir/dest]```"],["SYNOPSIS","**install** [_options_] [**-s**] [**--strip-program**=_PROGRAM_] _source_... _dest_\n\n**install** [_options_] -t _DIRECTORY_ _source_...\n\n**install** [_options_] -d _directories_..."],["DESCRIPTION","**install** copies files while setting permissions and ownership. It is primarily used in Makefiles and installation scripts to place files with correct attributes.\n\nThe tool combines cp, chmod, chown, and mkdir functionality, streamlining installation tasks. It can also strip binaries and back up existing files. The default permission mode is **rwxr-xr-x** (755)."],["PARAMETERS","**-m** _mode_, **--mode**=_mode_\n> Set permission mode (as in chmod), instead of the default rwxr-xr-x.\n\n**-o** _owner_, **--owner**=_owner_\n> Set ownership (super-user only).\n\n**-g** _group_, **--group**=_group_\n> Set group ownership instead of the process's current group.\n\n**-d**, **--directory**\n> Treat all arguments as directory names; create all components of the specified directories.\n\n**-D**\n> Create all leading parent directory components of dest, then copy source to dest.\n\n**-t** _DIRECTORY_, **--target-directory**=_DIRECTORY_\n> Copy all source arguments into DIRECTORY.\n\n**-T**, **--no-target-directory**\n> Treat dest as a normal file, not a directory.\n\n**-C**, **--compare**\n> Compare source and destination; do not modify dest if content, ownership, and permissions are unchanged.\n\n**-s**, **--strip**\n> Strip symbol tables from installed binaries.\n\n**--strip-program**=_PROGRAM_\n> Program used to strip binaries (default: strip).\n\n**-p**, **--preserve-timestamps**\n> Apply access/modification times of source files to destination files.\n\n**-b**\n> Make a backup of each existing destination file.\n\n**--backup**[=_CONTROL_]\n> Make a backup of each existing destination file, with optional version control method.\n\n**-S** _suffix_, **--suffix**=_suffix_\n> Override the usual backup suffix.\n\n**-v**, **--verbose**\n> Print the name of each file or directory as it is created.\n\n**-c**\n> Ignored; for compatibility with older Unix versions.\n\n**-Z**, **--context**\n> Set SELinux security context of destination files to the default type."],["CAVEATS","Not intended for general file copying. The default permission mode is 755 (rwxr-xr-x), unlike cp which preserves source permissions. Requires appropriate privileges for ownership changes."],["HISTORY","**install** is a traditional Unix utility, part of **GNU coreutils** on Linux. It has been used in Makefiles since early Unix for standardized software installation procedures."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[cp](/man/cp)(1), [chmod](/man/chmod)(1), [chown](/man/chown)(1), [mkdir](/man/mkdir)(1), [make](/man/make)(1)"]],"e":["cp","chmod","chown","mkdir","make"],"x":"install -m [755] [source] [dest]"},"kaggle-kernels":{"n":"kaggle-kernels","t":"manages Kaggle notebooks from the command line","s":[["TAGLINE","manages Kaggle notebooks from the command line"],["TLDR","**List kernels**\n\n```kaggle kernels list```\n\n**Push kernel to Kaggle**\n\n```kaggle kernels push -p [path]```\n\n**Pull kernel from Kaggle**\n\n```kaggle kernels pull [owner/kernel-name]```\n\n**Get kernel output**\n\n```kaggle kernels output [owner/kernel-name]```\n\n**Check kernel status**\n\n```kaggle kernels status [owner/kernel-name]```"],["SYNOPSIS","**kaggle** **kernels** _subcommand_ [_options_]"],["PARAMETERS","**list**\n> List available kernels.\n\n**push** **-p** _path_\n> Push kernel to Kaggle.\n\n**pull** _kernel_\n> Download kernel files.\n\n**output** _kernel_\n> Download kernel output.\n\n**status** _kernel_\n> Check kernel run status.\n\n**init** **-p** _path_\n> Initialize kernel metadata."],["DESCRIPTION","**kaggle kernels** manages Kaggle notebooks (kernels) from the command line. Part of the Kaggle CLI, it allows pushing, pulling, and monitoring computational notebooks used for data science projects."],["INSTALL","```nix: nix profile install nixpkgs#kaggle```"],["SEE ALSO","[kaggle](/man/kaggle)(1), [kaggle-datasets](/man/kaggle-datasets)(1)"]],"e":["kaggle","kaggle-datasets"],"x":"kaggle kernels list"},"kh":{"n":"kh","t":"Run macOS ARM64 binaries on Linux aarch64 (userspace translation layer)","s":[["TAGLINE","Run macOS ARM64 binaries on Linux aarch64 (userspace translation layer)"],["TLDR","**Install** the CLI and **ensure** a guest bottle\n\n```cargo install kakehashi```\n\n```kh bottle ensure```\n\n**Install** Darwin guest tools into the bottle\n\n```kh install 7zip```\n\n```kh install curl```\n\n**Run** a Darwin binary (guest args after **--**)\n\n```kh run 7zz -- --help```\n\n```kh run 7zz -- a demo.7z README.md```\n\n**Inspect** a Mach-O binary without executing it\n\n```kh inspect [path/to/binary] --sections --imports```\n\n**Trace** syscalls for a guest binary\n\n```kh trace [path/to/binary] -- [args]```"],["SYNOPSIS","**kh** [**-v**...] [**--json**] _subcommand_ [_options_] [_args_]"],["DESCRIPTION","**kh** is the command-line front end for **Kakehashi**, a userspace translation layer that loads Darwin **Mach-O** binaries (macOS ARM64) on **Linux aarch64**, maps a freestanding **libSystem**, and translates BSD syscalls so real guest tools can run without a JIT.\n\nLive execution requires **Linux aarch64** (bare metal, VM, or Docker/Colima). Dry-load and inspect work on any host, including macOS. Guest tools are installed into a single **bottle** (default under the XDG data home, typically **~/.local/share/kakehashi/bottle/**), which provides a macOS-like filesystem layout; **/Volumes/linux/…** bridges to the host root.\n\nVerified guest tools include Darwin **7zz** (7-Zip) and **curl**. Full Apple frameworks, GUI apps, codesign, and complete curl feature parity are not claimed. Install with **cargo install kakehashi** (Rust 1.88+)."],["PARAMETERS","**-v**, **--verbose**\n> Increase log verbosity (repeatable). Overrides **KAKEHASHI_LOG** when set.\n\n**--json**\n> Emit JSON where a subcommand supports it.\n\n**inspect** [_path_] [**--sections**] [**--imports**] [**--find** _substr_] [**--load-commands**] [**--image**] [**--page-size** _bytes_] [**--host-page-size**]\n> Static Mach-O inspection without execution.\n\n**run** _path_ [**--root** _dir_] [**--max-syscalls** _n_] [**--expect-code** _code_] [**--guest-page-size** _bytes_] [**--dry-load**] [**--**] [_guest_args_…]\n> Run a Mach-O binary under the translation layer. Guest argv follows the program name (often after **--** when using installed bottle names like **7zz**).\n\n**trace** _path_ [**--root** _dir_] [**--max-events** _n_] [**--**] [_guest_args_…]\n> Capture syscall/trap events for a guest binary.\n\n**bottle create|ensure|destroy|path|status**\n> Manage the registered bottle (**ensure** creates if missing and refreshes libSystem).\n\n**install** _package_\n> Install a guest tool into the bottle. Packages include **7zip**, **curl**, **xcode-tools** (aliases **clt**, **git**), or **list**."],["CONFIGURATION","**KAKEHASHI_ROOT** / **--root**\n> Bottle root path.\n\n**KAKEHASHI_DATA_DIR**, **KAKEHASHI_CONFIG_DIR**, **KAKEHASHI_CACHE_DIR**\n> Override XDG-style data, config, and cache locations.\n\n**KAKEHASHI_LIBSYSTEM**\n> Path to guest **libSystem.B.dylib** (or freestanding equivalent) when not using the embedded copy.\n\n**KAKEHASHI_LOG**\n> Tracing filter when **-v** is not set (default **warn**).\n\n**KAKEHASHI_HYPERCALL**\n> Hypercall path is on by default for guest threads; set **0** only for debug.\n\n**KAKEHASHI_FORCE_DOWNLOAD**, **KAKEHASHI_7ZZ**, **KAKEHASHI_CURL**, **KAKEHASHI_XCODE_TOOLS_VERSION**\n> Control optional guest-tool downloads and versions for **kh install**."],["CAVEATS","Live **run**/**trace** need **Linux aarch64** (4 KiB or 16 KiB pages). Not related to Darling; do not expect GUI, codesign, or full Apple Security.framework. Syscall-heavy multi-file workloads can be several times slower than native Linux tools. Experimental — guest support is expanding."],["HISTORY","**Kakehashi** is an open-source CLI-first userspace Darwin→Linux aarch64 translation layer (Apache-2.0). The shipped binary name is **kh**."],["SEE ALSO","[darling](/man/darling)(1), [wine](/man/wine)(1), [cargo](/man/cargo)(1), [7z](/man/7z)(1), [curl](/man/curl)(1)"],["RESOURCES","```[Source code](https://github.com/wie-project/kakehashi)```\n\n```[Documentation](https://github.com/wie-project/kakehashi/tree/main/docs)```"]],"e":["darling","wine","cargo","7z","curl"],"x":"cargo install kakehashi"},"lid":{"n":"lid","t":"Display a user's groups or a group's users","s":[["TAGLINE","Display a user's groups or a group's users"],["TLDR","List **groups** for a user\n\n```sudo lid [username]```\n\nList **users** in a group\n\n```sudo lid -g [groupname]```\n\nList groups for the **current** user\n\n```sudo lid```"],["SYNOPSIS","**lid** [_options_] [_name_]"],["DESCRIPTION","**lid** displays a user's groups or a group's users. It reads from the system's user and group databases to show membership information. Without arguments, it shows groups for the current user."],["PARAMETERS","**-g**, **--group**\n> Treat the argument as a group name and list its members\n\n**name**\n> User or group name to query (user by default)"],["CAVEATS","Requires root privileges to access all group membership information. Part of the **libuser** library tools for user and group account management."],["INSTALL","```dnf: sudo dnf install libuser```\n\n```apk: sudo apk add libuser```\n\n```zypper: sudo zypper install libuser```"],["SEE ALSO","[groups](/man/groups)(1), [id](/man/id)(1), [getent](/man/getent)(1), [gpasswd](/man/gpasswd)(1), [groupmems](/man/groupmems)(1)"]],"e":["groups","id","getent","gpasswd","groupmems"],"x":"sudo lid [username]"},"listnr":{"n":"listnr","t":"Local dual-lane meeting transcription for macOS","s":[["TAGLINE","Local dual-lane meeting transcription for macOS"],["TLDR","**Install** via Homebrew and grant permissions\n\n```brew tap rokib16x/listnr https://github.com/rokib16x/listnr```\n\n```brew trust --tap rokib16x/listnr```\n\n```brew install listnr```\n\n```listnr setup```\n\nStart the **interactive** shell, then live-transcribe\n\n```listnr```\n\n```/live```\n\nOne-shot session for **N seconds** with two remote speakers expected\n\n```listnr start --seconds 60 --speakers 2```\n\nTranscribe **Bangla** (or other supported languages)\n\n```listnr start --language bn --seconds 120```\n\nSpeak any language; transcript in **English**\n\n```listnr start --language bn --translate --seconds 120```\n\n**List / download** Whisper models\n\n```listnr models list```\n\n```listnr models download whisper-large-v2```\n\nCheck **permissions** and environment\n\n```listnr doctor```"],["SYNOPSIS","**listnr** [_command_] [_options_]"],["PARAMETERS","**(no command)**\n> Start the interactive REPL (**listnr>** prompt). Common in-session commands: **/live** [_seconds_], **/stop** or **q**, **/lang** _code_, **/translate**, **/speakers** _n_, **/model** _id_, **/diarize**, **/sensitivity** high|low, **/dump**, **/status**, **/help**.\n\n**start** [**--seconds** _n_] [**--speakers** _n_] [**--language** _code_] [**--translate**] ...\n> One-shot capture without the interactive shell. Transcript goes to **stdout**; progress and meters to **stderr**. Omitting **--seconds** runs until Ctrl+C.\n\n**setup**\n> Guide you through granting **Microphone** and **Screen & System Audio Recording** permissions to the host terminal app.\n\n**doctor**\n> Report which permissions and dependencies are missing.\n\n**models list**\n> List available WhisperKit model ids and sizes.\n\n**models download** _id_\n> Prefetch model weights from Hugging Face into the local cache."],["DESCRIPTION","**listnr** is a macOS command-line meeting listener. It captures **two separate audio lanes**: your **microphone** (always labeled **You**) and **system / speaker audio** (remote participants). Both lanes are transcribed **on-device** with **WhisperKit** (Core ML). Remote speakers are split into **Speaker 1…N** with **SpeakerKit** diarization after the session ends. Audio and transcripts never leave the machine except for the one-time model download from Hugging Face.\n\nUsing a **headset** is strongly recommended so remote audio does not bleed into the microphone lane. Sessions write Markdown transcripts under **~/Documents/Listnr/**. Optional **/dump** writes raw WAV debug audio with owner-only permissions.\n\nSupported languages include English, Bangla, Hindi, Spanish, French, German, Japanese, and Chinese. **/translate** uses Whisper's translate-to-English task (one-way). Default models balance live latency; larger models improve accuracy at the cost of real-time headroom."],["CAVEATS","**macOS 14+ on Apple Silicon only** (Intel Macs are not supported). Permissions attach to the **terminal app**, not the binary—switching Terminal ↔ iTerm requires re-granting. Beta software (**0.x**): CLI flags and behavior may change. Diarization of remote speakers runs **after** you stop (live labels are all **Others**). Memory grows with session length (~460 MB per hour of dual-lane audio in RAM). Recording other people may require **all-party consent** under local law—obtain agreement before use. Session options like language and model do not currently persist across restarts."],["HISTORY","**Listnr** is an MIT-licensed open-source project by Rokibul Hasan. It builds on **WhisperKit** / **SpeakerKit** for on-device speech and diarization, and is distributed as a prebuilt Homebrew formula, notarized **.pkg**, and source build via Swift Package Manager."],["SEE ALSO","[whisper](/man/whisper)(1), [ffmpeg](/man/ffmpeg)(1)"],["RESOURCES","```[Source code](https://github.com/rokib16x/listnr)```\n\n```[Homepage](https://github.com/rokib16x/listnr)```"]],"e":["whisper","ffmpeg"],"x":"brew tap rokib16x/listnr https://github.com/rokib16x/listnr"},"lsmod":{"n":"lsmod","t":"show the status of modules currently loaded in the Linux kernel","s":[["TAGLINE","show the status of modules currently loaded in the Linux kernel"],["TLDR","**List all loaded kernel modules**\n\n```lsmod```\n\n**Filter by name**\n\n```lsmod | grep [nvidia]```\n\n**Sort modules by size** (largest first)\n\n```lsmod | sort -k 2 -n -r```\n\n**Count loaded modules**\n\n```lsmod | tail -n +2 | wc -l```\n\n**Find who is using a module**\n\n```lsmod | awk '$1 == \"[btusb]\" { print $4 }'```"],["SYNOPSIS","**lsmod**"],["DESCRIPTION","**lsmod** is a trivial program that nicely formats the contents of `/proc/modules`, showing which loadable kernel modules are currently in memory. It takes no arguments (apart from `--help` / `--version` on some distros).\n\nThe output has one module per line, with three columns:\n\n- **Module** — name of the module\n- **Size** — memory footprint in bytes\n- **Used by** — reference count followed by a comma-separated list of modules or subsystems that depend on it\n\nA reference count greater than zero means the module is in use and cannot simply be `rmmod`-ed without unloading its dependents first."],["OUTPUT COLUMNS","**Module**\n> Module name as registered with the kernel.\n\n**Size**\n> Memory used by the module's code and data, in bytes.\n\n**Used by**\n> Use count followed by the names of modules that depend on this module. A dash (`-`) means nothing currently uses it."],["CAVEATS","`lsmod` only reports what the kernel has loaded; built-in modules compiled directly into the kernel image do not appear. For more detail on a specific module (author, license, parameters, dependencies), use `modinfo`."],["HISTORY","**lsmod** originally shipped with the older **modutils**/**module-init-tools** suites. It is now part of **kmod**, the current kernel module userspace tools written by **Lucas De Marchi** and others, which replaced module-init-tools around Linux 3.2."],["INSTALL","```apt: sudo apt install kmod```\n\n```dnf: sudo dnf install kmod```\n\n```pacman: sudo pacman -S kmod```\n\n```apk: sudo apk add kmod```\n\n```zypper: sudo zypper install kmod```\n\n```brew: brew install kmod```\n\n```nix: nix profile install nixpkgs#kmod```"],["SEE ALSO","[modprobe](/man/modprobe)(8), [rmmod](/man/rmmod)(8), [insmod](/man/insmod)(8), [modinfo](/man/modinfo)(8), [depmod](/man/depmod)(8), [kmod](/man/kmod)(8)"]],"e":["modprobe","rmmod","insmod","modinfo","depmod","kmod"],"x":"lsmod"},"mvn-compile":{"n":"mvn-compile","t":"compile the main source code of a Maven project","s":[["TAGLINE","compile the main source code of a Maven project"],["TLDR","**Compile** the project\n\n```mvn compile```\n\n**Activate a profile** during compilation\n\n```mvn compile -P [profile_name]```\n\n**Compile without** running tests later in the lifecycle\n\n```mvn compile -DskipTests```\n\n**Compile in offline** mode (no network)\n\n```mvn compile -o```\n\n**Force update** of dependencies\n\n```mvn compile -U```\n\n**Run with a specific** JDK toolchain\n\n```mvn compile -Dmaven.compiler.release=[17]```\n\n**Quiet compile**\n\n```mvn compile -q```\n\n**Compile a specific module** in a multi-module build\n\n```mvn -pl [module/path] -am compile```"],["SYNOPSIS","**mvn compile** [_options_]"],["PARAMETERS","**-P** _profile_\n> Activate one or more comma-separated Maven profiles.\n\n**-DskipTests**\n> Skip test compilation in subsequent phases. Tests are still compiled if **mvn test** runs without this flag.\n\n**-Dmaven.compiler.release** _N_\n> Set the **--release** flag passed to javac, controlling the target JDK API level.\n\n**-o**, **--offline**\n> Run in offline mode; never reach out to remote repositories.\n\n**-U**, **--update-snapshots**\n> Force updates of snapshot dependencies and plugins.\n\n**-pl** _MODULES_\n> Restrict the reactor build to a comma-separated list of modules.\n\n**-am**, **--also-make**\n> Together with **-pl**, also build dependencies of the selected modules.\n\n**-q**, **--quiet**\n> Print only errors and the final result.\n\n**-X**, **--debug**\n> Enable verbose debug output (full stack traces, effective POM).\n\n**-T** _N_\n> Build in parallel using _N_ threads (or **1C** for one per core)."],["DESCRIPTION","**mvn compile** runs the **compile** phase of the Maven default lifecycle, executing all phases that come before it (validate, initialize, generate-sources, process-resources, ...) and finally invoking **maven-compiler-plugin** to compile **src/main/java** into **target/classes**. Resources from **src/main/resources** are copied during the **process-resources** phase, so they are available on the classpath after **compile** completes.\n\nThe compiler version and target language level are governed by the **maven-compiler-plugin** configuration in **pom.xml** or by the **maven.compiler.source**, **maven.compiler.target**, and **maven.compiler.release** properties. Use **-T** for parallel multi-module builds, and **-pl/-am** to compile only the relevant module of a large reactor."],["CAVEATS","Test sources in **src/test/java** are compiled in a separate phase (**test-compile**) and are not produced by **mvn compile** alone. Generated sources from annotation processors are written to **target/generated-sources/annotations** and must be on the source path for IDE awareness."],["INSTALL","```dnf: sudo dnf install maven```\n\n```pacman: sudo pacman -S maven```\n\n```apk: sudo apk add maven```\n\n```zypper: sudo zypper install maven```\n\n```brew: brew install maven```\n\n```nix: nix profile install nixpkgs#maven```"],["SEE ALSO","[mvn](/man/mvn)(1), [mvn-package](/man/mvn-package)(1), [mvn-install](/man/mvn-install)(1), [javac](/man/javac)(1)"]],"e":["mvn","mvn-package","mvn-install","javac"],"x":"mvn compile"},"ndc":{"n":"ndc","t":"BIND name daemon controller","s":[["TAGLINE","BIND name daemon controller"],["TLDR","**Reload BIND configuration**\n\n```ndc reload```\n\n**Show BIND status**\n\n```ndc status```\n\n**Stop BIND server**\n\n```ndc stop```\n\n**Start BIND server**\n\n```ndc start```\n\n**Flush cache**\n\n```ndc flush```\n\n**Restart BIND**\n\n```ndc restart```"],["SYNOPSIS","**ndc** [_command_]"],["PARAMETERS","_COMMAND_\n> Control command.\n\n**reload**\n> Reload configuration.\n\n**status**\n> Show server status.\n\n**stop**\n> Stop server.\n\n**start**\n> Start server.\n\n**flush**\n> Flush DNS cache.\n\n**restart**\n> Restart server.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ndc** is the BIND name daemon controller. It manages the BIND DNS server.\n\nThe tool sends commands to named. Legacy tool, replaced by rndc."],["CAVEATS","Legacy tool. Use rndc instead. Requires running named."],["HISTORY","ndc was the original **BIND** control utility, now deprecated in favor of rndc."],["SEE ALSO","[rndc](/man/rndc)(1), [named](/man/named)(1), [bind](/man/bind)(1)"]],"e":["rndc","named","bind"],"x":"ndc reload"},"netexec":{"n":"netexec","t":"network security tool for Windows/Active Directory environments","s":[["TAGLINE","network security tool for Windows/Active Directory environments"],["TLDR","**Enumerate SMB shares**\n\n```netexec smb [192.168.1.0/24] -u [user] -p [password] --shares```\n\n**Check credentials**\n\n```netexec smb [target] -u [user] -p [password]```\n\n**Execute command**\n\n```netexec smb [target] -u [admin] -p [password] -x \"[whoami]\"```\n\n**Pass the hash**\n\n```netexec smb [target] -u [user] -H [hash]```\n\n**Enumerate users**\n\n```netexec smb [target] -u [user] -p [password] --users```\n\n**Spray passwords**\n\n```netexec smb [target] -u [users.txt] -p [password]```\n\n**WinRM access**\n\n```netexec winrm [target] -u [user] -p [password]```\n\n**LDAP enumeration**\n\n```netexec ldap [target] -u [user] -p [password] --users```"],["SYNOPSIS","**netexec** _protocol_ _target_ [_-u user_] [_-p pass_] [_-H hash_] [_options_]"],["PARAMETERS","**smb**\n> SMB protocol.\n\n**winrm**\n> WinRM protocol.\n\n**ldap**\n> LDAP protocol.\n\n**ssh**\n> SSH protocol.\n\n**mssql**\n> MSSQL protocol.\n\n**ftp**\n> FTP protocol.\n\n**rdp**\n> RDP protocol.\n\n**wmi**\n> WMI protocol.\n\n**vnc**\n> VNC protocol.\n\n**nfs**\n> NFS protocol.\n\n**-u** _USER_\n> Username or file.\n\n**-p** _PASS_\n> Password or file.\n\n**-H** _HASH_\n> NTLM hash.\n\n**-d** _DOMAIN_\n> Domain name.\n\n**-x** _CMD_\n> Execute command.\n\n**-X** _PS_CMD_\n> Execute PowerShell.\n\n**--shares**\n> Enumerate shares.\n\n**--users**\n> Enumerate users.\n\n**--groups**\n> Enumerate groups.\n\n**--local-auth**\n> Use local authentication.\n\n**--sam**\n> Dump SAM database.\n\n**--lsa**\n> Dump LSA secrets.\n\n**-t** _THREADS_\n> Number of concurrent threads (default: 100).\n\n**--timeout** _SECONDS_\n> Max timeout in seconds for connections.\n\n**--jitter** _INTERVAL_\n> Random delay between connections.\n\n**--verbose**\n> Enable verbose output.\n\n**--debug**\n> Enable debug level information."],["DESCRIPTION","**netexec** (formerly CrackMapExec) is a network security tool for Windows/Active Directory environments. It tests credentials, enumerates resources, and executes commands across networks.\n\nMultiple protocols are supported: SMB, WinRM, LDAP, SSH, MSSQL, FTP, RDP, WMI, VNC, and NFS. Each protocol has specific enumeration and exploitation modules.\n\nCredential testing validates usernames and passwords across targets. Pass-the-hash uses NTLM hashes directly. Password spraying tests one password against many accounts.\n\nShare enumeration reveals accessible network shares. User and group enumeration maps Active Directory structure. These provide reconnaissance for security assessments.\n\nCommand execution requires appropriate privileges. Results are collected across all targets for analysis.\n\nThe tool is designed for authorized penetration testing and security assessments."],["CAVEATS","Only use with authorization. May trigger security alerts. Some features require administrative access. Heavy scanning can cause lockouts."],["HISTORY","**NetExec** is the successor to **CrackMapExec** (CME), created by **byt3bl33d3r** around **2015**. After CrackMapExec's development slowed, NetExec forked the project in **2023** to continue maintenance and development."],["INSTALL","```nix: nix profile install nixpkgs#netexec```"],["SEE ALSO","[smbclient](/man/smbclient)(1), [rpcclient](/man/rpcclient)(1), [ldapsearch](/man/ldapsearch)(1), [nmap](/man/nmap)(1)"]],"e":["smbclient","rpcclient","ldapsearch","nmap"],"x":"netexec smb [192.168.1.0/24] -u [user] -p [password] --shares"},"nordvpn":{"n":"nordvpn","t":"command-line interface for the NordVPN service, providing secure VPN","s":[["TAGLINE","command-line interface for the NordVPN service, providing secure VPN"],["TLDR","**Interactively log into** a NordVPN account\n\n```nordvpn login```\n\nDisplay the **connection status**\n\n```nordvpn status```\n\nConnect to the **nearest NordVPN server**\n\n```nordvpn connect```\n\nList all **available countries**\n\n```nordvpn countries```\n\nConnect to a server in a **specific country**\n\n```nordvpn connect [Germany]```\n\nConnect to a server in a **specific country and city**\n\n```nordvpn connect [Germany] [Berlin]```\n\nSet **autoconnect option**\n\n```nordvpn set autoconnect on```"],["SYNOPSIS","**nordvpn** _command_ [_options_]"],["PARAMETERS","**login**\n> Authenticate with NordVPN account\n\n**logout**\n> Log out of the current account\n\n**connect, c**\n> Connect to a VPN server\n\n**disconnect, d**\n> Disconnect from the VPN\n\n**status**\n> Show current connection status\n\n**countries**\n> List available server countries\n\n**cities _country_**\n> List available cities in a country\n\n**set, s**\n> Configure NordVPN settings\n\n**settings**\n> Display current settings\n\n**whitelist**\n> Manage whitelisted ports and subnets"],["DESCRIPTION","**nordvpn** is the command-line interface for the NordVPN service, providing secure VPN connections to servers worldwide. It manages authentication, server connections, and client settings.\n\nThe client supports various connection protocols (NordLynx/WireGuard, OpenVPN) and features like kill switch, auto-connect, and split tunneling. Server selection can be automatic (nearest/fastest) or manual by country, city, or specific server.\n\nThe NordVPN daemon must be running for the CLI to function; it's typically managed as a systemd service."],["CAVEATS","Requires an active NordVPN subscription. The nordvpnd daemon must be running (usually started via systemd). Initial login opens a browser for authentication. The kill switch, when enabled, blocks internet access if the VPN disconnects."],["INSTALL","```nix: nix profile install nixpkgs#nordvpn```"],["SEE ALSO","[openvpn](/man/openvpn)(8), [wg](/man/wg)(8), [nmcli](/man/nmcli)(1)"]],"e":["openvpn","wg","nmcli"],"x":"nordvpn login"},"npm-install-test":{"n":"npm-install-test","t":"runs npm install followed by npm test","s":[["TAGLINE","runs npm install followed by npm test"],["TLDR","**Install and test**\n\n```npm install-test```\n\n**Short form**\n\n```npm it```"],["SYNOPSIS","**npm** **install-test**"],["DESCRIPTION","**npm install-test** runs npm install followed by npm test. Combines dependency installation with test execution. Alias: npm it."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-install](/man/npm-install)(1), [npm-test](/man/npm-test)(1)"]],"e":["npm","npm-install","npm-test"],"x":"npm install-test"},"npm-team":{"n":"npm-team","t":"manages organization teams","s":[["TAGLINE","manages organization teams"],["TLDR","**List teams**\n\n```npm team ls [@org]```\n\n**Create team**\n\n```npm team create [@org:team]```\n\n**Destroy team**\n\n```npm team destroy [@org:team]```\n\n**Add user to team**\n\n```npm team add [@org:team] [user]```\n\n**Remove user from team**\n\n```npm team rm [@org:team] [user]```"],["SYNOPSIS","**npm** **team** [_command_] [_options_]"],["PARAMETERS","**ls** _@org_ | _@org:team_\n> List teams in an organization, or members of a specific team.\n\n**create** _@org:team_\n> Create a new team under the given organization scope.\n\n**destroy** _@org:team_\n> Delete an existing team (the developers team cannot be removed).\n\n**add** _@org:team_ _user_\n> Add a user to the given team.\n\n**rm** _@org:team_ _user_\n> Remove a user from the given team.\n\n**--otp** _code_\n> Provide a two-factor authentication one-time password.\n\n**--registry** _url_\n> Target registry URL (defaults to https://registry.npmjs.org).\n\n**--parseable**\n> Output in tab-separated parseable format.\n\n**--json**\n> Output results as JSON.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npm team** manages organization teams. Controls package access groups.\n\nThe command handles team membership. Part of npm organizations feature."],["CAVEATS","Requires organization. Admin access needed. Paid feature."],["HISTORY","npm team provides **team-based access control** for npm organization packages."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-org](/man/npm-org)(1), [npm-access](/man/npm-access)(1)"]],"e":["npm","npm-org","npm-access"],"x":"npm team ls [@org]"},"openssl-verify":{"n":"openssl-verify","t":"verifies certificate chains against trusted CAs","s":[["TAGLINE","verifies certificate chains against trusted CAs"],["TLDR","**Verify certificate**\n\n```openssl verify [certificate.crt]```\n\n**Verify with CA file**\n\n```openssl verify -CAfile [ca.crt] [certificate.crt]```\n\n**Verify with CA directory**\n\n```openssl verify -CApath [/etc/ssl/certs/] [certificate.crt]```\n\n**Verify certificate chain**\n\n```openssl verify -CAfile [ca.crt] -untrusted [intermediate.crt] [leaf.crt]```\n\n**Show verification details**\n\n```openssl verify -verbose [certificate.crt]```\n\n**Show the built certificate chain**\n\n```openssl verify -show_chain -CAfile [ca.crt] [certificate.crt]```\n\n**Verify with CRL checking**\n\n```openssl verify -crl_download -crl_check -CAfile [ca.crt] [certificate.crt]```"],["SYNOPSIS","**openssl verify** [_options_] _cert_..."],["PARAMETERS","**-CAfile** _file_\n> CA certificate file for trusted root certificates.\n\n**-CApath** _dir_\n> Directory of CA certificates (hashed filenames).\n\n**-CAstore** _uri_\n> URI for trusted CA certificate store.\n\n**-untrusted** _file_\n> File containing untrusted intermediate certificates.\n\n**-partial_chain**\n> Allow verification to succeed with incomplete chain if any certificate in the chain is trusted.\n\n**-verbose**\n> Print extra information about verification operations.\n\n**-show_chain**\n> Display information about the certificate chain built during verification.\n\n**-x509_strict**\n> Strict X.509 compliance checking (disables non-compliant workarounds).\n\n**-attime** _timestamp_\n> Verify at specified time (seconds since epoch) instead of current time.\n\n**-no_check_time**\n> Suppress checking validity period of certificates and CRLs.\n\n**-purpose** _purpose_\n> Certificate purpose (sslclient, sslserver, smimesign, smimeencrypt, etc.).\n\n**-CRLfile** _file_\n> File containing CRLs in PEM format for revocation checking.\n\n**-crl_download**\n> Attempt to download CRL information for certificates.\n\n**-crl_check**\n> Check end entity certificate against CRL.\n\n**-crl_check_all**\n> Check all certificates in chain against CRL.\n\n**-check_ss_sig**\n> Verify the signature on the self-signed root CA (disabled by default)."],["DESCRIPTION","**openssl verify** verifies certificate chains against trusted CAs. It checks signatures, validity periods, and trust chains. Returns exit status 0 on successful verification."],["CAVEATS","Does not check revocation by default; use -crl_check or -crl_download. System CA store location varies by distribution. Use -verbose for diagnostic info on verification failures."],["HISTORY","Certificate verification is fundamental to PKI, with openssl verify providing command-line access to OpenSSL's verification functions. The -show_chain option was added in OpenSSL 1.1.0."],["INSTALL","```apt: sudo apt install openssl```\n\n```dnf: sudo dnf install openssl```\n\n```pacman: sudo pacman -S openssl```\n\n```apk: sudo apk add openssl```\n\n```zypper: sudo zypper install openssl```\n\n```brew: brew install openssl```\n\n```nix: nix profile install nixpkgs#openssl```"],["SEE ALSO","[openssl-x509](/man/openssl-x509)(1), [openssl-s_client](/man/openssl-s_client)(1), [openssl-crl](/man/openssl-crl)(1)"]],"e":["openssl-x509","openssl-s_client","openssl-crl"],"x":"openssl verify [certificate.crt]"},"oyo":{"n":"oyo","t":"Step-through diff viewer for the terminal","s":[["TAGLINE","Step-through diff viewer for the terminal"],["TLDR","**View a diff**\n\n```oyo [path/to/file]```\n\n**Use as a git difftool**\n\n```git difftool -t oyo```"],["SYNOPSIS","**oyo** [_options_] [_file_]"],["DESCRIPTION","**oyo** is a step-through diff viewer that extends traditional diffs with an evolution mode. You can use it like a normal diff viewer with scrolling and hunk navigation, or step through changes one at a time watching the code evolve from old to new.\n\nView modes include unified, split, evolution, and blame. It supports clipboard integration and can be configured as a git difftool."],["CONFIGURATION","**~/.config/oyo/config.toml**\n> Configuration file for UI settings including view mode, auto-centering, line wrapping, and scrollbar display."],["CAVEATS","Best used with Git integration. Standalone diff viewing requires properly formatted diff input."],["HISTORY","**oyo** was created by **ahkohd** and is written in **Rust**."],["INSTALL","```aur: yay -S oyo```"],["SEE ALSO","[diff](/man/diff)(1), [delta](/man/delta)(1), [difftastic](/man/difftastic)(1)"]],"e":["diff","delta","difftastic"],"x":"oyo [path/to/file]"},"pamfunc":{"n":"pamfunc","t":"applies mathematical functions","s":[["TAGLINE","applies mathematical functions"],["TLDR","**Multiply pixel values**\n\n```pamfunc -multiplier=[1.5] [input.pam] > [output.pam]```\n\n**Add to pixel values**\n\n```pamfunc -adder=[50] [input.pam] > [output.pam]```\n\n**Apply power function**\n\n```pamfunc -power=[2.2] [input.pam] > [output.pam]```"],["SYNOPSIS","**pamfunc** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Input PAM/PNM file.\n\n**-multiplier** _FACTOR_\n> Multiply values.\n\n**-adder** _VALUE_\n> Add to values.\n\n**-power** _EXPONENT_\n> Apply power function.\n\n**-divisor** _VALUE_\n> Divide values."],["DESCRIPTION","**pamfunc** applies mathematical functions. Transforms pixel values.\n\nThe tool performs arithmetic on images. Part of Netpbm."],["CAVEATS","Part of Netpbm. Values clamped to valid range."],["HISTORY","pamfunc is part of **Netpbm** for mathematical image transformations."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamarith](/man/pamarith)(1), [pnmgamma](/man/pnmgamma)(1), [netpbm](/man/netpbm)(1)"]],"e":["pamarith","pnmgamma","netpbm"],"x":"pamfunc -multiplier=[1.5] [input.pam] > [output.pam]"},"pct-cpusets":{"n":"pct-cpusets","t":"print assigned CPU sets for Proxmox LXC containers","s":[["TAGLINE","print assigned CPU sets for Proxmox LXC containers"],["TLDR","**Print** the list of assigned CPU sets\n\n```pct cpusets```"],["SYNOPSIS","**pct cpusets**"],["DESCRIPTION","**pct cpusets** prints the list of assigned CPU sets for LXC containers managed by Proxmox VE. It is a diagnostic subcommand of **pct**, useful when inspecting how container processes are pinned or constrained to host CPUs."],["HISTORY","**pct cpusets** is part of the **Proxmox VE** **pct** tool for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-status](/man/pct-status)(1), [pct-set](/man/pct-set)(1)"],["RESOURCES","```[Documentation](https://pve.proxmox.com/pve-docs/pct.1.html)```\n\n```[Homepage](https://www.proxmox.com)```"]],"e":["pct","pct-status","pct-set"],"x":"pct cpusets"},"peek":{"n":"peek","t":"simple animated GIF screen recorder for Linux","s":[["TAGLINE","simple animated GIF screen recorder for Linux"],["TLDR","**Start recording**\n\n```peek```\n\n**Record specific window**\n\n```peek --window```\n\n**Record to specific format**\n\n```peek --format [gif|mp4|webm]```\n\n**Record to file**\n\n```peek -o [output.gif]```"],["SYNOPSIS","**peek** [_options_]"],["PARAMETERS","**-o**, **--output** _file_\n> Output file path.\n\n**--format** _format_\n> Output format (gif, mp4, webm, apng).\n\n**--window**\n> Select window to record.\n\n**--fullscreen**\n> Record full screen.\n\n**--delay** _seconds_\n> Delay before recording."],["DESCRIPTION","**peek** is a simple animated GIF screen recorder for Linux. It provides an easy-to-use interface for creating screen recordings, particularly useful for documentation and bug reports."],["Start peek GUI","peek"],["Record as MP4","peek --format mp4"],["Record with delay","peek --delay 3 -o demo.gif\n```"],["KEYBOARD SHORTCUTS","```\nCtrl+R - Start/stop recording\nCtrl+P - Pause/resume\nEscape - Cancel\n```"],["CONFIGURATION","Settings available through GUI:\n- Frame rate\n- Downscale factor\n- Output format\n- Recording area"],["CAVEATS","GIF files can be large. Wayland support varies. Requires FFmpeg for some formats."],["HISTORY","peek was created by **Philipp Wolfer** as a simple screen-to-GIF recorder for Linux desktops."],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [byzanz-record](/man/byzanz-record)(1), [recordmydesktop](/man/recordmydesktop)(1)"],["INSTALL","```apt: sudo apt install peek```\n\n```pacman: sudo pacman -S peek```\n\n```apk: sudo apk add peek```\n\n```zypper: sudo zypper install peek```\n\n```nix: nix profile install nixpkgs#peek```"]],"e":["ffmpeg","byzanz-record","recordmydesktop"],"x":"peek"},"piper":{"n":"piper","t":"Fast local neural text-to-speech engine","s":[["TAGLINE","Fast local neural text-to-speech engine"],["TLDR","**Synthesize speech**\n\n```echo \"Hello world\" | piper --model [en_US-lessac-medium] --output_file [output.wav]```\n\n**List models**\n\n```piper --list-models```\n\n**Use specific model**\n\n```piper -m [model.onnx] -c [model.json] < [input.txt] > [output.wav]```\n\n**Set speaker**\n\n```piper --model [model.onnx] --speaker [0] < [input.txt]```\n\n**Adjust speaking rate**\n\n```piper --model [model.onnx] --length_scale [1.5] < [input.txt]```\n\n**JSON input mode**\n\n```echo '{\"text\": \"Hello\"}' | piper --model [model.onnx] --json-input```"],["SYNOPSIS","**piper** [_--model file_] [_--output_file file_] [_options_]"],["PARAMETERS","**-m**, **--model** _FILE_\n> ONNX model file.\n\n**-c**, **--config** _FILE_\n> JSON config file.\n\n**--output_file** _FILE_\n> Output WAV file.\n\n**--output_dir** _DIR_\n> Output directory.\n\n**--speaker** _ID_\n> Speaker ID for multi-speaker.\n\n**--length_scale** _FLOAT_\n> Speaking rate (higher=slower).\n\n**--noise_scale** _FLOAT_\n> Variation in speech.\n\n**--json-input**\n> JSON input format.\n\n**--list-models**\n> Show available models."],["DESCRIPTION","**piper** is a fast, local neural text-to-speech system that generates natural-sounding speech from text using ONNX-based voice models. It runs entirely offline after model download, requiring no internet connection or cloud API.\n\nEach model is trained for a specific language and voice. Multi-speaker models support selecting different voice variants via speaker ID. The length_scale and noise_scale parameters control speaking rate and variation, allowing fine-tuning of output characteristics.\n\nInput is read from stdin and output as WAV audio. JSON input mode enables structured text processing with per-utterance settings. Models are available for many languages through the Piper project's model repository."],["CAVEATS","Models must be downloaded before use (typically 15-75 MB each). Speech quality varies significantly between models and languages. GPU acceleration via CUDA is optional but improves performance for batch processing. Output is always WAV format; convert with ffmpeg for other formats."],["HISTORY","**Piper** was created by **Michael Hansen** (rhasspy) for offline voice assistants. It provides fast, high-quality TTS suitable for embedded and edge devices."],["INSTALL","```dnf: sudo dnf install piper```\n\n```pacman: sudo pacman -S piper```\n\n```apk: sudo apk add piper```\n\n```zypper: sudo zypper install piper```\n\n```nix: nix profile install nixpkgs#piper```"],["SEE ALSO","[espeak](/man/espeak)(1), [festival](/man/festival)(1), [mimic](/man/mimic)(1), [say](/man/say)(1)"]],"e":["espeak","festival","mimic","say"],"x":"echo \"Hello world\" | piper --model [en_US-lessac-medium] --output_file [output.wav]"},"pnmenlarge":{"n":"pnmenlarge","t":"Enlarge PNM images by pixel replication","s":[["TAGLINE","Enlarge PNM images by pixel replication"],["TLDR","**Enlarge image 2x**\n\n```pnmenlarge [2] [input.pnm] > [output.pnm]```\n\n**Enlarge image 4x**\n\n```pnmenlarge [4] [input.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmenlarge** _factor_ [_file_]"],["PARAMETERS","_factor_\n> Enlargement factor (integer)."],["DESCRIPTION","**pnmenlarge** enlarges PNM images by replicating pixels. Simple nearest-neighbor scaling without interpolation. Part of Netpbm toolkit. For smoother scaling, use pamscale."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamscale](/man/pamscale)(1), [pnmscale](/man/pnmscale)(1)"]],"e":["pamscale","pnmscale"],"x":"pnmenlarge [2] [input.pnm] > [output.pnm]"},"podselect":{"n":"podselect","t":"Extract sections from Perl POD files","s":[["TAGLINE","Extract sections from Perl POD files"],["TLDR","**Extract specific sections from POD**\n\n```podselect -section [NAME] [file.pod]```\n\n**Extract multiple sections**\n\n```podselect -section [NAME] -section [SYNOPSIS] [file.pod]```\n\n**Process multiple files**\n\n```podselect -section [NAME] [*.pod]```"],["SYNOPSIS","**podselect** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Input POD files.\n\n**-section** _NAME_\n> Section to extract."],["DESCRIPTION","**podselect** extracts specific named sections from Perl POD (Plain Old Documentation) files. It filters the POD content and outputs only the sections matching the **-section** arguments.\n\nMultiple **-section** flags can be specified to extract several sections at once. This is useful for extracting individual sections like NAME, SYNOPSIS, or DESCRIPTION from larger POD documents. Part of the standard Perl distribution."],["CAVEATS","Perl-specific format. Standard Perl utility."],["HISTORY","podselect was created for **selective POD** section extraction."],["INSTALL","```pacman: sudo pacman -S perl-pod-parser```\n\n```apk: sudo apk add perl-pod-parser```"],["SEE ALSO","[pod2text](/man/pod2text)(1), [podchecker](/man/podchecker)(1), [perldoc](/man/perldoc)(1)"]],"e":["pod2text","podchecker","perldoc"],"x":"podselect -section [NAME] [file.pod]"},"psalm":{"n":"psalm","t":"Static analysis tool for PHP type safety","s":[["TAGLINE","Static analysis tool for PHP type safety"],["TLDR","**Analyze project**\n\n```psalm```\n\n**Analyze with specific level**\n\n```psalm --level=[5]```\n\n**Analyze specific file**\n\n```psalm [src/File.php]```\n\n**Initialize configuration**\n\n```psalm --init```\n\n**Generate baseline**\n\n```psalm --set-baseline=[baseline.xml]```\n\n**Show info-level issues**\n\n```psalm --show-info=true```\n\n**Output as JSON**\n\n```psalm --output-format=json```\n\n**Analyze only changed files** (based on git diff)\n\n```psalm --diff```\n\n**Run security taint analysis**\n\n```psalm --taint-analysis```\n\n**Fix issues automatically**\n\n```psalm --alter --issues=[MissingReturnType]```"],["SYNOPSIS","**psalm** [_--level n_] [_--set-baseline file_] [_--config file_] [_options_] [_paths_]"],["PARAMETERS","**--level** _N_\n> Error level (1-9, where 1 is strictest).\n\n**--config**, **-c** _FILE_\n> Configuration file.\n\n**--init**\n> Initialize psalm.xml.\n\n**--set-baseline** _FILE_\n> Generate baseline file.\n\n**--use-baseline** _FILE_\n> Use baseline file.\n\n**--ignore-baseline**\n> Ignore baseline.\n\n**--show-info**\n> Show info-level issues.\n\n**--output-format** _FMT_\n> Output format (console, json, xml, etc.).\n\n**--alter**\n> Modify files to fix issues.\n\n**--issues** _LIST_\n> Issues to fix with --alter.\n\n**--threads** _N_\n> Parallel analysis threads.\n\n**--memory-limit** _SIZE_\n> Memory limit.\n\n**--diff**\n> Only analyze files changed since last run.\n\n**--taint-analysis**\n> Enable taint analysis to detect security vulnerabilities in data flow.\n\n**--clear-cache**\n> Clear cache.\n\n**--stats**\n> Show type coverage statistics."],["DESCRIPTION","**psalm** is a static analysis tool for PHP that finds bugs and enforces type safety. It understands PHP type hints and PHPDoc annotations.\n\nError levels range from 1 (strictest) to 9 (most permissive). Higher levels allow more potential issues, suitable for legacy codebases.\n\nConfiguration in psalm.xml defines analysis scope, excluded files, and issue severity. Project-specific settings enable gradual adoption.\n\nBaselines record existing issues to ignore. New code must pass analysis while legacy problems can be addressed over time.\n\nThe alter mode automatically fixes certain issues like adding return types or parameter types. This accelerates migration to stricter typing.\n\nPsalm understands complex PHP patterns including generics, union types, and template types through PHPDoc syntax."],["CONFIGURATION","**psalm.xml** (or **psalm.xml.dist**)\n> Project configuration file defining error level, analysis scope, excluded directories, plugin loading, and issue severity overrides. Created with **--init**.\n\n**baseline.xml**\n> Baseline file recording existing issues to ignore, allowing strict analysis on new code while deferring legacy fixes. Generated with **--set-baseline**."],["CAVEATS","Strict levels may be impractical for legacy code. Some PHP patterns are difficult to analyze. Memory usage scales with project size."],["HISTORY","**Psalm** was created by **Matthew Brown** at **Vimeo** around **2016**. It brought advanced static analysis to PHP, supporting features like generics before PHP had native support."],["INSTALL","```pacman: sudo pacman -S psalm```\n\n```brew: brew install psalm```"],["SEE ALSO","[phpstan](/man/phpstan)(1), [php](/man/php)(1), [composer](/man/composer)(1), [phpcs](/man/phpcs)(1)"]],"e":["phpstan","php","composer","phpcs"],"x":"psalm"},"route":{"n":"route","t":"Display and manage IP routing table","s":[["TAGLINE","Display and manage IP routing table"],["TLDR","**Display the routing table with numeric addresses**\n\n```route -n```\n\n**Add a default gateway**\n\n```sudo route add default gw [192.168.1.1]```\n\n**Add a route to a network**\n\n```sudo route add -net [10.0.0.0] netmask [255.0.0.0] gw [192.168.1.1]```\n\n**Add a route to a host**\n\n```sudo route add -host [192.168.2.100] gw [192.168.1.1]```\n\n**Add an IPv6 route**\n\n```sudo route -6 add [2001:db8::/32] gw [fe80::1] dev [eth0]```\n\n**Delete a route to a network**\n\n```sudo route del -net [10.0.0.0] netmask [255.0.0.0]```\n\n**Delete the default gateway**\n\n```sudo route del default```\n\n**Add a reject route to block traffic to a network**\n\n```sudo route add -net [10.0.0.0] netmask [255.0.0.0] reject```"],["SYNOPSIS","**route** [**-CFvnNee**] [**-A** _family_|**-4**|**-6**]\n\n**route** [**-v**] [**-A** _family_|**-4**|**-6**] **add**|**del** [**-net**|**-host**] _target_ [**netmask** _mask_] [**gw** _gateway_] [**metric** _N_] [**mss** _M_] [**window** _W_] [**irtt** _I_] [**reject**] [**dev** _interface_]\n\n**route** [**-V**|**--version**] [**-h**|**--help**]"],["PARAMETERS","**-n**\n> Show numeric addresses instead of resolving hostnames; speeds up display by avoiding DNS lookups\n\n**-v**\n> Verbose output\n\n**-e**\n> Display routing table in **netstat**(8) format; use **-ee** for extended output including MTU, window, and irtt columns\n\n**-C**\n> Operate on the kernel routing cache instead of the FIB\n\n**-N**\n> Show symbolic network names rather than CIDR notation (opposite of **-n**)\n\n**-4**\n> Operate on IPv4 routes (alias for **-A inet**)\n\n**-6**\n> Operate on IPv6 routes (alias for **-A inet6**)\n\n**-A** _family_\n> Specify address family (e.g., **inet**, **inet6**)\n\n**-F**\n> Operate on the kernel FIB (Forwarding Information Base) routing table (default)\n\n**-V**, **--version**\n> Display version information\n\n**add**\n> Add a new route\n\n**del**\n> Delete a route\n\n**-net**\n> Target is a network address\n\n**-host**\n> Target is a single host address\n\n**gw** _gateway_\n> Route packets via the specified gateway; the gateway must already be reachable\n\n**netmask** _mask_\n> Specify the subnet mask for a network route\n\n**dev** _interface_\n> Force the route to be associated with the specified network interface\n\n**metric** _N_\n> Set the metric (priority) for the route; lower values are preferred\n\n**mss** _M_\n> Set the TCP Maximum Segment Size (MSS) in bytes for connections using this route\n\n**window** _W_\n> Set the TCP window size in bytes for connections using this route\n\n**irtt** _I_\n> Set the initial round-trip time in milliseconds (1–12000) used by TCP for this route\n\n**reject**\n> Install a blocking route that causes lookups to fail with \"Network unreachable\"; useful for preventing traffic to specific destinations\n\n**mod**, **dyn**, **reinstate**\n> Diagnostic flags set by routing daemons to mark dynamic or modified routes; not normally used manually"],["DESCRIPTION","**route** displays and manipulates the kernel IP routing table. The kernel uses routing tables to determine where to send network packets based on their destination addresses.\n\nWithout arguments, **route** displays the current routing table. The **-n** flag shows numeric addresses, avoiding DNS lookups and speeding up display.\n\nRoutes can be added for networks (ranges of addresses) or specific hosts. The default route (0.0.0.0/0 for IPv4) handles traffic that doesn't match any more specific route—typically pointing to the internet gateway.\n\nThe gateway specified with **gw** must be directly reachable via an existing route before a new route through it can be added. CIDR prefix notation (e.g., **10.0.0.0/8**) is supported for the target, and is equivalent to specifying a target with a **netmask**.\n\nModern Linux systems prefer **ip route** from the iproute2 package, but **route** remains available for compatibility and is familiar to administrators from older systems."],["OUTPUT COLUMNS","**Destination**: Target network or host address\n\n**Gateway**: Next-hop gateway address; `*` means the destination is directly reachable (no gateway needed)\n\n**Genmask**: Subnet mask for the destination; `255.255.255.255` for host routes, `0.0.0.0` for the default route\n\n**Flags**: Route state flags — **U**=up, **H**=host route, **G**=uses a gateway, **R**=reinstated, **D**=dynamically installed, **M**=modified by routing daemon, **A**=installed by addrconf, **!=**reject route\n\n**Metric**: Distance to the target, typically in hops; lower values are preferred when multiple routes match\n\n**Ref**: Number of references to this route (not used in Linux; always 0)\n\n**Use**: Count of route lookups\n\n**Iface**: Network interface used to send packets on this route"],["CAVEATS","Route changes made with **route** are not persistent across reboots. Use network configuration files or a network manager (such as NetworkManager or systemd-networkd) for permanent routes.\n\nThe **route** command is deprecated in favor of **ip route** from the iproute2 package, which offers more features and a consistent syntax.\n\nThe specified gateway must be reachable before adding a route through it; set up a direct route to the gateway first if necessary.\n\nModifying routes requires root privileges. Incorrect routing configuration can cause complete loss of network connectivity."],["INSTALL","```apt: sudo apt install net-tools```\n\n```dnf: sudo dnf install net-tools```\n\n```pacman: sudo pacman -S net-tools```\n\n```apk: sudo apk add net-tools```\n\n```zypper: sudo zypper install net-tools```\n\n```brew: brew install net-tools```\n\n```nix: nix profile install nixpkgs#net-tools```"],["SEE ALSO","[ip](/man/ip)(8), [ip-route](/man/ip-route)(8), [ifconfig](/man/ifconfig)(8), [netstat](/man/netstat)(8), [traceroute](/man/traceroute)(8)"]],"e":["ip","ip-route","ifconfig","netstat","traceroute"],"x":"route -n"},"shasum":{"n":"shasum","t":"Compute and verify SHA message digests","s":[["TAGLINE","Compute and verify SHA message digests"],["TLDR","**Calculate SHA-1 hash**\n\n```shasum [file]```\n\n**Calculate SHA-256 hash**\n\n```shasum -a 256 [file]```\n\n**Calculate SHA-512 hash**\n\n```shasum -a 512 [file]```\n\n**Check hashes from file**\n\n```shasum -c [checksums.txt]```\n\n**Calculate hash from stdin**\n\n```echo -n \"[text]\" | shasum```\n\n**Binary mode**\n\n```shasum -b [file]```\n\n**Create checksums for multiple files**\n\n```shasum -a 256 [*.tar.gz] > [SHA256SUMS]```"],["SYNOPSIS","**shasum** [_-a algorithm_] [_-b_] [_-c_] [_options_] [_files_]"],["PARAMETERS","**-a** _ALG_\n> Algorithm (1, 224, 256, 384, 512, 512224, 512256).\n\n**-b**\n> Binary mode.\n\n**-c**, **--check**\n> Check hashes from file.\n\n**-t**\n> Text mode (default).\n\n**-U**, **--UNIVERSAL**\n> Read in Universal Newlines mode (produces same digest on Windows/Unix/Mac).\n\n**-0**, **--01**\n> Read in BITS mode (ASCII '0' interpreted as 0-bit, '1' as 1-bit).\n\n**-s**, **--status**\n> Status only, don't output anything (with -c).\n\n**--ignore-missing**\n> Don't fail or report status for missing files (with -c).\n\n**-q**, **--quiet**\n> Don't print OK for each successfully verified file (with -c).\n\n**-w**, **--warn**\n> Warn about improperly formatted checksum lines.\n\n**--strict**\n> Exit non-zero for improperly formatted lines.\n\n**--tag**\n> BSD-style output including algorithm name.\n\n**-h**, **--help**\n> Show help."],["DESCRIPTION","**shasum** is a unified Perl-based utility for computing and verifying SHA message digests across all algorithm variants. The **-a** option selects the algorithm: SHA-1 (default), SHA-224, SHA-256, SHA-384, SHA-512, and the truncated SHA-512/224 and SHA-512/256 variants. SHA-256 is the most commonly used for software distribution verification and security applications.\n\nIn check mode (**-c**), shasum reads a file containing previously generated checksums and verifies each listed file against its recorded hash, reporting success or failure. Output format shows the hash followed by the filename, with BSD-style output (**--tag**) additionally including the algorithm name. Both formats are understood during verification.\n\nThis command is the standard SHA utility on macOS and systems with Perl installed, providing equivalent functionality to the separate **sha1sum**, **sha256sum**, and **sha512sum** commands found in GNU coreutils."],["CAVEATS","SHA-1 is cryptographically weakened - use SHA-256+ for security. Hash verification requires original hash to be trusted."],["HISTORY","**shasum** is a Perl script included with Perl. It provides a unified interface to SHA algorithms, mirroring the functionality of sha256sum and similar coreutils programs."],["INSTALL","```apt: sudo apt install perl```\n\n```dnf: sudo dnf install perl```\n\n```pacman: sudo pacman -S perl```\n\n```apk: sudo apk add perl-utils```\n\n```zypper: sudo zypper install perl```\n\n```brew: brew install perl```\n\n```nix: nix profile install nixpkgs#perl```"],["SEE ALSO","[sha1sum](/man/sha1sum)(1), [sha256sum](/man/sha256sum)(1), [md5sum](/man/md5sum)(1), [openssl](/man/openssl)(1), [b2sum](/man/b2sum)(1)"]],"e":["sha1sum","sha256sum","md5sum","openssl","b2sum"],"x":"shasum [file]"},"showfigfonts":{"n":"showfigfonts","t":"Preview available FIGlet fonts","s":[["TAGLINE","Preview available FIGlet fonts"],["TLDR","**Show all fonts**\n\n```showfigfonts```\n\n**Show specific text**\n\n```showfigfonts \"[Hello]\"```\n\n**Show fonts from directory**\n\n```showfigfonts -d [/usr/share/figlet]```"],["SYNOPSIS","**showfigfonts** [_-d dir_] [_text_]"],["PARAMETERS","**-d** _DIR_\n> Font directory.\n\n_text_\n> Sample text to display."],["DESCRIPTION","**showfigfonts** previews all available FIGlet fonts by rendering sample text in each one, making it easy to visually compare styles and select the right font for ASCII art. By default, each font's own name is used as the sample text, but custom text can be specified as an argument.\n\nThe **-d** option allows previewing fonts from any directory, not just the default FIGlet font path. Output shows the font name followed by the rendered text for each installed font, providing a convenient catalog for font selection."],["CAVEATS","Large font directories take time. Output can be very long. Some fonts are wide."],["HISTORY","**showfigfonts** is distributed with **figlet** for font preview. It helps users discover available fonts."],["INSTALL","```apt: sudo apt install figlet```\n\n```dnf: sudo dnf install figlet```\n\n```pacman: sudo pacman -S figlet```\n\n```apk: sudo apk add figlet```\n\n```zypper: sudo zypper install figlet```\n\n```brew: brew install figlet```\n\n```nix: nix profile install nixpkgs#figlet```"],["SEE ALSO","[figlet](/man/figlet)(1), [toilet](/man/toilet)(1), [banner](/man/banner)(1)"]],"e":["figlet","toilet","banner"],"x":"showfigfonts"},"slashem":{"n":"slashem","t":"Super Lotsa Added Stuff Hack roguelike game","s":[["TAGLINE","Super Lotsa Added Stuff Hack roguelike game"],["TLDR","**Start a game**\n\n```slashem```\n\n**Start with a specific player name**\n\n```slashem -u [name]```\n\n**Show high scores**\n\n```slashem -s```"],["SYNOPSIS","**slashem** [_options_]"],["PARAMETERS","**-u** _NAME_\n> Set player name.\n\n**-s**\n> Show high scores.\n\n**-X**\n> Discovery mode (non-scoring).\n\n**-D**\n> Debug/wizard mode."],["PREVIEW","```\n ------       -----\n |....|  ###  |...|\n |..@.|###   #|.>.|\n |....|      #|...|\n ------       -----\n```"],["DESCRIPTION","**SLASH'EM** (Super Lotsa Added Stuff Hack - Extended Magic) is a roguelike dungeon exploration game, a variant of NetHack with many additional features. It adds new roles, races, monsters, items, and dungeon branches while maintaining the core NetHack gameplay.\n\nControls are identical to NetHack: hjklyubn for movement, **i** for inventory, **?** for help."],["CAVEATS","Requires knowledge of NetHack-style roguelike controls. Permadeath applies. More content but also more complex than standard NetHack."],["HISTORY","**SLASH'EM** development began in **1997** as a merge of two NetHack variants: SLASH and Strstrng's Hack. It became one of the most popular NetHack variants."],["INSTALL","```brew: brew install slashem```"],["SEE ALSO","[nethack-console](/man/nethack-console)(6), [rogue](/man/rogue)(6), [angband](/man/angband)(6)"]],"e":["nethack-console","rogue","angband"],"x":"slashem"},"snmptest":{"n":"snmptest","t":"Interactive SNMP request testing tool","s":[["TAGLINE","Interactive SNMP request testing tool"],["TLDR","**Start interactive SNMP session**\n\n```snmptest -v [2c] -c [public] [host]```\n\n**Test with SNMPv3**\n\n```snmptest -v [3] -u [user] -l authPriv -a SHA -A [authpass] [host]```"],["SYNOPSIS","**snmptest** [_options_] _agent_"],["PARAMETERS","**-v** _version_\n> SNMP version (1, 2c, 3).\n\n**-c** _community_\n> Community string.\n\n**-u** _user_\n> SNMPv3 username.\n\n**-l** _level_\n> Security level."],["DESCRIPTION","**snmptest** provides an interactive SNMP session for testing and debugging. Send arbitrary SNMP requests interactively. Part of Net-SNMP suite for development and troubleshooting."],["INSTALL","```apt: sudo apt install snmp```\n\n```apk: sudo apk add net-snmp-tools```"],["SEE ALSO","[snmpget](/man/snmpget)(1), [snmpwalk](/man/snmpwalk)(1)"]],"e":["snmpget","snmpwalk"],"x":"snmptest -v [2c] -c [public] [host]"},"su":{"n":"su","t":"Switch user identity or run commands","s":[["TAGLINE","Switch user identity or run commands"],["TLDR","**Switch** to superuser (root)\n\n```su```\n\n**Switch** to a specific user\n\n```su [username]```\n\n**Switch** to user with full login shell\n\n```su - [username]```\n\n**Execute** a command as another user\n\n```su - [username] -c \"[command]\"```\n\n**Switch** to user with a specific shell\n\n```su -s /[path/to/shell] [username]```"],["SYNOPSIS","**su** [_options_] [_-_] [_user_]"],["PARAMETERS","**-, -l, --login**\n> Provide a login shell environment\n\n**-c, --command _command_**\n> Pass a single command to the shell\n\n**-s, --shell _shell_**\n> Use the specified shell instead of the default\n\n**-p, --preserve-environment**\n> Preserve the entire environment\n\n**-m**\n> Preserve environment (same as -p)\n\n**-g, --group _group_**\n> Specify the primary group"],["DESCRIPTION","**su** (substitute user) allows running a shell or command as a different user. Without a username, it switches to the root user by default.\n\nThe difference between **su** and **su -** is that the latter provides a full login environment, resetting environment variables and changing to the target user's home directory."],["CAVEATS","Requires the target user's password (or root privileges). On many modern systems, **sudo** is preferred for running commands as root. Using **su -** is recommended over plain **su** to get a clean environment."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add heimdal-su```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[sudo](/man/sudo)(8), [login](/man/login)(1), [runuser](/man/runuser)(1)"]],"e":["sudo","login","runuser"],"x":"su"},"systemctl-revert":{"n":"systemctl-revert","t":"Restore units to vendor defaults","s":[["TAGLINE","Restore units to vendor defaults"],["TLDR","**Revert** units to vendor defaults\n\n```systemctl revert [unit1 unit2 ...]```\n\nRevert **user** unit\n\n```systemctl revert [unit] --user```"],["SYNOPSIS","**systemctl revert** _UNIT_..."],["PARAMETERS","**--user**\n> Operate on user units"],["DESCRIPTION","**systemctl revert** restores unit files to their original vendor versions by removing all local customizations. This undoes the effects of `edit`, `enable`, `disable`, `set-property`, and `mask` commands.\n\nIt removes drop-in files from `/etc/systemd/system/<unit>.d/`, the unit file from `/etc/systemd/system/` if it's a local copy, and any symlinks created by enable/disable/mask."],["CAVEATS","Irreversibly removes all local customizations. Cannot recover deleted customizations without backups. Use carefully on production systems."],["HISTORY","The **revert** subcommand provides a clean way to reset unit configuration to distribution defaults, useful when troubleshooting or when customizations are no longer needed."],["SEE ALSO","[systemctl-edit](/man/systemctl-edit)(1), [systemctl-mask](/man/systemctl-mask)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemctl-edit","systemctl-mask","systemctl"],"x":"systemctl revert [unit1 unit2 ...]"},"tb":{"n":"tb","t":"Terminal task and note board","s":[["TAGLINE","Terminal task and note board"],["TLDR","**Display all tasks and notes**\n\n```tb```\n\n**Create a new task**\n\n```tb -t [Buy groceries]```\n\n**Create a task on a specific board**\n\n```tb -t @[work] [Finish report]```\n\n**Create a note**\n\n```tb -n [Remember to call John]```\n\n**Check/uncheck tasks** as complete\n\n```tb -c [1] [2]```\n\n**Set task priority** (1=normal, 2=medium, 3=high)\n\n```tb -p [1] [3]```\n\n**Delete items**\n\n```tb -d [1] [2]```\n\n**Search for items**\n\n```tb -f [keyword]```\n\n**Display timeline view**\n\n```tb -i```"],["SYNOPSIS","**tb** [_options_] [_arguments_]"],["PARAMETERS","**-t**, **--task** _description_\n> Create a new task; use @board to assign to boards\n\n**-n**, **--note** _body_\n> Create a new note\n\n**-c**, **--check** _ids_\n> Toggle completion status of tasks\n\n**-b**, **--begin** _ids_\n> Start or pause tasks\n\n**-s**, **--star** _ids_\n> Star or unstar items\n\n**-p**, **--priority** _id_ _level_\n> Set task priority (1=normal, 2=medium, 3=high)\n\n**-m**, **--move** _id_ _@board_\n> Move item to a different board\n\n**-d**, **--delete** _ids_\n> Delete items (moves to archive)\n\n**-e**, **--edit** _id_ _description_\n> Edit item description\n\n**-f**, **--find** _keywords_\n> Search items by keywords\n\n**-l**, **--list** _attributes_\n> List items filtered by attributes\n\n**-a**, **--archive**\n> Display archived items\n\n**-r**, **--restore** _ids_\n> Restore items from archive\n\n**--clear**\n> Delete all checked (completed) tasks\n\n**-y**, **--copy** _ids_\n> Copy item descriptions to clipboard\n\n**-i**, **--timeline**\n> Display items grouped by creation date\n\n**--taskbook-dir** _path_\n> Use a custom taskbook storage directory\n\n**-h**, **--help**\n> Show help information\n\n**-v**, **--version**\n> Show version number"],["DESCRIPTION","**tb** (Taskbook) is a command-line task and note manager organized around boards. It provides a minimal interface for creating, organizing, and tracking tasks and notes directly from the terminal.\n\nItems are organized into boards using the **@boardname** syntax. Tasks without a board assignment go to the default \"My Board\". Multiple boards can be assigned to a single item. The interface displays completion statistics showing pending, done, and note counts.\n\nTasks can have priorities (shown with different colors), be starred for emphasis, and marked as in-progress with the begin command. Deleted items are archived and can be restored. The timeline view groups items by creation date instead of boards.\n\nData is stored in JSON format at **~/.taskbook/storage**. Configuration options can be set in **~/.taskbook.json**."],["CAVEATS","Taskbook requires Node.js and npm for installation. The archive grows over time with deleted items; periodic manual cleanup may be needed. Board names are case-sensitive."],["HISTORY","**Taskbook** was created by **Klaus Sinani** (klaudiosinani) and released on **GitHub** in **2018**. Written in JavaScript for Node.js, it was designed to bring task management directly into the terminal workflow, emphasizing simplicity and a minimal learning curve."],["SEE ALSO","[task](/man/task)(1), [todo.txt-cli](/man/todo.txt-cli)(1), [todoist](/man/todoist)(1)"]],"e":["task","todo.txt-cli","todoist"],"x":"tb"},"tiv":{"n":"tiv","t":"Display images in terminal using ANSI","s":[["TAGLINE","Display images in terminal using ANSI"],["TLDR","**Display an image**\n\n```tiv [image.png]```\n\n**Display with specific width**\n\n```tiv -w [80] [image.jpg]```\n\n**Display with specific height**\n\n```tiv -h [24] [image.png]```\n\n**Display multiple images**\n\n```tiv [image1.png] [image2.jpg] [image3.gif]```\n\n**Show a directory as a thumbnail grid**\n\n```tiv [/path/to/images/]```\n\n**Use a custom number of thumbnail columns**\n\n```tiv -c [5] [/path/to/images/]```\n\n**Use 256-color mode** (e.g. macOS Terminal)\n\n```tiv -2 [image.png]```"],["SYNOPSIS","**tiv** [_options_] _image_ [_image_...]"],["PARAMETERS","**-w** _num_\n> Set the maximum output width to _num_ characters.\n\n**-h** _num_\n> Set the maximum output height to _num_ lines.\n\n**-2**, **--256**\n> Use 256 colors instead of 24-bit true color. Needed to display properly on macOS Terminal.\n\n**-0**\n> No block character adjustment: always use the top half block character.\n\n**-c** _num_\n> Number of thumbnail columns in 'dir' mode (3 by default).\n\n**-d**, **--dir**\n> Force 'dir' (thumbnail grid) mode. Selected automatically for more than one input.\n\n**-f**, **--full**\n> Force 'full' mode. Selected automatically for a single input.\n\n**-C** _hex_\n> Use _hex_ color (0xFFFFFF white by default) as the background for transparent PNG/GIF.\n\n**-x**\n> Use new Unicode Teletext/legacy characters (experimental).\n\n**--help**\n> Display help information."],["DESCRIPTION","**tiv** (Terminal Image Viewer) displays images directly in the terminal using RGB ANSI escape codes and Unicode block graphics characters. It maps each 4x8 pixel cell to the block character and foreground/background colors that best approximate the original.\n\nImages are loaded with the bundled CImg library: PNG support is compiled in, while other formats (JPEG, GIF, BMP, and more) are handled by invoking ImageMagick's `convert`. Images are scaled to fit the terminal while preserving aspect ratio.\n\nWhen given a directory or more than one file, **tiv** switches to 'dir' mode and renders a grid of thumbnails (override with **-f**/**-d**). For best results, use a terminal with true color (24-bit) support. The **-2** 256-color mode works on older terminals but produces lower quality output. Block characters provide roughly 2x vertical resolution compared to standard characters."],["CAVEATS","Quality depends on terminal capabilities and font. True color terminal support gives the best results. Non-PNG formats require ImageMagick to be installed. Animated GIFs display only the first frame. Results vary significantly between terminal emulators."],["HISTORY","**tiv** is the C++ TerminalImageViewer by Stefan Haustein, a lightweight way to preview images without leaving the terminal. It renders with RGB ANSI codes and Unicode block characters and is popular among developers who work primarily in terminals. Several unrelated tools also ship a `tiv` binary, so behavior can differ between installations."],["INSTALL","```apt: sudo apt install tiv```\n\n```brew: brew install tiv```\n\n```nix: nix profile install nixpkgs#tiv```"],["SEE ALSO","[chafa](/man/chafa)(1), [viu](/man/viu)(1), [timg](/man/timg)(1), [catimg](/man/catimg)(1), [img2txt](/man/img2txt)(1), [jp2a](/man/jp2a)(1)"]],"e":["chafa","viu","timg","catimg","img2txt","jp2a"],"x":"tiv [image.png]"},"tlmgr-candidates":{"n":"tlmgr-candidates","t":"Show available repository candidates for a TeX Live package","s":[["TAGLINE","Show available repository candidates for a TeX Live package"],["TLDR","**Show package candidates**\n\n```tlmgr candidates [package]```"],["SYNOPSIS","**tlmgr candidates** _package_"],["PARAMETERS","_package_\n> Package name to check."],["DESCRIPTION","**tlmgr candidates** shows which repositories provide a package. Displays available versions from configured repositories. Part of TeX Live Manager for multi-repository setups."],["SEE ALSO","[tlmgr](/man/tlmgr)(1), [tlmgr-repository](/man/tlmgr-repository)(1), [tlmgr-info](/man/tlmgr-info)(1)"]],"e":["tlmgr","tlmgr-repository","tlmgr-info"],"x":"tlmgr candidates [package]"},"vdirsyncer":{"n":"vdirsyncer","t":"Synchronize calendars and contacts","s":[["TAGLINE","Synchronize calendars and contacts"],["TLDR","**Discover collections from servers**\n\n```vdirsyncer discover```\n\n**Synchronize all configured pairs**\n\n```vdirsyncer sync```\n\n**Synchronize specific pair**\n\n```vdirsyncer sync [pair_name]```\n\n**Repair item storage**\n\n```vdirsyncer repair [storage_name]```\n\n**Show version**\n\n```vdirsyncer --version```"],["SYNOPSIS","**vdirsyncer** [_options_] _command_ [_arguments_]"],["COMMANDS","**discover**: Find collections on remote servers.\n\n**sync**: Synchronize configured pairs.\n\n**repair**: Fix storage inconsistencies.\n\n**metasync**: Sync metadata like colors."],["PARAMETERS","**--verbosity** _level_\n> Set output verbosity (DEBUG, INFO, WARNING, etc.).\n\n**--config** _file_\n> Use alternative config file.\n\n**--version**\n> Display version.\n\n**--help**\n> Display help."],["DESCRIPTION","**vdirsyncer** synchronizes calendars (CalDAV) and contacts (CardDAV) between servers and local storage. It downloads remote data to local directories where tools like khal and khard can access and modify them.\n\nThe tool supports bidirectional sync, meaning local changes sync back to servers. It can also synchronize between two servers directly. Configuration defines storage pairs specifying source and destination.\n\nFirst run requires discover to detect available collections (calendars/addressbooks), then sync downloads the data. Subsequent syncs transfer only changes in both directions."],["CONFIGURATION","Config file: ~/.config/vdirsyncer/config\n\nDefines storages (local or remote) and pairs connecting them. Supports CalDAV, CardDAV, and local filesystem storage types."],["CAVEATS","Initial discover is required before sync. Google CardDAV has known issues with data integrity. Always backup before first sync. Conflicts may need manual resolution."],["HISTORY","**vdirsyncer** was created as part of the pimutils project to provide command-line personal information management. It fills the role for calendars and contacts that OfflineIMAP fills for email, enabling offline access and local tooling for CalDAV/CardDAV data."],["INSTALL","```dnf: sudo dnf install vdirsyncer```\n\n```pacman: sudo pacman -S vdirsyncer```\n\n```apk: sudo apk add vdirsyncer```\n\n```brew: brew install vdirsyncer```\n\n```nix: nix profile install nixpkgs#vdirsyncer```"],["SEE ALSO","[khal](/man/khal)(1), [calcurse](/man/calcurse)(1)"]],"e":["khal","calcurse"],"x":"vdirsyncer discover"},"vidir":{"n":"vidir","t":"Edit filenames in a text editor","s":[["TAGLINE","Edit filenames in a text editor"],["TLDR","**Edit filenames in editor**\n\n```vidir```\n\n**Edit specific files**\n\n```vidir [*.txt]```\n\n**Edit directory contents**\n\n```vidir [path/to/directory/]```\n\n**Use specific editor**\n\n```EDITOR=[vim] vidir```\n\n**Verbose mode**\n\n```vidir -v```"],["SYNOPSIS","**vidir** [_-v_] [_files_|_directory_]"],["PARAMETERS","**-v**, **--verbose**\n> Verbose output.\n\n_files_\n> Files to edit.\n\n_directory_\n> Directory to edit."],["ENVIRONMENT","**EDITOR** - Editor to use\n**VISUAL** - Visual editor"],["DESCRIPTION","**vidir** opens a list of filenames in a text editor, where each line contains a number and a filename. Editing the filenames renames the corresponding files, and deleting lines removes them. All changes are applied when you save and quit the editor.\n\nThis approach leverages familiar editor features like search-and-replace, macros, and multi-cursor editing for powerful batch renaming operations that would be tedious with individual **mv** commands."],["EXAMPLE","Editor shows:\n```\n1\tfile1.txt\n2\tfile2.txt\n3\told_name.doc\n```\n\nEdit to:\n```\n1\tdocument1.txt\n2\tdocument2.txt\n3\tnew_name.doc\n```"],["CAVEATS","Part of the **moreutils** package. Deleting a line removes the file permanently. Do not change the line numbers at the beginning of each line. Use **-v** to see what operations are performed."],["HISTORY","**vidir** is part of **moreutils** by **Joey Hess**. It provides intuitive file renaming using familiar editor interfaces."],["INSTALL","```apt: sudo apt install moreutils```\n\n```dnf: sudo dnf install moreutils```\n\n```pacman: sudo pacman -S moreutils```\n\n```apk: sudo apk add moreutils```\n\n```zypper: sudo zypper install moreutils```\n\n```brew: brew install moreutils```\n\n```nix: nix profile install nixpkgs#moreutils```"],["SEE ALSO","[qmv](/man/qmv)(1), [rename](/man/rename)(1), [mmv](/man/mmv)(1)"]],"e":["qmv","rename","mmv"],"x":"vidir"},"warp-cli":{"n":"warp-cli","t":"Cloudflare WARP VPN client CLI","s":[["TAGLINE","Cloudflare WARP VPN client CLI"],["TLDR","**Connect**\n\n```warp-cli connect```\n\n**Disconnect**\n\n```warp-cli disconnect```\n\n**Check status**\n\n```warp-cli status```\n\n**Register**\n\n```warp-cli register```\n\n**Set mode**\n\n```warp-cli set-mode warp```\n\n**Show settings**\n\n```warp-cli settings```"],["SYNOPSIS","**warp-cli** _command_ [_options_]"],["PARAMETERS","**connect**\n> Connect to WARP.\n\n**disconnect**\n> Disconnect.\n\n**status**\n> Show status.\n\n**register**\n> Register device.\n\n**set-mode** _MODE_\n> Set connection mode.\n\n**settings**\n> Show settings."],["DESCRIPTION","**warp-cli** is the command-line interface for managing Cloudflare WARP, a VPN and secure DNS service that encrypts traffic between the device and Cloudflare's network. It provides commands to connect, disconnect, check status, and configure the WARP client.\n\nThe client supports multiple connection modes including full WARP VPN mode that routes all traffic through Cloudflare's network, and DNS-over-HTTPS mode that only encrypts DNS queries for private name resolution. A WARP+ premium tier offers optimized routing through Cloudflare's Argo network for improved performance.\n\nDevice registration is required before first use, and the warp-svc daemon must be running for the CLI to function. The tool is available on Linux, macOS, and Windows."],["CAVEATS","Cloudflare service. Registration required. Daemon must run."],["HISTORY","**warp-cli** is the CLI for **Cloudflare WARP**, a VPN and DNS service by Cloudflare."],["INSTALL","```apt: sudo apt install libghc-wai-app-static-dev```\n\n```pacman: sudo pacman -S warp```\n\n```apk: sudo apk add warp```\n\n```zypper: sudo zypper install warp```\n\n```nix: nix profile install nixpkgs#warp```"],["SEE ALSO","[cloudflared](/man/cloudflared)(1)"]],"e":["cloudflared"],"x":"warp-cli connect"},"wofi":{"n":"wofi","t":"Wayland application launcher","s":[["TAGLINE","Wayland application launcher"],["TLDR","Show **apps** list\n\n```wofi -S drun```\n\nShow **all** commands\n\n```wofi -S run```\n\n**Dmenu** mode from stdin\n\n```printf \"Choice1\\nChoice2\\nChoice3\" | wofi -d```"],["SYNOPSIS","**wofi** [_OPTIONS_]"],["PARAMETERS","**-S, --show** _MODE_\n> Show mode: drun (applications), run (commands), dmenu\n\n**-d, --dmenu**\n> Run in dmenu mode (read from stdin)\n\n**--style** _FILE_\n> Custom CSS stylesheet\n\n**--conf** _FILE_\n> Configuration file"],["DESCRIPTION","**wofi** is an application launcher for wlroots-based Wayland compositors. It functions similarly to rofi and dmenu, providing a searchable menu for launching applications or selecting items.\n\nThe drun mode shows desktop applications while run mode shows all executables in PATH."],["CAVEATS","Wayland only (wlroots-based compositors). Styling uses CSS. Some rofi features may not be available."],["INSTALL","```apt: sudo apt install wofi```\n\n```dnf: sudo dnf install wofi```\n\n```pacman: sudo pacman -S wofi```\n\n```apk: sudo apk add wofi```\n\n```zypper: sudo zypper install wofi```\n\n```nix: nix profile install nixpkgs#wofi```"],["SEE ALSO","[rofi](/man/rofi)(1), [dmenu](/man/dmenu)(1), [fuzzel](/man/fuzzel)(1)"]],"e":["rofi","dmenu","fuzzel"],"x":"wofi -S drun"},"xbps-reconfigure":{"n":"xbps-reconfigure","t":"Reconfigure Void Linux packages","s":[["TAGLINE","Reconfigure Void Linux packages"],["TLDR","**Reconfigure a package**\n\n```sudo xbps-reconfigure [package_name]```\n\n**Force reconfigure a package**\n\n```sudo xbps-reconfigure -f [package_name]```\n\n**Reconfigure all packages**\n\n```sudo xbps-reconfigure -a```\n\n**Reconfigure kernel**\n\n```sudo xbps-reconfigure -f linux[5.15]```\n\n**Verbose output**\n\n```sudo xbps-reconfigure -v [package_name]```\n\n**Reconfigure in alternate root**\n\n```sudo xbps-reconfigure -r [/mnt] [package_name]```"],["SYNOPSIS","**xbps-reconfigure** [_options_] _package_ | **-a**"],["PARAMETERS","**-a**, **--all**\n> Reconfigure all packages.\n\n**-f**, **--force**\n> Force reconfiguration even if already configured.\n\n**-r** _rootdir_\n> Use alternate root directory.\n\n**-v**, **--verbose**\n> Verbose output.\n\n**-d**, **--debug**\n> Debug mode.\n\n**-h**, **--help**\n> Display help.\n\n**-V**, **--version**\n> Display version."],["DESCRIPTION","**xbps-reconfigure** runs post-installation configuration scripts for packages on Void Linux. It's used to configure packages that weren't properly configured or to reconfigure after changing configuration files.\n\nThe tool is useful for recovering from interrupted installations, reconfiguring after manual config changes, or rebuilding kernel modules. By default, only unconfigured packages are processed; use -f to force reconfiguration.\n\nCommon use cases include reconfiguring the kernel after updates, rerunning DKMS module builds, and fixing packages with configuration issues."],["CAVEATS","Void Linux specific (XBPS package manager). Some packages may overwrite manual configuration changes when reconfigured. Kernel reconfiguration requires specifying exact version."],["HISTORY","**xbps-reconfigure** is part of XBPS (X Binary Package System), the package manager developed specifically for Void Linux. XBPS was designed from scratch to be fast, portable, and bug-free, providing a modern alternative to traditional package managers."],["INSTALL","```apk: sudo apk add xbps```\n\n```nix: nix profile install nixpkgs#xbps```"],["SEE ALSO","[xbps-install](/man/xbps-install)(1), [xbps-remove](/man/xbps-remove)(1), [xbps-query](/man/xbps-query)(1)"]],"e":["xbps-install","xbps-remove","xbps-query"],"x":"sudo xbps-reconfigure [package_name]"},"xml-list":{"n":"xml-list","t":"List element structure of an XML document","s":[["TAGLINE","List element structure of an XML document"],["TLDR","**List all elements** in the document\n\n```xml el [file.xml]```\n\n**Show elements with attributes**\n\n```xml el -a [file.xml]```\n\n**Show elements with attributes and values**\n\n```xml el -v [file.xml]```\n\n**Show unique element paths**\n\n```xml el -u [file.xml]```"],["SYNOPSIS","**xml el** [_options_] _file_"],["PARAMETERS","_FILE_\n> XML input file (use - for stdin).\n\n**-a**\n> Show attributes of each element.\n\n**-v**\n> Show attributes and values.\n\n**-u**\n> Show only unique element paths."],["DESCRIPTION","**xml el** (also accepted as **xml elements** / **xml list**) prints the element structure of an XML document as XPath-like paths, one per line. It is part of the **xmlstarlet** toolkit.\n\nUseful for quickly exploring unfamiliar XML files, producing summaries of element hierarchies, or generating templates for **xml sel** queries."],["INSTALL","```dnf: sudo dnf install xmlstarlet```\n\n```pacman: sudo pacman -S xmlstarlet```\n\n```apk: sudo apk add xmlstarlet```\n\n```zypper: sudo zypper install xmlstarlet```\n\n```brew: brew install xmlstarlet```\n\n```nix: nix profile install nixpkgs#xmlstarlet```"],["SEE ALSO","[xml-elements](/man/xml-elements)(1), [xml-select](/man/xml-select)(1), [xmlstarlet](/man/xmlstarlet)(1)"]],"e":["xml-elements","xml-select","xmlstarlet"],"x":"xml el [file.xml]"}});
