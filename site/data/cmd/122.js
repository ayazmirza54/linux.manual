window.LCL_BUCKET(122,{"accelerate":{"n":"accelerate","t":"distributed PyTorch training launcher","s":[["TAGLINE","distributed PyTorch training launcher"],["TLDR","**Launch** a training script with default configuration\n\n```accelerate launch [train.py]```\n\n**Configure** accelerate for your hardware\n\n```accelerate config```\n\nLaunch with **specific GPU** configuration\n\n```accelerate launch --num_processes [4] --gpu_ids [0,1,2,3] [train.py]```\n\nLaunch training on **multiple machines**\n\n```accelerate launch --num_machines [2] --machine_rank [0] --main_process_ip [192.168.1.1] [train.py]```"],["SYNOPSIS","**accelerate** _command_ [_options_] [_script_] [_script_args_]"],["DESCRIPTION","**accelerate** is a Hugging Face library that enables PyTorch code to run on any distributed configuration with minimal code changes. It handles the complexity of distributed training across multiple GPUs, TPUs, and machines while keeping your training code simple.\n\nThe tool abstracts away the boilerplate needed for mixed precision training, gradient accumulation, and multi-device parallelism. It automatically detects available hardware and configures the training environment appropriately."],["PARAMETERS","**config**\n> Run the configuration wizard to set up your environment\n\n**launch**\n> Launch a training script with the configured settings\n\n**--num_processes** _n_\n> Total number of processes to launch\n\n**--gpu_ids** _ids_\n> Comma-separated GPU IDs to use\n\n**--mixed_precision** _type_\n> Enable mixed precision: no, fp16, bf16\n\n**--num_machines** _n_\n> Number of machines for distributed training\n\n**--machine_rank** _n_\n> Rank of the current machine (0-indexed)\n\n**--main_process_ip** _ip_\n> IP address of the main machine\n\n**--main_process_port** _port_\n> Port for the main machine (default: 29500)\n\n**--use_deepspeed**\n> Enable DeepSpeed for training\n\n**--use_fsdp**\n> Enable Fully Sharded Data Parallel\n\n**test**\n> Test your accelerate configuration\n\n**env**\n> Print environment information"],["CONFIGURATION","Running **accelerate config** creates a YAML configuration file at **~/.cache/huggingface/accelerate/default_config.yaml**. This file stores settings for compute environment type, distributed training backend, number of processes, mixed precision mode, and DeepSpeed/FSDP options. The configuration can also be specified per-project by placing an **accelerate_config.yaml** in the project directory or by passing **--config_file** to the launch command."],["CAVEATS","Requires PyTorch to be installed. Configuration should match your actual hardware; mismatches can cause silent failures or crashes. DeepSpeed and FSDP have additional dependencies. Some features require specific GPU architectures (e.g., bf16 requires Ampere or newer)."],["HISTORY","**accelerate** was developed by Hugging Face and first released in **2021**. It was created to simplify distributed training and mixed precision workflows, reducing the barrier to training large models on diverse hardware configurations."],["SEE ALSO","[python](/man/python)(1), [torchrun](/man/torchrun)(1)"],["RESOURCES","```[Source code](https://github.com/huggingface/accelerate)```\n\n```[Documentation](https://huggingface.co/docs/accelerate)```"]],"e":["python","torchrun"],"x":"accelerate launch [train.py]"},"afmtodit":{"n":"afmtodit","t":"Adobe font metrics to groff font description converter","s":[["TAGLINE","Adobe font metrics to groff font description converter"],["TLDR","**Convert** AFM file to groff font\n\n```afmtodit [font.afm] [textmap] [fontname]```\n\nGenerate **italic corrections** (in thousandths of an em)\n\n```afmtodit -i [50] [font.afm] [textmap] [fontname]```\n\nConvert with **encoding** file\n\n```afmtodit -e [encoding.enc] [font.afm] [textmap] [fontname]```"],["SYNOPSIS","**afmtodit** [_-ckmnsvx_] [_-a n_] [_-d desc_] [_-e enc_] [_-f name_] [_-i n_] [_-o out_] _afm-file_ _map-file_ _font_"],["DESCRIPTION","**afmtodit** converts Adobe Font Metrics (AFM) files to font description files suitable for use with the groff typesetting system. It creates the font metrics files that groff's PostScript driver needs to properly position and space characters when typesetting documents.\n\nThe tool maps PostScript glyph names to groff character names using a user-supplied map file, and includes kerning pairs and ligature information in the output by default. It also computes a slant parameter (from the AFM ItalicAngle or the **-a** option) that groff uses when positioning accents over italic glyphs."],["PARAMETERS","**-a** _n_\n> Slant value written to the font file, used by groff to position accents (default: negative ItalicAngle from the AFM file).\n\n**-c**\n> Include identifying comments for the PostScript font in the output.\n\n**-d** _file_\n> Use an alternate device description (DESC) file instead of the default.\n\n**-e** _file_\n> Reencode the PostScript font using the given encoding file.\n\n**-f** _name_\n> Internal name of the groff font.\n\n**-i** _n_\n> Generate italic and subscript corrections from _n_ (in thousandths of an em).\n\n**-k**\n> Omit kerning data; use only for monospaced (constant-width) fonts.\n\n**-m**\n> Prevent negative left italic correction values.\n\n**-n**\n> Do not output a ligatures command; use for monospaced fonts.\n\n**-o** _file_\n> Output file name\n\n**-s**\n> Mark the font as special.\n\n**-v**\n> Print version information and exit.\n\n**-x**\n> Do not use the built-in Adobe Glyph List."],["CAVEATS","Requires valid AFM files from the font vendor. The text map file must correctly map PostScript glyph names. Output is specific to groff's PostScript driver."],["HISTORY","**afmtodit** is part of GNU groff, developed since **1989**. It enables the use of PostScript fonts in groff documents, extending the typesetting system beyond its original font set."],["INSTALL","```apt: sudo apt install groff```\n\n```dnf: sudo dnf install groff```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[groff](/man/groff)(1), [addftinfo](/man/addftinfo)(1)"],["RESOURCES","```[Source code](https://git.savannah.gnu.org/cgit/groff.git)```\n\n```[Homepage](https://www.gnu.org/software/groff/)```\n\n```[Documentation](https://www.gnu.org/software/groff/manual/)```"]],"e":["groff","addftinfo"],"x":"afmtodit [font.afm] [textmap] [fontname]"},"autopsy":{"n":"autopsy","t":"Web-based digital forensics analysis interface","s":[["TAGLINE","Web-based digital forensics analysis interface"],["TLDR","**Start** Autopsy server\n\n```autopsy```\n\nStart on **specific port**\n\n```autopsy -p [9999]```\n\nRestrict access to **specific host**\n\n```autopsy [localhost]```\n\nSpecify **evidence locker** directory\n\n```autopsy -d [path/to/locker]```"],["SYNOPSIS","**autopsy** [_-c_] [_-C_] [_-d evid_locker_] [_-i device filesystem mnt_] [_-p port_] [_addr_]"],["DESCRIPTION","**autopsy** is a graphical interface for The Sleuth Kit forensic analysis tools. It starts a local web server and provides a browser-based interface for disk analysis, file recovery, and forensic investigation.\n\nThe tool allows examiners to analyze file systems, recover deleted files, create timelines, and search for evidence without command-line knowledge."],["PARAMETERS","**-p** _port_\n> HTTP server port (default: 9999)\n\n**-c**\n> Force cookie inclusion in URL (even for localhost)\n\n**-C**\n> Force no cookie in URL\n\n**-d** _dir_\n> Specify evidence locker directory (overrides default)\n\n**-i** _device filesystem mnt_\n> Enable live analysis mode (specify device, filesystem type, and mount point)\n\n_addr_\n> IP address or hostname restricting which client can connect"],["FEATURES","- File system analysis\n- Deleted file recovery\n- Timeline creation\n- Keyword searching\n- Hash filtering\n- Image mounting"],["CAVEATS","For authorized forensic investigation only. Web interface requires browser. Legacy version (v2); Autopsy 4 is a standalone Java desktop application. The Sleuth Kit tools must be installed."],["HISTORY","**Autopsy** was created by **Brian Carrier** as a web-based front-end for The Sleuth Kit, first released in **2001**. Version 4 (2015) moved to a Java desktop application."],["INSTALL","```aur: yay -S autopsy```\n\n```nix: nix profile install nixpkgs#autopsy```"],["SEE ALSO","[foremost](/man/foremost)(1), [bulk_extractor](/man/bulk_extractor)(1)"]],"e":["foremost","bulk_extractor"],"x":"autopsy"},"avahi-autoipd.action":{"n":"avahi-autoipd.action","t":"Handle IPv4 link-local address events","s":[["TAGLINE","Handle IPv4 link-local address events"],["TLDR","**Add** an acquired IPv4LL address to a network interface (BIND event)\n\n```/etc/avahi/avahi-autoipd.action BIND [interface] [ip_address]```\n\n**Remove** address due to conflict detection (CONFLICT event)\n\n```/etc/avahi/avahi-autoipd.action CONFLICT [interface] [ip_address]```\n\n**Remove** address when routable address becomes available (UNBIND event)\n\n```/etc/avahi/avahi-autoipd.action UNBIND [interface] [ip_address]```\n\n**Remove** address on daemon shutdown (STOP event)\n\n```/etc/avahi/avahi-autoipd.action STOP [interface] [ip_address]```"],["SYNOPSIS","**/etc/avahi/avahi-autoipd.action** _event_ _interface_ _address_"],["PARAMETERS","**_event_**\n> Event type: **BIND**, **CONFLICT**, **UNBIND**, or **STOP**\n\n**_interface_**\n> Network interface name (e.g., eth0, wlan0)\n\n**_address_**\n> IPv4LL address from the 169.254.0.0/16 range"],["DESCRIPTION","**avahi-autoipd.action** is the action script automatically called by **avahi-autoipd** whenever an IPv4 Link-Local address has been acquired or when an IP address conflict is detected. The script handles adding or removing the specified address from the network interface.\n\nThe script receives three arguments: an event string, the network interface name, and the IPv4LL address. Based on the event type, it performs the appropriate network configuration:\n\n> **BIND** - An IP address has been successfully acquired; the script adds it to the network interface\n\n> **CONFLICT** - avahi-autoipd detected the IP address is already in use on the local network; the script removes the configured address\n\n> **UNBIND** - A routable address has been configured to the interface by another program; the script removes the IPv4LL address to avoid conflicts\n\n> **STOP** - avahi-autoipd is shutting down; the script removes the IP address from the interface\n\nThe default script typically uses **ip** or **ifconfig** commands to manipulate interface addresses. Users can customize this script to integrate with specific network management systems or add additional configuration steps."],["CAVEATS","The script runs with elevated privileges and is executed automatically by **avahi-autoipd**. Modifications to this script should be tested carefully as errors can prevent proper IPv4LL address configuration. Always maintain a backup before customization."],["SEE ALSO","[avahi-autoipd](/man/avahi-autoipd)(8), [ip](/man/ip)(8), [ifconfig](/man/ifconfig)(8)"]],"e":["avahi-autoipd","ip","ifconfig"],"x":"/etc/avahi/avahi-autoipd.action BIND [interface] [ip_address]"},"bcftools":{"n":"bcftools","t":"Variant calling and VCF/BCF file manipulation","s":[["TAGLINE","Variant calling and VCF/BCF file manipulation"],["TLDR","**View a VCF/BCF file**\n\n```bcftools view [input.vcf.gz]```\n\n**Filter variants** by region\n\n```bcftools view -r [chr1:1000000-2000000] [input.vcf.gz]```\n\n**Convert VCF to BCF**\n\n```bcftools view -Ob -o [output.bcf] [input.vcf.gz]```\n\n**Call variants** from aligned reads\n\n```bcftools mpileup -f [reference.fa] [aligned.bam] | bcftools call -mv -Oz -o [calls.vcf.gz]```\n\n**Merge multiple VCF files**\n\n```bcftools merge [file1.vcf.gz] [file2.vcf.gz] -Oz -o [merged.vcf.gz]```\n\n**Filter variants** by quality\n\n```bcftools filter -i 'QUAL>30 && DP>10' [input.vcf.gz] -Oz -o [filtered.vcf.gz]```\n\n**Extract sample genotypes**\n\n```bcftools query -f '%CHROM\\t%POS\\t%REF\\t%ALT[\\t%GT]\\n' [input.vcf.gz]```\n\n**Index a VCF/BCF file**\n\n```bcftools index [input.vcf.gz]```"],["SYNOPSIS","**bcftools** _subcommand_ [_options_] [_file_]"],["DESCRIPTION","**bcftools** is a suite of utilities for variant calling and manipulating files in the Variant Call Format (VCF) and its binary counterpart BCF. It is part of the SAMtools/HTSlib project and is widely used in bioinformatics for genomic variant analysis.\n\nAll commands work transparently with both VCF and BCF files, compressed or uncompressed. The tools are designed to work in pipelines, reading from stdin and writing to stdout."],["SUBCOMMANDS","**Variant Calling**\n> mpileup, call\n\n**File Operations**\n> view, merge, concat, sort, index, convert\n\n**Filtering & Querying**\n> filter, query, norm\n\n**Statistics**\n> stats, roh, gtcheck\n\n**Annotation**\n> annotate, csq, fill-tags\n\n**Manipulation**\n> reheader, isec, head, cnv, polysomy\n\n**Consensus**\n> consensus\n\n**Plugins**\n> plugin (e.g. +split, +scatter, +fill-tags, +setGT)"],["PARAMETERS","**-Ob**\n> Output compressed BCF\n\n**-Oz**\n> Output compressed VCF (bgzipped)\n\n**-Ov**\n> Output uncompressed VCF\n\n**-Ou**\n> Output uncompressed BCF (fastest for piping)\n\n**-r** _region_\n> Restrict to comma-separated regions (chr:from-to format), requires an index\n\n**-R** _file_\n> Restrict to regions listed in a file (VCF, BED, or tab-delimited)\n\n**-t** _region_\n> Restrict to targets, streamed without an index\n\n**-T** _file_\n> Restrict to targets listed in a file\n\n**-s** _samples_\n> Comma-separated list of samples to include (prefix ^ to exclude)\n\n**-S** _file_\n> Read the sample list from a file\n\n**--threads** _int_\n> Number of extra worker threads used for output compression\n\n**-W, --write-index** _[fmt]_\n> Automatically index the output (tbi or csi)\n\n**-i** _expression_\n> Include sites matching filter expression\n\n**-e** _expression_\n> Exclude sites matching filter expression\n\n**-o** _file_\n> Output file name"],["CAVEATS","Use **-Ou** when piping between bcftools subcommands to avoid unnecessary compression overhead. Indexed files are required for random access and some operations. VCF/BCF files should be sorted by chromosome and position for most operations."],["HISTORY","bcftools was developed as part of the **SAMtools** project, initially created by **Heng Li** at the Wellcome Sanger Institute. It became a separate project around **2014** with the HTSlib library rewrite, gaining significant functionality for variant analysis."],["INSTALL","```apt: sudo apt install bcftools```\n\n```dnf: sudo dnf install bcftools```\n\n```aur: yay -S bcftools```\n\n```zypper: sudo zypper install bcftools```\n\n```brew: brew install bcftools```\n\n```nix: nix profile install nixpkgs#bcftools```"],["SEE ALSO","[samtools](/man/samtools)(1), [tabix](/man/tabix)(1), [bgzip](/man/bgzip)(1), [vcftools](/man/vcftools)(1)"],["RESOURCES","```[Homepage](https://samtools.github.io/bcftools/)```\n\n```[Source code](https://github.com/samtools/bcftools)```\n\n```[Documentation](https://samtools.github.io/bcftools/bcftools.html)```"]],"e":["samtools","tabix","bgzip","vcftools"],"x":"bcftools view [input.vcf.gz]"},"bootnode":{"n":"bootnode","t":"Ethereum network bootstrap node","s":[["TAGLINE","Ethereum network bootstrap node"],["TLDR","**Generate a node key**\n\n```bootnode -genkey [node.key]```\n\n**Run a bootnode** with existing key\n\n```bootnode -nodekey [node.key]```\n\n**Run on specific address** and port\n\n```bootnode -nodekey [node.key] -addr [192.168.1.1:30301]```\n\n**Write node URL** to file\n\n```bootnode -nodekey [node.key] -writeaddress```\n\n**Run with verbosity**\n\n```bootnode -nodekey [node.key] -verbosity [4]```"],["SYNOPSIS","**bootnode** [_options_]"],["DESCRIPTION","**bootnode** runs a bootstrap node for the Ethereum Discovery Protocol. Bootstrap nodes help new nodes discover and connect to peers in the Ethereum network without being full nodes themselves.\n\nThe tool is part of the go-ethereum (geth) project and implements the Node Discovery Protocol v4 and v5, enabling peer-to-peer network formation."],["PARAMETERS","**-genkey** _file_\n> Generate a new node key and save to file\n\n**-nodekey** _file_\n> Path to the node's private key file\n\n**-addr** _ip:port_\n> Listen address (default: :30301)\n\n**-writeaddress**\n> Write the node's public key/URL to stdout\n\n**-verbosity** _level_\n> Logging verbosity (0=silent, 5=detail)\n\n**-nat** _method_\n> NAT port mapping mechanism (none, upnp, pmp, extip:IP)\n\n**-netrestrict** _CIDR_\n> Restrict network communication to given CIDR\n\n**-v5**\n> Enable Discovery v5 protocol"],["CAVEATS","Bootnodes should have stable, publicly reachable IP addresses. The node key must be kept secure as it identifies the node. Bootnodes do not participate in blockchain consensus or store chain data."],["HISTORY","bootnode was developed as part of the **go-ethereum** project by the Ethereum Foundation. It implements the Kademlia-like discovery protocol that enables Ethereum's peer-to-peer network to function without centralized coordination."],["SEE ALSO","[geth](/man/geth)(1)"]],"e":["geth"],"x":"bootnode -genkey [node.key]"},"builtin":{"n":"builtin","t":"Execute a shell built-in command directly","s":[["TAGLINE","Execute a shell built-in command directly"],["TLDR","**Run** built-in command\n\n```builtin cd [/path]```\n\n**Check** if command is built-in\n\n```type cd```"],["SYNOPSIS","**builtin** _command_ [_args_]"],["DESCRIPTION","**builtin** forces the shell to run a built-in command even if a function or alias with the same name exists. It's a bash built-in command used to bypass shell functions and aliases to access the original built-in implementation.\n\nThe command is useful when functions or aliases shadow built-in commands."],["USAGE","```bash"],["Function shadows cd","cd() {\n    echo \"Going to $1\"\n    builtin cd \"$1\"\n}"],["Direct builtin usage","builtin cd /tmp  # Bypasses any cd function\n```"],["COMMON BUILT-INS","- **cd** - Change directory\n- **echo** - Print text\n- **read** - Read input\n- **test** - Conditional evaluation\n- **export** - Set environment variables\n- **source** - Execute script\n- **alias** - Create aliases\n- **set** - Set shell options"],["WORKFLOW","```bash"],["Create function that wraps cd","cd() {\n    echo \"Changing to $1\"\n    builtin cd \"$1\" && ls\n}"],["Use the function","cd /tmp  # Prints message and lists directory"],["Use built-in directly","builtin cd /tmp  # Just changes directory\n```"],["CAVEATS","Only works with shell built-in commands. No effect on external commands. Behavior varies between shells (bash, zsh, etc.). Not needed unless functions/aliases shadow built-ins."],["HISTORY","**builtin** has been part of bash and other shells since the late **1980s** to provide access to built-in commands when shadowed."],["SEE ALSO","[command](/man/command)(1), [type](/man/type)(1)"]],"e":["command","type"],"x":"builtin cd [/path]"},"bun-add":{"n":"bun-add","t":"Add packages to a Bun project","s":[["TAGLINE","Add packages to a Bun project"],["TLDR","**Add a dependency**\n\n```bun add [lodash]```\n\n**Add a dev dependency**\n\n```bun add -D [jest]```\n\n**Add multiple packages**\n\n```bun add [react] [react-dom]```\n\n**Add with exact version**\n\n```bun add --exact [typescript@5.0.0]```\n\n**Add globally**\n\n```bun add -g [typescript]```\n\n**Add from git repository**\n\n```bun add git+https://github.com/[user]/[repo]```\n\n**Add as peer dependency**\n\n```bun add -P [react]```\n\n**Dry run without changes**\n\n```bun add --dry-run [package]```"],["SYNOPSIS","**bun add** [_options_] _package_ ..."],["DESCRIPTION","**bun add** adds packages to package.json and installs them into node_modules. It updates or creates bun.lockb for reproducible installs. The command is significantly faster than npm or yarn alternatives."],["PARAMETERS","**-D, --dev**\n> Add to devDependencies\n\n**-P, --peer**\n> Add to peerDependencies\n\n**-O, --optional**\n> Add to optionalDependencies\n\n**-E, --exact**\n> Use exact version without ^ or ~ prefix\n\n**-g, --global**\n> Install globally to Bun's global store\n\n**--production**\n> Skip devDependencies\n\n**--registry** _url_\n> Use custom npm registry\n\n**--lockfile-only**\n> Update lockfile without installing\n\n**--dry-run**\n> Simulate without making changes\n\n**--force**\n> Force install despite warnings\n\n**--trust**\n> Allow lifecycle scripts for added packages"],["PACKAGE SOURCES","Supports npm registry packages, semver ranges, git URLs (git+https://...), local paths (file:../path), and workspace packages (workspace:*)."],["CAVEATS","Creates a binary bun.lockb lockfile (or bun.lock text lockfile in newer versions). Package versions default to caret (^) ranges unless --exact is specified. Global packages are stored in Bun's global store, not system-wide."],["INSTALL","```pacman: sudo pacman -S bun```\n\n```brew: brew install bun```\n\n```nix: nix profile install nixpkgs#bun```"],["SEE ALSO","[bun](/man/bun)(1), [bun-install](/man/bun-install)(1), [bun-remove](/man/bun-remove)(1), [npm-install](/man/npm-install)(1), [yarn](/man/yarn)(1)"]],"e":["bun","bun-install","bun-remove","npm-install","yarn"],"x":"bun add [lodash]"},"catchsegv":{"n":"catchsegv","t":"catch segmentation faults and print backtraces","s":[["TAGLINE","catch segmentation faults and print backtraces"],["TLDR","**Run program with segfault catching**\n\n```catchsegv [./program]```\n\n**Run with arguments**\n\n```catchsegv [./program] [arg1] [arg2]```"],["SYNOPSIS","**catchsegv** _program_ [_args_...]"],["DESCRIPTION","**catchsegv** is a debugging wrapper that intercepts segmentation faults in a program and prints a symbolic backtrace to help diagnose the crash. It works by using LD_PRELOAD to inject libSegFault.so, which installs a signal handler for SIGSEGV and other fatal signals.\n\nWhen the wrapped program crashes, catchsegv displays a stack trace with function names, source file locations (if debug symbols are available), and memory map information. This provides a lightweight alternative to running programs under a full debugger when you only need crash diagnostics.\n\nThe tool was part of the glibc debug utilities but has been removed from glibc 2.35 and later. Modern alternatives include AddressSanitizer and core dump analysis with GDB."],["ENVIRONMENT","**SEGFAULT_OUTPUT_NAME**\n> Output file (default: stderr)\n\n**SEGFAULT_SIGNALS**\n> Signals to handle: segv, ill, bus, stkflt, abrt, fpe\n\n**SEGFAULT_USE_ALTSTACK**\n> Use alternate stack for handler"],["DEBUG SYMBOLS","Compile with -g flag for function names and line numbers in backtrace:\n\n```gcc -g -o program program.c```\n\nWithout debug symbols, only memory addresses are shown."],["CAVEATS","Removed from glibc 2.35+. Modern alternatives include out-of-process debuggers and post-mortem analysis tools. Uses non-async-signal-safe functions in handler."],["ALTERNATIVES","- GDB with core dumps\n- AddressSanitizer (-fsanitize=address)\n- Custom signal handlers with backtrace()\n- Valgrind"],["SEE ALSO","[gdb](/man/gdb)(1), [addr2line](/man/addr2line)(1)"]],"e":["gdb","addr2line"],"x":"catchsegv [./program]"},"claude-account":{"n":"claude-account","t":"Switch isolated Claude Code accounts on Linux","s":[["TAGLINE","Switch isolated Claude Code accounts on Linux"],["TLDR","**Install** a release binary and wire the PATH shim\n\n```./claude-account install```\n\n**Add** a named account profile (opens official Claude login)\n\n```claude account add [name]```\n\n**Switch** the active profile\n\n```claude account use [name]```\n\n**List** profiles and show the current one\n\n```claude account list```\n\n```claude account current```\n\n**Remove** a profile (optionally purge local data)\n\n```claude account remove [name]```\n\nRun Claude normally after switching — the shim sets an isolated config dir\n\n```claude```"],["SYNOPSIS","**claude-account** **install**\n\n**claude** **account** _subcommand_ [_options_]"],["DESCRIPTION","**claude-account** is a Linux-only profile switcher for Claude Code. After **install**, it places a **claude** shim earlier on **PATH** that sets an isolated **CLAUDE_CONFIG_DIR** per named account and forwards all other arguments to the real Claude Code executable.\n\nClaude Code itself still performs login, logout, credentials, and token refresh. **claude-account** never reads or copies credential contents. It is an independent community project and is not made, endorsed, or supported by Anthropic.\n\nDefault storage uses XDG paths under **~/.config/claude-account/** and **~/.local/share/claude-account/**. **CLAUDE_ACCOUNT_HOME** can relocate all application data. For profile isolation, the wrapper clears **ANTHROPIC_API_KEY**, **ANTHROPIC_AUTH_TOKEN**, and **CLAUDE_CODE_OAUTH_TOKEN** from the child environment unless **CLAUDE_ACCOUNT_PRESERVE_AUTH_ENV=1** is set."],["COMMANDS","**install**\n\n> Install the shim directory and print the **export PATH=...** line to add to the shell profile.\n\n**account add** _name_ [**--email** _addr_] [**--sso**] [**--console**]\n\n> Register a profile and run Claude Code's official login for it. The first profile becomes active.\n\n**account use** _name_\n\n> Make _name_ the active profile for newly launched Claude processes.\n\n**account list**\n\n> List registered profiles.\n\n**account current**\n\n> Print only the active profile name (script-friendly).\n\n**account remove** _name_ [**--purge**] [**--yes**] [**--force**]\n\n> Log out and unregister a profile. **--purge** deletes settings, sessions, plugins, and history. Removing the active profile requires **--force**."],["CAVEATS","Linux only. Requires an existing Claude Code installation. Switching does not affect already-running Claude sessions. The install binary is **claude-account**; day-to-day profile commands are invoked as **claude account ...** through the shim."],["SEE ALSO","[claude](/man/claude)(1)"],["RESOURCES","```[Source code](https://github.com/hamzarehmandeveloper/claude-account)```"]],"e":["claude"],"x":"./claude-account install"},"concourse":{"n":"concourse","t":"container-based continuous integration server","s":[["TAGLINE","container-based continuous integration server"],["TLDR","**Start a quickstart all-in-one** Concourse instance\n\n```concourse quickstart --add-local-user [admin]:[password] --main-team-local-user [admin]```\n\n**Start the web node** with PostgreSQL\n\n```concourse web --postgres-host [localhost] --postgres-user [concourse] --postgres-password [password]```\n\n**Start a worker node**\n\n```concourse worker --work-dir [/var/lib/concourse/worker] --tsa-host [127.0.0.1:2222]```\n\n**Generate RSA session signing key**\n\n```concourse generate-key -t rsa -f [/path/to/session_signing_key]```\n\n**Generate SSH key** for TSA host or worker\n\n```concourse generate-key -t ssh -f [/path/to/tsa_host_key]```\n\n**Run database migrations**\n\n```concourse migrate --postgres-host [localhost] --postgres-user [concourse]```\n\n**Retire a worker** gracefully\n\n```concourse retire-worker --name [worker-name]```\n\n**Display help** for a command\n\n```concourse [web|worker|quickstart] --help```"],["SYNOPSIS","**concourse** _COMMAND_ [_OPTIONS_]"],["COMMANDS","**quickstart**\n> Start an all-in-one Concourse instance with web and worker components.\n\n**web**\n> Start the Concourse web node (ATC/TSA).\n\n**worker**\n> Start a Concourse worker node.\n\n**migrate**\n> Run database migrations.\n\n**generate-key**\n> Generate encryption keys for Concourse components.\n\n**land-worker**\n> Safely drain and stop a worker.\n\n**retire-worker**\n> Remove a worker from the cluster."],["PARAMETERS","**--add-local-user** _USER_:_PASSWORD_\n> Add a local user for authentication.\n\n**--main-team-local-user** _USER_\n> Grant main team access to a local user.\n\n**--postgres-host** _HOST_\n> PostgreSQL server hostname.\n\n**--postgres-user** _USER_\n> PostgreSQL username.\n\n**--postgres-password** _PASSWORD_\n> PostgreSQL password.\n\n**--external-url** _URL_\n> Externally reachable URL of the Concourse web UI.\n\n**--work-dir** _PATH_\n> Directory for worker to store container data.\n\n**--tsa-host** _HOST_:_PORT_\n> TSA host for worker registration (default: 127.0.0.1:2222).\n\n**--session-signing-key** _PATH_\n> RSA key for signing and verifying user session tokens (web node).\n\n**--tsa-host-key** _PATH_\n> SSH private key for the TSA worker registration gateway (web node).\n\n**--tsa-authorized-keys** _PATH_\n> File containing authorized worker public keys (web node).\n\n**--tsa-public-key** _PATH_\n> TSA host public key for verification (worker node).\n\n**--tsa-worker-private-key** _PATH_\n> Worker private key for authenticating to TSA (worker node).\n\n**--runtime** _RUNTIME_\n> Container runtime to use (guardian or containerd).\n\n**--help**\n> Display help for the command."],["DESCRIPTION","**Concourse** is a container-based continuous integration system. The concourse CLI is used to run and manage Concourse server components including the web node (ATC for scheduling, TSA for worker registration) and worker nodes (for running pipeline tasks).\n\nAll configuration is defined via flags or environment variables. Each flag has a corresponding env var with the format **CONCOURSE_FLAG_NAME** (uppercase, underscores instead of dashes).\n\nFor typical usage, the **quickstart** command provides an all-in-one setup suitable for development. Production deployments typically run separate web and worker processes, often on different machines.\n\nUsers interact with Concourse through the **fly** CLI, not the concourse command directly."],["CAVEATS","The quickstart command is intended for development and testing, not production. Production deployments require separate web and worker nodes with proper security configuration including TLS certificates and key management. Worker nodes need sufficient disk space and container runtime permissions."],["HISTORY","Concourse was created at Pivotal (now VMware Tanzu) and first released in **2014**. It was designed as a \"pipeline-based\" CI/CD system where everything is configured as code. Unlike traditional CI systems, Concourse has no plugins—all functionality comes from resources (versioned artifacts) and tasks (scripts in containers). The project was open-sourced and continues active development on GitHub."],["INSTALL","```aur: yay -S concourse```"],["SEE ALSO","[fly](/man/fly)(1), [docker](/man/docker)(1), [podman](/man/podman)(1), [jenkins](/man/jenkins)(1)"]],"e":["fly","docker","podman","jenkins"],"x":"concourse quickstart --add-local-user [admin]:[password] --main-team-local-user [admin]"},"convmv":{"n":"convmv","t":"filename encoding converter","s":[["TAGLINE","filename encoding converter"],["TLDR","**Preview converting filenames** from Latin1 to UTF-8\n\n```convmv -f latin1 -t utf-8 [path/to/files/*]```\n\n**Actually convert filenames** (apply changes)\n\n```convmv -f latin1 -t utf-8 --notest [path/to/files/*]```\n\n**Convert filenames recursively** in a directory\n\n```convmv -f latin1 -t utf-8 -r --notest [path/to/directory]```\n\n**Convert from Windows encoding** to UTF-8\n\n```convmv -f cp1252 -t utf-8 --notest [path/to/files/*]```\n\n**Lowercase all filenames**\n\n```convmv --lower --notest [path/to/files/*]```\n\n**Fix double-encoded UTF-8** filenames\n\n```convmv --fixdouble -f utf-8 -t utf-8 --notest [path/to/files/*]```"],["SYNOPSIS","**convmv** [_options_] _file_..."],["PARAMETERS","**-f** _ENCODING_\n> Source encoding (from encoding).\n\n**-t** _ENCODING_\n> Target encoding (to encoding).\n\n**-r**\n> Recurse into subdirectories.\n\n**--notest**\n> Actually perform the conversion (default is dry run).\n\n**--nfc**\n> Normalize to Unicode NFC form.\n\n**--nfd**\n> Normalize to Unicode NFD form.\n\n**--lower**\n> Convert filenames to lowercase.\n\n**--upper**\n> Convert filenames to uppercase.\n\n**--fixdouble**\n> Fix double-encoded UTF-8 filenames.\n\n**--list**\n> List available encodings.\n\n**--preserve-mtimes**\n> Preserve modification times on directories."],["DESCRIPTION","**convmv** is a utility for converting filenames between different character encodings. It's essential when files created on one system with a particular locale need to be used on another system with a different character encoding.\n\nCommon use cases include migrating files from Windows (using cp1252 or ISO-8859-1) to Linux with UTF-8, or fixing filenames that were created with incorrect encoding assumptions. The tool can also fix double-encoded UTF-8, which occurs when UTF-8 bytes are mistakenly encoded as UTF-8 again.\n\nBy default, convmv performs a dry run showing what would be changed. The **--notest** flag must be explicitly provided to actually rename files. This safety feature prevents accidental mass renaming operations."],["CAVEATS","Always run without **--notest** first to preview changes. The tool cannot detect the source encoding automatically; you must know what encoding was used originally. Converting to a less expressive encoding may cause data loss. Symbolic links and hard links require careful handling."],["HISTORY","convmv was written by Björn Jacke and has been a standard tool for handling encoding issues on Unix systems. It became particularly important during the transition from locale-specific encodings to UTF-8 as the universal standard in the 2000s."],["INSTALL","```dnf: sudo dnf install convmv```\n\n```pacman: sudo pacman -S convmv```\n\n```zypper: sudo zypper install convmv```\n\n```brew: brew install convmv```\n\n```nix: nix profile install nixpkgs#convmv```"],["SEE ALSO","[mv](/man/mv)(1), [rename](/man/rename)(1), [iconv](/man/iconv)(1), [recode](/man/recode)(1)"],["RESOURCES","```[Homepage](https://www.j3e.de/linux/convmv/)```"]],"e":["mv","rename","iconv","recode"],"x":"convmv -f latin1 -t utf-8 [path/to/files/*]"},"crane-delete":{"n":"crane-delete","t":"remove container images from a registry","s":[["TAGLINE","remove container images from a registry"],["TLDR","**Delete an image by tag**\n\n```crane delete [registry.example.com/repo:tag]```\n\n**Delete an image by digest**\n\n```crane delete [registry.example.com/repo]@[sha256:abc123...]```\n\n**Delete with verbose output**\n\n```crane delete -v [registry.example.com/repo:tag]```"],["SYNOPSIS","**crane** **delete** [_options_] _image_ref_"],["PARAMETERS","**-v**, **--verbose**\n> Enable verbose logging.\n\n**--platform** _PLATFORM_\n> Specifies the platform in the form os/arch (e.g., linux/amd64).\n\n**--insecure**\n> Allow image references to be fetched without TLS."],["DESCRIPTION","**crane delete** removes an image manifest from a container registry by tag or digest reference. The image reference must include the full registry path.\n\nWhen deleting by tag, the registry removes the tag-to-manifest mapping. When deleting by digest, the manifest itself is removed. Use with caution as deletion may be permanent depending on registry configuration and policies.\n\nThis command sends an HTTP DELETE request to the registry's manifest endpoint. It requires appropriate authentication and authorization for the target repository."],["CAVEATS","Some registries do not support deletion or require special permissions. Deleting a tag does not necessarily free storage until garbage collection runs. Deleting a manifest by digest may affect other tags pointing to the same manifest. Docker Hub and some hosted registries may have rate limits or restrictions on deletion."],["INSTALL","```pacman: sudo pacman -S crane```\n\n```apk: sudo apk add crane```\n\n```zypper: sudo zypper install crane```\n\n```brew: brew install crane```\n\n```nix: nix profile install nixpkgs#crane```"],["SEE ALSO","[crane](/man/crane)(1), [crane-tag](/man/crane-tag)(1), [crane-digest](/man/crane-digest)(1), [crane-ls](/man/crane-ls)(1), [crane-manifest](/man/crane-manifest)(1)"]],"e":["crane","crane-tag","crane-digest","crane-ls","crane-manifest"],"x":"crane delete [registry.example.com/repo:tag]"},"ctypes":{"n":"ctypes","t":"Python foreign function interface to C libraries","s":[["TAGLINE","Python foreign function interface to C libraries"],["TLDR","**Load a shared library**\n\n```python -c \"from ctypes import CDLL; lib = CDLL('[libname.so]')\"```\n\n**Call a C function**\n\n```python -c \"from ctypes import CDLL; lib = CDLL('libc.so.6'); print(lib.getpid())\"```\n\n**Pass string argument to C function**\n\n```python -c \"from ctypes import CDLL, c_char_p; lib = CDLL('libc.so.6'); lib.puts(c_char_p(b'Hello'))\"```\n\n**Define function argument and return types**\n\n```python -c \"from ctypes import CDLL, c_int; lib = CDLL('libm.so.6'); lib.abs.argtypes = [c_int]; lib.abs.restype = c_int; print(lib.abs(-42))\"```\n\n**Access Windows DLL**\n\n```python -c \"from ctypes import windll; windll.user32.MessageBoxW(0, 'Hello', 'Title', 0)\"```"],["SYNOPSIS","```python\nfrom ctypes import CDLL, c_int, c_char_p, POINTER\n```"],["TYPES","**c_int**, **c_long**, **c_longlong**\n> Integer types.\n\n**c_uint**, **c_ulong**\n> Unsigned integers.\n\n**c_float**, **c_double**\n> Floating point types.\n\n**c_char**, **c_wchar**\n> Character types.\n\n**c_char_p**, **c_wchar_p**\n> String pointers (char*, wchar_t*).\n\n**c_void_p**\n> Void pointer.\n\n**POINTER(type)**\n> Pointer to type.\n\n**Structure**, **Union**\n> Base classes for C structs/unions."],["DESCRIPTION","**ctypes** is a Python standard library module for calling functions in C shared libraries and DLLs. It provides C-compatible data types and allows calling functions with appropriate argument and return type specifications.\n\nThe module enables Python code to interface with native libraries without writing C extension modules. It handles type conversion between Python and C, pointer management, and structure/union definitions.\n\nCommon uses include accessing system libraries, using hardware interfaces, integrating legacy C code, and calling platform-specific APIs (Windows DLLs, macOS frameworks)."],["CAVEATS","Incorrect type specifications can cause crashes or memory corruption. Pointers and memory management require careful handling. Library paths vary by OS; use **find_library** for portability. Performance overhead exists compared to C extensions. Not suitable for C++ libraries with name mangling."],["HISTORY","ctypes was created by **Thomas Heller** and merged into Python's standard library in version **2.5** (2006). It was based on the earlier ctypes project available on PyPI since **2003**. The module provided a crucial capability for Python to interface with native code without compilation, enabling pure-Python access to system libraries."],["SEE ALSO","[cython](/man/cython)(1)"]],"e":["cython"],"x":"python -c \"from ctypes import CDLL; lib = CDLL('[libname.so]')\""},"date":{"n":"date","t":"display or set the system date and time","s":[["TAGLINE","display or set the system date and time"],["TLDR","**Show** current date and time\n\n```date```\n\n**Custom** format\n\n```date [\"+%Y-%m-%d %H:%M:%S\"]```\n\n**Show** date in UTC\n\n```date -u```\n\n**Set** date (requires root)\n\n```sudo date -s [\"2024-01-15 10:30:00\"]```\n\n**Show** date from timestamp\n\n```date -d [@1234567890]```"],["SYNOPSIS","**date** [_options_] [_+format_]"],["DESCRIPTION","**date** displays or sets the system date and time. It supports extensive output formatting through format specifiers, time zone conversions, relative date calculations, and Unix timestamp conversions. The command is essential for timestamping, logging, scheduling, and time-based operations.\n\nBeyond simple display, date excels at date arithmetic: you can specify relative dates like \"tomorrow\", \"next monday\", \"2 days ago\", or \"+3 hours\". It can parse dates from strings, convert Unix timestamps to human-readable format, and format output for any purpose from ISO 8601 standards to custom formats.\n\nThe tool is one of the most commonly used Unix utilities, present since the earliest Unix versions. It's frequently used in shell scripts for generating timestamped filenames, calculating time differences, or performing date-based conditional logic.\n\nFormat specifiers allow precise control over output, from simple \"YYYY-MM-DD\" to complex locale-specific formats. The tool respects timezone settings and can display UTC or any specific timezone."],["PARAMETERS","**-d**, **--date=**_string_\n> Display specified date/time\n\n**-s**, **--set=**_string_\n> Set system date/time (requires root)\n\n**-u**, **--utc**\n> Print/set UTC time\n\n**-r**, **--reference=**_file_\n> Display file's modification time\n\n**-I**[_timespec_]\n> ISO 8601 format\n\n**-R**, **--rfc-email**\n> RFC 5322 format"],["FORMAT SPECIFIERS","**Date:**\n- **%Y** - Year (2024)\n- **%y** - Year (24)\n- **%m** - Month (01-12)\n- **%d** - Day (01-31)\n- **%B** - Month name (January)\n- **%b** - Short month (Jan)\n- **%A** - Weekday (Monday)\n- **%a** - Short weekday (Mon)\n\n**Time:**\n- **%H** - Hour 24h (00-23)\n- **%I** - Hour 12h (01-12)\n- **%M** - Minute (00-59)\n- **%S** - Second (00-59)\n- **%p** - AM/PM\n- **%Z** - Timezone name\n\n**Combined:**\n- **%s** - Unix epoch seconds\n- **%F** - Full date (YYYY-MM-DD)\n- **%T** - Full time (HH:MM:SS)\n- **%c** - Locale's date and time"],["WORKFLOW","```bash"],["Current date/time","date"],["ISO 8601 format","date -I\ndate \"+%Y-%m-%d\""],["Custom format","date \"+%Y-%m-%d %H:%M:%S\""],["Unix timestamp","date +%s"],["From timestamp","date -d @1234567890"],["Relative dates","date -d \"tomorrow\"\ndate -d \"next monday\"\ndate -d \"2 days ago\"\ndate -d \"+3 hours\""],["UTC time","date -u"],["File modification time","date -r file.txt"],["RFC format","date -R\n```"],["RELATIVE DATES","```bash\ndate -d \"1 day ago\"\ndate -d \"2 weeks ago\"\ndate -d \"next friday\"\ndate -d \"last month\"\ndate -d \"3 years ago\"\ndate -d \"+5 days\"\n```"],["COMMON USES","**Timestamped filenames:**\n```bash\nbackup-$(date +%Y%m%d).tar.gz\nlog-$(date +%Y-%m-%d_%H-%M-%S).txt\n```\n\n**Logging:**\n```bash\necho \"$(date): Starting backup\" >> log.txt\n```\n\n**Conditionals:**\n```bash\nif [ $(date +%u) -eq 1 ]; then\n    echo \"It's Monday\"\nfi\n```"],["CAVEATS","Setting date requires root. System clock vs hardware clock different. Timezone affects output. Leap seconds not always handled. Different behavior on BSD vs GNU. Relative dates parsing can be ambiguous."],["HISTORY","**date** has been part of Unix since Version 1 in **1971**, one of the original Unix commands."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[timedatectl](/man/timedatectl)(1), [hwclock](/man/hwclock)(8), [cal](/man/cal)(1)"],["RESOURCES","```[Homepage](https://www.gnu.org/software/coreutils/)```\n\n```[Documentation](https://www.gnu.org/software/coreutils/manual/html_node/date-invocation.html)```"]],"e":["timedatectl","hwclock","cal"],"x":"date"},"diesel":{"n":"diesel","t":"Rust ORM command-line interface","s":[["TAGLINE","Rust ORM command-line interface"],["TLDR","**Setup database** (create if not exists)\n\n```diesel setup```\n\n**Generate a new migration**\n\n```diesel migration generate [migration_name]```\n\n**Run pending migrations**\n\n```diesel migration run```\n\n**Revert last migration**\n\n```diesel migration revert```\n\n**Print current schema**\n\n```diesel print-schema```\n\n**Generate schema.rs from database**\n\n```diesel print-schema > src/schema.rs```\n\n**Check migration status**\n\n```diesel migration list```"],["SYNOPSIS","**diesel** _command_ [_options_]"],["PARAMETERS","**setup**\n> Create database and run migrations.\n\n**migration generate** _name_\n> Create new migration files.\n\n**migration run**\n> Run all pending migrations.\n\n**migration revert**\n> Revert the latest migration.\n\n**migration redo**\n> Revert and re-run latest migration.\n\n**migration list**\n> List all migrations and their status.\n\n**print-schema**\n> Output database schema as Rust code.\n\n**database setup**\n> Create the database.\n\n**database reset**\n> Drop and recreate database.\n\n**--database-url** _url_\n> Database connection URL.\n\n**--config-file** _file_\n> Path to diesel.toml.\n\n**--migration-dir** _dir_\n> Directory containing migrations."],["DESCRIPTION","**Diesel** CLI is the command-line tool for the Diesel ORM, a safe, extensible ORM and query builder for Rust. It manages database migrations and generates Rust code representing the database schema.\n\nMigrations are SQL files in a migrations directory, with \"up\" and \"down\" files for applying and reverting changes. The CLI tracks applied migrations in a database table.\n\nThe **print-schema** command introspects the database and generates Rust code (schema.rs) that Diesel uses for compile-time query validation. This ensures queries are type-checked against the actual database structure."],["CONFIGURATION","**diesel.toml**:\n```toml\n[print_schema]\nfile = \"src/schema.rs\"\nwith_docs = true\n\n[migrations_directory]\ndir = \"migrations\"\n```"],["CAVEATS","Requires database driver libraries (libpq for PostgreSQL, libmysqlclient for MySQL). The DATABASE_URL environment variable or --database-url must be set. Schema generation requires an accessible database. Migration SQL varies by database backend."],["HISTORY","Diesel was created by **Sean Griffin** (also a Ruby on Rails contributor) and first released in **2015**. It was designed to leverage Rust's type system for database safety. The CLI tool provides migration management similar to ActiveRecord or Knex. Diesel has become one of the most popular Rust database libraries."],["INSTALL","```brew: brew install diesel```"],["SEE ALSO","[sqlx](/man/sqlx)(1), [cargo](/man/cargo)(1)"]],"e":["sqlx","cargo"],"x":"diesel setup"},"dmitry":{"n":"dmitry","t":"information gathering reconnaissance tool","s":[["TAGLINE","information gathering reconnaissance tool"],["TLDR","**Perform whois lookup**\n\n```dmitry -w [domain.com]```\n\n**Get netcraft info**\n\n```dmitry -n [domain.com]```\n\n**Search for subdomains**\n\n```dmitry -s [domain.com]```\n\n**Search for email** addresses\n\n```dmitry -e [domain.com]```\n\n**Perform TCP port** scan\n\n```dmitry -p [domain.com]```\n\n**Port scan with banner** grabbing\n\n```dmitry -pb [domain.com]```\n\n**Run all information** gathering\n\n```dmitry -winsepo [output.txt] [domain.com]```"],["SYNOPSIS","**dmitry** [_options_] _host_"],["PARAMETERS","_HOST_\n> Target domain or IP address.\n\n**-w**\n> Perform whois lookup.\n\n**-n**\n> Retrieve Netcraft information.\n\n**-s**\n> Search for subdomains.\n\n**-e**\n> Search for email addresses.\n\n**-p**\n> Perform TCP port scan.\n\n**-b**\n> Read banner from discovered ports (requires -p).\n\n**-f**\n> Report filtered ports (requires -p).\n\n**-t** _SECONDS_\n> Set TTL for the portscan module (default 2 seconds).\n\n**-o** _FILE_\n> Save output to file.\n\n**-i**\n> Perform Internet number whois lookup.\n\n**-h**\n> Display help information."],["DESCRIPTION","**dmitry** (Deepmagic Information Gathering Tool) is a UNIX command-line application for information gathering during penetration testing. It collects data about a host through various passive and active techniques.\n\nThe tool combines multiple reconnaissance functions: whois lookups, Netcraft queries, subdomain enumeration, email harvesting, and TCP port scanning. Results can be consolidated into a single report file.\n\ndmitry is designed for the information gathering phase of security assessments, helping testers understand the target environment before more active testing."],["CAVEATS","Some queries may be rate-limited. Port scanning should only be performed with authorization. Email harvesting depends on public data availability. Results may vary based on target configuration."],["HISTORY","dmitry was developed for security professionals as a reconnaissance tool. It consolidates multiple information gathering techniques into a single utility, streamlining the initial phases of penetration testing engagements."],["INSTALL","```apt: sudo apt install dmitry```\n\n```nix: nix profile install nixpkgs#dmitry```"],["SEE ALSO","[nmap](/man/nmap)(1), [whois](/man/whois)(1), [dig](/man/dig)(1), [theHarvester](/man/theHarvester)(1)"]],"e":["nmap","whois","dig","theHarvester"],"x":"dmitry -w [domain.com]"},"docker-context":{"n":"docker-context","t":"manage Docker daemon connection contexts","s":[["TAGLINE","manage Docker daemon connection contexts"],["TLDR","**List contexts**\n\n```docker context ls```\n\n**Show current context**\n\n```docker context show```\n\n**Create a new context**\n\n```docker context create [name] --docker \"host=ssh://[user@host]\"```\n\n**Switch to context**\n\n```docker context use [name]```\n\n**Remove a context**\n\n```docker context rm [name]```\n\n**Inspect context details**\n\n```docker context inspect [name]```"],["SYNOPSIS","**docker** **context** _command_ [_options_]"],["SUBCOMMANDS","**ls**\n> List contexts.\n\n**show**\n> Print current context.\n\n**create**\n> Create a new context.\n\n**use**\n> Set current context.\n\n**rm**\n> Remove contexts.\n\n**inspect**\n> Display detailed information.\n\n**update**\n> Update a context.\n\n**export**\n> Export a context.\n\n**import**\n> Import a context."],["DESCRIPTION","**docker context** manages Docker contexts, which are configuration profiles that store connection information for different Docker engines. Contexts enable seamless switching between local, remote, and cloud-based Docker hosts without manually reconfiguring connection settings.\n\nEach context contains endpoint information, TLS certificates, and other metadata needed to communicate with a Docker daemon. This is particularly useful for developers and operators who work with multiple Docker environments, such as local development, staging servers, and production clusters.\n\nThe default context named \"default\" connects to the local Docker daemon via Unix socket. Additional contexts can be created to connect to remote Docker hosts over SSH, TCP, or other protocols."],["CONFIGURATION","**~/.docker/contexts**\n> Directory storing context metadata and TLS certificates for custom contexts."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker](/man/docker)(1), [docker-machine](/man/docker-machine)(1)"],["RESOURCES","```[Source code](https://github.com/docker/cli)```\n\n```[Documentation](https://docs.docker.com/reference/cli/docker/)```"]],"e":["docker","docker-machine"],"x":"docker context ls"},"docker-tag":{"n":"docker-tag","t":"create tags for Docker images","s":[["TAGLINE","create tags for Docker images"],["TLDR","**Tag an image**\n\n```docker tag [source_image] [target_image]:[tag]```\n\n**Tag for private registry**\n\n```docker tag [image] [registry.example.com/image]:[tag]```\n\n**Tag by image ID**\n\n```docker tag [image_id] [repository]:[tag]```\n\n**Add latest tag**\n\n```docker tag [image]:[version] [image]:latest```"],["SYNOPSIS","**docker** **tag** _source_image[:tag]_ _target_image[:tag]_"],["DESCRIPTION","**docker tag** creates a tag that references an existing image. An image can have multiple tags. Tags are used to version images and prepare them for pushing to registries."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"],["SEE ALSO","[docker-image-tag](/man/docker-image-tag)(1)"]],"e":["docker-image-tag"],"x":"docker tag [source_image] [target_image]:[tag]"},"dollar":{"n":"dollar","t":"shell expansion operator for variables and commands","s":[["TAGLINE","shell expansion operator for variables and commands"],["TLDR","**Reference a variable**\n\n```echo $[VARIABLE]```\n\n**Command substitution**\n\n```result=$(command)```\n\n**Arithmetic expansion**\n\n```echo $((1 + 2))```\n\n**Parameter expansion with default**\n\n```echo ${VAR:-[default]}```\n\n**String length**\n\n```echo ${#VAR}```"],["SYNOPSIS","**$**_name_\n**${**_name_**}**\n**$(**_command_**)**\n**$((**_expression_**))**"],["PARAMETERS","**${var}**\n> Value of variable.\n\n**${var:-default}**\n> Use default if var is unset or empty.\n\n**${var:=default}**\n> Assign default if var is unset or empty.\n\n**${var:+alternate}**\n> Use alternate value if var is set.\n\n**${var:?error}**\n> Exit with error message if var is unset.\n\n**${#var}**\n> String length of var.\n\n**${var%pattern}**\n> Remove shortest suffix match.\n\n**${var%%pattern}**\n> Remove longest suffix match.\n\n**${var#pattern}**\n> Remove shortest prefix match.\n\n**${var##pattern}**\n> Remove longest prefix match.\n\n**${var/old/new}**\n> Replace first occurrence of old with new.\n\n**${var//old/new}**\n> Replace all occurrences of old with new.\n\n**${var:offset:length}**\n> Substring extraction.\n\n**$?**\n> Exit status of last command.\n\n**$$**\n> PID of the current shell.\n\n**$!**\n> PID of last background job.\n\n**$0**\n> Script or shell name.\n\n**$1**-**$9**\n> Positional parameters.\n\n**$@**\n> All positional parameters as separate words.\n\n**$***\n> All positional parameters as a single word.\n\n**$#**\n> Number of positional parameters."],["DESCRIPTION","**$** is the shell's expansion operator for variables, commands, and arithmetic. It triggers substitution of values before command execution.\n\nVariable expansion **$VAR** or **${VAR}** retrieves the variable's value. Braces allow modifiers and are required for array access and complex expansions.\n\nCommand substitution **$(command)** executes the command and substitutes its output. This replaces the older backtick syntax.\n\nArithmetic expansion **$((expr))** evaluates mathematical expressions."],["CAVEATS","Unquoted expansions undergo word splitting and glob expansion. Always quote **\"$var\"** unless you specifically want splitting.\n\n**$*** vs **$@** behave differently in quotes: **\"$@\"** preserves argument boundaries, **\"$*\"** joins them.\n\nCommand substitution strips trailing newlines from output."],["INSTALL","```aur: yay -S dollar```"],["SEE ALSO","[bash](/man/bash)(1), [sh](/man/sh)(1), [env](/man/env)(1), [export](/man/export)(1)"]],"e":["bash","sh","env","export"],"x":"echo $[VARIABLE]"},"e4defrag":{"n":"e4defrag","t":"ext4 filesystem defragmentation tool","s":[["TAGLINE","ext4 filesystem defragmentation tool"],["TLDR","**Defragment** a whole mounted filesystem\n\n```sudo e4defrag [/dev/sdXN]```\n\nDefragment a **single file**\n\n```sudo e4defrag [path/to/file]```\n\nDefragment a **directory** and everything below it\n\n```sudo e4defrag [path/to/directory]```\n\nCheck the **fragmentation level** without defragmenting\n\n```sudo e4defrag -c [path/to/file_or_device]```\n\nPrint **verbose** per-file information\n\n```sudo e4defrag -v [/dev/sdXN]```"],["SYNOPSIS","**e4defrag** [**-c**] [**-v**] _target_..."],["DESCRIPTION","**e4defrag** defragments an ext4 filesystem. It can operate on entire filesystems, directories, or individual files.\n\nUnlike offline defragmenters, e4defrag can run while the filesystem is mounted and in use."],["PARAMETERS","**-c**\n> Check the current fragmentation state and print a fragmentation score without defragmenting. No root privileges are needed for files you own.\n\n**-v**\n> Verbose output, printing the result for each file\n\n_target_\n> Device, directory, or file to defragment. A device must be mounted."],["CAVEATS","ext4 only: the kernel's EXT4_IOC_MOVE_EXT ioctl is what does the work, so ext2/ext3 filesystems are rejected. The target filesystem must be mounted, since e4defrag operates online. Defragmentation needs free contiguous space, so it is largely ineffective on nearly full filesystems. Files with a fragmentation score under 30 are already considered fine and are left alone."],["INSTALL","```apt: sudo apt install e2fsprogs```\n\n```dnf: sudo dnf install e2fsprogs```\n\n```pacman: sudo pacman -S e2fsprogs```\n\n```apk: sudo apk add e2fsprogs-extra```\n\n```zypper: sudo zypper install e2fsprogs```\n\n```brew: brew install e2fsprogs```\n\n```nix: nix profile install nixpkgs#e2fsprogs```"],["SEE ALSO","[e2freefrag](/man/e2freefrag)(8), [e2fsck](/man/e2fsck)(8), [filefrag](/man/filefrag)(8)"],["RESOURCES","```[Source code](https://git.kernel.org/pub/scm/fs/ext2/e2fsprogs.git)```\n\n```[Homepage](https://e2fsprogs.sourceforge.net/)```"]],"e":["e2freefrag","e2fsck","filefrag"],"x":"sudo e4defrag [/dev/sdXN]"},"egrep":{"n":"egrep","t":"extended regex pattern searcher","s":[["TAGLINE","extended regex pattern searcher"],["TLDR","**Search for pattern** in file\n\n```egrep \"[pattern]\" [file.txt]```\n\n**Search case-insensitive**\n\n```egrep -i \"[pattern]\" [file.txt]```\n\n**Search with line** numbers\n\n```egrep -n \"[pattern]\" [file.txt]```\n\n**Search recursively**\n\n```egrep -r \"[pattern]\" [directory]```\n\n**Search for multiple** patterns (alternation)\n\n```egrep \"[pattern1]|[pattern2]\" [file.txt]```\n\n**Count matches**\n\n```egrep -c \"[pattern]\" [file.txt]```\n\n**Show only matching** filenames\n\n```egrep -l \"[pattern]\" [*.txt]```"],["SYNOPSIS","**egrep** [_options_] _pattern_ [_file_...]"],["PARAMETERS","_PATTERN_\n> Extended regular expression.\n\n_FILE_\n> File(s) to search.\n\n**-i**\n> Case-insensitive search.\n\n**-n**\n> Show line numbers.\n\n**-r**, **-R**\n> Recursive search.\n\n**-l**\n> Show only filenames.\n\n**-c**\n> Count matching lines.\n\n**-v**\n> Invert match (non-matching lines).\n\n**-o**\n> Show only matched parts.\n\n**--help**\n> Display help information."],["DESCRIPTION","**egrep** searches files for lines matching an extended regular expression pattern. It's equivalent to `grep -E` and supports ERE syntax including +, ?, |, and () without escaping.\n\nExtended regular expressions are more readable than basic grep patterns for complex matches. Alternation (|), grouping (), and quantifiers work directly without backslashes.\n\negrep is deprecated in favor of `grep -E` but remains widely available for compatibility."],["CAVEATS","Deprecated - use grep -E. Behavior may vary between implementations. Large files may be slow. Binary files may produce garbage output."],["HISTORY","egrep was originally a separate program with extended regex support. Modern grep includes this functionality with the -E flag, making standalone egrep redundant but preserved for compatibility."],["INSTALL","```apt: sudo apt install grep```\n\n```dnf: sudo dnf install grep```\n\n```pacman: sudo pacman -S grep```\n\n```apk: sudo apk add grep```\n\n```zypper: sudo zypper install grep```\n\n```brew: brew install grep```"],["SEE ALSO","[grep](/man/grep)(1), [fgrep](/man/fgrep)(1), [sed](/man/sed)(1), [awk](/man/awk)(1)"]],"e":["grep","fgrep","sed","awk"],"x":"egrep \"[pattern]\" [file.txt]"},"explodepkg":{"n":"explodepkg","t":"extract Slackware package contents without installing","s":[["TAGLINE","extract Slackware package contents without installing"],["TLDR","**Extract** a package to current directory\n\n```explodepkg [path/to/package.tgz]```\n\nExtract **multiple** packages\n\n```explodepkg [path/to/package1.tgz] [path/to/package2.tgz]```"],["SYNOPSIS","**explodepkg** _packages_"],["DESCRIPTION","**explodepkg** extracts the contents of Slackware packages to the current directory. Unlike installpkg, it does not install the package to the system or run installation scripts.\n\nSlackware packages are compressed tarballs (.tgz or .txz) with a specific structure. This tool unpacks them for inspection, extracting files to see package contents, documentation, or to create modified packages.\n\nUseful for inspecting package contents before installation, debugging package issues, or extracting specific files without full installation. Part of Slackware's pkgtools suite."],["PARAMETERS","_packages_\n> One or more Slackware packages to extract"],["CAVEATS","Slackware Linux specific. Extracts to current directory, not system paths. Does not run doinst.sh scripts. Part of pkgtools."],["SEE ALSO","[installpkg](/man/installpkg)(8), [removepkg](/man/removepkg)(8), [upgradepkg](/man/upgradepkg)(8), [makepkg](/man/makepkg)(8)"],["RESOURCES","```[Homepage](http://www.slackware.com/)```"]],"e":["installpkg","removepkg","upgradepkg","makepkg"],"x":"explodepkg [path/to/package.tgz]"},"expo":{"n":"expo","t":"React Native development platform and build toolchain","s":[["TAGLINE","React Native development platform and build toolchain"],["TLDR","**Create new project**\n\n```expo init [project-name]```\n\n**Start development** server\n\n```expo start```\n\n**Start on specific** platform\n\n```expo start --ios```\n\n**Build for production**\n\n```expo build:android```\n\n**Publish to Expo**\n\n```expo publish```\n\n**Log in to** Expo account\n\n```expo login```\n\n**Install package**\n\n```expo install [expo-camera]```"],["SYNOPSIS","**expo** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Expo CLI command.\n\n**init** _NAME_\n> Create new project.\n\n**start**\n> Start development server.\n\n**build:android**/**build:ios**\n> Build for platform.\n\n**publish**\n> Publish to Expo.\n\n**install** _PACKAGE_\n> Install compatible package.\n\n**login**\n> Authenticate with Expo.\n\n**--help**\n> Display help information."],["CONFIGURATION","**app.json**\n> Expo project configuration including app name, version, SDK version, and platform-specific settings.\n\n**eas.json**\n> EAS Build and Submit configuration for managed builds and deployments."],["DESCRIPTION","**Expo** is a platform for building React Native applications. The CLI provides tools for development, building, and deployment of cross-platform mobile apps.\n\nExpo simplifies React Native development with managed workflow, pre-configured native modules, and OTA (over-the-air) updates. The Expo Go app enables testing without native builds.\n\nThe platform handles iOS and Android builds in the cloud without local toolchains."],["CAVEATS","Some native modules unavailable in managed workflow. Build service may have queues. Ejecting loses some Expo features. App size includes Expo runtime."],["HISTORY","Expo was created to simplify React Native development, providing a managed workflow and build service. It's maintained by **Expo** (formerly Exponent) and widely used for React Native projects."],["SEE ALSO","[react-native](/man/react-native)(1), [npm](/man/npm)(1)"]],"e":["react-native","npm"],"x":"expo init [project-name]"},"foreman":{"n":"foreman","t":"process manager for Procfile-based applications","s":[["TAGLINE","process manager for Procfile-based applications"],["TLDR","**Start** an application with the Procfile in the current directory\n\n```foreman start```\n\nStart an application with a **specified Procfile**\n\n```foreman start -f [Procfile]```\n\nStart a **specific** application\n\n```foreman start [process]```\n\n**Validate** Procfile format\n\n```foreman check```\n\nRun **one-off commands** with the process's environment\n\n```foreman run [command]```\n\nStart all processes **except** the one named \"worker\"\n\n```foreman start -m all=1,worker=0```"],["SYNOPSIS","**foreman** _command_ [_options_]"],["PARAMETERS","**start**\n> Start all processes defined in the Procfile\n\n**check**\n> Validate Procfile format\n\n**run**\n> Run a one-off command using the app's environment\n\n**export**\n> Export processes to another format (systemd, upstart, etc.)\n\n**-f**, **--procfile** _FILE_\n> Specify an alternate Procfile\n\n**-d**, **--root** _DIR_\n> Specify an alternate application root\n\n**-e**, **--env** _FILE_\n> Specify environment file(s) to load\n\n**-m**, **--formation** _SPEC_\n> Specify process formation (e.g., web=2,worker=1)\n\n**-p**, **--port** _PORT_\n> Specify starting port number\n\n**-t**, **--timeout** _SECONDS_\n> Specify shutdown timeout"],["CONFIGURATION","**Procfile**\n> Defines process types as name:command lines (e.g., web: python app.py).\n\n**.env**\n> Environment variables loaded automatically by foreman."],["DESCRIPTION","**foreman** is a process manager for Procfile-based applications. It reads a Procfile that declares the processes that comprise your application and runs them in development, making it easy to manage multiple processes.\n\nA Procfile defines process types as lines with format **name: command**. For example: **web: python app.py** and **worker: python worker.py**. Foreman starts all defined processes and forwards their output to the terminal.\n\nForeman also loads environment variables from **.env** files and can export process definitions to system init formats like systemd, upstart, or launchd for production deployment."],["CAVEATS","foreman is primarily designed for development environments. For production, export to a proper process manager. Port assignment is sequential starting from the **-p** value, which may conflict with existing services."],["HISTORY","foreman was created by David Dollar at Heroku in **2011** as a way to manage Procfile-based applications locally. The Procfile format became a standard for defining process types in twelve-factor applications and is used by many PaaS providers."],["INSTALL","```brew: brew install foreman```\n\n```nix: nix profile install nixpkgs#foreman```"],["SEE ALSO","[pm2](/man/pm2)(1)"]],"e":["pm2"],"x":"foreman start"},"gftp":{"n":"gftp","t":"graphical FTP and SFTP client","s":[["TAGLINE","graphical FTP and SFTP client"],["TLDR","**Start gFTP**\n\n```gftp```\n\n**Connect to server**\n\n```gftp [ftp.example.com]```\n\n**Connect with port**\n\n```gftp [ftp://server:2121]```\n\n**Connect via SSH**\n\n```gftp [ssh://user@server]```"],["SYNOPSIS","**gftp** [_options_] [_url_]"],["PARAMETERS","_URL_\n> Server URL to connect to.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version."],["DESCRIPTION","**gftp** is a graphical FTP, SFTP, and HTTP client for Linux. It provides a dual-pane file manager interface for transferring files between local and remote systems.\n\nThe application supports multiple protocols, bookmarks, queue management, and recursive transfers. It handles resume for interrupted transfers.\n\ngftp offers a user-friendly alternative to command-line file transfer tools."],["CAVEATS","GTK application requires X/Wayland. FTP credentials sent in cleartext. Some features protocol-dependent."],["HISTORY","gFTP was created by **Brian Masney** in 1998 as a GTK-based FTP client for Linux, later adding support for SFTP and other protocols."],["INSTALL","```dnf: sudo dnf install gftp```\n\n```nix: nix profile install nixpkgs#gftp```"],["SEE ALSO","[ftp](/man/ftp)(1), [sftp](/man/sftp)(1), [filezilla](/man/filezilla)(1)"],["RESOURCES","```[Source code](https://github.com/masneyb/gftp)```"]],"e":["ftp","sftp","filezilla"],"x":"gftp"},"ghc":{"n":"ghc","t":"Compiler for the Haskell functional programming language","s":[["TAGLINE","Compiler for the Haskell functional programming language"],["TLDR","**Compile a Haskell file**\n\n```ghc [file.hs]```\n\n**Compile with optimization**\n\n```ghc -O2 [file.hs]```\n\n**Compile to object file only**\n\n```ghc -c [file.hs]```\n\n**Specify output name**\n\n```ghc -o [program] [file.hs]```\n\n**Enable all warnings**\n\n```ghc -Wall [file.hs]```\n\n**Compile modules in parallel**\n\n```ghc -j [file.hs]```\n\n**Enable a language extension**\n\n```ghc -XOverloadedStrings [file.hs]```"],["SYNOPSIS","**ghc** [_options_] _files_"],["PARAMETERS","_FILES_\n> Haskell source files (.hs, .lhs).\n\n**-o** _FILE_\n> Output file name.\n\n**-c**\n> Compile to object file only.\n\n**-O**, **-O1**\n> Enable standard optimization.\n\n**-O2**\n> Enable aggressive optimization with additional passes.\n\n**-O0**\n> Disable optimization (default).\n\n**-Wall**\n> Enable most warnings.\n\n**-w**\n> Suppress all warnings.\n\n**-Werror**\n> Treat warnings as errors.\n\n**-i**_DIR_\n> Add directory to import search path.\n\n**-package** _PKG_\n> Expose the specified package.\n\n**--make**\n> Build program and resolve module dependencies automatically.\n\n**-e** _EXPR_\n> Evaluate a single expression and exit.\n\n**-j**[_N_]\n> Compile N modules in parallel.\n\n**-threaded**\n> Use the threaded runtime system.\n\n**-prof**\n> Enable profiling.\n\n**-fllvm**\n> Compile via LLVM backend.\n\n**-X**_EXTENSION_\n> Enable a language extension (e.g., -XOverloadedStrings).\n\n**-cpp**\n> Run the C preprocessor on source files.\n\n**-v**[_N_]\n> Set verbosity level (0-3).\n\n**--help**\n> Display help information."],["DESCRIPTION","**ghc** (Glasgow Haskell Compiler) is the leading compiler for the Haskell programming language. It compiles Haskell source code to native machine code, producing efficient executables.\n\nGHC supports the full Haskell language standard plus numerous extensions for advanced type system features, parallelism, and performance. The --make mode (default) automatically handles module dependencies. GHC also provides native code generation and an optional LLVM backend."],["CAVEATS","Compilation can be memory-intensive. Large projects benefit from incremental builds. Extension flags vary by GHC version. The **-O2** level significantly increases compile time compared to **-O**."],["HISTORY","GHC was started at the **University of Glasgow** in **1989**. It has become the de facto standard Haskell compiler, actively developed by the Haskell community and industrial users."],["INSTALL","```apt: sudo apt install ghc```\n\n```dnf: sudo dnf install ghc```\n\n```pacman: sudo pacman -S ghc```\n\n```apk: sudo apk add ghc```\n\n```zypper: sudo zypper install ghc```\n\n```brew: brew install ghc```\n\n```nix: nix profile install nixpkgs#ghc```"],["SEE ALSO","[ghci](/man/ghci)(1), [runghc](/man/runghc)(1), [cabal](/man/cabal)(1), [stack](/man/stack)(1)"]],"e":["ghci","runghc","cabal","stack"],"x":"ghc [file.hs]"},"ghcid":{"n":"ghcid","t":"auto-reloading GHCi daemon for rapid Haskell development","s":[["TAGLINE","auto-reloading GHCi daemon for rapid Haskell development"],["TLDR","**Start** in the current project, auto-detecting ghci or cabal repl\n\n```ghcid```\n\n**Use** a specific repl command\n\n```ghcid --command=\"[cabal repl]\"```\n\n**Run** an expression after every successful load\n\n```ghcid --test=[main]```\n\n**Run** the test even when there are warnings\n\n```ghcid --test=[:main] --warnings```\n\n**Build** a particular component\n\n```ghcid --target=[lib:foo]```\n\n**Reload** when non-Haskell files change\n\n```ghcid --reload=[templates] --reload=[config.yaml]```\n\n**Clear** the screen on each reload\n\n```ghcid --clear```\n\n**Run** hlint when the code compiles cleanly\n\n```ghcid --lint```"],["SYNOPSIS","**ghcid** [_options_] [_module_ ...]"],["PARAMETERS","**-c**, **--command**=_COMMAND_\n> Command to run. Defaults to **ghci**, or **cabal repl** / **stack ghci** when a project is detected.\n\n**--target**=_TARGET_\n> Component to build, e.g. **lib:foo** for Cabal or **foo:lib** for Stack. May be repeated.\n\n**-T**, **--test**=_EXPR_\n> GHCi expression to run after a successful load.\n\n**-r**, **--run**[=_EXPR_]\n> Like **--test**, but defaults to **main**.\n\n**--test-message**=_MESSAGE_\n> Message shown before running the test. Defaults to **Running test...**.\n\n**-W**, **--warnings**\n> Allow tests to run even when the code produced warnings.\n\n**--lint**[=_COMMAND_]\n> Run a linter when there are no errors. Defaults to **hlint**.\n\n**--setup**=_COMMAND_\n> Commands passed to ghci on stdin at startup, usually **:set** something. May be repeated.\n\n**--reload**=_PATH_\n> Also reload when this file or directory changes. Defaults to none.\n\n**--restart**=_PATH_\n> Restart the whole command when this path changes. Defaults to **.ghci** and any **.cabal** file, unless a custom command or stack is in use.\n\n**-C**, **--directory**=_DIR_\n> Set the current directory. Defaults to **.**.\n\n**-o**, **--outputfile**=_FILE_\n> Write the full output to a file, in a format editors can parse.\n\n**--project**=_NAME_\n> Project name shown in the title. Defaults to the current directory.\n\n**--clear**\n> Clear the screen when reloading.\n\n**-S**, **--no-status**\n> Suppress status messages.\n\n**--reverse-errors**\n> Reverse the output order. Works best with **--no-height-limit**.\n\n**--no-height-limit**\n> Disable the height limit and print all messages.\n\n**--height**=_N_\n> Number of lines to use. Defaults to the console height.\n\n**-w**, **--width**=_N_\n> Number of columns to use. Defaults to the console width.\n\n**-n**, **--max-messages**=_N_\n> Maximum number of messages to print.\n\n**--colour**[=_always/never/auto_], **--color**\n> Colour the output. Defaults to **auto**.\n\n**--no-title**\n> Do not update the shell title or icon.\n\n**-t**, **--topmost**\n> Keep the window on top. Windows only.\n\n**--poll**[=_SECONDS_]\n> Poll for changes every N seconds instead of using filesystem notifications. Defaults to **0.1**.\n\n**--allow-eval**\n> Execute REPL commands written in source comments.\n\n**--ignore-loaded**\n> Keep going when no files are loaded. Requires **--reload**.\n\n**--no-cabal-repl-rtsopts**\n> Disable the default **+RTS -N -RTS** passed to an auto-selected **cabal repl**.\n\n**--server**\n> Enable the local ghcid socket server.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ghcid** is \"GHCi as a daemon\": it starts a GHCi session, watches your source files, and reloads whenever something changes, formatting the errors and warnings to fit the console. The author describes it as GHC plus a bit of an IDE.\n\nIts value is the speed of the feedback loop. Because GHCi stays resident, a reload only recompiles what changed, so errors typically appear within a fraction of a second of saving. The output is deliberately truncated to the terminal height, so a clean build shows **All good** and a broken one shows the first error at the top, where you are already looking.\n\nWith no arguments **ghcid** picks a sensible repl command for the project, using **cabal repl** or **stack ghci** when it detects one and falling back to plain **ghci**. **--test** turns it into a continuous test runner by evaluating an expression after each successful load, which combined with **--warnings** gives a tight red/green cycle without leaving the editor.\n\nThe tool is intentionally narrow: it does not integrate with editors, expose GHCi to you directly, depend on the GHC API, or run a web server. **--outputfile** exists so editors can consume its results instead."],["CAVEATS","**ghcid** is only as reliable as the underlying repl. If **cabal repl** or **ghci** cannot load your project by hand, **ghcid** will not fix it; test the command directly first and pass a working one with **--command**.\n\nReloading only picks up changes GHCi itself can see. Changes to **.cabal** files, CPP includes, or generated code often need a full restart, which is what **--restart** is for. Non-Haskell assets need an explicit **--restart** or **--reload**.\n\nFilesystem notifications can be unreliable over network mounts, in containers and in some virtual machines; use **--poll** when saves seem to go unnoticed.\n\n**--test** runs inside the same GHCi session, so an expression that loops forever or calls **exitWith** will hang or kill the daemon.\n\n**--allow-eval** executes code found in comments, so avoid it on code you have not read."],["HISTORY","**ghcid** was written by **Neil Mitchell**, with significant contributions from **JP Moresmau**, and released around **2014**. It became a standard part of many Haskell development setups by offering fast, editor-independent feedback, and is distributed through **Hackage** and **Stackage**."],["INSTALL","```dnf: sudo dnf install ghcid```\n\n```brew: brew install ghcid```\n\n```nix: nix profile install nixpkgs#ghcid```"],["SEE ALSO","[ghci](/man/ghci)(1), [ghc](/man/ghc)(1), [cabal](/man/cabal)(1), [stack](/man/stack)(1), [hlint](/man/hlint)(1)"],["RESOURCES","```[Source code](https://github.com/ndmitchell/ghcid)```\n\n```[Documentation](https://hackage.haskell.org/package/ghcid)```"]],"e":["ghci","ghc","cabal","stack","hlint"],"x":"ghcid"},"git-tag":{"n":"git-tag","t":"Create, list, and manage tags","s":[["TAGLINE","Create, list, and manage tags"],["TLDR","**List tags**\n\n```git tag```\n\n**Create lightweight tag**\n\n```git tag [name]```\n\n**Create annotated tag**\n\n```git tag -a [name] -m \"[message]\"```\n\n**Create signed tag**\n\n```git tag -s [name] -m \"[message]\"```\n\n**Tag specific commit**\n\n```git tag [name] [commit]```\n\n**Delete tag**\n\n```git tag -d [name]```\n\n**Push tag to remote**\n\n```git push origin [name]```\n\n**List tags matching pattern**\n\n```git tag -l \"[v1.*]\"```"],["SYNOPSIS","**git** **tag** [_options_] [_name_] [_commit_]"],["PARAMETERS","**-a**, **--annotate**\n> Create annotated tag.\n\n**-m**, **--message** _msg_\n> Tag message.\n\n**-s**, **--sign**\n> Create signed tag.\n\n**-d**, **--delete**\n> Delete tag.\n\n**-f**, **--force**\n> Force replace tag.\n\n**-l**, **--list** _pattern_\n> List matching tags.\n\n**-n** _num_\n> Show lines of annotation.\n\n**--contains** _commit_\n> Tags containing commit.\n\n**--sort** _key_\n> Sort tags.\n\n**--points-at** _object_\n> List tags pointing at the given object.\n\n**--format** _format_\n> Format output using a `git for-each-ref`-style placeholder string.\n\n**-v**, **--verify**\n> Verify the GPG signature of a signed tag."],["DESCRIPTION","**git tag** creates, lists, deletes, and verifies tag objects. Tags mark specific points in history as important, typically used for release versions.\n\nLightweight tags are simple pointers to a commit, while annotated tags store extra metadata such as the tagger name, date, and a message. Signed tags add a GPG signature for verification."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-branch](/man/git-branch)(1), [git-commit](/man/git-commit)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git-tag)```"]],"e":["git-branch","git-commit"],"x":"git tag"},"git-unlock":{"n":"git-unlock","t":"Resume tracking changes to a locked file","s":[["TAGLINE","Resume tracking changes to a locked file"],["TLDR","**Unlock a file**\n\n```git unlock [file]```"],["SYNOPSIS","**git** **unlock** _file_"],["DESCRIPTION","**git unlock** runs `git update-index --no-skip-worktree` on a file, clearing the **skip-worktree** bit so Git resumes tracking local changes to it. This reverses `git lock`, which sets `--skip-worktree` to make Git ignore modifications to a file (typically one holding local config or secrets that shouldn't be committed).\n\nPart of the **git-extras** package."],["CAVEATS","Uses the `skip-worktree` bit, not `assume-unchanged`; the two are similar but intended for different purposes, and tools that check one won't necessarily see the other."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-lock](/man/git-lock)(1), [git-update-index](/man/git-update-index)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-unlock)```"]],"e":["git-lock","git-update-index","git-extras"],"x":"git unlock [file]"},"httpstat":{"n":"httpstat","t":"Curl statistics made simple and visual","s":[["TAGLINE","Curl statistics made simple and visual"],["TLDR","**Show HTTP request statistics** for a URL\n\n```httpstat [https://example.com]```\n\n**Follow redirects**\n\n```httpstat -L [https://example.com]```\n\n**Send a POST request**\n\n```httpstat -X POST -d \"[data]\" [https://example.com/api]```\n\n**Use custom headers**\n\n```httpstat -H \"Authorization: Bearer [token]\" [https://example.com/api]```"],["SYNOPSIS","**httpstat** [_curl_options_] _URL_"],["DESCRIPTION","**httpstat** visualizes **curl** statistics in a clear and beautiful way. It wraps around curl to capture and display the time spent in each phase of an HTTP request: **DNS lookup**, **TCP connection**, **TLS handshake**, **server processing**, and **content transfer**. This breakdown helps identify performance bottlenecks in web requests.\n\nBecause httpstat is a wrapper around cURL, you can pass any cURL-supported option after the URL (except **-w**, **-D**, **-o**, **-s**, **-S** which are used internally)."],["CAVEATS","Relies on curl being installed on the system. Some curl options (-w, -D, -o, -s, -S) conflict with httpstat's internal usage and cannot be passed through. The Python version requires Python 3."],["HISTORY","**httpstat** was created by **Reorx** and is written in **Python** as a single-file script with no dependencies. It quickly became popular and inspired implementations in **Go**, **Rust**, and **Nim**. The Go version by **Dave Cheney** is a standalone binary that does not require curl."],["INSTALL","```brew: brew install httpstat```\n\n```nix: nix profile install nixpkgs#httpstat```"],["SEE ALSO","[curl](/man/curl)(1), [wget](/man/wget)(1)"]],"e":["curl","wget"],"x":"httpstat [https://example.com]"},"in-toto-record":{"n":"in-toto-record","t":"creates supply chain attestations for build steps","s":[["TAGLINE","creates supply chain attestations for build steps"],["TLDR","**Start recording**\n\n```in-toto-record start -n [step_name] -k [key.pem]```\n\n**Stop recording**\n\n```in-toto-record stop -n [step_name] -k [key.pem] -p [products]```\n\n**Record with materials**\n\n```in-toto-record start -n [step] -k [key] -m [src/]```\n\n**Specify products**\n\n```in-toto-record stop -n [step] -k [key] -p [dist/]```"],["SYNOPSIS","**in-toto-record** _command_ [_options_]"],["PARAMETERS","**start**\n> Begin recording step.\n\n**stop**\n> Finish recording step.\n\n**-n** _NAME_\n> Step name (used to associate link with layout step).\n\n**-k** _KEY_, **--signing-key** _KEY_\n> Path to signing key file.\n\n**-g** _ID_, **--gpg** _ID_\n> GPG keyid used to sign the resulting link metadata.\n\n**--gpg-home** _PATH_\n> Path to GPG home directory.\n\n**-m** _MATERIALS_\n> Input materials paths (used with start).\n\n**-p** _PRODUCTS_\n> Output products paths (used with stop).\n\n**-d** _DIR_\n> Directory to store resulting link metadata (used with stop).\n\n**--exclude** _PATTERN_\n> Path patterns to exclude from recording.\n\n**--base-path** _PATH_\n> Base path for relative material/product paths.\n\n**--lstrip-paths** _PREFIX_\n> Left-strip path prefix before storing in link metadata.\n\n**--use-dsse**\n> Use DSSE envelope format for signing.\n\n**-v**\n> Verbose output.\n\n**-q**\n> Quiet mode."],["DESCRIPTION","**in-toto-record** creates supply chain attestations for build steps. It records materials before and products after a step, generating signed link metadata.\n\nThe tool supports split recording where start and stop are separate commands. This allows recording steps that span multiple commands or sessions."],["CAVEATS","Requires signing key. Part of in-toto framework. Links must be verified."],["HISTORY","in-toto-record is part of **in-toto**, a framework for supply chain integrity developed at **NYU** Secure Systems Lab."],["SEE ALSO","[in-toto-run](/man/in-toto-run)(1), [in-toto-sign](/man/in-toto-sign)(1)"]],"e":["in-toto-run","in-toto-sign"],"x":"in-toto-record start -n [step_name] -k [key.pem]"},"in-toto-run":{"n":"in-toto-run","t":"executes a command while recording supply chain metadata","s":[["TAGLINE","executes a command while recording supply chain metadata"],["TLDR","**Run and record command**\n\n```in-toto-run -n [step_name] -k [key.pem] -- [command]```\n\n**Record with materials and products**\n\n```in-toto-run -n [build] -k [key] -m [src/] -p [dist/] -- make```\n\n**Exclude patterns**\n\n```in-toto-run -n [step] -k [key] --exclude [*.log] -- ./build.sh```\n\n**Record without running**\n\n```in-toto-run -n [step] -k [key] -m [files] -p [files]```"],["SYNOPSIS","**in-toto-run** [_options_] [-- _command_]"],["PARAMETERS","**-n** _NAME_\n> Step name for link metadata.\n\n**-k** _KEY_\n> Private key for signing.\n\n**-m** _MATERIALS_\n> Materials (inputs) to record.\n\n**-p** _PRODUCTS_\n> Products (outputs) to record.\n\n**--exclude** _PATTERN_\n> Exclude files matching pattern.\n\n**-c** _COMMAND_\n> Command to run (alternative to --).\n\n**--help**\n> Display help information."],["DESCRIPTION","**in-toto-run** executes a command while recording supply chain metadata. It captures materials before execution and products after, creating a signed link file.\n\nThe tool is central to in-toto supply chain security. Each link provides cryptographic proof of what inputs and outputs a build step had."],["CAVEATS","Requires signing key. Links must match layout. Python-based tool."],["HISTORY","in-toto-run is the primary recording tool in **in-toto**, developed at **NYU** for protecting software supply chain integrity."],["SEE ALSO","[in-toto-record](/man/in-toto-record)(1), [in-toto-sign](/man/in-toto-sign)(1)"]],"e":["in-toto-record","in-toto-sign"],"x":"in-toto-run -n [step_name] -k [key.pem] -- [command]"},"kmscon":{"n":"kmscon","t":"terminal emulator that runs on the Linux console using the kernel mode setting","s":[["TAGLINE","terminal emulator that runs on the Linux console using the kernel mode setting"],["TLDR","**Start** kmscon on the first available TTY\n\n```sudo kmscon```\n\nStart on a **specific TTY**\n\n```sudo kmscon --vt [/dev/ttyX]```\n\nEnable **mouse** support\n\n```sudo kmscon --mouse```\n\nSpecify **login command**\n\n```sudo kmscon -l [command]```"],["SYNOPSIS","**kmscon** [_options_]"],["PARAMETERS","**--vt** _TTY_\n> Specify the virtual terminal to use\n\n**--mouse**\n> Enable mouse support\n\n**-l**, **--login** _COMMAND_\n> Specify the login command to execute\n\n**--font-name** _NAME_\n> Set the font to use\n\n**--font-size** _SIZE_\n> Set font size in points"],["DESCRIPTION","**kmscon** is a terminal emulator that runs on the Linux console using the kernel mode setting (KMS) and framebuffer. It provides a modern terminal experience on TTYs without requiring X11.\n\nUnlike the traditional text-mode VT, kmscon uses the GPU framebuffer, enabling features like Unicode support, TrueType fonts, and hardware-accelerated rendering on virtual terminals."],["CAVEATS","Requires KMS support from the graphics driver. May conflict with other framebuffer applications. Some systems may need kernel parameters adjusted for proper operation."],["HISTORY","kmscon was developed as part of the systemd project to provide a modern replacement for the Linux VT subsystem. It uses libtsm for terminal emulation and works with Wayland-like infrastructure."],["INSTALL","```dnf: sudo dnf install kmscon```\n\n```pacman: sudo pacman -S kmscon```\n\n```zypper: sudo zypper install kmscon```\n\n```nix: nix profile install nixpkgs#kmscon```"],["SEE ALSO","[getty](/man/getty)(8), [agetty](/man/agetty)(8)"]],"e":["getty","agetty"],"x":"sudo kmscon"},"kubectl-uncordon":{"n":"kubectl-uncordon","t":"marks a node as schedulable","s":[["TAGLINE","marks a node as schedulable"],["TLDR","**Uncordon node**\n\n```kubectl uncordon [node-name]```\n\n**Uncordon multiple nodes**\n\n```kubectl uncordon [node1] [node2]```\n\n**Uncordon with selector**\n\n```kubectl uncordon -l [key=value]```\n\n**Dry run**\n\n```kubectl uncordon [node-name] --dry-run=client```"],["SYNOPSIS","**kubectl uncordon** [_options_] _node_"],["PARAMETERS","_NODE_\n> Node name to uncordon.\n\n**-l** _SELECTOR_\n> Label selector.\n\n**--dry-run** _MODE_\n> Simulate operation.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kubectl uncordon** marks a node as schedulable. It reverses the effect of cordon.\n\nThe command is used after node maintenance. New pods can be scheduled once uncordoned."],["CAVEATS","Subcommand of kubectl. Only marks schedulable. Pods don't automatically move back."],["HISTORY","kubectl uncordon provides node scheduling restoration for **Kubernetes** after maintenance."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-cordon](/man/kubectl-cordon)(1), [kubectl-drain](/man/kubectl-drain)(1)"]],"e":["kubectl","kubectl-cordon","kubectl-drain"],"x":"kubectl uncordon [node-name]"},"kwin":{"n":"kwin","t":"window manager for KDE Plasma","s":[["TAGLINE","window manager for KDE Plasma"],["TLDR","**Start KWin (X11)**\n\n```kwin_x11 --replace```\n\n**Start KWin Wayland**\n\n```kwin_wayland```\n\n**Restart KWin**\n\n```kwin_x11 --replace &```\n\n**Reconfigure KWin**\n\n```qdbus org.kde.KWin /KWin reconfigure```\n\n**List effects**\n\n```qdbus org.kde.KWin /Effects listLoadedEffects```"],["SYNOPSIS","**kwin_x11** [_options_]\n\n**kwin_wayland** [_options_]"],["PARAMETERS","**--replace**\n> Replace running window manager.\n\n**--crashes** _n_\n> Crash limit for restart.\n\n**--lock**\n> Start with screen locked.\n\n**--no-kactivities**\n> Disable KActivities."],["DESCRIPTION","**KWin** is the window manager for KDE Plasma. It handles window placement, decorations, virtual desktops, and desktop effects (compositing).\n\nKWin supports both X11 (kwin_x11) and Wayland (kwin_wayland) sessions, providing features like window tiling, wobbly windows, desktop grid, and scripting."],["D-BUS CONTROL","```bash"],["Toggle present windows","qdbus org.kde.KWin /Effects togglePresentWindows"],["Show desktop grid","qdbus org.kde.KWin /Effects showDesktopGrid"],["Reconfigure after settings change","qdbus org.kde.KWin /KWin reconfigure\n```"],["CAVEATS","Wayland session has different capabilities than X11. Some effects require OpenGL. Scripting uses JavaScript."],["HISTORY","KWin was originally written by **Matthias Ettrich** as part of KDE 1 in **1997**. It has evolved significantly, adding compositing support in KDE 4 and Wayland support in Plasma 5."],["INSTALL","```apt: sudo apt install kwin-x11```\n\n```dnf: sudo dnf install kwin-x11```\n\n```pacman: sudo pacman -S kwin-x11```"],["SEE ALSO","[plasmashell](/man/plasmashell)(1), [systemsettings](/man/systemsettings)(1), [qdbus](/man/qdbus)(1)"]],"e":["plasmashell","systemsettings","qdbus"],"x":"kwin_x11 --replace"},"links2":{"n":"links2","t":"Web browser with both text and graphics mode support","s":[["TAGLINE","Web browser with both text and graphics mode support"],["TLDR","**Browse in graphics mode**\n\n```links2 -g [https://example.com]```\n\n**Browse in text mode**\n\n```links2 [https://example.com]```\n\n**Dump rendered page to stdout**\n\n```links2 -dump [https://example.com]```\n\n**Dump with specific text width**\n\n```links2 -dump -width [120] [https://example.com]```\n\n**Dump page source**\n\n```links2 -source [https://example.com]```\n\n**Start in graphics mode with specific resolution**\n\n```links2 -g -mode [1024x768x24] [https://example.com]```"],["SYNOPSIS","**links2** [_options_] [_url_]"],["PARAMETERS","**-g**\n> Start in graphics mode.\n\n**-t**\n> Force text mode.\n\n**-driver** _driver_\n> Select graphics driver (x, svgalib, fb, directfb).\n\n**-mode** _WxHxD_\n> Graphics mode resolution and color depth (e.g., 1024x768x24).\n\n**-depth** _N_\n> Color depth in graphics mode.\n\n**-anonymous**\n> Anonymous browsing mode (disables local file access and bookmarks).\n\n**-dump**\n> Dump rendered page text to stdout and exit.\n\n**-width** _N_\n> Text width in characters for -dump mode.\n\n**-source**\n> Dump raw HTML page source to stdout and exit.\n\n**-version**\n> Print version and exit."],["DESCRIPTION","**links2** is a web browser with both text and graphics mode support. In graphics mode, it can display images and render pages more visually while still being lightweight. It is an enhanced version of the original links browser.\n\nIn text mode, it works similarly to lynx or w3m. In graphics mode (-g), it provides a full visual browsing experience using X11, framebuffer, or SVGAlib.\n\nThe **-dump** option is useful for scripting, converting web pages to plain text output."],["CAVEATS","Graphics mode requires X11 or framebuffer support. Not all graphics drivers are available on all systems. JavaScript is not supported."],["INSTALL","```apt: sudo apt install links2```\n\n```nix: nix profile install nixpkgs#links2```"],["SEE ALSO","[links](/man/links)(1), [lynx](/man/lynx)(1), [w3m](/man/w3m)(1), [curl](/man/curl)(1), [wget](/man/wget)(1)"]],"e":["links","lynx","w3m","curl","wget"],"x":"links2 -g [https://example.com]"},"magick-compare":{"n":"magick-compare","t":"compares two images and outputs their differences","s":[["TAGLINE","compares two images and outputs their differences"],["TLDR","**Compare two images**\n\n```magick compare [image1.png] [image2.png] [diff.png]```\n\n**Calculate difference metric**\n\n```magick compare -metric AE [image1.png] [image2.png] null:```\n\n**Use specific comparison method**\n\n```magick compare -metric RMSE [image1.png] [image2.png] null:```\n\n**Highlight differences in red**\n\n```magick compare -highlight-color red [image1.png] [image2.png] [diff.png]```\n\n**Search for a subimage** within a larger image\n\n```magick compare -subimage-search [haystack.png] [needle.png] [result.png]```\n\n**Print detailed per-channel** distortion stats\n\n```magick compare -verbose -metric AE [image1.png] [image2.png] null:```"],["SYNOPSIS","**magick** **compare** [_options_] _image1_ _image2_ [_diff_]"],["PARAMETERS","**-metric** _type_\n> Comparison metric (AE, RMSE, PSNR, MAE, MSE, NCC, PHASH, DSSIM). Default: RMSE.\n\n**-highlight-color** _color_\n> Color used to mark differing pixels.\n\n**-lowlight-color** _color_\n> Color used for matching pixels.\n\n**-fuzz** _percent_\n> Color tolerance for comparison.\n\n**-subimage-search**\n> Search for the smaller image inside the larger.\n\n**-dissimilarity-threshold** _value_\n> Maximum permitted distortion for a match (default: 0.2).\n\n**-similarity-threshold** _value_\n> Minimum distortion considered similar (default: 0.0).\n\n**-channel** _type_\n> Restrict comparison to specified channels.\n\n**-verbose**\n> Print per-channel distortion metrics."],["DESCRIPTION","**magick compare** compares two images and outputs their differences. Part of ImageMagick. Produces a difference image and/or calculates numerical metrics. Useful for regression testing and quality assurance."],["INSTALL","```pacman: sudo pacman -S imagemagick```\n\n```apk: sudo apk add imagemagick```\n\n```brew: brew install imagemagick```\n\n```nix: nix profile install nixpkgs#imagemagick```"],["SEE ALSO","[magick](/man/magick)(1), [magick-convert](/man/magick-convert)(1)"]],"e":["magick","magick-convert"],"x":"magick compare [image1.png] [image2.png] [diff.png]"},"mandb":{"n":"mandb","t":"creates and updates the manual page index caches","s":[["TAGLINE","creates and updates the manual page index caches"],["TLDR","**Update** man page database\n\n```mandb```\n\nUpdate **single entry**\n\n```mandb --filename [path/to/file]```\n\n**Create** database from scratch\n\n```mandb --create```\n\nProcess **user databases** only\n\n```mandb --user-db```\n\n**Don't purge** obsolete entries\n\n```mandb --no-purge```\n\n**Test** man pages validity\n\n```mandb --test```"],["SYNOPSIS","**mandb** [_options_] [_manpath_]"],["DESCRIPTION","**mandb** creates and updates the manual page index caches. These caches are used by man and apropos to quickly find manual pages. It scans the manual page directories and builds a database of page names and descriptions."],["PARAMETERS","**--filename FILE**\n> Update single file entry\n\n**-c, --create**\n> Create database from scratch\n\n**-u, --user-db**\n> Process only user databases\n\n**-p, --no-purge**\n> Don't purge obsolete entries\n\n**-t, --test**\n> Test validity without updating\n\n**-q, --quiet**\n> Suppress warnings\n\n**-d, --debug**\n> Print debugging information"],["CAVEATS","Usually run automatically by package managers. May require root for system-wide databases."],["INSTALL","```apt: sudo apt install man-db```\n\n```dnf: sudo dnf install man-db```\n\n```pacman: sudo pacman -S man-db```\n\n```apk: sudo apk add man-db```\n\n```brew: brew install man-db```\n\n```nix: nix profile install nixpkgs#man-db```"],["SEE ALSO","[man](/man/man)(1), [apropos](/man/apropos)(1), [whatis](/man/whatis)(1), [manpath](/man/manpath)(1)"]],"e":["man","apropos","whatis","manpath"],"x":"mandb"},"mods":{"n":"mods","t":"AI assistant for the command line by Charm","s":[["TAGLINE","AI assistant for the command line by Charm"],["TLDR","**Ask a one-off question**\n\n```mods \"[explain what SIGPIPE does]\"```\n\n**Pipe file contents into a prompt**\n\n```cat [file.py] | mods \"[review this code for bugs]\"```\n\n**Select a specific model**\n\n```mods -m [gpt-4o] \"[summarise this]\"```\n\n**Continue the last conversation**\n\n```mods -C \"[and how would I test that?]\"```\n\n**Resume a named conversation**\n\n```mods -c [refactor] \"[next step]\"```\n\n**Format the response as Markdown**\n\n```mods -f \"[write release notes from these commits]\"```\n\n**List saved conversations**\n\n```mods -l```\n\n**Apply a custom role / system prompt**\n\n```mods --role [shell] \"[find large files in /var]\"```"],["SYNOPSIS","**mods** [_options_] [_prompt_]"],["PARAMETERS","_PROMPT_\n> Text passed to the model. Combined with any data read from STDIN.\n\n**-m**, **--model** _NAME_\n> Use the named model (for example `gpt-4o`, `claude-3-5-sonnet`, a local Ollama model).\n\n**-M**, **--ask-model**\n> Prompt interactively to choose a model.\n\n**-f**, **--format**\n> Ask the model for formatted (Markdown) output and render it in the terminal.\n\n**--format-as** _FORMAT_\n> Specify the output format (for example `markdown`, `json`).\n\n**-r**, **--raw**\n> Print the raw, unformatted response.\n\n**-q**, **--quiet**\n> Suppress non-error output.\n\n**-P**, **--prompt**\n> Include the prompt from the command arguments and STDIN in the response.\n\n**-p**, **--prompt-args**\n> Include the CLI prompt arguments in the response.\n\n**--max-tokens** _N_\n> Limit the response length.\n\n**--no-limit**\n> Do not restrict the response length.\n\n**--word-wrap** _WIDTH_\n> Wrap output at the given column (default: 80).\n\n**-t**, **--title** _NAME_\n> Title the current conversation for later retrieval.\n\n**-l**, **--list**\n> List saved conversations.\n\n**-c**, **--continue** _NAME_\n> Resume a saved conversation by name.\n\n**-C**, **--continue-last**\n> Resume the most recent conversation.\n\n**-s**, **--show** _NAME_\n> Print a saved conversation.\n\n**--no-cache**\n> Do not persist the conversation to the cache.\n\n**--role** _NAME_\n> Apply a custom role / system prompt defined in the config.\n\n**--temp** _FLOAT_\n> Sampling temperature.\n\n**--topp** _FLOAT_, **--topk** _INT_\n> Top-p / top-k sampling parameters.\n\n**--theme** _NAME_\n> UI theme (`charm`, `catppuccin`, `dracula`, `base16`).\n\n**-x**, **--http-proxy** _URL_\n> Route requests through an HTTP proxy.\n\n**--settings**\n> Open the settings file in `$EDITOR`."],["DESCRIPTION","**mods** is a command-line AI companion that turns STDIN and arguments into a prompt, sends it to a configured LLM, and streams the response back to the terminal. It supports OpenAI, Anthropic, Cohere, Groq, Google Gemini, Azure, and local providers such as Ollama and LocalAI, selected per-request via **--model** or in the YAML configuration.\n\nConversations are cached to disk so they can be resumed by name or continued from the last turn, and Markdown rendering via Glamour lets replies display with syntax highlighting and headings when **--format** is set."],["CAVEATS","Requires an API key (or local endpoint) configured for the chosen provider; commercial providers incur per-token costs. Prompts sent from the shell are stored in plain text in the cache directory unless **--no-cache** is used.\n\nAs of **March 2026** the upstream project is archived; Charm recommends **Crush** as its successor, though existing mods installations continue to work."],["HISTORY","**mods** was created by **Charm** as part of their suite of terminal tools (alongside gum, glow, and charm). It was archived in March 2026 in favour of Charm's newer Crush CLI."],["INSTALL","```brew: brew install mods```\n\n```nix: nix profile install nixpkgs#mods```"],["SEE ALSO","[llm](/man/llm)(1), [ollama](/man/ollama)(1), [glow](/man/glow)(1), [gum](/man/gum)(1), [charm](/man/charm)(1)"]],"e":["llm","ollama","glow","gum","charm"],"x":"mods \"[explain what SIGPIPE does]\""},"npm-why":{"n":"npm-why","t":"shows why a package is installed","s":[["TAGLINE","shows why a package is installed"],["TLDR","**Explain why a package is installed**\n\n```npm why [package-name]```\n\n**Explain a specific version**\n\n```npm why [package-name]@[version]```\n\n**JSON output**\n\n```npm why [package-name] --json```\n\n**Limit depth** of the dependency tree\n\n```npm why [package-name] --long```\n\n**Target a specific workspace**\n\n```npm why [package-name] --workspace=[workspace-name]```"],["SYNOPSIS","**npm** **why** _package-spec_..."],["PARAMETERS","_PACKAGE-SPEC_\n> Package name, optionally with version range (e.g., lodash@^4).\n\n**--json**\n> Output results in JSON format.\n\n**--long**\n> Show extended information.\n\n**--workspace** _NAME_\n> Limit to a named workspace or path.\n\n**--include-workspace-root**\n> Include root project in addition to workspaces.\n\n**--help**\n> Display help information."],["DESCRIPTION","**npm why** shows why a package is installed. Alias for npm explain.\n\nThe command traces dependency paths. Shows all routes to package."],["CAVEATS","Alias for npm explain. Shows all dependency paths. Requires installed package."],["HISTORY","npm why provides **dependency tracing** to understand package relationships."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-explain](/man/npm-explain)(1), [npm-ls](/man/npm-ls)(1)"]],"e":["npm","npm-explain","npm-ls"],"x":"npm why [package-name]"},"pct-console":{"n":"pct-console","t":"attaches to a container's TTY console, providing direct terminal access","s":[["TAGLINE","attaches to a container's TTY console, providing direct terminal access"],["TLDR","**Attach** to container console by ID\n\n```pct console [100]```"],["SYNOPSIS","**pct console** _vmid_"],["DESCRIPTION","**pct console** attaches to a container's TTY console, providing direct terminal access. This is useful for accessing containers that don't have network connectivity or for troubleshooting boot issues."],["PARAMETERS","**vmid**\n> The numeric ID of the container"],["CAVEATS","Press **Ctrl+a** then **q** to detach from the console without terminating the session. The container must be running. For non-interactive command execution, use **pct exec** instead. For entering the container's namespace directly, use **pct enter**."],["HISTORY","**pct console** is part of the **Proxmox VE** virtualization platform for managing LXC containers."],["SEE ALSO","[pct](/man/pct)(1), [pct-enter](/man/pct-enter)(1), [pct-exec](/man/pct-exec)(1)"]],"e":["pct","pct-enter","pct-exec"],"x":"pct console [100]"},"pnmquantall":{"n":"pnmquantall","t":"Quantize multiple images to shared palette","s":[["TAGLINE","Quantize multiple images to shared palette"],["TLDR","**Quantize multiple images to same palette**\n\n```pnmquantall [256] [image1.pnm] [image2.pnm] ...```"],["SYNOPSIS","**pnmquantall** _ncolors_ _files_..."],["PARAMETERS","_ncolors_\n> Maximum number of colors.\n\n_files_\n> Input image files (modified in place)."],["DESCRIPTION","**pnmquantall** reduces multiple PNM images to use a common color palette. Useful for creating consistent color animations. Part of Netpbm toolkit."],["CAVEATS","Modifies files in place."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm-extras```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmquantall](/man/ppmquantall)(1), [pnmquant](/man/pnmquant)(1)"]],"e":["ppmquantall","pnmquant"],"x":"pnmquantall [256] [image1.pnm] [image2.pnm] ..."},"powershell":{"n":"powershell","t":"Cross-platform shell and scripting language","s":[["TAGLINE","Cross-platform shell and scripting language"],["TLDR","**Start PowerShell**\n\n```pwsh```\n\n**Execute a command**\n\n```pwsh -Command \"[Get-Process]\"```\n\n**Run a script file**\n\n```pwsh -File [script.ps1]```\n\n**Run without profile**\n\n```pwsh -NoProfile```\n\n**Execute and exit**\n\n```pwsh -Command \"[command]\" -NoExit```\n\n**Run in non-interactive mode**\n\n```pwsh -NonInteractive -Command \"[command]\"```\n\n**Output in specific format**\n\n```pwsh -Command \"[Get-Process | ConvertTo-Json]\"```\n\n**Check version**\n\n```pwsh -Version```"],["SYNOPSIS","**pwsh** [_-Command command_] [_-File script_] [_-NoProfile_] [_-NonInteractive_] [_options_]"],["PARAMETERS","**-Command**, **-c** _COMMAND_\n> Execute specified command string.\n\n**-File**, **-f** _FILE_\n> Run script file.\n\n**-NoProfile**, **-nop**\n> Don't load profile scripts.\n\n**-NoLogo**, **-nol**\n> Hide startup banner.\n\n**-NonInteractive**, **-noni**\n> Don't prompt for input.\n\n**-NoExit**, **-noe**\n> Don't exit after running command.\n\n**-InputFormat** _FORMAT_\n> Input format: Text, XML.\n\n**-OutputFormat** _FORMAT_\n> Output format: Text, XML.\n\n**-EncodedCommand** _BASE64_\n> Run base64-encoded command.\n\n**-ExecutionPolicy** _POLICY_\n> Set execution policy for session.\n\n**-ConfigurationName** _NAME_\n> Use specific configuration.\n\n**-WorkingDirectory** _PATH_\n> Set initial working directory.\n\n**-Login**, **-l**\n> Run as login shell.\n\n**-Version**, **-v**\n> Show version.\n\n**-Help**, **-?**\n> Show help."],["DESCRIPTION","**PowerShell** (pwsh) is Microsoft's cross-platform shell and scripting language. It uses object-oriented pipelines rather than text streams, making data manipulation more structured.\n\nCommands (cmdlets) follow Verb-Noun naming: Get-Process, Set-Location, Remove-Item. Objects passed through pipelines retain properties and methods, enabling rich querying without text parsing.\n\nPowerShell integrates with .NET, providing access to its entire library. Variables are typed, parameters are named, and error handling uses try/catch/finally constructs.\n\nProfiles (~/.config/powershell/profile.ps1 on Linux) customize the environment. Functions, aliases, and variables defined in profiles persist across sessions.\n\nThe language supports advanced features: classes, modules, remoting, jobs (background tasks), and workflows. It's equally capable as an interactive shell and for automation scripting."],["CONFIGURATION","**~/.config/powershell/profile.ps1** (Linux/macOS), **$HOME\\Documents\\PowerShell\\profile.ps1** (Windows)\n> User profile script executed on every session start. Defines functions, aliases, variables, and prompt customizations.\n\n**$PROFILE.AllUsersAllHosts**\n> System-wide profile for all users, typically in the PowerShell installation directory.\n\n**PSModulePath**\n> Environment variable listing directories where PowerShell searches for modules."],["CAVEATS","PowerShell 7+ (pwsh) differs from Windows PowerShell 5.1. Not all Windows modules work on Linux. Case-insensitive by default (unlike bash). Different quoting rules than POSIX shells. .NET dependency adds startup overhead."],["HISTORY","**PowerShell** was created by **Jeffrey Snover** at Microsoft, released in **2006** for Windows. Open-source cross-platform PowerShell Core was released in **2016**, later renamed to PowerShell 7 in **2020**. It brought modern shell capabilities to Windows and extended to Linux/macOS."],["INSTALL","```brew: brew install powershell```\n\n```nix: nix profile install nixpkgs#powershell```"],["SEE ALSO","[bash](/man/bash)(1), [zsh](/man/zsh)(1), [fish](/man/fish)(1), [dotnet](/man/dotnet)(1)"]],"e":["bash","zsh","fish","dotnet"],"x":"pwsh"},"pveperf":{"n":"pveperf","t":"Benchmark Proxmox host performance","s":[["TAGLINE","Benchmark Proxmox host performance"],["TLDR","Benchmark **root filesystem** performance\n\n```pveperf```\n\nBenchmark a **specific mount point**\n\n```pveperf [/mnt/storage]```"],["SYNOPSIS","**pveperf** [_path_]"],["PARAMETERS","_path_\n> Mount point to test (default: /)"],["DESCRIPTION","**pveperf** runs a quick benchmark to measure CPU and storage performance on a Proxmox VE host. It reports CPU BOGOMIPS, regex processing speed (REGEX/SECOND), hard disk size, buffered read speed, average seek time, fsync rate (FSYNCS/SECOND), and DNS resolution times (external and internal).\n\nThe results help identify potential performance bottlenecks and verify that storage meets minimum requirements for virtualization workloads. The fsync rate is particularly important for database and VM disk performance. Default test runs on the root filesystem mount point."],["CAVEATS","Benchmark results are approximate and may vary between runs. For accurate storage benchmarking, consider dedicated tools like [fio](/man/fio)(1). Results should be interpreted in context of workload requirements. Requires root privileges."],["HISTORY","Part of **Proxmox VE**, providing quick system performance validation. Useful for initial hardware assessment and troubleshooting performance issues in virtualization environments."],["SEE ALSO","[pveversion](/man/pveversion)(1), [pvesh](/man/pvesh)(1)"]],"e":["pveversion","pvesh"],"x":"pveperf"},"qm-reset":{"n":"qm-reset","t":"Hard reset a Proxmox virtual machine","s":[["TAGLINE","Hard reset a Proxmox virtual machine"],["TLDR","**Reset** a virtual machine\n\n```qm reset 100```\n\nReset and **skip lock** (root only)\n\n```qm reset --skiplock true 100```"],["SYNOPSIS","**qm reset** [_options_] _vmid_"],["PARAMETERS","**--skiplock** _true|false_\n> Ignore locks and force reset (root only)"],["DESCRIPTION","**qm reset** performs a hard reset on a virtual machine, equivalent to pressing the reset button on physical hardware. This immediately restarts the VM without graceful shutdown.\n\nUse this when the guest OS is unresponsive and a graceful reboot is not possible."],["CAVEATS","Hard reset may cause data loss in the guest if filesystems are not properly synchronized. The skiplock option requires root privileges."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management tools for virtual machine administration."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-reboot](/man/qm-reboot)(1), [qm-shutdown](/man/qm-shutdown)(1)"]],"e":["qm","qm-reboot","qm-shutdown"],"x":"qm reset 100"},"qm-stop":{"n":"qm-stop","t":"Force stop a Proxmox virtual machine","s":[["TAGLINE","Force stop a Proxmox virtual machine"],["TLDR","**Stop a VM** immediately\n\n```qm stop [vm_id]```\n\n**Stop with a timeout** (wait up to N seconds)\n\n```qm stop [vm_id] --timeout [30]```\n\n**Skip lock check** (root only)\n\n```qm stop [vm_id] --skiplock true```\n\n**Keep storage volumes active** after stopping\n\n```qm stop [vm_id] --keepActive true```\n\n**Override a pending shutdown** and stop immediately\n\n```qm stop [vm_id] --overrule-shutdown true```"],["SYNOPSIS","**qm stop** [_OPTIONS_] _vmid_"],["DESCRIPTION","**qm stop** immediately stops a running QEMU/KVM virtual machine in Proxmox VE. This performs a hard shutdown, equivalent to pulling the power cord, which may result in data loss if the guest OS is not properly shut down first."],["PARAMETERS","**vmid**\n> The numeric ID of the virtual machine.\n\n**--timeout** _seconds_\n> Wait for the VM to stop for the specified number of seconds.\n\n**--skiplock** _boolean_\n> Skip the lock check (only root can use this option).\n\n**--keepActive** _boolean_\n> Do not deactivate storage volumes after stopping.\n\n**--overrule-shutdown** _boolean_\n> Override a still-running shutdown task to force an immediate stop."],["CAVEATS","This is a forceful stop that does not cleanly shut down the guest operating system. Use **qm shutdown** for a graceful shutdown via ACPI. Data loss may occur if unsaved data exists in the guest."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-start](/man/qm-start)(1), [qm-shutdown](/man/qm-shutdown)(1)"]],"e":["qm","qm-start","qm-shutdown"],"x":"qm stop [vm_id]"},"qtile":{"n":"qtile","t":"Hackable tiling window manager in Python","s":[["TAGLINE","Hackable tiling window manager in Python"],["TLDR","**Start** the window manager\n\n```qtile start```\n\n**Check** configuration file for errors\n\n```qtile check```\n\nShow **resource usage** information\n\n```qtile top --force```\n\n**Run command** on specific group\n\n```qtile run-cmd --group [groupname] --float xterm```\n\n**Restart** the window manager\n\n```qtile cmd-obj --object cmd --function restart```\n\nStart with **Wayland** backend\n\n```qtile start --backend wayland```"],["SYNOPSIS","**qtile** _command_ [_options_]"],["DESCRIPTION","**qtile** is a full-featured, hackable tiling window manager written and configured in Python. It provides a dynamic tiling layout system with extensive customization through Python configuration."],["PARAMETERS","**start**\n> Start the window manager (run from .xsession or similar)\n\n**start --backend** _BACKEND_\n> Start with specified backend (x11 or wayland)\n\n**check**\n> Check configuration file for errors (default: ~/.config/qtile/config.py)\n\n**top**\n> Show current resource usage information\n\n**run-cmd**\n> Launch a program with specific options\n\n**cmd-obj**\n> Execute a command object method\n\n**shell**\n> Open interactive Python shell for qtile control\n\n**migrate**\n> Migrate the user's config.py to a newer Qtile API version\n\n**repl**\n> Connect a Python REPL into the running Qtile instance for live introspection\n\n**--group** _GROUP_\n> Specify the group (workspace) for run-cmd\n\n**--float**\n> Launch window as floating\n\n**--object** _OBJECT_\n> Specify object for cmd-obj\n\n**--function** _FUNCTION_\n> Specify function to call for cmd-obj"],["CAVEATS","Configuration is done in Python at ~/.config/qtile/config.py. Always run qtile check after modifying configuration to verify syntax. Qtile requires X11 or Wayland (via qtile start --backend wayland)."],["HISTORY","**qtile** was created as a highly configurable tiling window manager that uses Python for configuration, allowing complex customizations and scripting capabilities."],["INSTALL","```dnf: sudo dnf install qtile```\n\n```pacman: sudo pacman -S qtile```"],["SEE ALSO","[i3](/man/i3)(1), [bspwm](/man/bspwm)(1), [awesome](/man/awesome)(1)"]],"e":["i3","bspwm","awesome"],"x":"qtile start"},"ranlib":{"n":"ranlib","t":"Generate index for static library archives","s":[["TAGLINE","Generate index for static library archives"],["TLDR","**Generate index for archive**\n\n```ranlib [libfoo.a]```\n\n**Generate index using actual timestamps** (non-deterministic mode)\n\n```ranlib -U [libfoo.a]```\n\n**Generate index in deterministic mode** (reproducible builds)\n\n```ranlib -D [libfoo.a]```\n\n**Process multiple archives**\n\n```ranlib [*.a]```"],["SYNOPSIS","**ranlib** [_options_] _archive_..."],["DESCRIPTION","**ranlib** generates or updates the symbol table index in a static library archive (.a file), enabling the linker to quickly locate symbols without scanning every object file in the archive. Without this index, the linker must process object files in order and may fail to resolve symbols that appear in a later object file than the one referencing them.\n\nWhile modern versions of **ar** automatically create this index when building archives (equivalent to **ar -s**), ranlib remains useful for regenerating indexes on archives that have been modified or transferred between systems."],["PARAMETERS","**-c**\n> Don't warn when the library has to be created (for compatibility).\n\n**-D**\n> Operate in deterministic mode. Use zero for UIDs, GIDs, and timestamps. Enabled by default on some systems.\n\n**-U**\n> Do not operate in deterministic mode; use actual UIDs, GIDs, timestamps, and file modes.\n\n**-t**\n> Update the timestamp of the symbol map of an archive.\n\n**-v**\n> Verbose output.\n\n**-V**\n> Print version information."],["CAVEATS","Mostly redundant with modern ar. Index may need updating after modifications. Some systems use ar -s instead."],["HISTORY","**ranlib** dates back to early Unix, when archive indexes were not automatically maintained. Part of **GNU Binutils**, it remains for compatibility though ar now typically handles indexing automatically."],["INSTALL","```apt: sudo apt install binutils```\n\n```dnf: sudo dnf install binutils```\n\n```pacman: sudo pacman -S binutils```\n\n```apk: sudo apk add binutils```\n\n```zypper: sudo zypper install binutils```\n\n```brew: brew install binutils```\n\n```nix: nix profile install nixpkgs#binutils```"],["SEE ALSO","[ar](/man/ar)(1), [nm](/man/nm)(1), [ld](/man/ld)(1), [objdump](/man/objdump)(1)"]],"e":["ar","nm","ld","objdump"],"x":"ranlib [libfoo.a]"},"robo":{"n":"robo","t":"PHP task runner framework","s":[["TAGLINE","PHP task runner framework"],["TLDR","**List tasks**\n\n```robo```\n\n**Run task**\n\n```robo [task-name]```\n\n**Run with arguments**\n\n```robo [task-name] [--arg=value]```\n\n**Use specific file**\n\n```robo -f [RoboFile.php] [task]```\n\n**Verbose output**\n\n```robo [task] -v```\n\n**Dry run**\n\n```robo [task] --simulate```"],["SYNOPSIS","**robo** [_-f file_] [_options_] [_task_] [_args_]"],["PARAMETERS","**-f** _FILE_\n> RoboFile path.\n\n**-v**\n> Verbose output.\n\n**--simulate**\n> Dry run mode.\n\n**--load-from** _DIR_\n> Load from directory.\n\n**-n**\n> No interaction.\n\n**--help**\n> Show help."],["DESCRIPTION","**robo** is a PHP task runner framework that uses an object-oriented approach to define automation tasks. Tasks are written as PHP class methods in a **RoboFile.php**, providing full access to PHP's language features and ecosystem for build automation, deployment, and development workflows.\n\nThe framework includes built-in tasks for common operations like file manipulation (copy, move, watch), running shell commands, SSH remote execution, and parallel task execution. The **--simulate** flag enables dry-run mode to preview what commands would be executed without actually running them, which is useful for testing complex task sequences.\n\nRobo integrates naturally with Composer-based PHP projects and serves a similar role to Make, Rake, or Gulp for the PHP ecosystem."],["CAVEATS","Requires PHP. RoboFile.php needed. Composer recommended."],["HISTORY","**Robo** was created as a modern PHP task runner, providing a clean API for automation tasks in PHP projects."],["INSTALL","```nix: nix profile install nixpkgs#robo```"],["SEE ALSO","[composer](/man/composer)(1), [make](/man/make)(1), [rake](/man/rake)(1)"]],"e":["composer","make","rake"],"x":"robo"},"rst2html":{"n":"rst2html","t":"Convert reStructuredText to HTML","s":[["TAGLINE","Convert reStructuredText to HTML"],["TLDR","**Convert RST to HTML**\n\n```rst2html [input.rst] [output.html]```\n\n**Convert to stdout**\n\n```rst2html [input.rst]```\n\n**With stylesheet**\n\n```rst2html --stylesheet=[style.css] [input.rst] [output.html]```\n\n**Standalone document**\n\n```rst2html --embed-stylesheet [input.rst] [output.html]```"],["SYNOPSIS","**rst2html** [_options_] [_source_] [_destination_]"],["PARAMETERS","**--stylesheet** _file_\n> CSS stylesheet path.\n\n**--embed-stylesheet**\n> Embed CSS in HTML.\n\n**--template** _file_\n> Document template.\n\n**--no-doc-title**\n> No document title.\n\n**--strict**\n> Strict mode.\n\n**--quiet**, **-q**\n> Suppress warnings."],["DESCRIPTION","**rst2html** converts reStructuredText documents to HTML. It's part of Docutils, providing the reference implementation for RST processing."],["RST SYNTAX","```rst\nTitle\n=====\n\nSubtitle\n--------\n\nParagraph text with **bold** and *italic*.\n\n* List item 1\n* List item 2\n\n::\n\n    Code block\n```"],["CAVEATS","Part of Docutils package (python-docutils). Multiple variants: rst2html, rst2html4, rst2html5."],["HISTORY","rst2html is part of **Docutils** by **David Goodger** and contributors, implementing reStructuredText processing."],["INSTALL","```apk: sudo apk add py3-docutils```"],["SEE ALSO","[sphinx-build](/man/sphinx-build)(1), [pandoc](/man/pandoc)(1)"]],"e":["sphinx-build","pandoc"],"x":"rst2html [input.rst] [output.html]"},"rum":{"n":"rum","t":"Simple tool for managing running background jobs","s":[["TAGLINE","Simple tool for managing running background jobs"],["TLDR","**Start a tracked background job**\n\n```rum [my_long_script.sh]```\n\n**List all tracked runs**\n\n```rum --list```\n\n**Show the output log of a run**\n\n```rum --log [id]```\n\n**Follow the live output of a running job**\n\n```rum --log --follow [id]```\n\n**Remove a tracked run by ID**\n\n```rum --remove [id]```"],["SYNOPSIS","**rum** [_options_] [_command_...]"],["PARAMETERS","**--list**\n> List all tracked background runs with their status.\n\n**--log** _ID_\n> Show the output log of a tracked run.\n\n**--follow**\n> Used with --log to follow output in real time (similar to tail -f).\n\n**--remove** _ID_\n> Remove a tracked run by its ID.\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**rum** lets you run and manage long-running background processes. You prefix any command with **rum** to start tracking it. Each run gets a unique ID for management. Output is captured and can be viewed later with --log, making it lighter than screen or tmux for simple job tracking, especially useful over SSH sessions that may disconnect."],["HISTORY","**rum** was created by **Daniel Csillag** (dccsillag) and is written in **Rust**."],["INSTALL","```aur: yay -S rum```"],["SEE ALSO","[screen](/man/screen)(1), [tmux](/man/tmux)(1), [nohup](/man/nohup)(1), [disown](/man/disown)(1)"]],"e":["screen","tmux","nohup","disown"],"x":"rum [my_long_script.sh]"},"secrethub":{"n":"secrethub","t":"Encrypted secret management service CLI","s":[["TAGLINE","Encrypted secret management service CLI"],["TLDR","**Write secret**\n\n```secrethub write [org/repo/secret]```\n\n**Read secret**\n\n```secrethub read [org/repo/secret]```\n\n**List secrets**\n\n```secrethub ls [org/repo]```\n\n**Run with secrets**\n\n```secrethub run -- [command]```\n\n**Initialize repo**\n\n```secrethub repo init [org/repo]```\n\n**Inject secrets to file**\n\n```secrethub inject -i [template.env] -o [.env]```"],["SYNOPSIS","**secrethub** _command_ [_options_] [_args_]"],["PARAMETERS","**write** _path_\n> Store a secret at the given path (reads value from stdin or prompt).\n\n**read** _path_\n> Retrieve and print a secret value.\n\n**ls** _path_\n> List secrets, directories, or repositories at a path.\n\n**run** **--** _command_\n> Execute a command with secrets injected as environment variables.\n\n**inject** **-i** _template_ **-o** _output_\n> Render a template, substituting secret references with actual values.\n\n**repo init** _path_\n> Initialize a new repository.\n\n**generate** _path_\n> Generate and store a random secret at the given path.\n\n**--clip**\n> Copy the secret to the clipboard instead of printing it (with **read**).\n\n**--env-file** _file_\n> Load environment variable references from a file (with **run**)."],["DESCRIPTION","**secrethub** was a secret management service CLI that provided secure credential storage with end-to-end encryption and a zero-knowledge architecture. Secrets were organized in repositories using a path-based hierarchy (org/repo/secret) and encrypted client-side before transmission.\n\nThe **run** command injected secrets into environment variables for a child process, keeping credentials out of config files and shell history. Template injection via **inject** generated configuration files with secrets substituted from references, enabling secure deployment workflows.\n\nBuilt-in access control allowed team sharing of secrets with granular permissions. The service was acquired by 1Password and has been discontinued; users should migrate to alternatives like 1Password, HashiCorp Vault, or similar tools."],["CAVEATS","Service discontinued. Use alternatives. Account required."],["HISTORY","**SecretHub** was a secret management service founded in the Netherlands. It was **acquired by 1Password in April 2021** and subsequently **discontinued**, with service end-of-life on **January 1, 2022**. Functionality was absorbed into 1Password Secrets Automation and the 1Password CLI (**op**). Existing users were provided migration guides for Python, Go, JavaScript, Terraform, and the Kubernetes operator."],["SEE ALSO","[op](/man/op)(1), [vault](/man/vault)(1), [pass](/man/pass)(1)"]],"e":["op","vault","pass"],"x":"secrethub write [org/repo/secret]"},"sfdk-emulator-device-model-show":{"n":"sfdk-emulator-device-model-show","t":"Show Sailfish emulator device model details","s":[["TAGLINE","Show Sailfish emulator device model details"],["TLDR","**Show device model details**\n\n```sfdk emulator device-model show [model_name]```"],["SYNOPSIS","**sfdk emulator device-model show** _model_"],["DESCRIPTION","**sfdk emulator device-model show** displays detailed information about a specific Sailfish OS emulator device model. Part of Sailfish SDK."],["SEE ALSO","[sfdk-emulator](/man/sfdk-emulator)(1), [sfdk-emulator-device-model-list](/man/sfdk-emulator-device-model-list)(1)"]],"e":["sfdk-emulator","sfdk-emulator-device-model-list"],"x":"sfdk emulator device-model show [model_name]"},"slimrb":{"n":"slimrb","t":"Compile Slim templates to HTML","s":[["TAGLINE","Compile Slim templates to HTML"],["TLDR","**Compile Slim to HTML**\n\n```slimrb [template.slim]```\n\n**Compile to file**\n\n```slimrb [template.slim] [output.html]```\n\n**Pretty output**\n\n```slimrb -p [template.slim]```\n\n**With Rails support**\n\n```slimrb -r [template.slim]```\n\n**Compile from stdin**\n\n```echo \"[h1 Hello]\" | slimrb```"],["SYNOPSIS","**slimrb** [_-p_] [_-r_] [_options_] [_input_] [_output_]"],["PARAMETERS","**-p**, **--pretty**\n> Pretty print HTML.\n\n**-r**, **--rails**\n> Rails mode.\n\n**-e**, **--erb**\n> Convert from ERB.\n\n**-l**, **--logic-less**\n> Logic-less mode.\n\n**-t**, **--translator**\n> Translation mode."],["DESCRIPTION","**slimrb** is the command-line compiler for the Slim template language, a lightweight Ruby templating engine that reduces HTML markup to its essential parts. It reads Slim files and renders them to HTML, ERB, or raw Ruby code for inspection and debugging.\n\nSlim uses indentation-based nesting and eliminates closing tags, producing templates that are significantly shorter than equivalent HTML or ERB. The compiler supports pretty-printed output for debugging, Rails-compatible code generation, and conversion to ERB format for use in contexts that don't support Slim directly.\n\nThe tool integrates with Ruby's Tilt template interface and works with Rails, Sinatra, and other Rack-based frameworks. Plugins can extend the syntax with logic-less templates, translation support, and other features."],["CAVEATS","Ruby required. Slim gem needed. Ruby-specific templating."],["HISTORY","**slimrb** is the CLI compiler for **Slim**, a fast and lightweight Ruby templating engine."],["SEE ALSO","[erb](/man/erb)(1), [haml](/man/haml)(1), [ruby](/man/ruby)(1)"]],"e":["erb","haml","ruby"],"x":"slimrb [template.slim]"},"spottopgm":{"n":"spottopgm","t":"Convert SPOT satellite images to PGM","s":[["TAGLINE","Convert SPOT satellite images to PGM"],["TLDR","**Convert the default band** of a SPOT image to PGM\n\n```spottopgm [input.spot] > [output.pgm]```\n\n**Select a specific band** (1, 2 or 3)\n\n```spottopgm -2 [input.spot] > [output.pgm]```\n\n**Crop to a rectangular region** of columns and lines\n\n```spottopgm [firstcol] [firstline] [lastcol] [lastline] [input.spot] > [output.pgm]```"],["SYNOPSIS","**spottopgm** [**-1**|**-2**|**-3**] [_firstcol_ _firstline_ _lastcol_ _lastline_] [_file_]"],["PARAMETERS","**-1**, **-2**, **-3**\n> Select which of the three spectral bands to extract (infrared, visible, and ultraviolet, though the Netpbm author acknowledges the exact mapping is uncertain). Only one band at a time is produced; combined multispectral PPM output is not supported.\n\n_firstcol_ _firstline_ _lastcol_ _lastline_\n> When supplied, crop the output to the rectangle bounded by the given column/line indices."],["DESCRIPTION","**spottopgm** converts images in the SPOT satellite format to PGM (Portable Graymap) format. The SPOT format is used by imagery from the SPOT (Satellite Pour l'Observation de la Terre) Earth-observation satellite system, which captures high-resolution imagery for cartography, agriculture, and environmental monitoring. This tool is part of the Netpbm image-processing toolkit, which provides converters between many image formats."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[fitstopnm](/man/fitstopnm)(1), [pgmnorm](/man/pgmnorm)(1)"]],"e":["fitstopnm","pgmnorm"],"x":"spottopgm [input.spot] > [output.pgm]"},"starlit":{"n":"starlit","t":"Minimal and customizable weather CLI","s":[["TAGLINE","Minimal and customizable weather CLI"],["TLDR","**Display weather for a specific city**\n\n```starlit [seattle]```\n\n**Start interactive weather mode**\n\n```starlit --interactive```\n\n**Open the configuration file for editing**\n\n```starlit --edit```"],["SYNOPSIS","**starlit** [_city_] [_options_]"],["DESCRIPTION","**starlit** is a minimal and customizable weather CLI powered by the OpenWeatherMap API and styled with the Rich library and terminal text effects. It supports interactive mode, customizable color themes, and multi-language output."],["CAVEATS","Requires an OpenWeatherMap API key."],["HISTORY","**starlit** was created by **Ashley Lee** (ashuhlee) and is written in **Python**."],["SEE ALSO","[stormy](/man/stormy)(1), [rustormy](/man/rustormy)(1), [curl](/man/curl)(1)"]],"e":["stormy","rustormy","curl"],"x":"starlit [seattle]"},"sync":{"n":"sync","t":"Flush filesystem buffers to disk","s":[["TAGLINE","Flush filesystem buffers to disk"],["TLDR","**Flush all file system buffers** to disk\n\n```sync```\n\n**Synchronize a specific file**\n\n```sync [path/to/file]```\n\n**Synchronize data only** (skip metadata)\n\n```sync -d [path/to/file]```\n\n**Synchronize file systems** containing specific files\n\n```sync -f [path/to/file]```"],["SYNOPSIS","**sync** [_OPTION_] [_FILE_]..."],["PARAMETERS","**-d**, **--data**\n> Sync only file data, skip unnecessary metadata\n\n**-f**, **--file-system**\n> Sync the file systems containing the specified files\n\n**--help**\n> Display help and exit\n\n**--version**\n> Output version information and exit"],["DESCRIPTION","**sync** flushes file system buffers, forcing changed blocks (dirty pages) to be written to disk. This ensures data currently held in memory is persisted to storage.\n\nLinux buffers write operations in memory for performance, bundling and reordering disk I/O. While efficient, this means an unexpected crash or power loss could result in data loss. sync forces the kernel to write buffered data to disk, ensuring integrity.\n\nWhen called without arguments, sync flushes all buffers system-wide. With FILE arguments, only those files are synchronized."],["CAVEATS","sync guarantees data reaches the device driver but does not guarantee physical writes to the disk platter or flash memory have completed. For SSDs and some drives with write caching, data may still be in the drive's internal cache. Modern utilities like **umount**, **reboot**, and **shutdown** invoke sync automatically."],["HISTORY","sync has been part of Unix since the early days, ensuring filesystem consistency before shutdown or media removal. Written by Jim Meyering for GNU coreutils. The underlying sync(2) system call has existed since the original Unix."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[umount](/man/umount)(8), [shutdown](/man/shutdown)(8)"]],"e":["umount","shutdown"],"x":"sync"},"tasktimer":{"n":"tasktimer","t":"Dead simple TUI task timer","s":[["TAGLINE","Dead simple TUI task timer"],["TLDR","**Launch the task timer**\n\n```tt```"],["SYNOPSIS","**tt**"],["DESCRIPTION","**tasktimer** is a minimal TUI task timer. You type a task description and press Enter to start timing; press Enter again to stop, or type a new task to switch. All task durations are summed in the header. Press Ctrl+C to stop and display a summary."],["HISTORY","**tasktimer** was created by **Carlos Alexandro Becker** (caarlos0) and is written in **Go**."],["INSTALL","```nix: nix profile install nixpkgs#tasktimer```"],["SEE ALSO","[watson](/man/watson)(1), [pomo](/man/pomo)(1)"]],"e":["watson","pomo"],"x":"tt"},"tcptraceroute":{"n":"tcptraceroute","t":"Traceroute using TCP packets","s":[["TAGLINE","Traceroute using TCP packets"],["TLDR","**Trace** route to a host\n\n```tcptraceroute host```\n\nTrace to specific **destination port**\n\n```tcptraceroute host 80```\n\nSpecify **source port** and address\n\n```tcptraceroute host -p 12345 -s 192.168.1.10```\n\nSet **first and maximum TTL**\n\n```tcptraceroute host -f 1 -m 30```\n\nSet **wait time** and queries per hop\n\n```tcptraceroute host -w 3 -q 3```\n\nSpecify **network interface**\n\n```tcptraceroute host -i eth0```\n\nUse **numeric** output only\n\n```tcptraceroute -n host```"],["SYNOPSIS","**tcptraceroute** [_options_] _host_ [_destination_port_] [_packet_length_]"],["DESCRIPTION","**tcptraceroute** is a traceroute implementation using TCP packets. It can trace through firewalls that block traditional ICMP or UDP traceroute packets by using TCP SYN packets to a specified port."],["PARAMETERS","**-p PORT**\n> Set local source port\n\n**-s ADDRESS**\n> Set source IP address\n\n**-i INTERFACE**\n> Specify network interface to use\n\n**-f TTL**\n> Set the initial (first) TTL value\n\n**-m TTL**\n> Set the maximum TTL value (default: 30)\n\n**-w SECONDS**\n> Set the timeout for each probe (default: 3)\n\n**-q QUERIES**\n> Set number of queries per hop (default: 3)\n\n**-n**\n> Print numeric addresses only (no DNS resolution)\n\n**-N**\n> Enable lookup of hostnames\n\n**-S**\n> Set SYN flag on outgoing packets\n\n**-A**\n> Set ACK flag on outgoing packets\n\n**-E**\n> Set ECN flag on outgoing packets"],["CAVEATS","Requires root privileges or appropriate capabilities to send raw packets. The destination port defaults to 80 (HTTP). Some firewalls may still block TCP probes depending on their configuration."],["HISTORY","**tcptraceroute** was created as an alternative to traditional traceroute that could bypass firewalls blocking ICMP and UDP packets by using TCP SYN packets."],["INSTALL","```apk: sudo apk add tcptraceroute```\n\n```brew: brew install tcptraceroute```\n\n```nix: nix profile install nixpkgs#tcptraceroute```"],["SEE ALSO","[traceroute](/man/traceroute)(8), [mtr](/man/mtr)(8), [tracepath](/man/tracepath)(8)"]],"e":["traceroute","mtr","tracepath"],"x":"tcptraceroute host"},"textlint":{"n":"textlint","t":"Pluggable natural language linter","s":[["TAGLINE","Pluggable natural language linter"],["TLDR","**Lint markdown files**\n\n```textlint \"[docs/**/*.md]\"```\n\n**Lint a directory**\n\n```textlint [docs/]```\n\n**Auto-fix problems**\n\n```textlint --fix [README.md]```\n\n**Dry run to preview fixes**\n\n```textlint --fix --dry-run --format diff [README.md]```\n\n**Use specific config file**\n\n```textlint -c [.textlintrc.json] [file.md]```\n\n**Lint stdin**\n\n```cat [README.md] | textlint --stdin --stdin-filename \"README.md\"```\n\n**Output in JSON format**\n\n```textlint --format json [docs/]```\n\n**Initialize configuration**\n\n```textlint --init```"],["SYNOPSIS","**textlint** [_options_] [_file_|_dir_|_glob_...]"],["PARAMETERS","**-c**, **--config** _file_\n> Configuration file path.\n\n**--ignore-path** _file_\n> File with patterns to ignore (default: .textlintignore).\n\n**--init**\n> Create configuration file if not exists.\n\n**--fix**\n> Automatically fix problems.\n\n**--dry-run**\n> Preview fixes without writing.\n\n**--debug**\n> Output debugging information.\n\n**--print-config**\n> Print resolved configuration.\n\n**--stdin**\n> Lint text from stdin.\n\n**--stdin-filename** _name_\n> Filename for stdin input.\n\n**-f**, **--format** _format_\n> Output format: stylish, json, checkstyle, compact, junit, tap, unix.\n\n**-o**, **--output-file** _file_\n> Write report to file.\n\n**--no-color**\n> Disable colored output.\n\n**--quiet**\n> Report errors only.\n\n**--no-textlintrc**\n> Disable .textlintrc loading.\n\n**--rule** _rule_\n> Rule package to use.\n\n**--preset** _preset_\n> Preset package to load.\n\n**--plugin** _plugin_\n> Plugin package to use.\n\n**--cache**\n> Only check changed files.\n\n**-v**, **--version**\n> Display version.\n\n**-h**, **--help**\n> Display help."],["DESCRIPTION","**textlint** is a pluggable linter for natural language text. It checks prose for style issues, grammar problems, and writing inconsistencies using configurable rules.\n\nRules are installed as npm packages and configured in .textlintrc. Available rules cover spelling, terminology, sentence length, redundant phrases, and writing style guidelines. Presets bundle commonly used rule sets.\n\nThe tool supports Markdown, plain text, and other formats through plugins. It integrates with editors and CI/CD pipelines for automated text quality checks.\n\nAuto-fix mode can automatically correct many issues. Use dry-run with diff format to preview changes before applying.\n\nInstall via npm: `npm install textlint`"],["CAVEATS","Requires Node.js and npm. Rules must be installed separately. Configuration complexity increases with multiple rules. Some rules may conflict with each other."],["HISTORY","**textlint** was created by azu as an ESLint-inspired linter for prose. It applies the pluggable architecture pattern to natural language checking, allowing the community to develop specialized rules for different writing styles and languages."],["INSTALL","```nix: nix profile install nixpkgs#textlint```"],["SEE ALSO","[eslint](/man/eslint)(1), [vale](/man/vale)(1), [markdownlint](/man/markdownlint)(1), [proselint](/man/proselint)(1)"]],"e":["eslint","vale","markdownlint","proselint"],"x":"textlint \"[docs/**/*.md]\""},"tlmgr-restore":{"n":"tlmgr-restore","t":"Restore TeX Live packages from backups","s":[["TAGLINE","Restore TeX Live packages from backups"],["TLDR","**List available backups**\n\n```tlmgr restore```\n\n**Restore package**\n\n```tlmgr restore [package] [revision]```\n\n**Restore all from backup**\n\n```tlmgr restore --all```"],["SYNOPSIS","**tlmgr restore** [_options_] [_package_ [_revision_]]"],["PARAMETERS","**--all**\n> Restore all packages.\n\n**--backupdir** _dir_\n> Backup source directory.\n\n**--dry-run**\n> Show what would be restored.\n\n_revision_\n> Specific revision to restore."],["DESCRIPTION","**tlmgr restore** restores TeX Live packages from backups. Reverts to previous versions after problematic updates. Part of TeX Live Manager."],["SEE ALSO","[tlmgr-backup](/man/tlmgr-backup)(1), [tlmgr](/man/tlmgr)(1)"]],"e":["tlmgr-backup","tlmgr"],"x":"tlmgr restore"},"transcode":{"n":"transcode","t":"Linux video stream processing tool","s":[["TAGLINE","Linux video stream processing tool"],["TLDR","**Transcode a video file with a specific export module**\n\n```transcode -i [input.avi] -y [xvid] -o [output.avi]```\n\n**Transcode with explicit import and export modules**\n\n```transcode -i [input.vob] -x [vob] -y [xvid,lame] -o [output.avi]```\n\n**Extract audio to a separate file**\n\n```transcode -i [video.avi] -y null,tcaud -m [audio.mp3]```\n\n**Resize video using filtered zoom**\n\n```transcode -i [input.avi] -Z [640x480] -y [xvid] -o [output.avi]```\n\n**Set video bitrate and encode a specific frame range**\n\n```transcode -i [input.avi] -w [1500] -c [0-5000] -y [xvid] -o [output.avi]```\n\n**Crop 16 pixels from top and bottom of the video**\n\n```transcode -i [input.avi] -j [16,0] -y [xvid] -o [output.avi]```"],["SYNOPSIS","**transcode** **-i** _input_ [**-o** _output_] [**-y** _export_module_] [_options_]"],["PARAMETERS","**-i** _FILE_\n> Input file (required). The only mandatory option.\n\n**-o** _FILE_\n> Output file.\n\n**-x** _MODULE_\n> Import module (e.g., vob, dvd, raw). Determines how input is decoded.\n\n**-y** _MODULE_[,_AMODULE_]\n> Export module for video, optionally with a separate audio export module (e.g., xvid,lame).\n\n**-Z** _WxH_[,_MODE_]\n> Resize video using zoom. Append \"fast\" for fast zoom mode (e.g., 640x480,fast).\n\n**-j** _TOP_,_LEFT_[,_BOTTOM_,_RIGHT_]\n> Crop pixels from the image borders. Two values are expanded symmetrically.\n\n**-w** _BITRATE_\n> Video encoder bitrate in kbps.\n\n**-b** _BITRATE_\n> Audio encoder bitrate in kbps.\n\n**-m** _FILE_\n> Write audio output to a separate file.\n\n**-p** _FILE_\n> Read audio from an external file instead of the main input.\n\n**-c** _RANGE_\n> Encode only the specified frame range (e.g., 0-5000 or 0:05:00-0:10:00).\n\n**-s** _FACTOR_\n> Adjust audio volume by the given factor (e.g., 4.47 to increase).\n\n**-R** _N_[,_FILE_]\n> Multi-pass encoding. Use -R 1 for first pass and -R 2 for second pass.\n\n**-T** _TITLE_\n> Select a DVD title to process.\n\n**--multi_input**\n> Enable batch processing of multiple input files."],["DESCRIPTION","**transcode** is a Linux video stream processing tool for converting between video and audio formats. It uses a modular architecture with separate import and export modules for different codecs and containers.\n\nThe tool supports filtering operations including resizing, cropping, deinterlacing, and volume adjustment. It can extract audio tracks separately and perform multi-pass encoding for better quality. Transcode has been largely superseded by **FFmpeg**, which offers broader format support and more active development."],["CAVEATS","Legacy tool no longer actively developed. FFmpeg is generally preferred for new projects. The modular architecture means available codecs depend on which modules are installed. The only mandatory option is **-i**; all others are discretionary."],["INSTALL","```aur: yay -S transcode```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [mencoder](/man/mencoder)(1), [mplayer](/man/mplayer)(1)"]],"e":["ffmpeg","mencoder","mplayer"],"x":"transcode -i [input.avi] -y [xvid] -o [output.avi]"},"udisksctl":{"n":"udisksctl","t":"Command-line udisks2 storage device manager","s":[["TAGLINE","Command-line udisks2 storage device manager"],["TLDR","Show disk **status** overview\n\n```udisksctl status```\n\nShow **detailed info** about a device\n\n```udisksctl info -b /dev/sdX```\n\n**Mount** a device partition\n\n```udisksctl mount -b /dev/sdX1```\n\n**Unmount** a device partition\n\n```udisksctl unmount -b /dev/sdX1```\n\n**Power off** a device safely\n\n```udisksctl power-off -b /dev/sdX```\n\n**Monitor** daemon events\n\n```udisksctl monitor```\n\n**Unlock** an encrypted device\n\n```udisksctl unlock -b /dev/sdX1```\n\n**Lock** an encrypted device\n\n```udisksctl lock -b /dev/sdX1```"],["SYNOPSIS","**udisksctl** _COMMAND_ [_OPTIONS_]"],["DESCRIPTION","**udisksctl** is a command-line interface for interacting with the udisksd daemon. It allows unprivileged users to query and manipulate storage devices, with authorization managed through polkit. Operations include mounting, unmounting, and powering off devices."],["PARAMETERS","**status**\n> Show high-level information about disk drives and block devices\n\n**info**\n> Show detailed information about an object, device, or drive\n\n**mount**\n> Mount a device; filesystem appears in /run/media hierarchy\n\n**unmount**\n> Unmount a mounted device\n\n**unlock**\n> Unlock an encrypted device\n\n**lock**\n> Lock an unlocked encrypted device\n\n**loop-setup**\n> Set up a loop device\n\n**loop-delete**\n> Delete a loop device\n\n**power-off**\n> Safely power off a drive for removal\n\n**smart-simulate**\n> Simulate disk failure scenarios using SMART data\n\n**monitor**\n> Monitor the daemon for events\n\n**dump**\n> Show current state of the daemon\n\n**-b, --block-device DEVICE**\n> Specify block device (e.g., /dev/sda)\n\n**--object-path PATH**\n> Specify UDisks object path\n\n**--filesystem-type TYPE**\n> Specify filesystem type for mount\n\n**--options OPTIONS**\n> Specify mount options\n\n**--force**\n> Force unmount (lazy unmount)"],["CAVEATS","The command-line interface is not intended for scripts; syntax may change without backward compatibility. No superuser privileges are required as authorization is handled through polkit."],["HISTORY","**udisksctl** is part of **udisks2**, the successor to DeviceKit-disks, providing modern disk management facilities for Linux desktop environments."],["INSTALL","```apt: sudo apt install udisks2```\n\n```dnf: sudo dnf install udisks2```\n\n```pacman: sudo pacman -S udisks2```\n\n```apk: sudo apk add udisks2```\n\n```zypper: sudo zypper install udisks2```"],["SEE ALSO","[udisksd](/man/udisksd)(8), [mount](/man/mount)(8), [umount](/man/umount)(8), [polkit](/man/polkit)(8)"]],"e":["udisksd","mount","umount","polkit"],"x":"udisksctl status"},"upt":{"n":"upt","t":"Universal cross-platform package manager wrapper","s":[["TAGLINE","Universal cross-platform package manager wrapper"],["TLDR","**Install package**\n\n```upt install [package]```\n\n**Remove package**\n\n```upt remove [package]```\n\n**Update packages**\n\n```upt upgrade```\n\n**Search package**\n\n```upt search [query]```\n\n**List installed**\n\n```upt list```\n\n**Show info**\n\n```upt info [package]```"],["SYNOPSIS","**upt** _command_ [_options_] [_package_]"],["PARAMETERS","**install**\n> Install package.\n\n**remove**\n> Remove package.\n\n**upgrade**\n> Update all.\n\n**search**\n> Find packages.\n\n**list**\n> Show installed.\n\n**info**\n> Package details."],["DESCRIPTION","**upt** (Universal Package Tool) provides a single, consistent command-line interface that wraps the native package manager on any system. It automatically detects which package manager is available and translates upt commands into the appropriate native commands for apt, pacman, brew, and others.\n\nThis allows users and scripts to use the same commands across different Linux distributions and macOS without needing to remember the syntax differences between package managers. Commands like **install**, **remove**, **search**, and **upgrade** work identically regardless of the underlying system.\n\nThe tool delegates all actual package operations to the native package manager, so it does not introduce any compatibility layers or alternative package formats. Root privileges may be required depending on the underlying package manager's requirements."],["CAVEATS","Wrapper tool. Underlying PM varies. May need sudo."],["HISTORY","**upt** (Universal Package Tool) provides a unified interface to various system package managers."],["SEE ALSO","[apt](/man/apt)(1), [pacman](/man/pacman)(1), [brew](/man/brew)(1)"]],"e":["apt","pacman","brew"],"x":"upt install [package]"},"vcsh":{"n":"vcsh","t":"Manage dotfiles with multiple Git repos","s":[["TAGLINE","Manage dotfiles with multiple Git repos"],["TLDR","**Initialize repo**\n\n```vcsh init [reponame]```\n\n**Clone repo**\n\n```vcsh clone [url] [reponame]```\n\n**Run git command**\n\n```vcsh [reponame] [git-command]```\n\n**List repos**\n\n```vcsh list```\n\n**Enter repo**\n\n```vcsh enter [reponame]```\n\n**Status all**\n\n```vcsh status```\n\n**List tracked files**\n\n```vcsh list-tracked [reponame]```\n\n**Pull all repos**\n\n```vcsh pull```\n\n**Add and commit** a file in a repo\n\n```vcsh [reponame] add [~/.bashrc] && vcsh [reponame] commit -m \"[message]\"```\n\n**Push all repos**\n\n```vcsh push```\n\n**Rename a repo**\n\n```vcsh rename [oldname] [newname]```"],["SYNOPSIS","**vcsh** [_init|clone|list|enter|run|pull|push_] [_reponame_] [_args_]"],["PARAMETERS","**init** _reponame_\n> Initialize an empty repo.\n\n**clone** _url_ [_reponame_]\n> Clone from URL.\n\n**list**\n> List all repos.\n\n**list-tracked** [_reponame_]\n> List tracked files.\n\n**list-untracked** [**-a**] [**-r**]\n> List untracked files. -a shows all, -r is recursive.\n\n**enter** _reponame_\n> Spawn subshell with $GIT_DIR set.\n\n**run** _reponame_ _command_\n> Run command with $GIT_DIR and $GIT_WORK_TREE set.\n\n**pull**\n> Pull from all repos.\n\n**push**\n> Push all repos.\n\n**rename** _old_ _new_\n> Rename a repository.\n\n**status** [_reponame_]\n> Show status of all or one repo.\n\n**write-gitignore** _reponame_\n> Write .gitignore.d/reponame via git ls-files.\n\n**delete** _reponame_\n> Delete a repository."],["DESCRIPTION","**vcsh** manages configuration files (dotfiles) using multiple Git repositories that all share the same working directory ($HOME). Each repository tracks a separate set of files, allowing modular organization of shell configs, editor settings, and other dotfiles.\n\nThe tool uses bare Git repositories to avoid conflicts between repos that coexist in the same directory. The **enter** command opens a subshell scoped to a specific repository, while running git commands directly against a named repo is also supported."],["ENVIRONMENT","**VCSH_REPO_D**\n> Directory where bare repositories are stored (default: $XDG_CONFIG_HOME/vcsh/repo.d)."],["CAVEATS","Requires basic Git knowledge. Bare repositories can be confusing -- use `vcsh enter` to inspect repo state. Files tracked by multiple repos will cause conflicts."],["HISTORY","**vcsh** (version control system for $HOME) was created by **Richard Hartmann** for managing multiple Git repositories overlaid on $HOME."],["INSTALL","```dnf: sudo dnf install vcsh```\n\n```pacman: sudo pacman -S vcsh```\n\n```zypper: sudo zypper install vcsh```\n\n```brew: brew install vcsh```\n\n```nix: nix profile install nixpkgs#vcsh```"],["SEE ALSO","[git](/man/git)(1), [mr](/man/mr)(1), [stow](/man/stow)(1), [yadm](/man/yadm)(1), [chezmoi](/man/chezmoi)(1)"]],"e":["git","mr","stow","yadm","chezmoi"],"x":"vcsh init [reponame]"},"virt-manager":{"n":"virt-manager","t":"Graphical virtual machine manager","s":[["TAGLINE","Graphical virtual machine manager"],["TLDR","Launch **GUI**\n\n```virt-manager```\n\n**Connect** to hypervisor\n\n```virt-manager --connect [hypervisor_uri]```\n\nRun in **foreground**\n\n```virt-manager --no-fork```\n\n**Debug** mode\n\n```virt-manager --debug```\n\nOpen **New VM** wizard\n\n```virt-manager --show-domain-creator```\n\nShow VM **details**\n\n```virt-manager --show-domain-editor [name|id|uuid]```\n\nShow VM **performance**\n\n```virt-manager --show-domain-performance [name|id|uuid]```\n\nShow **connection** details\n\n```virt-manager --show-host-summary```"],["SYNOPSIS","**virt-manager** [_OPTIONS_]"],["PARAMETERS","**--connect** _URI_\n> Connect to specified hypervisor\n\n**--no-fork**\n> Don't fork into background\n\n**--debug**\n> Print debug output\n\n**--show-domain-creator**\n> Open new VM wizard\n\n**--show-domain-editor** _VM_\n> Show VM details window\n\n**--show-domain-performance** _VM_\n> Show VM performance window\n\n**--show-host-summary**\n> Show connection details\n\n**--show-domain-console** _VM_\n> Open the console window for a domain\n\n**--spice-disable-auto-usbredir**\n> Disable automatic USB redirection via SPICE\n\n**--version**\n> Show program version and exit"],["DESCRIPTION","**virt-manager** is a desktop user interface for managing KVM and Xen virtual machines and LXC containers. It provides a graphical interface for creating, configuring, and monitoring virtual machines.\n\nThe tool connects to libvirt and can manage both local and remote hypervisors."],["CAVEATS","Requires libvirt and a hypervisor (KVM, Xen, etc.). Remote management requires proper libvirt configuration. Hardware virtualization support recommended."],["INSTALL","```dnf: sudo dnf install virt-manager```\n\n```pacman: sudo pacman -S virt-manager```\n\n```apk: sudo apk add virt-manager```\n\n```zypper: sudo zypper install virt-manager```\n\n```brew: brew install virt-manager```\n\n```nix: nix profile install nixpkgs#virt-manager```"],["SEE ALSO","[virsh](/man/virsh)(1), [virt-install](/man/virt-install)(1), [virt-clone](/man/virt-clone)(1), [virt-viewer](/man/virt-viewer)(1), [qemu](/man/qemu)(1), [kvm](/man/kvm)(1)"]],"e":["virsh","virt-install","virt-clone","virt-viewer","qemu","kvm"],"x":"virt-manager"},"waymore":{"n":"waymore","t":"Find archived URLs from multiple sources","s":[["TAGLINE","Find archived URLs from multiple sources"],["TLDR","**Find URLs for a domain**\n\n```waymore -i [example.com]```\n\n**Retrieve URLs only** (skip response downloads)\n\n```waymore -i [example.com] -mode U```\n\n**Retrieve both URLs and archived responses**\n\n```waymore -i [example.com] -mode B```\n\n**Output URLs to a specific file**\n\n```waymore -i [example.com] -oU [urls.txt]```\n\n**Exclude subdomains**\n\n```waymore -i [example.com] -n```\n\n**Check estimated time and request count** before running\n\n```waymore -i [example.com] -co```\n\n**Limit requests per source**\n\n```waymore -i [example.com] -lr [100]```"],["SYNOPSIS","**waymore** [_-i domain_] [_-mode mode_] [_-oU file_] [_options_]"],["PARAMETERS","**-i** _DOMAIN_\n> Input domain or file of domains.\n\n**-mode** _MODE_\n> Mode: U (URLs only), R (Responses only), B (Both). Default: U.\n\n**-n**, **--no-subs**\n> Do not include subdomains of the target domain.\n\n**-oU** _FILE_\n> Output file for discovered URLs.\n\n**-oR** _DIR_\n> Output directory for downloaded responses.\n\n**-xcc**\n> Exclude Common Crawl results.\n\n**-xav**\n> Exclude Alien Vault OTX results.\n\n**-lr** _NUM_\n> Limit requests per source (0 = no limit).\n\n**-co**, **--check-only**\n> Check estimated request count and time without running.\n\n**-ow**, **--output-overwrite**\n> Overwrite output file instead of appending.\n\n**-f** _REGEX_\n> Filter URLs with keyword regex.\n\n**-v**, **--verbose**\n> Verbose output."],["DESCRIPTION","**waymore** is a reconnaissance tool that discovers URLs and archived responses for a domain by querying multiple web archive sources including the Wayback Machine, Common Crawl, Alien Vault OTX, URLScan, VirusTotal, GhostArchive, and Intelligence X. It provides broader coverage than tools that query only a single archive.\n\nThe tool operates in three modes: URL mode (U) extracts known URLs for the target domain from archive indices, response mode (R) retrieves the actual archived page contents, and both mode (B) does both. Results can be filtered to exclude specific sources or match keyword patterns.\n\nMulti-threaded processing speeds up large queries, and output can be saved to files for further analysis. The tool is commonly used in security research and bug bounty programs for discovering forgotten endpoints, old API paths, and removed content."],["CAVEATS","Rate limited by archive sources. Large domains can take significant time; use -co to estimate first. Requires Python 3. Configuration via config.yml for API keys and defaults."],["HISTORY","**waymore** was created by **xnl-h4ck3r** for web reconnaissance, searching multiple archive sources for URLs."],["INSTALL","```nix: nix profile install nixpkgs#waymore```"],["SEE ALSO","[waybackurls](/man/waybackurls)(1), [gau](/man/gau)(1), [hakrawler](/man/hakrawler)(1)"]],"e":["waybackurls","gau","hakrawler"],"x":"waymore -i [example.com]"},"xpmtoppm":{"n":"xpmtoppm","t":"Convert XPM pixmaps to PPM format","s":[["TAGLINE","Convert XPM pixmaps to PPM format"],["TLDR","Convert an **XPM** image to **PPM**\n\n```xpmtoppm [input.xpm] > [output.ppm]```\n\nRead from **standard input**\n\n```cat [input.xpm] | xpmtoppm > [output.ppm]```\n\nExtract the **alpha (transparency) channel** to a separate PBM file\n\n```xpmtoppm --alphaout=[alpha.pbm] [input.xpm] > [output.ppm]```"],["SYNOPSIS","**xpmtoppm** [_options_] [_file_]"],["PARAMETERS","**--alphaout=**_file_\n> Write the transparency mask to the specified PBM file. If the input has no transparency information, the file contains all white (opaque) values. Specify `-` to write the alpha output to standard output instead of the image."],["DESCRIPTION","**xpmtoppm** reads an X11 pixmap (XPM version 1 or 3) as input and produces a PPM image as output. It handles transparency and color-mapped images. Part of the Netpbm toolkit."],["CAVEATS","Only a limited set of XPM version 3 features are supported; many valid XPM images may be rejected. Input lines longer than 8K characters cause an error."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtoxpm](/man/ppmtoxpm)(1), [xbmtopbm](/man/xbmtopbm)(1), [netpbm](/man/netpbm)(1)"]],"e":["ppmtoxpm","xbmtopbm","netpbm"],"x":"xpmtoppm [input.xpm] > [output.ppm]"}});
