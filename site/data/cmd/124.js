window.LCL_BUCKET(124,{"Xnest":{"n":"Xnest","t":"A nested X server that runs as a client window","s":[["TAGLINE","A nested X server that runs as a client window"],["TLDR","**Start nested X server on display :1**\n\n```Xnest :1```\n\n**Start with specific window size**\n\n```Xnest :1 -geometry [800x600]```\n\n**Start with multiple screens**\n\n```Xnest :1 -scrns [2]```\n\n**Connect to specific parent display**\n\n```Xnest :1 -display [:0]```\n\n**Start fullscreen**\n\n```Xnest :1 -fullscreen```"],["SYNOPSIS","**Xnest** [:_display_] [_options_]"],["PARAMETERS","**-display** _name_\n> Parent X server to connect to. Defaults to DISPLAY environment variable.\n\n**-geometry** _WxH+X+Y_\n> Window geometry for the nested server.\n\n**-scrns** _num_\n> Number of screens to create.\n\n**-depth** _n_\n> Default color depth.\n\n**-fullscreen**\n> Run fullscreen on the parent display.\n\n**-bw** _n_\n> Border width in pixels.\n\n**-name** _string_\n> Window name for the nested server.\n\n**-sss**\n> Enable separate screen saver for each screen.\n\n**-sync**\n> Synchronize window and graphics operations with the parent server. Useful for debugging but significantly slows performance."],["DESCRIPTION","**Xnest** is both an X client and an X server. It runs as a window within another X server (the parent), appearing as a regular X client to the parent while providing a complete X server environment to its own clients.\n\nApplications connect to Xnest using its display number (e.g., `:1`) and receive a fully functional X environment. Multiple screens can be referenced by the number after the dot in the display name (e.g., `xterm -display :1.1` for the second screen). This enables testing window managers, running isolated X sessions, or displaying remote X applications in a contained window.\n\nXnest manages fonts by loading them locally and passing the font name to the real server. For better performance and modern X extension support, **Xephyr** is recommended as a replacement."],["CAVEATS","Lacks modern X extensions (XRender, Composite, RandR). No hardware acceleration. Resource intensive. Xephyr is recommended for most use cases. Requires display number different from parent."],["HISTORY","**Xnest** was developed as a debugging and testing tool for X11. It enabled developers to test applications and window managers without risking their primary desktop. While still functional, the more capable Xephyr server has largely superseded it for modern use."],["INSTALL","```apt: sudo apt install xnest```\n\n```pacman: sudo pacman -S xorg-server-xnest```\n\n```apk: sudo apk add xorg-server-xnest```"],["SEE ALSO","[Xvfb](/man/Xvfb)(1), [Xorg](/man/Xorg)(1), [startx](/man/startx)(1)"]],"e":["Xvfb","Xorg","startx"],"x":"Xnest :1"},"age-keygen":{"n":"age-keygen","t":"Generate encryption key pairs for age","s":[["TAGLINE","Generate encryption key pairs for age"],["TLDR","**Generate** a new key pair (printed to stdout)\n\n```age-keygen```\n\n**Save** key to a file\n\n```age-keygen -o [key.txt]```\n\n**Extract public key** from an existing identity file\n\n```age-keygen -y [key.txt]```\n\n**Extract public key** from an SSH ed25519 key\n\n```age-keygen -y [~/.ssh/id_ed25519]```\n\n**Generate** a hybrid post-quantum key pair (age v1.3.0+)\n\n```age-keygen -pq -o [key.txt]```"],["SYNOPSIS","**age-keygen** [_options_] [**-o** _file_]\n\n**age-keygen** **-y** [_identity-file_]"],["PARAMETERS","**-o** _file_\n> Write key to file instead of stdout. Will not overwrite if file exists.\n\n**-y** [_file_]\n> Read an identity file (or stdin) and output the corresponding public key (recipient). Works with age private keys and ed25519 SSH keys.\n\n**-pq**\n> Generate hybrid post-quantum keys using ML-KEM-768 + X25519 (age v1.3.0+).\n\n**--version**\n> Print version and exit."],["OUTPUT FORMAT","```"],["public key: age1...","AGE-SECRET-KEY-1...\n```\n\nPost-quantum keys use prefixes **age1pq1...** (public) and **AGE-SECRET-KEY-PQ-1...** (private)."],["DESCRIPTION","**age-keygen** generates X25519 key pairs for use with the age encryption tool. The private key (identity) is printed to stdout or written to a file, while the public key is included as a comment in the output.\n\nThe generated keys are simple text strings: public keys start with \"age1\" and private keys start with \"AGE-SECRET-KEY-1\". The **-y** flag extracts the public key from an existing identity, useful for sharing your recipient without exposing the private key."],["CAVEATS","Keep the private key secret; anyone with it can decrypt files meant for you. There is no key revocation mechanism. Back up your keys securely. The **-o** flag will not overwrite existing files. Post-quantum recipients are approximately 2000 characters long."],["HISTORY","**age-keygen** was released alongside age in **2019** by **Filippo Valsorda**. It provides a minimal key generation tool that follows the same design philosophy as age itself: simple, secure, and with no unnecessary options. Version **1.3.0** added hybrid post-quantum key generation with the **-pq** flag."],["INSTALL","```apt: sudo apt install age```\n\n```dnf: sudo dnf install age```\n\n```pacman: sudo pacman -S age```\n\n```apk: sudo apk add age```\n\n```zypper: sudo zypper install age```\n\n```brew: brew install age```\n\n```nix: nix profile install nixpkgs#age```"],["SEE ALSO","[age](/man/age)(1), [ssh-keygen](/man/ssh-keygen)(1), [gpg](/man/gpg)(1)"]],"e":["age","ssh-keygen","gpg"],"x":"age-keygen"},"apm_available":{"n":"apm_available","t":"Check for Advanced Power Management support","s":[["TAGLINE","Check for Advanced Power Management support"],["TLDR","**Check** if APM is available\n\n```apm_available && echo \"APM supported\"```\n\nUse in **script conditionals**\n\n```if apm_available; then apm -l; fi```"],["SYNOPSIS","**apm_available**"],["PARAMETERS","This command takes no arguments."],["DESCRIPTION","**apm_available** tests whether Advanced Power Management (APM) support is available on the current system by checking for the /proc/apm file. It returns exit status 0 if APM is supported and 1 if not.\n\nThis utility is useful in scripts that need to choose between APM and ACPI for power management queries."],["CAVEATS","APM is deprecated on modern systems in favor of ACPI. Most modern hardware only supports ACPI. The tool checks for APM BIOS availability, not functionality."],["HISTORY","**apm_available** is part of the apmd package from the **1990s** and early **2000s**, when APM was the standard power management interface for PCs."],["SEE ALSO","[apm](/man/apm)(1), [acpi_available](/man/acpi_available)(1), [acpi](/man/acpi)(1)"]],"e":["apm","acpi_available","acpi"],"x":"apm_available && echo \"APM supported\""},"apt-secure":{"n":"apt-secure","t":"Archive authentication and trust model documentation.","s":[["TAGLINE","Archive authentication and trust model documentation."],["TLDR","This is a **manual page** describing APT's security model, not a command.\n\nRead the man page:\n\n```man apt-secure```"],["DESCRIPTION","**apt-secure** is a manual page (not a command) that describes APT's archive authentication system. It explains how APT verifies package integrity using cryptographic signatures to prevent man-in-the-middle attacks and compromised mirror servers.\n\nThe document covers:\n- How Release files are signed with GPG\n- How Packages files are verified via checksums in the Release file\n- How to add trusted keys for repository verification\n- Security implications of untrusted repositories\n- The chain of trust from archive maintainer to end user"],["KEY CONCEPTS","**Release.gpg**\n> Detached GPG signature for Release file\n\n**InRelease**\n> Release file with inline signature\n\n**Trusted keys**\n> Stored in /etc/apt/trusted.gpg.d/\n\n**signed-by**\n> Modern method to specify per-repository keys"],["CONFIGURATION","**/etc/apt/trusted.gpg.d/**\n> Directory for trusted keyring files used to verify repository signatures.\n\n**/etc/apt/sources.list**\n> Package sources, supporting signed-by option for per-repository keys."],["CAVEATS","Understanding apt-secure is important for adding third-party repositories safely. Using allow-insecure or allow-unauthenticated bypasses security."],["INSTALL","```apt: sudo apt install apt```\n\n```dnf: sudo dnf install apt```\n\n```pacman: sudo pacman -S apt```\n\n```apk: sudo apk add apt```\n\n```brew: brew install apt```\n\n```nix: nix profile install nixpkgs#apt```"],["SEE ALSO","[apt](/man/apt)(8), [apt-get](/man/apt-get)(8), [apt-key](/man/apt-key)(8), [gpg](/man/gpg)(1)"]],"e":["apt","apt-get","apt-key","gpg"],"x":"man apt-secure"},"archey":{"n":"archey","t":"Display system information with ASCII art logo","s":[["TAGLINE","Display system information with ASCII art logo"],["TLDR","Print a **system summary** (OS, kernel, hardware)\n\n```archey```"],["SYNOPSIS","**archey** [_options_]"],["DESCRIPTION","**archey** is a simple tool for stylishly displaying system information in the terminal. It shows an ASCII art logo of the Linux distribution alongside system details like OS, kernel, uptime, packages, shell, resolution, desktop environment, and hardware information."],["PARAMETERS","**-c, --color**\n> Force colored output\n\n**-n, --no-color**\n> Disable colored output"],["CONFIGURATION","**~/.config/archey4/config.yaml**\n> User-specific configuration for archey4, controlling which entries to display, colors, and logo.\n\n**/etc/archey4/config.yaml**\n> System-wide configuration for archey4."],["CAVEATS","Different versions exist for different distributions (archey, archey3, archey4). Display varies based on the distribution and available system information."],["HISTORY","Originally created for **Arch Linux** by Melik Manukyan. Various forks exist including archey3 (Python 3) and archey4."],["SEE ALSO","[neofetch](/man/neofetch)(1), [screenfetch](/man/screenfetch)(1), [fastfetch](/man/fastfetch)(1)"],["RESOURCES","```[Source code](https://github.com/HorlogeSkynet/archey4)```"]],"e":["neofetch","screenfetch","fastfetch"],"x":"archey"},"aurutils":{"n":"aurutils","t":"Utilities for managing AUR packages in a local repository","s":[["TAGLINE","Utilities for managing AUR packages in a local repository"],["TLDR","**Build** a package and add it to a local repository\n\n```aur build [package]```\n\n**Build** an AUR package and its dependencies into a local repository\n\n```aur sync [package]```\n\n**Upgrade** all AUR packages in a local repository\n\n```aur sync -u```\n\n**Search** the AUR\n\n```aur search [query]```\n\n**Check** installed foreign packages for AUR updates\n\n```pacman -Qm | aur vercmp```\n\n**Fetch** PKGBUILD sources\n\n```aur fetch [package]```"],["SYNOPSIS","**aur** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**aurutils** is a collection of utilities for managing local Arch User Repository (AUR) packages. It helps build, maintain, and deploy AUR packages in a local repository.\n\nThe suite integrates with pacman, allowing AUR packages to be managed like official packages."],["PARAMETERS","**build**\n> Build packages from AUR\n\n**sync**\n> Synchronize packages to local repository\n\n**fetch**\n> Download PKGBUILDs\n\n**search**\n> Search AUR\n\n**vercmp**\n> Compare packages against the AUR, reading name and version pairs from stdin\n\n**repo**\n> Inspect or manage a local repository database\n\n**depends**\n> Recursively download and parse AUR dependencies\n\n**graph**\n> Generate a dependency graph for topological ordering"],["CAVEATS","Requires local repository setup. More complex than yay or paru for casual use. Designed for advanced users who want fine control."],["HISTORY","**aurutils** was developed for users wanting reproducible, local repository-based AUR package management, as opposed to direct installation helpers. It is distributed under the ISC license."],["INSTALL","```aur: yay -S aurutils```"],["SEE ALSO","[pacman](/man/pacman)(8), [yay](/man/yay)(1), [paru](/man/paru)(1), [makepkg](/man/makepkg)(8)"],["RESOURCES","```[Source code](https://github.com/aurutils/aurutils)```"]],"e":["pacman","yay","paru","makepkg"],"x":"aur build [package]"},"bitchat-tui":{"n":"bitchat-tui","t":"Peer-to-peer encrypted chat over Bluetooth LE in the terminal","s":[["TAGLINE","Peer-to-peer encrypted chat over Bluetooth LE in the terminal"],["TLDR","**Launch bitchat-tui** and connect to nearby peers\n\n```bitchat-tui```\n\n**Set your nickname** after launching\n\n```/name [alice]```\n\n**Join a channel** with an optional password\n\n```/j #[channel] [password]```\n\n**Send a direct message** to a user\n\n```/dm [bob] [message]```\n\n**List all online users**\n\n```/online```\n\n**Quick reply to the last DM**\n\n```/reply```\n\n**Return to the public chat room**\n\n```/public```\n\n**Leave the current channel**\n\n```/leave```\n\n**Block a user**\n\n```/block @[username]```\n\n**Show all available commands**\n\n```/help```"],["SYNOPSIS","**bitchat-tui**"],["DESCRIPTION","**bitchat-tui** is a terminal user interface client for BitChat, a secure peer-to-peer messaging protocol that operates over Bluetooth Low Energy. It enables off-grid encrypted communication without requiring internet connectivity or central servers. The application automatically scans for nearby peers via Bluetooth LE and establishes direct mesh connections.\n\nAll messages are end-to-end encrypted using **X25519** Elliptic Curve Diffie-Hellman for key exchange, **AES-256-GCM** for message encryption with integrity authentication, and **Ed25519** digital signatures for verifying message authenticity. Channel passwords are derived using **PBKDF2-SHA256** with 100,000 rounds. Large messages are automatically fragmented and reassembled across Bluetooth LE's limited packet size.\n\nThe TUI provides a sidebar-based interface built with **ratatui** and **crossterm**, organizing conversations into public chat, named channels, direct messages, and a blocked users list. Unread message indicators and section counters help track activity across channels.\n\nWritten in Rust, bitchat-tui runs on Linux, macOS, and Windows."],["COMMANDS","**/help**\n> List all available commands.\n\n**/name** _nick_\n> Change your nickname for the current session.\n\n**/status**\n> Show connection information and current status.\n\n**/clear**\n> Clear all messages from the current conversation view.\n\n**/exit**\n> Quit the application.\n\n**/public**\n> Return to the **#public** chat room.\n\n**/dm** _name_ [_message_]\n> Start a direct message with a user, optionally sending an initial message.\n\n**/reply**\n> Quick reply to the last person who sent you a private message.\n\n**/j** **#**_channel_ [_password_]\n> Join a channel, optionally with a password.\n\n**/leave**\n> Leave the current channel and return to **#public**.\n\n**/pass** _password_\n> Set the channel password (owner only).\n\n**/transfer** **@**_user_\n> Transfer channel ownership (owner only).\n\n**/channels**\n> List all channels you have discovered or joined.\n\n**/online**, **/w**\n> List visible network users.\n\n**/block** **@**_user_\n> Block messages from a specific user.\n\n**/unblock** **@**_user_\n> Unblock a previously blocked user."],["CONFIGURATION","State is persisted in **~/.bitchat/state.json** and includes:\n\n**Identity** -- Ed25519 signing key and X25519 encryption key, generated on first launch\n**Nickname** -- User-chosen display name\n**Channels** -- Joined channels, ownership info, and encrypted channel passwords\n**Blocked peers** -- SHA256 fingerprints of blocked users\n\nChannel passwords are encrypted at rest using AES-256-GCM with keys derived from the identity key."],["CAVEATS","The application accepts no command-line flags or options; all configuration is done interactively through TUI commands after launch. Requires a Bluetooth LE capable adapter. On Linux, the **dbus** system is required for Bluetooth access. Building from source on Windows requires Microsoft Visual C++ Build Tools with the \"Desktop development with C++\" workload. Communication range is limited by Bluetooth LE signal reach, typically around 10-100 meters depending on hardware."],["INSTALL","```aur: yay -S bitchat-tui```"],["SEE ALSO","[bluetuith](/man/bluetuith)(1), [bluetoothctl](/man/bluetoothctl)(1), [hcitool](/man/hcitool)(1)"]],"e":["bluetuith","bluetoothctl","hcitool"],"x":"bitchat-tui"},"blastn":{"n":"blastn","t":"Nucleotide-to-nucleotide sequence alignment search","s":[["TAGLINE","Nucleotide-to-nucleotide sequence alignment search"],["TLDR","**Align sequences** using megablast with e-value threshold\n\n```blastn -query [query.fa] -subject [subject.fa] -evalue [1e-9]```\n\nAlign sequences using **standard blastn**\n\n```blastn -task blastn -query [query.fa] -subject [subject.fa]```\n\nAlign with **custom tabular output** to file\n\n```blastn -query [query.fa] -subject [subject.fa] -outfmt '6 qseqid qlen qstart qend sseqid slen sstart send bitscore evalue pident' -out [output.tsv]```\n\nSearch **nucleotide databases** with multiple threads\n\n```blastn -query [query.fa] -db [path/to/blast_db] -num_threads [16] -max_target_seqs [10]```\n\nSearch the **remote non-redundant** nucleotide database\n\n```blastn -query [query.fa] -db nt -remote```\n\nDisplay **help**\n\n```blastn -h```"],["SYNOPSIS","**blastn** [_options_]"],["DESCRIPTION","**blastn** performs nucleotide-to-nucleotide BLAST (Basic Local Alignment Search Tool) searches. It finds regions of similarity between nucleotide sequences, which can indicate functional, structural, or evolutionary relationships.\n\nThe tool supports multiple search algorithms including megablast (default, for highly similar sequences), discontiguous megablast, and standard blastn. Results can help identify genes, compare genomes, or find homologous sequences."],["PARAMETERS","**-query** _file_\n> Input file with query sequences in FASTA format\n\n**-subject** _file_\n> Subject sequence file for direct comparison\n\n**-db** _name_\n> BLAST database name for searching\n\n**-evalue** _threshold_\n> Expectation value (E) threshold for reporting matches\n\n**-task** _name_\n> Algorithm variant: megablast (default), dc-megablast, blastn, blastn-short, or rmblastn\n\n**-word_size** _n_\n> Length of initial exact match seed (smaller finds more divergent hits)\n\n**-perc_identity** _n_\n> Report only alignments at or above this percent identity\n\n**-outfmt** _format_\n> Output format (0=pairwise, 6=tabular, 7=tabular with headers, 11=BLAST archive)\n\n**-out** _file_\n> Output file name\n\n**-num_threads** _n_\n> Number of CPU threads to use\n\n**-max_target_seqs** _n_\n> Maximum number of aligned sequences to keep\n\n**-remote**\n> Execute search on NCBI servers"],["CAVEATS","Large database searches require significant memory and time. Remote searches are rate-limited. Local database searches require pre-formatted BLAST databases created with **makeblastdb**."],["HISTORY","BLAST was developed at NCBI (National Center for Biotechnology Information) with the original algorithm published in **1990** by Altschul et al. It has become one of the most widely used bioinformatics tools."],["INSTALL","```apt: sudo apt install ncbi-blast+```"],["SEE ALSO","[blastp](/man/blastp)(1), [blastx](/man/blastx)(1), [tblastn](/man/tblastn)(1), [makeblastdb](/man/makeblastdb)(1)"],["RESOURCES","```[Homepage](https://blast.ncbi.nlm.nih.gov/)```\n\n```[Documentation](https://www.ncbi.nlm.nih.gov/books/NBK279690/)```"]],"e":["blastp","blastx","tblastn","makeblastdb"],"x":"blastn -query [query.fa] -subject [subject.fa] -evalue [1e-9]"},"bore-local":{"n":"bore-local","t":"Expose local ports through a remote tunnel","s":[["TAGLINE","Expose local ports through a remote tunnel"],["TLDR","**Expose** a local port using the public bore.pub server\n\n```bore local [local_port] --to bore.pub```\n\n**Expose** a local port to your own remote Bore server\n\n```bore local [local_port] --to [remote_server_address]```\n\nExpose a **specific local host** instead of localhost\n\n```bore local [local_port] --local-host [host] --to [remote_server_address]```\n\nRequest a **specific remote port** explicitly\n\n```bore local [local_port] --to [remote_server_address] --port [remote_port]```\n\nUse a **secret for authentication**\n\n```bore local [local_port] --to [remote_server_address] --secret [your_secret]```"],["SYNOPSIS","**bore local** [_options_] _local_port_"],["DESCRIPTION","**bore local** creates a tunnel from a local port to a remote bore server, making the local service accessible via the remote server's public address. This is useful for exposing development servers, webhooks, or other local services.\n\nThe connection is maintained as long as the command runs, and traffic is forwarded bidirectionally."],["PARAMETERS","**-t, --to** _address_\n> Remote bore server address (required)\n\n**-l, --local-host** _host_\n> Local host to forward from (default: localhost)\n\n**-p, --port** _port_\n> Remote port to request on the server (default: 0, meaning the server assigns a random available port)\n\n**-s, --secret** _secret_\n> Authentication secret for the server\n\n**-h, --help**\n> Display help information"],["CAVEATS","When **--port** is 0 (the default), the server assigns a random available port and prints the resulting public address on startup. Connection stability depends on network conditions. The secret must match the server's configured secret if authentication is required. Forwarded traffic is not encrypted by bore itself."],["INSTALL","```pacman: sudo pacman -S bore```\n\n```nix: nix profile install nixpkgs#bore```"],["SEE ALSO","[bore](/man/bore)(1), [ssh](/man/ssh)(1)"],["RESOURCES","```[Source code](https://github.com/ekzhang/bore)```\n\n```[Homepage](https://bore.pub)```"]],"e":["bore","ssh"],"x":"bore local [local_port] --to bore.pub"},"bzmore":{"n":"bzmore","t":"View bzip2 compressed files with more pager","s":[["TAGLINE","View bzip2 compressed files with more pager"],["TLDR","**View** compressed file\n\n```bzmore [file.txt.bz2]```"],["SYNOPSIS","**bzmore** [_file_...]"],["DESCRIPTION","**bzmore** is a filter for viewing bzip2-compressed files page by page using the more pager. It decompresses files and displays them with basic paging functionality.\n\nThe tool is similar to bzless but uses the simpler more pager."],["FEATURES","- Page-by-page viewing\n- Basic search\n- Original file preserved\n- Multiple file support"],["KEYBINDINGS","- **Space** - Next page\n- **/pattern** - Search\n- **q** - Quit\n- **h** - Help"],["WORKFLOW","```bash"],["View compressed file","bzmore logfile.bz2"],["View multiple files","bzmore *.bz2\n```"],["CAVEATS","Less feature-rich than bzless. No backward navigation. Limited search capabilities. Consider using bzless instead for better functionality."],["HISTORY","**bzmore** has been part of bzip2 utilities since **1996**, modeled after zmore."],["INSTALL","```apt: sudo apt install bzip2```\n\n```dnf: sudo dnf install bzip2```\n\n```pacman: sudo pacman -S bzip2```\n\n```apk: sudo apk add bzip2```\n\n```zypper: sudo zypper install bzip2```\n\n```brew: brew install bzip2```\n\n```nix: nix profile install nixpkgs#bzip2```"],["SEE ALSO","[more](/man/more)(1), [bzless](/man/bzless)(1), [bzcat](/man/bzcat)(1)"],["RESOURCES","```[Source code](https://gitlab.com/bzip2/bzip2)```\n\n```[Homepage](https://sourceware.org/bzip2/)```"]],"e":["more","bzless","bzcat"],"x":"bzmore [file.txt.bz2]"},"cariddi":{"n":"cariddi","t":"crawl URLs for endpoints, secrets, and sensitive data","s":[["TAGLINE","crawl URLs for endpoints, secrets, and sensitive data"],["TLDR","**Crawl domains from list**\n\n```cat [domains.txt] | cariddi```\n\n**Hunt for secrets**\n\n```cat [domains.txt] | cariddi -s```\n\n**Hunt for endpoints**\n\n```cat [domains.txt] | cariddi -e```\n\n**Intensive subdomain crawling**\n\n```cat [urls.txt] | cariddi -intensive```\n\n**Hunt for file extensions**\n\n```cat [domains.txt] | cariddi -ext [3]```\n\n**Use proxy**\n\n```cat [domains.txt] | cariddi -proxy [http://127.0.0.1:8080]```\n\n**Random user agent**\n\n```cat [domains.txt] | cariddi -rua```"],["SYNOPSIS","**cariddi** [_options_]"],["DESCRIPTION","**cariddi** is a reconnaissance tool that crawls URLs and scans for endpoints, secrets, API keys, file extensions, and tokens. It accepts domain lists via standard input, making it easy to chain with subdomain discovery tools in automated security assessment pipelines.\n\nThe tool performs passive analysis of crawled pages, extracting sensitive data such as hardcoded credentials, authentication tokens, and API keys using pattern matching. It supports configurable crawl depth, concurrency, and proxy settings for thorough yet controlled scanning.\n\nDesigned for bug bounty hunting and authorized penetration testing, cariddi outputs results in plain text or JSON format for integration with other security tools."],["PARAMETERS","**-s**\n> Hunt for secrets\n\n**-e**\n> Hunt for endpoints\n\n**-info**\n> Hunt for useful information\n\n**-ext** _level_\n> Hunt for file extensions (1=juicy to 7=less juicy)\n\n**-intensive**\n> Crawl subdomains (*.target.com)\n\n**-proxy** _url_\n> HTTP or SOCKS5 proxy\n\n**-rua**\n> Random browser user agent per request\n\n**-d** _depth_\n> Crawl depth limit\n\n**-c** _n_\n> Concurrent requests\n\n**-t** _seconds_\n> Request timeout\n\n**-o** _file_\n> Output file\n\n**-json**\n> JSON output format\n\n**-plain**\n> Plain text output"],["DETECTION TARGETS","- API keys and secrets\n- Authentication tokens\n- Sensitive file extensions\n- Hidden endpoints\n- Information disclosure"],["CAVEATS","For authorized security testing only. Integrates well with subdomain discovery tools. GPL-3.0 licensed."],["INSTALL","```brew: brew install cariddi```\n\n```nix: nix profile install nixpkgs#cariddi```"],["SEE ALSO","[subfinder](/man/subfinder)(1), [httpx](/man/httpx)(1), [nuclei](/man/nuclei)(1)"],["RESOURCES","```[Source code](https://github.com/edoardottt/cariddi)```"]],"e":["subfinder","httpx","nuclei"],"x":"cat [domains.txt] | cariddi"},"choom":{"n":"choom","t":"display and adjust OOM-killer score","s":[["TAGLINE","display and adjust OOM-killer score"],["TLDR","Display the **OOM-killer score** of a process\n\n```choom -p [pid]```\n\n**Change** the adjust OOM-killer score of a process\n\n```choom -p [pid] -n [-1000..+1000]```\n\n**Run** a command with a specific OOM-killer score\n\n```choom -n [-1000..+1000] [command] [arguments]```"],["SYNOPSIS","**choom** [_options_]"],["DESCRIPTION","**choom** displays and changes the Out-Of-Memory (OOM) killer score adjustment for processes. The OOM killer terminates processes when the system runs critically low on memory.\n\nThe score adjustment ranges from -1000 (never kill) to +1000 (kill first). A value of -1000 effectively disables OOM killing for that process."],["PARAMETERS","**-p, --pid** _pid_\n> Specify the process ID\n\n**-n, --adjust** _value_\n> Set the OOM score adjustment (-1000 to +1000)"],["CAVEATS","Requires appropriate privileges to modify OOM scores of other users' processes. Setting -1000 on too many processes can leave the system with no candidate to kill during memory exhaustion, potentially causing a system hang."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[proc](/man/proc)(5), [kill](/man/kill)(1), [ps](/man/ps)(1)"],["RESOURCES","```[Source code](https://github.com/util-linux/util-linux)```\n\n```[Documentation](https://man7.org/linux/man-pages/man1/choom.1.html)```"]],"e":["proc","kill","ps"],"x":"choom -p [pid]"},"codiff":{"n":"codiff","t":"Diff DWARF debug info between two object files","s":[["TAGLINE","Diff DWARF debug info between two object files"],["TLDR","**Diff two object files** (default: show struct and function changes)\n\n```codiff [old.o] [new.o]```\n\n**Only show struct changes**\n\n```codiff -s [old.o] [new.o]```\n\n**Only show function changes**\n\n```codiff -f [old.o] [new.o]```\n\n**Print terse type changes**\n\n```codiff -t [old.o] [new.o]```\n\n**Verbose output** with full per-symbol detail\n\n```codiff -V [old.o] [new.o]```\n\n**Quiet mode** (exit status only, no output)\n\n```codiff -q [old.o] [new.o]```"],["SYNOPSIS","**codiff** [_options_] _OLD_FILE_ _NEW_FILE_"],["PARAMETERS","**-s**, **--structs**\n> Show diffs of struct/union/class definitions: added, removed, resized, or re-arranged members.\n\n**-f**, **--functions**\n> Show diffs of function signatures and prototypes: parameter or return-type changes, additions, removals.\n\n**-t**, **--terse_type_changes**\n> Print a compact summary line for each changed type instead of the full breakdown.\n\n**-V**, **--verbose**\n> Verbose output: show offsets, sizes, alignment, and per-member changes for each affected type.\n\n**-q**, **--quiet**\n> Only set the exit status; suppress all output. Useful for scripts.\n\n**-F** _list_, **--format_path** _list_\n> Comma-separated list of debug formats to try in order (e.g. **dwarf**, **ctf**, **btf**).\n\n**--help**\n> Print help text and exit."],["DESCRIPTION","**codiff** compares the **DWARF** (or **CTF/BTF**) debugging information embedded in two ELF object files and reports the differences in C-level types, structures, and function prototypes. It is part of the **dwarves** suite alongside **pahole**, **pfunct**, **pglobal**, and **pdwtags**.\n\nThe typical use case is verifying the impact of a patch on a kernel module, library, or large binary: a developer compiles the same translation unit before and after a change and runs **codiff** to see which structures grew, which fields moved, and which exported functions had their signatures altered. Because the comparison is structural rather than textual, renames of unrelated symbols and reordering of unrelated declarations are filtered out automatically.\n\nWithout **-s** or **-f**, both struct and function diffs are produced. The tool requires the input files to be built with debugging information (**-g** for GCC/Clang)."],["CAVEATS","Input files must contain compatible debug information; stripped binaries produce no output. Comparisons across different toolchain or DWARF versions can yield noise. **codiff** is intended for object files (.o) and small libraries — full vmlinux comparisons may take significant time and memory."],["HISTORY","**codiff** was written by **Arnaldo Carvalho de Melo** as part of the **dwarves** project, first released in **2007** to help kernel developers analyse the binary impact of struct-layout changes. The toolset evolved together with **pahole** and is now widely used in the **Linux kernel** workflow, including by **BTF** generation for **eBPF**."],["INSTALL","```apt: sudo apt install pahole```\n\n```pacman: sudo pacman -S pahole```\n\n```apk: sudo apk add pahole```\n\n```nix: nix profile install nixpkgs#pahole```"],["SEE ALSO","[readelf](/man/readelf)(1), [objdump](/man/objdump)(1)"]],"e":["readelf","objdump"],"x":"codiff [old.o] [new.o]"},"dnf-download":{"n":"dnf-download","t":"Download RPM packages without installing","s":[["TAGLINE","Download RPM packages without installing"],["TLDR","**Download a package** to the current directory\n\n```dnf download [package]```\n\n**Download to a specific directory**\n\n```dnf download [package] --destdir [path/to/directory]```\n\n**Download a package and all its dependencies**\n\n```dnf download --resolve [package]```\n\n**Download the source RPM** of a package\n\n```dnf download --source [package]```\n\n**Print the URL** instead of downloading\n\n```dnf download --url [package]```"],["SYNOPSIS","**dnf download** [_options_] _package_ [_package_...]"],["DESCRIPTION","**dnf download** downloads RPM packages from configured DNF repositories without installing them. It is part of the **dnf-plugins-core** package. Useful for obtaining packages for offline installation, inspection, or distributing to air-gapped systems."],["PARAMETERS","**--destdir** _dir_\n> Download destination directory. Defaults to the current directory.\n\n**--url**\n> Print the download URL to stdout instead of downloading.\n\n**--source**\n> Download the source RPM instead of the binary package. Enables source repositories automatically.\n\n**--resolve**\n> Also download all uninstalled dependencies of the specified packages.\n\n**--alldeps**\n> When used with --resolve, download all dependencies (including already installed ones).\n\n**--arch** _arch_\n> Limit to packages of the given architecture."],["CAVEATS","Requires **dnf-plugins-core** to be installed. Downloaded packages are not automatically installed. When using --resolve, only missing dependencies are downloaded by default."],["SEE ALSO","[dnf](/man/dnf)(8), [rpm](/man/rpm)(8), [yumdownloader](/man/yumdownloader)(1)"]],"e":["dnf","rpm","yumdownloader"],"x":"dnf download [package]"},"dvc":{"n":"dvc","t":"version control system for machine learning projects","s":[["TAGLINE","version control system for machine learning projects"],["TLDR","**Initialize DVC repository**\n\n```dvc init```\n\n**Track a data file or directory**\n\n```dvc add [data/dataset.csv]```\n\n**Push tracked data to remote storage**\n\n```dvc push```\n\n**Pull tracked data from remote storage**\n\n```dvc pull```\n\n**Reproduce a pipeline**\n\n```dvc repro```\n\n**Show pipeline DAG**\n\n```dvc dag```\n\n**Configure remote storage**\n\n```dvc remote add -d [myremote] [s3://bucket/path]```\n\n**Show differences in tracked data**\n\n```dvc diff```"],["SYNOPSIS","**dvc** [**--cd** _dir_] _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> DVC operation to perform.\n\n**init**\n> Initialize DVC in a Git repository.\n\n**add** _FILE_\n> Track a file or directory with DVC.\n\n**push**\n> Upload tracked data to remote storage.\n\n**pull**\n> Download tracked data from remote storage.\n\n**repro**\n> Reproduce pipeline stages.\n\n**diff**\n> Show changes in tracked data between commits.\n\n**fetch**\n> Download tracked data from remote without checkout.\n\n**checkout**\n> Checkout data files matching current .dvc files.\n\n**gc**\n> Garbage-collect unused cache files.\n\n**remote add** _NAME_ _URL_\n> Add remote storage.\n\n**config** _OPTION_ [_VALUE_]\n> Get or set DVC configuration options.\n\n**dag**\n> Visualize pipeline stages as a directed acyclic graph.\n\n**destroy**\n> Remove all DVC files and directories from the project.\n\n**--cd** _dir_\n> Change to directory before executing the command.\n\n**-v**, **--verbose**\n> Increase output verbosity.\n\n**-q**, **--quiet**\n> Suppress output.\n\n**--version**\n> Show DVC version.\n\n**-h**, **--help**\n> Display help information."],["CONFIGURATION","**.dvc/config**\n> Repository-level DVC configuration including remote storage settings.\n\n**~/.config/dvc/config**\n> Global user configuration for DVC defaults and preferences.\n\n**.dvc/config.local**\n> Local repository config for machine-specific settings not committed to Git."],["DESCRIPTION","**DVC** (Data Version Control) is a version control system for machine learning projects. It tracks large files, datasets, and models alongside Git, without storing them in the Git repository.\n\nDVC stores file metadata (.dvc files) in Git while the actual data goes to configurable remote storage (S3, GCS, Azure, SSH, etc.). This enables versioning large files and sharing datasets across teams.\n\nThe pipeline feature defines reproducible ML workflows, tracking dependencies and outputs for experiment management."],["CAVEATS","Requires Git repository. Large data transfers depend on network speed. Remote storage may incur costs. Pipeline reproduction needs matching environment."],["HISTORY","DVC was created by **iterative.ai** and released in **2017**. It addresses the challenge of versioning large datasets and ML models that don't fit well in Git, enabling reproducible machine learning workflows. In **2025**, DVC was acquired by **lakeFS**."],["INSTALL","```brew: brew install dvc```\n\n```nix: nix profile install nixpkgs#dvc```"],["SEE ALSO","[git](/man/git)(1), [dvc-init](/man/dvc-init)(1), [dvc-add](/man/dvc-add)(1), [dvc-diff](/man/dvc-diff)(1), [dvc-fetch](/man/dvc-fetch)(1), [dvc-dag](/man/dvc-dag)(1), [dvc-gc](/man/dvc-gc)(1), [mlflow](/man/mlflow)(1)"]],"e":["git","dvc-init","dvc-add","dvc-diff","dvc-fetch","dvc-dag","dvc-gc","mlflow"],"x":"dvc init"},"extrepo":{"n":"extrepo","t":"external Debian repository manager","s":[["TAGLINE","external Debian repository manager"],["TLDR","**Search** for a repository\n\n```extrepo search keyword```\n\n**Enable** a repository\n\n```sudo extrepo enable repository_name```\n\n**Disable** a repository\n\n```sudo extrepo disable repository_name```\n\n**Update** a repository\n\n```sudo extrepo update repository_name```\n\n**List** all available repositories\n\n```extrepo search```"],["SYNOPSIS","**extrepo** [_command_] [_arguments_]"],["DESCRIPTION","**extrepo** manages external Debian repositories in a secure, standardized way. It provides a curated collection of third-party repositories that have been vetted by the Debian project, each with verified GPG keys and proper configuration.\n\nInstead of manually editing /etc/apt/sources.list or using add-apt-repository with untrusted sources, extrepo offers a safe method to enable popular external repositories like Docker, Node.js, Kubernetes, and other projects. All repository configurations are maintained centrally and include proper security verification.\n\nThis tool helps avoid common pitfalls like insecure repository additions, missing GPG keys, or incorrect sources.list syntax when adding third-party software sources to Debian systems."],["PARAMETERS","**search [KEYWORD]**\n> Search for repositories matching keyword (or list all if no keyword)\n\n**enable REPO**\n> Enable the specified repository\n\n**disable REPO**\n> Disable the specified repository\n\n**update REPO**\n> Update the repository configuration"],["CAVEATS","Requires root privileges for enable/disable operations. Only repositories vetted by the Debian project are available. After enabling a repository, run apt update to refresh package lists."],["HISTORY","**extrepo** was developed for Debian to provide a secure method for managing external repositories, replacing manual sources.list editing with verified repository configurations."],["SEE ALSO","[apt](/man/apt)(8), [add-apt-repository](/man/add-apt-repository)(1)"]],"e":["apt","add-apt-repository"],"x":"extrepo search keyword"},"gh-a11y":{"n":"gh-a11y","t":"configure GitHub CLI accessibility settings","s":[["TAGLINE","configure GitHub CLI accessibility settings"],["TLDR","**Show accessibility guidance and settings**\n\n```gh a11y```\n\n**Open the GitHub Accessibility site in the browser**\n\n```gh a11y --web```\n\n**Enable accessible 4-bit color palette**\n\n```gh config set accessible_colors enabled```\n\n**Enable accessible prompter for screen readers**\n\n```gh config set accessible_prompter enabled```\n\n**Disable motion-based spinners in favor of text indicators**\n\n```gh config set spinner disabled```"],["SYNOPSIS","**gh** **a11y** [_flags_]"],["PARAMETERS","**-w**, **--web**\n> Open the GitHub Accessibility site in the default web browser.\n\n**--help**\n> Show help for command."],["DESCRIPTION","**gh a11y** is an alias for **gh accessibility**. It displays guidance on GitHub CLI's accessibility experiences and how to enable them. The command itself only prints guidance; individual features are turned on through **gh config set** or environment variables.\n\n**Accessible colors**: enables a 4-bit ANSI color palette with increased contrast based on the terminal background. Enable with `gh config set accessible_colors enabled` or the `GH_ACCESSIBLE_COLORS` environment variable.\n\n**Label colors**: displays issue and pull request label colors using their custom RGB hex values in true-color terminals. Enable with `gh config set color_labels enabled` or the `GH_COLOR_LABELS` environment variable.\n\n**Accessible prompter**: replaces cursor-manipulating interactive prompts with non-interactive equivalents that work correctly with speech synthesizers and braille displays. Enable with `gh config set accessible_prompter enabled` or the `GH_ACCESSIBLE_PROMPTER` environment variable.\n\n**Text-based spinners**: replaces animated spinner characters with plain text progress indicators. Enable with `gh config set spinner disabled` or the `GH_SPINNER_DISABLED` environment variable."],["CAVEATS","Accessibility features were introduced as a **public preview** in GitHub CLI v2.72.0. This is a built-in core command, not a separately installed extension. Running `gh a11y` alone only displays guidance text."],["HISTORY","GitHub CLI accessibility features were announced in **May 2025** as a public preview, with **gh accessibility** (and its **gh a11y** alias) added to surface the available settings."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh-accessibility](/man/gh-accessibility)(1), [gh](/man/gh)(1), [gh-config](/man/gh-config)(1)"]],"e":["gh-accessibility","gh","gh-config"],"x":"gh a11y"},"gh-copilot":{"n":"gh-copilot","t":"AI-powered command suggestions and explanations","s":[["TAGLINE","AI-powered command suggestions and explanations"],["TLDR","**Ask Copilot a question**\n\n```gh copilot suggest \"[how to find large files]\"```\n\n**Explain a command**\n\n```gh copilot explain \"[git rebase -i HEAD~3]\"```\n\n**Get shell command suggestions**\n\n```gh copilot suggest -t shell \"[task description]\"```\n\n**Get gh CLI suggestions**\n\n```gh copilot suggest -t gh \"[task description]\"```"],["SYNOPSIS","**gh** **copilot** _command_ [_options_]"],["SUBCOMMANDS","**suggest**\n> Get command suggestions.\n\n**explain**\n> Explain a command.\n\n**config**\n> Configure Copilot."],["PARAMETERS","**-t**, **--target** _type_\n> Target type (shell, gh, git)."],["DESCRIPTION","**gh copilot** provides AI-powered assistance using GitHub Copilot for the command line. It helps users discover and understand commands through natural language interactions.\n\nThe suggest subcommand generates shell, git, or gh commands based on task descriptions, making it easier to find the right command syntax. The explain subcommand breaks down complex commands into understandable explanations, helping users learn what commands do before executing them.\n\nThis integration brings GitHub Copilot's AI capabilities directly into the terminal workflow, requiring an active GitHub Copilot subscription."],["CAVEATS","Deprecated. The **gh copilot** extension was archived in **October 2025** in favor of the standalone, agentic **GitHub Copilot CLI** (invoked as **copilot**). Existing installations still work, but new users should install the replacement instead."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-agent](/man/gh-agent)(1)"],["RESOURCES","```[Source code](https://github.com/github/gh-copilot)```\n\n```[Documentation](https://docs.github.com/copilot/github-copilot-in-the-cli)```"]],"e":["gh","gh-agent"],"x":"gh copilot suggest \"[how to find large files]\""},"gh-pr-comment":{"n":"gh-pr-comment","t":"Add comments to pull requests","s":[["TAGLINE","Add comments to pull requests"],["TLDR","**Add comment to PR**\n\n```gh pr comment [number] -b \"[comment]\"```\n\n**Comment interactively**\n\n```gh pr comment [number]```\n\n**Comment from file**\n\n```gh pr comment [number] -F [comment.md]```\n\n**Edit last comment**\n\n```gh pr comment [number] --edit-last -b \"[new text]\"```"],["SYNOPSIS","**gh** **pr** **comment** [_number_] [_options_]"],["PARAMETERS","**-b**, **--body** _text_\n> Comment body.\n\n**-F**, **--body-file** _file_\n> Read body from file.\n\n**--edit-last**\n> Edit last comment.\n\n**-R**, **--repo** _owner/repo_\n> Repository."],["DESCRIPTION","**gh pr comment** adds comments to pull requests from the command line. It provides flexible options for composing comments, including inline text, file input, and interactive editing.\n\nWhen called without the --body flag, the command opens your default editor for composing the comment. Comments support full GitHub-flavored Markdown including code blocks, tables, task lists, and @ mentions. The --edit-last option allows modifying your most recent comment on a pull request."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh-pr](/man/gh-pr)(1)"]],"e":["gh-pr"],"x":"gh pr comment [number] -b \"[comment]\""},"gh-run":{"n":"gh-run","t":"Manage GitHub Actions workflow runs","s":[["TAGLINE","Manage GitHub Actions workflow runs"],["TLDR","**List workflow runs**\n\n```gh run list```\n\n**View run details**\n\n```gh run view [run-id]```\n\n**Watch a run**\n\n```gh run watch [run-id]```\n\n**Download artifacts**\n\n```gh run download [run-id]```\n\n**Rerun failed jobs**\n\n```gh run rerun [run-id] --failed```"],["SYNOPSIS","**gh run** _command_ [_options_]"],["PARAMETERS","**list**\n> List recent workflow runs.\n\n**view** _RUN-ID_\n> View run details.\n\n**watch** _RUN-ID_\n> Watch run progress live.\n\n**download** _RUN-ID_\n> Download run artifacts.\n\n**rerun** _RUN-ID_\n> Rerun a workflow run.\n\n**cancel** _RUN-ID_\n> Cancel a running workflow.\n\n**--workflow** _NAME_\n> Filter by workflow name.\n\n**--branch** _BRANCH_\n> Filter by branch.\n\n**--failed**\n> Only rerun failed jobs.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gh run** manages GitHub Actions workflow runs from the command line. It provides visibility into CI/CD execution including logs, status, and artifacts without visiting github.com.\n\nThe watch command provides live progress updates for running workflows. Download retrieves artifacts produced by workflows, and rerun enables retriggering workflows with options for full or partial reruns."],["CAVEATS","Requires Actions enabled on repository. Log access may be limited. Artifacts have retention limits."],["HISTORY","gh run was added to the **GitHub CLI** alongside broader GitHub Actions integration, enabling workflow management from the command line."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-workflow](/man/gh-workflow)(1), [gh-pr-checks](/man/gh-pr-checks)(1)"],["RESOURCES","```[Documentation](https://cli.github.com/manual/gh_run)```\n\n```[Source code](https://github.com/cli/cli)```"]],"e":["gh","gh-workflow","gh-pr-checks"],"x":"gh run list"},"giftopnm":{"n":"giftopnm","t":"Convert GIF images to PNM format","s":[["TAGLINE","Convert GIF images to PNM format"],["TLDR","**Convert GIF to PNM**\n\n```giftopnm [input.gif] > [output.pnm]```\n\n**Convert specific frame**\n\n```giftopnm --image [2] [input.gif] > [output.pnm]```\n\n**Show comments**\n\n```giftopnm --comments [input.gif]```\n\n**Output all frames** of an animated GIF\n\n```giftopnm --all-images [input.gif] > [output.pnm]```\n\n**Verbose** output showing image details\n\n```giftopnm --verbose [input.gif] > [output.pnm]```"],["SYNOPSIS","**giftopnm** [_options_] [_file_]"],["PARAMETERS","**--image** _num_\n> Select specific image/frame.\n\n**--comments**\n> Print image comments.\n\n**--all-images**\n> Output all frames.\n\n**--verbose**\n> Display information about the GIF input.\n\n**--repair**\n> Try to recover corrupted GIF data."],["DESCRIPTION","**giftopnm** converts GIF images into the Portable Any Map (PNM) format family, which includes PBM (bitmap), PGM (grayscale), and PPM (color) formats. It is a core component of the Netpbm image processing toolkit.\n\nThe tool automatically selects the appropriate output format based on the input GIF's color characteristics. For animated GIFs, it can extract individual frames or output all frames sequentially. PNM formats are uncompressed, making them ideal intermediate formats for image processing pipelines."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtogif](/man/ppmtogif)(1), [pamtogif](/man/pamtogif)(1), [pnmtopng](/man/pnmtopng)(1), [pnmtojpeg](/man/pnmtojpeg)(1), [gifsicle](/man/gifsicle)(1), [convert](/man/convert)(1)"]],"e":["ppmtogif","pamtogif","pnmtopng","pnmtojpeg","gifsicle","convert"],"x":"giftopnm [input.gif] > [output.pnm]"},"git-coauthor":{"n":"git-coauthor","t":"Add co-author attribution to the last commit","s":[["TAGLINE","Add co-author attribution to the last commit"],["TLDR","**Add a coauthor to the last commit**\n\n```git coauthor \"[Name]\" \"[email@example.com]\"```\n\n**Add a second coauthor** (run again against the same commit)\n\n```git coauthor \"[Name2]\" \"[email2@example.com]\"```"],["SYNOPSIS","**git coauthor** _name_ _email_"],["PARAMETERS","_NAME_\n> Coauthor's name. Required.\n\n_EMAIL_\n> Coauthor's email. Required."],["DESCRIPTION","**git coauthor** is a **git-extras** command that amends the last commit message to add a **Co-authored-by:** trailer, the format GitHub and GitLab recognize to credit multiple contributors on one commit.\n\nIt takes exactly one name and one email per invocation. To credit several people, run the command once per coauthor: since it appends to any **Co-authored-by:** trailers already present rather than replacing them, running it repeatedly against the same commit stacks up multiple coauthors.\n\nThis supports pair programming, mob programming, and collaborative workflows where attribution should reflect everyone who contributed, not just whoever typed **git commit**."],["CAVEATS","Part of the **git-extras** package. Amends the last commit, which rewrites its hash, so avoid using it on commits already pushed to a shared branch. Both name and email are required; the command has no flags and does not validate its arguments. Coauthor emails must match the contributor's platform account for GitHub/GitLab to link the attribution to a profile."],["HISTORY","git coauthor is part of **git-extras**, the community-maintained collection of Git utility scripts, added to support GitHub's co-author trailer convention for attributing collaborative work."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-commit](/man/git-commit)(1), [git-authors](/man/git-authors)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-coauthor)```"]],"e":["git-commit","git-authors","git-extras"],"x":"git coauthor \"[Name]\" \"[email@example.com]\""},"git-count":{"n":"git-count","t":"Count commits in repository","s":[["TAGLINE","Count commits in repository"],["TLDR","**Count total commits**\n\n```git count```\n\n**Count commits by author**\n\n```git count --all```"],["SYNOPSIS","**git count** [_options_]"],["PARAMETERS","**--all**\n> Count commits per author.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git count** shows the total number of commits in the repository. With the --all flag, it shows commit counts broken down by author.\n\nThe command provides a quick overview of repository activity and contributor participation. It's simpler than parsing **git shortlog** output manually.\n\nWith the **--all** flag, it produces a ranked list of contributors by commit count, making it useful for project statistics and activity reports."],["CAVEATS","Part of git-extras package. Counts commits on current branch. Merge commits included."],["HISTORY","git count is part of **git-extras**, providing simple commit counting that complements git shortlog."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-count-objects](/man/git-count-objects)(1), [git-shortlog](/man/git-shortlog)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```"]],"e":["git-count-objects","git-shortlog"],"x":"git count"},"git":{"n":"git","t":"Distributed version control system for tracking code changes","s":[["TAGLINE","Distributed version control system for tracking code changes"],["TLDR","**Clone a repository**\n\n```git clone [https://github.com/owner/repo.git]```\n\n**Stage and commit changes**\n\n```git add . && git commit -m \"[message]\"```\n\n**Push to remote**\n\n```git push origin [branch]```\n\n**Pull changes**\n\n```git pull```\n\n**Create and switch branch**\n\n```git checkout -b [branch-name]```"],["SYNOPSIS","**git** [_options_] _command_ [_args_]"],["PARAMETERS","**init**\n> Create an empty Git repository or reinitialize an existing one.\n\n**clone** _URL_\n> Clone a repository into a new directory.\n\n**add** _FILES_\n> Stage files for commit.\n\n**commit**\n> Record changes to repository.\n\n**status**\n> Show working tree status.\n\n**diff**\n> Show changes between commits, the working tree, and the index.\n\n**push**\n> Update remote refs and associated objects.\n\n**pull**\n> Fetch and merge from remote.\n\n**fetch**\n> Download objects and refs from another repository without merging.\n\n**checkout**\n> Switch branches or restore working tree files.\n\n**switch**\n> Switch branches.\n\n**branch**\n> List, create, or delete branches.\n\n**merge**\n> Join two or more development histories together.\n\n**rebase**\n> Reapply commits on top of another base tip.\n\n**log**\n> Show commit history.\n\n**remote**\n> Manage the set of tracked repositories.\n\n**-v**, **--version**\n> Print the Git suite version.\n\n**-C** _PATH_\n> Run as if git was started in _PATH_ instead of the current directory.\n\n**-c** _NAME_=_VALUE_\n> Pass a configuration value for the duration of the command.\n\n**-p**, **--paginate**\n> Pipe output into a pager (usually `less`).\n\n**--git-dir** _PATH_\n> Set the path to the repository's `.git` directory.\n\n**--work-tree** _PATH_\n> Set the path to the working tree.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git** is a distributed version control system for tracking changes in source code. It enables collaboration through branching, merging, and remote repositories while maintaining complete history.\n\nGit's distributed nature means every clone contains full history. Branches are lightweight, enabling feature development workflows. The staging area provides fine-grained commit control."],["CONFIGURATION","**~/.gitconfig**\n> Global user configuration including identity, aliases, and default behaviors.\n\n**.git/config**\n> Repository-specific configuration for remotes, branches, and local settings.\n\n**~/.gitignore_global**\n> Global ignore patterns applied to all repositories."],["CAVEATS","Learning curve for advanced features. Large binary files need git-lfs. History rewriting affects collaborators."],["HISTORY","Git was created by **Linus Torvalds** in **2005** for Linux kernel development after BitKeeper licensing issues. It has become the standard version control system for most software projects."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[gh](/man/gh)(1), [git-commit](/man/git-commit)(1), [git-push](/man/git-push)(1), [git-log](/man/git-log)(1), [git-branch](/man/git-branch)(1)"],["RESOURCES","```[Source code](https://github.com/git/git)```\n\n```[Homepage](https://git-scm.com/)```\n\n```[Documentation](https://git-scm.com/docs/git)```"]],"e":["gh","git-commit","git-push","git-log","git-branch"],"x":"git clone [https://github.com/owner/repo.git]"},"gitnr":{"n":"gitnr","t":"Create .gitignore files from templates","s":[["TAGLINE","Create .gitignore files from templates"],["TLDR","**Create** .gitignore for current directory\n\n```gitnr```\n\n**Create** .gitignore for specific language\n\n```gitnr rust```\n\n**Create** .gitignore with multiple templates\n\n```gitnr rust python node```\n\n**List** available templates\n\n```gitnr --list```"],["SYNOPSIS","**gitnr** [_options_] [_templates_]"],["PARAMETERS","**-l, --list**\n> List all available templates\n\n**-s, --search** _QUERY_\n> Search for templates\n\n**-a, --append**\n> Append to existing .gitignore\n\n**-o, --output** _FILE_\n> Output to specific file\n\n**-f, --force**\n> Overwrite existing file\n\n**-v, --version**\n> Display version and exit\n\n**-h, --help**\n> Display help and exit"],["DESCRIPTION","**gitnr** creates .gitignore files using templates from the GitHub gitignore repository. It supports hundreds of templates for different languages, frameworks, and tools.\n\nThe tool fetches templates from GitHub's official gitignore repository and combines multiple templates when needed."],["AVAILABLE TEMPLATES","Common templates include:\n- Languages: rust, python, node, go, java\n- IDEs: vscode, intellij, vim\n- Tools: docker, terraform, ansible\n- OS: macos, windows, linux"],["CAVEATS","Requires internet connection to fetch templates. GitHub API rate limits may apply. Some templates may need customization for specific project needs."],["HISTORY","**gitnr** was created as a convenient way to generate .gitignore files without browsing the GitHub repository manually."],["INSTALL","```brew: brew install gitnr```\n\n```nix: nix profile install nixpkgs#gitnr```"],["SEE ALSO","[gitignore](https://git-scm.com/docs/gitignore), [gitignore.io](https://www.toptal.com/developers/gitignore)"]],"e":[],"x":"gitnr"},"glab-pipeline":{"n":"glab-pipeline","t":"Manage GitLab CI/CD pipelines","s":[["TAGLINE","Manage GitLab CI/CD pipelines"],["TLDR","**List pipelines**\n\n```glab pipeline list```\n\n**View pipeline**\n\n```glab pipeline view [id]```\n\n**Run pipeline**\n\n```glab pipeline run```\n\n**Run with variables**\n\n```glab pipeline run --variables [KEY:VALUE]```\n\n**Show CI status**\n\n```glab pipeline ci status```\n\n**View jobs**\n\n```glab pipeline ci view```"],["SYNOPSIS","**glab** **pipeline** _command_ [_options_]"],["SUBCOMMANDS","**list**\n> List pipelines.\n\n**view**\n> View pipeline details.\n\n**run**\n> Trigger pipeline.\n\n**status**\n> Show pipeline status.\n\n**delete**\n> Delete pipeline.\n\n**ci**\n> CI-related commands."],["PARAMETERS","**--variables** _vars_\n> Pipeline variables.\n\n**--branch** _branch_\n> Branch to run on."],["DESCRIPTION","**glab pipeline** manages GitLab CI/CD pipelines from the command line. It provides subcommands for viewing, triggering, and monitoring pipelines, as well as inspecting individual job details and logs."],["INSTALL","```apt: sudo apt install glab```\n\n```dnf: sudo dnf install glab```\n\n```pacman: sudo pacman -S glab```\n\n```apk: sudo apk add glab```\n\n```zypper: sudo zypper install glab```\n\n```brew: brew install glab```\n\n```nix: nix profile install nixpkgs#glab```"],["SEE ALSO","[glab](/man/glab)(1)"]],"e":["glab"],"x":"glab pipeline list"},"gnomon":{"n":"gnomon","t":"Prepend timestamps to command output","s":[["TAGLINE","Prepend timestamps to command output"],["TLDR","**Prepend elapsed time** to each line of output\n\n```[command] | gnomon```\n\n**Show total elapsed time** since the command started\n\n```[command] | gnomon --type=elapsed-total```\n\n**Show absolute timestamps** for each line\n\n```[command] | gnomon --type=absolute```\n\n**Highlight lines** taking longer than 1.5 seconds\n\n```[command] | gnomon --high=1.5```\n\n**Set both high and medium** thresholds for color coding\n\n```[command] | gnomon --high=3.0 --medium=1.0```\n\n**Ignore lines** shorter than a threshold\n\n```[command] | gnomon --ignore-blank --high=0.5```"],["SYNOPSIS","**gnomon** [_options_]"],["PARAMETERS","**--type** _TYPE_\n> Timestamp type: **elapsed-line** (default), **elapsed-total**, or **absolute**.\n\n**--ignore-blank**\n> Do not prepend timestamps to blank lines.\n\n**--real-time** _THRESHOLD_\n> Set a threshold in seconds after which gnomon prints output in real time rather than buffering.\n\n**--high** _SECONDS_\n> Threshold in seconds for high (red) time coloring. Default: **1.0**.\n\n**--medium** _SECONDS_\n> Threshold in seconds for medium (yellow) time coloring. Default: **0.5**.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gnomon** is a command-line utility that prepends timestamp information to the output of another command, piped through stdin. By default, it shows how long each line of output took to appear, making it useful for identifying slow steps in build processes, test suites, or log output.\n\nThe tool color-codes output based on configurable time thresholds: lines exceeding the **--high** threshold are shown in red, those exceeding **--medium** in yellow, and the rest in the default color."],["CAVEATS","Requires **Node.js** and is installed via npm (`npm install -g gnomon`). Adds minor latency to output. Best suited for diagnosing slow processes rather than precise benchmarking."],["INSTALL","```pacman: sudo pacman -S gnomon```"],["SEE ALSO","[ts](/man/ts)(1), [time](/man/time)(1), [pv](/man/pv)(1)"]],"e":["ts","time","pv"],"x":"[command] | gnomon"},"groffer":{"n":"groffer","t":"Universal groff document viewer","s":[["TAGLINE","Universal groff document viewer"],["TLDR","**View man page**\n\n```groffer [man-page]```\n\n**View file in X window**\n\n```groffer --x [file.roff]```\n\n**View as PDF**\n\n```groffer --pdf [file.roff]```\n\n**View as text in terminal**\n\n```groffer --tty [file.roff]```\n\n**Specify viewer**\n\n```groffer --viewer [evince] [file.roff]```"],["SYNOPSIS","**groffer** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Files or man pages to display.\n\n**--pdf**\n> Output as PDF.\n\n**--ps**\n> Output as PostScript.\n\n**--tty**\n> Output to terminal.\n\n**--x**\n> Display in X window.\n\n**--viewer** _PROG_\n> Specify viewer program.\n\n**--mode** _MODE_\n> Display mode.\n\n**--help**\n> Display help information."],["DESCRIPTION","**groffer** is a wrapper for groff that automatically selects the best display method for a document. It can handle man pages, roff files, and various input formats.\n\nThe tool detects the environment and chooses between X viewers, PDF viewers, or terminal display, simplifying the groff workflow significantly."],["CAVEATS","Part of groff package. Viewer selection depends on installed applications. Some modes require X display."],["HISTORY","groffer was developed as part of **GNU groff** to provide a user-friendly interface for viewing groff documents."],["SEE ALSO","[groff](/man/groff)(1), [man](/man/man)(1), [xpdf](/man/xpdf)(1)"],["RESOURCES","```[Source code](https://git.savannah.gnu.org/cgit/groff.git)```\n\n```[Homepage](https://www.gnu.org/software/groff/)```"]],"e":["groff","man","xpdf"],"x":"groffer [man-page]"},"gst-device-monitor-1.0":{"n":"gst-device-monitor-1.0","t":"GStreamer multimedia device detection tool","s":[["TAGLINE","GStreamer multimedia device detection tool"],["TLDR","**List all devices**\n\n```gst-device-monitor-1.0```\n\n**Monitor for device changes**\n\n```gst-device-monitor-1.0 --follow```\n\n**List audio sources only**\n\n```gst-device-monitor-1.0 Audio/Source```\n\n**List video sources**\n\n```gst-device-monitor-1.0 Video/Source```\n\n**Include hidden devices**\n\n```gst-device-monitor-1.0 --include-hidden```\n\n**Filter by capabilities**\n\n```gst-device-monitor-1.0 Video/Source:video/x-raw```"],["SYNOPSIS","**gst-device-monitor-1.0** [_options_] [_filter_]"],["PARAMETERS","**-f**, **--follow**\n> Continue monitoring for device changes.\n\n**-i**, **--include-hidden**\n> Include devices from hidden providers.\n\n**--help**\n> Show help synopsis.\n\n**--version**\n> Print version and exit."],["DESCRIPTION","**gst-device-monitor-1.0** is a GStreamer tool for detecting and listing available multimedia devices. It shows audio sources (microphones), audio sinks (speakers), video sources (webcams), and other media devices.\n\nBy default, it lists all devices and exits. Use **--follow** to continuously monitor for device additions and removals."],["CAVEATS","Requires GStreamer 1.4+ with appropriate plugins. Device detection depends on installed plugins (ALSA, PulseAudio, V4L2). May require proper permissions to detect some devices."],["INSTALL","```apt: sudo apt install gstreamer1.0-plugins-base-apps```\n\n```pacman: sudo pacman -S gst-plugins-base```\n\n```apk: sudo apk add gst-plugins-base```\n\n```brew: brew install gst-plugins-base```"],["SEE ALSO","[gst-launch-1.0](/man/gst-launch-1.0)(1), [gst-inspect-1.0](/man/gst-inspect-1.0)(1), [gst-discoverer-1.0](/man/gst-discoverer-1.0)(1)"]],"e":["gst-launch-1.0","gst-inspect-1.0","gst-discoverer-1.0"],"x":"gst-device-monitor-1.0"},"gyb":{"n":"gyb","t":"command-line tool for backing up and restoring Gmail","s":[["TAGLINE","command-line tool for backing up and restoring Gmail"],["TLDR","**Backup Gmail**\n\n```gyb --email [user@gmail.com] --action backup```\n\n**Restore emails**\n\n```gyb --email [user@gmail.com] --action restore```\n\n**Estimate backup size**\n\n```gyb --email [user@gmail.com] --action estimate```\n\n**Backup only recent messages**\n\n```gyb --email [user@gmail.com] --action backup --search \"newer_than:[7d]\"```\n\n**Count messages**\n\n```gyb --email [user@gmail.com] --action count```\n\n**Fast incremental backup** (skip refreshing labels for already-backed-up messages)\n\n```gyb --email [user@gmail.com] --action backup --fast-incremental```"],["SYNOPSIS","**gyb** [_options_]"],["PARAMETERS","**--email** _ADDR_\n> Gmail address.\n\n**--action** _ACT_\n> Action: backup, restore, restore-mbox, estimate, count, purge, print-labels, quota, reindex.\n\n**--local-folder** _DIR_\n> Local backup directory.\n\n**--search** _QUERY_\n> Gmail search query to filter messages.\n\n**--fast-incremental**\n> Skip refreshing labels/flags for already-backed-up messages.\n\n**--label-restored** _LABEL_\n> On restore, additionally apply this label to all messages.\n\n**--strip-labels**\n> Remove existing labels during restore.\n\n**--spam-trash**\n> Include Spam and Trash folders.\n\n**--service-account**\n> Use Google Service Account authentication.\n\n**--help**\n> Display help information."],["DESCRIPTION","**GYB** (Got Your Back) is a command-line tool for backing up and restoring Gmail using the Gmail API over HTTPS.\n\nIt supports incremental backups, search queries to filter specific messages, and can also restore from mbox exports (e.g., Google Takeout). Backups are stored locally with a SQLite index."],["CAVEATS","Requires OAuth authorization. Rate limited by Gmail API. Large mailboxes take time."],["HISTORY","GYB was created by **Jay Lee** as an open-source Gmail backup tool, widely used for Google Workspace migrations."],["INSTALL","```brew: brew install gyb```\n\n```nix: nix profile install nixpkgs#gyb```"],["SEE ALSO","[offlineimap](/man/offlineimap)(1), [mbsync](/man/mbsync)(1), [mutt](/man/mutt)(1)"]],"e":["offlineimap","mbsync","mutt"],"x":"gyb --email [user@gmail.com] --action backup"},"heroku":{"n":"heroku","t":"CLI for Heroku platform-as-a-service","s":[["TAGLINE","CLI for Heroku platform-as-a-service"],["TLDR","**Login**\n\n```heroku login```\n\n**Create app**\n\n```heroku create [app-name]```\n\n**Deploy via Git**\n\n```git push heroku main```\n\n**View logs**\n\n```heroku logs --tail```\n\n**Open app**\n\n```heroku open```\n\n**Run command on dyno**\n\n```heroku run [bash]```\n\n**Scale dynos**\n\n```heroku ps:scale web=[2]```\n\n**Set environment variable**\n\n```heroku config:set [KEY]=[VALUE] -a [app-name]```\n\n**List apps**\n\n```heroku apps```\n\n**Show release history**\n\n```heroku releases -a [app-name]```\n\n**Rollback to a previous release**\n\n```heroku rollback [v42] -a [app-name]```\n\n**Connect to Postgres**\n\n```heroku pg:psql -a [app-name]```\n\n**Toggle maintenance mode**\n\n```heroku maintenance:on -a [app-name]```"],["SYNOPSIS","**heroku** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Subcommand to execute.\n\n**login**\n> Authenticate with Heroku.\n\n**create**\n> Create new application.\n\n**logs**\n> View application logs.\n\n**run**\n> Run one-off command.\n\n**ps**\n> Process management.\n\n**config**\n> Manage environment variables.\n\n**addons**\n> Manage add-ons.\n\n**apps**\n> Manage applications.\n\n**releases**\n> Show app release history; **rollback** to revert.\n\n**maintenance**\n> Toggle maintenance mode (**:on** / **:off**).\n\n**pg**\n> Manage Heroku Postgres databases.\n\n**redis**\n> Manage Heroku Redis instances.\n\n**domains**\n> List, add, or remove custom domains.\n\n**certs**\n> Manage SSL/TLS certificates.\n\n**buildpacks**\n> Manage buildpacks for an app.\n\n**-a**, **--app** _APP_\n> Target application name.\n\n**-r**, **--remote** _REMOTE_\n> Git remote of app to use.\n\n**-j**, **--json**\n> Format output as JSON.\n\n**--help**\n> Display help information."],["DESCRIPTION","**heroku** is the CLI for Heroku platform-as-a-service. It manages applications, addons, and infrastructure on Heroku cloud.\n\nThe tool handles deployment, scaling, logging, and configuration. It integrates with Git for deployments and supports multiple languages."],["CAVEATS","Requires Heroku account. Some features require payment. Rate limits apply."],["HISTORY","Heroku was originally launched in **2007** for Ruby applications and was acquired by **Salesforce** in **2010**. The CLI tool provides command-line management of the platform."],["INSTALL","```brew: brew install heroku```\n\n```nix: nix profile install nixpkgs#heroku```"],["SEE ALSO","[git](/man/git)(1), [docker](/man/docker)(1), [flyctl](/man/flyctl)(1)"]],"e":["git","docker","flyctl"],"x":"heroku login"},"if":{"n":"if","t":"Shell conditional statement","s":[["TAGLINE","Shell conditional statement"],["TLDR","**Basic if statement**\n\n```if [[ condition ]]; then command; fi```\n\n**If-else**\n\n```if [[ -f file ]]; then echo \"exists\"; else echo \"missing\"; fi```\n\n**If-elif-else**\n\n```if [[ $x -eq 1 ]]; then cmd1; elif [[ $x -eq 2 ]]; then cmd2; else cmd3; fi```\n\n**Test file exists**\n\n```if [[ -e file ]]; then echo \"found\"; fi```\n\n**Test string equality**\n\n```if [[ \"$a\" == \"$b\" ]]; then echo \"equal\"; fi```\n\n**Test command exit status**\n\n```if grep -q \"pattern\" file; then echo \"found\"; fi```\n\n**Numeric comparison**\n\n```if [[ $count -gt 10 ]]; then echo \"more than 10\"; fi```"],["SYNOPSIS","**if** _test-commands_; **then** _commands_; [**elif** _test-commands_; **then** _commands_;] [**else** _commands_;] **fi**"],["PARAMETERS","_test-commands_\n> A list of commands whose exit status determines the branch taken. An exit status of 0 (success) means the condition is true.\n\n**then**\n> Introduces commands to execute when the preceding condition is true.\n\n**elif**\n> Else-if clause; tests an additional condition if prior conditions were false.\n\n**else**\n> Commands to execute if all preceding conditions were false.\n\n**fi**\n> End of the if block."],["DESCRIPTION","**if** is a shell builtin conditional statement. It executes the _test-commands_ list and, if the exit status is zero (success), runs the corresponding **then** clause. If non-zero, each **elif** clause is tested in turn. If no condition succeeds and an **else** clause is present, its commands are executed.\n\nAlthough **if** is most commonly used with **test** or **[[ ]]** expressions, any command can serve as the condition since the decision is based on exit status. For example, **if grep -q pattern file** branches on whether grep found a match."],["CAVEATS","Shell builtin. Spaces inside **[ ]** and **[[ ]]** are required (e.g., **[ \"$a\" = \"$b\" ]**, not **[\"$a\"=\"$b\"]**). The POSIX-compatible test syntax is **[ ]**, while **[[ ]]** is a bash/zsh extension with additional features like pattern matching and regex."],["HISTORY","**if** is a standard **Unix shell** construct, present in all POSIX-compliant shells since the original Bourne shell."],["INSTALL","```apk: sudo apk add execline```\n\n```brew: brew install execline```\n\n```nix: nix profile install nixpkgs#execline```"],["SEE ALSO","[test](/man/test)(1), [bash](/man/bash)(1), [while](/man/while)(1), [for](/man/for)(1)"]],"e":["test","bash","while","for"],"x":"if [[ condition ]]; then command; fi"},"iscsiadm":{"n":"iscsiadm","t":"command-line tool for managing ISCSI initiator connections","s":[["TAGLINE","command-line tool for managing ISCSI initiator connections"],["TLDR","Show **active sessions**\n\n```sudo iscsiadm -m session```\n\nList all **known nodes**\n\n```sudo iscsiadm -m node```\n\n**Discover** iSCSI targets on a portal\n\n```sudo iscsiadm -m discovery -t sendtargets -p [ip_address]```\n\n**Log in** to an iSCSI target\n\n```sudo iscsiadm -m node -T [iqn] -p [ip_address]:3260 -l```\n\n**Log out** from an iSCSI target\n\n```sudo iscsiadm -m node -T [iqn] -p [ip_address]:3260 -u```\n\n**Create** a node manually (for CHAP authentication)\n\n```sudo iscsiadm -m node -o new -T [iqn] -p [ip_address]:3260```\n\nConfigure **CHAP authentication**\n\n```sudo iscsiadm -m node -T [iqn] -p [ip_address]:3260 -o update -n node.session.auth.authmethod -v CHAP```"],["SYNOPSIS","**iscsiadm** **-m** _mode_ [_options_]"],["PARAMETERS","**-m**, **--mode** _MODE_\n> Operation mode: discovery, node, session, iface\n\n**-t**, **--type** _TYPE_\n> Discovery type: sendtargets, isns\n\n**-p**, **--portal** _IP:PORT_\n> Target portal address\n\n**-T**, **--targetname** _IQN_\n> Target iSCSI Qualified Name\n\n**-l**, **--login**\n> Log in to target\n\n**-u**, **--logout**\n> Log out from target\n\n**-o**, **--op** _OP_\n> Operation: new, delete, update, show\n\n**-n**, **--name** _NAME_\n> Parameter name\n\n**-v**, **--value** _VALUE_\n> Parameter value"],["DESCRIPTION","**iscsiadm** is the command-line tool for managing iSCSI initiator connections. It handles discovery of iSCSI targets, authentication configuration, and session management.\n\niSCSI allows block storage to be accessed over IP networks, presenting remote storage as local SCSI devices. The initiator (client) connects to targets (storage) using IQNs (iSCSI Qualified Names) for identification."],["CAVEATS","Requires root privileges. The iscsid daemon must be running. CHAP credentials must be configured before login for authenticated targets. Changes to node configuration require logout/login to take effect."],["HISTORY","iscsiadm is part of the Open-iSCSI project, which provides the standard iSCSI implementation for Linux. iSCSI was standardized in RFC 3720 in **2004**."],["INSTALL","```apt: sudo apt install open-iscsi```\n\n```pacman: sudo pacman -S open-iscsi```\n\n```apk: sudo apk add open-iscsi```\n\n```zypper: sudo zypper install open-iscsi```"],["SEE ALSO","[lsblk](/man/lsblk)(8)"]],"e":["lsblk"],"x":"sudo iscsiadm -m session"},"iwctl":{"n":"iwctl","t":"command-line interface for iwd, a modern wireless network configuration daemon","s":[["TAGLINE","command-line interface for iwd, a modern wireless network configuration daemon"],["TLDR","Run **interactive** mode\n\n```iwctl```\n\nDisplay Wi-Fi **stations**\n\n```iwctl station list```\n\n**Scan** for networks\n\n```iwctl station [station] scan```\n\nDisplay **found networks**\n\n```iwctl station [station] get-networks```\n\n**Connect** to a network\n\n```iwctl station [station] connect [network_name]```\n\nDisplay **help**\n\n```iwctl -h```"],["SYNOPSIS","**iwctl** [_options_] [_command_]"],["PARAMETERS","**station list**\n> List wireless stations (interfaces)\n\n**station** _STATION_ **scan**\n> Trigger network scan\n\n**station** _STATION_ **get-networks**\n> Show discovered networks\n\n**station** _STATION_ **connect** _SSID_\n> Connect to a network\n\n**station** _STATION_ **disconnect**\n> Disconnect from current network\n\n**known-networks list**\n> List saved network profiles\n\n**-h**, **--help**\n> Display help"],["DESCRIPTION","**iwctl** is the command-line interface for iwd (iNet Wireless Daemon), a modern wireless network configuration daemon. It provides interactive and command-line modes for managing Wi-Fi connections.\n\niwd is designed as a lightweight alternative to wpa_supplicant, with faster connection times and simpler configuration. Credentials are prompted interactively or can be pre-configured."],["CAVEATS","Requires iwd daemon to be running. Network credentials are stored in /var/lib/iwd/. May conflict with NetworkManager if both try to manage the same interface."],["HISTORY","iwd was developed by Intel and first released in **2017** as a modern replacement for wpa_supplicant. It aims to provide simpler configuration and faster connection times while supporting modern wireless security protocols."],["INSTALL","```apt: sudo apt install iwd```\n\n```dnf: sudo dnf install iwd```\n\n```pacman: sudo pacman -S iwd```\n\n```apk: sudo apk add iwd```\n\n```zypper: sudo zypper install iwd```\n\n```nix: nix profile install nixpkgs#iwd```"],["SEE ALSO","[iw](/man/iw)(8), [nmcli](/man/nmcli)(1), [wpa_supplicant](/man/wpa_supplicant)(8)"]],"e":["iw","nmcli","wpa_supplicant"],"x":"iwctl"},"jj-next":{"n":"jj-next","t":"moves the working copy to a child change","s":[["TAGLINE","moves the working copy to a child change"],["TLDR","**Move to child change**\n\n```jj next```\n\n**Move multiple steps**\n\n```jj next [2]```\n\n**Edit the target change**\n\n```jj next --edit```\n\n**Move to specific branch**\n\n```jj next --branch [name]```"],["SYNOPSIS","**jj next** [_options_] [_count_]"],["PARAMETERS","_COUNT_\n> Number of changes to move forward.\n\n**--edit**\n> Edit target change instead of creating new.\n\n**--branch** _NAME_\n> Follow specific branch.\n\n**--help**\n> Display help information."],["DESCRIPTION","**jj next** moves the working copy to a child change. It navigates forward in the change history.\n\nThe command is useful for reviewing changes in sequence. Combined with prev, it enables navigation through history."],["CAVEATS","Subcommand of jj. Requires child changes to exist. Ambiguity with multiple children."],["HISTORY","jj next is part of **Jujutsu**, providing intuitive history navigation for the change-centric model."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-prev](/man/jj-prev)(1), [jj-edit](/man/jj-edit)(1)"]],"e":["jj","jj-prev","jj-edit"],"x":"jj next"},"katana":{"n":"katana","t":"next-generation web crawling and spidering framework by ProjectDiscovery","s":[["TAGLINE","next-generation web crawling and spidering framework by ProjectDiscovery"],["TLDR","**Crawl a URL**\n\n```katana -u [https://example.com]```\n\n**Crawl with headless browser**\n\n```katana -u [https://example.com] -headless```\n\n**Crawl multiple URLs from file**\n\n```katana -list [urls.txt]```\n\n**Set crawl depth**\n\n```katana -u [https://example.com] -depth [3]```\n\n**Output to file**\n\n```katana -u [https://example.com] -o [output.txt]```\n\n**Enable JavaScript parsing**\n\n```katana -u [https://example.com] -js-crawl```"],["SYNOPSIS","**katana** [_options_]"],["PARAMETERS","**-u** _url_\n> Target URL to crawl.\n\n**-list** _file_\n> File containing URLs to crawl.\n\n**-headless**\n> Use headless browser for crawling.\n\n**-depth** _int_\n> Maximum crawl depth.\n\n**-js-crawl**\n> Enable JavaScript file crawling.\n\n**-o** _file_\n> Output file for results.\n\n**-json**\n> Output in JSON format.\n\n**-silent**\n> Silent mode, minimal output."],["DESCRIPTION","**katana** is a next-generation web crawling and spidering framework by ProjectDiscovery. It supports both standard and headless browser crawling modes, making it effective for JavaScript-heavy applications. Designed for security research and reconnaissance."],["INSTALL","```brew: brew install katana```\n\n```nix: nix profile install nixpkgs#katana```"],["SEE ALSO","[httpx](/man/httpx)(1), [nuclei](/man/nuclei)(1), [subfinder](/man/subfinder)(1)"]],"e":["httpx","nuclei","subfinder"],"x":"katana -u [https://example.com]"},"knotc":{"n":"knotc","t":"control utility for Knot DNS server","s":[["TAGLINE","control utility for Knot DNS server"],["TLDR","**Check server status**\n\n```knotc status```\n\n**Reload server configuration**\n\n```knotc reload```\n\n**Stop DNS server**\n\n```knotc stop```\n\n**Force zone transfer**\n\n```knotc zone-retransfer [zone]```\n\n**Sign zone with DNSSEC**\n\n```knotc zone-sign [zone]```\n\n**Flush zone to file**\n\n```knotc zone-flush [zone]```"],["SYNOPSIS","**knotc** [_options_] _action_ [_args_...]"],["PARAMETERS","**status**\n> Check if server is running.\n\n**reload**\n> Reload configuration and zones.\n\n**stop**\n> Stop the server.\n\n**zone-retransfer** _zone_\n> Force zone transfer from master.\n\n**zone-sign** _zone_\n> Trigger DNSSEC re-signing.\n\n**zone-flush** _zone_\n> Flush zone journal to file.\n\n**-c** _file_\n> Configuration file path.\n\n**-s** _path_\n> Control socket path.\n\n**-t** _seconds_\n> Control timeout."],["DESCRIPTION","**knotc** is the control utility for Knot DNS server. It communicates with the running knotd daemon through a control socket to manage zones, check status, and trigger operations. Supports interactive mode with command completion."],["INSTALL","```apt: sudo apt install knot```\n\n```dnf: sudo dnf install knot```\n\n```pacman: sudo pacman -S knot```\n\n```apk: sudo apk add knot```\n\n```zypper: sudo zypper install knot```\n\n```brew: brew install knot```"],["SEE ALSO","[kdig](/man/kdig)(1)"]],"e":["kdig"],"x":"knotc status"},"koji-help":{"n":"koji-help","t":"displays help information for Koji commands","s":[["TAGLINE","displays help information for Koji commands"],["TLDR","Display **all available commands**\n\n```koji help```\n\nDisplay help for **specific category**\n\n```koji help [category]```\n\nDisplay **help** for this command\n\n```koji help --help```"],["SYNOPSIS","**koji help** [_category_]"],["DESCRIPTION","**koji help** displays help information for Koji commands. Without arguments, it lists all available commands. With a category argument, it shows commands in that category."],["PARAMETERS","**category**\n> Filter commands by category (e.g., admin, all, bind, build, download, info, misc, monitor, search).\n\n**--admin**\n> Show admin commands.\n\n**-h, --help**\n> Display help information"],["CAVEATS","Use `koji [subcommand] --help` for detailed help on specific commands rather than this general overview."],["INSTALL","```dnf: sudo dnf install koji```\n\n```brew: brew install koji```\n\n```nix: nix profile install nixpkgs#koji```"],["SEE ALSO","[koji](/man/koji)(1)"]],"e":["koji"],"x":"koji help"},"latexmk":{"n":"latexmk","t":"perl script that automates LaTeX document compilation","s":[["TAGLINE","perl script that automates LaTeX document compilation"],["TLDR","**Compile a LaTeX document** to PDF\n\n```latexmk -pdf [document.tex]```\n\n**Compile and continuously watch** for changes\n\n```latexmk -pdf -pvc [document.tex]```\n\n**Compile using XeLaTeX**\n\n```latexmk -xelatex [document.tex]```\n\n**Compile using LuaLaTeX**\n\n```latexmk -lualatex [document.tex]```\n\n**Clean auxiliary files**\n\n```latexmk -c [document.tex]```\n\n**Clean all generated files** including output\n\n```latexmk -C [document.tex]```\n\n**Compile with shell escape** (for minted, etc.)\n\n```latexmk -pdf -shell-escape [document.tex]```\n\n**Silent mode** (less output)\n\n```latexmk -pdf -silent [document.tex]```"],["SYNOPSIS","**latexmk** [_-pdf_|_-xelatex_|_-lualatex_] [_-pvc_] [_-c_|_-C_] [_options_] [_file.tex_]"],["PARAMETERS","**-pdf**\n> Generate PDF via pdflatex.\n\n**-xelatex**\n> Generate PDF via xelatex.\n\n**-lualatex**\n> Generate PDF via lualatex.\n\n**-dvi**\n> Generate DVI file.\n\n**-ps**\n> Generate PostScript file.\n\n**-pvc**\n> Preview continuously (watch mode).\n\n**-pv**\n> Preview after compilation.\n\n**-c**\n> Clean up auxiliary files.\n\n**-C**\n> Clean up all generated files including output.\n\n**-f**\n> Force processing past errors.\n\n**-g**\n> Force full processing (ignore timestamps).\n\n**-silent**\n> Reduce output verbosity.\n\n**-verbose**\n> Increase output verbosity.\n\n**-interaction=**_mode_\n> Set interaction mode: nonstopmode, batchmode, errorstopmode, scrollmode.\n\n**-shell-escape**\n> Enable shell escape (for packages like minted).\n\n**-outdir=**_dir_, **-output-directory=**_dir_\n> Output directory for generated files.\n\n**-auxdir=**_dir_\n> Directory for auxiliary files.\n\n**-jobname=**_name_\n> Set output base name.\n\n**-bibtex**, **-nobibtex**\n> Force or disable running bibtex/biber.\n\n**-recorder**\n> Enable recorder to track file dependencies.\n\n**-norc**\n> Skip reading initialization (rc) files.\n\n**-deps**\n> Show a list of dependent files (Make-compatible format).\n\n**-diagnostics**\n> Print detailed diagnostics."],["DESCRIPTION","**latexmk** is a Perl script that automates LaTeX document compilation. It determines the correct sequence and number of runs needed, automatically running latex, bibtex/biber, makeindex, and other tools as required.\n\nLaTeX documents often need multiple passes: cross-references, bibliographies, and indexes require running latex/bibtex repeatedly. Latexmk analyzes log files and auxiliary files to determine when recompilation is needed and in what order.\n\nThe preview continuously mode (-pvc) watches source files and recompiles on changes. Combined with a PDF viewer that auto-refreshes (like Zathura, Skim, or SumatraPDF), this provides a live preview workflow.\n\nConfiguration can be set in **latexmkrc** files (system-wide, user, or project-specific). Common settings include default options, custom rules for specialized processing, and viewer preferences.\n\nFor complex documents with custom build steps, latexmk supports custom dependency rules and processing scripts, making it adaptable to most LaTeX workflows."],["CAVEATS","Requires Perl. Some packages may need special latexmkrc rules. Preview mode viewer support varies by platform. Shell escape enables arbitrary code execution - use only with trusted documents. May not detect all dependency changes."],["HISTORY","**latexmk** was written by John Collins, building on earlier work by Evan McLean and others. The script has been continuously maintained and updated since the **1990s**, becoming the standard automation tool for LaTeX compilation. It's included in TeX Live and MiKTeX distributions."],["INSTALL","```dnf: sudo dnf install texlive```\n\n```apk: sudo apk add texlive```\n\n```zypper: sudo zypper install texlive```\n\n```brew: brew install texlive```"],["SEE ALSO","[pdflatex](/man/pdflatex)(1), [xelatex](/man/xelatex)(1), [lualatex](/man/lualatex)(1), [bibtex](/man/bibtex)(1), [biber](/man/biber)(1)"]],"e":["pdflatex","xelatex","lualatex","bibtex","biber"],"x":"latexmk -pdf [document.tex]"},"llama.cpp":{"n":"llama.cpp","t":"port of Meta's LLaMA model to C/C++ for efficient CPU and GPU inference","s":[["TAGLINE","port of Meta's LLaMA model to C/C++ for efficient CPU and GPU inference"],["TLDR","**Run interactive chat**\n\n```./main -m [model.gguf] -i```\n\n**Generate text with prompt**\n\n```./main -m [model.gguf] -p \"[Your prompt here]\"```\n\n**Set context size**\n\n```./main -m [model.gguf] -c [4096] -p \"[prompt]\"```\n\n**Use multiple threads**\n\n```./main -m [model.gguf] -t [8] -p \"[prompt]\"```\n\n**Run server mode**\n\n```./server -m [model.gguf] --port [8080]```\n\n**Quantize model**\n\n```./quantize [model.gguf] [output.gguf] [q4_0]```"],["SYNOPSIS","**main** [_options_] -m _model_ -p _prompt_"],["PARAMETERS","**-m** _model_\n> Path to GGUF model file.\n\n**-p** _prompt_\n> Input prompt.\n\n**-i**\n> Interactive mode.\n\n**-c** _size_\n> Context size.\n\n**-t** _threads_\n> Number of threads.\n\n**-n** _tokens_\n> Number of tokens to generate.\n\n**--temp** _temp_\n> Temperature for sampling.\n\n**-ngl** _layers_\n> GPU layers to offload."],["DESCRIPTION","**llama.cpp** is a port of Meta's LLaMA model to C/C++ for efficient CPU and GPU inference. It supports various quantization formats and runs LLMs on consumer hardware.\n\nThe project includes tools for model conversion, quantization, and serving."],["SUPPORTED FORMATS","```\nGGUF - Current format\nQuantizations: q4_0, q4_1, q5_0, q5_1, q8_0\nGPU: CUDA, Metal, OpenCL\n```"],["CAVEATS","Models must be converted to GGUF format. Memory requirements depend on model size and quantization. GPU support varies by backend."],["HISTORY","llama.cpp was created by **Georgi Gerganov** in March **2023** after Meta released LLaMA weights, enabling local LLM inference."],["INSTALL","```brew: brew install llama.cpp```"],["SEE ALSO","[llamafile](/man/llamafile)(1), [ollama](/man/ollama)(1)"]],"e":["llamafile","ollama"],"x":"./main -m [model.gguf] -i"},"lychee":{"n":"lychee","t":"fast link checker","s":[["TAGLINE","fast link checker"],["TLDR","**Check links in file**\n\n```lychee [README.md]```\n\n**Check entire directory**\n\n```lychee [docs/]```\n\n**Check website**\n\n```lychee [https://example.com]```\n\n**Exclude patterns**\n\n```lychee --exclude [example.com] [file.md]```\n\n**Output formats**\n\n```lychee --format json [file.md]```\n\n**Skip offline check**\n\n```lychee --offline [file.md]```"],["SYNOPSIS","**lychee** [_options_] _inputs_"],["PARAMETERS","_INPUTS_\n> Files, directories, or URLs.\n\n**--exclude** _PATTERN_\n> Exclude matching URLs.\n\n**--format** _FMT_\n> Output format (plain, json, markdown).\n\n**--offline**\n> Skip network checks.\n\n**--cache**\n> Use cache for results.\n\n**--help**\n> Display help information."],["DESCRIPTION","**lychee** is a fast link checker. It validates links in markdown, HTML, and plain text.\n\nThe tool is written in Rust for speed. It checks both local and remote links concurrently."],["CAVEATS","Network checks may be rate-limited. Some sites block automated requests. Rust-based."],["HISTORY","lychee was created as a fast, modern **Rust-based** link checker for documentation and websites."],["INSTALL","```pacman: sudo pacman -S lychee```\n\n```zypper: sudo zypper install lychee```\n\n```brew: brew install lychee```\n\n```nix: nix profile install nixpkgs#lychee```"],["SEE ALSO","[linkchecker](/man/linkchecker)(1), [wget](/man/wget)(1), [curl](/man/curl)(1)"]],"e":["linkchecker","wget","curl"],"x":"lychee [README.md]"},"machinectl":{"n":"machinectl","t":"controls the systemd machine manager","s":[["TAGLINE","controls the systemd machine manager"],["TLDR","**List** running machines\n\n```machinectl list```\n\n**Start** a machine\n\n```sudo machinectl start machine_name```\n\n**Stop** a running machine\n\n```sudo machinectl stop machine_name```\n\nOpen **interactive shell** in machine\n\n```sudo machinectl shell machine_name```\n\n**Login** to a machine\n\n```sudo machinectl login machine_name```\n\nShow machine **status**\n\n```machinectl status machine_name```\n\n**Execute** command in machine\n\n```sudo machinectl shell machine_name /bin/command```"],["SYNOPSIS","**machinectl** [_OPTIONS_] _COMMAND_ [_NAME_...]"],["DESCRIPTION","**machinectl** controls the systemd machine manager. It manages local containers and virtual machines through systemd-machined, including starting, stopping, and interacting with them."],["COMMANDS","**list**\n> List running machines\n\n**status NAME**\n> Show machine runtime status\n\n**show NAME**\n> Show machine properties\n\n**start NAME**\n> Start a machine as systemd service\n\n**stop NAME**\n> Stop a running machine\n\n**poweroff NAME**\n> Power off a machine\n\n**reboot NAME**\n> Reboot a machine\n\n**terminate NAME**\n> Terminate a machine immediately\n\n**kill NAME**\n> Send signal to machine processes\n\n**login NAME**\n> Open login prompt to machine\n\n**shell [[USER@]NAME [COMMAND...]]**\n> Open shell or run command in machine\n\n**copy-to NAME PATH [PATH]**\n> Copy files to machine\n\n**copy-from NAME PATH [PATH]**\n> Copy files from machine\n\n**bind NAME PATH [PATH]**\n> Bind mount directory into machine\n\n**list-images**\n> List available machine images\n\n**image-status NAME**\n> Show image status\n\n**pull-raw URL [NAME]**\n> Download raw disk image\n\n**pull-tar URL [NAME]**\n> Download tar image\n\n**import-raw FILE [NAME]**\n> Import raw disk image\n\n**import-tar FILE [NAME]**\n> Import tar archive\n\n**remove NAME**\n> Remove machine image\n\n**enable NAME**\n> Enable machine to start at boot\n\n**disable NAME**\n> Disable machine from starting at boot\n\n**clean**\n> Remove hidden or read-only machine images"],["PARAMETERS","**-p, --property**\n> Show specific property\n\n**-a, --all**\n> Show all properties\n\n**-q, --quiet**\n> Suppress output\n\n**--uid USER**\n> User to run shell command as\n\n**-E, --setenv VAR=VALUE**\n> Set environment variable in shell\n\n**-H**, **--host** _USER@HOST_\n> Execute on remote host via SSH.\n\n**-M**, **--machine** _NAME_\n> Execute in local container.\n\n**-h**, **--help**\n> Print a short help text and exit.\n\n**--version**\n> Print a short version string and exit.\n\n**--no-pager**\n> Do not pipe output into a pager.\n\n**--no-legend**\n> Do not print the legend (column headers and hints)."],["CAVEATS","Machines are typically started using systemd-nspawn. The shell command requires systemd-machined to be running. Container images are stored in /var/lib/machines/."],["HISTORY","**machinectl** is part of **systemd**, providing container and VM management through systemd-machined and systemd-nspawn."],["INSTALL","```apt: sudo apt install systemd-container```\n\n```dnf: sudo dnf install systemd-container```\n\n```zypper: sudo zypper install systemd-container```"],["SEE ALSO","[systemd-nspawn](/man/systemd-nspawn)(1), [systemctl](/man/systemctl)(1)"]],"e":["systemd-nspawn","systemctl"],"x":"machinectl list"},"magick-identify":{"n":"magick-identify","t":"displays information about image files","s":[["TAGLINE","displays information about image files"],["TLDR","**Show image information**\n\n```magick identify [image.png]```\n\n**Show verbose information**\n\n```magick identify -verbose [image.png]```\n\n**Show specific property**\n\n```magick identify -format \"%wx%h\" [image.png]```\n\n**Show all images in directory**\n\n```magick identify [*.jpg]```"],["SYNOPSIS","**magick** **identify** [_options_] _image_..."],["PARAMETERS","**-verbose**\n> Show detailed information.\n\n**-format** _string_\n> Output format string.\n\n**-quiet**\n> Suppress warnings.\n\n**-ping**\n> Only read basic attributes."],["DESCRIPTION","**magick identify** displays information about image files. Part of ImageMagick. Shows format, dimensions, color space, file size, and other metadata. Supports format strings for custom output."],["INSTALL","```pacman: sudo pacman -S imagemagick```\n\n```apk: sudo apk add imagemagick```\n\n```brew: brew install imagemagick```\n\n```nix: nix profile install nixpkgs#imagemagick```"],["SEE ALSO","[magick](/man/magick)(1), [file](/man/file)(1)"]],"e":["magick","file"],"x":"magick identify [image.png]"},"mkfs.btrfs":{"n":"mkfs.btrfs","t":"creates a Btrfs on one or more devices","s":[["TAGLINE","creates a Btrfs on one or more devices"],["TLDR","Create **Btrfs filesystem** on partition\n\n```sudo mkfs.btrfs /dev/sdXY```\n\nCreate **single-device** filesystem\n\n```sudo mkfs.btrfs -m single -d single /dev/sdX```\n\nCreate **RAID1** filesystem on multiple devices\n\n```sudo mkfs.btrfs -m raid1 -d raid1 /dev/sdX /dev/sdY```\n\nSet filesystem **label**\n\n```sudo mkfs.btrfs -L \"mylabel\" /dev/sdX```\n\n**Force** creation (overwrite existing)\n\n```sudo mkfs.btrfs -f /dev/sdX```\n\nCreate with specific **node size**\n\n```sudo mkfs.btrfs -n 32k /dev/sdX```"],["SYNOPSIS","**mkfs.btrfs** [_options_] _device_ [_device_...]"],["DESCRIPTION","**mkfs.btrfs** creates a Btrfs (B-tree filesystem) on one or more devices. Btrfs supports multiple device configurations, copy-on-write, snapshots, subvolumes, and various RAID levels."],["PARAMETERS","**-L, --label NAME**\n> Set filesystem label (max 256 characters)\n\n**-f, --force**\n> Force overwrite of existing filesystem\n\n**-m, --metadata PROFILE**\n> Metadata profile (single, dup, raid0, raid1, raid10, raid5, raid6)\n\n**-d, --data PROFILE**\n> Data profile (single, dup, raid0, raid1, raid10, raid5, raid6)\n\n**-n, --nodesize SIZE**\n> Set node/leaf size (default 16KB)\n\n**-s, --sectorsize SIZE**\n> Set sector size\n\n**-O, --features LIST**\n> Enable or disable features\n\n**-r, --rootdir DIR**\n> Copy contents of directory to filesystem root\n\n**-U, --uuid UUID**\n> Specify filesystem UUID\n\n**--mixed**\n> Mix data and metadata in same block groups (for small devices)"],["CAVEATS","Creating a filesystem destroys existing data. Default profile is raid1 for metadata and single for data when multiple devices are specified. RAID5/6 support is still considered experimental."],["HISTORY","**mkfs.btrfs** is part of **btrfs-progs**, the Btrfs filesystem utilities. Btrfs was developed as a modern copy-on-write filesystem for Linux."],["INSTALL","```apt: sudo apt install btrfs-progs```\n\n```dnf: sudo dnf install btrfs-progs```\n\n```pacman: sudo pacman -S btrfs-progs```\n\n```apk: sudo apk add btrfs-progs```\n\n```brew: brew install btrfs-progs```\n\n```nix: nix profile install nixpkgs#btrfs-progs```"],["SEE ALSO","[btrfs](/man/btrfs)(8), [btrfs-filesystem](/man/btrfs-filesystem)(8), [mkfs](/man/mkfs)(8)"]],"e":["btrfs","btrfs-filesystem","mkfs"],"x":"sudo mkfs.btrfs /dev/sdXY"},"more":{"n":"more","t":"filter for paging through text one screenful at a time","s":[["TAGLINE","filter for paging through text one screenful at a time"],["TLDR","**Open a file** for paging\n\n```more [path/to/file]```\n\n**Start at a specific line**\n\n```more +[line_number] [path/to/file]```\n\n**Search for a string** before displaying\n\n```more +/[pattern] [path/to/file]```\n\n**Squeeze** multiple blank lines into one\n\n```more -s [path/to/file]```\n\nSet **lines per screen**\n\n```more -n [20] [path/to/file]```"],["SYNOPSIS","**more** [**-d**] [**-l**] [**-s**] [**-p**] [**-c**] [**-n** _lines_] [**+**_linenum_] [**+/**_pattern_] [_file_...]"],["PARAMETERS","**-d**, **--silent**\n> Prompt with \"[Press space to continue, 'q' to quit.]\" instead of ringing bell on errors.\n\n**-l**, **--logical**\n> Do not pause after lines containing form feed characters.\n\n**-e**, **--exit-on-eof**\n> Exit automatically at end of file. Enabled by default if not on a terminal.\n\n**-f**, **--no-pause**\n> Count logical lines rather than screen lines (long lines are not folded).\n\n**-p**, **--print-over**\n> Clear screen before displaying; do not scroll.\n\n**-c**, **--clean-print**\n> Paint each screen from top, clearing remainder of each line.\n\n**-s**, **--squeeze**\n> Compress multiple consecutive blank lines into one.\n\n**-u**, **--plain**\n> Suppress underlining.\n\n**-n** _number_, **--lines** _number_\n> Specify number of lines per screenful.\n\n**-**_number_\n> Equivalent to **--lines** _number_.\n\n**+**_number_\n> Start display at specified line number.\n\n**+/**_pattern_\n> Search for pattern before displaying.\n\n**-h**, **--help**\n> Display help text and exit.\n\n**-V**, **--version**\n> Print version information and exit."],["INTERACTIVE COMMANDS","**Space** or **z**\n> Display next screenful.\n\n**Enter**\n> Display next line.\n\n**d** or **^D**\n> Scroll forward half screen (default 11 lines).\n\n**s**\n> Skip forward one line.\n\n**f**\n> Skip forward one screenful.\n\n**b** or **^B**\n> Skip backward one screenful (files only).\n\n**/**_pattern_\n> Search forward for pattern.\n\n**n**\n> Repeat last search.\n\n**'**\n> Return to position of last search.\n\n**q** or **Q**\n> Exit.\n\n**h** or **?**\n> Display help summary.\n\n**=**\n> Display current line number.\n\n**v**\n> Open editor at current line.\n\n**:f**\n> Display current filename and line number.\n\n**:n**\n> Go to next file.\n\n**:p**\n> Go to previous file.\n\n**!**_command_\n> Execute command in subshell.\n\n**.**\n> Repeat previous command.\n\n**^L**\n> Redraw screen."],["DESCRIPTION","**more** is a filter for paging through text one screenful at a time. It displays file contents interactively, allowing forward navigation and searching through the text.\n\nCommands follow vi conventions and can be preceded by numeric arguments. The pager reads from files or standard input, making it useful in pipelines."],["CAVEATS","The more command is considered primitive compared to **less**, which offers backward scrolling and more features. Consider using less for most purposes. The **MORE** environment variable can set default options. If **POSIXLY_CORRECT** is set, **-e** is not enabled by default."],["HISTORY","The **more** command appeared in **3.0BSD**. It was written by **Eric Shienbrood** at UC Berkeley and was one of the first screen-oriented utilities. It became a standard Unix pager, though **less** (written by Mark Nudelman in 1983) has largely superseded it due to additional features."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[less](/man/less)(1), [most](/man/most)(1), [pg](/man/pg)(1), [cat](/man/cat)(1), [head](/man/head)(1), [tail](/man/tail)(1)"]],"e":["less","most","pg","cat","head","tail"],"x":"more [path/to/file]"},"newgrp":{"n":"newgrp","t":"changes the current real group ID to the specified group","s":[["TAGLINE","changes the current real group ID to the specified group"],["TLDR","**Switch** to a different primary group\n\n```newgrp [group_name]```\n\n**Switch** group and reinitialize the environment as a login shell\n\n```newgrp - [group_name]```\n\n**Reset** to default primary group\n\n```newgrp```\n\n**Rerun last command with sudo** after permission denied (common pattern)\n\n```sudo newgrp [group_name]```"],["SYNOPSIS","**newgrp** [**-**] [_group_]"],["PARAMETERS","**-**\n> Reinitialize the environment as if the user had logged in\n\n**group**\n> Group name to switch to; must be a member or know group password"],["DESCRIPTION","**newgrp** changes the current real group ID to the specified group and also tries to add the group to the user groupset. If a hyphen is provided, the environment is reinitialized as a login shell. Files created after running newgrp will have the new group as their group owner.\n\nIf not root, the user will be prompted for a password if the user does not have a password and the group does, or if the user is not listed as a member and the group has a password. The user will be denied access if the group password is empty and the user is not listed as a member. If no group is specified, the group is changed to the default group listed in /etc/passwd."],["CAVEATS","This starts a new shell with the changed group. The original shell's group remains unchanged. Group passwords are rarely used and considered insecure. The change only affects the new shell and its children."],["HISTORY","**newgrp** is a standard POSIX command that has been part of Unix systems since early versions. It provides a way to temporarily switch group context without logging out and back in."],["INSTALL","```apt: sudo apt install login```\n\n```apk: sudo apk add util-linux-login```"],["SEE ALSO","[groups](/man/groups)(1), [id](/man/id)(1), [sg](/man/sg)(1), [gpasswd](/man/gpasswd)(1), [chgrp](/man/chgrp)(1)"]],"e":["groups","id","sg","gpasswd","chgrp"],"x":"newgrp [group_name]"},"ninvaders":{"n":"ninvaders","t":"terminal Space Invaders game","s":[["TAGLINE","terminal Space Invaders game"],["TLDR","**Play game**\n\n```ninvaders```\n\n**Set skill level**\n\n```ninvaders -l [5]```\n\n**Show version**\n\n```ninvaders -v```"],["SYNOPSIS","**ninvaders** [_options_]"],["PARAMETERS","**-l** _LEVEL_\n> Starting skill level.\n\n**-v**\n> Show version.\n\n**--help**\n> Display help information."],["PREVIEW","```\n /\\ /\\ /\\ /\\ /\\\n <> <> <> <> <>\n {} {} {} {} {}\n\n      /A\\\n```"],["DESCRIPTION","**ninvaders** is a terminal Space Invaders game. Classic arcade gameplay in the terminal.\n\nThe tool uses ncurses for display. Keyboard controlled shooter game."],["CAVEATS","Terminal game. ncurses-based. Simple controls."],["HISTORY","ninvaders implements **Space Invaders**, the classic 1978 arcade game, for the terminal."],["INSTALL","```zypper: sudo zypper install ninvaders```\n\n```brew: brew install ninvaders```\n\n```nix: nix profile install nixpkgs#ninvaders```"],["SEE ALSO","[moon-buggy](/man/moon-buggy)(1), [bastet](/man/bastet)(1), [netris](/man/netris)(1)"]],"e":["moon-buggy","bastet","netris"],"x":"ninvaders"},"npm-edit":{"n":"npm-edit","t":"open an installed package's directory in the default editor","s":[["TAGLINE","open an installed package's directory in the default editor"],["TLDR","**Edit an installed package**\n\n```npm edit [package]```\n\n**Edit a nested subpackage**\n\n```npm edit [package]/[subpackage]```\n\n**Edit with a specific editor**\n\n```EDITOR=[vim] npm edit [package]```\n\n**Configure the default editor** persistently\n\n```npm config set editor [code]```"],["SYNOPSIS","**npm edit** _pkg_[**/**_subpkg_...]"],["DESCRIPTION","**npm edit** opens an installed package's directory in the default editor so it can be inspected or modified in place under `node_modules`. After the editor exits, npm rebuilds the package so any changes (for example to native addons) take effect.\n\nThe editor is taken from the npm **editor** config, which defaults to the **EDITOR** or **VISUAL** environment variables (or `notepad.exe` on Windows, `vi` on Unix)."],["PARAMETERS","_pkg_\n> Name of the dependency to edit.\n\n**/**_subpkg_\n> Optional nested package path, to drill into a transitive dependency under `node_modules`.\n\n**editor** (config)\n> Editor used to open the package. Set with `npm config set editor <name>` or via **EDITOR**/**VISUAL**."],["CAVEATS","Changes made to files in `node_modules` are lost the next time npm reinstalls or updates the package. For persistent local modifications use **patch-package** or fork the package upstream. **npm edit** is not workspace-aware: in a workspace it operates on the root `node_modules`."],["HISTORY","**npm edit** has been part of **npm** since early releases, providing a quick way to read or hack on installed dependencies during debugging."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-explore](/man/npm-explore)(1)"]],"e":["npm","npm-explore"],"x":"npm edit [package]"},"ntfsfix":{"n":"ntfsfix","t":"fixes common NTFS problems that prevent the filesystem from being mounted","s":[["TAGLINE","fixes common NTFS problems that prevent the filesystem from being mounted"],["TLDR","Repair an **NTFS filesystem** on a block device\n\n```sudo ntfsfix [/dev/sdXN]```"],["SYNOPSIS","**ntfsfix** [_options_] _device_"],["PARAMETERS","**-b, --clear-bad-sectors**\n> Clear the list of bad sectors\n\n**-d, --clear-dirty**\n> Clear the dirty flag on the volume\n\n**-n, --no-action**\n> Perform a dry run; don't actually write changes"],["DESCRIPTION","**ntfsfix** fixes common NTFS problems that prevent the filesystem from being mounted. It resets the NTFS journal, clears the dirty flag, and repairs some structural issues without performing a full filesystem check.\n\nThis tool is useful when Windows was not shut down properly and the NTFS partition is marked as needing a check (dirty). It allows Linux to mount the partition in read-write mode without booting into Windows first.\n\nFor comprehensive NTFS repair, Windows' built-in **chkdsk** should be used, as ntfsfix only handles basic issues that prevent mounting."],["CAVEATS","**Not a full repair tool**; use Windows chkdsk for comprehensive repairs. The partition must be unmounted. Does not fix all NTFS corruption—serious issues still require Windows. Part of the ntfs-3g package."],["INSTALL","```apt: sudo apt install ntfs-3g```\n\n```dnf: sudo dnf install ntfs-3g```\n\n```pacman: sudo pacman -S ntfs-3g```\n\n```apk: sudo apk add ntfs-3g-progs```\n\n```zypper: sudo zypper install ntfs-3g```\n\n```brew: brew install ntfs-3g```"],["SEE ALSO","[ntfs-3g](/man/ntfs-3g)(8), [mount](/man/mount)(8), [fsck](/man/fsck)(8)"]],"e":["ntfs-3g","mount","fsck"],"x":"sudo ntfsfix [/dev/sdXN]"},"ntpq":{"n":"ntpq","t":"queries the NTP daemon for status and configuration information","s":[["TAGLINE","queries the NTP daemon for status and configuration information"],["TLDR","Start **ntpq in interactive mode**\n\n```ntpq```\n\nPrint a **list of NTP peers**\n\n```ntpq -p```\n\nPrint a list of NTP peers **without resolving hostnames**\n\n```ntpq -n -p```\n\nUse ntpq in **debugging mode**\n\n```ntpq -d```\n\nPrint **NTP system variables** values\n\n```ntpq -c rv```"],["SYNOPSIS","**ntpq** [_options_] [_host_...]"],["PARAMETERS","**-n, --numeric**\n> Display addresses numerically without DNS resolution\n\n**-c _command_, --command _command_**\n> Execute a command without entering interactive mode\n\n**-d, --debug-level**\n> Enable debugging output\n\n**-i, --interactive**\n> Force interactive mode.\n\n**-4**, **-6**\n> Resolve hostnames using IPv4 or IPv6 only.\n\n**-w, --wide**\n> Use a wider peer table so long hostnames are not truncated.\n\n**-k** _FILE_, **--keyfile** _FILE_\n> Read symmetric keys from _FILE_ for authenticated control requests.\n\n**-a** _KEYID_, **--authentication** _KEYID_\n> Use the given key id when sending authenticated queries.\n\n**-p, --peers**\n> Print a summary of all configured peers (same as interactive `peers`).\n\n**-V, --version**\n> Print version information and exit."],["DESCRIPTION","**ntpq** queries the NTP daemon for status and configuration information. It can display peer associations, system variables, and synchronization statistics in both interactive and command-line modes.\n\nThe peer list (**-p**) shows all configured time sources with columns indicating:\n- **remote**: Server address (* = current sync source)\n- **refid**: Reference clock or upstream server\n- **st**: Stratum level\n- **reach**: Reachability register (377 = all recent queries successful)\n- **delay/offset/jitter**: Timing metrics in milliseconds\n\nInteractive mode provides additional commands for detailed inspection of the NTP daemon's state."],["CAVEATS","Queries the local ntpd by default; specify a hostname to query remote daemons. Requires ntpd to be running. Some information may require authentication to access. The output format is cryptic; refer to NTP documentation for interpretation."],["INSTALL","```apt: sudo apt install ntpsec```\n\n```dnf: sudo dnf install ntpsec```\n\n```apk: sudo apk add ntpsec```\n\n```zypper: sudo zypper install ntpsec```"],["SEE ALSO","[ntpd](/man/ntpd)(8), [ntpdate](/man/ntpdate)(8), [chronyc](/man/chronyc)(1), [timedatectl](/man/timedatectl)(1)"]],"e":["ntpd","ntpdate","chronyc","timedatectl"],"x":"ntpq"},"ouch":{"n":"ouch","t":"compresses and decompresses archives","s":[["TAGLINE","compresses and decompresses archives"],["TLDR","**Compress files**\n\n```ouch compress [file1] [file2] [archive.tar.gz]```\n\n**Decompress archive**\n\n```ouch decompress [archive.zip]```\n\n**List archive contents**\n\n```ouch list [archive.tar.gz]```\n\n**Decompress to directory**\n\n```ouch decompress [archive.zip] -d [output/]```"],["SYNOPSIS","**ouch** [_command_] [_options_] _files_"],["PARAMETERS","**compress** _FILES_ _ARCHIVE_\n> Create archive.\n\n**decompress** _ARCHIVE_\n> Extract archive.\n\n**list** _ARCHIVE_\n> List contents.\n\n**-d** _DIR_\n> Output directory.\n\n**-y**\n> Yes to all prompts.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ouch** compresses and decompresses archives. Smart format detection.\n\nThe tool handles multiple archive formats. Rust-based compression utility."],["CAVEATS","Format detected by extension. Written in Rust. Supports many formats."],["HISTORY","ouch was created as a **user-friendly compression** tool with smart format detection."],["INSTALL","```pacman: sudo pacman -S ouch```\n\n```zypper: sudo zypper install ouch```\n\n```brew: brew install ouch```\n\n```nix: nix profile install nixpkgs#ouch```"],["SEE ALSO","[tar](/man/tar)(1), [zip](/man/zip)(1), [7z](/man/7z)(1)"]],"e":["tar","zip","7z"],"x":"ouch compress [file1] [file2] [archive.tar.gz]"},"pacinfo":{"n":"pacinfo","t":"displays detailed information about packages from the pacman database","s":[["TAGLINE","displays detailed information about packages from the pacman database"],["TLDR","Display information about a **specific package**\n\n```pacinfo [package_name]```\n\nDisable **low-speed timeouts** for downloads\n\n```pacinfo --no-timeout [package_name]```\n\nDisplay sizes in bytes and dates as **Unix timestamps**\n\n```pacinfo --raw [package_name]```\n\nDisplay **additional** package information\n\n```pacinfo --verbose [package_name]```\n\nDisplay **help**\n\n```pacinfo --help```"],["SYNOPSIS","**pacinfo** [_options_] _package_..."],["PARAMETERS","**--raw**\n> Display sizes in bytes and dates as Unix timestamps\n\n**--verbose**\n> Display additional package information\n\n**--no-timeout**\n> Disable low-speed download timeouts\n\n**--config** _file_\n> Use alternate pacman configuration file\n\n**--dbpath** _path_\n> Use alternate database path\n\n**--root** _path_\n> Use alternate installation root\n\n**--help**\n> Display help information\n\n**--version**\n> Display version information"],["DESCRIPTION","**pacinfo** displays detailed information about packages from the pacman database. It shows package metadata including name, version, description, dependencies, install date, size, and other attributes stored in the package database.\n\nThe tool can query both locally installed packages and packages available in configured repositories. It provides more detailed output options than pacman's built-in query commands, with machine-readable raw output for scripting."],["CAVEATS","Requires packages to be in pacman's database (either installed or available in repositories). Network access needed for repository package queries if sync database is not current."],["HISTORY","Part of **pacutils**, a collection of helper utilities for pacman by Andrew Gregory. Provides enhanced package information display compared to standard **pacman -Qi** and **pacman -Si** commands."],["SEE ALSO","[pacman](/man/pacman)(8), [pacconf](/man/pacconf)(8), [pacsift](/man/pacsift)(8)"]],"e":["pacman","pacconf","pacsift"],"x":"pacinfo [package_name]"},"pactrans":{"n":"pactrans","t":"Install, remove, and upgrade packages using ALPM transactions","s":[["TAGLINE","Install, remove, and upgrade packages using ALPM transactions"],["TLDR","**Install** a package from repository\n\n```sudo pactrans --install [package]```\n\n**Remove** a package\n\n```sudo pactrans --remove [package]```\n\n**Upgrade** all installed packages\n\n```sudo pactrans --sysupgrade```\n\nInstall a **package file**\n\n```sudo pactrans --file [path/to/package.pkg.tar.zst]```\n\n**Install and remove** packages in a single transaction\n\n```sudo pactrans --install [new_package] --remove [old_package]```\n\n**Print** what would happen without executing\n\n```pactrans --print-only --install [package]```\n\n**Install from a specific repository**\n\n```sudo pactrans [repo]/[package]```"],["SYNOPSIS","**pactrans** [_options_] [_target_...]"],["PARAMETERS","**--install**\n> Interpret following targets as packages to install from repositories.\n\n**--remove**\n> Interpret following targets as packages to remove.\n\n**--sysupgrade**\n> Upgrade all installed packages to the latest available versions.\n\n**--file**\n> Interpret following targets as local package files to install.\n\n**--spec**\n> Interpret following targets as pkgspecs: install if a file or repo package, remove if a local database entry.\n\n**--print-only**\n> Display what the transaction would do and exit without making changes.\n\n**--no-confirm**\n> Do not prompt for confirmation.\n\n**--no-deps**\n> Ignore dependency checks.\n\n**--dbonly**\n> Make changes to the database without extracting or removing files.\n\n**--config** _file_\n> Use an alternate pacman configuration file.\n\n**--dbpath** _path_\n> Use an alternate database path.\n\n**--sysroot** _path_\n> Set an alternate system root."],["DESCRIPTION","**pactrans** handles package installation, removal, and system upgrades using the ALPM (Arch Linux Package Management) library. It provides a unified interface for common package operations with support for mixed transactions.\n\nThe tool can install and remove packages in a single transaction. Package specifications can include repository prefixes (e.g., **extra/vim**) for precise control over package sources. If only a package name is provided, repositories are searched in the order they are declared.\n\nThe **pacinstall** and **pacremove** commands are aliases that differ only in their default interpretation of targets but are otherwise identical to **pactrans**.\n\nIf stdin is not connected to a terminal, package specifications are read from stdin."],["CAVEATS","Requires root privileges for system modifications. Uses pacman's configuration and database. Part of the **pacutils** package. Transaction conflicts must be resolved manually."],["HISTORY","**pactrans** is part of **pacutils**, created by **Andrew Gregory**. It provides a programmatic interface to ALPM transactions, useful for scripts and automation that need fine-grained control over package operations."],["SEE ALSO","[pacman](/man/pacman)(8), [pacsync](/man/pacsync)(1), [pacinstall](/man/pacinstall)(1), [pacremove](/man/pacremove)(1), [pacconf](/man/pacconf)(1)"]],"e":["pacman","pacsync","pacinstall","pacremove","pacconf"],"x":"sudo pactrans --install [package]"},"pamrecolor":{"n":"pamrecolor","t":"changes the colors of an image while preserving luminance","s":[["TAGLINE","changes the colors of an image while preserving luminance"],["TLDR","**Colorize grayscale image**\n\n```pamrecolor -colorfile [color.ppm] [gray.ppm] > [output.ppm]```\n\n**Apply color from solid**\n\n```pamrecolor -targetcolor \"rgb:ff/00/00\" [gray.ppm] > [red.ppm]```\n\n**Preserve luminance**\n\n```pamrecolor -colorfile [palette.ppm] -rmult [1.0] [input.ppm] > [output.ppm]```"],["SYNOPSIS","**pamrecolor** [_options_] [_pamfile_]"],["PARAMETERS","**-colorfile** _file_\n> Source color image.\n\n**-targetcolor** _color_\n> Single target color.\n\n**-rmult**, **-gmult**, **-bmult** _factor_\n> Channel multipliers."],["DESCRIPTION","**pamrecolor** changes the colors of an image while preserving luminance. It can colorize grayscale images or remap colors from one image using colors from another."],["EXAMPLE","```bash"],["Colorize old photo with sepia","pamrecolor -targetcolor \"rgb:94/6d/50\" old_photo.ppm > sepia.ppm"],["Apply palette from reference","pamrecolor -colorfile reference.ppm target.ppm > recolored.ppm\n```"],["CAVEATS","Results depend on luminance preservation. Works best on grayscale or low-saturation images."],["HISTORY","pamrecolor is part of **Netpbm**, providing color manipulation tools for image processing."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmchange](/man/ppmchange)(1), [pamgamma](/man/pamgamma)(1), [ppmtopgm](/man/ppmtopgm)(1)"]],"e":["ppmchange","pamgamma","ppmtopgm"],"x":"pamrecolor -colorfile [color.ppm] [gray.ppm] > [output.ppm]"},"pik":{"n":"pik","t":"Interactive TUI for finding and killing processes","s":[["TAGLINE","Interactive TUI for finding and killing processes"],["TLDR","**Launch the interactive process picker**\n\n```pik```\n\n**Filter processes by name**\n\n```pik [process_name]```"],["SYNOPSIS","**pik** [_options_] [_pattern_]"],["DESCRIPTION","**pik** (Process Interactive Kill) is a terminal tool for interactively finding and killing processes. It works like **pkill** but with a live, interactive search interface where you type to filter processes and select which to kill. It supports prefix search for arguments and ports, configurable readline-style key mappings, and cross-platform operation."],["HISTORY","**pik** was created by **Jacek Kurlit** (jacek-kurlit) and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S pik```\n\n```zypper: sudo zypper install pik```\n\n```nix: nix profile install nixpkgs#pik```"],["SEE ALSO","[pkill](/man/pkill)(1), [kill](/man/kill)(1), [htop](/man/htop)(1), [procs](/man/procs)(1)"]],"e":["pkill","kill","htop","procs"],"x":"pik"},"pkgctl":{"n":"pkgctl","t":"Arch Linux package maintainer toolset","s":[["TAGLINE","Arch Linux package maintainer toolset"],["TLDR","**Authenticate** with the Arch Linux GitLab instance\n\n```pkgctl auth login```\n\n**Build** a package inside a clean chroot\n\n```pkgctl build```\n\n**Update** the binary repository database after a release\n\n```pkgctl db update```\n\n**Compare** the current package with the repository version\n\n```pkgctl diff```\n\n**Release** a package to the binary repositories\n\n```pkgctl release```\n\n**Clone** a packaging repository from GitLab\n\n```pkgctl repo clone [pkgbase]```\n\nDisplay **version** information\n\n```pkgctl version```"],["SYNOPSIS","**pkgctl** [_command_] [_options_]"],["COMMANDS","**auth**\n> Authenticate with GitLab and other services\n\n**build**\n> Build packages in clean chroot\n\n**db update**\n> Update binary repository database\n\n**diff**\n> Compare package files\n\n**release**\n> Release build artifacts\n\n**repo**\n> Manage Git packaging repositories\n\n**version**\n> Display version information"],["DESCRIPTION","**pkgctl** is the unified command-line interface for Arch Linux package maintainer tools. It consolidates various devtools commands into a single entry point, simplifying the workflow for building and releasing Arch packages.\n\nThe tool integrates with Arch infrastructure including GitLab, the binary repositories, and the Arch Build System (ABS)."],["CAVEATS","Intended for Arch package maintainers. Requires devtools package. Authentication needed for some operations. Understanding of PKGBUILD files required."],["HISTORY","**pkgctl** was introduced as part of modernizing Arch Linux devtools. It provides a unified interface replacing scattered tools like archbuild, archrelease, and others, improving the package maintainer experience."],["SEE ALSO","[makepkg](/man/makepkg)(8), [pacman](/man/pacman)(8), [devtools](/man/devtools)(7), [asp](/man/asp)(1), [pkgctl-build](/man/pkgctl-build)(1)"]],"e":["makepkg","pacman","devtools","asp","pkgctl-build"],"x":"pkgctl auth login"},"plasma-apply-wallpaperimage":{"n":"plasma-apply-wallpaperimage","t":"Set KDE Plasma desktop wallpaper","s":[["TAGLINE","Set KDE Plasma desktop wallpaper"],["TLDR","**Apply** a wallpaper\n\n```plasma-apply-wallpaperimage [/path/to/image]```\n\nApply with **fill mode**\n\n```plasma-apply-wallpaperimage --fill-mode [preserveAspectCrop] [/path/to/image]```\n\nDisplay **help**\n\n```plasma-apply-wallpaperimage --help```"],["SYNOPSIS","**plasma-apply-wallpaperimage** [**--fill-mode** _mode_] _image_path_"],["PARAMETERS","**--fill-mode _mode_**\n> How to fit image: stretch, preserveAspectFit, preserveAspectCrop, tile, tileVertically, tileHorizontally, pad\n\n**image_path**\n> Path to wallpaper image file\n\n**--help**\n> Display help information"],["DESCRIPTION","**plasma-apply-wallpaperimage** sets the desktop wallpaper in KDE Plasma from the command line. It supports various fill modes to control how the image is scaled or tiled to fit the screen.\n\nThe tool is useful for scripts that change wallpapers based on time of day, weather, or other conditions."],["CAVEATS","Requires running Plasma session. Image must be in supported format (PNG, JPG, etc.). Applies to all desktops/monitors by default."],["HISTORY","**plasma-apply-wallpaperimage** is part of **KDE Plasma Workspace**, providing scriptable wallpaper control alongside other plasma-apply tools."],["INSTALL","```apt: sudo apt install plasma-workspace```\n\n```dnf: sudo dnf install plasma-workspace```\n\n```pacman: sudo pacman -S plasma-workspace```\n\n```apk: sudo apk add plasma-workspace```"],["SEE ALSO","[plasma-apply-colorscheme](/man/plasma-apply-colorscheme)(1), [feh](/man/feh)(1), [nitrogen](/man/nitrogen)(1)"]],"e":["plasma-apply-colorscheme","feh","nitrogen"],"x":"plasma-apply-wallpaperimage [/path/to/image]"},"pmidi":{"n":"pmidi","t":"Play MIDI files via ALSA sequencer","s":[["TAGLINE","Play MIDI files via ALSA sequencer"],["TLDR","**Play MIDI file**\n\n```pmidi -p [port] [file.mid]```\n\n**List MIDI ports**\n\n```pmidi -l```\n\n**Play with delay**\n\n```pmidi -d [seconds] -p [port] [file.mid]```"],["SYNOPSIS","**pmidi** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> MIDI file to play.\n\n**-p** _PORT_\n> MIDI output port.\n\n**-l**\n> List available ports.\n\n**-d** _SECONDS_\n> Delay before playing.\n\n**--help**\n> Display help."],["DESCRIPTION","**pmidi** plays standard MIDI files through the ALSA sequencer interface. It sends MIDI events to a specified output port, which can be a hardware synthesizer, software synth like FluidSynth or TiMidity, or any ALSA MIDI device.\n\nUse **-l** to list available MIDI ports and **-p** to select the target port for playback. The tool provides straightforward MIDI file playback without the overhead of a full media player."],["CAVEATS","ALSA only. Requires MIDI port. Linux specific."],["HISTORY","pmidi is part of **ALSA utilities** for MIDI playback."],["INSTALL","```apt: sudo apt install pmidi```\n\n```zypper: sudo zypper install pmidi```\n\n```nix: nix profile install nixpkgs#pmidi```"],["SEE ALSO","[aplaymidi](/man/aplaymidi)(1), [timidity](/man/timidity)(1), [fluidsynth](/man/fluidsynth)(1)"]],"e":["aplaymidi","timidity","fluidsynth"],"x":"pmidi -p [port] [file.mid]"},"pmount":{"n":"pmount","t":"Mount removable devices as regular user","s":[["TAGLINE","Mount removable devices as regular user"],["TLDR","**Mount** device to /media/\n\n```pmount [/dev/sdb1]```\n\nMount with specific **filesystem** and label\n\n```pmount -t [ext4] [/dev/sdb1] [mylabel]```\n\nMount **CD-ROM** read-only\n\n```pmount -t iso9660 -r [/dev/cdrom]```\n\nMount **NTFS** with read-write\n\n```pmount -t ntfs -w [/dev/sdX]```\n\nDisplay **mounted** removable devices\n\n```pmount```"],["SYNOPSIS","**pmount** [**-t** _type_] [**-r**|**-w**] _device_ [_label_]"],["PARAMETERS","**-t, --type _fstype_**\n> Filesystem type\n\n**-r, --read-only**\n> Mount read-only\n\n**-w, --read-write**\n> Force read-write mount\n\n**-s, --sync**\n> Mount with sync option (no write caching)\n\n**-A, --noatime**\n> Mount with noatime option\n\n**-u, --umask _mask_**\n> Set umask for mount\n\n**-e, --exec**\n> Allow execution of binaries (default: noexec)\n\n**-c, --charset _charset_**\n> Set I/O character set (default: utf8 in UTF-8 locale)\n\n**-d, --debug**\n> Enable verbose debug messages\n\n**label**\n> Mount point name under /media/"],["DESCRIPTION","**pmount** allows regular users to mount removable devices without root privileges. It mounts to /media/ with the device name or specified label as mount point. Default mount flags are: async,atime,nodev,noexec,noauto,nosuid,user,rw.\n\nThe tool enforces security restrictions: only devices in /media can be mounted, and dangerous options like suid are disabled. It's designed for desktop environments lacking automatic mounting."],["CAVEATS","Only works with removable devices. Mount point created automatically under /media/. Use pumount to unmount. Requires pmount package and appropriate permissions."],["HISTORY","**pmount** was created to provide secure user mounting before udisks and modern desktop automounters. It remains useful for minimal systems or when automatic mounting is undesired."],["INSTALL","```apt: sudo apt install pmount```\n\n```dnf: sudo dnf install pmount```\n\n```nix: nix profile install nixpkgs#pmount```"],["SEE ALSO","[pumount](/man/pumount)(1), [mount](/man/mount)(8), [udisksctl](/man/udisksctl)(1)"]],"e":["pumount","mount","udisksctl"],"x":"pmount [/dev/sdb1]"},"postfix":{"n":"postfix","t":"Control the Postfix mail system","s":[["TAGLINE","Control the Postfix mail system"],["TLDR","**Check** configuration\n\n```sudo postfix check```\n\nCheck **status**\n\n```sudo postfix status```\n\n**Start** Postfix\n\n```sudo postfix start```\n\n**Stop** gracefully\n\n```sudo postfix stop```\n\n**Flush** mail queue\n\n```sudo postfix flush```\n\n**Reload** configuration\n\n```sudo postfix reload```"],["SYNOPSIS","**postfix** [**-c** _config_dir_] [**-D**] _command_"],["COMMANDS","**check**\n> Verify configuration for errors\n\n**start**\n> Start the Postfix mail system\n\n**stop**\n> Stop the Postfix mail system\n\n**abort**\n> Stop immediately without finishing deliveries\n\n**flush**\n> Force delivery of queued mail\n\n**reload**\n> Reload configuration without restart\n\n**status**\n> Check if Postfix is running"],["PARAMETERS","**-c _config_dir_**\n> Alternate configuration directory\n\n**-D**\n> Debug mode"],["DESCRIPTION","**postfix** is the control program for the Postfix mail transfer agent. It manages starting, stopping, and reconfiguring the mail system components.\n\nPostfix is designed for security and performance, running multiple separate processes with minimal privileges. The control program coordinates these processes."],["CONFIGURATION","**/etc/postfix/main.cf**\n> Primary configuration file defining hostname, domain, relay settings, network restrictions, TLS, and all major Postfix parameters.\n\n**/etc/postfix/master.cf**\n> Defines Postfix service components (smtpd, cleanup, local, etc.) and their process limits, chroot settings, and command-line options.\n\n**/etc/postfix/virtual**\n> Virtual alias mapping for email address rewriting. Requires **postmap** to rebuild the database after changes.\n\n**/etc/postfix/transport**\n> Controls mail routing by destination domain. Requires **postmap** after editing."],["CAVEATS","Requires root privileges for most commands. Mail queue persists across restarts. Check configuration before reload. Abort may lose mail in transit."],["HISTORY","**Postfix** was written by **Wietse Venema** at IBM Research as a secure, fast replacement for Sendmail. Released in 1998, it became one of the most popular MTAs due to its security design and ease of configuration."],["INSTALL","```apt: sudo apt install postfix```\n\n```dnf: sudo dnf install postfix```\n\n```pacman: sudo pacman -S postfix```\n\n```apk: sudo apk add postfix```\n\n```zypper: sudo zypper install postfix```\n\n```nix: nix profile install nixpkgs#postfix```"],["SEE ALSO","[postconf](/man/postconf)(1), [postqueue](/man/postqueue)(1), [sendmail](/man/sendmail)(8)"]],"e":["postconf","postqueue","sendmail"],"x":"sudo postfix check"},"ppmtopuzz":{"n":"ppmtopuzz","t":"Convert PPM to X puzzle image format","s":[["TAGLINE","Convert PPM to X puzzle image format"],["TLDR","**Convert PPM to X puzzle format**\n\n```ppmtopuzz [input.ppm] > [output.puzz]```"],["SYNOPSIS","**ppmtopuzz** [_file_]"],["DESCRIPTION","**ppmtopuzz** converts PPM images to X puzzle format for the xpuzzle game. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[ppmtoxpm](/man/ppmtoxpm)(1)"]],"e":["ppmtoxpm"],"x":"ppmtopuzz [input.ppm] > [output.puzz]"},"pstopnm":{"n":"pstopnm","t":"Convert PostScript and PDF to PNM format","s":[["TAGLINE","Convert PostScript and PDF to PNM format"],["TLDR","**Convert PostScript to PNM**\n\n```pstopnm [input.ps]```\n\n**Convert with resolution**\n\n```pstopnm -dpi [300] [input.ps]```\n\n**Convert to specific format**\n\n```pstopnm -ppm [input.ps]```\n\n**Convert PDF**\n\n```pstopnm [input.pdf]```"],["SYNOPSIS","**pstopnm** [_options_] _file_"],["PARAMETERS","**-dpi** _n_\n> Output resolution.\n\n**-xsize** _n_\n> Output width.\n\n**-ysize** _n_\n> Output height.\n\n**-ppm**\n> Force PPM output.\n\n**-pgm**\n> Force PGM output.\n\n**-pbm**\n> Force PBM output.\n\n**-stdout**\n> Write to stdout."],["DESCRIPTION","**pstopnm** converts PostScript and PDF files to PNM format. Uses Ghostscript for rendering. Part of Netpbm toolkit for document conversion."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[gs](/man/gs)(1), [pnmtops](/man/pnmtops)(1)"]],"e":["gs","pnmtops"],"x":"pstopnm [input.ps]"},"pve-firewall":{"n":"pve-firewall","t":"Proxmox distributed firewall manager","s":[["TAGLINE","Proxmox distributed firewall manager"],["TLDR","**Compile** and print all firewall rules\n\n```pve-firewall compile```\n\nShow information about the **local network**\n\n```pve-firewall localnet```\n\n**Restart** the Proxmox VE Firewall service\n\n```pve-firewall restart```\n\n**Start** the Proxmox VE Firewall service\n\n```pve-firewall start```\n\n**Stop** the Proxmox VE Firewall service\n\n```pve-firewall stop```\n\n**Simulate** all firewall rules\n\n```pve-firewall simulate```\n\nShow the **status** of Proxmox VE Firewall\n\n```pve-firewall status```"],["SYNOPSIS","**pve-firewall** _command_ [_options_]"],["PARAMETERS","**compile**, **c**\n> Compile and print all firewall rules\n\n**localnet**, **l**\n> Show local network information\n\n**restart**, **r**\n> Restart the firewall service\n\n**start**\n> Start the firewall service\n\n**stop**\n> Stop the firewall service\n\n**simulate**, **si**\n> Simulate firewall rules without applying\n\n**status**\n> Show firewall service status"],["DESCRIPTION","**pve-firewall** manages the Proxmox VE distributed firewall. It generates iptables rules based on cluster-wide and per-VM/container configurations. The firewall can be configured at datacenter, node, VM, and container levels.\n\nThe compile and simulate commands are useful for debugging firewall configurations before applying them. The firewall supports rule sets, IP sets, aliases, and security groups for organized configuration management."],["CAVEATS","Firewall rules are cluster-wide configuration stored in pmxcfs. Changes may affect all cluster nodes. Misconfiguration can lock out management access; ensure a working console connection before major changes."],["HISTORY","Part of **Proxmox VE**, providing integrated firewall management for virtualization environments. Builds on iptables/nftables and integrates with Proxmox's cluster filesystem for distributed configuration."],["SEE ALSO","[qm](/man/qm)(1), [pct](/man/pct)(1), [pvesh](/man/pvesh)(1)"]],"e":["qm","pct","pvesh"],"x":"pve-firewall compile"},"rdfind":{"n":"rdfind","t":"Find and handle duplicate files efficiently","s":[["TAGLINE","Find and handle duplicate files efficiently"],["TLDR","**Find duplicates**\n\n```rdfind [directory]```\n\n**Find in multiple directories**\n\n```rdfind [dir1] [dir2]```\n\n**Delete duplicates**\n\n```rdfind -deleteduplicates true [directory]```\n\n**Create hardlinks**\n\n```rdfind -makehardlinks true [directory]```\n\n**Create symlinks**\n\n```rdfind -makesymlinks true [directory]```\n\n**Dry run**\n\n```rdfind -dryrun true [directory]```\n\n**Output results file**\n\n```rdfind -outputname [results.txt] [directory]```"],["SYNOPSIS","**rdfind** [_-deleteduplicates true_] [_-makehardlinks true_] [_options_] _directories_"],["PARAMETERS","**-deleteduplicates** _BOOL_\n> Delete duplicates.\n\n**-makehardlinks** _BOOL_\n> Replace with hardlinks.\n\n**-makesymlinks** _BOOL_\n> Replace with symlinks.\n\n**-dryrun** _BOOL_\n> Simulate only.\n\n**-ignoreempty** _BOOL_\n> Ignore empty files.\n\n**-removeidentinode** _BOOL_\n> Remove same inode files.\n\n**-outputname** _FILE_\n> Results filename.\n\n**-minsize** _BYTES_\n> Minimum file size."],["DESCRIPTION","**rdfind** (redundant data find) efficiently locates duplicate files across one or more directory trees using a multi-phase detection algorithm. It first groups files by size, then computes partial checksums on the first bytes of same-sized files, and finally performs full checksums only on files that still match, making it fast even on large file sets.\n\nOnce duplicates are identified, rdfind can delete them, replace them with hardlinks (saving disk space while keeping the same path), or replace them with symbolic links. A results file lists all duplicates found for manual review, and the **-dryrun** flag simulates operations without modifying the filesystem. The first file encountered in the argument order is always kept as the original."],["CAVEATS","Hardlinks only on same filesystem. Symlinks may break if original moves. Careful with deletion."],["HISTORY","**rdfind** (really delete find) was created for efficient duplicate detection. Its multi-stage algorithm handles large file sets quickly."],["INSTALL","```apt: sudo apt install rdfind```\n\n```dnf: sudo dnf install rdfind```\n\n```pacman: sudo pacman -S rdfind```\n\n```apk: sudo apk add rdfind```\n\n```zypper: sudo zypper install rdfind```\n\n```brew: brew install rdfind```\n\n```nix: nix profile install nixpkgs#rdfind```"],["SEE ALSO","[fdupes](/man/fdupes)(1), [jdupes](/man/jdupes)(1), [duff](/man/duff)(1)"]],"e":["fdupes","jdupes","duff"],"x":"rdfind [directory]"},"ruff":{"n":"ruff","t":"Extremely fast Python linter and formatter","s":[["TAGLINE","Extremely fast Python linter and formatter"],["TLDR","**Check current directory**\n\n```ruff check```\n\n**Check specific files**\n\n```ruff check [file.py] [other.py]```\n\n**Auto-fix issues**\n\n```ruff check --fix```\n\n**Format code**\n\n```ruff format```\n\n**Show fixable issues**\n\n```ruff check --show-fixes```\n\n**Watch mode**\n\n```ruff check --watch```\n\n**Select specific rules**\n\n```ruff check --select [E,F,W]```\n\n**Ignore rules**\n\n```ruff check --ignore [E501]```"],["SYNOPSIS","**ruff** [_check_] [_format_] [_--fix_] [_--select rules_] [_options_] [_paths_]"],["PARAMETERS","**check**\n> Run linting.\n\n**format**\n> Format code.\n\n**--fix**\n> Auto-fix violations.\n\n**--unsafe-fixes**\n> Apply unsafe fixes.\n\n**--select** _RULES_\n> Rules to enable.\n\n**--ignore** _RULES_\n> Rules to ignore.\n\n**--extend-select** _RULES_\n> Additional rules.\n\n**--config** _FILE_\n> Configuration file.\n\n**--show-fixes**\n> Show available fixes.\n\n**--watch**\n> Watch for changes.\n\n**--output-format** _FMT_\n> Output format (text, json, etc.).\n\n**--statistics**\n> Show statistics.\n\n**--diff**\n> Show diff for fixes."],["DESCRIPTION","**ruff** is an extremely fast Python linter written in Rust. It implements rules from Flake8, isort, pyupgrade, and other tools.\n\nSpeed is the primary advantage. Ruff is 10-100x faster than traditional Python linters. This enables real-time feedback and CI integration without delays.\n\nRule selection controls which checks run. Categories include E (pycodestyle errors), F (Pyflakes), W (warnings), I (isort), UP (pyupgrade), and many more.\n\nThe formatter is Black-compatible, providing consistent code style. It integrates with the linter for a unified toolchain.\n\nConfiguration in pyproject.toml or ruff.toml customizes behavior. Per-file ignores and extends support complex project needs.\n\nFix mode modifies files automatically. Unsafe fixes may change behavior and require separate opt-in."],["CONFIGURATION","**pyproject.toml** `[tool.ruff]`\n> Ruff configuration section in the standard Python project file, defining rule selections, line length, per-file ignores, and target Python version.\n\n**ruff.toml**\n> Standalone Ruff configuration file as an alternative to pyproject.toml.\n\n**.ruff.toml**\n> Hidden variant of the standalone configuration file, searched in parent directories."],["CAVEATS","Rust-based - requires separate installation from Python. Not all Flake8 plugins are implemented. Rapidly evolving."],["HISTORY","**Ruff** was created by **Charlie Marsh** in **2022**. It achieved immediate popularity due to its speed, replacing multiple Python tools with a single fast alternative."],["INSTALL","```dnf: sudo dnf install ruff```\n\n```pacman: sudo pacman -S ruff```\n\n```apk: sudo apk add ruff```\n\n```brew: brew install ruff```\n\n```nix: nix profile install nixpkgs#ruff```"],["SEE ALSO","[flake8](/man/flake8)(1), [black](/man/black)(1), [pylint](/man/pylint)(1), [isort](/man/isort)(1)"]],"e":["flake8","black","pylint","isort"],"x":"ruff check"},"samba":{"n":"samba","t":"SMB/CIFS file sharing and Active Directory server","s":[["TAGLINE","SMB/CIFS file sharing and Active Directory server"],["TLDR","**Start the Samba AD DC daemon** in the foreground\n\n```samba -i```\n\n**Start with a specific configuration file**\n\n```samba -s [/etc/samba/smb.conf]```\n\n**Start with debug logging**\n\n```samba -d [3]```\n\n**Check Samba version**\n\n```samba --version```\n\n**Start as a classic NT4-style domain controller**\n\n```samba --option=\"server role=classic primary domain controller\"```"],["SYNOPSIS","**samba** [_-D_] [_-i_] [_-M model_] [_--no-process-group_] [_-d debuglevel_] [_-s configfile_]"],["PARAMETERS","**-D**, **--daemon**\n> Run as a daemon (background process)\n\n**-i**, **--interactive**\n> Run interactively in the foreground\n\n**-M** _model_, **--model**=_model_\n> Select process model: single, standard, prefork, thread\n\n**-d** _level_, **--debuglevel**=_level_\n> Set debug logging level (0-10)\n\n**-s** _file_, **--configfile**=_file_\n> Use alternate configuration file\n\n**--no-process-group**\n> Don't create a new process group\n\n**-V**, **--version**\n> Print version information"],["DESCRIPTION","**Samba** is a software suite that implements the SMB/CIFS protocol, allowing Unix systems to share files and printers with Windows clients. The samba daemon provides Active Directory Domain Controller services.\n\nThe suite consists of several components: **smbd** handles file and print services, **nmbd** provides NetBIOS name resolution and browsing, **winbindd** handles user and group lookups, and **samba** itself acts as an AD domain controller when configured.\n\nConfiguration is managed through **/etc/samba/smb.conf**, which defines shares, authentication, and network settings. Samba can integrate with existing Windows domains or serve as a complete replacement for Windows Server AD infrastructure."],["CONFIGURATION","**/etc/samba/smb.conf**\n> Main Samba configuration file defining shares, authentication methods, network settings, and domain controller parameters.\n\n**/var/lib/samba/**\n> Samba state directory containing databases for user accounts, group mappings, and domain data."],["CAVEATS","Running Samba as an AD DC requires careful DNS and Kerberos configuration. The samba daemon (AD DC mode) and smbd/nmbd (file server mode) are mutually exclusive configurations. Firewall rules must allow ports 139, 445 for SMB, and 88, 389, 636 for AD services."],["HISTORY","Samba was created by **Andrew Tridgell** in **1992** as a reverse-engineered implementation of SMB. Originally named \"smbserver,\" it was renamed Samba after trademark concerns. Active Directory support was added in version 4.0 (**2012**), enabling full domain controller functionality."],["INSTALL","```apt: sudo apt install samba-ad-dc```\n\n```dnf: sudo dnf install samba-dc```\n\n```pacman: sudo pacman -S samba```\n\n```apk: sudo apk add samba-dc```\n\n```zypper: sudo zypper install samba-ad-dc```\n\n```brew: brew install samba```\n\n```nix: nix profile install nixpkgs#samba```"],["SEE ALSO","[smbclient](/man/smbclient)(1)"]],"e":["smbclient"],"x":"samba -i"},"sfdk-undeploy":{"n":"sfdk-undeploy","t":"Remove deployed apps from devices","s":[["TAGLINE","Remove deployed apps from devices"],["TLDR","**Remove deployed application**\n\n```sfdk undeploy```"],["SYNOPSIS","**sfdk undeploy** [_options_]"],["DESCRIPTION","**sfdk undeploy** removes a previously deployed application from the target device. Part of Sailfish SDK."],["SEE ALSO","[sfdk](/man/sfdk)(1), [sfdk-deploy](/man/sfdk-deploy)(1)"]],"e":["sfdk","sfdk-deploy"],"x":"sfdk undeploy"},"soup":{"n":"soup","t":"Fine-tune and post-train large language models from one YAML config","s":[["TAGLINE","Fine-tune and post-train large language models from one YAML config"],["TLDR","**Install** the light CLI (no PyTorch) or the training stack\n\n```pip install soup-cli```\n\n```pip install \"soup-cli[train]\"```\n\nCreate a config from a **template**\n\n```soup init --template chat```\n\n**Train** from a YAML config\n\n```soup train --config soup.yaml```\n\n**Chat** with a fine-tuned model or adapter\n\n```soup chat --model ./output```\n\nRun **batch inference**\n\n```soup infer --model ./output --input prompts.jsonl```\n\n**Export** to GGUF for Ollama / llama.cpp\n\n```soup export --model ./output --format gguf --quant q4_k_m```\n\nStart an **OpenAI-compatible** server\n\n```soup serve --model ./output --port 8000```\n\nCheck GPU, deps, and environment\n\n```soup doctor```"],["SYNOPSIS","**soup** [_global-options_] _command_ [_args_]"],["PARAMETERS","**init** [**--template** _name_]\n> Create a **soup.yaml** config interactively or from a template (**chat**, **code**, **tool-calling**, **medical**, **reasoning**, **vision**, compliance templates such as **hipaa** / **soc2**, and others).\n\n**train** **--config** _file_\n> Run supervised or preference fine-tuning (SFT, DPO, KTO, ORPO, SimPO, GRPO, PPO, and more) from YAML. Handles LoRA/QLoRA, batch size, quantization, and multi-GPU when the **[train]** extra is installed. Optional layer streaming (**stream_layers: true**) keeps the frozen base out of VRAM for small GPUs.\n\n**chat** **--model** _path_\n> Interactive chat against a local model or adapter directory.\n\n**infer** **--model** _path_ **--input** _file_\n> Batch inference over prompts (JSONL and related formats).\n\n**serve** **--model** _path_ [**--port** _n_] [**--backend** _name_]\n> Serve an OpenAI-compatible HTTP API (optional backends include **vllm**, **sglang**).\n\n**merge** **--adapter** _path_\n> Merge a LoRA adapter into the base model weights.\n\n**export** **--model** _path_ **--format** _fmt_\n> Export for deployment (**gguf**, **onnx**, **tensorrt**, **awq**, **gptq**, and others).\n\n**push** **--model** _path_ **--repo** _user/name_\n> Upload a model or adapter to the Hugging Face Hub.\n\n**data** _subcommand_ ...\n> Dataset tools: **inspect**, **validate**, **convert**, **dedup**, **split**, **generate**, **forge**, and many quality / PII / contamination helpers.\n\n**eval** _subcommand_ ...\n> Evaluation: benchmarks, custom JSONL tasks, LLM-as-judge, gates, and comparison.\n\n**ship** **--base** _model_ **--adapter** _path_ **--task-eval** _file_\n> Emit a SHIP / DON'T-SHIP gate (task win plus regression suite). Exit codes: **0** ship, **2** don't ship, **3** bad flags, **1** runtime error.\n\n**doctor**\n> Diagnose GPU, drivers, Python packages, and environment.\n\n**recipes** **list**|**show**|**use**|**search**\n> Browse and materialize ready-made training recipes.\n\n**version** [**--full**] [**--json**]\n> Print the installed Soup version.\n\n**--log-level** **quiet**|**normal**|**verbose**|**debug**\n> Global logging verbosity (Rich-formatted output)."],["DESCRIPTION","**soup** is the command-line interface for **Soup** (PyPI package **soup-cli**): a Python toolkit that turns LLM fine-tuning into a single config file and a few subcommands. You describe base model, data, task type, and training knobs in **soup.yaml**, then run **soup train**. The same binary covers data prep, evaluation, export, local serving, adapter management, and shipping gates.\n\nThe default **pip install soup-cli** is intentionally light (CLI, config, and data tools without PyTorch). Fine-tuning needs **pip install \"soup-cli[train]\"** (or **[all]** for the full optional stack). Training can run on CUDA GPUs, Apple Silicon (MPS), or CPU for smoke tests; optional extras enable Unsloth, MLX, serving backends, UI, and more.\n\nSupported data formats include Alpaca, ShareGPT, ChatML, preference pairs (DPO-style), KTO, vision, audio, and pretraining text. Models load through Hugging Face **AutoModelForCausalLM**, so most Hub text-generation checkpoints work without custom glue code."],["CONFIGURATION","Primary file: **soup.yaml** (path passed to **--config**). Core fields include **base** (model id or path), **task** (for example **sft**), **data.train** / format / split, **training** (epochs, learning rate, LoRA, quantization, optional **stream_layers**), and **output**. Schema details live in the project docs under **docs/** and **config/schema.py**."],["CAVEATS","Fine-tuning requires substantial disk, RAM, and preferably a GPU; an 8B QLoRA run typically wants on the order of 8 GB+ VRAM without layer streaming. The bare **soup-cli** install cannot train until the **[train]** extra is installed. Layer streaming is still marked BETA. Prefer double quotes around extras in shells that treat **[train]** as a glob (**pip install \"soup-cli[train]\"**). Some advanced commands need optional extras (**[serve]**, **[ui]**, **[mcp]**, and others)."],["HISTORY","**Soup** is an Apache-2.0 open-source project maintained as **soup-cli** on PyPI. Recent releases focus on low-VRAM training (layer streaming for 8B models on ~4 GB laptop GPUs), preference-training losses over that path, and shipping / eval gates for CI."],["SEE ALSO","[ollama](/man/ollama)(1), [llama](/man/llama)(1), [huggingface-cli](/man/huggingface-cli)(1), [pip](/man/pip)(1)"],["RESOURCES","```[Source code](https://github.com/MakazhanAlpamys/Soup)```\n\n```[Homepage](https://trysoup.dev)```\n\n```[Documentation](https://github.com/MakazhanAlpamys/Soup/tree/main/docs)```"]],"e":["ollama","llama","huggingface-cli","pip"],"x":"pip install soup-cli"},"steamos-bootdebug":{"n":"steamos-bootdebug","t":"Configure SteamOS chainloader and kernel boot debugging","s":[["TAGLINE","Configure SteamOS chainloader and kernel boot debugging"],["TLDR","**Show** the current chainloader/kernel debug status\n\n```sudo steamos-bootdebug status```\n\n**Enable the GRUB chainloader menu** at boot (lets you pick A/B slots or edit kernel params)\n\n```sudo steamos-bootdebug menu```\n\n**Enable verbose** chainloader logging to the console\n\n```sudo steamos-bootdebug verbose```\n\n**Disable** menu and verbose output (silent boot)\n\n```sudo steamos-bootdebug quiet```\n\n**Enable kernel debug** output to the console (loud kernel messages)\n\n```sudo steamos-bootdebug kernel-debug```\n\n**Enable kernel debug** for the **next boot only**, then revert\n\n```sudo steamos-bootdebug kernel-debug-once```\n\n**Restore** the normal silent kernel boot\n\n```sudo steamos-bootdebug kernel-quiet```\n\n**Write** the chainloader log to the **EFI** partition\n\n```sudo steamos-bootdebug log enable```\n\n**Show** the contents of the on-disk chainloader log\n\n```sudo steamos-bootdebug log show```"],["SYNOPSIS","**steamos-bootdebug** _command_ [_image_]"],["PARAMETERS","**menu**\n> Enable the chainloader boot menu, allowing GRUB editing and A/B slot selection at boot.\n\n**verbose**\n> Send chainloader log output to the system console.\n\n**quiet**\n> Disable both the menu and verbose console output.\n\n**log enable**\n> Persist chainloader logs to a file on the EFI System Partition (**/esp**).\n\n**log disable**\n> Stop persisting chainloader logs to the filesystem.\n\n**log show**\n> Print the contents of the chainloader log file.\n\n**kernel-debug** [_image_]\n> Enable kernel debug output (no **quiet** parameter, debug messages on console) for the named slot _image_, or both if omitted.\n\n**kernel-debug-once** [_image_]\n> Enable kernel debug for **only the next boot**, then automatically revert.\n\n**kernel-quiet** [_image_]\n> Restore the standard silent kernel boot.\n\n**status**\n> Print the current debug configuration of both slots."],["DESCRIPTION","**steamos-bootdebug** is a small administration script that ships with SteamOS 3+ on the **Steam Deck**. It surfaces controls for the SteamOS **chainloader** — the small bootloader stub that decides which of the two A/B system images to load — and for the kernel command line baked into each image.\n\nThe chainloader runs before GRUB and normally boots the active slot silently. **steamos-bootdebug menu** flips it into interactive mode so the user can edit GRUB entries (useful for adding **systemd.unit=rescue.target**, alternate **root=** parameters, and so on). **verbose** and **quiet** toggle whether chainloader log lines go to the console or are suppressed; **log enable**/**disable**/**show** capture those lines to a file in the EFI System Partition for post-mortem inspection.\n\nThe **kernel-debug**/**kernel-quiet** family edits the kernel command line of one or both A/B images: enabling kernel debug removes the **quiet** parameter, enables verbose printk output, and is invaluable for diagnosing display, GPU, or storage init issues. **kernel-debug-once** is the safest option — it self-reverts after a single boot, so you cannot accidentally leave the system in a noisy state."],["CAVEATS","**Read-only rootfs**: SteamOS uses an immutable root, so **steamos-bootdebug** writes to the EFI System Partition rather than **/etc**. Boot menu output is rendered on whichever display the firmware picks; if you boot the Steam Deck while docked and undock during the menu, the menu may be lost on the internal panel and you must blindly navigate or reboot. The optional _image_ argument selects the **A** or **B** slot — if omitted, both slots are modified."],["HISTORY","**steamos-bootdebug** is shipped by Valve in **SteamOS 3** (the Arch-based Linux distribution running on the Steam Deck since **February 2022**). It is part of the **steamos-customizations** family of scripts that adapt a stock Arch install to the read-only A/B image model used on the Deck."],["SEE ALSO","[grub](/man/grub)(8), [steamos-readonly](/man/steamos-readonly)(1), [bootctl](/man/bootctl)(1), [efibootmgr](/man/efibootmgr)(8)"]],"e":["grub","steamos-readonly","bootctl","efibootmgr"],"x":"sudo steamos-bootdebug status"},"stormlock":{"n":"stormlock","t":"simple centralized lock with lease expiration","s":[["TAGLINE","simple centralized lock with lease expiration"],["TLDR","**Acquire a lease** on a resource\n\n```stormlock acquire [resource]```\n\n**Acquire with explicit TTL**\n\n```stormlock acquire --ttl [60s] [resource]```\n\n**Release a lease** using its id\n\n```stormlock release [resource] [lease-id]```\n\n**Renew an existing lease**\n\n```stormlock renew [resource] [lease-id]```\n\n**Show the current lease** for a resource\n\n```stormlock current [resource]```\n\n**Print only the lease id**\n\n```stormlock current --id-only [resource]```\n\n**Check whether a lease is still held**\n\n```stormlock is-held [resource] [lease-id]```"],["SYNOPSIS","**stormlock** _subcommand_ [_options_] _resource_ [_lease-id_]"],["PARAMETERS","**acquire** _RESOURCE_ [**--ttl** _DUR_]\n> Attempt to acquire a lease. On success prints the lease id.\n\n**release** _RESOURCE_ _LEASE_ID_\n> Release the given lease on the given resource.\n\n**renew** _RESOURCE_ _LEASE_ID_ [**--ttl** _DUR_]\n> Extend the lease duration.\n\n**current** _RESOURCE_ [**--id-only**]\n> Show the current lease holder and id. `--id-only` prints only the id.\n\n**is-held** _RESOURCE_ _LEASE_ID_\n> Exit with status 0 if the given lease is still active, non-zero otherwise.\n\n**-c**, **--config** _FILE_\n> Path to the configuration file. Overrides `STORMLOCK_CONFIG`.\n\n**--ttl** _DURATION_\n> Time-to-live for the lease (e.g. `30s`, `5m`, `1h`). Defaults come from the config file.\n\n**--help**\n> Show help."],["CONFIGURATION","**~/.config/stormlock/config.toml**\n> Default configuration location. Uses INI/TOML syntax with `[default]`, per-resource, and `[backend.<name>]` sections. Common keys: `ttl`, `principal`, `backend`.\n\n**STORMLOCK_CONFIG**\n> Overrides the configuration file path."],["BACKENDS","Supported storage backends:\n\n- **etcd** — uses etcd leases.\n- **redis** — uses Redis keys with TTL.\n- **dynamodb** — AWS DynamoDB table with conditional writes.\n- **postgresql** — PostgreSQL row with expiry column.\n\nCustom backends can be added by implementing the `stormlock.Backend` interface and registering it via Python entry points."],["DESCRIPTION","**stormlock** is a simple centralized lock manager aimed at human operators and scripts that need occasional coordination (deployments, migrations, cron runs). Each lock is associated with a principal (who holds it) and a lease with a TTL that auto-expires, so a crashed holder cannot deadlock the resource.\n\nTypical flow: `stormlock acquire <resource>` prints a lease id; run the protected work; `stormlock release <resource> <lease-id>` when done. For long-running work, periodically call `stormlock renew` to extend the lease."],["CAVEATS","Requires a reachable backend. Locks are advisory — cooperating clients must all check the lock before acting. TTL must be tuned to be longer than the worst-case work unit (or renewed during it). The tool is written in Python; install via `pip install stormlock[etcd,redis,...]` with the backend extras you need."],["HISTORY","**stormlock** is maintained by **Thayne McCombs** (tmccombs) on GitHub. It is released under the Apache 2.0 license."],["SEE ALSO","[flock](/man/flock)(1), [redis-cli](/man/redis-cli)(1), [etcdctl](/man/etcdctl)(1), [consul](/man/consul)(1)"]],"e":["flock","redis-cli","etcdctl","consul"],"x":"stormlock acquire [resource]"},"svgr":{"n":"svgr","t":"Transform SVG files into React components","s":[["TAGLINE","Transform SVG files into React components"],["TLDR","**Convert SVG to React**\n\n```svgr [icon.svg]```\n\n**Output to file**\n\n```svgr [icon.svg] -o [Icon.jsx]```\n\n**Convert directory**\n\n```svgr [icons/] -d [components/]```\n\n**With TypeScript**\n\n```svgr --typescript [icon.svg]```\n\n**As named export**\n\n```svgr --export-type named [icon.svg]```\n\n**With custom template**\n\n```svgr --template [template.js] [icon.svg]```"],["SYNOPSIS","**svgr** [_-o file_] [_-d dir_] [_--typescript_] [_options_] _input_"],["PARAMETERS","**-o** _FILE_\n> Output file.\n\n**-d** _DIR_\n> Output directory.\n\n**--typescript**\n> TypeScript output.\n\n**--export-type** _TYPE_\n> Export style.\n\n**--template** _FILE_\n> Custom template.\n\n**--icon**\n> Icon mode (1em size)."],["DESCRIPTION","**svgr** transforms SVG files into React components, generating JSX code that can be imported and used directly in React applications. This eliminates the need to manage separate SVG asset files and enables dynamic styling and prop-based customization of icons and illustrations.\n\nThe tool supports TypeScript output with proper type definitions, named and default exports, and various component patterns. Batch conversion processes entire directories of SVG files into a component library with an automatic index file. Custom templates control the exact shape of generated component code.\n\nSVGR integrates with webpack, Rollup, and other bundlers as a loader/plugin, and also works as a standalone CLI or Node.js API. It optionally runs SVGO optimization before generating components."],["CAVEATS","React specific. Node.js required. JSX output."],["HISTORY","**SVGR** was created to transform SVG files into React components, simplifying icon usage in React applications."],["SEE ALSO","[svgo](/man/svgo)(1), [react-scripts](/man/react-scripts)(1)"]],"e":["svgo","react-scripts"],"x":"svgr [icon.svg]"},"systeroid":{"n":"systeroid","t":"Powerful alternative to sysctl with TUI","s":[["TAGLINE","Powerful alternative to sysctl with TUI"],["TLDR","**List all kernel parameters**\n\n```systeroid -A```\n\n**Display parameters in tree format**\n\n```systeroid -T```\n\n**Set a kernel parameter**\n\n```systeroid [kernel.hostname]=[myhost]```\n\n**Search parameters matching a pattern**\n\n```systeroid -r [net.ipv4]```\n\n**Display parameter values with documentation**\n\n```systeroid -E [kernel.hostname]```\n\n**Output parameters as JSON**\n\n```systeroid -A --output json```\n\n**Launch the TUI**\n\n```systeroid-tui```"],["SYNOPSIS","**systeroid** [_options_] [_variable_[**=**_value_] ...]"],["DESCRIPTION","**systeroid** is a more powerful alternative to **sysctl** for managing kernel parameters at runtime via procfs. It provides both a CLI and TUI interface, supports tree-like display, JSON output, regex pattern matching, and can fetch official Linux kernel documentation for parameters."],["OPTIONS","**-A**, **--all**\n> List all kernel parameters.\n\n**-T**, **--tree**\n> Display parameters in a tree hierarchy.\n\n**-r**, **--pattern** _REGEX_\n> Filter parameters matching a regex pattern.\n\n**-E**, **--explain**\n> Show the kernel documentation for a parameter.\n\n**--output** _FORMAT_\n> Output format (default or json).\n\n**-p**, **--load** _FILE_\n> Load parameter values from a configuration file.\n\n**-q**, **--quiet**\n> Suppress normal output.\n\n**-N**, **--names**\n> Print only parameter names, not values."],["HISTORY","**systeroid** was created by **Orhun Poyraz** (orhun) and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S systeroid```\n\n```apk: sudo apk add systeroid```\n\n```nix: nix profile install nixpkgs#systeroid```"],["SEE ALSO","[sysctl](/man/sysctl)(1), [proc](/man/proc)(5), [tuned](/man/tuned)(1)"]],"e":["sysctl","proc","tuned"],"x":"systeroid -A"},"tlmgr-install":{"n":"tlmgr-install","t":"Install TeX Live packages","s":[["TAGLINE","Install TeX Live packages"],["TLDR","**Install package**\n\n```tlmgr install [package]```\n\n**Install multiple packages**\n\n```tlmgr install [package1] [package2]```\n\n**Install with dependencies**\n\n```tlmgr install --with-doc --with-src [package]```\n\n**Dry run**\n\n```tlmgr install --dry-run [package]```"],["SYNOPSIS","**tlmgr install** [_options_] _package_..."],["PARAMETERS","**--with-doc**\n> Include documentation.\n\n**--with-src**\n> Include source files.\n\n**--dry-run**\n> Show what would be installed.\n\n**--file**\n> Package is a local file.\n\n**--reinstall**\n> Reinstall package."],["DESCRIPTION","**tlmgr install** installs TeX Live packages from CTAN repositories. Resolves dependencies automatically. Part of TeX Live Manager for managing LaTeX packages and fonts."],["SEE ALSO","[tlmgr-remove](/man/tlmgr-remove)(1), [tlmgr-update](/man/tlmgr-update)(1), [tlmgr](/man/tlmgr)(1)"]],"e":["tlmgr-remove","tlmgr-update","tlmgr"],"x":"tlmgr install [package]"},"tmt-run":{"n":"tmt-run","t":"Execute tmt test steps","s":[["TAGLINE","Execute tmt test steps"],["TLDR","**Run** all test steps\n\n```tmt run```\n\n**Discover** what tests would run\n\n```tmt run discover -v```\n\nRun with **provisioning** options\n\n```tmt run -a provision -h [container] -i [fedora:rawhide]```\n\nRun selected **plans** and tests\n\n```tmt run plan -n [/plan/name] test -n [/test/name]```\n\nShow **results** in browser\n\n```tmt run -l report -h html -o```\n\nRun with **context**\n\n```tmt run -c [key=value]```\n\nRun **interactively**\n\n```tmt run -a execute -h tmt --interactive```\n\n**Dry run** with verbose\n\n```tmt run -n -vvv```"],["SYNOPSIS","**tmt run** [_OPTIONS_] [_STEP_...]"],["PARAMETERS","**-a, --all**\n> Run all test steps\n\n**-l, --last**\n> Use the last run\n\n**-n, --dry**\n> Dry run mode (show actions without executing)\n\n**-v, --verbose**\n> Increase verbosity (can be repeated)\n\n**-c, --context** _KEY=VALUE_\n> Set context variables\n\n**discover**\n> Show what tests would be run\n\n**provision**\n> Provision the guest (container, VM, or remote system) that will run the tests\n\n**prepare**\n> Prepare the provisioned system for testing (install packages, apply roles)\n\n**execute**\n> Run the tests\n\n**report**\n> Generate test reports\n\n**finish**\n> Clean up after testing"],["DESCRIPTION","**tmt run** executes test management tool steps for test execution. By default, all steps (discover, provision, prepare, execute, report, finish) are executed in sequence. Individual steps can be run separately or with modified options.\n\nThe command supports different provisioning methods including containers, virtual machines, and remote systems. Tests can be filtered by name, context, or plan."],["CAVEATS","Running all steps requires appropriate provisioning backend. Container-based execution needs Podman or Docker. Some steps require root privileges. Large test suites may require significant resources."],["INSTALL","```dnf: sudo dnf install tmt```\n\n```brew: brew install tmt```"],["SEE ALSO","[tmt](/man/tmt)(1), [tmt-try](/man/tmt-try)(1), [podman](/man/podman)(1)"]],"e":["tmt","tmt-try","podman"],"x":"tmt run"},"xsltproc":{"n":"xsltproc","t":"Command-line XSLT processor","s":[["TAGLINE","Command-line XSLT processor"],["TLDR","**Transform XML** using an XSLT stylesheet\n\n```xsltproc [stylesheet.xsl] [input.xml]```\n\n**Save output to a file**\n\n```xsltproc -o [output.html] [stylesheet.xsl] [input.xml]```\n\n**Pass a string parameter** to the stylesheet\n\n```xsltproc --stringparam [name] \"[value]\" [stylesheet.xsl] [input.xml]```\n\n**Pass an XPath expression** as parameter\n\n```xsltproc --param [name] \"[xpath]\" [stylesheet.xsl] [input.xml]```\n\n**Process XInclude** elements in the input\n\n```xsltproc --xinclude [stylesheet.xsl] [input.xml]```\n\n**Transform HTML** input\n\n```xsltproc --html [stylesheet.xsl] [input.html]```\n\n**Show timing information**\n\n```xsltproc --timing [stylesheet.xsl] [input.xml]```\n\n**Disable network access** for external resources\n\n```xsltproc --nonet [stylesheet.xsl] [input.xml]```"],["SYNOPSIS","**xsltproc** [_options_] [_stylesheet_] [_xml-file_]"],["PARAMETERS","**-o**, **--output** _file_\n> Write output to file instead of stdout.\n\n**--stringparam** _name_ _value_\n> Pass a string parameter to the stylesheet.\n\n**--param** _name_ _value_\n> Pass an XPath expression result as parameter.\n\n**--xinclude**\n> Process XInclude elements before transformation.\n\n**--html**\n> Treat input as HTML instead of XML.\n\n**--nonet**\n> Disable network access (for DTDs, entities, stylesheets).\n\n**--novalid**\n> Skip DTD validation of input document.\n\n**--noout**\n> Suppress output of result.\n\n**--timing**\n> Display timing for parsing and transformation.\n\n**--profile**\n> Output profiling information for optimization.\n\n**--maxdepth** _value_\n> Maximum template stack depth (default: 3000).\n\n**--nowrite**\n> Refuse to write to any file.\n\n**--nomkdir**\n> Refuse to create directories.\n\n**-v**, **--verbose**\n> Output each step during processing.\n\n**-V**, **--version**\n> Show libxml and libxslt versions."],["DESCRIPTION","**xsltproc** is a command-line XSLT processor for transforming XML documents using XSLT stylesheets. It is part of libxslt, the XSLT C library for GNOME, but operates independently of the GNOME desktop.\n\nIf the input XML document contains a stylesheet processing instruction, xsltproc can automatically use the referenced stylesheet without command-line specification.\n\nThe tool supports XSLT 1.0 and EXSLT extensions. Output can be XML, HTML, or text depending on the stylesheet's **xsl:output** directive."],["CAVEATS","xsltproc only supports XSLT 1.0; XSLT 2.0 and 3.0 features are not available. For XSLT 2.0+, consider Saxon or other processors. Return codes indicate specific error types (4 = stylesheet parse error, 5 = stylesheet error, 6 = document error)."],["HISTORY","xsltproc was developed by **Daniel Veillard** as part of the **libxslt** library for the GNOME project, with the first release around **2001**. It is built on libxml2 for XML parsing. The tool has become a standard for command-line XSLT processing on Unix-like systems and is included in most Linux distributions."],["INSTALL","```apt: sudo apt install xsltproc```\n\n```dnf: sudo dnf install libxslt```\n\n```pacman: sudo pacman -S libxslt```\n\n```apk: sudo apk add libxslt```\n\n```brew: brew install libxslt```\n\n```nix: nix profile install nixpkgs#libxslt```"],["SEE ALSO","[xmlstarlet](/man/xmlstarlet)(1), [xmllint](/man/xmllint)(1), [saxon](/man/saxon)(1), [xalan](/man/xalan)(1)"]],"e":["xmlstarlet","xmllint","saxon","xalan"],"x":"xsltproc [stylesheet.xsl] [input.xml]"},"zgen":{"n":"zgen","t":"Lightweight Zsh plugin manager","s":[["TAGLINE","Lightweight Zsh plugin manager"],["TLDR","**Load Oh My Zsh**\n\n```zgen oh-my-zsh```\n\n**Load Oh My Zsh plugin**\n\n```zgen oh-my-zsh plugins/[git]```\n\n**Load plugin from GitHub**\n\n```zgen load [zsh-users/zsh-autosuggestions]```\n\n**Load theme**\n\n```zgen oh-my-zsh themes/[robbyrussell]```\n\n**Save configuration and generate init script**\n\n```zgen save```\n\n**Update all plugins**\n\n```zgen update```\n\n**Reset and regenerate**\n\n```zgen reset```"],["SYNOPSIS","**zgen** _command_ [_arguments_]"],["SUBCOMMANDS","**oh-my-zsh** [_plugin_or_theme_]\n> Load Oh My Zsh or specific component\n\n**load** _repo_ [_file_]\n> Load plugin from GitHub repository\n\n**save**\n> Save current configuration to init script\n\n**update**\n> Update all plugins\n\n**reset**\n> Delete generated init script (regenerates on next load)\n\n**list**\n> List loaded plugins\n\n**selfupdate**\n> Update zgen itself\n\n**clone** _repo_\n> Clone repository without loading"],["DESCRIPTION","**zgen** is a lightweight plugin manager for Zsh. It generates a static init script from your plugin configuration, making shell startup fast because plugins don't need to be loaded dynamically each time.\n\nThe typical workflow:\n1. Define plugins in **.zshrc** using zgen commands\n2. Run **zgen save** to generate the init script\n3. Subsequent shell starts load the pre-generated script\n\nzgen supports Oh My Zsh, Prezto, and any GitHub-hosted Zsh plugin. It handles cloning repositories and sourcing the appropriate files.\n\nConfiguration example in .zshrc:\n```\nsource \"${HOME}/.zgen/zgen.zsh\"\nif ! zgen saved; then\n  zgen oh-my-zsh\n  zgen oh-my-zsh plugins/git\n  zgen load zsh-users/zsh-syntax-highlighting\n  zgen save\nfi\n```"],["CAVEATS","After changing plugins in .zshrc, run **zgen reset** and restart the shell to regenerate the init script.\n\nzgen development is less active. Consider **zinit** or **sheldon** for actively maintained alternatives.\n\nPlugin updates require manual **zgen update**. No automatic update mechanism."],["SEE ALSO","[zsh](/man/zsh)(1), [zplug](/man/zplug)(1), [antigen](/man/antigen)(1)"]],"e":["zsh","zplug","antigen"],"x":"zgen oh-my-zsh"}});
