window.LCL_BUCKET(125,{"addftinfo":{"n":"addftinfo","t":"Add font metric information to troff font files","s":[["TAGLINE","Add font metric information to troff font files"],["TLDR","**Add** font metrics to a troff font file\n\n```addftinfo [resolution] [unitwidth] [font] < [input.tfm] > [output.tfm]```\n\nAdd metrics for **300 dpi** resolution\n\n```addftinfo 300 300 TR < times-roman.tfm > TR.tfm```"],["SYNOPSIS","**addftinfo** [_-v_] _resolution_ _unitwidth_ _font_ < _input_ > _output_"],["DESCRIPTION","**addftinfo** is a groff (GNU troff) utility that adds computed font metric information to troff font files. It reads a font file, calculates additional metrics needed for typesetting, and outputs an enhanced font file.\n\nThe tool is typically used as part of the font installation process for groff, adding metrics like character heights and depths that aren't always present in basic font files."],["PARAMETERS","**resolution**\n> Device resolution in dots per inch\n\n**unitwidth**\n> Point size at which the font unitwidth was designed\n\n**font**\n> Groff font name (R, I, B, BI, etc.)\n\n**-v**, **--version**\n> Print version information"],["CAVEATS","The input must be a valid troff font description file. Output metrics are computed approximations and may not perfectly match the actual font rendering. This tool is specific to the groff typesetting system."],["HISTORY","**addftinfo** is part of GNU groff, which was developed starting in **1989** as a free replacement for the Unix troff typesetting system. The utility was created to simplify font installation."],["INSTALL","```apt: sudo apt install groff```\n\n```dnf: sudo dnf install groff```\n\n```pacman: sudo pacman -S groff```\n\n```apk: sudo apk add groff```\n\n```zypper: sudo zypper install groff```\n\n```brew: brew install groff```\n\n```nix: nix profile install nixpkgs#groff```"],["SEE ALSO","[groff](/man/groff)(1), [troff](/man/troff)(1), [afmtodit](/man/afmtodit)(1), [tfmtodit](/man/tfmtodit)(1)"]],"e":["groff","troff","afmtodit","tfmtodit"],"x":"addftinfo [resolution] [unitwidth] [font] < [input.tfm] > [output.tfm]"},"agy":{"n":"agy","t":"Google Antigravity IDE launcher and command-line client","s":[["TAGLINE","Google Antigravity IDE launcher and command-line client"],["TLDR","**Launch Antigravity** in the current directory\n\n```agy .```\n\n**Open a specific project folder** in Antigravity\n\n```agy [path/to/project]```\n\n**Open a specific file** for editing\n\n```agy [path/to/file]```\n\n**Open a file at a specific line**\n\n```agy --goto [path/to/file]:[line]```\n\n**Start a new window** rather than reusing an existing one\n\n```agy --new-window [path/to/project]```\n\n**Show installed version**\n\n```agy --version```"],["SYNOPSIS","**agy** [_options_] [_path_ ...]"],["PARAMETERS","**-n**, **--new-window**\n> Open a new window instead of reusing an existing one.\n\n**-r**, **--reuse-window**\n> Reuse the most recently active window.\n\n**-g**, **--goto** _file:line[:column]_\n> Open the file at the given line (and optional column).\n\n**-d**, **--diff** _file1 file2_\n> Open a diff editor comparing two files.\n\n**-a**, **--add** _folder_\n> Add the folder to the most recently active window.\n\n**-w**, **--wait**\n> Wait for the files to be closed before returning.\n\n**--user-data-dir** _DIR_\n> Use a custom directory for user data.\n\n**--extensions-dir** _DIR_\n> Use a custom location for installed extensions.\n\n**--list-extensions**\n> List installed extensions and exit.\n\n**--install-extension** _EXT_\n> Install or update the given extension.\n\n**--uninstall-extension** _EXT_\n> Uninstall the given extension.\n\n**-v**, **--version**\n> Print version information and exit.\n\n**-h**, **--help**\n> Print help and exit."],["DESCRIPTION","**agy** is the command-line launcher for **Google Antigravity**, an AI-first development environment built on the Visual Studio Code foundation. It opens files, folders, and workspaces in the Antigravity editor and provides access to its agentic coding features, including the **Gemini**-powered agent panel and the Manager surface for coordinating multiple AI agents.\n\nThe launcher follows the conventions of VS Code style CLIs: paths passed as positional arguments are opened as either files or folders, and flags control window behavior, extensions, and user-data directories. On macOS it is typically installed via the \"Install 'agy' command in PATH\" action from inside Antigravity; on Linux distributions the binary may also be exposed as **antigravity**, in which case a symlink such as **/usr/local/bin/agy → /usr/bin/antigravity** is commonly used to satisfy tools that detect Antigravity by the **agy** name.\n\nIn addition to opening the GUI, **agy** is the integration point for the Gemini CLI and other tools that delegate edits or sessions to Antigravity. It does not run a headless agent itself; for that, Antigravity exposes separate companion binaries."],["CAVEATS","On some Linux installations the binary is named **antigravity**; create a symlink to **agy** if other tooling (for example the Gemini CLI) expects that name. The CLI is in active development and flag behavior closely mirrors VS Code, so subtle differences may appear between releases."],["HISTORY","Antigravity was introduced by **Google** in **November 2025** as an \"agent-first\" IDE fork of Visual Studio Code, with the **agy** launcher shipped alongside the desktop application for Linux, macOS, and Windows. The tool is closely tied to the Gemini model family and to Google's broader investment in agentic developer tools."],["SEE ALSO","[code](/man/code)(1), [gemini](/man/gemini)(1), [claude](/man/claude)(1)"]],"e":["code","gemini","claude"],"x":"agy ."},"aptitude-run-state-bundle":{"n":"aptitude-run-state-bundle","t":"Replay aptitude state from a bundle","s":[["TAGLINE","Replay aptitude state from a bundle"],["TLDR","**Run** aptitude with state bundle\n\n```aptitude-run-state-bundle [bundle.tar.bz2]```\n\nRun with **specific aptitude args**\n\n```aptitude-run-state-bundle [bundle.tar.bz2] -- search [pattern]```"],["SYNOPSIS","**aptitude-run-state-bundle** _bundle_ [_-- aptitude-args_]"],["DESCRIPTION","**aptitude-run-state-bundle** runs aptitude using state from a previously created bundle. This allows reproducing exact package states for debugging or testing dependency resolution.\n\nThe tool extracts the bundle, configures aptitude to use the extracted state, and runs aptitude with any provided arguments."],["PARAMETERS","**bundle**\n> State bundle file (.tar.bz2)\n\n**-- args**\n> Arguments to pass to aptitude"],["CAVEATS","Used for debugging purposes. The bundle's state may not match current repository state. Primarily useful for aptitude developers."],["HISTORY","**aptitude-run-state-bundle** complements aptitude-create-state-bundle, enabling developers to reproduce reported issues."],["INSTALL","```apt: sudo apt install aptitude```"],["SEE ALSO","[aptitude](/man/aptitude)(8), [aptitude-create-state-bundle](/man/aptitude-create-state-bundle)(1)"],["RESOURCES","```[Source code](https://salsa.debian.org/apt-team/aptitude)```"]],"e":["aptitude","aptitude-create-state-bundle"],"x":"aptitude-run-state-bundle [bundle.tar.bz2]"},"apx":{"n":"apx","t":"Package manager with containerized subsystem support","s":[["TAGLINE","Package manager with containerized subsystem support"],["TLDR","View documentation for managing **package managers**\n\n```apx pkgmanagers --help```\n\nView documentation for managing **stacks**\n\n```apx stacks --help```\n\nView documentation for managing **subsystems**\n\n```apx subsystems --help```\n\n**Enter** a subsystem shell\n\n```apx [subsystem] enter```\n\n**Install** a package in a subsystem\n\n```apx [subsystem] install [package]```\n\n**Run** a command in a subsystem\n\n```apx [subsystem] run [command]```\n\n**Export** an application from a subsystem to the host\n\n```apx [subsystem] export --app [application]```"],["SYNOPSIS","**apx** [_subsystem_] _command_ [_options_]"],["DESCRIPTION","**apx** is a package management utility for Vanilla OS that supports multiple package sources through containerized subsystems. It allows installing packages from different Linux distributions (Debian, Fedora, Arch, etc.) without affecting the host system.\n\nPackages are installed in isolated subsystems based on Podman containers. Applications can be exported to the host system and integrated with the desktop environment while maintaining separation from the immutable host OS."],["SUBCOMMANDS","**pkgmanagers**\n> Manage package manager configurations\n\n**stacks**\n> Manage stack configurations (base images and package managers)\n\n**subsystems**\n> Manage subsystems (container environments)\n\n**enter**\n> Enter a subsystem's shell\n\n**install**\n> Install packages in a subsystem\n\n**remove**\n> Remove packages from a subsystem\n\n**run**\n> Run a command in a subsystem\n\n**search**\n> Search for packages in a subsystem\n\n**update**\n> Update the list of available packages in a subsystem\n\n**upgrade**\n> Upgrade installed packages in a subsystem\n\n**export**\n> Export an application or binary from a subsystem to the host system\n\n**unexport**\n> Remove a previously exported application or binary"],["CONFIGURATION","**/etc/apx/apx.json**\n> System-wide configuration defining the container storage path, apnx storage path, and default stack settings.\n\n**~/.local/share/apx/**\n> Per-user directory containing stacks, subsystems, and package manager configurations."],["CAVEATS","Requires Podman for container management. First-time subsystem creation downloads container images which may be large. Exported applications run inside containers, which may have slight performance overhead."],["HISTORY","**apx** was developed as part of Vanilla OS, an immutable Linux distribution first released in **2022**. It provides a novel approach to package management that preserves system immutability while allowing users to install software from multiple Linux ecosystems."],["INSTALL","```dnf: sudo dnf install apx```\n\n```aur: yay -S apx```\n\n```nix: nix profile install nixpkgs#apx```"],["SEE ALSO","[apx-pkgmanagers](/man/apx-pkgmanagers)(1), [apx-stacks](/man/apx-stacks)(1), [apx-subsystems](/man/apx-subsystems)(1), [podman](/man/podman)(1)"],["RESOURCES","```[Source code](https://github.com/Vanilla-OS/apx)```\n\n```[Homepage](https://apx.vanillaos.org/)```\n\n```[Documentation](https://docs.vanillaos.org/docs/en/apx)```"]],"e":["apx-pkgmanagers","apx-stacks","apx-subsystems","podman"],"x":"apx pkgmanagers --help"},"archwiki-rs":{"n":"archwiki-rs","t":"Browse the Arch Linux Wiki from the terminal","s":[["TAGLINE","Browse the Arch Linux Wiki from the terminal"],["TLDR","**Search** the Arch Wiki by title\n\n```archwiki-rs search \"[query]\"```\n\n**Search** within page text content\n\n```archwiki-rs search \"[query]\" -t```\n\n**Read** a page in the terminal\n\n```archwiki-rs read-page \"[Neovim]\"```\n\n**Read** a page as Markdown\n\n```archwiki-rs read-page \"[Neovim]\" --format markdown```\n\n**Download** page and category names for offline search\n\n```archwiki-rs sync-wiki```\n\n**List** all available pages\n\n```archwiki-rs list-pages```"],["SYNOPSIS","**archwiki-rs** _command_ [_options_] [_query_]"],["DESCRIPTION","**archwiki-rs** is a command-line tool for browsing the Arch Linux Wiki from the terminal. It can search pages by title or text, render page content as plain text or Markdown, and cache pages for offline reading.\n\nThe tool provides offline-capable access to Arch Wiki documentation, useful for system administration and troubleshooting."],["PARAMETERS","**read-page** _page_\n> Fetch and display a wiki page. Use **--format** to choose the output format (e.g. markdown).\n\n**search** _query_\n> Search pages by title; add **-t** to search page text content instead.\n\n**list-pages**\n> List all available wiki pages (tree by default, **-f** for a flat list, **-c** to filter by category).\n\n**list-categories**\n> List all available wiki categories.\n\n**list-languages**\n> List the supported page languages.\n\n**sync-wiki**\n> Download the page and category names used by the search and list commands.\n\n**local-wiki**\n> Download a complete local copy of the Arch Wiki (makes many requests).\n\n**info**\n> Show application metadata and configuration, such as the cache directory.\n\n**completions** _shell_\n> Generate a shell completion file.\n\n**--json**, **--json-raw**\n> Emit machine-readable JSON output."],["CAVEATS","Content may be outdated if used offline. Formatting may differ from web version. Some wiki features not available in terminal."],["HISTORY","**archwiki-rs** was created as a Rust-based tool for Arch Linux users who prefer terminal-based documentation access."],["SEE ALSO","[man](/man/man)(1), [tldr](/man/tldr)(1), [curl](/man/curl)(1)"],["RESOURCES","```[Source code](https://github.com/lucifayr/archwiki-rs)```"]],"e":["man","tldr","curl"],"x":"archwiki-rs search \"[query]\""},"argon2":{"n":"argon2","t":"Hash passwords using the Argon2 algorithm","s":[["TAGLINE","Hash passwords using the Argon2 algorithm"],["TLDR","**Hash** a password\n\n```echo -n \"password\" | argon2 [salt] -e```\n\nHash with **Argon2id** variant\n\n```echo -n \"password\" | argon2 [salt] -id -e```\n\nHash with **custom parameters**\n\n```echo -n \"password\" | argon2 [salt] -t [3] -m [16] -p [4] -e```\n\n**Output raw bytes** instead of encoded form\n\n```echo -n \"password\" | argon2 [salt] -r```\n\n**Pin to Argon2 version 13** (current standard)\n\n```echo -n \"password\" | argon2 [salt] -v [13] -e```"],["SYNOPSIS","**argon2** _salt_ [_-d_|_-i_|_-id_] [_-t iterations_] [_-m memory_] [_-p parallelism_] [_-l length_] [_-e_|_-r_] [_-v_ _10|13_]"],["DESCRIPTION","**argon2** is a command-line tool for the Argon2 password hashing algorithm, winner of the Password Hashing Competition in 2015. It provides strong, memory-hard password hashing resistant to GPU and ASIC attacks.\n\nThe tool supports Argon2d (data-dependent), Argon2i (data-independent), and Argon2id (hybrid) variants."],["PARAMETERS","**-d**\n> Use Argon2d (data-dependent, GPU-resistant). Default is Argon2i.\n\n**-i**\n> Use Argon2i (data-independent, side-channel resistant). This is the default if no variant flag is given.\n\n**-id**\n> Use Argon2id (hybrid; recommended for password hashing).\n\n**-t** _N_\n> Time cost (iterations). Default: _3_.\n\n**-m** _N_\n> Memory cost expressed as 2^_N_ KiB. Default: _12_ (4 MiB).\n\n**-p** _N_\n> Parallelism (threads). Default: _1_.\n\n**-l** _N_\n> Output hash length in bytes. Default: _32_.\n\n**-e**\n> Print only the encoded hash (PHC string format).\n\n**-r**\n> Print only the raw hash bytes.\n\n**-v** _10|13_\n> Argon2 algorithm version. Default: _13_.\n\n**-h**\n> Display tool usage and exit."],["CAVEATS","Salt must be provided as a positional argument and should be at least 8 random bytes. Higher memory/time costs improve security but increase computation time. Argon2id is recommended for password hashing. The CLI hashes only — there is no built-in verification mode; verify hashes from a library that understands the PHC-encoded output."],["HISTORY","**Argon2** was designed by Alex Biryukov, Daniel Dinu, and Dmitry Khovratovich, winning the Password Hashing Competition in **2015**. It's recommended by OWASP for password hashing."],["INSTALL","```apt: sudo apt install argon2```\n\n```dnf: sudo dnf install argon2```\n\n```pacman: sudo pacman -S argon2```\n\n```apk: sudo apk add argon2```\n\n```zypper: sudo zypper install argon2```\n\n```brew: brew install argon2```"],["SEE ALSO","[openssl](/man/openssl)(1), [bcrypt](/man/bcrypt)(1), [mkpasswd](/man/mkpasswd)(1)"]],"e":["openssl","bcrypt","mkpasswd"],"x":"echo -n \"password\" | argon2 [salt] -e"},"arm-none-eabi-gdb":{"n":"arm-none-eabi-gdb","t":"Debug bare-metal ARM firmware interactively.","s":[["TAGLINE","Debug bare-metal ARM firmware interactively."],["TLDR","**Start debugging an ARM firmware binary**\n\n```arm-none-eabi-gdb [firmware.elf]```\n\n**Connect to a remote target** via OpenOCD\n\n```arm-none-eabi-gdb -ex \"target remote localhost:3333\" [firmware.elf]```\n\n**Start with a command script**\n\n```arm-none-eabi-gdb -x [commands.gdb] [firmware.elf]```\n\n**Load and run firmware on target**\n\n```arm-none-eabi-gdb -ex \"target remote :3333\" -ex \"load\" -ex \"continue\" [firmware.elf]```\n\n**Start in TUI mode** with source display\n\n```arm-none-eabi-gdb -tui [firmware.elf]```"],["SYNOPSIS","**arm-none-eabi-gdb** [_options_] [_executable_]"],["PARAMETERS","**-ex** _command_\n> Execute a GDB command after loading the program.\n\n**-x** _file_\n> Execute commands from a script file.\n\n**-tui**\n> Start in Text User Interface mode with source window.\n\n**-q**, **--quiet**\n> Suppress introductory and copyright messages.\n\n**-batch**\n> Run in batch mode for non-interactive use.\n\n**-cd** _directory_\n> Change to directory before running.\n\n**-s** _file_\n> Read symbols from the specified file.\n\n**--args**\n> Pass remaining arguments to the program."],["COMMON GDB COMMANDS","**target remote** _host:port_\n> Connect to remote debug server (OpenOCD, J-Link, etc.)\n\n**load**\n> Download program to target flash/RAM.\n\n**monitor reset halt**\n> Reset target and halt at start.\n\n**break** _location_\n> Set breakpoint at function or address.\n\n**continue** / **c**\n> Continue execution.\n\n**step** / **next**\n> Step into / Step over source lines.\n\n**stepi** / **nexti**\n> Step by single instructions.\n\n**info registers**\n> Display CPU register contents.\n\n**x** _address_\n> Examine memory at address."],["DESCRIPTION","**arm-none-eabi-gdb** is the GNU Debugger configured for bare-metal ARM targets (Cortex-M, Cortex-A without OS). It enables source-level debugging of firmware running on microcontrollers and embedded systems through JTAG/SWD debug probes.\n\nThe debugger connects to hardware targets via debug servers like OpenOCD, J-Link GDB Server, or ST-Link utilities. These servers provide the bridge between GDB's remote protocol and the physical debug interface on the microcontroller.\n\nWith an ELF file containing debug symbols, arm-none-eabi-gdb provides full source-level debugging: setting breakpoints, inspecting variables, stepping through code, and examining memory and registers. It supports ARM-specific features like viewing special registers and peripheral memory regions."],["CONFIGURATION","**~/.gdbinit**\n> GDB initialization commands executed at startup, such as target connection settings and custom macros."],["CAVEATS","Requires a debug probe (J-Link, ST-Link, CMSIS-DAP) and debug server (OpenOCD, pyOCD) connected to the target. The ELF file must match the firmware on target exactly for symbols to align correctly. Hardware breakpoints are limited in number on most Cortex-M devices (typically 4-6). Optimized code may have confusing debug behavior due to reordering and inlining."],["HISTORY","The GNU Debugger (GDB) originated in **1986** as part of the GNU Project. ARM cross-compilation toolchains have included GDB builds targeting bare-metal ARM since the early **2000s**, supporting the growing embedded systems market. The **arm-none-eabi** prefix denotes the ARM architecture with no operating system (bare metal) using the Embedded ABI."],["INSTALL","```pacman: sudo pacman -S arm-none-eabi-gdb```\n\n```brew: brew install arm-none-eabi-gdb```"],["SEE ALSO","[openocd](/man/openocd)(1), [gdb](/man/gdb)(1)"]],"e":["openocd","gdb"],"x":"arm-none-eabi-gdb [firmware.elf]"},"asciiart":{"n":"asciiart","t":"Convert images to ASCII art","s":[["TAGLINE","Convert images to ASCII art"],["TLDR","Read an image from a **file** and print in ASCII\n\n```asciiart [path/to/image.jpg]```\n\nRead an image from a **URL** and print in ASCII\n\n```asciiart [www.example.com/image.jpg]```\n\nChoose the output **width**\n\n```asciiart -w [50] [path/to/image.jpg]```\n\n**Colorize** the ASCII output\n\n```asciiart -c [path/to/image.jpg]```\n\nChoose the output **format**\n\n```asciiart -f [text|html] [path/to/image.jpg]```\n\n**Invert** the character map\n\n```asciiart -i [path/to/image.jpg]```"],["SYNOPSIS","**asciiart** [_options_] _image_"],["DESCRIPTION","**asciiart** converts images to ASCII art, rendering pictures using text characters. It analyzes the brightness of each region in the image and maps it to appropriate ASCII characters.\n\nThe tool supports reading images from local files or URLs and can output plain text or HTML. Color output uses ANSI escape codes for terminal display."],["PARAMETERS","**-w, --width** _n_\n> Set the output width in characters (default: 100)\n\n**-c, --color**\n> Enable colorized output using ANSI codes\n\n**-f, --format** _type_\n> Output format: **text** (default) or **html**\n\n**-i, --invert-chars**\n> Invert the character mapping (swap light and dark)\n\n**-v, --version**\n> Display the version number\n\n**-h, --help**\n> Show the help message"],["CAVEATS","Output quality depends on the terminal font and character width. Monospace fonts produce the best results. HTML output can be viewed in web browsers for better color support. Very wide images may wrap in terminals."],["HISTORY","**asciiart** is a Ruby gem written by Stephen Schor (nodanaonlyzuul). It is packaged for Debian and Ubuntu and installed via **gem install asciiart**."],["SEE ALSO","[jp2a](/man/jp2a)(1), [img2txt](/man/img2txt)(1), [figlet](/man/figlet)(1)"],["RESOURCES","```[Source code](https://github.com/nodanaonlyzuul/asciiart)```\n\n```[Documentation](https://github.com/nodanaonlyzuul/asciiart/blob/main/README.md)```"]],"e":["jp2a","img2txt","figlet"],"x":"asciiart [path/to/image.jpg]"},"aws-s3-rb":{"n":"aws-s3-rb","t":"Remove S3 buckets","s":[["TAGLINE","Remove S3 buckets"],["TLDR","**Delete an empty S3 bucket**\n\n```aws s3 rb s3://[bucket-name]```\n\n**Force delete a bucket** and all its non-versioned objects\n\n```aws s3 rb s3://[bucket-name] --force```\n\n**Delete a bucket in a specific region**\n\n```aws s3 rb s3://[bucket-name] --region [us-east-1]```"],["SYNOPSIS","**aws s3 rb** _S3Uri_ [_--force_] [_options_]"],["DESCRIPTION","**aws s3 rb** (remove bucket) deletes an S3 bucket. By default, the bucket must be completely empty of objects before it can be deleted.\n\nUse the **--force** option to automatically delete all non-versioned objects in the bucket before removing the bucket itself. This is a destructive operation that cannot be undone."],["PARAMETERS","**S3Uri**\n> The S3 URI of the bucket to delete (s3://bucket-name)\n\n**--force**\n> Delete all objects in the bucket before deleting the bucket itself"],["CAVEATS","The **--force** option only deletes non-versioned objects. If the bucket contains versioned objects or delete markers, the bucket deletion will fail even with **--force**. For buckets with versioning enabled, use **aws s3api delete-object** with **--version-id** to remove all object versions first, or configure a lifecycle policy to expire versions. Bucket deletion is permanent and the bucket name becomes available for others to use."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws-s3](/man/aws-s3)(1), [aws-s3-mb](/man/aws-s3-mb)(1), [aws-s3-rm](/man/aws-s3-rm)(1), [aws-s3-ls](/man/aws-s3-ls)(1)"]],"e":["aws-s3","aws-s3-mb","aws-s3-rm","aws-s3-ls"],"x":"aws s3 rb s3://[bucket-name]"},"aws-secretsmanager":{"n":"aws-secretsmanager","t":"Store and manage secrets securely","s":[["TAGLINE","Store and manage secrets securely"],["TLDR","**Create a new secret**\n\n```aws secretsmanager create-secret --name [secret-name] --secret-string \"[secret-value]\"```\n\n**Get the value** of a secret\n\n```aws secretsmanager get-secret-value --secret-id [secret-name]```\n\n**List all secrets**\n\n```aws secretsmanager list-secrets```\n\n**Update a secret** value\n\n```aws secretsmanager put-secret-value --secret-id [secret-name] --secret-string \"[new-value]\"```\n\n**Delete a secret** (with recovery window)\n\n```aws secretsmanager delete-secret --secret-id [secret-name] --recovery-window-in-days [7]```\n\n**Delete a secret immediately** (no recovery)\n\n```aws secretsmanager delete-secret --secret-id [secret-name] --force-delete-without-recovery```\n\n**Rotate a secret**\n\n```aws secretsmanager rotate-secret --secret-id [secret-name]```\n\n**Generate a random password**\n\n```aws secretsmanager get-random-password --password-length [32]```"],["SYNOPSIS","**aws secretsmanager** _subcommand_ [_options_]"],["DESCRIPTION","**aws secretsmanager** is the AWS CLI interface for Amazon Secrets Manager, a service for securely storing, managing, and retrieving secrets such as database credentials, API keys, and other sensitive information.\n\nSecrets Manager enables automatic rotation of secrets, fine-grained access control through IAM policies, and audit logging via CloudTrail. It supports replication across regions for disaster recovery."],["SUBCOMMANDS","**Secret Management**\n> create-secret, delete-secret, describe-secret, get-secret-value, put-secret-value, update-secret, restore-secret\n\n**Listing**\n> list-secrets, list-secret-version-ids\n\n**Rotation**\n> rotate-secret, cancel-rotate-secret\n\n**Replication**\n> replicate-secret-to-regions, remove-regions-from-replication, stop-replication-to-replica\n\n**Access Control**\n> get-resource-policy, put-resource-policy, delete-resource-policy, validate-resource-policy\n\n**Tagging**\n> tag-resource, untag-resource\n\n**Utilities**\n> get-random-password, batch-get-secret-value, update-secret-version-stage"],["CAVEATS","Deleted secrets enter a recovery window (default 30 days) before permanent deletion; use **--force-delete-without-recovery** to skip this. Secret values have a 64KB size limit. Automatic rotation requires a Lambda function. Secrets Manager charges per secret per month plus per API call."],["HISTORY","AWS Secrets Manager was launched in **April 2018** to provide a centralized secrets management service with built-in rotation capabilities. It replaced ad-hoc solutions like storing secrets in environment variables, configuration files, or Parameter Store."],["INSTALL","```dnf: sudo dnf install aws```\n\n```pacman: sudo pacman -S aws-cli```\n\n```apk: sudo apk add aws-cli```\n\n```zypper: sudo zypper install aws-cli```"],["SEE ALSO","[aws](/man/aws)(1), [aws-ssm](/man/aws-ssm)(1), [vault](/man/vault)(1)"],["RESOURCES","```[Source code](https://github.com/aws/aws-cli)```\n\n```[Homepage](https://aws.amazon.com/cli/)```\n\n```[Documentation](https://docs.aws.amazon.com/cli/latest/reference/secretsmanager/index.html)```"]],"e":["aws","aws-ssm","vault"],"x":"aws secretsmanager create-secret --name [secret-name] --secret-string \"[secret-value]\""},"basenc":{"n":"basenc","t":"Encode and decode with multiple base encodings.","s":[["TAGLINE","Encode and decode with multiple base encodings."],["TLDR","**Encode** with base64\n\n```basenc --base64 [file]```\n\n**Encode** with base32\n\n```basenc --base32 [file]```\n\n**Decode** base64\n\n```basenc --base64 -d [file.b64]```\n\nUse **base16** (hex)\n\n```basenc --base16 [file]```\n\nUse **base2** (binary)\n\n```basenc --base2 [file]```"],["SYNOPSIS","**basenc** _encoding_ [_options_] [_file_]"],["DESCRIPTION","**basenc** is a unified encoding/decoding tool supporting multiple base encodings including base64, base32, base16 (hex), base2 (binary), z85, and others. It consolidates functionality previously split across multiple tools.\n\nThe tool is part of GNU coreutils and provides a single interface for various encoding schemes."],["PARAMETERS","**--base64**\n> Base64 encoding\n\n**--base64url**\n> Base64 URL-safe encoding\n\n**--base32**\n> Base32 encoding\n\n**--base32hex**\n> Base32 with extended hex alphabet\n\n**--base58**\n> Base58 encoding (avoids visually similar characters; useful for transcription)\n\n**--base16**\n> Base16 (hexadecimal)\n\n**--base2lsbf**\n> Base2 (binary) least significant bit first\n\n**--base2msbf**\n> Base2 (binary) most significant bit first\n\n**--z85**\n> Z85 encoding (ZeroMQ)\n\n**-d**, **--decode**\n> Decode data\n\n**-w**, **--wrap=**_cols_\n> Wrap encoded lines after _cols_ characters (default: 76, 0 = no wrap)\n\n**-i**, **--ignore-garbage**\n> Ignore non-alphabet characters"],["WORKFLOW","```bash"],["Base64 encode","basenc --base64 file.bin"],["Base32 decode","basenc --base32 -d file.b32"],["Hexadecimal","basenc --base16 file.bin"],["Binary representation","echo \"A\" | basenc --base2msbf\n```"],["CAVEATS","Exactly one encoding option is required. With **--z85**, encoding input length must be a multiple of 4 bytes and decoding input a multiple of 5. Relatively new tool (not on older systems); fall back to base64/base32 where basenc is unavailable."],["HISTORY","**basenc** was added to GNU coreutils in version **8.31** (2019) to provide a unified interface for multiple encoding schemes."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[base64](/man/base64)(1), [base32](/man/base32)(1), [b2sum](/man/b2sum)(1), [xxd](/man/xxd)(1)"],["RESOURCES","```[Source code](https://github.com/coreutils/coreutils)```\n\n```[Documentation](https://www.gnu.org/software/coreutils/manual/html_node/basenc-invocation.html)```"]],"e":["base64","base32","b2sum","xxd"],"x":"basenc --base64 [file]"},"boo":{"n":"boo","t":"Screen-style terminal multiplexer built on libghostty","s":[["TAGLINE","Screen-style terminal multiplexer built on libghostty"],["TLDR","**Create a new session** (named after the current directory by default)\n\n```boo new [session_name]```\n\n**Create a detached session** running a command\n\n```boo new [session_name] -d -- [command]```\n\n**List** all sessions\n\n```boo ls```\n\n**Reattach** to a session\n\n```boo attach [session_name]```\n\n**Open the full-screen session manager**\n\n```boo ui```\n\n**Send text to a detached session** and submit it\n\n```boo send [session_name] --text '[make]' --enter```\n\n**Wait until a session's output settles**, then read its screen\n\n```boo wait [session_name] --idle && boo peek [session_name]```\n\n**Kill** a session\n\n```boo kill [session_name]```"],["SYNOPSIS","**boo** \\<command\\> [options] [arguments]"],["COMMANDS","**new [NAME]**\n\n> Create a new session. Defaults to the current directory name. With **-d** the session starts detached, and a command to run can be passed after **--**.\n\n**attach NAME**\n\n> Reattach to a session and restore its screen contents, styles, cursor and scrollback. Aliases: **at**, **a**. Attaching steals the session from any other attached client.\n\n**ls**\n\n> List sessions. **--json** prints machine-readable output.\n\n**ui**\n\n> Full-screen session manager with keybindings for switching, creating, resizing and killing sessions.\n\n**send NAME**\n\n> Send input to a session without attaching. **--text TEXT** sends literal text, **--enter** submits it, **--key KEY1,KEY2** sends control keys such as Enter, C-c or Up.\n\n**peek NAME**\n\n> Print the session's rendered screen state. **--scrollback** includes history, **--json** adds size, cursor and title.\n\n**wait NAME**\n\n> Block until a condition is met: **--text TEXT** waits for the text to appear on screen, **--idle** waits until output has been quiet for 2 seconds, **--timeout DUR** sets a maximum wait (500ms, 2s, 1m, 4h, 1d).\n\n**rename OLD NEW**\n\n> Rename a session.\n\n**kill NAME**\n\n> End a session. **--all** ends all sessions.\n\n**help [command]**\n\n> Show the overview, details for one command, or all help pages with **--all**."],["KEY BINDINGS","The prefix key is **Ctrl-a**, following GNU screen defaults.\n\n**C-a d**\n\n> Detach from the current session; it keeps running in the background.\n\n**C-a l**\n\n> Redraw the screen.\n\n**C-a a**\n\n> Send a literal Ctrl-a to the program in the session."],["DESCRIPTION","**boo** is a terminal multiplexer in the spirit of GNU screen: one window per session, a Ctrl-a prefix key, and sessions that survive disconnects. Its distinguishing feature is that all session output is parsed through **libghostty-vt**, the terminal emulation core of the Ghostty terminal, so the full screen state - contents, SGR styles, cursor position, scrolling regions, terminal modes and window title - is reconstructed faithfully when reattaching instead of being approximated.\n\nA client/daemon architecture keeps sessions alive: the client manages the TTY while a per-session daemon owns the PTY-attached child process and feeds its output into a persistent ghostty-vt terminal stream. Terminal queries are answered even while detached, which prevents TUI programs from hanging in background sessions.\n\nEvery command except **attach** works without a terminal, making boo scriptable for automation and AI agents: **send** types into a session, **wait** blocks until output settles or a text appears, and **peek** reads the rendered screen, optionally as JSON.\n\nExit codes: 0 success, 1 error, 2 usage error, 3 session not found, 4 wait timeout."],["CAVEATS","Only one client can be attached to a session at a time; attaching steals the session, and there is no equivalent of screen's multi-display mode. Each session has exactly one window - there are no splits or tabs, the intended model is one session per task juggled via **boo ui**. The **Ctrl-a** prefix is not yet configurable, and sessions always run with TERM=xterm-256color. The project is young and not a drop-in replacement for screen."],["HISTORY","boo was released by Coder in 2026, written in Zig and built on libghostty-vt, the MIT-licensed terminal emulation library extracted from the Ghostty terminal emulator. It revives the GNU screen model of simple persistent sessions while adding accurate modern terminal emulation and automation primitives aimed at scripted and agent-driven workflows."],["SEE ALSO","[screen](/man/screen)(1), [tmux](/man/tmux)(1), [zellij](/man/zellij)(1), [abduco](/man/abduco)(1), [dtach](/man/dtach)(1), [ghostty](/man/ghostty)(1)"],["RESOURCES","```[Source code](https://github.com/coder/boo)```"]],"e":["screen","tmux","zellij","abduco","dtach","ghostty"],"x":"boo new [session_name]"},"bundle":{"n":"bundle","t":"Ruby dependency manager","s":[["TAGLINE","Ruby dependency manager"],["TLDR","**Install** dependencies\n\n```bundle install```\n\n**Update** gems\n\n```bundle update```\n\n**Execute** in bundle context\n\n```bundle exec [rake test]```\n\n**Show** gem location\n\n```bundle show [rails]```\n\n**Create** new gem\n\n```bundle gem [my_gem]```"],["SYNOPSIS","**bundle** _command_ [_options_]"],["DESCRIPTION","**bundle** (Bundler) is a dependency manager for Ruby. It ensures that the exact gem versions specified in a Gemfile are installed and loaded, preventing version conflicts and ensuring consistent environments.\n\nThe tool is essential for Ruby development and comes bundled with Ruby since version 2.6."],["PARAMETERS","**install**\n> Install gems from Gemfile\n\n**update** [_gems_]\n> Update gems\n\n**exec** _command_\n> Execute command with bundle environment\n\n**show** _gem_\n> Show gem installation path\n\n**list**\n> List installed gems\n\n**outdated**\n> Show outdated gems\n\n**clean**\n> Remove unused gems\n\n**lock**\n> Create/update Gemfile.lock\n\n**gem** _name_\n> Create new gem scaffold"],["GEMFILE","Gemfile specifies dependencies:\n```ruby\nsource 'https://rubygems.org'\n\ngem 'rails', '~> 7.0'\ngem 'pg', '>= 1.0'\ngem 'puma'\n\ngroup :development do\n  gem 'debug'\nend\n```"],["WORKFLOW","```bash"],["Install dependencies","bundle install"],["Update specific gem","bundle update rails"],["Run command with correct gem versions","bundle exec rails server\nbundle exec rake test"],["Check for outdated gems","bundle outdated"],["Remove unused gems","bundle clean\n```"],["FEATURES","- Dependency resolution\n- Version locking (Gemfile.lock)\n- Isolated gem environments\n- Gem groups (development, test, production)\n- Local gem path override\n- Git repository gems"],["CONFIGURATION","**Gemfile**\n> Specifies Ruby gem dependencies for the project. Located in the project root directory.\n\n**.bundle/config**\n> Per-project Bundler configuration including install paths, without groups, and deployment settings. Also reads from ~/.bundle/config for global defaults."],["CAVEATS","Gemfile.lock should be committed for apps (not gems). bundle exec needed to use correct versions. Local gem modifications lost on bundle install. Conflicts possible with system gems. Large projects have slow resolution."],["HISTORY","**Bundler** was created by Carl Lerche, Yehuda Katz, and André Arko around **2009** to solve Ruby's dependency management problems, becoming the standard in **2010**."],["INSTALL","```pacman: sudo pacman -S ruby-bundler```\n\n```apk: sudo apk add ruby-bundler```"],["SEE ALSO","[gem](/man/gem)(1), [ruby](/man/ruby)(1), [rake](/man/rake)(1)"],["RESOURCES","```[Source code](https://github.com/rubygems/rubygems)```\n\n```[Homepage](https://bundler.io/)```"]],"e":["gem","ruby","rake"],"x":"bundle install"},"capabilities":{"n":"capabilities","t":"Linux process privilege capabilities system","s":[["TAGLINE","Linux process privilege capabilities system"],["TLDR","**View capabilities of an executable**\n\n```getcap [/path/to/binary]```\n\n**View capabilities recursively in a directory**\n\n```getcap -r [/path/to/directory]```\n\n**Set a capability on an executable**\n\n```sudo setcap cap_net_bind_service=+ep [/path/to/binary]```\n\n**Remove all capabilities from an executable**\n\n```sudo setcap -r [/path/to/binary]```\n\n**View current process capabilities**\n\n```grep Cap /proc/self/status```\n\n**Decode capability hex values**\n\n```capsh --decode=[hex_value]```\n\n**Print current shell capabilities**\n\n```capsh --print```"],["SYNOPSIS","**getcap** [_options_] _file_...\n\n**setcap** _capabilities_ _file_\n\n**capsh** [_options_]"],["PARAMETERS","**getcap -r** _directory_\n> Recursively search for files with capabilities.\n\n**getcap -v**\n> Verbose output, display all searched files even without capabilities.\n\n**setcap** _cap=flags_ _file_\n> Set capability (flags: e=effective, p=permitted, i=inheritable). Use + to add, - to remove.\n\n**setcap -r** _file_\n> Remove all capabilities from a file.\n\n**setcap -q**\n> Quiet mode, suppress warnings.\n\n**capsh --print**\n> Print current capabilities and securebits.\n\n**capsh --decode** _hex_\n> Decode capability bitmask into human-readable names.\n\n**capsh --drop** _cap_\n> Drop a capability from the bounding set.\n\n**capsh --caps**=_cap-set_\n> Set the prevailing process capabilities.\n\n**capsh --keep**=_0|1_\n> Set the keep-capabilities flag (0=off, 1=on)."],["COMMON CAPABILITIES","**cap_net_bind_service**: Bind to ports below 1024\n**cap_net_raw**: Use raw sockets (e.g., ping)\n**cap_net_admin**: Network administration (interfaces, firewall, routing)\n**cap_sys_admin**: Broad system administration (mount, sethostname, etc.)\n**cap_sys_ptrace**: Trace arbitrary processes with ptrace\n**cap_dac_override**: Bypass file read, write, and execute permission checks\n**cap_setuid/cap_setgid**: Change UID/GID of a process\n**cap_chown**: Change file ownership arbitrarily\n**cap_kill**: Send signals to any process\n**cap_fowner**: Bypass permission checks on operations that require file owner"],["DESCRIPTION","**Linux capabilities** divide the privileges traditionally held by root into distinct units that can be independently granted to executables. Starting with kernel 2.2, instead of running an entire program as root, specific capabilities allow granting only the permissions needed.\n\nFor example, a web server that needs to bind to port 80 can be given only **cap_net_bind_service** instead of full root access. This follows the principle of least privilege, limiting damage from security vulnerabilities.\n\nCapabilities exist in three sets per thread: **permitted** (maximum capabilities available), **effective** (currently active for permission checks), and **inheritable** (preserved across execve). File capabilities are stored in extended attributes and control which capabilities are gained when a binary is executed.\n\nThe **libcap** library provides user-space tools and APIs for managing capabilities, including **getcap**, **setcap**, and **capsh**."],["CAVEATS","Capabilities are Linux-specific and not portable to other Unix systems. Not all filesystems support capability extended attributes (e.g., NFS, FAT). Some applications check for UID 0 explicitly rather than capabilities. Capability inheritance rules are complex and easy to misconfigure. Docker and containers manage capabilities separately via their runtime configuration. **cap_sys_admin** is intentionally overloaded and grants a wide range of privileges."],["HISTORY","POSIX capabilities were proposed in the POSIX.1e draft standard in the **1990s**, though the standard was never finalized. Linux implemented capabilities starting in kernel **2.2** (1999), with significant improvements in **2.6.24** (2008) adding file capabilities. The feature has become increasingly important for containerization and security-conscious system administration."],["SEE ALSO","[getcap](/man/getcap)(8), [setcap](/man/setcap)(8), [capsh](/man/capsh)(1)"]],"e":["getcap","setcap","capsh"],"x":"getcap [/path/to/binary]"},"conda-repoquery":{"n":"conda-repoquery","t":"advanced package query tool","s":[["TAGLINE","advanced package query tool"],["TLDR","**Search for packages**\n\n```conda repoquery search [package_name]```\n\n**Show package dependencies**\n\n```conda repoquery depends [package_name]```\n\n**Show what depends on a package**\n\n```conda repoquery whoneeds [package_name]```"],["SYNOPSIS","**conda** **repoquery** _subcommand_ [_options_] [_query_]"],["SUBCOMMANDS","**search**\n> Search for packages.\n\n**depends**\n> Show package dependencies.\n\n**whoneeds**\n> Show reverse dependencies."],["CONFIGURATION","**~/.condarc**\n> User-level conda configuration for channels, package settings, and solver options.\n\n**/opt/conda/.condarc**\n> System-wide conda configuration."],["DESCRIPTION","**conda repoquery** provides advanced package querying capabilities using libmamba. It can search repositories and analyze dependency relationships."],["CAVEATS","Requires libmamba solver. May not be available in older conda versions."],["SEE ALSO","[conda](/man/conda)(1), [conda-search](/man/conda-search)(1)"],["RESOURCES","```[Source code](https://github.com/conda/conda)```\n\n```[Homepage](https://docs.conda.io)```\n\n```[Documentation](https://docs.conda.io/projects/conda/en/stable/commands/index.html)```"]],"e":["conda","conda-search"],"x":"conda repoquery search [package_name]"},"cut":{"n":"cut","t":"extract sections from lines of files","s":[["TAGLINE","extract sections from lines of files"],["TLDR","**Extract** fields\n\n```cut -f [1,3] [file.txt]```\n\n**Extract** columns by character\n\n```cut -c [1-10] [file.txt]```\n\n**Custom** delimiter\n\n```cut -d [:] -f [1,7] [/etc/passwd]```\n\n**Extract** bytes\n\n```cut -b [1-5] [file.txt]```"],["SYNOPSIS","**cut** [_options_] [_file_...]"],["DESCRIPTION","**cut** removes sections from each line of files. It extracts columns by character position, byte position, or field delimiter, making it useful for processing structured text data.\n\nThe command is commonly used in shell scripts for parsing columnar data."],["PARAMETERS","**-f**, **--fields** _list_\n> Select fields (delimiter-separated)\n\n**-c**, **--characters** _list_\n> Select characters\n\n**-b**, **--bytes** _list_\n> Select bytes\n\n**-d**, **--delimiter** _char_\n> Field delimiter (default: TAB)\n\n**--output-delimiter** _string_\n> Output delimiter\n\n**-s**, **--only-delimited**\n> Don't print lines without delimiter\n\n**--complement**\n> Complement the selection"],["RANGE SPECIFICATION","- **N** - Nth item\n- **N-** - From Nth to end\n- **N-M** - From Nth to Mth\n- **-M** - From beginning to Mth\n- **N,M** - Nth and Mth"],["WORKFLOW","```bash"],["Extract first field (default TAB delimiter)","cut -f 1 data.tsv"],["Extract multiple fields","cut -f 1,3,5 data.tsv"],["CSV with custom delimiter","cut -d ',' -f 2 data.csv"],["Extract username from /etc/passwd","cut -d ':' -f 1 /etc/passwd"],["Extract characters 1-10","cut -c 1-10 file.txt"],["Extract first 20 bytes","cut -b 1-20 file.txt"],["Change output delimiter","cut -d ',' -f 1,2 --output-delimiter=':' data.csv\n```"],["COMMON USES","**Parse CSV:**\n```bash\ncut -d ',' -f 2,4 data.csv\n```\n\n**Extract columns from ps:**\n```bash\nps aux | cut -c 1-20,50-70\n```\n\n**Get usernames:**\n```bash\ncut -d ':' -f 1 /etc/passwd\n```\n\n**Field range:**\n```bash\ncut -f 2-5 data.tsv\n```"],["CAVEATS","Cannot handle varying amounts of whitespace (use awk instead). Delimiter must be single character. Cannot reorder fields (use awk). No regular expression support. Quote handling in CSV requires other tools. Only operates on bytes/characters/fields, not by column width."],["HISTORY","**cut** has been part of Unix since the early **1980s**, part of System III and later POSIX standards."],["INSTALL","```apt: sudo apt install coreutils```\n\n```dnf: sudo dnf install coreutils```\n\n```pacman: sudo pacman -S coreutils```\n\n```apk: sudo apk add coreutils```\n\n```zypper: sudo zypper install coreutils```\n\n```brew: brew install coreutils```\n\n```nix: nix profile install nixpkgs#coreutils```"],["SEE ALSO","[paste](/man/paste)(1), [awk](/man/awk)(1), [tr](/man/tr)(1), [column](/man/column)(1)"],["RESOURCES","```[Source code](https://git.savannah.gnu.org/cgit/coreutils.git)```\n\n```[Homepage](https://www.gnu.org/software/coreutils/)```\n\n```[Documentation](https://www.gnu.org/software/coreutils/manual/html_node/cut-invocation.html)```"]],"e":["paste","awk","tr","column"],"x":"cut -f [1,3] [file.txt]"},"cutycapt":{"n":"cutycapt","t":"WebKit web page to image converter","s":[["TAGLINE","WebKit web page to image converter"],["TLDR","**Capture webpage as PNG**\n\n```cutycapt --url=[https://example.com] --out=[screenshot.png]```\n\n**Capture as PDF**\n\n```cutycapt --url=[https://example.com] --out=[page.pdf]```\n\n**Set viewport size**\n\n```cutycapt --url=[url] --out=[out.png] --min-width=[1920] --min-height=[1080]```\n\n**Capture with delay**\n\n```cutycapt --url=[url] --out=[out.png] --delay=[2000]```\n\n**Disable JavaScript**\n\n```cutycapt --url=[url] --out=[out.png] --javascript=off```\n\n**Run headless with Xvfb**\n\n```xvfb-run cutycapt --url=[url] --out=[out.png]```"],["SYNOPSIS","**cutycapt** --url=_url_ --out=_file_ [_options_]"],["DESCRIPTION","**cutycapt** captures WebKit rendering of web pages to images or documents. Supports PNG, JPEG, PDF, SVG, PS, and other formats. Uses Qt WebKit engine for rendering."],["PARAMETERS","**--url** _url_\n> URL to capture (http://, file://, etc.)\n\n**--out** _file_\n> Output file path\n\n**--out-format** _format_\n> Output format (overrides extension)\n\n**--min-width** _pixels_\n> Minimum width (default: 800)\n\n**--min-height** _pixels_\n> Minimum height (default: 600)\n\n**--max-width** _pixels_\n> Maximum width\n\n**--max-height** _pixels_\n> Maximum height\n\n**--delay** _ms_\n> Delay after load (default: 0)\n\n**--max-wait** _ms_\n> Maximum wait time (default: 90000)\n\n**--zoom** _factor_\n> Zoom factor\n\n**--javascript** on|off\n> Enable/disable JavaScript\n\n**--plugins** on|off\n> Enable/disable plugins\n\n**--proxy** _host:port_\n> HTTP proxy\n\n**--header** _name:value_\n> Custom request header\n\n**--method** get|post|put\n> HTTP method"],["OUTPUT FORMATS","svg, pdf, ps, png, jpeg, tiff, gif, bmp, mng, ppm, xbm, xpm"],["CAVEATS","Requires X server. Use xvfb-run for headless environments. Based on older Qt WebKit (not Chromium)."],["INSTALL","```apt: sudo apt install cutycapt```"],["SEE ALSO","[wkhtmltoimage](/man/wkhtmltoimage)(1), [chromium](/man/chromium)(1), [firefox](/man/firefox)(1)"],["RESOURCES","```[Homepage](http://cutycapt.sourceforge.net)```"]],"e":["wkhtmltoimage","chromium","firefox"],"x":"cutycapt --url=[https://example.com] --out=[screenshot.png]"},"dog":{"n":"dog","t":"command-line DNS client with colorful output","s":[["TAGLINE","command-line DNS client with colorful output"],["TLDR","**Query A records**\n\n```dog [example.com]```\n\n**Query specific record** type\n\n```dog [example.com] [MX]```\n\n**Use specific DNS** server\n\n```dog [example.com] @[8.8.8.8]```\n\n**Query multiple record** types\n\n```dog [example.com] A AAAA MX```\n\n**Output in JSON** format\n\n```dog [example.com] --json```\n\n**Use DNS over** HTTPS\n\n```dog [example.com] --https @[https://cloudflare-dns.com/dns-query]```\n\n**Use DNS over** TLS\n\n```dog [example.com] --tls @[1.1.1.1]```\n\n**Print only the first result** (script-friendly)\n\n```dog [example.com] --short```"],["SYNOPSIS","**dog** [_options_] [_domain_] [_type_...] [@_server_]\n\nArguments are order-independent: dog recognises a record type by its name, a nameserver by the leading **@**, and treats anything else as the domain."],["PARAMETERS","**-q**, **--query**=_HOST_\n> Host name or domain to query (may also be given positionally).\n\n**-t**, **--type**=_TYPE_\n> Record type to look up: A, AAAA, CAA, CNAME, HINFO, MX, NS, PTR, SOA, SRV, TXT. Repeatable.\n\n**-n**, **--nameserver**=_ADDR_\n> Address of the nameserver to send the query to (may also be given as @_ADDR_).\n\n**--class**=_CLASS_\n> Network class of the query: IN (default), CH, or HS.\n\n**-U**, **--udp**\n> Send the query over plain UDP (the default).\n\n**-T**, **--tcp**\n> Send the query over TCP.\n\n**-S**, **--tls**\n> Send the query using DNS-over-TLS.\n\n**-H**, **--https**\n> Send the query using DNS-over-HTTPS.\n\n**--edns**=_SETTING_\n> Control EDNS: **disable**, **hide**, or **show**.\n\n**--txid**=_NUMBER_\n> Use a specific transaction ID instead of a random one.\n\n**-Z**=_TWEAKS_\n> Uncommon protocol-level tweaks, such as **aa**, **ad**, **cd**, or **bufsize=**_N_.\n\n**-1**, **--short**\n> Display nothing but the first result.\n\n**-J**, **--json**\n> Print the response as JSON.\n\n**--color**, **--colour**=_WHEN_\n> When to colourise output: **always**, **automatic**, or **never**.\n\n**--seconds**\n> Print durations as seconds instead of human-readable time units.\n\n**--time**\n> Print the time the response took to arrive.\n\n**--help**\n> Display help information.\n\n**-v**, **--version**\n> Display version information."],["DESCRIPTION","**dog** is a command-line DNS client with colorful output and modern features. It serves as an alternative to dig with a more user-friendly interface and support for encrypted DNS protocols.\n\nThe tool supports DNS over HTTPS (DoH) and DNS over TLS (DoT), enabling secure DNS queries. Output is colorized by default for readability, with JSON output available for scripting.\n\ndog provides a simplified interface compared to dig while maintaining power-user features. Multiple record types can be queried simultaneously, and responses are clearly formatted."],["CAVEATS","Upstream development has been dormant since **2021** and the project's homepage (dns.lookup.dog) is offline, so distribution packages may lag behind the repository. Colours are disabled automatically when output is not a terminal, but pass **--color=never** to be sure in scripts. DoH and DoT require a resolver that speaks them. dog does not cover every corner of dig: there is no zone transfer (AXFR), no batch `-f` file mode, and no `+trace` equivalent."],["HISTORY","dog was created by **Benjamin Sago** (ogham), also known for the `exa` file lister, and first released in **2020**. Written in Rust, it was designed as a friendlier alternative to dig, with colourised output, order-independent arguments, and built-in support for encrypted DNS transports. It inspired **doggo**, a Go reimplementation of the same idea that is more actively maintained."],["INSTALL","```pacman: sudo pacman -S dog```\n\n```apk: sudo apk add dog```\n\n```zypper: sudo zypper install dog```\n\n```nix: nix profile install nixpkgs#dog```"],["SEE ALSO","[dig](/man/dig)(1), [doggo](/man/doggo)(1), [drill](/man/drill)(1), [nslookup](/man/nslookup)(1)"],["RESOURCES","```[Source code](https://github.com/ogham/dog)```"]],"e":["dig","doggo","drill","nslookup"],"x":"dog [example.com]"},"dolt-checkout":{"n":"dolt-checkout","t":"switch branches or restore working tables","s":[["TAGLINE","switch branches or restore working tables"],["TLDR","**Switch to branch**\n\n```dolt checkout [branch_name]```\n\n**Create and switch** to new branch\n\n```dolt checkout -b [new_branch]```\n\n**Restore a table** to its HEAD version, discarding changes\n\n```dolt checkout [table_name]```\n\n**Restore a table** as it was at a given commit\n\n```dolt checkout [commit_hash] -- [table_name]```\n\n**Create a branch tracking** a remote branch\n\n```dolt checkout --track [origin]/[branch_name]```\n\n**Discard local changes** and force the switch\n\n```dolt checkout --force [branch_name]```"],["SYNOPSIS","**dolt checkout** _branch_\n\n**dolt checkout** _table_...\n\n**dolt checkout** _commit_ [--] _table_...\n\n**dolt checkout** **-b** _new-branch_ [_start-point_]\n\n**dolt checkout** **--track** _remote_/_branch_"],["PARAMETERS","**-b** _new-branch_ [_start-point_]\n> Create a branch named _new-branch_ starting at _start-point_ (defaults to HEAD) and switch to it.\n\n**-B** _new-branch_ [_start-point_]\n> Like **-b**, but forcibly resets the branch to _start-point_ if it already exists.\n\n**-f**, **--force**\n> Wipe out the current changes and check out the new branch anyway.\n\n**-t**, **--track**\n> When creating a branch, set up upstream tracking configuration.\n\n**--overwrite-ignore**\n> Silently overwrite ignored tables when switching branches.\n\n**--no-overwrite-ignore**\n> Abort the checkout if ignored tables would be overwritten."],["DESCRIPTION","**dolt checkout** switches branches or restores tables in the working set. Like `git checkout` it serves several purposes: moving between branches, creating branches, and discarding uncommitted changes.\n\nGiven a branch name, it points the working database at that branch. With **-b** it creates the branch first. Given one or more table names, it restores those tables to match HEAD, throwing away uncommitted changes; adding a commit before `--` restores them as of that commit instead.\n\nDolt's branches are cheap, since they are pointers into the same content-addressed storage, so branching per feature, per import, or per experiment is a normal workflow on data as well as schema."],["CAVEATS","Restoring a table discards its uncommitted changes with no way back: there is no reflog for the working set. Unlike Git, **dolt checkout** does not accept a bare commit hash to enter a detached HEAD; use `dolt checkout -b <branch> <commit>` to work from an old commit. When a `dolt sql-server` is running against the database, the CLI shares the server's session state, so checkouts made from the shell are visible to connected clients."],["HISTORY","dolt checkout mirrors **git checkout** for Dolt's versioned database, giving Git users a familiar way to move between branches of data. Dolt has since also grown Git's newer split of the command into `dolt branch` for branch management and `dolt reset`/`dolt revert` for undoing work, but `checkout` remains the everyday entry point."],["INSTALL","```pacman: sudo pacman -S dolt```\n\n```brew: brew install dolt```\n\n```nix: nix profile install nixpkgs#dolt```"],["SEE ALSO","[dolt](/man/dolt)(1), [dolt-branch](/man/dolt-branch)(1), [dolt-merge](/man/dolt-merge)(1), [git-checkout](/man/git-checkout)(1)"],["RESOURCES","```[Source code](https://github.com/dolthub/dolt)```\n\n```[Documentation](https://www.dolthub.com/docs/cli-reference/cli/)```"]],"e":["dolt","dolt-branch","dolt-merge","git-checkout"],"x":"dolt checkout [branch_name]"},"file-hierarchy":{"n":"file-hierarchy","t":"systemd filesystem hierarchy documentation","s":[["TAGLINE","systemd filesystem hierarchy documentation"],["TLDR","**View file hierarchy documentation**\n\n```man file-hierarchy```"],["DESCRIPTION","**file-hierarchy** is a systemd documentation page (man page) describing the file system hierarchy used by systemd-based Linux systems. It documents the purpose of directories under /, /etc, /usr, /var, and other key locations.\n\nThis is not an executable command but a reference document explaining where files should be placed and why."],["KEY DIRECTORIES","```\n/               Root filesystem\n/boot           Boot loader files\n/etc            Host-specific configuration\n/home           User home directories\n/root           Root user's home\n/run            Runtime variable data\n/srv            Service data\n/tmp            Temporary files\n/usr            Vendor-supplied OS resources\n/usr/bin        Executables\n/usr/lib        Libraries\n/usr/share      Architecture-independent data\n/var            Variable data\n/var/cache      Application caches\n/var/lib        State information\n/var/log        Log files\n/var/tmp        Persistent temporary files\n```"],["SYSTEMD SPECIFICS","- /run replaces /var/run\n- /usr/lib/systemd for unit files\n- /etc/systemd for local overrides\n- Merged /usr (bin, sbin, lib merged)"],["CAVEATS","This is a documentation page, not an executable command. The hierarchy described is specific to systemd-based distributions and may differ on other Linux systems."],["INSTALL","```apt: sudo apt install file```\n\n```dnf: sudo dnf install file```\n\n```pacman: sudo pacman -S file```\n\n```apk: sudo apk add file```\n\n```zypper: sudo zypper install file```\n\n```brew: brew install file```\n\n```nix: nix profile install nixpkgs#file```"],["SEE ALSO","[mount](/man/mount)(8), [umount](/man/umount)(8)"]],"e":["mount","umount"],"x":"man file-hierarchy"},"foremost":{"n":"foremost","t":"file carving and data recovery tool","s":[["TAGLINE","file carving and data recovery tool"],["TLDR","**Recover files from disk image**\n\n```foremost -i [disk.img] -o [output_dir]```\n\n**Recover specific file types**\n\n```foremost -t [jpg,png,pdf] -i [disk.img]```\n\n**Recover from device**\n\n```sudo foremost -i [/dev/sda1] -o [output_dir]```\n\n**Show all recoverable types**\n\n```foremost -h```\n\n**Verbose output**\n\n```foremost -v -i [disk.img] -o [output_dir]```\n\n**Use custom config**\n\n```foremost -c [foremost.conf] -i [disk.img]```"],["SYNOPSIS","**foremost** [_options_] -i _input_ -o _output_dir_"],["DESCRIPTION","**foremost** is a file carving tool for recovering files from disk images or devices. It searches for file headers and footers, extracting data between them regardless of filesystem state.\n\nThe tool is useful for data recovery and forensic analysis, recovering files even from corrupted or partially overwritten media."],["PARAMETERS","**-i** _input_\n> Input file or device.\n\n**-o** _directory_\n> Output directory.\n\n**-t** _types_\n> File types to extract.\n\n**-c** _file_\n> Configuration file.\n\n**-v**\n> Verbose output.\n\n**-V**\n> Display version.\n\n**-q**\n> Quick mode.\n\n**-a**\n> Write all headers.\n\n**-w**\n> Only write audit file."],["CONFIGURATION","**/etc/foremost.conf**\n> Configuration file defining file signatures, headers, and footers for recovery."],["FILE TYPES","Common types: jpg, gif, png, bmp, avi, exe, mpg, wav, riff, wmv, mov, pdf, ole, doc, zip, rar, htm, cpp"],["CAVEATS","Fragmented files may not recover correctly. Output directory must be empty. Large media requires significant space. Some file types need configuration."],["HISTORY","**foremost** was developed by the **US Air Force Office of Special Investigations** and **The Center for Information Systems Security Studies and Research** around **2001**. It was designed for forensic file recovery and released as open source."],["INSTALL","```apt: sudo apt install foremost```\n\n```dnf: sudo dnf install foremost```\n\n```pacman: sudo pacman -S foremost```\n\n```brew: brew install foremost```\n\n```nix: nix profile install nixpkgs#foremost```"],["SEE ALSO","[scalpel](/man/scalpel)(1), [photorec](/man/photorec)(1), [testdisk](/man/testdisk)(1), [dd](/man/dd)(1)"],["RESOURCES","```[Homepage](http://foremost.sourceforge.net/)```"]],"e":["scalpel","photorec","testdisk","dd"],"x":"foremost -i [disk.img] -o [output_dir]"},"gc":{"n":"gc","t":"count graph components","s":[["TAGLINE","count graph components"],["TLDR","**Count** nodes and edges in a graph (the default)\n\n```gc [graph.dot]```\n\n**Count** connected components\n\n```gc -c [graph.dot]```\n\n**Count** everything: nodes, edges, connected components and clusters\n\n```gc -a [graph.dot]```\n\n**Count** nodes only\n\n```gc -n [graph.dot]```\n\n**Recurse** into subgraphs and report counts for each\n\n```gc -r [graph.dot]```\n\n**Read** a graph from standard input\n\n```cat [graph.dot] | gc -a```"],["SYNOPSIS","**gc** [**-necCaDUrsv?**] [_files_]"],["DESCRIPTION","**gc** is a graph analogue of **wc**: it reads one or more graphs in DOT format and prints counts of their nodes, edges, connected components and clusters to standard output. With no options it reports node and edge counts. When multiple graphs are given, a total line is printed at the end.\n\nEach input file may contain several graphs. Output is one line per graph, followed by the graph name, so results can be fed into other text tools. If no files are named, **gc** reads from standard input.\n\nClusters are subgraphs whose name begins with **cluster**, the same convention **dot** uses when drawing boxed groups."],["PARAMETERS","**-n**\n> Count nodes.\n\n**-e**\n> Count edges.\n\n**-c**\n> Count connected components.\n\n**-C**\n> Count clusters (subgraphs whose name starts with **cluster**).\n\n**-a**\n> Count all of the above; equivalent to **-encC**.\n\n**-r**\n> Recurse into subgraphs, reporting counts for each.\n\n**-s**\n> Silent: suppress output and only set the exit status.\n\n**-D**\n> Only process directed graphs.\n\n**-U**\n> Only process undirected graphs.\n\n**-v**\n> Verbose output.\n\n**-?**\n> Print usage information."],["CAVEATS","Input must be valid **DOT** format; malformed graphs cause errors rather than counts. For directed graphs, the components counted are weakly connected components. **-D** and **-U** skip graphs of the wrong kind, so a filtered run may print nothing at all.\n\nThis command is unrelated to garbage collection. In PowerShell, **gc** is an alias for **Get-Content**, and some version control wrappers alias it to a commit shortcut, so check what is on your **PATH** before assuming."],["HISTORY","**gc** is part of the **Graphviz** suite, developed at **AT&T Labs Research** starting in the early **1990s** by **Stephen North**, **Eleftherios Koutsofios**, **Emden Gansner** and others. Graphviz was open-sourced in **2004** and remains actively maintained."],["INSTALL","```apt: sudo apt install graphviz```\n\n```dnf: sudo dnf install graphviz```\n\n```pacman: sudo pacman -S graphviz```\n\n```apk: sudo apk add graphviz```\n\n```zypper: sudo zypper install graphviz```\n\n```brew: brew install graphviz```\n\n```nix: nix profile install nixpkgs#graphviz```"],["SEE ALSO","[wc](/man/wc)(1), [acyclic](/man/acyclic)(1), [ccomps](/man/ccomps)(1), [sccmap](/man/sccmap)(1), [tred](/man/tred)(1), [gvpr](/man/gvpr)(1), [dot](/man/dot)(1)"],["RESOURCES","```[Source code](https://gitlab.com/graphviz/graphviz)```\n\n```[Homepage](https://graphviz.org/)```\n\n```[Documentation](https://graphviz.org/docs/cli/gc/)```"]],"e":["wc","acyclic","ccomps","sccmap","tred","gvpr","dot"],"x":"gc [graph.dot]"},"gh-cs":{"n":"gh-cs","t":"shorthand for codespaces management","s":[["TAGLINE","shorthand for codespaces management"],["TLDR","**Create a codespace for a repository**\n\n```gh cs create -R [owner/repo]```\n\n**List codespaces**\n\n```gh cs list```\n\n**Connect to codespace via SSH**\n\n```gh cs ssh -c [codespace-name]```\n\n**Open codespace in VS Code**\n\n```gh cs code```\n\n**Stop a running codespace**\n\n```gh cs stop -c [codespace-name]```\n\n**Delete a codespace**\n\n```gh cs delete -c [codespace-name]```\n\n**Copy files from codespace to local machine**\n\n```gh cs cp remote:[/path/to/file] [local/path]```"],["SYNOPSIS","**gh cs** _command_ [_options_]"],["PARAMETERS","**create**\n> Create a new codespace.\n\n**list**\n> List available codespaces.\n\n**ssh**\n> SSH into a codespace.\n\n**code**\n> Open codespace in Visual Studio Code.\n\n**stop**\n> Stop a running codespace.\n\n**delete**\n> Delete codespaces.\n\n**ports**\n> List and manage port forwarding.\n\n**cp**\n> Copy files between local and remote file systems.\n\n**logs**\n> Access codespace logs.\n\n**rebuild**\n> Rebuild a codespace.\n\n**view**\n> View details about a codespace.\n\n**jupyter**\n> Open a codespace in JupyterLab.\n\n**edit**\n> Edit a codespace.\n\n**-c** _CODESPACE_, **--codespace** _CODESPACE_\n> Target codespace name. If omitted, a list is displayed for selection.\n\n**-R** _REPO_, **--repo** _REPO_\n> Repository for new codespace.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gh cs** is the shorthand alias for **gh codespace**, managing GitHub Codespaces from the command line. Codespaces are cloud-hosted development environments that provide a full VS Code experience with configurable compute resources.\n\nThe command enables creating, connecting, and managing codespaces without visiting github.com. It supports SSH connections for terminal access, integration with local VS Code installations, and file copying between local and remote environments."],["CAVEATS","Requires GitHub account with Codespaces access. Usage may incur charges. Codespaces have timeout and retention limits."],["HISTORY","gh cs was added to the **GitHub CLI** to support Codespaces, GitHub's cloud development environment launched in **2020**."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-codespace](/man/gh-codespace)(1), [gh-repo](/man/gh-repo)(1), [code](/man/code)(1)"]],"e":["gh","gh-codespace","gh-repo","code"],"x":"gh cs create -R [owner/repo]"},"git-fsck":{"n":"git-fsck","t":"Verify connectivity and validity of repository objects","s":[["TAGLINE","Verify connectivity and validity of repository objects"],["TLDR","**Check repository integrity**\n\n```git fsck```\n\n**Check with unreachable objects**\n\n```git fsck --unreachable```\n\n**Check connectivity only** (faster, skips blob content)\n\n```git fsck --connectivity-only```\n\n**Write dangling objects** to .git/lost-found for recovery\n\n```git fsck --lost-found```\n\n**Verbose check with named objects**\n\n```git fsck --verbose --name-objects```\n\n**Check without reflog references**\n\n```git fsck --no-reflogs```\n\n**Strict check** for stricter validation\n\n```git fsck --strict```"],["SYNOPSIS","**git** **fsck** [_options_] [_objects_...]"],["PARAMETERS","**--unreachable**\n> Print objects that exist but are not reachable from any reference node.\n\n**--dangling**\n> Print objects that exist but are never directly used (default).\n\n**--no-dangling**\n> Omit dangling object information from output.\n\n**--full**\n> Check objects in alternate object pools and pack files. This is now the default.\n\n**--connectivity-only**\n> Check only connectivity of reachable objects, skipping blob content. Faster but cannot detect corrupt blobs.\n\n**--strict**\n> Enable stricter checking, e.g. catching file modes with g+w bit set.\n\n**--lost-found**\n> Write dangling objects to .git/lost-found/commit/ or .git/lost-found/other/.\n\n**--no-reflogs**\n> Do not consider reflog entries when determining reachability.\n\n**--cache**\n> Consider objects in the index as head nodes for reachability traces.\n\n**--root**\n> Report root nodes.\n\n**--tags**\n> Report tags.\n\n**--name-objects**\n> Display a descriptive name showing how objects are reachable (e.g. HEAD~25^2:src/).\n\n**--verbose**\n> Be chatty.\n\n**--progress**, **--no-progress**\n> Force or suppress progress status reporting."],["DESCRIPTION","**git fsck** (file system check) performs integrity verification on all objects in the Git object database. It validates that objects are well-formed, correctly connected, and that the repository structure is consistent and uncorrupted.\n\nThe command checks for corrupted objects, broken links, invalid object types, and unreachable objects that exist but are not referenced by any branch or tag. Unreachable objects are often the result of rebases, amended commits, or deleted branches, and can be useful for recovering accidentally deleted work via the --lost-found option.\n\nRegular fsck runs are recommended after suspected repository corruption, disk errors, or hardware failures. The --full option (now default) extends checking to objects inside pack files. Use --connectivity-only for faster checks that skip blob content validation."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-gc](/man/git-gc)(1), [git-prune](/man/git-prune)(1), [git-reflog](/man/git-reflog)(1)"]],"e":["git-gc","git-prune","git-reflog"],"x":"git fsck"},"git-interactive-rebase-tool":{"n":"git-interactive-rebase-tool","t":"Terminal-based sequence editor for git interactive rebase","s":[["TAGLINE","Terminal-based sequence editor for git interactive rebase"],["TLDR","**Use** as git sequence editor\n\n```git rebase -i HEAD~5```\n\n**Configure** as default editor\n\n```git config --global sequence.editor interactive-rebase-tool```"],["SYNOPSIS","**interactive-rebase-tool** [_options_] [_todo-file_]"],["PARAMETERS","**--version**\n> Display version and exit\n\n**--help**\n> Display help and exit"],["DESCRIPTION","**interactive-rebase-tool** is a full-featured terminal-based sequence editor for git interactive rebase. It provides a visual interface for editing rebase todo files with features like syntax highlighting, undo/redo, and visual indicators.\n\nThe tool makes it easier to manage complex rebases by providing a clear view of commits and their operations (pick, squash, fixup, reword, edit, drop, etc.)."],["KEYBINDINGS","**↑/↓** or **k/j**\n> Navigate commits\n\n**Page Up/Page Down**\n> Page navigation\n\n**Home/End**\n> Jump to beginning/end\n\n**p**\n> Pick commit\n\n**r**\n> Reword commit\n\n**e**\n> Edit commit\n\n**s**\n> Squash commit\n\n**f**\n> Fixup commit\n\n**d**\n> Drop commit\n\n**b**\n> Toggle break action\n\n**E**\n> Edit exec command\n\n**v**\n> Toggle visual mode for batch operations\n\n**c**\n> Show commit information\n\n**Ctrl+z**\n> Undo\n\n**Ctrl+y**\n> Redo\n\n**!**\n> Open in external editor\n\n**?**\n> Display built-in help\n\n**w**\n> Save changes\n\n**q**\n> Abort rebase"],["CAVEATS","Must be configured as Git's sequence editor via `git config --global sequence.editor interactive-rebase-tool`. Terminal must support required escape sequences."],["HISTORY","**interactive-rebase-tool** was created to improve the git interactive rebase experience by providing a visual editor instead of editing todo files manually."],["INSTALL","```dnf: sudo dnf install git-interactive-rebase-tool```\n\n```brew: brew install git-interactive-rebase-tool```\n\n```nix: nix profile install nixpkgs#git-interactive-rebase-tool```"],["SEE ALSO","[git-rebase](/man/git-rebase)(1), [git](/man/git)(1), [vim](/man/vim)(1)"]],"e":["git-rebase","git","vim"],"x":"git rebase -i HEAD~5"},"gladtex":{"n":"gladtex","t":"Convert LaTeX formulas in HTML to images","s":[["TAGLINE","Convert LaTeX formulas in HTML to images"],["TLDR","**Convert HTML with LaTeX**\n\n```gladtex [input.htex]```\n\n**Specify output file**\n\n```gladtex -o [output.html] [input.htex]```\n\n**Set image directory**\n\n```gladtex -d [images/] [input.htex]```\n\n**Set resolution**\n\n```gladtex -r [150] [input.htex]```"],["SYNOPSIS","**gladtex** [_options_] _file_"],["PARAMETERS","_FILE_\n> Input .htex file.\n\n**-o** _FILE_\n> Output file.\n\n**-d** _DIR_\n> Image directory.\n\n**-r** _DPI_\n> Image resolution.\n\n**-b** _COLOR_\n> Background color.\n\n**--help**\n> Display help information."],["DESCRIPTION","**gladtex** converts LaTeX formulas embedded in HTML documents to images. It processes `.htex` files containing LaTeX math markup and produces standard HTML with rendered formula images.\n\nThe tool enables embedding mathematical notation in web pages by rendering formulas through LaTeX and converting them to images that display in any browser."],["CAVEATS","Requires LaTeX installation. Output is images, not MathML. Consider MathJax for modern sites."],["HISTORY","gladtex was created to enable mathematical formulas in HTML before widespread browser support for MathML and JavaScript math rendering."],["INSTALL","```nix: nix profile install nixpkgs#gladtex```"],["SEE ALSO","[latex](/man/latex)(1), [pandoc](/man/pandoc)(1)"]],"e":["latex","pandoc"],"x":"gladtex [input.htex]"},"gpicview":{"n":"gpicview","t":"Lightweight image viewer for LXDE","s":[["TAGLINE","Lightweight image viewer for LXDE"],["TLDR","**Open image**\n\n```gpicview [image.jpg]```\n\n**Start GPicView**\n\n```gpicview```"],["SYNOPSIS","**gpicview** [_options_] [_file_]"],["PARAMETERS","_file_\n> Image file to open.\n\n**--help**\n> Display help.\n\n**--version**\n> Show version."],["DESCRIPTION","**GPicView** is a lightweight image viewer for the LXDE desktop environment. It provides fast image viewing with minimal resource usage, supporting common formats like JPEG, PNG, GIF, and BMP.\n\nThe viewer offers basic navigation, zoom, rotation, and slideshow features. It is designed to be simple and responsive, ideal for systems with limited resources."],["KEYBOARD SHORTCUTS","```\nSpace/N     Next image\nBackspace/P Previous image\n+/=         Zoom in\n-           Zoom out\n1           Original size\nF           Fit to window\nR           Rotate right\nL           Rotate left\nF11         Fullscreen\n```"],["CAVEATS","Limited editing features. No support for RAW formats. Part of LXDE but can run standalone. May lack features of heavier viewers like gThumb."],["HISTORY","GPicView was created as part of the **LXDE** (Lightweight X11 Desktop Environment) project, designed for fast performance on low-resource systems. It follows the LXDE philosophy of being lightweight and simple."],["INSTALL","```apt: sudo apt install gpicview```\n\n```dnf: sudo dnf install gpicview```\n\n```pacman: sudo pacman -S gpicview```\n\n```apk: sudo apk add gpicview```\n\n```zypper: sudo zypper install gpicview```"],["SEE ALSO","[feh](/man/feh)(1), [eog](/man/eog)(1), [sxiv](/man/sxiv)(1), [imv](/man/imv)(1)"],["RESOURCES","```[Source code](https://github.com/lxde/gpicview)```\n\n```[Homepage](https://wiki.lxde.org/en/GPicView)```"]],"e":["feh","eog","sxiv","imv"],"x":"gpicview [image.jpg]"},"grumphp":{"n":"grumphp","t":"PHP code quality enforcement via git hooks","s":[["TAGLINE","PHP code quality enforcement via git hooks"],["TLDR","**Generate a starter configuration file**\n\n```grumphp configure```\n\n**Run all configured tasks**\n\n```grumphp run```\n\n**Run specific tasks**\n\n```grumphp run --tasks=[phpcs,phpunit]```\n\n**Run a specific testsuite**\n\n```grumphp run --testsuite=[mytestsuite]```\n\n**Register GrumPHP's git hooks**\n\n```grumphp git:init```\n\n**Unregister GrumPHP's git hooks**\n\n```grumphp git:deinit```\n\n**Manually run the pre-commit hook tasks**\n\n```grumphp git:pre-commit```"],["SYNOPSIS","**grumphp** _command_ [_options_]"],["PARAMETERS","_COMMAND_\n> Command to run.\n\n**configure**\n> Generate a starter grumphp.yml configuration file, guessing sensible defaults.\n\n**run**\n> Execute all configured tasks against the codebase.\n\n**git:init**\n> Register GrumPHP's git hooks (run automatically after `composer install`).\n\n**git:deinit**\n> Unregister GrumPHP's git hooks.\n\n**git:pre-commit**\n> Run the tasks bound to the pre-commit hook.\n\n**git:commit-msg**\n> Run the tasks that validate the commit message.\n\n**--tasks** _TASK1,TASK2_\n> Only run the given comma-separated tasks.\n\n**--testsuite** _NAME_\n> Only run tasks belonging to the given testsuite.\n\n**--help**\n> Display help information."],["DESCRIPTION","**GrumPHP** is a PHP code quality tool that runs checks on git commits. It enforces coding standards by running tasks like PHPStan, PHP_CodeSniffer, and PHPUnit before allowing commits.\n\nThe tool integrates with git hooks to prevent commits that fail quality checks. It is configurable via a grumphp.yml file in the project root."],["CONFIGURATION","**grumphp.yml**\n> Project-level configuration file defining tasks, parameters, and git hook settings."],["CAVEATS","PHP project specific. Requires Composer. May slow down commits with many tasks."],["HISTORY","GrumPHP was created by **phpro** to automate PHP code quality enforcement through git hooks."],["SEE ALSO","[composer](/man/composer)(1), [phpcs](/man/phpcs)(1), [phpstan](/man/phpstan)(1)"],["RESOURCES","```[Source code](https://github.com/phpro/grumphp)```\n\n```[Documentation](https://github.com/phpro/grumphp/tree/v2.x/doc)```"]],"e":["composer","phpcs","phpstan"],"x":"grumphp configure"},"hg-log":{"n":"hg-log","t":"displays repository commit history","s":[["TAGLINE","displays repository commit history"],["TLDR","**Show commit history**\n\n```hg log```\n\n**Show last n commits**\n\n```hg log -l [5]```\n\n**Show specific file history**\n\n```hg log [file]```\n\n**Show with patch**\n\n```hg log -p```\n\n**Graph view**\n\n```hg log -G```\n\n**Show specific revision**\n\n```hg log -r [revision]```\n\n**Show commits by a specific user**\n\n```hg log -u [username]```\n\n**Show commits matching a keyword**\n\n```hg log -k [keyword]```"],["SYNOPSIS","**hg log** [_options_] [_file_]"],["PARAMETERS","_FILE_\n> Limit to specific file.\n\n**-l**, **--limit** _N_\n> Limit number of entries.\n\n**-r**, **--rev** _REV_\n> Show specific revision.\n\n**-p**, **--patch**\n> Show patches.\n\n**-G**, **--graph**\n> Show DAG graph.\n\n**-b**, **--branch** _BRANCH_\n> Show branch commits.\n\n**-u**, **--user** _USER_\n> Show commits by user.\n\n**-k**, **--keyword** _TEXT_\n> Search commit messages.\n\n**--template** _TPL_\n> Output template.\n\n**--help**\n> Display help information."],["DESCRIPTION","**hg log** displays repository commit history. It shows changesets with author, date, summary, and other metadata.\n\nThe command supports filtering by file, branch, or revision range. Graph mode visualizes branch and merge structure."],["CAVEATS","Large histories can be slow. Use -l to limit. Templates for custom output."],["HISTORY","Log is a core **Mercurial** command for viewing repository history since version 1.0."],["SEE ALSO","[hg](/man/hg)(1), [hg-status](/man/hg-status)(1), [hg-commit](/man/hg-commit)(1), [git-log](/man/git-log)(1)"]],"e":["hg","hg-status","hg-commit","git-log"],"x":"hg log"},"host":{"n":"host","t":"simple DNS lookup utility","s":[["TAGLINE","simple DNS lookup utility"],["TLDR","**Lookup IP address**\n\n```host [example.com]```\n\n**Reverse DNS lookup**\n\n```host [192.168.1.1]```\n\n**Query specific DNS server**\n\n```host [example.com] [8.8.8.8]```\n\n**Query specific record type**\n\n```host -t [MX] [example.com]```\n\n**Verbose output**\n\n```host -v [example.com]```\n\n**Query all records**\n\n```host -a [example.com]```"],["SYNOPSIS","**host** [**-aACdlnrsTUwv**] [**-c** _class_] [**-N** _ndots_] [**-p** _port_] [**-R** _number_] [**-t** _type_] [**-W** _wait_] [**-m** _flag_] [**-4**|**-6**] _name_ [_server_]"],["DESCRIPTION","**host** is a simple DNS lookup utility. It converts hostnames to IP addresses and vice versa, and can query specific DNS record types.\n\nThe tool provides quick DNS queries without the complexity of dig, making it useful for simple lookups and troubleshooting."],["PARAMETERS","**-t** _type_\n> Query type (A, AAAA, MX, NS, TXT, SOA, etc.).\n\n**-a**\n> Equivalent to -v -t ANY; queries all record types with verbose output.\n\n**-A**\n> Like -a but omits RRSIG, NSEC, and NSEC3 records.\n\n**-C**\n> Check SOA record consistency across authoritative name servers.\n\n**-v**\n> Verbose output (equivalent to -d).\n\n**-l**\n> Perform a zone transfer (AXFR) and list NS, PTR, and A/AAAA records.\n\n**-4**\n> Use IPv4 transport only.\n\n**-6**\n> Use IPv6 transport only.\n\n**-r**\n> Non-recursive query (clears RD bit).\n\n**-s**\n> Do not try the next name server after a SERVFAIL response.\n\n**-W** _seconds_\n> Query timeout in seconds (default: 5 for UDP, 10 for TCP).\n\n**-w**\n> Wait forever (set query timeout to maximum).\n\n**-R** _retries_\n> Number of UDP query retries.\n\n**-T**\n> Use TCP instead of UDP.\n\n**-U**\n> Force UDP, even for ANY queries.\n\n**-p** _port_\n> Query port (default: 53).\n\n**-N** _ndots_\n> Number of dots required for an absolute name lookup.\n\n**-c** _class_\n> Query class (IN, HS, CH; default: IN).\n\n**-m** _flag_\n> Memory debugging (record, usage, or trace).\n\n**-V**\n> Print version number and exit."],["CAVEATS","Less detailed than dig. Caching may affect results. Some record types need explicit query. Name server defaults to system resolver."],["HISTORY","**host** is part of **BIND** (Berkeley Internet Name Domain), developed originally at **UC Berkeley** in the **1980s**. It provides a simplified interface compared to dig and nslookup, targeting users who need quick DNS lookups."],["INSTALL","```apt: sudo apt install bind9-host```\n\n```apk: sudo apk add bind-tools```\n\n```nix: nix profile install nixpkgs#host```"],["SEE ALSO","[dig](/man/dig)(1), [nslookup](/man/nslookup)(1), [drill](/man/drill)(1), [getent](/man/getent)(1)"]],"e":["dig","nslookup","drill","getent"],"x":"host [example.com]"},"http-prompt":{"n":"http-prompt","t":"Interactive command-line HTTP client with autocomplete and syntax highlighting","s":[["TAGLINE","Interactive command-line HTTP client with autocomplete and syntax highlighting"],["TLDR","Launch a session targeting **localhost**\n\n```http-prompt```\n\nLaunch a session with a **specific URL**\n\n```http-prompt [http://example.com]```\n\nLaunch a session with **authentication**\n\n```http-prompt [localhost:8000/api] --auth [username:password]```"],["SYNOPSIS","**http-prompt** [_URL_] [_options_]"],["PARAMETERS","**--auth** _USER:PASS_\n> Set HTTP basic authentication credentials\n\n**--version**\n> Display version information"],["DESCRIPTION","**http-prompt** is an interactive command-line HTTP client featuring autocomplete and syntax highlighting. It provides a REPL (Read-Eval-Print Loop) environment for exploring and testing HTTP APIs.\n\nBuilt on top of HTTPie, it offers an interactive way to build HTTP requests with features like tab completion for HTTP methods, headers, and previously used values. Sessions persist settings and can be resumed.\n\nWithin the prompt, you can set headers, query parameters, and body data incrementally, then execute requests with standard HTTP methods (GET, POST, PUT, DELETE, etc.)."],["CAVEATS","Requires Python and HTTPie. Sessions are stored locally and persist between invocations. Not suitable for automated scripting; use HTTPie or curl for that purpose."],["HISTORY","http-prompt was created by Chang-Hung Liang and released in **2016**. It was inspired by the interactive nature of database clients and aims to provide similar convenience for HTTP API exploration."],["INSTALL","```brew: brew install http-prompt```"],["SEE ALSO","[http](/man/http)(1), [curl](/man/curl)(1), [httpie](/man/httpie)(1)"]],"e":["http","curl","httpie"],"x":"http-prompt"},"ip6tables-save":{"n":"ip6tables-save","t":"save current IPv6 firewall rules","s":[["TAGLINE","save current IPv6 firewall rules"],["TLDR","This command is an alias of **iptables-save** for IPv6 firewall\n\n```tldr iptables-save```"],["INSTALL","```pacman: sudo pacman -S iptables```\n\n```apk: sudo apk add iptables```\n\n```zypper: sudo zypper install iptables```\n\n```brew: brew install iptables```\n\n```nix: nix profile install nixpkgs#iptables```"],["SEE ALSO","[iptables-save](/man/iptables-save)(8), [ip6tables](/man/ip6tables)(8), [ip6tables-restore](/man/ip6tables-restore)(8)"]],"e":["iptables-save","ip6tables","ip6tables-restore"],"x":"tldr iptables-save"},"kaniko":{"n":"kaniko","t":"builds container images from Dockerfiles inside containers or Kubernetes","s":[["TAGLINE","builds container images from Dockerfiles inside containers or Kubernetes"],["TLDR","**Build and push image** to registry\n\n```/kaniko/executor --dockerfile=[Dockerfile] --context=[dir://workspace] --destination=[gcr.io/project/image:tag]```\n\n**Build without pushing**\n\n```/kaniko/executor --dockerfile=[Dockerfile] --context=[.] --no-push```\n\n**Build with caching** enabled\n\n```/kaniko/executor --dockerfile=[Dockerfile] --destination=[registry/image] --cache=true```\n\n**Save image as tarball**\n\n```/kaniko/executor --dockerfile=[Dockerfile] --context=[.] --tar-path=[image.tar] --no-push```\n\n**Build specific target** stage\n\n```/kaniko/executor --dockerfile=[Dockerfile] --destination=[registry/image] --target=[build-stage]```\n\n**Build with build arguments**\n\n```/kaniko/executor --dockerfile=[Dockerfile] --destination=[registry/image] --build-arg=[VERSION=1.0]```"],["SYNOPSIS","**/kaniko/executor** [_options_]"],["PARAMETERS","**--dockerfile** _path_\n> Path to Dockerfile. Default: Dockerfile.\n\n**--context** _uri_\n> Build context: dir://, git://, s3://, gs://, or local path.\n\n**--destination** _registry/image:tag_\n> Registry to push image. Required unless --no-push.\n\n**--no-push**\n> Build image but don't push to registry.\n\n**--tar-path** _path_\n> Save image as tarball instead of pushing.\n\n**--cache**\n> Enable layer caching.\n\n**--cache-repo** _repository_\n> Repository for storing cached layers.\n\n**--cache-ttl** _duration_\n> Cache expiration time.\n\n**--target** _stage_\n> Build up to specified Dockerfile stage.\n\n**--build-arg** _KEY=value_\n> Set build-time variable. Repeatable.\n\n**--ignore-var-run**\n> Ignore /var/run when taking image snapshot. Default: true.\n\n**--ignore-path** _path_\n> Ignore specified path when taking image snapshot. Repeatable.\n\n**--snapshot-mode** _mode_\n> Snapshot mode: full or redo (default: redo).\n\n**--single-snapshot**\n> Take one snapshot at end instead of per layer.\n\n**--reproducible**\n> Strip timestamps for reproducible builds.\n\n**--registry-mirror** _mirror_\n> Use registry mirror as pull-through cache.\n\n**--insecure**\n> Push to insecure (HTTP) registry.\n\n**--skip-tls-verify**\n> Skip TLS certificate verification.\n\n**--insecure-pull**\n> Pull from insecure (HTTP) registry.\n\n**--skip-tls-verify-pull**\n> Skip TLS certificate verification when pulling.\n\n**--kaniko-dir** _path_\n> Kaniko working directory (default: /kaniko).\n\n**--image-fs-extract-retry** _count_\n> Number of retries for extracting image filesystem. Default: 0.\n\n**--image-download-retry** _count_\n> Number of retries for downloading remote image with exponential backoff. Default: 0.\n\n**--label** _KEY=value_\n> Set image label metadata. Repeatable.\n\n**--verbosity** _level_\n> Log level: panic, fatal, error, warn, info, debug, trace."],["DESCRIPTION","**kaniko** builds container images from Dockerfiles inside containers or Kubernetes clusters without requiring privileged access or a Docker daemon. It executes each Dockerfile command in userspace and snapshots the filesystem.\n\nThe executor runs as a container image (**gcr.io/kaniko-project/executor**), making it ideal for CI/CD pipelines in Kubernetes where Docker-in-Docker is unavailable or undesirable for security reasons.\n\nContext sources include local directories (**dir://**), Git repositories (**git://**), Google Cloud Storage (**gs://**), Amazon S3 (**s3://**), and Azure Blob Storage. Authentication is handled via mounted credentials or environment variables.\n\nLayer caching with **--cache** significantly speeds up builds by reusing unchanged layers. Cached layers are stored in the registry specified by **--cache-repo** or derived from **--destination**.\n\nA debug image (**gcr.io/kaniko-project/executor:debug**) includes busybox for troubleshooting."],["CAVEATS","Kaniko runs inside containers and is not meant for direct host execution. Some Dockerfile features may behave differently than with Docker daemon. The **redo** snapshot mode may miss metadata-only changes; use **full** mode if needed. The **--reproducible** flag takes a single snapshot at the end of the build, so only one layer is appended to the base image."],["HISTORY","Kaniko was developed by **Google** and released in **2018** as part of Google Container Tools. It was created to enable secure container builds in Kubernetes environments where running Docker daemon is impractical or forbidden. The project is maintained by the open-source community and widely used in cloud-native CI/CD pipelines."],["INSTALL","```nix: nix profile install nixpkgs#kaniko```"],["SEE ALSO","[docker](/man/docker)(1), [buildah](/man/buildah)(1), [podman](/man/podman)(1)"]],"e":["docker","buildah","podman"],"x":"/kaniko/executor --dockerfile=[Dockerfile] --context=[dir://workspace] --destination=[gcr.io/project/image:tag]"},"kryptor":{"n":"kryptor","t":"Modern file encryption and signing tool","s":[["TAGLINE","Modern file encryption and signing tool"],["TLDR","**Encrypt** files with a passphrase\n\n```kryptor -e [file]```\n\n**Decrypt** files\n\n```kryptor -d [file.kryptor]```\n\n**Generate** a key pair\n\n```kryptor -g```\n\n**Sign** a file\n\n```kryptor -s [file]```\n\n**Verify** a signature\n\n```kryptor -v [file]```"],["SYNOPSIS","**kryptor** [*options*] [*files*...]"],["DESCRIPTION","**kryptor** encrypts and signs files using modern cryptographic primitives with few knobs—positioned as a friendlier alternative to **age** + **minisign**. Supports passphrases, symmetric keys, and asymmetric multi-recipient encryption; private keys are encrypted at rest. Cross-platform CLI for Windows, Linux, and macOS."],["PARAMETERS","**-e**, **--encrypt**\n\n> Encrypt files/directories.\n\n**-d**, **--decrypt**\n\n> Decrypt.\n\n**-g**, **--generate**\n\n> Create key pairs.\n\n**-s**, **--sign** / **-v**, **--verify**\n\n> Detached signatures with optional comments.\n\nExact long options and recipient flags: **kryptor --help** or https://www.kryptor.co.uk/"],["CAVEATS","Keep passphrases and private keys safe; lost keys mean lost data. Verify you are using the official binary. Encryption does not replace backups."],["INSTALL","```nix: nix profile install nixpkgs#kryptor```"],["SEE ALSO","[age](/man/age)(1), [minisign](/man/minisign)(1), [gpg](/man/gpg)(1)"],["RESOURCES","```[Source code](https://github.com/samuel-lucas6/Kryptor)```\n\n```[Homepage](https://www.kryptor.co.uk/)```"]],"e":["age","minisign","gpg"],"x":"kryptor -e [file]"},"mp4decrypt":{"n":"mp4decrypt","t":"Decrypt encrypted MP4 files","s":[["TAGLINE","Decrypt encrypted MP4 files"],["TLDR","**Decrypt** an MP4 file using a track ID and key\n\n```mp4decrypt --key [track_id]:[hex_key] [input.mp4] [output.mp4]```\n\n**Decrypt** using a **KID** (Key ID) and key for MPEG-CENC content\n\n```mp4decrypt --key [hex_kid]:[hex_key] [input.mp4] [output.mp4]```\n\n**Decrypt** with **multiple keys** for separate audio and video tracks\n\n```mp4decrypt --key 1:[hex_key1] --key 2:[hex_key2] [input.mp4] [output.mp4]```\n\n**Decrypt** fragmented MP4 segments with a separate init file\n\n```mp4decrypt --fragments-info [init.mp4] --key 1:[hex_key] [segment.m4s] [output.m4s]```\n\n**Decrypt** with **progress** display\n\n```mp4decrypt --show-progress --key 1:[hex_key] [input.mp4] [output.mp4]```"],["SYNOPSIS","**mp4decrypt** [_options_] _input_ _output_"],["PARAMETERS","**--key** _\\<id\\>_:_\\<key\\>_\n> Specify a decryption key. _id_ is either a track ID (decimal) or a 128-bit KID (hex). _key_ is the 128-bit decryption key in hex. May be specified multiple times for different tracks.\n\n**--fragments-info** _\\<filename\\>_\n> Decrypt fragments using track information from a separate file. Used for fragmented MP4 workflows (DASH/HLS segments).\n\n**--show-progress**\n> Display progress details during decryption"],["DESCRIPTION","**mp4decrypt** decrypts MP4 files that have been encrypted with content protection schemes. It is part of the **Bento4** SDK, a cross-platform C++ toolkit for working with ISO-MP4 files, MPEG-DASH, HLS, and CMAF. It is the decryption counterpart to **mp4encrypt**.\n\nThe tool auto-detects the encryption scheme from the input file metadata. Supported schemes include **MPEG-CENC** (AES-CTR), **MPEG-CBCS** (AES-CBC subsample pattern), **PIFF** (Microsoft PlayReady), **OMA-PDCF**, **Marlin IPMP**, and **ISMACRYP**. These cover the DRM systems commonly used in industry: Widevine, PlayReady, FairPlay, and Marlin.\n\nFor **OMA DCF** files, use **1** as the track index. For **Marlin IPMP/ACGK** encryption, use **0** as the track ID. KIDs (hex Key IDs) are only applicable to **MPEG-CENC** family encryption."],["CAVEATS","You must supply the decryption keys yourself; mp4decrypt does not fetch, derive, or crack keys. When decrypting fragmented MP4 segments, omitting **--fragments-info** can result in 0-byte output files, and in some cases the tool may silently produce output that is not actually decrypted. In several failure modes (wrong key, wrong key ID format), mp4decrypt may produce output files without reporting an error. The open-source version is licensed under **GPLv2** with copyleft requirements; commercial use requires a separate license from Axiomatic Systems."],["HISTORY","**mp4decrypt** is part of the **Bento4** toolkit created by **Gilles Boccon-Gibod** of **Axiomatic Systems, LLC**, with development dating back to **2002**. The toolkit was first publicly available on SourceForge around **2005** and has been continuously expanded to support modern encryption schemes (MPEG-CENC, CBCS) and adaptive streaming formats (DASH, HLS, CMAF). It has been forked by **Dolby Laboratories** and **XBMC/Kodi**."],["INSTALL","```apk: sudo apk add bento4```\n\n```brew: brew install bento4```\n\n```nix: nix profile install nixpkgs#bento4```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [ffprobe](/man/ffprobe)(1)"]],"e":["ffmpeg","ffprobe"],"x":"mp4decrypt --key [track_id]:[hex_key] [input.mp4] [output.mp4]"},"mssql-cli":{"n":"mssql-cli","t":"Interactive Microsoft SQL Server command-line query tool","s":[["TAGLINE","Interactive Microsoft SQL Server command-line query tool"],["TLDR","**Connect** to a server\n\n```mssql-cli -S [server] -U [user] -d [database]```\n\n**Run a query** and exit\n\n```mssql-cli -S [server] -U [user] -d [db] -Q \"[SELECT 1]\"```"],["SYNOPSIS","**mssql-cli** [*options*]"],["DESCRIPTION","**mssql-cli** is a dbcli-style interactive client for SQL Server with multi-line editing, auto-completion, and pretty result tables. Install via **pip install mssql-cli**. Related to the broader dbcli family (**pgcli**, **mycli**)."],["PARAMETERS","**-S** *server*\n\n> SQL Server host.\n\n**-U** *user* / **-P** *password*\n\n> Credentials (prefer env/prompt over shell history).\n\n**-d** *database*\n\n> Database name.\n\n**-Q** *query*\n\n> Execute query non-interactively.\n\n**-E**\n\n> Use integrated authentication where supported.\n\nFull options: **mssql-cli --help**."],["CAVEATS","Check project maintenance status for your platform; Microsoft also ships **sqlcmd** and Azure Data Studio. Handle credentials securely."],["SEE ALSO","[sqlcmd](/man/sqlcmd)(1), [pgcli](/man/pgcli)(1), [mycli](/man/mycli)(1)"],["RESOURCES","```[Source code](https://github.com/dbcli/mssql-cli)```"]],"e":["sqlcmd","pgcli","mycli"],"x":"mssql-cli -S [server] -U [user] -d [database]"},"ng-generate":{"n":"ng-generate","t":"creates Angular artifacts using schematics","s":[["TAGLINE","creates Angular artifacts using schematics"],["TLDR","**Generate component**\n\n```ng generate component [name]```\n\n**Generate service**\n\n```ng generate service [name]```\n\n**Generate module**\n\n```ng generate module [name]```\n\n**Generate with shorthand**\n\n```ng g c [name]```\n\n**Generate in specific path**\n\n```ng generate component [path/name]```\n\n**Dry run**\n\n```ng generate component [name] --dry-run```"],["SYNOPSIS","**ng** **generate** _schematic_ _name_ [_options_]"],["SCHEMATICS","**component** (c) / **directive** (d) / **pipe** (p) / **service** (s)\n> Generate the corresponding Angular artifact.\n\n**module** (m)\n> Generate an NgModule.\n\n**guard** (g) / **interceptor**\n> Generate a route guard or HTTP interceptor.\n\n**class** / **interface** / **enum**\n> Generate a plain TypeScript class, interface, or enum.\n\n**resolver**\n> Generate a route data resolver.\n\n**library**\n> Generate a publishable library workspace.\n\n**application**\n> Add a new application to a multi-project workspace.\n\n**environments**\n> Create environment files for a project."],["PARAMETERS","**--dry-run**, **-d**\n> Show what would be generated without writing files.\n\n**--force**\n> Overwrite existing files.\n\n**--skip-tests**\n> Do not generate .spec.ts test files.\n\n**--skip-import**\n> Do not import the generated piece into its NgModule.\n\n**--standalone**\n> Generate a standalone component/directive/pipe (Angular 14+).\n\n**--inline-template**, **-t**\n> Place HTML inline in the component decorator.\n\n**--inline-style**, **-s**\n> Place CSS inline in the component decorator.\n\n**--style** _ext_\n> Stylesheet extension: css, scss, sass, less, none.\n\n**--prefix** _prefix_\n> Override the project's selector prefix.\n\n**--export**\n> Export the generated declaration from its NgModule.\n\n**--project** _name_\n> Target a specific project in the workspace."],["DESCRIPTION","**ng generate** creates Angular artifacts using schematics. It scaffolds components, services, modules, directives, pipes, guards, interceptors, and more, wiring them into the project's NgModule (or generating them as standalone in newer Angular versions) and adding matching .spec.ts files. The shorthand **ng g** is equivalent.\n\nSchematics live in **@schematics/angular** and other npm packages; libraries shipped via **ng add** can register their own (for example, **@angular/material** adds **material:navigation**). The available list can be inspected with **ng generate --help**."],["CAVEATS","Must be run inside an Angular CLI workspace (a directory containing **angular.json**). Newly generated files follow the project's coding-style settings (selector prefix, indent, style extension). The set of available schematics depends on which packages have been added with **ng add**."],["SEE ALSO","[ng](/man/ng)(1), [ng-add](/man/ng-add)(1)"]],"e":["ng","ng-add"],"x":"ng generate component [name]"},"ntfsls":{"n":"ntfsls","t":"lists files on NTFS partitions","s":[["TAGLINE","lists files on NTFS partitions"],["TLDR","**List directory contents**\n\n```ntfsls [/dev/sda1]```\n\n**List specific directory**\n\n```ntfsls [/dev/sda1] -p [/Users]```\n\n**Show all files including hidden**\n\n```ntfsls -a [/dev/sda1]```\n\n**Long listing format**\n\n```ntfsls -l [/dev/sda1]```"],["SYNOPSIS","**ntfsls** [_options_] _device_ [_path_]"],["PARAMETERS","_DEVICE_\n> NTFS partition device.\n\n**-p** _PATH_\n> Directory path to list.\n\n**-a**, **--all**\n> Show hidden files.\n\n**-l**, **--long**\n> Long listing format.\n\n**-F**, **--classify**\n> Append file type indicator.\n\n**--help**\n> Display help information."],["DESCRIPTION","**ntfsls** lists the contents of directories on NTFS partitions without mounting the filesystem. It reads the NTFS structures directly from the block device, providing an ls-like interface for examining NTFS volumes."],["CAVEATS","Part of ntfs-3g. Read-only operation. Device must be NTFS formatted."],["HISTORY","ntfsls was created as part of **ntfs-3g** tools for NTFS filesystem inspection."],["INSTALL","```apt: sudo apt install ntfs-3g```\n\n```dnf: sudo dnf install ntfs-3g```\n\n```pacman: sudo pacman -S ntfs-3g```\n\n```apk: sudo apk add ntfs-3g-progs```\n\n```zypper: sudo zypper install ntfs-3g```\n\n```brew: brew install ntfs-3g```"],["SEE ALSO","[ntfs-3g](/man/ntfs-3g)(1), [ls](/man/ls)(1), [ntfscat](/man/ntfscat)(1)"]],"e":["ntfs-3g","ls","ntfscat"],"x":"ntfsls [/dev/sda1]"},"odps-resource":{"n":"odps-resource","t":"manage MaxCompute (ODPS) resources","s":[["TAGLINE","manage MaxCompute (ODPS) resources"],["TLDR","**List all resources in the current project**\n\n```odps resource list```\n\n**Upload a JAR resource**\n\n```odps resource add [file.jar] -t jar```\n\n**Upload a Python file resource**\n\n```odps resource add [script.py] -t py```\n\n**Upload a generic file resource with a comment**\n\n```odps resource add [data.csv] -t file -c \"[description]\"```\n\n**Delete a resource by name**\n\n```odps resource drop [resource_name]```\n\n**Download a resource to a local file**\n\n```odps resource get [resource_name] -o [output_file]```"],["SYNOPSIS","**odps** **resource** [_command_] [_options_]"],["PARAMETERS","**list**\n> List all resources in the current project.\n\n**add** _FILE_\n> Upload a local file as a resource.\n\n**drop** _NAME_\n> Delete a resource by name.\n\n**get** _NAME_\n> Download a resource to local storage.\n\n**-t** _TYPE_\n> Resource type: jar, py, file, archive, or table.\n\n**-c** _COMMENT_\n> Add a comment or description to the resource.\n\n**-o** _OUTPUT_\n> Output file path for the get command.\n\n**-f**\n> Force overwrite if the resource already exists.\n\n**--help**\n> Display help information."],["DESCRIPTION","**odps resource** manages resources in Alibaba Cloud MaxCompute (formerly ODPS). Resources include JAR files for UDFs, Python scripts, plain files, and archives that can be referenced by MaxCompute jobs.\n\nUploaded resources are stored in the current project and can be used in SQL statements, MapReduce jobs, and user-defined functions. Part of the ODPS CLI (odpscmd) toolset."],["CAVEATS","Resource size limits apply (default 512 MB for files). Requires a valid ODPS configuration with project and credentials. Resource names must be unique within a project; use -f to overwrite existing resources."],["HISTORY","odps resource provides **resource management** for Alibaba Cloud MaxCompute."],["SEE ALSO","[odps](/man/odps)(1), [odps-func](/man/odps-func)(1), [odps-tunnel](/man/odps-tunnel)(1)"]],"e":["odps","odps-func","odps-tunnel"],"x":"odps resource list"},"pam.conf":{"n":"pam.conf","t":"Legacy PAM configuration file format where all services are configured in a single file","s":[["TAGLINE","Legacy PAM configuration file format where all services are configured in a single file"],["TLDR","**Basic authentication entry**\n\n```login auth required pam_unix.so```\n\n**Account management entry**\n\n```login account required pam_unix.so```\n\n**Session entry**\n\n```login session required pam_limits.so```\n\n**Password entry**\n\n```login password required pam_unix.so```"],["SYNOPSIS","**/etc/pam.conf**"],["PARAMETERS","**service**\n> Application name.\n\n**type**\n> auth, account, password, session.\n\n**control**\n> required, requisite, sufficient, optional.\n\n**module**\n> PAM module path.\n\n**arguments**\n> Module arguments."],["DESCRIPTION","**pam.conf** is the legacy PAM configuration file format where all services are configured in a single file. Modern systems typically use the /etc/pam.d/ directory with per-service files instead.\n\nEach line defines a rule with the format: service type control module-path [arguments]."],["EXAMPLE","```"],["/etc/pam.conf","login   auth       required   pam_securetty.so\nlogin   auth       required   pam_unix.so\nlogin   account    required   pam_unix.so\nlogin   session    required   pam_limits.so\nlogin   password   required   pam_unix.so\n\nother   auth       required   pam_deny.so\nother   account    required   pam_deny.so\n```"],["CONTROL FLAGS","```\nrequired   - Must succeed, continue checking\nrequisite  - Must succeed, fail immediately if not\nsufficient - Success ends checking, failure continues\noptional   - Result ignored unless only module\n```"],["CAVEATS","/etc/pam.d/ preferred on modern systems. Syntax errors can lock you out. Test with another root session."],["SEE ALSO","[pam.d](/man/pam.d)(5), [pam](/man/pam)(8), [pam_unix](/man/pam_unix)(8), [passwd](/man/passwd)(1), [sudo](/man/sudo)(8)"]],"e":["pam.d","pam","pam_unix","passwd","sudo"],"x":"login auth required pam_unix.so"},"pbmtextps":{"n":"pbmtextps","t":"renders text as PBM bitmap using PostScript fonts via Ghostscript","s":[["TAGLINE","renders text as PBM bitmap using PostScript fonts via Ghostscript"],["TLDR","**Render text using PostScript fonts**\n\n```pbmtextps -font [Times-Roman] \"[Hello World]\" > [output.pbm]```\n\n**Render with specific size**\n\n```pbmtextps -fontsize [48] \"[text]\" > [output.pbm]```"],["SYNOPSIS","**pbmtextps** [_options_] _text_"],["PARAMETERS","**-font** _name_\n> PostScript font name.\n\n**-fontsize** _n_\n> Font size in points.\n\n**-resolution** _n_\n> Output resolution in DPI."],["DESCRIPTION","**pbmtextps** renders text as PBM bitmap using PostScript fonts via Ghostscript. Provides high-quality text rendering with any PostScript font. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pbmtext](/man/pbmtext)(1), [ppmlabel](/man/ppmlabel)(1)"]],"e":["pbmtext","ppmlabel"],"x":"pbmtextps -font [Times-Roman] \"[Hello World]\" > [output.pbm]"},"ping":{"n":"ping","t":"uses ICMP ECHO_REQUEST packets to test network connectivity to a host","s":[["TAGLINE","uses ICMP ECHO_REQUEST packets to test network connectivity to a host"],["TLDR","**Ping a host** continuously\n\n```ping [host]```\n\n**Ping a host** a specific number of times\n\n```ping -c [5] [host]```\n\n**Ping with a specific interval** (seconds)\n\n```ping -i [0.5] [host]```\n\n**Ping with a specific packet size**\n\n```ping -s [1024] [host]```\n\n**Ping with a specific TTL**\n\n```ping -t [64] [host]```\n\n**Ping and show only summary** statistics\n\n```ping -q -c [10] [host]```\n\n**Ping using IPv6**\n\n```ping6 [host]```\n\n**Ping with audible alert** on response\n\n```ping -a [host]```"],["SYNOPSIS","**ping** [_options_] _destination_\n\n**ping6** [_options_] _destination_"],["PARAMETERS","**-c** _COUNT_\n> Stop after sending COUNT packets\n\n**-i** _INTERVAL_\n> Wait INTERVAL seconds between packets (default: 1)\n\n**-s** _SIZE_\n> Packet data size in bytes (default: 56, total 64 with ICMP header)\n\n**-t** _TTL_\n> Set IP Time To Live\n\n**-w** _DEADLINE_\n> Exit after DEADLINE seconds regardless of packets\n\n**-W** _TIMEOUT_\n> Wait TIMEOUT seconds for each response\n\n**-q**\n> Quiet output; only show summary\n\n**-v**\n> Verbose output\n\n**-a**\n> Audible ping (beep on reply)\n\n**-f**\n> Flood ping; send packets as fast as possible (root only)\n\n**-n**\n> Numeric output only; don't resolve hostnames\n\n**-4**\n> Use IPv4 only\n\n**-6**\n> Use IPv6 only\n\n**-I** _INTERFACE_\n> Use specified network interface or address\n\n**-R**\n> Record route (IPv4, may be ignored by hosts)\n\n**-D**\n> Print timestamps\n\n**-Q** _TOS_\n> Set Quality of Service bits\n\n**-p** _PATTERN_\n> Fill packet with specified pattern bytes"],["DESCRIPTION","**ping** uses ICMP ECHO_REQUEST packets to test network connectivity to a host. It measures round-trip time (RTT) and packet loss, making it essential for network troubleshooting and monitoring.\n\nEach response shows the sequence number, TTL, and time in milliseconds. The TTL value indicates how many router hops occurred (starting from the remote host's initial TTL, typically 64 or 255). Statistics at the end show packets transmitted/received, loss percentage, and RTT min/avg/max/mdev.\n\nThe command requires network access and may need root privileges for some options like flood ping. On many systems, **ping6** or **ping -6** is used for IPv6 targets."],["CAVEATS","Some hosts and firewalls block ICMP packets, causing ping to fail even when the host is reachable. Flood ping (**-f**) can overwhelm networks and should be used carefully. The **-R** record route option is often ignored by modern routers for security reasons. Very small intervals require root privileges."],["HISTORY","The ping command was written by **Mike Muuss** at the US Army Ballistic Research Laboratory in **December 1983**. The name comes from the sound of sonar, reflecting its purpose of testing if a remote host is \"there\". Muuss wrote it to diagnose network problems and it became one of the most widely used network utilities. The original implementation was for BSD Unix and has since been ported to virtually every operating system."],["INSTALL","```apt: sudo apt install iputils-ping```\n\n```apk: sudo apk add iputils-ping```"],["SEE ALSO","[traceroute](/man/traceroute)(8), [mtr](/man/mtr)(8), [netstat](/man/netstat)(8), [ss](/man/ss)(8)"]],"e":["traceroute","mtr","netstat","ss"],"x":"ping [host]"},"psidtopgm":{"n":"psidtopgm","t":"Convert PostScript image data to PGM","s":[["TAGLINE","Convert PostScript image data to PGM"],["TLDR","**Convert PostScript image to PGM**\n\n```psidtopgm [width] [height] [bits] [input.ps] > [output.pgm]```"],["SYNOPSIS","**psidtopgm** _width_ _height_ _bits_ [_file_]"],["PARAMETERS","_width_\n> Image width.\n\n_height_\n> Image height.\n\n_bits_\n> Bits per sample."],["DESCRIPTION","**psidtopgm** extracts a grayscale image from a PostScript image data operator output and converts to PGM. Part of Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pstopnm](/man/pstopnm)(1), [pgmtopbm](/man/pgmtopbm)(1)"]],"e":["pstopnm","pgmtopbm"],"x":"psidtopgm [width] [height] [bits] [input.ps] > [output.pgm]"},"pueue-edit":{"n":"pueue-edit","t":"Edit queued pueue task properties","s":[["TAGLINE","Edit queued pueue task properties"],["TLDR","**Edit a task's command** in your default editor\n\n```pueue edit [task_id]```\n\n**Edit a task's working directory**\n\n```pueue edit --path [task_id]```\n\n**Edit a task's label**\n\n```pueue edit --label [task_id]```\n\n**Edit a task's priority**\n\n```pueue edit --priority [task_id]```\n\n**Edit multiple tasks at once**\n\n```pueue edit [task_id1] [task_id2]```"],["SYNOPSIS","**pueue** **edit** [_options_] _task_id_..."],["DESCRIPTION","**pueue edit** modifies queued or stashed tasks. Opens the task property in your default editor (set via EDITOR or VISUAL environment variable) for modification. Only works on tasks that have not started running yet.\n\nMultiple task IDs can be provided to edit several tasks in sequence."],["PARAMETERS","**--command**\n> Edit the task's command. This is the default if no flag is specified.\n\n**--path**\n> Edit the task's working directory.\n\n**--label**\n> Edit the task's label.\n\n**--priority**\n> Edit the task's priority value."],["CAVEATS","Only queued or stashed tasks can be edited. Running, paused, or finished tasks cannot be modified. The editor used is determined by the EDITOR or VISUAL environment variable."],["INSTALL","```pacman: sudo pacman -S pueue```\n\n```apk: sudo apk add pueue```\n\n```zypper: sudo zypper install pueue```\n\n```brew: brew install pueue```\n\n```nix: nix profile install nixpkgs#pueue```"],["SEE ALSO","[pueue](/man/pueue)(1), [pueue-add](/man/pueue-add)(1), [pueue-stash](/man/pueue-stash)(1), [pueue-status](/man/pueue-status)(1), [pueue-restart](/man/pueue-restart)(1)"]],"e":["pueue","pueue-add","pueue-stash","pueue-status","pueue-restart"],"x":"pueue edit [task_id]"},"rdoc":{"n":"rdoc","t":"Generate Ruby documentation from source code","s":[["TAGLINE","Generate Ruby documentation from source code"],["TLDR","**Generate documentation**\n\n```rdoc [file.rb]```\n\n**Generate for directory**\n\n```rdoc [lib/]```\n\n**Generate with title**\n\n```rdoc --title \"[Project Name]\" [lib/]```\n\n**Output to directory**\n\n```rdoc -o [doc/] [lib/]```\n\n**Show help for class**\n\n```ri [Array]```"],["SYNOPSIS","**rdoc** [_options_] [_files_...]"],["PARAMETERS","**-o**, **--output** _dir_\n> Output directory.\n\n**--title** _text_\n> Documentation title.\n\n**-f**, **--format** _format_\n> Output format (html, ri).\n\n**-m**, **--main** _file_\n> Main page file.\n\n**-x**, **--exclude** _pattern_\n> Exclude files.\n\n**-a**, **--all**\n> Include all methods."],["DESCRIPTION","**RDoc** generates documentation from Ruby source files. It parses comments and code structure to create HTML documentation or ri data files."],["EXAMPLES","```bash"],["Generate HTML docs","rdoc lib/"],["With options","rdoc --title \"My Gem\" --main README.md lib/"],["Generate ri data","rdoc -f ri lib/"],["Exclude tests","rdoc -x test lib/"],["View with ri","ri String#split\nri Array\n```"],["DOCUMENTATION FORMAT","```ruby"],["Main description of class","#"],["obj = MyClass.new","#"],["@return [Boolean] success status","class MyClass\n  # Method description\n  def my_method(name)\n  end\nend\n```"],["CAVEATS","Part of Ruby standard library. Alternative: YARD for enhanced features."],["HISTORY","RDoc was created by **Dave Thomas** for Ruby documentation, included in Ruby since version 1.8."],["INSTALL","```apt: sudo apt install ruby```\n\n```dnf: sudo dnf install ruby```\n\n```pacman: sudo pacman -S ruby```\n\n```apk: sudo apk add ruby-rdoc```\n\n```zypper: sudo zypper install ruby```\n\n```brew: brew install ruby```\n\n```nix: nix profile install nixpkgs#ruby```"],["SEE ALSO","[ri](/man/ri)(1), [yard](/man/yard)(1), [ruby](/man/ruby)(1)"]],"e":["ri","yard","ruby"],"x":"rdoc [file.rb]"},"runit":{"n":"runit","t":"Cross-platform Unix init scheme with service supervision","s":[["TAGLINE","Cross-platform Unix init scheme with service supervision"],["TLDR","**Start runit init**\n\n```runit```\n\n**Run as stage 2**\n\n```runsvdir -P [/etc/service]```\n\n**Check service status**\n\n```sv status [service_name]```\n\n**Start service**\n\n```sv start [service_name]```\n\n**Stop service**\n\n```sv stop [service_name]```\n\n**Restart service**\n\n```sv restart [service_name]```"],["SYNOPSIS","**runit** [_options_]\n\n**sv** _command_ _service_"],["COMMANDS","**sv start** _SERVICE_\n> Start service.\n\n**sv stop** _SERVICE_\n> Stop service.\n\n**sv restart** _SERVICE_\n> Restart service.\n\n**sv status** _SERVICE_\n> Check status.\n\n**sv reload** _SERVICE_\n> Send SIGHUP.\n\n**sv once** _SERVICE_\n> Run once, no restart."],["DESCRIPTION","**runit** is a Unix init scheme with service supervision. It's an alternative to systemd and SysV init.\n\nThree stages handle system boot. Stage 1 initializes, stage 2 runs services, stage 3 shuts down.\n\nService directories contain run scripts. A simple executable script defines the service.\n\nSupervision restarts crashed services. Automatic recovery maintains system availability.\n\nLogging integrates with svlogd. Per-service log rotation and management."],["SERVICE STRUCTURE","- `/etc/service/name/run` - Service script\n- `/etc/service/name/log/run` - Logger script\n- `/etc/service/name/supervise/` - Status directory"],["CONFIGURATION","**/etc/service/**\n> Default service directory where symlinks to service definitions are placed. Each subdirectory represents a supervised service.\n\n**/etc/sv/**\n> Service definition directory containing run scripts and optional log/run scripts for each service.\n\n**/etc/runit/1**\n> Stage 1 script executed during system initialization before services start.\n\n**/etc/runit/2**\n> Stage 2 script that starts the service supervisor (typically runsvdir).\n\n**/etc/runit/3**\n> Stage 3 script executed during system shutdown for cleanup tasks."],["CAVEATS","Different from systemd syntax. Learning curve for run script creation. Less widespread than systemd."],["HISTORY","**runit** was created by **Gerrit Pape** around **2004** as a lightweight init system. It's used by Void Linux as the default init and available on many distributions."],["INSTALL","```apt: sudo apt install runit```\n\n```apk: sudo apk add runit```\n\n```brew: brew install runit```\n\n```nix: nix profile install nixpkgs#runit```"],["SEE ALSO","[sv](/man/sv)(1), [runsv](/man/runsv)(1), [runsvdir](/man/runsvdir)(1), [svlogd](/man/svlogd)(8)"]],"e":["sv","runsv","runsvdir","svlogd"],"x":"runit"},"sealert":{"n":"sealert","t":"Analyze and explain SELinux denial messages","s":[["TAGLINE","Analyze and explain SELinux denial messages"],["TLDR","**Analyze** all recent SELinux denials\n\n```sudo sealert -a /var/log/audit/audit.log```\n\n**Lookup** specific alert ID\n\n```sudo sealert -l alert_id```\n\nDisplay **summary** of recent alerts\n\n```sudo sealert -b```\n\nDisplay **all alerts** in HTML format\n\n```sudo sealert -a /var/log/audit/audit.log -H```"],["SYNOPSIS","**sealert** [_options_]"],["PARAMETERS","**-a**, **--analyze** _file_\n> Scan an audit log file for AVCs, analyze them, and write alerts to stdout.\n\n**-l**, **--lookupid** _id_\n> Look up a specific alert by ID and write it to stdout. Use `*` to return all alerts.\n\n**-b**, **--browser**\n> Launch the alert browser GUI.\n\n**-f**, **--fix** _uuid_\n> Execute the fix command for the AVC with the given UUID. Requires **--plugin**.\n\n**-P**, **--plugin** _plugin_\n> Specify the plugin name to use with **--fix**.\n\n**-H**\n> Output alerts in HTML format instead of plain text (used with **-l** or **-a**).\n\n**-s**, **--service**\n> Start the sealert D-Bus service (typically invoked by D-Bus).\n\n**-S**, **--noservice**\n> Start sealert as a standalone application without the D-Bus service.\n\n**-u**, **--user** _user_\n> Log on as the specified user.\n\n**-p**, **--password** _password_\n> Set the user password."],["DESCRIPTION","**sealert** analyzes and explains SELinux AVC (Access Vector Cache) denial messages. It provides human-readable explanations of why access was denied and suggests solutions.\n\nPart of setroubleshoot, it helps administrators understand and resolve SELinux policy violations."],["CAVEATS","Requires SELinux audit logging to be enabled. Solutions are suggestions and should be reviewed before applying."],["HISTORY","Part of **setroubleshoot-server** package, providing user-friendly SELinux troubleshooting."],["SEE ALSO","[audit2why](/man/audit2why)(1), [audit2allow](/man/audit2allow)(1), [ausearch](/man/ausearch)(8)"]],"e":["audit2why","audit2allow","ausearch"],"x":"sudo sealert -a /var/log/audit/audit.log"},"secon":{"n":"secon","t":"Display SELinux security contexts","s":[["TAGLINE","Display SELinux security contexts"],["TLDR","Get **current execution** context\n\n```secon```\n\nGet context of a **process**\n\n```secon --pid 1```\n\nGet context of a **file**\n\n```secon --file path/to/file```\n\nGet context of a **symlink** (don't resolve)\n\n```secon --link path/to/symlink```\n\n**Parse** a context specification\n\n```secon system_u:system_r:container_t:s0```"],["SYNOPSIS","**secon** [_options_] [_context_]"],["PARAMETERS","**--pid** _pid_\n> Get context of specified process\n\n**--file** _path_\n> Get context of file (follows symlinks)\n\n**--link** _path_\n> Get context of symlink (don't follow)"],["DESCRIPTION","**secon** retrieves and displays SELinux security contexts. It can show the context of the current execution, processes, files, or parse context specifications.\n\nOutput includes user, role, type, and level components of the SELinux context."],["CAVEATS","Requires SELinux to be enabled. File context may differ from actual access context."],["HISTORY","Part of **libselinux-utils**, providing SELinux context inspection tools."],["INSTALL","```apt: sudo apt install policycoreutils```\n\n```dnf: sudo dnf install policycoreutils```\n\n```zypper: sudo zypper install policycoreutils```\n\n```nix: nix profile install nixpkgs#policycoreutils```"],["SEE ALSO","[runcon](/man/runcon)(1), [chcon](/man/chcon)(1), [semanage](/man/semanage)(8)"]],"e":["runcon","chcon","semanage"],"x":"secon"},"showkey":{"n":"showkey","t":"Display keyboard keycodes and scancodes","s":[["TAGLINE","Display keyboard keycodes and scancodes"],["TLDR","**View** keycodes in decimal\n\n```sudo showkey```\n\n**Display** scancodes in hexadecimal\n\n```sudo showkey -s```\n\n**Display** keycodes in decimal\n\n```sudo showkey -k```\n\n**Display** keycodes in ASCII, decimal, and hexadecimal\n\n```sudo showkey -a```"],["SYNOPSIS","**showkey** [_options_]"],["PARAMETERS","**-s**, **--scancodes**\n> Display scancodes in hexadecimal.\n\n**-k**, **--keycodes**\n> Display keycodes in decimal (default mode).\n\n**-a**, **--ascii**\n> Display key values in ASCII, decimal, and hexadecimal.\n\n**-h**, **--help**\n> Display help message.\n\n**-V**, **--version**\n> Print version number and exit."],["DESCRIPTION","**showkey** prints to standard output either the scan codes, the keycode, or the ASCII code of each key pressed. This is useful for debugging keyboard-related issues, key remapping, and understanding keyboard input at a low level.\n\nIn scancode and keycode modes, the program runs until 10 seconds have elapsed since the last key press or release event. In ASCII mode, the program terminates when the user types Ctrl+D."],["CAVEATS","Must be run as root or with sudo. Only works on the Linux console, not in terminal emulators under X11 or Wayland. The keyboard becomes unusable for normal input while running."],["HISTORY","**showkey** is part of the **kbd** (keyboard tools) package for Linux console utilities."],["INSTALL","```apt: sudo apt install kbd```\n\n```dnf: sudo dnf install kbd```\n\n```pacman: sudo pacman -S kbd```\n\n```apk: sudo apk add kbd```\n\n```zypper: sudo zypper install kbd```\n\n```brew: brew install showkey```\n\n```nix: nix profile install nixpkgs#kbd```"],["SEE ALSO","[loadkeys](/man/loadkeys)(1), [dumpkeys](/man/dumpkeys)(1), [getkeycodes](/man/getkeycodes)(1), [xmodmap](/man/xmodmap)(1)"]],"e":["loadkeys","dumpkeys","getkeycodes","xmodmap"],"x":"sudo showkey"},"sirix-cli":{"n":"sirix-cli","t":"command-line client for SirixDB bitemporal databases","s":[["TAGLINE","command-line client for SirixDB bitemporal databases"],["TLDR","**Create** a JSON database and resource\n\n```sirix-cli -l /tmp/mydb create json -r myresource -d '{\"name\": \"Alice\"}'```\n\n**Query** a resource\n\n```sirix-cli -l /tmp/mydb query -r myresource```\n\nRun a **JSONiq** expression ($$ is the document root)\n\n```sirix-cli -l /tmp/mydb query -r myresource '$$.name'```\n\n**Update** and create a new revision\n\n```sirix-cli -l /tmp/mydb update -r myresource '{\"team\": \"engineering\"}' -im as-first-child```\n\nQuery a **previous revision**\n\n```sirix-cli -l /tmp/mydb query -r myresource -rev 1```\n\nShow **revision history** for a resource\n\n```sirix-cli -l /tmp/mydb resource-history myresource```"],["SYNOPSIS","**sirix-cli** **-l** _path_ _COMMAND_ [_ARGS_] [_OPTIONS_]"],["PARAMETERS","**-l** _path_\n> Local database path (directory)\n\n**create** _type_ **-r** _resource_ **-d** _data_\n> Create a database resource (for example **json**) with initial document data\n\n**query** **-r** _resource_ [_expression_]\n> Read the resource; optional JSONiq/XQuery expression; **-rev** selects a revision\n\n**update** **-r** _resource_ _data_ **-im** _mode_\n> Apply an update that creates a new revision\n\n**resource-history** _resource_\n> List revision history for a resource"],["DESCRIPTION","**sirix-cli** is the full-featured command-line interface for **SirixDB**, an embeddable, append-only, bitemporal database that stores every commit as a space-efficient snapshot via structural sharing. Unlike tools that overwrite current state, SirixDB keeps full history so any past revision can be opened as quickly as the latest.\n\nThe CLI operates on a local database directory (**-l**), creating JSON (or XML) resources, running JSONiq/XQuery queries, applying updates that produce new revisions, and inspecting history. It complements **sirix-shell** (interactive query REPL) and the REST API / MCP server for remote and agent use."],["CAVEATS","SirixDB requires a modern Java runtime (or a GraalVM native build of the CLI). Write concurrency is one writer per resource; readers are snapshot-isolated. Paths and storage layout are managed by SirixDB under the database directory—do not manually edit those files while the database is open."],["HISTORY","**SirixDB** is an open-source bitemporal storage system maintained by Johannes Lichtenberger and contributors; **sirix-cli** is the Kotlin CLI module in the main **sirix** repository."],["SEE ALSO","[sirix-shell](/man/sirix-shell)(1), [java](/man/java)(1), [jq](/man/jq)(1)"],["RESOURCES","```[Source code](https://github.com/sirixdb/sirix)```\n\n```[Homepage](https://sirix.io)```\n\n```[Documentation](https://sirix.io/docs/)```"]],"e":["sirix-shell","java","jq"],"x":"sirix-cli -l /tmp/mydb create json -r myresource -d '{\"name\": \"Alice\"}'"},"sox":{"n":"sox","t":"Universal command-line audio processor","s":[["TAGLINE","Universal command-line audio processor"],["TLDR","**Convert audio format**\n\n```sox [input.wav] [output.mp3]```\n\n**Concatenate files**\n\n```sox [file1.wav] [file2.wav] [output.wav]```\n\n**Change sample rate**\n\n```sox [input.wav] -r [44100] [output.wav]```\n\n**Mix to mono**\n\n```sox [input.wav] [output.wav] channels 1```\n\n**Add reverb**\n\n```sox [input.wav] [output.wav] reverb```\n\n**Trim audio**\n\n```sox [input.wav] [output.wav] trim [0] [10]```\n\n**Normalize volume**\n\n```sox [input.wav] [output.wav] norm```\n\n**Record audio**\n\n```sox -d [output.wav]```"],["SYNOPSIS","**sox** [_options_] _infiles_ _outfile_ [_effects_...]"],["DESCRIPTION","**sox** (Sound eXchange) is a command-line audio processing tool. It converts between formats, applies effects, and can record and play audio.\n\nThe tool supports many audio formats and provides extensive processing capabilities including filtering, mixing, and format conversion."],["PARAMETERS","**-r** _rate_\n> Sample rate.\n\n**-c** _channels_\n> Number of channels.\n\n**-b** _bits_\n> Sample size in bits.\n\n**-e** _encoding_\n> Sample encoding.\n\n**-t** _type_\n> File type.\n\n**-v** _factor_\n> Volume adjustment.\n\n**-d**\n> Default audio device.\n\n**--combine** _type_\n> Combine method (concatenate, merge, mix)."],["EFFECTS","**trim**: Cut portion\n**fade**: Fade in/out\n**reverb**: Add reverb\n**norm**: Normalize\n**speed**: Change speed\n**pitch**: Shift pitch\n**equalizer**: EQ adjustment\n**compand**: Compression"],["CAVEATS","Some formats need external libraries. Complex effects chains can be slow. Quality depends on parameters. Some effects modify duration."],["HISTORY","**SoX** was started by **Lance Norskog** in **1991** and has been developed by many contributors. It became the Swiss Army knife of audio processing on Unix systems."],["INSTALL","```apt: sudo apt install sox```\n\n```dnf: sudo dnf install sox```\n\n```pacman: sudo pacman -S sox```\n\n```apk: sudo apk add sox```\n\n```zypper: sudo zypper install sox```\n\n```brew: brew install sox```\n\n```nix: nix profile install nixpkgs#sox```"],["SEE ALSO","[ffmpeg](/man/ffmpeg)(1), [lame](/man/lame)(1), [flac](/man/flac)(1), [aplay](/man/aplay)(1)"]],"e":["ffmpeg","lame","flac","aplay"],"x":"sox [input.wav] [output.mp3]"},"systemctl-add-wants":{"n":"systemctl-add-wants","t":"Add soft systemd unit dependencies","s":[["TAGLINE","Add soft systemd unit dependencies"],["TLDR","Add **Wants** dependency to a target\n\n```systemctl add-wants [target] [unit]```\n\nAdd **multiple** dependencies\n\n```systemctl add-wants [target] [unit1 unit2 ...]```\n\nAdd **user-level** dependency\n\n```systemctl add-wants [target] [unit] --user```"],["SYNOPSIS","**systemctl add-wants** _TARGET_ _UNIT_..."],["PARAMETERS","**--system**\n> Operate on system configuration (default)\n\n**--user**\n> Operate on user configuration\n\n**--runtime**\n> Make temporary changes until reboot\n\n**--global**\n> Apply globally for all user logins"],["DESCRIPTION","**systemctl add-wants** appends `Wants=` dependencies to a specified systemd target for one or more units. A `Wants=` relationship establishes a soft dependency: the target will attempt to start the listed units, but will not fail if they cannot be activated.\n\nThis command provides a programmatic way to establish unit dependencies without manually editing unit files or creating symlinks in `.wants/` directories. It is commonly used to enable services at specific boot targets."],["CAVEATS","Changes made without --runtime are persistent and survive reboots. Wants dependencies are weaker than Requires; units may fail to start without affecting the target. For critical dependencies where failure should propagate, use add-requires instead."],["HISTORY","The **add-wants** subcommand was added to **systemctl** in systemd version **217**. Systemd was created by **Lennart Poettering** and **Kay Sievers** at Red Hat and has been the default init system for most major Linux distributions since **2015**."],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemctl-add-requires](/man/systemctl-add-requires)(1)"]],"e":["systemctl","systemctl-add-requires"],"x":"systemctl add-wants [target] [unit]"},"theattyr":{"n":"theattyr","t":"Terminal theater for playing VT100 art and animations","s":[["TAGLINE","Terminal theater for playing VT100 art and animations"],["TLDR","**Launch with the file browser**\n\n```theattyr```\n\n**Play a specific animation**\n\n```theattyr [fireworks.vt]```\n\n**Set playback frame rate**\n\n```theattyr --fps [30]```"],["SYNOPSIS","**theattyr** [_options_] [_file.vt_]"],["DESCRIPTION","**theattyr** is a terminal application for browsing and playing VT100 art and animations from the retro ANSI art scene. It includes 100+ pre-loaded VT100 animation files, supports RetroArch shaders for visual effects, and provides an interactive file browser with adjustable playback speed."],["HISTORY","**theattyr** was created by **Orhun Parmaksiz** and is written in **Rust**."],["INSTALL","```nix: nix profile install nixpkgs#theattyr```"],["SEE ALSO","[asciinema](/man/asciinema)(1), [terminaltexteffects](/man/terminaltexteffects)(1), [vhs](/man/vhs)(1)"]],"e":["asciinema","terminaltexteffects","vhs"],"x":"theattyr"},"transmission-cli":{"n":"transmission-cli","t":"Command-line BitTorrent downloader","s":[["TAGLINE","Command-line BitTorrent downloader"],["TLDR","**Download a torrent**\n\n```transmission-cli [path/to/file.torrent]```\n\n**Download a magnet link**\n\n```transmission-cli \"[magnet:?xt=urn:btih:...]\"```\n\n**Download to a specific directory**\n\n```transmission-cli -w [path/to/download/dir] [file.torrent]```\n\n**Download with speed limits** (KB/s)\n\n```transmission-cli -d [500] -u [100] [file.torrent]```\n\n**Download and exit immediately** when complete (don't seed)\n\n```transmission-cli -m [file.torrent]```\n\n**Download with peer blocklist enabled**\n\n```transmission-cli -b [file.torrent]```\n\n**Run in quiet mode**\n\n```transmission-cli -q [file.torrent]```"],["SYNOPSIS","**transmission-cli** [**-b**] [**-d** _limit_] [**-u** _limit_] [**-g** _dir_] [**-m**] [**-w** _dir_] [**-q**] _torrent-file_|_magnet-uri_"],["PARAMETERS","**-b**, **--blocklist**\n> Enable peer blocklists. Transmission understands the bluetack blocklist format.\n\n**-d** _limit_\n> Set maximum download speed in KB/s. 0 means unlimited.\n\n**-u** _limit_\n> Set maximum upload speed in KB/s. 0 means unlimited.\n\n**-f** _script_\n> Run a script when the torrent finishes downloading.\n\n**-g**, **--config-dir** _dir_\n> Directory for configuration files. Allows sharing config between different Transmission clients.\n\n**-m**\n> Do not seed after download completes; exit immediately.\n\n**-p**, **--port** _port_\n> Set the port to listen for incoming peers. Default: 51413.\n\n**-w** _dir_\n> Directory to save downloaded files. Defaults to current directory.\n\n**-q**\n> Quiet mode. Suppress most output, show only critical messages.\n\n**-h**, **--help**\n> Display help message and exit.\n\n**-v**, **--version**\n> Display version number and exit."],["DESCRIPTION","**transmission-cli** is a lightweight, command-line BitTorrent client for downloading and uploading torrents. It supports both .torrent files and magnet URIs, making it suitable for servers, headless systems, and scripted torrent operations.\n\nThe client handles a single torrent per invocation, downloading the content and optionally seeding upon completion. Progress is displayed in the terminal showing download/upload speeds, peer connections, and completion percentage.\n\nConfiguration is stored in ~/.config/transmission by default, allowing resume functionality if a download is interrupted. The client respects the TRANSMISSION_HOME environment variable for custom config locations and http_proxy for tracker announces.\n\nSending SIGHUP to a running transmission-cli process contacts the tracker for additional peers."],["CAVEATS","transmission-cli handles only one torrent at a time and is not designed for continuous operation. For managing multiple torrents or running as a service, use **transmission-daemon** with **transmission-remote** instead. The client must remain running for the entire download; closing it will interrupt the transfer."],["HISTORY","**Transmission** was originally created by **Eric Petit** and **Josh Elsasser** in **2005** for macOS, with Linux support following shortly after. The command-line interface was added to provide headless operation capabilities. Transmission became known for its lightweight design and cross-platform support, becoming one of the default BitTorrent clients on many Linux distributions."],["INSTALL","```apt: sudo apt install transmission-cli```\n\n```dnf: sudo dnf install transmission-cli```\n\n```pacman: sudo pacman -S transmission-cli```\n\n```apk: sudo apk add transmission-cli```\n\n```brew: brew install transmission-cli```"],["SEE ALSO","[transmission-daemon](/man/transmission-daemon)(1), [transmission-remote](/man/transmission-remote)(1)"]],"e":["transmission-daemon","transmission-remote"],"x":"transmission-cli [path/to/file.torrent]"},"transmission-remote":{"n":"transmission-remote","t":"Control Transmission daemon remotely","s":[["TAGLINE","Control Transmission daemon remotely"],["TLDR","**List all torrents**\n\n```transmission-remote -l```\n\n**Add a torrent**\n\n```transmission-remote -a [path/to/file.torrent]```\n\n**Add a magnet link**\n\n```transmission-remote -a \"[magnet:?xt=urn:btih:...]\"```\n\n**Start a specific torrent** by ID\n\n```transmission-remote -t [id] -s```\n\n**Pause a torrent**\n\n```transmission-remote -t [id] -S```\n\n**Remove a torrent** (keep downloaded files)\n\n```transmission-remote -t [id] -r```\n\n**Remove a torrent and delete files**\n\n```transmission-remote -t [id] -rad```\n\n**Connect to remote daemon** with authentication\n\n```transmission-remote [host:port] --auth [user:password] -l```"],["SYNOPSIS","**transmission-remote** [_host:port_] [**--auth** _user:pass_] [**-t** _id_] [_options_]"],["PARAMETERS","**-l**, **--list**\n> List all torrents with status, progress, and speeds.\n\n**-a** _file_, **--add** _file_\n> Add a torrent file or magnet URI.\n\n**-t** _id_\n> Select torrent(s) by ID. Can be single ID, range (1-5), comma list (1,3,5), or 'all'.\n\n**-s**, **--start**\n> Start the selected torrent(s).\n\n**-S**, **--stop**\n> Stop (pause) the selected torrent(s).\n\n**-r**, **--remove**\n> Remove the selected torrent(s) but keep downloaded files.\n\n**-rad**, **--remove-and-delete**\n> Remove the selected torrent(s) and delete downloaded files.\n\n**-d** _limit_, **--downlimit** _limit_\n> Set global download speed limit in KB/s.\n\n**-u** _limit_, **--uplimit** _limit_\n> Set global upload speed limit in KB/s.\n\n**--auth** _user:pass_\n> Provide authentication credentials for the daemon.\n\n**--port** _port_\n> Connect to specified RPC port (default: 9091).\n\n**--ssl**\n> Use SSL/TLS for the RPC connection.\n\n**-i**, **--info**\n> Show detailed information about the selected torrent.\n\n**-if**, **--info-files**\n> List files in the selected torrent.\n\n**-ip**, **--info-peers**\n> List connected peers for the selected torrent."],["DESCRIPTION","**transmission-remote** is a command-line utility for controlling the Transmission BitTorrent daemon remotely. It connects to transmission-daemon (default: localhost:9091) to manage torrents, configure settings, and monitor transfer status.\n\nThe tool provides full control over the daemon including adding and removing torrents, starting and stopping transfers, setting speed limits, viewing detailed torrent information, and managing individual files within torrents. Multiple torrents can be selected using the **-t** option with IDs, ranges, or 'all'.\n\nAuthentication can be provided via the **--auth** option, the TR_AUTH environment variable, or a .netrc file. For remote daemons, specify the host and port as the first argument."],["CAVEATS","Requires transmission-daemon to be running. Default connection is localhost:9091; remote connections may require firewall configuration. Authentication credentials in command-line arguments may be visible to other users via process listings; prefer environment variables or .netrc for sensitive environments."],["HISTORY","**transmission-remote** was developed as part of the **Transmission** project, which began in **2005**. The remote control utility was created to enable headless operation of the BitTorrent client, allowing system administrators to manage torrent downloads on servers without graphical interfaces. It has become a standard tool for automated torrent management in scripts and server environments."],["INSTALL","```apt: sudo apt install transmission-cli```\n\n```dnf: sudo dnf install transmission-cli```\n\n```pacman: sudo pacman -S transmission-cli```\n\n```apk: sudo apk add transmission-remote```\n\n```brew: brew install transmission-cli```"],["SEE ALSO","[transmission-daemon](/man/transmission-daemon)(1), [transmission-cli](/man/transmission-cli)(1), [transmission-create](/man/transmission-create)(1), [transmission-show](/man/transmission-show)(1)"]],"e":["transmission-daemon","transmission-cli","transmission-create","transmission-show"],"x":"transmission-remote -l"},"tuios":{"n":"tuios","t":"Terminal UI OS and window manager multiplexer","s":[["TAGLINE","Terminal UI OS and window manager multiplexer"],["TLDR","**Launch the terminal multiplexer**\n\n```tuios```\n\n**Start in SSH server mode**\n\n```tuios --ssh```\n\n**Play back a tape recording**\n\n```tuios play [recording.tape]```"],["SYNOPSIS","**tuios** [_options_]"],["DESCRIPTION","**tuios** is a terminal-based window manager built with Go using Bubble Tea. It provides a Vim-like modal interface, 9 independent workspaces, automatic tiling, mouse support, SSH server mode, tape scripting for automation, and 60Hz real-time updates."],["HISTORY","**tuios** was created by **Gaurav Gosain** (Gaurav-Gosain) and is written in **Go**."],["INSTALL","```brew: brew install tuios```\n\n```nix: nix profile install nixpkgs#tuios```"],["SEE ALSO","[tmux](/man/tmux)(1), [screen](/man/screen)(1), [zellij](/man/zellij)(1)"]],"e":["tmux","screen","zellij"],"x":"tuios"},"turm":{"n":"turm","t":"TUI for the Slurm workload manager","s":[["TAGLINE","TUI for the Slurm workload manager"],["TLDR","**Launch the Slurm job TUI**\n\n```turm```\n\n**Show only your jobs**\n\n```turm -u $USER```\n\n**Filter by partition**\n\n```turm --partition [gpu]```"],["SYNOPSIS","**turm** [_squeue-options_]"],["DESCRIPTION","**turm** provides a convenient terminal user interface for managing Slurm cluster jobs. It parses the output of squeue to display job information, updates the queue every two seconds, and displays log files with inotify-based live updates. It accepts the same options as squeue."],["HISTORY","**turm** was created by **Karim Abou Zeid** (kabouzeid) and is written in **Python**."],["SEE ALSO","[squeue](/man/squeue)(1), [scontrol](/man/scontrol)(1), [sacct](/man/sacct)(1)"]],"e":["squeue","scontrol","sacct"],"x":"turm"},"uv-sync":{"n":"uv-sync","t":"Synchronize project virtual environment","s":[["TAGLINE","Synchronize project virtual environment"],["TLDR","**Sync environment**\n\n```uv sync```\n\n**Sync without dev dependencies**\n\n```uv sync --no-dev```\n\n**Sync specific extra**\n\n```uv sync --extra [name]```\n\n**Sync from frozen lock**\n\n```uv sync --frozen```"],["SYNOPSIS","**uv** **sync** [_options_]"],["PARAMETERS","**--frozen**\n> Error if lock needs update.\n\n**--locked**\n> Assert lockfile is current.\n\n**--no-dev**\n> Skip dev dependencies.\n\n**--extra** _name_\n> Include optional extra.\n\n**--all-extras**\n> Include all extras."],["DESCRIPTION","**uv sync** synchronizes the virtual environment with project dependencies. Creates venv if needed, installs/removes packages to match lockfile. Core command for environment management."],["INSTALL","```dnf: sudo dnf install uv```\n\n```pacman: sudo pacman -S uv```\n\n```apk: sudo apk add uv```\n\n```brew: brew install uv```\n\n```nix: nix profile install nixpkgs#uv```"],["SEE ALSO","[uv](/man/uv)(1), [uv-lock](/man/uv-lock)(1), [uv-add](/man/uv-add)(1)"]],"e":["uv","uv-lock","uv-add"],"x":"uv sync"},"while":{"n":"while","t":"Shell loop with conditional execution","s":[["TAGLINE","Shell loop with conditional execution"],["TLDR","**Basic while loop**\n\n```while [condition]; do [command]; done```\n\n**Infinite loop**\n\n```while true; do [command]; sleep [1]; done```\n\n**Read file line by line**\n\n```while read -r line; do echo \"$line\"; done < [file.txt]```\n\n**Loop until command fails**\n\n```while [command]; do echo \"still running\"; done```\n\n**Counter loop**\n\n```i=0; while [ $i -lt 10 ]; do echo $i; i=$((i+1)); done```\n\n**Process command output**\n\n```[command] | while read -r line; do echo \"$line\"; done```\n\n**Loop with break**\n\n```while true; do if [condition]; then break; fi; done```"],["SYNOPSIS","**while** _CONDITION_; **do** _COMMANDS_; **done**"],["DESCRIPTION","**while** is a shell control structure that repeatedly executes a block of commands as long as the condition command returns a zero (success) exit status. The loop terminates when the condition returns non-zero.\n\nThe condition is typically a **test** command (or its **[** equivalent), but any command can be used. The loop executes as long as the command succeeds.\n\nCommon patterns include reading files line by line with **read**, implementing retry logic, and creating daemon-like processes that run indefinitely."],["PARAMETERS","**break**\n> Exit the loop immediately.\n\n**break** _N_\n> Exit N levels of nested loops.\n\n**continue**\n> Skip remaining commands and start next iteration.\n\n**continue** _N_\n> Continue at the Nth enclosing loop."],["CAVEATS","Using a pipe to while creates a subshell, so variable changes inside the loop are not visible outside. Use process substitution or here-strings to avoid this: **while read line; do ...; done < <(command)**. Always use **read -r** to prevent backslash interpretation."],["HISTORY","The while loop has been a fundamental shell control structure since the original Bourne shell in Unix Version 7 (1979). The syntax is specified by POSIX and works identically across all POSIX-compliant shells including bash, dash, ksh, and zsh."],["SEE ALSO","[for](/man/for)(1), [until](/man/until)(1), [test](/man/test)(1), [bash](/man/bash)(1)"]],"e":["for","until","test","bash"],"x":"while [condition]; do [command]; done"},"wireplumber":{"n":"wireplumber","t":"Session and policy manager for PipeWire","s":[["TAGLINE","Session and policy manager for PipeWire"],["TLDR","**Enable** with systemd\n\n```systemctl --user --now enable wireplumber```\n\n**Run** manually\n\n```wireplumber```\n\nUse custom **config** file\n\n```wireplumber --config-file [path/to/file]```\n\nDisplay **help**\n\n```wireplumber --help```\n\nDisplay **version**\n\n```wireplumber --version```"],["SYNOPSIS","**wireplumber** [_OPTIONS_]"],["PARAMETERS","**-c**, **--config-file** _FILE_\n> Use the specified main configuration file instead of `wireplumber.conf`.\n\n**-n**, **--name** _NAME_\n> Use a custom daemon name (useful for running multiple instances).\n\n**-d**, **--debug** _LEVEL_\n> Set log level (e.g. `D` for debug, `I` for info, `W` for warnings, `E` for errors) or a GLib-style domain:level spec.\n\n**-v**, **--verbose**\n> Increase log verbosity (equivalent to `--debug=D`).\n\n**--help**\n> Display help information.\n\n**--version**\n> Display version information."],["DESCRIPTION","**wireplumber** is a modular session/policy manager for PipeWire. It handles device routing, audio policy decisions, and session management for the PipeWire multimedia framework.\n\nThe tool provides a GObject-based high-level library that wraps PipeWire's API for easier integration."],["CAVEATS","Requires PipeWire to be running. Usually managed by systemd on modern systems. Configuration is Lua-based."],["INSTALL","```apt: sudo apt install wireplumber```\n\n```dnf: sudo dnf install wireplumber```\n\n```pacman: sudo pacman -S wireplumber```\n\n```apk: sudo apk add wireplumber```\n\n```zypper: sudo zypper install wireplumber```\n\n```brew: brew install wireplumber```\n\n```nix: nix profile install nixpkgs#wireplumber```"],["SEE ALSO","[wpctl](/man/wpctl)(1), [pipewire](/man/pipewire)(1), [pw-cli](/man/pw-cli)(1)"]],"e":["wpctl","pipewire","pw-cli"],"x":"systemctl --user --now enable wireplumber"},"zabbix_agentd":{"n":"zabbix_agentd","t":"Zabbix monitoring agent daemon","s":[["TAGLINE","Zabbix monitoring agent daemon"],["TLDR","**Start agent**\n\n```zabbix_agentd```\n\n**With config file**\n\n```zabbix_agentd -c [/etc/zabbix/zabbix_agentd.conf]```\n\n**Foreground mode**\n\n```zabbix_agentd -f```\n\n**Test a single item key**\n\n```zabbix_agentd -t [system.cpu.load]```\n\n**Print all supported items with current values**\n\n```zabbix_agentd -p```\n\n**Validate configuration file**\n\n```zabbix_agentd -T```\n\n**Increase log level at runtime**\n\n```zabbix_agentd -R log_level_increase```"],["SYNOPSIS","**zabbix_agentd** [_-c config_] [_-f_] [_-t item_] [_options_]"],["PARAMETERS","**-c**, **--config** _config-file_\n> Use an alternate config file instead of the default (/usr/local/etc/zabbix_agentd.conf).\n\n**-f**, **--foreground**\n> Run agent in foreground instead of as a daemon.\n\n**-p**, **--print**\n> Print all known items and their current values, then exit.\n\n**-t**, **--test** _item-key_\n> Test a single item key and print the result, then exit.\n\n**-R**, **--runtime-control** _runtime-option_\n> Perform administrative functions. Options: userparameter_reload, log_level_increase[=target], log_level_decrease[=target].\n\n**-T**, **--test-config**\n> Validate configuration file and exit.\n\n**-h**, **--help**\n> Display help and exit.\n\n**-V**, **--version**\n> Output version information and exit."],["DESCRIPTION","**zabbix_agentd** is the Zabbix monitoring agent daemon that collects system metrics and sends them to a Zabbix server or proxy. It gathers data on CPU, memory, disk, network, and other system resources using built-in item keys.\n\nThe agent supports two operating modes: passive mode where the server queries the agent on demand, and active mode where the agent initiates connections and sends data at configured intervals. Custom monitoring can be added through user parameters, which define commands that the agent executes to collect application-specific metrics.\n\nThe **-t** flag tests individual item keys for troubleshooting, and **-p** prints all supported items with their current values. Runtime control commands via **-R** allow changing log levels and reloading configuration without restarting the daemon."],["CAVEATS","Requires a Zabbix server or proxy to receive collected data. A configuration file is needed; the default path is /usr/local/etc/zabbix_agentd.conf. Some item keys require root privileges. Runtime control (-R) is not supported on OpenBSD, NetBSD, and Windows."],["HISTORY","**zabbix_agentd** is the agent daemon for **Zabbix**, an enterprise monitoring solution."],["INSTALL","```apt: sudo apt install zabbix-agent```\n\n```dnf: sudo dnf install zabbix-agent```\n\n```pacman: sudo pacman -S zabbix-agent```\n\n```apk: sudo apk add zabbix-agent```\n\n```zypper: sudo zypper install zabbix-agent```"],["SEE ALSO","[zabbix_server](/man/zabbix_server)(1), [zabbix_proxy](/man/zabbix_proxy)(1), [zabbix_get](/man/zabbix_get)(1)"]],"e":["zabbix_server","zabbix_proxy","zabbix_get"],"x":"zabbix_agentd"}});
