window.LCL_BUCKET(126,{"airflow":{"n":"airflow","t":"Platform to author, schedule, and monitor data workflows","s":[["TAGLINE","Platform to author, schedule, and monitor data workflows"],["TLDR","**Start the Airflow scheduler**\n\n```airflow scheduler```\n\n**Start the web server** on a specific port\n\n```airflow webserver --port [8080]```\n\n**List all DAGs**\n\n```airflow dags list```\n\n**Trigger a DAG run**\n\n```airflow dags trigger [dag_id]```\n\n**Trigger a DAG** with configuration parameters\n\n```airflow dags trigger [dag_id] --conf '{\"key\": \"value\"}'```\n\n**Test a specific task** in a DAG\n\n```airflow tasks test [dag_id] [task_id] [execution_date]```\n\n**Pause a DAG** (stop scheduling)\n\n```airflow dags pause [dag_id]```\n\n**Unpause a DAG** (resume scheduling)\n\n```airflow dags unpause [dag_id]```\n\n**List all DAG runs** for a specific DAG\n\n```airflow dags list-runs -d [dag_id]```\n\n**Initialize the database**\n\n```airflow db migrate```"],["SYNOPSIS","**airflow** _command_ [_subcommand_] [_options_]"],["PARAMETERS","**scheduler**\n> Start the Airflow scheduler daemon to trigger DAG runs\n\n**webserver**\n> Start the Airflow web interface server\n\n**triggerer**\n> Start the async trigger service for deferrable operators\n\n**dags**\n> Manage DAGs (list, trigger, pause, unpause, test, delete, backfill)\n\n**tasks**\n> Manage and test individual tasks (run, test, clear, list, render)\n\n**db**\n> Database operations (migrate, reset, clean, check, shell)\n\n**connections**\n> Manage connection configurations (add, delete, list, export, import)\n\n**variables**\n> Manage Airflow variables (get, set, delete, list, export, import)\n\n**pools**\n> Manage resource pools for task concurrency control\n\n**users**\n> Manage Airflow users (create, delete, list)\n\n**config**\n> View and manage configuration settings\n\n**providers**\n> Display information about installed providers\n\n**info**\n> Show system and environment information\n\n**version**\n> Display Airflow version\n\n**-o**, **--output** _format_\n> Output format: table, json, yaml, plain\n\n**-v**, **--verbose**\n> Enable verbose logging"],["DESCRIPTION","**Apache Airflow** is a platform for programmatically authoring, scheduling, and monitoring workflows. The CLI provides comprehensive control over DAGs (Directed Acyclic Graphs), tasks, connections, and the Airflow services.\n\nWorkflows are defined as Python code, creating DAGs that describe how tasks should be organized and executed. The scheduler triggers tasks based on defined schedules and dependencies, while the web interface provides monitoring and manual intervention capabilities.\n\nThe tool manages connections to external systems (databases, APIs, cloud services) and variables for configuration. Resource pools allow controlling task concurrency. The database stores metadata about DAG runs, task states, and history.\n\nCommon workflows include initializing the database with **db migrate**, starting the **scheduler** and **webserver**, and using **dags trigger** to manually start DAG runs. Tasks can be tested individually without affecting production state using **tasks test**."],["CONFIGURATION","**~/airflow/airflow.cfg**\n> Main configuration file controlling executor type, database connection, web server settings, scheduler behavior, and logging. Can also be overridden via environment variables prefixed with AIRFLOW__.\n\n**~/airflow/webserver_config.py**\n> Web server authentication and security configuration, including RBAC settings and authentication backends."],["CAVEATS","Requires proper configuration via **airflow.cfg** or environment variables before first use. The scheduler must be running for DAGs to execute on schedule. Database must be initialized with **airflow db migrate** before starting services. Some features require additional dependencies or executor configurations (Celery, Kubernetes)."],["HISTORY","Apache Airflow was created at **Airbnb** in **2014** by Maxime Beauchemin to manage their complex data pipelines. It was open-sourced in **2015** and became an Apache Incubator project in **2016**. It graduated to a top-level Apache project in **2019**. The platform has grown to become one of the most widely used workflow orchestration tools, with version 2.0 released in **December 2020** introducing significant architectural improvements, and version 3.0 bringing further enhancements."],["SEE ALSO","[luigi](/man/luigi)(1), [prefect](/man/prefect)(1), [dagster](/man/dagster)(1), [cron](/man/cron)(8), [docker](/man/docker)(1), [kubectl](/man/kubectl)(1)"]],"e":["luigi","prefect","dagster","cron","docker","kubectl"],"x":"airflow scheduler"},"ansible-inventory":{"n":"ansible-inventory","t":"Show Ansible inventory information","s":[["TAGLINE","Show Ansible inventory information"],["TLDR","**List** all hosts in inventory\n\n```ansible-inventory --list```\n\nShow **graph** of inventory\n\n```ansible-inventory --graph```\n\nShow **host details**\n\n```ansible-inventory --host [hostname]```\n\nUse **specific inventory**\n\n```ansible-inventory -i [inventory.ini] --list```\n\nOutput in **YAML** format\n\n```ansible-inventory --list --yaml```\n\nList hosts matching a **group pattern**\n\n```ansible-inventory --graph [webservers]```\n\n**Export** inventory to a file\n\n```ansible-inventory --list --export --output [inventory.json]```\n\nShow inventory in **TOML** format\n\n```ansible-inventory --list --toml```"],["SYNOPSIS","**ansible-inventory** [_options_] [_-i inventory_] [_--list_|_--graph_|_--host host_] [_group_]"],["DESCRIPTION","**ansible-inventory** inspects and displays Ansible inventory data. It shows how Ansible interprets your inventory, including dynamic inventories, group memberships, and host variables.\n\nThis tool is useful for debugging inventory configurations and understanding the effective inventory structure that Ansible will use."],["PARAMETERS","**--list**\n> Output all hosts in JSON format\n\n**--graph**\n> Output inventory as tree graph\n\n**--host** _hostname_\n> Output variables for specific host\n\n**-i** _inventory_\n> Inventory source\n\n**--yaml**\n> Output in YAML format\n\n**--vars**\n> Include host/group variables in output\n\n**--export**\n> Output compatible with inventory plugins\n\n**-y**\n> YAML output (shorthand for --yaml)\n\n**--playbook-dir** _path_\n> Set playbook directory for relative paths"],["CONFIGURATION","**/etc/ansible/hosts**\n> Default inventory file listing managed hosts and groups.\n\n**/etc/ansible/ansible.cfg**\n> System-wide Ansible configuration, including default inventory path.\n\n**~/.ansible.cfg**\n> Per-user Ansible configuration overriding system defaults.\n\n**ansible.cfg**\n> Project-level configuration in the current directory, highest priority."],["CAVEATS","Dynamic inventories are evaluated when this command runs. Large inventories may produce extensive output. Variables shown are pre-evaluation (no Jinja2 templating)."],["HISTORY","**ansible-inventory** was introduced in Ansible 2.4 (**2017**) to provide better inventory inspection capabilities, replacing ad-hoc methods of debugging inventory issues."],["INSTALL","```dnf: sudo dnf install ansible-core```\n\n```pacman: sudo pacman -S ansible-core```\n\n```apk: sudo apk add ansible-core```\n\n```zypper: sudo zypper install ansible-core```"],["SEE ALSO","[ansible](/man/ansible)(1), [ansible-playbook](/man/ansible-playbook)(1)"]],"e":["ansible","ansible-playbook"],"x":"ansible-inventory --list"},"apkleaks":{"n":"apkleaks","t":"Scan APKs for hardcoded secrets and keys","s":[["TAGLINE","Scan APKs for hardcoded secrets and keys"],["TLDR","**Scan** APK for hardcoded secrets\n\n```apkleaks -f [app.apk]```\n\nScan with **JSON output**\n\n```apkleaks -f [app.apk] -o [results.json]```\n\nScan with **custom patterns**\n\n```apkleaks -f [app.apk] -p [patterns.json]```\n\n**Verbose** output\n\n```apkleaks -f [app.apk] -v```"],["SYNOPSIS","**apkleaks** -f _apk_file_ [_-o output_] [_-p patterns_] [_options_]"],["DESCRIPTION","**apkleaks** scans Android APK files for hardcoded secrets, API keys, and sensitive information. It decompiles the APK, searches through code and resources using regex patterns, and reports potential security issues.\n\nThe tool helps identify accidental exposure of credentials, private keys, URLs with tokens, and other sensitive data that developers may have inadvertently included in their applications."],["PARAMETERS","**-f** _file_\n> APK file to analyze\n\n**-o** _file_\n> Output file for results\n\n**-p** _file_\n> Custom patterns file (JSON)\n\n**-a** _args_\n> Additional arguments to pass to the jadx decompiler.\n\n**--json**\n> Output results in JSON format.\n\n**-v**, **--verbose**\n> Verbose output, including matched line numbers.\n\n**--disassemble**\n> Disassemble bytecode (slower, may catch additional secrets)."],["DETECTED PATTERNS","- API keys (AWS, Google, Facebook, etc.)\n- Private keys and certificates\n- Tokens and secrets in URLs\n- Hardcoded passwords\n- Database credentials\n- Custom regex patterns"],["CAVEATS","May produce false positives requiring manual verification. Only detects string-based secrets; encrypted or obfuscated data won't be found. Decompilation may fail for heavily protected APKs."],["HISTORY","**apkleaks** was created for Android security assessments, automating the process of identifying leaked secrets that manual code review might miss."],["INSTALL","```brew: brew install apkleaks```\n\n```nix: nix profile install nixpkgs#apkleaks```"],["SEE ALSO","[apktool](/man/apktool)(1), [jadx](/man/jadx)(1), [androguard](/man/androguard)(1)"]],"e":["apktool","jadx","androguard"],"x":"apkleaks -f [app.apk]"},"apptainer-sign":{"n":"apptainer-sign","t":"Cryptographically sign SIF container images","s":[["TAGLINE","Cryptographically sign SIF container images"],["TLDR","**Sign** a container image using the default PGP key\n\n```apptainer sign [path/to/image.sif]```\n\nSign a container image using a **specific private key** file\n\n```apptainer sign --key [path/to/private.pem] [path/to/image.sif]```\n\nSign a container image using a **specific PGP key index**\n\n```apptainer sign -k [key_index] [path/to/image.sif]```\n\nSign a **specific object group** within the image\n\n```apptainer sign -g [group_id] [path/to/image.sif]```\n\nSign a **specific object by ID** within the image\n\n```apptainer sign -i [object_id] [path/to/image.sif]```"],["SYNOPSIS","**apptainer sign** [_options_] _image.sif_"],["DESCRIPTION","**apptainer sign** adds digital signatures to SIF container images, enabling verification of image integrity and authenticity. Signatures can use PGP keys or X.509 certificates.\n\nSigning containers is essential for establishing trust in shared container images and meeting security requirements in production environments."],["PARAMETERS","**--key** _file_\n> Path to a private key file for signing\n\n**-k, --keyidx** _index_\n> Index of the PGP key to use from the keyring\n\n**-g, --group-id** _id_\n> Sign only a specific object group within the SIF\n\n**-i, --sif-id** _id_\n> Sign only a specific object by ID within the SIF"],["CAVEATS","A PGP key pair or X.509 certificate must be configured before signing. The private key must be kept secure; compromised keys should be revoked immediately."],["INSTALL","```dnf: sudo dnf install apptainer```\n\n```pacman: sudo pacman -S apptainer```\n\n```apk: sudo apk add apptainer```\n\n```zypper: sudo zypper install apptainer```\n\n```brew: brew install apptainer```\n\n```nix: nix profile install nixpkgs#apptainer```"],["SEE ALSO","[apptainer](/man/apptainer)(1), [apptainer-verify](/man/apptainer-verify)(1), [apptainer-pull](/man/apptainer-pull)(1), [gpg](/man/gpg)(1)"]],"e":["apptainer","apptainer-verify","apptainer-pull","gpg"],"x":"apptainer sign [path/to/image.sif]"},"aria2c":{"n":"aria2c","t":"Multi-protocol multi-source download utility","s":[["TAGLINE","Multi-protocol multi-source download utility"],["TLDR","**Download** single file\n\n```aria2c [URL]```\n\nDownload with **16 connections**\n\n```aria2c -x 16 -s 16 [URL]```\n\nDownload to **specific directory**\n\n```aria2c -d [/path/to/dir] [URL]```\n\n**Continue** interrupted download\n\n```aria2c -c [URL]```\n\nDownload from **magnet link**\n\n```aria2c \"[magnet:?xt=urn:btih:...]\"```\n\nDownload with **RPC enabled**\n\n```aria2c --enable-rpc --rpc-listen-all```"],["SYNOPSIS","**aria2c** [_options_] [_URL_|_TORRENT_|_METALINK_]"],["DESCRIPTION","**aria2c** is the command-line interface for aria2, the multi-protocol download utility. It supports parallel downloads, segmented downloading, and multiple source URLs for maximizing download speed.\n\nThe tool handles HTTP/HTTPS, FTP, SFTP, BitTorrent, and Metalink, with extensive configuration options."],["PARAMETERS","**-x** _n_\n> Max connections per server (default: 1)\n\n**-s** _n_\n> Split file into n segments\n\n**-c**\n> Continue/resume download\n\n**-d** _dir_\n> Download directory\n\n**-o** _file_\n> Output filename\n\n**--file-allocation** _method_\n> none, prealloc, trunc, falloc\n\n**--max-overall-download-limit** _speed_\n> Overall speed limit\n\n**--enable-rpc**\n> Enable RPC interface\n\n**--bt-enable-lpd**\n> Enable Local Peer Discovery\n\n**--dht-listen-port** _port_\n> DHT port for BitTorrent\n\n**-j** _n_\n> Maximum number of parallel downloads (default: 5)\n\n**--seed-time** _minutes_\n> Seed time in minutes for BitTorrent (0 = no seeding)\n\n**-i** _file_, **--input-file** _file_\n> Read URLs from file\n\n**-q**, **--quiet**\n> Quiet mode"],["CONFIGURATION","**~/.aria2/aria2.conf**\n> Default configuration file for setting download options, connection limits, and protocol-specific settings."],["CAVEATS","aria2c is the same as aria2; the c stands for command-line. Server may limit connections despite client settings. BitTorrent features need proper network configuration."],["HISTORY","**aria2** was created by **Tatsuhiro Tsujikawa** and first released around **2006**. It is written in C++ and supports HTTP/HTTPS, FTP, SFTP, BitTorrent, and Metalink."],["INSTALL","```apt: sudo apt install aria2```\n\n```dnf: sudo dnf install aria2```\n\n```pacman: sudo pacman -S aria2```\n\n```apk: sudo apk add aria2```\n\n```zypper: sudo zypper install aria2```\n\n```brew: brew install aria2```\n\n```nix: nix profile install nixpkgs#aria2```"],["SEE ALSO","[aria2](/man/aria2)(1), [wget](/man/wget)(1), [curl](/man/curl)(1)"]],"e":["aria2","wget","curl"],"x":"aria2c [URL]"},"az-advisor":{"n":"az-advisor","t":"View and manage Azure best-practice recommendations","s":[["TAGLINE","View and manage Azure best-practice recommendations"],["TLDR","**List all Azure Advisor recommendations**\n\n```az advisor recommendation list```\n\n**List recommendations** for a specific category\n\n```az advisor recommendation list --category [Cost|Security|HighAvailability|Performance|OperationalExcellence]```\n\n**Show Advisor configuration** for the subscription\n\n```az advisor configuration show```\n\n**List configuration** for the entire subscription\n\n```az advisor configuration list```\n\n**Disable a recommendation**\n\n```az advisor recommendation disable --name [recommendation-name] --resource-group [rg-name]```\n\n**Enable a previously disabled** recommendation\n\n```az advisor recommendation enable --name [recommendation-name] --resource-group [rg-name]```"],["SYNOPSIS","**az** **advisor** _subcommand_ [_options_]"],["SUBCOMMANDS","**configuration list**\n> List Azure Advisor configuration for the entire subscription.\n\n**configuration show**\n> Show Azure Advisor configuration for the given subscription or resource group.\n\n**configuration update**\n> Update Azure Advisor configuration.\n\n**recommendation list**\n> List Azure Advisor recommendations.\n\n**recommendation disable**\n> Dismiss Azure Advisor recommendations.\n\n**recommendation enable**\n> Enable Azure Advisor recommendations."],["DESCRIPTION","**az advisor** manages Azure Advisor, a personalized cloud consultant that helps you follow best practices to optimize your Azure deployments. Azure Advisor analyzes resource configuration and usage telemetry, then recommends solutions to improve cost effectiveness, performance, reliability, and security.\n\nRecommendations are categorized into: Cost, Security, HighAvailability (Reliability), Performance, and OperationalExcellence."],["CAVEATS","Requires Azure CLI to be installed and authenticated with **az login**. Appropriate Azure RBAC permissions are needed to view and manage Advisor recommendations."],["INSTALL","```dnf: sudo dnf install azure-cli```\n\n```pacman: sudo pacman -S azure-cli```\n\n```zypper: sudo zypper install azure-cli```\n\n```brew: brew install azure-cli```\n\n```nix: nix profile install nixpkgs#azure-cli```"],["SEE ALSO","[az](/man/az)(1), [az-config](/man/az-config)(1), [az-feedback](/man/az-feedback)(1)"]],"e":["az","az-config","az-feedback"],"x":"az advisor recommendation list"},"batcat":{"n":"batcat","t":"Debian/Ubuntu name for the bat file viewer","s":[["TAGLINE","Debian/Ubuntu name for the bat file viewer"],["TLDR","> On Debian and Ubuntu the **bat** binary is installed as **batcat**. All options below are **bat** options.\n\n**Print** a file with syntax highlighting and line numbers\n\n```batcat [path/to/file]```\n\n**Concatenate** multiple files (like **cat**)\n\n```batcat [file1] [file2]```\n\n**Print** without decorations (no line numbers, headers, or grid)\n\n```batcat --plain [path/to/file]```\n\n**Show** non-printable characters\n\n```batcat --show-all [path/to/file]```\n\n**Highlight** a specific line range\n\n```batcat --line-range [40:60] [path/to/file]```\n\n**Force** a language for highlighting\n\n```batcat --language [json] [path/to/file]```\n\n**Make** the bat alias permanent in your shell config\n\n```alias bat=batcat```"],["SYNOPSIS","**batcat** [_options_] [_file_...]"],["DESCRIPTION","**batcat** is the command name used by the **bat** binary on Debian and Ubuntu. The package ships the program as **batcat** because the name **bat** is already taken by the unrelated **bacula-console-qt** package. Functionally it is identical to **bat**: a **cat** clone with syntax highlighting, Git integration, automatic paging, and file headers.\n\nMany distributions other than Debian/Ubuntu install it as **bat**. To use the **bat** name on Debian-based systems, add `alias bat=batcat` to your shell configuration, or create a symlink such as `~/.local/bin/bat`.\n\nSee **bat** for the full set of options and configuration. Common flags include **-p**/**--plain**, **-A**/**--show-all**, **-n**/**--number**, **--language**, **--theme**, **--style**, **--paging**, **--line-range**, and **--diff**."],["ENVIRONMENT","**BAT_THEME**\n> Default syntax-highlighting theme.\n\n**BAT_PAGER**\n> Pager command used by bat (defaults to **less**).\n\n**BAT_STYLE**\n> Default value for the **--style** option."],["CONFIGURATION","**~/.config/bat/config**\n> Default command-line options applied automatically. Shared with bat. Run `batcat --config-file` to print the active path."],["INSTALL","```apt: sudo apt install bat```\n\n```dnf: sudo dnf install bat```\n\n```pacman: sudo pacman -S bat```\n\n```zypper: sudo zypper install bat```\n\n```brew: brew install bat```\n\n```nix: nix profile install nixpkgs#bat```"],["SEE ALSO","[bat](/man/bat)(1), [cat](/man/cat)(1), [less](/man/less)(1), [highlight](/man/highlight)(1)"],["RESOURCES","```[Source code](https://github.com/sharkdp/bat)```"]],"e":["bat","cat","less","highlight"],"x":"batcat [path/to/file]"},"cal":{"n":"cal","t":"Display a calendar in the terminal","s":[["TAGLINE","Display a calendar in the terminal"],["TLDR","Display calendar for the **current month**\n\n```cal```\n\nDisplay **3 months** spanning the current date\n\n```cal -3```\n\nDisplay the **whole year**\n\n```cal -y```\n\nDisplay the **next twelve months**\n\n```cal -Y```\n\nUse **Monday** as the first day of the week\n\n```cal -m```\n\nDisplay a calendar for a **specific year**\n\n```cal [year]```\n\nDisplay a calendar for a **specific month and year**\n\n```cal [month] [year]```"],["SYNOPSIS","**cal** [_options_] [[_month_] _year_]"],["DESCRIPTION","**cal** displays a calendar in the terminal with the current day highlighted. It can show single months, multiple months, or entire years.\n\nBy default, Sunday is shown as the first day of the week. Use **-m** to start weeks on Monday."],["PARAMETERS","**-3, --three**\n> Display previous, current, and next month\n\n**-y, --year**\n> Display calendar for the current year\n\n**-Y, --twelve**\n> Display the next twelve months\n\n**-m, --monday**\n> Use Monday as the first day of the week\n\n**-s, --sunday**\n> Use Sunday as the first day of the week (default)\n\n**-j, --julian**\n> Display Julian days (day of year)\n\n**-n** _num_\n> Display _num_ months starting from current"],["INSTALL","```apt: sudo apt install ncal```\n\n```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[ncal](/man/ncal)(1), [date](/man/date)(1), [calcurse](/man/calcurse)(1)"],["RESOURCES","```[Source code](https://github.com/util-linux/util-linux)```"]],"e":["ncal","date","calcurse"],"x":"cal"},"cgdelete":{"n":"cgdelete","t":"remove control groups","s":[["TAGLINE","remove control groups"],["TLDR","**Delete a cgroup**\n\n```cgdelete -g [cpu]:[/mygroup]```\n\n**Delete a cgroup** from multiple controllers\n\n```cgdelete -g [cpu,memory]:[/mygroup]```\n\n**Delete a cgroup recursively** (including children)\n\n```cgdelete -r -g [cpu]:[/mygroup]```\n\n**Delete cgroup and all empty parents**\n\n```cgdelete -g [memory]:[/parent/child]```"],["SYNOPSIS","**cgdelete** [**-r**] [**-g** _controllers_:_path_]..."],["DESCRIPTION","**cgdelete** removes cgroups from the Linux control group hierarchy. Cgroups (control groups) are used to limit, account for, and isolate resource usage of processes.\n\nThe command removes the specified cgroup directory from the cgroup filesystem. By default, the cgroup must be empty (no processes or child cgroups) before deletion. The -r flag allows recursive deletion of child cgroups.\n\ncgdelete is part of libcgroup-tools, which provides user-space utilities for managing cgroups v1. Note that cgroups v2 uses a unified hierarchy and different management approaches."],["PARAMETERS","**-g** _controllers_:_path_\n> Specify controller(s) and cgroup path to delete.\n\n**-r**\n> Recursively delete child cgroups.\n\n**-h**, **--help**\n> Display help information."],["CAVEATS","Cannot delete cgroups that contain running processes. Requires appropriate permissions (typically root). The specified controllers must be mounted. Does not work with cgroups v2 unified hierarchy (use direct filesystem operations or systemd instead). Empty parent cgroups may be automatically removed."],["HISTORY","**cgdelete** is part of **libcgroup**, a library and tools for managing Linux cgroups. The project was started by **Red Hat** around **2008** to provide user-space utilities for cgroups v1. As Linux moved toward cgroups v2, systemd became the primary cgroup manager on most systems, though libcgroup tools remain useful for cgroups v1 systems."],["INSTALL","```apt: sudo apt install cgroup-tools```\n\n```apk: sudo apk add cgroup-tools```"],["SEE ALSO","[cgcreate](/man/cgcreate)(1), [cgset](/man/cgset)(1), [cgget](/man/cgget)(1), [lscgroup](/man/lscgroup)(1)"],["RESOURCES","```[Source code](https://github.com/libcgroup/libcgroup)```"]],"e":["cgcreate","cgset","cgget","lscgroup"],"x":"cgdelete -g [cpu]:[/mygroup]"},"clang":{"n":"clang","t":"LLVM C/C++/Objective-C compiler","s":[["TAGLINE","LLVM C/C++/Objective-C compiler"],["TLDR","**Compile** C program\n\n```clang [program.c] -o [program]```\n\n**Compile** with optimization\n\n```clang -O3 [program.c] -o [program]```\n\n**Compile** with debug info\n\n```clang -g [program.c] -o [program]```\n\n**Enable** all warnings\n\n```clang -Wall -Wextra [program.c] -o [program]```\n\n**Compile** C++ program\n\n```clang++ [program.cpp] -o [program]```"],["SYNOPSIS","**clang** [_options_] _file_..."],["DESCRIPTION","**clang** is a C, C++, and Objective-C compiler based on the LLVM compiler infrastructure. It provides fast compilation, excellent diagnostics with clear error messages, and is designed as a drop-in replacement for GCC on most platforms.\n\nClang is known for its expressive error messages that include source highlighting, fix-it hints, and template backtrace improvements over GCC. Its modular library-based architecture enables reuse in tools like clang-format, clang-tidy, and clangd.\n\nThe compiler supports modern language standards (C17, C++20, C++23), sanitizers for detecting memory errors and undefined behavior at runtime, and extensive static analysis capabilities. It is the default compiler on macOS and FreeBSD and widely used on Linux."],["PARAMETERS","**-o** _file_\n> Output file name\n\n**-c**\n> Compile without linking\n\n**-g**\n> Generate debug information\n\n**-O**[_level_]\n> Optimization (0, 1, 2, 3, fast, s, z)\n\n**-std=**_standard_\n> Language standard (c11, c17, c++17, c++20)\n\n**-Wall**\n> Enable common warnings\n\n**-Wextra**\n> Enable extra warnings\n\n**-Werror**\n> Treat warnings as errors\n\n**-I** _dir_\n> Add include directory\n\n**-L** _dir_\n> Add library directory\n\n**-l** _lib_\n> Link library\n\n**-fsanitize=**_type_\n> Enable sanitizer (address, thread, memory, undefined)"],["OPTIMIZATION LEVELS","- **-O0** - No optimization (default)\n- **-O1** - Basic optimization\n- **-O2** - Moderate optimization (recommended)\n- **-O3** - Aggressive optimization\n- **-Os** - Optimize for size\n- **-Ofast** - Maximum speed (may break standards)"],["WORKFLOW","```bash"],["Simple compile","clang hello.c -o hello"],["With warnings and optimization","clang -Wall -Wextra -O2 program.c -o program"],["Debug build","clang -g -O0 program.c -o program"],["C++ compilation","clang++ -std=c++20 program.cpp -o program"],["With sanitizers","clang -fsanitize=address -g program.c -o program"],["Multiple source files","clang main.c utils.c -o program"],["Link with library","clang program.c -o program -lm -lpthread\n```"],["SANITIZERS","**Address Sanitizer:**\n> Detect memory errors\n\n**Thread Sanitizer:**\n> Detect data races\n\n**Undefined Behavior Sanitizer:**\n> Detect undefined behavior\n\n**Memory Sanitizer:**\n> Detect uninitialized memory reads"],["CAVEATS","Some GCC-specific features not supported. Different optimization behavior than GCC. Sanitizers add overhead. Error messages more verbose than some prefer. Compilation may be slower than GCC in some cases."],["HISTORY","**clang** was created by Apple and the LLVM community starting in **2007** as a modern, modular compiler to replace GCC, first released in **2009**."],["INSTALL","```apt: sudo apt install clang```\n\n```dnf: sudo dnf install clang```\n\n```pacman: sudo pacman -S clang```\n\n```apk: sudo apk add clang22```\n\n```zypper: sudo zypper install clang```\n\n```nix: nix profile install nixpkgs#clang```"],["SEE ALSO","[gcc](/man/gcc)(1), [clang++](/man/clang++)(1)"]],"e":["gcc","clang++"],"x":"clang [program.c] -o [program]"},"clido":{"n":"clido","t":"command-line todo list manager","s":[["TAGLINE","command-line todo list manager"],["TLDR","**List tasks**\n\n```clido list```\n\n**Add a new task**\n\n```clido add \"[task description]\"```\n\n**Complete a task**\n\n```clido done [task_id]```\n\n**Remove a task**\n\n```clido remove [task_id]```"],["SYNOPSIS","**clido** _command_ [_options_]"],["DESCRIPTION","**clido** is a lightweight command-line todo list manager designed for developers who prefer terminal-based workflows. It provides straightforward task management with commands for adding, listing, completing, and removing tasks.\n\nTasks are stored locally in plain text files and can be managed without requiring any external services or accounts. The minimal interface keeps task management fast and distraction-free, making it easy to integrate into shell scripts or daily workflows."],["SEE ALSO","[task](/man/task)(1)"]],"e":["task"],"x":"clido list"},"column":{"n":"column","t":"format text into aligned columns","s":[["TAGLINE","format text into aligned columns"],["TLDR","Format output for **specific width**\n\n```printf \"header1 header2\\nbar foo\\n\" | column -c 30```\n\n**Auto-align** columns in tabular format\n\n```printf \"header1 header2\\nbar foo\\n\" | column -t```\n\nSpecify column **delimiter** for table mode\n\n```printf \"header1,header2\\nbar,foo\\n\" | column -t -s ,```\n\nFill **rows before columns**\n\n```printf \"header1\\nbar\\nfoobar\\n\" | column -c 30 -x```"],["SYNOPSIS","**column** [_options_] [_file_...]"],["DESCRIPTION","**column** formats text from stdin or a file into multiple columns. By default, columns are filled before rows using whitespace as separator.\n\nThe table mode (-t) is particularly useful for aligning structured data into readable tables."],["PARAMETERS","**-c, --output-width** _width_\n> Output width in characters\n\n**-t, --table**\n> Create a table with aligned columns\n\n**-s, --separator** _chars_\n> Column delimiters for table mode (default: whitespace)\n\n**-x, --fillrows**\n> Fill rows before filling columns\n\n**-o, --output-separator** _string_\n> Table output column separator"],["CAVEATS","Part of util-linux. The table mode requires consistent delimiter usage. Very long lines may cause formatting issues."],["INSTALL","```apt: sudo apt install bsdextrautils```\n\n```apk: sudo apk add util-linux-misc```"],["SEE ALSO","[colrm](/man/colrm)(1), [cut](/man/cut)(1), [paste](/man/paste)(1)"],["RESOURCES","```[Source code](https://github.com/util-linux/util-linux)```\n\n```[Documentation](https://man7.org/linux/man-pages/man1/column.1.html)```"]],"e":["colrm","cut","paste"],"x":"printf \"header1 header2\\nbar foo\\n\" | column -c 30"},"crane":{"n":"crane","t":"interact with container registries without Docker","s":[["TAGLINE","interact with container registries without Docker"],["TLDR","**Pull image**\n\n```crane pull [registry/image:tag] [output.tar]```\n\n**Push image**\n\n```crane push [image.tar] [registry/image:tag]```\n\n**Copy image between registries**\n\n```crane copy [src/image:tag] [dst/image:tag]```\n\n**List tags for image**\n\n```crane ls [registry/image]```\n\n**Get image digest**\n\n```crane digest [registry/image:tag]```\n\n**Get image manifest**\n\n```crane manifest [registry/image:tag]```\n\n**Get image configuration**\n\n```crane config [registry/image:tag]```\n\n**Authenticate to registry**\n\n```crane auth login [registry] -u [user] -p [password]```"],["SYNOPSIS","**crane** _command_ [_options_] [_arguments_]"],["DESCRIPTION","**crane** is a tool for interacting with container registries. It can pull, push, copy, and inspect container images without requiring a container runtime like Docker. Part of the go-containerregistry project."],["COMMANDS","**pull**\n> Download image as tarball\n\n**push**\n> Upload tarball as image\n\n**copy**\n> Copy image between registries\n\n**ls**\n> List tags for repository\n\n**digest**\n> Get image digest\n\n**manifest**\n> Get image manifest\n\n**config**\n> Get image config\n\n**catalog**\n> List repositories in registry\n\n**auth**\n> Manage registry authentication\n\n**append**\n> Append layers to image\n\n**flatten**\n> Flatten image layers\n\n**mutate**\n> Modify image metadata\n\n**validate**\n> Validate image\n\n**export**\n> Export filesystem from image"],["PARAMETERS","**--platform** _os/arch_\n> Target platform (e.g., linux/amd64)\n\n**--insecure**\n> Allow insecure registry connections\n\n**-v**, **--verbose**\n> Verbose output"],["CONFIGURATION","**~/.docker/config.json**\n> Docker configuration file storing registry credentials used by crane for authentication."],["ENVIRONMENT","**DOCKER_CONFIG**\n> Path to Docker config for credentials"],["CAVEATS","Does not require Docker daemon. Uses OCI registry API directly. Credentials read from Docker config by default."],["INSTALL","```pacman: sudo pacman -S crane```\n\n```apk: sudo apk add crane```\n\n```zypper: sudo zypper install crane```\n\n```brew: brew install crane```\n\n```nix: nix profile install nixpkgs#crane```"],["SEE ALSO","[docker](/man/docker)(1), [skopeo](/man/skopeo)(1), [cosign](/man/cosign)(1)"],["RESOURCES","```[Source code](https://github.com/google/go-containerregistry)```\n\n```[Documentation](https://github.com/google/go-containerregistry/blob/main/cmd/crane/doc/crane.md)```"]],"e":["docker","skopeo","cosign"],"x":"crane pull [registry/image:tag] [output.tar]"},"crontab":{"n":"crontab","t":"user cron table editor","s":[["TAGLINE","user cron table editor"],["TLDR","**Edit** crontab\n\n```crontab -e```\n\n**List** cron jobs\n\n```crontab -l```\n\n**Remove** crontab\n\n```crontab -r```\n\n**Edit** specific user's crontab\n\n```sudo crontab -e -u [username]```"],["SYNOPSIS","**crontab** [_options_]"],["DESCRIPTION","**crontab** manages scheduled tasks for users. It provides an interface to edit, list, and remove the cron table, which contains commands to be executed at specified times and dates. The cron daemon reads these tables and executes jobs at their scheduled times.\n\nEach user can have their own crontab file, allowing for user-level task scheduling without requiring root privileges. The crontab command provides a safe way to edit these files, performing syntax validation and notifying the cron daemon of changes. Jobs can be scheduled to run at specific times, regular intervals, or special events like system startup."],["PARAMETERS","**-e**\n> Edit crontab\n\n**-l**\n> List crontab\n\n**-r**\n> Remove crontab\n\n**-u** _user_\n> Specify user\n\n**-i**\n> Interactive mode (prompt before removing)"],["CRONTAB FORMAT","```\n* * * * * command\n│ │ │ │ │\n│ │ │ │ └── Day of week (0-7, Sunday = 0 or 7)\n│ │ │ └──── Month (1-12)\n│ │ └────── Day of month (1-31)\n│ └──────── Hour (0-23)\n└────────── Minute (0-59)\n```"],["SPECIAL CHARACTERS","***** - Any value\n**,** - List (e.g., 1,3,5)\n**-** - Range (e.g., 1-5)\n**/** - Step (e.g., */5 = every 5)"],["SPECIAL STRINGS","**@reboot** - Run at startup\n**@yearly** / **@annually** - Once a year (0 0 1 1 *)\n**@monthly** - Once a month (0 0 1 * *)\n**@weekly** - Once a week (0 0 * * 0)\n**@daily** / **@midnight** - Once a day (0 0 * * *)\n**@hourly** - Once an hour (0 * * * *)"],["CONFIGURATION","**/var/spool/cron/crontabs/[username]**\n> User's crontab file (location varies by system). Edited via crontab -e, not directly."],["CAVEATS","Limited environment variables. No output unless redirected or mailed. Time is in server timezone. Requires cron daemon running. Jobs don't run if system is off. Syntax errors silently ignored. Use absolute paths. Check /var/log/cron or /var/log/syslog for errors."],["HISTORY","**cron** was included in **Version 7 Unix** (1979). The **crontab** interface was added to allow user-level scheduling without requiring direct file editing."],["INSTALL","```apt: sudo apt install cron```\n\n```dnf: sudo dnf install cronie```\n\n```pacman: sudo pacman -S cronie```\n\n```apk: sudo apk add cronie```\n\n```zypper: sudo zypper install cron```\n\n```nix: nix profile install nixpkgs#cron```"],["SEE ALSO","[cron](/man/cron)(8), [at](/man/at)(1)"]],"e":["cron","at"],"x":"crontab -e"},"csvpy":{"n":"csvpy","t":"Interactive Python shell for CSV files","s":[["TAGLINE","Interactive Python shell for CSV files"],["TLDR","**Open a CSV file** in a Python shell\n\n```csvpy [data.csv]```\n\n**Open with tab delimiter**\n\n```csvpy -t [data.tsv]```\n\n**Load as an agate Table** for analysis\n\n```csvpy --agate [data.csv]```\n\n**Load as DictReader** (rows as dictionaries)\n\n```csvpy --dict [data.csv]```\n\n**Open with custom encoding**\n\n```csvpy -e [latin1] [data.csv]```\n\n**Open file without headers**\n\n```csvpy -H [data.csv]```\n\n**Open with a custom delimiter**\n\n```csvpy -d \"[;]\" [data.csv]```"],["SYNOPSIS","**csvpy** [_options_] _file_"],["PARAMETERS","_FILE_\n> CSV file to load into the Python environment. Also accepts piped input from stdin.\n\n**-d** _CHAR_, **--delimiter** _CHAR_\n> Field delimiter character (default: comma).\n\n**-t**, **--tabs**\n> Use tab characters as the field delimiter.\n\n**-q** _CHAR_, **--quotechar** _CHAR_\n> Character used for quoting fields.\n\n**-e** _ENCODING_, **--encoding** _ENCODING_\n> Specify the input file encoding (e.g., utf-8, latin1).\n\n**-H**, **--no-header-row**\n> Indicate that the file has no header row.\n\n**-K** _N_, **--skip-lines** _N_\n> Skip the first N lines of the input before processing.\n\n**--agate**\n> Load the file as an agate Table instead of a csv.reader object. The data is available as a variable named **table**.\n\n**--dict**\n> Load as a csv.DictReader where each row is a dictionary keyed by column headers.\n\n**-I**, **--no-inference**\n> Disable type inference when used with --agate, keeping all values as strings.\n\n**-S**\n> Disable CSV dialect sniffing.\n\n**--blanks**\n> Do not convert empty strings to None.\n\n**--null-value** _VALUES_\n> Convert specified values to None (can specify multiple).\n\n**-y** _N_, **--sniff-limit** _N_\n> Limit the number of bytes used for CSV dialect sniffing.\n\n**-l**, **--linenumbers**\n> Insert a column of line numbers at the beginning of the output.\n\n**-z** _N_, **--field-size-limit** _N_\n> Maximum length of a single field in the input CSV.\n\n**-V**, **--version**\n> Display version information."],["DESCRIPTION","**csvpy** is part of csvkit that loads a CSV file into an interactive Python shell for exploration and analysis. If IPython is installed, it is used as the shell for a richer experience.\n\nIn default mode, the CSV data is loaded into a variable named **reader**, a csv.reader object. With **--dict**, it becomes a DictReader where each row is a dictionary. With **--agate**, it becomes a **table** variable with full analysis capabilities including sorting, filtering, and aggregation.\n\nThis tool is useful for quick data exploration, testing data transformations, and prototyping analysis code. The interactive environment allows immediate feedback while working with the data."],["CAVEATS","Large files may use significant memory when loaded entirely. The default csv.reader is consumed after a single iteration; use **--agate** for reusable data. Requires familiarity with Python for effective use."],["HISTORY","**csvpy** is part of **csvkit**, created by Christopher Groskopf in **2011**. It provides a quick way to drop into Python for ad-hoc data analysis without writing boilerplate file loading code."],["SEE ALSO","[csvkit](/man/csvkit)(1), [csvlook](/man/csvlook)(1), [csvstat](/man/csvstat)(1), [csvcut](/man/csvcut)(1), [csvgrep](/man/csvgrep)(1), [python](/man/python)(1), [ipython](/man/ipython)(1)"]],"e":["csvkit","csvlook","csvstat","csvcut","csvgrep","python","ipython"],"x":"csvpy [data.csv]"},"docsify":{"n":"docsify","t":"dynamic Markdown documentation site generator","s":[["TAGLINE","dynamic Markdown documentation site generator"],["TLDR","**Initialize new documentation** site\n\n```docsify init [./docs]```\n\n**Initialize with a specific theme**\n\n```docsify init [./docs] --theme [dark]```\n\n**Start local preview** server\n\n```docsify serve [./docs]```\n\n**Serve on specific port**\n\n```docsify serve [./docs] --port [4000]```\n\n**Serve and open in browser** automatically\n\n```docsify serve [./docs] --open```\n\n**Generate sidebar** automatically\n\n```docsify generate [./docs]```"],["SYNOPSIS","**docsify** _command_ [_options_] [_path_]"],["PARAMETERS","_COMMAND_\n> Operation: init, serve, generate.\n\n_PATH_\n> Documentation directory.\n\n**init** [_PATH_]\n> Initialize new docsify project. Shorthand: **i**.\n\n**--local, -l**\n> Copy docsify files to the docs path instead of using CDN (default: false).\n\n**--theme, -t** _THEME_\n> Choose a theme: vue, buble, dark, pure (default: vue).\n\n**--plugins, -p**\n> Add plugin script tags to index.html.\n\n**serve** [_PATH_]\n> Start local server with live reload. Shorthand: **s**.\n\n**--port, -p** _N_\n> Server port (default: 3000).\n\n**--host, -H** _HOST_\n> Host to bind to (default: localhost).\n\n**--open, -o**\n> Open browser automatically.\n\n**generate** [_PATH_]\n> Generate sidebar file. Shorthand: **g**.\n\n**--sidebar, -s** _FILE_\n> Sidebar filename (default: _sidebar.md).\n\n**--overwrite, -o**\n> Allow overwriting existing files.\n\n**--help**\n> Display help information."],["DESCRIPTION","**docsify** is a documentation site generator that dynamically renders Markdown files without building static HTML. It loads and parses Markdown files on the fly, providing instant updates during editing.\n\nThe tool creates simple, elegant documentation websites from Markdown files. Features include full-text search, multiple themes, plugin support, and GitHub Pages compatibility. Configuration is minimal, typically just an index.html and Markdown files.\n\ndocsify is particularly suited for project documentation, requiring no build step and supporting features like cover pages, sidebars, and embedded code samples."],["CAVEATS","Requires JavaScript for rendering (no static HTML). SEO may be affected by client-side rendering. Large documentation sites may have initial load delay."],["HISTORY","docsify was created by **QingWei-Li** (cinwell) and released as an open-source project. It gained popularity as a lightweight alternative to documentation generators like GitBook and VuePress, emphasizing simplicity and no build requirements. It is installed via npm: **npm i docsify-cli -g**."],["SEE ALSO","[mkdocs](/man/mkdocs)(1), [vuepress](/man/vuepress)(1), [gitbook](/man/gitbook)(1), [npm](/man/npm)(1)"]],"e":["mkdocs","vuepress","gitbook","npm"],"x":"docsify init [./docs]"},"dolphin":{"n":"dolphin","t":"KDE file manager with integrated features","s":[["TAGLINE","KDE file manager with integrated features"],["TLDR","**Launch** the file manager\n\n```dolphin```\n\nOpen **specific directories**\n\n```dolphin [path/to/directory1] [path/to/directory2]```\n\nOpen with files/directories **selected**\n\n```dolphin --select [path/to/file1] [path/to/file2]```\n\nOpen a **new window**\n\n```dolphin --new-window```\n\nOpen in **split view**\n\n```dolphin --split [path/to/directory1] [path/to/directory2]```\n\nLaunch the **daemon** for faster startup\n\n```dolphin --daemon```"],["SYNOPSIS","**dolphin** [_options_] [_urls_]"],["DESCRIPTION","**dolphin** is KDE's file manager. It provides a graphical interface for managing files and directories with features like tabs, split view, integrated terminal, and file previews.\n\nPart of the KDE Plasma desktop environment but can be used in other desktop environments with the KDE/Qt libraries installed."],["PARAMETERS","**--select** _files_\n> Open with specified files or directories selected in their parent folder.\n\n**--new-window**\n> Force opening a new window instead of reusing an existing one.\n\n**--split** _dir1_ _dir2_\n> Open two directories side by side in split view.\n\n**--daemon**\n> Start Dolphin as a D-Bus service in the background for faster subsequent launches.\n\n**-h**, **--help**\n> Display help information.\n\n**--author**\n> Show author information.\n\n**--version**\n> Show version information."],["CAVEATS","KDE application, requires Qt/KDE libraries. Full functionality requires KDE desktop environment. For GNOME, see Nautilus; for XFCE, see Thunar. The --daemon flag keeps Dolphin running in the background which uses memory even when no windows are open."],["INSTALL","```apt: sudo apt install dolphin```\n\n```dnf: sudo dnf install dolphin```\n\n```pacman: sudo pacman -S dolphin```\n\n```apk: sudo apk add dolphin```\n\n```zypper: sudo zypper install dolphin```"],["SEE ALSO","[nautilus](/man/nautilus)(1), [thunar](/man/thunar)(1), [ranger](/man/ranger)(1), [nemo](/man/nemo)(1), [mc](/man/mc)(1)"]],"e":["nautilus","thunar","ranger","nemo","mc"],"x":"dolphin"},"dotnet-publish":{"n":"dotnet-publish","t":"deployment package builder and publisher","s":[["TAGLINE","deployment package builder and publisher"],["TLDR","**Publish for deployment** (Release by default on .NET 8+)\n\n```dotnet publish```\n\n**Publish a self-contained** app for a target runtime\n\n```dotnet publish -r [linux-x64] --self-contained```\n\n**Publish a single-file** executable\n\n```dotnet publish -r [linux-x64] -p:PublishSingleFile=true```\n\n**Publish to a directory**\n\n```dotnet publish -o [./publish]```\n\n**Publish trimmed**, for smaller self-contained output\n\n```dotnet publish -r [linux-x64] --self-contained -p:PublishTrimmed=true```\n\n**Cross-compile** by naming the OS and architecture separately\n\n```dotnet publish --os [linux] --arch [arm64]```\n\n**Publish using a profile** from Properties/PublishProfiles\n\n```dotnet publish -p:PublishProfile=[FolderProfile]```"],["SYNOPSIS","**dotnet publish** [_project_|_solution_|_file_] [_options_]"],["PARAMETERS","_PROJECT_ | _SOLUTION_ | _FILE_\n> What to publish. Defaults to the project or solution found in the current directory.\n\n**-c**, **--configuration** _CONFIG_\n> Build configuration. Defaults to **Release** when building `net8.0` or later with the .NET 8 SDK or newer, and to Debug otherwise.\n\n**-r**, **--runtime** _RID_\n> Publish for a given Runtime Identifier, such as `linux-x64` or `osx-arm64`. Pair it with **--self-contained** or **--no-self-contained**.\n\n**-a**, **--arch** _ARCH_ / **--os** _OS_\n> Shorthand that combines with the current platform to form the RID. Do not combine these with **-r**.\n\n**--sc**, **--self-contained**\n> Publish the .NET runtime alongside the app, so nothing needs to be installed on the target.\n\n**--no-self-contained**\n> Publish framework-dependent: a compatible .NET runtime must already exist on the target.\n\n**--ucr**, **--use-current-runtime**\n> Use the machine's own runtime as the target.\n\n**-o**, **--output** _DIR_\n> Output directory. Defaults to `bin/<configuration>/<framework>/publish/`, with the RID appended for self-contained output.\n\n**-f**, **--framework** _TFM_\n> Publish for one target framework of a multi-targeted project.\n\n**-p**, **--property**:_NAME_=_VALUE_\n> Set an MSBuild property. This is how `PublishSingleFile`, `PublishTrimmed`, `PublishReadyToRun`, and `PublishAot` are enabled.\n\n**--no-build**\n> Do not build first; use existing output. Implies **--no-restore**.\n\n**--no-restore**\n> Skip the implicit `dotnet restore`.\n\n**--no-dependencies**\n> Ignore project-to-project references and restore only the root project.\n\n**--artifacts-path** _DIR_\n> Put all build output under one directory, separated by project.\n\n**--manifest** _FILE_\n> Trim the published package set against a target manifest.\n\n**--version-suffix** _SUFFIX_\n> Replace the `*` in the project's version field.\n\n**--nologo**\n> Suppress the startup banner.\n\n**--tl**:[**auto**|**on**|**off**]\n> Control the Terminal Logger for build output.\n\n**-v**, **--verbosity** _LEVEL_\n> One of `quiet`, `minimal` (the default), `normal`, `detailed`, `diagnostic`."],["DESCRIPTION","**dotnet publish** compiles an application and lays out everything needed to run it into one directory. That is more than `dotnet build` produces: alongside the assemblies it writes a `.deps.json` listing every dependency, a `.runtimeconfig.json` describing the runtime the app expects, and the dependencies themselves copied out of the NuGet cache. It is the only officially supported way to prepare a .NET app for deployment.\n\nThe main choice is **framework-dependent** versus **self-contained**. Framework-dependent output is small and portable across architectures but requires a matching .NET runtime on the target. Self-contained output bundles the runtime, so it runs anywhere, at the cost of tens of megabytes and of being tied to one RID.\n\nBeyond that, several MSBuild properties reshape the output. `PublishSingleFile` bundles everything into one executable (which implies self-contained). `PublishTrimmed` removes IL that static analysis can prove unreachable. `PublishReadyToRun` precompiles assemblies ahead of time to cut startup latency, and `PublishAot` compiles to a native binary with no IL or JIT at all.\n\nThe command is a thin front end to MSBuild's `Publish` target, so any MSBuild property or publish profile applies, and `-c` and `-o` simply map onto the `Configuration` and `PublishDir` properties."],["CAVEATS","The default configuration changed: with the .NET 8 SDK and later, `dotnet publish` uses **Release** for `net8.0`+ targets, where older SDKs defaulted to Debug. Scripts that relied on the old behaviour, or that pass `-c Debug` believing it to be the default, need reviewing.\n\nTrimming and AOT are the sharp edges. Both rely on static analysis, so code reached only through reflection or dynamic loading can be removed or fail to compile, and the failure usually appears at runtime rather than at publish time. Test trimmed output, and heed the trim warnings rather than suppressing them.\n\nPublishing a *solution* with `-o` is an error in recent SDKs, because all projects' outputs would collide in one directory; use the `PublishDir` property instead. Publishing into a folder underneath the project directory makes successive runs nest output inside itself. And `--no-build` quietly implies `--no-restore`, so a stale `bin` directory will be published without complaint."],["HISTORY","**dotnet publish** arrived with the first .NET Core SDK in **2016** and replaced the platform-specific packaging that .NET Framework applications had needed. Its capabilities have widened with each release: self-contained deployment, then single-file bundling and ReadyToRun in .NET Core 3, trimming made viable in .NET 6, and Native AOT from .NET 7 onward, which together turned a runtime once seen as heavyweight into one that can produce small, dependency-free native executables."],["INSTALL","```dnf: sudo dnf install dotnet-host```\n\n```pacman: sudo pacman -S dotnet-host```\n\n```apk: sudo apk add dotnet-host```\n\n```brew: brew install dotnet```"],["SEE ALSO","[dotnet](/man/dotnet)(1), [dotnet-build](/man/dotnet-build)(1), [dotnet-run](/man/dotnet-run)(1), [dotnet-restore](/man/dotnet-restore)(1), [msbuild](/man/msbuild)(1)"],["RESOURCES","```[Source code](https://github.com/dotnet/sdk)```\n\n```[Documentation](https://learn.microsoft.com/en-us/dotnet/core/tools/dotnet-publish)```"]],"e":["dotnet","dotnet-build","dotnet-run","dotnet-restore","msbuild"],"x":"dotnet publish"},"dtc":{"n":"dtc","t":"Device Tree Compiler","s":[["TAGLINE","Device Tree Compiler"],["TLDR","**Compile DTS to DTB**\n\n```dtc -I dts -O dtb -o [output.dtb] [input.dts]```\n\n**Decompile DTB to DTS**\n\n```dtc -I dtb -O dts -o [output.dts] [input.dtb]```\n\n**Check DTS syntax**\n\n```dtc -I dts -O dts [input.dts]```\n\n**Include search path**\n\n```dtc -I dts -O dtb -i [include_path] -o [output.dtb] [input.dts]```\n\n**Generate assembly output**\n\n```dtc -I dts -O asm -o [output.S] [input.dts]```"],["SYNOPSIS","**dtc** [_options_] _input_file_"],["PARAMETERS","**-I** _format_\n> Input format (dts, dtb, fs).\n\n**-O** _format_\n> Output format (dts, dtb, asm).\n\n**-o** _file_\n> Output file.\n\n**-i** _path_\n> Include search path.\n\n**-W** _warning_\n> Enable warning.\n\n**-E** _error_\n> Enable error.\n\n**-@**\n> Generate symbols for overlays."],["DESCRIPTION","**dtc** (Device Tree Compiler) compiles Device Tree Source (DTS) files to Device Tree Blob (DTB) format and performs the reverse operation of decompiling DTB back to DTS. Device trees are data structures that describe hardware components and their relationships, allowing Linux kernels to support various hardware platforms without hardcoded board-specific code.\n\nThe compiler is essential for embedded Linux development, particularly for ARM, RISC-V, and PowerPC platforms. DTS files provide a human-readable description of hardware (CPUs, memory, peripherals, interrupts, buses), while DTB is the binary format loaded by bootloaders and passed to the kernel at runtime.\n\ndtc supports device tree overlays, which allow runtime modification of the base device tree. This is particularly useful for add-on hardware like Raspberry Pi HATs or BeagleBone capes, where the overlay describes additional hardware without modifying the base system device tree.\n\nThe tool includes validation and warning systems to catch common errors in device tree syntax and structure. It can also generate assembly output for inclusion in firmware builds."],["INSTALL","```apt: sudo apt install device-tree-compiler```\n\n```dnf: sudo dnf install dtc```\n\n```pacman: sudo pacman -S dtc```\n\n```apk: sudo apk add dtc```\n\n```zypper: sudo zypper install dtc```\n\n```brew: brew install dtc```\n\n```nix: nix profile install nixpkgs#dtc```"]],"e":[],"x":"dtc -I dts -O dtb -o [output.dtb] [input.dts]"},"dump.exfat":{"n":"dump.exfat","t":"exFAT filesystem information display","s":[["TAGLINE","exFAT filesystem information display"],["TLDR","Print on-disk information for **exFAT filesystem**\n\n```dump.exfat [/dev/sdXY]```\n\nShow **directory entry** information for a specific path\n\n```dump.exfat -d [/path/to/file] [/dev/sdXY]```\n\n**Scan** directory entries from a given path **recursively**\n\n```dump.exfat -s [/] -r [/dev/sdXY]```\n\nShow **cluster chain** along with directory entries\n\n```dump.exfat -d [/path/to/file] -c [/dev/sdXY]```\n\nPrint **version** information\n\n```dump.exfat -V```"],["SYNOPSIS","**dump.exfat** [**-d** _file-path_] [**-s** _dir-path_] [**-r**] [**-c**] _device_"],["DESCRIPTION","**dump.exfat** displays detailed on-disk structural information for exFAT (Extended File Allocation Table) filesystems. It reads and presents filesystem metadata including boot sector parameters, FAT (File Allocation Table) region layout, cluster sizes, volume serial numbers, and other low-level details.\n\nThe tool is primarily used for forensic analysis, debugging filesystem issues, and understanding exFAT structure. It reveals information like volume label, filesystem version, bytes per sector, sectors per cluster, FAT offset and length, cluster heap offset, and root directory location.\n\nexFAT is commonly used on flash media (SD cards, USB drives) and external drives due to its support for large files and broad compatibility across Windows, macOS, and Linux. dump.exfat helps diagnose corruption, verify filesystem parameters, and analyze how data is organized on the storage device.\n\nThe tool operates as a read-only utility and does not modify the filesystem. For accurate results, the filesystem should ideally be unmounted, though read-only access to mounted filesystems is possible."],["PARAMETERS","**-V**\n> Print version and exit.\n\n**-d**, **--dentry-set=**_path_\n> Print directory entry information for a given path on the device.\n\n**-s**, **--scan-dir=**_dir-path_\n> Scan and print directory entry information from a given path.\n\n**-r**, **--recursive**\n> Scan directory entries recursively. Only works with **-s**.\n\n**-c**, **--cluster-chain**\n> Print cluster chain information alongside directory entries. Only works with **-d** or **-s**.\n\n_device_\n> The device containing the exFAT filesystem."],["CAVEATS","Part of exfatprogs package. Read-only operation. Absence of doubly-allocated clusters does not guarantee they are not doubly-allocated unless scanning starts from the root directory recursively."],["INSTALL","```apt: sudo apt install exfatprogs```\n\n```dnf: sudo dnf install exfatprogs```\n\n```pacman: sudo pacman -S exfatprogs```\n\n```apk: sudo apk add exfatprogs```\n\n```zypper: sudo zypper install exfatprogs```\n\n```nix: nix profile install nixpkgs#exfatprogs```"],["SEE ALSO","[fsck.exfat](/man/fsck.exfat)(8), [mkfs.exfat](/man/mkfs.exfat)(8), [tune.exfat](/man/tune.exfat)(8), [exfatlabel](/man/exfatlabel)(8)"]],"e":["fsck.exfat","mkfs.exfat","tune.exfat","exfatlabel"],"x":"dump.exfat [/dev/sdXY]"},"ebook-meta":{"n":"ebook-meta","t":"ebook metadata editor","s":[["TAGLINE","ebook metadata editor"],["TLDR","**Show metadata**\n\n```ebook-meta [book.epub]```\n\n**Set title**\n\n```ebook-meta [book.epub] --title \"[New Title]\"```\n\n**Set author**\n\n```ebook-meta [book.epub] --authors \"[Author Name]\"```\n\n**Set cover**\n\n```ebook-meta [book.epub] --cover [cover.jpg]```\n\n**Get cover image**\n\n```ebook-meta [book.epub] --get-cover [cover.jpg]```\n\n**Set multiple fields**\n\n```ebook-meta [book.epub] --title \"[Title]\" --authors \"[Author]\" --publisher \"[Publisher]\"```"],["SYNOPSIS","**ebook-meta** _file_ [_options_]"],["DESCRIPTION","**ebook-meta** reads and writes metadata in ebook files. Part of Calibre, it handles various formats including EPUB, MOBI, AZW3, and PDF.\n\nThe tool can display existing metadata and modify fields like title, author, publisher, date, and cover image without converting the file."],["PARAMETERS","**--title** _title_\n> Set book title.\n\n**--authors** _names_\n> Set authors (comma-separated).\n\n**--publisher** _name_\n> Set publisher.\n\n**--date** _date_\n> Set publication date.\n\n**--isbn** _isbn_\n> Set ISBN.\n\n**--tags** _tags_\n> Set tags (comma-separated).\n\n**--series** _name_\n> Set series name.\n\n**--series-index** _n_\n> Set position in series.\n\n**--cover** _file_\n> Set cover from image.\n\n**--get-cover** _file_\n> Extract cover to file.\n\n**--language** _lang_\n> Set language.\n\n**--comments** _text_\n> Set description/comments.\n\n**--rating** _n_\n> Set the rating (1 to 5).\n\n**--author-sort** _name_\n> Set the string used to sort by author.\n\n**--to-opf** _file.opf_\n> Write the book's metadata to an OPF file.\n\n**--from-opf** _file.opf_\n> Read metadata from an OPF file and apply it to the book.\n\n**--identifier** _id:value_\n> Set an identifier, e.g. `--identifier doi:10.1234/foo`. Repeatable."],["CAVEATS","Not all formats support all metadata fields. PDF metadata limited. Changes are in-place by default. Some metadata requires specific format support."],["HISTORY","**ebook-meta** is part of **Calibre**, created by **Kovid Goyal** in **2006**. It provides command-line access to Calibre's metadata editing capabilities, essential for batch processing and scripting."],["SEE ALSO","[ebook-convert](/man/ebook-convert)(1), [calibre](/man/calibre)(1), [exiftool](/man/exiftool)(1)"],["RESOURCES","```[Source code](https://github.com/kovidgoyal/calibre)```\n\n```[Homepage](https://calibre-ebook.com/)```\n\n```[Documentation](https://manual.calibre-ebook.com/generated/en/ebook-meta.html)```"]],"e":["ebook-convert","calibre","exiftool"],"x":"ebook-meta [book.epub]"},"emuto":{"n":"emuto","t":"data transformation tool with query language","s":[["TAGLINE","data transformation tool with query language"],["TLDR","**Transform JSON data**\n\n```emuto '[.data.items | map(.name)]' [input.json]```\n\n**Transform from stdin**\n\n```cat [data.json] | emuto '[.items]'```\n\n**Output formatted**\n\n```emuto --pretty '[.]' [input.json]```"],["SYNOPSIS","**emuto** [_options_] _expression_ [_file_]"],["PARAMETERS","_EXPRESSION_\n> Emuto transformation expression.\n\n_FILE_\n> Input JSON file.\n\n**--pretty**\n> Pretty-print output.\n\n**--help**\n> Display help information."],["DESCRIPTION","**emuto** is a data transformation tool using a jq-like query language. It processes JSON, CSV, TSV, DSV, and plain text through expressions that select, filter, and transform data structures.\n\nThe expression language supports object access, array operations, mapping, and filtering. It provides a functional approach to JSON manipulation.\n\nemuto is useful for data extraction, format conversion, and JSON processing in scripts and pipelines."],["CAVEATS","Expression syntax differs from jq. May not handle all JSON edge cases. Performance varies with data size."],["HISTORY","emuto was developed as a JSON transformation tool, providing jq-like functionality with its own expression syntax for data manipulation."],["INSTALL","```aur: yay -S emuto```"],["SEE ALSO","[jq](/man/jq)(1), [gron](/man/gron)(1), [fx](/man/fx)(1)"]],"e":["jq","gron","fx"],"x":"emuto '[.data.items | map(.name)]' [input.json]"},"fasthex":{"n":"fasthex","t":"High-performance hex dumper","s":[["TAGLINE","High-performance hex dumper"],["TLDR","**Display a file as a hex dump**\n\n```fasthex [path/to/file]```\n\n**Pipe output to a file**\n\n```fasthex [path/to/file] > [output.txt]```\n\n**Skip bytes and read a limited length**\n\n```fasthex -s 1KiB -n 512 [path/to/file]```\n\n**Display with colors**\n\n```fasthex --color=always [path/to/file]```\n\n**Show binary representation**\n\n```fasthex -b [path/to/file]```"],["SYNOPSIS","**fasthex** [_options_] [_file_...]"],["PARAMETERS","**-s**, **--skip** _N_\n> Skip first N bytes (negative counts from end). Supports KiB, MiB, GiB suffixes.\n\n**-n**, **--length** _N_\n> Read only N bytes.\n\n**-W**, **--width** _N_\n> Bytes per row. Default: 16.\n\n**-g**, **--group** _N_\n> Bytes per group: 1, 2, 4, or 8.\n\n**-x**, **--hex**\n> One-byte hexadecimal display.\n\n**-b**, **--binary**\n> Binary display (8 bits per byte).\n\n**-A**, **--no-ascii**\n> Hide the ASCII panel.\n\n**-L**, **--color** _WHEN_\n> Color mode: auto, always, or never.\n\n**-w**, **--squeeze**\n> Replace identical consecutive rows with **\\***.\n\n**-r**, **--reverse**\n> Convert a hex dump back to binary.\n\n**-h**, **--help**\n> Show help.\n\n**-v**, **--version**\n> Show version."],["DESCRIPTION","**fasthex** is a Rust hex dumper optimized for speed on large files. It memory-maps input and formats output in parallel using SIMD (AVX2, SSE4.1, or scalar fallback), with double-buffered I/O and zero-copy kernel pipe transfers when possible.\n\nMultiple files are concatenated as one stream. With no file argument, **fasthex** reads from stdin. Output follows the classic offset / hex bytes / ASCII layout familiar from **xxd** and **hexdump**, with additional format modes (octal, decimal, plain hex string, C include style) and configurable endianness, borders, and color schemes."],["CAVEATS","Install via **cargo install --git**; there are no published crate releases yet. Color output to a TTY may use a slower path than piping to a file or **/dev/null**."],["INSTALL","```aur: yay -S fasthex```"],["SEE ALSO","[xxd](/man/xxd)(1), [hexdump](/man/hexdump)(1), [hexyl](/man/hexyl)(1)"],["RESOURCES","```[Source code](https://github.com/CallMeAlphabet/fasthex)```"]],"e":["xxd","hexdump","hexyl"],"x":"fasthex [path/to/file]"},"flatpak-update":{"n":"flatpak-update","t":"update installed Flatpak applications and runtimes","s":[["TAGLINE","update installed Flatpak applications and runtimes"],["TLDR","**Update all** installed applications and runtimes\n\n```flatpak update```\n\nUpdate all and **auto-confirm** prompts\n\n```flatpak update -y```\n\nUpdate a **specific application**\n\n```flatpak update [com.example.app]```\n\n**Downgrade** to a specific commit\n\n```flatpak update --commit [COMMIT_HASH] [com.example.app]```\n\nUpdate only **user installations**\n\n```flatpak update --user```\n\nUpdate only **system installations**\n\n```flatpak update --system```\n\n**Download** updates without deploying\n\n```flatpak update --no-deploy```"],["SYNOPSIS","**flatpak update** [_options_] [_ref..._]\n\n**flatpak update** [_options_] **--appstream** [_remote_]"],["PARAMETERS","**-u, --user**\n> Update per-user installations\n\n**--system**\n> Update system-wide installations\n\n**--commit** _commit_\n> Update to a specific commit (can downgrade)\n\n**--no-pull**\n> Deploy locally available versions without downloading\n\n**--no-deploy**\n> Download updates without deploying them\n\n**--app**\n> Only update applications\n\n**--runtime**\n> Only update runtimes\n\n**-y, --assumeyes**\n> Automatically answer yes to all prompts\n\n**--noninteractive**\n> Minimal output for scripting/automation\n\n**--appstream** [_remote_]\n> Update appstream metadata for remotes\n\n**-v, --verbose**\n> Output debug information"],["DESCRIPTION","**Flatpak update** updates applications and runtimes to their latest versions from configured remotes. Without arguments, it updates everything including appstream metadata for all remotes.\n\nReferences follow the format **(app|runtime)/ID/ARCH/BRANCH**, where most elements are optional. The tool automatically resolves partial references.\n\nThe command offers to uninstall unused end-of-life runtimes while preserving those explicitly installed or pinned. Use **--commit** to update or downgrade to a specific version identified by its commit hash."],["CAVEATS","Updates may require significant download bandwidth and disk space. Using **--commit** to pin specific versions prevents automatic security updates. The **--no-deploy** option is useful for pre-downloading updates on slow connections. Masked applications (via **flatpak mask**) are skipped during updates."],["HISTORY","Part of the **Flatpak** project developed by Alexander Larsson at Red Hat. Flatpak evolved from the xdg-app project around **2015** and provides a sandboxed, distribution-agnostic application delivery system for Linux."],["INSTALL","```apt: sudo apt install flatpak```\n\n```dnf: sudo dnf install flatpak```\n\n```pacman: sudo pacman -S flatpak```\n\n```apk: sudo apk add flatpak```\n\n```zypper: sudo zypper install flatpak```\n\n```nix: nix profile install nixpkgs#flatpak```"],["SEE ALSO","[flatpak](/man/flatpak)(1), [flatpak-install](/man/flatpak-install)(1), [flatpak-mask](/man/flatpak-mask)(1), [flatpak-remote-info](/man/flatpak-remote-info)(1)"],["RESOURCES","```[Homepage](https://flatpak.org/)```\n\n```[Source code](https://github.com/flatpak/flatpak)```"]],"e":["flatpak","flatpak-install","flatpak-mask","flatpak-remote-info"],"x":"flatpak update"},"fsck":{"n":"fsck","t":"filesystem check and repair utility","s":[["TAGLINE","filesystem check and repair utility"],["TLDR","**Check** filesystem\n\n```sudo fsck /dev/sdXN```\n\nCheck and **report** statistics upon completion\n\n```sudo fsck -r /dev/sdXN```\n\n**Automatically** repair without prompting\n\n```sudo fsck -a /dev/sdXN```\n\nCheck **all** filesystems from /etc/fstab\n\n```sudo fsck -A```\n\n**Force** check even if clean\n\n```sudo fsck -f /dev/sdXN```\n\nShow **progress**\n\n```sudo fsck -C /dev/sdXN```"],["SYNOPSIS","**fsck** [_-sAVRTMNP_] [_-t fstype_] [_filesystem_...] [_--_] [_fs-specific-options_]"],["DESCRIPTION","**fsck** checks and repairs Linux filesystems. It is a front-end that calls filesystem-specific checking utilities (fsck.ext4, fsck.xfs, etc.). The filesystem should be unmounted when running fsck."],["PARAMETERS","**-a**\n> Automatically repair without prompting\n\n**-A**\n> Check all filesystems listed in /etc/fstab\n\n**-C [FD]**\n> Show progress bar\n\n**-f**\n> Force check even if filesystem seems clean\n\n**-M**\n> Do not check mounted filesystems\n\n**-N**\n> Dry run - show what would be done\n\n**-P**\n> Check filesystems in parallel (with -A)\n\n**-R**\n> Skip root filesystem (with -A)\n\n**-r** _[fd]_\n> Report statistics for each fsck upon completion (exit status, time, memory)\n\n**-l**\n> Lock the whole-disk device with an exclusive flock. Can only be used with one device (mutually exclusive with -A).\n\n**-s**\n> Serialize fsck operations\n\n**-T**\n> Do not show title on startup\n\n**-t TYPE**\n> Specify filesystem type\n\n**-V**\n> Verbose output\n\n**-y**\n> Answer \"yes\" to all questions\n\n**-n**\n> Answer \"no\" to all questions (read-only check)"],["CONFIGURATION","**/etc/fstab**\n> Defines which filesystems are checked with the -A option and in what order (pass field)."],["CAVEATS","Never run fsck on a mounted filesystem (except in read-only mode). Automatic repair (**-a** or **-y**) may cause data loss. Always backup important data before repair."],["HISTORY","**fsck** is part of **util-linux**, providing a unified interface for filesystem checking across different filesystem types."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[e2fsck](/man/e2fsck)(8), [xfs_repair](/man/xfs_repair)(8), [mount](/man/mount)(8), [fstab](/man/fstab)(5)"]],"e":["e2fsck","xfs_repair","mount","fstab"],"x":"sudo fsck /dev/sdXN"},"gdaladdo":{"n":"gdaladdo","t":"builds raster overview pyramids","s":[["TAGLINE","builds raster overview pyramids"],["TLDR","**Build overviews**\n\n```gdaladdo [image.tif]```\n\n**Build specific levels**\n\n```gdaladdo [image.tif] [2] [4] [8] [16]```\n\n**Build using average resampling**\n\n```gdaladdo -r average [image.tif]```\n\n**Build external overviews**\n\n```gdaladdo -ro [image.tif]```\n\n**Build with compression**\n\n```gdaladdo --config COMPRESS_OVERVIEW JPEG [image.tif]```"],["SYNOPSIS","**gdaladdo** [_options_] _filename_ [_levels_...]"],["PARAMETERS","**-r** _method_\n> Resampling method (nearest, average, gauss, cubic).\n\n**-ro**\n> Create external overview file.\n\n**--config** _key_ _value_\n> Set configuration option.\n\n**-b** _band_\n> Build for specific band only.\n\n**-clean**\n> Remove existing overviews."],["DESCRIPTION","**gdaladdo** builds or rebuilds overview images (also called pyramids) for raster datasets. Overviews are downsampled versions of the original image at multiple resolutions, enabling efficient display and processing at different zoom levels.\n\nWithout specified levels, the tool generates default pyramids. Custom levels define the subsampling factors (e.g., 2, 4, 8 means half, quarter, eighth resolution). Different resampling methods affect quality and performance: nearest is fastest, average produces smoother results, and cubic provides highest quality.\n\nOverviews can be stored internally in the file or as separate .ovr sidecar files. Compression settings reduce overview file size. gdaladdo is essential for working with large imagery in GIS applications and web mapping services."],["INSTALL","```apt: sudo apt install gdal-bin```\n\n```apk: sudo apk add gdal-tools```"],["SEE ALSO","[gdal_translate](/man/gdal_translate)(1), [gdalinfo](/man/gdalinfo)(1)"],["RESOURCES","```[Documentation](https://gdal.org/programs/gdaladdo.html)```\n\n```[Source code](https://github.com/OSGeo/gdal)```"]],"e":["gdal_translate","gdalinfo"],"x":"gdaladdo [image.tif]"},"git-credential":{"n":"git-credential","t":"Retrieve and store authentication credentials for Git","s":[["TAGLINE","Retrieve and store authentication credentials for Git"],["TLDR","**Get credentials for a URL**\n\n```echo \"url=https://github.com\" | git credential fill```\n\n**Store credentials in configured helpers**\n\n```git credential approve << EOF\nurl=https://github.com\nusername=user\npassword=token\nEOF```\n\n**Reject (erase) stored credentials**\n\n```git credential reject << EOF\nurl=https://github.com\nusername=user\npassword=token\nEOF```"],["SYNOPSIS","**git credential** _action_"],["PARAMETERS","**fill**\n> Read credential attributes from stdin, look up matching credentials via configured helpers, and output the result including username and password.\n\n**approve**\n> Send credential attributes to all configured helpers for storage. No output is emitted.\n\n**reject**\n> Notify all configured helpers to erase matching credentials. No output is emitted.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git credential** is a low-level interface for storing and retrieving credentials using Git's credential helper system. It interfaces with configured helpers (cache, store, osxkeychain, manager, etc.) to retrieve, store, and erase credentials.\n\nThe command is typically invoked automatically by Git during push/pull operations when authentication is needed. Direct use is primarily for debugging credential issues or scripting authentication workflows. Input and output use a key=value format on stdin/stdout with attributes like protocol, host, username, and password.\n\nCredential helpers are configured via **git config credential.helper**."],["INPUT ATTRIBUTES","**protocol**\n> The protocol over which the credential will be used (e.g., https, ssh).\n\n**host**\n> The remote hostname for which the credential is used.\n\n**username**\n> The credential's username, if already known.\n\n**password**\n> The credential's password, if being provided or returned.\n\n**path**\n> The path with which the credential will be used (e.g., for accessing remote https repositories)."],["CAVEATS","Usually called internally by Git, not directly by users. Credential helpers must be configured via **git config** for this command to be useful. The **reject** action requires the same credential attributes that were originally stored. Sensitive credentials may be exposed on stdin/stdout."],["HISTORY","The git credential subsystem was introduced in **Git 1.7.9** (2012) to standardize credential management across helpers, replacing the earlier ad-hoc authentication handling."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-config](/man/git-config)(1), [git-credential-store](/man/git-credential-store)(1), [git-credential-cache](/man/git-credential-cache)(1)"]],"e":["git-config","git-credential-store","git-credential-cache"],"x":"echo \"url=https://github.com\" | git credential fill"},"git-psykorebase":{"n":"git-psykorebase","t":"Rebase a branch onto another using a single merge commit","s":[["TAGLINE","Rebase a branch onto another using a single merge commit"],["TLDR","**Rebase current branch onto another** using a merge commit\n\n```git psykorebase [primary_branch]```\n\n**Rebase a specific branch onto another**\n\n```git psykorebase [primary_branch] [secondary_branch]```\n\n**Force a merge commit** even if fast-forward is possible\n\n```git psykorebase [primary_branch] --no-ff```\n\n**Continue after resolving conflicts**\n\n```git psykorebase --continue```"],["SYNOPSIS","**git** **psykorebase** _primary-branch_ [_secondary-branch_] [_--no-ff_]\n\n**git** **psykorebase** **--continue**"],["PARAMETERS","_PRIMARY-BRANCH_\n> Branch to rebase onto.\n\n_SECONDARY-BRANCH_\n> Branch being rebased; defaults to the current branch.\n\n**--no-ff**\n> Force a merge commit instead of a fast-forward.\n\n**-c**, **--continue**\n> Resume after manually resolving a conflict."],["DESCRIPTION","**git psykorebase** rebases a branch onto another by checking out the primary branch, branching from it, and merging the secondary branch in with a single generated merge commit, rather than replaying every individual commit like `git rebase` does. This git-extras command avoids re-resolving the same conflict once per commit, at the cost of collapsing the rebase into one merge point.\n\nIf the merge conflicts, it leaves a temporary `<secondary>-rebased-on-top-of-<primary>` branch checked out and tells you to resolve the conflict and commit, then run `git psykorebase --continue` to finish renaming the branches into place."],["CAVEATS","Part of git-extras. Because it uses one merge instead of replaying commits, individual commit-level history from the secondary branch relative to the primary is not preserved the way an interactive rebase would."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-rebase](/man/git-rebase)(1), [git-merge](/man/git-merge)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md#git-psykorebase)```"]],"e":["git-rebase","git-merge","git-extras"],"x":"git psykorebase [primary_branch]"},"git-repack":{"n":"git-repack","t":"Pack loose objects into pack files","s":[["TAGLINE","Pack loose objects into pack files"],["TLDR","**Repack objects**\n\n```git repack```\n\n**Repack aggressively**\n\n```git repack -a -d```\n\n**Repack with delta compression**\n\n```git repack -a -d --depth=[250] --window=[250]```\n\n**Repack all objects**\n\n```git repack -A -d```\n\n**Repack with geometry**\n\n```git repack --geometric=[2]```"],["SYNOPSIS","**git** **repack** [_options_]"],["PARAMETERS","**-a**\n> Pack all objects.\n\n**-A**\n> Pack all reachable objects.\n\n**-d**\n> Remove redundant packs.\n\n**-f**\n> Force delta recomputation.\n\n**--depth** _n_\n> Delta chain depth.\n\n**--window** _n_\n> Delta window size.\n\n**--geometric** _factor_\n> Geometric repacking.\n\n**-l**\n> Local objects only."],["DESCRIPTION","**git repack** consolidates loose objects into pack files, improving storage efficiency and access time. It reorganizes the repository's object database by combining individual loose objects into compressed packs.\n\nRepacking is useful after many small commits or after importing objects. The `-a -d` combination is common for aggressive repacking that removes redundant packs."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-gc](/man/git-gc)(1)"]],"e":["git-gc"],"x":"git repack"},"gnumeric":{"n":"gnumeric","t":"lightweight GNOME spreadsheet application","s":[["TAGLINE","lightweight GNOME spreadsheet application"],["TLDR","**Start Gnumeric**\n\n```gnumeric```\n\n**Open spreadsheet**\n\n```gnumeric [spreadsheet.xlsx]```\n\n**Convert to CSV**\n\n```ssconvert [input.xlsx] [output.csv]```\n\n**Convert to PDF**\n\n```ssconvert [input.xlsx] [output.pdf]```"],["SYNOPSIS","**gnumeric** [_options_] [_files_]"],["PARAMETERS","_FILES_\n> Spreadsheet files to open.\n\n**-n**, **--new**\n> Open new window.\n\n**--no-splash**\n> Start without showing the splash screen.\n\n**-g**, **--geometry** _WIDTHxHEIGHT+XOFF+YOFF_\n> Set the size and position of the first window.\n\n**-L** _LOCALE_, **--locale** _LOCALE_\n> Set locale.\n\n**-v**, **--version**\n> Display the version of Gnumeric.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Gnumeric** is a spreadsheet application for GNOME. It provides spreadsheet functionality including formulas, charts, and data analysis with high calculation accuracy.\n\n**Gnumeric** supports various file formats including Excel, OpenDocument, and CSV. It is known for accurate statistical functions and being lightweight compared to full office suites."],["CAVEATS","GTK application requiring a display server. Fewer features than LibreOffice Calc but lighter weight. Known for excellent statistical accuracy. Use **ssconvert** for command-line format conversions without launching the GUI."],["HISTORY","Gnumeric was started by **Miguel de Icaza** as part of the **GNOME** project, focusing on accuracy and standards compliance."],["INSTALL","```apt: sudo apt install gnumeric```\n\n```dnf: sudo dnf install gnumeric```\n\n```pacman: sudo pacman -S gnumeric```\n\n```apk: sudo apk add gnumeric```\n\n```zypper: sudo zypper install gnumeric```\n\n```brew: brew install gnumeric```\n\n```nix: nix profile install nixpkgs#gnumeric```"],["SEE ALSO","[libreoffice](/man/libreoffice)(1)"]],"e":["libreoffice"],"x":"gnumeric"},"gocryptotrader":{"n":"gocryptotrader","t":"cryptocurrency trading engine with CLI client","s":[["TAGLINE","cryptocurrency trading engine with CLI client"],["TLDR","**Start the trading engine**\n\n```gocryptotrader```\n\n**Start with a specific config file**\n\n```gocryptotrader -config [config.json]```\n\n**List supported exchanges using the CLI client**\n\n```gctcli getexchanges```\n\n**Get ticker for a currency pair**\n\n```gctcli getticker --exchange [binance] --pair [BTC-USDT] --asset [spot]```\n\n**Get account portfolio summary**\n\n```gctcli getportfoliosummary```\n\n**Enable or disable an exchange**\n\n```gctcli enableexchange --exchange [binance]```\n\n**Get orderbook for a currency pair**\n\n```gctcli getorderbook --exchange [binance] --pair [BTC-USDT] --asset [spot]```"],["SYNOPSIS","**gocryptotrader** [_options_]\n\n**gctcli** _command_ [_options_]"],["PARAMETERS","**-config** _FILE_\n> Path to configuration file.\n\n**-verbose**\n> Enable verbose output.\n\n**-version**\n> Display version.\n\n**-help**\n> Display help.\n\n**-grpchost** _HOST_\n> gRPC host address (gctcli, default: localhost).\n\n**-grpcport** _PORT_\n> gRPC port (gctcli, default: 9052)."],["DESCRIPTION","**gocryptotrader** is an open-source cryptocurrency trading engine written in Go. It supports simultaneous connections to multiple exchanges for trading, portfolio management, and market data retrieval.\n\nThe companion CLI tool **gctcli** provides command-line access to the trading engine via gRPC, allowing users to query tickers, manage orders, view portfolios, and configure exchange connections. The engine supports backtesting, live trading, and paper trading modes."],["CAVEATS","Requires API keys from supported exchanges for trading functionality. The trading engine must be running for gctcli commands to work. Written in Go."],["SEE ALSO","[freqtrade](/man/freqtrade)(1), [cointop](/man/cointop)(1)"]],"e":["freqtrade","cointop"],"x":"gocryptotrader"},"goenv":{"n":"goenv","t":"Go version manager","s":[["TAGLINE","Go version manager"],["TLDR","**List available Go versions**\n\n```goenv install -l```\n\n**Install a Go version**\n\n```goenv install [1.21.0]```\n\n**Install the latest stable version**\n\n```goenv install latest```\n\n**List installed versions**\n\n```goenv versions```\n\n**Set global Go version**\n\n```goenv global [1.21.0]```\n\n**Set local version for directory**\n\n```goenv local [1.21.0]```\n\n**Show current version**\n\n```goenv version```\n\n**Uninstall a version**\n\n```goenv uninstall [1.21.0]```"],["SYNOPSIS","**goenv** _command_ [_args_]"],["SUBCOMMANDS","**install** _version_\n> Install a Go version.\n\n**uninstall** _version_\n> Uninstall a Go version.\n\n**versions**\n> List installed versions.\n\n**version**\n> Show current active version.\n\n**global** _version_\n> Set global default version.\n\n**local** _version_\n> Set directory-specific version (writes a `.go-version` file).\n\n**shell** _version_\n> Set a shell-specific version via the `GOENV_VERSION` variable.\n\n**which** _command_\n> Show the full path to the executable that goenv will invoke.\n\n**rehash**\n> Rebuild shim executables.\n\n**init**\n> Configure shell environment."],["PARAMETERS","**-l**, **--list**\n> List available versions for install."],["DESCRIPTION","**goenv** is a Go version manager inspired by rbenv and pyenv. It allows installing multiple Go versions side by side and switching between them per-project or globally.\n\nVersion selection is managed through **.go-version** files and shims that intercept Go commands, routing them to the appropriate version."],["CONFIGURATION","**~/.goenv/version**\n> Global Go version setting.\n\n**.go-version**\n> Per-directory Go version override file.\n\n**GOENV_ROOT**\n> Environment variable specifying the goenv installation directory (default ~/.goenv)."],["INSTALL","```brew: brew install goenv```"],["SEE ALSO","[go](/man/go)(1), [pyenv](/man/pyenv)(1), [rbenv](/man/rbenv)(1)"]],"e":["go","pyenv","rbenv"],"x":"goenv install -l"},"gource":{"n":"gource","t":"Animated version control history visualization","s":[["TAGLINE","Animated version control history visualization"],["TLDR","**Visualize current repo**\n\n```gource```\n\n**Visualize specific repo**\n\n```gource [/path/to/repo]```\n\n**Run in fullscreen mode**\n\n```gource -f```\n\n**Output PPM stream and pipe to ffmpeg for video**\n\n```gource -1280x720 -o - | ffmpeg -y -r 60 -f image2pipe -vcodec ppm -i - -vcodec libx264 -pix_fmt yuv420p [output.mp4]```\n\n**Set time scale**\n\n```gource --seconds-per-day [0.1]```\n\n**Hide specific elements**\n\n```gource --hide filenames,mouse,progress```\n\n**Visualize with a date range**\n\n```gource --start-date \"[2024-01-01]\" --stop-date \"[2024-12-31]\"```"],["SYNOPSIS","**gource** [_options_] [_path_]"],["PARAMETERS","_PATH_\n> Repository path (defaults to current directory).\n\n**-o**, **--output-ppm-stream** _FILE_\n> Output PPM image stream to file ('-' for stdout). Used with ffmpeg for video creation.\n\n**-r**, **--output-framerate** _FPS_\n> Framerate of output (25, 30, 60). Used with --output-ppm-stream.\n\n**-WIDTHxHEIGHT**, **--viewport** _WIDTHxHEIGHT_\n> Set viewport size (e.g., 1280x720).\n\n**--seconds-per-day** _SEC_\n> Time in seconds for each day of history (default: 10).\n\n**--hide** _ELEMENTS_\n> Hide display elements (comma-separated): bloom, date, dirnames, files, filenames, mouse, progress, root, tree, users, usernames.\n\n**--start-date** _DATE_\n> Start at a date (YYYY-MM-DD format).\n\n**--stop-date** _DATE_\n> Stop at a date.\n\n**-f**, **--fullscreen**\n> Fullscreen mode.\n\n**--title** _TITLE_\n> Set a title.\n\n**--load-config** _FILE_\n> Load a configuration file.\n\n**--save-config** _FILE_\n> Save current options to a configuration file.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**gource** visualizes version control history as an animated tree. Developers appear as avatars making changes, with files and directories branching from the center in a real-time animation.\n\nThe tool supports Git, SVN, Mercurial, and Bazaar repositories. Output can be piped to video encoders like ffmpeg for creating shareable recordings of project history."],["CAVEATS","Requires OpenGL. Large repositories with many files or commits may be slow to render. The output stream uses uncompressed PPM format, so piped video files can be very large before encoding. Use --seconds-per-day and --hide to optimize for large repos."],["HISTORY","gource was created by **Andrew Caudwell** to visualize software development history as engaging animations."],["INSTALL","```apt: sudo apt install gource```\n\n```dnf: sudo dnf install gource```\n\n```pacman: sudo pacman -S gource```\n\n```apk: sudo apk add gource```\n\n```zypper: sudo zypper install gource```\n\n```brew: brew install gource```\n\n```nix: nix profile install nixpkgs#gource```"],["SEE ALSO","[git-log](/man/git-log)(1), [ffmpeg](/man/ffmpeg)(1)"]],"e":["git-log","ffmpeg"],"x":"gource"},"gtop":{"n":"gtop","t":"terminal-based graphical activity monitor written in Node","s":[["TAGLINE","terminal-based graphical activity monitor written in Node"],["TLDR","**Start gtop**\n\n```gtop```"],["SYNOPSIS","**gtop**"],["KEYBOARD COMMANDS","**q**\n> Quit.\n\n**p**\n> Sort by PID.\n\n**c**\n> Sort by CPU.\n\n**m**\n> Sort by memory."],["DESCRIPTION","**gtop** is a terminal-based graphical activity monitor written in Node.js. It displays CPU, memory, network, disk, and process information with visual graphs.\n\nThe interface shows system metrics in panels: CPU usage per core with bars, memory and swap usage, network throughput graphs, disk usage bars, and a process list.\n\nCPU panel displays each core's usage as a horizontal bar with percentage. Memory shows used/total with progress bars. Network displays send/receive rates.\n\nProcess list shows top processes by resource usage. Sort by CPU (**c**), memory (**m**), or PID (**p**). The list updates in real-time.\n\ngtop inspired several similar tools written in other languages (gotop, btm) offering additional features and better performance."],["CAVEATS","Requires Node.js runtime. Higher CPU usage compared to native alternatives. Limited configuration options. No custom layouts or themes."],["HISTORY","gtop was created by **Can Güney Aksakalli** (aksakalli) and first released in **2017**. Written in Node.js using blessed-contrib, it popularized the graphical terminal monitor style. The visual design inspired many successors including gotop (Go), bashtop/bpytop (Python), and bottom (Rust). gtop remains popular for its simplicity and ease of installation via npm."],["INSTALL","```pacman: sudo pacman -S gtop```\n\n```brew: brew install gtop```\n\n```nix: nix profile install nixpkgs#gtop```"],["SEE ALSO","[htop](/man/htop)(1), [gotop](/man/gotop)(1), [btm](/man/btm)(1), [top](/man/top)(1)"],["RESOURCES","```[Source code](https://github.com/aksakalli/gtop)```"]],"e":["htop","gotop","btm","top"],"x":"gtop"},"hadolint":{"n":"hadolint","t":"dockerfile linter that validates best practices and common mistakes","s":[["TAGLINE","dockerfile linter that validates best practices and common mistakes"],["TLDR","**Lint a Dockerfile**\n\n```hadolint [path/to/Dockerfile]```\n\n**Lint from stdin**\n\n```cat Dockerfile | hadolint -```\n\n**Ignore specific rules**\n\n```hadolint --ignore [DL3008] --ignore [DL3009] [path/to/Dockerfile]```\n\n**Output as JSON**\n\n```hadolint --format json [path/to/Dockerfile]```\n\n**Only allow images from trusted registries**\n\n```hadolint --trusted-registry [docker.io] [path/to/Dockerfile]```\n\n**Use a custom config file**\n\n```hadolint --config [path/to/.hadolint.yaml] [path/to/Dockerfile]```\n\n**Set failure threshold severity**\n\n```hadolint --failure-threshold [warning] [path/to/Dockerfile]```"],["SYNOPSIS","**hadolint** [_options_] _dockerfile_"],["PARAMETERS","_DOCKERFILE_\n> Dockerfile to lint.\n\n**--ignore** _RULE_\n> Ignore specific rule.\n\n**--format** _FMT_\n> Output format: tty, json, checkstyle, codeclimate, gitlab_codeclimate, gnu, codacy, sonarqube, sarif (default: tty).\n\n**--trusted-registry** _REG_\n> Trusted container registry. Images from untrusted registries generate warnings.\n\n**--config** _FILE_\n> Config file path (default: .hadolint.yaml).\n\n**--failure-threshold** _SEVERITY_\n> Exit with failure code only for rules at or above this severity (error, warning, info, style, ignore, none).\n\n**--no-color**\n> Disable colored output.\n\n**--no-fail**\n> Don't exit with a failure status code.\n\n**-t**, **--strict-labels**\n> Check that labels conform to defined format.\n\n**--help**\n> Display help information."],["DESCRIPTION","**Hadolint** is a Dockerfile linter that validates best practices and common mistakes. It uses ShellCheck to validate shell commands in RUN instructions.\n\nThe tool checks for security issues, inefficient layering, and deprecated syntax. It helps create optimized, secure container images."],["CAVEATS","Shell checking requires bash syntax. Some rules may not apply to all use cases. Configuration file recommended."],["HISTORY","Hadolint was created to improve Dockerfile quality by combining Docker best practices with ShellCheck's shell analysis."],["INSTALL","```dnf: sudo dnf install hadolint```\n\n```brew: brew install hadolint```\n\n```nix: nix profile install nixpkgs#hadolint```"],["SEE ALSO","[docker](/man/docker)(1), [shellcheck](/man/shellcheck)(1)"]],"e":["docker","shellcheck"],"x":"hadolint [path/to/Dockerfile]"},"haml":{"n":"haml","t":"templating language that compiles to HTML","s":[["TAGLINE","templating language that compiles to HTML"],["TLDR","**Render a Haml file to HTML** (prints to stdout)\n\n```haml render [input.haml]```\n\n**Render and save the result to a file**\n\n```haml render [input.haml] > [output.html]```\n\n**Render from stdin**\n\n```cat [input.haml] | haml render -```\n\n**Show the compiled Ruby source** instead of evaluating it\n\n```haml compile [input.haml]```\n\n**Check Haml syntax** without evaluating\n\n```haml compile --check [input.haml]```\n\n**Show the parsed syntax tree**\n\n```haml parse [input.haml]```\n\n**Display the installed Haml version**\n\n```haml version```"],["SYNOPSIS","**haml** _command_ [_options_] _file_"],["PARAMETERS","**render** _file_\n> Render the Haml template and print the resulting HTML to stdout. Use **-** to read from stdin.\n\n**compile** _file_\n> Print the compiled Ruby source for the template instead of evaluating it.\n\n**-c**, **--check**\n> With **compile**, only parse and validate the generated Ruby; print \"Syntax OK\" or the error instead of the source.\n\n**temple** _file_\n> Print the intermediate Temple expression used to generate the Ruby code.\n\n**parse** _file_\n> Print the parsed abstract syntax tree.\n\n**version**\n> Print the installed Haml version.\n\n**-r**, **--require** _file_\n> Require a Ruby library before rendering (render command only).\n\n**-I**, **--load-path** _path_\n> Add a directory to Ruby's load path (render command only).\n\n**--no-escape-html**\n> Disable automatic escaping of `=` output (escaping is on by default).\n\n**--no-escape-attrs**\n> Disable automatic escaping of attribute values (escaping is on by default)."],["DESCRIPTION","**HAML** (HTML Abstraction Markup Language) is a templating language that compiles to HTML. It uses indentation for nesting and provides a concise syntax for HTML elements, attributes, and embedded Ruby.\n\nSince Haml 6, the engine is built on Temple and the `haml` executable is a subcommand-based CLI (render, compile, temple, parse, version) rather than a single set of flags.\n\nHAML is commonly used in Ruby on Rails applications, via the separate `haml-rails` gem, but can be used standalone for any HTML generation."],["HAML SYNTAX","```haml\n%html\n  %head\n    %title My Page\n  %body\n    %h1 Hello World\n    %p.intro Welcome!\n    #content\n      %ul\n        - items.each do |item|\n          %li= item\n```"],["CAVEATS","Requires Ruby. Whitespace-sensitive; indentation errors cause failures. The CLI's subcommands (render, compile, temple, parse) replaced the older single-invocation `haml input output` style and flags like `--rails` or `--style` in Haml 6; scripts written for Haml 3/4/5 need updating."],["HISTORY","HAML was created by **Hampton Catlin** in **2006** as part of the Ruby ecosystem. Haml 6 (2022) replaced the original hand-written compiler with one built on Temple, rewrote the CLI around Thor subcommands, and dropped the html4/xhtml output format options."],["INSTALL","```apt: sudo apt install ruby-haml```\n\n```pacman: sudo pacman -S ruby-haml```"],["SEE ALSO","[erb](/man/erb)(1), [slim](/man/slim)(1), [pug](/man/pug)(1)"],["RESOURCES","```[Source code](https://github.com/haml/haml)```\n\n```[Homepage](https://haml.info/)```\n\n```[Documentation](https://haml.info/docs.html)```"]],"e":["erb","slim","pug"],"x":"haml render [input.haml]"},"hledger-add":{"n":"hledger-add","t":"interactively prompts for new transactions and appends them to the journal file","s":[["TAGLINE","interactively prompts for new transactions and appends them to the journal file"],["TLDR","**Add transactions interactively**\n\n```hledger add```\n\n**Add to specific file**\n\n```hledger add -f [journal.ledger]```\n\n**Don't allow creating new accounts**\n\n```hledger add --no-new-accounts```\n\n**Pre-fill the first prompts** (date, description, account, amount) from the command line\n\n```hledger add [today] [\"best buy\"] [expenses:supplies] [$20]```\n\n**Ignore balance assertions while adding**\n\n```hledger add -I```"],["SYNOPSIS","**hledger** **add** [_options_] [_entry-fields_]"],["PARAMETERS","**-f**, **--file** _file_\n> Use specified journal file (the entry is appended to the first one given).\n\n**--no-new-accounts**\n> Don't allow creating new accounts; only accounts already used in the journal may be entered.\n\n**--layout=hledger1|**_COL_\n> How posting amounts should be aligned: right-aligned as in hledger 1 (default), or with decimal marks aligned at column COL.\n\n**-I**, **--ignore-assertions**\n> Disable balance assertion checking while entering amounts."],["DESCRIPTION","**hledger add** interactively prompts for new transactions and appends them to the journal file. It guides through date (defaults to today, accepting words like \"yesterday\"/\"today\"/\"tomorrow\"), an optional transaction code, description, account names, and amounts, with tab-completion drawn from existing entries.\n\nIt tries to provide useful defaults, using the most similar (by description) and nearest-dated existing transaction as a template. Initial values for the first few prompts can also be given as arguments on the command line. Readline-style edit keys work during entry; enter **<** at any prompt to go back a step. Amounts can include a balance assertion (**AMOUNT = BALANCE**) or balance assignment (**= BALANCEAMOUNT**), and hledger re-checks the journal's balance assertions after each amount, rejecting ones that would fail. Enter **.**, or press Control-d or Control-c, when finished adding transactions."],["CAVEATS","This is one of the only hledger commands that writes to the journal file (see also **import**); existing transactions are never changed. It works only with journal-format files, not timeclock or timedot files. **add** won't let you record a posting dated earlier than an existing balance assignment in that account; run with **-I** to work around this."],["INSTALL","```apt: sudo apt install hledger```\n\n```dnf: sudo dnf install hledger```\n\n```pacman: sudo pacman -S hledger```\n\n```apk: sudo apk add hledger```\n\n```zypper: sudo zypper install hledger```\n\n```brew: brew install hledger```\n\n```nix: nix profile install nixpkgs#hledger```"],["SEE ALSO","[hledger](/man/hledger)(1), [hledger-import](/man/hledger-import)(1), [hledger-accounts](/man/hledger-accounts)(1)"]],"e":["hledger","hledger-import","hledger-accounts"],"x":"hledger add"},"installpkg":{"n":"installpkg","t":"installs Slackware packages, which are compressed tar archives containing","s":[["TAGLINE","installs Slackware packages, which are compressed tar archives containing"],["TLDR","**Install** a Slackware package\n\n```sudo installpkg [path/to/package.tgz]```\n\n**Simulate** installation and report to stdout\n\n```installpkg -warn [path/to/package.tgz]```\n\n**Create** a package from current directory\n\n```installpkg -m [package_name.tgz]```\n\n**Install** contents of current directory as named package\n\n```sudo installpkg -r [package_name.tgz]```"],["SYNOPSIS","**installpkg** [_options_] _package_"],["PARAMETERS","**-warn**\n> Generate installation report without actually installing\n\n**-m** _NAME_\n> Make a package from the current directory\n\n**-r** _NAME_\n> Install current directory contents as a package\n\n**-menu**\n> Interactive package browser\n\n**-priority** _ORDER_\n> Set search order for packages"],["DESCRIPTION","**installpkg** installs Slackware packages, which are compressed tar archives containing files and installation scripts. It extracts package contents to the filesystem and runs any included doinst.sh installation script.\n\nPackage metadata is stored in /var/log/packages, allowing tracking of installed files for later removal or upgrade. The tool handles the .tgz, .txz, .tbz, and .tlz package formats."],["CAVEATS","Slackware-specific package tool. Does not handle dependencies automatically. Overwrites existing files without warning unless using -warn option."],["HISTORY","installpkg has been part of Slackware Linux since its early releases in **1993**. Slackware's package management is intentionally simple, preferring manual dependency handling to give users full control."],["SEE ALSO","[removepkg](/man/removepkg)(8), [upgradepkg](/man/upgradepkg)(8), [pkgtool](/man/pkgtool)(8)"]],"e":["removepkg","upgradepkg","pkgtool"],"x":"sudo installpkg [path/to/package.tgz]"},"ipcmk":{"n":"ipcmk","t":"creates System V IPC resources","s":[["TAGLINE","creates System V IPC resources"],["TLDR","Create a **shared memory** segment\n\n```ipcmk -M [segment_size_in_bytes]```\n\nCreate a **semaphore** set\n\n```ipcmk -S [element_size]```\n\nCreate a **message queue**\n\n```ipcmk -Q```\n\nCreate shared memory with specific **permissions**\n\n```ipcmk -M [segment_size] -p [0644]```"],["SYNOPSIS","**ipcmk** [_options_]"],["PARAMETERS","**-M**, **--shmem** _SIZE_\n> Create shared memory segment of specified size in bytes\n\n**-S**, **--semaphore** _COUNT_\n> Create semaphore set with specified number of elements\n\n**-Q**, **--queue**\n> Create message queue\n\n**-p**, **--mode** _MODE_\n> Set permissions (octal, default 0644)"],["DESCRIPTION","**ipcmk** creates System V IPC (Inter-Process Communication) resources. These resources allow processes to communicate and synchronize: shared memory enables direct data sharing, semaphores coordinate access to resources, and message queues enable asynchronous messaging.\n\nCreated resources are identified by a key and ID, which can be viewed with ipcs and removed with ipcrm. Resources persist until explicitly removed or system reboot."],["CAVEATS","System V IPC has kernel limits on the number and size of resources. Resources are not automatically cleaned up when creating processes exit. Modern applications often prefer POSIX IPC or other mechanisms."],["HISTORY","System V IPC was introduced in AT&T UNIX System V in the early **1980s**. While older than POSIX alternatives, it remains widely used and supported across Unix-like systems."],["INSTALL","```apt: sudo apt install util-linux```\n\n```dnf: sudo dnf install util-linux```\n\n```pacman: sudo pacman -S util-linux```\n\n```apk: sudo apk add util-linux-misc```\n\n```zypper: sudo zypper install util-linux```\n\n```brew: brew install util-linux```\n\n```nix: nix profile install nixpkgs#util-linux```"],["SEE ALSO","[ipcs](/man/ipcs)(1), [ipcrm](/man/ipcrm)(1), [lsipc](/man/lsipc)(1)"]],"e":["ipcs","ipcrm","lsipc"],"x":"ipcmk -M [segment_size_in_bytes]"},"isisdl":{"n":"isisdl","t":"downloads course materials from TU Berlin's ISIS/Moodle platform","s":[["TAGLINE","downloads course materials from TU Berlin's ISIS/Moodle platform"],["TLDR","**Download all courses**\n\n```isisdl```\n\n**Download specific course**\n\n```isisdl -c [course_id]```\n\n**List available courses**\n\n```isisdl -l```\n\n**Download with sync**\n\n```isisdl -s```\n\n**Set download directory**\n\n```isisdl -d [/path/to/dir]```"],["SYNOPSIS","**isisdl** [_options_]"],["PARAMETERS","**-c** _ID_\n> Specific course ID.\n\n**-l**, **--list**\n> List available courses.\n\n**-d** _PATH_\n> Download directory.\n\n**-s**, **--sync**\n> Sync mode (skip existing).\n\n**--config**\n> Show configuration.\n\n**--help**\n> Display help information."],["DESCRIPTION","**isisdl** downloads course materials from TU Berlin's ISIS/Moodle platform. It authenticates and bulk downloads files.\n\nThe tool organizes downloads by course structure. It supports syncing to avoid re-downloading existing files."],["CAVEATS","TU Berlin specific. Requires ISIS account. Respects rate limits."],["HISTORY","isisdl was created to help **TU Berlin** students download course materials from the ISIS learning management system."],["INSTALL","```nix: nix profile install nixpkgs#isisdl```"],["SEE ALSO","[wget](/man/wget)(1), [curl](/man/curl)(1), [youtube-dl](/man/youtube-dl)(1)"]],"e":["wget","curl","youtube-dl"],"x":"isisdl"},"jj-git":{"n":"jj-git","t":"provides Git interoperability for Jujutsu","s":[["TAGLINE","provides Git interoperability for Jujutsu"],["TLDR","**Clone a Git repository**\n\n```jj git clone [url]```\n\n**Initialize jj in an existing Git repository (colocated)**\n\n```jj git init --colocate```\n\n**Fetch from all remotes**\n\n```jj git fetch```\n\n**Push current branch to remote**\n\n```jj git push```\n\n**Import Git refs into jj**\n\n```jj git import```\n\n**Export jj refs to Git**\n\n```jj git export```\n\n**Add a Git remote**\n\n```jj git remote add [name] [url]```"],["SYNOPSIS","**jj git** _subcommand_ [_options_]"],["PARAMETERS","**clone** _URL_ [_DESTINATION_]\n> Clone a Git repository and create a jj repo.\n\n**init** [_DESTINATION_]\n> Create a new Git-backed jj repository.\n\n**fetch**\n> Fetch from Git remotes.\n\n**push**\n> Push to a Git remote.\n\n**import**\n> Import Git refs (branches, tags) into jj's internal state.\n\n**export**\n> Export jj refs to Git refs.\n\n**remote** _subcommand_\n> Manage Git remotes (add, remove, list, rename).\n\n**--remote** _NAME_\n> Specify remote name.\n\n**--branch** _BRANCH_\n> Specify branch (for push/fetch).\n\n**--help**\n> Display help information."],["DESCRIPTION","**jj git** provides Git interoperability for Jujutsu (jj), a modern version control system. It enables cloning, fetching, pushing, and managing remotes on Git servers.\n\nJujutsu uses Git as its storage backend, so all explicit Git interop commands live under the **git** subcommand. The **init --colocate** option allows using jj alongside an existing .git directory, keeping both in sync."],["CAVEATS","Subcommand of jj. Requires Git backend. Some Git workflows (e.g., staging area) are handled differently by jj."],["INSTALL","```pacman: sudo pacman -S jujutsu```\n\n```apk: sudo apk add jujutsu```\n\n```zypper: sudo zypper install jujutsu```\n\n```brew: brew install jujutsu```\n\n```nix: nix profile install nixpkgs#jujutsu```"],["SEE ALSO","[jj](/man/jj)(1), [jj-git-push](/man/jj-git-push)(1), [jj-git-clone](/man/jj-git-clone)(1), [git](/man/git)(1)"]],"e":["jj","jj-git-push","jj-git-clone","git"],"x":"jj git clone [url]"},"jxlinfo":{"n":"jxlinfo","t":"Display information about JPEG XL image files","s":[["TAGLINE","Display information about JPEG XL image files"],["TLDR","**Show codestream header information** for a JPEG XL file\n\n```jxlinfo [path/to/image.jxl]```\n\n**Verbose output** with full details about the bitstream\n\n```jxlinfo -v [path/to/image.jxl]```\n\n**Inspect multiple files** in a single invocation\n\n```jxlinfo [image1.jxl] [image2.jxl] [image3.jxl]```\n\n**Display help** and exit\n\n```jxlinfo -h```"],["SYNOPSIS","**jxlinfo** [_-v_] [_-h_] _INPUT_"],["PARAMETERS","**-v**, **--verbose**\n> Produce more verbose output, including detailed bitstream and frame information.\n\n**-h**, **--help**, **-?**\n> Display the help message listing all available options and exit.\n\n**_INPUT_**\n> One or more input JPEG XL image filenames to inspect."],["DESCRIPTION","**jxlinfo** prints information about JPEG XL (**.jxl**) image files by parsing the codestream header without performing a full decode. It reports image dimensions, bit depth, color encoding (including ICC profile presence), animation properties, and container box structure.\n\nThe tool is part of **libjxl-tools**, the reference implementation distribution for JPEG XL maintained by the JPEG XL Project. It is commonly used to verify that a file is a valid JPEG XL codestream, to inspect encoding parameters before decoding, and to debug encoder output.\n\nIn verbose mode, **jxlinfo** prints additional details such as frame headers, extra channels, animation timing, and intensity target. The non-verbose mode is suitable for scripting where a brief summary is needed."],["CAVEATS","Output format is not stable across versions of libjxl and should not be parsed by scripts that depend on exact field names. The tool inspects only the codestream header; it does not validate that the file can be decoded without errors. For full decoding, use **djxl**(1)."],["HISTORY","**jxlinfo** ships with **libjxl**, the reference implementation of the JPEG XL image coding system developed by the **JPEG XL Project** and contributors from Google, Cloudinary, and others. JPEG XL was standardized as **ISO/IEC 18181** starting in **2021**. The tool is packaged in **libjxl-tools** on Debian, Ubuntu, Fedora, and other distributions."],["INSTALL","```apt: sudo apt install libjxl-tools```\n\n```apk: sudo apk add libjxl-tools```\n\n```zypper: sudo zypper install libjxl-tools```"],["SEE ALSO","[djxl](/man/djxl)(1), [cjxl](/man/cjxl)(1), [identify](/man/identify)(1), [exiftool](/man/exiftool)(1)"]],"e":["djxl","cjxl","identify","exiftool"],"x":"jxlinfo [path/to/image.jxl]"},"lrunzip":{"n":"lrunzip","t":"decompresses files compressed with lrzip","s":[["TAGLINE","decompresses files compressed with lrzip"],["TLDR","This command is an **alias** of lrzip --decompress\n\n```tldr lrzip```"],["SYNOPSIS","**lrunzip** [_options_] [_file_]"],["DESCRIPTION","**lrunzip** decompresses files compressed with lrzip. It is equivalent to running `lrzip --decompress`."],["INSTALL","```apt: sudo apt install lrzip```\n\n```pacman: sudo pacman -S lrzip```\n\n```apk: sudo apk add lrzip```\n\n```zypper: sudo zypper install lrzip```\n\n```brew: brew install lrzip```\n\n```nix: nix profile install nixpkgs#lrzip```"],["SEE ALSO","[lrzip](/man/lrzip)(1), [lrztar](/man/lrztar)(1)"]],"e":["lrzip","lrztar"],"x":"tldr lrzip"},"mate-search-tool":{"n":"mate-search-tool","t":"graphical file search utility for the MATE desktop environment","s":[["TAGLINE","graphical file search utility for the MATE desktop environment"],["TLDR","Search files containing a **specific string** in their name\n\n```mate-search-tool --named=[string] --path=[path/to/directory]```\n\nSearch files **without waiting** for user confirmation\n\n```mate-search-tool --start --named=[string] --path=[path/to/directory]```\n\nSearch files with name matching a **specific regex**\n\n```mate-search-tool --start --regex=[string] --path=[path/to/directory]```\n\nSet a **sorting order** in search results\n\n```mate-search-tool --start --named=[string] --path=[path/to/directory] --sortby=[name|folder|size|type|date]```\n\nSet a **descending sorting** order\n\n```mate-search-tool --start --named=[string] --path=[path/to/directory] --descending```\n\nSearch files owned by a **specific user/group**\n\n```mate-search-tool --start --user=[value] --path=[path/to/directory]```"],["SYNOPSIS","**mate-search-tool** [_options_]"],["PARAMETERS","**--named _string_**\n> Search for files containing string in their name\n\n**--regex _pattern_**\n> Search for files matching the regular expression\n\n**--path _directory_**\n> Directory to search in\n\n**--start**\n> Begin search immediately without confirmation\n\n**--sortby _field_**\n> Sort results by: name, folder, size, type, or date\n\n**--descending**\n> Sort in descending order\n\n**--user _name_**\n> Search for files owned by specified user\n\n**--group _name_**\n> Search for files owned by specified group"],["DESCRIPTION","**mate-search-tool** is a graphical file search utility for the MATE desktop environment. It provides an easy-to-use interface for locating files based on name patterns, content, ownership, modification time, and other criteria.\n\nThe tool can search recursively through directories and supports both simple wildcard matching and full regular expressions. Results are displayed in a sortable list with file details.\n\nCommand-line options allow pre-configuring search parameters, useful for creating desktop shortcuts or scripts that launch specific searches."],["CAVEATS","Searching large directory trees can be slow. The tool does not index files; each search scans the filesystem in real-time. For faster searches on large systems, consider using **locate** with an updated database."],["INSTALL","```apt: sudo apt install mate-utils```\n\n```dnf: sudo dnf install mate-utils```\n\n```pacman: sudo pacman -S mate-utils```\n\n```apk: sudo apk add mate-utils```\n\n```zypper: sudo zypper install mate-search-tool```\n\n```nix: nix profile install nixpkgs#mate-utils```"],["SEE ALSO","[find](/man/find)(1), [locate](/man/locate)(1)"]],"e":["find","locate"],"x":"mate-search-tool --named=[string] --path=[path/to/directory]"},"mdp":{"n":"mdp","t":"A command-line based markdown presentation tool","s":[["TAGLINE","A command-line based markdown presentation tool"],["TLDR","**Present a markdown file as slides**\n\n```mdp [presentation.md]```\n\n**Read presentation from stdin**\n\n```cat [presentation.md] | mdp```\n\n**Present with color fading disabled**\n\n```mdp -f [presentation.md]```\n\n**Present with inverted colors**\n\n```mdp -i [presentation.md]```\n\n**Present with character entity expansion enabled**\n\n```mdp -e [presentation.md]```\n\n**Present with transparency disabled**\n\n```mdp -t [presentation.md]```"],["SYNOPSIS","**mdp** [_options_] [_FILE_]"],["PARAMETERS","_FILE_\n> Markdown presentation file. If no file is specified or if the file name is -, reads from standard input.\n\n**-e**, **--expand**\n> Enable character entity expansion (e.g. '&gt;' becomes '>').\n\n**-f**, **--nofade**\n> Disable color fading in 256 color mode.\n\n**-i**, **--invert**\n> Swap black and white colors.\n\n**-t**, **--notrans**\n> Disable transparency in transparent terminal.\n\n**-d**, **--debug**\n> Enable debug messages on stderr. Add multiple times to increase debug level.\n\n**-h**, **--help**\n> Display usage message and exit.\n\n**-v**, **--version**\n> Display version and license information."],["DESCRIPTION","**mdp** is a command-line based markdown presentation tool. It renders presentations in the terminal using standard markdown syntax.\n\nHorizontal rules are used as slide separators. Headers prefixed with **@** are displayed as title and author in the top and bottom bars. Supported formatting includes headlines, code blocks, block quotes, unordered lists, bold text, underlined text, and inline code.\n\nNavigation uses arrow keys, h/j/k/l, Space, Enter, Page Up/Down. Press **g** or **Home** to go to the first slide, **G** or **End** for the last. Number keys **1-9** jump to a specific slide. Press **r** to reload and **q** to quit."],["CAVEATS","Terminal-based only. Limited formatting compared to graphical presentation tools. UTF-8 support varies by terminal."],["HISTORY","mdp was created by **Michael Göhler** as a terminal-based presentation tool using markdown for slide content."],["INSTALL","```apt: sudo apt install mdp```\n\n```pacman: sudo pacman -S mdp```\n\n```brew: brew install mdp```\n\n```nix: nix profile install nixpkgs#mdp```"],["SEE ALSO","[presenterm](/man/presenterm)(1), [slides](/man/slides)(1)"]],"e":["presenterm","slides"],"x":"mdp [presentation.md]"},"mkfs.ntfs":{"n":"mkfs.ntfs","t":"creates an NTFS filesystem on a device or file","s":[["TAGLINE","creates an NTFS filesystem on a device or file"],["TLDR","Create an **NTFS filesystem** inside partition Y on device X\n\n```sudo mkfs.ntfs [/dev/sdXY]```\n\nCreate an NTFS filesystem with a **volume label**\n\n```sudo mkfs.ntfs -L [volume_label] [/dev/sdXY]```\n\nCreate an NTFS filesystem with a **random UUID**\n\n```sudo mkfs.ntfs -U [/dev/sdXY]```\n\nPerform a **quick format** (skip zeroing and bad sector check)\n\n```sudo mkfs.ntfs -f [/dev/sdXY]```\n\nEnable **compression** on the filesystem\n\n```sudo mkfs.ntfs -C [/dev/sdXY]```\n\nCreate an NTFS filesystem with a specific **cluster size**\n\n```sudo mkfs.ntfs -c [4096] [/dev/sdXY]```\n\n**Simulate** formatting without writing to device\n\n```sudo mkfs.ntfs -n [/dev/sdXY]```"],["SYNOPSIS","**mkfs.ntfs** [**-f**|**-Q**] [**-c** _cluster-size_] [**-L** _label_] [**-C**] [**-U**] [**-n**] [**-q**|**-v**] _device_ [_number-of-sectors_]"],["PARAMETERS","**-f, -Q, --fast, --quick**\n> Perform quick format; skips zeroing volume and bad sector checking\n\n**-L, --label _string_**\n> Set volume label for the filesystem\n\n**-C, --enable-compression**\n> Enable filesystem-level compression\n\n**-U, --with-uuid**\n> Generate random volume UUID\n\n**-c, --cluster-size _bytes_**\n> Set cluster size from 256 to 2,097,152 bytes; default is 4096\n\n**-s, --sector-size _bytes_**\n> Set sector size: 256, 512, 1024, 2048, or 4096 bytes\n\n**-p, --partition-start _sector_**\n> Specify partition start sector\n\n**-H, --heads _num_**\n> Number of heads; required for Windows boot capability\n\n**-S, --sectors-per-track _num_**\n> Sectors per track; required for Windows boot capability\n\n**-z, --mft-zone-multiplier _num_**\n> MFT zone size multiplier (1-4); affects fragmentation behavior\n\n**-I, --no-indexing**\n> Disable content indexing on the volume\n\n**-n, --no-action**\n> Simulate formatting without writing changes\n\n**-q, --quiet**\n> Suppress non-error output\n\n**-v, --verbose**\n> Enable verbose output"],["DESCRIPTION","**mkfs.ntfs** (also known as **mkntfs**) creates an NTFS filesystem on a device or file. NTFS is the primary filesystem used by Windows NT and later versions, supporting features like file permissions, compression, encryption, and large volumes.\n\nThe utility automatically determines filesystem size if sector count is omitted. It creates the Master File Table (MFT), system files, and directory structures required by NTFS. The tool is part of the **ntfs-3g** package."],["CAVEATS","Windows chkdsk may report uppercase filename warnings due to Unicode version differences between Windows editions and mkntfs. For bootable Windows partitions, the -H and -S options must be set correctly. NTFS write support on Linux requires ntfs-3g driver."],["HISTORY","NTFS was developed by **Microsoft** and introduced with **Windows NT 3.1 in 1993**. It replaced FAT as the primary filesystem for Windows. The Linux ntfs-3g driver and mkfs.ntfs utility were developed by the **NTFS-3G** project, providing reliable read/write access to NTFS volumes from Linux systems."],["INSTALL","```apt: sudo apt install ntfs-3g```\n\n```dnf: sudo dnf install ntfs-3g```\n\n```pacman: sudo pacman -S ntfs-3g```\n\n```apk: sudo apk add ntfs-3g-progs```\n\n```zypper: sudo zypper install ntfs-3g```\n\n```brew: brew install ntfs-3g```"],["SEE ALSO","[mkfs](/man/mkfs)(8), [mkfs.ext4](/man/mkfs.ext4)(8), [mkfs.vfat](/man/mkfs.vfat)(8), [ntfs-3g](/man/ntfs-3g)(8), [ntfsfix](/man/ntfsfix)(8), [fdisk](/man/fdisk)(8), [mount](/man/mount)(8), [badblocks](/man/badblocks)(8)"]],"e":["mkfs","mkfs.ext4","mkfs.vfat","ntfs-3g","ntfsfix","fdisk","mount","badblocks"],"x":"sudo mkfs.ntfs [/dev/sdXY]"},"mtvtoppm":{"n":"mtvtoppm","t":"converts MTV ray tracer format to PPM","s":[["TAGLINE","converts MTV ray tracer format to PPM"],["TLDR","**Convert MTV ray tracer output to PPM**\n\n```mtvtoppm [input.mtv] > [output.ppm]```"],["SYNOPSIS","**mtvtoppm** [_file_]"],["DESCRIPTION","**mtvtoppm** reads an MTV ray tracer image file as input and produces a PPM image as output. MTV refers to the output format of the MTV ray tracer program by Mark T. VandeWettering (named after his initials). The format is a simple uncompressed truecolor raster format. Files typically use the .pic or .mtv extension. Part of the Netpbm toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[rawtoppm](/man/rawtoppm)(1), [qrttoppm](/man/qrttoppm)(1)"]],"e":["rawtoppm","qrttoppm"],"x":"mtvtoppm [input.mtv] > [output.ppm]"},"nix-flake-show":{"n":"nix-flake-show","t":"displays flake outputs","s":[["TAGLINE","displays flake outputs"],["TLDR","**Show flake outputs**\n\n```nix flake show```\n\n**Show remote flake**\n\n```nix flake show [github:NixOS/nixpkgs]```\n\n**Show as JSON**\n\n```nix flake show --json```\n\n**Show all systems**\n\n```nix flake show --all-systems```"],["SYNOPSIS","**nix** **flake** **show** [_options_] [_flake-url_]"],["PARAMETERS","_FLAKE-URL_\n> Flake reference.\n\n**--json**\n> Output as JSON.\n\n**--all-systems**\n> Show all system outputs.\n\n**--help**\n> Display help information."],["DESCRIPTION","**nix flake show** displays flake outputs. Shows packages, apps, and devShells.\n\nThe command lists available attributes. Tree view of flake structure."],["CAVEATS","Flakes must be enabled. Evaluates flake. Network for remote flakes."],["HISTORY","nix flake show is part of **Nix flakes** for inspecting flake output structure."],["INSTALL","```apt: sudo apt install nix-bin```\n\n```dnf: sudo dnf install nix```\n\n```pacman: sudo pacman -S nix```\n\n```apk: sudo apk add nix```\n\n```zypper: sudo zypper install nix```\n\n```nix: nix profile install nixpkgs#nix```"],["SEE ALSO","[nix-flake](/man/nix-flake)(1), [nix-flake-info](/man/nix-flake-info)(1), [nix](/man/nix)(1)"]],"e":["nix-flake","nix-flake-info","nix"],"x":"nix flake show"},"npm-list":{"n":"npm-list","t":"displays the dependency tree of installed packages","s":[["TAGLINE","displays the dependency tree of installed packages"],["TLDR","**List installed packages**\n\n```npm list```\n\n**List global packages**\n\n```npm list -g```\n\n**List only top-level packages**\n\n```npm list --depth=0```\n\n**List as JSON**\n\n```npm list --json```\n\n**List specific package**\n\n```npm list [package-name]```\n\n**List production dependencies**\n\n```npm list --prod```"],["SYNOPSIS","**npm** **list** [_package_] [_options_]"],["PARAMETERS","**-g**, **--global**\n> List global packages.\n\n**--depth** _n_\n> Dependency tree depth.\n\n**--json**\n> Output as JSON.\n\n**--prod**\n> Production dependencies only.\n\n**--dev**\n> Development dependencies only.\n\n**--all**\n> Show all packages."],["DESCRIPTION","**npm list** displays the dependency tree of installed packages. Shows versions and relationships. Use depth=0 for top-level only. Alias: npm ls."],["INSTALL","```pacman: sudo pacman -S npm```\n\n```apk: sudo apk add npm```\n\n```brew: brew install npm```"],["SEE ALSO","[npm](/man/npm)(1), [npm-install](/man/npm-install)(1)"]],"e":["npm","npm-install"],"x":"npm list"},"offlineimap":{"n":"offlineimap","t":"synchronizes IMAP mailboxes with local Maildir storage","s":[["TAGLINE","synchronizes IMAP mailboxes with local Maildir storage"],["TLDR","**Sync all accounts**\n\n```offlineimap```\n\n**Sync specific account**\n\n```offlineimap -a [account_name]```\n\n**Sync specific folder**\n\n```offlineimap -f [INBOX]```\n\n**Dry run** (show what would happen)\n\n```offlineimap --dry-run```\n\n**Quick sync** (skip flag sync)\n\n```offlineimap -q```\n\n**Sync once and exit**\n\n```offlineimap -o```\n\n**Sync with custom config**\n\n```offlineimap -c [/path/to/config]```\n\n**Show folder list**\n\n```offlineimap --info```"],["SYNOPSIS","**offlineimap** [_-a account_] [_-f folder_] [_-o_] [_-q_] [_options_]"],["PARAMETERS","**-a** _ACCOUNT_\n> Sync specific account.\n\n**-f** _FOLDER_\n> Sync specific folder.\n\n**-o**\n> One-time sync (no repeat).\n\n**-q**, **--quick**\n> Quick sync, skip flags.\n\n**-c** _FILE_\n> Configuration file.\n\n**-u** _UI_\n> User interface (quiet, basic, blinkenlights).\n\n**--dry-run**\n> Show actions without executing.\n\n**--info**\n> Show account/folder info.\n\n**-l** _FILE_\n> Log file.\n\n**--version**\n> Show version."],["DESCRIPTION","**offlineimap** synchronizes IMAP mailboxes with local Maildir storage. It enables offline access to email and backup of IMAP accounts.\n\nBidirectional sync keeps local and remote in agreement. New messages, deletions, and flag changes propagate both ways. Conflicts are handled according to configuration.\n\nMaildir format stores each message as a file, enabling access by mail clients like mutt, notmuch, and others. The local copy works when offline.\n\nConfiguration defines accounts, servers, and folder mappings. Multiple accounts can sync independently. Folder filtering controls which mailboxes to mirror.\n\nOAuth2 authentication works with Gmail and other providers requiring modern auth. Password storage can use system keyrings or external commands.\n\nQuick mode speeds up syncs by skipping unchanged folders. One-shot mode runs once without repeated checking."],["CAVEATS","Initial sync of large mailboxes takes time. Configuration can be complex. Deprecated in favor of isstralsync. Gmail quirks may need special handling."],["HISTORY","**offlineimap** was created by **John Googin** around **2002** for syncing IMAP mail to local storage. It became essential for offline email workflows. The project is now in maintenance mode, with **isstralsync** as the recommended successor."],["INSTALL","```dnf: sudo dnf install offlineimap```\n\n```pacman: sudo pacman -S offlineimap```\n\n```apk: sudo apk add offlineimap```\n\n```brew: brew install offlineimap```\n\n```nix: nix profile install nixpkgs#offlineimap```"],["SEE ALSO","[mbsync](/man/mbsync)(1), [mutt](/man/mutt)(1), [notmuch](/man/notmuch)(1)"]],"e":["mbsync","mutt","notmuch"],"x":"offlineimap"},"opusenc":{"n":"opusenc","t":"encodes audio to Opus format","s":[["TAGLINE","encodes audio to Opus format"],["TLDR","**Encode to Opus**\n\n```opusenc [input.wav] [output.opus]```\n\n**Set bitrate**\n\n```opusenc --bitrate [128] [input.wav] [output.opus]```\n\n**Set quality**\n\n```opusenc --vbr --comp [10] [input.wav] [output.opus]```\n\n**Add metadata**\n\n```opusenc --title \"[title]\" --artist \"[artist]\" [input.wav] [output.opus]```\n\n**Encode from pipe**\n\n```ffmpeg -i [input] -f wav - | opusenc - [output.opus]```"],["SYNOPSIS","**opusenc** [_options_] _input_ _output_"],["PARAMETERS","_INPUT_\n> Input audio file.\n\n_OUTPUT_\n> Output Opus file.\n\n**--bitrate** _KBPS_\n> Target bitrate in kbit/s. Default is 64 for mono, 96 for stereo.\n\n**--vbr**\n> Use variable bitrate (default).\n\n**--cvbr**\n> Use constrained variable bitrate.\n\n**--hard-cbr**\n> Use hard constant bitrate.\n\n**--comp** _LEVEL_\n> Encoding complexity, 0 (fast) to 10 (slowest, best quality). Default 10.\n\n**--framesize** _MS_\n> Frame size in ms (2.5, 5, 10, 20, 40, 60). Default 20.\n\n**--music** / **--speech**\n> Tune the encoder for music or for speech.\n\n**--downmix-mono** / **--downmix-stereo**\n> Force mono or stereo output.\n\n**--title** _TITLE_, **--artist** _NAME_, **--album** _NAME_\n> Set Vorbis-style metadata tags.\n\n**--raw**, **--raw-rate** _HZ_, **--raw-chan** _N_\n> Treat input as headerless PCM with the given sample rate and channel count.\n\n**--help**\n> Display help information."],["DESCRIPTION","**opusenc** encodes raw or container audio (WAV, AIFF, FLAC, or raw PCM) to the Opus codec inside an Ogg container. Opus is a low-latency, royalty-free codec well suited to both music and speech at bitrates from 6 to 510 kbit/s.\n\nBy default opusenc uses VBR at complexity 10, which gives the best quality for the size at the cost of CPU time. Reading from `-` lets you pipe from `ffmpeg`, `sox`, or similar tools."],["CAVEATS","Shipped as part of **opus-tools**. Input must be WAV, AIFF, FLAC, or raw PCM; MP3 and other lossy formats should be decoded first (typically via ffmpeg). Opus is lossy: re-encoding from another lossy source compounds artifacts."],["HISTORY","opusenc was created for encoding audio to the **Opus codec** format."],["INSTALL","```apt: sudo apt install opus-tools```\n\n```dnf: sudo dnf install opus-tools```\n\n```pacman: sudo pacman -S opus-tools```\n\n```apk: sudo apk add opus-tools```\n\n```zypper: sudo zypper install opus-tools```\n\n```brew: brew install opus-tools```\n\n```nix: nix profile install nixpkgs#opus-tools```"],["SEE ALSO","[opusdec](/man/opusdec)(1), [ffmpeg](/man/ffmpeg)(1), [lame](/man/lame)(1)"]],"e":["opusdec","ffmpeg","lame"],"x":"opusenc [input.wav] [output.opus]"},"pam_ftp":{"n":"pam_ftp","t":"provides anonymous FTP authentication","s":[["TAGLINE","provides anonymous FTP authentication"],["TLDR","**Enable anonymous FTP auth**\n\n```auth sufficient pam_ftp.so```\n\n**With custom users**\n\n```auth sufficient pam_ftp.so users=ftp,anonymous```\n\n**Ignore anonymous users**\n\n```auth sufficient pam_ftp.so ignore```"],["SYNOPSIS","**pam_ftp.so** [_options_]"],["PARAMETERS","**users=**_LIST_\n> Comma-separated user list.\n\n**ignore**\n> Ignore for non-anonymous."],["DESCRIPTION","**pam_ftp** provides anonymous FTP authentication. Uses email as password.\n\nThe module validates email format passwords. For FTP services."],["CAVEATS","FTP specific. Email validation only. Security considerations."],["HISTORY","pam_ftp enables **anonymous FTP authentication** with email validation."],["SEE ALSO","[pam](/man/pam)(8), [pam_unix](/man/pam_unix)(8), [vsftpd](/man/vsftpd)(8)"]],"e":["pam","pam_unix","vsftpd"],"x":"auth sufficient pam_ftp.so"},"pg_combinebackup":{"n":"pg_combinebackup","t":"merges incremental backups with a full backup","s":[["TAGLINE","merges incremental backups with a full backup"],["TLDR","**Combine incremental backups**\n\n```pg_combinebackup [full_backup] [incr_backup] -o [output_dir]```\n\n**Combine multiple incrementals**\n\n```pg_combinebackup [full] [incr1] [incr2] -o [output]```"],["SYNOPSIS","**pg_combinebackup** [_options_] _backup_dir_... **-o** _output_dir_"],["PARAMETERS","**-o**, **--output** _dir_\n> Output directory for the combined backup. Must not already exist.\n\n**-n**, **--dry-run**\n> Show what would be done without actually writing output.\n\n**-T** _olddir=newdir_\n> Relocate a tablespace directory during combination.\n\n**--no-sync**\n> Skip fsync of the output directory (faster but less safe).\n\n**--sync-method** _method_\n> Method for syncing: **fsync** (default) or **syncfs**.\n\n**--manifest-checksums** _algorithm_\n> Checksum algorithm for the backup manifest: **CRC32C** (default), **SHA224**, **SHA256**, **SHA384**, **SHA512**, or **NONE**.\n\n**--no-manifest**\n> Do not generate a backup manifest in the output.\n\n**-d**, **--debug**\n> Print debug output.\n\n**-V**, **--version**\n> Print version and exit."],["DESCRIPTION","**pg_combinebackup** reconstructs a full backup by merging a base (full) backup with one or more incremental backups taken by **pg_basebackup --incremental**. The backup directories must be listed in chronological order: the full backup first, followed by each incremental in the order they were taken.\n\nThe output is a synthetic full backup that can be used directly with **pg_restore** or as a new base for future incremental backups. Introduced in PostgreSQL 17."],["CAVEATS","The output directory must not already exist. All backups in the chain must be present and listed in order. The full backup must have been taken with **--manifest** (the default). Incremental backup support requires PostgreSQL 17 or later."],["INSTALL","```dnf: sudo dnf install postgresql17```\n\n```apk: sudo apk add postgresql17```\n\n```zypper: sudo zypper install postgresql17```"],["SEE ALSO","[pg_basebackup](/man/pg_basebackup)(1)"]],"e":["pg_basebackup"],"x":"pg_combinebackup [full_backup] [incr_backup] -o [output_dir]"},"pm-suspend":{"n":"pm-suspend","t":"Suspend system to RAM","s":[["TAGLINE","Suspend system to RAM"],["TLDR","**Suspend to RAM (S3)**\n\n```sudo pm-suspend```\n\n**Turn display on after resume if it stays blank**\n\n```sudo pm-suspend --quirk-dpms-on```\n\n**Re-POST the video BIOS on resume**\n\n```sudo pm-suspend --quirk-vbe-post```\n\n**Save/restore PCI config for the VGA card**\n\n```sudo pm-suspend --quirk-save-pci```\n\n**Persist quirks that worked to the last-known-working DB**\n\n```sudo pm-suspend --store-quirks-as-lkw```"],["SYNOPSIS","**pm-suspend** [_--quirk-*_] [_--store-quirks-as-lkw_] [_--help_]"],["PARAMETERS","**--quirk-dpms-on**\n> Use DPMS to turn the display on after resume if it remains blank.\n\n**--quirk-dpms-suspend**\n> Use DPMS to switch the display off before suspending.\n\n**--quirk-radeon-off**\n> Disable Radeon display before suspend, re-enable on resume (legacy ThinkPads).\n\n**--quirk-s3-bios**\n> Call the video BIOS during S3 resume.\n\n**--quirk-s3-mode**\n> Set the video card to VGA text mode, then call the BIOS on resume.\n\n**--quirk-vbe-post**\n> Re-POST the video card via the VESA BIOS on resume.\n\n**--quirk-vbemode-restore**\n> Save and restore the VESA mode across suspend to prevent X corruption.\n\n**--quirk-vbestate-restore**\n> Save and restore low-level video state around suspend.\n\n**--quirk-vga-mode-3**\n> Force VGA text mode 3 on resume.\n\n**--quirk-save-pci**\n> Save and restore the PCI configuration space of the VGA card.\n\n**--store-quirks-as-lkw**\n> If the invocation succeeds, write the quirks used to `/var/cache/pm-utils/last_known_working.quirkdb` so they are applied automatically next time.\n\n**--help**\n> Show help."],["DESCRIPTION","**pm-suspend** puts the machine into ACPI S3 (suspend-to-RAM). Most devices are powered off, but system state is preserved in RAM, allowing near-instant resume at the cost of continuous (small) power draw. Typical laptops can maintain S3 for one to three days on battery.\n\n**pm-suspend** is part of **pm-utils**. It runs hook scripts in `/etc/pm/sleep.d/` and `/usr/lib/pm-utils/sleep.d/` before sleeping and again on resume. Hooks receive `suspend`/`resume` (or `hibernate`/`thaw`) as their first argument:\n\n```bash\n#!/bin/sh\ncase \"$1\" in\n    suspend|hibernate) : # before sleep ;;\n    resume|thaw)       : # after wake ;;\nesac\n```\n\nVideo hardware is the most common source of resume failures; the `--quirk-*` flags apply well-known workarounds. Use `pm-suspend --auto-quirks` (when supplied by the distribution) or combine `--store-quirks-as-lkw` with the quirks that fix your machine to make them persistent."],["CAVEATS","Deprecated on systemd-based distributions; prefer **systemctl suspend** (which uses systemd-logind and systemd-sleep). Requires root and a working ACPI/kernel suspend path. The right set of `--quirk-*` flags is hardware-specific; try them one at a time when troubleshooting black screens on resume."],["HISTORY","**pm-utils** was developed by **Richard Hughes**, **Peter Jones**, and the Fedora/openSUSE power-management teams in the late 2000s as a replacement for the older `hal`-based scripts. It became the standard suspend/hibernate frontend on most Linux distributions until superseded by **systemd-logind** and **systemd-sleep** around 2012-2015."],["INSTALL","```apk: sudo apk add pm-utils```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [rtcwake](/man/rtcwake)(8)"]],"e":["systemctl","rtcwake"],"x":"sudo pm-suspend"},"podman-images":{"n":"podman-images","t":"List locally stored container images","s":[["TAGLINE","List locally stored container images"],["TLDR","**List all images**\n\n```podman images```\n\n**List with digests**\n\n```podman images --digests```\n\n**Filter images** by reference\n\n```podman images --filter reference=[nginx*]```\n\n**Show image IDs only**\n\n```podman images -q```\n\n**Show all images** including intermediate layers\n\n```podman images -a```\n\n**Custom output format**\n\n```podman images --format \"{{.Repository}}:{{.Tag}} {{.Size}}\"```\n\n**List dangling** (untagged) images\n\n```podman images --filter dangling=true```"],["SYNOPSIS","**podman images** [_options_] [_repository_[:_tag_]]"],["PARAMETERS","**-a**, **--all**\n> Show all images including intermediate image layers.\n\n**--digests**\n> Show image digests.\n\n**-f**, **--filter** _filter_\n> Filter output based on conditions (key=value or key!=value).\n\n**--format** _format_\n> Change output format using Go templates or 'json'.\n\n**--history**\n> Display the history of image names (useful when images are re-tagged or untagged).\n\n**-n**, **--noheading**\n> Omit the table header from the output.\n\n**--no-trunc**\n> Do not truncate output (show full image IDs).\n\n**-q**, **--quiet**\n> Only display image IDs.\n\n**--sort** _field_\n> Sort by: created, id, repository, size, or tag (default: created)."],["DESCRIPTION","**podman images** lists container images stored locally. It shows repository, tag, image ID, creation date, and size for each image."],["EXAMPLES","```bash"],["List all images","podman images"],["With full IDs","podman images --no-trunc"],["Filter dangling images","podman images --filter dangling=true"],["Custom format","podman images --format \"{{.Repository}}:{{.Tag}} {{.Size}}\""],["Filter by label","podman images --filter label=maintainer=me"],["Sort by size","podman images --sort size\n```"],["FILTERS","```\ndangling=true        - Untagged images (<none> tag)\nreference=name:tag   - Match image reference pattern\nbefore=image         - Created before specified image\nsince=image          - Created after specified image\nlabel=key=value      - Has label (also supports label!=key)\nid=image_id          - Match image ID\ncontainers=true      - Images with running containers\nintermediate=true    - Intermediate build layers\nreadonly=true        - Read-only images\n```"],["CAVEATS","Size shown may be shared between images. Use --all to see intermediate layers."],["HISTORY","podman images is part of **Podman** by **Red Hat**, providing Docker-compatible image listing."],["INSTALL","```apt: sudo apt install podman```\n\n```dnf: sudo dnf install podman```\n\n```pacman: sudo pacman -S podman```\n\n```apk: sudo apk add podman```\n\n```zypper: sudo zypper install podman```\n\n```brew: brew install podman```\n\n```nix: nix profile install nixpkgs#podman```"],["SEE ALSO","[podman-pull](/man/podman-pull)(1), [podman-rmi](/man/podman-rmi)(1), [podman-image](/man/podman-image)(1), [podman-build](/man/podman-build)(1), [podman-ps](/man/podman-ps)(1), [docker-images](/man/docker-images)(1)"]],"e":["podman-pull","podman-rmi","podman-image","podman-build","podman-ps","docker-images"],"x":"podman images"},"powertop":{"n":"powertop","t":"Analyze and optimize power consumption","s":[["TAGLINE","Analyze and optimize power consumption"],["TLDR","**Calibrate** power measurements\n\n```sudo powertop --calibrate```\n\nGenerate **HTML report**\n\n```sudo powertop --html=[report.html]```\n\n**Auto-tune** for optimal power\n\n```sudo powertop --auto-tune```\n\nMeasure for specific **duration**\n\n```sudo powertop --time=[60]```"],["SYNOPSIS","**powertop** [**--calibrate**] [**--html** _file_] [**--auto-tune**] [**--time** _seconds_]"],["PARAMETERS","**--calibrate**\n> Calibrate power measurement (cycles display states)\n\n**--html _file_**\n> Generate HTML report\n\n**--auto-tune**\n> Apply all suggested power optimizations\n\n**--time _seconds_**\n> Measurement duration (default: 20)\n\n**--csv _file_**\n> Generate CSV report\n\n**--iteration _num_**\n> Number of iterations"],["DESCRIPTION","**powertop** analyzes system power consumption and suggests optimizations. It identifies power-hungry processes, devices, and kernel settings, providing actionable recommendations.\n\nThe interactive mode shows real-time power usage breakdown. Reports can identify specific software or hardware causing excessive power drain on laptops."],["CAVEATS","Calibration requires uninterrupted run and may take several minutes. Auto-tune changes are not persistent across reboots. Some optimizations may affect performance or stability. Root required for most functions."],["HISTORY","**powertop** was developed by **Intel** to help Linux users optimize battery life. It became a standard tool for laptop power management and is included in most distributions."],["INSTALL","```apt: sudo apt install powertop```\n\n```dnf: sudo dnf install powertop```\n\n```pacman: sudo pacman -S powertop```\n\n```apk: sudo apk add powertop```\n\n```zypper: sudo zypper install powertop```\n\n```nix: nix profile install nixpkgs#powertop```"],["SEE ALSO","[powerstat](/man/powerstat)(8), [tlp](/man/tlp)(1), [tuned](/man/tuned)(8)"]],"e":["powerstat","tlp","tuned"],"x":"sudo powertop --calibrate"},"ppmtoleaf":{"n":"ppmtoleaf","t":"Convert PPM to Interleaf image format","s":[["TAGLINE","Convert PPM to Interleaf image format"],["TLDR","**Convert a PPM to Interleaf image** format on stdout\n\n```ppmtoleaf [input.ppm] > [output.leaf]```\n\n**Read from stdin** and write Interleaf on stdout\n\n```cat [input.ppm] | ppmtoleaf > [output.leaf]```"],["SYNOPSIS","**ppmtoleaf** [_ppmfile_]"],["DESCRIPTION","**ppmtoleaf** reads a PPM image and writes it as an Interleaf raster image on standard output. The Interleaf format was used by the Interleaf desktop publishing system in the late 1980s and early 1990s.\n\nWith no input file, **ppmtoleaf** reads from standard input. Part of the Netpbm toolkit."],["CAVEATS","The Interleaf format is obsolete; this utility exists for compatibility with legacy documents. No options are provided beyond the input filename. Output goes to stdout, so redirect to a file."],["HISTORY","Part of the **Netpbm** package, originally from Jef Poskanzer's PBMplus toolkit."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[leaftoppm](/man/leaftoppm)(1), [ppmtopict](/man/ppmtopict)(1), [ppm](/man/ppm)(5)"]],"e":["leaftoppm","ppmtopict","ppm"],"x":"ppmtoleaf [input.ppm] > [output.leaf]"},"pyats-shell":{"n":"pyats-shell","t":"Interactive pyATS testing shell","s":[["TAGLINE","Interactive pyATS testing shell"],["TLDR","**Start pyATS shell**\n\n```pyats shell```\n\n**Start with testbed**\n\n```pyats shell --testbed-file [testbed.yaml]```"],["SYNOPSIS","**pyats shell** [_options_]"],["PARAMETERS","**--testbed-file** _file_\n> Testbed YAML file.\n\n**--no-ipython**\n> Use plain Python shell."],["DESCRIPTION","**pyats shell** starts an interactive Python shell with pyATS libraries loaded. Pre-loads testbed and device objects for network testing and automation. Part of Cisco pyATS framework."],["SEE ALSO","[pyats](/man/pyats)(1)"]],"e":["pyats"],"x":"pyats shell"},"qm-importovf":{"n":"qm-importovf","t":"Import a VM from an OVF manifest into Proxmox VE","s":[["TAGLINE","Import a VM from an OVF manifest into Proxmox VE"],["TLDR","Create a VM from an **OVF** package into the given storage\n\n```qm importovf [vmid] [path/to/manifest.ovf] [storage]```"],["SYNOPSIS","**qm importovf** _vmid_ _manifest_ _storage_ [_OPTIONS_]"],["PARAMETERS","**vmid**\n> Numeric ID for the new virtual machine\n\n**manifest**\n> Path to the `.ovf` Open Virtualization Format manifest\n\n**storage**\n> Destination Proxmox storage for disks"],["DESCRIPTION","**qm importovf** imports a virtual machine described by an Open Virtualization Format (OVF) package into Proxmox VE. OVF packages usually include a `.ovf` descriptor plus one or more disk images (often `.vmdk`). The command creates a new VM with the given ID and places disks on the specified storage.\n\nOVF is commonly used when migrating from VMware and other platforms that export OVA/OVF appliances. After import, review network, CPU, and disk bus settings under `qm config` / the web UI."],["CAVEATS","All referenced disk files must sit next to the manifest (or at the paths the OVF declares). Some guest hardware options do not map 1:1 to QEMU and need manual tuning. Ensure the target `vmid` is unused."],["HISTORY","Part of **Proxmox VE** QEMU/KVM management (`qm`)."],["INSTALL","```dnf: sudo dnf install qm```"],["SEE ALSO","[qm](/man/qm)(1), [qm-import](/man/qm-import)(1), [qm-importdisk](/man/qm-importdisk)(1)"],["RESOURCES","```[Homepage](https://www.proxmox.com)```\n\n```[Documentation](https://pve.proxmox.com/pve-docs/qm.1.html)```"]],"e":["qm","qm-import","qm-importdisk"],"x":"qm importovf [vmid] [path/to/manifest.ovf] [storage]"},"rip":{"n":"rip","t":"a safe and ergonomic alternative to rm","s":[["TAGLINE","a safe and ergonomic alternative to rm"],["TLDR","**Delete file safely**\n\n```rip [file.txt]```\n\n**Delete multiple files and directories**\n\n```rip [file1.txt] [file2.txt] [directory/]```\n\n**Inspect file info before deleting** (shows size and first few lines)\n\n```rip -i [file.txt]```\n\n**List files deleted from the current directory**\n\n```rip --seance```\n\n**Restore the last deleted file**\n\n```rip --unbury```\n\n**Restore a specific file** from the graveyard\n\n```rip --unbury [filename]```\n\n**Restore all files listed by seance** (combine -s and -u)\n\n```rip -su```\n\n**Permanently delete all files** in the graveyard\n\n```rip --decompose```\n\n**Use a custom graveyard directory**\n\n```rip --graveyard [path/to/graveyard] [file.txt]```"],["SYNOPSIS","**rip** [_FLAGS_] [_OPTIONS_] [_TARGET..._]"],["PARAMETERS","**-d**, **--decompose**\n> Permanently delete (unlink) the entire graveyard.\n\n**-i**, **--inspect**\n> Print info about TARGET before prompting for action.\n\n**-s**, **--seance**\n> Print files that were sent under the current directory.\n\n**-u**, **--unbury** _target_\n> Undo the last removal by the current user, or specify file(s) in the graveyard.\n\n**--graveyard** _graveyard_\n> Set directory where deleted files go to rest.\n\n**-h**, **--help**\n> Print help information.\n\n**-V**, **--version**\n> Print version information."],["DESCRIPTION","**rip** (Rm ImProved) is a safer alternative to rm that moves deleted files to a \"graveyard\" directory instead of permanently removing them. The graveyard preserves the original filesystem path structure, making it straightforward to restore files to their exact original locations when needed.\n\nThe **--seance** flag lists files deleted from under the current directory. The **--unbury** flag restores files, either the most recently deleted item or a specific named file. Combining **-s** and **-u** restores everything listed by seance. When graveyard contents are no longer needed, **--decompose** permanently removes them to reclaim disk space.\n\nThe graveyard location defaults to /tmp/graveyard-$USER but can be customized with the **--graveyard** flag. No data is overwritten; if files sharing the same path are deleted, they are renamed as numbered backups. The **--inspect** option shows size and first few lines of a file, or total size and first few entries of a directory, before prompting for deletion."],["CAVEATS","The graveyard uses disk space. Large deletes may fill /tmp storage. This is not a secure delete tool; files in the graveyard can be read by anyone with access."],["INSTALL","```aur: yay -S rip```"],["SEE ALSO","[rm](/man/rm)(1), [trash-cli](/man/trash-cli)(1), [safe-rm](/man/safe-rm)(1)"]],"e":["rm","trash-cli","safe-rm"],"x":"rip [file.txt]"},"ruget":{"n":"ruget","t":"Alternative file downloader written in Rust","s":[["TAGLINE","Alternative file downloader written in Rust"],["TLDR","**Download** URL contents to file\n\n```ruget https://example.com/file```\n\nDownload to **specified output** file\n\n```ruget -o file_name https://example.com/file```"],["SYNOPSIS","**ruget** [_options_] _URL_"],["PARAMETERS","**-o**, **--output** _file_\n> Specify output filename"],["DESCRIPTION","**ruget** is a download utility written in Rust, serving as an alternative to wget. It downloads files from URLs with a simple command-line interface.\n\nThe tool focuses on simplicity and modern Rust implementation."],["CAVEATS","May have fewer features than wget or curl. Check project documentation for supported protocols and options."],["HISTORY","Developed as a Rust alternative to traditional download tools like wget."],["INSTALL","```aur: yay -S ruget```"],["SEE ALSO","[wget](/man/wget)(1), [curl](/man/curl)(1), [aria2c](/man/aria2c)(1)"]],"e":["wget","curl","aria2c"],"x":"ruget https://example.com/file"},"simplehttpserver":{"n":"simplehttpserver","t":"Serve files via Python HTTP server","s":[["TAGLINE","Serve files via Python HTTP server"],["TLDR","**Serve current directory** on port 8000\n\n```python3 -m http.server```\n\n**Serve on a specific port**\n\n```python3 -m http.server [8080]```\n\n**Bind to localhost** only\n\n```python3 -m http.server --bind [127.0.0.1]```\n\n**Serve a specific directory** (Python 3.7+)\n\n```python3 -m http.server --directory [path/to/dir]```\n\n**Enable CGI** script execution\n\n```python3 -m http.server --cgi```\n\n**Python 2 equivalent** (deprecated)\n\n```python -m SimpleHTTPServer [8080]```"],["SYNOPSIS","**python3 -m http.server** [**-b** _address_] [**-d** _directory_] [**--cgi**] [**--protocol** _version_] [_port_]\n\n**python -m SimpleHTTPServer** [_port_]"],["PARAMETERS","_port_\n> Port number (default: 8000).\n\n**-b**, **--bind** _ADDRESS_\n> Bind to address (default: all interfaces). Python 3 only.\n\n**-d**, **--directory** _DIR_\n> Serve specified directory instead of current directory (Python 3.7+).\n\n**--cgi**\n> Enable CGI script execution from /cgi-bin.\n\n**--protocol** _VERSION_\n> HTTP protocol version (default: HTTP/1.0)."],["DESCRIPTION","**http.server** (Python 3) and **SimpleHTTPServer** (Python 2) are Python standard library modules that launch a basic HTTP server for serving static files. They require no installation or configuration, making them ideal for quick file sharing, local development, and testing.\n\nIn Python 2, the module is invoked as **python -m SimpleHTTPServer**, while Python 3 renamed it to **python3 -m http.server**. The server provides directory listings in a web browser and handles GET and HEAD requests. It binds to all interfaces on port 8000 by default, but the port and bind address can be customized."],["CAVEATS","Not suitable for production use. No authentication, encryption, or access control. Python 2 and SimpleHTTPServer are deprecated; use Python 3 http.server instead. The **--cgi** option is deprecated since Python 3.13."],["HISTORY","**SimpleHTTPServer** has been part of the Python standard library since Python 2. Python 3 reorganized the module as **http.server** with added options for binding, directory selection, and CGI support."],["INSTALL","```aur: yay -S simplehttpserver```"],["SEE ALSO","[python](/man/python)(1), [serve](/man/serve)(1), [http-server](/man/http-server)(1)"]],"e":["python","serve","http-server"],"x":"python3 -m http.server"},"slop":{"n":"slop","t":"Interactive screen region selection for X11","s":[["TAGLINE","Interactive screen region selection for X11"],["TLDR","Get a **screen selection** and output geometry\n\n```slop```\n\n**Double click** instead of click-and-drag\n\n```slop -D```\n\n**Highlight** selection instead of outline\n\n```slop -l```\n\nSpecify **output format**\n\n```slop -f \"%g\"```\n\nSet selection **color**\n\n```slop -c [red],[green],[blue],[alpha]```\n\nSet **border size**\n\n```slop -b [thickness]```"],["SYNOPSIS","**slop** [**-klqnD**] [_OPTIONS_]"],["PARAMETERS","**-f, --format** _string_\n> Output format using specifiers: %x, %y, %w, %h, %i (window ID), %c (cancel), %g (geometry)\n\n**-b, --bordersize** _float_\n> Selection rectangle border thickness\n\n**-p, --padding** _float_\n> Padding around selection (negative values shrink)\n\n**-c, --color** _r,g,b,a_\n> Selection box color as RGBA floats (0.0-1.0)\n\n**-t, --tolerance** _float_\n> Pixel threshold to distinguish click from drag\n\n**-D, --nodrag**\n> Use two-click mode instead of click-and-drag\n\n**-l, --highlight**\n> Fill selection area instead of drawing outline\n\n**-n, --nodecorations** _int_\n> Window decoration removal aggressiveness (0-2)\n\n**-k, --nokeyboard**\n> Disable keyboard cancellation\n\n**-q, --quiet**\n> Suppress warning messages\n\n**-r, --shader** _string_\n> Custom shader from ~/.config/slop\n\n**-o, --noopengl**\n> Disable OpenGL acceleration\n\n**-x, --xdisplay** _display_\n> Specify X display to use\n\n**-v, --version**\n> Display version\n\n**-h, --help**\n> Display help"],["DESCRIPTION","**slop** (Select Operation) queries the user for a screen region selection and outputs the geometry to stdout. Users can click and drag to create a selection rectangle, or select an existing window. The output format is configurable for integration with other tools.\n\nCommon use cases include screenshot region selection, window geometry capture, and integration with screen recording tools. The output can be parsed by scripts to perform operations on the selected area."],["CAVEATS","Requires X11; does not work on Wayland without XWayland. OpenGL acceleration requires appropriate drivers. Custom shaders must be placed in ~/.config/slop directory. Selection may include window decorations unless **-n** flag is used."],["HISTORY","**slop** was created by **naelstrof** as a successor to the selection functionality in **scrot**. The tool is designed to be modular and integrate with other utilities via shell pipelines. It is commonly used alongside **maim** for screenshots and various screen recording tools."],["INSTALL","```apt: sudo apt install slop```\n\n```dnf: sudo dnf install slop```\n\n```pacman: sudo pacman -S slop```\n\n```apk: sudo apk add slop```\n\n```zypper: sudo zypper install slop```\n\n```nix: nix profile install nixpkgs#slop```"],["SEE ALSO","[maim](/man/maim)(1), [scrot](/man/scrot)(1), [xdotool](/man/xdotool)(1), [xwininfo](/man/xwininfo)(1)"]],"e":["maim","scrot","xdotool","xwininfo"],"x":"slop"},"snort":{"n":"snort","t":"Network intrusion detection and prevention system","s":[["TAGLINE","Network intrusion detection and prevention system"],["TLDR","**Sniffer mode**\n\n```snort -v -i [eth0]```\n\n**Packet logger**\n\n```snort -dev -l [/var/log/snort] -i [eth0]```\n\n**IDS mode**\n\n```snort -c [/etc/snort/snort.conf] -i [eth0]```\n\n**Test configuration**\n\n```snort -T -c [/etc/snort/snort.conf]```\n\n**Read pcap file**\n\n```snort -r [capture.pcap] -c [snort.conf]```\n\n**Daemon mode**\n\n```snort -D -c [snort.conf] -i [eth0]```\n\n**Alert modes**\n\n```snort -A [fast] -c [snort.conf] -i [eth0]```"],["SYNOPSIS","**snort** [_-c config_] [_-l logdir_] [_-i interface_] [_options_]"],["PARAMETERS","**-c** _FILE_\n> Configuration file.\n\n**-i** _INTERFACE_\n> Network interface.\n\n**-l** _DIR_\n> Log directory.\n\n**-A** _MODE_\n> Alert mode (fast, full, console).\n\n**-D**\n> Daemon mode.\n\n**-r** _FILE_\n> Read pcap.\n\n**-T**\n> Test configuration.\n\n**-v**\n> Verbose.\n\n**-d**\n> Dump packet data.\n\n**-e**\n> Display link layer."],["DESCRIPTION","**snort** is an open-source network intrusion detection and prevention system (IDS/IPS) that performs real-time traffic analysis and packet logging. It examines network traffic against a set of user-defined rules to detect attacks, probes, and suspicious activity.\n\nThe tool operates in three modes: sniffer mode displays packets on the console, logger mode records traffic to pcap files for offline analysis, and IDS mode applies detection rules and generates alerts. Rules use a flexible language that matches on protocol, content patterns, flow direction, and other packet characteristics.\n\nSnort can operate inline as an IPS to actively block detected threats, or passively as an IDS that only monitors and alerts. Its rule format has become an industry standard, with thousands of community and commercial rules available for detecting known vulnerabilities, malware, and policy violations."],["CONFIGURATION","**/etc/snort/snort.conf**\n> Main configuration file defining network variables, preprocessors, output plugins, and rule paths.\n\n**/etc/snort/rules/**\n> Directory containing detection rule files loaded by the configuration."],["CAVEATS","Requires root for capture. Rule tuning needed. High bandwidth challenging."],["HISTORY","**Snort** was created by **Martin Roesch** in **1998**. It became the most widely deployed IDS, now maintained by Cisco."],["INSTALL","```apk: sudo apk add snort```\n\n```brew: brew install snort```\n\n```nix: nix profile install nixpkgs#snort```"],["SEE ALSO","[suricata](/man/suricata)(1), [tcpdump](/man/tcpdump)(1), [zeek](/man/zeek)(1)"]],"e":["suricata","tcpdump","zeek"],"x":"snort -v -i [eth0]"},"steamos-select-branch":{"n":"steamos-select-branch","t":"Select SteamOS update release channel","s":[["TAGLINE","Select SteamOS update release channel"],["TLDR","View **current** branch\n\n```steamos-select-branch -c```\n\n**List** available branches\n\n```steamos-select-branch -l```\n\nSwitch to **beta** branch\n\n```steamos-select-branch beta```\n\nSwitch to **stable** release\n\n```steamos-select-branch stable```\n\nSwitch to **bleeding edge**\n\n```steamos-select-branch main```"],["SYNOPSIS","**steamos-select-branch** [_OPTIONS_] [_BRANCH_]"],["PARAMETERS","**-c**\n> View current branch\n\n**-l**\n> List all available branches"],["BRANCHES","**stable**\n> Stable release channel (default, recommended)\n\n**beta**\n> Beta testing channel with newer features\n\n**main**\n> Bleeding edge development channel"],["DESCRIPTION","**steamos-select-branch** selects a SteamOS system release branch, controlling which update channel the system uses. The stable branch receives well-tested updates, beta provides preview releases, and main offers the latest development builds.\n\nAfter switching branches, run `steamos-update` to update to the selected branch's latest version."],["CAVEATS","Beta and main branches may contain bugs and instability. Switching to older branches may require a factory reset. Updates on non-stable branches may occur more frequently."],["HISTORY","**steamos-select-branch** is part of the **SteamOS** utilities developed by **Valve**. The branch system allows users to opt into testing newer features before stable release."],["SEE ALSO","[steamos-update](/man/steamos-update)(1)"]],"e":["steamos-update"],"x":"steamos-select-branch -c"},"termframe":{"n":"termframe","t":"Terminal output SVG screenshot tool","s":[["TAGLINE","Terminal output SVG screenshot tool"],["TLDR","**Capture command output as SVG**\n\n```termframe -o [output.svg] -- ls --color=always```\n\n**Auto-detect terminal size**\n\n```termframe --width auto --height auto -o [out.svg] -- [command]```\n\n**Use a specific color theme**\n\n```termframe --theme \"[Solarized Dark]\" -o [out.svg] -- [command]```"],["SYNOPSIS","**termframe** [_options_] **-o** _output.svg_ **--** _command_"],["PARAMETERS","**-o**, **--output** _FILE_\n> Output SVG file path.\n\n**-W**, **--width** _VALUE_\n> Terminal width (number, auto, or range like 80..120).\n\n**-H**, **--height** _VALUE_\n> Terminal height (number, auto, or range like 24..50).\n\n**--theme** _NAME_\n> Color theme from iTerm2-Color-Schemes.\n\n**--title** _TEXT_\n> Set window title.\n\n**--font-size** _SIZE_\n> Adjust font size.\n\n**--padding** _VALUE_\n> Configure window padding.\n\n**--window-style** _STYLE_\n> Window style (e.g., macos, compact).\n\n**--shell** _SHELL_\n> Specify shell to use.\n\n**--config** _FILE_\n> Override configuration file path (use - to disable defaults)."],["DESCRIPTION","**termframe** is a non-interactive terminal emulator that executes a command, renders its output in a virtual terminal session, and exports the result as an SVG file. It supports full color ranges (16, 256, truecolor), text styles, font embedding, and dark/light mode."],["HISTORY","**termframe** was created by **Pavel Ivanov** (pamburus) and is written in **Rust**."],["INSTALL","```brew: brew install termframe```"],["SEE ALSO","[silicon](/man/silicon)(1), [t-rec](/man/t-rec)(1)"]],"e":["silicon","t-rec"],"x":"termframe -o [output.svg] -- ls --color=always"},"tock":{"n":"tock","t":"Digital clock for the terminal","s":[["TAGLINE","Digital clock for the terminal"],["TLDR","**Display a centered 24-hour clock with seconds**\n\n```tock -s -m -c```\n\n**Custom color and font dimensions**\n\n```tock -C [4] -W [3] -H [2]```\n\n**Custom date format**\n\n```tock -f \"[%A, %B %d]\"```"],["SYNOPSIS","**tock** [_options_]"],["PARAMETERS","**-s**, **--second**\n> Display seconds.\n\n**-m**, **--military**\n> Use 24-hour (military) time format.\n\n**-c**, **--center**\n> Center the clock in the terminal (overrides manual positioning).\n\n**-C**, **--color** _COLOR_\n> Set the clock color using an ANSI value 0-7 (default: 2).\n\n**-x**, **--x** _X_\n> Horizontal 0-indexed position of the clock's top-left corner (default: 0).\n\n**-y**, **--y** _Y_\n> Vertical 0-indexed position of the clock's top-left corner (default: 0).\n\n**-W**, **--width** _WIDTH_\n> Font width in characters per tile (default: 2).\n\n**-H**, **--height** _HEIGHT_\n> Font height in characters per tile (default: 1).\n\n**-f**, **--format** _FORMAT_\n> Custom date format string (default: \"%F | %Z\").\n\n**-h**, **--help**\n> Display help information."],["INTERACTIVE KEYS","**q**\n> Quit the clock.\n\n**s**\n> Toggle seconds display.\n\n**m**\n> Toggle 24-hour (military) time.\n\n**0**-**7**\n> Change the ANSI color at runtime."],["DESCRIPTION","**tock** is a digital clock for the terminal inspired by tty-clock. It supports customizable positioning, font sizing, 12/24-hour modes, color selection, seconds display, and date formatting."],["CAVEATS","UNIX only due to termios usage."],["HISTORY","**tock** was created by **nwtnni** and is written in **Rust**."],["INSTALL","```brew: brew install tock```"],["SEE ALSO","[date](/man/date)(1), [tty-clock](/man/tty-clock)(1), [figlet](/man/figlet)(1)"]],"e":["date","tty-clock","figlet"],"x":"tock -s -m -c"},"tredis":{"n":"tredis","t":"Terminal UI for Redis data viewing and management","s":[["TAGLINE","Terminal UI for Redis data viewing and management"],["TLDR","**Launch the Redis TUI**\n\n```tredis```\n\n**Connect to a specific Redis server**\n\n```tredis --host [localhost] --port [6379]```\n\n**Connect to a specific database**\n\n```tredis --host [localhost] --port [6379] --db [2]```"],["SYNOPSIS","**tredis** [_options_]"],["DESCRIPTION","**tredis** is a terminal-based Redis data viewer and manager that provides a modern TUI alternative to the traditional redis-cli. It allows you to browse, view, and manage Redis data with an intuitive terminal interface."],["HISTORY","**tredis** was created by **Huseyin Babal** (huseyinbabal) and is written in **Rust**."],["SEE ALSO","[redis-cli](/man/redis-cli)(1)"]],"e":["redis-cli"],"x":"tredis"},"whence":{"n":"whence","t":"Show how a command name would be interpreted","s":[["TAGLINE","Show how a command name would be interpreted"],["TLDR","**Show how a name is resolved** (path, alias, or type)\n\n```whence [command]```\n\n**Verbose description** (like type or command -V)\n\n```whence -v [command]```\n\n**csh-style output** with function bodies (zsh; same as which)\n\n```whence -c [command]```\n\n**Word type only**: alias, builtin, command, function, hashed, reserved, or none (zsh)\n\n```whence -w [command]```\n\n**All matches** along PATH (and other interpretations)\n\n```whence -a [command]```\n\n**Force PATH search** even if name is an alias, function, or builtin\n\n```whence -p [command]```\n\n**Show all occurrences in csh format** (zsh; same as where)\n\n```whence -ca [command]```"],["SYNOPSIS","**whence** [**-afpv**] _name_...\n\n**whence** [**-vcwfpamsS**] [**-x** _num_] _name_... (zsh)"],["PARAMETERS","**-v**\n> Produce a more verbose report\n\n**-a**\n> Report all interpretations / search the entire command path (not only the first match)\n\n**-p**\n> Do a PATH search even if name is an alias, reserved word, function, or builtin\n\n**-f**\n> Skip the search for shell functions (ksh). In zsh, with **-c**, display the contents of a shell function\n\n**-c** (zsh)\n> Print results in a csh-like format (takes precedence over **-v**). Equivalent to the zsh **which** builtin when used alone\n\n**-w** (zsh)\n> Print `name: word` where word is one of: alias, builtin, command, function, hashed, reserved, or none\n\n**-m** (zsh)\n> Treat arguments as patterns and display matching commands (quote pattern characters)\n\n**-s** (zsh)\n> If a pathname contains symlinks, also print the symlink-free pathname\n\n**-S** (zsh)\n> Like **-s**, but print intermediate symlink resolution steps\n\n**-x** _num_ (zsh)\n> Expand tabs when outputting shell functions with **-c** (same idea as **functions -x**)"],["DESCRIPTION","**whence** is a shell builtin in **ksh** and **zsh** that reports how each _name_ would be interpreted if used as a command. Unlike the external **which** utility, it understands shell aliases, functions, builtins, reserved words, and hashed commands, not only executables on PATH.\n\nWith no options, **whence** typically prints the resolved path for external commands or an indication of the alias or other shell object. **-v** adds a descriptive message similar to **type** or **command -V**. **-p** forces a filesystem PATH search when a name is shadowed by a shell construct.\n\nIn **zsh**, **whence** is the underlying implementation for several related builtins:\n\n- **which** is equivalent to **whence -c**\n- **where** is equivalent to **whence -ca**\n- **type** is equivalent to **whence -v**\n\n**ksh** provides a smaller option set (**-a**, **-f**, **-p**, **-v**). **bash** and POSIX shells do not provide **whence**; use **type**, **command -v**, or **command -V** there instead."],["EXIT STATUS","**0**\n> Name was found (recognized as a command form the shell understands)\n\n**non-zero**\n> Name was not recognized. In zsh, a not-found message for **-v**, **-c**, or **-w** is written to standard output (not standard error), which differs from some other shells"],["EXAMPLES","**Resolve commands in zsh**\n```\n$ whence ls\n/bin/ls\n\n$ whence -v ls\nls is /bin/ls\n\n$ whence -w cd\ncd: builtin\n\n$ whence -w for\nfor: reserved\n```\n\n**Find every match on PATH (zsh)**\n```\n$ whence -a python3\n```"],["CAVEATS","**whence** is not a standalone program and is not available in bash or plain sh. Scripts that need portability should prefer **command -v** (POSIX) or **type**.\n\nPattern matching with **-m** (zsh) only applies to the final path component of a command name; pass the basename, not a full path with `/`.\n\nOption letters and output formats differ between ksh and zsh. Features such as **-c**, **-w**, **-m**, **-s**, and **-S** are zsh extensions."],["HISTORY","**whence** originates in the KornShell (**ksh**) as the shell's command-lookup reporter. **zsh** adopted it and built a richer interface on top, with **which**, **where**, and **type** implemented as variants of **whence**. It is not specified by POSIX."],["SEE ALSO","[type](/man/type)(1), [which](/man/which)(1), [command](/man/command)(1), [hash](/man/hash)(1), [whereis](/man/whereis)(1)"],["RESOURCES","```[Documentation](https://zsh.sourceforge.io/Doc/Release/Shell-Builtin-Commands.html#index-whence)```\n\n```[Source code](https://github.com/zsh-users/zsh)```"]],"e":["type","which","command","hash","whereis"],"x":"whence [command]"},"whiptail":{"n":"whiptail","t":"Terminal dialog boxes for shell scripts","s":[["TAGLINE","Terminal dialog boxes for shell scripts"],["TLDR","Display a simple **message** box\n\n```whiptail --title \"[title]\" --msgbox \"[message]\" [height] [width]```\n\nDisplay a **yes/no** choice\n\n```whiptail --title \"[title]\" --yesno \"[message]\" [height] [width]```\n\n**Customize** yes/no button text\n\n```whiptail --title \"[title]\" --yes-button \"[text]\" --no-button \"[text]\" --yesno \"[message]\" [height] [width]```\n\nDisplay a **text input** box\n\n```result=$(whiptail --title \"[title]\" --inputbox \"[message]\" [height] [width] [default] 3>&1 1>&2 2>&3)```\n\nDisplay a **password** input box\n\n```result=$(whiptail --title \"[title]\" --passwordbox \"[message]\" [height] [width] 3>&1 1>&2 2>&3)```\n\nDisplay a **menu** with choices\n\n```result=$(whiptail --title \"[title]\" --menu \"[message]\" [height] [width] [menu_height] \"val1\" \"text1\" \"val2\" \"text2\" 3>&1 1>&2 2>&3)```\n\nDisplay a **checklist** for multiple selections\n\n```result=$(whiptail --title \"[title]\" --checklist \"[message]\" [height] [width] [list_height] \"opt1\" \"text1\" ON \"opt2\" \"text2\" OFF 3>&1 1>&2 2>&3)```\n\nDisplay a **progress bar** (gauge)\n\n```echo [50] | whiptail --title \"[title]\" --gauge \"[message]\" [height] [width] [0]```"],["SYNOPSIS","**whiptail** [**--title** _title_] [**--backtitle** _backtitle_] _box-type_ [_box-options_] _height_ _width_"],["PARAMETERS","**--title** _title_\n> Dialog title\n\n**--backtitle** _text_\n> Background title\n\n**--msgbox** _text_ _height_ _width_\n> Display message with OK button\n\n**--yesno** _text_ _height_ _width_\n> Display yes/no dialog\n\n**--inputbox** _text_ _height_ _width_ [_init_]\n> Text input dialog\n\n**--passwordbox** _text_ _height_ _width_\n> Password input (hidden text)\n\n**--menu** _text_ _height_ _width_ _menu-height_ [_tag_ _item_]...\n> Selection menu\n\n**--checklist** _text_ _height_ _width_ _list-height_ [_tag_ _item_ _status_]...\n> Multiple choice checklist\n\n**--radiolist** _text_ _height_ _width_ _list-height_ [_tag_ _item_ _status_]...\n> Radio button list\n\n**--gauge** _text_ _height_ _width_ _percent_\n> Progress bar\n\n**--yes-button** _text_\n> Custom yes button label\n\n**--no-button** _text_\n> Custom no button label\n\n**--ok-button** _text_\n> Custom OK button label\n\n**--cancel-button** _text_\n> Custom cancel button label\n\n**--defaultno**\n> Default to No in yes/no dialogs\n\n**--nocancel**\n> Suppress the Cancel button\n\n**--separate-output**\n> Output checklist results one per line\n\n**--scrolltext**\n> Force display of a vertical scrollbar\n\n**--topleft**\n> Place dialog in the top-left corner\n\n**--clear**\n> Clear the screen on exit\n\n**--fullbuttons**\n> Use full-width buttons instead of compact\n\n**--output-fd** _FD_\n> Direct output to given file descriptor instead of stderr\n\n**--textbox** _file_ _height_ _width_\n> Display contents of a file in a scrollable box\n\n**--infobox** _text_ _height_ _width_\n> Display message without waiting for input"],["DESCRIPTION","**whiptail** displays text-based dialog boxes from shell scripts, providing a user-friendly interface for input and selection. It creates ncurses-based dialogs that work in terminal environments, supporting various dialog types including messages, input boxes, menus, and progress bars.\n\nOutput from input dialogs goes to stderr, requiring file descriptor redirection (3>&1 1>&2 2>&3) to capture in shell variables. Exit codes indicate user choices (0 for OK/Yes, 1 for Cancel/No)."],["CAVEATS","Dialog dimensions must be specified in characters and should fit the terminal. Results go to stderr, requiring fd redirection for capture. A lightweight alternative to **dialog** with fewer features but smaller dependencies."],["HISTORY","Created as a lightweight, newt-based reimplementation of the **dialog** utility. Part of the **newt** library developed by Red Hat. Commonly used in Debian installer and system configuration scripts due to its small footprint."],["INSTALL","```apt: sudo apt install whiptail```\n\n```dnf: sudo dnf install newt```\n\n```apk: sudo apk add newt```\n\n```zypper: sudo zypper install newt```\n\n```brew: brew install newt```\n\n```nix: nix profile install nixpkgs#newt```"],["SEE ALSO","[dialog](/man/dialog)(1), [zenity](/man/zenity)(1), [gum](/man/gum)(1)"]],"e":["dialog","zenity","gum"],"x":"whiptail --title \"[title]\" --msgbox \"[message]\" [height] [width]"},"wuphf":{"n":"wuphf","t":"Multi-agent collaboration office for AI coding assistants","s":[["TAGLINE","Multi-agent collaboration office for AI coding assistants"],["TLDR","**Launch** the WUPHF office and open the web UI\n\n```npx wuphf```\n\n**Run** with a specific agent provider\n\n```npx wuphf --provider [codex]```\n\n**Use** the tmux-based terminal UI instead of the web interface\n\n```npx wuphf --tui```\n\n**Pick** a predefined agent team pack\n\n```npx wuphf --pack [coding-team]```\n\n**Choose** a memory backend\n\n```npx wuphf --memory-backend [markdown]```\n\n**Start a one-on-one** session with a specific agent\n\n```npx wuphf --1o1 [ceo]```\n\n**Run** without opening the browser automatically\n\n```npx wuphf --no-open```\n\n**Use a custom** web UI port\n\n```npx wuphf --web-port [7891]```"],["SYNOPSIS","**wuphf** [_options_] [_subcommand_]"],["PARAMETERS","**--memory-backend** _name_\n> Select the shared-memory backend: **nex**, **gbrain**, **markdown** (default), or **none**.\n\n**--pack** _name_\n> Choose a preconfigured agent team: **starter**, **founding-team**, **coding-team**, **lead-gen-agency**, or **revops**.\n\n**--provider** _name_\n> LLM CLI to drive each agent: **claude-code** (default), **codex**, or **openclaw**.\n\n**--tui**\n> Use the tmux-based terminal UI instead of the web UI.\n\n**--no-nex**\n> Disable the Nex backend while keeping local integrations.\n\n**--no-open**\n> Do not open the default browser when the web UI starts.\n\n**--opus-ceo**\n> Upgrade the CEO agent from Sonnet to Opus.\n\n**--collab**\n> Enable collaborative mode (default).\n\n**--unsafe**\n> Bypass agent permission checks; intended only for local experimentation.\n\n**--web-port** _N_\n> Web UI port (default **7891**).\n\n**--1o1** [_agent_]\n> Start a one-on-one chat with the CEO, or with the named agent if given.\n\n**init**\n> Run initial setup and write a configuration file.\n\n**shred**\n> Terminate the active office session and clean up local state."],["DESCRIPTION","**wuphf** is a \"Slack for AI employees\" — a multi-agent collaboration layer that spawns a small office of role-based agents (CEO, PM, engineers, designer, CMO, CRO) sharing a common memory store and a chat-style channel. Instead of running each AI assistant in isolation, wuphf lets them see each other's messages, claim tasks, and ship work together while preserving context across sessions.\n\nUnder the hood wuphf delegates each agent to an existing CLI such as **claude-code**, **codex**, or **openclaw**, and exposes a web UI by default at **http://localhost:7891** with the team roster, a shared channel, and a message composer. A **--tui** mode renders the same office inside a tmux session for terminal-only environments.\n\nMemory is pluggable via **--memory-backend**: the **markdown** backend keeps a human-readable Markdown brain on disk, while **nex** and **gbrain** integrate with hosted services. Agent rosters are described by the **--pack** option, and custom packs can be defined in configuration."],["CONFIGURATION","After **wuphf init**, configuration is stored locally and read on each launch. Required runtime dependencies are **node** / **npx**, **python3**, **curl**, **tmux** (for **--tui** mode), and at least one supported LLM CLI on **PATH** (**claude**, **codex**, or **openclaw**). API keys are taken from the underlying CLI's own configuration — wuphf does not store them itself."],["CAVEATS","wuphf is an early-stage orchestration layer; flag names and pack contents change between releases. The **--unsafe** flag disables per-agent permission checks and should never be used outside isolated, throw-away environments. Because agents call external LLM CLIs, costs and rate limits depend on the provider account behind each CLI."],["HISTORY","**wuphf** is developed by **Nex** and published at **github.com/nex-crm/wuphf**. The name is a nod to the fictional notification service from the TV show *Parks and Recreation*. It is distributed via **npm** and runs without a global install through **npx wuphf**."],["SEE ALSO","[claude](/man/claude)(1), [codex](/man/codex)(1), [tmux](/man/tmux)(1), [npx](/man/npx)(1)"]],"e":["claude","codex","tmux","npx"],"x":"npx wuphf"},"xbps-remove":{"n":"xbps-remove","t":"Remove Void Linux packages","s":[["TAGLINE","Remove Void Linux packages"],["TLDR","**Remove** a package\n\n```xbps-remove [package]```\n\n**Remove** a package and its dependencies\n\n```xbps-remove -R [package]```\n\n**Remove** orphan packages\n\n```xbps-remove -o```\n\n**Remove** obsolete packages from the cache\n\n```xbps-remove -O```"],["SYNOPSIS","**xbps-remove** [_options_] [_package_...]"],["PARAMETERS","**-R, --recursive**\n> Remove package and its dependencies\n\n**-o, --remove-orphans**\n> Remove orphaned packages (unused dependencies)\n\n**-O, --clean-cache**\n> Remove obsolete packages from cache\n\n**-y, --yes**\n> Assume yes to all questions\n\n**-n, --dry-run**\n> Show what would be done\n\n**-f, --force**\n> Force removal\n\n**-v, --verbose**\n> Verbose output"],["DESCRIPTION","**xbps-remove** removes packages from a Void Linux system. It can remove individual packages, recursively remove packages with their dependencies, and clean up orphaned packages.\n\nThe **-o** option is useful for removing packages that were installed as dependencies but are no longer needed by any installed package."],["CAVEATS","Requires root privileges. Be careful with **-R** as it may remove packages you want to keep. Running **-o** regularly helps keep the system clean. Part of the XBPS package management system for Void Linux."],["INSTALL","```apk: sudo apk add xbps```\n\n```nix: nix profile install nixpkgs#xbps```"],["SEE ALSO","[xbps-install](/man/xbps-install)(1), [xbps-query](/man/xbps-query)(1)"]],"e":["xbps-install","xbps-query"],"x":"xbps-remove [package]"},"xbps-tui":{"n":"xbps-tui","t":"TUI wrapper for Void Linux package manager","s":[["TAGLINE","TUI wrapper for Void Linux package manager"],["TLDR","**Launch the package manager TUI**\n\n```xbps-tui```\n\n**Launch in dry-run mode** (preview actions without executing)\n\n```xbps-tui --dry-run```"],["SYNOPSIS","**xbps-tui** [_options_]"],["PARAMETERS","**--dry-run**\n> Preview install/remove actions without executing them."],["DESCRIPTION","**xbps-tui** is an interactive terminal frontend for the xbps package manager used by Void Linux. It supports fuzzy searching packages by name and description, filtering by installed, updatable, or marked packages, and marking packages for installation or removal with root execution.\n\nPress **/** or **Ctrl+f** to enter fuzzy search mode. Use **h**/**k** or arrow keys to filter between all packages, installed, updatable, marked for install, and marked for removal. Set **XBPS_TUI_DATA** environment variable for a custom data directory."],["HISTORY","**xbps-tui** was created by **lukeflo** and is written in **Rust**."],["INSTALL","```nix: nix profile install nixpkgs#xbps```"],["SEE ALSO","[xbps-install](/man/xbps-install)(1), [xbps-query](/man/xbps-query)(1), [xbps-remove](/man/xbps-remove)(1)"]],"e":["xbps-install","xbps-query","xbps-remove"],"x":"xbps-tui"}});
