window.LCL_BUCKET(127,{"a2query":{"n":"a2query","t":"Query Apache2 configuration state","s":[["TAGLINE","Query Apache2 configuration state"],["TLDR","List enabled Apache **modules**\n\n```sudo a2query -m```\n\nCheck if a **specific module** is enabled\n\n```sudo a2query -m [module_name]```\n\nList enabled **virtual hosts**\n\n```sudo a2query -s```\n\nDisplay the currently enabled **Multi Processing Module**\n\n```sudo a2query -M```\n\nDisplay **Apache version**\n\n```sudo a2query -v```\n\nList enabled **configuration files**\n\n```sudo a2query -c```"],["SYNOPSIS","**a2query** [_-m_ [_MODULE_]] [_-s_ [_SITE_]] [_-c_ [_CONF_]] [_-a_] [_-v_] [_-M_] [_-d_] [_-h_]"],["DESCRIPTION","**a2query** is a Debian-specific utility designed to retrieve configuration values from a locally installed Apache2 web server. It provides a robust interface primarily intended for use in maintainer scripts but also useful for system administrators.\n\nArguments for **-c**, **-m**, and **-s** options are compared flexibly, ignoring leading **mod_** prefixes and **.conf** or **.load** suffixes."],["PARAMETERS","**-m [MODULE]**\n> Check whether a module is enabled; returns all enabled modules if no argument provided\n\n**-s [SITE]**\n> Check whether a site is enabled; returns all enabled sites if no argument provided\n\n**-c [CONF]**\n> Check if a configuration is enabled; returns all enabled configs if no argument provided\n\n**-M**\n> Returns the current Apache2 MPM (Multi Processing Module)\n\n**-v**\n> Returns the installed Apache2 version\n\n**-a**\n> Displays the Apache2 Module Magic Version (API version) number\n\n**-q**\n> Suppresses output; useful when only return codes matter\n\n**-h**\n> Shows usage help and exits"],["CAVEATS","This utility is **Debian-specific** and not available on other Linux distributions. Exit code **32** indicates the requested module, site, or configuration was not found."],["HISTORY","Created as part of the Apache2 package for **Debian GNU/Linux**. Written by Arno Toell."],["INSTALL","```apt: sudo apt install apache2```\n\n```zypper: sudo zypper install apache2```\n\n```brew: brew install apache2```"],["SEE ALSO","[a2enmod](/man/a2enmod)(8), [a2dismod](/man/a2dismod)(8), [a2enconf](/man/a2enconf)(8), [a2disconf](/man/a2disconf)(8), [a2ensite](/man/a2ensite)(8), [a2dissite](/man/a2dissite)(8), [apache2ctl](/man/apache2ctl)(8)"],["RESOURCES","```[Homepage](https://httpd.apache.org/)```\n\n```[Documentation](https://manpages.debian.org/a2query)```"]],"e":["a2enmod","a2dismod","a2enconf","a2disconf","a2ensite","a2dissite","apache2ctl"],"x":"sudo a2query -m"},"apktool":{"n":"apktool","t":"Reverse engineer Android APK files","s":[["TAGLINE","Reverse engineer Android APK files"],["TLDR","**Decode** (decompile) an APK\n\n```apktool d [app.apk]```\n\nDecode to **specific directory**\n\n```apktool d [app.apk] -o [output_dir]```\n\n**Build** (recompile) an APK\n\n```apktool b [decoded_dir]```\n\nBuild with **output name**\n\n```apktool b [decoded_dir] -o [output.apk]```\n\nDecode **without resources**\n\n```apktool d [app.apk] -r```\n\nDecode **without sources**\n\n```apktool d [app.apk] -s```"],["SYNOPSIS","**apktool** _d_|_b_ [_options_] _apk_|_dir_"],["DESCRIPTION","**apktool** is a reverse engineering tool for Android APK files. It decodes resources to nearly original form, allowing inspection and modification of manifests, layouts, and other XML resources. It also disassembles Dalvik bytecode to smali.\n\nAfter modifications, apktool can rebuild the APK, which then needs to be signed before installation."],["PARAMETERS","**d**, **decode**\n> Decode APK to directory\n\n**b**, **build**\n> Build APK from directory\n\n**-o** _path_, **--output** _path_\n> Output file/directory\n\n**-r**, **--no-res**\n> Don't decode resources\n\n**-s**, **--no-src**\n> Don't decode sources (smali)\n\n**-f**, **--force**\n> Force overwrite existing files\n\n**--frame-path** _dir_\n> Framework files directory\n\n**-p** _dir_, **--frame-path** _dir_\n> Store/load framework files\n\n**--use-aapt2**\n> Use aapt2 instead of aapt"],["CONFIGURATION","**~/.local/share/apktool/framework/**\n> Directory for stored framework files used during decoding and rebuilding."],["CAVEATS","Rebuilt APKs must be signed before installation. Some protection schemes may prevent successful decoding. Resource decoding may fail for heavily modified framework."],["HISTORY","**apktool** was created by Brut.all (Connor Tumbleson maintains the fork) and first released around **2010**. It became the standard tool for Android APK reverse engineering."],["INSTALL","```apt: sudo apt install apktool```\n\n```brew: brew install apktool```\n\n```nix: nix profile install nixpkgs#apktool```"],["SEE ALSO","[apksigner](/man/apksigner)(1), [jadx](/man/jadx)(1)"]],"e":["apksigner","jadx"],"x":"apktool d [app.apk]"},"apptainer-search":{"n":"apptainer-search","t":"Search a container library for available images.","s":[["TAGLINE","Search a container library for available images."],["TLDR","**Search** for container images matching a query\n\n```apptainer search [query]```\n\nSearch for container images for a **specific architecture**\n\n```apptainer search --arch [amd64|arm64|386|ppc64le|s390x] [query]```\n\nSearch for only **signed** container images\n\n```apptainer search --signed [query]```\n\nSearch in a **specific Container Library**\n\n```apptainer search --library [library_url] [query]```"],["SYNOPSIS","**apptainer search** [_options_] _query_"],["DESCRIPTION","**apptainer search** queries a Container Library for available container images matching the specified search terms. Results include image names, descriptions, and available tags.\n\nBy default, searches are performed against the Sylabs Container Library. Alternative libraries can be specified using the **--library** option."],["PARAMETERS","**--arch** _architecture_\n> Filter results by CPU architecture (default: amd64).\n\n**--signed**\n> Only show containers with cryptographic signatures.\n\n**--library** _url_\n> Specify an alternate Container Library URI to search."],["INSTALL","```dnf: sudo dnf install apptainer```\n\n```pacman: sudo pacman -S apptainer```\n\n```apk: sudo apk add apptainer```\n\n```zypper: sudo zypper install apptainer```\n\n```brew: brew install apptainer```\n\n```nix: nix profile install nixpkgs#apptainer```"],["SEE ALSO","[apptainer](/man/apptainer)(1), [apptainer-pull](/man/apptainer-pull)(1), [apptainer-push](/man/apptainer-push)(1), [apptainer-delete](/man/apptainer-delete)(1)"],["RESOURCES","```[Source code](https://github.com/apptainer/apptainer)```\n\n```[Homepage](https://apptainer.org)```\n\n```[Documentation](https://apptainer.org/docs/)```"]],"e":["apptainer","apptainer-pull","apptainer-push","apptainer-delete"],"x":"apptainer search [query]"},"bpftrace":{"n":"bpftrace","t":"High-level tracing language for Linux eBPF.","s":[["TAGLINE","High-level tracing language for Linux eBPF."],["TLDR","List all **available probes**\n\n```sudo bpftrace -l```\n\nRun a **one-liner program**\n\n```sudo bpftrace -e 'tracepoint:raw_syscalls:sys_enter { @[comm] = count(); }'```\n\nRun a **program from file**\n\n```sudo bpftrace [path/to/file]```\n\nTrace a program by **PID**\n\n```sudo bpftrace -e 'tracepoint:raw_syscalls:sys_enter /pid == 123/ { @[comm] = count(); }'```\n\n**Trace a child** command until it exits\n\n```sudo bpftrace -c '[command]' -e 'tracepoint:raw_syscalls:sys_enter { @[comm] = count(); }'```\n\n**Dry run** to validate probes without executing\n\n```sudo bpftrace --dry-run -e '[one_line_program]'```\n\n**Write output** to a file\n\n```sudo bpftrace -o [output.txt] -e '[one_line_program]'```\n\nDisplay **version**\n\n```bpftrace -V```"],["SYNOPSIS","**bpftrace** [_options_] [_program_|_file_]"],["DESCRIPTION","**bpftrace** is a high-level tracing language for Linux eBPF. It provides a powerful scripting interface for kernel and userspace tracing, similar to DTrace on other Unix systems.\n\nPrograms can attach to tracepoints, kprobes, uprobes, and other probe types to collect and aggregate data about system behavior in real-time."],["PARAMETERS","**-l** [_SEARCH_]\n> List probes matching a search pattern (supports wildcards)\n\n**-e** _program_\n> Execute a one-liner program\n\n**-d** _STAGE_\n> Produce debug output for a given compilation stage (ast, codegen, dis, verifier, etc.)\n\n**--dry-run**\n> Validate and compile the program without attaching probes\n\n**-p** _PID_\n> Attach to process or filter actions by PID\n\n**-c** _COMMAND_\n> Run command as a child process; bpftrace exits when it terminates\n\n**-o** _FILENAME_\n> Write tracing output to file instead of stdout\n\n**-f** _FORMAT_\n> Output format: `text` (default) or `json`\n\n**-B** _MODE_\n> Buffer mode for output: `none`, `line` (default), or `full`\n\n**-I** _DIR_\n> Add directory to the C header search path (repeatable)\n\n**-v**\n> Verbose output\n\n**-q**\n> Quiet mode; suppress non-error messages\n\n**-V**, **--version**\n> Display version information\n\n**--unsafe**\n> Allow unsafe operations like `system()` calls\n\n**--info**\n> Print information about kernel features and bpftrace build"],["CAVEATS","Requires root privileges. Kernel must have eBPF and BTF support enabled. Some probes may not be available on all kernel versions. Performance impact varies by probe type and frequency."],["HISTORY","**bpftrace** was created by Alastair Robertson and first released in **2018**. It was inspired by DTrace and aimed to bring similar high-level tracing capabilities to Linux using eBPF."],["INSTALL","```apt: sudo apt install bpftrace```\n\n```dnf: sudo dnf install bpftrace```\n\n```pacman: sudo pacman -S bpftrace```\n\n```apk: sudo apk add bpftrace```\n\n```zypper: sudo zypper install bpftrace```\n\n```nix: nix profile install nixpkgs#bpftrace```"],["SEE ALSO","[bpftool](/man/bpftool)(8), [perf](/man/perf)(1), [strace](/man/strace)(1)"]],"e":["bpftool","perf","strace"],"x":"sudo bpftrace -l"},"bt":{"n":"bt","t":"Bittensor blockchain CLI tool","s":[["TAGLINE","Bittensor blockchain CLI tool"],["TLDR","**Check wallet balance**\n\n```btcli wallet balance --wallet.name [default]```\n\n**List** available subnets\n\n```btcli subnets list```\n\n**Register** on a subnet\n\n```btcli subnets register --netuid [1] --wallet.name [default]```\n\n**View** stake information\n\n```btcli stake show --wallet.name [default]```\n\n**Transfer** TAO tokens\n\n```btcli wallet transfer --dest [address] --amount [1.0]```"],["SYNOPSIS","**btcli** _command_ [_subcommand_] [_options_]"],["DESCRIPTION","**btcli** is the command-line interface for the **Bittensor** decentralized machine learning network. It provides tools for managing wallets, staking TAO tokens, registering on subnets, and interacting with the Bittensor blockchain.\n\nBittensor is a protocol that creates a decentralized marketplace for machine intelligence, where miners provide computational resources and validators assess quality. The CLI is the primary tool for participants to manage their involvement in the network."],["CAVEATS","Requires a Bittensor wallet to be created before most operations. Blockchain operations involve real cryptocurrency and transaction fees. Network registration may require proof-of-work or recycled TAO. Always verify transaction details before confirming."],["INSTALL","```apt: sudo apt install bootterm```\n\n```brew: brew install bootterm```\n\n```nix: nix profile install nixpkgs#bootterm```"],["SEE ALSO","[python](/man/python)(1)"]],"e":["python"],"x":"btcli wallet balance --wallet.name [default]"},"bun-install":{"n":"bun-install","t":"Install project dependencies","s":[["TAGLINE","Install project dependencies"],["TLDR","**Install all dependencies**\n\n```bun install```\n\n**Add a dependency**\n\n```bun add [package]```\n\n**Add a dev dependency**\n\n```bun add -d [package]```\n\n**Add with exact version**\n\n```bun add --exact [package]```\n\n**Remove a dependency**\n\n```bun remove [package]```\n\n**Install without running lifecycle scripts**\n\n```bun install --ignore-scripts```\n\n**Install for production only**\n\n```bun install --production```"],["SYNOPSIS","**bun install** [_options_]\n\n**bun add** [_options_] _package_ ...\n\n**bun remove** _package_ ..."],["DESCRIPTION","**bun install** is Bun's built-in package manager, a fast alternative to npm, yarn, and pnpm. It installs dependencies from package.json into a node_modules folder compatible with Node.js.\n\nThe package manager uses a global cache to avoid re-downloading packages and employs the fastest system calls available for writing files."],["PARAMETERS","**-d, --dev**\n> Add as development dependency\n\n**--exact**\n> Pin package to exact version\n\n**--production**\n> Install only production dependencies\n\n**--ignore-scripts**\n> Skip running lifecycle scripts\n\n**--frozen-lockfile**\n> Error if lockfile needs update\n\n**--force**\n> Force re-download of all packages\n\n**--global**\n> Install package globally"],["LOCKFILE","Bun uses **bun.lock** (text-based) as the default lockfile since v1.2. Remove other lockfiles (package-lock.json, yarn.lock, pnpm-lock.yaml) before first install to avoid conflicts."],["TRUSTED DEPENDENCIES","Bun does not run dependency lifecycle scripts by default for security. Allow-list trusted packages using **trustedDependencies** in package.json."],["CAVEATS","Compatible with existing package.json files. Supports workspaces, git/http/tarball dependencies, and custom registries. Some npm-specific features may behave differently."],["INSTALL","```pacman: sudo pacman -S bun```\n\n```brew: brew install bun```\n\n```nix: nix profile install nixpkgs#bun```"],["SEE ALSO","[bun](/man/bun)(1), [bun-add](/man/bun-add)(1), [bun-remove](/man/bun-remove)(1), [npm](/man/npm)(1), [yarn](/man/yarn)(1)"],["RESOURCES","```[Source code](https://github.com/oven-sh/bun)```\n\n```[Homepage](https://bun.sh)```\n\n```[Documentation](https://bun.sh/docs)```"]],"e":["bun","bun-add","bun-remove","npm","yarn"],"x":"bun install"},"cargo-remove":{"n":"cargo-remove","t":"Remove dependencies from a Cargo.toml manifest","s":[["TAGLINE","Remove dependencies from a Cargo.toml manifest"],["TLDR","**Remove dependency**\n\n```cargo remove [serde]```\n\n**Remove dev dependency**\n\n```cargo remove --dev [mockall]```\n\n**Remove build dependency**\n\n```cargo remove --build [cc]```\n\n**Remove multiple dependencies**\n\n```cargo remove [serde] [tokio] [regex]```\n\n**Remove target-specific dependency**\n\n```cargo remove --target [x86_64-pc-windows-gnu] [winapi]```\n\n**Dry run**\n\n```cargo remove --dry-run [package]```"],["SYNOPSIS","**cargo remove** [_options_] _dep_..."],["DESCRIPTION","**cargo remove** removes one or more dependencies from the Cargo.toml manifest file. It can target regular dependencies, dev-dependencies, build-dependencies, or platform-specific dependency sections.\n\nThe command modifies Cargo.toml directly but does not update Cargo.lock until the next build or `cargo update`. Multiple dependencies can be removed in a single invocation. Use `--dry-run` to preview changes before applying them."],["PARAMETERS","**--dev**, **-D**\n> Remove from dev-dependencies\n\n**--build**, **-B**\n> Remove from build-dependencies\n\n**--target** _triple_\n> Remove from target-specific dependencies\n\n**-p**, **--package** _spec_\n> Package to modify\n\n**--dry-run**\n> Show changes without modifying manifest\n\n**--manifest-path** _path_\n> Path to Cargo.toml\n\n**-v**, **--verbose**\n> Verbose output\n\n**-q**, **--quiet**\n> Suppress output"],["CAVEATS","Does not remove dependencies from Cargo.lock until next build. Use cargo-machete to detect unused dependencies."],["INSTALL","```apt: sudo apt install cargo```\n\n```dnf: sudo dnf install cargo```\n\n```apk: sudo apk add cargo```\n\n```zypper: sudo zypper install cargo```\n\n```nix: nix profile install nixpkgs#cargo```"],["SEE ALSO","[cargo](/man/cargo)(1), [cargo-add](/man/cargo-add)(1), [cargo-update](/man/cargo-update)(1)"],["RESOURCES","```[Source code](https://github.com/rust-lang/cargo)```\n\n```[Documentation](https://doc.rust-lang.org/cargo/commands/cargo-remove.html)```"]],"e":["cargo","cargo-add","cargo-update"],"x":"cargo remove [serde]"},"cli-wrapper":{"n":"cli-wrapper","t":"Debian helper that launches Mono CLI assemblies as normal commands","s":[["TAGLINE","Debian helper that launches Mono CLI assemblies as normal commands"],["TLDR","Show the wrapper's built-in help (it deliberately ships **no manual page**)\n\n```cli-wrapper --help```\n\nRun a **CLI/.NET assembly** through the system's CLI runtime\n\n```cli [path/to/program.exe]```\n\nShow which runtime **/usr/bin/cli** currently resolves to\n\n```update-alternatives --display cli```\n\nInspect the wrapper itself\n\n```file $(command -v cli-wrapper)```"],["SYNOPSIS","**cli-wrapper** [_options_]"],["DESCRIPTION","**cli-wrapper** is a small helper shipped by the Mono runtime packages on Debian and its derivatives. Debian packages containing a CLI (Common Language Infrastructure, i.e. .NET) assembly do not put the `.exe` file in `/usr/bin` directly. They install a launcher that hands the assembly to whichever CLI runtime the system has selected, so a managed program can be invoked like any other native command.\n\nThe runtime is registered with `update-alternatives` under the generic name **cli**. On a typical system this resolves to **mono**, but any conforming runtime can be substituted without touching the packages that depend on it. That indirection is what allows `apt` to install .NET software independently of the runtime actually present.\n\nThe wrapper is minimal and is not meant to be run by hand: it exists so that packaging tools and the alternatives system have a stable target to point at."],["CAVEATS","Debian explicitly marks this command **UNDOCUMENTED**: `man cli-wrapper` only tells you to run it with a help switch and refers you to the Mono documentation. Its behaviour, and even its presence, varies between Mono versions, and it is absent from current `mono-runtime` packages, so it is mostly encountered on older Debian, Ubuntu, and Raspbian installs. Do not script against it; invoke **mono** or the generic **cli** alternative instead."],["HISTORY","The wrapper dates from Debian's CLI policy work in the mid-2000s, when `cli-common` and the Mono packages introduced a runtime-agnostic way to ship .NET assemblies, at a time when Mono, Portable.NET, and other runtimes still competed. Once Mono became the only practical CLI runtime in Debian, the indirection lost most of its purpose and the wrapper was gradually retired."],["SEE ALSO","[mono](/man/mono)(1), [update-alternatives](/man/update-alternatives)(1), [dotnet](/man/dotnet)(1)"],["RESOURCES","```[Source code](https://github.com/mono/mono)```\n\n```[Homepage](https://www.mono-project.com)```\n\n```[Documentation](https://www.mono-project.com/docs/)```"]],"e":["mono","update-alternatives","dotnet"],"x":"cli-wrapper --help"},"conda-search":{"n":"conda-search","t":"package repository searcher","s":[["TAGLINE","package repository searcher"],["TLDR","**Search for a package**\n\n```conda search [package_name]```\n\n**Search with version pattern**\n\n```conda search \"[package_name]>=[version]\"```\n\n**Search in specific channel**\n\n```conda search -c [conda-forge] [package_name]```\n\n**Show detailed info**\n\n```conda search --info [package_name]```\n\n**Search for a specific platform**\n\n```conda search --platform [linux-64] [package_name]```"],["SYNOPSIS","**conda** **search** [_options_] [_query_]"],["PARAMETERS","**-c**, **--channel** _channel_\n> Search in specific channel.\n\n**--info**\n> Show detailed package information.\n\n**--platform** _subdir_\n> Search for the given platform (e.g., osx-64, linux-64, win-64). Default is the current platform.\n\n**-O**, **--override-channels**\n> Do not search default or .condarc channels; only search specified channels.\n\n**--json**\n> Output in JSON format.\n\n**-v**, **--verbose**\n> Use once for info, twice for debug, three times for trace."],["CONFIGURATION","**~/.condarc**\n> User-level conda configuration for channels, package settings, and solver options.\n\n**/opt/conda/.condarc**\n> System-wide conda configuration."],["DESCRIPTION","**conda search** searches configured channels for packages matching the query. It shows available versions and build variants.\n\nPackage specifications can include version constraints like **>=**, **<=**, **==**."],["INSTALL","```dnf: sudo dnf install conda```\n\n```nix: nix profile install nixpkgs#conda```"],["SEE ALSO","[conda](/man/conda)(1), [conda-install](/man/conda-install)(1), [conda-repoquery](/man/conda-repoquery)(1)"]],"e":["conda","conda-install","conda-repoquery"],"x":"conda search [package_name]"},"convert":{"n":"convert","t":"image manipulation and format conversion","s":[["TAGLINE","image manipulation and format conversion"],["TLDR","**Convert** image format\n\n```convert [input.png] [output.jpg]```\n\n**Resize** image\n\n```convert [input.jpg] -resize [800x600] [output.jpg]```\n\n**Rotate** image\n\n```convert [input.jpg] -rotate [90] [output.jpg]```\n\n**Add** border\n\n```convert [input.jpg] -border [10x10] -bordercolor [black] [output.jpg]```\n\n**Compress** image\n\n```convert [input.jpg] -quality [85] [output.jpg]```"],["SYNOPSIS","**convert** [_options_] _input_ _output_"],["DESCRIPTION","**convert** is the primary command-line interface for ImageMagick, one of the most versatile image processing tools available. It supports over 200 image formats and can perform virtually any image transformation including format conversion, resizing, rotation, cropping, color manipulation, and applying artistic effects.\n\nThe tool operates by reading one or more input images, applying a series of transformations specified by command-line options, and writing the result to an output file. Multiple operations can be chained together in a single command, with operations applied in the order specified.\n\nconvert is widely used in web development for generating thumbnails, in photography workflows for batch processing, and in scientific computing for image analysis. Its scriptable nature makes it ideal for automation, though complex operations can consume significant memory and CPU time. In ImageMagick 7+, the command is being replaced by the unified **magick** command, though convert remains available for compatibility."],["PARAMETERS","**-resize** _geometry_\n> Resize image (e.g., 50%, 800x600, 800x)\n\n**-rotate** _degrees_\n> Rotate image\n\n**-crop** _geometry_\n> Crop image\n\n**-quality** _value_\n> Compression quality (1-100)\n\n**-scale** _geometry_\n> Scale image (faster, lower quality)\n\n**-thumbnail** _geometry_\n> Create thumbnail\n\n**-blur** _radius_\n> Blur image\n\n**-sharpen** _radius_\n> Sharpen image\n\n**-negate**\n> Invert colors\n\n**-monochrome**\n> Convert to black and white\n\n**-flip**\n> Flip vertically\n\n**-flop**\n> Flip horizontally"],["GEOMETRY SPECIFICATIONS","- **800x600** - Maximum dimensions (keep aspect)\n- **800x600!** - Exact dimensions (ignore aspect)\n- **800x600^** - Minimum dimensions (crop)\n- **50%** - Percentage scale\n- **800x** - Width (calculate height)\n- **x600** - Height (calculate width)"],["WORKFLOW","```bash"],["Convert format","convert photo.png photo.jpg"],["Resize maintaining aspect ratio","convert input.jpg -resize 800x600 output.jpg"],["Exact size (may distort)","convert input.jpg -resize 800x600! output.jpg"],["Resize by percentage","convert input.jpg -resize 50% output.jpg"],["Thumbnail","convert input.jpg -thumbnail 200x200 thumb.jpg"],["Rotate","convert input.jpg -rotate 90 output.jpg"],["Crop (width x height + x_offset + y_offset)","convert input.jpg -crop 800x600+100+100 output.jpg"],["Quality compression","convert input.jpg -quality 85 output.jpg"],["Multiple operations","convert input.jpg -resize 800x600 -rotate 90 -quality 90 output.jpg\n```"],["BATCH OPERATIONS","```bash"],["Convert all PNG to JPG","for img in *.png; do\n    convert \"$img\" \"${img%.png}.jpg\"\ndone"],["Resize all images","for img in *.jpg; do\n    convert \"$img\" -resize 800x600 \"resized_$img\"\ndone\n```"],["EFFECTS","```bash"],["Blur","convert input.jpg -blur 0x8 output.jpg"],["Sharpen","convert input.jpg -sharpen 0x1 output.jpg"],["Border","convert input.jpg -border 5x5 -bordercolor black output.jpg"],["Add text","convert input.jpg -pointsize 36 -fill white \\\n    -annotate +50+50 'Hello' output.jpg\n```"],["CAVEATS","Large images consume memory. Complex operations can be slow. Default quality settings may reduce file size significantly. Some operations change aspect ratio. Security vulnerabilities in old versions. Consider using `magick` command (ImageMagick 7+)."],["HISTORY","**convert** is part of ImageMagick, created by John Cristy in **1987**, becoming one of the most versatile image manipulation tools."],["INSTALL","```pacman: sudo pacman -S imagemagick```\n\n```apk: sudo apk add imagemagick```\n\n```brew: brew install imagemagick```\n\n```nix: nix profile install nixpkgs#imagemagick```"],["SEE ALSO","[mogrify](/man/mogrify)(1), [identify](/man/identify)(1), [ffmpeg](/man/ffmpeg)(1)"],["RESOURCES","```[Source code](https://github.com/ImageMagick/ImageMagick)```\n\n```[Homepage](https://imagemagick.org)```\n\n```[Documentation](https://imagemagick.org/script/convert.php)```"]],"e":["mogrify","identify","ffmpeg"],"x":"convert [input.png] [output.jpg]"},"crespo":{"n":"crespo","t":"Turn a codebase into a compact XML blueprint for LLMs","s":[["TAGLINE","Turn a codebase into a compact XML blueprint for LLMs"],["TLDR","**Generate a structural blueprint of a project**\n\n```crespo [./myproject]```\n\n**Write the blueprint to a named file**\n\n```crespo [./myproject] --output [blueprint.xml]```\n\n**Add AI-generated one-line file summaries**\n\n```crespo [./myproject] --mode summary --groq [api_key]```\n\n**Include full source with secrets redacted**\n\n```crespo [./myproject] --mode concat```\n\n**Analyze a remote GitHub repository directly**\n\n```crespo --git [https://github.com/user/repo]```"],["SYNOPSIS","**crespo** _PATH_ [**--mode** _structure|summary|concat_] [**--git** _URL_] [**--groq** _KEY_] [**--output** _FILENAME_]"],["DESCRIPTION","**crespo** converts a source tree into a compact XML \"blueprint\" intended to be fed to large language models. Instead of concatenating raw files, it uses **Tree-sitter** AST parsing to extract only the structural parts of each file, imports, classes, functions, and the connections between modules, which the project reports cuts token usage substantially compared to pasting the whole codebase.\n\nIt works in three modes. The default **structure** mode emits an AST skeleton of imports, classes, and functions. The **summary** mode adds a one-line, AI-generated description per file and requires a Groq API key. The **concat** mode embeds the full source of each file inside the structured XML while redacting detected secrets.\n\ncrespo is installed from PyPI with **pip install crespo** and supports Python, JavaScript, TypeScript, JSX, TSX, Rust, Go, Java, C, and C++. It can analyze a local directory or clone and analyze a GitHub repository in place with **--git**."],["PARAMETERS","**--mode** _structure|summary|concat_\n\n> Output mode. **structure** (default) emits the AST skeleton, **summary** adds per-file AI summaries, **concat** includes redacted full source.\n\n**--git** _URL_\n\n> Clone and analyze a GitHub repository directly instead of a local path.\n\n**--groq** _KEY_\n\n> Groq API key used by **summary** mode. The key is saved for future runs.\n\n**--output** _FILENAME_\n\n> Write the blueprint to a custom filename."],["CAVEAT","**summary** mode is not local-only: it sends file information to the Groq API and requires a valid API key, so avoid it on private or sensitive code unless that is acceptable. Although **concat** mode attempts to redact secrets, automated redaction is best-effort and should not be relied on as a guarantee before sharing output."],["SEE ALSO","[repomix](/man/repomix)(1), [git](/man/git)(1), [pip](/man/pip)(1)"],["RESOURCES","```[Source code](https://github.com/hrudulmmn/crespo)```"]],"e":["repomix","git","pip"],"x":"crespo [./myproject]"},"dagger":{"n":"dagger","t":"Programmable CI/CD engine that runs pipelines as code","s":[["TAGLINE","Programmable CI/CD engine that runs pipelines as code"],["TLDR","**Initialize** a new Dagger module in the current directory\n\n```dagger init --sdk=[go|python|typescript]```\n\n**Call** a function defined in the current module\n\n```dagger call [function_name] --[arg]=[value]```\n\n**Develop** the module (regenerate SDK bindings after editing dagger.json)\n\n```dagger develop```\n\n**Install** another module as a dependency\n\n```dagger install [github.com/owner/repo@version]```\n\n**Open** an interactive shell inside the module context\n\n```dagger```\n\n**Run** an external command with the Dagger engine attached\n\n```dagger run -- [go test ./...]```\n\n**Query** the Dagger API directly with GraphQL\n\n```dagger query < [query.graphql]```"],["SYNOPSIS","**dagger** [_global-options_] _command_ [_command-options_] [_arguments_]"],["PARAMETERS","**call**\n> Invoke a function from the current module; arguments are passed as **--flag=value** pairs and outputs are streamed back.\n\n**init**\n> Scaffold a new module in the current directory. Requires **--sdk** to choose the language.\n\n**develop**\n> Regenerate code bindings after editing **dagger.json** or adding dependencies.\n\n**install**\n> Add a module reference (local path or remote Git URL) as a dependency.\n\n**run**\n> Execute an arbitrary command with a transient Dagger engine session, exposing the engine on **DAGGER_SESSION_PORT**.\n\n**query**\n> Send raw GraphQL queries to the Dagger engine (read from a file or stdin).\n\n**login** / **logout**\n> Authenticate against Dagger Cloud for pipeline traces and caching.\n\n**-m**, **--mod** _REF_\n> Use a remote module instead of the local one (e.g. **github.com/dagger/dagger/ci**).\n\n**--progress** _MODE_\n> Progress UI: **auto**, **plain**, or **tty**.\n\n**--silent**\n> Suppress progress output entirely.\n\n**-v**, **--debug**\n> Verbose / debug logging."],["DESCRIPTION","**dagger** is a CLI for a portable build engine that executes pipelines as code rather than YAML. Each pipeline is expressed as a function in **Go**, **Python**, **TypeScript**, **Java**, **PHP**, or **Elixir** using the matching Dagger SDK. Functions take typed inputs (directories, secrets, services, primitives), describe a graph of containerized operations, and return typed outputs.\n\nThe engine runs locally on Docker, Podman, or any OCI-compatible runtime, and identically inside any CI (GitHub Actions, GitLab CI, Jenkins, CircleCI, Azure Pipelines, Buildkite). Because every step is content-addressed and cached, repeated calls reuse prior results, making local iteration as fast as the CI run.\n\n**dagger call** is the primary entry point: it locates the module (defined by **dagger.json**), invokes the requested function with the provided flags, and streams the result. Modules can compose other modules from local paths or Git URLs, enabling reusable building blocks across projects and organizations."],["MODULE LAYOUT","A typical module directory contains:\n\n```\ndagger.json     # module manifest (sdk, dependencies)\n.dagger/        # generated SDK bindings (do not edit)\nmain.<ext>      # user-authored functions\n```\n\nAfter editing **dagger.json** or any function signature, run **dagger develop** to regenerate the bindings."],["CAVEATS","A working OCI runtime (Docker, Podman, nerdctl) is required; Dagger spins up its own engine container the first time it is invoked. Function arguments are strongly typed and case-sensitive; **--myArg** and **--my-arg** are not interchangeable. Modules pinned to a Git ref can break on **dagger develop** if the upstream SDK changes its surface. The cache lives in a Docker volume named **dagger-engine-***, which can grow large; prune with **docker volume rm**."],["HISTORY","**Dagger** was founded by **Solomon Hykes** (creator of Docker), **Sam Alba**, and **Andrea Luzzardi** and publicly launched in **March 2022**. The initial 0.x releases drove pipelines via **CUE**. In **early 2023** the project pivoted to a polyglot **module** model with native-language SDKs, and **dagger call** replaced the older **dagger do** entry point. Version **0.9** introduced the module system as the default in late 2023."],["INSTALL","```pacman: sudo pacman -S dagger```\n\n```brew: brew install dagger```"],["SEE ALSO","[docker](/man/docker)(1), [podman](/man/podman)(1), [act](/man/act)(1), [buildah](/man/buildah)(1)"]],"e":["docker","podman","act","buildah"],"x":"dagger init --sdk=[go|python|typescript]"},"dmypy":{"n":"dmypy","t":"mypy daemon for fast incremental type checking","s":[["TAGLINE","mypy daemon for fast incremental type checking"],["TLDR","**Start mypy daemon**\n\n```dmypy start```\n\n**Run type check** via daemon\n\n```dmypy run [file.py]```\n\n**Check entire project**\n\n```dmypy run -- --strict [.]```\n\n**Stop the daemon**\n\n```dmypy stop```\n\n**Check daemon status**\n\n```dmypy status```\n\n**Restart the daemon**\n\n```dmypy restart```\n\n**Kill daemon forcefully**\n\n```dmypy kill```"],["SYNOPSIS","**dmypy** _command_ [_options_] [_files_...]"],["PARAMETERS","_COMMAND_\n> Operation: start, stop, run, status, restart, kill.\n\n**start**\n> Start the mypy daemon.\n\n**stop**\n> Stop the daemon gracefully.\n\n**run** [_FILES_]\n> Type check files via daemon.\n\n**status**\n> Show daemon status.\n\n**restart**\n> Restart the daemon.\n\n**kill**\n> Forcefully terminate daemon.\n\n**--status-file** _FILE_\n> Custom status file location.\n\n**--help**\n> Display help information."],["DESCRIPTION","**dmypy** is the daemon interface for mypy, Python's static type checker. It keeps mypy running as a background process, dramatically reducing type checking time by maintaining cached state between runs.\n\nThe daemon pre-computes and caches type information, making incremental checks nearly instantaneous. This is particularly valuable for large codebases where full mypy runs would be slow.\n\ndmypy integrates with editors and CI systems that need fast, repeated type checks. It handles the daemon lifecycle and forwards type checking requests to the running mypy instance."],["CAVEATS","Daemon must be restarted after configuration changes. Memory usage increases with project size. May need restart if mypy version changes. Not suitable for parallel type checking."],["HISTORY","dmypy was introduced as part of **mypy**, originally created by **Jukka Lehtosalo** at Dropbox. The daemon mode was added to address performance concerns with large codebases requiring frequent type checks during development."],["INSTALL","```apk: sudo apk add py3-mypy```"],["SEE ALSO","[mypy](/man/mypy)(1), [python](/man/python)(1), [pyright](/man/pyright)(1)"],["RESOURCES","```[Source code](https://github.com/python/mypy)```\n\n```[Homepage](https://mypy-lang.org/)```\n\n```[Documentation](https://mypy.readthedocs.io/en/stable/mypy_daemon.html)```"]],"e":["mypy","python","pyright"],"x":"dmypy start"},"docker-system":{"n":"docker-system","t":"manage Docker system resources and disk usage","s":[["TAGLINE","manage Docker system resources and disk usage"],["TLDR","**Show disk usage**\n\n```docker system df```\n\n**Show detailed disk usage**\n\n```docker system df -v```\n\n**Remove unused data**\n\n```docker system prune```\n\n**Remove all unused data including volumes**\n\n```docker system prune -a --volumes```\n\n**Show system information**\n\n```docker system info```\n\n**Show system events**\n\n```docker system events```"],["SYNOPSIS","**docker** **system** _command_ [_options_]"],["SUBCOMMANDS","**df**\n> Show Docker disk usage.\n\n**prune**\n> Remove unused data.\n\n**info**\n> Display system-wide information.\n\n**events**\n> Get real-time events from server."],["DESCRIPTION","**docker system** manages Docker system resources. Useful for monitoring disk usage and cleaning up unused containers, images, networks, and volumes."],["INSTALL","```apt: sudo apt install docker-cli```\n\n```dnf: sudo dnf install docker-cli```\n\n```pacman: sudo pacman -S docker```\n\n```apk: sudo apk add docker-cli```\n\n```zypper: sudo zypper install docker```\n\n```brew: brew install docker```\n\n```nix: nix profile install nixpkgs#docker```"]],"e":[],"x":"docker system df"},"drm_info":{"n":"drm_info","t":"display DRM device information","s":[["TAGLINE","display DRM device information"],["TLDR","**Display info about all DRM devices** with formatted output\n\n```drm_info```\n\n**Query a specific DRM device**\n\n```drm_info [/dev/dri/card0]```\n\n**Output device information** in JSON format\n\n```drm_info -j```\n\n**Submit GPU information** to the DRM database\n\n```drm_info -j | curl -d @- https://drmdb.emersion.fr/submit```"],["SYNOPSIS","**drm_info** [**-j**] [**--**] [_path_...]"],["PARAMETERS","**-j**\n> Output info in JSON format. Otherwise the output is pretty-printed.\n\n**path**\n> Zero or more device paths to query, such as /dev/dri/card0. When omitted, the tool automatically scans /dev/dri/card* for all available devices.\n\n**--**\n> End of options delimiter."],["DESCRIPTION","**drm_info** is a small utility to dump information about DRM (Direct Rendering Manager) devices. It queries the Linux kernel's DRM subsystem and displays details about available GPUs, their capabilities, supported formats, connectors, encoders, CRTCs, and planes.\n\nThe tool is useful for debugging display issues, checking GPU feature support, and contributing hardware information to the community DRM database. It provides insight into what the DRM driver exposes for each graphics device."],["CAVEATS","Requires appropriate permissions to access DRM device files under /dev/dri/. May need to run as root or be a member of the video group. The tool depends on libdrm and json-c libraries being installed."],["HISTORY","**drm_info** was created by Simon Ser (emersion) and is hosted on freedesktop.org GitLab. It serves as the data collection tool for drmdb, a community database tracking GPU and DRM driver feature support across different hardware."],["INSTALL","```apt: sudo apt install drm-info```\n\n```dnf: sudo dnf install drm_info```\n\n```pacman: sudo pacman -S drm-info```\n\n```apk: sudo apk add drm_info```\n\n```zypper: sudo zypper install drm_info```\n\n```nix: nix profile install nixpkgs#drm_info```"],["SEE ALSO","[amdgpu_top](/man/amdgpu_top)(1), [nvidia-smi](/man/nvidia-smi)(1), [lspci](/man/lspci)(8), [xrandr](/man/xrandr)(1), [modetest](/man/modetest)(1)"],["RESOURCES","```[Source code](https://gitlab.freedesktop.org/emersion/drm_info)```"]],"e":["amdgpu_top","nvidia-smi","lspci","xrandr","modetest"],"x":"drm_info"},"filebeat":{"n":"filebeat","t":"lightweight log shipper for Elastic Stack","s":[["TAGLINE","lightweight log shipper for Elastic Stack"],["TLDR","**Start filebeat** with default configuration\n\n```filebeat```\n\n**Start with a specific configuration** file\n\n```filebeat -c [path/to/filebeat.yml]```\n\n**Test configuration** file syntax\n\n```filebeat test config```\n\n**Test output connectivity** (Elasticsearch, Logstash, etc.)\n\n```filebeat test output```\n\n**Enable a module**\n\n```filebeat modules enable [nginx]```\n\n**List available modules**\n\n```filebeat modules list```\n\n**Set up dashboards and index templates**\n\n```filebeat setup```\n\n**Run once and exit** (process existing files)\n\n```filebeat --once```"],["SYNOPSIS","**filebeat** [_COMMAND_] [_FLAGS_]"],["COMMANDS","**run**\n> Run Filebeat (default command if none specified).\n\n**test config**\n> Test configuration file syntax.\n\n**test output**\n> Test connectivity to configured outputs.\n\n**modules enable** _MODULE_...\n> Enable one or more modules.\n\n**modules disable** _MODULE_...\n> Disable one or more modules.\n\n**modules list**\n> List available and enabled modules.\n\n**setup**\n> Set up index templates, dashboards, and pipelines.\n\n**export config**\n> Export current configuration to stdout.\n\n**export template**\n> Export index template to stdout.\n\n**export dashboard**\n> Export a Kibana dashboard to stdout."],["PARAMETERS","**-c** _FILE_\n> Specify configuration file (default: filebeat.yml).\n\n**-e**\n> Log to stderr instead of syslog/file.\n\n**--modules** _MODULES_\n> Comma-separated list of modules to run.\n\n**--once**\n> Run harvesters once and exit when done.\n\n**--path.config** _PATH_\n> Path to configuration files.\n\n**--path.data** _PATH_\n> Path to data directory.\n\n**--path.logs** _PATH_\n> Path to log files.\n\n**--strict.perms**\n> Enforce strict permission checking on config files (default: true).\n\n**-v**\n> Enable verbose logging.\n\n**-d** _SELECTOR_\n> Enable debug output for specific components."],["CONFIGURATION","**filebeat.yml**\n> Main configuration file specifying inputs, outputs, modules, and processing options.\n\n**/etc/filebeat/modules.d/*.yml**\n> Module-specific configuration files for pre-built log parsers."],["DESCRIPTION","**Filebeat** is a lightweight log shipper from the Elastic Stack (ELK). It monitors log files, collects log events, and forwards them to Elasticsearch, Logstash, or other outputs for indexing and analysis.\n\nFilebeat uses harvesters to read log files line by line and sends the data to configured outputs. It maintains state information to track read positions, ensuring reliable delivery even after restarts. Modules provide pre-built configurations for common applications like nginx, Apache, MySQL, and system logs.\n\nConfiguration is defined in filebeat.yml, specifying inputs (log paths), outputs (Elasticsearch/Logstash endpoints), and processing options."],["CAVEATS","Configuration files require strict permissions (owner read/write only) by default. When running via systemd, the -e flag may override logging settings. Modules must be enabled before use. Index templates and dashboards require Elasticsearch and Kibana connectivity during setup."],["HISTORY","Filebeat is part of the **Beats** family of lightweight data shippers developed by **Elastic**. It evolved from Logstash Forwarder (Lumberjack) to provide a more resource-efficient solution for log collection. The Beats platform was introduced around **2015**, with Filebeat becoming the primary choice for log shipping in the Elastic Stack."],["INSTALL","```brew: brew install filebeat```\n\n```nix: nix profile install nixpkgs#filebeat```"],["SEE ALSO","[logstash](/man/logstash)(1), [elasticsearch](/man/elasticsearch)(1), [journalctl](/man/journalctl)(1)"]],"e":["logstash","elasticsearch","journalctl"],"x":"filebeat"},"gh-mintty":{"n":"gh-mintty","t":"GitHub CLI help topic for MinTTY terminal compatibility on Windows","s":[["TAGLINE","GitHub CLI help topic for MinTTY terminal compatibility on Windows"],["TLDR","**Show MinTTY compatibility help**\n\n```gh help mintty```\n\n**Work around MinTTY input issues** using winpty (may cause UI bugs)\n\n```winpty gh auth login```"],["SYNOPSIS","**gh help mintty**"],["DESCRIPTION","**gh-mintty** is a help topic in the GitHub CLI describing known compatibility issues between **gh** and MinTTY, the default terminal emulator shipped with Git for Windows. MinTTY lacks proper pseudo-console support, which breaks **gh**'s interactive prompts.\n\nThree workarounds exist:\n\n1. Reinstall Git for Windows with \"Enable experimental support for pseudo consoles\" checked.\n2. Run `C:\\Program Files\\Git\\bin\\bash.exe` from a different terminal emulator (e.g. Windows Terminal), which provides access to Git for Windows tools without MinTTY.\n3. Prefix **gh** commands with `winpty` (e.g. `winpty gh auth login`), though this can cause UI rendering bugs."],["CAVEATS","Windows-specific topic. The `winpty` workaround may cause UI bugs. The experimental pseudo-console option in Git for Windows is the most complete fix."],["INSTALL","```apt: sudo apt install gh```\n\n```dnf: sudo dnf install gh```\n\n```pacman: sudo pacman -S github-cli```\n\n```apk: sudo apk add github-cli```\n\n```zypper: sudo zypper install gh```\n\n```brew: brew install gh```\n\n```nix: nix profile install nixpkgs#gh```"],["SEE ALSO","[gh](/man/gh)(1), [gh-help](/man/gh-help)(1), [gh-config](/man/gh-config)(1)"]],"e":["gh","gh-help","gh-config"],"x":"gh help mintty"},"git-local-commits":{"n":"git-local-commits","t":"List unpushed local commits","s":[["TAGLINE","List unpushed local commits"],["TLDR","**Show local commits not pushed**\n\n```git local-commits```\n\n**Show local commits with a graph**\n\n```git local-commits --graph```"],["SYNOPSIS","**git** **local-commits** [_git-log-options_]"],["PARAMETERS","_GIT-LOG-OPTIONS_\n> Any options accepted by **git log**; passed through unchanged."],["DESCRIPTION","**git local-commits**, a git-extras command, lists commits on the current branch that haven't been pushed to its upstream tracking branch. It runs `git log @{upstream}..@`, so it requires HEAD to be tracking a remote branch. Any extra arguments are forwarded directly to `git log`, so flags like `--graph` or `--stat` work as expected.\n\nThis provides a quick way to see what changes will be pushed before actually pushing them."],["CAVEATS","Requires the current branch to have an upstream set; otherwise Git reports an error about there being no upstream configured."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-log](/man/git-log)(1), [git-extras](/man/git-extras)(1)"],["RESOURCES","```[Source code](https://github.com/tj/git-extras)```\n\n```[Documentation](https://github.com/tj/git-extras/blob/master/Commands.md)```"]],"e":["git-log","git-extras"],"x":"git local-commits"},"git-mr":{"n":"git-mr","t":"Check out GitLab merge requests","s":[["TAGLINE","Check out GitLab merge requests"],["TLDR","**Check out merge request from origin**\n\n```git mr [51]```\n\n**Check out merge request from a specific remote**\n\n```git mr [51] [upstream]```\n\n**Check out merge request by URL**\n\n```git mr [https://gitlab.com/owner/repo/merge_requests/51]```\n\n**Clean up all local mr/ branches**\n\n```git mr clean```"],["SYNOPSIS","**git mr** _number_ [_remote_]\n\n**git mr** _url_\n\n**git mr** **clean**"],["PARAMETERS","_NUMBER_\n> Merge request number.\n\n_REMOTE_\n> Remote name (default: origin).\n\n_URL_\n> GitLab merge request URL (e.g. https://gitlab.tld/owner/repo/merge_requests/453).\n\n**clean**\n> Delete all local mr/ branches.\n\n**--help**\n> Display help information."],["DESCRIPTION","**git mr** fetches a GitLab merge request head by its number or URL and checks it out in a local branch named mr/_number_. It handles ref fetching and branch creation in a single step, simplifying the code review workflow.\n\nFor GitHub repositories, the equivalent command is `git pr`."],["CAVEATS","Part of git-extras package. GitLab-specific functionality. GitHub equivalent is git pr."],["HISTORY","git mr is part of **git-extras**, providing GitLab merge request checkout similar to git pr for GitHub pull requests."],["INSTALL","```zypper: sudo zypper install git-mr```"],["SEE ALSO","[git-pr](/man/git-pr)(1), [git-checkout](/man/git-checkout)(1)"]],"e":["git-pr","git-checkout"],"x":"git mr [51]"},"git-var":{"n":"git-var","t":"Display Git logical variables","s":[["TAGLINE","Display Git logical variables"],["TLDR","**Show Git variables**\n\n```git var -l```\n\n**Show editor**\n\n```git var GIT_EDITOR```\n\n**Show author identity**\n\n```git var GIT_AUTHOR_IDENT```\n\n**Show committer identity**\n\n```git var GIT_COMMITTER_IDENT```\n\n**Show configured pager**\n\n```git var GIT_PAGER```"],["SYNOPSIS","**git** **var** [_variable_]"],["PARAMETERS","**-l**\n> List all variables."],["DESCRIPTION","**git var** shows Git logical variables such as the configured editor, pager, and author/committer identity. These values are resolved from environment variables and Git configuration, following Git's own lookup order (e.g. `$GIT_EDITOR` falls back to `core.editor`, then `$VISUAL`, then `$EDITOR`).\n\nOther recognized variables include `GIT_SEQUENCE_EDITOR` (editor for `git rebase -i`), `GIT_DEFAULT_BRANCH`, `GIT_SHELL_PATH`, `GIT_ATTR_SYSTEM`/`GIT_ATTR_GLOBAL`, and `GIT_CONFIG_SYSTEM`/`GIT_CONFIG_GLOBAL`.\n\nThe command is useful in scripts that need to query Git's resolved settings without parsing config files directly."],["INSTALL","```apt: sudo apt install git```\n\n```dnf: sudo dnf install git```\n\n```pacman: sudo pacman -S git```\n\n```apk: sudo apk add git```\n\n```zypper: sudo zypper install git```\n\n```brew: brew install git```\n\n```nix: nix profile install nixpkgs#git```"],["SEE ALSO","[git-config](/man/git-config)(1), [git-commit](/man/git-commit)(1)"],["RESOURCES","```[Documentation](https://git-scm.com/docs/git-var)```\n\n```[Source code](https://github.com/git/git)```"]],"e":["git-config","git-commit"],"x":"git var -l"},"glxgears":{"n":"glxgears","t":"OpenGL rotating gears demo and sanity check","s":[["TAGLINE","OpenGL rotating gears demo and sanity check"],["TLDR","**Run the gears demo** (shows FPS in terminal)\n\n```glxgears```\n\n**Run in fullscreen mode**\n\n```glxgears -fullscreen```\n\n**Print OpenGL renderer information**\n\n```glxgears -info```\n\n**Run at specific window size**\n\n```glxgears -geometry [800x600]```\n\n**Run with stereo rendering** (if supported)\n\n```glxgears -stereo```\n\n**Run without animation** (static image)\n\n```glxgears -iacknowledgethatthistoolisnotabenchmark```"],["SYNOPSIS","**glxgears** [_-display display_] [_-info_] [_-stereo_] [_-fullscreen_] [_-geometry WxH_]"],["PARAMETERS","**-display** _display_\n> Specify X display to use.\n\n**-info**\n> Print OpenGL renderer and GLX visual information.\n\n**-stereo**\n> Enable stereo rendering (requires hardware support).\n\n**-fullscreen**\n> Run in fullscreen mode.\n\n**-geometry** _WxH_\n> Set window size (width x height).\n\n**-iacknowledgethatthistoolisnotabenchmark**\n> Disable animation (for static screenshots)."],["DESCRIPTION","**glxgears** is an OpenGL demo that renders three rotating gears. It displays frames per second (FPS) in the terminal every 5 seconds, making it commonly used to verify that OpenGL and GPU acceleration are working correctly.\n\nThe application creates a simple GLX window and renders the classic gears animation using OpenGL. When working properly, modern systems typically show several thousand FPS (capped by vsync to monitor refresh rate when composited, or much higher when unthrottled).\n\nThe FPS display makes **glxgears** useful for quick GPU sanity checks: if you see reasonable FPS (hundreds to thousands) and smooth animation, OpenGL acceleration is working. Very low FPS (under 60) may indicate software rendering or driver issues.\n\nDespite common use as a \"benchmark,\" glxgears tests very little of modern GPU capabilities. It uses ancient OpenGL features that aren't representative of real application performance."],["CAVEATS","**NOT a valid benchmark** for GPU performance comparison. FPS varies wildly based on vsync, window size, compositing, and driver settings. Only tests minimal OpenGL functionality. Cannot detect many GPU driver issues. Stereo mode requires specific hardware."],["HISTORY","**glxgears** was written by Brian Paul around **1999** as part of Mesa, the open-source OpenGL implementation. It was created as a simple demo to test GLX (OpenGL Extension to X11) functionality. Despite disclaimers, it became widely misused as a benchmark due to its readily visible FPS counter. The \"iacknowledgethatthistoolisnotabenchmark\" flag was added as a humorous commentary on this misuse."],["INSTALL","```apt: sudo apt install mesa-utils```\n\n```pacman: sudo pacman -S mesa-utils```\n\n```apk: sudo apk add mesa-utils```"],["SEE ALSO","[glxinfo](/man/glxinfo)(1), [vulkaninfo](/man/vulkaninfo)(1), [xdpyinfo](/man/xdpyinfo)(1), [vkcube](/man/vkcube)(1)"]],"e":["glxinfo","vulkaninfo","xdpyinfo","vkcube"],"x":"glxgears"},"gnatprep":{"n":"gnatprep","t":"Ada source file preprocessor","s":[["TAGLINE","Ada source file preprocessor"],["TLDR","**Preprocess an Ada source file**\n\n```gnatprep [input.adb] [output.adb]```\n\n**Preprocess with symbol definitions file**\n\n```gnatprep [input.adb] [output.adb] [defs.txt]```\n\n**Define a symbol on command line**\n\n```gnatprep -DDEBUG=True [input.adb] [output.adb]```\n\n**Replace preprocessor lines with blanks**\n\n```gnatprep -b [input.adb] [output.adb]```\n\n**Retain preprocessor lines as comments**\n\n```gnatprep -c [input.adb] [output.adb]```\n\n**List defined symbols**\n\n```gnatprep -s [input.adb] [output.adb] [defs.txt]```"],["SYNOPSIS","**gnatprep** _inputfile_ _outputfile_ [_definitions_file_] [_switches_]"],["PARAMETERS","**-b**\n> Replace preprocessor and deleted lines with blank lines.\n\n**-c**\n> Retain preprocessor and deleted lines as comments.\n\n**-D**_symbol_=_value_\n> Define symbol on command line.\n\n**-s**\n> List symbol names and values to stdout.\n\n**-T**\n> Use only symbols defined on the command line or in the definitions file (do not use environment variables).\n\n**-u**\n> Treat undefined symbols as FALSE.\n\n**-r**\n> Generate source reference pragmas.\n\n**-v**\n> Verbose mode, display file names as they are processed."],["DESCRIPTION","**gnatprep** is a preprocessor for Ada programs. It processes conditional compilation directives (#if, #elsif, #else, #end if) and symbol substitutions ($symbol) in Ada source files. While designed for GNAT, it is not dependent on GNAT-specific features and can be used independently."],["CAVEATS","Neither -b nor -c preserves original line numbers. If both are absent, preprocessor and deleted lines are removed entirely unless -r is specified."],["INSTALL","```apt: sudo apt install gnat-12```\n\n```dnf: sudo dnf install gcc-gnat```\n\n```apk: sudo apk add gcc-gnat```"],["SEE ALSO","[gnatmake](/man/gnatmake)(1), [gcc](/man/gcc)(1), [gprbuild](/man/gprbuild)(1), [m4](/man/m4)(1)"]],"e":["gnatmake","gcc","gprbuild","m4"],"x":"gnatprep [input.adb] [output.adb]"},"gts2stl":{"n":"gts2stl","t":"converts GTS files to STL format","s":[["TAGLINE","converts GTS files to STL format"],["TLDR","**Convert a GTS file to STL**\n\n```gts2stl < [input.gts] > [output.stl]```\n\n**Convert with verbose output**\n\n```gts2stl --verbose < [input.gts] > [output.stl]```\n\n**Convert with reversed face normals**\n\n```gts2stl --revert < [input.gts] > [output.stl]```"],["SYNOPSIS","**gts2stl** [_options_] < _input.gts_ > _output.stl_"],["PARAMETERS","**-v**, **--verbose**\n> Print statistics about the surface.\n\n**-r**, **--revert**\n> Revert face normals (flip the orientation of triangles).\n\n**-h**, **--help**\n> Display the help message."],["DESCRIPTION","**gts2stl** converts GTS (GNU Triangulated Surface) files to STL (STereoLithography) format. GTS is a library for 3D surface mesh operations, and this tool exports meshes to the widely-used STL format.\n\nSTL files are commonly used for 3D printing and CAD applications. The conversion preserves the triangular mesh structure. The output is in ASCII STL format."],["GTS FORMAT","GTS files contain:\n- Vertex count, edge count, face count\n- Vertex coordinates\n- Edge definitions (vertex pairs)\n- Face definitions (edge triplets)"],["CAVEATS","Reads from stdin, writes to stdout. Large meshes may take time. GTS format is less common than STL. Part of GTS library package."],["HISTORY","gts2stl is part of the **GTS Library** (GNU Triangulated Surface), developed for computational geometry operations. The library provides tools for surface mesh manipulation, boolean operations, and format conversion."],["INSTALL","```apt: sudo apt install libgts-bin```"],["SEE ALSO","[stl2gts](/man/stl2gts)(1), [meshlab](/man/meshlab)(1), [openscad](/man/openscad)(1)"]],"e":["stl2gts","meshlab","openscad"],"x":"gts2stl < [input.gts] > [output.stl]"},"hg-remove":{"n":"hg-remove","t":"schedules files for removal from the repository","s":[["TAGLINE","schedules files for removal from the repository"],["TLDR","**Remove file**\n\n```hg remove [file]```\n\n**Remove with force**\n\n```hg remove -f [file]```\n\n**Remove after deletion**\n\n```hg remove -A```\n\n**Remove directory**\n\n```hg remove [directory]```\n\n**Dry run**\n\n```hg remove -n [file]```"],["SYNOPSIS","**hg remove** [_options_] _files_"],["PARAMETERS","_FILES_\n> Files to remove.\n\n**-f**, **--force**\n> Remove even if modified.\n\n**-A**, **--after**\n> Record removal of missing files.\n\n**-I**, **--include** _PATTERN_\n> Include pattern.\n\n**-X**, **--exclude** _PATTERN_\n> Exclude pattern.\n\n**-n**, **--dry-run**\n> Show what would be done.\n\n**--help**\n> Display help information."],["DESCRIPTION","**hg remove** schedules files for removal from the repository. The files are deleted from the working directory and marked for removal on next commit.\n\nThe -A flag records removal of files already deleted from the filesystem. Force removes modified files."],["CAVEATS","Removal requires commit. History preserved. Use forget to just untrack."],["HISTORY","Remove is a core **Mercurial** command for managing tracked files since version 1.0."],["INSTALL","```apt: sudo apt install mercurial```\n\n```dnf: sudo dnf install mercurial```\n\n```pacman: sudo pacman -S mercurial```\n\n```apk: sudo apk add mercurial```\n\n```zypper: sudo zypper install mercurial```\n\n```brew: brew install mercurial```\n\n```nix: nix profile install nixpkgs#mercurial```"],["SEE ALSO","[hg](/man/hg)(1), [hg-add](/man/hg-add)(1)"]],"e":["hg","hg-add"],"x":"hg remove [file]"},"hledger-accounts":{"n":"hledger-accounts","t":"lists account names from the journal","s":[["TAGLINE","lists account names from the journal"],["TLDR","**List all accounts**\n\n```hledger accounts```\n\n**List accounts from specific file**\n\n```hledger accounts -f [journal.ledger]```\n\n**List accounts matching pattern**\n\n```hledger accounts [expenses]```\n\n**List with depth limit**\n\n```hledger accounts --depth [2]```\n\n**Show account tree**\n\n```hledger accounts --tree```\n\n**List used accounts only**\n\n```hledger accounts --used```\n\n**List declared accounts only**\n\n```hledger accounts --declared```\n\n**Show accounts with their types**\n\n```hledger accounts --types```\n\n**Drop first N account name components**\n\n```hledger accounts --drop [1]```"],["SYNOPSIS","**hledger** **accounts** [_options_] [_query_]"],["PARAMETERS","**-f**, **--file** _file_\n> Use specified journal file.\n\n**--depth** _n_\n> Limit account depth shown.\n\n**--tree**\n> Show accounts as tree structure.\n\n**--used**\n> Show only accounts with transactions.\n\n**--declared**\n> Show only accounts declared with account directives.\n\n**--flat**\n> Show accounts as a flat list with full names (default).\n\n**--drop** _n_\n> In flat mode, omit the first n account name components.\n\n**--types**\n> Show each account's type, if known."],["DESCRIPTION","**hledger accounts** lists account names from the journal. Part of the hledger plain text accounting tool. It shows accounts that are declared (with account directives), posted to, or both (the default). Accounts can be filtered by query pattern, limited by depth, and displayed as a flat list or tree."],["INSTALL","```apt: sudo apt install hledger```\n\n```dnf: sudo dnf install hledger```\n\n```pacman: sudo pacman -S hledger```\n\n```apk: sudo apk add hledger```\n\n```zypper: sudo zypper install hledger```\n\n```brew: brew install hledger```\n\n```nix: nix profile install nixpkgs#hledger```"],["SEE ALSO","[hledger](/man/hledger)(1), [hledger-balance](/man/hledger-balance)(1), [hledger-print](/man/hledger-print)(1), [ledger](/man/ledger)(1)"]],"e":["hledger","hledger-balance","hledger-print","ledger"],"x":"hledger accounts"},"jack_connect":{"n":"jack_connect","t":"creates audio connections between JACK ports","s":[["TAGLINE","creates audio connections between JACK ports"],["TLDR","**Connect output to input**\n\n```jack_connect [system:capture_1] [app:input_1]```\n\n**Connect application ports**\n\n```jack_connect [app1:output] [app2:input]```\n\n**List all ports**\n\n```jack_lsp```\n\n**List with connections**\n\n```jack_lsp -c```\n\n**Disconnect ports**\n\n```jack_disconnect [source] [destination]```"],["SYNOPSIS","**jack_connect** [_options_] _source_port_ _destination_port_"],["PARAMETERS","_source_port_\n> Output port (client:port format).\n\n_destination_port_\n> Input port (client:port format).\n\n**-s**, **--server** _servername_\n> Connect to the named JACK server.\n\n**-h**, **--help**\n> Display help information."],["DESCRIPTION","**jack_connect** creates audio connections between JACK ports. JACK (JACK Audio Connection Kit) provides low-latency audio routing between applications.\n\nThe command links output ports to input ports, enabling audio to flow between applications. Use **jack_lsp** to list available ports and **jack_disconnect** to remove connections."],["CAVEATS","Requires running JACK server. Port names include client name. Connections don't persist across restarts. Consider QjackCtl for GUI management."],["HISTORY","JACK was created by **Paul Davis** and others, starting in **2002**. It provides professional-grade audio routing on Linux and macOS, used extensively in audio production."],["INSTALL","```apt: sudo apt install jack-example-tools```\n\n```pacman: sudo pacman -S jack-example-tools```\n\n```zypper: sudo zypper install jack-example-tools```\n\n```nix: nix profile install nixpkgs#jack-example-tools```"],["SEE ALSO","[jackd](/man/jackd)(1), [pulseaudio](/man/pulseaudio)(1), [pipewire](/man/pipewire)(1)"]],"e":["jackd","pulseaudio","pipewire"],"x":"jack_connect [system:capture_1] [app:input_1]"},"kubectl-expose":{"n":"kubectl-expose","t":"creates a service for a resource","s":[["TAGLINE","creates a service for a resource"],["TLDR","**Expose deployment**\n\n```kubectl expose deployment [name] --port=[80]```\n\n**Expose with type**\n\n```kubectl expose deployment [name] --port=[80] --type=[LoadBalancer]```\n\n**Expose pod**\n\n```kubectl expose pod [pod-name] --port=[8080] --target-port=[80]```\n\n**Expose with name**\n\n```kubectl expose deployment [name] --port=[80] --name=[service-name]```\n\n**Expose NodePort**\n\n```kubectl expose deployment [name] --port=[80] --type=[NodePort]```"],["SYNOPSIS","**kubectl expose** [_options_] _resource_ _name_"],["PARAMETERS","_RESOURCE_\n> Resource type to expose.\n\n_NAME_\n> Resource name.\n\n**--port** _PORT_\n> Service port.\n\n**--target-port** _PORT_\n> Container port.\n\n**--type** _TYPE_\n> Service type (ClusterIP, NodePort, LoadBalancer).\n\n**--name** _NAME_\n> Service name.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kubectl expose** creates a Kubernetes Service that provides stable network access to a set of pods managed by a deployment, replica set, pod, or other resource. It automatically generates the service configuration by inspecting the target resource's labels and port definitions, creating a selector-based service without requiring a manual manifest.\n\nThe command supports four service types: ClusterIP (default) for internal cluster access, NodePort for exposing the service on each node's IP at a static port, LoadBalancer for provisioning an external load balancer through the cloud provider, and ExternalName for mapping to a DNS name. The `--port` flag sets the service's listening port while `--target-port` specifies the container port to forward traffic to.\n\nThis approach is convenient for quick service creation during development or prototyping. For production environments, defining services declaratively in manifest files alongside the workload definitions provides better reproducibility and version control."],["CAVEATS","Subcommand of kubectl. Service type affects accessibility. Consider using manifests."],["HISTORY","kubectl expose provides quick service creation for **Kubernetes** workload networking."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-create](/man/kubectl-create)(1), [kubectl-run](/man/kubectl-run)(1)"]],"e":["kubectl","kubectl-create","kubectl-run"],"x":"kubectl expose deployment [name] --port=[80]"},"kubectl-set":{"n":"kubectl-set","t":"updates resource fields","s":[["TAGLINE","updates resource fields"],["TLDR","**Set image**\n\n```kubectl set image deployment/[name] [container]=[image:tag]```\n\n**Set environment**\n\n```kubectl set env deployment/[name] [KEY=value]```\n\n**Set resources**\n\n```kubectl set resources deployment/[name] -c [container] --limits=[cpu=200m,memory=512Mi]```\n\n**Set service account**\n\n```kubectl set serviceaccount deployment/[name] [sa-name]```\n\n**Set selector**\n\n```kubectl set selector service/[name] [app=myapp]```"],["SYNOPSIS","**kubectl set** _command_ [_options_]"],["PARAMETERS","**image** _RESOURCE_ _CONTAINER=IMAGE_\n> Update container image.\n\n**env** _RESOURCE_ _VAR=VALUE_\n> Set environment variables.\n\n**resources** _RESOURCE_\n> Set resource limits.\n\n**serviceaccount** _RESOURCE_ _SA_\n> Set service account.\n\n**selector** _RESOURCE_ _LABELS_\n> Set selector.\n\n**--help**\n> Display help information."],["DESCRIPTION","**kubectl set** updates resource fields. It modifies specific aspects of running workloads.\n\nThe command is faster than full apply for targeted changes. It triggers rollouts for deployments."],["CAVEATS","Subcommand of kubectl. Triggers rollout. Consider apply for declarative management."],["HISTORY","kubectl set provides targeted resource updates for **Kubernetes** operational tasks."],["INSTALL","```apt: sudo apt install kubectl```\n\n```pacman: sudo pacman -S kubectl```\n\n```apk: sudo apk add kubectl```\n\n```brew: brew install kubectl```\n\n```nix: nix profile install nixpkgs#kubectl```"],["SEE ALSO","[kubectl](/man/kubectl)(1), [kubectl-apply](/man/kubectl-apply)(1), [kubectl-rollout](/man/kubectl-rollout)(1)"]],"e":["kubectl","kubectl-apply","kubectl-rollout"],"x":"kubectl set image deployment/[name] [container]=[image:tag]"},"limine-snapper-sync":{"n":"limine-snapper-sync","t":"synchronizes Limine bootloader menu entries with Btrfs snapshots","s":[["TAGLINE","synchronizes Limine bootloader menu entries with Btrfs snapshots"],["TLDR","Sync Snapper snapshots into **Limine boot entries**\n\n```limine-snapper-sync```\n\n**List** snapshot boot entries managed for Limine\n\n```limine-snapper-list```\n\nShow **details** of bootable snapshots\n\n```limine-snapper-info```\n\n**Restore** the system from a bootable snapshot\n\n```limine-snapper-restore```\n\nRun the **background watcher** for new Snapper snapshots\n\n```limine-snapper-watcher```"],["SYNOPSIS","**limine-snapper-sync**\n\n**limine-snapper-list**\n\n**limine-snapper-info**\n\n**limine-snapper-restore**\n\n**limine-snapper-watcher**"],["DESCRIPTION","**limine-snapper-sync** synchronizes Limine bootloader menu entries with Btrfs snapshots managed by Snapper. This allows users to boot directly into previous system states from the Limine boot menu.\n\nThe toolset includes several commands: **limine-snapper-sync** performs manual synchronization, **limine-snapper-list** shows current bootable snapshots, **limine-snapper-info** displays detailed snapshot information, **limine-snapper-restore** recovers the system from a snapshot, and **limine-snapper-watcher** monitors for snapshot changes in real-time.\n\nWhen snapshots are created or deleted via Snapper, running sync updates the Limine boot menu to reflect available restore points."],["CAVEATS","Requires a Btrfs filesystem with Snapper configured for snapshot management. The Limine bootloader must be properly installed and configured. Restoring from a snapshot will revert system files to the snapshot state, potentially losing recent changes."],["INSTALL","```aur: yay -S limine-snapper-sync```"],["SEE ALSO","[limine](/man/limine)(1), [limine-entry-tool](/man/limine-entry-tool)(1), [snapper](/man/snapper)(8), [btrfs](/man/btrfs)(8)"]],"e":["limine","limine-entry-tool","snapper","btrfs"],"x":"limine-snapper-sync"},"logsave":{"n":"logsave","t":"executes a command and saves both stdout and stderr to a log file","s":[["TAGLINE","executes a command and saves both stdout and stderr to a log file"],["TLDR","**Save command output** to log\n\n```logsave [path/to/logfile] [command]```\n\nSave **stdin** to log\n\n```logsave [logfile] -```\n\n**Append** to log file\n\n```logsave -a [logfile] [command]```\n\nRun with **verbose** output\n\n```logsave -v [logfile] [command]```"],["SYNOPSIS","**logsave** [_options_] _logfile_ _command_ [_arguments_]"],["DESCRIPTION","**logsave** executes a command and saves both stdout and stderr to a log file. It includes timestamps and exit status, making it useful for recording the output of scheduled tasks or system scripts."],["PARAMETERS","**-a**\n> Append to log file instead of replacing\n\n**-v**\n> Verbose mode\n\n**-**\n> Read from stdin instead of running command"],["CAVEATS","Part of e2fsprogs package. The log file is created with timestamps for start, end, and command exit status."],["INSTALL","```apt: sudo apt install logsave```\n\n```apk: sudo apk add e2fsprogs-extra```"],["SEE ALSO","[script](/man/script)(1), [tee](/man/tee)(1), [logger](/man/logger)(1)"]],"e":["script","tee","logger"],"x":"logsave [path/to/logfile] [command]"},"luanti":{"n":"luanti","t":"voxel game engine and sandbox game","s":[["TAGLINE","voxel game engine and sandbox game"],["TLDR","**Start the game**\n\n```luanti```\n\n**Start with specific game**\n\n```luanti --game [minetest_game]```\n\n**Start server only**\n\n```luanti --server```\n\n**Connect to server**\n\n```luanti --address [server.example.com] --port [30000]```\n\n**Start with specific world**\n\n```luanti --world [/path/to/world]```\n\n**Set player name**\n\n```luanti --name [playername]```\n\n**Migrate map database**\n\n```luanti --migrate [sqlite3] --world [/path/to/world]```\n\n**Run in terminal** (no graphics)\n\n```luanti --terminal --server```"],["SYNOPSIS","**luanti** [_--server_] [_--address addr_] [_--world path_] [_--game name_] [_options_]"],["PARAMETERS","**--server**\n> Run as dedicated server.\n\n**--address** _ADDR_\n> Address to connect to or bind.\n\n**--port** _PORT_\n> Network port (default 30000).\n\n**--world** _PATH_\n> Path to world directory.\n\n**--worldname** _NAME_\n> World name in worlds directory.\n\n**--game**, **--gameid** _ID_\n> Game to use.\n\n**--name** _NAME_\n> Player name.\n\n**--password** _PASS_\n> Password for server.\n\n**--config** _FILE_\n> Configuration file.\n\n**--info**\n> Print more information.\n\n**--verbose**\n> Print even more information.\n\n**--terminal**\n> Run in terminal mode (server).\n\n**--migrate** _BACKEND_\n> Migrate world to different backend.\n\n**--version**\n> Print version.\n\n**--help**\n> Show help."],["DESCRIPTION","**Luanti** (formerly Minetest) is a voxel game engine and sandbox game. Players explore, build, and modify procedurally generated worlds made of cubic blocks.\n\nThe engine supports modding through Lua scripts. Games are modpacks that define gameplay, blocks, crafting, and creatures. The default Minetest Game provides survival and creative modes.\n\nMultiplayer servers host persistent worlds where players collaborate or compete. Servers can run different games with custom rules and content.\n\nWorld data stores in SQLite by default, with options for LevelDB or PostgreSQL for larger servers. Map generation uses various algorithms for terrain, caves, and structures.\n\nContent is available through the built-in content browser, including games, mods, and texture packs from the community."],["CAVEATS","Performance depends on hardware and world complexity. Large servers need tuning. Some mods may conflict. The engine was renamed from Minetest to Luanti in 2024."],["HISTORY","**Minetest** was created by **Perttu Ahola** (celeron55) in **2010**, inspired by Minecraft and InfiniMiner. It was designed as an open-source, moddable voxel engine. In **2024**, the project was renamed to **Luanti** to establish its own identity separate from Minecraft."],["INSTALL","```pacman: sudo pacman -S luanti```\n\n```apk: sudo apk add luanti```\n\n```zypper: sudo zypper install luanti```\n\n```nix: nix profile install nixpkgs#luanti```"],["SEE ALSO","[luantiserver](/man/luantiserver)(1), [minetestserver](/man/minetestserver)(1)"]],"e":["luantiserver","minetestserver"],"x":"luanti"},"lxc-ls":{"n":"lxc-ls","t":"lists Linux containers on the system","s":[["TAGLINE","lists Linux containers on the system"],["TLDR","List **all** containers\n\n```sudo lxc-ls```\n\nList **active** containers\n\n```sudo lxc-ls --active```\n\nList **frozen** containers\n\n```sudo lxc-ls --frozen```\n\nList **stopped** containers\n\n```sudo lxc-ls --stopped```\n\nList with **fancy output**\n\n```sudo lxc-ls --fancy```\n\nList with **custom fancy columns**\n\n```sudo lxc-ls --fancy -F [NAME,STATE,IPV4,IPV6]```\n\nList containers matching a **regex filter**\n\n```sudo lxc-ls --filter ['^web.*']```"],["SYNOPSIS","**lxc-ls** [_options_]"],["DESCRIPTION","**lxc-ls** lists Linux containers on the system. It can filter by state and display detailed information in various formats."],["PARAMETERS","**--active**\n> Show running and frozen containers\n\n**--frozen**\n> Show only frozen containers\n\n**--running**\n> Show only running containers\n\n**--stopped**\n> Show only stopped containers\n\n**-f, --fancy**\n> Column-formatted detailed output\n\n**-F, --fancy-format COLS**\n> Custom columns for fancy output\n\n**-1**\n> One container per line\n\n**--filter** _regex_\n> Filter container names by regular expression\n\n**-?, --help**\n> Display help information"],["FANCY OUTPUT COLUMNS","NAME, STATE, AUTOSTART, GROUPS, IPV4, IPV6, UNPRIVILEGED"],["CAVEATS","Requires root or appropriate LXC permissions for system containers. Unprivileged containers can be listed without root if configured for the current user."],["INSTALL","```apt: sudo apt install lxc```\n\n```dnf: sudo dnf install lxc```\n\n```pacman: sudo pacman -S lxc```\n\n```apk: sudo apk add lxc```\n\n```zypper: sudo zypper install lxc```\n\n```brew: brew install lxc```\n\n```nix: nix profile install nixpkgs#lxc```"],["SEE ALSO","[lxc-info](/man/lxc-info)(1), [lxc-start](/man/lxc-start)(1), [lxc-stop](/man/lxc-stop)(1), [lxc-create](/man/lxc-create)(1), [lxc-destroy](/man/lxc-destroy)(1)"]],"e":["lxc-info","lxc-start","lxc-stop","lxc-create","lxc-destroy"],"x":"sudo lxc-ls"},"lzfgrep":{"n":"lzfgrep","t":"Search compressed files for fixed strings","s":[["TAGLINE","Search compressed files for fixed strings"],["TLDR","**Search for a fixed string** in a compressed file\n\n```lzfgrep [string] [file.lz]```\n\n**Case-insensitive search**\n\n```lzfgrep -i [string] [file.lz]```\n\n**Show line numbers** with matches\n\n```lzfgrep -n [string] [file.lz]```\n\n**Count matching lines**\n\n```lzfgrep -c [string] [file.lz]```\n\n**Search across multiple** compressed files\n\n```lzfgrep [string] [file1.lz] [file2.lz]```\n\n**List files containing** matches\n\n```lzfgrep -l [string] [*.lz]```"],["SYNOPSIS","**lzfgrep** [_grep_options_] [**-e**] _pattern_ [_file_...]"],["PARAMETERS","All options are passed directly to **grep**(1). Common options include:\n\n**-i**\n> Case-insensitive matching.\n\n**-n**\n> Show line numbers.\n\n**-c**\n> Print count of matching lines only.\n\n**-l**\n> Print names of files with matches.\n\n**-v**\n> Invert match (show non-matching lines).\n\n**-w**\n> Match whole words only.\n\n**-e** _pattern_\n> Specify the pattern explicitly."],["DESCRIPTION","**lzfgrep** searches for fixed strings in files that may be compressed with xz, lzma, gzip, or bzip2. It is equivalent to **lzgrep -F** and is faster than regex-based search for literal string patterns. Files are automatically decompressed on the fly before searching.\n\nlzfgrep is provided as part of xz-utils for backward compatibility with LZMA Utils. If no file is specified, standard input is decompressed if necessary and fed to grep."],["CAVEATS","When reading from standard input, gzip and bzip2 compressed files are not supported. All options are passed directly to grep."],["INSTALL","```dnf: sudo dnf install xz```\n\n```pacman: sudo pacman -S xz```\n\n```apk: sudo apk add xz```\n\n```zypper: sudo zypper install xz```\n\n```brew: brew install xz```\n\n```nix: nix profile install nixpkgs#xz```"],["SEE ALSO","[lzgrep](/man/lzgrep)(1), [fgrep](/man/fgrep)(1), [lzegrep](/man/lzegrep)(1), [xzgrep](/man/xzgrep)(1), [zgrep](/man/zgrep)(1), [grep](/man/grep)(1)"]],"e":["lzgrep","fgrep","lzegrep","xzgrep","zgrep","grep"],"x":"lzfgrep [string] [file.lz]"},"makechrootpkg":{"n":"makechrootpkg","t":"builds Arch Linux packages in a clean chroot environment","s":[["TAGLINE","builds Arch Linux packages in a clean chroot environment"],["TLDR","**Build the PKGBUILD in the current directory using a clean chroot**\n\n```makechrootpkg -c -r [/var/lib/archbuild/extra-x86_64]```\n\n**Build without cleaning the chroot first**\n\n```makechrootpkg -r [/var/lib/archbuild/extra-x86_64]```\n\n**Update the chroot before building**\n\n```makechrootpkg -u -r [/var/lib/archbuild/extra-x86_64]```\n\n**Install a local package into the chroot before building**\n\n```makechrootpkg -r [chroot] -I [package.pkg.tar.zst]```\n\n**Run namcap and checkpkg on the built package**\n\n```makechrootpkg -c -n -C -r [chroot]```\n\n**Bind-mount a directory read-write into the chroot**\n\n```makechrootpkg -d [/host/path] -r [chroot]```\n\n**Pass extra arguments to makepkg**\n\n```makechrootpkg -c -r [chroot] -- --skippgpcheck```"],["SYNOPSIS","**makechrootpkg** [_options_] **-r** _chroot_ [**--**] [_makepkg-args_]"],["PARAMETERS","**-r** _dir_\n> Chroot directory to build in (required).\n\n**-c**\n> Clean the chroot copy before building.\n\n**-u**\n> Update the working copy (chroot) before building.\n\n**-I** _pkg_\n> Install the given package file into the working copy before building.\n\n**-d** _dir[:dest]_\n> Bind-mount a host directory into the chroot as read-write.\n\n**-D** _dir[:dest]_\n> Bind-mount a host directory into the chroot as read-only.\n\n**-t** _dir[:opts]_\n> Mount a tmpfs at the specified directory inside the chroot.\n\n**-l** _name_\n> Name of the working copy directory (defaults to `$USER`).\n\n**-n**\n> Run **namcap** quality checks on the built package.\n\n**-C**\n> Run **checkpkg** validation against the previous package version.\n\n**-T**\n> Build inside a temporary copy (discarded after the build).\n\n**-U** _user_\n> Run **makepkg** as the specified user.\n\n**-x** _when_\n> Launch an interactive shell inside the chroot: `never`, `always`, or `failure`.\n\n**-h**\n> Display usage information."],["DESCRIPTION","**makechrootpkg** builds a PKGBUILD inside a clean, isolated chroot environment. This ensures reproducible builds by insulating the build from the host system's installed packages and configuration, making it the standard method for building official Arch Linux packages.\n\nThe chroot is managed as a root template plus per-user working copies that are cloned from it. Options such as **-c** and **-u** refresh the working copy, while **-I** and **-d** let you customize the build environment."],["SETUP","```bash"],["Create the chroot template (once)","mkarchroot /var/lib/archbuild/extra-x86_64/root base-devel"],["Build the package in the current directory","cd package-dir\nmakechrootpkg -c -r /var/lib/archbuild/extra-x86_64\n```"],["CAVEATS","Requires the **devtools** package. The chroot must be initialized with **mkarchroot** before first use. Root privileges are needed; slower than running **makepkg** directly but produces cleaner, more reproducible builds. Anything after **--** is forwarded to **makepkg**."],["HISTORY","**makechrootpkg** is part of Arch Linux **devtools**, maintained by Arch Linux developers and Trusted Users for building official packages in reproducible chroot environments."],["SEE ALSO","[makepkg](/man/makepkg)(8), [pacman](/man/pacman)(8), [devtools](/man/devtools)(7), [pkgctl](/man/pkgctl)(1), [systemd-nspawn](/man/systemd-nspawn)(1)"]],"e":["makepkg","pacman","devtools","pkgctl","systemd-nspawn"],"x":"makechrootpkg -c -r [/var/lib/archbuild/extra-x86_64]"},"mcopy":{"n":"mcopy","t":"copies files to/from MS-DOS filesystems","s":[["TAGLINE","copies files to/from MS-DOS filesystems"],["TLDR","**Copy file to MS-DOS disk**\n\n```mcopy [file.txt] [a:]```\n\n**Copy from MS-DOS disk**\n\n```mcopy [a:file.txt] [.]```\n\n**Copy multiple files**\n\n```mcopy [*.txt] [a:]```\n\n**Preserve modification time**\n\n```mcopy -m [file] [a:]```\n\n**Overwrite without prompt**\n\n```mcopy -o [file] [a:]```\n\n**Copy recursively**\n\n```mcopy -s [dir/] [a:]```"],["SYNOPSIS","**mcopy** [_options_] _source_ _target_"],["PARAMETERS","_SOURCE_\n> Source file or directory.\n\n_TARGET_\n> Target location.\n\n**-m**\n> Preserve modification time.\n\n**-o**\n> Overwrite without asking.\n\n**-s**\n> Recursive copy.\n\n**-n**\n> No confirmation.\n\n**--help**\n> Display help information."],["DESCRIPTION","**mcopy** copies files to/from MS-DOS filesystems. It's part of the mtools package.\n\nThe tool accesses FAT filesystems without mounting. Useful for floppy disks and USB drives."],["CAVEATS","Part of mtools. Requires mtools config. FAT filesystem only."],["HISTORY","mcopy is part of **mtools**, a collection of utilities for accessing MS-DOS disks from Unix."],["INSTALL","```apt: sudo apt install mtools```\n\n```dnf: sudo dnf install mtools```\n\n```pacman: sudo pacman -S mtools```\n\n```apk: sudo apk add mtools```\n\n```zypper: sudo zypper install mtools```\n\n```brew: brew install mtools```\n\n```nix: nix profile install nixpkgs#mtools```"],["SEE ALSO","[mdel](/man/mdel)(1)"]],"e":["mdel"],"x":"mcopy [file.txt] [a:]"},"mpiexec":{"n":"mpiexec","t":"launches MPI parallel programs","s":[["TAGLINE","launches MPI parallel programs"],["TLDR","**Run** a parallel MPI program with N processes\n\n```mpiexec -n [4] [program]```\n\n**Run across hosts** listed in a hostfile\n\n```mpiexec -n [8] -f [hostfile] [program]```\n\n**Run on a specific host**\n\n```mpiexec -host [host1,host2] -n [4] [program]```\n\n**Run different programs** in the same MPI job (MPMD)\n\n```mpiexec -n [2] [prog_a] : -n [4] [prog_b]```\n\n**Pass an environment variable** to the launched ranks\n\n```mpiexec -genv [OMP_NUM_THREADS] [2] -n [4] [program]```\n\n**Show what would be launched** without running\n\n```mpiexec -info```"],["SYNOPSIS","**mpiexec** [_global-options_] [_local-options_ _executable_ [_args_]] [**:** _local-options_ _executable_ [_args_]]..."],["PARAMETERS","**-n** _num_, **-np** _num_\n> Number of processes (ranks) to launch.\n\n**-f** _hostfile_, **-machinefile** _hostfile_\n> File listing nodes (and optional slot counts) to use.\n\n**-host** _hostlist_\n> Comma-separated list of hosts.\n\n**-hosts** _hostlist_\n> Same as **-host** (Open MPI).\n\n**-ppn** _num_, **-perhost** _num_\n> Processes per node.\n\n**-wdir** _dir_\n> Working directory for launched processes.\n\n**-env** _var_ _value_\n> Set an environment variable for this command segment.\n\n**-genv** _var_ _value_\n> Set an environment variable globally for all segments.\n\n**-envlist** _list_\n> Forward the named variables from the launching environment.\n\n**-bind-to** _target_\n> Bind processes (core, socket, numa, none).\n\n**-map-by** _policy_\n> Process placement policy (slot, node, socket, numa, ...).\n\n**-x** _VAR_\n> Open MPI: export environment variable to remote processes.\n\n**-info**\n> Print build/configuration information for the MPI runtime."],["DESCRIPTION","**mpiexec** is the standard MPI process launcher defined by the MPI-2 specification. It starts a parallel MPI job by spawning the requested number of processes (ranks) and arranging the communication infrastructure between them. The exact implementation depends on which MPI is installed: **MPICH** / Hydra, **Open MPI**, Intel MPI, and others all provide an **mpiexec** binary with overlapping but not identical option sets.\n\n**mpiexec** also supports MPMD (Multiple Program, Multiple Data) jobs, in which several executables run together as one MPI world by separating their arguments with a colon (**:**).\n\nOn most implementations **mpirun** is a synonym (sometimes the canonical name) for **mpiexec**; the two are interchangeable for the common cases."],["CAVEATS","Flags differ subtly between MPI implementations (especially Hydra vs. Open MPI); a script that works against one launcher may need adjustment for another. The launcher must be able to reach every host (typically via SSH) and the MPI runtime libraries must match across the nodes. Mixing **mpiexec** binaries from different MPI installations in a single shell session leads to confusing failures."],["HISTORY","**mpiexec** was introduced in the **MPI-2** standard (1997) as a portable, recommended way to launch MPI programs, replacing implementation-specific launchers. Modern MPI distributions still ship both **mpiexec** and **mpirun**."],["INSTALL","```dnf: sudo dnf install openmpi```\n\n```pacman: sudo pacman -S openmpi```\n\n```apk: sudo apk add openmpi```\n\n```brew: brew install openmpi```\n\n```nix: nix profile install nixpkgs#openmpi```"],["SEE ALSO","[mpirun](/man/mpirun)(1), [mpicc](/man/mpicc)(1)"]],"e":["mpirun","mpicc"],"x":"mpiexec -n [4] [program]"},"net":{"n":"net","t":"samba network utility","s":[["TAGLINE","samba network utility"],["TLDR","**List shares on server**\n\n```net share```\n\n**List sessions**\n\n```net session```\n\n**View user info**\n\n```net user [username]```\n\n**Join domain**\n\n```net ads join -U [admin]```\n\n**Map network drive**\n\n```net use [Z:] [\\\\\\\\server\\\\share]```\n\n**Show workgroup members**\n\n```net rpc group members \"[group]\" -S [server]```"],["SYNOPSIS","**net** [_command_] [_options_]"],["PARAMETERS","_COMMAND_\n> Net subcommand.\n\n**share**\n> Manage shares.\n\n**session**\n> View sessions.\n\n**user**\n> User management.\n\n**ads**\n> Active Directory ops.\n\n**rpc**\n> RPC commands.\n\n**--help**\n> Display help information."],["DESCRIPTION","**net** is a Samba network utility. It provides Windows networking functions on Linux.\n\nThe tool manages shares, users, and domains. Part of Samba suite."],["CAVEATS","Part of Samba. Windows networking. Requires proper configuration."],["HISTORY","net is part of **Samba**, providing Windows-compatible network operations on Unix systems."],["INSTALL","```apt: sudo apt install samba-common-bin```\n\n```dnf: sudo dnf install samba-common-tools```\n\n```apk: sudo apk add samba-common-tools```"],["SEE ALSO","[smbclient](/man/smbclient)(1), [samba](/man/samba)(1), [wbinfo](/man/wbinfo)(1)"]],"e":["smbclient","samba","wbinfo"],"x":"net share"},"netdiscover":{"n":"netdiscover","t":"discovers network hosts using ARP","s":[["TAGLINE","discovers network hosts using ARP"],["TLDR","**Scan local network**\n\n```netdiscover```\n\n**Scan specific range**\n\n```netdiscover -r [192.168.1.0/24]```\n\n**Passive mode**\n\n```netdiscover -p```\n\n**Use specific interface**\n\n```netdiscover -i [eth0]```\n\n**Fast scan**\n\n```netdiscover -f```\n\n**Scan from file**\n\n```netdiscover -l [ranges.txt]```"],["SYNOPSIS","**netdiscover** [_options_]"],["PARAMETERS","**-r** _RANGE_\n> IP range to scan.\n\n**-p**\n> Passive mode (listen only).\n\n**-i** _IFACE_\n> Network interface.\n\n**-f**\n> Fast mode.\n\n**-l** _FILE_\n> Read ranges from file.\n\n**--help**\n> Display help information."],["DESCRIPTION","**netdiscover** discovers network hosts using ARP. It finds active hosts on local network.\n\nThe tool sends ARP requests. Shows MAC addresses and vendors."],["CAVEATS","Requires root. Local network only. May trigger IDS alerts."],["HISTORY","netdiscover was created for **network reconnaissance** and discovering hosts on local networks."],["INSTALL","```apt: sudo apt install netdiscover```\n\n```zypper: sudo zypper install netdiscover```\n\n```nix: nix profile install nixpkgs#netdiscover```"],["SEE ALSO","[arp-scan](/man/arp-scan)(1), [nmap](/man/nmap)(1), [arping](/man/arping)(1)"]],"e":["arp-scan","nmap","arping"],"x":"netdiscover"},"nub":{"n":"nub","t":"All-in-one toolkit that augments Node.js","s":[["TAGLINE","All-in-one toolkit that augments Node.js"],["TLDR","**Run** a TypeScript or JavaScript file with automatic transpilation\n\n```nub [file.ts]```\n\n**Run** a file and restart it on changes\n\n```nub watch [file.ts]```\n\n**Execute** an npm script from package.json\n\n```nub run [script]```\n\n**Execute** a package binary (faster npx alternative)\n\n```nubx [command]```\n\n**Install** all project dependencies\n\n```nub install```\n\n**Add** a package to the project\n\n```nub add [package]```\n\n**Install** and switch to a specific Node.js version\n\n```nub node install [version]```\n\n**Update** Nub itself to the latest version\n\n```nub upgrade```"],["SYNOPSIS","**nub** [_options_] _file_\n\n**nub** _subcommand_ [_arguments_]\n\n**nubx** [_options_] _command_"],["DESCRIPTION","**Nub** is a fast all-in-one toolkit that augments Node.js instead of replacing it. Written in **Rust** with the embedded oxc parser for transpilation, it bundles a file runner, a script runner, a package manager, a package executor, and a Node.js version manager behind a single command.\n\nUnlike alternative runtimes, Nub does not ship its own JavaScript engine. It runs files on the existing Node.js installation while adding native TypeScript execution, automatic **.env** loading, and a watch mode. The file runner aims to be a drop-in replacement for the **node** binary, accepting the same flags and environment variables through passthrough.\n\nThe companion **nubx** command runs package binaries from the registry or from local **node_modules/.bin**, serving as a faster substitute for **npx**. The package manager subcommands (install, ci, add, remove, update) provide an alternative to **npm**, **pnpm**, and **yarn**, while **nub node** and **nub pm** manage Node.js versions and Corepack-style package manager shims."],["PARAMETERS","**nub** _file_\n> Run a TypeScript or JavaScript file with automatic transpilation and .env loading.\n\n**nub run** _script_\n> Execute a script defined in the package.json file.\n\n**nub watch** _file_\n> Run a file and automatically restart it when source files change.\n\n**nubx** _command_\n> Execute a package binary from the registry or from local node_modules/.bin.\n\n**nub install**\n> Install all project dependencies.\n\n**nub ci**\n> Perform a clean install of dependencies.\n\n**nub add** _packages_\n> Add one or more packages to the project.\n\n**nub remove** _package_\n> Remove a package from the project.\n\n**nub update**\n> Update project dependencies.\n\n**nub node install** _version_\n> Download and provision a specific Node.js version.\n\n**nub node ls**\n> List the cached Node.js versions.\n\n**nub node which**\n> Print the path to the resolved Node.js binary.\n\n**nub pm shim**\n> Register global package manager shims (Corepack-style).\n\n**nub upgrade**\n> Update Nub itself to the latest release."],["CAVEATS","Nub augments rather than replaces Node.js, so a working Node.js installation is still required for execution. As a young project the command surface and flags may change between releases; check the documentation for the version you have installed."],["HISTORY","Nub was published in 2025 as a Rust-based companion to Node.js, positioned alongside all-in-one runtimes such as **bun** and **deno** but focused on speeding up existing Node.js workflows rather than introducing a separate runtime."],["INSTALL","```aur: yay -S nub```"],["SEE ALSO","[node](/man/node)(1), [npm](/man/npm)(1), [npx](/man/npx)(1), [pnpm](/man/pnpm)(1), [yarn](/man/yarn)(1), [bun](/man/bun)(1), [deno](/man/deno)(1)"],["RESOURCES","```[Source code](https://github.com/nubjs/nub)```\n\n```[Homepage](https://nubjs.com)```\n\n```[Documentation](https://nubjs.com/docs)```"]],"e":["node","npm","npx","pnpm","yarn","bun","deno"],"x":"nub [file.ts]"},"pacman-key":{"n":"pacman-key","t":"wrapper around GnuPG for managing the keyring used by pacman to verify package","s":[["TAGLINE","wrapper around GnuPG for managing the keyring used by pacman to verify package"],["TLDR","**Initialize** the pacman keyring\n\n```sudo pacman-key --init```\n\nAdd the **default Arch Linux keys**\n\n```sudo pacman-key --populate```\n\n**List keys** from the public keyring\n\n```pacman-key -l```\n\n**Add** the specified keys\n\n```sudo pacman-key -a [path/to/keyfile.gpg]```\n\n**Receive a key** from a key server\n\n```sudo pacman-key -r \"[uid|name|email]\"```\n\nPrint the **fingerprint** of a specific key\n\n```pacman-key -f \"[uid|name|email]\"```\n\n**Sign** an imported key locally\n\n```sudo pacman-key --lsign-key \"[uid|name|email]\"```\n\n**Remove** a specific key\n\n```sudo pacman-key -d \"[uid|name|email]\"```"],["SYNOPSIS","**pacman-key** [_options_] [_operation_]"],["PARAMETERS","**--init**\n> Initialize the pacman keyring\n\n**--populate**\n> Reload the default keys from the archlinux-keyring package\n\n**-l, --list-keys**\n> List keys from the public keyring\n\n**-a, --add _file_**\n> Add keys from a file\n\n**-r, --recv-keys _keyid_**\n> Fetch keys from a keyserver\n\n**-f, --finger _keyid_**\n> Show key fingerprint\n\n**--lsign-key _keyid_**\n> Locally sign a key (trust it)\n\n**-d, --delete _keyid_**\n> Remove a key from the keyring\n\n**--refresh-keys**\n> Update keys from the keyserver"],["DESCRIPTION","**pacman-key** is a wrapper around GnuPG for managing the keyring used by pacman to verify package signatures. It handles key initialization, importing, signing, and trust management.\n\nOn a fresh Arch Linux installation, run **--init** to create the keyring, then **--populate** to add the official Arch Linux packager keys. For third-party repositories (like AUR helpers or custom repos), manually add and sign their keys.\n\nKey signing (**--lsign-key**) marks a key as trusted, allowing pacman to install packages signed by that key without warnings."],["CAVEATS","Requires root privileges for most operations. Key initialization needs sufficient entropy; if it hangs, generate activity (disk I/O, network, typing). The keyring is stored in **/etc/pacman.d/gnupg/**. Corrupted keyrings can be reset by removing this directory and reinitializing."],["HISTORY","Introduced with pacman 4.0 in 2011 to support package signing, enhancing security by cryptographically verifying package integrity and authenticity."],["INSTALL","```apt: sudo apt install pacman-package-manager```\n\n```dnf: sudo dnf install pacman```\n\n```pacman: sudo pacman -S pacman```\n\n```apk: sudo apk add pacman```\n\n```nix: nix profile install nixpkgs#pacman```"],["SEE ALSO","[pacman](/man/pacman)(8), [gpg](/man/gpg)(1), [makepkg](/man/makepkg)(8)"]],"e":["pacman","gpg","makepkg"],"x":"sudo pacman-key --init"},"pangolin":{"n":"pangolin","t":"self-hosted tunneled reverse proxy with built-in authentication","s":[["TAGLINE","self-hosted tunneled reverse proxy with built-in authentication"],["TLDR","**Connect to a Pangolin server with ID and secret**\n\n```pangolin --id [client_id] --secret [client_secret] --endpoint [https://pangolin.example.com]```\n\n**Connect with debug logging**\n\n```pangolin --id [client_id] --secret [client_secret] --endpoint [https://pangolin.example.com] --log-level DEBUG```\n\n**Display help**\n\n```pangolin --help```"],["SYNOPSIS","**pangolin** [_options_]"],["PARAMETERS","**--id** _ID_\n> Client identifier for authentication.\n\n**--secret** _SECRET_\n> Client secret for authentication.\n\n**--endpoint** _URL_\n> Pangolin server endpoint URL.\n\n**--log-level** _LEVEL_\n> Set logging level (e.g., DEBUG).\n\n**--help**\n> Display help information."],["DESCRIPTION","**Pangolin** is a self-hosted tunneled reverse proxy server with built-in identity and access management. It uses WireGuard tunnels via its companion client **Newt** to securely expose private resources without complex network configurations. Pangolin serves as an alternative to Cloudflare Tunnels and Ngrok."],["CAVEATS","Requires a running Pangolin server component. Client ID and secret must be provisioned on the server first. The companion tunnel client is called Newt."],["SEE ALSO","[ssh](/man/ssh)(1), [ngrok](/man/ngrok)(1)"]],"e":["ssh","ngrok"],"x":"pangolin --id [client_id] --secret [client_secret] --endpoint [https://pangolin.example.com]"},"papernews":{"n":"papernews","t":"Build a typeset daily news PDF from RSS and Hacker News feeds","s":[["TAGLINE","Build a typeset daily news PDF from RSS and Hacker News feeds"],["TLDR","**Run the full pipeline** locally\n\n```python -m papernews build```\n\n**Fetch** new articles only\n\n```python -m papernews gather```\n\n**Summarize** fetched articles with Claude\n\n```python -m papernews summarize```\n\n**Rewrite** article bodies for the PDF layout\n\n```python -m papernews rewrite```\n\n**Render** the current edition to PDF\n\n```python -m papernews render```\n\n**Start** the web server and scheduler\n\n```python -m papernews web```"],["SYNOPSIS","**papernews** _subcommand_"],["PARAMETERS","**gather**\n> Pull new items from configured sources and extract article text\n\n**summarize**\n> Generate short ledes for fetched articles\n\n**rewrite**\n> Produce clean, translated article bodies for rendering\n\n**render**\n> Build a LaTeX PDF from stored articles\n\n**build**\n> Run gather, summarize, rewrite, and render in sequence\n\n**web**\n> Start the HTTP service with background ingest and PDF delivery"],["DESCRIPTION","**papernews** is a self-hosted tool that turns RSS feeds and Hacker News stories into one consistently typeset PDF. It fetches sources listed in **sources.toml**, extracts article bodies, uses Claude to summarize and rewrite them, and renders the result with **xelatex**.\n\nThe output is meant for calm offline reading on e-ink devices such as a reMarkable, but any PDF viewer works. A typical edition contains a cover page, table of contents, world-news snippets, and full article text set in two-column Latin Modern typography.\n\nIn server mode, **papernews** exposes **/** for a landing page and **/digest.pdf** for the current cached edition. A background scheduler reruns ingest on a fixed interval or cron schedule. State is stored locally in SQLite, so content survives restarts.\n\nConfigured sources can be Hacker News rankings or any Atom/RSS feed. Non-English articles can be translated to English during the rewrite step."],["CONFIGURATION","**sources.toml**\n> Defines feed order, source kind (**hn** or **rss**), limits, and HN filtering thresholds\n\n**papernews/template.tex.j2**\n> Controls page size, fonts, cover layout, and article formatting\n\n**.env**\n> Holds **ANTHROPIC_API_KEY** and ingest schedule variables such as **INGEST_INTERVAL_SECONDS** or **INGEST_SCHEDULE**"],["CAVEATS","Summarize and rewrite steps call the Anthropic API, so article text leaves the machine for processing. Gather and render stay local.\n\nRendering requires **xelatex** and related TeX packages. The first PDF build can take one to two minutes before caching makes later requests fast.\n\nThis is a hobby project; configuration and output quality may change between versions."],["SEE ALSO","[curl](/man/curl)(1), [docker](/man/docker)(1)"]],"e":["curl","docker"],"x":"python -m papernews build"},"pgtree":{"n":"pgtree","t":"Unix process hierarchy tree for specific processes","s":[["TAGLINE","Unix process hierarchy tree for specific processes"],["TLDR","**Show the process tree for a command name**\n\n```pgtree [process_name]```\n\n**Show the process tree and send a signal**\n\n```pgtree -s [SIGNAL] [process_name]```"],["SYNOPSIS","**pgtree** [_options_] _pattern_"],["PARAMETERS","**-s** _SIGNAL_\n> Send a signal to matched processes and their children."],["DESCRIPTION","**pgtree** is a Unix process hierarchy tree display that combines pgrep and pstree functionality. It finds processes matching a pattern and displays their full process tree, and can send signals to matched processes and all their children. It works out of the box on any Unix system using the default OS Python installation."],["HISTORY","**pgtree** was created by **joknarf** and is written in **Python**."],["SEE ALSO","[pstree](/man/pstree)(1), [pgrep](/man/pgrep)(1), [ps](/man/ps)(1), [kill](/man/kill)(1)"]],"e":["pstree","pgrep","ps","kill"],"x":"pgtree [process_name]"},"physlock":{"n":"physlock","t":"locks all virtual consoles and prevents unauthorized access to the system","s":[["TAGLINE","locks all virtual consoles and prevents unauthorized access to the system"],["TLDR","**Lock** all consoles\n\n```physlock```\n\nLock and **mute** kernel messages\n\n```physlock -m```\n\nLock and **disable SysRq**\n\n```physlock -s```\n\nLock with **custom message**\n\n```physlock -p \"[Locked!]\"```\n\nLock and **detach** (for suspend scripts)\n\n```physlock -d```"],["SYNOPSIS","**physlock** [**-d**] [**-m**] [**-s**] [**-p** _message_] [**-l**]"],["PARAMETERS","**-d**\n> Fork and detach (daemonize)\n\n**-m**\n> Mute kernel messages while locked\n\n**-s**\n> Disable SysRq key while locked\n\n**-p _message_**\n> Display message before password prompt\n\n**-l**\n> Only lock the current tty"],["DESCRIPTION","**physlock** locks all virtual consoles and prevents unauthorized access to the system. Unlike screen lockers that only protect graphical sessions, physlock secures all TTYs, preventing access through Ctrl+Alt+F1-F6 switching.\n\nAuthentication is required from the current user or root to unlock. This makes it suitable for securing systems with physical access concerns, particularly servers or kiosks."],["CAVEATS","Does not lock graphical displays; use alongside a screen locker (e.g., i3lock, slock) for X11/Wayland sessions. Requires PAM for authentication. Detach mode (**-d**) is useful in suspend/hibernate scripts (e.g., systemd sleep hooks). SysRq disable is only effective if the kernel has CONFIG_MAGIC_SYSRQ enabled. Must be run as root or with appropriate permissions."],["HISTORY","**physlock** was created by **Bert Muennich** as a lightweight alternative to vlock. It focuses on securing physical console access with minimal dependencies, making it suitable for embedded and minimal systems."],["INSTALL","```apt: sudo apt install physlock```\n\n```apk: sudo apk add physlock```\n\n```zypper: sudo zypper install physlock```\n\n```nix: nix profile install nixpkgs#physlock```"],["SEE ALSO","[vlock](/man/vlock)(1), [xlock](/man/xlock)(1), [loginctl](/man/loginctl)(1)"]],"e":["vlock","xlock","loginctl"],"x":"physlock"},"pio-pkg":{"n":"pio-pkg","t":"unified package manager for PlatformIO","s":[["TAGLINE","unified package manager for PlatformIO"],["TLDR","**Install package**\n\n```pio pkg install -l [library_name]```\n\n**Install platform**\n\n```pio pkg install -p [espressif32]```\n\n**Install tool**\n\n```pio pkg install -t [openocd]```\n\n**List installed packages**\n\n```pio pkg list```\n\n**Update packages**\n\n```pio pkg update```"],["SYNOPSIS","**pio pkg** _command_ [_options_]"],["PARAMETERS","**install**\n> Install packages.\n\n**uninstall**\n> Remove packages.\n\n**update**\n> Update packages.\n\n**list**\n> List installed packages.\n\n**show**\n> Show package details.\n\n**-l**, **--library** _name_\n> Library package.\n\n**-p**, **--platform** _name_\n> Platform package.\n\n**-t**, **--tool** _name_\n> Tool package."],["DESCRIPTION","**pio pkg** is the unified package manager for PlatformIO. Manages libraries, platforms, and tools through a single interface. Replaces legacy pio lib and pio platform commands."],["INSTALL","```aur: yay -S pio```"],["SEE ALSO","[pio-lib](/man/pio-lib)(1), [pio-platform](/man/pio-platform)(1)"]],"e":["pio-lib","pio-platform"],"x":"pio pkg install -l [library_name]"},"pnmcut":{"n":"pnmcut","t":"Extract rectangular region from PNM images","s":[["TAGLINE","Extract rectangular region from PNM images"],["TLDR","**Cut rectangular region**\n\n```pnmcut [x] [y] [width] [height] [input.pnm] > [output.pnm]```\n\n**Cut from top-left corner**\n\n```pnmcut -left [0] -top [0] -width [100] -height [100] [input.pnm] > [output.pnm]```\n\n**Cut using bottom-right**\n\n```pnmcut -left [10] -top [10] -right [200] -bottom [150] [input.pnm] > [output.pnm]```\n\n**Pad if outside bounds**\n\n```pnmcut -pad [x] [y] [width] [height] [input.pnm] > [output.pnm]```"],["SYNOPSIS","**pnmcut** [_options_] _x_ _y_ _width_ _height_ [_pnmfile_]"],["PARAMETERS","**-left** _n_\n> Left column.\n\n**-right** _n_\n> Right column.\n\n**-top** _n_\n> Top row.\n\n**-bottom** _n_\n> Bottom row.\n\n**-width** _n_\n> Width in pixels.\n\n**-height** _n_\n> Height in pixels.\n\n**-pad**\n> Pad with black if out of bounds."],["DESCRIPTION","**pnmcut** extracts a rectangular region from a PNM image. Coordinates are zero-indexed from top-left corner.\n\nPart of Netpbm toolkit for image manipulation."],["EXAMPLES","```bash"],["Cut 100x100 from position 50,50","pnmcut 50 50 100 100 image.ppm > crop.ppm"],["Using named parameters","pnmcut -left 100 -top 100 -width 200 -height 150 image.ppm > crop.ppm"],["Cut with padding","pnmcut -pad -10 -10 120 120 image.ppm > padded.ppm"],["Chain with conversion","jpegtopnm photo.jpg | pnmcut 0 0 640 480 | pnmtojpeg > thumb.jpg\n```"],["CAVEATS","Superseded by pamcut with more features. Negative coordinates cut from opposite edge."],["HISTORY","pnmcut is part of **Netpbm** by **Jef Poskanzer**, providing basic cropping functionality."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pamcut](/man/pamcut)(1), [pnmpaste](/man/pnmpaste)(1), [pnmcrop](/man/pnmcrop)(1), [netpbm](/man/netpbm)(1)"]],"e":["pamcut","pnmpaste","pnmcrop","netpbm"],"x":"pnmcut [x] [y] [width] [height] [input.pnm] > [output.pnm]"},"pro":{"n":"pro","t":"Manage Ubuntu Pro services and subscriptions","s":[["TAGLINE","Manage Ubuntu Pro services and subscriptions"],["TLDR","**Attach** system to Ubuntu Pro\n\n```sudo pro attach```\n\nDisplay **status**\n\n```pro status```\n\n**Fix** a specific vulnerability\n\n```pro fix [CVE-2023-1234]```\n\nDisplay **security status**\n\n```pro security-status```\n\nList **unavailable** packages\n\n```pro security-status --unavailable```\n\nList **third-party** packages\n\n```pro security-status --thirdparty```"],["SYNOPSIS","**pro** [**attach**|**status**|**fix**|**security-status**] [_options_]"],["COMMANDS","**attach**\n> Connect system to Ubuntu Pro subscription\n\n**detach**\n> Disconnect from Ubuntu Pro\n\n**status**\n> Show Ubuntu Pro service status\n\n**fix**\n> Apply fixes for specific CVEs or USNs\n\n**security-status**\n> Show security support status\n\n**enable**\n> Enable specific Pro services\n\n**disable**\n> Disable specific Pro services"],["PARAMETERS","**--unavailable**\n> Show packages no longer available\n\n**--thirdparty**\n> Show third-party packages\n\n**--format _format_**\n> Output format (json, yaml)"],["DESCRIPTION","**pro** manages Ubuntu Pro (formerly Ubuntu Advantage) services and subscriptions. It provides extended security maintenance, kernel livepatch, and compliance tools for Ubuntu systems.\n\nThe tool can automatically apply security fixes and provides visibility into the security status of installed packages."],["CAVEATS","Requires Ubuntu Pro subscription for most features. Free tier available for personal use. Some services require reboot to activate."],["HISTORY","**pro** replaced the **ubuntu-advantage-tools** command, providing a simpler interface to Ubuntu's commercial support services. It was renamed to align with the Ubuntu Pro branding."],["SEE ALSO","[apt](/man/apt)(8), [unattended-upgrades](/man/unattended-upgrades)(8)"]],"e":["apt","unattended-upgrades"],"x":"sudo pro attach"},"pvetui":{"n":"pvetui","t":"Terminal UI for Proxmox VE cluster management","s":[["TAGLINE","Terminal UI for Proxmox VE cluster management"],["TLDR","**Launch the Proxmox VE TUI**\n\n```pvetui```"],["SYNOPSIS","**pvetui** [_options_]"],["DESCRIPTION","**pvetui** is a terminal user interface for managing Proxmox VE clusters, nodes, virtual machines, and LXC containers. It supports starting, stopping, migrating, and monitoring resources, secure authentication via API tokens or passwords, integrated SSH shells, VNC console access with an embedded noVNC client, multi-cluster profile switching, and plugin extensibility including a community scripts installer."],["CAVEATS","Requires network access to a Proxmox VE instance and valid authentication credentials."],["HISTORY","**pvetui** was created by **devnullvoid** and is written in **Go**."],["INSTALL","```zypper: sudo zypper install pvetui```\n\n```brew: brew install pvetui```\n\n```nix: nix profile install nixpkgs#pvetui```"],["SEE ALSO","[ssh](/man/ssh)(1), [virsh](/man/virsh)(1), [virsh-help](/man/virsh-help)(1)"]],"e":["ssh","virsh","virsh-help"],"x":"pvetui"},"racket":{"n":"racket","t":"General-purpose multi-paradigm programming language","s":[["TAGLINE","General-purpose multi-paradigm programming language"],["TLDR","**Start REPL**\n\n```racket```\n\n**Run script**\n\n```racket [script.rkt]```\n\n**Evaluate expression**\n\n```racket -e \"(+ 1 2 3)\"```\n\n**Run with specific language**\n\n```racket -l [typed/racket] [script.rkt]```\n\n**Interactive with file**\n\n```racket -it [script.rkt]```\n\n**Compile to bytecode**\n\n```raco make [script.rkt]```\n\n**Create executable**\n\n```raco exe [script.rkt]```"],["SYNOPSIS","**racket** [_-e expr_] [_-l lang_] [_-it_] [_options_] [_file_] [_args_]"],["PARAMETERS","**-e** _EXPR_\n> Evaluate expression.\n\n**-l** _LANG_\n> Use language.\n\n**-f** _FILE_\n> Load file.\n\n**-t** _FILE_\n> Require file.\n\n**-i**\n> Interactive mode.\n\n**-it** _FILE_\n> Load and interact.\n\n**-I** _LANG_\n> Set language and interact.\n\n**-m**, **--main**\n> Run main submodule.\n\n**-v**, **--version**\n> Show version.\n\n**-h**, **--help**\n> Show help.\n\n**--**\n> End options."],["DESCRIPTION","**racket** is the main implementation of Racket, a general-purpose programming language in the Lisp/Scheme family.\n\nThe language emphasizes language-oriented programming. The #lang directive at file start selects the language: racket, typed/racket, scribble, and many others.\n\nThe REPL provides interactive development. Tab completion, history, and inline documentation support exploratory programming.\n\nraco is the Racket command tool for package management, compilation, documentation, and other tasks. It extends Racket's capabilities.\n\nTyped Racket adds optional static typing. Type annotations catch errors at compile time while maintaining Racket's expressiveness.\n\nThe DrRacket IDE provides a graphical environment with debugging, profiling, and educational features."],["CAVEATS","Different from standard Scheme in some areas. Package system requires network access. Large standard distribution."],["HISTORY","**Racket** evolved from **PLT Scheme**, which began at **Rice University** in **1995**. The name changed to Racket in **2010** to reflect its evolution beyond Scheme. It's widely used in programming language research and education."],["INSTALL","```apt: sudo apt install racket```\n\n```dnf: sudo dnf install racket```\n\n```pacman: sudo pacman -S racket```\n\n```apk: sudo apk add racket```\n\n```zypper: sudo zypper install racket```\n\n```brew: brew install racket```\n\n```nix: nix profile install nixpkgs#racket```"],["SEE ALSO","[raco](/man/raco)(1), [drracket](/man/drracket)(1), [scheme](/man/scheme)(1), [guile](/man/guile)(1)"]],"e":["raco","drracket","scheme","guile"],"x":"racket"},"rasttopnm":{"n":"rasttopnm","t":"Convert Sun raster images to PNM format","s":[["TAGLINE","Convert Sun raster images to PNM format"],["TLDR","**Convert** a Sun raster file to PNM on stdout\n\n```rasttopnm [input.ras] > [output.pnm]```\n\n**Read from stdin** and write PNM on stdout\n\n```cat [input.ras] | rasttopnm > [output.pnm]```\n\n**Output the index plane** of a colormapped raster (debugging)\n\n```rasttopnm -index [input.ras] > [output.pgm]```\n\n**Convert and pipe** to PNG\n\n```rasttopnm [input.ras] | pnmtopng > [output.png]```"],["SYNOPSIS","**rasttopnm** [**-index**] [_rastfile_]"],["PARAMETERS","_rastfile_\n> Sun Rasterfile (.ras) input. Reads from stdin if omitted.\n\n**-index**\n> For colormapped rasters, output the raw color-index plane as PGM instead of dereferencing it through the colormap to produce PPM. Mainly useful for inspecting the underlying data."],["DESCRIPTION","**rasttopnm** converts Sun Rasterfile images (.ras) to Portable Anymap (PNM) format, automatically selecting PBM, PGM, or PPM output based on the input image's color depth and colormap. Sun Rasterfile was the native image format on SunOS and Solaris systems and is commonly produced by older X11 tools.\n\nOutput goes to standard output. Part of the **Netpbm** toolkit."],["CAVEATS","Some less-common Rasterfile encodings (e.g. RT_BYTE_ENCODED RLE with unusual colormaps) may not be supported on every Netpbm build. The converter chooses the narrowest PNM subtype that can losslessly represent the input."],["INSTALL","```apt: sudo apt install netpbm```\n\n```dnf: sudo dnf install netpbm```\n\n```pacman: sudo pacman -S netpbm```\n\n```apk: sudo apk add netpbm```\n\n```zypper: sudo zypper install netpbm```\n\n```brew: brew install netpbm```\n\n```nix: nix profile install nixpkgs#netpbm```"],["SEE ALSO","[pnmtorast](/man/pnmtorast)(1), [xwdtopnm](/man/xwdtopnm)(1), [pnm](/man/pnm)(5)"]],"e":["pnmtorast","xwdtopnm","pnm"],"x":"rasttopnm [input.ras] > [output.pnm]"},"recon-ng":{"n":"recon-ng","t":"Web reconnaissance and OSINT framework","s":[["TAGLINE","Web reconnaissance and OSINT framework"],["TLDR","**Start recon-ng** interactive console\n\n```recon-ng```\n\n**Create or load a workspace**\n\n```recon-ng -w [workspace_name]```\n\n**List available modules**\n\n```marketplace search```\n\n**Install a module**\n\n```marketplace install [module_name]```\n\n**Load a module**\n\n```modules load [module_name]```\n\n**Show module options**\n\n```options list```\n\n**Run the loaded module**\n\n```run```"],["SYNOPSIS","**recon-ng** [**-w** _workspace_] [**-r** _resource_] [_options_]"],["PARAMETERS","**-w** _workspace_\n> Create or load the specified workspace\n\n**-r** _resource_\n> Execute resource file (automation script)\n\n**--no-analytics**\n> Disable anonymous usage tracking\n\n**--no-version**\n> Disable version check at startup\n\n**--no-marketplace**\n> Disable remote marketplace access\n\n**--stealth**\n> Disable all passive network requests made by the framework itself\n\n**--accessible**\n> Use accessible output where available\n\n**-h**, **--help**\n> Display help message"],["CONSOLE COMMANDS","**workspaces list**\n> List available workspaces\n\n**workspaces create** _name_\n> Create a new workspace\n\n**marketplace search** [_term_]\n> Search available modules in marketplace\n\n**marketplace install** _module_\n> Install a module from marketplace\n\n**modules load** _module_\n> Load an installed module\n\n**options set** _option_ _value_\n> Set module option\n\n**options list**\n> Display current module options\n\n**run**\n> Execute the loaded module\n\n**show** _table_\n> Display data from database tables (hosts, contacts, domains, etc.)\n\n**keys add** _name_ _value_\n> Add API key for external services\n\n**keys list**\n> List configured API keys"],["DESCRIPTION","**recon-ng** is a modular reconnaissance framework for gathering open-source intelligence (OSINT). Designed with a Metasploit-like interface, it provides a familiar workflow for security professionals conducting the information-gathering phase of assessments.\n\nThe framework uses workspaces to organize reconnaissance projects, with each workspace maintaining its own database of collected data (domains, hosts, contacts, credentials, etc.). Modules can query this data and add new findings, building comprehensive intelligence profiles.\n\nModules cover various reconnaissance categories: discovery (subdomain enumeration, port scanning), recon (WHOIS, DNS records, social media), import (data ingestion), and reporting (HTML, JSON, Excel exports). Many modules integrate with external APIs (Shodan, VirusTotal, HaveIBeenPwned) requiring API keys."],["CONFIGURATION","**~/.recon-ng/workspaces/**\n> Workspace directories containing per-project SQLite databases with collected hosts, contacts, domains, and credentials.\n\n**keys add** _name_ _value_\n> API keys for external services (Shodan, VirusTotal, HaveIBeenPwned, etc.) stored in the workspace database and required by many modules."],["CAVEATS","**Authorization required**: Only perform reconnaissance against targets you have permission to assess. Unauthorized reconnaissance may violate laws and terms of service.\n\nMany powerful modules require API keys from external services. Configure keys with **keys add** before using those modules. Some services have rate limits or require paid subscriptions.\n\nThe marketplace must be accessed to install modules beyond the base set. Ensure network connectivity for marketplace operations.\n\nModule results vary in accuracy. Cross-reference findings from multiple sources and validate critical information manually."],["HISTORY","Recon-ng was developed by **Tim Tomes** while working at Black Hills Information Security. First released around **2012**, it was designed to bring the modular, database-driven approach of Metasploit to the reconnaissance phase. The framework is written in Python and continues active development with community-contributed modules."],["INSTALL","```apk: sudo apk add recon-ng```\n\n```brew: brew install recon-ng```\n\n```nix: nix profile install nixpkgs#recon-ng```"],["SEE ALSO","[maltego](/man/maltego)(1), [amass](/man/amass)(1), [nmap](/man/nmap)(1)"]],"e":["maltego","amass","nmap"],"x":"recon-ng"},"remmina":{"n":"remmina","t":"Remote desktop client application","s":[["TAGLINE","Remote desktop client application"],["TLDR","**Launch Remmina**\n\n```remmina```\n\n**Connect to saved connection**\n\n```remmina -c [connection_name]```\n\n**Connect to RDP server**\n\n```remmina -c rdp://[user]@[host]```\n\n**Connect to VNC server**\n\n```remmina -c vnc://[host]```\n\n**Connect to SSH**\n\n```remmina -c ssh://[user]@[host]```"],["SYNOPSIS","**remmina** [_-a_|_-i_|_-n_|_-q_|_-v_] [_-c file_] [_-e file_] [_-p tabindex_] [_-s server_] [_-t protocol_] [_options_]"],["PARAMETERS","**-c**, **--connect** _file_\n> Connect to a desktop described in a file or a supported URI (RDP, VNC, SSH, SPICE).\n\n**-e**, **--edit** _file_\n> Open and edit a connection profile file.\n\n**-n**, **--new**\n> Create a new connection profile.\n\n**-t**, **--protocol** _protocol_\n> Use default protocol for new connections (RDP, VNC, SSH, SPICE).\n\n**-s**, **--server** _server_\n> Use default server name for new connections (used with --new).\n\n**-p**, **--pref** _tabindex_\n> Show preferences dialog page.\n\n**-i**, **--icon**\n> Start as tray icon.\n\n**-k**, **--kiosk**\n> Start in kiosk mode (thin client).\n\n**-q**, **--quit**\n> Quit the application.\n\n**-v**, **--version**\n> Show version.\n\n**--update-profile**\n> Modify a connection profile non-interactively (requires --set-option).\n\n**--set-option** _OPTION[=VALUE]_\n> Set profile option, used with --update-profile."],["DESCRIPTION","**Remmina** is a GTK remote desktop client supporting multiple protocols including RDP, VNC, SSH, SPICE, and more. It provides a unified interface for managing remote connections."],["EXAMPLES","```bash"],["Launch GUI","remmina"],["Connect to RDP","remmina -c rdp://administrator@192.168.1.100"],["Connect to VNC","remmina -c vnc://192.168.1.100:5900"],["SSH connection","remmina -c ssh://user@server.example.com"],["Connect using saved profile","remmina -c ~/.local/share/remmina/myserver.remmina"],["Update a profile's username non-interactively","remmina --update-profile ~/.local/share/remmina/myserver.remmina --set-option username=admin\n```"],["PROTOCOLS","```\nRDP    - Windows Remote Desktop\nVNC    - Virtual Network Computing\nSSH    - Secure Shell\nSPICE  - Virtual machine display\nNX     - NoMachine protocol\nSFTP   - Secure file transfer\n```"],["CONFIGURATION","**~/.local/share/remmina/**\n> Directory containing saved connection profiles as .remmina files, each storing server address, protocol, credentials, and display settings.\n\n**~/.config/remmina/remmina.pref**\n> Global preferences file controlling default behavior, keyboard shortcuts, appearance, and plugin settings."],["CAVEATS","Requires protocol plugins. FreeRDP for RDP support. Some features require additional packages."],["HISTORY","Remmina was forked from **grdc** in 2009 by **Vic Lee** to create a more feature-rich remote desktop client."],["INSTALL","```apt: sudo apt install remmina```\n\n```dnf: sudo dnf install remmina```\n\n```pacman: sudo pacman -S remmina```\n\n```apk: sudo apk add remmina```\n\n```zypper: sudo zypper install remmina```\n\n```nix: nix profile install nixpkgs#remmina```"],["SEE ALSO","[xfreerdp](/man/xfreerdp)(1), [vncviewer](/man/vncviewer)(1), [ssh](/man/ssh)(1)"]],"e":["xfreerdp","vncviewer","ssh"],"x":"remmina"},"runsvdir":{"n":"runsvdir","t":"Start and monitor runit service directory","s":[["TAGLINE","Start and monitor runit service directory"],["TLDR","**Start supervising services** in a directory\n\n```runsvdir [/etc/service]```\n\n**Run each service in a new session** and separate process group\n\n```runsvdir -P [/etc/service]```\n\n**Start with a log command** for error output\n\n```runsvdir [/etc/service] \"log: .........................................\"```"],["SYNOPSIS","**runsvdir** [**-P**] _dir_ [_log_]"],["PARAMETERS","**-P**\n> Use setsid to run each runsv process in a new session and separate process group.\n\n_dir_\n> Service directory to monitor. Each subdirectory (or symlink to a directory) is started as a supervised service via runsv.\n\n_log_\n> Optional log command. If specified (must be at least seven characters), runsvdir pipes its error output through this command, similar to daemontools' readproctitle."],["DESCRIPTION","**runsvdir** starts and monitors a collection of **runsv**(8) processes. It scans _dir_ at least every five seconds. When a new subdirectory or symlink to a directory appears, runsvdir starts a new runsv process for it. When a subdirectory is removed, the corresponding runsv process is sent a TERM signal.\n\nSubdirectories whose names start with a dot are ignored. The maximum number of services is 1000.\n\nSending runsvdir a TERM signal causes it to exit with status 0. Sending a HUP signal causes it to send TERM to all running runsv processes and then exit with status 111."],["SERVICE MANAGEMENT","- Create symlink in dir: service starts automatically on next scan\n- Remove symlink: service stops via TERM signal\n- Use the **sv** command to control individual services"],["CAVEATS","Usually started by runit init at system boot. The filesystem should not be unmounted while runsvdir is running. Permissions on service directories must be correct for runsv to start."],["HISTORY","**runsvdir** is the service directory supervisor in **runit**, created by Gerrit Pape. It watches for service directories and manages runsv processes for each."],["INSTALL","```apt: sudo apt install runit```\n\n```apk: sudo apk add runit```\n\n```brew: brew install runit```\n\n```nix: nix profile install nixpkgs#runit```"],["SEE ALSO","[runit](/man/runit)(8), [runsv](/man/runsv)(8), [sv](/man/sv)(8), [runsvchdir](/man/runsvchdir)(8), [chpst](/man/chpst)(8), [svlogd](/man/svlogd)(8)"]],"e":["runit","runsv","sv","runsvchdir","chpst","svlogd"],"x":"runsvdir [/etc/service]"},"rustup-self":{"n":"rustup-self","t":"Manage the rustup installation itself","s":[["TAGLINE","Manage the rustup installation itself"],["TLDR","**Upgrade** the rustup binary itself\n\n```rustup self update```\n\n**Remove** rustup and its installed toolchains\n\n```rustup self uninstall```"],["SYNOPSIS","**rustup** **self** _command_"],["PARAMETERS","**update**\n> Update rustup itself.\n\n**uninstall**\n> Remove rustup and all toolchains.\n\n**upgrade-data**\n> Upgrade internal data."],["DESCRIPTION","**rustup self** manages the rustup installation itself. Update rustup to latest version or completely uninstall Rust and rustup."],["INSTALL","```apt: sudo apt install rustup```\n\n```dnf: sudo dnf install rustup```\n\n```pacman: sudo pacman -S rustup```\n\n```zypper: sudo zypper install rustup```\n\n```brew: brew install rustup```\n\n```nix: nix profile install nixpkgs#rustup```"],["SEE ALSO","[rustup](/man/rustup)(1), [rustup-update](/man/rustup-update)(1)"]],"e":["rustup","rustup-update"],"x":"rustup self update"},"sdcv":{"n":"sdcv","t":"Console dictionary using StarDict format","s":[["TAGLINE","Console dictionary using StarDict format"],["TLDR","**Look up word**\n\n```sdcv [word]```\n\n**Look up with specific dictionary**\n\n```sdcv -u \"[dictionary_name]\" [word]```\n\n**List available dictionaries**\n\n```sdcv -l```\n\n**Non-interactive mode**\n\n```sdcv -n [word]```\n\n**Show exact matches only**\n\n```sdcv -e [word]```\n\n**Use data directory**\n\n```sdcv --data-dir [/path/to/dicts] [word]```\n\n**JSON output**\n\n```sdcv --json [word]```"],["SYNOPSIS","**sdcv** [_-u dict_] [_-n_] [_-e_] [_-j_] [_--data-dir dir_] [_options_] [_word_]"],["PARAMETERS","**-l**, **--list-dicts**\n> List available dictionaries.\n\n**-u** _DICT_\n> Use specific dictionary.\n\n**-n**, **--non-interactive**\n> Non-interactive mode.\n\n**-e**, **--exact-search**\n> Exact matches only.\n\n**-x**, **--only-data-dir**\n> Only use dictionaries in data-dir, do not search in user and system directories.\n\n**--data-dir** _DIR_\n> Dictionary data directory.\n\n**-j**, **--json**\n> JSON output.\n\n**--color**\n> Colored output.\n\n**--utf8-input**\n> UTF-8 input.\n\n**--utf8-output**\n> UTF-8 output.\n\n**-v**, **--version**\n> Display version and exit.\n\n**-h**, **--help**\n> Show help."],["DESCRIPTION","**sdcv** (StarDict console version) provides command-line dictionary lookup. It uses StarDict format dictionaries, which are widely available.\n\nDictionaries are stored in ~/.stardict/dic or system directories. Many languages and specialized dictionaries are available for download.\n\nInteractive mode provides a prompt for multiple lookups. Non-interactive mode suits scripting and integration.\n\nFuzzy search finds similar words when exact matches fail. This helps with spelling variations or typos.\n\nMultiple dictionaries can be searched simultaneously. Results from all are shown unless a specific dictionary is selected.\n\nOutput formatting supports terminal display and programmatic processing. JSON output enables integration with other tools."],["CONFIGURATION","**~/.stardict/dic/**\n> Default user dictionary directory where StarDict format dictionaries are stored.\n\n**/usr/share/stardict/dic/**\n> System-wide dictionary directory.\n\n**STARDICT_DATA_DIR**\n> Environment variable overriding the default dictionary data directory.\n\n**SDCV_PAGER**\n> Environment variable to specify a pager for output display.\n\n**SDCV_HISTFILE**\n> Environment variable to set the history file location.\n\n**~/.sdcv_ordering**\n> File specifying dictionary ordering for search results."],["CAVEATS","Requires dictionary files to be installed. Quality varies by dictionary. Some dictionaries are large. Character encoding issues possible."],["HISTORY","**sdcv** is the console version of **StarDict**, an open-source dictionary project started in **2003** by **Hu Zheng**. StarDict became popular for its cross-platform GUI, and sdcv extended it to command-line users. The current maintained version is by **Evgeniy Dushistov** on GitHub."],["INSTALL","```apt: sudo apt install sdcv```\n\n```dnf: sudo dnf install sdcv```\n\n```pacman: sudo pacman -S sdcv```\n\n```apk: sudo apk add sdcv```\n\n```zypper: sudo zypper install sdcv```\n\n```brew: brew install sdcv```\n\n```nix: nix profile install nixpkgs#sdcv```"],["SEE ALSO","[dict](/man/dict)(1), [dictd](/man/dictd)(8), [espeak](/man/espeak)(1)"]],"e":["dict","dictd","espeak"],"x":"sdcv [word]"},"selene":{"n":"selene","t":"Fast Lua linter written in Rust","s":[["TAGLINE","Fast Lua linter written in Rust"],["TLDR","**Lint Lua files** in the current directory\n\n```selene .```\n\n**Lint specific files**\n\n```selene [path/to/file1.lua] [path/to/file2.lua]```\n\n**Lint with a custom configuration file**\n\n```selene --config [path/to/selene.toml] [path/to/files]```\n\n**Lint and suppress summary** information\n\n```selene --no-summary [path/to/files]```\n\n**Lint with quiet output** showing only essential information\n\n```selene --quiet [path/to/files]```\n\n**Lint with JSON output** for tooling integration\n\n```selene --display-style json [path/to/files]```\n\n**Validate configuration file** for errors\n\n```selene validate-config```"],["SYNOPSIS","**selene** [_flags_] [_options_] _files_...\n\n**selene** _subcommand_"],["PARAMETERS","**--allow-warnings**\n> Exit successfully when only warnings (no errors) occur.\n\n**--color** _color_\n> Control color output. Possible values: **Always**, **Auto**, **Never**. Default: **Auto**.\n\n**--config** _config_\n> Path to a TOML configuration file. Default: **selene.toml** in current directory.\n\n**--display-style** _style_\n> Set the output display format. Possible values: **Json**, **Json2**, **Rich**, **Quiet**.\n\n**--num-threads** _n_\n> Number of threads to use. Defaults to the number of logical CPU cores.\n\n**--pattern** _pattern_\n> A glob pattern to match files to check. Can be specified multiple times.\n\n**--no-exclude**\n> Do not respect exclude patterns from configuration.\n\n**-n**, **--no-summary**\n> Suppress the summary information at the end of linting.\n\n**-q**, **--quiet**\n> Display only essential information. Equivalent to **--display-style=quiet**.\n\n**-h**, **--help**\n> Print help information.\n\n**-V**, **--version**\n> Print version information."],["SUBCOMMANDS","**validate-config**\n> Report any errors in the selene configuration file.\n\n**capabilities**\n> Print the capabilities of the current selene build.\n\n**generate-roblox-std**\n> Generate a Roblox standard library definition.\n\n**update-roblox-std**\n> Update the Roblox standard library definition.\n\n**upgrade-std**\n> Upgrade standard library format to the latest version.\n\n**help**\n> Print help for selene or a specific subcommand."],["DESCRIPTION","**selene** is a fast, modern Lua linter written in Rust. It analyzes Lua code to detect bugs, style issues, and potential problems before runtime. The tool supports standard Lua as well as Luau (Roblox's Lua dialect) through configurable standard libraries.\n\nConfiguration is done via a **selene.toml** file where you can enable or disable specific lints, set their severity levels, and configure standard library definitions. The linter integrates well with editors and CI/CD pipelines through its JSON output modes.\n\nselene is particularly popular in the Roblox development community but works for any Lua project. It can be extended with custom lint rules and standard library definitions."],["CONFIGURATION","**selene.toml**\n> Project-level configuration file specifying enabled lints, severity levels, standard library definitions, and file exclude patterns.\n\n**std**\n> Configuration key in selene.toml specifying the Lua standard library variant (e.g., \"lua51\", \"lua52\", \"luau\", or a custom definition file)."],["CAVEATS","selene requires a **selene.toml** configuration file for customization. Without one, it uses default settings which may not match your Lua variant or coding standards. For Luau/Roblox projects, you need to generate or configure the appropriate standard library definitions."],["HISTORY","**selene** was created by Kampfkarren (Lucas) and first released as an open-source project on GitHub. It was written in Rust for performance and safety, gaining popularity particularly within the Roblox development community where fast, reliable Lua linting is essential. The project continues active development with regular releases adding support for new Luau features and additional lint rules."],["INSTALL","```pacman: sudo pacman -S selene```\n\n```brew: brew install selene```\n\n```nix: nix profile install nixpkgs#selene```"],["SEE ALSO","[luacheck](/man/luacheck)(1), [stylua](/man/stylua)(1), [lua](/man/lua)(1)"]],"e":["luacheck","stylua","lua"],"x":"selene ."},"serverless":{"n":"serverless","t":"Deploy serverless functions to cloud providers","s":[["TAGLINE","Deploy serverless functions to cloud providers"],["TLDR","**Create new service**\n\n```serverless create --template [aws-nodejs] --name [my-service]```\n\n**Deploy service**\n\n```serverless deploy```\n\n**Deploy single function**\n\n```serverless deploy function -f [functionName]```\n\n**Invoke function**\n\n```serverless invoke -f [functionName]```\n\n**View logs**\n\n```serverless logs -f [functionName]```\n\n**Remove service**\n\n```serverless remove```\n\n**Run locally**\n\n```serverless invoke local -f [functionName]```\n\n**Print config**\n\n```serverless print```"],["SYNOPSIS","**serverless** [_create_] [_deploy_] [_invoke_] [_logs_] [_remove_] [_options_]"],["PARAMETERS","**create**\n> Create new service.\n\n**deploy**\n> Deploy service.\n\n**deploy function**\n> Deploy single function.\n\n**invoke** **-f** _FUNC_\n> Invoke function.\n\n**invoke local**\n> Invoke locally.\n\n**logs** **-f** _FUNC_\n> View function logs.\n\n**remove**\n> Remove service.\n\n**package**\n> Package without deploying.\n\n**print**\n> Print configuration.\n\n**--template**, **-t** _TPL_\n> Project template.\n\n**--name**, **-n** _NAME_\n> Service name.\n\n**--stage**, **-s** _STAGE_\n> Deployment stage.\n\n**--region**, **-r** _REGION_\n> AWS region.\n\n**--config**, **-c** _FILE_\n> Config file."],["DESCRIPTION","**serverless** (sls) is a framework for building serverless applications. It deploys functions to AWS Lambda, Azure Functions, Google Cloud Functions, and others.\n\nService configuration in serverless.yml defines functions, events, and resources. Events trigger functions from HTTP, queues, schedules, and other sources.\n\nDeployment packages code and provisions infrastructure. CloudFormation (AWS) or equivalent handles resource creation.\n\nLocal invocation tests functions without deployment. This speeds up development iteration.\n\nLogs stream function output from cloud providers. They show invocations, errors, and execution details.\n\nPlugins extend functionality: custom domains, monitoring, optimization, and framework integrations."],["CONFIGURATION","**serverless.yml**\n> Main service configuration file defining functions, events, resources, plugins, and provider settings.\n\n**~/.serverlessrc**\n> User-level configuration for tracking and telemetry preferences.\n\n**AWS_ACCESS_KEY_ID** / **AWS_SECRET_ACCESS_KEY**\n> AWS credentials for deployment (or configured via AWS CLI profiles)."],["CAVEATS","Cloud provider credentials required. Deployment creates many resources. Cold starts affect latency. Provider lock-in considerations."],["HISTORY","**Serverless Framework** was created by **Austen Collins** in **2015**, originally named JAWS. It pioneered serverless deployment tooling and became the most popular framework for Lambda development."],["INSTALL","```brew: brew install serverless```"],["SEE ALSO","[aws](/man/aws)(1), [sam](/man/sam)(1), [terraform](/man/terraform)(1), [pulumi](/man/pulumi)(1)"]],"e":["aws","sam","terraform","pulumi"],"x":"serverless create --template [aws-nodejs] --name [my-service]"},"setquota":{"n":"setquota","t":"Set disk quotas from the command line","s":[["TAGLINE","Set disk quotas from the command line"],["TLDR","**Set quota for a user** with soft and hard limits\n\n```sudo setquota -u [username] [block-soft] [block-hard] [inode-soft] [inode-hard] [/path/to/filesystem]```\n\n**Set quota for a group**\n\n```sudo setquota -g [groupname] [block-soft] [block-hard] [inode-soft] [inode-hard] [/path/to/filesystem]```\n\n**Copy quota settings** from one user to another\n\n```sudo setquota -u -p [prototype_user] [target_user] [/path/to/filesystem]```\n\n**Set quota on all filesystems** with quotas enabled\n\n```sudo setquota -a -u [username] [block-soft] [block-hard] [inode-soft] [inode-hard]```\n\n**Set grace times** for users\n\n```sudo setquota -t -u [block-grace] [inode-grace] [/path/to/filesystem]```\n\n**Set quotas from batch input**\n\n```echo \"[username] [block-soft] [block-hard] [inode-soft] [inode-hard]\" | sudo setquota -b [/path/to/filesystem]```"],["SYNOPSIS","**setquota** [**-u**|**-g**|**-P**] [_options_] _name_ _block-softlimit_ _block-hardlimit_ _inode-softlimit_ _inode-hardlimit_ _filesystem_\n\n**setquota** [**-u**|**-g**|**-P**] **-p** _protoname_ _name_ _filesystem_\n\n**setquota** **-t** [**-u**|**-g**|**-P**] _block-grace_ _inode-grace_ _filesystem_\n\n**setquota** **-T** [**-u**|**-g**|**-P**] _name_ _block-time_ _inode-time_ _filesystem_"],["PARAMETERS","**-u**, **--user**\n> Set user quotas for the named user. This is the default.\n\n**-g**, **--group**\n> Set group quotas for the named group.\n\n**-P**, **--project**\n> Set project quotas for the named project.\n\n**-p**, **--prototype** _protoname_\n> Copy quota settings from a prototype user, group, or project.\n\n**-b**, **--batch**\n> Read quota information from standard input. Format: name block-softlimit block-hardlimit inode-softlimit inode-hardlimit.\n\n**-t**, **--edit-period**\n> Set grace times (in seconds) for users, groups, or projects.\n\n**-T**, **--edit-times**\n> Alter time remaining for individual users when soft limit is enforced.\n\n**-a**, **--all**\n> Apply to all filesystems with quotas listed in /etc/mtab.\n\n**-F**, **--format** _quotaformat_\n> Specify quota format: **vfsold**, **vfsv0**, **vfsv1**, **rpc**, or **xfs**.\n\n**-r**, **--remote**\n> Edit remote quota using rpc.rquotad."],["DESCRIPTION","**setquota** is a command-line quota editor that sets disk quotas for users, groups, or projects on a filesystem. Unlike the interactive **edquota** command, setquota allows quota configuration in a single command, making it suitable for scripts and automation.\n\nQuotas can limit both disk space usage (blocks) and number of files (inodes). Each limit has a soft limit (can be exceeded temporarily) and a hard limit (absolute maximum). When a soft limit is exceeded, the grace period begins, after which the soft limit becomes a hard limit.\n\nBlock limits are typically specified in kilobytes, though this may vary by quota format. If a numeric value is given instead of a username, it is treated as a UID/GID/project ID."],["CAVEATS","Requires root privileges. The filesystem must have quota support enabled and quota files initialized with **quotacheck**. Changes take effect immediately without requiring a remount. Not all quota formats support all features."],["HISTORY","**setquota** is part of the Linux Diskquota Tools project. Disk quotas have been part of Unix systems since the 1980s, originally introduced in BSD Unix. The Linux quota system evolved from these early implementations, with the modern tools supporting multiple quota formats including XFS native quotas and NFS remote quotas."],["INSTALL","```apt: sudo apt install quota```\n\n```dnf: sudo dnf install quota```\n\n```pacman: sudo pacman -S quota-tools```\n\n```apk: sudo apk add quota-tools```\n\n```zypper: sudo zypper install quota```\n\n```nix: nix profile install nixpkgs#quota```"],["SEE ALSO","[quota](/man/quota)(1), [edquota](/man/edquota)(8), [repquota](/man/repquota)(8), [quotacheck](/man/quotacheck)(8), [quotaon](/man/quotaon)(8)"]],"e":["quota","edquota","repquota","quotacheck","quotaon"],"x":"sudo setquota -u [username] [block-soft] [block-hard] [inode-soft] [inode-hard] [/path/to/filesystem]"},"sfdk-emulator-remove":{"n":"sfdk-emulator-remove","t":"Remove installed Sailfish OS emulators","s":[["TAGLINE","Remove installed Sailfish OS emulators"],["TLDR","**Remove emulator**\n\n```sfdk emulator remove [emulator_name]```"],["SYNOPSIS","**sfdk emulator remove** _name_ [_options_]"],["DESCRIPTION","**sfdk emulator remove** removes an installed Sailfish OS emulator. Part of Sailfish SDK."],["SEE ALSO","[sfdk-emulator](/man/sfdk-emulator)(1), [sfdk-emulator-install](/man/sfdk-emulator-install)(1)"]],"e":["sfdk-emulator","sfdk-emulator-install"],"x":"sfdk emulator remove [emulator_name]"},"stalonetray":{"n":"stalonetray","t":"Stand-alone X11 system tray","s":[["TAGLINE","Stand-alone X11 system tray"],["TLDR","**Start stalonetray** with default settings\n\n```stalonetray```\n\n**Start with custom background color**\n\n```stalonetray --background \"[#2e3440]\"```\n\n**Start with custom icon size**\n\n```stalonetray --icon-size [32]```\n\n**Start with window strut** to reserve screen space\n\n```stalonetray --window-strut [auto]```\n\n**Start in dockapp mode** for WindowMaker\n\n```stalonetray --dockapp-mode [wmaker]```\n\n**Start with icons growing from bottom-right**\n\n```stalonetray --icon-gravity [SE]```\n\n**Use a custom configuration file**\n\n```stalonetray --config [~/.config/stalonetray/stalonetrayrc]```"],["SYNOPSIS","**stalonetray** [_options_]"],["PARAMETERS","**-bg**, **--background** _color_\n> Tray background color (hex triplet or X11 color name).\n\n**-i**, **--icon-size** _n_\n> Default icon size in pixels. Default: 24, minimum: 16.\n\n**--icon-gravity** _gravity_\n> Icon positioning gravity: **NW**, **NE**, **SW**, or **SE**. Default: NW.\n\n**--grow-gravity** _gravity_\n> Direction for tray growth: **NW**, **NE**, **SW**, **SE**, **N**, **S**, **E**, or **W**.\n\n**--geometry** _geometry_\n> Initial tray window geometry (WIDTHxHEIGHT+X+Y).\n\n**--max-geometry** _geometry_\n> Maximum tray dimensions.\n\n**--window-type** _type_\n> EWMH window type: **desktop**, **dock**, **normal**, **toolbar**, **utility**. Default: dock.\n\n**--window-strut** _mode_\n> Reserve screen space: **auto**, **top**, **bottom**, **left**, **right**, or **none**.\n\n**-d**, **--dockapp-mode** _mode_\n> Dockapp mode: **none**, **simple**, or **wmaker**.\n\n**--slot-size** _size_\n> Grid slot size for icon placement.\n\n**--sticky**\n> Make tray window appear on all desktops.\n\n**--skip-taskbar**\n> Remove tray window from taskbar.\n\n**--kludges** _list_\n> Enable workarounds: fix_window_pos, force_icons_size, use_icons_hints.\n\n**--log-level** _level_\n> Logging verbosity: **err**, **info**, or **trace**.\n\n**-c**, **--config** _file_\n> Configuration file path."],["DESCRIPTION","**stalonetray** is a stand-alone system tray (notification area) implementation for X11. It provides a minimalist tray that works with any EWMH-compliant window manager, making it ideal for lightweight setups or window managers without built-in system trays.\n\nThe tray supports the freedesktop.org System Tray Protocol and XEMBED specification for embedding application icons. It can be positioned anywhere on screen and configured to reserve space (struts) so other windows don't overlap it.\n\nConfiguration can be done via command-line options or a configuration file at **~/.stalonetrayrc**. The file uses keyword-argument pairs with command-line options taking precedence."],["CONFIGURATION","**~/.stalonetrayrc**\n> Default configuration file using keyword-argument pairs matching command-line option names (e.g., background #2e3440, icon_size 24)."],["CAVEATS","Some applications may not properly support the system tray protocol, requiring **--kludges** options. Window type **dock** may behave differently across window managers. Build requires only Xlib and optionally libXinerama. The trace log level requires building with --enable-debug."],["HISTORY","**stalonetray** was created as a minimal, dependency-free system tray implementation for X11. It fills the need for a system tray in window managers that don't provide one natively, such as i3, bspwm, or dwm. The project follows the freedesktop.org specifications for system tray interoperability."],["INSTALL","```apt: sudo apt install stalonetray```\n\n```dnf: sudo dnf install stalonetray```\n\n```pacman: sudo pacman -S stalonetray```\n\n```apk: sudo apk add stalonetray```\n\n```zypper: sudo zypper install stalonetray```\n\n```nix: nix profile install nixpkgs#stalonetray```"],["SEE ALSO","[trayer](/man/trayer)(1), [polybar](/man/polybar)(1), [tint](/man/tint)(1)"]],"e":["trayer","polybar","tint"],"x":"stalonetray"},"synopkg":{"n":"synopkg","t":"Synology NAS package management utility","s":[["TAGLINE","Synology NAS package management utility"],["TLDR","**List** installed packages\n\n```synopkg list --name```\n\nList packages **depending on** another\n\n```synopkg list --depend-on [package]```\n\n**Start, stop, or restart** a package\n\n```sudo synopkg [start|stop|restart] [package]```\n\nShow package **status**\n\n```synopkg status [package]```\n\n**Uninstall** a package\n\n```sudo synopkg uninstall [package]```\n\nCheck for **updates**\n\n```synopkg checkupdate [package]```\n\n**Upgrade** all packages\n\n```sudo synopkg upgradeall```\n\n**Install** from SPK file\n\n```sudo synopkg install [path/to/package.spk]```"],["SYNOPSIS","**synopkg** _command_ [_OPTIONS_]"],["COMMANDS","**list** [**--name**] [**--depend-on** _pkg_]\n> List installed packages\n\n**install** _file.spk_\n> Install package from SPK file\n\n**uninstall** _package_\n> Remove installed package\n\n**start** _package_\n> Start a package service\n\n**stop** _package_\n> Stop a package service\n\n**restart** _package_\n> Restart a package service\n\n**status** _package_\n> Show package running status\n\n**is_onoff** _package_\n> Check if package is enabled\n\n**checkupdate** _package_\n> Check for package updates\n\n**upgradeall**\n> Upgrade all packages to latest versions\n\n**log** _package_\n> View package logs"],["PARAMETERS","**--name**\n> Show only package names\n\n**--depend-on** _package_\n> List packages depending on specified package"],["DESCRIPTION","**synopkg** is the package management utility for Synology DiskStation Manager (DSM), the operating system for Synology NAS devices. It provides command-line control over package installation, updates, and service management.\n\nThe tool wraps each package's start/stop scripts located at `/var/packages/<package>/scripts/start-stop-status`. It automatically handles dependency resolution when installing packages and integrates with DSM's Package Center."],["CAVEATS","Requires root privileges for install/uninstall/start/stop operations. SSH access must be enabled on the Synology NAS. Only accepts SPK package format. Installation has the same constraints as the web UI Package Center."],["HISTORY","**synopkg** is developed by **Synology Inc.** as part of their DiskStation Manager operating system. DSM provides a Linux-based platform for Synology NAS devices, first introduced in the mid-**2000s**. The command-line tools complement the web-based administration interface."],["SEE ALSO","[synoupgrade](/man/synoupgrade)(8), [synoservice](/man/synoservice)(8)"]],"e":["synoupgrade","synoservice"],"x":"synopkg list --name"},"systemctl-tui":{"n":"systemctl-tui","t":"Fast, simple TUI for systemd services","s":[["TAGLINE","Fast, simple TUI for systemd services"],["TLDR","**Launch the interactive service manager**\n\n```systemctl-tui```\n\n**Limit displayed units** by pattern\n\n```systemctl-tui --limit-units \"[pattern]\"```"],["SYNOPSIS","**systemctl-tui** [_options_]"],["DESCRIPTION","**systemctl-tui** lets you quickly browse systemd service status and logs, start/stop/restart/reload services, and view/edit unit files from an interactive terminal interface.\n\nIt aims to do a small number of things well: viewing service status, reading logs, and managing services without needing to remember `systemctl` and `journalctl` subcommands."],["PARAMETERS","**--limit-units** _pattern_\n> Limit the view to units matching the given pattern."],["CAVEATS","Linux only. WSL requires systemd to be enabled. Service management operations (start, stop, restart) require appropriate privileges."],["HISTORY","**systemctl-tui** was created by **Reilly Wood** (rgwood) and is written in **Rust**."],["INSTALL","```pacman: sudo pacman -S systemctl-tui```\n\n```nix: nix profile install nixpkgs#systemctl-tui```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [systemd-manager-tui](/man/systemd-manager-tui)(1), [journalctl](/man/journalctl)(1)"]],"e":["systemctl","systemd-manager-tui","journalctl"],"x":"systemctl-tui"},"systemctl":{"n":"systemctl","t":"Control systemd services and system","s":[["TAGLINE","Control systemd services and system"],["TLDR","Show all **running services**\n\n```systemctl status```\n\nList **failed** units\n\n```systemctl --failed```\n\n**Start/Stop/Restart** a service\n\n```systemctl start|stop|restart unit```\n\n**Enable/Disable** a service at boot\n\n```systemctl enable|disable unit```\n\nReload **systemd configuration**\n\n```systemctl daemon-reload```\n\nCheck if a unit is **active/enabled/failed**\n\n```systemctl is-active|is-enabled|is-failed unit```\n\nList all **service units** by state\n\n```systemctl list-units -t service --state running```\n\nShow **contents** of a unit file\n\n```systemctl cat unit```"],["SYNOPSIS","**systemctl** [_OPTIONS_...] _COMMAND_ [_UNIT_...]"],["DESCRIPTION","**systemctl** controls the systemd system and service manager. It can be used to introspect and control the state of the systemd system, manage services, check unit dependencies, and perform system operations like reboot or shutdown."],["PARAMETERS","**-t, --type=TYPE**\n> Filter by unit type (service, socket, target, mount, timer, etc.)\n\n**--state=STATE**\n> Filter by unit state (active, inactive, failed, running, etc.)\n\n**-a, --all**\n> Show all units including inactive ones\n\n**--user**\n> Manage user services instead of system services\n\n**--no-block**\n> Return immediately without waiting for operation to complete\n\n**--now**\n> Combine enable/disable with start/stop\n\n**--force**\n> Override safety checks or symlink conflicts\n\n**-q, --quiet**\n> Suppress output\n\n**-n, --lines=NUM**\n> Number of journal lines to show with status\n\n**--no-pager**\n> Do not pipe output into a pager\n\n**-o, --output=FORMAT**\n> Control journal output format (short, verbose, json, etc.)"],["COMMON COMMANDS","**list-units**: List active or specified units\n**list-unit-files**: Show installed unit files and their enablement state\n**status**: Show runtime status and recent logs\n**start/stop**: Start or stop units\n**restart**: Stop then start units\n**reload**: Reload unit configuration without stopping\n**enable/disable**: Enable or disable unit activation at boot\n**mask/unmask**: Completely disable or re-enable a unit\n**edit**: Edit unit file with drop-in snippets\n**cat**: Show contents of unit file\n**show**: Display unit properties in machine-readable format\n**daemon-reload**: Reload systemd configuration and unit files\n**is-active/is-enabled/is-failed**: Check unit state\n**kill**: Send signal to unit processes\n**poweroff/reboot/halt**: System power commands\n**suspend/hibernate**: Enter sleep states"],["CAVEATS","Some commands require root privileges. The **--user** flag manages per-user services in ~/.config/systemd/user/. Use **daemon-reload** after modifying unit files. Masked units cannot be started even manually."],["HISTORY","**systemctl** is part of **systemd**, introduced in 2010 by Lennart Poettering. It replaced traditional SysV init scripts and service management tools like **service** and **chkconfig**."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[journalctl](/man/journalctl)(1), [service](/man/service)(8)"]],"e":["journalctl","service"],"x":"systemctl status"},"systemd-inhibit":{"n":"systemd-inhibit","t":"Block system sleep or shutdown","s":[["TAGLINE","Block system sleep or shutdown"],["TLDR","**List** all active inhibition locks\n\n```systemd-inhibit --list```\n\n**Block** system shutdown while running a command\n\n```systemd-inhibit --what shutdown [command]```\n\n**Keep** the system from sleeping or idling\n\n```systemd-inhibit --what sleep:idle wget [https://example.com/file]```\n\n**Ignore** lid close switch until the script exits\n\n```systemd-inhibit --what sleep:handle-lid-switch [path/to/script]```\n\n**Ignore** power button press while command is running\n\n```systemd-inhibit --what handle-power-key [command]```\n\n**Describe** who and why created the inhibitor\n\n```systemd-inhibit --who [$USER] --why [reason] --what [operation] [command]```"],["SYNOPSIS","**systemd-inhibit** [_options_] [_command_] [_arguments_]"],["PARAMETERS","**--list**\n> List all active inhibitor locks\n\n**--what _operation_**\n> Operations to inhibit (shutdown, sleep, idle, handle-power-key, handle-suspend-key, handle-hibernate-key, handle-lid-switch)\n\n**--who _name_**\n> Descriptive name of who is holding the lock\n\n**--why _reason_**\n> Descriptive reason for holding the lock\n\n**--mode _mode_**\n> Either \"block\" or \"delay\""],["DESCRIPTION","**systemd-inhibit** creates inhibitor locks that can block or delay system sleep, shutdown, and hardware key handling while a command runs. The lock is held for the duration of the command's execution.\n\nMultiple operations can be specified by separating them with colons. The **--list** option shows all active inhibitors from all sources, not just those created by this tool."],["CAVEATS","Inhibitor locks should be used sparingly to avoid blocking normal system operation. \"Delay\" mode only holds off the operation briefly. Requires appropriate privileges for some operations. Part of the systemd suite."],["INSTALL","```apt: sudo apt install systemd```\n\n```dnf: sudo dnf install systemd```\n\n```pacman: sudo pacman -S systemd```\n\n```zypper: sudo zypper install systemd```\n\n```brew: brew install systemd```\n\n```nix: nix profile install nixpkgs#systemd```"],["SEE ALSO","[systemctl](/man/systemctl)(1), [loginctl](/man/loginctl)(1)"]],"e":["systemctl","loginctl"],"x":"systemd-inhibit --list"},"typeset":{"n":"typeset","t":"Declare shell variables with attributes","s":[["TAGLINE","Declare shell variables with attributes"],["TLDR","**Declare an integer variable**\n\n```typeset -i [count]=0```\n\n**Declare a read-only variable**\n\n```typeset -r [CONSTANT]=\"[value]\"```\n\n**Declare an uppercase variable**\n\n```typeset -u [name]=\"[value]\"```\n\n**Declare a lowercase variable**\n\n```typeset -l [name]=\"[VALUE]\"```\n\n**Export a variable** to child processes\n\n```typeset -x [PATH]=\"[/usr/bin]\"```\n\n**Declare an array**\n\n```typeset -a [myarray]=([one] [two] [three])```\n\n**Declare an associative array** (bash 4+)\n\n```typeset -A [mymap]=([key1]=\"[val1]\" [key2]=\"[val2]\")```\n\n**List all variables** with specified attribute\n\n```typeset -i```"],["SYNOPSIS","**typeset** [**-aAfFgilnrtux**] [**-p**] [_name_[=_value_]...]"],["PARAMETERS","**-a**\n> Declare indexed array variable\n\n**-A**\n> Declare associative array variable (bash 4+)\n\n**-f**\n> Display function names and definitions\n\n**-F**\n> Display function names only\n\n**-g**\n> Create global variable when used in a function (bash only)\n\n**-i**\n> Treat variable as integer; arithmetic evaluation on assignment\n\n**-l**\n> Convert value to lowercase on assignment\n\n**-n**\n> Create nameref (reference to another variable)\n\n**-r**\n> Make variable read-only\n\n**-t**\n> Give variable the trace attribute\n\n**-u**\n> Convert value to uppercase on assignment\n\n**-x**\n> Export variable to environment of child processes\n\n**-p**\n> Display attributes and values of variables"],["DESCRIPTION","**typeset** is a shell builtin that declares variables and assigns them attributes. In bash, it is a synonym for **declare** and is provided for compatibility with ksh (KornShell).\n\nVariables can be given type attributes like integer (**-i**) for automatic arithmetic evaluation, or transformation attributes like uppercase (**-u**) and lowercase (**-l**). The read-only attribute (**-r**) prevents modification after assignment.\n\nWhen used inside a function without **-g**, typeset creates a local variable that shadows any global variable of the same name. The variable is restored when the function exits.\n\nWithout arguments, typeset displays all variables with their attributes. With attribute flags alone (e.g., **typeset -i**), it lists variables having that attribute."],["CAVEATS","In bash, **typeset** is marked as obsolete in favor of **declare**, but both work identically. Behavior differs between shells (bash, ksh, zsh). The **-n** nameref option requires bash 4.3+ or ksh93. Associative arrays (**-A**) require bash 4+."],["HISTORY","**typeset** originated in the **KornShell** (ksh) developed by **David Korn** at Bell Labs in the early **1980s**. When bash was created, it implemented similar functionality as **declare** but included **typeset** as an alias for ksh compatibility. The command remains important for portable shell scripts targeting multiple Korn-like shells."],["SEE ALSO","[declare](/man/declare)(1), [local](/man/local)(1), [export](/man/export)(1), [readonly](/man/readonly)(1), [bash](/man/bash)(1)"]],"e":["declare","local","export","readonly","bash"],"x":"typeset -i [count]=0"},"usql":{"n":"usql","t":"Universal command-line database client","s":[["TAGLINE","Universal command-line database client"],["TLDR","**Connect to PostgreSQL**\n\n```usql postgres://[user]:[pass]@[host]/[database]```\n\n**Connect to MySQL**\n\n```usql mysql://[user]:[pass]@[host]/[database]```\n\n**Connect to SQLite**\n\n```usql sqlite:[database.db]```\n\n**Execute query**\n\n```usql [connection_url] -c \"[SELECT * FROM table]\"```\n\n**Execute file**\n\n```usql [connection_url] -f [script.sql]```\n\n**List databases**\n\n```usql [connection_url] -c \"\\\\l\"```\n\n**Describe table**\n\n```usql [connection_url] -c \"\\\\d [table_name]\"```"],["SYNOPSIS","**usql** [_-c command_] [_-f file_] [_options_] _connection_url_"],["PARAMETERS","**-c** _CMD_\n> Execute command and exit.\n\n**-f** _FILE_\n> Execute file.\n\n**-o** _FILE_\n> Output to file.\n\n**-w**, **--no-password**\n> Never prompt for password.\n\n**-W**, **--password**\n> Always prompt for password.\n\n**-X**, **--no-rc**\n> Don't read rc file.\n\n**-t**, **--tuples-only**\n> Only print rows.\n\n**-q**, **--quiet**\n> Quiet mode.\n\n**-v** _NAME=VALUE_\n> Set variable.\n\n**--help**\n> Show help.\n\n**--version**\n> Show version."],["DESCRIPTION","**usql** is a universal command-line interface for SQL databases. It connects to PostgreSQL, MySQL, SQLite, SQL Server, Oracle, and many other databases (including NoSQL databases) with a consistent psql-inspired interface.\n\nConnection strings follow URL format. The scheme identifies the database type: postgres://, mysql://, sqlite:, mssql://, oracle://, etc. When no scheme is given, usql auto-detects: directories use PostgreSQL, regular files use SQLite3 or DuckDB.\n\nBackslash commands work across databases: \\\\l lists databases, \\\\dt lists tables, \\\\d describes objects, \\\\drivers lists available database drivers.\n\nQuery output can be formatted as tables, CSV, JSON, or other formats. Tab completion assists with table and column names, and history persists across sessions."],["CAVEATS","Not all features available on all databases. Some drivers need installation. Complex queries may have database-specific syntax."],["HISTORY","**usql** was created by **Kenneth Shaw** around **2017** to provide a universal SQL client. It addresses the fragmentation of database CLIs with a single, consistent tool."],["INSTALL","```nix: nix profile install nixpkgs#usql```"],["SEE ALSO","[psql](/man/psql)(1), [mysql](/man/mysql)(1), [sqlite3](/man/sqlite3)(1), [sqlcmd](/man/sqlcmd)(1)"]],"e":["psql","mysql","sqlite3","sqlcmd"],"x":"usql postgres://[user]:[pass]@[host]/[database]"},"virt-what":{"n":"virt-what","t":"Detect virtualization technology","s":[["TAGLINE","Detect virtualization technology"],["TLDR","**Detect** virtualization technology (requires root)\n\n```sudo virt-what```\n\n**Log** output to file\n\n```sudo virt-what > [path/to/file]```\n\nDisplay **version**\n\n```virt-what --version```"],["SYNOPSIS","**virt-what** [_OPTIONS_]"],["PARAMETERS","**--version**\n> Display version information"],["DESCRIPTION","**virt-what** is a shell script that detects if you are running inside a virtual machine. It prints one or more lines identifying the virtualization technology. If nothing is printed, the system is running on bare metal.\n\nThe tool can detect various hypervisors including KVM, QEMU, Xen (HVM and PV), VMware, VirtualBox, Hyper-V, Docker, LXC, OpenVZ, Podman, and others. Multiple lines may be printed for nested virtualization (e.g., both \"kvm\" and \"hyperv\" when running KVM inside Hyper-V)."],["OUTPUT VALUES","Common output values include: **kvm**, **qemu**, **xen**, **xen-hvm**, **xen-pv**, **vmware**, **virtualbox**, **hyperv**, **docker**, **lxc**, **openvz**, **podman**, **parallels**, **bhyve**, **uml** (User-Mode Linux)."],["CAVEATS","Requires root privileges for reliable detection. Some detection methods read DMI data or kernel modules that are only accessible as root. Nested virtualization may produce multiple lines of output. Container detection (Docker, LXC, Podman) is also supported."],["HISTORY","**virt-what** was created by **Richard W.M. Jones** at Red Hat. It is distributed as part of the virt-what package and is commonly used in provisioning scripts to detect the runtime environment."],["INSTALL","```apt: sudo apt install virt-what```\n\n```dnf: sudo dnf install virt-what```\n\n```pacman: sudo pacman -S virt-what```\n\n```apk: sudo apk add virt-what```\n\n```zypper: sudo zypper install virt-what```\n\n```nix: nix profile install nixpkgs#virt-what```"],["SEE ALSO","[systemd-detect-virt](/man/systemd-detect-virt)(1), [lscpu](/man/lscpu)(1)"]],"e":["systemd-detect-virt","lscpu"],"x":"sudo virt-what"},"yarac":{"n":"yarac","t":"Compile YARA rules to binary","s":[["TAGLINE","Compile YARA rules to binary"],["TLDR","**Compile rules**\n\n```yarac [rules.yar] [rules.yarc]```\n\n**Compile multiple files**\n\n```yarac [rule1.yar] [rule2.yar] [output.yarc]```\n\n**Show warnings**\n\n```yarac -w [rules.yar] [output.yarc]```"],["SYNOPSIS","**yarac** [_-w_] [_options_] _rules_ _output_"],["PARAMETERS","**-w**\n> Show warnings.\n\n**-d** _VAR=VAL_\n> Define variable.\n\n**--help**\n> Show help."],["DESCRIPTION","**yarac** compiles YARA rule source files into an optimized binary format. Precompiled rules load significantly faster than parsing source files each time, which matters when scanning large file sets or running rules on production systems.\n\nMultiple rule files can be combined into a single compiled output, consolidating an entire rule set into one deployable binary. External variables can be defined at compile time with **-d** to parameterize rules without modifying the source.\n\nCompiled rule files are not portable across different YARA versions, so rules must be recompiled after upgrading YARA. The **-w** flag enables warnings during compilation to catch potential issues in rule definitions before deployment."],["CAVEATS","YARA required. Rule syntax must be valid. Binary not portable across versions."],["HISTORY","**yarac** is part of **YARA**, a pattern matching tool for malware researchers."],["INSTALL","```apt: sudo apt install yara```\n\n```dnf: sudo dnf install yara```\n\n```pacman: sudo pacman -S yara```\n\n```apk: sudo apk add yara```\n\n```zypper: sudo zypper install yara```\n\n```brew: brew install yara```\n\n```nix: nix profile install nixpkgs#yara```"],["SEE ALSO","[yara](/man/yara)(1), [clamav](/man/clamav)(1)"]],"e":["yara","clamav"],"x":"yarac [rules.yar] [rules.yarc]"},"yek":{"n":"yek","t":"Serialize code repositories for LLM consumption","s":[["TAGLINE","Serialize code repositories for LLM consumption"],["TLDR","**Serialize current directory** for LLM consumption\n\n```yek```\n\n**Serialize specific directories**\n\n```yek [src/] [tests/]```\n\n**Serialize with token limit**\n\n```yek --tokens [128k]```\n\n**Serialize with size limit**\n\n```yek --max-size [10MB]```\n\n**Pipe output** to clipboard\n\n```yek | pbcopy```\n\n**Include line numbers** in output\n\n```yek --line-numbers```\n\n**Include directory tree** header\n\n```yek --tree-header```\n\n**Output as JSON**\n\n```yek --json```\n\n**Use glob patterns**\n\n```yek \"[src/**/*.ts]\"```"],["SYNOPSIS","**yek** [_options_] [_input-paths_...]"],["PARAMETERS","**--tokens** _count_\n> Use token counting mode instead of byte mode (e.g., 128k, 100)\n\n**--max-size** _size_\n> Limit output by byte size (e.g., 10MB, 128K); default: 10MB\n\n**--json**\n> Output results in JSON format\n\n**--output-dir** _path_\n> Directory for output files; uses temp directory if not specified\n\n**--output-name** _name_\n> Filename written to current directory\n\n**--output-template** _template_\n> Custom format using FILE_PATH and FILE_CONTENT placeholders\n\n**--ignore-patterns** _patterns_\n> Additional patterns to ignore (extends .gitignore)\n\n**--unignore-patterns** _patterns_\n> Override built-in ignore rules\n\n**--line-numbers**\n> Include line numbers in output\n\n**-t**, **--tree-header**\n> Include directory tree at the start of output\n\n**--tree-only**\n> Show only directory structure without file contents\n\n**--no-config**\n> Skip loading configuration files\n\n**--config-file** _path_\n> Use specific configuration file\n\n**--debug**\n> Enable debug logging"],["DESCRIPTION","**yek** (Farsi for \"one\") is a fast Rust-based CLI tool that serializes code repositories into text optimized for LLM (Large Language Model) consumption. It combines files into a single output with intelligent ordering and automatic filtering.\n\nThe tool respects **.gitignore** rules, uses Git history to prioritize important files, and automatically skips binary and large files. Output can be split into chunks based on token count or byte size.\n\nWhen output is piped, yek automatically streams content instead of writing to files. This enables workflows like **yek | pbcopy** to quickly copy a codebase to clipboard for pasting into an LLM chat.\n\nConfiguration can be stored in **yek.toml** or **yek.yaml** files for project-specific settings."],["CAVEATS","Token counting is approximate and may vary from actual LLM tokenization. Very large repositories may need chunking with **--tokens** or **--max-size**. Glob patterns must be quoted to prevent shell expansion."],["HISTORY","**yek** was created by **Mohsen Azimi** as a high-performance tool for preparing code for LLM analysis. Written in **Rust**, it achieved significant speed improvements over similar tools—benchmarks show it running 230× faster than alternatives like Repomix. The name means \"one\" in Farsi (یک), reflecting its purpose of combining files into one output."],["INSTALL","```brew: brew install yek```\n\n```nix: nix profile install nixpkgs#yek```"],["SEE ALSO","[tree](/man/tree)(1), [find](/man/find)(1), [cat](/man/cat)(1), [rg](/man/rg)(1)"]],"e":["tree","find","cat","rg"],"x":"yek"}});
